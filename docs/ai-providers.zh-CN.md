# AI Providers

AI 供应商配置在管理后台完成：

```text
http://localhost:8766/admin/models
```

基本流程：

1. 添加供应商凭据。
2. 测试凭据连接。
3. 启用一个或多个模型。
4. 配置模型能力、输入限制、价格和参数。
5. 在功能路由中选择默认模型或允许模型池。

如果 Agent 或生成工具提示“没有可用模型配置”，通常是还没有启用模型，或凭据被禁用。
