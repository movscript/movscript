#!/usr/bin/env node
import { createRequire as __movscriptCreateRequire } from "node:module";
var require = __movscriptCreateRequire(import.meta.url);
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var __commonJS = (cb, mod) => function __require2() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// ../cli/dist/index.cjs
var require_dist = __commonJS({
  "../cli/dist/index.cjs"(exports, module) {
    "use strict";
    var __create2 = Object.create;
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __getProtoOf2 = Object.getPrototypeOf;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __commonJS2 = (cb, mod) => function __require2() {
      return mod || (0, cb[__getOwnPropNames2(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    };
    var __export = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM2 = (mod, isNodeMode, target) => (target = mod != null ? __create2(__getProtoOf2(mod)) : {}, __copyProps2(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp2(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var require_error = __commonJS2({
      "../../node_modules/.pnpm/commander@12.1.0/node_modules/commander/lib/error.js"(exports2) {
        "use strict";
        var CommanderError2 = class extends Error {
          /**
           * Constructs the CommanderError class
           * @param {number} exitCode suggested exit code which could be used with process.exit
           * @param {string} code an id string representing the error
           * @param {string} message human-readable description of the error
           */
          constructor(exitCode, code, message) {
            super(message);
            Error.captureStackTrace(this, this.constructor);
            this.name = this.constructor.name;
            this.code = code;
            this.exitCode = exitCode;
            this.nestedError = void 0;
          }
        };
        var InvalidArgumentError2 = class extends CommanderError2 {
          /**
           * Constructs the InvalidArgumentError class
           * @param {string} [message] explanation of why argument is invalid
           */
          constructor(message) {
            super(1, "commander.invalidArgument", message);
            Error.captureStackTrace(this, this.constructor);
            this.name = this.constructor.name;
          }
        };
        exports2.CommanderError = CommanderError2;
        exports2.InvalidArgumentError = InvalidArgumentError2;
      }
    });
    var require_argument = __commonJS2({
      "../../node_modules/.pnpm/commander@12.1.0/node_modules/commander/lib/argument.js"(exports2) {
        "use strict";
        var { InvalidArgumentError: InvalidArgumentError2 } = require_error();
        var Argument2 = class {
          /**
           * Initialize a new command argument with the given name and description.
           * The default is that the argument is required, and you can explicitly
           * indicate this with <> around the name. Put [] around the name for an optional argument.
           *
           * @param {string} name
           * @param {string} [description]
           */
          constructor(name, description) {
            this.description = description || "";
            this.variadic = false;
            this.parseArg = void 0;
            this.defaultValue = void 0;
            this.defaultValueDescription = void 0;
            this.argChoices = void 0;
            switch (name[0]) {
              case "<":
                this.required = true;
                this._name = name.slice(1, -1);
                break;
              case "[":
                this.required = false;
                this._name = name.slice(1, -1);
                break;
              default:
                this.required = true;
                this._name = name;
                break;
            }
            if (this._name.length > 3 && this._name.slice(-3) === "...") {
              this.variadic = true;
              this._name = this._name.slice(0, -3);
            }
          }
          /**
           * Return argument name.
           *
           * @return {string}
           */
          name() {
            return this._name;
          }
          /**
           * @package
           */
          _concatValue(value, previous) {
            if (previous === this.defaultValue || !Array.isArray(previous)) {
              return [value];
            }
            return previous.concat(value);
          }
          /**
           * Set the default value, and optionally supply the description to be displayed in the help.
           *
           * @param {*} value
           * @param {string} [description]
           * @return {Argument}
           */
          default(value, description) {
            this.defaultValue = value;
            this.defaultValueDescription = description;
            return this;
          }
          /**
           * Set the custom handler for processing CLI command arguments into argument values.
           *
           * @param {Function} [fn]
           * @return {Argument}
           */
          argParser(fn) {
            this.parseArg = fn;
            return this;
          }
          /**
           * Only allow argument value to be one of choices.
           *
           * @param {string[]} values
           * @return {Argument}
           */
          choices(values) {
            this.argChoices = values.slice();
            this.parseArg = (arg, previous) => {
              if (!this.argChoices.includes(arg)) {
                throw new InvalidArgumentError2(
                  `Allowed choices are ${this.argChoices.join(", ")}.`
                );
              }
              if (this.variadic) {
                return this._concatValue(arg, previous);
              }
              return arg;
            };
            return this;
          }
          /**
           * Make argument required.
           *
           * @returns {Argument}
           */
          argRequired() {
            this.required = true;
            return this;
          }
          /**
           * Make argument optional.
           *
           * @returns {Argument}
           */
          argOptional() {
            this.required = false;
            return this;
          }
        };
        function humanReadableArgName(arg) {
          const nameOutput = arg.name() + (arg.variadic === true ? "..." : "");
          return arg.required ? "<" + nameOutput + ">" : "[" + nameOutput + "]";
        }
        exports2.Argument = Argument2;
        exports2.humanReadableArgName = humanReadableArgName;
      }
    });
    var require_help = __commonJS2({
      "../../node_modules/.pnpm/commander@12.1.0/node_modules/commander/lib/help.js"(exports2) {
        "use strict";
        var { humanReadableArgName } = require_argument();
        var Help2 = class {
          constructor() {
            this.helpWidth = void 0;
            this.sortSubcommands = false;
            this.sortOptions = false;
            this.showGlobalOptions = false;
          }
          /**
           * Get an array of the visible subcommands. Includes a placeholder for the implicit help command, if there is one.
           *
           * @param {Command} cmd
           * @returns {Command[]}
           */
          visibleCommands(cmd) {
            const visibleCommands = cmd.commands.filter((cmd2) => !cmd2._hidden);
            const helpCommand = cmd._getHelpCommand();
            if (helpCommand && !helpCommand._hidden) {
              visibleCommands.push(helpCommand);
            }
            if (this.sortSubcommands) {
              visibleCommands.sort((a, b) => {
                return a.name().localeCompare(b.name());
              });
            }
            return visibleCommands;
          }
          /**
           * Compare options for sort.
           *
           * @param {Option} a
           * @param {Option} b
           * @returns {number}
           */
          compareOptions(a, b) {
            const getSortKey = (option) => {
              return option.short ? option.short.replace(/^-/, "") : option.long.replace(/^--/, "");
            };
            return getSortKey(a).localeCompare(getSortKey(b));
          }
          /**
           * Get an array of the visible options. Includes a placeholder for the implicit help option, if there is one.
           *
           * @param {Command} cmd
           * @returns {Option[]}
           */
          visibleOptions(cmd) {
            const visibleOptions = cmd.options.filter((option) => !option.hidden);
            const helpOption = cmd._getHelpOption();
            if (helpOption && !helpOption.hidden) {
              const removeShort = helpOption.short && cmd._findOption(helpOption.short);
              const removeLong = helpOption.long && cmd._findOption(helpOption.long);
              if (!removeShort && !removeLong) {
                visibleOptions.push(helpOption);
              } else if (helpOption.long && !removeLong) {
                visibleOptions.push(
                  cmd.createOption(helpOption.long, helpOption.description)
                );
              } else if (helpOption.short && !removeShort) {
                visibleOptions.push(
                  cmd.createOption(helpOption.short, helpOption.description)
                );
              }
            }
            if (this.sortOptions) {
              visibleOptions.sort(this.compareOptions);
            }
            return visibleOptions;
          }
          /**
           * Get an array of the visible global options. (Not including help.)
           *
           * @param {Command} cmd
           * @returns {Option[]}
           */
          visibleGlobalOptions(cmd) {
            if (!this.showGlobalOptions) return [];
            const globalOptions = [];
            for (let ancestorCmd = cmd.parent; ancestorCmd; ancestorCmd = ancestorCmd.parent) {
              const visibleOptions = ancestorCmd.options.filter(
                (option) => !option.hidden
              );
              globalOptions.push(...visibleOptions);
            }
            if (this.sortOptions) {
              globalOptions.sort(this.compareOptions);
            }
            return globalOptions;
          }
          /**
           * Get an array of the arguments if any have a description.
           *
           * @param {Command} cmd
           * @returns {Argument[]}
           */
          visibleArguments(cmd) {
            if (cmd._argsDescription) {
              cmd.registeredArguments.forEach((argument) => {
                argument.description = argument.description || cmd._argsDescription[argument.name()] || "";
              });
            }
            if (cmd.registeredArguments.find((argument) => argument.description)) {
              return cmd.registeredArguments;
            }
            return [];
          }
          /**
           * Get the command term to show in the list of subcommands.
           *
           * @param {Command} cmd
           * @returns {string}
           */
          subcommandTerm(cmd) {
            const args = cmd.registeredArguments.map((arg) => humanReadableArgName(arg)).join(" ");
            return cmd._name + (cmd._aliases[0] ? "|" + cmd._aliases[0] : "") + (cmd.options.length ? " [options]" : "") + // simplistic check for non-help option
            (args ? " " + args : "");
          }
          /**
           * Get the option term to show in the list of options.
           *
           * @param {Option} option
           * @returns {string}
           */
          optionTerm(option) {
            return option.flags;
          }
          /**
           * Get the argument term to show in the list of arguments.
           *
           * @param {Argument} argument
           * @returns {string}
           */
          argumentTerm(argument) {
            return argument.name();
          }
          /**
           * Get the longest command term length.
           *
           * @param {Command} cmd
           * @param {Help} helper
           * @returns {number}
           */
          longestSubcommandTermLength(cmd, helper) {
            return helper.visibleCommands(cmd).reduce((max, command) => {
              return Math.max(max, helper.subcommandTerm(command).length);
            }, 0);
          }
          /**
           * Get the longest option term length.
           *
           * @param {Command} cmd
           * @param {Help} helper
           * @returns {number}
           */
          longestOptionTermLength(cmd, helper) {
            return helper.visibleOptions(cmd).reduce((max, option) => {
              return Math.max(max, helper.optionTerm(option).length);
            }, 0);
          }
          /**
           * Get the longest global option term length.
           *
           * @param {Command} cmd
           * @param {Help} helper
           * @returns {number}
           */
          longestGlobalOptionTermLength(cmd, helper) {
            return helper.visibleGlobalOptions(cmd).reduce((max, option) => {
              return Math.max(max, helper.optionTerm(option).length);
            }, 0);
          }
          /**
           * Get the longest argument term length.
           *
           * @param {Command} cmd
           * @param {Help} helper
           * @returns {number}
           */
          longestArgumentTermLength(cmd, helper) {
            return helper.visibleArguments(cmd).reduce((max, argument) => {
              return Math.max(max, helper.argumentTerm(argument).length);
            }, 0);
          }
          /**
           * Get the command usage to be displayed at the top of the built-in help.
           *
           * @param {Command} cmd
           * @returns {string}
           */
          commandUsage(cmd) {
            let cmdName = cmd._name;
            if (cmd._aliases[0]) {
              cmdName = cmdName + "|" + cmd._aliases[0];
            }
            let ancestorCmdNames = "";
            for (let ancestorCmd = cmd.parent; ancestorCmd; ancestorCmd = ancestorCmd.parent) {
              ancestorCmdNames = ancestorCmd.name() + " " + ancestorCmdNames;
            }
            return ancestorCmdNames + cmdName + " " + cmd.usage();
          }
          /**
           * Get the description for the command.
           *
           * @param {Command} cmd
           * @returns {string}
           */
          commandDescription(cmd) {
            return cmd.description();
          }
          /**
           * Get the subcommand summary to show in the list of subcommands.
           * (Fallback to description for backwards compatibility.)
           *
           * @param {Command} cmd
           * @returns {string}
           */
          subcommandDescription(cmd) {
            return cmd.summary() || cmd.description();
          }
          /**
           * Get the option description to show in the list of options.
           *
           * @param {Option} option
           * @return {string}
           */
          optionDescription(option) {
            const extraInfo = [];
            if (option.argChoices) {
              extraInfo.push(
                // use stringify to match the display of the default value
                `choices: ${option.argChoices.map((choice) => JSON.stringify(choice)).join(", ")}`
              );
            }
            if (option.defaultValue !== void 0) {
              const showDefault = option.required || option.optional || option.isBoolean() && typeof option.defaultValue === "boolean";
              if (showDefault) {
                extraInfo.push(
                  `default: ${option.defaultValueDescription || JSON.stringify(option.defaultValue)}`
                );
              }
            }
            if (option.presetArg !== void 0 && option.optional) {
              extraInfo.push(`preset: ${JSON.stringify(option.presetArg)}`);
            }
            if (option.envVar !== void 0) {
              extraInfo.push(`env: ${option.envVar}`);
            }
            if (extraInfo.length > 0) {
              return `${option.description} (${extraInfo.join(", ")})`;
            }
            return option.description;
          }
          /**
           * Get the argument description to show in the list of arguments.
           *
           * @param {Argument} argument
           * @return {string}
           */
          argumentDescription(argument) {
            const extraInfo = [];
            if (argument.argChoices) {
              extraInfo.push(
                // use stringify to match the display of the default value
                `choices: ${argument.argChoices.map((choice) => JSON.stringify(choice)).join(", ")}`
              );
            }
            if (argument.defaultValue !== void 0) {
              extraInfo.push(
                `default: ${argument.defaultValueDescription || JSON.stringify(argument.defaultValue)}`
              );
            }
            if (extraInfo.length > 0) {
              const extraDescripton = `(${extraInfo.join(", ")})`;
              if (argument.description) {
                return `${argument.description} ${extraDescripton}`;
              }
              return extraDescripton;
            }
            return argument.description;
          }
          /**
           * Generate the built-in help text.
           *
           * @param {Command} cmd
           * @param {Help} helper
           * @returns {string}
           */
          formatHelp(cmd, helper) {
            const termWidth = helper.padWidth(cmd, helper);
            const helpWidth = helper.helpWidth || 80;
            const itemIndentWidth = 2;
            const itemSeparatorWidth = 2;
            function formatItem(term, description) {
              if (description) {
                const fullText = `${term.padEnd(termWidth + itemSeparatorWidth)}${description}`;
                return helper.wrap(
                  fullText,
                  helpWidth - itemIndentWidth,
                  termWidth + itemSeparatorWidth
                );
              }
              return term;
            }
            function formatList(textArray) {
              return textArray.join("\n").replace(/^/gm, " ".repeat(itemIndentWidth));
            }
            let output3 = [`Usage: ${helper.commandUsage(cmd)}`, ""];
            const commandDescription = helper.commandDescription(cmd);
            if (commandDescription.length > 0) {
              output3 = output3.concat([
                helper.wrap(commandDescription, helpWidth, 0),
                ""
              ]);
            }
            const argumentList = helper.visibleArguments(cmd).map((argument) => {
              return formatItem(
                helper.argumentTerm(argument),
                helper.argumentDescription(argument)
              );
            });
            if (argumentList.length > 0) {
              output3 = output3.concat(["Arguments:", formatList(argumentList), ""]);
            }
            const optionList = helper.visibleOptions(cmd).map((option) => {
              return formatItem(
                helper.optionTerm(option),
                helper.optionDescription(option)
              );
            });
            if (optionList.length > 0) {
              output3 = output3.concat(["Options:", formatList(optionList), ""]);
            }
            if (this.showGlobalOptions) {
              const globalOptionList = helper.visibleGlobalOptions(cmd).map((option) => {
                return formatItem(
                  helper.optionTerm(option),
                  helper.optionDescription(option)
                );
              });
              if (globalOptionList.length > 0) {
                output3 = output3.concat([
                  "Global Options:",
                  formatList(globalOptionList),
                  ""
                ]);
              }
            }
            const commandList2 = helper.visibleCommands(cmd).map((cmd2) => {
              return formatItem(
                helper.subcommandTerm(cmd2),
                helper.subcommandDescription(cmd2)
              );
            });
            if (commandList2.length > 0) {
              output3 = output3.concat(["Commands:", formatList(commandList2), ""]);
            }
            return output3.join("\n");
          }
          /**
           * Calculate the pad width from the maximum term length.
           *
           * @param {Command} cmd
           * @param {Help} helper
           * @returns {number}
           */
          padWidth(cmd, helper) {
            return Math.max(
              helper.longestOptionTermLength(cmd, helper),
              helper.longestGlobalOptionTermLength(cmd, helper),
              helper.longestSubcommandTermLength(cmd, helper),
              helper.longestArgumentTermLength(cmd, helper)
            );
          }
          /**
           * Wrap the given string to width characters per line, with lines after the first indented.
           * Do not wrap if insufficient room for wrapping (minColumnWidth), or string is manually formatted.
           *
           * @param {string} str
           * @param {number} width
           * @param {number} indent
           * @param {number} [minColumnWidth=40]
           * @return {string}
           *
           */
          wrap(str, width, indent, minColumnWidth = 40) {
            const indents = " \\f\\t\\v\xA0\u1680\u2000-\u200A\u202F\u205F\u3000\uFEFF";
            const manualIndent = new RegExp(`[\\n][${indents}]+`);
            if (str.match(manualIndent)) return str;
            const columnWidth = width - indent;
            if (columnWidth < minColumnWidth) return str;
            const leadingStr = str.slice(0, indent);
            const columnText = str.slice(indent).replace("\r\n", "\n");
            const indentString = " ".repeat(indent);
            const zeroWidthSpace = "\u200B";
            const breaks = `\\s${zeroWidthSpace}`;
            const regex = new RegExp(
              `
|.{1,${columnWidth - 1}}([${breaks}]|$)|[^${breaks}]+?([${breaks}]|$)`,
              "g"
            );
            const lines = columnText.match(regex) || [];
            return leadingStr + lines.map((line, i) => {
              if (line === "\n") return "";
              return (i > 0 ? indentString : "") + line.trimEnd();
            }).join("\n");
          }
        };
        exports2.Help = Help2;
      }
    });
    var require_option = __commonJS2({
      "../../node_modules/.pnpm/commander@12.1.0/node_modules/commander/lib/option.js"(exports2) {
        "use strict";
        var { InvalidArgumentError: InvalidArgumentError2 } = require_error();
        var Option2 = class {
          /**
           * Initialize a new `Option` with the given `flags` and `description`.
           *
           * @param {string} flags
           * @param {string} [description]
           */
          constructor(flags, description) {
            this.flags = flags;
            this.description = description || "";
            this.required = flags.includes("<");
            this.optional = flags.includes("[");
            this.variadic = /\w\.\.\.[>\]]$/.test(flags);
            this.mandatory = false;
            const optionFlags = splitOptionFlags(flags);
            this.short = optionFlags.shortFlag;
            this.long = optionFlags.longFlag;
            this.negate = false;
            if (this.long) {
              this.negate = this.long.startsWith("--no-");
            }
            this.defaultValue = void 0;
            this.defaultValueDescription = void 0;
            this.presetArg = void 0;
            this.envVar = void 0;
            this.parseArg = void 0;
            this.hidden = false;
            this.argChoices = void 0;
            this.conflictsWith = [];
            this.implied = void 0;
          }
          /**
           * Set the default value, and optionally supply the description to be displayed in the help.
           *
           * @param {*} value
           * @param {string} [description]
           * @return {Option}
           */
          default(value, description) {
            this.defaultValue = value;
            this.defaultValueDescription = description;
            return this;
          }
          /**
           * Preset to use when option used without option-argument, especially optional but also boolean and negated.
           * The custom processing (parseArg) is called.
           *
           * @example
           * new Option('--color').default('GREYSCALE').preset('RGB');
           * new Option('--donate [amount]').preset('20').argParser(parseFloat);
           *
           * @param {*} arg
           * @return {Option}
           */
          preset(arg) {
            this.presetArg = arg;
            return this;
          }
          /**
           * Add option name(s) that conflict with this option.
           * An error will be displayed if conflicting options are found during parsing.
           *
           * @example
           * new Option('--rgb').conflicts('cmyk');
           * new Option('--js').conflicts(['ts', 'jsx']);
           *
           * @param {(string | string[])} names
           * @return {Option}
           */
          conflicts(names) {
            this.conflictsWith = this.conflictsWith.concat(names);
            return this;
          }
          /**
           * Specify implied option values for when this option is set and the implied options are not.
           *
           * The custom processing (parseArg) is not called on the implied values.
           *
           * @example
           * program
           *   .addOption(new Option('--log', 'write logging information to file'))
           *   .addOption(new Option('--trace', 'log extra details').implies({ log: 'trace.txt' }));
           *
           * @param {object} impliedOptionValues
           * @return {Option}
           */
          implies(impliedOptionValues) {
            let newImplied = impliedOptionValues;
            if (typeof impliedOptionValues === "string") {
              newImplied = { [impliedOptionValues]: true };
            }
            this.implied = Object.assign(this.implied || {}, newImplied);
            return this;
          }
          /**
           * Set environment variable to check for option value.
           *
           * An environment variable is only used if when processed the current option value is
           * undefined, or the source of the current value is 'default' or 'config' or 'env'.
           *
           * @param {string} name
           * @return {Option}
           */
          env(name) {
            this.envVar = name;
            return this;
          }
          /**
           * Set the custom handler for processing CLI option arguments into option values.
           *
           * @param {Function} [fn]
           * @return {Option}
           */
          argParser(fn) {
            this.parseArg = fn;
            return this;
          }
          /**
           * Whether the option is mandatory and must have a value after parsing.
           *
           * @param {boolean} [mandatory=true]
           * @return {Option}
           */
          makeOptionMandatory(mandatory = true) {
            this.mandatory = !!mandatory;
            return this;
          }
          /**
           * Hide option in help.
           *
           * @param {boolean} [hide=true]
           * @return {Option}
           */
          hideHelp(hide = true) {
            this.hidden = !!hide;
            return this;
          }
          /**
           * @package
           */
          _concatValue(value, previous) {
            if (previous === this.defaultValue || !Array.isArray(previous)) {
              return [value];
            }
            return previous.concat(value);
          }
          /**
           * Only allow option value to be one of choices.
           *
           * @param {string[]} values
           * @return {Option}
           */
          choices(values) {
            this.argChoices = values.slice();
            this.parseArg = (arg, previous) => {
              if (!this.argChoices.includes(arg)) {
                throw new InvalidArgumentError2(
                  `Allowed choices are ${this.argChoices.join(", ")}.`
                );
              }
              if (this.variadic) {
                return this._concatValue(arg, previous);
              }
              return arg;
            };
            return this;
          }
          /**
           * Return option name.
           *
           * @return {string}
           */
          name() {
            if (this.long) {
              return this.long.replace(/^--/, "");
            }
            return this.short.replace(/^-/, "");
          }
          /**
           * Return option name, in a camelcase format that can be used
           * as a object attribute key.
           *
           * @return {string}
           */
          attributeName() {
            return camelcase(this.name().replace(/^no-/, ""));
          }
          /**
           * Check if `arg` matches the short or long flag.
           *
           * @param {string} arg
           * @return {boolean}
           * @package
           */
          is(arg) {
            return this.short === arg || this.long === arg;
          }
          /**
           * Return whether a boolean option.
           *
           * Options are one of boolean, negated, required argument, or optional argument.
           *
           * @return {boolean}
           * @package
           */
          isBoolean() {
            return !this.required && !this.optional && !this.negate;
          }
        };
        var DualOptions = class {
          /**
           * @param {Option[]} options
           */
          constructor(options) {
            this.positiveOptions = /* @__PURE__ */ new Map();
            this.negativeOptions = /* @__PURE__ */ new Map();
            this.dualOptions = /* @__PURE__ */ new Set();
            options.forEach((option) => {
              if (option.negate) {
                this.negativeOptions.set(option.attributeName(), option);
              } else {
                this.positiveOptions.set(option.attributeName(), option);
              }
            });
            this.negativeOptions.forEach((value, key) => {
              if (this.positiveOptions.has(key)) {
                this.dualOptions.add(key);
              }
            });
          }
          /**
           * Did the value come from the option, and not from possible matching dual option?
           *
           * @param {*} value
           * @param {Option} option
           * @returns {boolean}
           */
          valueFromOption(value, option) {
            const optionKey = option.attributeName();
            if (!this.dualOptions.has(optionKey)) return true;
            const preset = this.negativeOptions.get(optionKey).presetArg;
            const negativeValue = preset !== void 0 ? preset : false;
            return option.negate === (negativeValue === value);
          }
        };
        function camelcase(str) {
          return str.split("-").reduce((str2, word) => {
            return str2 + word[0].toUpperCase() + word.slice(1);
          });
        }
        function splitOptionFlags(flags) {
          let shortFlag;
          let longFlag;
          const flagParts = flags.split(/[ |,]+/);
          if (flagParts.length > 1 && !/^[[<]/.test(flagParts[1]))
            shortFlag = flagParts.shift();
          longFlag = flagParts.shift();
          if (!shortFlag && /^-[^-]$/.test(longFlag)) {
            shortFlag = longFlag;
            longFlag = void 0;
          }
          return { shortFlag, longFlag };
        }
        exports2.Option = Option2;
        exports2.DualOptions = DualOptions;
      }
    });
    var require_suggestSimilar = __commonJS2({
      "../../node_modules/.pnpm/commander@12.1.0/node_modules/commander/lib/suggestSimilar.js"(exports2) {
        "use strict";
        var maxDistance = 3;
        function editDistance(a, b) {
          if (Math.abs(a.length - b.length) > maxDistance)
            return Math.max(a.length, b.length);
          const d = [];
          for (let i = 0; i <= a.length; i++) {
            d[i] = [i];
          }
          for (let j = 0; j <= b.length; j++) {
            d[0][j] = j;
          }
          for (let j = 1; j <= b.length; j++) {
            for (let i = 1; i <= a.length; i++) {
              let cost = 1;
              if (a[i - 1] === b[j - 1]) {
                cost = 0;
              } else {
                cost = 1;
              }
              d[i][j] = Math.min(
                d[i - 1][j] + 1,
                // deletion
                d[i][j - 1] + 1,
                // insertion
                d[i - 1][j - 1] + cost
                // substitution
              );
              if (i > 1 && j > 1 && a[i - 1] === b[j - 2] && a[i - 2] === b[j - 1]) {
                d[i][j] = Math.min(d[i][j], d[i - 2][j - 2] + 1);
              }
            }
          }
          return d[a.length][b.length];
        }
        function suggestSimilar(word, candidates) {
          if (!candidates || candidates.length === 0) return "";
          candidates = Array.from(new Set(candidates));
          const searchingOptions = word.startsWith("--");
          if (searchingOptions) {
            word = word.slice(2);
            candidates = candidates.map((candidate) => candidate.slice(2));
          }
          let similar = [];
          let bestDistance = maxDistance;
          const minSimilarity = 0.4;
          candidates.forEach((candidate) => {
            if (candidate.length <= 1) return;
            const distance = editDistance(word, candidate);
            const length = Math.max(word.length, candidate.length);
            const similarity = (length - distance) / length;
            if (similarity > minSimilarity) {
              if (distance < bestDistance) {
                bestDistance = distance;
                similar = [candidate];
              } else if (distance === bestDistance) {
                similar.push(candidate);
              }
            }
          });
          similar.sort((a, b) => a.localeCompare(b));
          if (searchingOptions) {
            similar = similar.map((candidate) => `--${candidate}`);
          }
          if (similar.length > 1) {
            return `
(Did you mean one of ${similar.join(", ")}?)`;
          }
          if (similar.length === 1) {
            return `
(Did you mean ${similar[0]}?)`;
          }
          return "";
        }
        exports2.suggestSimilar = suggestSimilar;
      }
    });
    var require_command = __commonJS2({
      "../../node_modules/.pnpm/commander@12.1.0/node_modules/commander/lib/command.js"(exports2) {
        "use strict";
        var EventEmitter = __require("events").EventEmitter;
        var childProcess = __require("child_process");
        var path = __require("path");
        var fs = __require("fs");
        var process2 = __require("process");
        var { Argument: Argument2, humanReadableArgName } = require_argument();
        var { CommanderError: CommanderError2 } = require_error();
        var { Help: Help2 } = require_help();
        var { Option: Option2, DualOptions } = require_option();
        var { suggestSimilar } = require_suggestSimilar();
        var Command2 = class _Command extends EventEmitter {
          /**
           * Initialize a new `Command`.
           *
           * @param {string} [name]
           */
          constructor(name) {
            super();
            this.commands = [];
            this.options = [];
            this.parent = null;
            this._allowUnknownOption = false;
            this._allowExcessArguments = true;
            this.registeredArguments = [];
            this._args = this.registeredArguments;
            this.args = [];
            this.rawArgs = [];
            this.processedArgs = [];
            this._scriptPath = null;
            this._name = name || "";
            this._optionValues = {};
            this._optionValueSources = {};
            this._storeOptionsAsProperties = false;
            this._actionHandler = null;
            this._executableHandler = false;
            this._executableFile = null;
            this._executableDir = null;
            this._defaultCommandName = null;
            this._exitCallback = null;
            this._aliases = [];
            this._combineFlagAndOptionalValue = true;
            this._description = "";
            this._summary = "";
            this._argsDescription = void 0;
            this._enablePositionalOptions = false;
            this._passThroughOptions = false;
            this._lifeCycleHooks = {};
            this._showHelpAfterError = false;
            this._showSuggestionAfterError = true;
            this._outputConfiguration = {
              writeOut: (str) => process2.stdout.write(str),
              writeErr: (str) => process2.stderr.write(str),
              getOutHelpWidth: () => process2.stdout.isTTY ? process2.stdout.columns : void 0,
              getErrHelpWidth: () => process2.stderr.isTTY ? process2.stderr.columns : void 0,
              outputError: (str, write) => write(str)
            };
            this._hidden = false;
            this._helpOption = void 0;
            this._addImplicitHelpCommand = void 0;
            this._helpCommand = void 0;
            this._helpConfiguration = {};
          }
          /**
           * Copy settings that are useful to have in common across root command and subcommands.
           *
           * (Used internally when adding a command using `.command()` so subcommands inherit parent settings.)
           *
           * @param {Command} sourceCommand
           * @return {Command} `this` command for chaining
           */
          copyInheritedSettings(sourceCommand) {
            this._outputConfiguration = sourceCommand._outputConfiguration;
            this._helpOption = sourceCommand._helpOption;
            this._helpCommand = sourceCommand._helpCommand;
            this._helpConfiguration = sourceCommand._helpConfiguration;
            this._exitCallback = sourceCommand._exitCallback;
            this._storeOptionsAsProperties = sourceCommand._storeOptionsAsProperties;
            this._combineFlagAndOptionalValue = sourceCommand._combineFlagAndOptionalValue;
            this._allowExcessArguments = sourceCommand._allowExcessArguments;
            this._enablePositionalOptions = sourceCommand._enablePositionalOptions;
            this._showHelpAfterError = sourceCommand._showHelpAfterError;
            this._showSuggestionAfterError = sourceCommand._showSuggestionAfterError;
            return this;
          }
          /**
           * @returns {Command[]}
           * @private
           */
          _getCommandAndAncestors() {
            const result = [];
            for (let command = this; command; command = command.parent) {
              result.push(command);
            }
            return result;
          }
          /**
           * Define a command.
           *
           * There are two styles of command: pay attention to where to put the description.
           *
           * @example
           * // Command implemented using action handler (description is supplied separately to `.command`)
           * program
           *   .command('clone <source> [destination]')
           *   .description('clone a repository into a newly created directory')
           *   .action((source, destination) => {
           *     console.log('clone command called');
           *   });
           *
           * // Command implemented using separate executable file (description is second parameter to `.command`)
           * program
           *   .command('start <service>', 'start named service')
           *   .command('stop [service]', 'stop named service, or all if no name supplied');
           *
           * @param {string} nameAndArgs - command name and arguments, args are `<required>` or `[optional]` and last may also be `variadic...`
           * @param {(object | string)} [actionOptsOrExecDesc] - configuration options (for action), or description (for executable)
           * @param {object} [execOpts] - configuration options (for executable)
           * @return {Command} returns new command for action handler, or `this` for executable command
           */
          command(nameAndArgs, actionOptsOrExecDesc, execOpts) {
            let desc = actionOptsOrExecDesc;
            let opts = execOpts;
            if (typeof desc === "object" && desc !== null) {
              opts = desc;
              desc = null;
            }
            opts = opts || {};
            const [, name, args] = nameAndArgs.match(/([^ ]+) *(.*)/);
            const cmd = this.createCommand(name);
            if (desc) {
              cmd.description(desc);
              cmd._executableHandler = true;
            }
            if (opts.isDefault) this._defaultCommandName = cmd._name;
            cmd._hidden = !!(opts.noHelp || opts.hidden);
            cmd._executableFile = opts.executableFile || null;
            if (args) cmd.arguments(args);
            this._registerCommand(cmd);
            cmd.parent = this;
            cmd.copyInheritedSettings(this);
            if (desc) return this;
            return cmd;
          }
          /**
           * Factory routine to create a new unattached command.
           *
           * See .command() for creating an attached subcommand, which uses this routine to
           * create the command. You can override createCommand to customise subcommands.
           *
           * @param {string} [name]
           * @return {Command} new command
           */
          createCommand(name) {
            return new _Command(name);
          }
          /**
           * You can customise the help with a subclass of Help by overriding createHelp,
           * or by overriding Help properties using configureHelp().
           *
           * @return {Help}
           */
          createHelp() {
            return Object.assign(new Help2(), this.configureHelp());
          }
          /**
           * You can customise the help by overriding Help properties using configureHelp(),
           * or with a subclass of Help by overriding createHelp().
           *
           * @param {object} [configuration] - configuration options
           * @return {(Command | object)} `this` command for chaining, or stored configuration
           */
          configureHelp(configuration) {
            if (configuration === void 0) return this._helpConfiguration;
            this._helpConfiguration = configuration;
            return this;
          }
          /**
           * The default output goes to stdout and stderr. You can customise this for special
           * applications. You can also customise the display of errors by overriding outputError.
           *
           * The configuration properties are all functions:
           *
           *     // functions to change where being written, stdout and stderr
           *     writeOut(str)
           *     writeErr(str)
           *     // matching functions to specify width for wrapping help
           *     getOutHelpWidth()
           *     getErrHelpWidth()
           *     // functions based on what is being written out
           *     outputError(str, write) // used for displaying errors, and not used for displaying help
           *
           * @param {object} [configuration] - configuration options
           * @return {(Command | object)} `this` command for chaining, or stored configuration
           */
          configureOutput(configuration) {
            if (configuration === void 0) return this._outputConfiguration;
            Object.assign(this._outputConfiguration, configuration);
            return this;
          }
          /**
           * Display the help or a custom message after an error occurs.
           *
           * @param {(boolean|string)} [displayHelp]
           * @return {Command} `this` command for chaining
           */
          showHelpAfterError(displayHelp = true) {
            if (typeof displayHelp !== "string") displayHelp = !!displayHelp;
            this._showHelpAfterError = displayHelp;
            return this;
          }
          /**
           * Display suggestion of similar commands for unknown commands, or options for unknown options.
           *
           * @param {boolean} [displaySuggestion]
           * @return {Command} `this` command for chaining
           */
          showSuggestionAfterError(displaySuggestion = true) {
            this._showSuggestionAfterError = !!displaySuggestion;
            return this;
          }
          /**
           * Add a prepared subcommand.
           *
           * See .command() for creating an attached subcommand which inherits settings from its parent.
           *
           * @param {Command} cmd - new subcommand
           * @param {object} [opts] - configuration options
           * @return {Command} `this` command for chaining
           */
          addCommand(cmd, opts) {
            if (!cmd._name) {
              throw new Error(`Command passed to .addCommand() must have a name
- specify the name in Command constructor or using .name()`);
            }
            opts = opts || {};
            if (opts.isDefault) this._defaultCommandName = cmd._name;
            if (opts.noHelp || opts.hidden) cmd._hidden = true;
            this._registerCommand(cmd);
            cmd.parent = this;
            cmd._checkForBrokenPassThrough();
            return this;
          }
          /**
           * Factory routine to create a new unattached argument.
           *
           * See .argument() for creating an attached argument, which uses this routine to
           * create the argument. You can override createArgument to return a custom argument.
           *
           * @param {string} name
           * @param {string} [description]
           * @return {Argument} new argument
           */
          createArgument(name, description) {
            return new Argument2(name, description);
          }
          /**
           * Define argument syntax for command.
           *
           * The default is that the argument is required, and you can explicitly
           * indicate this with <> around the name. Put [] around the name for an optional argument.
           *
           * @example
           * program.argument('<input-file>');
           * program.argument('[output-file]');
           *
           * @param {string} name
           * @param {string} [description]
           * @param {(Function|*)} [fn] - custom argument processing function
           * @param {*} [defaultValue]
           * @return {Command} `this` command for chaining
           */
          argument(name, description, fn, defaultValue) {
            const argument = this.createArgument(name, description);
            if (typeof fn === "function") {
              argument.default(defaultValue).argParser(fn);
            } else {
              argument.default(fn);
            }
            this.addArgument(argument);
            return this;
          }
          /**
           * Define argument syntax for command, adding multiple at once (without descriptions).
           *
           * See also .argument().
           *
           * @example
           * program.arguments('<cmd> [env]');
           *
           * @param {string} names
           * @return {Command} `this` command for chaining
           */
          arguments(names) {
            names.trim().split(/ +/).forEach((detail) => {
              this.argument(detail);
            });
            return this;
          }
          /**
           * Define argument syntax for command, adding a prepared argument.
           *
           * @param {Argument} argument
           * @return {Command} `this` command for chaining
           */
          addArgument(argument) {
            const previousArgument = this.registeredArguments.slice(-1)[0];
            if (previousArgument && previousArgument.variadic) {
              throw new Error(
                `only the last argument can be variadic '${previousArgument.name()}'`
              );
            }
            if (argument.required && argument.defaultValue !== void 0 && argument.parseArg === void 0) {
              throw new Error(
                `a default value for a required argument is never used: '${argument.name()}'`
              );
            }
            this.registeredArguments.push(argument);
            return this;
          }
          /**
           * Customise or override default help command. By default a help command is automatically added if your command has subcommands.
           *
           * @example
           *    program.helpCommand('help [cmd]');
           *    program.helpCommand('help [cmd]', 'show help');
           *    program.helpCommand(false); // suppress default help command
           *    program.helpCommand(true); // add help command even if no subcommands
           *
           * @param {string|boolean} enableOrNameAndArgs - enable with custom name and/or arguments, or boolean to override whether added
           * @param {string} [description] - custom description
           * @return {Command} `this` command for chaining
           */
          helpCommand(enableOrNameAndArgs, description) {
            if (typeof enableOrNameAndArgs === "boolean") {
              this._addImplicitHelpCommand = enableOrNameAndArgs;
              return this;
            }
            enableOrNameAndArgs = enableOrNameAndArgs ?? "help [command]";
            const [, helpName, helpArgs] = enableOrNameAndArgs.match(/([^ ]+) *(.*)/);
            const helpDescription = description ?? "display help for command";
            const helpCommand = this.createCommand(helpName);
            helpCommand.helpOption(false);
            if (helpArgs) helpCommand.arguments(helpArgs);
            if (helpDescription) helpCommand.description(helpDescription);
            this._addImplicitHelpCommand = true;
            this._helpCommand = helpCommand;
            return this;
          }
          /**
           * Add prepared custom help command.
           *
           * @param {(Command|string|boolean)} helpCommand - custom help command, or deprecated enableOrNameAndArgs as for `.helpCommand()`
           * @param {string} [deprecatedDescription] - deprecated custom description used with custom name only
           * @return {Command} `this` command for chaining
           */
          addHelpCommand(helpCommand, deprecatedDescription) {
            if (typeof helpCommand !== "object") {
              this.helpCommand(helpCommand, deprecatedDescription);
              return this;
            }
            this._addImplicitHelpCommand = true;
            this._helpCommand = helpCommand;
            return this;
          }
          /**
           * Lazy create help command.
           *
           * @return {(Command|null)}
           * @package
           */
          _getHelpCommand() {
            const hasImplicitHelpCommand = this._addImplicitHelpCommand ?? (this.commands.length && !this._actionHandler && !this._findCommand("help"));
            if (hasImplicitHelpCommand) {
              if (this._helpCommand === void 0) {
                this.helpCommand(void 0, void 0);
              }
              return this._helpCommand;
            }
            return null;
          }
          /**
           * Add hook for life cycle event.
           *
           * @param {string} event
           * @param {Function} listener
           * @return {Command} `this` command for chaining
           */
          hook(event, listener) {
            const allowedValues = ["preSubcommand", "preAction", "postAction"];
            if (!allowedValues.includes(event)) {
              throw new Error(`Unexpected value for event passed to hook : '${event}'.
Expecting one of '${allowedValues.join("', '")}'`);
            }
            if (this._lifeCycleHooks[event]) {
              this._lifeCycleHooks[event].push(listener);
            } else {
              this._lifeCycleHooks[event] = [listener];
            }
            return this;
          }
          /**
           * Register callback to use as replacement for calling process.exit.
           *
           * @param {Function} [fn] optional callback which will be passed a CommanderError, defaults to throwing
           * @return {Command} `this` command for chaining
           */
          exitOverride(fn) {
            if (fn) {
              this._exitCallback = fn;
            } else {
              this._exitCallback = (err) => {
                if (err.code !== "commander.executeSubCommandAsync") {
                  throw err;
                } else {
                }
              };
            }
            return this;
          }
          /**
           * Call process.exit, and _exitCallback if defined.
           *
           * @param {number} exitCode exit code for using with process.exit
           * @param {string} code an id string representing the error
           * @param {string} message human-readable description of the error
           * @return never
           * @private
           */
          _exit(exitCode, code, message) {
            if (this._exitCallback) {
              this._exitCallback(new CommanderError2(exitCode, code, message));
            }
            process2.exit(exitCode);
          }
          /**
           * Register callback `fn` for the command.
           *
           * @example
           * program
           *   .command('serve')
           *   .description('start service')
           *   .action(function() {
           *      // do work here
           *   });
           *
           * @param {Function} fn
           * @return {Command} `this` command for chaining
           */
          action(fn) {
            const listener = (args) => {
              const expectedArgsCount = this.registeredArguments.length;
              const actionArgs = args.slice(0, expectedArgsCount);
              if (this._storeOptionsAsProperties) {
                actionArgs[expectedArgsCount] = this;
              } else {
                actionArgs[expectedArgsCount] = this.opts();
              }
              actionArgs.push(this);
              return fn.apply(this, actionArgs);
            };
            this._actionHandler = listener;
            return this;
          }
          /**
           * Factory routine to create a new unattached option.
           *
           * See .option() for creating an attached option, which uses this routine to
           * create the option. You can override createOption to return a custom option.
           *
           * @param {string} flags
           * @param {string} [description]
           * @return {Option} new option
           */
          createOption(flags, description) {
            return new Option2(flags, description);
          }
          /**
           * Wrap parseArgs to catch 'commander.invalidArgument'.
           *
           * @param {(Option | Argument)} target
           * @param {string} value
           * @param {*} previous
           * @param {string} invalidArgumentMessage
           * @private
           */
          _callParseArg(target, value, previous, invalidArgumentMessage) {
            try {
              return target.parseArg(value, previous);
            } catch (err) {
              if (err.code === "commander.invalidArgument") {
                const message = `${invalidArgumentMessage} ${err.message}`;
                this.error(message, { exitCode: err.exitCode, code: err.code });
              }
              throw err;
            }
          }
          /**
           * Check for option flag conflicts.
           * Register option if no conflicts found, or throw on conflict.
           *
           * @param {Option} option
           * @private
           */
          _registerOption(option) {
            const matchingOption = option.short && this._findOption(option.short) || option.long && this._findOption(option.long);
            if (matchingOption) {
              const matchingFlag = option.long && this._findOption(option.long) ? option.long : option.short;
              throw new Error(`Cannot add option '${option.flags}'${this._name && ` to command '${this._name}'`} due to conflicting flag '${matchingFlag}'
-  already used by option '${matchingOption.flags}'`);
            }
            this.options.push(option);
          }
          /**
           * Check for command name and alias conflicts with existing commands.
           * Register command if no conflicts found, or throw on conflict.
           *
           * @param {Command} command
           * @private
           */
          _registerCommand(command) {
            const knownBy = (cmd) => {
              return [cmd.name()].concat(cmd.aliases());
            };
            const alreadyUsed = knownBy(command).find(
              (name) => this._findCommand(name)
            );
            if (alreadyUsed) {
              const existingCmd = knownBy(this._findCommand(alreadyUsed)).join("|");
              const newCmd = knownBy(command).join("|");
              throw new Error(
                `cannot add command '${newCmd}' as already have command '${existingCmd}'`
              );
            }
            this.commands.push(command);
          }
          /**
           * Add an option.
           *
           * @param {Option} option
           * @return {Command} `this` command for chaining
           */
          addOption(option) {
            this._registerOption(option);
            const oname = option.name();
            const name = option.attributeName();
            if (option.negate) {
              const positiveLongFlag = option.long.replace(/^--no-/, "--");
              if (!this._findOption(positiveLongFlag)) {
                this.setOptionValueWithSource(
                  name,
                  option.defaultValue === void 0 ? true : option.defaultValue,
                  "default"
                );
              }
            } else if (option.defaultValue !== void 0) {
              this.setOptionValueWithSource(name, option.defaultValue, "default");
            }
            const handleOptionValue = (val, invalidValueMessage, valueSource) => {
              if (val == null && option.presetArg !== void 0) {
                val = option.presetArg;
              }
              const oldValue = this.getOptionValue(name);
              if (val !== null && option.parseArg) {
                val = this._callParseArg(option, val, oldValue, invalidValueMessage);
              } else if (val !== null && option.variadic) {
                val = option._concatValue(val, oldValue);
              }
              if (val == null) {
                if (option.negate) {
                  val = false;
                } else if (option.isBoolean() || option.optional) {
                  val = true;
                } else {
                  val = "";
                }
              }
              this.setOptionValueWithSource(name, val, valueSource);
            };
            this.on("option:" + oname, (val) => {
              const invalidValueMessage = `error: option '${option.flags}' argument '${val}' is invalid.`;
              handleOptionValue(val, invalidValueMessage, "cli");
            });
            if (option.envVar) {
              this.on("optionEnv:" + oname, (val) => {
                const invalidValueMessage = `error: option '${option.flags}' value '${val}' from env '${option.envVar}' is invalid.`;
                handleOptionValue(val, invalidValueMessage, "env");
              });
            }
            return this;
          }
          /**
           * Internal implementation shared by .option() and .requiredOption()
           *
           * @return {Command} `this` command for chaining
           * @private
           */
          _optionEx(config, flags, description, fn, defaultValue) {
            if (typeof flags === "object" && flags instanceof Option2) {
              throw new Error(
                "To add an Option object use addOption() instead of option() or requiredOption()"
              );
            }
            const option = this.createOption(flags, description);
            option.makeOptionMandatory(!!config.mandatory);
            if (typeof fn === "function") {
              option.default(defaultValue).argParser(fn);
            } else if (fn instanceof RegExp) {
              const regex = fn;
              fn = (val, def) => {
                const m = regex.exec(val);
                return m ? m[0] : def;
              };
              option.default(defaultValue).argParser(fn);
            } else {
              option.default(fn);
            }
            return this.addOption(option);
          }
          /**
           * Define option with `flags`, `description`, and optional argument parsing function or `defaultValue` or both.
           *
           * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space. A required
           * option-argument is indicated by `<>` and an optional option-argument by `[]`.
           *
           * See the README for more details, and see also addOption() and requiredOption().
           *
           * @example
           * program
           *     .option('-p, --pepper', 'add pepper')
           *     .option('-p, --pizza-type <TYPE>', 'type of pizza') // required option-argument
           *     .option('-c, --cheese [CHEESE]', 'add extra cheese', 'mozzarella') // optional option-argument with default
           *     .option('-t, --tip <VALUE>', 'add tip to purchase cost', parseFloat) // custom parse function
           *
           * @param {string} flags
           * @param {string} [description]
           * @param {(Function|*)} [parseArg] - custom option processing function or default value
           * @param {*} [defaultValue]
           * @return {Command} `this` command for chaining
           */
          option(flags, description, parseArg, defaultValue) {
            return this._optionEx({}, flags, description, parseArg, defaultValue);
          }
          /**
           * Add a required option which must have a value after parsing. This usually means
           * the option must be specified on the command line. (Otherwise the same as .option().)
           *
           * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space.
           *
           * @param {string} flags
           * @param {string} [description]
           * @param {(Function|*)} [parseArg] - custom option processing function or default value
           * @param {*} [defaultValue]
           * @return {Command} `this` command for chaining
           */
          requiredOption(flags, description, parseArg, defaultValue) {
            return this._optionEx(
              { mandatory: true },
              flags,
              description,
              parseArg,
              defaultValue
            );
          }
          /**
           * Alter parsing of short flags with optional values.
           *
           * @example
           * // for `.option('-f,--flag [value]'):
           * program.combineFlagAndOptionalValue(true);  // `-f80` is treated like `--flag=80`, this is the default behaviour
           * program.combineFlagAndOptionalValue(false) // `-fb` is treated like `-f -b`
           *
           * @param {boolean} [combine] - if `true` or omitted, an optional value can be specified directly after the flag.
           * @return {Command} `this` command for chaining
           */
          combineFlagAndOptionalValue(combine = true) {
            this._combineFlagAndOptionalValue = !!combine;
            return this;
          }
          /**
           * Allow unknown options on the command line.
           *
           * @param {boolean} [allowUnknown] - if `true` or omitted, no error will be thrown for unknown options.
           * @return {Command} `this` command for chaining
           */
          allowUnknownOption(allowUnknown = true) {
            this._allowUnknownOption = !!allowUnknown;
            return this;
          }
          /**
           * Allow excess command-arguments on the command line. Pass false to make excess arguments an error.
           *
           * @param {boolean} [allowExcess] - if `true` or omitted, no error will be thrown for excess arguments.
           * @return {Command} `this` command for chaining
           */
          allowExcessArguments(allowExcess = true) {
            this._allowExcessArguments = !!allowExcess;
            return this;
          }
          /**
           * Enable positional options. Positional means global options are specified before subcommands which lets
           * subcommands reuse the same option names, and also enables subcommands to turn on passThroughOptions.
           * The default behaviour is non-positional and global options may appear anywhere on the command line.
           *
           * @param {boolean} [positional]
           * @return {Command} `this` command for chaining
           */
          enablePositionalOptions(positional = true) {
            this._enablePositionalOptions = !!positional;
            return this;
          }
          /**
           * Pass through options that come after command-arguments rather than treat them as command-options,
           * so actual command-options come before command-arguments. Turning this on for a subcommand requires
           * positional options to have been enabled on the program (parent commands).
           * The default behaviour is non-positional and options may appear before or after command-arguments.
           *
           * @param {boolean} [passThrough] for unknown options.
           * @return {Command} `this` command for chaining
           */
          passThroughOptions(passThrough = true) {
            this._passThroughOptions = !!passThrough;
            this._checkForBrokenPassThrough();
            return this;
          }
          /**
           * @private
           */
          _checkForBrokenPassThrough() {
            if (this.parent && this._passThroughOptions && !this.parent._enablePositionalOptions) {
              throw new Error(
                `passThroughOptions cannot be used for '${this._name}' without turning on enablePositionalOptions for parent command(s)`
              );
            }
          }
          /**
           * Whether to store option values as properties on command object,
           * or store separately (specify false). In both cases the option values can be accessed using .opts().
           *
           * @param {boolean} [storeAsProperties=true]
           * @return {Command} `this` command for chaining
           */
          storeOptionsAsProperties(storeAsProperties = true) {
            if (this.options.length) {
              throw new Error("call .storeOptionsAsProperties() before adding options");
            }
            if (Object.keys(this._optionValues).length) {
              throw new Error(
                "call .storeOptionsAsProperties() before setting option values"
              );
            }
            this._storeOptionsAsProperties = !!storeAsProperties;
            return this;
          }
          /**
           * Retrieve option value.
           *
           * @param {string} key
           * @return {object} value
           */
          getOptionValue(key) {
            if (this._storeOptionsAsProperties) {
              return this[key];
            }
            return this._optionValues[key];
          }
          /**
           * Store option value.
           *
           * @param {string} key
           * @param {object} value
           * @return {Command} `this` command for chaining
           */
          setOptionValue(key, value) {
            return this.setOptionValueWithSource(key, value, void 0);
          }
          /**
           * Store option value and where the value came from.
           *
           * @param {string} key
           * @param {object} value
           * @param {string} source - expected values are default/config/env/cli/implied
           * @return {Command} `this` command for chaining
           */
          setOptionValueWithSource(key, value, source) {
            if (this._storeOptionsAsProperties) {
              this[key] = value;
            } else {
              this._optionValues[key] = value;
            }
            this._optionValueSources[key] = source;
            return this;
          }
          /**
           * Get source of option value.
           * Expected values are default | config | env | cli | implied
           *
           * @param {string} key
           * @return {string}
           */
          getOptionValueSource(key) {
            return this._optionValueSources[key];
          }
          /**
           * Get source of option value. See also .optsWithGlobals().
           * Expected values are default | config | env | cli | implied
           *
           * @param {string} key
           * @return {string}
           */
          getOptionValueSourceWithGlobals(key) {
            let source;
            this._getCommandAndAncestors().forEach((cmd) => {
              if (cmd.getOptionValueSource(key) !== void 0) {
                source = cmd.getOptionValueSource(key);
              }
            });
            return source;
          }
          /**
           * Get user arguments from implied or explicit arguments.
           * Side-effects: set _scriptPath if args included script. Used for default program name, and subcommand searches.
           *
           * @private
           */
          _prepareUserArgs(argv, parseOptions) {
            if (argv !== void 0 && !Array.isArray(argv)) {
              throw new Error("first parameter to parse must be array or undefined");
            }
            parseOptions = parseOptions || {};
            if (argv === void 0 && parseOptions.from === void 0) {
              if (process2.versions?.electron) {
                parseOptions.from = "electron";
              }
              const execArgv = process2.execArgv ?? [];
              if (execArgv.includes("-e") || execArgv.includes("--eval") || execArgv.includes("-p") || execArgv.includes("--print")) {
                parseOptions.from = "eval";
              }
            }
            if (argv === void 0) {
              argv = process2.argv;
            }
            this.rawArgs = argv.slice();
            let userArgs;
            switch (parseOptions.from) {
              case void 0:
              case "node":
                this._scriptPath = argv[1];
                userArgs = argv.slice(2);
                break;
              case "electron":
                if (process2.defaultApp) {
                  this._scriptPath = argv[1];
                  userArgs = argv.slice(2);
                } else {
                  userArgs = argv.slice(1);
                }
                break;
              case "user":
                userArgs = argv.slice(0);
                break;
              case "eval":
                userArgs = argv.slice(1);
                break;
              default:
                throw new Error(
                  `unexpected parse option { from: '${parseOptions.from}' }`
                );
            }
            if (!this._name && this._scriptPath)
              this.nameFromFilename(this._scriptPath);
            this._name = this._name || "program";
            return userArgs;
          }
          /**
           * Parse `argv`, setting options and invoking commands when defined.
           *
           * Use parseAsync instead of parse if any of your action handlers are async.
           *
           * Call with no parameters to parse `process.argv`. Detects Electron and special node options like `node --eval`. Easy mode!
           *
           * Or call with an array of strings to parse, and optionally where the user arguments start by specifying where the arguments are `from`:
           * - `'node'`: default, `argv[0]` is the application and `argv[1]` is the script being run, with user arguments after that
           * - `'electron'`: `argv[0]` is the application and `argv[1]` varies depending on whether the electron application is packaged
           * - `'user'`: just user arguments
           *
           * @example
           * program.parse(); // parse process.argv and auto-detect electron and special node flags
           * program.parse(process.argv); // assume argv[0] is app and argv[1] is script
           * program.parse(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
           *
           * @param {string[]} [argv] - optional, defaults to process.argv
           * @param {object} [parseOptions] - optionally specify style of options with from: node/user/electron
           * @param {string} [parseOptions.from] - where the args are from: 'node', 'user', 'electron'
           * @return {Command} `this` command for chaining
           */
          parse(argv, parseOptions) {
            const userArgs = this._prepareUserArgs(argv, parseOptions);
            this._parseCommand([], userArgs);
            return this;
          }
          /**
           * Parse `argv`, setting options and invoking commands when defined.
           *
           * Call with no parameters to parse `process.argv`. Detects Electron and special node options like `node --eval`. Easy mode!
           *
           * Or call with an array of strings to parse, and optionally where the user arguments start by specifying where the arguments are `from`:
           * - `'node'`: default, `argv[0]` is the application and `argv[1]` is the script being run, with user arguments after that
           * - `'electron'`: `argv[0]` is the application and `argv[1]` varies depending on whether the electron application is packaged
           * - `'user'`: just user arguments
           *
           * @example
           * await program.parseAsync(); // parse process.argv and auto-detect electron and special node flags
           * await program.parseAsync(process.argv); // assume argv[0] is app and argv[1] is script
           * await program.parseAsync(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
           *
           * @param {string[]} [argv]
           * @param {object} [parseOptions]
           * @param {string} parseOptions.from - where the args are from: 'node', 'user', 'electron'
           * @return {Promise}
           */
          async parseAsync(argv, parseOptions) {
            const userArgs = this._prepareUserArgs(argv, parseOptions);
            await this._parseCommand([], userArgs);
            return this;
          }
          /**
           * Execute a sub-command executable.
           *
           * @private
           */
          _executeSubCommand(subcommand, args) {
            args = args.slice();
            let launchWithNode = false;
            const sourceExt = [".js", ".ts", ".tsx", ".mjs", ".cjs"];
            function findFile(baseDir, baseName) {
              const localBin = path.resolve(baseDir, baseName);
              if (fs.existsSync(localBin)) return localBin;
              if (sourceExt.includes(path.extname(baseName))) return void 0;
              const foundExt = sourceExt.find(
                (ext) => fs.existsSync(`${localBin}${ext}`)
              );
              if (foundExt) return `${localBin}${foundExt}`;
              return void 0;
            }
            this._checkForMissingMandatoryOptions();
            this._checkForConflictingOptions();
            let executableFile = subcommand._executableFile || `${this._name}-${subcommand._name}`;
            let executableDir = this._executableDir || "";
            if (this._scriptPath) {
              let resolvedScriptPath;
              try {
                resolvedScriptPath = fs.realpathSync(this._scriptPath);
              } catch (err) {
                resolvedScriptPath = this._scriptPath;
              }
              executableDir = path.resolve(
                path.dirname(resolvedScriptPath),
                executableDir
              );
            }
            if (executableDir) {
              let localFile = findFile(executableDir, executableFile);
              if (!localFile && !subcommand._executableFile && this._scriptPath) {
                const legacyName = path.basename(
                  this._scriptPath,
                  path.extname(this._scriptPath)
                );
                if (legacyName !== this._name) {
                  localFile = findFile(
                    executableDir,
                    `${legacyName}-${subcommand._name}`
                  );
                }
              }
              executableFile = localFile || executableFile;
            }
            launchWithNode = sourceExt.includes(path.extname(executableFile));
            let proc;
            if (process2.platform !== "win32") {
              if (launchWithNode) {
                args.unshift(executableFile);
                args = incrementNodeInspectorPort(process2.execArgv).concat(args);
                proc = childProcess.spawn(process2.argv[0], args, { stdio: "inherit" });
              } else {
                proc = childProcess.spawn(executableFile, args, { stdio: "inherit" });
              }
            } else {
              args.unshift(executableFile);
              args = incrementNodeInspectorPort(process2.execArgv).concat(args);
              proc = childProcess.spawn(process2.execPath, args, { stdio: "inherit" });
            }
            if (!proc.killed) {
              const signals = ["SIGUSR1", "SIGUSR2", "SIGTERM", "SIGINT", "SIGHUP"];
              signals.forEach((signal) => {
                process2.on(signal, () => {
                  if (proc.killed === false && proc.exitCode === null) {
                    proc.kill(signal);
                  }
                });
              });
            }
            const exitCallback = this._exitCallback;
            proc.on("close", (code) => {
              code = code ?? 1;
              if (!exitCallback) {
                process2.exit(code);
              } else {
                exitCallback(
                  new CommanderError2(
                    code,
                    "commander.executeSubCommandAsync",
                    "(close)"
                  )
                );
              }
            });
            proc.on("error", (err) => {
              if (err.code === "ENOENT") {
                const executableDirMessage = executableDir ? `searched for local subcommand relative to directory '${executableDir}'` : "no directory for search for local subcommand, use .executableDir() to supply a custom directory";
                const executableMissing = `'${executableFile}' does not exist
 - if '${subcommand._name}' is not meant to be an executable command, remove description parameter from '.command()' and use '.description()' instead
 - if the default executable name is not suitable, use the executableFile option to supply a custom name or path
 - ${executableDirMessage}`;
                throw new Error(executableMissing);
              } else if (err.code === "EACCES") {
                throw new Error(`'${executableFile}' not executable`);
              }
              if (!exitCallback) {
                process2.exit(1);
              } else {
                const wrappedError = new CommanderError2(
                  1,
                  "commander.executeSubCommandAsync",
                  "(error)"
                );
                wrappedError.nestedError = err;
                exitCallback(wrappedError);
              }
            });
            this.runningCommand = proc;
          }
          /**
           * @private
           */
          _dispatchSubcommand(commandName, operands, unknown) {
            const subCommand = this._findCommand(commandName);
            if (!subCommand) this.help({ error: true });
            let promiseChain;
            promiseChain = this._chainOrCallSubCommandHook(
              promiseChain,
              subCommand,
              "preSubcommand"
            );
            promiseChain = this._chainOrCall(promiseChain, () => {
              if (subCommand._executableHandler) {
                this._executeSubCommand(subCommand, operands.concat(unknown));
              } else {
                return subCommand._parseCommand(operands, unknown);
              }
            });
            return promiseChain;
          }
          /**
           * Invoke help directly if possible, or dispatch if necessary.
           * e.g. help foo
           *
           * @private
           */
          _dispatchHelpCommand(subcommandName) {
            if (!subcommandName) {
              this.help();
            }
            const subCommand = this._findCommand(subcommandName);
            if (subCommand && !subCommand._executableHandler) {
              subCommand.help();
            }
            return this._dispatchSubcommand(
              subcommandName,
              [],
              [this._getHelpOption()?.long ?? this._getHelpOption()?.short ?? "--help"]
            );
          }
          /**
           * Check this.args against expected this.registeredArguments.
           *
           * @private
           */
          _checkNumberOfArguments() {
            this.registeredArguments.forEach((arg, i) => {
              if (arg.required && this.args[i] == null) {
                this.missingArgument(arg.name());
              }
            });
            if (this.registeredArguments.length > 0 && this.registeredArguments[this.registeredArguments.length - 1].variadic) {
              return;
            }
            if (this.args.length > this.registeredArguments.length) {
              this._excessArguments(this.args);
            }
          }
          /**
           * Process this.args using this.registeredArguments and save as this.processedArgs!
           *
           * @private
           */
          _processArguments() {
            const myParseArg = (argument, value, previous) => {
              let parsedValue = value;
              if (value !== null && argument.parseArg) {
                const invalidValueMessage = `error: command-argument value '${value}' is invalid for argument '${argument.name()}'.`;
                parsedValue = this._callParseArg(
                  argument,
                  value,
                  previous,
                  invalidValueMessage
                );
              }
              return parsedValue;
            };
            this._checkNumberOfArguments();
            const processedArgs = [];
            this.registeredArguments.forEach((declaredArg, index) => {
              let value = declaredArg.defaultValue;
              if (declaredArg.variadic) {
                if (index < this.args.length) {
                  value = this.args.slice(index);
                  if (declaredArg.parseArg) {
                    value = value.reduce((processed, v) => {
                      return myParseArg(declaredArg, v, processed);
                    }, declaredArg.defaultValue);
                  }
                } else if (value === void 0) {
                  value = [];
                }
              } else if (index < this.args.length) {
                value = this.args[index];
                if (declaredArg.parseArg) {
                  value = myParseArg(declaredArg, value, declaredArg.defaultValue);
                }
              }
              processedArgs[index] = value;
            });
            this.processedArgs = processedArgs;
          }
          /**
           * Once we have a promise we chain, but call synchronously until then.
           *
           * @param {(Promise|undefined)} promise
           * @param {Function} fn
           * @return {(Promise|undefined)}
           * @private
           */
          _chainOrCall(promise, fn) {
            if (promise && promise.then && typeof promise.then === "function") {
              return promise.then(() => fn());
            }
            return fn();
          }
          /**
           *
           * @param {(Promise|undefined)} promise
           * @param {string} event
           * @return {(Promise|undefined)}
           * @private
           */
          _chainOrCallHooks(promise, event) {
            let result = promise;
            const hooks = [];
            this._getCommandAndAncestors().reverse().filter((cmd) => cmd._lifeCycleHooks[event] !== void 0).forEach((hookedCommand) => {
              hookedCommand._lifeCycleHooks[event].forEach((callback) => {
                hooks.push({ hookedCommand, callback });
              });
            });
            if (event === "postAction") {
              hooks.reverse();
            }
            hooks.forEach((hookDetail) => {
              result = this._chainOrCall(result, () => {
                return hookDetail.callback(hookDetail.hookedCommand, this);
              });
            });
            return result;
          }
          /**
           *
           * @param {(Promise|undefined)} promise
           * @param {Command} subCommand
           * @param {string} event
           * @return {(Promise|undefined)}
           * @private
           */
          _chainOrCallSubCommandHook(promise, subCommand, event) {
            let result = promise;
            if (this._lifeCycleHooks[event] !== void 0) {
              this._lifeCycleHooks[event].forEach((hook) => {
                result = this._chainOrCall(result, () => {
                  return hook(this, subCommand);
                });
              });
            }
            return result;
          }
          /**
           * Process arguments in context of this command.
           * Returns action result, in case it is a promise.
           *
           * @private
           */
          _parseCommand(operands, unknown) {
            const parsed = this.parseOptions(unknown);
            this._parseOptionsEnv();
            this._parseOptionsImplied();
            operands = operands.concat(parsed.operands);
            unknown = parsed.unknown;
            this.args = operands.concat(unknown);
            if (operands && this._findCommand(operands[0])) {
              return this._dispatchSubcommand(operands[0], operands.slice(1), unknown);
            }
            if (this._getHelpCommand() && operands[0] === this._getHelpCommand().name()) {
              return this._dispatchHelpCommand(operands[1]);
            }
            if (this._defaultCommandName) {
              this._outputHelpIfRequested(unknown);
              return this._dispatchSubcommand(
                this._defaultCommandName,
                operands,
                unknown
              );
            }
            if (this.commands.length && this.args.length === 0 && !this._actionHandler && !this._defaultCommandName) {
              this.help({ error: true });
            }
            this._outputHelpIfRequested(parsed.unknown);
            this._checkForMissingMandatoryOptions();
            this._checkForConflictingOptions();
            const checkForUnknownOptions = () => {
              if (parsed.unknown.length > 0) {
                this.unknownOption(parsed.unknown[0]);
              }
            };
            const commandEvent = `command:${this.name()}`;
            if (this._actionHandler) {
              checkForUnknownOptions();
              this._processArguments();
              let promiseChain;
              promiseChain = this._chainOrCallHooks(promiseChain, "preAction");
              promiseChain = this._chainOrCall(
                promiseChain,
                () => this._actionHandler(this.processedArgs)
              );
              if (this.parent) {
                promiseChain = this._chainOrCall(promiseChain, () => {
                  this.parent.emit(commandEvent, operands, unknown);
                });
              }
              promiseChain = this._chainOrCallHooks(promiseChain, "postAction");
              return promiseChain;
            }
            if (this.parent && this.parent.listenerCount(commandEvent)) {
              checkForUnknownOptions();
              this._processArguments();
              this.parent.emit(commandEvent, operands, unknown);
            } else if (operands.length) {
              if (this._findCommand("*")) {
                return this._dispatchSubcommand("*", operands, unknown);
              }
              if (this.listenerCount("command:*")) {
                this.emit("command:*", operands, unknown);
              } else if (this.commands.length) {
                this.unknownCommand();
              } else {
                checkForUnknownOptions();
                this._processArguments();
              }
            } else if (this.commands.length) {
              checkForUnknownOptions();
              this.help({ error: true });
            } else {
              checkForUnknownOptions();
              this._processArguments();
            }
          }
          /**
           * Find matching command.
           *
           * @private
           * @return {Command | undefined}
           */
          _findCommand(name) {
            if (!name) return void 0;
            return this.commands.find(
              (cmd) => cmd._name === name || cmd._aliases.includes(name)
            );
          }
          /**
           * Return an option matching `arg` if any.
           *
           * @param {string} arg
           * @return {Option}
           * @package
           */
          _findOption(arg) {
            return this.options.find((option) => option.is(arg));
          }
          /**
           * Display an error message if a mandatory option does not have a value.
           * Called after checking for help flags in leaf subcommand.
           *
           * @private
           */
          _checkForMissingMandatoryOptions() {
            this._getCommandAndAncestors().forEach((cmd) => {
              cmd.options.forEach((anOption) => {
                if (anOption.mandatory && cmd.getOptionValue(anOption.attributeName()) === void 0) {
                  cmd.missingMandatoryOptionValue(anOption);
                }
              });
            });
          }
          /**
           * Display an error message if conflicting options are used together in this.
           *
           * @private
           */
          _checkForConflictingLocalOptions() {
            const definedNonDefaultOptions = this.options.filter((option) => {
              const optionKey = option.attributeName();
              if (this.getOptionValue(optionKey) === void 0) {
                return false;
              }
              return this.getOptionValueSource(optionKey) !== "default";
            });
            const optionsWithConflicting = definedNonDefaultOptions.filter(
              (option) => option.conflictsWith.length > 0
            );
            optionsWithConflicting.forEach((option) => {
              const conflictingAndDefined = definedNonDefaultOptions.find(
                (defined) => option.conflictsWith.includes(defined.attributeName())
              );
              if (conflictingAndDefined) {
                this._conflictingOption(option, conflictingAndDefined);
              }
            });
          }
          /**
           * Display an error message if conflicting options are used together.
           * Called after checking for help flags in leaf subcommand.
           *
           * @private
           */
          _checkForConflictingOptions() {
            this._getCommandAndAncestors().forEach((cmd) => {
              cmd._checkForConflictingLocalOptions();
            });
          }
          /**
           * Parse options from `argv` removing known options,
           * and return argv split into operands and unknown arguments.
           *
           * Examples:
           *
           *     argv => operands, unknown
           *     --known kkk op => [op], []
           *     op --known kkk => [op], []
           *     sub --unknown uuu op => [sub], [--unknown uuu op]
           *     sub -- --unknown uuu op => [sub --unknown uuu op], []
           *
           * @param {string[]} argv
           * @return {{operands: string[], unknown: string[]}}
           */
          parseOptions(argv) {
            const operands = [];
            const unknown = [];
            let dest = operands;
            const args = argv.slice();
            function maybeOption(arg) {
              return arg.length > 1 && arg[0] === "-";
            }
            let activeVariadicOption = null;
            while (args.length) {
              const arg = args.shift();
              if (arg === "--") {
                if (dest === unknown) dest.push(arg);
                dest.push(...args);
                break;
              }
              if (activeVariadicOption && !maybeOption(arg)) {
                this.emit(`option:${activeVariadicOption.name()}`, arg);
                continue;
              }
              activeVariadicOption = null;
              if (maybeOption(arg)) {
                const option = this._findOption(arg);
                if (option) {
                  if (option.required) {
                    const value = args.shift();
                    if (value === void 0) this.optionMissingArgument(option);
                    this.emit(`option:${option.name()}`, value);
                  } else if (option.optional) {
                    let value = null;
                    if (args.length > 0 && !maybeOption(args[0])) {
                      value = args.shift();
                    }
                    this.emit(`option:${option.name()}`, value);
                  } else {
                    this.emit(`option:${option.name()}`);
                  }
                  activeVariadicOption = option.variadic ? option : null;
                  continue;
                }
              }
              if (arg.length > 2 && arg[0] === "-" && arg[1] !== "-") {
                const option = this._findOption(`-${arg[1]}`);
                if (option) {
                  if (option.required || option.optional && this._combineFlagAndOptionalValue) {
                    this.emit(`option:${option.name()}`, arg.slice(2));
                  } else {
                    this.emit(`option:${option.name()}`);
                    args.unshift(`-${arg.slice(2)}`);
                  }
                  continue;
                }
              }
              if (/^--[^=]+=/.test(arg)) {
                const index = arg.indexOf("=");
                const option = this._findOption(arg.slice(0, index));
                if (option && (option.required || option.optional)) {
                  this.emit(`option:${option.name()}`, arg.slice(index + 1));
                  continue;
                }
              }
              if (maybeOption(arg)) {
                dest = unknown;
              }
              if ((this._enablePositionalOptions || this._passThroughOptions) && operands.length === 0 && unknown.length === 0) {
                if (this._findCommand(arg)) {
                  operands.push(arg);
                  if (args.length > 0) unknown.push(...args);
                  break;
                } else if (this._getHelpCommand() && arg === this._getHelpCommand().name()) {
                  operands.push(arg);
                  if (args.length > 0) operands.push(...args);
                  break;
                } else if (this._defaultCommandName) {
                  unknown.push(arg);
                  if (args.length > 0) unknown.push(...args);
                  break;
                }
              }
              if (this._passThroughOptions) {
                dest.push(arg);
                if (args.length > 0) dest.push(...args);
                break;
              }
              dest.push(arg);
            }
            return { operands, unknown };
          }
          /**
           * Return an object containing local option values as key-value pairs.
           *
           * @return {object}
           */
          opts() {
            if (this._storeOptionsAsProperties) {
              const result = {};
              const len = this.options.length;
              for (let i = 0; i < len; i++) {
                const key = this.options[i].attributeName();
                result[key] = key === this._versionOptionName ? this._version : this[key];
              }
              return result;
            }
            return this._optionValues;
          }
          /**
           * Return an object containing merged local and global option values as key-value pairs.
           *
           * @return {object}
           */
          optsWithGlobals() {
            return this._getCommandAndAncestors().reduce(
              (combinedOptions, cmd) => Object.assign(combinedOptions, cmd.opts()),
              {}
            );
          }
          /**
           * Display error message and exit (or call exitOverride).
           *
           * @param {string} message
           * @param {object} [errorOptions]
           * @param {string} [errorOptions.code] - an id string representing the error
           * @param {number} [errorOptions.exitCode] - used with process.exit
           */
          error(message, errorOptions) {
            this._outputConfiguration.outputError(
              `${message}
`,
              this._outputConfiguration.writeErr
            );
            if (typeof this._showHelpAfterError === "string") {
              this._outputConfiguration.writeErr(`${this._showHelpAfterError}
`);
            } else if (this._showHelpAfterError) {
              this._outputConfiguration.writeErr("\n");
              this.outputHelp({ error: true });
            }
            const config = errorOptions || {};
            const exitCode = config.exitCode || 1;
            const code = config.code || "commander.error";
            this._exit(exitCode, code, message);
          }
          /**
           * Apply any option related environment variables, if option does
           * not have a value from cli or client code.
           *
           * @private
           */
          _parseOptionsEnv() {
            this.options.forEach((option) => {
              if (option.envVar && option.envVar in process2.env) {
                const optionKey = option.attributeName();
                if (this.getOptionValue(optionKey) === void 0 || ["default", "config", "env"].includes(
                  this.getOptionValueSource(optionKey)
                )) {
                  if (option.required || option.optional) {
                    this.emit(`optionEnv:${option.name()}`, process2.env[option.envVar]);
                  } else {
                    this.emit(`optionEnv:${option.name()}`);
                  }
                }
              }
            });
          }
          /**
           * Apply any implied option values, if option is undefined or default value.
           *
           * @private
           */
          _parseOptionsImplied() {
            const dualHelper = new DualOptions(this.options);
            const hasCustomOptionValue = (optionKey) => {
              return this.getOptionValue(optionKey) !== void 0 && !["default", "implied"].includes(this.getOptionValueSource(optionKey));
            };
            this.options.filter(
              (option) => option.implied !== void 0 && hasCustomOptionValue(option.attributeName()) && dualHelper.valueFromOption(
                this.getOptionValue(option.attributeName()),
                option
              )
            ).forEach((option) => {
              Object.keys(option.implied).filter((impliedKey) => !hasCustomOptionValue(impliedKey)).forEach((impliedKey) => {
                this.setOptionValueWithSource(
                  impliedKey,
                  option.implied[impliedKey],
                  "implied"
                );
              });
            });
          }
          /**
           * Argument `name` is missing.
           *
           * @param {string} name
           * @private
           */
          missingArgument(name) {
            const message = `error: missing required argument '${name}'`;
            this.error(message, { code: "commander.missingArgument" });
          }
          /**
           * `Option` is missing an argument.
           *
           * @param {Option} option
           * @private
           */
          optionMissingArgument(option) {
            const message = `error: option '${option.flags}' argument missing`;
            this.error(message, { code: "commander.optionMissingArgument" });
          }
          /**
           * `Option` does not have a value, and is a mandatory option.
           *
           * @param {Option} option
           * @private
           */
          missingMandatoryOptionValue(option) {
            const message = `error: required option '${option.flags}' not specified`;
            this.error(message, { code: "commander.missingMandatoryOptionValue" });
          }
          /**
           * `Option` conflicts with another option.
           *
           * @param {Option} option
           * @param {Option} conflictingOption
           * @private
           */
          _conflictingOption(option, conflictingOption) {
            const findBestOptionFromValue = (option2) => {
              const optionKey = option2.attributeName();
              const optionValue = this.getOptionValue(optionKey);
              const negativeOption = this.options.find(
                (target) => target.negate && optionKey === target.attributeName()
              );
              const positiveOption = this.options.find(
                (target) => !target.negate && optionKey === target.attributeName()
              );
              if (negativeOption && (negativeOption.presetArg === void 0 && optionValue === false || negativeOption.presetArg !== void 0 && optionValue === negativeOption.presetArg)) {
                return negativeOption;
              }
              return positiveOption || option2;
            };
            const getErrorMessage = (option2) => {
              const bestOption = findBestOptionFromValue(option2);
              const optionKey = bestOption.attributeName();
              const source = this.getOptionValueSource(optionKey);
              if (source === "env") {
                return `environment variable '${bestOption.envVar}'`;
              }
              return `option '${bestOption.flags}'`;
            };
            const message = `error: ${getErrorMessage(option)} cannot be used with ${getErrorMessage(conflictingOption)}`;
            this.error(message, { code: "commander.conflictingOption" });
          }
          /**
           * Unknown option `flag`.
           *
           * @param {string} flag
           * @private
           */
          unknownOption(flag) {
            if (this._allowUnknownOption) return;
            let suggestion = "";
            if (flag.startsWith("--") && this._showSuggestionAfterError) {
              let candidateFlags = [];
              let command = this;
              do {
                const moreFlags = command.createHelp().visibleOptions(command).filter((option) => option.long).map((option) => option.long);
                candidateFlags = candidateFlags.concat(moreFlags);
                command = command.parent;
              } while (command && !command._enablePositionalOptions);
              suggestion = suggestSimilar(flag, candidateFlags);
            }
            const message = `error: unknown option '${flag}'${suggestion}`;
            this.error(message, { code: "commander.unknownOption" });
          }
          /**
           * Excess arguments, more than expected.
           *
           * @param {string[]} receivedArgs
           * @private
           */
          _excessArguments(receivedArgs) {
            if (this._allowExcessArguments) return;
            const expected = this.registeredArguments.length;
            const s = expected === 1 ? "" : "s";
            const forSubcommand = this.parent ? ` for '${this.name()}'` : "";
            const message = `error: too many arguments${forSubcommand}. Expected ${expected} argument${s} but got ${receivedArgs.length}.`;
            this.error(message, { code: "commander.excessArguments" });
          }
          /**
           * Unknown command.
           *
           * @private
           */
          unknownCommand() {
            const unknownName = this.args[0];
            let suggestion = "";
            if (this._showSuggestionAfterError) {
              const candidateNames = [];
              this.createHelp().visibleCommands(this).forEach((command) => {
                candidateNames.push(command.name());
                if (command.alias()) candidateNames.push(command.alias());
              });
              suggestion = suggestSimilar(unknownName, candidateNames);
            }
            const message = `error: unknown command '${unknownName}'${suggestion}`;
            this.error(message, { code: "commander.unknownCommand" });
          }
          /**
           * Get or set the program version.
           *
           * This method auto-registers the "-V, --version" option which will print the version number.
           *
           * You can optionally supply the flags and description to override the defaults.
           *
           * @param {string} [str]
           * @param {string} [flags]
           * @param {string} [description]
           * @return {(this | string | undefined)} `this` command for chaining, or version string if no arguments
           */
          version(str, flags, description) {
            if (str === void 0) return this._version;
            this._version = str;
            flags = flags || "-V, --version";
            description = description || "output the version number";
            const versionOption = this.createOption(flags, description);
            this._versionOptionName = versionOption.attributeName();
            this._registerOption(versionOption);
            this.on("option:" + versionOption.name(), () => {
              this._outputConfiguration.writeOut(`${str}
`);
              this._exit(0, "commander.version", str);
            });
            return this;
          }
          /**
           * Set the description.
           *
           * @param {string} [str]
           * @param {object} [argsDescription]
           * @return {(string|Command)}
           */
          description(str, argsDescription) {
            if (str === void 0 && argsDescription === void 0)
              return this._description;
            this._description = str;
            if (argsDescription) {
              this._argsDescription = argsDescription;
            }
            return this;
          }
          /**
           * Set the summary. Used when listed as subcommand of parent.
           *
           * @param {string} [str]
           * @return {(string|Command)}
           */
          summary(str) {
            if (str === void 0) return this._summary;
            this._summary = str;
            return this;
          }
          /**
           * Set an alias for the command.
           *
           * You may call more than once to add multiple aliases. Only the first alias is shown in the auto-generated help.
           *
           * @param {string} [alias]
           * @return {(string|Command)}
           */
          alias(alias) {
            if (alias === void 0) return this._aliases[0];
            let command = this;
            if (this.commands.length !== 0 && this.commands[this.commands.length - 1]._executableHandler) {
              command = this.commands[this.commands.length - 1];
            }
            if (alias === command._name)
              throw new Error("Command alias can't be the same as its name");
            const matchingCommand = this.parent?._findCommand(alias);
            if (matchingCommand) {
              const existingCmd = [matchingCommand.name()].concat(matchingCommand.aliases()).join("|");
              throw new Error(
                `cannot add alias '${alias}' to command '${this.name()}' as already have command '${existingCmd}'`
              );
            }
            command._aliases.push(alias);
            return this;
          }
          /**
           * Set aliases for the command.
           *
           * Only the first alias is shown in the auto-generated help.
           *
           * @param {string[]} [aliases]
           * @return {(string[]|Command)}
           */
          aliases(aliases) {
            if (aliases === void 0) return this._aliases;
            aliases.forEach((alias) => this.alias(alias));
            return this;
          }
          /**
           * Set / get the command usage `str`.
           *
           * @param {string} [str]
           * @return {(string|Command)}
           */
          usage(str) {
            if (str === void 0) {
              if (this._usage) return this._usage;
              const args = this.registeredArguments.map((arg) => {
                return humanReadableArgName(arg);
              });
              return [].concat(
                this.options.length || this._helpOption !== null ? "[options]" : [],
                this.commands.length ? "[command]" : [],
                this.registeredArguments.length ? args : []
              ).join(" ");
            }
            this._usage = str;
            return this;
          }
          /**
           * Get or set the name of the command.
           *
           * @param {string} [str]
           * @return {(string|Command)}
           */
          name(str) {
            if (str === void 0) return this._name;
            this._name = str;
            return this;
          }
          /**
           * Set the name of the command from script filename, such as process.argv[1],
           * or require.main.filename, or __filename.
           *
           * (Used internally and public although not documented in README.)
           *
           * @example
           * program.nameFromFilename(require.main.filename);
           *
           * @param {string} filename
           * @return {Command}
           */
          nameFromFilename(filename) {
            this._name = path.basename(filename, path.extname(filename));
            return this;
          }
          /**
           * Get or set the directory for searching for executable subcommands of this command.
           *
           * @example
           * program.executableDir(__dirname);
           * // or
           * program.executableDir('subcommands');
           *
           * @param {string} [path]
           * @return {(string|null|Command)}
           */
          executableDir(path2) {
            if (path2 === void 0) return this._executableDir;
            this._executableDir = path2;
            return this;
          }
          /**
           * Return program help documentation.
           *
           * @param {{ error: boolean }} [contextOptions] - pass {error:true} to wrap for stderr instead of stdout
           * @return {string}
           */
          helpInformation(contextOptions) {
            const helper = this.createHelp();
            if (helper.helpWidth === void 0) {
              helper.helpWidth = contextOptions && contextOptions.error ? this._outputConfiguration.getErrHelpWidth() : this._outputConfiguration.getOutHelpWidth();
            }
            return helper.formatHelp(this, helper);
          }
          /**
           * @private
           */
          _getHelpContext(contextOptions) {
            contextOptions = contextOptions || {};
            const context = { error: !!contextOptions.error };
            let write;
            if (context.error) {
              write = (arg) => this._outputConfiguration.writeErr(arg);
            } else {
              write = (arg) => this._outputConfiguration.writeOut(arg);
            }
            context.write = contextOptions.write || write;
            context.command = this;
            return context;
          }
          /**
           * Output help information for this command.
           *
           * Outputs built-in help, and custom text added using `.addHelpText()`.
           *
           * @param {{ error: boolean } | Function} [contextOptions] - pass {error:true} to write to stderr instead of stdout
           */
          outputHelp(contextOptions) {
            let deprecatedCallback;
            if (typeof contextOptions === "function") {
              deprecatedCallback = contextOptions;
              contextOptions = void 0;
            }
            const context = this._getHelpContext(contextOptions);
            this._getCommandAndAncestors().reverse().forEach((command) => command.emit("beforeAllHelp", context));
            this.emit("beforeHelp", context);
            let helpInformation = this.helpInformation(context);
            if (deprecatedCallback) {
              helpInformation = deprecatedCallback(helpInformation);
              if (typeof helpInformation !== "string" && !Buffer.isBuffer(helpInformation)) {
                throw new Error("outputHelp callback must return a string or a Buffer");
              }
            }
            context.write(helpInformation);
            if (this._getHelpOption()?.long) {
              this.emit(this._getHelpOption().long);
            }
            this.emit("afterHelp", context);
            this._getCommandAndAncestors().forEach(
              (command) => command.emit("afterAllHelp", context)
            );
          }
          /**
           * You can pass in flags and a description to customise the built-in help option.
           * Pass in false to disable the built-in help option.
           *
           * @example
           * program.helpOption('-?, --help' 'show help'); // customise
           * program.helpOption(false); // disable
           *
           * @param {(string | boolean)} flags
           * @param {string} [description]
           * @return {Command} `this` command for chaining
           */
          helpOption(flags, description) {
            if (typeof flags === "boolean") {
              if (flags) {
                this._helpOption = this._helpOption ?? void 0;
              } else {
                this._helpOption = null;
              }
              return this;
            }
            flags = flags ?? "-h, --help";
            description = description ?? "display help for command";
            this._helpOption = this.createOption(flags, description);
            return this;
          }
          /**
           * Lazy create help option.
           * Returns null if has been disabled with .helpOption(false).
           *
           * @returns {(Option | null)} the help option
           * @package
           */
          _getHelpOption() {
            if (this._helpOption === void 0) {
              this.helpOption(void 0, void 0);
            }
            return this._helpOption;
          }
          /**
           * Supply your own option to use for the built-in help option.
           * This is an alternative to using helpOption() to customise the flags and description etc.
           *
           * @param {Option} option
           * @return {Command} `this` command for chaining
           */
          addHelpOption(option) {
            this._helpOption = option;
            return this;
          }
          /**
           * Output help information and exit.
           *
           * Outputs built-in help, and custom text added using `.addHelpText()`.
           *
           * @param {{ error: boolean }} [contextOptions] - pass {error:true} to write to stderr instead of stdout
           */
          help(contextOptions) {
            this.outputHelp(contextOptions);
            let exitCode = process2.exitCode || 0;
            if (exitCode === 0 && contextOptions && typeof contextOptions !== "function" && contextOptions.error) {
              exitCode = 1;
            }
            this._exit(exitCode, "commander.help", "(outputHelp)");
          }
          /**
           * Add additional text to be displayed with the built-in help.
           *
           * Position is 'before' or 'after' to affect just this command,
           * and 'beforeAll' or 'afterAll' to affect this command and all its subcommands.
           *
           * @param {string} position - before or after built-in help
           * @param {(string | Function)} text - string to add, or a function returning a string
           * @return {Command} `this` command for chaining
           */
          addHelpText(position, text) {
            const allowedValues = ["beforeAll", "before", "after", "afterAll"];
            if (!allowedValues.includes(position)) {
              throw new Error(`Unexpected value for position to addHelpText.
Expecting one of '${allowedValues.join("', '")}'`);
            }
            const helpEvent = `${position}Help`;
            this.on(helpEvent, (context) => {
              let helpStr;
              if (typeof text === "function") {
                helpStr = text({ error: context.error, command: context.command });
              } else {
                helpStr = text;
              }
              if (helpStr) {
                context.write(`${helpStr}
`);
              }
            });
            return this;
          }
          /**
           * Output help information if help flags specified
           *
           * @param {Array} args - array of options to search for help flags
           * @private
           */
          _outputHelpIfRequested(args) {
            const helpOption = this._getHelpOption();
            const helpRequested = helpOption && args.find((arg) => helpOption.is(arg));
            if (helpRequested) {
              this.outputHelp();
              this._exit(0, "commander.helpDisplayed", "(outputHelp)");
            }
          }
        };
        function incrementNodeInspectorPort(args) {
          return args.map((arg) => {
            if (!arg.startsWith("--inspect")) {
              return arg;
            }
            let debugOption;
            let debugHost = "127.0.0.1";
            let debugPort = "9229";
            let match;
            if ((match = arg.match(/^(--inspect(-brk)?)$/)) !== null) {
              debugOption = match[1];
            } else if ((match = arg.match(/^(--inspect(-brk|-port)?)=([^:]+)$/)) !== null) {
              debugOption = match[1];
              if (/^\d+$/.test(match[3])) {
                debugPort = match[3];
              } else {
                debugHost = match[3];
              }
            } else if ((match = arg.match(/^(--inspect(-brk|-port)?)=([^:]+):(\d+)$/)) !== null) {
              debugOption = match[1];
              debugHost = match[3];
              debugPort = match[4];
            }
            if (debugOption && debugPort !== "0") {
              return `${debugOption}=${debugHost}:${parseInt(debugPort) + 1}`;
            }
            return arg;
          });
        }
        exports2.Command = Command2;
      }
    });
    var require_commander = __commonJS2({
      "../../node_modules/.pnpm/commander@12.1.0/node_modules/commander/index.js"(exports2) {
        "use strict";
        var { Argument: Argument2 } = require_argument();
        var { Command: Command2 } = require_command();
        var { CommanderError: CommanderError2, InvalidArgumentError: InvalidArgumentError2 } = require_error();
        var { Help: Help2 } = require_help();
        var { Option: Option2 } = require_option();
        exports2.program = new Command2();
        exports2.createCommand = (name) => new Command2(name);
        exports2.createOption = (flags, description) => new Option2(flags, description);
        exports2.createArgument = (name, description) => new Argument2(name, description);
        exports2.Command = Command2;
        exports2.Option = Option2;
        exports2.Argument = Argument2;
        exports2.Help = Help2;
        exports2.CommanderError = CommanderError2;
        exports2.InvalidArgumentError = InvalidArgumentError2;
        exports2.InvalidOptionArgumentError = InvalidArgumentError2;
      }
    });
    var index_exports = {};
    __export(index_exports, {
      createMovcliProgram: () => createMovcliProgram,
      main: () => main2,
      normalizeMovcliArgv: () => normalizeMovcliArgv,
      runMovcli: () => runMovcli2
    });
    module.exports = __toCommonJS(index_exports);
    var import_index = __toESM2(require_commander(), 1);
    var {
      program,
      createCommand,
      createArgument,
      createOption,
      CommanderError,
      InvalidArgumentError,
      InvalidOptionArgumentError,
      // deprecated old name
      Command,
      Argument,
      Option,
      Help
    } = import_index.default;
    var import_node_path11 = __require("path");
    var import_node_fs4 = __require("fs");
    var import_node_path42 = __require("path");
    var BackendHTTPError2 = class _BackendHTTPError2 extends Error {
      constructor(method, path, status, body, rawBody) {
        super(`Backend ${method} ${path} failed: HTTP ${status} ${backendErrorMessage2(body, rawBody)}`);
        this.method = method;
        this.path = path;
        this.status = status;
        this.body = body;
        this.name = "BackendHTTPError";
      }
      method;
      path;
      status;
      body;
      static async fromResponse(method, path, res) {
        const rawBody = await res.text();
        return new _BackendHTTPError2(method, path, res.status, parseJSONBody2(rawBody), rawBody);
      }
      toJSON() {
        return normalizeBackendHTTPError2(this.method, this.path, this.status, this.body);
      }
    };
    function normalizeBackendHTTPError2(method, path, status, body) {
      const bodyRecord = isRecord30(body) ? body : void 0;
      return {
        type: "backend_http_error",
        method,
        path,
        status,
        ...bodyRecord ? { body: bodyRecord } : {},
        ...bodyRecord && typeof bodyRecord.code === "string" ? { code: bodyRecord.code } : {},
        ...bodyRecord && typeof bodyRecord.field === "string" ? { field: bodyRecord.field } : {},
        ...bodyRecord && Array.isArray(bodyRecord.allowed_values) ? { allowed_values: bodyRecord.allowed_values } : {},
        ...bodyRecord && isRecord30(bodyRecord.suggested_fix) ? { suggested_fix: bodyRecord.suggested_fix } : {},
        ...bodyRecord && Number.isInteger(bodyRecord.required_min) ? { required_min: bodyRecord.required_min } : {},
        ...bodyRecord && Number.isInteger(bodyRecord.allowed_max) ? { allowed_max: bodyRecord.allowed_max } : {},
        ...bodyRecord && Number.isInteger(bodyRecord.actual_count) ? { actual_count: bodyRecord.actual_count } : {},
        ...bodyRecord && isRecord30(bodyRecord.details) ? { details: bodyRecord.details } : {}
      };
    }
    function parseJSONBody2(rawBody) {
      if (!rawBody.trim()) return void 0;
      try {
        return JSON.parse(rawBody);
      } catch {
        return rawBody;
      }
    }
    function backendErrorMessage2(body, rawBody) {
      if (isRecord30(body) && typeof body.error === "string") return body.error;
      return rawBody;
    }
    function isRecord30(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    var import_node_fs2 = __require("fs");
    var import_node_path22 = __require("path");
    var import_node_fs3 = __require("fs");
    var import_node_os3 = __require("os");
    var import_node_path5 = __require("path");
    var import_node_crypto2 = __require("crypto");
    var MOVSCRIPT_WORKSPACE_MANIFEST_FILE_NAME3 = "manifest.json";
    var MOVSCRIPT_WORKSPACE_CONFIG_TOML_FILE_NAME2 = "config.toml";
    var MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA3 = "movscript.project-workspace.v1";
    var MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME3 = "providers";
    var MOVSCRIPT_WORKSPACE_BACKEND_DIR_NAME2 = "backend";
    var MOVSCRIPT_WORKSPACE_BIN_DIR_NAME2 = "bin";
    var MOVSCRIPT_WORKSPACE_REALMS_DIR_NAME2 = "realms";
    function resolveMovScriptWorkspaceRootPaths2(workspaceDir2) {
      const rootDir = resolveMovScriptHomeDir3(workspaceDir2);
      const controlDir = rootDir;
      const realmsDir = (0, import_node_path5.join)(controlDir, MOVSCRIPT_WORKSPACE_REALMS_DIR_NAME2);
      return {
        workspaceDir: rootDir,
        rootDir,
        controlDir,
        configTomlPath: (0, import_node_path5.join)(controlDir, MOVSCRIPT_WORKSPACE_CONFIG_TOML_FILE_NAME2),
        manifestPath: (0, import_node_path5.join)(controlDir, MOVSCRIPT_WORKSPACE_MANIFEST_FILE_NAME3),
        realmsDir,
        providersDir: (0, import_node_path5.join)(realmsDir, "local", MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME3),
        backendDir: (0, import_node_path5.join)(controlDir, MOVSCRIPT_WORKSPACE_BACKEND_DIR_NAME2),
        binDir: (0, import_node_path5.join)(controlDir, MOVSCRIPT_WORKSPACE_BIN_DIR_NAME2)
      };
    }
    function resolveMovScriptHomeDir3(workspaceDir2) {
      const input3 = workspaceDir2?.trim();
      if (input3) return (0, import_node_path5.resolve)(input3);
      const explicit = process.env.MOVSCRIPT_HOME?.trim();
      if (explicit) return (0, import_node_path5.resolve)(explicit);
      const legacy = process.env.MOVSCRIPT_WORKSPACE_DIR?.trim();
      if (legacy) return (0, import_node_path5.resolve)(legacy);
      return process.cwd();
    }
    function ensureMovScriptWorkspaceRoot2(paths) {
      (0, import_node_fs3.mkdirSync)(paths.controlDir, { recursive: true });
      (0, import_node_fs3.mkdirSync)(paths.rootDir, { recursive: true });
      (0, import_node_fs3.mkdirSync)(paths.realmsDir, { recursive: true });
      (0, import_node_fs3.mkdirSync)(paths.providersDir, { recursive: true });
      (0, import_node_fs3.mkdirSync)(paths.backendDir, { recursive: true });
      (0, import_node_fs3.mkdirSync)(paths.binDir, { recursive: true });
      const current = readMovScriptWorkspaceRootManifest2(paths.manifestPath);
      if (current) return current;
      const manifest = defaultMovScriptWorkspaceRootManifest2();
      writeMovScriptWorkspaceRootManifest2(paths.manifestPath, manifest);
      return manifest;
    }
    function defaultMovScriptWorkspaceRootManifest2(now = /* @__PURE__ */ new Date()) {
      const timestamp = now.toISOString();
      return {
        schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA3,
        workspaceId: (0, import_node_crypto2.randomUUID)(),
        createdAt: timestamp,
        updatedAt: timestamp,
        layout: {
          providerConfigRoot: MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME3
        }
      };
    }
    function readMovScriptWorkspaceRootManifest2(manifestPath) {
      const parsed = readJSON5(manifestPath);
      if (!isRecord210(parsed) || parsed.schema !== MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA3) return void 0;
      const workspaceId = stringField19(parsed.workspaceId);
      if (!workspaceId) return void 0;
      const backend = normalizeBackend2(parsed.backend);
      const activeRealm = normalizeRealmRecord2(parsed.activeRealm);
      const activeUserId = numberField10(parsed.activeUserId);
      return {
        schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA3,
        workspaceId,
        createdAt: stringField19(parsed.createdAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
        updatedAt: stringField19(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
        ...backend ? { backend } : {},
        ...activeRealm ? { activeRealm } : {},
        ...activeUserId !== void 0 ? { activeUserId } : {},
        layout: normalizeWorkspaceLayout2(parsed.layout)
      };
    }
    function writeMovScriptWorkspaceRootManifest2(manifestPath, manifest) {
      writeJSONAtomic4(manifestPath, {
        ...manifest,
        schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA3,
        updatedAt: manifest.updatedAt || (/* @__PURE__ */ new Date()).toISOString(),
        layout: normalizeWorkspaceLayout2(manifest.layout)
      });
    }
    function normalizeWorkspaceLayout2(_value) {
      return {
        providerConfigRoot: MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME3
      };
    }
    function normalizeBackend2(value) {
      if (!isRecord210(value)) return void 0;
      const kind = value.kind === "local" || value.kind === "cloud" || value.kind === "custom" ? value.kind : void 0;
      const baseURL = stringField19(value.baseURL);
      return kind || baseURL ? { ...kind ? { kind } : {}, ...baseURL ? { baseURL } : {} } : void 0;
    }
    function normalizeRealmRecord2(value) {
      if (!isRecord210(value)) return void 0;
      const kind = value.kind === "local" || value.kind === "cloud" ? value.kind : void 0;
      if (!kind) return void 0;
      if (kind === "local") return { kind, id: "local" };
      const id = stringField19(value.id);
      return id ? { kind, id: safeIdSegment2(id) } : void 0;
    }
    function safeIdSegment2(value) {
      const raw = String(value).trim();
      if (!/^[a-zA-Z0-9._-]{1,128}$/.test(raw) || raw === "." || raw === "..") {
        throw new Error(`invalid MovScript workspace id segment: ${String(value)}`);
      }
      return raw;
    }
    function readJSON5(filePath) {
      try {
        return JSON.parse((0, import_node_fs3.readFileSync)(filePath, "utf8"));
      } catch (error) {
        if (isNotFoundError5(error)) return void 0;
        throw error;
      }
    }
    function writeJSONAtomic4(filePath, value) {
      (0, import_node_fs3.mkdirSync)((0, import_node_path5.dirname)(filePath), { recursive: true });
      const tmpPath = `${filePath}.${process.pid}.${Date.now()}.tmp`;
      (0, import_node_fs3.writeFileSync)(tmpPath, `${JSON.stringify(value, null, 2)}
`, "utf8");
      (0, import_node_fs3.renameSync)(tmpPath, filePath);
    }
    function stringField19(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberField10(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function isRecord210(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function isNotFoundError5(error) {
      return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
    }
    var MOVSCRIPT_BACKEND_CONFIG_SCHEMA2 = "movscript.backend-config.v1";
    var MOVSCRIPT_BACKEND_AUTH_SCHEMA2 = "movscript.backend-auth.v1";
    var MOVSCRIPT_BACKEND_CONFIG_FILE_NAME2 = "config.json";
    var MOVSCRIPT_BACKEND_AUTH_FILE_NAME2 = "auth.json";
    var MOVSCRIPT_DEFAULT_BACKEND_BASE_URL2 = "http://localhost:8766";
    function resolveMovScriptBackendPaths2(workspaceDir2 = process.cwd(), realm) {
      const root = resolveMovScriptWorkspaceRootPaths2(workspaceDir2);
      const backendRealmsDir = (0, import_node_path22.join)(root.backendDir, "realms");
      const authRealm = normalizeRealm2(realm) ?? { kind: "local", id: "local" };
      return {
        workspaceDir: root.workspaceDir,
        controlDir: root.controlDir,
        backendDir: root.backendDir,
        backendRealmsDir,
        configPath: (0, import_node_path22.join)(root.backendDir, MOVSCRIPT_BACKEND_CONFIG_FILE_NAME2),
        authPath: (0, import_node_path22.join)(backendRealmsDir, ...backendRealmPath2(authRealm), MOVSCRIPT_BACKEND_AUTH_FILE_NAME2)
      };
    }
    function defaultMovScriptBackendConfig2(baseURL = MOVSCRIPT_DEFAULT_BACKEND_BASE_URL2) {
      return {
        schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA2,
        baseURL: normalizeBackendBaseURL2(baseURL),
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    function readMovScriptBackendConfig2(workspaceDir2 = process.cwd()) {
      const paths = resolveMovScriptBackendPaths2(workspaceDir2);
      const parsed = readJSON22(paths.configPath);
      if (!isRecord35(parsed) || parsed.schema !== MOVSCRIPT_BACKEND_CONFIG_SCHEMA2) return defaultMovScriptBackendConfig2();
      const baseURL = stringField28(parsed.baseURL) ?? MOVSCRIPT_DEFAULT_BACKEND_BASE_URL2;
      const realm = normalizeRealm2(parsed.realm);
      const activeUserId = idField14(parsed.activeUserId);
      return {
        schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA2,
        baseURL: normalizeBackendBaseURL2(baseURL),
        ...realm ? { realm } : {},
        ...activeUserId !== void 0 ? { activeUserId } : {},
        updatedAt: stringField28(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    function writeMovScriptBackendConfig2(workspaceDir2, config) {
      const paths = resolveMovScriptBackendPaths2(workspaceDir2);
      ensureMovScriptWorkspaceRoot2(resolveMovScriptWorkspaceRootPaths2(paths.workspaceDir));
      const next = {
        schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA2,
        baseURL: normalizeBackendBaseURL2(config.baseURL),
        ...normalizeRealm2(config.realm) ? { realm: normalizeRealm2(config.realm) } : {},
        ...config.activeUserId !== void 0 ? { activeUserId: config.activeUserId } : {},
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      writeJSONAtomic22(paths.configPath, next);
      return next;
    }
    function readMovScriptBackendAuth2(workspaceDir2 = process.cwd(), realmInput) {
      const config = readMovScriptBackendConfig2(workspaceDir2);
      const requestedRealm = normalizeRealm2(realmInput) ?? config.realm;
      const paths = resolveMovScriptBackendPaths2(workspaceDir2, requestedRealm);
      const parsed = readJSON22(paths.authPath);
      if (!isRecord35(parsed) || parsed.schema !== MOVSCRIPT_BACKEND_AUTH_SCHEMA2) return void 0;
      const token = stringField28(parsed.token);
      if (!token) return void 0;
      const tokenType = parsed.tokenType === "Bearer" ? "Bearer" : void 0;
      if (!tokenType) return void 0;
      const user = normalizeAuthUser2(parsed.user);
      const gitCredential = normalizeGitCredential2(parsed.gitCredential ?? parsed.git_credential);
      const realm = normalizeRealm2(parsed.realm) ?? requestedRealm;
      return {
        schema: MOVSCRIPT_BACKEND_AUTH_SCHEMA2,
        tokenType,
        token,
        ...realm ? { realm } : {},
        ...gitCredential ? { gitCredential } : {},
        ...user ? { user } : {},
        ...stringField28(parsed.expiresAt) ? { expiresAt: stringField28(parsed.expiresAt) } : {},
        updatedAt: stringField28(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    function writeMovScriptBackendAuth2(workspaceDir2, auth) {
      const realm = normalizeRealm2(auth.realm) ?? readMovScriptBackendConfig2(workspaceDir2).realm;
      const paths = resolveMovScriptBackendPaths2(workspaceDir2, realm);
      ensureMovScriptWorkspaceRoot2(resolveMovScriptWorkspaceRootPaths2(paths.workspaceDir));
      const next = {
        schema: MOVSCRIPT_BACKEND_AUTH_SCHEMA2,
        tokenType: "Bearer",
        token: auth.token,
        ...realm ? { realm } : {},
        ...normalizeGitCredential2(auth.gitCredential) ? { gitCredential: normalizeGitCredential2(auth.gitCredential) } : {},
        ...normalizeAuthUser2(auth.user) ? { user: normalizeAuthUser2(auth.user) } : {},
        ...auth.expiresAt ? { expiresAt: auth.expiresAt } : {},
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      writeJSONAtomic22(paths.authPath, next, 384);
      return next;
    }
    function clearMovScriptBackendAuth2(workspaceDir2 = process.cwd()) {
      const paths = resolveMovScriptBackendPaths2(workspaceDir2, readMovScriptBackendConfig2(workspaceDir2).realm);
      (0, import_node_fs2.rmSync)(paths.authPath, { force: true });
    }
    function resolveMovScriptBackendSession2(input3 = {}) {
      const root = resolveMovScriptBackendPaths2(input3.workspaceDir);
      const config = readMovScriptBackendConfig2(root.workspaceDir);
      const realm = normalizeRealm2(input3.realm) ?? config.realm;
      const paths = resolveMovScriptBackendPaths2(root.workspaceDir, realm);
      const auth = readMovScriptBackendAuth2(paths.workspaceDir, realm);
      const token = stringField28(input3.token) ?? stringField28(process.env.MOVCLI_TOKEN) ?? auth?.token;
      const userId = idField14(input3.userId) ?? idField14(auth?.user?.id) ?? idField14(config.activeUserId);
      const baseURL = normalizeBackendBaseURL2(stringField28(input3.server) ?? stringField28(process.env.MOVSCRIPT_API_BASE_URL) ?? config.baseURL);
      return {
        workspaceDir: paths.workspaceDir,
        baseURL,
        apiBaseURL: normalizeBackendAPIBaseURL2(baseURL),
        ...token ? { token } : {},
        tokenType: "Bearer",
        ...userId !== void 0 ? { userId: String(userId) } : {},
        ...auth?.user ? { user: auth.user } : {},
        configPath: paths.configPath,
        authPath: paths.authPath
      };
    }
    function backendRealmPath2(realm) {
      if (realm.kind === "local") return ["local"];
      return ["cloud", String(realm.id)];
    }
    function normalizeBackendBaseURL2(value) {
      const trimmed = value.trim().replace(/\/+$/, "");
      const withoutAPI = trimmed.endsWith("/api/v1") ? trimmed.slice(0, -"/api/v1".length) : trimmed;
      return withoutAPI || MOVSCRIPT_DEFAULT_BACKEND_BASE_URL2;
    }
    function normalizeBackendAPIBaseURL2(value) {
      return `${normalizeBackendBaseURL2(value)}/api/v1`;
    }
    async function loginMovScriptBackend2(input3) {
      const session = resolveMovScriptBackendSession2({ workspaceDir: input3.workspaceDir, server: input3.server });
      const response = await backendPost(session, "/auth/login", {
        username: input3.username,
        password: input3.password
      });
      const token = stringField28(response.token);
      if (!token) throw new Error("auth login response did not include token");
      return {
        baseURL: session.baseURL,
        token,
        tokenType: "Bearer",
        ...stringField28(response.expires_at) ? { expiresAt: stringField28(response.expires_at) } : {},
        ...isRecord35(response.user) ? { user: response.user } : {},
        ...normalizeGitCredential2(response.git_credential) ? { gitCredential: normalizeGitCredential2(response.git_credential) } : {}
      };
    }
    async function getMovScriptBackendMe2(input3 = {}) {
      const session = resolveMovScriptBackendSession2(input3);
      return asRecord2(await backendGet(session, "/auth/me"), "auth me response");
    }
    async function backendGet(session, path) {
      const res = await fetch(`${session.apiBaseURL}${path}`, { headers: backendHeaders(session) });
      if (!res.ok) throw new Error(await responseError("GET", path, res));
      return res.json();
    }
    async function backendPost(session, path, body) {
      const res = await fetch(`${session.apiBaseURL}${path}`, {
        method: "POST",
        headers: backendHeaders(session, true),
        body: JSON.stringify(body)
      });
      if (!res.ok) throw new Error(await responseError("POST", path, res));
      return asRecord2(await res.json(), `${path} response`);
    }
    function backendHeaders(session, json = false) {
      const headers = json ? { "Content-Type": "application/json" } : {};
      if (session.token) headers.Authorization = `Bearer ${session.token}`;
      if (session.userId) headers["X-User-ID"] = session.userId;
      return headers;
    }
    async function responseError(method, path, res) {
      const text = await res.text();
      return `${method} ${path} failed with ${res.status}${text.trim() ? `: ${text.trim()}` : ""}`;
    }
    function readJSON22(filePath) {
      try {
        return JSON.parse((0, import_node_fs2.readFileSync)(filePath, "utf8"));
      } catch (error) {
        if (isNotFoundError22(error)) return void 0;
        throw error;
      }
    }
    function writeJSONAtomic22(filePath, value, mode) {
      (0, import_node_fs2.mkdirSync)((0, import_node_path22.dirname)(filePath), { recursive: true });
      const tmpPath = `${filePath}.${process.pid}.${Date.now()}.tmp`;
      (0, import_node_fs2.writeFileSync)(tmpPath, `${JSON.stringify(value, null, 2)}
`, { encoding: "utf8", mode });
      (0, import_node_fs2.renameSync)(tmpPath, filePath);
    }
    function normalizeAuthUser2(value) {
      if (!isRecord35(value)) return void 0;
      const id = idField14(value.id);
      const username = stringField28(value.username);
      const displayName22 = stringField28(value.displayName) ?? stringField28(value.display_name);
      const primaryEmail = stringField28(value.primaryEmail) ?? stringField28(value.primary_email);
      const locale = stringField28(value.locale);
      const systemRole = stringField28(value.systemRole) ?? stringField28(value.system_role);
      const user = {
        ...id !== void 0 ? { id } : {},
        ...username ? { username } : {},
        ...displayName22 ? { displayName: displayName22 } : {},
        ...primaryEmail ? { primaryEmail } : {},
        ...locale ? { locale } : {},
        ...systemRole ? { systemRole } : {}
      };
      return Object.keys(user).length > 0 ? user : void 0;
    }
    function normalizeGitCredential2(value) {
      if (!isRecord35(value)) return void 0;
      const provider = stringField28(value.provider);
      const username = stringField28(value.username);
      if (provider !== "gitea" || !username) return void 0;
      const token = stringField28(value.token);
      const maskedToken = stringField28(value.maskedToken) ?? stringField28(value.masked_token);
      const status = stringField28(value.status);
      const lastError = stringField28(value.lastError) ?? stringField28(value.last_error);
      return {
        provider,
        username,
        ...token ? { token } : {},
        ...maskedToken ? { maskedToken } : {},
        ...status ? { status } : {},
        ...lastError ? { lastError } : {}
      };
    }
    function normalizeRealm2(value) {
      if (!isRecord35(value)) return void 0;
      const kind = value.kind === "local" || value.kind === "cloud" ? value.kind : void 0;
      if (!kind) return void 0;
      if (kind === "local") return { kind, id: "local" };
      const id = idField14(value.id);
      return id ? { kind, id: String(id) } : void 0;
    }
    function stringField28(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function idField14(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function isRecord35(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function asRecord2(value, label) {
      if (!isRecord35(value)) throw new Error(`${label} must be a JSON object`);
      return value;
    }
    function isNotFoundError22(error) {
      return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
    }
    var backendAPIBaseURL2 = normalizeBackendAPIBaseURL2(defaultMovScriptBackendBaseURL2());
    var backendRuntimeAuthToken2 = "";
    var configuredBackendDefaultWorkspaceDir2;
    function setMovScriptBackendAPIBaseURL2(next) {
      backendAPIBaseURL2 = normalizeBackendAPIBaseURL2(next);
    }
    function getMovScriptBackendAPIBaseURL2() {
      return backendAPIBaseURL2;
    }
    function setMovScriptBackendRuntimeAuthToken2(token) {
      backendRuntimeAuthToken2 = token?.trim() ?? "";
    }
    function getMovScriptBackendRuntimeAuthToken2() {
      return backendRuntimeAuthToken2;
    }
    function setMovScriptBackendDefaultWorkspaceDir2(workspaceDir2) {
      configuredBackendDefaultWorkspaceDir2 = workspaceDir2?.trim() || void 0;
    }
    function resolveMovScriptBackendDefaultWorkspaceDir2() {
      return configuredBackendDefaultWorkspaceDir2 || process.env.MOVSCRIPT_HOME || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd();
    }
    function defaultMovScriptBackendBaseURL2() {
      return typeof process !== "undefined" ? process.env.MOVSCRIPT_API_BASE_URL || MOVSCRIPT_DEFAULT_BACKEND_BASE_URL2 : MOVSCRIPT_DEFAULT_BACKEND_BASE_URL2;
    }
    async function backendGet22(path) {
      const headers = backendHeaders22();
      const res = await fetch(`${getMovScriptBackendAPIBaseURL2()}${path}`, { headers });
      if (!res.ok) {
        throw await BackendHTTPError2.fromResponse("GET", path, res);
      }
      return res.json();
    }
    async function backendPost22(path, body, userId) {
      const headers = backendHeaders22({ json: true, userId });
      const res = await fetch(`${getMovScriptBackendAPIBaseURL2()}${path}`, {
        method: "POST",
        headers,
        body: JSON.stringify(body)
      });
      if (!res.ok) {
        throw await BackendHTTPError2.fromResponse("POST", path, res);
      }
      return res.json();
    }
    async function backendPut2(path, body, userId) {
      const headers = backendHeaders22({ json: true, userId });
      const res = await fetch(`${getMovScriptBackendAPIBaseURL2()}${path}`, {
        method: "PUT",
        headers,
        body: JSON.stringify(body)
      });
      if (!res.ok) {
        throw await BackendHTTPError2.fromResponse("PUT", path, res);
      }
      const text = await res.text();
      return text.trim() ? JSON.parse(text) : null;
    }
    async function backendPatch2(path, body, userId) {
      const headers = backendHeaders22({ json: true, userId });
      const res = await fetch(`${getMovScriptBackendAPIBaseURL2()}${path}`, {
        method: "PATCH",
        headers,
        body: JSON.stringify(body)
      });
      if (!res.ok) {
        throw await BackendHTTPError2.fromResponse("PATCH", path, res);
      }
      const text = await res.text();
      return text.trim() ? JSON.parse(text) : null;
    }
    async function backendDelete2(path, userId) {
      const headers = backendHeaders22({ userId });
      const res = await fetch(`${getMovScriptBackendAPIBaseURL2()}${path}`, {
        method: "DELETE",
        headers
      });
      if (!res.ok) {
        throw await BackendHTTPError2.fromResponse("DELETE", path, res);
      }
      const text = await res.text();
      return text.trim() ? JSON.parse(text) : null;
    }
    function backendHeaders22(input3 = {}) {
      const headers = input3.json ? { "Content-Type": "application/json" } : {};
      const authToken = getMovScriptBackendRuntimeAuthToken2() || workspaceBackendAuthToken2();
      if (authToken) headers.Authorization = `Bearer ${authToken}`;
      const userId = typeof input3.userId === "number" || typeof input3.userId === "string" ? String(input3.userId) : workspaceBackendUserId2();
      if (userId) headers["X-User-ID"] = userId;
      return headers;
    }
    function workspaceBackendAuthToken2() {
      try {
        return readMovScriptBackendAuth2(resolveMovScriptBackendDefaultWorkspaceDir2())?.token ?? "";
      } catch {
        return "";
      }
    }
    function workspaceBackendUserId2() {
      try {
        return resolveMovScriptBackendSession2({ workspaceDir: resolveMovScriptBackendDefaultWorkspaceDir2() }).userId ?? "";
      } catch {
        return "";
      }
    }
    var import_node_fs32 = __require("fs");
    var import_node_os22 = __require("os");
    var import_node_path32 = __require("path");
    var MOVSCRIPT_DEFAULT_HOME_DIR_NAME2 = ".movscript";
    var MOVSCRIPT_RUNTIME_DIR_NAME2 = "runtime";
    var MOVSCRIPT_RUNTIME_APPS_DIR_NAME2 = "apps";
    var MOVSCRIPT_RUNTIME_SERVICES_DIR_NAME2 = "services";
    var MOVSCRIPT_RUNTIME_ENDPOINTS_DIR_NAME2 = "endpoints";
    function resolveMovScriptHomeDir22(options = {}) {
      const env = options.env ?? process.env;
      const explicit = stringValue14(env.MOVSCRIPT_HOME);
      if (explicit) return (0, import_node_path32.resolve)(explicit);
      const legacy = stringValue14(env.MOVSCRIPT_WORKSPACE_DIR);
      if (legacy) return (0, import_node_path32.resolve)(legacy);
      return (0, import_node_path32.resolve)(options.userHomeDir ?? (0, import_node_os22.homedir)(), MOVSCRIPT_DEFAULT_HOME_DIR_NAME2);
    }
    function resolveRuntimeHomePaths2(homeDir = resolveMovScriptHomeDir22()) {
      const runtimeDir = (0, import_node_path32.join)(homeDir, MOVSCRIPT_RUNTIME_DIR_NAME2);
      return {
        homeDir,
        runtimeDir,
        appsDir: (0, import_node_path32.join)(runtimeDir, MOVSCRIPT_RUNTIME_APPS_DIR_NAME2),
        servicesDir: (0, import_node_path32.join)(runtimeDir, MOVSCRIPT_RUNTIME_SERVICES_DIR_NAME2),
        endpointsDir: (0, import_node_path32.join)(runtimeDir, MOVSCRIPT_RUNTIME_ENDPOINTS_DIR_NAME2)
      };
    }
    function readRuntimeHomeSnapshot2(homeDir = resolveMovScriptHomeDir22()) {
      const paths = resolveRuntimeHomePaths2(homeDir);
      return {
        homeDir: paths.homeDir,
        apps: readAppRecords2(paths.appsDir),
        services: readServiceRecords2(paths.servicesDir),
        endpoints: readEndpointRecords2(paths.endpointsDir)
      };
    }
    function activeAppRecords2(snapshot) {
      return snapshot.apps.filter((record) => record.ready && pidIsAlive2(record.pid));
    }
    function activeServiceRecords2(snapshot) {
      return snapshot.services.filter((record) => record.ready && pidIsAlive2(record.pid));
    }
    function activeEndpointRecords2(snapshot) {
      return snapshot.endpoints.filter((record) => record.ready && pidIsAlive2(record.pid));
    }
    function findRuntimeApp2(snapshot, applicationId) {
      return activeAppRecords2(snapshot).find((record) => record.applicationId === applicationId);
    }
    function findRuntimeService2(snapshot, serviceName) {
      return activeServiceRecords2(snapshot).find((record) => record.serviceName === serviceName);
    }
    function findRuntimeEndpoint2(snapshot, serviceName) {
      return activeEndpointRecords2(snapshot).find((record) => record.serviceName === serviceName);
    }
    function pidIsAlive2(pid) {
      if (pid === void 0) return true;
      if (!Number.isInteger(pid) || pid <= 0) return false;
      try {
        process.kill(pid, 0);
        return true;
      } catch {
        return false;
      }
    }
    function readAppRecords2(appsDir) {
      return readJSONFiles2(appsDir).flatMap(({ path, value }) => {
        const raw = asRecord22(value);
        if (!raw) return [];
        const applicationId = stringValue14(raw.applicationId ?? raw.appId ?? raw.id) ?? fileStem2(path);
        if (!applicationId) return [];
        const endpoint = endpointRecordFrom2(raw.endpoint, path, { applicationId });
        return [{
          applicationId,
          owner: stringValue14(raw.owner ?? raw.runtimeOwner),
          profile: stringValue14(raw.profile),
          pid: numberValue8(raw.pid),
          status: statusValue2(raw.status),
          ready: readyValue2(raw),
          ...endpoint ? { endpoint } : {},
          updatedAt: stringValue14(raw.updatedAt ?? raw.startedAt ?? raw.createdAt),
          raw,
          path
        }];
      });
    }
    function readServiceRecords2(servicesDir) {
      if (!(0, import_node_fs32.existsSync)(servicesDir)) return [];
      const records = [];
      for (const serviceName of safeReaddir2(servicesDir)) {
        const serviceDir = (0, import_node_path32.join)(servicesDir, serviceName);
        for (const { path, value } of readJSONFiles2(serviceDir)) {
          const raw = asRecord22(value);
          if (!raw) continue;
          const instanceId = stringValue14(raw.instanceId ?? raw.id) ?? fileStem2(path);
          if (!instanceId) continue;
          const endpoint = endpointRecordFrom2(raw.endpoint, path, { serviceName, instanceId });
          records.push({
            serviceName: stringValue14(raw.serviceName) ?? serviceName,
            instanceId,
            ownerApplicationId: stringValue14(raw.ownerApplicationId ?? raw.applicationId ?? raw.appId),
            profile: stringValue14(raw.profile),
            pid: numberValue8(raw.pid),
            status: statusValue2(raw.status),
            ready: readyValue2(raw),
            ...endpoint ? { endpoint } : {},
            updatedAt: stringValue14(raw.updatedAt ?? raw.startedAt ?? raw.createdAt),
            raw,
            path
          });
        }
      }
      return records;
    }
    function readEndpointRecords2(endpointsDir) {
      return readJSONFiles2(endpointsDir).flatMap(({ path, value }) => {
        const record = endpointRecordFrom2(value, path, { serviceName: fileStem2(path) });
        return record ? [record] : [];
      });
    }
    function endpointRecordFrom2(value, path, defaults) {
      const raw = asRecord22(value);
      if (!raw) return void 0;
      const serviceName = stringValue14(raw.serviceName ?? raw.service ?? defaults.serviceName);
      const applicationId = stringValue14(raw.applicationId ?? raw.appId ?? defaults.applicationId);
      const instanceId = stringValue14(raw.instanceId ?? raw.id ?? defaults.instanceId);
      const baseURL = stringValue14(raw.baseURL ?? raw.baseUrl ?? raw.url);
      const url = stringValue14(raw.url ?? raw.endpointURL ?? raw.endpointUrl ?? raw.mcpURL ?? raw.mcpUrl);
      const healthURL = stringValue14(raw.healthURL ?? raw.healthUrl ?? raw.healthEndpoint);
      const port = numberValue8(raw.port);
      const pid = numberValue8(raw.pid);
      const protocol = stringValue14(raw.protocol);
      if (!serviceName && !applicationId && !baseURL && !url && !healthURL && !port && !pid) return void 0;
      return {
        ...serviceName ? { serviceName } : {},
        ...applicationId ? { applicationId } : {},
        ...instanceId ? { instanceId } : {},
        ...baseURL ? { baseURL } : {},
        ...url ? { url } : {},
        ...healthURL ? { healthURL } : {},
        ...protocol ? { protocol } : {},
        ...pid !== void 0 ? { pid } : {},
        ...port !== void 0 ? { port } : {},
        status: statusValue2(raw.status),
        ready: readyValue2(raw),
        updatedAt: stringValue14(raw.updatedAt ?? raw.startedAt ?? raw.createdAt),
        raw,
        path
      };
    }
    function readJSONFiles2(dir) {
      if (!(0, import_node_fs32.existsSync)(dir)) return [];
      return safeReaddir2(dir).filter((name) => name.endsWith(".json")).map((name) => {
        const path = (0, import_node_path32.join)(dir, name);
        return { path, value: readJSON32(path) };
      });
    }
    function readJSON32(path) {
      try {
        return JSON.parse((0, import_node_fs32.readFileSync)(path, "utf8"));
      } catch {
        return void 0;
      }
    }
    function safeReaddir2(dir) {
      try {
        return (0, import_node_fs32.readdirSync)(dir);
      } catch {
        return [];
      }
    }
    function fileStem2(path) {
      const name = path.split(/[\\/]/).pop();
      if (!name) return void 0;
      return name.endsWith(".json") ? name.slice(0, -5) : name;
    }
    function statusValue2(value) {
      if (value === "starting" || value === "ready" || value === "stopping" || value === "stopped" || value === "error") return value;
      return "unknown";
    }
    function readyValue2(raw) {
      if (typeof raw.ready === "boolean") return raw.ready;
      const status = statusValue2(raw.status);
      return status === "ready" || status === "unknown";
    }
    function stringValue14(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberValue8(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function asRecord22(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value) ? value : void 0;
    }
    var LOCAL_NODE_GATEWAY_SERVICE5 = "movscript.local-node.gateway";
    var DATA_SERVICE3 = "movscript.data.service";
    var adminCommandSpecs2 = [
      {
        commandId: "admin.provider_template.list",
        mcpToolName: "admin_provider_template_list",
        cliPath: ["provider-template", "list"],
        description: "Admin-only: list provider templates that can be used to create provider accounts.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: (args) => adminPathWithQuery2("/admin/provider-templates", args)
      },
      {
        commandId: "admin.provider.list",
        mcpToolName: "admin_provider_list",
        cliPath: ["provider", "list"],
        description: "Admin-only: list configured AI providers and their public configuration state.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: (args) => adminPathWithQuery2("/admin/providers", args)
      },
      {
        commandId: "admin.provider.create",
        mcpToolName: "admin_provider_create",
        cliPath: ["provider", "create"],
        description: "Admin-only: create an AI provider from a backend provider payload.",
        method: "POST",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/providers"
      },
      {
        commandId: "admin.provider.credential.create",
        mcpToolName: "admin_provider_credential_create",
        cliPath: ["provider", "credential", "create"],
        description: "Admin-only: create credentials for an existing AI provider.",
        method: "POST",
        inputSchema: adminProviderCredentialSchema2(true),
        path: (args) => `/admin/providers/${adminRequiredPathArg2(args, ["providerID", "providerId", "provider_id"])}/credentials`
      },
      {
        commandId: "admin.provider.credential.update",
        mcpToolName: "admin_provider_credential_update",
        cliPath: ["provider", "credential", "update"],
        description: "Admin-only: update one credential key for an existing AI provider.",
        method: "PATCH",
        inputSchema: adminProviderCredentialSchema2(true, true),
        path: (args) => `/admin/providers/${adminRequiredPathArg2(args, ["providerID", "providerId", "provider_id"])}/credentials/${adminRequiredPathArg2(args, ["credentialKey", "credential_key"])}`
      },
      {
        commandId: "admin.provider.credential.set_primary",
        mcpToolName: "admin_provider_credential_set_primary",
        cliPath: ["provider", "credential", "set-primary"],
        description: "Admin-only: mark a provider credential key as the primary credential.",
        method: "POST",
        inputSchema: adminProviderCredentialSchema2(false, true),
        path: (args) => `/admin/providers/${adminRequiredPathArg2(args, ["providerID", "providerId", "provider_id"])}/credentials/${adminRequiredPathArg2(args, ["credentialKey", "credential_key"])}/primary`
      },
      {
        commandId: "admin.provider.asset_library.get",
        mcpToolName: "admin_provider_asset_library_get",
        cliPath: ["provider", "asset-library", "get"],
        description: "Admin-only: read provider asset-library settings used for provider-side asset references.",
        method: "GET",
        inputSchema: adminProviderSchema2(),
        path: (args) => `/admin/providers/${adminRequiredPathArg2(args, ["providerID", "providerId", "provider_id"])}/asset-library`
      },
      {
        commandId: "admin.provider.asset_library.update",
        mcpToolName: "admin_provider_asset_library_update",
        cliPath: ["provider", "asset-library", "update"],
        description: "Admin-only: update provider asset-library settings used for provider-side asset references.",
        method: "PUT",
        inputSchema: adminProviderSchema2(true),
        path: (args) => `/admin/providers/${adminRequiredPathArg2(args, ["providerID", "providerId", "provider_id"])}/asset-library`
      },
      {
        commandId: "admin.model.catalog_template.list",
        mcpToolName: "admin_model_catalog_template_list",
        cliPath: ["model", "catalog-template", "list"],
        description: "Admin-only: list model catalog templates; pass query.lab to filter by lab.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: (args) => adminPathWithQuery2("/admin/model-catalog/templates", args)
      },
      {
        commandId: "admin.model.import.preview",
        mcpToolName: "admin_model_import_preview",
        cliPath: ["model", "import", "preview"],
        description: "Admin-only: preview importing provider models into catalog entries and route bindings.",
        method: "POST",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/model-imports/preview"
      },
      {
        commandId: "admin.model.import.apply",
        mcpToolName: "admin_model_import_apply",
        cliPath: ["model", "import", "apply"],
        description: "Admin-only: apply a model import into catalog entries and route bindings.",
        method: "POST",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/model-imports/apply"
      },
      {
        commandId: "admin.model.catalog.list",
        mcpToolName: "admin_model_catalog_list",
        cliPath: ["model", "catalog", "list"],
        description: "Admin-only: list model catalog entries with route binding summary.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: (args) => adminPathWithQuery2("/admin/model-catalog", args)
      },
      {
        commandId: "admin.model.catalog.create",
        mcpToolName: "admin_model_catalog_create",
        cliPath: ["model", "catalog", "create"],
        description: "Admin-only: create a model catalog entry.",
        method: "POST",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/model-catalog"
      },
      {
        commandId: "admin.model.catalog.update",
        mcpToolName: "admin_model_catalog_update",
        cliPath: ["model", "catalog", "update"],
        description: "Admin-only: update a model catalog entry.",
        method: "PUT",
        inputSchema: adminCatalogEntrySchema2(true),
        path: (args) => `/admin/model-catalog/${adminRequiredPathArg2(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id", "id"])}`
      },
      {
        commandId: "admin.model.catalog.delete",
        mcpToolName: "admin_model_catalog_delete",
        cliPath: ["model", "catalog", "delete"],
        description: "Admin-only: delete a model catalog entry.",
        method: "DELETE",
        inputSchema: adminCatalogEntrySchema2(false),
        path: (args) => `/admin/model-catalog/${adminRequiredPathArg2(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id", "id"])}`
      },
      {
        commandId: "admin.model.route.diagnose",
        mcpToolName: "admin_model_route_diagnose",
        cliPath: ["model", "route", "diagnose"],
        description: "Admin-only: diagnose route/model/provider readiness for a model operation.",
        method: "POST",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/model-routes/diagnose"
      },
      {
        commandId: "admin.model.route_binding.create",
        mcpToolName: "admin_model_route_binding_create",
        cliPath: ["model", "route-binding", "create"],
        description: "Admin-only: create a route binding for a catalog entry.",
        method: "POST",
        inputSchema: adminRouteBindingSchema2(true),
        path: (args) => `/admin/model-catalog/${adminRequiredPathArg2(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"])}/route-bindings`
      },
      {
        commandId: "admin.model.route_binding.update",
        mcpToolName: "admin_model_route_binding_update",
        cliPath: ["model", "route-binding", "update"],
        description: "Admin-only: update a route binding for a catalog entry.",
        method: "PUT",
        inputSchema: adminRouteBindingSchema2(true, true),
        path: (args) => `/admin/model-catalog/${adminRequiredPathArg2(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"])}/route-bindings/${adminRequiredPathArg2(args, ["bindingID", "bindingId", "binding_id"])}`
      },
      {
        commandId: "admin.model.route_binding.delete",
        mcpToolName: "admin_model_route_binding_delete",
        cliPath: ["model", "route-binding", "delete"],
        description: "Admin-only: delete a route binding from a catalog entry.",
        method: "DELETE",
        inputSchema: adminRouteBindingSchema2(false, true),
        path: (args) => `/admin/model-catalog/${adminRequiredPathArg2(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"])}/route-bindings/${adminRequiredPathArg2(args, ["bindingID", "bindingId", "binding_id"])}`
      },
      {
        commandId: "admin.generation_tools.settings.get",
        mcpToolName: "admin_generation_tools_settings_get",
        cliPath: ["generation-tools", "settings", "get"],
        description: "Admin-only: read generation tool server settings.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: () => "/admin/settings/generation-tools"
      },
      {
        commandId: "admin.generation_tools.settings.update",
        mcpToolName: "admin_generation_tools_settings_update",
        cliPath: ["generation-tools", "settings", "update"],
        description: "Admin-only: update generation tool server settings.",
        method: "PUT",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/settings/generation-tools"
      },
      {
        commandId: "admin.resource_access.settings.get",
        mcpToolName: "admin_resource_access_settings_get",
        cliPath: ["resource-access", "settings", "get"],
        description: "Admin-only: read resource public access profiles, including public tunnel settings.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: () => "/admin/settings/resource-access"
      },
      {
        commandId: "admin.resource_access.settings.update",
        mcpToolName: "admin_resource_access_settings_update",
        cliPath: ["resource-access", "settings", "update"],
        description: "Admin-only: update resource public access profiles, including public tunnel settings.",
        method: "PUT",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/settings/resource-access"
      },
      {
        commandId: "admin.public_tunnel.config.get",
        mcpToolName: "admin_public_tunnel_config_get",
        cliPath: ["public-tunnel", "config", "get"],
        description: "Admin-only alias: read public tunnel configuration from resource access profiles.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: () => "/admin/settings/resource-access"
      },
      {
        commandId: "admin.public_tunnel.config.update",
        mcpToolName: "admin_public_tunnel_config_update",
        cliPath: ["public-tunnel", "config", "update"],
        description: "Admin-only alias: update public tunnel configuration through resource access profiles.",
        method: "PUT",
        inputSchema: adminWriteSchema2(),
        path: () => "/admin/settings/resource-access"
      },
      {
        commandId: "admin.model.gateway_key.list",
        mcpToolName: "admin_model_gateway_key_list",
        cliPath: ["model", "gateway-key", "list"],
        description: "Admin-only: list model gateway API keys.",
        method: "GET",
        inputSchema: adminReadSchema2(),
        path: (args) => adminPathWithQuery2("/model-gateway/api-keys", args)
      },
      {
        commandId: "admin.model.gateway_key.create",
        mcpToolName: "admin_model_gateway_key_create",
        cliPath: ["model", "gateway-key", "create"],
        description: "Admin-only: create a model gateway API key.",
        method: "POST",
        inputSchema: adminWriteSchema2(),
        path: () => "/model-gateway/api-keys"
      },
      {
        commandId: "admin.model.gateway_key.update",
        mcpToolName: "admin_model_gateway_key_update",
        cliPath: ["model", "gateway-key", "update"],
        description: "Admin-only: update a model gateway API key.",
        method: "PATCH",
        inputSchema: adminGatewayKeySchema2(true),
        path: (args) => `/model-gateway/api-keys/${adminRequiredPathArg2(args, ["keyID", "keyId", "key_id", "id"])}`
      },
      {
        commandId: "admin.model.gateway_key.delete",
        mcpToolName: "admin_model_gateway_key_delete",
        cliPath: ["model", "gateway-key", "delete"],
        description: "Admin-only: delete a model gateway API key.",
        method: "DELETE",
        inputSchema: adminGatewayKeySchema2(false),
        path: (args) => `/model-gateway/api-keys/${adminRequiredPathArg2(args, ["keyID", "keyId", "key_id", "id"])}`
      }
    ];
    var adminCommandByMCPToolName2 = new Map(adminCommandSpecs2.map((spec) => [spec.mcpToolName, spec]));
    var adminCommandById2 = new Map(adminCommandSpecs2.map((spec) => [spec.commandId, spec]));
    function isAdminMCPToolName2(name) {
      return Boolean(name && adminCommandByMCPToolName2.has(name));
    }
    async function runMovScriptAdminCommand2(specOrName, args = {}) {
      const spec = typeof specOrName === "string" ? adminCommandByMCPToolName2.get(specOrName) ?? adminCommandById2.get(specOrName) : specOrName;
      if (!spec) throw new Error(`Unknown admin command: ${specOrName}`);
      const binding = bindAdminBackendRuntime2(args);
      const path = spec.path(args);
      const data = await callAdminBackend2(spec, path, args);
      return {
        schema: "movscript.command_result.v1",
        status: "ok",
        commandId: spec.commandId,
        mcpToolName: spec.mcpToolName,
        data,
        debug: {
          cli_argv: adminDebugCliArgv2(spec, args),
          method: spec.method,
          path,
          cwd: process.cwd(),
          ...binding.backendEndpoint ? { runtime_endpoint: binding.backendEndpoint } : {}
        }
      };
    }
    function unwrapCommandDataWithDebug2(execution) {
      if (isRecord43(execution.data) && !Array.isArray(execution.data)) {
        return {
          ...execution.data,
          debug: execution.debug
        };
      }
      return {
        data: execution.data,
        debug: execution.debug
      };
    }
    function bindAdminBackendRuntime2(args) {
      const workspaceDir2 = stringValue24(args.workspaceDir ?? args.workspace_dir) ?? stringValue24(args.projectDir ?? args.project_dir) ?? stringValue24(args.cwd) ?? process.env.MOVSCRIPT_WORKSPACE_DIR;
      if (workspaceDir2) setMovScriptBackendDefaultWorkspaceDir2((0, import_node_path42.resolve)(workspaceDir2));
      const token = stringValue24(args.token);
      if (token) setMovScriptBackendRuntimeAuthToken2(token);
      const explicitBackend = stringValue24(args.backendBaseURL ?? args.backend_base_url ?? args.server);
      if (explicitBackend) {
        setMovScriptBackendAPIBaseURL2(explicitBackend);
        return { backendEndpoint: explicitBackend };
      }
      const homeDir = resolveRuntimeHomeArg3(args);
      const runtimeHome = readRuntimeHomeSnapshot2(homeDir);
      const gatewayEndpoint = endpointURL9(
        findRuntimeEndpoint2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE5) ?? findRuntimeService2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE5)?.endpoint
      );
      const dataEndpoint = endpointURL9(
        findRuntimeEndpoint2(runtimeHome, DATA_SERVICE3) ?? findRuntimeService2(runtimeHome, DATA_SERVICE3)?.endpoint
      );
      const backendEndpoint = gatewayEndpoint ?? dataEndpoint;
      if (backendEndpoint) setMovScriptBackendAPIBaseURL2(backendEndpoint);
      return { backendEndpoint };
    }
    async function callAdminBackend2(spec, path, args) {
      switch (spec.method) {
        case "GET":
          return await backendGet22(path);
        case "POST":
          return await backendPost22(path, adminPayload2(args));
        case "PUT":
          return await backendPut2(path, adminPayload2(args));
        case "PATCH":
          return await backendPatch2(path, adminPayload2(args));
        case "DELETE": {
          const result = await backendDelete2(path);
          return result === null ? { status: "deleted" } : result;
        }
      }
    }
    function adminPathWithQuery2(path, args) {
      const query = isRecord43(args.query) ? args.query : {};
      const params = new URLSearchParams();
      for (const [key, value] of Object.entries(query)) {
        if (typeof value === "string" && value.trim()) params.set(key, value.trim());
        else if (typeof value === "number" && Number.isFinite(value)) params.set(key, String(value));
        else if (typeof value === "boolean") params.set(key, String(value));
      }
      const suffix = params.toString();
      return suffix ? `${path}?${suffix}` : path;
    }
    function adminPayload2(args) {
      const payload = args.payload ?? args.body;
      if (payload === void 0) return {};
      if (!isRecord43(payload)) throw new Error("admin command payload must be an object");
      return payload;
    }
    function adminRequiredPathArg2(args, keys) {
      for (const key of keys) {
        const value = stringValue24(args[key]);
        if (value) return encodeURIComponent(value);
      }
      throw new Error(`admin command requires one of: ${keys.join(", ")}`);
    }
    function adminDebugCliArgv2(spec, args) {
      const argv = ["movscript", "admin", ...spec.cliPath, "--json"];
      const homeDir = stringValue24(args.homeDir ?? args.home_dir ?? args.movscriptHome ?? args.movscript_home);
      if (homeDir) argv.push("--home-dir", homeDir);
      const workspaceDir2 = stringValue24(args.workspaceDir ?? args.workspace_dir);
      if (workspaceDir2) argv.push("--workspace", workspaceDir2);
      const projectDir = stringValue24(args.projectDir ?? args.project_dir);
      if (projectDir) argv.push("--project-dir", projectDir);
      const server = stringValue24(args.backendBaseURL ?? args.backend_base_url ?? args.server);
      if (server) argv.push("--server", server);
      if (args.token !== void 0) argv.push("--token", "<redacted>");
      appendPathArg2(argv, args, ["providerID", "providerId", "provider_id"], "--provider-id");
      appendPathArg2(argv, args, ["credentialKey", "credential_key"], "--credential-key");
      appendPathArg2(argv, args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"], "--catalog-entry-id");
      appendPathArg2(argv, args, ["bindingID", "bindingId", "binding_id"], "--binding-id");
      appendPathArg2(argv, args, ["keyID", "keyId", "key_id"], "--key-id");
      appendPathArg2(argv, args, ["id"], "--id");
      if (isRecord43(args.query)) {
        for (const [key, value] of Object.entries(args.query)) {
          argv.push("--query", `${key}=${String(value)}`);
        }
      }
      if (args.payload !== void 0 || args.body !== void 0) argv.push("--payload", "<json>");
      return argv;
    }
    function appendPathArg2(argv, args, keys, flag) {
      for (const key of keys) {
        const value = stringValue24(args[key]);
        if (value) {
          argv.push(flag, value);
          return;
        }
      }
    }
    function resolveRuntimeHomeArg3(args) {
      const homeDir = stringValue24(args.homeDir ?? args.home_dir ?? args.movscriptHome ?? args.movscript_home);
      return homeDir ? (0, import_node_path42.resolve)(homeDir) : resolveMovScriptHomeDir22();
    }
    function endpointURL9(endpoint) {
      if (!endpoint) return void 0;
      if (endpoint.protocol && endpoint.protocol !== "http" && endpoint.protocol !== "https") return void 0;
      if (endpoint.url) return endpoint.url;
      if (endpoint.baseURL) return endpoint.baseURL;
      if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
      if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
      return void 0;
    }
    function adminReadSchema2(extra = {}) {
      return objectSchema5({
        ...adminRuntimeProperties2(),
        query: {
          type: "object",
          additionalProperties: true,
          description: "Optional query string parameters for this fixed admin endpoint."
        },
        ...extra
      });
    }
    function adminWriteSchema2(extra = {}) {
      return objectSchema5({
        ...adminRuntimeProperties2(),
        payload: {
          type: "object",
          additionalProperties: true,
          description: "Backend request body for this fixed admin endpoint."
        },
        body: {
          type: "object",
          additionalProperties: true,
          description: "Alias for payload."
        },
        ...extra
      });
    }
    function adminProviderSchema2(includePayload = false) {
      const extra = {
        providerID: { type: "string", description: "Provider id." },
        providerId: { type: "string", description: "Alias for providerID." },
        provider_id: { type: "string", description: "Alias for providerID." }
      };
      return includePayload ? adminWriteSchema2(extra) : adminReadSchema2(extra);
    }
    function adminProviderCredentialSchema2(includePayload = false, includeCredentialKey = false) {
      return (includePayload ? adminWriteSchema2 : adminReadSchema2)({
        providerID: { type: "string", description: "Provider id." },
        providerId: { type: "string", description: "Alias for providerID." },
        provider_id: { type: "string", description: "Alias for providerID." },
        ...includeCredentialKey ? {
          credentialKey: { type: "string", description: "Provider credential key." },
          credential_key: { type: "string", description: "Alias for credentialKey." }
        } : {}
      });
    }
    function adminCatalogEntrySchema2(includePayload = false) {
      return (includePayload ? adminWriteSchema2 : adminReadSchema2)({
        catalogEntryID: { type: ["string", "number"], description: "Model catalog entry id." },
        catalogEntryId: { type: ["string", "number"], description: "Alias for catalogEntryID." },
        catalog_entry_id: { type: ["string", "number"], description: "Alias for catalogEntryID." },
        id: { type: ["string", "number"], description: "Alias for catalogEntryID." }
      });
    }
    function adminRouteBindingSchema2(includePayload = false, includeBinding = false) {
      return (includePayload ? adminWriteSchema2 : adminReadSchema2)({
        catalogEntryID: { type: ["string", "number"], description: "Model catalog entry id." },
        catalogEntryId: { type: ["string", "number"], description: "Alias for catalogEntryID." },
        catalog_entry_id: { type: ["string", "number"], description: "Alias for catalogEntryID." },
        ...includeBinding ? {
          bindingID: { type: ["string", "number"], description: "Route binding id." },
          bindingId: { type: ["string", "number"], description: "Alias for bindingID." },
          binding_id: { type: ["string", "number"], description: "Alias for bindingID." }
        } : {}
      });
    }
    function adminGatewayKeySchema2(includePayload = false) {
      return (includePayload ? adminWriteSchema2 : adminReadSchema2)({
        keyID: { type: ["string", "number"], description: "Model gateway API key id." },
        keyId: { type: ["string", "number"], description: "Alias for keyID." },
        key_id: { type: ["string", "number"], description: "Alias for keyID." },
        id: { type: ["string", "number"], description: "Alias for keyID." }
      });
    }
    function adminRuntimeProperties2() {
      return {
        homeDir: { type: "string", description: "Optional MovScript Home directory used to discover the daemon gateway." },
        home_dir: { type: "string", description: "Alias for homeDir." },
        movscriptHome: { type: "string", description: "Alias for homeDir." },
        movscript_home: { type: "string", description: "Alias for homeDir." },
        workspaceDir: { type: "string", description: "Optional workspace directory used for backend auth lookup." },
        workspace_dir: { type: "string", description: "Alias for workspaceDir." },
        projectDir: { type: "string", description: "Optional project directory used for backend auth lookup." },
        project_dir: { type: "string", description: "Alias for projectDir." },
        backendBaseURL: { type: "string", description: "Optional backend or daemon gateway base URL." },
        backend_base_url: { type: "string", description: "Alias for backendBaseURL." },
        server: { type: "string", description: "Alias for backendBaseURL, used by the MovScript CLI global --server." },
        token: { type: "string", description: "Optional backend bearer token. MCP responses redact this from debug argv." }
      };
    }
    function objectSchema5(properties, required = []) {
      return {
        type: "object",
        properties,
        ...required.length > 0 ? { required } : {},
        additionalProperties: false
      };
    }
    function stringValue24(value) {
      if (typeof value === "string" && value.trim()) return value.trim();
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      return void 0;
    }
    function isRecord43(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function registerAdminCommands(program2) {
      const admin = program2.command("admin").description("Run MovScript system administration commands through the shared CLI/MCP command runner");
      for (const spec of adminCommandSpecs2) {
        const command = ensureCommandPath(admin, spec.cliPath);
        command.description(spec.description);
        addAdminOptions(command);
        command.action(async (options, command2) => {
          await runAdminCommand(spec, options, command2);
        });
      }
    }
    function ensureCommandPath(root, path) {
      let current = root;
      for (const segment of path) {
        let child = current.commands.find((candidate) => candidate.name() === segment);
        if (!child) child = current.command(segment);
        current = child;
      }
      return current;
    }
    function addAdminOptions(command) {
      command.option("--home-dir <dir>", "MovScript Home directory used to discover the daemon gateway").option("--workspace <dir>", "Workspace root directory used for backend auth lookup").option("--project-dir <dir>", "MovScript project directory used for backend auth lookup").option("--server <url>", "Backend or daemon gateway base URL").option("--token <token>", "Backend bearer token").option("--query <key=value...>", "Query parameter; repeat for multiple values", collect).option("--payload <json>", "JSON object request body").option("--payload-file <path>", "Path to a JSON object request body").option("--provider-id <id>", "Provider id").option("--credential-key <key>", "Provider credential key").option("--catalog-entry-id <id>", "Model catalog entry id").option("--binding-id <id>", "Route binding id").option("--key-id <id>", "Model gateway API key id").option("--id <id>", "Generic id alias for delete/update commands").option("--yes", "Confirm delete or no-payload admin mutation").option("--json", "Print JSON output");
    }
    async function runAdminCommand(spec, options, command) {
      try {
        if (requiresConfirmation(spec, options)) {
          throw new Error(`${spec.commandId} requires --yes`);
        }
        const execution = await runMovScriptAdminCommand2(spec, adminArgs(options, command));
        console.log(JSON.stringify(execution, null, 2));
      } catch (error) {
        console.log(JSON.stringify({
          status: "error",
          error: {
            code: "admin_command_failed",
            message: errorMessage7(error)
          }
        }, null, 2));
        process.exitCode = 1;
      }
    }
    function adminArgs(options, command) {
      const global = commandGlobalOptions(command);
      return compactRecord3({
        homeDir: options.homeDir,
        workspaceDir: options.workspace ?? global.workspace,
        projectDir: options.projectDir,
        backendBaseURL: options.server ?? global.server,
        token: options.token ?? global.token,
        query: parseQuery(options.query),
        payload: parsePayload(options),
        providerId: options.providerId,
        credentialKey: options.credentialKey,
        catalogEntryId: options.catalogEntryId,
        bindingId: options.bindingId,
        keyId: options.keyId,
        id: options.id
      });
    }
    function commandGlobalOptions(command) {
      const root = rootCommand(command);
      const options = root.opts();
      const serverSource = root.getOptionValueSource?.("server");
      return {
        server: serverSource && serverSource !== "default" && typeof options.server === "string" ? options.server : void 0,
        token: typeof options.token === "string" ? options.token : process.env.MOVSCRIPT_DATA_SERVICE_TOKEN,
        workspace: typeof options.workspace === "string" ? options.workspace : void 0
      };
    }
    function rootCommand(command) {
      let current = command;
      while (current.parent) current = current.parent;
      return current;
    }
    function requiresConfirmation(spec, options) {
      if (options.yes) return false;
      if (spec.method === "DELETE") return true;
      if (spec.commandId === "admin.provider.credential.set_primary") return true;
      return false;
    }
    function parseQuery(values) {
      if (!values || values.length === 0) return void 0;
      const query = {};
      for (const value of values) {
        const index = value.indexOf("=");
        if (index <= 0) throw new Error(`invalid --query value: ${value}`);
        const key = value.slice(0, index).trim();
        const raw = value.slice(index + 1).trim();
        if (!key) throw new Error(`invalid --query key: ${value}`);
        query[key] = parseScalar(raw);
      }
      return query;
    }
    function parsePayload(options) {
      if (options.payload !== void 0 && options.payloadFile !== void 0) {
        throw new Error("pass only one of --payload or --payload-file");
      }
      const text = options.payloadFile !== void 0 ? (0, import_node_fs4.readFileSync)(options.payloadFile, "utf8") : options.payload;
      if (text === void 0) return void 0;
      const value = JSON.parse(text);
      if (!isRecord53(value)) throw new Error("admin payload must be a JSON object");
      return value;
    }
    function parseScalar(value) {
      if (value === "true") return true;
      if (value === "false") return false;
      if (value && Number.isFinite(Number(value))) return Number(value);
      return value;
    }
    function collect(value, previous = []) {
      previous.push(value);
      return previous;
    }
    function compactRecord3(input3) {
      return Object.fromEntries(Object.entries(input3).filter(([, value]) => value !== void 0));
    }
    function isRecord53(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function errorMessage7(error) {
      return error instanceof Error ? error.message : String(error);
    }
    var import_promises9 = __require("readline/promises");
    var import_node_process = __require("process");
    function registerAuthCommands(program2) {
      const auth = program2.command("auth").description("Manage MovScript backend auth stored under the workspace .movscript directory");
      auth.command("login").description("Login to the MovScript backend and store credentials in .movscript/backend/auth.json").option("--workspace <dir>", "Workspace root directory").option("--server <url>", "MovScript server URL").option("--username <username>", "Username").option("--password <password>", "Password (or set MOVCLI_PASSWORD)").option("--json", "Print JSON output").action(async (options, command) => {
        const resolvedOptions = resolveAuthOptions(options, command);
        const global = commandGlobalOptions2(command);
        const workspaceDir2 = resolvedOptions.workspace ?? global.workspace;
        const server = resolvedOptions.server ?? global.server;
        const username = resolvedOptions.username ?? await promptValue("Username: ");
        const password = resolvedOptions.password ?? process.env.MOVCLI_PASSWORD ?? await promptValue("Password: ");
        const login = await loginMovScriptBackend2({ workspaceDir: workspaceDir2, server, username, password });
        const authRecord = writeMovScriptBackendAuth2(workspaceDir2, {
          token: login.token,
          expiresAt: login.expiresAt,
          user: login.user,
          gitCredential: login.gitCredential
        });
        const userId = typeof login.user?.id === "string" || typeof login.user?.id === "number" ? login.user.id : void 0;
        const config = writeMovScriptBackendConfig2(workspaceDir2, {
          baseURL: login.baseURL,
          ...userId !== void 0 ? { activeUserId: userId } : {}
        });
        print(resolvedOptions, {
          status: "logged_in",
          workspaceDir: resolveMovScriptBackendSession2({ workspaceDir: workspaceDir2 }).workspaceDir,
          configPath: resolveMovScriptBackendSession2({ workspaceDir: workspaceDir2 }).configPath,
          authPath: resolveMovScriptBackendSession2({ workspaceDir: workspaceDir2 }).authPath,
          baseURL: config.baseURL,
          user: authRecord.user,
          gitCredential: authRecord.gitCredential ? {
            provider: authRecord.gitCredential.provider,
            username: authRecord.gitCredential.username,
            maskedToken: authRecord.gitCredential.maskedToken,
            status: authRecord.gitCredential.status
          } : void 0,
          expiresAt: authRecord.expiresAt
        });
      });
      auth.command("status").alias("info").description("Show workspace backend auth status").option("--workspace <dir>", "Workspace root directory").option("--server <url>", "MovScript server URL override").option("--json", "Print JSON output").action(async (options, command) => {
        const resolvedOptions = resolveAuthOptions(options, command);
        print(resolvedOptions, await currentAuthStatus(resolvedOptions, command));
      });
      auth.command("logout").description("Remove workspace backend credentials").option("--workspace <dir>", "Workspace root directory").option("--json", "Print JSON output").action((options, command) => {
        const resolvedOptions = resolveAuthOptions(options, command);
        const global = commandGlobalOptions2(command);
        const workspaceDir2 = resolvedOptions.workspace ?? global.workspace;
        const session = resolveMovScriptBackendSession2({ workspaceDir: workspaceDir2 });
        clearMovScriptBackendAuth2(session.workspaceDir);
        print(resolvedOptions, {
          status: "logged_out",
          workspaceDir: session.workspaceDir,
          authPath: session.authPath
        });
      });
      program2.command("whoami").description("Show current MovScript backend login information").option("--workspace <dir>", "Workspace root directory").option("--server <url>", "MovScript server URL override").option("--json", "Print JSON output").action(async (options, command) => {
        const resolvedOptions = resolveAuthOptions(options, command);
        print(resolvedOptions, await currentAuthStatus(resolvedOptions, command));
      });
    }
    function resolveAuthOptions(options, command) {
      const local = command.opts();
      const global = command.optsWithGlobals ? command.optsWithGlobals() : local;
      return {
        ...options,
        workspace: options.workspace ?? stringField35(local.workspace) ?? stringField35(global.workspace),
        server: options.server ?? stringField35(local.server) ?? stringField35(global.server),
        username: options.username ?? stringField35(local.username),
        password: options.password ?? stringField35(local.password),
        json: options.json === true || local.json === true || global.json === true
      };
    }
    async function currentAuthStatus(options, command) {
      const global = commandGlobalOptions2(command);
      const session = resolveMovScriptBackendSession2({
        workspaceDir: options.workspace ?? global.workspace,
        server: options.server ?? global.server,
        token: global.token
      });
      const auth = readMovScriptBackendAuth2(session.workspaceDir);
      let me;
      let verificationError;
      if (session.token) {
        try {
          me = await getMovScriptBackendMe2({ workspaceDir: session.workspaceDir, server: session.baseURL, token: session.token });
        } catch (error) {
          verificationError = error instanceof Error ? error.message : String(error);
        }
      }
      return {
        status: session.token ? me ? "authenticated" : "authenticated_offline" : "anonymous",
        workspaceDir: session.workspaceDir,
        baseURL: session.baseURL,
        userId: session.userId,
        user: recordField6(me?.user) ?? auth?.user ?? session.user,
        orgMemberships: Array.isArray(me?.org_memberships) ? me.org_memberships : void 0,
        gitCredential: auth?.gitCredential ? {
          provider: auth.gitCredential.provider,
          username: auth.gitCredential.username,
          maskedToken: auth.gitCredential.maskedToken,
          status: auth.gitCredential.status,
          lastError: auth.gitCredential.lastError
        } : void 0,
        expiresAt: auth?.expiresAt,
        authPath: session.authPath,
        configPath: session.configPath,
        ...verificationError ? { verificationError } : {}
      };
    }
    function commandGlobalOptions2(command) {
      const root = command.parent?.parent ?? command.parent ?? command;
      const options = root.optsWithGlobals ? root.optsWithGlobals() : root.opts();
      return {
        server: typeof options.server === "string" ? options.server : void 0,
        token: typeof options.token === "string" ? options.token : process.env.MOVSCRIPT_DATA_SERVICE_TOKEN,
        workspace: typeof options.workspace === "string" ? options.workspace : void 0
      };
    }
    async function promptValue(question) {
      const rl = (0, import_promises9.createInterface)({ input: import_node_process.stdin, output: import_node_process.stdout });
      try {
        return (await rl.question(question)).trim();
      } finally {
        rl.close();
      }
    }
    function print(options, result) {
      if (options.json) {
        console.log(JSON.stringify(result, null, 2));
        return;
      }
      if (result.status === "logged_in") {
        console.log(`Logged in to ${result.baseURL}`);
        printUserInfo(result.user);
        printGitCredential(result.gitCredential);
        if (result.expiresAt) console.log(`Expires: ${result.expiresAt}`);
        console.log(`Auth: ${result.authPath}`);
        return;
      }
      if (result.status === "logged_out") {
        console.log(`Logged out: ${result.authPath}`);
        return;
      }
      console.log(`Backend: ${result.baseURL}`);
      console.log(`Status: ${result.status}`);
      console.log(`Workspace: ${result.workspaceDir}`);
      printUserInfo(result.user, result.userId);
      printOrgMemberships(result.orgMemberships);
      printGitCredential(result.gitCredential);
      if (result.expiresAt) console.log(`Expires: ${result.expiresAt}`);
      if (result.verificationError) console.log(`Verification: ${result.verificationError}`);
      console.log(`Auth: ${result.authPath}`);
      console.log(`Config: ${result.configPath}`);
    }
    function printUserInfo(value, fallbackUserId) {
      const user = recordField6(value);
      const id = idField24(user?.id) ?? idField24(fallbackUserId);
      const username = stringField35(user?.username);
      const displayName22 = stringField35(user?.displayName) ?? stringField35(user?.display_name);
      const primaryEmail = stringField35(user?.primaryEmail) ?? stringField35(user?.primary_email);
      const locale = stringField35(user?.locale);
      const systemRole = stringField35(user?.systemRole) ?? stringField35(user?.system_role);
      if (displayName22 || username) console.log(`User: ${formatUserLabel({ displayName: displayName22, username, id })}`);
      else if (id !== void 0) console.log(`User ID: ${String(id)}`);
      if (primaryEmail) console.log(`Email: ${primaryEmail}`);
      if (systemRole) console.log(`Role: ${systemRole}`);
      if (locale) console.log(`Locale: ${locale}`);
    }
    function printOrgMemberships(value) {
      if (!Array.isArray(value) || value.length === 0) return;
      console.log("Organizations:");
      for (const item of value) {
        const membership = recordField6(item);
        if (!membership) continue;
        const name = stringField35(membership.org_name) ?? stringField35(membership.name) ?? stringField35(membership.org_slug) ?? scalarDisplayValue(membership.org_id);
        const role = stringField35(membership.role);
        const status = stringField35(membership.status);
        const plan = stringField35(membership.plan);
        const flags = [role, status, plan].filter(Boolean).join(", ");
        console.log(`  - ${name}${flags ? ` (${flags})` : ""}`);
      }
    }
    function printGitCredential(value) {
      const credential = recordField6(value);
      if (!credential) return;
      const provider = stringField35(credential.provider);
      const username = stringField35(credential.username);
      const status = stringField35(credential.status);
      const maskedToken = stringField35(credential.maskedToken) ?? stringField35(credential.masked_token);
      console.log(`Git: ${[provider, username].filter(Boolean).join(":") || "configured"}${status ? ` (${status})` : ""}`);
      if (maskedToken) console.log(`Git Token: ${maskedToken}`);
    }
    function formatUserLabel(input3) {
      if (input3.displayName && input3.username) return `${input3.displayName} (@${input3.username}${input3.id !== void 0 ? `, ${input3.id}` : ""})`;
      if (input3.username) return `${input3.username}${input3.id !== void 0 ? ` (${input3.id})` : ""}`;
      if (input3.displayName) return `${input3.displayName}${input3.id !== void 0 ? ` (${input3.id})` : ""}`;
      return input3.id !== void 0 ? String(input3.id) : "-";
    }
    function recordField6(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value) ? value : void 0;
    }
    function stringField35(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function idField24(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function scalarDisplayValue(value) {
      if (typeof value === "string" && value.trim()) return value.trim();
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      return "-";
    }
    var import_promises42 = __require("readline/promises");
    var import_node_process2 = __require("process");
    var import_promises22 = __require("fs/promises");
    var import_node_path52 = __require("path");
    var MOVSCRIPT_INTERPRET_CURRENT_DIR2 = ".interpret/current";
    var MOVSCRIPT_INTERPRET_MANIFESTS_DIR2 = ".interpret/manifests";
    var MOVSCRIPT_DOMAIN_TREE_PATH2 = ".interpret/current/domain-tree.json";
    var MOVSCRIPT_EDITOR_STATE_PATH2 = ".interpret/current/editor-state.json";
    var MOVSCRIPT_DOMAIN_INDEX_PATH2 = ".interpret/current/domain-index.json";
    var MOVSCRIPT_ASSET_INDEX_PATH2 = ".interpret/current/asset-index.json";
    var MOVSCRIPT_RELATION_GRAPH_PATH2 = ".interpret/current/relation-graph.json";
    function normalizeWorkspacePath8(path) {
      return path.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function safeWorkspacePathToken2(value) {
      return String(value).trim().replace(/[^a-zA-Z0-9_-]+/g, "_");
    }
    function semanticEntityId2(value, entityKind, fallback = "local") {
      void entityKind;
      return safeWorkspacePathToken2(rawIdentityValue2(value, fallback));
    }
    function entityPathSlug2(value, entityKind, fallback = "local") {
      void entityKind;
      return safeWorkspacePathToken2(rawIdentityValue2(value, fallback));
    }
    function entityIdentity2(value, entityKind, fallback = "local") {
      const id = semanticEntityId2(value, entityKind, fallback);
      return {
        id,
        slug: entityPathSlug2(id, entityKind, fallback)
      };
    }
    function entityRefAliases3(value, entityKind) {
      if (value === void 0 || value === null || String(value).trim() === "") return [];
      const raw = String(value).trim();
      const normalized = normalizeRefPath2(raw);
      const aliases = /* @__PURE__ */ new Set([raw, normalized]);
      if (entityKind) {
        aliases.add(semanticEntityId2(raw, entityKind));
        aliases.add(entityPathSlug2(raw, entityKind));
        aliases.add(semanticEntityId2(normalized, entityKind));
        aliases.add(entityPathSlug2(normalized, entityKind));
      }
      return [...aliases].filter(Boolean);
    }
    function sameEntityRef3(left, right, entityKind) {
      const leftAliases = new Set(entityRefAliases3(left, entityKind));
      return entityRefAliases3(right, entityKind).some((alias) => leftAliases.has(alias));
    }
    function displayEntityId2(value, entityKind) {
      void entityKind;
      return value;
    }
    function rawIdentityValue2(value, fallback) {
      if (value === void 0 || value === null || String(value).trim() === "") return fallback;
      const raw = String(value).trim();
      return raw.startsWith("-") ? `local_${raw.replace(/^-+/, "")}` : raw;
    }
    function normalizeRefPath2(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function isMovScriptSourcePath2(path) {
      const normalized = normalizeWorkspacePath8(path);
      if (MOVSCRIPT_SOURCE_ROOT_FILES2.has(normalized)) return true;
      const [first] = normalized.split("/");
      const fileName = normalized.split("/").pop();
      return first !== void 0 && MOVSCRIPT_SOURCE_COLLECTION_DIRS2.has(first) && fileName !== void 0 && MOVSCRIPT_SOURCE_ENTITY_FILES2.has(fileName) && isMovScriptSourceDocumentPath2(normalized);
    }
    function isMovScriptSourceDocumentPath2(path) {
      return path.endsWith(".json") || path.endsWith(".md");
    }
    function isMovScriptContentUnitDecisionPath2(path) {
      const normalized = normalizeWorkspacePath8(path);
      return isLegacyMovScriptContentUnitSelectionPath2(normalized) || isMovScriptContentUnitCandidatePath2(normalized);
    }
    function isMovScriptContentUnitCandidatePath2(path) {
      const normalized = normalizeWorkspacePath8(path);
      return /^content_units\/[^/]+\/candidates\/[^/]+\/content_candidate\.json$/.test(normalized);
    }
    function isLegacyMovScriptContentUnitSelectionPath2(path) {
      const normalized = normalizeWorkspacePath8(path);
      return /^content_units\/[^/]+\/selection\.json$/.test(normalized);
    }
    function isMovScriptNonSourceRootDirectory2(path) {
      const normalized = normalizeWorkspacePath8(path);
      if (normalized.includes("/")) return false;
      return normalized === ".movscript" || normalized === ".git" || normalized === ".base" || normalized === ".update" || normalized === "runtime" || normalized === "tmp" || normalized.startsWith(".");
    }
    var MOVSCRIPT_SOURCE_COLLECTION_DIRS2 = /* @__PURE__ */ new Set([
      "settings",
      "scripts",
      "content_units",
      "productions",
      "timeline",
      "project_standards"
    ]);
    var MOVSCRIPT_SOURCE_ROOT_FILES2 = /* @__PURE__ */ new Set([
      "project.json",
      "project_standards.json"
    ]);
    var MOVSCRIPT_SOURCE_ENTITY_FILES2 = /* @__PURE__ */ new Set([
      "setting.json",
      "setting_state.json",
      "asset.json",
      "script.json",
      "script.md",
      "script_version.json",
      "script_block.json",
      "content_unit.json",
      "keyframe.json",
      "production.json",
      "segment.json",
      "scene_moment.json",
      "shot.json",
      "storyboard.json",
      "audio_cue.json",
      "expression_unit.json"
    ]);
    function createNodeMovScriptWorkspaceFileRepository2(rootDir = process.cwd()) {
      const root = (0, import_node_path52.resolve)(rootDir);
      const repository = {
        async list(input3 = {}) {
          const path = normalizeWorkspacePath8(input3.path ?? "");
          const absolutePath = resolveWorkspacePath2(root, path);
          const entries = await (0, import_promises22.readdir)(absolutePath, { withFileTypes: true }).catch(() => []);
          return {
            path,
            entries: entries.map((entry) => ({
              path: normalizeWorkspacePath8((0, import_node_path52.join)(path, entry.name)),
              kind: entry.isDirectory() ? "directory" : "file"
            })).sort((left, right) => {
              if (left.kind !== right.kind) return left.kind === "directory" ? -1 : 1;
              return left.path.localeCompare(right.path);
            })
          };
        },
        async read(input3) {
          const path = normalizeWorkspacePath8(input3.path);
          const absolutePath = resolveWorkspacePath2(root, path);
          const content = await (0, import_promises22.readFile)(absolutePath, "utf8");
          const fileStat = await (0, import_promises22.stat)(absolutePath);
          const version = fileVersion2(fileStat.mtimeMs, fileStat.size);
          return { path, content, size: content.length, updatedAt: fileStat.mtime.toISOString(), version };
        },
        async write(input3) {
          const path = normalizeWorkspacePath8(input3.path);
          const absolutePath = resolveWorkspacePath2(root, path);
          await (0, import_promises22.mkdir)((0, import_node_path52.dirname)(absolutePath), { recursive: true });
          await assertExpectedFileVersion2(absolutePath, input3.expectedVersion);
          await writeTextFileAtomic2(absolutePath, input3.content);
          const fileStat = await (0, import_promises22.stat)(absolutePath);
          const version = fileVersion2(fileStat.mtimeMs, fileStat.size);
          return { path, content: input3.content, size: input3.content.length, updatedAt: fileStat.mtime.toISOString(), version };
        },
        async delete(input3) {
          await (0, import_promises22.rm)(resolveWorkspacePath2(root, normalizeWorkspacePath8(input3.path)), { force: true });
        }
      };
      Object.defineProperty(repository, "rootDir", {
        value: root,
        enumerable: false,
        configurable: false
      });
      return repository;
    }
    function getNodeMovScriptWorkspaceFileRepositoryRoot2(fileRepository) {
      const rootDir = fileRepository.rootDir;
      return typeof rootDir === "string" && rootDir ? rootDir : void 0;
    }
    function resolveWorkspacePath2(root, path) {
      const absolutePath = (0, import_node_path52.resolve)(root, path);
      const relativePath = (0, import_node_path52.relative)(root, absolutePath);
      if (relativePath === "" || !relativePath.startsWith("..") && !relativePath.startsWith("/")) return absolutePath;
      throw new Error("workspace path must stay inside the project workspace root");
    }
    async function writeTextFileAtomic2(filePath, content) {
      const tmpPath = (0, import_node_path52.join)((0, import_node_path52.dirname)(filePath), `.${Date.now()}-${Math.random().toString(36).slice(2)}.tmp`);
      try {
        await (0, import_promises22.writeFile)(tmpPath, content, "utf8");
        await (0, import_promises22.rename)(tmpPath, filePath);
      } catch (error) {
        await (0, import_promises22.rm)(tmpPath, { force: true }).catch(() => void 0);
        throw error;
      }
    }
    async function assertExpectedFileVersion2(filePath, expectedVersion) {
      if (expectedVersion === void 0) return;
      const fileStat = await (0, import_promises22.stat)(filePath).catch((error) => {
        if (isNotFoundError32(error)) return void 0;
        throw error;
      });
      const currentVersion = fileStat ? fileVersion2(fileStat.mtimeMs, fileStat.size) : null;
      if (currentVersion !== expectedVersion) {
        throw new Error(`workspace file changed: ${filePath}`);
      }
    }
    function fileVersion2(mtimeMs, size) {
      return `${Math.trunc(mtimeMs)}:${size}`;
    }
    function isNotFoundError32(error) {
      return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
    }
    var import_node_child_process3 = __require("child_process");
    var import_promises32 = __require("fs/promises");
    var import_node_path6 = __require("path");
    var import_node_util = __require("util");
    var execFileAsync2 = (0, import_node_util.promisify)(import_node_child_process3.execFile);
    var MOVSCRIPT_GIT_SOURCE_PATHS2 = [
      "project.json",
      "project_standards.json",
      "settings",
      "scripts",
      "content_units",
      "productions"
    ];
    async function inspectNodeMovScriptGitWorkspace2(rootDir) {
      const root = (0, import_node_path6.resolve)(rootDir);
      if (!await isGitAvailable2()) {
        return { rootDir: root, available: false, insideWorkTree: false, hasHead: false };
      }
      const repoRoot = await gitStdout2(root, ["rev-parse", "--show-toplevel"]).catch(() => void 0);
      if (!repoRoot) {
        return { rootDir: root, available: true, insideWorkTree: false, hasHead: false };
      }
      const head = await currentNodeMovScriptGitHead2(root);
      return {
        rootDir: root,
        available: true,
        insideWorkTree: true,
        hasHead: Boolean(head),
        repoRoot: repoRoot.trim(),
        ...head ? { head } : {}
      };
    }
    async function currentNodeMovScriptGitHead2(rootDir) {
      const head = await gitStdout2(rootDir, ["rev-parse", "--verify", "HEAD"]).catch(() => void 0);
      return head?.trim() || void 0;
    }
    async function readNodeMovScriptGitSourceFiles2(rootDir, gitRef, pathspecs = MOVSCRIPT_GIT_SOURCE_PATHS2) {
      const stdout = await gitStdout2(rootDir, [
        "ls-tree",
        "-r",
        "--name-only",
        gitRef,
        "--",
        ...pathspecs
      ]).catch(() => "");
      const paths = stdout.split("\n").map((path) => normalizeWorkspacePath8(path)).filter(Boolean);
      const files = [];
      for (const path of paths) {
        try {
          const content = await gitStdout2(rootDir, ["show", `${gitRef}:${path}`], 1024 * 1024 * 20);
          files.push({ path, content });
        } catch {
        }
      }
      return files.sort((left, right) => left.path.localeCompare(right.path));
    }
    async function readNodeMovScriptGitSourceFileChanges2(rootDir, gitRef, pathspecs = MOVSCRIPT_GIT_SOURCE_PATHS2) {
      const trackedStdout = await gitStdout2(rootDir, [
        "diff",
        "--name-status",
        "--find-renames",
        gitRef,
        "--",
        ...pathspecs
      ]).catch(() => "");
      const changes = trackedStdout.split("\n").map(parseGitNameStatusLine2).filter((change) => change !== void 0);
      const seenPaths = new Set(changes.map((change) => change.path));
      const untrackedStdout = await gitStdout2(rootDir, [
        "ls-files",
        "--others",
        "--exclude-standard",
        "--",
        ...pathspecs
      ]).catch(() => "");
      for (const path of untrackedStdout.split("\n").map((item) => normalizeWorkspacePath8(item)).filter(Boolean)) {
        if (seenPaths.has(path)) continue;
        changes.push({
          path,
          state: "added",
          statusCode: "??"
        });
        seenPaths.add(path);
      }
      return changes.sort((left, right) => {
        return left.path.localeCompare(right.path) || String(left.previousPath ?? "").localeCompare(String(right.previousPath ?? "")) || left.statusCode.localeCompare(right.statusCode);
      });
    }
    async function isGitAvailable2() {
      try {
        await execFileAsync2("git", ["--version"]);
        return true;
      } catch {
        return false;
      }
    }
    async function gitStdout2(rootDir, args, maxBuffer) {
      const { stdout } = await execFileAsync2("git", ["-C", rootDir, ...args], {
        ...maxBuffer ? { maxBuffer } : {}
      });
      return stdout;
    }
    function parseGitNameStatusLine2(line) {
      const parts = line.split("	").filter(Boolean);
      const statusCode = parts[0];
      if (!statusCode) return void 0;
      const status = statusCode[0];
      if (status === "R") {
        const previousPath = parts[1] ? normalizeWorkspacePath8(parts[1]) : void 0;
        const path2 = parts[2] ? normalizeWorkspacePath8(parts[2]) : void 0;
        if (!previousPath || !path2) return void 0;
        return {
          path: path2,
          previousPath,
          state: "moved",
          statusCode
        };
      }
      const path = parts[1] ? normalizeWorkspacePath8(parts[1]) : void 0;
      if (!path) return void 0;
      if (status === "A") return { path, state: "added", statusCode };
      if (status === "D") return { path, state: "deleted", statusCode };
      return { path, state: "modified", statusCode };
    }
    var import_node_fs5 = __require("fs");
    var import_node_path7 = __require("path");
    var import_node_fs6 = __require("fs");
    var import_node_path8 = __require("path");
    var MOVSCRIPT_TIMELINE_NAMESPACE_ENTITY_KINDS2 = ["production", "segment"];
    var MOVSCRIPT_SETTING_NAMESPACE_ENTITY_KINDS2 = ["setting", "setting_state", "state"];
    var MOVSCRIPT_SYSTEM_PRIMITIVE_KINDS2 = [
      "scene_moment",
      "expression_unit",
      "storyboard",
      "keyframe",
      "audio_cue",
      "asset",
      "timeline_assembly"
    ];
    var TIMELINE_NAMESPACE_KINDS2 = new Set(MOVSCRIPT_TIMELINE_NAMESPACE_ENTITY_KINDS2);
    var SETTING_NAMESPACE_KINDS2 = new Set(MOVSCRIPT_SETTING_NAMESPACE_ENTITY_KINDS2);
    var SYSTEM_PRIMITIVE_KINDS2 = new Set(MOVSCRIPT_SYSTEM_PRIMITIVE_KINDS2);
    function classifyMovScriptEntityKind2(kind) {
      if (TIMELINE_NAMESPACE_KINDS2.has(kind)) return "timeline_namespace";
      if (SETTING_NAMESPACE_KINDS2.has(kind)) return "setting_namespace";
      if (SYSTEM_PRIMITIVE_KINDS2.has(kind)) return "system_primitive";
      if (kind === "content_unit") return "content_unit";
      if (kind === "candidate" || kind === "selection" || kind === "raw_resource" || kind === "resource") return "resource_state";
      return void 0;
    }
    function projectMovScriptDomainNodeKind2(entityKind, record = {}) {
      const category = classifyMovScriptEntityKind2(entityKind);
      if (category === "timeline_namespace") {
        return stringField43(record.namespace_kind) ?? stringField43(record.timeline_namespace_kind) ?? stringField43(record.timelineNamespaceKind) ?? stringField43(record.kind) ?? entityKind;
      }
      if (category === "setting_namespace") {
        return stringField43(record.namespace_kind) ?? stringField43(record.setting_namespace_kind) ?? stringField43(record.settingNamespaceKind) ?? stringField43(record.setting_kind) ?? stringField43(record.settingKind) ?? stringField43(record.kind) ?? entityKind;
      }
      return entityKind;
    }
    function isMovScriptNamespaceCategory2(category) {
      return category === "timeline_namespace" || category === "setting_namespace";
    }
    function isMovScriptSystemPrimitiveKind2(kind) {
      return kind !== void 0 && SYSTEM_PRIMITIVE_KINDS2.has(kind);
    }
    function isMovScriptTimelineNamespaceKind2(kind) {
      return kind !== void 0 && TIMELINE_NAMESPACE_KINDS2.has(kind);
    }
    function isMovScriptSettingNamespaceKind2(kind) {
      return kind !== void 0 && SETTING_NAMESPACE_KINDS2.has(kind);
    }
    function isMovScriptNamespaceKind2(kind) {
      if (kind === "timeline_namespace" || kind === "setting_namespace") return true;
      return isMovScriptTimelineNamespaceKind2(kind) || isMovScriptSettingNamespaceKind2(kind);
    }
    function stringField43(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    var MOVSCRIPT_SPECIALIZED_CONTENT_UNIT_TYPES2 = [
      "timeline_assembly_ref",
      "production_ref",
      "segment_ref",
      "asset_ref",
      "keyframe_ref",
      "storyboard_ref",
      "audio_cue_ref",
      "scence_moment_ref",
      "scene_moment_ref",
      "expression_unit_ref"
    ];
    var CONTENT_UNIT_TARGET_ADAPTERS2 = {
      timeline_assembly_ref: {
        contentUnitType: "timeline_assembly_ref",
        targetCategory: "timeline_assembly",
        targetKind: "timeline_assembly",
        outputKind: "video",
        primaryRefField: "target_ref"
      },
      production_ref: {
        contentUnitType: "production_ref",
        targetCategory: "timeline_assembly",
        targetKind: "timeline_assembly",
        outputKind: "video",
        primaryRefKind: "production",
        primaryRefField: "production_ref",
        legacyNamespaceKind: "production"
      },
      segment_ref: {
        contentUnitType: "segment_ref",
        targetCategory: "timeline_assembly",
        targetKind: "timeline_assembly",
        outputKind: "video",
        primaryRefKind: "segment",
        primaryRefField: "segment_ref",
        legacyNamespaceKind: "segment"
      },
      asset_ref: primitiveAdapter2("asset_ref", "asset", "image"),
      keyframe_ref: primitiveAdapter2("keyframe_ref", "keyframe", "image"),
      storyboard_ref: primitiveAdapter2("storyboard_ref", "storyboard", "image"),
      audio_cue_ref: primitiveAdapter2("audio_cue_ref", "audio_cue", "audio"),
      scence_moment_ref: primitiveAdapter2("scence_moment_ref", "scene_moment", "video", "scene_moment", "scene_moment_ref"),
      scene_moment_ref: primitiveAdapter2("scene_moment_ref", "scene_moment", "video"),
      expression_unit_ref: primitiveAdapter2("expression_unit_ref", "expression_unit", void 0)
    };
    var PROMPT_REF_KINDS2 = /* @__PURE__ */ new Set([
      "production",
      "segment",
      "asset",
      "keyframe",
      "storyboard",
      "audio_cue",
      "scene_moment",
      "expression_unit",
      "content_unit"
    ]);
    function primitiveAdapter2(contentUnitType22, targetKind, outputKind, primaryRefKind = targetKind, primaryRefField = `${primaryRefKind}_ref`) {
      return {
        contentUnitType: contentUnitType22,
        targetCategory: "system_primitive",
        targetKind,
        outputKind,
        primaryRefKind,
        primaryRefField
      };
    }
    function hasSpecializedContentUnitType2(contentUnitType22) {
      return typeof contentUnitType22 === "string" && isMovScriptSpecializedContentUnitType2(contentUnitType22);
    }
    function isMovScriptSpecializedContentUnitType2(value) {
      return value !== void 0 && MOVSCRIPT_SPECIALIZED_CONTENT_UNIT_TYPES2.includes(value);
    }
    function contentUnitTargetAdapterFor2(contentUnitType22) {
      return isMovScriptSpecializedContentUnitType2(contentUnitType22) ? CONTENT_UNIT_TARGET_ADAPTERS2[contentUnitType22] : void 0;
    }
    function isContentUnitPromptRefKind2(value) {
      return value !== void 0 && PROMPT_REF_KINDS2.has(value);
    }
    function primaryRefKindForContentUnitType4(contentUnitType22) {
      return contentUnitTargetAdapterFor2(contentUnitType22)?.primaryRefKind;
    }
    function primaryRefFieldNameForKind3(kind) {
      return kind === "scene_moment" ? "scene_moment_ref" : `${kind}_ref`;
    }
    function contentUnitTypesForPromptRefKind4(kind) {
      if (kind === "production") return ["production_ref", "timeline_assembly_ref"];
      if (kind === "segment") return ["segment_ref", "timeline_assembly_ref"];
      if (kind === "scene_moment") return ["scence_moment_ref", "scene_moment_ref"];
      if (kind === "expression_unit") return ["expression_unit_ref"];
      return [`${kind}_ref`];
    }
    function primaryRefIdsForContentUnitRecord4(record, kind) {
      switch (kind) {
        case "asset":
          return compactStrings3(record.asset_ref);
        case "keyframe":
          return compactStrings3(record.keyframe_ref);
        case "storyboard":
          return compactStrings3(record.storyboard_ref);
        case "audio_cue":
          return compactStrings3(record.target_kind === "audio_cue" ? record.target_ref : void 0, record.audio_cue_ref);
        case "production":
          return compactStrings3(
            record.production_ref,
            record.target_kind === "production" ? record.target_ref : void 0,
            timelineAssemblyScopeRefFor2(record, "production")
          );
        case "segment":
          return compactStrings3(
            record.segment_ref,
            record.target_kind === "segment" ? record.target_ref : void 0,
            timelineAssemblyScopeRefFor2(record, "segment")
          );
        case "scene_moment":
          return compactStrings3(record.target_kind === "scene_moment" ? record.target_ref : void 0, record.scene_moment_ref, record.scence_moment_ref);
        case "expression_unit":
          return compactStrings3(record.target_kind === "expression_unit" ? record.target_ref : void 0, record.expression_unit_ref);
        case "content_unit":
          return compactStrings3(record.content_unit_ref);
        default:
          return [];
      }
    }
    function outputKindForContentUnitType2(contentUnitType22, explicitOutputKind) {
      const explicit = contentUnitOutputKind4(explicitOutputKind);
      if (explicit !== "metadata") return explicit;
      return contentUnitTargetAdapterFor2(contentUnitType22)?.outputKind ?? "metadata";
    }
    function expectedOutputKindForContentUnitType3(contentUnitType22) {
      return contentUnitTargetAdapterFor2(contentUnitType22)?.outputKind;
    }
    function normalizeContentUnitTarget3(record) {
      const contentUnitType22 = stringField222(record.content_unit_type ?? record.contentUnitType) ?? "";
      const adapter = contentUnitType22 ? contentUnitTargetAdapterFor2(contentUnitType22) : void 0;
      const explicitTargetCategory = stringField222(record.target_category ?? record.targetCategory);
      const explicitTargetKind = stringField222(record.target_kind ?? record.targetKind);
      const explicitTargetRef = idString4(record.target_ref ?? record.targetRef);
      const explicitScopeKind = stringField222(record.scope_kind ?? record.scopeKind);
      const explicitScopeRef = idString4(record.scope_ref ?? record.scopeRef);
      const explicitScope = explicitScopeKind && explicitScopeRef ? { scopeKind: explicitScopeKind, scopeRef: explicitScopeRef } : void 0;
      const diagnostics = [];
      const outputKind = outputKindForContentUnitType2(contentUnitType22, record.output_kind ?? record.outputKind);
      const primaryRefKind = adapter?.primaryRefKind;
      const primaryRefField = adapter?.primaryRefField ?? (primaryRefKind ? primaryRefFieldNameForKind3(primaryRefKind) : void 0);
      const primaryRefs = primaryRefKind ? primaryRefIdsForContentUnitRecord4(record, primaryRefKind) : [];
      if (!adapter) {
        const targetCategory = explicitWorkTargetCategory2(explicitTargetCategory) ?? (explicitTargetKind ? workTargetCategoryForKind3(explicitTargetKind) : void 0);
        if (explicitTargetCategory && isMovScriptNamespaceCategory2(explicitTargetCategory)) {
          diagnostics.push(namespaceTargetDiagnostic2(explicitTargetCategory, "target_category"));
        }
        if (explicitTargetKind && isMovScriptNamespaceKind2(explicitTargetKind)) {
          diagnostics.push(namespaceTargetDiagnostic2(explicitTargetKind, "target_kind"));
        }
        return {
          contentUnitType: contentUnitType22,
          outputKind,
          ...targetCategory && explicitTargetKind ? {
            target: {
              targetCategory,
              targetKind: explicitTargetKind,
              ...explicitTargetRef ? { targetRef: explicitTargetRef } : {}
            }
          } : {},
          primaryRefs,
          diagnostics
        };
      }
      if (adapter.contentUnitType === "timeline_assembly_ref") {
        if (explicitTargetCategory && isMovScriptNamespaceCategory2(explicitTargetCategory)) {
          diagnostics.push(namespaceTargetDiagnostic2(explicitTargetCategory, "target_category"));
        }
        if (explicitTargetKind && isMovScriptNamespaceKind2(explicitTargetKind)) {
          diagnostics.push(namespaceTargetDiagnostic2(explicitTargetKind, "target_kind"));
        }
        if (explicitTargetKind && explicitTargetKind !== "timeline_assembly") {
          diagnostics.push({
            severity: "error",
            code: "content_unit_target_kind_mismatch",
            message: `${contentUnitType22} must target timeline_assembly, not ${explicitTargetKind}`,
            field: "target_kind"
          });
        }
        const targetRef2 = explicitTargetKind === "timeline_assembly" && explicitTargetRef ? explicitTargetRef : explicitScope ? implicitTimelineAssemblyRef2(explicitScope.scopeKind, explicitScope.scopeRef) : void 0;
        const parsedScope = parseImplicitTimelineAssemblyRef2(targetRef2);
        if (!targetRef2) {
          diagnostics.push({
            severity: "error",
            code: "content_unit_target_ref_missing",
            message: `${contentUnitType22} requires target_ref or scope_kind/scope_ref`,
            field: "target_ref"
          });
        } else if (!parsedScope) {
          diagnostics.push({
            severity: "error",
            code: "content_unit_scope_ref_invalid",
            message: `${contentUnitType22} target_ref must use timeline_assembly:<scopeKind>:<scopeRef>`,
            field: "target_ref"
          });
        }
        return {
          contentUnitType: contentUnitType22,
          outputKind,
          target: {
            targetCategory: "timeline_assembly",
            targetKind: "timeline_assembly",
            ...targetRef2 ? { targetRef: targetRef2 } : {}
          },
          primaryRefField: adapter.primaryRefField,
          primaryRefs,
          ...parsedScope ? {
            scope: {
              category: "timeline_namespace",
              kind: parsedScope.scopeKind,
              ref: parsedScope.scopeRef,
              field: explicitTargetRef ? "target_ref" : "scope_ref"
            }
          } : {},
          diagnostics
        };
      }
      if (adapter.legacyNamespaceKind) {
        if (explicitTargetCategory && isMovScriptNamespaceCategory2(explicitTargetCategory)) {
          diagnostics.push(namespaceTargetDiagnostic2(explicitTargetCategory, "target_category"));
        }
        const scopeRef = firstUnique2(primaryRefs) ?? (explicitTargetKind === adapter.legacyNamespaceKind ? explicitTargetRef : void 0);
        if (!scopeRef) {
          diagnostics.push({
            severity: "error",
            code: "content_unit_scope_ref_missing",
            message: `${contentUnitType22} requires ${adapter.primaryRefField}`,
            field: adapter.primaryRefField
          });
        }
        if (explicitTargetKind && explicitTargetKind !== adapter.legacyNamespaceKind && explicitTargetKind !== "timeline_assembly") {
          diagnostics.push({
            severity: "error",
            code: "content_unit_target_kind_mismatch",
            message: `${contentUnitType22} cannot target ${explicitTargetKind}`,
            field: "target_kind"
          });
        }
        const targetRef2 = explicitTargetKind === "timeline_assembly" && explicitTargetRef ? explicitTargetRef : scopeRef ? implicitTimelineAssemblyRef2(adapter.legacyNamespaceKind, scopeRef) : void 0;
        return {
          contentUnitType: contentUnitType22,
          outputKind,
          target: {
            targetCategory: "timeline_assembly",
            targetKind: "timeline_assembly",
            ...targetRef2 ? { targetRef: targetRef2 } : {}
          },
          primaryRefKind,
          primaryRefField,
          primaryRefs,
          ...scopeRef ? {
            scope: {
              category: "timeline_namespace",
              kind: adapter.legacyNamespaceKind,
              ref: scopeRef,
              field: adapter.primaryRefField
            }
          } : {},
          legacyAlias: {
            contentUnitType: contentUnitType22,
            namespaceKind: adapter.legacyNamespaceKind
          },
          diagnostics
        };
      }
      if (explicitTargetCategory && isMovScriptNamespaceCategory2(explicitTargetCategory)) {
        diagnostics.push(namespaceTargetDiagnostic2(explicitTargetCategory, "target_category"));
      }
      if (explicitTargetKind && isMovScriptNamespaceKind2(explicitTargetKind)) {
        diagnostics.push(namespaceTargetDiagnostic2(explicitTargetKind, "target_kind"));
      }
      if (explicitTargetKind && explicitTargetKind !== adapter.targetKind) {
        diagnostics.push({
          severity: "error",
          code: "content_unit_target_kind_mismatch",
          message: `${contentUnitType22} must target ${adapter.targetKind}, not ${explicitTargetKind}`,
          field: "target_kind"
        });
      }
      const targetRef = explicitTargetKind === adapter.targetKind && explicitTargetRef ? explicitTargetRef : firstUnique2(primaryRefs);
      if (!targetRef) {
        diagnostics.push({
          severity: "error",
          code: "content_unit_target_ref_missing",
          message: `${contentUnitType22} requires ${primaryRefField ?? "target_ref"}`,
          field: primaryRefField
        });
      }
      return {
        contentUnitType: contentUnitType22,
        outputKind,
        target: {
          targetCategory: adapter.targetCategory,
          targetKind: adapter.targetKind,
          ...targetRef ? { targetRef } : {}
        },
        primaryRefKind,
        primaryRefField,
        primaryRefs,
        diagnostics
      };
    }
    function timelineAssemblyScopeFromContentUnitRecord2(record) {
      return normalizeContentUnitTarget3(record).scope;
    }
    function contentUnitTargetValidationDiagnostics2(record) {
      const contentUnitType22 = stringField222(record.content_unit_type ?? record.contentUnitType);
      return normalizeContentUnitTarget3(record).diagnostics.filter(
        (diagnostic22) => isContentUnitTargetValidationDiagnostic2(diagnostic22, contentUnitType22)
      );
    }
    function isContentUnitTargetValidationDiagnostic2(diagnostic22, contentUnitType22) {
      if (diagnostic22.code === "content_unit_namespace_target") return true;
      if (diagnostic22.code === "content_unit_target_kind_mismatch") return true;
      if (contentUnitType22 !== "timeline_assembly_ref") return false;
      return diagnostic22.code === "content_unit_target_ref_missing" || diagnostic22.code === "content_unit_scope_ref_invalid";
    }
    function normalizeContentUnitTargetEdges2(input3) {
      const normalized = normalizeContentUnitTarget3(input3.record);
      const origin = normalized.legacyAlias ? "legacy_alias" : "explicit_ref";
      const edges = [];
      if (normalized.target?.targetRef) {
        edges.push({
          source: input3.source,
          target: {
            category: normalized.target.targetCategory,
            kind: normalized.target.targetKind,
            id: normalized.target.targetRef
          },
          relation: "target",
          origin,
          field: contentUnitTargetEdgeField2(input3.record, normalized.primaryRefField, normalized.scope?.field, normalized.target.targetRef)
        });
      }
      if (normalized.scope) {
        edges.push({
          source: input3.source,
          target: input3.scopeTarget?.(normalized.scope) ?? {
            category: "timeline_namespace",
            kind: normalized.scope.kind,
            id: normalized.scope.ref
          },
          relation: "scope",
          origin,
          field: normalized.scope.field
        });
      }
      return edges;
    }
    function implicitTimelineAssemblyRef2(scopeKind, scopeRef) {
      return `timeline_assembly:${scopeKind}:${scopeRef}`;
    }
    function parseImplicitTimelineAssemblyRef2(value) {
      if (!value?.startsWith("timeline_assembly:")) return void 0;
      const [, scopeKind, ...scopeRefParts] = value.split(":");
      const scopeRef = scopeRefParts.join(":");
      if (!scopeKind?.trim() || !scopeRef.trim()) return void 0;
      return { scopeKind: scopeKind.trim(), scopeRef: scopeRef.trim() };
    }
    function workTargetCategoryForKind3(kind) {
      if (kind === "content_unit") return "content_unit";
      if (kind === "timeline_assembly") return "timeline_assembly";
      if (isMovScriptSystemPrimitiveKind2(kind)) return "system_primitive";
      return void 0;
    }
    function explicitWorkTargetCategory2(category) {
      if (category === "content_unit" || category === "timeline_assembly" || category === "system_primitive") return category;
      return void 0;
    }
    function timelineAssemblyScopeRefFor2(record, scopeKind) {
      const explicitScopeKind = stringField222(record.scope_kind ?? record.scopeKind);
      const explicitScopeRef = idString4(record.scope_ref ?? record.scopeRef);
      if (explicitScopeKind === scopeKind && explicitScopeRef) return explicitScopeRef;
      if (record.target_kind !== "timeline_assembly" && record.targetKind !== "timeline_assembly") return void 0;
      const parsedScope = parseImplicitTimelineAssemblyRef2(idString4(record.target_ref ?? record.targetRef));
      return parsedScope?.scopeKind === scopeKind ? parsedScope.scopeRef : void 0;
    }
    function namespaceTargetDiagnostic2(kind, field) {
      return {
        severity: "error",
        code: "content_unit_namespace_target",
        message: `namespace ${kind} cannot be a content unit target; use a system primitive or timeline assembly`,
        field
      };
    }
    function contentUnitTargetEdgeField2(record, primaryRefField, scopeField, targetRef) {
      const explicitTargetRef = stringField222(record.target_ref ?? record.targetRef);
      if (explicitTargetRef === targetRef) return "target_ref";
      return primaryRefField ?? scopeField ?? "target_ref";
    }
    function contentUnitOutputKind4(value) {
      if (value === "image" || value === "video" || value === "audio" || value === "text" || value === "metadata") return value;
      return "metadata";
    }
    function compactStrings3(...values) {
      return values.flatMap((value) => {
        const id = idString4(value);
        return id === void 0 ? [] : [id];
      });
    }
    function idString4(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function stringField222(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function firstUnique2(values) {
      const seen = /* @__PURE__ */ new Set();
      for (const value of values) {
        if (seen.has(value)) continue;
        seen.add(value);
        return value;
      }
      return void 0;
    }
    var MOVSCRIPT_EXPRESSION_UNIT_SLOT_KINDS2 = [
      "visual",
      "voice",
      "subtitle",
      "audio"
    ];
    var SLOT_KIND_SET2 = new Set(MOVSCRIPT_EXPRESSION_UNIT_SLOT_KINDS2);
    function isMovScriptExpressionUnitSlotKind2(value) {
      return typeof value === "string" && SLOT_KIND_SET2.has(value);
    }
    function normalizeExpressionUnitSlotKind2(value, fallback = "visual") {
      const text = normalizedToken2(value);
      if (!text) return fallback;
      if (isMovScriptExpressionUnitSlotKind2(text)) return text;
      if (text === "dialogue" || text === "narration" || text === "voiceover" || text === "voice_over" || text === "speech" || text === "verbal") return "voice";
      if (text === "caption" || text === "caption_card" || text === "text") return "subtitle";
      if (text === "sound" || text === "sound_effect" || text === "sfx" || text === "music" || text === "music_beat" || text === "ambience" || text === "ambient" || text === "foley") return "audio";
      if (text === "shot" || text === "action" || text === "visual_note" || text === "image" || text === "video" || text === "visual") return "visual";
      return fallback;
    }
    function expressionUnitSlotKindFromRecord2(record) {
      if (!record) return "visual";
      return normalizeExpressionUnitSlotKind2(
        record.slot_kind ?? record.slotKind ?? record.expression_kind ?? record.expressionKind ?? record.kind ?? record.role ?? record.modality
      );
    }
    function defaultOutputKindForExpressionUnitSlot2(slotKind) {
      const normalized = normalizeExpressionUnitSlotKind2(slotKind);
      if (normalized === "voice" || normalized === "audio") return "audio";
      if (normalized === "subtitle") return "text";
      return "video";
    }
    function normalizedToken2(value) {
      return String(value ?? "").trim().toLowerCase();
    }
    function normalizePathParentEdge2(child, parent) {
      if (!parent) {
        return {
          diagnostics: [{
            severity: "warning",
            code: "path_parent_missing",
            message: `no path parent found for ${child.kind}`
          }]
        };
      }
      return {
        edge: {
          source: child,
          target: parent,
          relation: "parent",
          origin: "path"
        },
        diagnostics: []
      };
    }
    function entityDir9(path) {
      return path.replace(/\/[^/]+$/, "");
    }
    function nearestParentPath3(path, candidates) {
      const candidateSet = new Set(candidates);
      const parts = path.split("/").filter(Boolean);
      for (let index = parts.length - 1; index > 0; index -= 1) {
        const candidate = parts.slice(0, index).join("/");
        if (candidateSet.has(candidate)) return candidate;
      }
      return void 0;
    }
    function assertExplicitParentRefMatchesPath2(input3) {
      const explicitParentRef = idString22(input3.explicitParentRef);
      if (!explicitParentRef || !input3.pathParent) return [];
      if (domainRefMatches2(input3.pathParent, explicitParentRef)) return [];
      const pathParentLabel = [
        input3.pathParent.kind,
        input3.pathParent.id !== void 0 ? String(input3.pathParent.id) : void 0,
        input3.pathParent.path
      ].filter(Boolean).join(" ");
      return [{
        severity: "error",
        code: "path_parent_ref_conflict",
        field: input3.field,
        ...input3.path ? { path: input3.path } : {},
        message: `${input3.child.kind} ${input3.field} ${explicitParentRef} conflicts with path parent ${pathParentLabel}; source path is the canonical instance parent`
      }];
    }
    function domainRefMatches2(ref, value) {
      const normalizedValue = normalizePathLike2(value);
      if (ref.id !== void 0 && String(ref.id) === value) return true;
      if (ref.path) {
        const normalizedPath = normalizePathLike2(ref.path);
        const normalizedDir = domainRefDir2(normalizedPath);
        if (normalizedPath === normalizedValue) return true;
        if (normalizedDir === normalizedValue) return true;
      }
      const pathTail = ref.path ? domainRefDir2(normalizePathLike2(ref.path)).split("/").filter(Boolean).at(-1) : void 0;
      return pathTail !== void 0 && pathTail === value;
    }
    function idString22(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function normalizePathLike2(value) {
      return value.replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
    }
    function domainRefDir2(path) {
      return path.endsWith(".json") ? entityDir9(path) : path;
    }
    var MOVSCRIPT_DOMAIN_PATH_SEMANTICS2 = {
      structureSource: "source_path",
      vocabularyRole: "labels_templates_and_aliases",
      rules: [
        "The source path is the canonical instance tree: parent/child structure is derived from the actual entity file location.",
        "project.namespace_vocabulary and namespace_kind fields name, template, alias, and project vocabulary; they do not create a second instance tree.",
        "Default path templates are writer hints and legacy compatibility paths. Custom namespace layouts are valid when the entity is placed under the intended parent path and uses the canonical entity filename.",
        "Namespace nodes are organizational scope only. They cannot directly own content-unit refs or become content-unit targets; use timeline_assembly_ref for namespace-scope output."
      ],
      diagnostics: []
    };
    var MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2 = {
      video: ["hook", "proof", "demo", "cta"],
      film: ["act", "sequence", "beat"],
      episode: ["act", "sequence", "beat"],
      lesson: ["segment"],
      custom: []
    };
    var MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATES2 = {
      ...MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2,
      short_video: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.video,
      series: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.episode,
      course: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.lesson
    };
    var MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATE_STRATEGIES2 = {
      video: {
        productionType: "video",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.video,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      },
      film: {
        productionType: "film",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.film,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      },
      episode: {
        productionType: "episode",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.episode,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      },
      lesson: {
        productionType: "lesson",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.lesson,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      },
      custom: {
        productionType: "custom",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.custom,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      },
      short_video: {
        productionType: "video",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.video,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      },
      series: {
        productionType: "episode",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.episode,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      },
      course: {
        productionType: "lesson",
        namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES2.lesson,
        initialNamespaces: ["production"],
        defaultPreviewKind: "production"
      }
    };
    function normalizeNamespaceVocabulary2(input3) {
      const record = isRecord63(input3) ? input3 : {};
      const nested = isRecord63(record.namespace_vocabulary) ? record.namespace_vocabulary : isRecord63(record.namespaceVocabulary) ? record.namespaceVocabulary : {};
      const timelineTemplate = stringField322(
        nested.timeline_template ?? nested.timelineTemplate ?? record.timeline_template ?? record.timelineTemplate
      );
      const templateNamespaces = timelineTemplate && isTimelineNamespaceTemplate2(timelineTemplate) ? [...MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATES2[timelineTemplate]] : [];
      const timelineNamespaces = uniqueStrings2([
        ...templateNamespaces,
        ...stringArray2(nested.timeline_namespaces ?? nested.timelineNamespaces),
        ...stringArray2(record.timeline_namespaces ?? record.timelineNamespaces)
      ]);
      const settingNamespaces = uniqueStrings2([
        ...stringArray2(nested.setting_namespaces ?? nested.settingNamespaces),
        ...stringArray2(record.setting_namespaces ?? record.settingNamespaces)
      ]);
      const diagnostics = [];
      if (timelineTemplate && !isTimelineNamespaceTemplate2(timelineTemplate)) {
        diagnostics.push({
          severity: "warning",
          code: "namespace_template_unknown",
          message: `unknown timeline namespace template: ${timelineTemplate}`,
          field: "timeline_template"
        });
      }
      return {
        timelineNamespaces,
        settingNamespaces,
        ...timelineTemplate ? { timelineTemplate } : {},
        diagnostics
      };
    }
    function isTimelineNamespaceTemplate2(value) {
      return Object.prototype.hasOwnProperty.call(MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATES2, value);
    }
    function stringArray2(value) {
      return Array.isArray(value) ? value.filter((item) => typeof item === "string" && item.trim().length > 0).map((item) => item.trim()) : [];
    }
    function uniqueStrings2(values) {
      const seen = /* @__PURE__ */ new Set();
      const out = [];
      for (const value of values) {
        if (seen.has(value)) continue;
        seen.add(value);
        out.push(value);
      }
      return out;
    }
    function stringField322(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function isRecord63(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function normalizeDomainFocus2(input3) {
      const diagnostics = [];
      const projectId = idString32(input3.projectId ?? input3.project_id);
      const productionId = idString32(input3.productionId ?? input3.production_id);
      const targetCategory = stringField422(input3.targetCategory ?? input3.target_category ?? input3.domainTargetCategory ?? input3.domain_target_category);
      const timelineAssemblyRef = idString32(input3.timelineAssemblyRef ?? input3.timeline_assembly_ref);
      const targetKind = timelineAssemblyRef ? "timeline_assembly" : stringField422(input3.targetKind ?? input3.target_kind ?? input3.domainTargetKind ?? input3.domain_target_kind);
      const targetRef = timelineAssemblyRef ?? idString32(input3.targetRef ?? input3.target_ref ?? input3.domainTargetRef ?? input3.domain_target_ref);
      const entityKind = stringField422(input3.entityKind ?? input3.entity_kind);
      const entityId22 = idString32(input3.entityId ?? input3.entity_id);
      const productionPath = stringField422(input3.productionPath ?? input3.production_path);
      const explicitScopeKind = stringField422(input3.scopeKind ?? input3.scope_kind ?? input3.namespaceKind ?? input3.namespace_kind);
      const explicitScopeRef = idString32(input3.scopeRef ?? input3.scope_ref ?? input3.namespaceRef ?? input3.namespace_ref ?? input3.namespacePath ?? input3.namespace_path);
      const parsedAssemblyScope = targetKind === "timeline_assembly" ? parseImplicitTimelineAssemblyRef2(targetRef) : void 0;
      const scopeKind = explicitScopeKind ?? parsedAssemblyScope?.scopeKind ?? (productionId ? "production" : void 0);
      const scopeRef = explicitScopeRef ?? parsedAssemblyScope?.scopeRef ?? productionPath ?? productionId;
      const scopeField = explicitScopeRef ? "scopeRef" : parsedAssemblyScope ? "targetRef" : productionPath ? "productionPath" : productionId ? "productionId" : void 0;
      if (targetCategory && isMovScriptNamespaceCategory2(targetCategory)) {
        diagnostics.push({
          severity: "error",
          code: "focus_namespace_target",
          message: `namespace category ${targetCategory} cannot be normalized as a work target`,
          field: "targetCategory"
        });
      }
      if (targetKind && isMovScriptNamespaceKind2(targetKind)) {
        diagnostics.push({
          severity: "error",
          code: "focus_namespace_target",
          message: `namespace ${targetKind} cannot be normalized as a work target`,
          field: "targetKind"
        });
      }
      const explicitTargetCategory = workTargetCategoryForCategory2(targetCategory) ?? (targetKind ? workTargetCategoryForKind22(targetKind) : void 0);
      const explicitTarget = explicitTargetCategory && targetKind ? {
        targetCategory: explicitTargetCategory,
        targetKind,
        ...targetRef ? { targetRef } : scopeKind && scopeRef && targetKind === "timeline_assembly" ? { targetRef: implicitTimelineAssemblyRef2(scopeKind, scopeRef) } : {}
      } : void 0;
      const timelineScope = scopeKind && scopeRef ? {
        category: "timeline_namespace",
        kind: scopeKind,
        ref: scopeRef,
        ...scopeField ? { field: scopeField } : {}
      } : void 0;
      return {
        ...projectId ? { projectId } : {},
        target: explicitTarget ?? (timelineScope ? {
          targetCategory: "timeline_assembly",
          targetKind: "timeline_assembly",
          targetRef: implicitTimelineAssemblyRef2(timelineScope.kind, timelineScope.ref)
        } : void 0),
        ...timelineScope ? { scope: timelineScope } : {},
        ...entityKind ? {
          entity: {
            kind: entityKind,
            ...classifyMovScriptEntityKind2(entityKind) ? { category: classifyMovScriptEntityKind2(entityKind) } : {},
            ...entityId22 !== void 0 ? { id: entityId22 } : {},
            ...input3.path ? { path: input3.path } : {}
          }
        } : {},
        diagnostics
      };
    }
    function workTargetCategoryForKind22(kind) {
      if (kind === "content_unit") return "content_unit";
      if (kind === "timeline_assembly") return "timeline_assembly";
      if (isMovScriptSystemPrimitiveKind2(kind)) return "system_primitive";
      return void 0;
    }
    function workTargetCategoryForCategory2(category) {
      if (category === "content_unit" || category === "timeline_assembly" || category === "system_primitive") return category;
      return void 0;
    }
    function stringField422(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function idString32(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    var MOVSCRIPT_NAMESPACE_FORBIDDEN_CONTENT_UNIT_FIELDS2 = [
      "content_unit_ref",
      "contentUnitRef",
      "content_unit_refs",
      "contentUnitRefs",
      "main_content_unit_id",
      "mainContentUnitId"
    ];
    var MOVSCRIPT_NAMESPACE_FORBIDDEN_PRODUCTION_STATE_FIELDS2 = [
      "candidate",
      "candidates",
      "candidate_ref",
      "candidateRef",
      "candidate_refs",
      "candidateRefs",
      "selection",
      "selections",
      "selected_candidate_id",
      "selectedCandidateId",
      "selected_candidate_ref",
      "selectedCandidateRef",
      "selected_resource_id",
      "selectedResourceId",
      "selected_resource_ref",
      "selectedResourceRef",
      "resource_id",
      "resourceId"
    ];
    function assertNamespaceCannotOwnContentUnitRef2(input3) {
      const record = input3.record ?? {};
      const namespaceKind = namespaceKindForSourceEntity2(input3, record);
      if (!namespaceKind) return [];
      const diagnostics = [];
      for (const field of MOVSCRIPT_NAMESPACE_FORBIDDEN_CONTENT_UNIT_FIELDS2) {
        if (!Object.prototype.hasOwnProperty.call(record, field)) continue;
        diagnostics.push({
          severity: "error",
          code: "namespace_content_unit_ref_forbidden",
          message: `namespace ${namespaceKind} must not own content unit ref field ${field}; create a content_unit targeting a system primitive or timeline assembly instead`,
          field,
          ...input3.path ? { path: input3.path } : {}
        });
      }
      return diagnostics;
    }
    function assertNamespaceCannotOwnProductionState2(input3) {
      const record = input3.record ?? {};
      const namespaceKind = namespaceKindForSourceEntity2(input3, record);
      if (!namespaceKind) return [];
      const diagnostics = [];
      for (const field of MOVSCRIPT_NAMESPACE_FORBIDDEN_PRODUCTION_STATE_FIELDS2) {
        if (!Object.prototype.hasOwnProperty.call(record, field)) continue;
        diagnostics.push({
          severity: "error",
          code: "namespace_production_state_forbidden",
          message: `namespace ${namespaceKind} must not own production state field ${field}; create/select candidates through a content_unit targeting a system primitive or timeline assembly instead`,
          field,
          ...input3.path ? { path: input3.path } : {}
        });
      }
      return diagnostics;
    }
    function namespaceKindForSourceEntity2(input3, record) {
      const entityKind = input3.entityKind ?? stringField53(record.kind);
      const category = input3.category ?? (entityKind ? classifyMovScriptEntityKind2(entityKind) : void 0);
      if (!isMovScriptNamespaceCategory2(category)) return void 0;
      return entityKind ? projectMovScriptDomainNodeKind2(entityKind, record) : category;
    }
    function stringField53(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    var ENTITY_KIND_FALLBACK_PREFIXES2 = {
      audio_cue: "audio",
      content_unit: "cu",
      expression_unit: "expression",
      keyframe: "keyframe",
      production: "production",
      scene_moment: "scene",
      segment: "segment",
      setting: "setting",
      setting_state: "state",
      state: "state",
      storyboard: "storyboard",
      asset: "asset"
    };
    function suggestMovScriptEntityId2(input3) {
      const fallbackPrefix = normalizedEntityIdToken2(
        input3.fallbackPrefix ?? (input3.entityKind ? ENTITY_KIND_FALLBACK_PREFIXES2[input3.entityKind] : void 0) ?? input3.entityKind ?? "item"
      ) || "item";
      const title = stringField62(input3.title);
      const titleToken = title ? normalizedEntityIdToken2(title) : void 0;
      if (titleToken) return titleToken;
      if (title) return `${fallbackPrefix}_${stableShortHash2(title)}`;
      return fallbackPrefix;
    }
    function allocateMovScriptEntityId2(input3) {
      const base = suggestMovScriptEntityId2(input3);
      const existingIds = new Set(
        [...input3.existingIds ?? []].map((id) => stringField62(id)?.toLowerCase()).filter((id) => Boolean(id))
      );
      if (!existingIds.has(base.toLowerCase())) return base;
      for (let index = 2; index < 1e4; index += 1) {
        const candidate = `${base}_${index}`;
        if (!existingIds.has(candidate.toLowerCase())) return candidate;
      }
      return `${base}_${stableShortHash2([...existingIds].join("\n"))}`;
    }
    function normalizedEntityIdToken2(value) {
      const text = stringField62(value);
      if (!text) return "";
      return text.normalize("NFKD").replace(/[\u0300-\u036f]/g, "").replace(/['’]/g, "").replace(/&/g, " and ").toLowerCase().replace(/[^a-z0-9_-]+/g, "_").replace(/_+/g, "_").replace(/^_+|_+$/g, "");
    }
    function stableShortHash2(value) {
      let hash = 2166136261;
      for (let index = 0; index < value.length; index += 1) {
        hash ^= value.charCodeAt(index);
        hash = Math.imul(hash, 16777619);
      }
      return (hash >>> 0).toString(36).slice(0, 6);
    }
    function stringField62(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      if (typeof value !== "string") return void 0;
      const text = value.trim();
      return text || void 0;
    }
    var WORKSPACE_ENTITY_KIND2 = {
      project_workspace: "project",
      project_standards_workspace: "project_standards",
      setting_workspace: "setting",
      setting_state_workspace: "setting_state",
      asset_workspace: "asset",
      script_workspace: "script",
      script_version_workspace: "script_version",
      script_block_workspace: "script_block",
      production_workspace: "production",
      segment_workspace: "segment",
      scene_moment_workspace: "scene_moment",
      storyboard_workspace: "storyboard",
      audio_cue_workspace: "audio_cue",
      expression_unit_workspace: "expression_unit",
      content_unit_workspace: "content_unit",
      keyframe_workspace: "keyframe"
    };
    var MOVSCRIPT_DOMAIN_WORKSPACE_MODELS2 = {
      project_workspace: {
        kind: "project_workspace",
        title: "Project workspace",
        entityKinds: ["project"],
        editablePathPatterns: ["project.json"],
        contextPathPatterns: [],
        schemaIds: ["movscript.project.v1"],
        instructions: ["Edit the project root entity in project.json. Interpret validates the source tree and writes .derived artifacts."]
      },
      project_standards_workspace: {
        kind: "project_standards_workspace",
        title: "Project standards workspace",
        entityKinds: ["project_standards"],
        editablePathPatterns: ["project_standards.json"],
        contextPathPatterns: ["project.json"],
        schemaIds: ["movscript.project_standards.v1"],
        instructions: ["Edit project-wide creative standards in project_standards.json. Interpret uses these standards when deriving generation context."]
      },
      setting_workspace: {
        kind: "setting_workspace",
        title: "Setting workspace",
        entityKinds: ["setting"],
        editablePathPatterns: ["settings/{settingSlug}/setting.json"],
        contextPathPatterns: ["project.json", "project_standards.json"],
        schemaIds: ["movscript.setting.v1"],
        instructions: ["Use setting workspaces for concrete film/music production entities to be made or reused, such as characters, props, places, instruments, costumes, or voice identities. Do not use settings for abstract styles or rules; put project-wide style/rules in project standards. Child setting namespaces/states are organized by their actual source path; namespace_kind only labels that path level. Assets belong under setting states."]
      },
      setting_state_workspace: {
        kind: "setting_state_workspace",
        title: "Setting state workspace",
        entityKinds: ["setting_state"],
        editablePathPatterns: ["settings/{settingSlug}/states/{settingStateSlug}/setting_state.json"],
        contextPathPatterns: ["settings/{settingSlug}/setting.json", "project_standards.json"],
        schemaIds: ["movscript.setting_state.v1"],
        instructions: ["Use setting state workspaces as namespaces under a setting for named conditions or versions of the same entity, such as base look, wet hair, damaged prop, side-view variant, or calm voice. The default states/ path is a legacy hint; custom setting namespace paths are valid when setting_state.json sits under the intended setting parent."]
      },
      asset_workspace: {
        kind: "asset_workspace",
        title: "Asset workspace",
        entityKinds: ["asset"],
        editablePathPatterns: [
          "settings/{settingSlug}/states/{settingStateSlug}/assets/{assetSlug}/asset.json"
        ],
        contextPathPatterns: ["settings/{settingSlug}/setting.json", "settings/{settingSlug}/states/{settingStateSlug}/setting_state.json", "project_standards.json"],
        schemaIds: ["movscript.asset.v1"],
        instructions: ["Assets are setting-state-owned resource slots and carriers for asset_ref content-unit generation/review. The owning setting state is the nearest setting_state ancestor in the source path, not a fixed states/ directory. Do not write new candidates or selections into asset.json; generated asset candidates belong to the linked asset_ref content unit and backend content-unit decision flow. Existing inline candidates/selection in asset.json are legacy compatibility data only. Downstream content-unit prompts can reference selected asset outputs with {{asset::id}} when dependency tracking matters; loose raw-resource guidance can use direct resource inputs. Image assets should prefer plain white or very clean backgrounds."]
      },
      script_workspace: {
        kind: "script_workspace",
        title: "Script workspace",
        entityKinds: ["script"],
        editablePathPatterns: ["scripts/{scriptSlug}/script.json", "scripts/{scriptSlug}/script.md"],
        contextPathPatterns: ["project.json"],
        schemaIds: ["movscript.script.v1"],
        instructions: ["Use script workspaces for screenplay roots and source text references."]
      },
      script_version_workspace: {
        kind: "script_version_workspace",
        title: "Script version workspace",
        entityKinds: ["script_version"],
        editablePathPatterns: ["scripts/{scriptSlug}/versions/{scriptVersionSlug}/script_version.json"],
        contextPathPatterns: ["scripts/{scriptSlug}/script.json"],
        schemaIds: ["movscript.script_version.v1"],
        instructions: ["Use script version workspaces for versioned script snapshots and block grouping."]
      },
      script_block_workspace: {
        kind: "script_block_workspace",
        title: "Script block workspace",
        entityKinds: ["script_block"],
        editablePathPatterns: ["scripts/{scriptSlug}/versions/{scriptVersionSlug}/blocks/{scriptBlockSlug}/script_block.json"],
        contextPathPatterns: ["scripts/{scriptSlug}/versions/{scriptVersionSlug}/script_version.json"],
        schemaIds: ["movscript.script_block.v1"],
        instructions: ["Use script block workspaces for addressable text blocks referenced by planning entities."]
      },
      production_workspace: {
        kind: "production_workspace",
        title: "Production workspace",
        entityKinds: ["production"],
        editablePathPatterns: ["productions/{productionSlug}/production.json"],
        contextPathPatterns: ["project.json", "project_standards.json", "settings/**", "scripts/**"],
        schemaIds: ["movscript.production.v1"],
        instructions: ["Production records are legacy/default timeline namespace nodes. Edit existing custom timeline namespace nodes in place; for new custom layouts, put production.json under the intended timeline path and set namespace_kind to the user-facing namespace label. Segment and scene moment children hold planning structure through path ancestry."]
      },
      segment_workspace: {
        kind: "segment_workspace",
        title: "Segment workspace",
        entityKinds: ["segment"],
        editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/segment.json"],
        contextPathPatterns: ["productions/{productionSlug}/production.json"],
        schemaIds: ["movscript.segment.v1"],
        instructions: ["Segment records are legacy/default timeline namespace nodes for rhythm sections. Edit existing custom namespace nodes in place; for new custom layouts, put segment.json under the intended timeline parent path and set namespace_kind to the user-facing label. Order lives in segment.json, not the directory name."]
      },
      scene_moment_workspace: {
        kind: "scene_moment_workspace",
        title: "Scene moment workspace",
        entityKinds: ["scene_moment"],
        editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/scene_moment.json"],
        contextPathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/segment.json", "settings/**"],
        schemaIds: ["movscript.scene_moment.v1"],
        instructions: ["Scene moments are stable production primitives and should remain under the intended timeline namespace path. They describe planning context and their own transition boundaries. Shot semantics live as expression_unit records with kind=shot; storyboards and keyframes may be direct scene moment children when they are scene-scoped."]
      },
      storyboard_workspace: {
        kind: "storyboard_workspace",
        title: "Storyboard workspace",
        entityKinds: ["storyboard"],
        editablePathPatterns: [
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/{expressionUnitSlug}/storyboards/{storyboardSlug}/storyboard.json",
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/storyboards/{storyboardSlug}/storyboard.json"
        ],
        contextPathPatterns: ["productions/**", "settings/**", "project_standards.json"],
        schemaIds: ["movscript.storyboard.v1"],
        instructions: ["Storyboards are production primitives under expression_unit(kind=shot) or directly under scene_moment. Runtime candidates and production decisions are stored outside storyboard.json. Downstream prompts can reference selected storyboard outputs with {{storyboard::id}} when dependency tracking matters; loose raw-resource guidance can use direct resource inputs."]
      },
      audio_cue_workspace: {
        kind: "audio_cue_workspace",
        title: "Audio cue workspace",
        entityKinds: ["audio_cue"],
        editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/audio_cues/{audioCueSlug}/audio_cue.json"],
        contextPathPatterns: [
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/scene_moment.json",
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/**",
          "settings/**",
          "project_standards.json"
        ],
        schemaIds: ["movscript.audio_cue.v1"],
        instructions: ["Audio cues are production primitives for sound, music, ambience, dialogue, or foley planning, normally parented by source path under a scene moment and optionally attached by refs to scene moments, storyboards, or shot plans."]
      },
      expression_unit_workspace: {
        kind: "expression_unit_workspace",
        title: "Expression unit workspace",
        entityKinds: ["expression_unit"],
        editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/{expressionUnitSlug}/expression_unit.json"],
        contextPathPatterns: [
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/scene_moment.json",
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/**"
        ],
        schemaIds: ["movscript.expression_unit.v1"],
        instructions: ["Expression units are scene-moment-owned form/edit slots. Their owning scene moment is the nearest scene_moment ancestor in the source path. Use slot_kind=visual|voice|subtitle|audio for new records; expression_kind/kind are legacy compatibility hints. Content units target expression_unit_ref and declare output_kind."]
      },
      content_unit_workspace: {
        kind: "content_unit_workspace",
        title: "Content unit workspace",
        entityKinds: ["content_unit"],
        editablePathPatterns: ["content_units/{contentUnitSlug}/content_unit.json"],
        contextPathPatterns: ["project_standards.json", "settings/**", "productions/**"],
        schemaIds: ["movscript.content_unit.v1"],
        instructions: ["Content units are project-level stable production units. They declare content_unit_type, output_kind, flat business refs, edit_prompt, and model_intent. Specialized content_unit_type adapters perform dependency and regeneration checks; unknown types are valid but untracked for regeneration. Use expression_unit_ref for shot expression units, keyframe_ref/storyboard_ref for visual anchors, audio_cue_ref for sound cues, and timeline_assembly_ref for namespace-scope assembly. In edit_prompt, use semantic refs such as {{asset::id}}, {{storyboard::id}}, {{keyframe::id}}, {{audio_cue::id}}, {{candidate::id}}, or {{resource::123}}; prompt compilation resolves selected backend candidates into resource mentions and resource_ids for generation."]
      },
      keyframe_workspace: {
        kind: "keyframe_workspace",
        title: "Keyframe workspace",
        entityKinds: ["keyframe"],
        editablePathPatterns: [
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/{expressionUnitSlug}/keyframes/{keyframeSlug}/keyframe.json",
          "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/keyframes/{keyframeSlug}/keyframe.json"
        ],
        contextPathPatterns: ["settings/**", "project_standards.json", "productions/**"],
        schemaIds: ["movscript.keyframe.v1"],
        instructions: ["Keyframes are production primitives and visual anchors under expression_unit(kind=shot) or directly under scene_moment. Content units reference keyframes through keyframe_ref or keyframe_refs."]
      }
    };
    var ENTITY_WORKSPACE_KIND2 = Object.entries(WORKSPACE_ENTITY_KIND2).reduce((out, [workspaceKind, entityKind]) => {
      out[entityKind] = workspaceKind;
      return out;
    }, {});
    function getMovScriptDomainWorkspaceModel2(kind) {
      const model = MOVSCRIPT_DOMAIN_WORKSPACE_MODELS2[kind];
      if (!model) throw new Error(`Unsupported MovScript workspace kind: ${kind}`);
      return model;
    }
    function resolveMovScriptDomainWorkspaceKindForEntity2(entityKind) {
      const normalized = normalizeEntityKind2(entityKind);
      return isSemanticEntityKind3(normalized) ? ENTITY_WORKSPACE_KIND2[normalized] : void 0;
    }
    function getMovScriptWorkspaceModel2(input3) {
      const entityKind = normalizeEntityKind2(input3.entityKind);
      if (!isSemanticEntityKind3(entityKind)) throw new Error(`Unsupported MovScript workspace entity kind: ${input3.entityKind}`);
      const workspaceKind = resolveMovScriptDomainWorkspaceKindForEntity2(entityKind);
      if (!workspaceKind) throw new Error(`Unsupported MovScript workspace entity kind: ${input3.entityKind}`);
      const model = getMovScriptDomainWorkspaceModel2(workspaceKind);
      return {
        workspaceKind,
        entityKind,
        ...input3.entityId !== void 0 ? { entityId: input3.entityId } : {},
        editablePaths: expandPathPatterns2(model.editablePathPatterns, input3),
        contextPaths: model.contextPathPatterns,
        pathSemantics: MOVSCRIPT_DOMAIN_PATH_SEMANTICS2,
        schemaIds: model.schemaIds,
        instructions: model.instructions
      };
    }
    function expandPathPatterns2(patterns, input3) {
      const slug = input3.entityId === void 0 ? void 0 : entityPathSlug2(input3.entityId, normalizeEntityKind2(input3.entityKind));
      return patterns.map((pattern) => pattern.replaceAll("{id}", slug ?? "{id}").replaceAll(`{${camelEntityKind3(input3.entityKind)}Slug}`, slug ?? `{${camelEntityKind3(input3.entityKind)}Slug}`));
    }
    function camelEntityKind3(entityKind) {
      return normalizeEntityKind2(entityKind).replace(/_([a-z])/g, (_match, char) => char.toUpperCase());
    }
    function normalizeEntityKind2(entityKind) {
      return entityKind.trim().replace(/^movscript\./, "").replace(/\.v\d+$/, "");
    }
    function isSemanticEntityKind3(value) {
      return Object.values(WORKSPACE_ENTITY_KIND2).some((kind) => kind === value);
    }
    function deriveMovScriptWorkspaceDomainIndex2(documents) {
      const entities = documents.flatMap((document) => indexedEntitiesFromDocument2(document));
      const byKind = /* @__PURE__ */ new Map();
      for (const entity of entities) {
        byKind.set(entity.entityKind, [...byKind.get(entity.entityKind) ?? [], entity]);
      }
      const namespaceVocabulary = normalizeNamespaceVocabulary2(byKind.get("project")?.[0]?.record);
      const domainNodes = deriveMovScriptDomainNodes2(entities);
      const domainEdges = deriveMovScriptDomainEdges2(entities, domainNodes);
      return { documents, entities, byKind, namespaceVocabulary, domainNodes, domainEdges };
    }
    function queryMovScriptWorkspaceEntities2(index, query = {}) {
      const source = query.entityKind ? index.byKind.get(query.entityKind) ?? [] : index.entities;
      const queryContext = createEntityQueryContext2(index);
      const out = source.filter((entity) => {
        const record = entity.record;
        if (query.kind && !entityKindMatches2(entity, query.kind)) return false;
        if (query.productionId !== void 0 && !entityMatchesScope2(entity, "production", query.productionId, queryContext)) return false;
        if (query.segmentId !== void 0 && !entityMatchesScope2(entity, "segment", query.segmentId, queryContext)) return false;
        if (query.sceneMomentId !== void 0 && !entityMatchesScope2(entity, "scene_moment", query.sceneMomentId, queryContext)) return false;
        if (query.storyboardId !== void 0 && !entityMatchesStoryboard2(entity, query.storyboardId, queryContext)) return false;
        if (query.contentUnitId !== void 0 && !entityPathMatchesContentUnit2(entity.path, query.contentUnitId)) return false;
        if (query.settingId !== void 0 && !entityMatchesScope2(entity, "setting", query.settingId, queryContext)) return false;
        if (query.settingStateId !== void 0 && !entityMatchesScope2(entity, "setting_state", query.settingStateId, queryContext)) return false;
        if (query.query && !recordMatchesQuery2(record, query.query)) return false;
        return !isDeletedWorkspaceRecord2(record);
      });
      return limitEntities2(out, query.limit);
    }
    function queryMovScriptWorkspaceSettings2(index, query = {}) {
      const references = queryMovScriptWorkspaceEntities2(index, {
        entityKind: "setting",
        kind: query.kind,
        query: query.query,
        limit: query.limit
      });
      if (query.settingId === void 0) return references;
      return references.filter((entity) => sameId8(entity.id, query.settingId));
    }
    function queryMovScriptWorkspaceAssets2(index, query = {}) {
      const assets = queryMovScriptWorkspaceEntities2(index, {
        entityKind: "asset",
        query: query.query,
        settingId: query.settingId,
        settingStateId: query.settingStateId,
        limit: query.limit
      }).filter((entity) => query.assetId === void 0 || sameId8(entity.id, query.assetId));
      if (!query.includeCandidates) return { assets };
      const candidates = assets.flatMap((entity) => {
        const rows = entity.record.candidates;
        return Array.isArray(rows) ? rows.filter(isRecord73) : [];
      });
      return { assets, candidates };
    }
    function queryMovScriptWorkspaceProductionContext2(index, query = {}) {
      const include = new Set(query.include ?? ["productions", "segments", "scene_moments", "storyboards", "audio_cues", "expression_units", "content_units", "keyframes"]);
      const result = {};
      if (include.has("productions")) {
        result.productions = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "production",
          query: query.query,
          limit: query.limit
        }).filter((entity) => query.productionId === void 0 || sameId8(entity.id, query.productionId));
      }
      if (include.has("segments")) {
        result.segments = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "segment",
          productionId: query.productionId,
          query: query.query,
          limit: query.limit
        }).filter((entity) => query.segmentId === void 0 || sameId8(entity.id, query.segmentId));
      }
      if (include.has("scene_moments")) {
        result.scene_moments = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "scene_moment",
          productionId: query.productionId,
          segmentId: query.segmentId,
          query: query.query,
          limit: query.limit
        }).filter((entity) => query.sceneMomentId === void 0 || sameId8(entity.id, query.sceneMomentId));
      }
      if (include.has("storyboards")) {
        result.storyboards = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "storyboard",
          productionId: query.productionId,
          segmentId: query.segmentId,
          sceneMomentId: query.sceneMomentId,
          query: query.query,
          limit: query.limit
        }).filter((entity) => query.storyboardId === void 0 || sameId8(entity.id, query.storyboardId));
      }
      if (include.has("audio_cues")) {
        result.audio_cues = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "audio_cue",
          productionId: query.productionId,
          segmentId: query.segmentId,
          sceneMomentId: query.sceneMomentId,
          storyboardId: query.storyboardId,
          query: query.query,
          limit: query.limit
        });
      }
      if (include.has("expression_units")) {
        result.expression_units = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "expression_unit",
          productionId: query.productionId,
          segmentId: query.segmentId,
          sceneMomentId: query.sceneMomentId,
          query: query.query,
          limit: query.limit
        });
      }
      if (include.has("content_units")) {
        result.content_units = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "content_unit",
          contentUnitId: query.contentUnitId,
          query: query.query,
          limit: query.limit
        });
      }
      if (include.has("keyframes")) {
        result.keyframes = queryMovScriptWorkspaceEntities2(index, {
          entityKind: "keyframe",
          sceneMomentId: query.sceneMomentId,
          contentUnitId: query.contentUnitId,
          query: query.query,
          limit: query.limit
        });
      }
      return result;
    }
    function indexedEntitiesFromDocument2(document) {
      const path = normalizeIndexedDocumentPath2(document.path);
      if (!isRecord73(document.data)) return [];
      const entityKind = entityKindFromSchema2(stringField72(document.data.schema)) ?? entityKindFromPath3(path);
      return entityKind ? [indexedEntity2(entityKind, workspaceRecordWithDocumentMetadata2(document.data, document), path, 0)] : [];
    }
    function indexedEntity2(entityKind, record, path, index) {
      const schema = stringField72(record.schema);
      const id = entityIdField2(entityKind, record);
      const clientId = stringField72(record.client_id ?? record.clientId);
      return {
        entityKind,
        record,
        path,
        index,
        ...id !== void 0 ? { id } : {},
        ...clientId ? { clientId } : {},
        ...schema ? { schema } : {}
      };
    }
    function deriveMovScriptDomainNodes2(entities) {
      return entities.flatMap((entity) => {
        const category = classifyMovScriptEntityKind2(entity.entityKind);
        if (!category) return [];
        return [pruneUndefined17({
          category,
          kind: projectMovScriptDomainNodeKind2(entity.entityKind, entity.record),
          ...entity.id !== void 0 ? { id: entity.id } : {},
          path: entity.path,
          title: stringField72(entity.record.title) ?? stringField72(entity.record.label),
          order: finiteNumber4(entity.record.order),
          metadata: {
            entityKind: entity.entityKind
          }
        })];
      });
    }
    function deriveMovScriptDomainPathParentEdges2(nodes) {
      const nodeByDir = /* @__PURE__ */ new Map();
      for (const node of nodes) {
        if (!node.path) continue;
        nodeByDir.set(entityDir9(node.path), node);
      }
      const edges = [];
      for (const node of nodes) {
        if (!node.path) continue;
        const parentPath = nearestParentPath3(entityDir9(node.path), nodeByDir.keys());
        if (!parentPath) continue;
        const parent = nodeByDir.get(parentPath);
        const normalized = normalizePathParentEdge2(domainRefFromNode3(node), parent ? domainRefFromNode3(parent) : void 0);
        if (normalized.edge) edges.push(normalized.edge);
      }
      return dedupeDomainEdges3(edges);
    }
    function deriveMovScriptDomainEdges2(entities, nodes) {
      return dedupeDomainEdges3([
        ...deriveMovScriptDomainPathParentEdges2(nodes),
        ...deriveMovScriptContentUnitTargetEdges2(entities, nodes)
      ]);
    }
    function deriveMovScriptContentUnitTargetEdges2(entities, nodes) {
      const nodeByPath = new Map(nodes.flatMap((node) => node.path ? [[node.path, node]] : []));
      const timelineNamespaceNodes = nodes.filter((node) => node.category === "timeline_namespace");
      const edges = [];
      for (const entity of entities) {
        if (entity.entityKind !== "content_unit") continue;
        const sourceNode = nodeByPath.get(entity.path);
        if (!sourceNode) continue;
        edges.push(...normalizeContentUnitTargetEdges2({
          source: domainRefFromNode3(sourceNode),
          record: entity.record,
          scopeTarget(scope) {
            return domainRefFromTimelineScope4(scope.kind, scope.ref, timelineNamespaceNodes);
          }
        }));
      }
      return edges;
    }
    function domainRefFromTimelineScope4(scopeKind, scopeRef, timelineNamespaceNodes) {
      const node = timelineNamespaceNodes.find(
        (candidate) => String(candidate.id ?? "") === scopeRef && (candidate.kind === scopeKind || candidate.metadata?.entityKind === scopeKind)
      );
      return node ? domainRefFromNode3(node) : { category: "timeline_namespace", kind: scopeKind, id: scopeRef };
    }
    function domainRefFromNode3(node) {
      return {
        category: node.category,
        kind: node.kind,
        ...node.id !== void 0 ? { id: node.id } : {},
        ...node.path ? { path: node.path } : {}
      };
    }
    function workspaceRecordWithDocumentMetadata2(record, document) {
      return {
        ...record,
        __workspace_path: document.path,
        ...document.version !== void 0 ? { __workspace_version: document.version } : {},
        ...document.updatedAt !== void 0 ? { __workspace_updated_at: document.updatedAt } : {}
      };
    }
    function entityKindFromSchema2(schema) {
      if (!schema) return void 0;
      const normalized = schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "");
      return schemaEntityKinds2[normalized];
    }
    function entityKindFromPath3(path) {
      const name = path.split("/").pop() ?? path;
      if (name === "project.json") return "project";
      if (name === "project_standards.json") return "project_standards";
      if (name === "script.meta.json" || name === "script.json") return "script";
      if (name === "setting.json") return "setting";
      if (name === "setting_state.json") return "setting_state";
      if (name === "asset.json") return "asset";
      if (name === "script_version.json") return "script_version";
      if (name === "script_block.json") return "script_block";
      if (name === "production.json") return "production";
      if (name === "segment.json") return "segment";
      if (name === "scene_moment.json") return "scene_moment";
      if (name === "storyboard.json") return "storyboard";
      if (name === "audio_cue.json") return "audio_cue";
      if (name === "expression_unit.json") return "expression_unit";
      if (name === "content_unit.json") return "content_unit";
      if (name === "keyframe.json") return "keyframe";
      return void 0;
    }
    var schemaEntityKinds2 = {
      project: "project",
      project_standards: "project_standards",
      script: "script",
      script_version: "script_version",
      script_block: "script_block",
      production: "production",
      segment: "segment",
      scene_moment: "scene_moment",
      storyboard: "storyboard",
      audio_cue: "audio_cue",
      expression_unit: "expression_unit",
      content_unit: "content_unit",
      keyframe: "keyframe",
      setting: "setting",
      setting_state: "setting_state",
      asset: "asset"
    };
    function entityPathMatchesProduction2(path, productionId) {
      return pathSegmentAfter11(path, "productions") !== void 0 && sameEntityRef3(pathSegmentAfter11(path, "productions"), productionId, "production");
    }
    function entityPathMatchesSegment2(path, segmentId) {
      return pathSegmentAfter11(path, "segments") !== void 0 && sameEntityRef3(pathSegmentAfter11(path, "segments"), segmentId, "segment");
    }
    function entityPathMatchesSceneMoment2(path, sceneMomentId) {
      return pathSegmentAfter11(path, "scene_moments") !== void 0 && sameEntityRef3(pathSegmentAfter11(path, "scene_moments"), sceneMomentId, "scene_moment");
    }
    function entityPathMatchesStoryboard2(path, storyboardId) {
      return pathSegmentAfter11(path, "storyboards") !== void 0 && sameEntityRef3(pathSegmentAfter11(path, "storyboards"), storyboardId, "storyboard");
    }
    function entityMatchesStoryboard2(entity, storyboardId, context) {
      if (entityMatchesScope2(entity, "storyboard", storyboardId, context)) return true;
      if (entityPathMatchesStoryboard2(entity.path, storyboardId)) return true;
      const storyboardRef = stringField72(entity.record.storyboard_ref);
      return storyboardRef !== void 0 && (entityPathMatchesStoryboard2(storyboardRef, storyboardId) || sameNormalizedEntityPath2(storyboardRef, storyboardId, "storyboard"));
    }
    function entityPathMatchesContentUnit2(path, contentUnitId) {
      return pathSegmentAfter11(path, "content_units") !== void 0 && sameEntityRef3(pathSegmentAfter11(path, "content_units"), contentUnitId, "content_unit");
    }
    function entityPathMatchesSetting2(path, settingId) {
      return pathSegmentAfter11(path, "settings") !== void 0 && sameEntityRef3(pathSegmentAfter11(path, "settings"), settingId, "setting");
    }
    function entityPathMatchesSettingState2(path, stateId) {
      return pathSegmentAfter11(path, "states") !== void 0 && sameEntityRef3(pathSegmentAfter11(path, "states"), stateId, "setting_state");
    }
    function createEntityQueryContext2(index) {
      const entityByPath = new Map(index.entities.map((entity) => [entity.path, entity]));
      const entityByDir = new Map(index.entities.map((entity) => [entityDir9(entity.path), entity]));
      const parentPathByPath = /* @__PURE__ */ new Map();
      for (const edge of index.domainEdges) {
        if (edge.relation !== "parent" || edge.origin !== "path") continue;
        if (!edge.source.path || !edge.target.path) continue;
        parentPathByPath.set(edge.source.path, edge.target.path);
      }
      for (const entity of index.entities) {
        if (parentPathByPath.has(entity.path)) continue;
        const parentDir = nearestParentPath3(entityDir9(entity.path), entityByDir.keys());
        const parent = parentDir ? entityByDir.get(parentDir) : void 0;
        if (parent) parentPathByPath.set(entity.path, parent.path);
      }
      return { entityByPath, parentPathByPath };
    }
    function entityMatchesScope2(entity, scopeKind, scopeRef, context) {
      if (entityRefMatchesScope2(entity, scopeKind, scopeRef)) return true;
      for (const ancestor of ancestorEntities2(entity, context)) {
        if (entityRefMatchesScope2(ancestor, scopeKind, scopeRef)) return true;
      }
      return legacyEntityPathMatchesScope2(entity.path, scopeKind, scopeRef);
    }
    function ancestorEntities2(entity, context) {
      const ancestors = [];
      const seen = /* @__PURE__ */ new Set([entity.path]);
      let currentPath = entity.path;
      while (true) {
        const parentPath = context.parentPathByPath.get(currentPath);
        if (!parentPath || seen.has(parentPath)) break;
        seen.add(parentPath);
        const parent = context.entityByPath.get(parentPath);
        if (!parent) break;
        ancestors.push(parent);
        currentPath = parent.path;
      }
      return ancestors;
    }
    function entityRefMatchesScope2(entity, scopeKind, scopeRef) {
      if (entity.entityKind !== scopeKind) return false;
      if (entity.id !== void 0 && sameEntityRef3(entity.id, scopeRef, scopeKind)) return true;
      return sameNormalizedEntityPath2(entity.path, scopeRef, scopeKind) || sameNormalizedEntityPath2(entityDir9(entity.path), scopeRef, scopeKind);
    }
    function legacyEntityPathMatchesScope2(path, scopeKind, scopeRef) {
      if (scopeKind === "production") return entityPathMatchesProduction2(path, scopeRef);
      if (scopeKind === "segment") return entityPathMatchesSegment2(path, scopeRef);
      if (scopeKind === "scene_moment") return entityPathMatchesSceneMoment2(path, scopeRef);
      if (scopeKind === "storyboard") return entityPathMatchesStoryboard2(path, scopeRef);
      if (scopeKind === "setting") return entityPathMatchesSetting2(path, scopeRef);
      if (scopeKind === "setting_state") return entityPathMatchesSettingState2(path, scopeRef);
      return false;
    }
    function recordMatchesQuery2(record, query) {
      const needle = query.trim().toLowerCase();
      if (!needle) return true;
      return ["id", "title", "label", "alias", "description", "content", "summary", "prompt", "notes"].some((key) => String(record[key] ?? "").toLowerCase().includes(needle));
    }
    function entityKindMatches2(entity, kind) {
      return projectMovScriptDomainNodeKind2(entity.entityKind, entity.record) === kind || recordKindMatches2(entity.record, kind);
    }
    function recordKindMatches2(record, kind) {
      return [
        record.kind,
        record.namespace_kind,
        record.namespaceKind,
        record.timeline_namespace_kind,
        record.timelineNamespaceKind,
        record.setting_namespace_kind,
        record.settingNamespaceKind,
        record.setting_kind,
        record.settingKind,
        record.content_unit_type,
        record.contentUnitType,
        record.asset_kind,
        record.assetKind,
        record.shot_kind,
        record.shotKind,
        record.segment_kind,
        record.segmentKind,
        record.slot_kind,
        record.slotKind,
        record.expression_kind,
        record.expressionKind,
        record.cue_kind,
        record.cueKind
      ].some((value) => stringField72(value) === kind);
    }
    function isDeletedWorkspaceRecord2(record) {
      return record.__delete === true || record.deleted === true;
    }
    function limitEntities2(items, limit) {
      if (limit === void 0) return items;
      if (!Number.isFinite(limit) || limit <= 0) return [];
      return items.slice(0, Math.floor(limit));
    }
    function sameId8(left, right) {
      const leftId = idField32(left);
      const rightId = idField32(right);
      return leftId !== void 0 && rightId !== void 0 && String(leftId) === String(rightId);
    }
    function entityIdField2(entityKind, record) {
      if (entityKind === "project") return idField32(record.project_id ?? record.ID ?? record.id);
      return idField32(record.ID ?? record.id);
    }
    function idField32(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function stringField72(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function finiteNumber4(value) {
      const next = Number(value);
      return Number.isFinite(next) ? next : void 0;
    }
    function pathSegmentAfter11(path, segment) {
      const parts = path.split("/");
      const index = parts.indexOf(segment);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function sameNormalizedEntityPath2(pathOrDir, ref, entityKind) {
      const left = normalizeWorkspacePath8(pathOrDir);
      const right = normalizeWorkspacePath8(String(ref));
      if (!left || !right) return false;
      if (left === right) return true;
      if (left.endsWith(".json") && entityDir9(left) === right) return true;
      if (right.endsWith(".json") && left === entityDir9(right)) return true;
      return sameEntityRef3(left, right, entityKind);
    }
    function normalizeIndexedDocumentPath2(path) {
      return normalizeWorkspacePath8(path).replace(/^\.movscript\//, "");
    }
    function isRecord73(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function pruneUndefined17(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    function dedupeDomainEdges3(edges) {
      const seen = /* @__PURE__ */ new Set();
      const out = [];
      for (const edge of edges) {
        const key = JSON.stringify(edge);
        if (seen.has(key)) continue;
        seen.add(key);
        out.push(edge);
      }
      return out;
    }
    function createMovScriptWorkspaceDomainRepository2(options) {
      const includeFile = options.includeFile ?? defaultWorkspaceDomainFileFilter2;
      return {
        async loadDocuments(input3 = {}) {
          return loadWorkspaceDocuments2(options.fileRepository, input3.path ?? "", includeFile);
        },
        async loadIndex(input3 = {}) {
          const documents = await loadWorkspaceDocuments2(options.fileRepository, input3.path ?? "", includeFile);
          return deriveMovScriptWorkspaceDomainIndex2(documents);
        }
      };
    }
    async function loadWorkspaceDocuments2(fileRepository, rootPath, includeFile) {
      const out = [];
      await collectWorkspaceDocuments2(fileRepository, normalizeWorkspacePath8(rootPath), includeFile, out);
      return out.sort((left, right) => left.path.localeCompare(right.path));
    }
    async function collectWorkspaceDocuments2(fileRepository, path, includeFile, out) {
      const listed = await fileRepository.list({ path });
      for (const entry of listed.entries) {
        if (entry.kind === "directory") {
          if (!path && isMovScriptNonSourceRootDirectory2(entry.path)) continue;
          await collectWorkspaceDocuments2(fileRepository, entry.path, includeFile, out);
          continue;
        }
        if (!includeFile(entry.path)) continue;
        const file = await fileRepository.read({ path: entry.path });
        out.push({
          path: file.path,
          data: parseWorkspaceDocument7(file.path, file.content),
          ...file.version !== void 0 ? { version: file.version } : {},
          ...file.updatedAt !== void 0 ? { updatedAt: file.updatedAt } : {}
        });
      }
    }
    function parseWorkspaceDocument7(path, content) {
      if (path.endsWith(".json")) {
        try {
          return JSON.parse(content);
        } catch {
          return void 0;
        }
      }
      return content;
    }
    function defaultWorkspaceDomainFileFilter2(path) {
      return isMovScriptSourcePath2(path);
    }
    var DEFAULT_SELECTION_FIELD2 = "lock";
    function buildMovScriptCandidateSelectionRecord2(candidate, input3 = {}) {
      const candidateId = idField42(candidate.id);
      const resourceId = resourceIdField6(candidate.resource_id ?? candidate.resourceId);
      const artifactRef = stringField82(candidate.artifact_ref ?? candidate.artifactRef);
      if (candidateId === void 0 && resourceId === void 0) {
        throw new Error("candidate selection requires candidate id or resource_id");
      }
      return pruneUndefined23({
        candidate_id: candidateId,
        resource_id: resourceId,
        artifact_ref: artifactRef,
        reason: input3.reason ?? "selected",
        selected_at: input3.selectedAt,
        selected_by: input3.selectedBy,
        metadata: input3.metadata
      });
    }
    function selectMovScriptCandidateRecord2(record, input3) {
      const candidate = findCandidate2(record, input3.candidateId);
      if (!candidate) throw new Error(`candidate not found: ${String(input3.candidateId)}`);
      const selection = buildMovScriptCandidateSelectionRecord2(candidate, input3);
      return {
        candidate,
        selection,
        record: {
          ...record,
          [selectionField2(input3)]: selection
        }
      };
    }
    function clearMovScriptCandidateSelectionRecord2(record, options = {}) {
      const field = selectionField2(options);
      const { [field]: _selection, ...nextRecord } = record;
      return nextRecord;
    }
    function findCandidate2(record, candidateId) {
      return arrayField8(record.candidates).filter(isRecord83).find((item) => sameId22(item.id, candidateId));
    }
    function selectionField2(options) {
      return options.selectionField?.trim() || DEFAULT_SELECTION_FIELD2;
    }
    function sameId22(left, right) {
      if (left === void 0 || right === void 0) return false;
      return String(left) === String(right);
    }
    function idField42(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function resourceIdField6(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function stringField82(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function arrayField8(value) {
      return Array.isArray(value) ? value : [];
    }
    function isRecord83(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined23(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    async function appendMovScriptInlineCandidate2(input3) {
      const targetPath = normalizeWorkspacePath22(input3.targetPath);
      const current = await readTargetRecord2(input3.fileRepository, targetPath);
      validateTargetKind2(input3.targetKind, current);
      const candidate = buildInlineCandidate2(input3.payload, input3.nonce);
      const candidates = arrayField22(current.candidates).filter(isRecord93);
      if (candidates.some((item) => String(item.id) === String(candidate.id))) {
        throw new Error(`candidate already exists: ${String(candidate.id)}`);
      }
      const record = {
        ...current,
        candidates: [...candidates, candidate]
      };
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord8(record) });
      return { path: targetPath, targetKind: input3.targetKind, candidate, record };
    }
    async function selectMovScriptInlineCandidate2(input3) {
      const targetPath = normalizeWorkspacePath22(input3.targetPath);
      const current = await readTargetRecord2(input3.fileRepository, targetPath);
      validateTargetKind2(input3.targetKind, current);
      const { candidate, record } = selectMovScriptCandidateRecord2(current, {
        candidateId: input3.candidateId,
        reason: input3.reason
      });
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord8(record) });
      return { path: targetPath, targetKind: input3.targetKind, candidate, record };
    }
    async function updateMovScriptInlineCandidate2(input3) {
      const targetPath = normalizeWorkspacePath22(input3.targetPath ?? input3.targetEntity?.path ?? workspacePath5(input3.targetRecord));
      const current = await readTargetRecord2(input3.fileRepository, targetPath);
      validateTargetKind2(input3.targetKind, current);
      const candidates = arrayField22(current.candidates).filter(isRecord93);
      const candidateIndex = candidates.findIndex((item) => String(item.id) === input3.candidateId);
      if (candidateIndex < 0) throw new Error(`candidate not found: ${input3.candidateId}`);
      const currentCandidate = candidates[candidateIndex];
      const candidate = pruneUndefined33({
        ...currentCandidate,
        ...input3.payload.resource_id !== void 0 || input3.payload.resourceId !== void 0 ? { resource_id: requiredResourceId6(input3.payload.resource_id ?? input3.payload.resourceId) } : {},
        ...input3.payload.artifact_ref !== void 0 || input3.payload.artifactRef !== void 0 ? { artifact_ref: stringField92(input3.payload.artifact_ref ?? input3.payload.artifactRef) } : {},
        ...input3.payload.source !== void 0 ? { source: input3.payload.source } : {},
        ...input3.payload.notes !== void 0 ? { notes: input3.payload.notes } : {},
        ...input3.payload.metadata !== void 0 ? { metadata: input3.payload.metadata } : {}
      });
      const nextCandidates = [...candidates];
      nextCandidates[candidateIndex] = candidate;
      const record = {
        ...current,
        candidates: nextCandidates
      };
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord8(record) });
      return { path: targetPath, targetKind: input3.targetKind, candidate, record };
    }
    function workspacePath5(record) {
      const value = record?.__workspace_path ?? record?.workspace_path ?? record?.path;
      if (typeof value === "string" && value.trim()) return value;
      throw new Error("candidate target path is required");
    }
    async function unlockMovScriptInlineCandidate2(input3) {
      const targetPath = normalizeWorkspacePath22(input3.targetPath);
      const current = await readTargetRecord2(input3.fileRepository, targetPath);
      validateTargetKind2(input3.targetKind, current);
      const record = clearMovScriptCandidateSelectionRecord2(current);
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord8(record) });
      return { path: targetPath, targetKind: input3.targetKind, record };
    }
    function readTargetRecord2(fileRepository, targetPath) {
      return fileRepository.read({ path: targetPath }).then((file) => {
        const parsed = JSON.parse(file.content);
        if (!isRecord93(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
        return parsed;
      });
    }
    function validateTargetKind2(targetKind, record) {
      if (targetKind === "content_unit") {
        throw new Error("content_unit candidates are backend decision records; use createContentCandidate/selectContentUnitCandidate");
      }
      if (record.kind === targetKind) return;
      const schemaKind = typeof record.schema === "string" ? record.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
      if (schemaKind === targetKind) return;
      throw new Error(`target kind mismatch: expected ${targetKind}`);
    }
    function buildInlineCandidate2(payload, nonce) {
      const resourceId = requiredResourceId6(payload.resource_id ?? payload.resourceId);
      if (resourceId === void 0) {
        throw new Error("resource_id required");
      }
      const id = payload.id ?? `candidate_${safeFileToken3(String(resourceId))}_${safeFileToken3(nonce ?? randomNonce3())}`;
      return pruneUndefined33({
        id,
        resource_id: resourceId,
        artifact_ref: stringField92(payload.artifact_ref ?? payload.artifactRef),
        source: payload.source ?? "manual",
        notes: payload.notes,
        metadata: payload.metadata
      });
    }
    function serializeWorkspaceRecord8(value) {
      return `${JSON.stringify(value, null, 2)}
`;
    }
    function normalizeWorkspacePath22(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function safeFileToken3(value) {
      return value.trim().replace(/[^a-zA-Z0-9_-]+/g, "_");
    }
    function requiredResourceId6(value) {
      if (value === void 0 || value === null || value === "") return void 0;
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      throw new Error("resource_id must be a positive integer RawResource ID");
    }
    function stringField92(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function randomNonce3() {
      const random = globalThis.crypto;
      if (random?.randomUUID) return random.randomUUID();
      return `${Date.now()}_${Math.random().toString(36).slice(2)}`;
    }
    function arrayField22(value) {
      return Array.isArray(value) ? value : [];
    }
    function isRecord93(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined33(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    async function updateMovScriptContentUnitEditPrompt2(input3) {
      const targetPath = normalizeWorkspacePath32(input3.targetPath);
      const current = await readContentUnitRecord2(input3.fileRepository, targetPath);
      const editPrompt = pruneUndefined42({
        text: stringValue34(input3.editPrompt.text),
        negative_text: stringValue34(input3.editPrompt.negative_text),
        notes: stringValue34(input3.editPrompt.notes),
        structured: isRecord102(input3.editPrompt.structured) ? input3.editPrompt.structured : void 0
      });
      const record = {
        ...current,
        edit_prompt: editPrompt
      };
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord22(record) });
      return { path: targetPath, record };
    }
    function readContentUnitRecord2(fileRepository, targetPath) {
      return fileRepository.read({ path: targetPath }).then((file) => {
        const parsed = JSON.parse(file.content);
        if (!isRecord102(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
        const schemaKind = typeof parsed.schema === "string" ? parsed.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
        if (parsed.kind !== "content_unit" && schemaKind !== "content_unit") {
          throw new Error("target kind mismatch: expected content_unit");
        }
        return parsed;
      });
    }
    function normalizeWorkspacePath32(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function serializeWorkspaceRecord22(value) {
      return `${JSON.stringify(value, null, 2)}
`;
    }
    function stringValue34(value) {
      return typeof value === "string" ? value : void 0;
    }
    function isRecord102(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined42(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    async function snapshotMovScriptVersionFromMarkdown2(input3) {
      const scriptId = semanticEntityId2(input3.scriptId, "script");
      const versionId = semanticEntityId2(input3.versionId, "script_version");
      const scriptDir = `scripts/${entityPathSlug2(scriptId, "script")}`;
      const scriptPath = normalizeWorkspacePath42(input3.sourcePath ?? `${scriptDir}/script.md`);
      const markdown = await input3.fileRepository.read({ path: scriptPath });
      const blocks = splitMarkdownBlocks2(markdown.content);
      const versionDir = `${scriptDir}/versions/${entityPathSlug2(versionId, "script_version")}`;
      const versionPath = `${versionDir}/script_version.json`;
      const createdAt = (input3.now ?? /* @__PURE__ */ new Date()).toISOString();
      const versionRecord = {
        schema: "movscript.script_version.v1",
        kind: "script_version",
        id: versionId,
        title: input3.versionLabel ?? String(versionId),
        version_label: input3.versionLabel ?? String(versionId),
        source_ref: scriptPath.replace(`${scriptDir}/`, ""),
        source_text_hash: hashText2(markdown.content),
        block_count: blocks.length,
        created_at: createdAt
      };
      await input3.fileRepository.write({
        path: versionPath,
        content: serializeWorkspaceRecord32(versionRecord)
      });
      const blockPaths = [];
      for (const [index, block] of blocks.entries()) {
        const blockId = String(index + 1).padStart(3, "0");
        const blockPath = `${versionDir}/blocks/${entityPathSlug2(blockId, "script_block")}/script_block.json`;
        blockPaths.push(blockPath);
        await input3.fileRepository.write({
          path: blockPath,
          content: serializeWorkspaceRecord32({
            schema: "movscript.script_block.v1",
            kind: "script_block",
            id: blockId,
            order: index + 1,
            text: block.text,
            block_kind: block.kind,
            source_range: block.sourceRange
          })
        });
      }
      return {
        scriptId,
        versionId,
        scriptPath,
        versionPath,
        blockPaths,
        blockCount: blockPaths.length
      };
    }
    function splitMarkdownBlocks2(markdown) {
      const lines = markdown.replace(/\r\n/g, "\n").split("\n");
      const blocks = [];
      let startLine = 1;
      let current = [];
      function flush(endLine) {
        const text = current.join("\n").trim();
        if (!text) return;
        blocks.push({
          text,
          kind: inferScriptBlockKind2(text),
          sourceRange: { start_line: startLine, end_line: endLine }
        });
        current = [];
      }
      for (const [index, line] of lines.entries()) {
        const lineNumber = index + 1;
        if (line.trim() === "") {
          flush(lineNumber - 1);
          startLine = lineNumber + 1;
          continue;
        }
        if (current.length === 0) startLine = lineNumber;
        current.push(line);
      }
      flush(lines.length);
      return blocks;
    }
    function inferScriptBlockKind2(text) {
      const firstLine2 = text.split("\n")[0]?.trim() ?? "";
      if (/^(INT\.|EXT\.|内景|外景)/i.test(firstLine2)) return "scene_heading";
      if (/^[A-Z][A-Z0-9 _-]{1,32}$/.test(firstLine2)) return "character";
      if (/^（.*）$|^\(.*\)$/.test(firstLine2)) return "parenthetical";
      return void 0;
    }
    function normalizeWorkspacePath42(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function serializeWorkspaceRecord32(value) {
      return `${JSON.stringify(value, null, 2)}
`;
    }
    function hashText2(value) {
      let hash = 5381;
      for (let index = 0; index < value.length; index += 1) {
        hash = (hash << 5) + hash ^ value.charCodeAt(index);
      }
      return `djb2_${(hash >>> 0).toString(16)}`;
    }
    async function upsertMovScriptSourceRecord2(input3) {
      const targetPath = normalizeWorkspacePath52(input3.targetPath);
      const current = await readOptionalWorkspaceRecord2(input3.fileRepository, targetPath);
      const incoming = stripWorkspacePrivateFields6({
        ...input3.record,
        ...input3.payload ?? {}
      });
      const expectedKind = stringValue44(incoming.kind);
      if (expectedKind !== void 0) assertRecordKind2(current, expectedKind, targetPath);
      const record = pruneUndefined52({
        ...current,
        ...incoming
      });
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord42(record) });
      return { path: targetPath, record };
    }
    async function updateMovScriptEntityTransition2(input3) {
      const targetPath = normalizeWorkspacePath52(input3.targetPath);
      const current = await readWorkspaceRecord2(input3.fileRepository, targetPath);
      const record = pruneUndefined52({
        ...current,
        transition: normalizeTransition3(input3.transition)
      });
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord42(record) });
      return { path: targetPath, record };
    }
    async function updateMovScriptStoryboardTimeline2(input3) {
      const targetPath = normalizeWorkspacePath52(input3.targetPath);
      const current = await readWorkspaceRecord2(input3.fileRepository, targetPath, "storyboard");
      const record = pruneUndefined52({
        ...current,
        timeline: normalizeTimeline3(input3.timeline)
      });
      await input3.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord42(record) });
      return { path: targetPath, record };
    }
    async function readWorkspaceRecord2(fileRepository, targetPath, expectedKind) {
      const file = await fileRepository.read({ path: targetPath });
      const parsed = JSON.parse(file.content);
      if (!isRecord112(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
      const schemaKind = typeof parsed.schema === "string" ? parsed.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
      if (expectedKind !== void 0 && parsed.kind !== expectedKind && schemaKind !== expectedKind) {
        throw new Error(`target kind mismatch: expected ${expectedKind}`);
      }
      return parsed;
    }
    async function readOptionalWorkspaceRecord2(fileRepository, targetPath) {
      const file = await fileRepository.read({ path: targetPath }).catch(() => void 0);
      if (!file) return {};
      const parsed = JSON.parse(file.content);
      if (!isRecord112(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
      return parsed;
    }
    function assertRecordKind2(current, expectedKind, targetPath) {
      const currentKind = typeof current.kind === "string" ? current.kind : typeof current.schema === "string" ? current.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
      if (currentKind !== void 0 && currentKind !== expectedKind) {
        throw new Error(`target kind mismatch: expected ${expectedKind} at ${targetPath}`);
      }
    }
    function normalizeTransition3(transition) {
      if (!transition) return void 0;
      return pruneUndefined52({
        in: stringValue44(transition.in),
        out: stringValue44(transition.out),
        notes: stringValue44(transition.notes)
      });
    }
    function normalizeTimeline3(timeline) {
      if (!timeline) return void 0;
      return pruneUndefined52({
        gap_after_sec: finiteNumber23(timeline.gap_after_sec),
        caption: stringValue44(timeline.caption),
        duration_sec: finiteNumber23(timeline.duration_sec)
      });
    }
    function serializeWorkspaceRecord42(value) {
      return `${JSON.stringify(value, null, 2)}
`;
    }
    function normalizeWorkspacePath52(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function stringValue44(value) {
      return typeof value === "string" && value.trim() ? value : void 0;
    }
    function finiteNumber23(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function isRecord112(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function stripWorkspacePrivateFields6(record) {
      return Object.fromEntries(
        Object.entries(record).filter(([key]) => !key.startsWith("__workspace_"))
      );
    }
    function pruneUndefined52(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    var CANDIDATE_SCHEMA2 = "movscript.candidate.v1";
    async function createMovScriptWorkspaceAssetSlotCandidate2(input3) {
      const targetPath = workspacePath22(input3.targetRecord);
      if (targetPath) {
        const result = await appendMovScriptInlineCandidate2({
          fileRepository: input3.fileRepository,
          targetPath,
          targetKind: "asset",
          nonce: input3.nonce,
          payload: inlineCandidatePayload2(input3.payload)
        });
        return {
          path: result.path,
          entityType: "candidate",
          record: workspaceCandidateRecordFromInline2({
            payload: input3.payload,
            targetKind: "asset",
            targetRecord: result.record,
            candidate: result.candidate,
            path: result.path
          }),
          candidate: result.candidate,
          targetRecord: result.record
        };
      }
      const record = createMovScriptWorkspaceAssetSlotCandidateRecord2(input3);
      const parentSlotId = positiveNumber5(record.asset_slot_id);
      if (!parentSlotId) throw new Error("\u5019\u9009\u7F3A\u5C11\u7236\u7D20\u6750\u9700\u6C42");
      const path = `${normalizeProjectPath2(input3.projectPath)}/assets/asset_slot_${parentSlotId}.candidates/candidate_${workspaceCandidateFileKey2(record)}.json`;
      await input3.fileRepository.write({ path, content: serializeWorkspaceRecord52(record) });
      return { path, entityType: "candidate", record };
    }
    async function createMovScriptWorkspaceKeyframeCandidate2(input3) {
      const targetPath = workspacePath22(input3.targetRecord) ?? stringValue53(input3.payload.target_path ?? input3.payload.targetPath);
      if (targetPath) {
        const result = await appendMovScriptInlineCandidate2({
          fileRepository: input3.fileRepository,
          targetPath,
          targetKind: "keyframe",
          nonce: input3.nonce,
          payload: inlineCandidatePayload2(input3.payload)
        });
        return {
          path: result.path,
          entityType: "candidate",
          record: workspaceCandidateRecordFromInline2({
            payload: input3.payload,
            targetKind: "keyframe",
            targetRecord: result.record,
            candidate: result.candidate,
            path: result.path
          }),
          candidate: result.candidate,
          targetRecord: result.record
        };
      }
      const record = createMovScriptWorkspaceKeyframeCandidateRecord2(input3);
      const productionId = positiveNumber5(record.production_id);
      if (!productionId) throw new Error("\u5019\u9009\u7F3A\u5C11\u5236\u4F5C ID");
      const targetKeyframeId = positiveNumber5(record.keyframe_id);
      if (!targetKeyframeId) throw new Error("\u5019\u9009\u7F3A\u5C11\u76EE\u6807\u5173\u952E\u5E27");
      const directory = `${normalizeProjectPath2(input3.projectPath)}/productions/${entityPathSlug2(productionId, "production")}/keyframes/${entityPathSlug2(targetKeyframeId, "keyframe")}.candidates`;
      const path = `${directory}/candidate_${workspaceCandidateFileKey2(record)}.json`;
      await input3.fileRepository.write({ path, content: serializeWorkspaceRecord52(record) });
      return { path, entityType: "candidate", record };
    }
    function createMovScriptWorkspaceAssetSlotCandidateRecord2(input3) {
      const parentSlotId = positiveNumber5(input3.payload.asset_slot_id ?? input3.payload.assetSlotId ?? input3.targetRecord?.ID ?? input3.targetRecord?.id);
      if (!parentSlotId) throw new Error("\u5019\u9009\u7F3A\u5C11\u7236\u7D20\u6750\u9700\u6C42");
      const resourceId = positiveNumber5(input3.payload.resource_id ?? input3.payload.resourceId);
      if (!resourceId) throw new Error("resource_id required");
      const clientId = localClientId2("asset_slot_candidate", parentSlotId, resourceId, input3.nonce);
      const note = stringValue53(input3.payload.note ?? input3.payload.notes);
      const sourceType = stringValue53(input3.payload.source_type ?? input3.payload.sourceType ?? input3.payload.source) ?? "manual";
      const sourceId = positiveNumber5(input3.payload.source_id ?? input3.payload.sourceId);
      return pruneUndefined62({
        schema: CANDIDATE_SCHEMA2,
        ID: -stablePositiveHash2(clientId),
        id: -stablePositiveHash2(clientId),
        client_id: clientId,
        target: { type: "asset_slot", id: parentSlotId },
        asset_slot_id: parentSlotId,
        kind: stringValue53(input3.payload.kind) ?? stringValue53(input3.targetRecord?.kind ?? input3.targetRecord?.asset_kind) ?? "image",
        title: note ?? `\u5019\u9009\u7D20\u6750 #${resourceId}`,
        description: note,
        priority: "normal",
        resource_id: resourceId,
        source_type: sourceType,
        ...sourceId ? { source_id: sourceId } : {},
        metadata_json: JSON.stringify(pruneUndefined62({
          source: "workspace_asset_slot_candidate",
          asset_slot_id: parentSlotId,
          resource_id: resourceId,
          source_type: sourceType,
          source_id: sourceId,
          score: numberValue23(input3.payload.score),
          note
        }))
      });
    }
    function createMovScriptWorkspaceKeyframeCandidateRecord2(input3) {
      const resourceId = positiveNumber5(input3.payload.resource_id ?? input3.payload.resourceId);
      if (!resourceId) throw new Error("resource_id required");
      const targetKeyframeId = keyframeCandidateTargetId2(input3.payload);
      if (!targetKeyframeId) throw new Error("\u5019\u9009\u7F3A\u5C11\u76EE\u6807\u5173\u952E\u5E27");
      const clientId = localClientId2("keyframe_candidate", targetKeyframeId, resourceId, input3.nonce);
      return pruneUndefined62({
        ...input3.payload,
        schema: CANDIDATE_SCHEMA2,
        ID: -stablePositiveHash2(clientId),
        id: -stablePositiveHash2(clientId),
        client_id: clientId,
        target: { type: "keyframe", id: targetKeyframeId },
        keyframe_id: targetKeyframeId,
        resource_id: resourceId,
        production_id: numberValue23(input3.payload.production_id ?? input3.payload.productionId),
        scene_moment_id: numberValue23(input3.payload.scene_moment_id ?? input3.payload.sceneMomentId),
        content_unit_id: numberValue23(input3.payload.content_unit_id ?? input3.payload.contentUnitId),
        canvas_id: numberValue23(input3.payload.canvas_id ?? input3.payload.canvasId),
        title: stringValue53(input3.payload.title) ?? `\u5019\u9009\u753B\u9762 #${resourceId}`,
        description: stringValue53(input3.payload.description) ?? "",
        prompt: stringValue53(input3.payload.prompt) ?? "",
        order: numberValue23(input3.payload.order ?? input3.payload.sort_order ?? input3.payload.sortOrder) ?? 0
      });
    }
    function inlineCandidatePayload2(payload) {
      const resourceId = payload.resource_id ?? payload.resourceId;
      return pruneUndefined62({
        id: stringValue53(payload.id ?? payload.client_id ?? payload.clientId),
        resource_id: resourceId === void 0 ? void 0 : requiredResourceId22(resourceId),
        artifact_ref: stringValue53(payload.artifact_ref ?? payload.artifactRef),
        source: stringValue53(payload.source ?? payload.source_type ?? payload.sourceType),
        notes: stringValue53(payload.notes ?? payload.note ?? payload.description),
        metadata: pruneUndefined62({
          source_id: payload.source_id ?? payload.sourceId,
          score: payload.score,
          raw: payload
        })
      });
    }
    function workspaceCandidateRecordFromInline2(input3) {
      const targetId = input3.targetRecord.ID ?? input3.targetRecord.id;
      const candidateId = stringValue53(input3.candidate.id) ?? localClientId2(`${input3.targetKind}_candidate`, String(targetId ?? "target"), Number(input3.candidate.resource_id) || 0);
      const numericCandidateId = -stablePositiveHash2(candidateId);
      return pruneUndefined62({
        schema: CANDIDATE_SCHEMA2,
        ID: numericCandidateId,
        id: numericCandidateId,
        client_id: candidateId,
        target: { type: input3.targetKind, id: targetId },
        asset_slot_id: input3.targetKind === "asset" ? numericId4(targetId) : void 0,
        keyframe_id: input3.targetKind === "keyframe" ? numericId4(targetId) : void 0,
        resource_id: resourceIdValue3(input3.candidate.resource_id),
        artifact_ref: stringValue53(input3.candidate.artifact_ref),
        source_type: input3.candidate.source,
        note: input3.candidate.notes,
        metadata_json: JSON.stringify(pruneUndefined62({
          source: "workspace_inline_candidate",
          target_path: input3.path,
          target_kind: input3.targetKind,
          candidate_id: input3.candidate.id,
          payload: input3.payload
        }))
      });
    }
    function keyframeCandidateTargetId2(payload) {
      const metadata = parseMetadata2(payload.metadata_json);
      return positiveNumber5(payload.keyframe_id ?? payload.keyframeId ?? metadata?.target_keyframe_id);
    }
    function workspacePath22(record) {
      return stringValue53(record?.__workspace_path ?? record?.workspace_path ?? record?.path);
    }
    function parseMetadata2(value) {
      if (isRecord122(value)) return value;
      if (typeof value !== "string" || !value.trim()) return void 0;
      try {
        const parsed = JSON.parse(value);
        return isRecord122(parsed) ? parsed : void 0;
      } catch {
        return void 0;
      }
    }
    function serializeWorkspaceRecord52(value) {
      return `${JSON.stringify(value, null, 2)}
`;
    }
    function workspaceCandidateFileKey2(record) {
      return safeFileToken22(stringValue53(record.client_id) ?? String(record.id ?? record.ID ?? Date.now()));
    }
    function localClientId2(prefix, targetId, resourceId, nonce) {
      return `${prefix}_${targetId}_${resourceId}_${nonce ?? randomNonce22()}`;
    }
    function randomNonce22() {
      const random = globalThis.crypto;
      if (random?.randomUUID) return random.randomUUID();
      return `${Date.now()}_${Math.random().toString(36).slice(2)}`;
    }
    function normalizeProjectPath2(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function safeFileToken22(value) {
      return value.replace(/[^a-zA-Z0-9_-]+/g, "_");
    }
    function positiveNumber5(value) {
      const number = numberValue23(value);
      return number !== void 0 && number > 0 ? number : void 0;
    }
    function numberValue23(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) {
        const number = Number(value);
        if (Number.isFinite(number)) return number;
      }
      return void 0;
    }
    function numericId4(value) {
      return numberValue23(value);
    }
    function resourceIdValue3(value) {
      if (value === void 0) return void 0;
      return requiredResourceId22(value);
    }
    function requiredResourceId22(value) {
      const id = positiveNumber5(value);
      if (id) return id;
      throw new Error("resource_id must be a positive integer RawResource ID");
    }
    function stringValue53(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function isRecord122(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined62(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    function stablePositiveHash2(value) {
      let hash = 0;
      for (let index = 0; index < value.length; index += 1) {
        hash = hash * 31 + value.charCodeAt(index) >>> 0;
      }
      return hash % 2e9 + 1;
    }
    async function upsertMovScriptWorkspaceSetting2(input3) {
      const sourcePath = input3.entity?.path ?? workspacePath32(input3.record ?? {});
      const current = stripEntityPrivateFields2(input3.entity?.record ?? input3.record ?? {});
      const payload = stripEntityPrivateFields2(input3.payload);
      const identity = entityIdentity2(entityIdentityValue2(current, payload, input3.now), "setting");
      const title = stringValue63(payload.title ?? current.title) ?? displayName2(identity.id);
      const path = sourcePath ?? `settings/${identity.slug}/setting.json`;
      const record = pruneUndefined72({
        ...current,
        ...payload,
        schema: "movscript.setting.v1",
        kind: "setting",
        id: identity.id,
        title,
        setting_kind: normalizeSettingKind2(payload.setting_kind ?? payload.kind ?? current.setting_kind ?? current.kind),
        profile: normalizeProfile2(payload, current)
      });
      await input3.fileRepository.write({ path, content: serializeWorkspaceRecord62(record) });
      return { path, entityKind: "setting", record };
    }
    async function upsertMovScriptWorkspaceAsset2(input3) {
      const sourcePath = input3.entity?.path ?? workspacePath32(input3.record ?? {});
      const current = stripEntityPrivateFields2(input3.entity?.record ?? input3.record ?? {});
      const payload = stripEntityPrivateFields2(input3.payload);
      const identity = entityIdentity2(entityIdentityValue2(current, payload, input3.now), "asset");
      const sourcePathSettingId = sourcePath ? pathSegmentAfter22(sourcePath, "settings") : void 0;
      const sourcePathSettingStateId = sourcePath ? pathSegmentAfter22(sourcePath, "states") : void 0;
      if (sourcePath && !sourcePathSettingStateId) {
        throw new Error("asset source path must be under a setting state");
      }
      const settingId = entityRef4(
        payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef ?? current.setting_id ?? current.settingId ?? current.setting_ref ?? current.settingRef,
        "setting"
      ) ?? sourcePathSettingId;
      const settingStateId = entityRef4(
        payload.setting_state_id ?? payload.settingStateId ?? payload.setting_state_ref ?? payload.settingStateRef ?? current.setting_state_id ?? current.settingStateId ?? current.setting_state_ref ?? current.settingStateRef,
        "setting_state"
      ) ?? sourcePathSettingStateId;
      if (!settingId) {
        throw new Error("asset requires setting_id; assets must be stored under settings/{setting}/states/{state}/assets/{asset}/asset.json");
      }
      if (!settingStateId) {
        throw new Error("asset requires setting_state_id; assets must be stored under settings/{setting}/states/{state}/assets/{asset}/asset.json");
      }
      const title = stringValue63(payload.title ?? current.title) ?? displayName2(identity.id);
      const path = sourcePath ?? assetPath2({ id: identity.id, settingId, settingStateId });
      const record = pruneUndefined72({
        ...stripAssetOwnershipAliasFields2(current),
        ...stripAssetOwnershipAliasFields2(payload),
        schema: "movscript.asset.v1",
        kind: "asset",
        id: identity.id,
        title,
        slot: stringValue63(payload.slot ?? payload.slot_key ?? payload.slotKey ?? current.slot ?? current.slot_key ?? current.slotKey) ?? identity.slug,
        asset_kind: normalizeAssetKind2(payload.asset_kind ?? payload.kind ?? current.asset_kind ?? current.kind),
        prompt_hint: stringValue63(payload.prompt_hint ?? payload.promptHint ?? current.prompt_hint ?? current.promptHint),
        setting_id: settingId,
        setting_state_id: settingStateId
      });
      await input3.fileRepository.write({ path, content: serializeWorkspaceRecord62(record) });
      return { path, entityKind: "asset", record };
    }
    async function upsertMovScriptWorkspaceSettingState2(input3) {
      const sourcePath = input3.entity?.path ?? workspacePath32(input3.record ?? {});
      const current = stripEntityPrivateFields2(input3.entity?.record ?? input3.record ?? {});
      const payload = stripEntityPrivateFields2(input3.payload);
      const identity = entityIdentity2(entityIdentityValue2(current, payload, input3.now), "setting_state");
      const sourcePathSettingId = sourcePath ? pathSegmentAfter22(sourcePath, "settings") : void 0;
      const settingId = entityRef4(
        payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef ?? current.setting_id ?? current.settingId ?? current.setting_ref ?? current.settingRef,
        "setting"
      ) ?? sourcePathSettingId;
      if (!settingId) {
        throw new Error("setting state requires setting_id; states must be stored under settings/{setting}/states/{state}/setting_state.json");
      }
      const title = stringValue63(payload.title ?? current.title) ?? displayName2(identity.id);
      const path = sourcePath ?? `settings/${entityPathSlug2(settingId, "setting")}/states/${identity.slug}/setting_state.json`;
      const record = pruneUndefined72({
        ...current,
        ...payload,
        schema: "movscript.setting_state.v1",
        kind: "setting_state",
        id: identity.id,
        title,
        setting_id: settingId,
        state_kind: stringValue63(payload.state_kind ?? payload.stateKind ?? payload.kind ?? current.state_kind ?? current.stateKind ?? current.kind),
        description: stringValue63(payload.description ?? current.description)
      });
      await input3.fileRepository.write({ path, content: serializeWorkspaceRecord62(record) });
      return { path, entityKind: "setting_state", record };
    }
    async function deleteMovScriptWorkspaceEntity2(input3) {
      const path = input3.entity?.path ?? workspacePath32(input3.record ?? {});
      if (!path) throw new Error("workspace entity path is required");
      await input3.fileRepository.delete({ path });
    }
    function movScriptWorkspaceAssetPath2(input3) {
      const settingSlug = entityPathSlug2(input3.settingId, "setting");
      const assetSlug = entityPathSlug2(input3.id, "asset");
      return `settings/${settingSlug}/states/${entityPathSlug2(input3.settingStateId, "setting_state")}/assets/${assetSlug}/asset.json`;
    }
    var assetPath2 = movScriptWorkspaceAssetPath2;
    function entityIdentityValue2(current, payload, now) {
      return payload.workspace_slug ?? payload.workspaceSlug ?? current.workspace_slug ?? current.workspaceSlug ?? payload.workspace_entity_id ?? payload.workspaceEntityId ?? current.workspace_entity_id ?? current.workspaceEntityId ?? payload.id ?? payload.ID ?? current.id ?? current.ID ?? current.client_id ?? current.clientId ?? `local_${(now ?? /* @__PURE__ */ new Date()).getTime()}`;
    }
    function entityRef4(value, entityKind) {
      if (value === void 0 || value === null || String(value).trim() === "") return void 0;
      return semanticEntityId2(value, entityKind);
    }
    function stripAssetOwnershipAliasFields2(record) {
      const output3 = {};
      for (const [key, value] of Object.entries(record)) {
        if (key === "setting_ref" || key === "settingRef" || key === "setting_state_ref" || key === "settingStateRef") continue;
        output3[key] = value;
      }
      return output3;
    }
    function normalizeSettingKind2(value) {
      const kind = stringValue63(value);
      if (kind === "person" || kind === "character") return "character";
      if (kind === "place" || kind === "location") return "location";
      if (kind === "prop" || kind === "world_rule" || kind === "style" || kind === "other") return kind;
      return "other";
    }
    function normalizeAssetKind2(value) {
      const kind = stringValue63(value);
      if (kind === "image" || kind === "video" || kind === "audio" || kind === "text" || kind === "reference" || kind === "other") return kind;
      return "other";
    }
    function normalizeProfile2(payload, current) {
      const existing = isRecord132(current.profile) ? current.profile : {};
      const profile = pruneUndefined72({
        ...existing,
        alias: payload.alias ?? current.alias,
        description: payload.description ?? current.description,
        content: payload.content ?? current.content,
        importance: payload.importance ?? current.importance
      });
      return Object.keys(profile).length > 0 ? profile : void 0;
    }
    function workspacePath32(record) {
      return stringValue63(record.__workspace_path ?? record.workspace_path ?? record.path);
    }
    function pathSegmentAfter22(path, segment) {
      const parts = path.split("/");
      const index = parts.indexOf(segment);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function stripEntityPrivateFields2(record) {
      const output3 = {};
      for (const [key, value] of Object.entries(record)) {
        if (key.startsWith("__workspace_")) continue;
        if (key === "name") continue;
        output3[key] = value;
      }
      return output3;
    }
    function serializeWorkspaceRecord62(value) {
      return `${JSON.stringify(value, null, 2)}
`;
    }
    function stringValue63(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function displayName2(value) {
      const text = stringValue63(value);
      return text ?? "Untitled";
    }
    function isRecord132(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined72(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0 && item !== "") output3[key] = item;
      }
      return output3;
    }
    async function upsertMovScriptWorkspaceScript2(input3) {
      const current = stripWorkspacePrivateFields22(input3.record ?? {});
      const scriptId = stableScriptId2(input3.scriptId);
      const scriptDir = `scripts/${entityPathSlug2(scriptId, "script")}`;
      const scriptPath = `${scriptDir}/script.json`;
      const sourcePath = `${scriptDir}/script.md`;
      const record = pruneUndefined82({
        ...current,
        ...input3.metadata,
        schema: "movscript.script.v1",
        kind: "script",
        id: scriptId,
        title: stringValue72(input3.metadata?.title ?? current.title) ?? `Script ${scriptId}`,
        description: stringValue72(input3.metadata?.description ?? current.description),
        script_kind: stringValue72(input3.metadata?.script_kind ?? input3.metadata?.script_type ?? current.script_kind ?? current.script_type) ?? "uncategorized",
        source_ref: "script.md",
        content: input3.sourceText,
        updated_at: (input3.now ?? /* @__PURE__ */ new Date()).toISOString(),
        created_at: stringValue72(current.created_at ?? current.CreatedAt) ?? (input3.now ?? /* @__PURE__ */ new Date()).toISOString()
      });
      await input3.fileRepository.write({ path: sourcePath, content: input3.sourceText });
      await input3.fileRepository.write({ path: scriptPath, content: serializeWorkspaceRecord72(record) });
      return { scriptId, scriptPath, sourcePath, record, sourceText: input3.sourceText };
    }
    async function readMovScriptWorkspaceScriptSource2(input3) {
      const record = input3.entity?.record ?? input3.record;
      const scriptPath = stringValue72(input3.entity?.path ?? workspacePath42(record));
      const sourceRef = stringValue72(record.source_ref) ?? "script.md";
      if (!scriptPath) return stringValue72(input3.record.content) ?? "";
      const sourcePath = `${scriptPath.replace(/\/script\.json$/, "")}/${sourceRef}`.replace(/\/+/g, "/");
      return input3.fileRepository.read({ path: sourcePath }).then((file) => file.content).catch(() => stringValue72(record.content) ?? "");
    }
    function workspacePath42(record) {
      return stringValue72(record.__workspace_path ?? record.workspace_path ?? record.path);
    }
    function stableScriptId2(value) {
      return semanticEntityId2(value, "script");
    }
    function stripWorkspacePrivateFields22(record) {
      const output3 = {};
      for (const [key, value] of Object.entries(record)) {
        if (key.startsWith("__workspace_")) continue;
        output3[key] = value;
      }
      return output3;
    }
    function serializeWorkspaceRecord72(value) {
      return `${JSON.stringify(value, null, 2)}
`;
    }
    function stringValue72(value) {
      return typeof value === "string" ? value : void 0;
    }
    function pruneUndefined82(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0 && item !== "") output3[key] = item;
      }
      return output3;
    }
    async function saveMovScriptProductionWorkspaceSnapshot2(input3) {
      const productionId = stableId2(input3.productionId, "production");
      const productionSlug = slugId2(productionId, "production");
      const productionPath = `productions/${productionSlug}/production.json`;
      const writtenPaths = [];
      const production = await readOptionalRecord3(input3.fileRepository, productionPath);
      await writeRecord3(input3.fileRepository, productionPath, pruneUndefined92({
        ...stripWorkspacePrivateFields32(production),
        schema: "movscript.production.v1",
        kind: "production",
        id: productionId,
        title: stringValue82(input3.snapshot.production?.title ?? production.title) ?? `Production ${displayId2(productionId, "production")}`,
        transition: normalizeTransition22(input3.snapshot.production?.transition ?? production.transition),
        updated_at: (input3.now ?? /* @__PURE__ */ new Date()).toISOString()
      }));
      writtenPaths.push(productionPath);
      for (const segment of input3.snapshot.segments) {
        const segmentId = stableId2(segment.id ?? segment.client_id ?? writtenPaths.length + 1, "segment");
        const segmentSlug = slugId2(segmentId, "segment");
        const segmentPath = `productions/${productionSlug}/segments/${segmentSlug}/segment.json`;
        const existingSegment = await readOptionalRecord3(input3.fileRepository, segmentPath);
        await writeRecord3(input3.fileRepository, segmentPath, pruneUndefined92({
          ...stripWorkspacePrivateFields32(existingSegment),
          schema: "movscript.segment.v1",
          kind: "segment",
          id: segmentId,
          title: stringValue82(segment.title) ?? stringValue82(existingSegment.title) ?? `Segment ${displayId2(segmentId, "segment")}`,
          segment_kind: stringValue82(segment.kind ?? existingSegment.segment_kind),
          summary: stringValue82(segment.summary ?? existingSegment.summary),
          order: finiteNumber32(segment.order) ?? finiteNumber32(existingSegment.order),
          transition: normalizeTransition22(segment.transition ?? existingSegment.transition),
          script_block_id: nullableRef2(segment.script_block_id ?? existingSegment.script_block_id, "script_block"),
          ...segment.__delete === true ? { __delete: true } : {}
        }));
        writtenPaths.push(segmentPath);
        for (const moment of segment.scene_moments ?? []) {
          const momentId = stableId2(moment.id ?? moment.client_id ?? `${segmentId}_${writtenPaths.length + 1}`, "scene_moment");
          const momentSlug = slugId2(momentId, "scene_moment");
          const momentDir = `productions/${productionSlug}/segments/${segmentSlug}/scene_moments/${momentSlug}`;
          const momentPath = `${momentDir}/scene_moment.json`;
          const existingMoment = await readOptionalRecord3(input3.fileRepository, momentPath);
          await writeRecord3(input3.fileRepository, momentPath, pruneUndefined92({
            ...stripWorkspacePrivateFields32(existingMoment),
            schema: "movscript.scene_moment.v1",
            kind: "scene_moment",
            id: momentId,
            title: stringValue82(moment.title) ?? stringValue82(existingMoment.title) ?? `Scene Moment ${displayId2(momentId, "scene_moment")}`,
            scene_code: stringValue82(moment.scene_code ?? existingMoment.scene_code),
            when: stringValue82(moment.time_text ?? existingMoment.when),
            where: stringValue82(moment.location_text ?? existingMoment.where),
            condition_text: stringValue82(moment.condition_text ?? existingMoment.condition_text),
            action: stringValue82(moment.action_text ?? existingMoment.action),
            emotion: stringValue82(moment.mood ?? existingMoment.emotion),
            description: stringValue82(moment.description ?? existingMoment.description),
            order: finiteNumber32(moment.order) ?? finiteNumber32(existingMoment.order),
            transition: normalizeTransition22(moment.transition ?? existingMoment.transition),
            script_block_id: nullableRef2(moment.script_block_id ?? existingMoment.script_block_id, "script_block"),
            setting_refs: normalizeSettingRefs2(moment.settings, existingMoment.setting_refs),
            ...moment.__delete === true ? { __delete: true } : {}
          }));
          writtenPaths.push(momentPath);
          await writeKeyframeNodes2({
            fileRepository: input3.fileRepository,
            writtenPaths,
            ownerDir: momentDir,
            ownerKind: "scene_moment",
            sceneMomentDir: momentDir,
            ownerTitle: stringValue82(moment.title ?? existingMoment.title) ?? displayId2(momentId, "scene_moment"),
            nodes: moment.keyframes ?? []
          });
          await writeStoryboardNodes2({
            fileRepository: input3.fileRepository,
            writtenPaths,
            ownerDir: momentDir,
            ownerKind: "scene_moment",
            sceneMomentDir: momentDir,
            ownerTitle: stringValue82(moment.title ?? existingMoment.title) ?? displayId2(momentId, "scene_moment"),
            nodes: moment.storyboards ?? [],
            settings: moment.settings
          });
          for (const audioCue of moment.audio_cues ?? []) {
            const audioCueId = stableId2(audioCue.id ?? audioCue.client_id ?? `${momentId}_${writtenPaths.length + 1}`, "audio_cue");
            const audioCuePath = `${momentDir}/audio_cues/${slugId2(audioCueId, "audio_cue")}/audio_cue.json`;
            const existingAudioCue = await readOptionalRecord3(input3.fileRepository, audioCuePath);
            await writeRecord3(input3.fileRepository, audioCuePath, pruneUndefined92({
              ...stripWorkspacePrivateFields32(existingAudioCue),
              schema: "movscript.audio_cue.v1",
              kind: "audio_cue",
              id: audioCueId,
              title: stringValue82(audioCue.title ?? existingAudioCue.title) ?? `Audio Cue ${displayId2(audioCueId, "audio_cue")}`,
              cue_kind: normalizeAudioCueKind2(audioCue.cue_kind ?? audioCue.kind ?? existingAudioCue.cue_kind),
              order: finiteNumber32(audioCue.order) ?? finiteNumber32(existingAudioCue.order),
              scope_ref: momentDir,
              expression_unit_ref: stringValue82(audioCue.expression_unit_ref ?? existingAudioCue.expression_unit_ref),
              storyboard_ref: normalizeStoryboardRef2(audioCue.storyboard_ref ?? audioCue.storyboard_id ?? existingAudioCue.storyboard_ref),
              timing: isRecord142(audioCue.timing) ? audioCue.timing : isRecord142(existingAudioCue.timing) ? existingAudioCue.timing : void 0,
              prompt_hint: stringValue82(audioCue.prompt_hint ?? existingAudioCue.prompt_hint),
              asset_refs: Array.isArray(audioCue.asset_refs) ? audioCue.asset_refs.filter(isString4) : existingAudioCue.asset_refs,
              ...audioCue.__delete === true ? { __delete: true } : {}
            }));
            writtenPaths.push(audioCuePath);
          }
          for (const expression of moment.expression_units ?? []) {
            const expressionId = stableId2(expression.id ?? expression.client_id ?? `${momentId}_${writtenPaths.length + 1}`, "expression_unit");
            const expressionPath = `${momentDir}/expression_units/${slugId2(expressionId, "expression_unit")}/expression_unit.json`;
            const existingExpression = await readOptionalRecord3(input3.fileRepository, expressionPath);
            await writeRecord3(input3.fileRepository, expressionPath, pruneUndefined92({
              ...stripWorkspacePrivateFields32(existingExpression),
              schema: "movscript.expression_unit.v1",
              kind: "expression_unit",
              id: expressionId,
              title: stringValue82(expression.title ?? existingExpression.title) ?? stringValue82(expression.text) ?? `Expression Unit ${displayId2(expressionId, "expression_unit")}`,
              slot_kind: normalizeExpressionUnitSlotKind2(expression.slot_kind ?? expression.slotKind ?? existingExpression.slot_kind ?? existingExpression.slotKind ?? expression.kind ?? existingExpression.expression_kind),
              modality: stringValue82(expression.modality ?? existingExpression.modality),
              role: stringValue82(expression.role ?? existingExpression.role),
              expression_kind: normalizeExpressionKind2(expression.kind ?? existingExpression.expression_kind),
              visual_kind: stringValue82(expression.visual_kind ?? existingExpression.visual_kind),
              speaker: stringValue82(expression.speaker ?? existingExpression.speaker),
              speaker_ref: stringValue82(expression.speaker_ref ?? existingExpression.speaker_ref),
              source_expression_ref: stringValue82(expression.source_expression_ref ?? existingExpression.source_expression_ref),
              text: stringValue82(expression.text ?? existingExpression.text) ?? "",
              note: stringValue82(expression.note ?? existingExpression.note),
              intent: stringValue82(expression.intent ?? existingExpression.intent),
              content: isRecord142(expression.content) ? expression.content : isRecord142(existingExpression.content) ? existingExpression.content : void 0,
              timing_intent: isRecord142(expression.timing_intent) ? expression.timing_intent : isRecord142(existingExpression.timing_intent) ? existingExpression.timing_intent : void 0,
              voice_profile_ref: stringValue82(expression.voice_profile_ref ?? existingExpression.voice_profile_ref),
              order: finiteNumber32(expression.order) ?? finiteNumber32(existingExpression.order),
              span: isRecord142(expression.span) ? expression.span : isRecord142(existingExpression.span) ? existingExpression.span : void 0,
              script_block_id: nullableRef2(expression.script_block_id ?? existingExpression.script_block_id, "script_block"),
              ...expression.__delete === true ? { __delete: true } : {}
            }));
            writtenPaths.push(expressionPath);
            await writeKeyframeNodes2({
              fileRepository: input3.fileRepository,
              writtenPaths,
              ownerDir: `${momentDir}/expression_units/${slugId2(expressionId, "expression_unit")}`,
              ownerKind: "expression_unit",
              sceneMomentDir: momentDir,
              ownerTitle: stringValue82(expression.title ?? existingExpression.title) ?? displayId2(expressionId, "expression_unit"),
              nodes: expression.keyframes ?? []
            });
            await writeStoryboardNodes2({
              fileRepository: input3.fileRepository,
              writtenPaths,
              ownerDir: `${momentDir}/expression_units/${slugId2(expressionId, "expression_unit")}`,
              ownerKind: "expression_unit",
              sceneMomentDir: momentDir,
              ownerTitle: stringValue82(expression.title ?? existingExpression.title) ?? displayId2(expressionId, "expression_unit"),
              nodes: expression.storyboards ?? [],
              settings: moment.settings
            });
          }
        }
      }
      return { productionPath, snapshot: input3.snapshot, writtenPaths };
    }
    async function writeKeyframeNodes2({
      fileRepository,
      writtenPaths,
      ownerDir,
      ownerKind,
      sceneMomentDir,
      ownerTitle,
      nodes
    }) {
      for (const keyframe of nodes) {
        const keyframeId = stableId2(keyframe.id ?? keyframe.client_id ?? `${ownerTitle}_${writtenPaths.length + 1}`, "keyframe");
        const keyframePath = `${ownerDir}/keyframes/${slugId2(keyframeId, "keyframe")}/keyframe.json`;
        const existingKeyframe = await readOptionalRecord3(fileRepository, keyframePath);
        await writeRecord3(fileRepository, keyframePath, pruneUndefined92({
          ...stripWorkspacePrivateFields32(existingKeyframe),
          schema: "movscript.keyframe.v1",
          kind: "keyframe",
          id: keyframeId,
          title: stringValue82(keyframe.title ?? existingKeyframe.title) ?? `Keyframe ${displayId2(keyframeId, "keyframe")}`,
          scene_moment_ref: sceneMomentDir,
          ...ownerKind === "expression_unit" ? { expression_unit_ref: ownerDir } : {},
          role: stringValue82(keyframe.role ?? existingKeyframe.role),
          visual_intent: stringValue82(keyframe.visual_intent ?? existingKeyframe.visual_intent),
          timing: isRecord142(keyframe.timing) ? keyframe.timing : isRecord142(existingKeyframe.timing) ? existingKeyframe.timing : void 0,
          composition: isRecord142(keyframe.composition) ? keyframe.composition : isRecord142(existingKeyframe.composition) ? existingKeyframe.composition : void 0,
          continuity: isRecord142(keyframe.continuity) ? keyframe.continuity : isRecord142(existingKeyframe.continuity) ? existingKeyframe.continuity : void 0,
          reference_asset_refs: Array.isArray(keyframe.reference_asset_refs) ? keyframe.reference_asset_refs.filter(isString4) : existingKeyframe.reference_asset_refs,
          reference_keyframe_refs: Array.isArray(keyframe.reference_keyframe_refs) ? keyframe.reference_keyframe_refs.filter(isString4) : existingKeyframe.reference_keyframe_refs,
          ...keyframe.__delete === true ? { __delete: true } : {}
        }));
        writtenPaths.push(keyframePath);
      }
    }
    async function writeStoryboardNodes2({
      fileRepository,
      writtenPaths,
      ownerDir,
      ownerKind,
      sceneMomentDir,
      ownerTitle,
      nodes,
      settings
    }) {
      for (const [storyboardIndex, storyboard] of nodes.entries()) {
        const storyboardId = stableId2(storyboard.id ?? storyboard.client_id ?? "main", "storyboard");
        const storyboardPath = `${ownerDir}/storyboards/${slugId2(storyboardId, "storyboard")}/storyboard.json`;
        const existingStoryboard = await readOptionalRecord3(fileRepository, storyboardPath);
        await writeRecord3(fileRepository, storyboardPath, pruneUndefined92({
          ...stripWorkspacePrivateFields32(existingStoryboard),
          schema: "movscript.storyboard.v1",
          kind: "storyboard",
          id: storyboardId,
          title: stringValue82(storyboard.title ?? existingStoryboard.title) ?? `${ownerTitle} storyboard`,
          order: finiteNumber32(storyboard.order) ?? finiteNumber32(existingStoryboard.order) ?? storyboardIndex + 1,
          slot: stringValue82(storyboard.slot ?? existingStoryboard.slot) ?? String(storyboardId),
          asset_kind: stringValue82(storyboard.asset_kind ?? existingStoryboard.asset_kind),
          scene_moment_ref: sceneMomentDir,
          ...ownerKind === "expression_unit" ? { expression_unit_ref: ownerDir } : {},
          transition: normalizeTransition22(storyboard.transition ?? existingStoryboard.transition),
          timeline: normalizeTimeline22(storyboard.timeline ?? {
            gap_after_sec: storyboard.gap_after_sec,
            caption: storyboard.caption,
            duration_sec: storyboard.duration_sec
          }, existingStoryboard.timeline),
          graph: isRecord142(storyboard.graph) ? storyboard.graph : isRecord142(existingStoryboard.graph) ? existingStoryboard.graph : void 0,
          setting_refs: normalizeSettingRefs2(storyboard.settings ?? settings, existingStoryboard.setting_refs),
          ...storyboard.__delete === true ? { __delete: true } : {}
        }));
        writtenPaths.push(storyboardPath);
      }
    }
    function normalizeSettingRefs2(refs, fallback) {
      if (!refs) return Array.isArray(fallback) ? fallback.filter(isRecord142) : void 0;
      return refs.map((ref) => pruneUndefined92({
        setting_id: stableId2(ref.id ?? ref.client_id ?? "unassigned", "setting"),
        setting_state_id: ref.setting_state_id ?? ref.setting_state_ref,
        role: stringValue82(ref.role),
        notes: stringValue82(ref.source_label),
        setting_kind: stringValue82(ref.kind),
        state: isRecord142(ref.state) ? ref.state : void 0,
        ...ref.__delete === true ? { __delete: true } : {}
      }));
    }
    function normalizeExpressionKind2(value) {
      const kind = stringValue82(value);
      if (kind === "shot") return "shot";
      if (kind === "dialogue" || kind === "narration" || kind === "subtitle" || kind === "caption" || kind === "action" || kind === "shot") return kind;
      if (kind === "visual" || kind === "visual_note") return "visual_note";
      if (kind === "voice" || kind === "verbal" || kind === "voiceover") return "dialogue";
      if (kind === "audio" || kind === "sound" || kind === "sound_effect" || kind === "sfx" || kind === "music" || kind === "ambience") return "action";
      return "visual_note";
    }
    function normalizeTransition22(value) {
      if (!isRecord142(value)) return void 0;
      return pruneUndefined92({
        in: stringValue82(value.in),
        out: stringValue82(value.out),
        notes: stringValue82(value.notes)
      });
    }
    function normalizeTimeline22(value, fallback) {
      const source = isRecord142(value) ? value : isRecord142(fallback) ? fallback : {};
      const timeline = pruneUndefined92({
        gap_after_sec: finiteNumber32(source.gap_after_sec),
        caption: stringValue82(source.caption),
        duration_sec: finiteNumber32(source.duration_sec)
      });
      return Object.keys(timeline).length ? timeline : void 0;
    }
    function normalizeAudioCueKind2(value) {
      const kind = stringValue82(value);
      if (kind === "sound_effect" || kind === "music" || kind === "ambience" || kind === "dialogue" || kind === "foley" || kind === "other") return kind;
      if (kind === "sound") return "sound_effect";
      if (kind === "music_beat") return "music";
      return "sound_effect";
    }
    function normalizeStoryboardRef2(value) {
      return stringValue82(value);
    }
    async function readRecord2(fileRepository, path) {
      const file = await fileRepository.read({ path });
      const parsed = JSON.parse(file.content);
      return isRecord142(parsed) ? parsed : {};
    }
    async function readOptionalRecord3(fileRepository, path) {
      return readRecord2(fileRepository, path).catch(() => ({}));
    }
    async function writeRecord3(fileRepository, path, record) {
      await fileRepository.write({ path, content: `${JSON.stringify(record, null, 2)}
` });
    }
    function stableId2(value, prefix) {
      return semanticEntityId2(value, prefix);
    }
    function slugId2(value, prefix) {
      return entityPathSlug2(value, prefix);
    }
    function nullableRef2(value, prefix) {
      if (value === null) return null;
      if (value === void 0 || String(value).trim() === "") return void 0;
      return stableId2(value, prefix);
    }
    function displayId2(value, prefix) {
      return displayEntityId2(value, prefix);
    }
    function finiteNumber32(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function stringValue82(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function isRecord142(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function isString4(value) {
      return typeof value === "string";
    }
    function stripWorkspacePrivateFields32(record) {
      const output3 = {};
      for (const [key, value] of Object.entries(record)) {
        if (key.startsWith("__workspace_")) continue;
        output3[key] = value;
      }
      return output3;
    }
    function pruneUndefined92(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0 && item !== "") output3[key] = item;
      }
      return output3;
    }
    async function upsertMovScriptContentUnit2(input3) {
      const contentUnitId = stableEntityId3(input3.unit.ID ?? input3.unit.id ?? input3.unit.client_id, "content_unit");
      const contentUnitPath = movScriptContentUnitPath2(input3.unit);
      const current = await readOptionalRecord22(input3.fileRepository, contentUnitPath);
      const record = normalizeContentUnitRecord2(input3.unit, current, contentUnitId);
      validateContentUnitTarget2(record);
      validateContentUnitPrimaryPromptRefs2(record);
      await writeRecord22(input3.fileRepository, contentUnitPath, record);
      return { contentUnitPath, record };
    }
    function movScriptContentUnitPath2(unit) {
      const id = stableEntityId3(unit.ID ?? unit.id ?? unit.client_id, "content_unit");
      return `${contentUnitDirectory3(id)}/content_unit.json`;
    }
    function normalizeContentUnitRecord2(unit, current, id) {
      const contentUnitType22 = stringValue92(unit.content_unit_type ?? unit.contentUnitType ?? current.content_unit_type) ?? "storyboard_ref";
      const outputKind = stringValue92(unit.output_kind ?? unit.outputKind ?? current.output_kind) ?? defaultOutputKind2(contentUnitType22);
      const scopeKind = stringValue92(unit.scope_kind ?? unit.scopeKind ?? current.scope_kind);
      const scopeRef = stringValue92(unit.scope_ref ?? unit.scopeRef ?? current.scope_ref);
      const targetKind = stringValue92(unit.target_kind ?? unit.targetKind ?? current.target_kind) ?? (contentUnitType22 === "timeline_assembly_ref" ? "timeline_assembly" : void 0);
      const targetRef = stringValue92(unit.target_ref ?? unit.targetRef ?? current.target_ref) ?? (contentUnitType22 === "timeline_assembly_ref" && scopeKind && scopeRef ? implicitTimelineAssemblyRef2(scopeKind, scopeRef) : void 0);
      return pruneUndefined102({
        ...stripWorkspacePrivateFields42(current),
        schema: "movscript.content_unit.v1",
        kind: "content_unit",
        id,
        title: stringValue92(unit.title ?? current.title) ?? "Untitled content unit",
        content_unit_type: contentUnitType22,
        output_kind: outputKind,
        target_category: stringValue92(unit.target_category ?? unit.targetCategory ?? current.target_category),
        target_kind: targetKind,
        target_ref: targetRef,
        scope_kind: scopeKind,
        scope_ref: scopeRef,
        generation_role: stringValue92(unit.generation_role ?? unit.generationRole ?? current.generation_role),
        order: finiteNumber42(unit.order) ?? finiteNumber42(current.order),
        description: stringValue92(unit.description ?? current.description) ?? "",
        asset_ref: stringValue92(unit.asset_ref ?? unit.assetRef ?? current.asset_ref),
        production_ref: stringValue92(unit.production_ref ?? unit.productionRef ?? current.production_ref),
        segment_ref: stringValue92(unit.segment_ref ?? unit.segmentRef ?? current.segment_ref),
        keyframe_ref: stringValue92(unit.keyframe_ref ?? unit.keyframeRef ?? current.keyframe_ref),
        storyboard_ref: stringValue92(unit.storyboard_ref ?? unit.storyboardRef ?? current.storyboard_ref),
        scene_moment_ref: stringValue92(unit.scene_moment_ref ?? unit.sceneMomentRef ?? current.scene_moment_ref),
        expression_unit_ref: stringValue92(unit.expression_unit_ref ?? unit.expressionUnitRef ?? current.expression_unit_ref),
        content_unit_ref: stringValue92(unit.content_unit_ref ?? unit.contentUnitRef ?? current.content_unit_ref),
        voice_profile_ref: stringValue92(unit.voice_profile_ref ?? unit.voiceProfileRef ?? current.voice_profile_ref),
        edit_prompt: normalizeEditPrompt2(unit.edit_prompt ?? unit.editPrompt ?? unit.prompt ?? current.edit_prompt),
        model_intent: isRecord152(unit.model_intent ?? unit.modelIntent) ? unit.model_intent ?? unit.modelIntent : current.model_intent,
        ...unit.__delete === true ? { __delete: true } : {}
      });
    }
    function contentUnitDirectory3(id) {
      return `content_units/${entityPathSlug2(id, "content_unit")}`;
    }
    function defaultOutputKind2(contentUnitType22) {
      return outputKindForContentUnitType2(contentUnitType22, void 0);
    }
    function validateContentUnitTarget2(record) {
      for (const diagnostic22 of contentUnitTargetValidationDiagnostics2(record)) {
        throw new Error(diagnostic22.message);
      }
    }
    function validateContentUnitPrimaryPromptRefs2(record) {
      const contentUnitType22 = stringValue92(record.content_unit_type);
      if (!contentUnitType22) return;
      const primaryKind = primaryRefKindForContentUnitType4(contentUnitType22);
      if (!primaryKind) return;
      const primaryRefs = primaryRefIdsForContentUnitRecord4(record, primaryKind);
      if (primaryRefs.length === 0) return;
      const promptRefs = parseContentUnitEditPromptRefs4(record.edit_prompt);
      for (const promptRef of promptRefs) {
        if (promptRef.kind !== primaryKind) continue;
        if (!primaryRefs.some((primaryRef) => sameRefId3(primaryRef, promptRef.id, primaryKind))) continue;
        throw new Error(`${contentUnitType22} content_unit edit_prompt must not reference its own ${primaryRefFieldNameForKind3(primaryKind)}: ${promptRef.raw}`);
      }
    }
    var PROMPT_REF_PATTERN4 = /\{\{([a-z_]+):([^{}:\s][^{}]*)\}\}/g;
    function parseContentUnitEditPromptRefs4(value) {
      const editPrompt = isRecord152(value) ? value : {};
      return [
        ...parsePromptRefsFromText4(stringValue92(editPrompt.text)),
        ...parsePromptRefsFromText4(stringValue92(editPrompt.negative_text)),
        ...parsePromptRefsFromText4(stringValue92(editPrompt.notes))
      ];
    }
    function parsePromptRefsFromText4(text) {
      if (!text) return [];
      const refs = [];
      for (const match of text.matchAll(PROMPT_REF_PATTERN4)) {
        const kind = promptRefKind4(match[1]);
        const id = match[2]?.trim();
        if (!kind || !id) continue;
        refs.push({ kind, id, raw: match[0] });
      }
      return refs;
    }
    function promptRefKind4(value) {
      return isContentUnitPromptRefKind2(value) ? value : void 0;
    }
    function sameRefId3(left, right, kind) {
      return sameEntityRef3(left, right, kind) || lastPathSegment10(left) === right || lastPathSegment10(right) === left;
    }
    function lastPathSegment10(value) {
      if (typeof value !== "string" || !value.includes("/")) return void 0;
      return value.split("/").filter(Boolean).at(-1);
    }
    function stableEntityId3(value, prefix) {
      return semanticEntityId2(value, prefix);
    }
    async function readOptionalRecord22(fileRepository, path) {
      return fileRepository.read({ path }).then((file) => {
        const parsed = JSON.parse(file.content);
        return isRecord152(parsed) ? parsed : {};
      }).catch(() => ({}));
    }
    async function writeRecord22(fileRepository, path, record) {
      await fileRepository.write({ path, content: `${JSON.stringify(record, null, 2)}
` });
    }
    function stripWorkspacePrivateFields42(record) {
      const output3 = {};
      for (const [key, value] of Object.entries(record)) {
        if (key.startsWith("__workspace_")) continue;
        output3[key] = value;
      }
      return output3;
    }
    function stringValue92(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function finiteNumber42(value) {
      const next = Number(value);
      return Number.isFinite(next) ? next : void 0;
    }
    function normalizeEditPrompt2(value) {
      if (typeof value === "string") return { text: value };
      if (isRecord152(value)) return value;
      return void 0;
    }
    function isRecord152(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined102(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0 && item !== "") output3[key] = item;
      }
      return output3;
    }
    function createMovScriptBackendDecisionStore2(options) {
      const baseUrl = options.baseUrl.replace(/\/+$/, "");
      const projectId = encodeURIComponent(String(options.projectId));
      const fetchImpl = options.fetch ?? globalThis.fetch;
      if (!fetchImpl) throw new Error("fetch is required for backend decision store");
      const headers = () => ({
        "content-type": "application/json",
        ...options.token ? { authorization: `Bearer ${options.token}` } : {},
        ...options.headers ?? {}
      });
      const request = async (path, init = {}) => {
        const response = await fetchImpl(`${baseUrl}/api/v1/projects/${projectId}${path}`, {
          ...init,
          headers: {
            ...headers(),
            ...init.headers ?? {}
          }
        });
        if (response.status === 404) {
          console.info("[movscript-decision-store] backend decision request not found", {
            projectId: options.projectId,
            method: init.method ?? "GET",
            path,
            status: response.status
          });
          return void 0;
        }
        if (!response.ok) {
          const body = await response.text().catch(() => "");
          console.warn("[movscript-decision-store] backend decision request failed", {
            projectId: options.projectId,
            method: init.method ?? "GET",
            path,
            status: response.status,
            body
          });
          throw new Error(`backend decision request failed: ${response.status}${body ? ` ${body}` : ""}`);
        }
        if (response.status === 204) return void 0;
        return await response.json();
      };
      return {
        async getContentUnitDecision(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await request(`/decisions?target_kind=content_unit&target_ref=${encodeURIComponent(targetRef)}`);
          if (isMovScriptDecisionStoreDebugEnabled2()) {
            console.info("[movscript-decision-store] get content unit decision", {
              projectId: options.projectId,
              contentUnitId: input3.contentUnitId,
              targetRef,
              found: Boolean(context),
              candidateCount: context?.candidates.length ?? 0,
              candidateIds: decisionCandidateIds2(context?.candidates ?? []),
              hasSelection: Boolean(context?.selection)
            });
          }
          return context;
        },
        async getContentUnitDecisions(input3) {
          const ids = uniqueContentUnitIds2(input3.contentUnitIds);
          if (ids.length === 0) return /* @__PURE__ */ new Map();
          const contexts = await request("/decisions/query", {
            method: "POST",
            body: JSON.stringify({
              target_kind: "content_unit",
              target_refs: ids.map(contentUnitDecisionTargetRef2)
            })
          }) ?? [];
          if (isMovScriptDecisionStoreDebugEnabled2()) {
            console.info("[movscript-decision-store] get content unit decisions", {
              projectId: options.projectId,
              requestedCount: ids.length,
              foundCount: contexts.length
            });
          }
          const byTargetRef = new Map(contexts.map((context) => [context.target_ref, context]));
          const out = /* @__PURE__ */ new Map();
          for (const id of ids) {
            const context = byTargetRef.get(contentUnitDecisionTargetRef2(id));
            if (context) out.set(String(id), context);
          }
          return out;
        },
        async replaceContentUnitCandidates(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await requiredDecisionContext2(request("/decisions/candidates", {
            method: "PUT",
            body: JSON.stringify({
              target_kind: "content_unit",
              target_ref: targetRef,
              candidates: input3.candidates
            })
          }));
          console.info("[movscript-decision-store] replace content unit candidates", {
            projectId: options.projectId,
            contentUnitId: input3.contentUnitId,
            targetRef,
            candidateCount: context.candidates.length,
            candidateIds: decisionCandidateIds2(context.candidates)
          });
          return context;
        },
        async upsertContentUnitCandidate(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await requiredDecisionContext2(request("/decisions/candidates", {
            method: "POST",
            body: JSON.stringify({
              target_kind: "content_unit",
              target_ref: targetRef,
              candidate: input3.candidate
            })
          }));
          console.info("[movscript-decision-store] upsert content unit candidate", {
            projectId: options.projectId,
            contentUnitId: input3.contentUnitId,
            targetRef,
            candidateId: idField52(input3.candidate.id),
            candidateCount: context.candidates.length,
            candidateIds: decisionCandidateIds2(context.candidates)
          });
          return context;
        },
        async selectContentUnitCandidate(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await requiredDecisionContext2(request("/decisions/selection", {
            method: "PUT",
            body: JSON.stringify({
              target_kind: "content_unit",
              target_ref: targetRef,
              candidate_id: stringIdField2(input3.candidateId),
              resource_id: input3.resourceId === void 0 ? void 0 : requiredResourceId32(input3.resourceId),
              stale_policy: input3.stalePolicy,
              reason: input3.reason,
              selected_at: input3.selectedAt,
              metadata: input3.metadata
            })
          }));
          console.info("[movscript-decision-store] select content unit candidate", {
            projectId: options.projectId,
            contentUnitId: input3.contentUnitId,
            targetRef,
            candidateId: stringIdField2(input3.candidateId),
            candidateCount: context.candidates.length,
            candidateIds: decisionCandidateIds2(context.candidates),
            hasSelection: Boolean(context.selection)
          });
          return context;
        },
        clearContentUnitSelection(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          return requiredDecisionContext2(request(`/decisions/selection?target_kind=content_unit&target_ref=${encodeURIComponent(targetRef)}`, {
            method: "DELETE"
          }));
        }
      };
    }
    function createMovScriptScopedProjectDataDecisionStore2(options) {
      const baseUrl = options.baseUrl.replace(/\/+$/, "");
      const fetchImpl = options.fetch ?? globalThis.fetch;
      if (!fetchImpl) throw new Error("fetch is required for scoped project data decision store");
      const headers = () => ({
        "content-type": "application/json",
        ...options.token ? { authorization: `Bearer ${options.token}` } : {},
        ...options.headers ?? {}
      });
      const scopePayload = () => pruneUndefined112({
        scope_kind: options.scopeKind,
        scope_id: options.scopeId === void 0 ? void 0 : String(options.scopeId),
        project_uid: options.projectUid,
        title: options.title
      });
      const targetQuery = (targetRef) => {
        const query = new URLSearchParams();
        query.set("project_uid", options.projectUid);
        query.set("target_kind", "content_unit");
        query.set("target_ref", targetRef);
        if (options.scopeKind) query.set("scope_kind", options.scopeKind);
        if (options.scopeId !== void 0) query.set("scope_id", String(options.scopeId));
        return query.toString();
      };
      const request = async (path, init = {}) => {
        const response = await fetchImpl(`${baseUrl}/api/v1/project-data${path}`, {
          ...init,
          headers: {
            ...headers(),
            ...init.headers ?? {}
          }
        });
        if (response.status === 404) {
          console.info("[movscript-decision-store] scoped project data request not found", {
            projectUid: options.projectUid,
            scopeKind: options.scopeKind,
            scopeId: options.scopeId,
            method: init.method ?? "GET",
            path,
            status: response.status
          });
          return void 0;
        }
        if (!response.ok) {
          const body = await response.text().catch(() => "");
          console.warn("[movscript-decision-store] scoped project data request failed", {
            projectUid: options.projectUid,
            scopeKind: options.scopeKind,
            scopeId: options.scopeId,
            method: init.method ?? "GET",
            path,
            status: response.status,
            body
          });
          throw new Error(`scoped project data decision request failed: ${response.status}${body ? ` ${body}` : ""}`);
        }
        if (response.status === 204) return void 0;
        return await response.json();
      };
      return {
        async getContentUnitDecision(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await request(`/decisions?${targetQuery(targetRef)}`);
          if (isMovScriptDecisionStoreDebugEnabled2()) {
            console.info("[movscript-decision-store] get scoped content unit decision", {
              projectUid: options.projectUid,
              contentUnitId: input3.contentUnitId,
              targetRef,
              found: Boolean(context),
              candidateCount: context?.candidates.length ?? 0
            });
          }
          return context;
        },
        async getContentUnitDecisions(input3) {
          const ids = uniqueContentUnitIds2(input3.contentUnitIds);
          if (ids.length === 0) return /* @__PURE__ */ new Map();
          const contexts = await request("/decisions/query", {
            method: "POST",
            body: JSON.stringify({
              ...scopePayload(),
              target_kind: "content_unit",
              target_refs: ids.map(contentUnitDecisionTargetRef2)
            })
          }) ?? [];
          if (isMovScriptDecisionStoreDebugEnabled2()) {
            console.info("[movscript-decision-store] get scoped content unit decisions", {
              projectUid: options.projectUid,
              requestedCount: ids.length,
              foundCount: contexts.length
            });
          }
          const byTargetRef = new Map(contexts.map((context) => [context.target_ref, context]));
          const out = /* @__PURE__ */ new Map();
          for (const id of ids) {
            const context = byTargetRef.get(contentUnitDecisionTargetRef2(id));
            if (context) out.set(String(id), context);
          }
          return out;
        },
        async replaceContentUnitCandidates(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await requiredDecisionContext2(request("/decisions/candidates", {
            method: "PUT",
            body: JSON.stringify({
              ...scopePayload(),
              target_kind: "content_unit",
              target_ref: targetRef,
              candidates: input3.candidates
            })
          }));
          console.info("[movscript-decision-store] replace scoped content unit candidates", {
            projectUid: options.projectUid,
            contentUnitId: input3.contentUnitId,
            targetRef,
            candidateCount: context.candidates.length
          });
          return context;
        },
        async upsertContentUnitCandidate(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await requiredDecisionContext2(request("/decisions/candidates", {
            method: "POST",
            body: JSON.stringify({
              ...scopePayload(),
              target_kind: "content_unit",
              target_ref: targetRef,
              candidate: input3.candidate
            })
          }));
          console.info("[movscript-decision-store] upsert scoped content unit candidate", {
            projectUid: options.projectUid,
            contentUnitId: input3.contentUnitId,
            targetRef,
            candidateId: idField52(input3.candidate.id),
            candidateCount: context.candidates.length
          });
          return context;
        },
        async selectContentUnitCandidate(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          const context = await requiredDecisionContext2(request("/decisions/selection", {
            method: "PUT",
            body: JSON.stringify({
              ...scopePayload(),
              target_kind: "content_unit",
              target_ref: targetRef,
              candidate_id: stringIdField2(input3.candidateId),
              resource_id: input3.resourceId === void 0 ? void 0 : requiredResourceId32(input3.resourceId),
              stale_policy: input3.stalePolicy,
              reason: input3.reason,
              selected_at: input3.selectedAt,
              metadata: input3.metadata
            })
          }));
          console.info("[movscript-decision-store] select scoped content unit candidate", {
            projectUid: options.projectUid,
            contentUnitId: input3.contentUnitId,
            targetRef,
            candidateId: stringIdField2(input3.candidateId),
            candidateCount: context.candidates.length,
            hasSelection: Boolean(context.selection)
          });
          return context;
        },
        clearContentUnitSelection(input3) {
          const targetRef = contentUnitDecisionTargetRef2(input3.contentUnitId);
          return requiredDecisionContext2(request(`/decisions/selection?${targetQuery(targetRef)}`, {
            method: "DELETE"
          }));
        }
      };
    }
    async function overlayMovScriptDecisionDocuments2(documents, decisionStore) {
      if (!decisionStore) return documents;
      const sourceDocuments = documents.filter((document) => !isMovScriptContentUnitDecisionPath2(document.path));
      const baseIndex = deriveMovScriptWorkspaceDomainIndex2(sourceDocuments);
      const contentUnits = baseIndex.byKind.get("content_unit") ?? [];
      const decisionsByContentUnitId = await getOverlayDecisionContexts2(decisionStore, contentUnits);
      const overlays = [];
      const rows = [];
      for (const contentUnit of contentUnits) {
        if (contentUnit.id === void 0) continue;
        const context = decisionsByContentUnitId.get(String(contentUnit.id));
        if (!context) continue;
        const contentUnitRef = entityDir23(contentUnit.path);
        rows.push({
          contentUnitId: contentUnit.id,
          targetRef: context.target_ref,
          candidateCount: context.candidates.length,
          candidateIds: decisionCandidateIds2(context.candidates)
        });
        overlays.push({
          path: contentUnitDecisionContextPath2(contentUnit.id),
          data: normalizeDecisionContext2(context, contentUnitRef)
        });
        for (const candidate of context.candidates) {
          const candidateId = idField52(candidate.id);
          if (candidateId === void 0) continue;
          overlays.push({
            path: `${contentUnitRef}/candidates/${entityPathSlug2(candidateId, "candidate")}/content_candidate.json`,
            data: normalizeContentUnitCandidate2(candidate, contentUnitRef)
          });
        }
      }
      if (rows.length > 0 && isMovScriptDecisionStoreDebugEnabled2()) {
        console.info("[movscript-decision-store] overlay content unit decisions", {
          contentUnitCount: contentUnits.length,
          rows
        });
      }
      const overlayPaths = new Set(overlays.map((document) => document.path));
      return [
        ...sourceDocuments.filter((document) => !overlayPaths.has(document.path)),
        ...overlays
      ].sort((left, right) => left.path.localeCompare(right.path));
    }
    async function getOverlayDecisionContexts2(decisionStore, contentUnits) {
      const contentUnitIds = uniqueContentUnitIds2(contentUnits.map((unit) => unit.id).filter((id) => id !== void 0));
      if (contentUnitIds.length === 0) return /* @__PURE__ */ new Map();
      if (decisionStore.getContentUnitDecisions) {
        return decisionStore.getContentUnitDecisions({ contentUnitIds });
      }
      const out = /* @__PURE__ */ new Map();
      for (const contentUnitId of contentUnitIds) {
        const context = await decisionStore.getContentUnitDecision({ contentUnitId });
        if (context) out.set(String(contentUnitId), context);
      }
      return out;
    }
    function uniqueContentUnitIds2(values) {
      const seen = /* @__PURE__ */ new Set();
      const out = [];
      for (const value of values) {
        const key = String(value);
        if (seen.has(key)) continue;
        seen.add(key);
        out.push(value);
      }
      return out;
    }
    function isMovScriptDecisionStoreDebugEnabled2() {
      return typeof process !== "undefined" && process.env?.MOVSCRIPT_DECISION_STORE_DEBUG === "1";
    }
    function contentUnitDecisionTargetRef2(contentUnitId) {
      return `content_units/${entityPathSlug2(contentUnitId, "content_unit")}`;
    }
    function contentUnitDecisionContextPath2(contentUnitId) {
      return `.movscript/decisions/content_units/${entityPathSlug2(contentUnitId, "content_unit")}/decision_context.json`;
    }
    function normalizeDecisionContext2(context, targetRef = context.target_ref) {
      return pruneUndefined112({
        ...context,
        schema: "movscript.decision_context.v1",
        target_kind: context.target_kind,
        target_ref: targetRef,
        candidates: context.candidates.map((candidate) => normalizeContentUnitCandidate2(candidate, targetRef)),
        selection: isRecord162(context.selection) ? normalizeContentUnitDecisionSelection2(context.selection) : void 0
      });
    }
    function normalizeContentUnitCandidate2(candidate, contentUnitRef) {
      return pruneUndefined112({
        ...candidate,
        schema: candidate.schema ?? "movscript.content_candidate.v1",
        content_unit_ref: candidate.content_unit_ref ?? contentUnitRef
      });
    }
    function normalizeContentUnitDecisionSelection2(selection) {
      return pruneUndefined112({
        candidate_id: selection.candidate_id,
        resource_id: selection.resource_id,
        stale_policy: selection.stale_policy === "accept_stale" ? "accept_stale" : "strict",
        reason: selection.reason,
        selected_at: selection.selected_at,
        selected_by: selection.selected_by,
        metadata: isRecord162(selection.metadata) ? selection.metadata : void 0
      });
    }
    async function requiredDecisionContext2(promise) {
      const context = await promise;
      if (!context) throw new Error("backend decision context not found");
      return context;
    }
    function entityDir23(path) {
      return path.replace(/\/[^/]+$/, "");
    }
    function idField52(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function decisionCandidateIds2(candidates) {
      return candidates.map((candidate) => idField52(candidate.id)).filter((id) => id !== void 0);
    }
    function stringIdField2(value) {
      const id = idField52(value);
      return id === void 0 ? void 0 : String(id);
    }
    function requiredResourceId32(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      throw new Error("resource_id must be a positive integer RawResource ID");
    }
    function isRecord162(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined112(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    function buildMovScriptContentCandidate2(input3) {
      const contentUnitId = stableEntityId22(input3.contentUnitId, "content_unit");
      const candidateId = stableEntityId22(input3.candidateId ?? `candidate_${Date.now()}`, "candidate");
      const path = `${contentUnitDirectory22(contentUnitId)}/candidates/${entityPathSlug2(candidateId, "candidate")}/content_candidate.json`;
      const record = pruneUndefined122({
        schema: "movscript.content_candidate.v1",
        id: candidateId,
        content_unit_ref: contentUnitDirectory22(contentUnitId),
        source: input3.source ?? "ai_generate",
        status: input3.status ?? "succeeded",
        producer: input3.producer ?? { kind: "runtime" },
        outputs: normalizeOutputs2(input3.outputs),
        prompt_snapshot: input3.promptSnapshot,
        created_at: input3.createdAt ?? (/* @__PURE__ */ new Date()).toISOString()
      });
      return { path, record };
    }
    function contentUnitDirectory22(id) {
      return `content_units/${entityPathSlug2(id, "content_unit")}`;
    }
    function stableEntityId22(value, prefix) {
      return semanticEntityId2(value, prefix);
    }
    function normalizeOutputs2(outputs) {
      return outputs.map((output3, index) => {
        const resourceId = requiredResourceId42(output3.resource_id, `outputs[${index}].resource_id`);
        return pruneUndefined122({
          ...output3,
          resource_id: resourceId,
          artifact_ref: stringField102(output3.artifact_ref)
        });
      });
    }
    function requiredResourceId42(value, name) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      throw new Error(`${name} must be a positive integer RawResource ID`);
    }
    function stringField102(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function pruneUndefined122(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    async function upsertMovScriptProjectStandards2(input3) {
      const current = stripWorkspacePrivateFields52(input3.record ?? {});
      const projectStyle = {
        ...parseProjectStyle3(current.project_style),
        ...input3.projectStyle
      };
      const record = pruneUndefined132({
        ...current,
        schema: "movscript.project_standards.v1",
        kind: "project_standards",
        id: "project_standards",
        title: stringValue102(current.title) ?? "Project standards",
        aspect_ratio: stringValue102(projectStyle.aspect_ratio ?? current.aspect_ratio),
        visual_style: stringValue102(projectStyle.visual_style ?? current.visual_style),
        project_style: JSON.stringify(projectStyle),
        updated_at: (input3.now ?? /* @__PURE__ */ new Date()).toISOString()
      });
      const path = "project_standards.json";
      await input3.fileRepository.write({ path, content: `${JSON.stringify(record, null, 2)}
` });
      return { path, record };
    }
    function parseProjectStyle3(value) {
      if (isRecord172(value)) return value;
      if (typeof value !== "string" || !value.trim()) return {};
      try {
        const parsed = JSON.parse(value);
        return isRecord172(parsed) ? parsed : {};
      } catch {
        return {};
      }
    }
    function stripWorkspacePrivateFields52(record) {
      const output3 = {};
      for (const [key, value] of Object.entries(record)) {
        if (key.startsWith("__workspace_")) continue;
        output3[key] = value;
      }
      return output3;
    }
    function stringValue102(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function isRecord172(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined132(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0 && item !== "") output3[key] = item;
      }
      return output3;
    }
    var MOVSCRIPT_PROJECT_STANDARD_SKILL_PATHS2 = [
      ".codex/skills/plugins/movscript_project-standards/project-standards/SKILL.md",
      ".claude/skills/plugins/movscript_project-standards/project-standards/SKILL.md",
      ".mova/skills/plugins/movscript_project-standards/project-standards/SKILL.md"
    ];
    async function syncMovScriptProjectStandardSkills2(input3) {
      const content = renderMovScriptProjectStandardSkill2(input3.standards);
      const results = [];
      for (const path of MOVSCRIPT_PROJECT_STANDARD_SKILL_PATHS2) {
        await input3.fileRepository.write({ path, content });
        results.push({ path, content });
      }
      return results;
    }
    function renderMovScriptProjectStandardSkill2(standards) {
      const projectStyle = parseProjectStyle23(standards.project_style);
      const coreSections = [
        fieldSection2("Aspect ratio", standards.aspect_ratio ?? projectStyle.aspect_ratio),
        fieldSection2("Visual style", standards.visual_style ?? projectStyle.visual_style),
        fieldSection2("Lighting style", standards.lighting_style ?? projectStyle.lighting_style),
        fieldSection2("Camera language", standards.camera_language ?? projectStyle.camera_language),
        fieldSection2("Color palette", standards.color_palette ?? projectStyle.color_palette),
        fieldSection2("Pacing rules", standards.pacing_rules ?? projectStyle.pacing_rules),
        listSection2("Shot size system", standards.shot_size_system ?? projectStyle.shot_size_system),
        listSection2("Negative rules", standards.negative_rules ?? projectStyle.negative_rules),
        customRulesSection2(standards.custom_rules ?? projectStyle.custom_rules),
        styleReferenceSection2(standards, projectStyle)
      ].filter(Boolean);
      return [
        "---",
        "name: project-standards",
        "description: Apply the current MovScript project standards from project_standards.json when planning, coding, prompting, or generating project-scoped work.",
        "---",
        "",
        "# MovScript Project Standards",
        "",
        "Generated from `project_standards.json`. Do not edit this file manually; update project standards through MovScript source or domain tools.",
        "",
        "Use this skill whenever work depends on this MovScript project, especially planning, content-unit prompts, generation, style-sensitive code, or provider behavior.",
        "",
        "## Rules",
        "",
        "- Treat `project_standards.json` as the source of truth.",
        "- Apply these standards as project-wide house style and constraints.",
        "- Do not change project standards unless the user explicitly asks to add, remove, replace, or refine them.",
        "- For visual generation that supports references, pass listed style reference resource ids as house-style references.",
        "",
        ...coreSections.length ? ["## Current Standards", "", ...coreSections] : ["## Current Standards", "", "- No project standards are currently filled.", ""]
      ].join("\n");
    }
    function fieldSection2(label, value) {
      const text = textValue2(value);
      return text ? `### ${label}

${text}
` : void 0;
    }
    function listSection2(label, value) {
      const items = arrayValues2(value);
      if (!items.length) return void 0;
      return `### ${label}

${items.map((item) => `- ${item}`).join("\n")}
`;
    }
    function customRulesSection2(value) {
      const rules = arrayRecords3(value).filter((rule) => rule.enabled !== false).map((rule) => {
        const key = textValue2(rule.key) ?? "custom_rule";
        const ruleValue = rule.value ?? rule.text ?? rule.description;
        const text = textValue2(ruleValue) ?? JSON.stringify(ruleValue);
        return text ? `- ${key}: ${text}` : void 0;
      }).filter((rule) => Boolean(rule));
      if (!rules.length) return void 0;
      return `### Custom rules

${rules.join("\n")}
`;
    }
    function styleReferenceSection2(standards, projectStyle) {
      const ids = uniqueNumbers2([
        ...resourceIdsFromValue4(standards.style_reference_images),
        ...resourceIdsFromValue4(standards.style_references),
        ...resourceIdsFromValue4(standards.reference_resource_ids),
        ...resourceIdsFromValue4(projectStyle.style_reference_images),
        ...resourceIdsFromValue4(projectStyle.style_references),
        ...resourceIdsFromValue4(projectStyle.reference_resource_ids),
        ...arrayRecords3(standards.custom_rules).filter((rule) => rule.enabled !== false).filter((rule) => textValue2(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue4(rule.value)),
        ...arrayRecords3(projectStyle.custom_rules).filter((rule) => rule.enabled !== false).filter((rule) => textValue2(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue4(rule.value))
      ]);
      if (!ids.length) return void 0;
      return `### Style reference resource ids

${ids.map((id) => `- ${id}`).join("\n")}
`;
    }
    function resourceIdsFromValue4(value) {
      if (Array.isArray(value)) return value.flatMap(resourceIdsFromValue4);
      if (typeof value === "number" && Number.isInteger(value)) return [value];
      if (isRecord182(value)) {
        return [
          ...resourceIdsFromValue4(value.resource_id),
          ...resourceIdsFromValue4(value.resource_ids),
          ...resourceIdsFromValue4(value.id)
        ];
      }
      const text = textValue2(value);
      if (!text) return [];
      return Array.from(text.matchAll(/\b(?:resource[_-]?id|resource)[_:#\s-]*(\d+)\b/gi)).map((match) => Number(match[1])).filter((id) => Number.isInteger(id));
    }
    function parseProjectStyle23(value) {
      if (isRecord182(value)) return value;
      const text = textValue2(value);
      if (!text) return {};
      try {
        const parsed = JSON.parse(text);
        return isRecord182(parsed) ? parsed : {};
      } catch {
        return {};
      }
    }
    function arrayValues2(value) {
      if (Array.isArray(value)) return value.map(textValue2).filter((item) => Boolean(item));
      const text = textValue2(value);
      return text ? [text] : [];
    }
    function arrayRecords3(value) {
      return Array.isArray(value) ? value.filter(isRecord182) : [];
    }
    function uniqueNumbers2(values) {
      return Array.from(new Set(values)).sort((left, right) => left - right);
    }
    function textValue2(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function isRecord182(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function deriveMovScriptWorkspacePreviewTimelines3(index) {
      const sourceEntities = canonicalEntities4(index);
      const contentUnitsByPrimaryRef = groupContentUnitsByPrimaryRef4(index);
      return sourceEntities.filter(isProductionWithId3).map((production) => {
        const productionDir = entityDir33(production.path);
        const segments = childEntities3(index, productionDir, "segment");
        const items = [];
        let order = 0;
        for (const segment of sortEntities4(segments)) {
          const segmentItemId = timelineItemId3(segment);
          items.push({
            ...timelineItem3(segmentItemId, "segment", segment, order++),
            transition: recordField23(segment.record.transition)
          });
          const sceneMoments = childEntities3(index, entityDir33(segment.path), "scene_moment");
          for (const sceneMoment of sortEntities4(sceneMoments)) {
            const sceneMomentItemId = timelineItemId3(sceneMoment);
            const sceneMomentContentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "scene_moment", sceneMoment);
            items.push({
              ...timelineItem3(sceneMomentItemId, "scene_moment", sceneMoment, order++),
              parentId: segmentItemId,
              transition: recordField23(sceneMoment.record.transition),
              contentUnitIds: sceneMomentContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
            });
            for (const contentUnit of sortEntities4(sceneMomentContentUnits)) {
              items.push({
                ...timelineItem3(timelineItemId3(contentUnit), "content_unit", contentUnit, order++),
                parentId: sceneMomentItemId
              });
            }
            for (const storyboard of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "storyboard"))) {
              const storyboardItemId = timelineItemId3(storyboard);
              const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "storyboard", storyboard);
              const timeline = recordField23(storyboard.record.timeline);
              items.push({
                ...timelineItem3(storyboardItemId, "storyboard", storyboard, order++),
                parentId: sceneMomentItemId,
                caption: stringField112(timeline?.caption),
                gapAfterSec: numberField25(timeline?.gap_after_sec),
                timing: timeline,
                transition: recordField23(storyboard.record.transition),
                contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
              });
              for (const contentUnit of sortEntities4(contentUnits)) {
                items.push({
                  ...timelineItem3(timelineItemId3(contentUnit), "content_unit", contentUnit, order++),
                  parentId: storyboardItemId
                });
              }
            }
            for (const keyframe of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "keyframe"))) {
              const keyframeItemId = timelineItemId3(keyframe);
              const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "keyframe", keyframe);
              items.push({
                ...timelineItem3(keyframeItemId, "keyframe", keyframe, order++),
                parentId: sceneMomentItemId,
                timing: recordField23(keyframe.record.timing),
                contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
              });
              for (const contentUnit of sortEntities4(contentUnits)) {
                items.push({
                  ...timelineItem3(timelineItemId3(contentUnit), "content_unit", contentUnit, order++),
                  parentId: keyframeItemId
                });
              }
            }
            for (const expressionUnit of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "expression_unit"))) {
              const expressionUnitItemId = timelineItemId3(expressionUnit);
              const expressionUnitContentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "expression_unit", expressionUnit);
              items.push({
                ...timelineItem3(expressionUnitItemId, "expression_unit", expressionUnit, order++),
                parentId: sceneMomentItemId,
                contentUnitIds: expressionUnitContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
              });
              for (const contentUnit of sortEntities4(expressionUnitContentUnits)) {
                items.push({
                  ...timelineItem3(timelineItemId3(contentUnit), "content_unit", contentUnit, order++),
                  parentId: expressionUnitItemId
                });
              }
              for (const storyboard of sortEntities4(childEntities3(index, entityDir33(expressionUnit.path), "storyboard"))) {
                const storyboardItemId = timelineItemId3(storyboard);
                const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "storyboard", storyboard);
                const timeline = recordField23(storyboard.record.timeline);
                items.push({
                  ...timelineItem3(storyboardItemId, "storyboard", storyboard, order++),
                  parentId: expressionUnitItemId,
                  caption: stringField112(timeline?.caption),
                  gapAfterSec: numberField25(timeline?.gap_after_sec),
                  timing: timeline,
                  transition: recordField23(storyboard.record.transition),
                  contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
                });
                for (const contentUnit of sortEntities4(contentUnits)) {
                  items.push({
                    ...timelineItem3(timelineItemId3(contentUnit), "content_unit", contentUnit, order++),
                    parentId: storyboardItemId
                  });
                }
              }
              for (const keyframe of sortEntities4(childEntities3(index, entityDir33(expressionUnit.path), "keyframe"))) {
                const keyframeItemId = timelineItemId3(keyframe);
                const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "keyframe", keyframe);
                items.push({
                  ...timelineItem3(keyframeItemId, "keyframe", keyframe, order++),
                  parentId: expressionUnitItemId,
                  timing: recordField23(keyframe.record.timing),
                  contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
                });
                for (const contentUnit of sortEntities4(contentUnits)) {
                  items.push({
                    ...timelineItem3(timelineItemId3(contentUnit), "content_unit", contentUnit, order++),
                    parentId: keyframeItemId
                  });
                }
              }
            }
            for (const audioCue of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "audio_cue"))) {
              items.push({
                ...timelineItem3(timelineItemId3(audioCue), "audio_cue", audioCue, order++),
                parentId: sceneMomentItemId,
                cueKind: stringField112(audioCue.record.cue_kind),
                timing: recordField23(audioCue.record.timing)
              });
            }
          }
        }
        return {
          schema: "movscript.preview_timeline.v1",
          productionId: production.id,
          productionPath: productionDir,
          items
        };
      });
    }
    function deriveMovScriptWorkspaceTimelineAssemblyPreviewTimeline2(index, scope) {
      const parsedTarget = parseImplicitTimelineAssemblyRef2(scope.targetRef);
      const scopeKind = parsedTarget?.scopeKind ?? scope.scopeKind;
      const scopeRef = parsedTarget?.scopeRef ?? String(scope.scopeRef);
      const scopeNode = timelineNamespaceNodeForScope2(index, scopeKind, scopeRef);
      if (!scopeNode?.path) return void 0;
      const scopeEntity = canonicalEntities4(index).find((entity) => entity.path === scopeNode.path);
      const scopeDir = entityDir33(scopeNode.path);
      const contentUnitsByPrimaryRef = groupContentUnitsByPrimaryRef4(index);
      const items = [];
      let order = 0;
      const appendTimelineNamespace = (namespace, parentId) => {
        const namespaceItemId = timelineNamespaceItemId2(namespace);
        items.push({
          ...timelineItem3(namespaceItemId, "timeline_namespace", namespace, order++),
          ...parentId ? { parentId } : {}
        });
        appendNamespaceChildren(entityDir33(namespace.path), namespaceItemId);
      };
      const appendSceneMoment = (sceneMoment, parentId) => {
        const sceneMomentItemId = timelineItemId3(sceneMoment);
        const sceneMomentContentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "scene_moment", sceneMoment);
        items.push({
          ...timelineItem3(sceneMomentItemId, "scene_moment", sceneMoment, order++),
          ...parentId ? { parentId } : {},
          transition: recordField23(sceneMoment.record.transition),
          contentUnitIds: sceneMomentContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
        });
        appendContentUnits(sceneMomentContentUnits, sceneMomentItemId);
        appendSceneMomentChildren(sceneMoment, sceneMomentItemId);
      };
      const appendContentUnits = (contentUnits, parentId) => {
        for (const contentUnit of sortEntities4(contentUnits)) {
          items.push({
            ...timelineItem3(timelineItemId3(contentUnit), "content_unit", contentUnit, order++),
            parentId
          });
        }
      };
      const appendSceneMomentChildren = (sceneMoment, sceneMomentItemId) => {
        for (const storyboard of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "storyboard"))) {
          const storyboardItemId = timelineItemId3(storyboard);
          const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "storyboard", storyboard);
          const timeline = recordField23(storyboard.record.timeline);
          items.push({
            ...timelineItem3(storyboardItemId, "storyboard", storyboard, order++),
            parentId: sceneMomentItemId,
            caption: stringField112(timeline?.caption),
            gapAfterSec: numberField25(timeline?.gap_after_sec),
            timing: timeline,
            transition: recordField23(storyboard.record.transition),
            contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
          });
          appendContentUnits(contentUnits, storyboardItemId);
        }
        for (const keyframe of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "keyframe"))) {
          const keyframeItemId = timelineItemId3(keyframe);
          const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "keyframe", keyframe);
          items.push({
            ...timelineItem3(keyframeItemId, "keyframe", keyframe, order++),
            parentId: sceneMomentItemId,
            timing: recordField23(keyframe.record.timing),
            contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
          });
          appendContentUnits(contentUnits, keyframeItemId);
        }
        for (const expressionUnit of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "expression_unit"))) {
          const expressionUnitItemId = timelineItemId3(expressionUnit);
          const expressionUnitContentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "expression_unit", expressionUnit);
          items.push({
            ...timelineItem3(expressionUnitItemId, "expression_unit", expressionUnit, order++),
            parentId: sceneMomentItemId,
            contentUnitIds: expressionUnitContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
          });
          appendContentUnits(expressionUnitContentUnits, expressionUnitItemId);
          for (const storyboard of sortEntities4(childEntities3(index, entityDir33(expressionUnit.path), "storyboard"))) {
            const storyboardItemId = timelineItemId3(storyboard);
            const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "storyboard", storyboard);
            const timeline = recordField23(storyboard.record.timeline);
            items.push({
              ...timelineItem3(storyboardItemId, "storyboard", storyboard, order++),
              parentId: expressionUnitItemId,
              caption: stringField112(timeline?.caption),
              gapAfterSec: numberField25(timeline?.gap_after_sec),
              timing: timeline,
              transition: recordField23(storyboard.record.transition),
              contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
            });
            appendContentUnits(contentUnits, storyboardItemId);
          }
          for (const keyframe of sortEntities4(childEntities3(index, entityDir33(expressionUnit.path), "keyframe"))) {
            const keyframeItemId = timelineItemId3(keyframe);
            const contentUnits = contentUnitsForEntity3(contentUnitsByPrimaryRef, "keyframe", keyframe);
            items.push({
              ...timelineItem3(keyframeItemId, "keyframe", keyframe, order++),
              parentId: expressionUnitItemId,
              timing: recordField23(keyframe.record.timing),
              contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined6)
            });
            appendContentUnits(contentUnits, keyframeItemId);
          }
        }
        for (const audioCue of sortEntities4(childEntities3(index, entityDir33(sceneMoment.path), "audio_cue"))) {
          items.push({
            ...timelineItem3(timelineItemId3(audioCue), "audio_cue", audioCue, order++),
            parentId: sceneMomentItemId,
            cueKind: stringField112(audioCue.record.cue_kind),
            timing: recordField23(audioCue.record.timing)
          });
        }
      };
      const appendNamespaceChildren = (parentDir, parentId) => {
        for (const namespace of sortEntities4(childEntities3(index, parentDir, "segment"))) {
          appendTimelineNamespace(namespace, parentId);
        }
        for (const sceneMoment of sortEntities4(childEntities3(index, parentDir, "scene_moment"))) {
          appendSceneMoment(sceneMoment, parentId);
        }
      };
      appendNamespaceChildren(scopeDir);
      return {
        schema: "movscript.preview_timeline.v1",
        targetKind: "timeline_assembly",
        targetRef: scope.targetRef ?? implicitTimelineAssemblyRef2(scopeKind, scopeRef),
        scopeKind,
        scopeRef,
        scopePath: scopeDir,
        scopeTitle: scopeNode.title,
        ...scopeEntity?.entityKind === "production" && scopeEntity.id !== void 0 ? {
          productionId: scopeEntity.id,
          productionPath: scopeDir
        } : {},
        items
      };
    }
    function groupContentUnitsByPrimaryRef4(index) {
      const out = /* @__PURE__ */ new Map();
      for (const entity of canonicalEntities4(index)) {
        if (entity.entityKind !== "content_unit") continue;
        const contentUnitType22 = stringField112(entity.record.content_unit_type);
        if (!contentUnitType22 || !hasSpecializedContentUnitType2(contentUnitType22)) continue;
        const assemblyScope = timelineAssemblyScopeFromContentUnitRecord2(entity.record);
        if (assemblyScope) {
          for (const key of primaryRefKeys4(assemblyScope.kind, assemblyScope.ref)) {
            out.set(key, [...out.get(key) ?? [], entity]);
          }
        }
        const primaryKind = primaryRefKindForContentUnitType4(contentUnitType22);
        if (!primaryKind) continue;
        const primaryRefs = primaryRefIdsForContentUnitRecord4(entity.record, primaryKind);
        if (primaryRefs.length !== 1) continue;
        const primaryRef = primaryRefs[0];
        if (!primaryRef) continue;
        for (const key of primaryRefKeys4(primaryKind, primaryRef)) {
          out.set(key, [...out.get(key) ?? [], entity]);
        }
      }
      return out;
    }
    function contentUnitsForEntity3(contentUnitsByPrimaryRef, entityKind, entity) {
      if (entity.id === void 0) return [];
      return contentUnitsByPrimaryRef.get(primaryRefKey4(entityKind, entity.id)) ?? [];
    }
    function primaryRefKey4(kind, id) {
      return `${kind}:${String(id)}`;
    }
    function primaryRefKeys4(kind, ref) {
      const value = String(ref);
      const keys = [primaryRefKey4(kind, value)];
      const lastSegment = value.split("/").filter(Boolean).at(-1);
      if (lastSegment && lastSegment !== value) keys.push(primaryRefKey4(kind, lastSegment));
      return keys;
    }
    function timelineItem3(id, itemType, entity, order) {
      return {
        id,
        itemType,
        entity: entityRef23(entity),
        order,
        title: stringField112(entity.record.title) ?? String(entity.id ?? entity.path)
      };
    }
    function timelineItemId3(entity) {
      return `${entity.entityKind}:${String(entity.id ?? entity.path)}`;
    }
    function timelineNamespaceItemId2(entity) {
      return `timeline_namespace:${String(entity.id ?? entity.path)}`;
    }
    function entityRef23(entity) {
      return {
        entityKind: entity.entityKind,
        ...entity.id !== void 0 ? { id: entity.id } : {},
        path: entity.path
      };
    }
    function childEntities3(index, parentDir, entityKind) {
      const collectionName = collectionDirForEntityKind3(entityKind);
      if (!collectionName) return [];
      return canonicalEntities4(index).filter((entity) => entity.entityKind === entityKind && entity.path.startsWith(`${parentDir}/${collectionName}/`) && entityDir33(entity.path).replace(`${parentDir}/${collectionName}/`, "").split("/").length === 1);
    }
    function collectionDirForEntityKind3(entityKind) {
      if (entityKind === "segment") return "segments";
      if (entityKind === "scene_moment") return "scene_moments";
      if (entityKind === "storyboard") return "storyboards";
      if (entityKind === "keyframe") return "keyframes";
      if (entityKind === "audio_cue") return "audio_cues";
      if (entityKind === "expression_unit") return "expression_units";
      return void 0;
    }
    function canonicalEntities4(index) {
      return index.entities;
    }
    function isProductionWithId3(entity) {
      return entity.entityKind === "production" && entity.id !== void 0;
    }
    function timelineNamespaceNodeForScope2(index, scopeKind, scopeRef) {
      const normalizedRef = normalizeRef2(scopeRef);
      return index.domainNodes.find((node) => {
        if (node.category !== "timeline_namespace") return false;
        if (node.kind !== scopeKind && node.metadata?.entityKind !== scopeKind) return false;
        const nodePath = node.path ? normalizeRef2(node.path) : void 0;
        const nodeDir = node.path ? normalizeRef2(entityDir33(node.path)) : void 0;
        return normalizeRef2(node.id) === normalizedRef || nodePath === normalizedRef || nodeDir === normalizedRef;
      });
    }
    function normalizeRef2(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      if (typeof value !== "string" || !value.trim()) return void 0;
      return value.trim().replace(/^\/+|\/+$/g, "");
    }
    function sortEntities4(entities) {
      return [...entities].sort((left, right) => {
        const leftOrder = numberField25(left.record.order) ?? Number.MAX_SAFE_INTEGER;
        const rightOrder = numberField25(right.record.order) ?? Number.MAX_SAFE_INTEGER;
        if (leftOrder !== rightOrder) return leftOrder - rightOrder;
        return left.path.localeCompare(right.path);
      });
    }
    function entityDir33(path) {
      return path.replace(/\/[^/]+$/, "");
    }
    function recordField23(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value) ? value : void 0;
    }
    function stringField112(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberField25(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function isDefined6(value) {
      return value !== void 0;
    }
    function createMovScriptWorkspaceService2(options) {
      const domainRepository = createMovScriptWorkspaceDomainRepository2({
        fileRepository: options.fileRepository
      });
      const indexCacheTtlMs = options.indexCacheTtlMs ?? 2e3;
      const indexCache = /* @__PURE__ */ new Map();
      const invalidateIndexCache = () => {
        indexCache.clear();
      };
      const loadIndex = async (input3) => {
        const cacheKey = normalizeWorkspacePath8(input3?.path ?? "");
        const nowMs = Date.now();
        const cached = indexCache.get(cacheKey);
        if (cached?.index && cached.expiresAt > nowMs) return cached.index;
        if (cached?.promise) return cached.promise;
        const promise = (async () => {
          const documents = await domainRepository.loadDocuments(input3);
          const decisionDocuments = await overlayMovScriptDecisionDocuments2(documents, options.decisionStore);
          return deriveMovScriptWorkspaceDomainIndex2(decisionDocuments);
        })();
        indexCache.set(cacheKey, { expiresAt: 0, promise });
        try {
          const index = await promise;
          indexCache.set(cacheKey, {
            expiresAt: Date.now() + Math.max(0, indexCacheTtlMs),
            index
          });
          return index;
        } catch (error) {
          if (indexCache.get(cacheKey)?.promise === promise) indexCache.delete(cacheKey);
          throw error;
        }
      };
      const invalidateAfter = async (operation) => {
        const result = await operation();
        invalidateIndexCache();
        return result;
      };
      return {
        async initializeProject(input3 = {}) {
          const now = options.now?.() ?? /* @__PURE__ */ new Date();
          const createdAt = now.toISOString();
          const existingWorkspace = await readJSONArtifact2(options.fileRepository, "workspace.json");
          const title = stringField122(input3.title) ?? "MovScript Project";
          const projectId = stringField122(input3.projectId) ?? stringField122(existingWorkspace?.project_id) ?? safeProjectSlug2(title);
          const projectUid = stringField122(input3.projectUid) ?? stringField122(existingWorkspace?.project_uid) ?? stringField122(existingWorkspace?.projectUid) ?? createMovScriptProjectUid2();
          const standardsRecord = {
            schema: "movscript.project_standards.v1",
            kind: "project_standards",
            id: "project_standards",
            project_id: projectId,
            title: "Project standards",
            ...input3.standards ?? {},
            updated_at: createdAt
          };
          const files = [
            await ensureMovScriptGitignore2(options.fileRepository),
            await writeJSONDocument2(options.fileRepository, ".movscript/config.json", {
              schema: "movscript.local_project_config.v1",
              project_uid: projectUid,
              remotes: {},
              created_at: createdAt,
              updated_at: createdAt
            }, Boolean(input3.overwrite)),
            await writeJSONDocument2(options.fileRepository, "workspace.json", {
              schema: "movscript.workspace.v2",
              project_uid: projectUid,
              project_id: projectId,
              title,
              created_at: stringField122(existingWorkspace?.created_at) ?? createdAt,
              updated_at: createdAt
            }, Boolean(input3.overwrite)),
            await writeJSONDocument2(options.fileRepository, "project.json", {
              schema: "movscript.project.v1",
              kind: "project",
              project_id: projectId,
              title,
              language: stringField122(input3.language),
              created_at: createdAt,
              updated_at: createdAt
            }, Boolean(input3.overwrite)),
            await writeJSONDocument2(options.fileRepository, "project_standards.json", standardsRecord, Boolean(input3.overwrite))
          ];
          const standardSkillFiles = await syncMovScriptProjectStandardSkills2({
            fileRepository: options.fileRepository,
            standards: standardsRecord
          });
          invalidateIndexCache();
          return { projectId, projectUid, files, standardSkillFiles };
        },
        getModel: getMovScriptWorkspaceModel2,
        loadIndex,
        async queryEntities(query = {}) {
          return queryMovScriptWorkspaceEntities2(await loadIndex(), query);
        },
        async querySettings(query = {}) {
          return queryMovScriptWorkspaceSettings2(await loadIndex(), query);
        },
        async queryAssets(query = {}) {
          return queryMovScriptWorkspaceAssets2(await loadIndex(), query);
        },
        async queryProductionContext(query = {}) {
          return queryMovScriptWorkspaceProductionContext2(await loadIndex(), query);
        },
        readEditorState() {
          return readJSONArtifact2(options.fileRepository, MOVSCRIPT_EDITOR_STATE_PATH2);
        },
        async readPreviewTimeline(productionId) {
          const timelines = deriveMovScriptWorkspacePreviewTimelines3(await loadIndex());
          return timelines.find((timeline) => samePreviewTimelineProduction2(timeline.productionId, productionId));
        },
        async readTimelineAssemblyPreviewTimeline(scope) {
          return deriveMovScriptWorkspaceTimelineAssemblyPreviewTimeline2(await loadIndex(), scope);
        },
        async readSceneMomentEditPlan(sceneMomentId) {
          const sceneMoment = queryMovScriptWorkspaceEntities2(await loadIndex(), { entityKind: "scene_moment" }).find((entity) => sameEntityId3(entity.id, sceneMomentId) || entity.path.includes(`/scene_moments/${entityPathSlug2(sceneMomentId, "scene_moment")}/`));
          if (!sceneMoment) return void 0;
          return readJSONArtifact2(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${sceneMoment.path.replace(/\/scene_moment\.json$/, "")}/edit_plan.json`);
        },
        readContentUnitRuntimePanel(contentUnitId) {
          return readJSONArtifact2(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/content_units/${entityPathSlug2(contentUnitId, "content_unit")}/runtime_panel.json`);
        },
        readContentUnitGenerationPrompt(contentUnitId) {
          return readJSONArtifact2(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/content_units/${entityPathSlug2(contentUnitId, "content_unit")}/generation_prompt.json`);
        },
        readContentUnitDependencyReport(contentUnitId) {
          return readJSONArtifact2(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/content_units/${entityPathSlug2(contentUnitId, "content_unit")}/dependency_report.json`);
        },
        readContentUnitSelectionValidity(contentUnitId) {
          return readJSONArtifact2(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/content_units/${entityPathSlug2(contentUnitId, "content_unit")}/selection_validity.json`);
        },
        async upsertSetting(input3) {
          const payload = await writePayloadWithGeneratedEntityId2(loadIndex, "setting", input3);
          return invalidateAfter(() => upsertMovScriptWorkspaceSetting2({
            fileRepository: options.fileRepository,
            now: options.now?.(),
            ...input3,
            payload
          }));
        },
        async upsertSettingState(input3) {
          const payload = await writePayloadWithGeneratedEntityId2(loadIndex, "setting_state", input3);
          return invalidateAfter(() => upsertMovScriptWorkspaceSettingState2({
            fileRepository: options.fileRepository,
            now: options.now?.(),
            ...input3,
            payload
          }));
        },
        async upsertAsset(input3) {
          const payload = await writePayloadWithGeneratedEntityId2(loadIndex, "asset", input3);
          return invalidateAfter(() => upsertMovScriptWorkspaceAsset2({
            fileRepository: options.fileRepository,
            now: options.now?.(),
            ...input3,
            payload
          }));
        },
        upsertScript(input3) {
          return invalidateAfter(() => upsertMovScriptWorkspaceScript2({
            fileRepository: options.fileRepository,
            now: options.now?.(),
            ...input3
          }));
        },
        readScriptSource(input3) {
          return readMovScriptWorkspaceScriptSource2({
            fileRepository: options.fileRepository,
            ...input3
          });
        },
        saveProductionSnapshot(input3) {
          return invalidateAfter(() => saveMovScriptProductionWorkspaceSnapshot2({
            fileRepository: options.fileRepository,
            now: options.now?.(),
            ...input3
          }));
        },
        deleteEntity(input3) {
          return invalidateAfter(() => deleteMovScriptWorkspaceEntity2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        snapshotScriptVersionFromMarkdown(input3) {
          return invalidateAfter(() => snapshotMovScriptVersionFromMarkdown2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        updateContentUnitEditPrompt(input3) {
          return invalidateAfter(() => updateMovScriptContentUnitEditPrompt2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        async upsertContentUnit(input3) {
          const unit = await contentUnitWithGeneratedEntityId2(loadIndex, input3.unit);
          return invalidateAfter(() => upsertMovScriptContentUnit2({
            fileRepository: options.fileRepository,
            ...input3,
            unit
          }));
        },
        async upsertProjectStandards(input3) {
          const result = await invalidateAfter(() => upsertMovScriptProjectStandards2({
            fileRepository: options.fileRepository,
            now: options.now?.(),
            ...input3
          }));
          const standardSkillFiles = await syncMovScriptProjectStandardSkills2({
            fileRepository: options.fileRepository,
            standards: result.record
          });
          invalidateIndexCache();
          return { ...result, standardSkillFiles };
        },
        updateEntityTransition(input3) {
          return invalidateAfter(() => updateMovScriptEntityTransition2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        updateStoryboardTimeline(input3) {
          return invalidateAfter(() => updateMovScriptStoryboardTimeline2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        upsertSourceRecord(input3) {
          return invalidateAfter(() => upsertMovScriptSourceRecord2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        updateExpressionUnitSource(input3) {
          return invalidateAfter(() => updateMovScriptExpressionUnit2(options.fileRepository, input3));
        },
        updateAudioCueSource(input3) {
          return invalidateAfter(() => updateMovScriptAudioCue2(options.fileRepository, input3));
        },
        appendCandidate(input3) {
          return invalidateAfter(() => appendMovScriptInlineCandidate2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        async createContentCandidate(input3) {
          const promptSnapshot = mergePromptSnapshots2(
            await readContentUnitGenerationPromptArtifact2(options.fileRepository, input3.contentUnitId),
            input3.promptSnapshot
          );
          if (options.decisionStore) {
            const result = buildMovScriptContentCandidate2({
              ...input3,
              ...promptSnapshot !== void 0 ? { promptSnapshot } : {}
            });
            await options.decisionStore.upsertContentUnitCandidate({
              contentUnitId: input3.contentUnitId,
              candidate: result.record
            });
            invalidateIndexCache();
            return result;
          }
          throw new Error("content unit candidate creation requires a backend scoped project data decisionStore");
        },
        async selectContentUnitCandidate(input3) {
          const decisionStore = options.decisionStore;
          if (!decisionStore) {
            throw new Error("content unit candidate selection requires a backend scoped project data decisionStore");
          }
          const candidate = await readBackendContentCandidateRecord2(decisionStore, input3.contentUnitId, input3.candidateId);
          const resourceId = input3.resourceId ?? firstCandidateResourceId3(candidate);
          const context = await decisionStore.selectContentUnitCandidate({
            contentUnitId: input3.contentUnitId,
            candidateId: input3.candidateId,
            ...resourceId !== void 0 ? { resourceId } : {},
            stalePolicy: input3.stalePolicy,
            reason: input3.reason,
            selectedAt: input3.selectedAt
          });
          invalidateIndexCache();
          return {
            path: contentUnitDecisionContextPath2(input3.contentUnitId),
            record: normalizeDecisionContext2(context),
            context
          };
        },
        async decideContentUnitCandidate(input3) {
          const decisionStore = options.decisionStore;
          if (!decisionStore) {
            throw new Error("content unit candidate decision requires a backend scoped project data decisionStore");
          }
          if (input3.decision === "adopt") {
            const candidate2 = await readBackendContentCandidateRecord2(decisionStore, input3.contentUnitId, input3.candidateId);
            const resourceId = input3.resourceId ?? firstCandidateResourceId3(candidate2);
            const context2 = await decisionStore.selectContentUnitCandidate({
              contentUnitId: input3.contentUnitId,
              candidateId: input3.candidateId,
              ...resourceId !== void 0 ? { resourceId } : {},
              stalePolicy: input3.stalePolicy,
              reason: input3.reason,
              selectedAt: input3.decidedAt,
              metadata: input3.metadata
            });
            invalidateIndexCache();
            return {
              path: contentUnitDecisionContextPath2(input3.contentUnitId),
              record: normalizeDecisionContext2(context2),
              context: context2
            };
          }
          const candidate = await readBackendContentCandidateRecord2(decisionStore, input3.contentUnitId, input3.candidateId);
          if (!candidate) throw new Error(`candidate not found: ${String(input3.candidateId)}`);
          const decidedAt = input3.decidedAt ?? options.now?.().toISOString();
          const context = await decisionStore.upsertContentUnitCandidate({
            contentUnitId: input3.contentUnitId,
            candidate: pruneUndefined142({
              ...candidate,
              decision_status: input3.decision,
              decision_reason: input3.reason,
              decided_at: decidedAt,
              decision_metadata: input3.metadata
            })
          });
          invalidateIndexCache();
          return {
            path: contentUnitDecisionContextPath2(input3.contentUnitId),
            record: normalizeDecisionContext2(context),
            context
          };
        },
        createAssetSlotCandidate(input3) {
          return invalidateAfter(() => createMovScriptWorkspaceAssetSlotCandidate2({
            fileRepository: options.fileRepository,
            projectPath: input3.projectPath ?? "",
            ...input3
          }));
        },
        createKeyframeCandidate(input3) {
          return invalidateAfter(() => createMovScriptWorkspaceKeyframeCandidate2({
            fileRepository: options.fileRepository,
            projectPath: input3.projectPath ?? "",
            ...input3
          }));
        },
        selectCandidate(input3) {
          return invalidateAfter(() => selectMovScriptInlineCandidate2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        updateCandidate(input3) {
          return invalidateAfter(() => updateMovScriptInlineCandidate2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        },
        unlockCandidate(input3) {
          return invalidateAfter(() => unlockMovScriptInlineCandidate2({
            fileRepository: options.fileRepository,
            ...input3
          }));
        }
      };
    }
    async function readJSONArtifact2(fileRepository, path) {
      const file = await fileRepository.read({ path: normalizeWorkspacePath8(path) }).catch(() => void 0);
      if (!file) return void 0;
      const parsed = JSON.parse(file.content);
      return isRecord192(parsed) ? parsed : void 0;
    }
    async function updateMovScriptExpressionUnit2(fileRepository, input3) {
      const normalizedPath = normalizeWorkspacePath8(input3.targetPath);
      const existing = await readJSONArtifact2(fileRepository, normalizedPath);
      if (!existing) throw new Error(`Expression unit source not found: ${normalizedPath}`);
      if (existing.kind !== "expression_unit") throw new Error(`Target is not an expression_unit: ${normalizedPath}`);
      const record = pruneUndefined142({
        ...existing,
        schema: stringField122(existing.schema) ?? "movscript.expression_unit.v1",
        kind: "expression_unit",
        title: input3.patch.title !== void 0 ? input3.patch.title : existing.title,
        slot_kind: input3.patch.slotKind !== void 0 ? normalizeExpressionUnitSlotKind2(input3.patch.slotKind) : normalizeExpressionUnitSlotKind2(existing.slot_kind ?? existing.slotKind ?? existing.expression_kind ?? existing.kind),
        expression_kind: input3.patch.expressionKind !== void 0 ? legacyExpressionKind2(input3.patch.expressionKind, stringField122(existing.expression_kind)) : existing.expression_kind,
        speaker: input3.patch.speaker !== void 0 ? input3.patch.speaker : existing.speaker,
        text: input3.patch.text !== void 0 ? input3.patch.text : existing.text,
        note: input3.patch.note !== void 0 ? input3.patch.note : existing.note,
        intent: input3.patch.intent !== void 0 ? input3.patch.intent : existing.intent
      });
      await fileRepository.write({
        path: normalizedPath,
        content: `${JSON.stringify(record, null, 2)}
`
      });
      return { path: normalizedPath, record };
    }
    function legacyExpressionKind2(value, fallback) {
      const text = String(value ?? "").trim().toLowerCase();
      if (text === "dialogue" || text === "narration" || text === "subtitle" || text === "caption" || text === "action" || text === "visual_note" || text === "shot") return text;
      if (text === "voice" || text === "verbal" || text === "voiceover") return "dialogue";
      if (text === "visual" || text === "image" || text === "video") return "visual_note";
      if (text === "audio" || text === "sound" || text === "sound_effect" || text === "sfx" || text === "music" || text === "ambience") return "action";
      return fallback ?? "visual_note";
    }
    async function updateMovScriptAudioCue2(fileRepository, input3) {
      const normalizedPath = normalizeWorkspacePath8(input3.targetPath);
      const existing = await readJSONArtifact2(fileRepository, normalizedPath);
      if (!existing) throw new Error(`Audio cue source not found: ${normalizedPath}`);
      if (existing.kind !== "audio_cue") throw new Error(`Target is not an audio_cue: ${normalizedPath}`);
      const record = pruneUndefined142({
        ...existing,
        schema: stringField122(existing.schema) ?? "movscript.audio_cue.v1",
        kind: "audio_cue",
        title: input3.patch.title !== void 0 ? input3.patch.title : existing.title,
        cue_kind: input3.patch.cueKind !== void 0 ? input3.patch.cueKind : existing.cue_kind,
        expression_unit_ref: input3.patch.expressionUnitRef !== void 0 ? input3.patch.expressionUnitRef : existing.expression_unit_ref,
        storyboard_ref: input3.patch.storyboardRef !== void 0 ? input3.patch.storyboardRef : existing.storyboard_ref,
        timing: input3.patch.timing !== void 0 ? input3.patch.timing : existing.timing,
        prompt_hint: input3.patch.promptHint !== void 0 ? input3.patch.promptHint : existing.prompt_hint,
        asset_refs: input3.patch.assetRefs !== void 0 ? input3.patch.assetRefs : existing.asset_refs
      });
      await fileRepository.write({
        path: normalizedPath,
        content: `${JSON.stringify(record, null, 2)}
`
      });
      return { path: normalizedPath, record };
    }
    function samePreviewTimelineProduction2(left, right) {
      return String(left) === String(right) || entityPathSlug2(left, "production") === entityPathSlug2(right, "production");
    }
    function contentUnitDerivedArtifactPath2(contentUnitId, filename) {
      return `${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/content_units/${entityPathSlug2(contentUnitId, "content_unit")}/${filename}`;
    }
    async function readContentUnitGenerationPromptArtifact2(fileRepository, contentUnitId) {
      return readJSONArtifact2(fileRepository, contentUnitDerivedArtifactPath2(contentUnitId, "generation_prompt.json"));
    }
    async function readBackendContentCandidateRecord2(decisionStore, contentUnitId, candidateId) {
      const context = await decisionStore.getContentUnitDecision({ contentUnitId });
      return context?.candidates.find((candidate) => String(candidate.id) === String(candidateId));
    }
    function mergePromptSnapshots2(runtimePrompt, promptSnapshot) {
      if (!runtimePrompt) return promptSnapshot;
      if (!promptSnapshot) return runtimePrompt;
      return pruneUndefined142({ ...runtimePrompt, ...promptSnapshot });
    }
    function firstCandidateResourceId3(candidate) {
      const firstOutput = arrayField32(candidate?.outputs).filter(isRecord192)[0];
      const resourceId = firstOutput?.resource_id;
      return resourceIdField22(resourceId);
    }
    function resourceIdField22(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function arrayField32(value) {
      return Array.isArray(value) ? value : [];
    }
    var MOVSCRIPT_GITIGNORE_PATH2 = ".gitignore";
    var MOVSCRIPT_INTERPRET_GITIGNORE_ENTRY2 = ".interpret/";
    var MOVSCRIPT_LOCAL_CONTROL_GITIGNORE_ENTRY2 = ".movscript/";
    var MOVSCRIPT_INTERPRET_GITIGNORE_BLOCK2 = [
      "# MovScript generated artifacts",
      MOVSCRIPT_INTERPRET_GITIGNORE_ENTRY2,
      MOVSCRIPT_LOCAL_CONTROL_GITIGNORE_ENTRY2,
      ""
    ].join("\n");
    async function ensureMovScriptGitignore2(fileRepository) {
      const normalizedPath = normalizeWorkspacePath8(MOVSCRIPT_GITIGNORE_PATH2);
      const existingFile = await fileRepository.read({ path: normalizedPath }).catch(() => void 0);
      const existingContent = existingFile?.content;
      if (existingContent !== void 0 && gitignoreContainsBuildEntry2(existingContent)) {
        return { path: normalizedPath, status: "skipped", content: existingContent };
      }
      const content = appendGitignoreBlock2(existingContent, MOVSCRIPT_INTERPRET_GITIGNORE_BLOCK2);
      await fileRepository.write({ path: normalizedPath, content });
      return {
        path: normalizedPath,
        status: existingContent === void 0 ? "created" : "updated",
        content
      };
    }
    function gitignoreContainsBuildEntry2(content) {
      const entries = new Set(content.split(/\r?\n/).map((line) => line.trim()).filter(Boolean));
      return (entries.has(".interpret") || entries.has(MOVSCRIPT_INTERPRET_GITIGNORE_ENTRY2)) && (entries.has(".movscript") || entries.has(MOVSCRIPT_LOCAL_CONTROL_GITIGNORE_ENTRY2));
    }
    function appendGitignoreBlock2(existingContent, block) {
      if (!existingContent) return block;
      const separator = existingContent.endsWith("\n") ? "\n" : "\n\n";
      return `${existingContent}${separator}${block}`;
    }
    async function writeJSONDocument2(fileRepository, path, record, overwrite) {
      const normalizedPath = normalizeWorkspacePath8(path);
      if (!overwrite) {
        const existing2 = await readJSONArtifact2(fileRepository, normalizedPath);
        if (existing2) {
          return { path: normalizedPath, status: "skipped", record: existing2 };
        }
      }
      const existing = await readJSONArtifact2(fileRepository, normalizedPath);
      await fileRepository.write({
        path: normalizedPath,
        content: `${JSON.stringify(pruneUndefined142(record), null, 2)}
`
      });
      return {
        path: normalizedPath,
        status: existing ? "updated" : "created",
        record: pruneUndefined142(record)
      };
    }
    async function writePayloadWithGeneratedEntityId2(loadIndex, entityKind, input3) {
      const current = {
        ...isRecord192(input3.record) ? input3.record : {},
        ...isRecord192(input3.entity?.record) ? input3.entity?.record : {}
      };
      const payload = isRecord192(input3.payload) ? input3.payload : {};
      if (hasEntityIdentity2(payload) || hasEntityIdentity2(current)) return payload;
      const existingIds = await existingWorkspaceEntityIds2(loadIndex, entityKind);
      return {
        ...payload,
        id: allocateMovScriptEntityId2({
          entityKind,
          title: entityTitleSeed2(payload, current),
          existingIds
        })
      };
    }
    async function contentUnitWithGeneratedEntityId2(loadIndex, unit) {
      if (hasEntityIdentity2(unit)) return unit;
      const existingIds = await existingWorkspaceEntityIds2(loadIndex, "content_unit");
      return {
        ...unit,
        id: allocateMovScriptEntityId2({
          entityKind: "content_unit",
          title: entityTitleSeed2(unit, {}),
          existingIds
        })
      };
    }
    async function existingWorkspaceEntityIds2(loadIndex, entityKind) {
      const entities = queryMovScriptWorkspaceEntities2(await loadIndex(), { entityKind });
      return entities.flatMap((entity) => [
        stringField122(entity.id),
        stringField122(entity.record.id),
        stringField122(entity.record.ID)
      ]).filter((id) => Boolean(id));
    }
    function hasEntityIdentity2(record) {
      return [
        record.workspace_slug,
        record.workspaceSlug,
        record.workspace_entity_id,
        record.workspaceEntityId,
        record.id,
        record.ID,
        record.client_id,
        record.clientId
      ].some((value) => stringField122(value) !== void 0);
    }
    function entityTitleSeed2(payload, current) {
      return payload.title ?? payload.name ?? payload.slot ?? payload.slot_key ?? payload.slotKey ?? payload.text ?? payload.description ?? current.title ?? current.name ?? current.slot ?? current.slot_key ?? current.slotKey ?? current.text ?? current.description;
    }
    function stringField122(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function safeProjectSlug2(value) {
      const slug = value.trim().toLowerCase().replace(/[^a-z0-9._-]+/g, "_").replace(/^_+|_+$/g, "");
      return slug || "movscript_project";
    }
    function createMovScriptProjectUid2() {
      const randomUUID4 = globalThis.crypto?.randomUUID?.();
      if (randomUUID4) return `prj_${randomUUID4.replace(/-/g, "")}`;
      const random = Math.random().toString(36).slice(2);
      return `prj_${Date.now().toString(36)}${random}`;
    }
    function sameEntityId3(left, right) {
      return String(left ?? "") === String(right ?? "");
    }
    function isRecord192(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function pruneUndefined142(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    var import_node_path9 = __require("path");
    function createNodeMovScriptWorkspaceService2(input3 = {}) {
      const projectDir = (0, import_node_path9.resolve)(input3.projectDir ?? input3.workspaceDir ?? process.cwd());
      const fileRepository = createNodeMovScriptWorkspaceFileRepository2(projectDir);
      const service22 = createMovScriptWorkspaceService2({
        fileRepository,
        decisionStore: input3.decisionStore,
        now: input3.now
      });
      return {
        ...service22,
        projectDir
      };
    }
    var RESOURCE_MENTION_RE2 = /@\[resource:([^\]\s]+)\]|\[\[resource::(\d+)\]\]|\{\{\s*resource:{1,2}\s*([^}]+?)\s*\}\}/g;
    var RESOURCE_MEDIA_TYPES2 = /* @__PURE__ */ new Set(["image", "video", "audio", "text", "any"]);
    function formatResourceMention2(resourceId, options = {}) {
      const id = String(resourceId);
      const mediaType = normalizeMentionPart2(options.mediaType);
      const role = normalizeMentionPart2(options.role);
      if (mediaType && role) return `@[resource:${mediaType}:${role}:${id}]`;
      if (mediaType) return `@[resource:${mediaType}:${id}]`;
      if (role) return `@[resource:${role}:${id}]`;
      return `@[resource:${id}]`;
    }
    function parseResourceMentions2(text) {
      const mentions = [];
      for (const match of (text ?? "").matchAll(RESOURCE_MENTION_RE2)) {
        const index = match.index;
        if (index === void 0) continue;
        const parsed = match[1] ? parseResourceMentionPayload2(match[1]) : match[3] ? parseLegacyResourceMentionPayload2(match[3]) : { id: Number(match[2]) };
        const id = parsed.id;
        if (!Number.isInteger(id) || id <= 0) continue;
        mentions.push({
          id,
          token: match[0],
          index,
          ...parsed.mediaType ? { mediaType: parsed.mediaType } : {},
          ...parsed.role ? { role: parsed.role } : {}
        });
      }
      return mentions;
    }
    function resourceIdsFromMentions2(text) {
      const ids = [];
      const seen = /* @__PURE__ */ new Set();
      for (const mention of parseResourceMentions2(text)) {
        if (seen.has(mention.id)) continue;
        seen.add(mention.id);
        ids.push(mention.id);
      }
      return ids;
    }
    function stripResourceMentions2(text) {
      return text.replace(RESOURCE_MENTION_RE2, "").replace(/[ \t]{2,}/g, " ").replace(/[ \t]+\n/g, "\n").replace(/\n{3,}/g, "\n\n").trim();
    }
    function parseResourceMentionPayload2(payload) {
      const parts = payload.split(":").map((part) => normalizeMentionPart2(part)).filter((part) => Boolean(part));
      if (parts.length === 0) return { id: 0 };
      const id = Number(parts[parts.length - 1]);
      if (!Number.isInteger(id) || id <= 0) return { id: 0 };
      const descriptors = parts.slice(0, -1);
      let mediaType = "";
      let role = "";
      if (descriptors.length === 1) {
        const first = descriptors[0] ?? "";
        if (RESOURCE_MEDIA_TYPES2.has(first)) {
          mediaType = first;
        } else {
          role = first;
        }
      } else if (descriptors.length > 1) {
        const first = descriptors[0] ?? "";
        if (RESOURCE_MEDIA_TYPES2.has(first)) {
          mediaType = first;
          role = descriptors.slice(1).join(":");
        } else {
          role = descriptors.join(":");
        }
      }
      return {
        id,
        ...mediaType ? { mediaType } : {},
        ...role ? { role } : {}
      };
    }
    function parseLegacyResourceMentionPayload2(payload) {
      const parts = payload.trim().split(/\s+/).filter(Boolean);
      const id = Number(parts.shift());
      if (!Number.isInteger(id) || id <= 0) return { id: 0 };
      let mediaType = "";
      let role = "";
      for (const part of parts) {
        const match = part.match(/^([a-zA-Z_][a-zA-Z0-9_-]*)=(.+)$/);
        if (!match) continue;
        const key = normalizeMentionPart2(match[1]);
        const value = normalizeMentionPart2(match[2]);
        if (!value) continue;
        if (key === "role") role = value;
        if (key === "media" || key === "media_type" || key === "mediatype") mediaType = value;
      }
      return {
        id,
        ...mediaType ? { mediaType } : {},
        ...role ? { role } : {}
      };
    }
    function normalizeMentionPart2(value) {
      return (value ?? "").trim().toLowerCase().replace(/[^a-z0-9_-]+/g, "_").replace(/^_+|_+$/g, "");
    }
    function projectStyleReferenceResourceIds3(index) {
      const standards = firstEntity2(index, "project_standards");
      if (!standards) return [];
      return uniqueIds3([
        ...resourceIdsFromValue22(standards.record.style_reference_images),
        ...resourceIdsFromValue22(standards.record.style_references),
        ...resourceIdsFromValue22(standards.record.reference_resource_ids),
        ...arrayField42(standards.record.custom_rules).filter(isRecord202).filter((rule) => rule.enabled !== false).filter((rule) => stringField132(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue22(rule.value))
      ]);
    }
    var PROMPT_REF_PATTERN22 = /\{\{([a-z_]+)::?([^{}:\s][^{}]*)\}\}/g;
    function parseContentUnitEditPromptRefs22(value) {
      const editPrompt = recordField33(value);
      const structuredText = structuredPromptTextFromUnknown3(editPrompt?.structured);
      return [
        ...parsePromptRefsFromText22(stringField132(editPrompt?.text), "edit_prompt.text"),
        ...parsePromptRefsFromText22(stringField132(editPrompt?.negative_text), "edit_prompt.negative_text"),
        ...parsePromptRefsFromText22(stringField132(editPrompt?.notes), "edit_prompt.notes"),
        ...parsePromptRefsFromText22(structuredText, "edit_prompt.structured")
      ];
    }
    function parseUnsupportedContentUnitEditPromptRefs3(value) {
      const editPrompt = recordField33(value);
      const structuredText = structuredPromptTextFromUnknown3(editPrompt?.structured);
      return [
        ...parseUnsupportedPromptRefsFromText3(stringField132(editPrompt?.text), "edit_prompt.text"),
        ...parseUnsupportedPromptRefsFromText3(stringField132(editPrompt?.negative_text), "edit_prompt.negative_text"),
        ...parseUnsupportedPromptRefsFromText3(stringField132(editPrompt?.notes), "edit_prompt.notes"),
        ...parseUnsupportedPromptRefsFromText3(structuredText, "edit_prompt.structured")
      ];
    }
    function parsePromptRefsFromText22(text, field) {
      if (!text) return [];
      const refs = [];
      for (const match of text.matchAll(PROMPT_REF_PATTERN22)) {
        const kind = promptRefKind22(match[1]);
        const id = match[2]?.trim();
        if (!kind || !id) continue;
        refs.push({
          kind,
          id,
          raw: match[0],
          source: {
            field,
            start: match.index,
            end: match.index === void 0 ? void 0 : match.index + match[0].length
          }
        });
      }
      return refs;
    }
    function parseUnsupportedPromptRefsFromText3(text, field) {
      if (!text) return [];
      const refs = [];
      for (const match of text.matchAll(PROMPT_REF_PATTERN22)) {
        const kindValue = match[1]?.trim();
        const id = match[2]?.trim();
        if (!kindValue || !id || promptRefKind22(kindValue)) continue;
        refs.push({
          kind: kindValue,
          id,
          raw: match[0],
          source: {
            field,
            start: match.index,
            end: match.index === void 0 ? void 0 : match.index + match[0].length
          }
        });
      }
      return refs;
    }
    function structuredPromptTextFromUnknown3(value) {
      const record = recordField33(value);
      if (!record) return void 0;
      const json = JSON.stringify(record);
      return json === "{}" ? void 0 : json;
    }
    function primaryRefKindForContentUnitType22(contentUnitType22) {
      return primaryRefKindForContentUnitType4(contentUnitType22);
    }
    function primaryRefIdsForContentUnitRecord22(record, kind) {
      return primaryRefIdsForContentUnitRecord4(record, kind);
    }
    function primaryRefFieldNameForKind22(kind) {
      return primaryRefFieldNameForKind3(kind);
    }
    function primaryContentUnitRefs3(contentUnit, kind) {
      return primaryRefIdsForContentUnitRecord22(contentUnit.record, kind).map((id) => ({ kind, id }));
    }
    function hasAmbiguousPrimaryRefs3(refs, kind) {
      const unique = [];
      for (const ref of refs) {
        if (unique.some((item) => samePromptRefId3(item.id, ref.id, kind))) continue;
        unique.push(ref);
      }
      return unique.length > 1;
    }
    function expectedOutputKindForContentUnitType22(contentUnitType22) {
      return expectedOutputKindForContentUnitType3(contentUnitType22);
    }
    function resolvePromptRefEntity3(index, ref) {
      if (ref.kind === "content_unit") {
        return queryMovScriptWorkspaceEntities2(index, { entityKind: "content_unit" }).find((entity) => sameEntityRef3(entity.id, ref.id, "content_unit") || sameEntityRef3(entityDir42(entity.path).split("/").pop(), ref.id, "content_unit"));
      }
      return findEntityByRef4(index, ref.kind, ref.id);
    }
    function resolveContentUnitForPromptRef3(index, ref) {
      if (ref.kind === "content_unit") return resolvePromptRefEntity3(index, ref);
      const expectedTypes = contentUnitTypesForPromptRefKind22(ref.kind);
      return queryMovScriptWorkspaceEntities2(index, { entityKind: "content_unit" }).find((entity) => {
        if (!expectedTypes.includes(String(entity.record.content_unit_type ?? ""))) return false;
        return primaryContentUnitRefs3(entity, ref.kind).some((candidate) => samePromptRefId3(candidate.id, ref.id, ref.kind));
      });
    }
    function resolveUpstreamSelectionForPromptRef2(index, ref) {
      const contentUnit = resolveContentUnitForPromptRef3(index, ref);
      if (!contentUnit) return void 0;
      const selection = readSelectedContentUnit2(index, entityDir42(contentUnit.path));
      if (!selection) return void 0;
      const candidateId = idField62(selection.candidate_id);
      const resourceId = resourceIdField32(selection.resource_id);
      const providerAsset = ref.kind === "asset" ? activeProviderAssetCertificationForPromptRef2(index, ref, { resourceId, candidateId }) : void 0;
      return {
        content_unit_ref: entityDir42(contentUnit.path),
        ...candidateId !== void 0 ? { candidate_id: candidateId } : {},
        ...resourceId !== void 0 ? { resource_id: resourceId } : {},
        ...providerAsset ? { provider_asset: providerAsset } : {},
        stale_policy: selection.stale_policy === "accept_stale" ? "accept_stale" : "strict",
        role: `${ref.kind}_ref`
      };
    }
    function activeProviderAssetCertificationForPromptRef2(index, ref, input3 = {}) {
      if (ref.kind !== "asset") return void 0;
      const asset = findEntityByRef4(index, "asset", ref.id);
      return activeProviderAssetCertification2(asset?.record, input3);
    }
    function activeProviderAssetCertification2(assetRecord, input3 = {}) {
      const certifications = recordField33(assetRecord?.provider_certifications) ?? recordField33(assetRecord?.providerCertifications);
      if (!certifications) return void 0;
      for (const [provider, rawCertification] of Object.entries(certifications)) {
        const certification = recordField33(rawCertification);
        if (!certification) continue;
        if (stringField132(certification.status) !== "active") continue;
        const assetUri = providerAssetUri2(certification);
        if (!assetUri) continue;
        const sourceResourceId2 = resourceIdField32(certification.source_resource_id ?? certification.sourceResourceId);
        if (input3.resourceId !== void 0 && sourceResourceId2 !== void 0 && sourceResourceId2 !== input3.resourceId) continue;
        const sourceCandidateId = idField62(certification.source_candidate_id ?? certification.sourceCandidateId);
        if (input3.candidateId !== void 0 && sourceCandidateId !== void 0 && String(sourceCandidateId) !== String(input3.candidateId)) continue;
        return {
          provider,
          status: "active",
          asset_uri: assetUri,
          ...stringField132(certification.hub_asset_id ?? certification.hubAssetId) ? { hub_asset_id: stringField132(certification.hub_asset_id ?? certification.hubAssetId) } : {},
          ...sourceResourceId2 !== void 0 ? { source_resource_id: sourceResourceId2 } : {},
          ...sourceCandidateId !== void 0 ? { source_candidate_id: sourceCandidateId } : {},
          ...stringField132(certification.source_hash ?? certification.sourceHash) ? { source_hash: stringField132(certification.source_hash ?? certification.sourceHash) } : {},
          ...stringField132(certification.certified_at ?? certification.certifiedAt) ? { certified_at: stringField132(certification.certified_at ?? certification.certifiedAt) } : {},
          ...stringField132(certification.updated_at ?? certification.updatedAt) ? { updated_at: stringField132(certification.updated_at ?? certification.updatedAt) } : {}
        };
      }
      return void 0;
    }
    function resolvePromptRefs2(index, refs, _primaryKind) {
      const upstreamSelections = [];
      const resolvedRefs = refs.map((ref) => {
        const role = "input";
        const entity = resolvePromptRefEntity3(index, ref);
        const selection = resolveUpstreamSelectionForPromptRef2(index, ref);
        if (selection) upstreamSelections.push(selection);
        return {
          ...ref,
          role,
          ...entity ? {
            resolved: {
              entityKind: entity.entityKind,
              ...entity.id !== void 0 ? { id: entity.id } : {},
              path: entity.path
            }
          } : {},
          ...selection ? { selection } : {}
        };
      });
      return { refs: resolvedRefs, upstreamSelections };
    }
    function promptRefKind22(value) {
      return isContentUnitPromptRefKind2(value) ? value : void 0;
    }
    function contentUnitTypesForPromptRefKind22(kind) {
      return contentUnitTypesForPromptRefKind4(kind);
    }
    function samePromptRefId3(left, right, kind) {
      return String(left) === String(right) || lastPathSegment22(left) === String(right) || lastPathSegment22(right) === String(left) || sameEntityRef3(left, right, kind);
    }
    function lastPathSegment22(value) {
      if (typeof value !== "string" || !value.includes("/")) return void 0;
      return value.split("/").filter(Boolean).at(-1);
    }
    function readSelectedContentUnit2(index, contentUnitRef) {
      const context = index.documents.find((document) => {
        if (!isRecord202(document.data)) return false;
        if (document.data.schema !== "movscript.decision_context.v1") return false;
        return document.data.target_kind === "content_unit" && document.data.target_ref === contentUnitRef;
      })?.data;
      const selection = recordField33(context?.selection);
      return selection ? {
        ...selection,
        stale_policy: selection.stale_policy === "accept_stale" ? "accept_stale" : "strict"
      } : void 0;
    }
    function readContentUnitCandidate2(index, contentUnitRef, candidateId) {
      const sourceCandidate = index.documents.find((document) => {
        if (!document.path.startsWith(`${contentUnitRef}/candidates/`)) return false;
        if (!document.path.endsWith("/content_candidate.json")) return false;
        const record = recordField33(document.data);
        return record !== void 0 && String(record.id ?? "") === String(candidateId);
      })?.data;
      const sourceRecord = recordField33(sourceCandidate);
      if (sourceRecord) return sourceRecord;
      const context = index.documents.find((document) => {
        const record = recordField33(document.data);
        return record?.schema === "movscript.decision_context.v1" && record.target_kind === "content_unit" && record.target_ref === contentUnitRef;
      })?.data;
      for (const candidate of arrayField42(recordField33(context)?.candidates).map(recordField33).filter(isDefined23)) {
        if (String(candidate.id ?? "") === String(candidateId)) return candidate;
      }
      return void 0;
    }
    function findEntityByRef4(index, entityKind, ref) {
      const value = idField62(ref);
      if (value === void 0) return void 0;
      const normalized = typeof value === "string" ? value.replace(/\/+$/, "") : String(value);
      return queryMovScriptWorkspaceEntities2(index, { entityKind }).find((entity) => {
        const dir = entityDir42(entity.path);
        return dir === normalized || entity.path === `${normalized}/${entityKind}.json` || sameEntityRef3(entity.id, value, entityKind);
      });
    }
    function requiredString3(value, message) {
      const next = stringField132(value);
      if (!next) throw new Error(message);
      return next;
    }
    function firstEntity2(index, entityKind) {
      return queryMovScriptWorkspaceEntities2(index, { entityKind, limit: 1 })[0];
    }
    function canonicalEntities23(index) {
      return index.entities;
    }
    function entityDir42(path) {
      return path.replace(/\/[^/]+$/, "");
    }
    function recordField33(value) {
      return isRecord202(value) ? value : void 0;
    }
    function arrayField42(value) {
      return Array.isArray(value) ? value : [];
    }
    function resourceIdsFromValue22(value) {
      if (Array.isArray(value)) return value.flatMap(resourceIdsFromValue22);
      const text = stringField132(value);
      if (text) {
        const ids = [];
        const patterns = [
          /resource#([0-9]+)/gi,
          /reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi,
          /style_reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi
        ];
        for (const pattern of patterns) {
          for (const match of text.matchAll(pattern)) {
            for (const part of String(match[1] ?? "").split(",")) {
              const parsed2 = Number(part.trim());
              if (Number.isInteger(parsed2) && parsed2 > 0) ids.push(parsed2);
            }
          }
        }
        if (ids.length > 0) return ids;
        const parsed = Number(text);
        return Number.isInteger(parsed) && parsed > 0 ? [parsed] : [];
      }
      const id = resourceIdField32(value);
      if (id !== void 0) return [id];
      if (isRecord202(value)) return resourceIdsFromValue22(value.resource_id ?? value.resourceId ?? value.id);
      return [];
    }
    function uniqueIds3(values) {
      const seen = /* @__PURE__ */ new Set();
      const output3 = [];
      for (const value of values) {
        const key = String(value);
        if (seen.has(key)) continue;
        seen.add(key);
        output3.push(value);
      }
      return output3;
    }
    function stringField132(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function contentUnitOutputKind22(value) {
      if (value === "image" || value === "video" || value === "audio" || value === "text" || value === "metadata") return value;
      return "metadata";
    }
    function idField62(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function resourceIdField32(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function providerAssetUri2(certification) {
      const direct = stringField132(certification.asset_uri ?? certification.assetUri);
      if (direct) return direct;
      const hubAssetId = stringField132(certification.hub_asset_id ?? certification.hubAssetId);
      return hubAssetId ? `asset://${hubAssetId}` : void 0;
    }
    function stableJsonValue2(value) {
      if (Array.isArray(value)) return value.map(stableJsonValue2);
      if (!isRecord202(value)) return value;
      return Object.fromEntries(Object.entries(value).filter(([key]) => !key.startsWith("__workspace_")).sort(([left], [right]) => left.localeCompare(right)).map(([key, item]) => [key, stableJsonValue2(item)]));
    }
    function isRecord202(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function isDefined23(value) {
      return value !== void 0;
    }
    var CONTENT_UNIT_ADAPTERS2 = Object.fromEntries(
      MOVSCRIPT_SPECIALIZED_CONTENT_UNIT_TYPES2.map((contentUnitType22) => [contentUnitType22, specializedAdapter2(contentUnitType22)])
    );
    function hasSpecializedContentUnitAdapter2(contentUnitType22) {
      return typeof contentUnitType22 === "string" && CONTENT_UNIT_ADAPTERS2[contentUnitType22] !== void 0;
    }
    function contentUnitAdapterFor2(contentUnitType22) {
      return CONTENT_UNIT_ADAPTERS2[contentUnitType22] ?? genericAdapter2(contentUnitType22);
    }
    function specializedAdapter2(contentUnitType22) {
      if (contentUnitType22 === "timeline_assembly_ref") return timelineAssemblyAdapter2();
      if (contentUnitType22 === "expression_unit_ref") return expressionUnitAdapter2();
      const targetAdapter = contentUnitTargetAdapterFor2(contentUnitType22);
      if (!targetAdapter?.primaryRefKind || !targetAdapter.outputKind) return genericAdapter2(contentUnitType22);
      return refAdapter2(targetAdapter.contentUnitType, targetAdapter.primaryRefKind, targetAdapter.outputKind);
    }
    function genericAdapter2(contentUnitType22) {
      return {
        type: contentUnitType22,
        version: "generic_prompt@1",
        outputKind: "metadata",
        validate(context) {
          return contentUnitTargetIssues2(context.contentUnit.record);
        },
        derivePrompt(context) {
          return basePrompt2(context, {
            adapterVersion: this.version,
            outputKind: contentUnitOutputKind22(context.contentUnit.record.output_kind),
            primaryKind: void 0,
            blockers: []
          });
        },
        collectDependencies(_context, prompt) {
          return dependenciesFromPrompt2(prompt);
        },
        deriveRuntimePanel(context, derivation) {
          return runtimePanelFor2(context, this.version, derivation.prompt);
        }
      };
    }
    function refAdapter2(type, primaryKind, outputKind) {
      return {
        type,
        version: `${type}@2`,
        outputKind,
        validate(context) {
          const issues = contentUnitTargetIssues2(context.contentUnit.record);
          const expectedOutputKind = expectedOutputKindForContentUnitType22(type);
          if (expectedOutputKind && context.contentUnit.record.output_kind !== expectedOutputKind) {
            issues.push({ severity: "error", message: `${type} output_kind must be ${expectedOutputKind}` });
          }
          const primaryRefs = primaryContentUnitRefs3(context.contentUnit, primaryKind);
          const primaryFieldName = primaryRefFieldNameForKind22(primaryKind);
          if (primaryRefs.length === 0) {
            issues.push({ severity: "error", message: `${type} requires ${primaryFieldName}` });
          }
          if (hasAmbiguousPrimaryRefs3(primaryRefs, primaryKind)) {
            issues.push({ severity: "error", message: `${type} accepts only one ${primaryFieldName}` });
          }
          for (const ref of primaryRefs) {
            if (ref.kind !== "content_unit" && !findEntityByRef4(context.index, ref.kind, ref.id)) {
              issues.push({ severity: "error", message: `${type} ${primaryFieldName} does not resolve: ${ref.id}` });
            }
          }
          return issues;
        },
        derivePrompt(context) {
          const prompt = basePrompt2(context, {
            adapterVersion: this.version,
            outputKind,
            primaryKind,
            blockers: []
          });
          const blockers = [
            ...prompt.blockers ?? [],
            ...promptBlockers2(context, prompt.refs, primaryKind)
          ];
          return {
            ...prompt,
            blockers: blockers.length > 0 ? dedupeBlockers3(blockers) : void 0
          };
        },
        collectDependencies(context, prompt) {
          const dependencies = dependenciesFromPrompt2(prompt);
          return {
            ...dependencies,
            blockers: prompt.blockers ?? promptBlockers2(context, prompt.refs, primaryKind)
          };
        },
        deriveRuntimePanel(context, derivation) {
          return runtimePanelFor2(context, this.version, derivation.prompt);
        }
      };
    }
    function contentUnitTargetIssues2(record) {
      return contentUnitTargetValidationDiagnostics2(record).map((diagnostic22) => ({ severity: diagnostic22.severity, message: diagnostic22.message }));
    }
    function timelineAssemblyAdapter2() {
      return {
        type: "timeline_assembly_ref",
        version: "timeline_assembly_ref@1",
        outputKind: "video",
        validate(context) {
          const issues = contentUnitTargetIssues2(context.contentUnit.record);
          if (context.contentUnit.record.output_kind !== "video") {
            issues.push({ severity: "error", message: "timeline_assembly_ref output_kind must be video" });
          }
          return issues;
        },
        derivePrompt(context) {
          return basePrompt2(context, {
            adapterVersion: this.version,
            outputKind: "video",
            primaryKind: void 0,
            blockers: []
          });
        },
        collectDependencies(_context, prompt) {
          return dependenciesFromPrompt2(prompt);
        },
        deriveRuntimePanel(context, derivation) {
          return runtimePanelFor2(context, this.version, derivation.prompt);
        }
      };
    }
    function expressionUnitAdapter2() {
      return {
        ...refAdapter2("expression_unit_ref", "expression_unit", "metadata"),
        version: "expression_unit_ref@1",
        outputKind: "metadata",
        derivePrompt(context) {
          const outputKind = contentUnitOutputKind22(context.contentUnit.record.output_kind);
          const prompt = basePrompt2(context, {
            adapterVersion: this.version,
            outputKind,
            primaryKind: "expression_unit",
            blockers: []
          });
          const blockers = [
            ...prompt.blockers ?? [],
            ...promptBlockers2(context, prompt.refs, "expression_unit")
          ];
          return {
            ...prompt,
            blockers: blockers.length > 0 ? dedupeBlockers3(blockers) : void 0
          };
        }
      };
    }
    function basePrompt2(context, options) {
      const editPrompt = normalizedEditPrompt2(context.contentUnit.record.edit_prompt);
      const refs = parseContentUnitEditPromptRefs22(context.contentUnit.record.edit_prompt);
      const unsupportedRefs = parseUnsupportedContentUnitEditPromptRefs3(context.contentUnit.record.edit_prompt);
      const resolved = resolvePromptRefs2(context.index, refs, options.primaryKind);
      const resolvedRefs = resolved.refs.map((ref) => annotateInputSelectionStatus2(context, ref));
      const modelIntent = recordField33(context.contentUnit.record.model_intent);
      const styleReferenceResourceIds = usesVisualStyleReferences3(options.outputKind) ? projectStyleReferenceResourceIds3(context.index) : [];
      const semanticInputs = resolvedRefs.filter((ref) => ref.role === "input" && ref.selection?.resource_id !== void 0 && ref.selection.stale !== true).map((ref) => ({
        role: `${ref.kind}_ref`,
        kind: inputKindForRef2(context, ref),
        ref: ref.raw,
        source_content_unit_ref: ref.selection?.content_unit_ref,
        candidate_id: ref.selection?.candidate_id,
        resource_id: ref.selection?.resource_id,
        provider_asset: ref.selection?.provider_asset,
        required: true
      }));
      const blockers = [
        ...options.blockers,
        ...unsupportedRefs.map((ref) => ({
          code: "unsupported_prompt_ref_kind",
          ref: ref.raw,
          message: `unsupported prompt ref kind "${ref.kind}": ${ref.raw}. Use system primitive, content_unit, candidate, or resource refs; namespace vocabulary is context, not a selected-resource dependency.`
        })),
        ...inputBlockers2(context, resolvedRefs)
      ];
      const runtimeRequest = {
        capability: stringField132(modelIntent?.capability) ?? capabilityForOutputKind2(options.outputKind),
        model_intent: modelIntent,
        inputs: [
          ...semanticInputs,
          ...styleReferenceResourceIds.map((resourceId) => ({
            role: "style_reference",
            kind: "image",
            resource_id: resourceId,
            required: false
          }))
        ],
        params: recordField33(modelIntent?.params),
        metadata: metadataForOutputKind2(options.outputKind, modelIntent, styleReferenceResourceIds)
      };
      return pruneUndefined152({
        schema: "movscript.content_unit_prompt.v1",
        content_unit_ref: entityDir42(context.contentUnit.path),
        content_unit_id: context.contentUnit.id,
        content_unit_type: String(context.contentUnit.record.content_unit_type ?? ""),
        output_kind: options.outputKind,
        adapter_version: options.adapterVersion,
        edit_prompt: editPrompt,
        model_intent: modelIntent,
        refs: resolvedRefs,
        runtime_request: pruneUndefined152(runtimeRequest),
        blockers: blockers.length > 0 ? blockers : void 0,
        created_at: context.createdAt
      });
    }
    function dependenciesFromPrompt2(prompt) {
      const entities = prompt.refs.reduce((out, ref) => {
        if (!ref.resolved) return out;
        out[ref.kind] = [...out[ref.kind] ?? [], {
          entityKind: ref.resolved.entityKind,
          id: ref.resolved.id,
          path: ref.resolved.path ?? "",
          index: 0,
          record: {}
        }];
        return out;
      }, {});
      return {
        entities,
        refs: prompt.refs,
        upstreamSelections: prompt.refs.map((ref) => ref.selection).filter(isDefined32),
        blockers: prompt.blockers ?? []
      };
    }
    function runtimePanelFor2(context, adapterVersion, prompt) {
      const status = prompt.blockers?.length ? "blocked" : "ready";
      return {
        schema: "movscript.content_unit_runtime_panel.v1",
        content_unit_ref: entityDir42(context.contentUnit.path),
        content_unit_id: context.contentUnit.id,
        content_unit_type: prompt.content_unit_type,
        adapter_version: adapterVersion,
        output_kind: prompt.output_kind,
        status,
        prompt: prompt.edit_prompt,
        runtime_request: prompt.runtime_request,
        review: prompt.blockers?.length ? { blockers: prompt.blockers.map((blocker) => blocker.message) } : void 0
      };
    }
    function promptBlockers2(context, _refs, primaryKind) {
      const blockers = [];
      const primaryRefs = primaryContentUnitRefs3(context.contentUnit, primaryKind);
      const primaryFieldName = primaryRefFieldNameForKind22(primaryKind);
      if (primaryRefs.length === 0) {
        blockers.push({
          code: "primary_ref_missing",
          message: `${context.contentUnit.record.content_unit_type} requires ${primaryFieldName}`
        });
      }
      if (hasAmbiguousPrimaryRefs3(primaryRefs, primaryKind)) {
        blockers.push({
          code: "primary_ref_ambiguous",
          message: `${context.contentUnit.record.content_unit_type} accepts only one ${primaryFieldName}`
        });
      }
      for (const ref of primaryRefs) {
        if (ref.kind !== "content_unit" && !findEntityByRef4(context.index, ref.kind, ref.id)) {
          blockers.push({
            code: "ref_not_found",
            ref: ref.id,
            message: `primary ref does not resolve: ${ref.id}`
          });
        }
      }
      return blockers;
    }
    function inputBlockers2(context, refs) {
      const blockers = [];
      for (const ref of refs.filter((item) => item.role === "input")) {
        if (ref.blocker) {
          blockers.push(ref.blocker);
          continue;
        }
        if (!ref.resolved) {
          blockers.push({
            code: "ref_not_found",
            ref: ref.raw,
            message: `prompt input ref does not resolve: ${ref.raw}`
          });
          continue;
        }
        const upstreamContentUnit = resolveContentUnitForPromptRef3(context.index, ref);
        if (!upstreamContentUnit) {
          blockers.push({
            code: "upstream_content_unit_not_found",
            ref: ref.raw,
            message: `prompt input has no ${ref.kind}_ref content unit: ${ref.raw}`
          });
          continue;
        }
        if (!ref.selection) {
          blockers.push({
            code: "upstream_selection_missing",
            ref: ref.raw,
            message: `prompt input content unit has no selected candidate: ${ref.raw}`
          });
          continue;
        }
        if (ref.selection.stale === true) {
          blockers.push({
            code: "upstream_selection_stale",
            ref: ref.raw,
            message: `prompt input selected candidate is stale: ${ref.raw}`
          });
          continue;
        }
        if (ref.selection.resource_id === void 0) {
          blockers.push({
            code: "upstream_resource_missing",
            ref: ref.raw,
            message: `prompt input selected candidate has no resource_id: ${ref.raw}`
          });
        }
      }
      return blockers;
    }
    function dedupeBlockers3(blockers) {
      const seen = /* @__PURE__ */ new Set();
      const output3 = [];
      for (const blocker of blockers) {
        const key = `${blocker.code}:${blocker.ref ?? ""}:${blocker.message}`;
        if (seen.has(key)) continue;
        seen.add(key);
        output3.push(blocker);
      }
      return output3;
    }
    function annotateInputSelectionStatus2(context, ref) {
      if (ref.role !== "input" || !ref.selection) return ref;
      const status = upstreamSelectionStatus2(context, ref);
      if (!status) return ref;
      return {
        ...ref,
        selection: {
          ...ref.selection,
          stale: status.stale
        },
        ...status.blocker ? { blocker: status.blocker } : {}
      };
    }
    function upstreamSelectionStatus2(context, ref) {
      const upstreamContentUnit = resolveContentUnitForPromptRef3(context.index, ref);
      if (!upstreamContentUnit) return void 0;
      const upstreamRef = entityDir42(upstreamContentUnit.path);
      const currentRef = entityDir42(context.contentUnit.path);
      const stack = context.promptStack ?? [];
      if (upstreamRef === currentRef || stack.includes(upstreamRef)) {
        return {
          stale: true,
          blocker: {
            code: "prompt_dependency_cycle",
            ref: ref.raw,
            message: `prompt input forms a dependency cycle: ${ref.raw}`
          }
        };
      }
      const selection = readSelectedContentUnit2(context.index, upstreamRef);
      const candidateId = idField62(selection?.candidate_id);
      const candidate = candidateId === void 0 ? void 0 : readContentUnitCandidate2(context.index, upstreamRef, candidateId);
      if (candidateId !== void 0 && !candidate) {
        return {
          stale: true,
          blocker: {
            code: "upstream_candidate_missing",
            ref: ref.raw,
            message: `prompt input selected candidate is missing: ${ref.raw}`
          }
        };
      }
      const candidatePrompt = normalizedContentUnitPrompt2(candidate?.prompt_snapshot);
      if (!candidatePrompt) return { stale: true };
      const contentUnitType22 = stringField132(upstreamContentUnit.record.content_unit_type);
      if (!contentUnitType22) return { stale: true };
      const adapter = contentUnitAdapterFor2(contentUnitType22);
      const currentPrompt = adapter.derivePrompt({
        ...context,
        contentUnit: upstreamContentUnit,
        promptStack: [...stack, currentRef]
      });
      return { stale: !sameCanonicalPrompt2(currentPrompt, candidatePrompt) };
    }
    function sameCanonicalPrompt2(left, right) {
      return JSON.stringify(stableJsonValue2(canonicalPromptComparisonValue2(left))) === JSON.stringify(stableJsonValue2(canonicalPromptComparisonValue2(right)));
    }
    function canonicalPromptComparisonValue2(prompt) {
      const refs = Array.isArray(prompt.refs) ? prompt.refs : [];
      const blockers = Array.isArray(prompt.blockers) ? prompt.blockers : [];
      return {
        content_unit_type: prompt.content_unit_type,
        output_kind: prompt.output_kind,
        edit_prompt: prompt.edit_prompt,
        model_intent: prompt.model_intent,
        refs: refs.map((ref) => ({
          kind: ref.kind,
          id: ref.id,
          raw: ref.raw,
          role: ref.role,
          resolved: ref.resolved,
          selection: ref.selection ? {
            content_unit_ref: ref.selection.content_unit_ref,
            candidate_id: ref.selection.candidate_id,
            resource_id: ref.selection.resource_id,
            artifact_ref: ref.selection.artifact_ref,
            provider_asset: ref.selection.provider_asset
          } : void 0
        })),
        runtime_request: prompt.runtime_request,
        blockers: blockers.map((blocker) => ({
          code: blocker.code,
          ref: blocker.ref
        }))
      };
    }
    function normalizedContentUnitPrompt2(value) {
      const record = recordField33(value);
      if (!record) return void 0;
      if (record.schema !== "movscript.content_unit_prompt.v1" && record.schema !== "movscript.content_unit_generation_prompt_snapshot.v1") return void 0;
      if (!Array.isArray(record.refs)) return void 0;
      if (!recordField33(record.runtime_request)) return void 0;
      return record;
    }
    function normalizedEditPrompt2(value) {
      const record = recordField33(value);
      if (!record) return void 0;
      return pruneUndefined152({
        text: stringField132(record.text),
        negative_text: stringField132(record.negative_text),
        notes: stringField132(record.notes),
        structured: isRecord202(record.structured) ? record.structured : void 0
      });
    }
    function capabilityForOutputKind2(outputKind) {
      switch (outputKind) {
        case "image":
          return "image";
        case "video":
          return "video";
        case "audio":
          return "audio";
        case "text":
          return "text";
        default:
          return "metadata";
      }
    }
    function inputKindForRef2(context, ref) {
      const upstream = resolveContentUnitForPromptRef3(context.index, ref);
      return contentUnitOutputKind22(upstream?.record.output_kind);
    }
    function usesVisualStyleReferences3(outputKind) {
      return outputKind === "image" || outputKind === "video";
    }
    function metadataForOutputKind2(outputKind, modelIntent, styleReferenceResourceIds) {
      const metadata = {};
      const duration = typeof modelIntent?.duration_sec === "number" && Number.isFinite(modelIntent.duration_sec) ? modelIntent.duration_sec : void 0;
      if (outputKind === "video" && duration !== void 0) metadata.duration_sec = duration;
      if (styleReferenceResourceIds.length > 0) metadata.style_reference_resource_ids = styleReferenceResourceIds;
      return Object.keys(metadata).length > 0 ? metadata : void 0;
    }
    function isDefined32(value) {
      return value !== void 0;
    }
    function pruneUndefined152(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    var INTERPRETER_VERSION2 = "movscript-interpreter@0.2.0";
    function deriveContentUnitArtifacts2(index, input3) {
      return canonicalEntities23(index).filter((entity) => entity.entityKind === "content_unit" && entity.id !== void 0).map((contentUnit) => deriveContentUnitArtifact2(index, contentUnit, {
        createdAt: input3.createdAt,
        interpreterVersion: input3.interpreterVersion ?? INTERPRETER_VERSION2
      }));
    }
    function deriveContentUnitArtifact2(index, contentUnit, input3) {
      if (contentUnit.id === void 0) throw new Error(`content_unit missing id: ${contentUnit.path}`);
      const contentUnitType22 = requiredString3(contentUnit.record.content_unit_type, `content_unit_type missing: ${contentUnit.path}`);
      const adapter = contentUnitAdapterFor2(contentUnitType22);
      const context = {
        index,
        contentUnit,
        interpreterVersion: input3.interpreterVersion ?? INTERPRETER_VERSION2,
        createdAt: input3.createdAt
      };
      const issues = adapter.validate(context);
      const generationPrompt = adapter.derivePrompt(context);
      const dependencies = adapter.collectDependencies(context, generationPrompt);
      const runtimePanel = adapter.deriveRuntimePanel(context, { dependencies, prompt: generationPrompt });
      const dependencyReport = {
        schema: "movscript.content_unit_dependency_report.v1",
        content_unit_ref: entityDir42(contentUnit.path),
        content_unit_type: contentUnitType22,
        dependencies: Object.entries(dependencies.entities).flatMap(([role, entities]) => {
          return entities.map((entity) => ({
            role,
            entityKind: entity.entityKind,
            ...entity.id !== void 0 ? { id: entity.id } : {},
            path: entity.path
          }));
        }),
        refs: dependencies.refs,
        upstream_selections: dependencies.upstreamSelections,
        ...dependencies.blockers.length > 0 ? { blockers: dependencies.blockers } : {},
        issues
      };
      const selectionValidity = selectionValidityFor2(index, contentUnit, generationPrompt);
      return {
        contentUnitId: contentUnit.id,
        contentUnitPath: contentUnit.path,
        runtimePanel,
        generationPrompt,
        dependencyReport,
        selectionValidity
      };
    }
    function selectionValidityFor2(index, contentUnit, currentPrompt) {
      const selection = readSelectedContentUnit2(index, entityDir42(contentUnit.path));
      const stalePolicy22 = selection?.stale_policy === "accept_stale" ? "accept_stale" : "strict";
      const selectedCandidateId = idField62(selection?.candidate_id);
      const selectedResourceId22 = resourceIdField32(selection?.resource_id);
      const candidate = selectedCandidateId === void 0 ? void 0 : readContentUnitCandidate2(index, entityDir42(contentUnit.path), selectedCandidateId);
      const candidatePromptRecord = recordField43(candidate?.prompt_snapshot);
      const candidatePrompt = isNormalizedContentUnitPrompt2(candidatePromptRecord) ? candidatePromptRecord : void 0;
      const staleReasons = selection ? staleReasonsFor2(currentPrompt, {
        candidateId: selectedCandidateId,
        candidate,
        candidatePrompt
      }) : [];
      return {
        schema: "movscript.content_unit_selection_validity.v2",
        content_unit_ref: entityDir42(contentUnit.path),
        selected: Boolean(selection),
        ...selectedCandidateId !== void 0 ? { candidate_id: selectedCandidateId } : {},
        ...selectedResourceId22 !== void 0 ? { resource_id: selectedResourceId22 } : {},
        stale: staleReasons.length > 0,
        stale_policy: stalePolicy22,
        reason: stringField132(selection?.reason),
        ...staleReasons.length > 0 ? { stale_reasons: staleReasons } : {}
      };
    }
    function staleReasonsFor2(currentPrompt, input3) {
      if (input3.candidateId !== void 0 && !input3.candidate) return ["candidate_missing"];
      const candidatePrompt = input3.candidatePrompt;
      if (!candidatePrompt) return ["candidate_prompt_missing"];
      const reasons = /* @__PURE__ */ new Set();
      if (!sameCanonical2(currentPrompt.edit_prompt, candidatePrompt.edit_prompt)) reasons.add("edit_prompt_changed");
      if (!sameCanonical2(currentPrompt.model_intent, candidatePrompt.model_intent)) reasons.add("model_intent_changed");
      if (!sameCanonical2(refComparisonValue2(currentPrompt), refComparisonValue2(candidatePrompt))) reasons.add("refs_changed");
      if (!sameCanonical2(currentPrompt.runtime_request, candidatePrompt.runtime_request)) reasons.add("runtime_inputs_changed");
      if ((currentPrompt.blockers?.length ?? 0) > 0) reasons.add("prompt_dependency_missing");
      return [...reasons];
    }
    function refComparisonValue2(prompt) {
      return prompt.refs.map((ref) => ({
        kind: ref.kind,
        id: ref.id,
        raw: ref.raw,
        role: ref.role,
        resolved: ref.resolved,
        selection: ref.selection
      }));
    }
    function isNormalizedContentUnitPrompt2(value) {
      const record = recordField43(value);
      return (record?.schema === "movscript.content_unit_prompt.v1" || record?.schema === "movscript.content_unit_generation_prompt_snapshot.v1") && Array.isArray(record.refs) && recordField43(record.runtime_request) !== void 0 && recordField43(record.edit_prompt) !== void 0;
    }
    function sameCanonical2(left, right) {
      return JSON.stringify(stableJsonValue2(left ?? null)) === JSON.stringify(stableJsonValue2(right ?? null));
    }
    function recordField43(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value) ? value : void 0;
    }
    function canonicalEntities32(index) {
      return index.entities;
    }
    function entityRef32(entity) {
      return {
        entityKind: entity.entityKind,
        ...entity.id !== void 0 ? { id: entity.id } : {},
        path: entity.path
      };
    }
    function entityRefKey2(ref) {
      return `${ref.entityKind}:${String(ref.id ?? ref.path ?? "")}`;
    }
    function entityRefMatches2(left, right) {
      if (left.entityKind !== right.entityKind) return false;
      if (left.id !== void 0 && right.id !== void 0 && String(left.id) === String(right.id)) return true;
      if (left.path && right.path && left.path === right.path) return true;
      return false;
    }
    function nearestParentEntity2(path, entitiesByDir) {
      const parent = nearestParentPath22(entityDir52(path), new Set(entitiesByDir.keys()));
      return parent ? entitiesByDir.get(parent) : void 0;
    }
    function nearestParentPath22(path, candidates) {
      const parts = path.split("/");
      for (let index = parts.length - 1; index > 0; index -= 1) {
        const candidate = parts.slice(0, index).join("/");
        if (candidates.has(candidate)) return candidate;
      }
      return void 0;
    }
    function relationTypeForParent2(parentType, childType) {
      if (parentType === "setting_state" && childType === "asset") return "owns";
      return "contains";
    }
    function dedupeRelations2(relations) {
      const seen = /* @__PURE__ */ new Set();
      const out = [];
      for (const relation of relations) {
        const key = JSON.stringify(relation);
        if (seen.has(key)) continue;
        seen.add(key);
        out.push(relation);
      }
      return out;
    }
    function entityDir52(path) {
      return path.replace(/\/[^/]+$/, "");
    }
    function normalizedRefDir2(value) {
      return typeof value === "string" ? value.replace(/\/+$/, "") : "";
    }
    function entityKey3(entityKind, id) {
      return `${entityKind}:${String(id ?? "")}`;
    }
    function findEntityByRef22(entities, entityKind, ref) {
      if (ref === void 0 || ref === null || String(ref).trim() === "") return void 0;
      return entities.find((entity) => entity.entityKind === entityKind && sameEntityRef3(entity.id, ref, entityKind));
    }
    function parentExpressionUnitForEntity2(entity, entityByPathDir, entityById) {
      const expressionUnitRef = normalizedRefDir2(entity.record.expression_unit_ref);
      if (expressionUnitRef) return entityByPathDir.get(expressionUnitRef) ?? entityById.get(entityKey3("expression_unit", expressionUnitRef));
      const expressionUnitId = pathSegmentAfter32(entity.path, "expression_units");
      return expressionUnitId ? entityById.get(entityKey3("expression_unit", expressionUnitId)) : void 0;
    }
    function pathSegmentAfter32(path, segment) {
      const parts = path.split("/");
      const index = parts.indexOf(segment);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function recordField52(value) {
      return isRecord212(value) ? value : void 0;
    }
    function arrayField52(value) {
      return Array.isArray(value) ? value : [];
    }
    function stringField142(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberField34(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function idField72(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function isRecord212(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function deriveAssetIndex2(index) {
      return {
        schema: "movscript.asset-index.v1",
        assets: canonicalEntities32(index).filter((entity) => entity.entityKind === "asset").map((entity) => ({
          ...entity.id !== void 0 ? { id: entity.id } : {},
          path: entity.path,
          owner: assetOwnerRef2(entity.path),
          slot: stringField142(entity.record.slot)
        }))
      };
    }
    function assetOwnerRef2(path) {
      const parts = path.split("/");
      const statesIndex = parts.indexOf("states");
      if (statesIndex >= 0) return { entityKind: "setting_state", id: parts[statesIndex + 1] };
      return { entityKind: "unknown" };
    }
    function deriveDomainTree2(index) {
      const nodes = /* @__PURE__ */ new Map();
      const roots = [];
      for (const entity of canonicalEntities32(index)) {
        if (!isTreeEntity2(entity)) continue;
        nodes.set(entity.path, treeNode2(entity));
      }
      for (const node of nodes.values()) {
        const parent = nearestParentNode2(node.path, nodes);
        if (parent) parent.children.push(node);
        else roots.push(node);
      }
      sortTreeNodes2(roots);
      return { schema: "movscript.domain-tree.v1", roots };
    }
    function isTreeEntity2(entity) {
      return true;
    }
    function treeNode2(entity) {
      const nodeCategory = classifyMovScriptEntityKind2(entity.entityKind);
      return {
        entityKind: entity.entityKind,
        ...nodeCategory ? { nodeCategory } : {},
        nodeKind: domainNodeKind2(entity, nodeCategory),
        ...entity.id !== void 0 ? { id: entity.id } : {},
        path: entity.path,
        title: stringField142(entity.record.title) ?? String(entity.id ?? entity.path),
        order: numberField34(entity.record.order),
        children: []
      };
    }
    function domainNodeKind2(entity, _nodeCategory) {
      return projectMovScriptDomainNodeKind2(entity.entityKind, entity.record);
    }
    function nearestParentNode2(path, nodes) {
      const parent = nearestParentPath22(path, new Set(nodes.keys()));
      return parent ? nodes.get(parent) : void 0;
    }
    function sortTreeNodes2(nodes) {
      nodes.sort((left, right) => {
        const leftOrder = left.order ?? Number.MAX_SAFE_INTEGER;
        const rightOrder = right.order ?? Number.MAX_SAFE_INTEGER;
        if (leftOrder !== rightOrder) return leftOrder - rightOrder;
        return left.path.localeCompare(right.path);
      });
      for (const node of nodes) sortTreeNodes2(node.children);
    }
    function deriveEditPlans2(index, contentUnitArtifacts) {
      const contentUnitArtifactByPath = new Map(contentUnitArtifacts.map((artifact) => [artifact.contentUnitPath, artifact]));
      const expressionUnits = queryMovScriptWorkspaceEntities2(index, { entityKind: "expression_unit" });
      const expressionByRef = /* @__PURE__ */ new Map();
      for (const expressionUnit of expressionUnits) {
        expressionByRef.set(entityDir42(expressionUnit.path), expressionUnit);
        if (expressionUnit.id !== void 0) expressionByRef.set(String(expressionUnit.id), expressionUnit);
      }
      return canonicalEntities23(index).filter((entity) => entity.entityKind === "scene_moment" && entity.id !== void 0).flatMap((sceneMoment) => {
        const units = contentUnitsForSceneMoment2(index, sceneMoment, expressionUnits);
        if (units.length === 0) return [];
        const production = productionForSceneMoment2(index, sceneMoment);
        if (!production?.id) return [];
        const items = units.flatMap((contentUnit, index2) => {
          const artifact = contentUnitArtifactByPath.get(contentUnit.path);
          if (!artifact) return [];
          return [trackItemFor2(contentUnit, artifact, sceneMoment, expressionByRef, index2)];
        }).sort((left, right) => left.order - right.order);
        const tracks = groupTrackItems2(items);
        const blockers = items.flatMap((item) => {
          if (!item.selected) {
            return [{
              code: "selection_missing",
              content_unit_id: item.content_unit_id,
              message: `content unit ${String(item.content_unit_id)} has no selected candidate`
            }];
          }
          if (item.stale) {
            return [{
              code: "selection_stale",
              content_unit_id: item.content_unit_id,
              message: `content unit ${String(item.content_unit_id)} selected candidate is stale`
            }];
          }
          if (item.resource_id === void 0) {
            return [{
              code: "resource_missing",
              content_unit_id: item.content_unit_id,
              message: `content unit ${String(item.content_unit_id)} selected candidate has no resource_id`
            }];
          }
          return [];
        });
        return [{
          schema: "movscript.edit_plan.v1",
          productionId: production.id,
          productionPath: entityDir42(production.path),
          sceneMomentId: sceneMoment.id,
          sceneMomentPath: entityDir42(sceneMoment.path),
          target_ref: entityDir42(sceneMoment.path),
          status: blockers.length > 0 ? "missing_selection" : "ready_to_compose",
          tracks,
          compose_inputs: items.filter((item) => item.resource_id !== void 0).map((item) => ({
            content_unit_id: item.content_unit_id,
            resource_id: item.resource_id,
            output_kind: item.output_kind,
            track_type: trackTypeFor2(item)
          })),
          ...blockers.length > 0 ? { blockers } : {}
        }];
      });
    }
    function contentUnitsForSceneMoment2(index, sceneMoment, expressionUnits) {
      const sceneMomentDir = entityDir42(sceneMoment.path);
      const expressions = expressionUnits.filter((expressionUnit) => entityDir42(expressionUnit.path).startsWith(`${sceneMomentDir}/expression_units/`));
      return queryMovScriptWorkspaceEntities2(index, { entityKind: "content_unit" }).filter((contentUnit) => {
        const targetKind = stringField132(contentUnit.record.target_kind);
        const targetRef = stringField132(contentUnit.record.target_ref);
        if (targetKind === "scene_moment" && refMatches2(sceneMoment, targetRef)) return true;
        if (String(contentUnit.record.content_unit_type ?? "") === "scene_moment_ref" && refMatches2(sceneMoment, contentUnit.record.scene_moment_ref)) return true;
        if (String(contentUnit.record.content_unit_type ?? "") === "scence_moment_ref" && refMatches2(sceneMoment, contentUnit.record.scene_moment_ref ?? contentUnit.record.scence_moment_ref)) return true;
        if (targetKind === "expression_unit" || String(contentUnit.record.content_unit_type ?? "") === "expression_unit_ref") {
          const expressionRef = targetRef ?? stringField132(contentUnit.record.expression_unit_ref);
          return expressions.some((expressionUnit) => refMatches2(expressionUnit, expressionRef));
        }
        return false;
      });
    }
    function productionForSceneMoment2(index, sceneMoment) {
      const productionId = sceneMoment.path.split("/")[1];
      return queryMovScriptWorkspaceEntities2(index, { entityKind: "production" }).find((entity) => sameEntityRef3(entity.id, productionId, "production") || entityDir42(entity.path) === `productions/${productionId}`);
    }
    function trackItemFor2(contentUnit, artifact, sceneMoment, expressionByRef, fallbackOrder) {
      const targetKind = stringField132(contentUnit.record.target_kind) ?? primaryTargetKind2(contentUnit);
      const targetRef = stringField132(contentUnit.record.target_ref) ?? primaryTargetRef2(contentUnit) ?? entityDir42(sceneMoment.path);
      const expressionUnit = targetKind === "expression_unit" ? expressionByRef.get(targetRef) ?? expressionByRef.get(lastPathSegment32(targetRef) ?? "") : void 0;
      return {
        id: `edit_item_${String(contentUnit.id ?? fallbackOrder)}`,
        content_unit_id: contentUnit.id ?? contentUnit.path,
        content_unit_ref: entityDir42(contentUnit.path),
        output_kind: artifact.runtimePanel.output_kind,
        target_kind: targetKind,
        target_ref: targetRef,
        ...expressionUnit ? { expression_unit_ref: entityDir42(expressionUnit.path) } : {},
        ...expressionUnit ? { expression_slot_kind: expressionUnitSlotKindFromRecord2(expressionUnit.record) } : {},
        ...expressionUnit ? { expression_modality: stringField132(expressionUnit.record.modality) ?? legacyExpressionModality2(expressionUnit) } : {},
        ...expressionUnit ? { expression_role: stringField132(expressionUnit.record.role) ?? stringField132(expressionUnit.record.expression_kind) } : {},
        ...artifact.selectionValidity.candidate_id !== void 0 ? { candidate_id: artifact.selectionValidity.candidate_id } : {},
        ...artifact.selectionValidity.resource_id !== void 0 ? { resource_id: artifact.selectionValidity.resource_id } : {},
        selected: artifact.selectionValidity.selected,
        stale: artifact.selectionValidity.stale,
        timing_intent: recordField33(expressionUnit?.record.timing_intent),
        generation_role: stringField132(contentUnit.record.generation_role),
        order: numberField42(contentUnit.record.order) ?? numberField42(expressionUnit?.record.order) ?? fallbackOrder
      };
    }
    function groupTrackItems2(items) {
      const byType = /* @__PURE__ */ new Map();
      for (const item of items) {
        const type = trackTypeFor2(item);
        byType.set(type, [...byType.get(type) ?? [], item]);
      }
      return [...byType.entries()].map(([type, trackItems]) => ({ type, items: trackItems }));
    }
    function trackTypeFor2(item) {
      if (item.output_kind === "video") return "video";
      if (item.output_kind === "audio") return item.expression_slot_kind === "voice" || item.expression_role === "dialogue" || item.expression_role === "narration" ? "voice" : "audio";
      if (item.output_kind === "text") return "subtitle";
      if (item.output_kind === "image") return "image";
      if (item.expression_slot_kind === "subtitle") return "subtitle";
      return "metadata";
    }
    function primaryTargetKind2(contentUnit) {
      const type = String(contentUnit.record.content_unit_type ?? "");
      if (type === "scene_moment_ref" || type === "scence_moment_ref") return "scene_moment";
      if (type === "expression_unit_ref") return "expression_unit";
      if (type === "content_unit_ref") return "content_unit";
      return "metadata";
    }
    function primaryTargetRef2(contentUnit) {
      return stringField132(contentUnit.record.expression_unit_ref) ?? stringField132(contentUnit.record.scene_moment_ref) ?? stringField132(contentUnit.record.scence_moment_ref) ?? stringField132(contentUnit.record.content_unit_ref);
    }
    function legacyExpressionModality2(expressionUnit) {
      const kind = stringField132(expressionUnit.record.expression_kind);
      if (kind === "dialogue" || kind === "narration") return "verbal";
      if (kind === "subtitle" || kind === "caption") return "text";
      if (kind === "visual_note" || kind === "action") return "visual";
      return void 0;
    }
    function refMatches2(entity, ref) {
      if (typeof ref !== "string" && typeof ref !== "number") return false;
      return sameEntityRef3(entity.id, ref, entity.entityKind) || entityDir42(entity.path) === String(ref).replace(/\/+$/, "") || lastPathSegment32(ref) === String(entity.id);
    }
    function lastPathSegment32(value) {
      if (typeof value !== "string") return void 0;
      return value.split("/").filter(Boolean).at(-1);
    }
    function numberField42(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function deriveImpactReport2(changedEntities, interpretationId, createdAt, index, relationGraph, semanticChanges = []) {
      const semanticChangesByEntity = groupSemanticChangesByEntity3(semanticChanges);
      return {
        schema: "movscript.impact-report.v1",
        interpretationId,
        createdAt,
        changedEntities: changedEntities.map((entity) => {
          const affectedContentUnits = affectedContentUnitsForChangedEntity2(entity, index, relationGraph);
          return {
            entityKind: entity.entityKind,
            ...entity.id !== void 0 ? { id: entity.id } : {},
            path: entity.path,
            state: entity.state,
            businessImpacts: businessImpactsForChangedEntity2(entity, semanticChangesByEntity),
            editorImpacts: editorImpactsForChangedEntity2(entity, affectedContentUnits),
            affectedContentUnits,
            staleMarkers: staleMarkersForChangedEntity2(entity, affectedContentUnits)
          };
        })
      };
    }
    function groupSemanticChangesByEntity3(semanticChanges) {
      const groups = /* @__PURE__ */ new Map();
      for (const change of semanticChanges) {
        const key = entitySemanticKey3(change.entity.kind, change.entity.id);
        groups.set(key, [...groups.get(key) ?? [], change]);
      }
      return groups;
    }
    function businessImpactsForChangedEntity2(entity, semanticChangesByEntity) {
      const changes = semanticChangesByEntity.get(entitySemanticKey3(entity.entityKind, entity.id)) ?? [];
      const labels = changes.map((change) => businessImpactLabel3(change.businessKind));
      return [...new Set(labels)].sort();
    }
    function businessImpactLabel3(businessKind) {
      const labels = {
        metadata_changed: "Metadata changed",
        semantic_input_changed: "Semantic input changed",
        reference_changed: "Reference changed",
        selection_changed: "Selection changed",
        sequence_reordered: "Sequence reordered",
        expression_unit_changed: "Expression unit changed",
        storyboard_changed: "Storyboard changed",
        keyframe_changed: "Keyframe changed",
        content_unit_changed: "Content unit changed",
        project_context_changed: "Project context changed",
        production_structure_changed: "Production structure changed",
        domain_entity_changed: "Domain entity changed"
      };
      return labels[businessKind];
    }
    function entitySemanticKey3(entityKind, id) {
      return `${entityKind}:${String(id ?? "")}`;
    }
    function editorImpactsForChangedEntity2(entity, affectedContentUnits = []) {
      switch (entity.entityKind) {
        case "project_standards":
          return ["Generation prompt bundles may need reinterpretation."];
        case "setting":
        case "setting_state":
        case "asset":
          return [
            "Setting asset index should be refreshed.",
            affectedContentUnits.length > 0 ? `${affectedContentUnits.length} content production context(s) using this setting may be stale.` : "Generation contexts using this setting may be stale."
          ];
        case "production":
        case "segment":
        case "scene_moment":
        case "storyboard":
        case "audio_cue":
        case "expression_unit":
          return [
            "Production planning tree should be refreshed.",
            "Preview timeline may need reinterpretation.",
            ...affectedContentUnits.length > 0 ? [`${affectedContentUnits.length} content production prompt bundle(s) may need reinterpretation.`] : []
          ];
        case "content_unit":
          return ["Content production context should be refreshed.", "Preview timeline items using this content unit may be stale."];
        case "keyframe":
          return [
            "Visual anchors and generation reference bundles may need reinterpretation.",
            ...affectedContentUnits.length > 0 ? [`${affectedContentUnits.length} content production prompt bundle(s) may need reinterpretation.`] : []
          ];
        default:
          return ["Domain index should be refreshed."];
      }
    }
    function affectedContentUnitsForChangedEntity2(changedEntity, index, relationGraph) {
      if (changedEntity.entityKind === "content_unit") {
        const sourceEntity = normalizeChangedEntityRef2(changedEntity, index);
        const contentUnit = canonicalEntities32(index).find((entity) => entity.entityKind === "content_unit" && entityRefMatches2(entityRef32(entity), sourceEntity));
        return changedEntity.id !== void 0 && hasSpecializedContentUnitAdapter2(contentUnit?.record.content_unit_type) ? [{ entityKind: "content_unit", id: changedEntity.id, path: changedEntity.path }] : [];
      }
      const changedRef = normalizeChangedEntityRef2(changedEntity, index);
      return canonicalEntities32(index).filter((entity) => entity.entityKind === "content_unit" && entity.id !== void 0 && hasSpecializedContentUnitAdapter2(entity.record.content_unit_type)).filter((contentUnit) => contentUnitReferencesChangedEntity2(entityRef32(contentUnit), changedRef, relationGraph)).map(entityRef32);
    }
    function contentUnitReferencesChangedEntity2(contentUnit, changedRef, relationGraph) {
      const visited = /* @__PURE__ */ new Set();
      const queue = [contentUnit];
      while (queue.length > 0) {
        const current = queue.shift();
        if (!current) continue;
        const currentKey = entityRefKey2(current);
        if (visited.has(currentKey)) continue;
        visited.add(currentKey);
        if (entityRefMatches2(current, changedRef)) return true;
        for (const relation of relationGraph.relations) {
          if (isForwardDependencyRelation2(relation, changedRef) && entityRefMatches2(relation.from, current)) {
            queue.push(relation.to);
          }
          if (isAncestorContextChange2(changedRef) && isParentContextRelation2(relation) && entityRefMatches2(relation.to, current)) {
            queue.push(relation.from);
          }
        }
      }
      return false;
    }
    function isForwardDependencyRelation2(relation, changedRef) {
      if (relation.type === "references" || relation.type === "uses" || relation.type === "targets") return true;
      if (relation.type === "owns") return changedRef.entityKind === "asset";
      if (relation.type === "contains") {
        return changedRef.entityKind === "keyframe" || changedRef.entityKind === "expression_unit" || changedRef.entityKind === "audio_cue" || changedRef.entityKind === "storyboard" || changedRef.entityKind === "scene_moment";
      }
      return false;
    }
    function isAncestorContextChange2(changedRef) {
      return changedRef.entityKind === "production" || changedRef.entityKind === "segment" || changedRef.entityKind === "setting" || changedRef.entityKind === "setting_state" || changedRef.entityKind === "scene_moment" || changedRef.entityKind === "expression_unit";
    }
    function isParentContextRelation2(relation) {
      return relation.type === "contains" || relation.type === "owns";
    }
    function normalizeChangedEntityRef2(changedEntity, index) {
      const existing = canonicalEntities32(index).find((entity) => {
        return entity.entityKind === changedEntity.entityKind && (entity.path === changedEntity.path || entity.id !== void 0 && changedEntity.id !== void 0 && String(entity.id) === String(changedEntity.id));
      });
      return existing ? entityRef32(existing) : {
        entityKind: changedEntity.entityKind,
        ...changedEntity.id !== void 0 ? { id: changedEntity.id } : {},
        path: changedEntity.path
      };
    }
    function staleMarkersForChangedEntity2(entity, affectedContentUnits) {
      if (affectedContentUnits.length === 0) return [];
      if (entity.entityKind === "content_unit") {
        return affectedContentUnits.map((contentUnit) => `content_unit:${String(contentUnit.id ?? contentUnit.path)}:self_changed`);
      }
      const reason = entity.entityKind === "asset" || entity.entityKind === "setting" || entity.entityKind === "setting_state" ? "setting_context_changed" : entity.entityKind === "keyframe" ? "visual_anchor_changed" : "planning_context_changed";
      return affectedContentUnits.map((contentUnit) => `content_unit:${String(contentUnit.id ?? contentUnit.path)}:${reason}`);
    }
    function deriveProductionWorkPlan2(input3) {
      const sourceIssues = input3.sourceIssues ?? [];
      const items = [
        ...sourceIssues.map((issue, index) => fixSourceWorkItem2(issue, index)),
        ...contentUnitWorkItems2(input3.index, input3.contentUnitArtifacts),
        ...affectedOutputWorkItems2(input3.impactReport)
      ].sort((left, right) => left.priority - right.priority || left.id.localeCompare(right.id));
      return {
        schema: "movscript.production_work_plan.v1",
        created_at: input3.createdAt,
        project: projectRef2(input3.index),
        source_status: {
          ready_to_interpret: !sourceIssues.some((issue) => issue.severity === "error"),
          has_pending_edits: (input3.changedEntities?.length ?? input3.impactReport.changedEntities.length) > 0,
          issue_count: sourceIssues.length
        },
        interpret_status: {
          status: "current",
          interpretation_id: input3.interpretationId,
          interpreted_at: input3.createdAt
        },
        items,
        summary: {
          open: items.filter((item) => item.status === "open" || item.status === "ready" || item.status === "blocked").length,
          blocking: items.filter((item) => item.severity === "blocking" || item.status === "blocked").length,
          human_recommended: items.filter((item) => item.recommended_actor === "human").length,
          agent_recommended: items.filter((item) => item.recommended_actor === "agent").length,
          ready_to_generate: items.filter((item) => item.kind === "generate_candidates" && item.status === "ready").length,
          stale_selections: items.filter((item) => item.kind === "review_stale_selection").length
        }
      };
    }
    function fixSourceWorkItem2(issue, index) {
      const severity = issue.severity === "error" ? "blocking" : "warning";
      return {
        id: stableWorkItemId2("fix_source", issue.path, issue.message, index),
        kind: "fix_source",
        status: issue.severity === "error" ? "blocked" : "open",
        severity,
        priority: issue.severity === "error" ? 10 : 30,
        reason: issue.message,
        target: {
          entityKind: "source_file",
          path: issue.path
        },
        blockers: issue.severity === "error" ? [{
          code: "source_issue",
          message: issue.message
        }] : void 0,
        allowed_actors: ["human", "agent"],
        recommended_actor: issue.message.includes("schema validation failed") ? "agent" : "human",
        actions: [{
          type: "open_editor",
          entityKind: "source_file",
          path: issue.path
        }],
        evidence: {
          severity: issue.severity,
          path: issue.path
        }
      };
    }
    function contentUnitWorkItems2(index, artifacts) {
      const items = [];
      for (const artifact of artifacts) {
        const contentUnitRef = contentUnitTarget2(artifact);
        const candidateCount = contentUnitCandidateCount2(index, entityDir52(artifact.contentUnitPath));
        const blockers = blockersFor2(artifact.dependencyReport.blockers ?? artifact.generationPrompt.blockers ?? []);
        const hasBlockingIssue = blockers.length > 0 || artifact.dependencyReport.issues.some((issue) => issue.severity === "error");
        if (hasBlockingIssue) {
          items.push({
            id: stableWorkItemId2("content_unit_blocked", String(artifact.contentUnitId)),
            kind: "edit_structure",
            status: "blocked",
            severity: "blocking",
            priority: 20,
            reason: `Content unit ${String(artifact.contentUnitId)} has unresolved production inputs.`,
            target: contentUnitRef,
            blockers: blockers.length > 0 ? blockers : artifact.dependencyReport.issues.map((issue) => ({
              code: "content_unit_issue",
              message: issue.message
            })),
            allowed_actors: ["human", "agent"],
            recommended_actor: "agent",
            actions: [
              {
                type: "open_editor",
                entityKind: "content_unit",
                entityId: artifact.contentUnitId,
                path: artifact.contentUnitPath
              },
              {
                type: "derive_content_unit_artifact",
                contentUnitId: artifact.contentUnitId
              }
            ],
            evidence: {
              runtimeStatus: artifact.runtimePanel.status,
              blockerCodes: blockers.map((blocker) => blocker.code),
              issueCount: artifact.dependencyReport.issues.length
            }
          });
          continue;
        }
        if (artifact.selectionValidity.stale) {
          items.push({
            id: stableWorkItemId2("review_stale_selection", String(artifact.contentUnitId), String(artifact.selectionValidity.candidate_id ?? "")),
            kind: "review_stale_selection",
            status: "open",
            severity: "warning",
            priority: 40,
            reason: `Selected candidate for content unit ${String(artifact.contentUnitId)} is stale.`,
            target: contentUnitRef,
            allowed_actors: ["human", "agent"],
            recommended_actor: "human",
            actions: [
              {
                type: "open_candidate_picker",
                contentUnitId: artifact.contentUnitId
              },
              {
                type: "agent_review_candidates",
                contentUnitId: artifact.contentUnitId
              },
              {
                type: "generate_candidates",
                contentUnitId: artifact.contentUnitId,
                capability: actionCapability2(artifact.runtimePanel.output_kind),
                suggestedCandidateCount: 2
              },
              {
                type: "accept_stale",
                contentUnitId: artifact.contentUnitId
              }
            ],
            evidence: {
              candidateId: artifact.selectionValidity.candidate_id,
              resourceId: artifact.selectionValidity.resource_id,
              staleReasons: artifact.selectionValidity.stale_reasons ?? []
            }
          });
          continue;
        }
        if (!artifact.selectionValidity.selected && candidateCount > 0) {
          items.push({
            id: stableWorkItemId2("select_candidate", String(artifact.contentUnitId)),
            kind: "select_candidate",
            status: "ready",
            severity: "warning",
            priority: 50,
            reason: `Content unit ${String(artifact.contentUnitId)} has candidates but no selected candidate.`,
            target: contentUnitRef,
            allowed_actors: ["human", "agent", "workflow"],
            recommended_actor: "human",
            actions: [
              {
                type: "open_candidate_picker",
                contentUnitId: artifact.contentUnitId
              },
              {
                type: "agent_review_candidates",
                contentUnitId: artifact.contentUnitId
              }
            ],
            evidence: {
              candidateCount
            }
          });
          continue;
        }
        if (artifact.runtimePanel.status === "ready" && !artifact.selectionValidity.selected) {
          items.push({
            id: stableWorkItemId2("generate_candidates", String(artifact.contentUnitId)),
            kind: "generate_candidates",
            status: "ready",
            severity: "suggestion",
            priority: 60,
            reason: `Content unit ${String(artifact.contentUnitId)} is ready to generate candidates.`,
            target: contentUnitRef,
            allowed_actors: ["agent", "workflow"],
            recommended_actor: "agent",
            actions: [
              {
                type: "derive_content_unit_artifact",
                contentUnitId: artifact.contentUnitId
              },
              {
                type: "generate_candidates",
                contentUnitId: artifact.contentUnitId,
                capability: actionCapability2(artifact.runtimePanel.output_kind),
                suggestedCandidateCount: 2
              }
            ],
            evidence: {
              outputKind: artifact.runtimePanel.output_kind,
              capability: artifact.runtimePanel.runtime_request?.capability
            }
          });
        }
      }
      return items;
    }
    function affectedOutputWorkItems2(impactReport) {
      const items = [];
      const seen = /* @__PURE__ */ new Set();
      for (const change of impactReport.changedEntities) {
        for (const contentUnit of change.affectedContentUnits) {
          const key = `${String(contentUnit.id ?? contentUnit.path)}:${change.entityKind}:${String(change.id ?? change.path)}`;
          if (seen.has(key)) continue;
          seen.add(key);
          items.push({
            id: stableWorkItemId2("review_affected_output", key),
            kind: "review_affected_output",
            status: "informational",
            severity: "suggestion",
            priority: 80,
            reason: `${change.entityKind} change may affect content unit ${String(contentUnit.id ?? contentUnit.path)}.`,
            target: contentUnit,
            upstream: [{
              entityKind: change.entityKind,
              ...change.id !== void 0 ? { id: change.id } : {},
              path: change.path
            }],
            allowed_actors: ["human", "workflow"],
            recommended_actor: "human",
            actions: [{
              type: "open_editor",
              entityKind: "content_unit",
              entityId: contentUnit.id,
              path: contentUnit.path
            }],
            evidence: {
              changedEntity: change,
              staleMarkers: change.staleMarkers
            }
          });
        }
      }
      return items;
    }
    function projectRef2(index) {
      const project = canonicalEntities32(index).find((entity) => entity.entityKind === "project");
      return project ? entityRef32(project) : void 0;
    }
    function contentUnitTarget2(artifact) {
      return {
        entityKind: "content_unit",
        id: artifact.contentUnitId,
        path: artifact.contentUnitPath
      };
    }
    function contentUnitCandidateCount2(index, contentUnitRef) {
      const candidateIds = /* @__PURE__ */ new Set();
      for (const document of index.documents) {
        if (!document.path.startsWith(`${contentUnitRef}/candidates/`)) continue;
        if (!document.path.endsWith("/content_candidate.json")) continue;
        const record = recordField52(document.data);
        const id = idField72(record?.id);
        if (id !== void 0) candidateIds.add(String(id));
      }
      for (const document of index.documents) {
        const context = recordField52(document.data);
        if (context?.schema !== "movscript.decision_context.v1") continue;
        if (context.target_kind !== "content_unit" || context.target_ref !== contentUnitRef) continue;
        const candidates = Array.isArray(context.candidates) ? context.candidates : [];
        for (const candidate of candidates) {
          const id = idField72(recordField52(candidate)?.id);
          if (id !== void 0) candidateIds.add(String(id));
        }
      }
      return candidateIds.size;
    }
    function blockersFor2(blockers) {
      return blockers.map((blocker) => ({
        code: blocker.code,
        message: blocker.message,
        ...stringField142(blocker.ref) ? { ref: stringField142(blocker.ref) } : {}
      }));
    }
    function actionCapability2(outputKind) {
      if (outputKind === "image" || outputKind === "video" || outputKind === "audio" || outputKind === "text") return outputKind;
      return "text";
    }
    function stableWorkItemId2(...parts) {
      return parts.filter((part) => part !== void 0 && String(part).trim() !== "").map((part) => String(part).replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "")).filter(Boolean).join(":");
    }
    function deriveRelationGraph2(index) {
      const relations = [];
      const sourceEntities = canonicalEntities32(index);
      const entities = sourceEntities.filter((entity) => entity.id !== void 0);
      const entityByPathDir = new Map(sourceEntities.map((entity) => [entityDir52(entity.path), entity]));
      const entityByPath = new Map(sourceEntities.map((entity) => [entity.path, entity]));
      const entityById = new Map(entities.map((entity) => [entityKey3(entity.entityKind, entity.id), entity]));
      const normalizedParentRelationKeys = /* @__PURE__ */ new Set();
      const normalizedContentUnitRelationKeys = /* @__PURE__ */ new Set();
      for (const edge of index.domainEdges ?? []) {
        if (edge.relation === "parent" && edge.origin === "path") {
          const parent = edge.target.path ? entityByPath.get(edge.target.path) : void 0;
          const child = edge.source.path ? entityByPath.get(edge.source.path) : void 0;
          if (!parent || !child) continue;
          const relation = {
            type: relationTypeForParent2(parent.entityKind, child.entityKind),
            from: entityRef32(parent),
            to: entityRef32(child)
          };
          normalizedParentRelationKeys.add(parentRelationKey2(parent, child));
          relations.push(relation);
          continue;
        }
        const contentUnitRelation = contentUnitRelationFromDomainEdge2(edge, entities, entityByPath);
        if (!contentUnitRelation) continue;
        if (edge.source.path) normalizedContentUnitRelationKeys.add(contentUnitDomainEdgeKey2(edge.source.path, edge.relation));
        relations.push(contentUnitRelation);
      }
      for (const entity of sourceEntities) {
        const parent = nearestParentEntity2(entity.path, entityByPathDir);
        if (parent && !normalizedParentRelationKeys.has(parentRelationKey2(parent, entity))) {
          relations.push({
            type: relationTypeForParent2(parent.entityKind, entity.entityKind),
            from: entityRef32(parent),
            to: entityRef32(entity)
          });
        }
        if (entity.entityKind === "content_unit" && hasSpecializedContentUnitAdapter2(entity.record.content_unit_type)) {
          for (const edge of normalizeContentUnitTargetEdges2({
            source: domainRefFromContentUnit2(entity),
            record: entity.record,
            scopeTarget(scope) {
              return domainRefFromTimelineScope22(scope.kind, scope.ref, index, entities, entityByPath);
            }
          })) {
            if (normalizedContentUnitRelationKeys.has(contentUnitDomainEdgeKey2(entity.path, edge.relation))) continue;
            const relation = contentUnitRelationFromDomainEdge2(edge, entities, entityByPath);
            if (relation) {
              normalizedContentUnitRelationKeys.add(contentUnitDomainEdgeKey2(entity.path, edge.relation));
              relations.push(relation);
            }
          }
          const targetKind = typeof entity.record.target_kind === "string" ? entity.record.target_kind : void 0;
          const targetRef = entity.record.target_ref;
          if (targetKind && targetRef !== void 0) {
            const target = targetKind === "content_unit" ? entities.find((candidate) => candidate.entityKind === "content_unit" && String(candidate.id ?? "") === String(targetRef)) : findEntityByRef22(entities, targetKind, targetRef) ?? entityByPathDir.get(normalizedRefDir2(targetRef));
            if (target) {
              relations.push({
                type: targetKind === "content_unit" ? "references" : "uses",
                from: entityRef32(entity),
                to: entityRef32(target),
                field: "target_ref"
              });
            }
          }
          const contentUnitType22 = String(entity.record.content_unit_type ?? "");
          const primaryKind = primaryRefKindForContentUnitType22(contentUnitType22);
          if (primaryKind) {
            for (const ref of primaryContentUnitRefs3(entity, primaryKind)) {
              const target = ref.kind === "content_unit" ? entities.find((candidate) => candidate.entityKind === "content_unit" && String(candidate.id ?? "") === ref.id) : findEntityByRef22(entities, ref.kind, ref.id) ?? entityByPathDir.get(normalizedRefDir2(ref.id));
              if (target) {
                relations.push({
                  type: ref.kind === "content_unit" ? "references" : "uses",
                  from: entityRef32(entity),
                  to: entityRef32(target),
                  field: primaryRefFieldNameForKind22(primaryKind)
                });
              }
            }
          }
          for (const ref of parseContentUnitEditPromptRefs22(entity.record.edit_prompt)) {
            const target = ref.kind === "content_unit" ? entities.find((candidate) => candidate.entityKind === "content_unit" && String(candidate.id ?? "") === ref.id) : findEntityByRef22(entities, ref.kind, ref.id) ?? entityByPathDir.get(normalizedRefDir2(ref.id));
            if (target) {
              relations.push({
                type: ref.kind === "content_unit" ? "references" : "uses",
                from: entityRef32(entity),
                to: entityRef32(target),
                field: ref.source.field
              });
            }
          }
        }
        if (entity.entityKind === "expression_unit") {
          const sourceExpression = findEntityByRef22(entities, "expression_unit", entity.record.source_expression_ref) ?? entityByPathDir.get(normalizedRefDir2(entity.record.source_expression_ref));
          const speaker = findEntityByRef22(entities, "setting", entity.record.speaker_ref) ?? entityByPathDir.get(normalizedRefDir2(entity.record.speaker_ref));
          if (sourceExpression) relations.push({ type: "references", from: entityRef32(entity), to: entityRef32(sourceExpression), field: "source_expression_ref" });
          if (speaker) relations.push({ type: "uses", from: entityRef32(entity), to: entityRef32(speaker), field: "speaker_ref" });
          for (const expressionRef of arrayField52(recordField52(entity.record.span)?.expression_refs)) {
            const expression = findEntityByRef22(entities, "expression_unit", expressionRef) ?? entityByPathDir.get(normalizedRefDir2(expressionRef));
            if (expression) relations.push({ type: "references", from: entityRef32(entity), to: entityRef32(expression), field: "span.expression_refs" });
          }
        }
        if (entity.entityKind === "audio_cue") {
          const scope = entityByPathDir.get(normalizedRefDir2(entity.record.scope_ref));
          const storyboard = entityByPathDir.get(normalizedRefDir2(entity.record.storyboard_ref));
          if (scope) relations.push({ type: "references", from: entityRef32(entity), to: entityRef32(scope), field: "scope_ref" });
          if (storyboard) relations.push({ type: "references", from: entityRef32(entity), to: entityRef32(storyboard), field: "storyboard_ref" });
          for (const assetRef32 of arrayField52(entity.record.asset_refs)) {
            const asset = findEntityByRef22(entities, "asset", assetRef32) ?? entityByPathDir.get(normalizedRefDir2(assetRef32));
            if (asset) relations.push({ type: "uses", from: entityRef32(entity), to: entityRef32(asset), field: "asset_refs" });
          }
        }
        if (entity.entityKind === "storyboard") {
          const expressionUnit = parentExpressionUnitForEntity2(entity, entityByPathDir, entityById);
          if (expressionUnit) relations.push({ type: "references", from: entityRef32(entity), to: entityRef32(expressionUnit), field: "expression_unit_ref" });
          for (const settingRef of arrayField52(entity.record.setting_refs).filter(isRecord212)) {
            const setting = findEntityByRef22(entities, "setting", settingRef.setting_id);
            const settingState = findEntityByRef22(entities, "setting_state", settingRef.setting_state_id);
            if (setting) relations.push({ type: "uses", from: entityRef32(entity), to: entityRef32(setting), field: "setting_refs.setting_id" });
            if (settingState) relations.push({ type: "uses", from: entityRef32(entity), to: entityRef32(settingState), field: "setting_refs.setting_state_id" });
          }
        }
        if (entity.entityKind === "keyframe") {
          const expressionUnit = parentExpressionUnitForEntity2(entity, entityByPathDir, entityById);
          if (expressionUnit) relations.push({ type: "references", from: entityRef32(entity), to: entityRef32(expressionUnit), field: "expression_unit_ref" });
          for (const assetRef32 of arrayField52(entity.record.reference_asset_refs)) {
            const asset = findEntityByRef22(entities, "asset", assetRef32) ?? entityByPathDir.get(normalizedRefDir2(assetRef32));
            if (asset) relations.push({ type: "uses", from: entityRef32(entity), to: entityRef32(asset), field: "reference_asset_refs" });
          }
        }
      }
      return { schema: "movscript.relation-graph.v1", relations: dedupeRelations2(relations) };
    }
    function parentRelationKey2(parent, child) {
      return `${parent.entityKind}:${parent.path}->${child.entityKind}:${child.path}`;
    }
    function contentUnitRelationFromDomainEdge2(edge, entities, entityByPath) {
      if (edge.relation !== "target" && edge.relation !== "scope") return void 0;
      const source = sourceContentUnitFromDomainEdge2(edge, entities, entityByPath);
      if (!source) return void 0;
      return {
        type: edge.relation === "target" ? "targets" : "uses",
        from: entityRef32(source),
        to: entityRefFromDomainEdgeTarget2(edge, entities, entityByPath),
        ...edge.field ? { field: edge.field } : {}
      };
    }
    function sourceContentUnitFromDomainEdge2(edge, entities, entityByPath) {
      const source = edge.source.path ? entityByPath.get(edge.source.path) : findEntityByRef22(entities, "content_unit", edge.source.id);
      return source?.entityKind === "content_unit" ? source : void 0;
    }
    function entityRefFromDomainEdgeTarget2(edge, entities, entityByPath) {
      const target = edge.target.path ? entityByPath.get(edge.target.path) : edge.target.kind && edge.target.id !== void 0 ? findEntityByRef22(entities, edge.target.kind, edge.target.id) : void 0;
      if (target) return entityRef32(target);
      return {
        entityKind: edge.target.kind,
        ...edge.target.id !== void 0 ? { id: edge.target.id } : {},
        ...edge.target.path ? { path: edge.target.path } : {}
      };
    }
    function contentUnitDomainEdgeKey2(path, relation) {
      return `${relation}:${path}`;
    }
    function domainRefFromContentUnit2(entity) {
      return {
        category: "content_unit",
        kind: "content_unit",
        ...entity.id !== void 0 ? { id: entity.id } : {},
        path: entity.path
      };
    }
    function domainRefFromTimelineScope22(scopeKind, scopeRef, index, entities, entityByPath) {
      const entity = findEntityByRef22(entities, scopeKind, scopeRef);
      if (entity) {
        return {
          category: "timeline_namespace",
          kind: entity.entityKind,
          ...entity.id !== void 0 ? { id: entity.id } : {},
          path: entity.path
        };
      }
      const node = index.domainNodes.find(
        (candidate) => candidate.category === "timeline_namespace" && candidate.kind === scopeKind && String(candidate.id ?? "") === scopeRef
      );
      if (node) {
        return {
          category: node.category,
          kind: node.kind,
          ...node.id !== void 0 ? { id: node.id } : {},
          ...node.path ? { path: node.path } : {}
        };
      }
      const pathEntity = entityByPath.get(normalizedRefDir2(scopeRef));
      if (pathEntity) {
        return {
          category: "timeline_namespace",
          kind: pathEntity.entityKind,
          ...pathEntity.id !== void 0 ? { id: pathEntity.id } : {},
          path: pathEntity.path
        };
      }
      return { category: "timeline_namespace", kind: scopeKind, id: scopeRef };
    }
    function deriveMovScriptWorkspaceArtifacts2(input3) {
      const relationGraph = deriveRelationGraph2(input3.index);
      const impactReport = deriveImpactReport2(input3.changedEntities, input3.interpretationId, input3.createdAt, input3.index, relationGraph, input3.semanticChanges);
      const contentUnitArtifacts = deriveContentUnitArtifacts2(input3.index, { createdAt: input3.createdAt });
      const editPlans = deriveEditPlans2(input3.index, contentUnitArtifacts);
      return {
        domainTree: deriveDomainTree2(input3.index),
        relationGraph,
        assetIndex: deriveAssetIndex2(input3.index),
        impactReport,
        previewTimelines: deriveMovScriptWorkspacePreviewTimelines3(input3.index),
        editPlans,
        contentUnitArtifacts,
        productionWorkPlan: deriveProductionWorkPlan2({
          index: input3.index,
          contentUnitArtifacts,
          impactReport,
          sourceIssues: input3.sourceIssues,
          changedEntities: input3.changedEntities,
          interpretationId: input3.interpretationId,
          createdAt: input3.createdAt
        })
      };
    }
    var import_node_crypto22 = __require("crypto");
    var MOVSCRIPT_CHECKPOINT_DIR2 = ".movscript/checkpoints";
    var MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR2 = `${MOVSCRIPT_CHECKPOINT_DIR2}/current/source`;
    var MOVSCRIPT_CHECKPOINT_CURRENT_MANIFEST_PATH2 = `${MOVSCRIPT_CHECKPOINT_DIR2}/current/manifest.json`;
    async function resolveWorkspaceSource2(fileRepository, options = {}) {
      const sourceFiles = filterWorkspaceSourceFiles2(
        await loadWorkspaceSourceFileSnapshots2(fileRepository),
        options
      );
      return { rootPath: "", mode: "source", files: sourceFiles };
    }
    async function loadWorkspaceFileSnapshots2(fileRepository, rootPath) {
      const files = [];
      await collectWorkspaceFileSnapshots2(fileRepository, rootPath, rootPath, files);
      return files.sort((left, right) => left.path.localeCompare(right.path));
    }
    async function loadCheckpointSourceSnapshots2(fileRepository, checkpointHash, options = {}) {
      const rootDir = getNodeMovScriptWorkspaceFileRepositoryRoot2(fileRepository);
      if (rootDir && (await inspectNodeMovScriptGitWorkspace2(rootDir)).insideWorkTree) {
        const gitRef = checkpointHash ?? await currentNodeMovScriptGitHead2(rootDir);
        if (gitRef) {
          return {
            basePath: gitRef,
            checkpointHash: gitRef,
            source: "git",
            files: filterWorkspaceSourceFiles2((await readNodeMovScriptGitSourceFiles2(rootDir, gitRef)).map((file) => ({
              path: file.path,
              relativePath: file.path,
              content: file.content,
              hash: contentHash2(file.content)
            })), options)
          };
        }
      }
      const snapshotFiles = await loadWorkspaceFileSnapshots2(fileRepository, MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR2);
      const normalizedSnapshotFiles = normalizeCheckpointSnapshotFiles2(snapshotFiles);
      if (normalizedSnapshotFiles.length > 0) {
        const manifest = await readJsonFile2(fileRepository, MOVSCRIPT_CHECKPOINT_CURRENT_MANIFEST_PATH2);
        return {
          basePath: MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR2,
          ...manifest?.checkpointHash ? { checkpointHash: manifest.checkpointHash } : {},
          source: "snapshot",
          files: filterWorkspaceSourceFiles2(
            normalizedSnapshotFiles.filter((file) => isMovScriptSourceRelativePath2(file.relativePath)),
            options
          )
        };
      }
      return {
        basePath: "empty",
        source: "empty",
        files: []
      };
    }
    async function writeDebugArtifacts2(fileRepository, artifacts, index, manifest, impactReportPath) {
      const files = /* @__PURE__ */ new Map();
      for (const document of index.documents) {
        files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${normalizeWorkspacePath8(document.path)}`, serializeWorkspaceDocument2(document.data));
      }
      files.set(MOVSCRIPT_DOMAIN_INDEX_PATH2, serializeWorkspaceDocument2(index));
      files.set(MOVSCRIPT_DOMAIN_TREE_PATH2, serializeWorkspaceDocument2(artifacts.domainTree));
      files.set(MOVSCRIPT_ASSET_INDEX_PATH2, serializeWorkspaceDocument2(artifacts.assetIndex));
      files.set(MOVSCRIPT_RELATION_GRAPH_PATH2, serializeWorkspaceDocument2(artifacts.relationGraph));
      files.set(MOVSCRIPT_EDITOR_STATE_PATH2, serializeWorkspaceDocument2({
        schema: "movscript.editor-state.v1",
        interpretation_id: manifest.interpretationId,
        interpreted_at: manifest.interpretedAt,
        source_status: {
          ready_to_interpret: manifest.review.readyToInterpret ?? true,
          issue_count: manifest.review.issues?.length ?? 0
        },
        summary: manifest.review.summary,
        contentUnitRuntimePanels: artifacts.contentUnitArtifacts.map(editorStateRuntimePanel2),
        content_unit_runtime_panels: artifacts.contentUnitArtifacts.map(editorStateRuntimePanel2)
      }));
      for (const previewTimeline of artifacts.previewTimelines) {
        files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${normalizeWorkspacePath8(previewTimeline.productionPath)}/preview_timeline.json`, serializeWorkspaceDocument2(previewTimeline));
      }
      for (const editPlan of artifacts.editPlans) {
        files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${normalizeWorkspacePath8(editPlan.sceneMomentPath)}/edit_plan.json`, serializeWorkspaceDocument2(editPlan));
      }
      for (const bundle of artifacts.contentUnitArtifacts) {
        const contentUnitDir = normalizeWorkspacePath8(bundle.contentUnitPath).replace(/\/content_unit\.json$/, "");
        files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${contentUnitDir}/runtime_panel.json`, serializeWorkspaceDocument2(bundle.runtimePanel));
        files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${contentUnitDir}/generation_prompt.json`, serializeWorkspaceDocument2(bundle.generationPrompt));
        files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${contentUnitDir}/dependency_report.json`, serializeWorkspaceDocument2(bundle.dependencyReport));
        files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR2}/${contentUnitDir}/selection_validity.json`, serializeWorkspaceDocument2(bundle.selectionValidity));
      }
      for (const path of await listRepositoryFiles2(fileRepository, MOVSCRIPT_INTERPRET_CURRENT_DIR2)) {
        if (!files.has(path)) await fileRepository.delete({ path });
      }
      for (const [path, content] of files) {
        await fileRepository.write({ path, content });
      }
      await fileRepository.write({ path: impactReportPath, content: serializeWorkspaceDocument2(artifacts.impactReport) });
      await fileRepository.write({
        path: `${MOVSCRIPT_INTERPRET_MANIFESTS_DIR2}/${manifest.interpretationId}.json`,
        content: serializeWorkspaceDocument2(manifest)
      });
    }
    function workspaceSnapshotId2(files) {
      return files.length === 0 ? "empty-working-tree" : sha2562(files.map((file) => `${file.relativePath}:${file.hash}`).join("\n"));
    }
    function normalizeCheckpointSnapshotFiles2(files) {
      return files.map((file) => {
        const normalizedPath = normalizeWorkspacePath8(file.path);
        const relativePath = normalizedPath.replace(new RegExp(`^${escapeRegExp4(MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR2)}/`), "").replace(/^checkpoints\/current\/source\//, "");
        return {
          ...file,
          relativePath
        };
      });
    }
    function filterWorkspaceSourceFiles2(files, options) {
      if (options.includeContentUnitDecisionDocuments !== false) return files;
      return files.filter((file) => !isMovScriptContentUnitDecisionPath2(file.relativePath));
    }
    async function loadWorkspaceSourceFileSnapshots2(fileRepository) {
      const files = [];
      await collectWorkspaceFileSnapshots2(fileRepository, "", "", files);
      return files.filter((file) => isMovScriptSourceRelativePath2(file.relativePath)).sort((left, right) => left.path.localeCompare(right.path));
    }
    async function listRepositoryFiles2(fileRepository, rootPath) {
      const files = [];
      await collectRepositoryFiles2(fileRepository, normalizeWorkspacePath8(rootPath), files);
      return files.sort((left, right) => left.localeCompare(right));
    }
    async function collectRepositoryFiles2(fileRepository, path, out) {
      let listed;
      try {
        listed = await fileRepository.list({ path });
      } catch {
        return;
      }
      for (const entry of listed.entries) {
        if (entry.kind === "directory") {
          await collectRepositoryFiles2(fileRepository, entry.path, out);
          continue;
        }
        out.push(normalizeWorkspacePath8(entry.path));
      }
    }
    async function collectWorkspaceFileSnapshots2(fileRepository, rootPath, path, out) {
      let listed;
      try {
        listed = await fileRepository.list({ path });
      } catch {
        return;
      }
      for (const entry of listed.entries) {
        if (entry.kind === "directory") {
          if (rootPath === "" && isMovScriptNonSourceRootDirectory2(entry.path)) continue;
          await collectWorkspaceFileSnapshots2(fileRepository, rootPath, entry.path, out);
          continue;
        }
        if (!isMovScriptSourceDocumentPath2(entry.path)) continue;
        const file = await fileRepository.read({ path: entry.path });
        const normalizedPath = normalizeWorkspacePath8(file.path);
        out.push({
          path: normalizedPath,
          relativePath: relativeWorkspacePath2(rootPath, normalizedPath),
          content: file.content,
          hash: contentHash2(file.content)
        });
      }
    }
    async function readJsonFile2(fileRepository, path) {
      try {
        const file = await fileRepository.read({ path });
        return JSON.parse(file.content);
      } catch {
        return void 0;
      }
    }
    function serializeWorkspaceDocument2(value) {
      if (typeof value === "string") return value;
      return `${JSON.stringify(jsonSerializable2(value), null, 2)}
`;
    }
    function editorStateRuntimePanel2(bundle) {
      return {
        ...bundle.runtimePanel,
        contentUnitId: bundle.contentUnitId,
        contentUnitPath: bundle.contentUnitPath,
        content_unit_id: bundle.runtimePanel.content_unit_id ?? bundle.contentUnitId,
        content_unit_path: bundle.contentUnitPath
      };
    }
    function jsonSerializable2(value) {
      if (value instanceof Map) {
        return Object.fromEntries(Array.from(value.entries()).map(([key, item]) => [String(key), jsonSerializable2(item)]));
      }
      if (Array.isArray(value)) return value.map(jsonSerializable2);
      if (value && typeof value === "object") {
        return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, jsonSerializable2(item)]));
      }
      return value;
    }
    function relativeWorkspacePath2(rootPath, path) {
      const root = normalizeWorkspacePath8(rootPath);
      const normalized = normalizeWorkspacePath8(path);
      if (!root) return normalized;
      return normalized === root ? "" : normalized.replace(new RegExp(`^${escapeRegExp4(root)}/?`), "");
    }
    function isMovScriptSourceRelativePath2(path) {
      return isMovScriptSourcePath2(path);
    }
    function contentHash2(content) {
      return (0, import_node_crypto22.createHash)("sha256").update(content).digest("hex");
    }
    function sha2562(value) {
      return (0, import_node_crypto22.createHash)("sha256").update(value).digest("hex");
    }
    function escapeRegExp4(value) {
      return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    }
    async function loadLatestInterpretManifest2(fileRepository) {
      const files = await loadWorkspaceFileSnapshots2(fileRepository, MOVSCRIPT_INTERPRET_MANIFESTS_DIR2);
      const manifests = (await Promise.all(files.filter((file) => file.path.endsWith(".json")).map(async (file) => {
        const manifest = parseWorkspaceDocument22(file.path, file.content);
        if (!isInterpretManifest2(manifest)) return void 0;
        return { path: file.path, manifest };
      }))).filter((item) => item !== void 0);
      return manifests.sort((left, right) => {
        return right.manifest.interpretedAt.localeCompare(left.manifest.interpretedAt) || right.manifest.interpretationId.localeCompare(left.manifest.interpretationId);
      })[0];
    }
    async function loadLatestImpactReport2(fileRepository, latestInterpretation) {
      const path = latestInterpretation?.manifest.output.impactReportPath;
      if (!path) return void 0;
      try {
        const file = await fileRepository.read({ path });
        const report = parseWorkspaceDocument22(file.path, file.content);
        return isImpactReportArtifact2(report) ? report : void 0;
      } catch {
        return void 0;
      }
    }
    function deriveV1RegenerationPlan2(input3) {
      const impactReportTargets = input3.impactReport ? targetsFromImpactReport2(input3.impactReport, input3.review.staleSelections) : [];
      const legacyImpactTargets = input3.impactReport ? [] : input3.review.productionImpacts.filter((impact) => impact.contentUnit).map((impact) => targetFromProductionImpact2(impact, input3.review.staleSelections));
      const affectedContentUnits = dedupeTargets2([
        ...impactReportTargets,
        ...legacyImpactTargets,
        ...input3.review.staleSelections.map(targetFromStaleSelection2).filter((target) => target !== void 0)
      ]);
      const promptBundles = affectedContentUnits.filter((target) => {
        return target.stale === true || target.reasons.some((reason) => reason.includes("reference") || reason.includes("selection") || reason.includes("content_unit"));
      });
      const previewTimelines = input3.impactReport ? previewTimelinesFromImpactReport2(input3.impactReport) : previewTimelinesFromProductionImpacts2(input3.review.productionImpacts);
      const changedEntities = input3.impactReport?.changedEntities ?? input3.review.productionImpacts.map((impact) => ({
        entityKind: impact.sourceChanges[0]?.entity.kind ?? "unknown",
        id: impact.sourceChanges[0]?.entity.id,
        path: impact.sourceChanges[0]?.sourceChange.path ?? "",
        state: impact.sourceChanges[0]?.sourceChange.operation ?? "modified",
        businessImpacts: [...new Set(impact.sourceChanges.map((change) => change.businessKind).filter((kind) => typeof kind === "string"))].sort(),
        editorImpacts: [impact.kind],
        affectedContentUnits: impact.contentUnit ? [{
          entityKind: "content_unit",
          id: impact.contentUnit.id,
          path: impact.contentUnit.path
        }] : [],
        staleMarkers: []
      }));
      return {
        schema: "movscript.workspace-regeneration-plan.v1",
        operation: "regen-plan",
        createdAt: input3.createdAt,
        status: "ready",
        ...input3.latestInterpretation ? {
          interpret: {
            interpretationId: input3.latestInterpretation.manifest.interpretationId,
            interpretedAt: input3.latestInterpretation.manifest.interpretedAt,
            manifestPath: input3.latestInterpretation.path,
            impactReportPath: input3.latestInterpretation.manifest.output.impactReportPath
          }
        } : {},
        changedEntities,
        affectedContentUnits,
        promptBundles,
        previewTimelines,
        summary: {
          changedEntities: changedEntities.length,
          affectedContentUnits: affectedContentUnits.length,
          staleContentUnits: input3.review.staleSelections.length,
          promptBundles: promptBundles.length,
          previewTimelines: previewTimelines.length
        }
      };
    }
    function targetsFromImpactReport2(impactReport, staleSelections) {
      return impactReport.changedEntities.flatMap((change) => {
        return change.affectedContentUnits.map((contentUnit) => targetFromImpactReportChange2(change, contentUnit, staleSelections));
      });
    }
    function targetFromImpactReportChange2(change, contentUnit, staleSelections) {
      const staleSelection = staleSelections.map(selectionRecord2).find((selection) => sameOptionalId3(selection?.contentUnitId, contentUnit.id) || sameOptionalPath3(selection?.contentUnitPath, contentUnit.path));
      return pruneUndefined162({
        contentUnitId: contentUnit.id,
        contentUnitPath: contentUnit.path,
        reasons: [
          ...change.businessImpacts,
          ...change.editorImpacts,
          ...change.staleMarkers
        ].filter(isString22),
        selected: staleSelection?.selected,
        stale: staleSelection?.stale,
        candidateId: staleSelection?.candidateId,
        resourceId: staleSelection?.resourceId,
        staleReasons: staleSelection?.staleReasons
      });
    }
    function targetFromProductionImpact2(impact, staleSelections) {
      const staleSelection = staleSelections.map(selectionRecord2).find((selection) => sameOptionalId3(selection?.contentUnitId, impact.contentUnit?.id) || sameOptionalPath3(selection?.contentUnitPath, impact.contentUnit?.path));
      return pruneUndefined162({
        contentUnitId: impact.contentUnit?.id,
        contentUnitPath: impact.contentUnit?.path,
        reasons: [
          impact.kind,
          ...impact.sourceChanges.map((change) => change.businessKind)
        ].filter(isString22),
        selected: staleSelection?.selected,
        stale: staleSelection?.stale,
        candidateId: staleSelection?.candidateId,
        resourceId: staleSelection?.resourceId,
        staleReasons: staleSelection?.staleReasons
      });
    }
    function targetFromStaleSelection2(selection) {
      const record = selectionRecord2(selection);
      if (!record) return void 0;
      return pruneUndefined162({
        contentUnitId: record.contentUnitId,
        contentUnitPath: record.contentUnitPath,
        reasons: ["selection_stale", ...record.staleReasons ?? []],
        selected: record.selected,
        stale: record.stale,
        candidateId: record.candidateId,
        resourceId: record.resourceId,
        staleReasons: record.staleReasons
      });
    }
    function previewTimelinesFromProductionImpacts2(impacts) {
      const timelineEntityKinds = /* @__PURE__ */ new Set(["production", "segment", "scene_moment", "shot", "storyboard", "keyframe", "audio_cue", "expression_unit", "content_unit"]);
      const seen = /* @__PURE__ */ new Set();
      const out = [];
      for (const impact of impacts) {
        for (const change of impact.sourceChanges) {
          if (!timelineEntityKinds.has(change.entity.kind)) continue;
          const path = stringField152(change.sourceChange.path);
          if (!path) continue;
          const productionId = productionIdFromPath2(path);
          const key = `${productionId ?? ""}:${path}`;
          if (seen.has(key)) continue;
          seen.add(key);
          out.push(pruneUndefined162({
            productionId,
            path,
            reasons: [impact.kind, change.businessKind].filter(isString22)
          }));
        }
      }
      return out;
    }
    function previewTimelinesFromImpactReport2(impactReport) {
      const timelineEntityKinds = /* @__PURE__ */ new Set(["production", "segment", "scene_moment", "shot", "storyboard", "keyframe", "audio_cue", "expression_unit", "content_unit"]);
      const seen = /* @__PURE__ */ new Set();
      const out = [];
      for (const change of impactReport.changedEntities) {
        if (!timelineEntityKinds.has(change.entityKind)) continue;
        const path = stringField152(change.path);
        if (!path) continue;
        const productionId = productionIdFromPath2(path);
        const key = `${productionId ?? ""}:${path}`;
        if (seen.has(key)) continue;
        seen.add(key);
        out.push(pruneUndefined162({
          productionId,
          path,
          reasons: [...change.businessImpacts, ...change.editorImpacts].filter(isString22)
        }));
      }
      return out;
    }
    function dedupeTargets2(targets) {
      const merged = /* @__PURE__ */ new Map();
      for (const target of targets) {
        const key = String(target.contentUnitId ?? target.contentUnitPath ?? "");
        if (!key) continue;
        const existing = merged.get(key);
        if (!existing) {
          merged.set(key, {
            ...target,
            reasons: [...new Set(target.reasons)].sort()
          });
          continue;
        }
        merged.set(key, pruneUndefined162({
          ...existing,
          ...target,
          reasons: [.../* @__PURE__ */ new Set([...existing.reasons, ...target.reasons])].sort(),
          staleReasons: [.../* @__PURE__ */ new Set([...existing.staleReasons ?? [], ...target.staleReasons ?? []])].sort()
        }));
      }
      return [...merged.values()].sort((left, right) => {
        return String(left.contentUnitId ?? left.contentUnitPath ?? "").localeCompare(String(right.contentUnitId ?? right.contentUnitPath ?? ""));
      });
    }
    function selectionRecord2(value) {
      if (!isRecord222(value)) return void 0;
      return pruneUndefined162({
        contentUnitId: idField82(value.contentUnitId),
        contentUnitPath: stringField152(value.contentUnitPath),
        selected: typeof value.selected === "boolean" ? value.selected : void 0,
        stale: typeof value.stale === "boolean" ? value.stale : void 0,
        candidateId: idField82(value.candidateId),
        resourceId: resourceIdField42(value.resourceId),
        staleReasons: Array.isArray(value.staleReasons) ? value.staleReasons.filter(isString22) : void 0
      });
    }
    function productionIdFromPath2(path) {
      const parts = normalizeWorkspacePath62(path).split("/");
      return parts[0] === "productions" ? parts[1] : void 0;
    }
    function sameOptionalId3(left, right) {
      return left !== void 0 && right !== void 0 && String(left) === String(right);
    }
    function sameOptionalPath3(left, right) {
      return typeof left === "string" && typeof right === "string" && normalizeWorkspacePath62(left).replace(/\/[^/]+$/, "") === normalizeWorkspacePath62(right).replace(/\/[^/]+$/, "");
    }
    function idField82(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function resourceIdField42(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function stringField152(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function isString22(value) {
      return typeof value === "string" && value.trim() !== "";
    }
    function normalizeWorkspacePath62(path) {
      return path.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+/g, "/").replace(/\/$/, "");
    }
    function pruneUndefined162(value) {
      const out = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) out[key] = item;
      }
      return out;
    }
    function parseWorkspaceDocument22(path, content) {
      if (!path.endsWith(".json")) return content;
      try {
        return JSON.parse(content);
      } catch {
        return void 0;
      }
    }
    function isInterpretManifest2(value) {
      return isRecord222(value) && value.schema === "movscript.workspace-interpret.v1" && typeof value.interpretationId === "string" && typeof value.interpretedAt === "string" && isRecord222(value.output);
    }
    function isImpactReportArtifact2(value) {
      return isRecord222(value) && value.schema === "movscript.impact-report.v1" && Array.isArray(value.changedEntities);
    }
    function isRecord222(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function diffMovScriptJsonValues2(before, after) {
      return diffJsonValueAtPointer2(before, after, "");
    }
    function jsonPointerToFieldPath2(pointer) {
      if (pointer === "") return "*";
      return pointer.split("/").slice(1).map(unescapeJsonPointerSegment2).join(".");
    }
    function diffJsonValueAtPointer2(before, after, pointer) {
      if (jsonValueEquals3(before, after)) return [];
      if (Array.isArray(before) && Array.isArray(after)) return diffJsonArrayAtPointer2(before, after, pointer);
      if (isRecord232(before) && isRecord232(after)) return diffJsonObjectAtPointer2(before, after, pointer);
      if (before === void 0) return [{ path: pointer, operation: "added", newValue: after }];
      if (after === void 0) return [{ path: pointer, operation: "removed", oldValue: before }];
      return [{ path: pointer, operation: "replaced", oldValue: before, newValue: after }];
    }
    function diffJsonObjectAtPointer2(before, after, pointer) {
      const keys = [.../* @__PURE__ */ new Set([...Object.keys(before), ...Object.keys(after)])].sort();
      return keys.flatMap((key) => {
        const childPointer = appendJsonPointerSegment2(pointer, key);
        if (!(key in before)) return [{ path: childPointer, operation: "added", newValue: after[key] }];
        if (!(key in after)) return [{ path: childPointer, operation: "removed", oldValue: before[key] }];
        return diffJsonValueAtPointer2(before[key], after[key], childPointer);
      });
    }
    function diffJsonArrayAtPointer2(before, after, pointer) {
      const beforeKeys = before.map(stableArrayItemKey2);
      const afterKeys = after.map(stableArrayItemKey2);
      const canTrackItems = beforeKeys.every((key) => key !== void 0) && afterKeys.every((key) => key !== void 0) && new Set(beforeKeys).size === beforeKeys.length && new Set(afterKeys).size === afterKeys.length;
      if (canTrackItems) {
        return diffTrackableJsonArrayAtPointer2(before, after, beforeKeys, afterKeys, pointer);
      }
      const length = Math.max(before.length, after.length);
      const changes = [];
      for (let index = 0; index < length; index += 1) {
        const childPointer = appendJsonPointerSegment2(pointer, String(index));
        if (index >= before.length) {
          changes.push({ path: childPointer, operation: "added", newValue: after[index] });
        } else if (index >= after.length) {
          changes.push({ path: childPointer, operation: "removed", oldValue: before[index] });
        } else {
          changes.push(...diffJsonValueAtPointer2(before[index], after[index], childPointer));
        }
      }
      return changes;
    }
    function diffTrackableJsonArrayAtPointer2(before, after, beforeKeys, afterKeys, pointer) {
      const changes = [];
      const beforeIndexByKey = new Map(beforeKeys.map((key, index) => [key, index]));
      const afterIndexByKey = new Map(afterKeys.map((key, index) => [key, index]));
      const sharedKeys = beforeKeys.filter((key) => afterIndexByKey.has(key));
      const sameMembers = beforeKeys.length === afterKeys.length && sharedKeys.length === beforeKeys.length;
      const orderChanged = sameMembers && beforeKeys.some((key, index) => key !== afterKeys[index]);
      if (orderChanged) {
        changes.push({
          path: pointer,
          operation: "reordered",
          oldValue: beforeKeys,
          newValue: afterKeys
        });
      }
      for (const key of beforeKeys) {
        if (afterIndexByKey.has(key)) continue;
        const oldIndex = beforeIndexByKey.get(key);
        if (oldIndex === void 0) continue;
        changes.push({
          path: appendJsonPointerSegment2(pointer, String(oldIndex)),
          operation: "removed",
          itemKey: key,
          oldIndex,
          oldValue: before[oldIndex]
        });
      }
      for (const key of afterKeys) {
        if (beforeIndexByKey.has(key)) continue;
        const newIndex = afterIndexByKey.get(key);
        if (newIndex === void 0) continue;
        changes.push({
          path: appendJsonPointerSegment2(pointer, String(newIndex)),
          operation: "added",
          itemKey: key,
          newIndex,
          newValue: after[newIndex]
        });
      }
      for (const key of sharedKeys) {
        const oldIndex = beforeIndexByKey.get(key);
        const newIndex = afterIndexByKey.get(key);
        if (oldIndex === void 0 || newIndex === void 0) continue;
        if (oldIndex !== newIndex) {
          changes.push({
            path: appendJsonPointerSegment2(pointer, String(newIndex)),
            operation: "moved",
            itemKey: key,
            oldIndex,
            newIndex
          });
        }
        changes.push(...diffJsonValueAtPointer2(
          before[oldIndex],
          after[newIndex],
          appendJsonPointerSegment2(pointer, String(newIndex))
        ));
      }
      return changes.sort(compareJsonChanges2);
    }
    function stableArrayItemKey2(value) {
      if (isRecord232(value)) {
        const id = value.id ?? value.ID ?? value.client_id ?? value.key ?? value.name;
        if (typeof id === "string" && id.trim()) return id.trim();
        if (typeof id === "number" && Number.isFinite(id)) return String(id);
      }
      if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
        return `${typeof value}:${String(value)}`;
      }
      if (value === null) return "null:null";
      return void 0;
    }
    function compareJsonChanges2(left, right) {
      return left.path.localeCompare(right.path) || left.operation.localeCompare(right.operation) || String(left.itemKey ?? "").localeCompare(String(right.itemKey ?? ""));
    }
    function appendJsonPointerSegment2(pointer, segment) {
      return `${pointer}/${escapeJsonPointerSegment2(segment)}`;
    }
    function escapeJsonPointerSegment2(segment) {
      return segment.replace(/~/g, "~0").replace(/\//g, "~1");
    }
    function unescapeJsonPointerSegment2(segment) {
      return segment.replace(/~1/g, "/").replace(/~0/g, "~");
    }
    function jsonValueEquals3(left, right) {
      return JSON.stringify(left) === JSON.stringify(right);
    }
    function isRecord232(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function jsonFileChangesFromFiles2(changedFiles, workingFiles, baselineFiles) {
      const workingByPath = new Map(workingFiles.map((file) => [file.path, file]));
      const baselineByRelativePath = new Map(baselineFiles.map((file) => [file.relativePath, file]));
      return changedFiles.flatMap((file) => {
        if (!file.path.endsWith(".json")) return [];
        const workingFile = workingByPath.get(file.path);
        const baselineFile = baselineByRelativePath.get(file.path);
        const before = baselineFile ? parseJsonFile2(baselineFile) : void 0;
        const after = workingFile ? parseJsonFile2(workingFile) : void 0;
        return [{
          path: workingFile?.path ?? file.path,
          state: file.state,
          ...before !== void 0 ? { oldValue: before } : {},
          ...after !== void 0 ? { newValue: after } : {},
          fieldChanges: fieldChangesForJsonFileChange2(file.state, before, after)
        }];
      });
    }
    function fieldChangesForJsonFileChange2(state, before, after) {
      if (state === "added") {
        return [{ field: "*", operation: "added", newValue: after }];
      }
      if (state === "deleted") {
        return [{ field: "*", operation: "deleted", oldValue: before }];
      }
      return diffMovScriptJsonValues2(before, after).map(jsonChangeToFieldChange2);
    }
    function jsonChangeToFieldChange2(change) {
      return {
        field: jsonPointerToFieldPath2(change.path),
        operation: workspaceOperationForJsonChange2(change),
        ...change.oldValue !== void 0 ? { oldValue: change.oldValue } : {},
        ...change.newValue !== void 0 ? { newValue: change.newValue } : {},
        jsonPointer: change.path || "/",
        jsonOperation: change.operation,
        ...change.oldIndex !== void 0 ? { oldIndex: change.oldIndex } : {},
        ...change.newIndex !== void 0 ? { newIndex: change.newIndex } : {}
      };
    }
    function workspaceOperationForJsonChange2(change) {
      if (change.operation === "added") return "added";
      if (change.operation === "removed") return "deleted";
      return "modified";
    }
    function parseJsonFile2(file) {
      try {
        return JSON.parse(file.content);
      } catch {
        return void 0;
      }
    }
    function buildSourceDomainGraph2(files) {
      const records = files.map((file) => {
        const data = parseWorkspaceDocument32(file.path, file.content);
        const entityKind = sourceEntityKindFromRelativePath2(file.relativePath);
        const id = isRecord242(data) && entityKind ? sourceEntityStableId2(data, entityKind) : void 0;
        return {
          file,
          data,
          entityKind,
          ...id !== void 0 ? { id } : {},
          dir: file.relativePath.replace(/\/[^/]+$/, "")
        };
      });
      const byId = /* @__PURE__ */ new Map();
      for (const record of records) {
        if (!record.entityKind) continue;
        if (record.id !== void 0) {
          for (const alias of entityRefAliases3(record.id, record.entityKind)) {
            byId.set(entityKey22(record.entityKind, alias), record);
          }
        }
        const directoryId = stableDirectoryIdForSourceEntity2(record.file.relativePath, record.entityKind);
        if (directoryId !== void 0) {
          for (const alias of entityRefAliases3(directoryId, record.entityKind)) {
            byId.set(entityKey22(record.entityKind, alias), record);
          }
        }
      }
      return {
        records,
        entityPaths: new Set(records.map((record) => record.dir)),
        byId
      };
    }
    function changedEntitiesFromFiles2(changedFiles, sourceGraph, currentGraph) {
      const sourceByPath = new Map(sourceGraph.records.map((record) => [record.file.path, record]));
      const currentByRelativePath = new Map(currentGraph.records.map((record) => [record.file.relativePath, record]));
      const jsonFileChangesByPath = new Map(jsonFileChangesFromFiles2(
        changedFiles,
        sourceGraph.records.map((record) => record.file),
        currentGraph.records.map((record) => record.file)
      ).map((change) => [change.path, change]));
      return changedFiles.flatMap((file) => {
        if (!file.path.endsWith(".json")) return [];
        const sourceRecord = sourceByPath.get(file.path);
        const currentRecord = currentByRelativePath.get(file.path);
        const record = sourceRecord ?? currentRecord;
        const entity = isRecord242(record?.data) ? record.data : {};
        const entityKind = record?.entityKind ?? entityKindFromFilePath2(file.path, entity);
        const id = sourceEntityStableId2(entity, entityKind);
        return [{
          entityKind,
          path: sourceRecord?.file.path ?? file.path,
          ...id !== void 0 ? { id } : {},
          ...typeof entity.client_id === "string" ? { clientId: entity.client_id } : {},
          state: file.state,
          fieldChanges: jsonFileChangesByPath.get(sourceRecord?.file.path ?? file.path)?.fieldChanges ?? []
        }];
      });
    }
    function sourceRecordByPathOrId2(graph, entityKind, ref) {
      const normalizedRef = typeof ref === "string" ? normalizeWorkspacePath8(ref) : String(ref);
      return graph.records.find((record) => {
        return record.entityKind === entityKind && (record.dir === normalizedRef || record.file.relativePath === normalizedRef || String(record.id) === String(ref));
      }) ?? entityRefAliases3(ref, entityKind).map((alias) => graph.byId.get(entityKey22(entityKind, alias))).find((record) => record !== void 0);
    }
    function sourceEntityKindFromRelativePath2(path) {
      const normalized = normalizeWorkspacePath8(path);
      const fileName = normalized.split("/").pop();
      if (fileName === "project.json") return "project";
      if (fileName === "project_standards.json") return "project_standards";
      if (fileName === "setting.json") return "setting";
      if (fileName === "setting_state.json") return "setting_state";
      if (fileName === "asset.json") return "asset";
      if (fileName === "script.json") return "script";
      if (fileName === "script_version.json") return "script_version";
      if (fileName === "script_block.json") return "script_block";
      if (fileName === "content_unit.json") return "content_unit";
      if (fileName === "keyframe.json") return "keyframe";
      if (fileName === "production.json") return "production";
      if (fileName === "segment.json") return "segment";
      if (fileName === "scene_moment.json") return "scene_moment";
      if (fileName === "storyboard.json") return "storyboard";
      if (fileName === "audio_cue.json") return "audio_cue";
      if (fileName === "expression_unit.json") return "expression_unit";
      return void 0;
    }
    function stableDirectoryIdForSourceEntity2(path, entityKind) {
      const parts = normalizeWorkspacePath8(path).split("/");
      if (entityKind === "project" || entityKind === "project_standards") return void 0;
      if (!sourceEntityKindFromRelativePath2(path)) return void 0;
      const filename = parts.at(-1);
      if (!filename?.endsWith(".json")) return void 0;
      return parts.at(-2);
    }
    function sourceEntityStableId2(record, entityKind) {
      if (entityKind === "project") return idField92(record.project_id ?? record.ID ?? record.id);
      return idField92(record.id ?? record.ID);
    }
    function parseWorkspaceDocument32(path, content) {
      if (!path.endsWith(".json")) return content;
      try {
        return JSON.parse(content);
      } catch {
        return void 0;
      }
    }
    function entityKindFromFilePath2(path, record) {
      const schema = typeof record.schema === "string" ? record.schema : void 0;
      if (schema) {
        const schemaKind = schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "");
        if (schemaKind === "shot") return "unknown";
        if (schemaKind !== "shot") return schemaKind;
      }
      const fileName = path.split("/").pop() ?? path;
      const prefix = /^([a-z_]+)_/.exec(fileName)?.[1];
      return prefix ?? fileName.replace(/\.[^.]+$/, "");
    }
    function entityKey22(entityKind, id) {
      return `${entityKind}:${String(id ?? "")}`;
    }
    function idField92(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function isRecord242(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function interpretWorkspaceOverview2(input3) {
      const project = projectInfoFromSource2(input3.source);
      const affectedEntityKinds = [...new Set(input3.inspection.changedEntities.map((entity) => entity.entityKind))].sort();
      const interpretationStatus = !input3.latestInterpretation ? "missing" : input3.inspection.summary.total > 0 ? "stale" : "current";
      return {
        schema: "movscript.workspace-overview.v1",
        operation: "overview",
        createdAt: input3.createdAt,
        workspace: {
          ...project.projectId !== void 0 ? { projectId: project.projectId } : {},
          ...project.title !== void 0 ? { title: project.title } : {},
          sourcePath: input3.source.rootPath
        },
        source: {
          mode: input3.source.mode,
          documentCount: input3.source.files.length,
          entityCount: input3.source.files.filter((file) => sourceEntityKindFromRelativePath2(file.relativePath) !== void 0).length,
          issueCount: input3.inspection.issues.length,
          hasPendingEdits: input3.inspection.summary.total > 0,
          readyToInterpret: input3.inspection.readyToInterpret
        },
        interpret: {
          status: interpretationStatus,
          ...input3.latestInterpretation ? { lastInterpretationId: input3.latestInterpretation.manifest.interpretationId, lastInterpretedAt: input3.latestInterpretation.manifest.interpretedAt } : {},
          currentIsStale: interpretationStatus !== "current"
        },
        changes: {
          ...input3.inspection.summary,
          affectedEntityKinds
        },
        regeneration: input3.regeneration.summary,
        nextActions: nextActionsForOverview2(input3.inspection, input3.regeneration, interpretationStatus)
      };
    }
    function projectInfoFromSource2(source) {
      const projectFile = source.files.find((file) => file.relativePath === "project.json");
      const project = projectFile ? parseWorkspaceDocument42(projectFile.path, projectFile.content) : void 0;
      if (!isRecord252(project)) return {};
      const projectId = idField102(project.project_id ?? project.ID ?? project.id);
      return {
        ...projectId !== void 0 ? { projectId } : {},
        ...typeof project.title === "string" ? { title: project.title } : {}
      };
    }
    function nextActionsForOverview2(inspection, regeneration, interpretationStatus) {
      if (!inspection.readyToInterpret) return ["inspect"];
      if (interpretationStatus !== "current") return ["inspect", "interpret"];
      if (regeneration.summary.staleContentUnits > 0 || regeneration.summary.affectedContentUnits > 0) return ["regen plan"];
      return [];
    }
    function parseWorkspaceDocument42(path, content) {
      if (!path.endsWith(".json")) return content;
      try {
        return JSON.parse(content);
      } catch {
        return void 0;
      }
    }
    function idField102(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function isRecord252(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function diffMovScriptFileSnapshots2(workingFiles, baselineFiles, options = {}) {
      const basePath = options.basePath ?? "";
      const workingByRelativePath = new Map(workingFiles.map((file) => [file.relativePath, file]));
      const baselineByRelativePath = new Map(baselineFiles.map((file) => [file.relativePath, file]));
      const keys = [.../* @__PURE__ */ new Set([...workingByRelativePath.keys(), ...baselineByRelativePath.keys()])].sort();
      return keys.flatMap((relativePath) => {
        const working = workingByRelativePath.get(relativePath);
        const baseline = baselineByRelativePath.get(relativePath);
        if (working && !baseline) {
          return [{
            path: working.path,
            currentPath: joinWorkspacePath2(basePath, relativePath),
            state: "added",
            contentHash: working.hash
          }];
        }
        if (!working && baseline) {
          return [{
            path: relativePath,
            currentPath: baseline.path,
            state: "deleted",
            currentContentHash: baseline.hash
          }];
        }
        if (working && baseline && working.hash !== baseline.hash) {
          return [{
            path: working.path,
            currentPath: baseline.path,
            state: "modified",
            contentHash: working.hash,
            currentContentHash: baseline.hash
          }];
        }
        return [];
      });
    }
    function joinWorkspacePath2(root, relativePath) {
      return root ? `${root.replace(/\/$/, "")}/${relativePath}` : relativePath;
    }
    function productionImpactsFromSemanticChanges2(semanticChanges, artifacts, staleSelections) {
      const impacts = [];
      const impactBySource = new Map(artifacts.impactReport.changedEntities.map((entity) => [
        entityImpactKey2(entity.entityKind, entity.id, entity.path),
        entity
      ]));
      for (const change of semanticChanges) {
        if (change.propagation === "none") {
          impacts.push({
            kind: "diagnostic_only",
            businessKinds: [change.businessKind],
            businessImpacts: [businessImpactLabel22(change.businessKind)],
            sourceChanges: [change],
            reshootRequired: false
          });
          continue;
        }
        const staleMatches = staleSelections.filter((selection) => {
          if (change.entity.kind === "content_unit") {
            return sameOptionalId22(selection.contentUnitId, change.entity.id) || sameOptionalPath22(selection.contentUnitPath, change.sourceChange.path);
          }
          const sourceImpact2 = impactBySource.get(entityImpactKey2(change.entity.kind, change.entity.id, change.sourceChange.path));
          return sourceImpact2?.affectedContentUnits.some((contentUnit) => {
            return sameOptionalId22(contentUnit.id, selection.contentUnitId) || sameOptionalPath22(contentUnit.path, selection.contentUnitPath);
          }) ?? false;
        });
        if (change.propagation === "self") {
          for (const selection of staleMatches) {
            impacts.push({
              contentUnit: {
                ...selection.contentUnitId !== void 0 ? { id: selection.contentUnitId } : {},
                ...selection.contentUnitPath !== void 0 ? { path: selection.contentUnitPath } : {}
              },
              kind: "self_selection_stale",
              businessKinds: [change.businessKind],
              businessImpacts: [businessImpactLabel22(change.businessKind)],
              sourceChanges: [change],
              reshootRequired: false
            });
          }
          if (staleMatches.length === 0) {
            impacts.push({
              kind: "diagnostic_only",
              businessKinds: [change.businessKind],
              businessImpacts: [businessImpactLabel22(change.businessKind)],
              sourceChanges: [change],
              reshootRequired: false
            });
          }
          continue;
        }
        const sourceImpact = impactBySource.get(entityImpactKey2(change.entity.kind, change.entity.id, change.sourceChange.path));
        const affectedContentUnits = sourceImpact?.affectedContentUnits ?? [];
        for (const contentUnit of affectedContentUnits) {
          impacts.push({
            contentUnit: {
              ...contentUnit.id !== void 0 ? { id: contentUnit.id } : {},
              ...contentUnit.path !== void 0 ? { path: entityDir62(contentUnit.path) } : {}
            },
            kind: "downstream_reference_changed",
            businessKinds: [change.businessKind],
            businessImpacts: [businessImpactLabel22(change.businessKind)],
            sourceChanges: [change],
            reshootRequired: false
          });
        }
        if (affectedContentUnits.length === 0) {
          impacts.push({
            kind: "diagnostic_only",
            businessKinds: [change.businessKind],
            businessImpacts: [businessImpactLabel22(change.businessKind)],
            sourceChanges: [change],
            reshootRequired: false
          });
        }
      }
      return dedupeProductionImpacts2(impacts);
    }
    function entityImpactKey2(entityKind, id, path) {
      return `${entityKind}:${String(id ?? "")}:${path ? entityDir62(path) : ""}`;
    }
    function sameOptionalId22(left, right) {
      return left !== void 0 && right !== void 0 && String(left) === String(right);
    }
    function sameOptionalPath22(left, right) {
      if (typeof left !== "string" || typeof right !== "string") return false;
      return entityDir62(left) === entityDir62(right) || normalizeWorkspacePath72(left) === normalizeWorkspacePath72(right);
    }
    function dedupeProductionImpacts2(impacts) {
      const seen = /* @__PURE__ */ new Set();
      return impacts.filter((impact) => {
        const source = impact.sourceChanges[0];
        const key = [
          impact.kind,
          impact.contentUnit?.id ?? impact.contentUnit?.path ?? "",
          source?.entity.kind ?? "",
          source?.entity.id ?? "",
          source?.businessKind ?? "",
          source?.sourceChange.path ?? "",
          source?.fields.join(",") ?? ""
        ].join("|");
        if (seen.has(key)) return false;
        seen.add(key);
        return true;
      });
    }
    function entityDir62(path) {
      return normalizeWorkspacePath72(path).replace(/\/[^/]+$/, "");
    }
    function normalizeWorkspacePath72(path) {
      return path.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+/g, "/").replace(/\/$/, "");
    }
    function businessImpactLabel22(businessKind) {
      const labels = {
        metadata_changed: "Metadata changed",
        semantic_input_changed: "Semantic input changed",
        reference_changed: "Reference changed",
        selection_changed: "Selection changed",
        sequence_reordered: "Sequence reordered",
        expression_unit_changed: "Expression unit changed",
        storyboard_changed: "Storyboard changed",
        keyframe_changed: "Keyframe changed",
        content_unit_changed: "Content unit changed",
        project_context_changed: "Project context changed",
        production_structure_changed: "Production structure changed",
        domain_entity_changed: "Domain entity changed"
      };
      return labels[businessKind];
    }
    function businessChangesFromChangedEntities2(changedEntities, sourceGraph, currentGraph, semanticChanges = []) {
      const semanticByEntity = groupSemanticChangesByEntity22(semanticChanges);
      return changedEntities.map((entity) => {
        const record = sourceRecordForChangedEntity2(sourceGraph, entity) ?? sourceRecordForChangedEntity2(currentGraph, entity);
        const title = isRecord262(record?.data) ? stringField162(record.data.title) : void 0;
        const entitySemanticChanges = semanticByEntity.get(entitySemanticKey22(entity.entityKind, entity.id)) ?? [];
        const businessKinds = uniqueSorted3(entitySemanticChanges.map((change) => change.businessKind));
        const semanticKinds = uniqueSorted3(entitySemanticChanges.map((change) => change.kind));
        return {
          ...entity,
          ...title !== void 0 ? { title } : {},
          summary: businessChangeSummary2(entity, title, businessKinds),
          impactAreas: businessImpactAreasForEntityKind2(entity.entityKind),
          sourcePaths: [entity.path],
          semanticKinds,
          businessKinds
        };
      });
    }
    function summarizeReview2(changedFiles, businessChanges, issues) {
      return {
        total: changedFiles.length,
        added: changedFiles.filter((file) => file.state === "added").length,
        modified: changedFiles.filter((file) => file.state === "modified").length,
        deleted: changedFiles.filter((file) => file.state === "deleted").length,
        businessChanges: businessChanges.length,
        errors: issues.filter((issue) => issue.severity === "error").length,
        warnings: issues.filter((issue) => issue.severity === "warning").length
      };
    }
    function sourceRecordForChangedEntity2(graph, entity) {
      return graph.records.find((record) => {
        return record.entityKind === entity.entityKind && (record.file.path === entity.path || record.file.relativePath === entity.path || record.id !== void 0 && entity.id !== void 0 && String(record.id) === String(entity.id));
      });
    }
    function businessChangeSummary2(entity, title, businessKinds = []) {
      const label = title ?? String(entity.id ?? entity.path);
      const state = businessStateVerb2(entity.state);
      const primaryBusinessKind = businessKinds[0];
      if (entity.state === "modified" && primaryBusinessKind) return `${businessKindLabel2(primaryBusinessKind)}: ${label}`;
      return `${businessEntityLabel2(entity.entityKind)} ${state}: ${label}`;
    }
    function businessStateVerb2(state) {
      switch (state) {
        case "added":
          return "added";
        case "modified":
          return "changed";
        case "deleted":
          return "deleted";
        case "unchanged":
          return "unchanged";
      }
    }
    function businessEntityLabel2(entityKind) {
      const labels = {
        project: "Project",
        project_standards: "Project standards",
        script: "Script",
        script_version: "Script version",
        script_block: "Script block",
        production: "Production",
        segment: "Segment",
        scene_moment: "Scene moment",
        shot: "Shot",
        storyboard: "Storyboard",
        audio_cue: "Audio cue",
        expression_unit: "Expression unit",
        content_unit: "Content unit",
        keyframe: "Keyframe",
        setting: "Setting",
        setting_state: "Setting state",
        asset: "Asset"
      };
      return labels[entityKind] ?? entityKind;
    }
    function businessImpactAreasForEntityKind2(entityKind) {
      switch (entityKind) {
        case "project":
        case "project_standards":
        case "script":
        case "script_version":
        case "script_block":
          return ["workspace_context", "generation_prompts"];
        case "setting":
        case "setting_state":
        case "asset":
          return ["asset_index", "generation_context"];
        case "production":
        case "segment":
        case "scene_moment":
        case "shot":
        case "storyboard":
        case "audio_cue":
        case "expression_unit":
          return ["planning_tree", "preview_timeline", "generation_prompts"];
        case "content_unit":
        case "keyframe":
          return ["content_production", "generation_prompts", "preview_timeline"];
        default:
          return ["domain_index"];
      }
    }
    function groupSemanticChangesByEntity22(semanticChanges) {
      const groups = /* @__PURE__ */ new Map();
      for (const change of semanticChanges) {
        const key = entitySemanticKey22(change.entity.kind, change.entity.id);
        groups.set(key, [...groups.get(key) ?? [], change]);
      }
      return groups;
    }
    function entitySemanticKey22(entityKind, id) {
      return `${entityKind}:${String(id ?? "")}`;
    }
    function uniqueSorted3(values) {
      return [...new Set(values)].sort();
    }
    function businessKindLabel2(kind) {
      const labels = {
        metadata_changed: "Metadata changed",
        semantic_input_changed: "Semantic input changed",
        reference_changed: "Reference changed",
        selection_changed: "Selection changed",
        sequence_reordered: "Sequence reordered",
        expression_unit_changed: "Expression unit changed",
        storyboard_changed: "Storyboard changed",
        keyframe_changed: "Keyframe changed",
        content_unit_changed: "Content unit changed",
        project_context_changed: "Project context changed",
        production_structure_changed: "Production structure changed",
        domain_entity_changed: "Domain entity changed"
      };
      return labels[kind];
    }
    function stringField162(value) {
      return typeof value === "string" && value.trim() ? value : void 0;
    }
    function isRecord262(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    var FIELD_SEMANTIC_RULES2 = [
      {
        matches: (entity, fieldChange) => entity.entityKind === "expression_unit" && isExpressionUnitSemanticInputField2(fieldChange.field),
        semanticKind: "semantic_input_changed",
        businessKind: "expression_unit_changed",
        propagation: "self"
      },
      {
        matches: (_entity, fieldChange) => isSelectionField2(fieldChange.field),
        semanticKind: "selection_changed",
        businessKind: "selection_changed",
        propagation: "downstream_reference"
      },
      {
        matches: (_entity, fieldChange) => isReferenceField2(fieldChange.field),
        semanticKind: "reference_changed",
        businessKind: "reference_changed",
        propagation: "downstream_reference"
      },
      {
        matches: (_entity, fieldChange) => isMetadataField2(fieldChange.field),
        semanticKind: "metadata_changed",
        businessKind: "metadata_changed",
        propagation: "none"
      }
    ];
    function semanticChangesFromEntityChanges2(changedEntities) {
      return changedEntities.flatMap((entity) => {
        const fieldChanges = entity.fieldChanges?.length ? entity.fieldChanges : [{ field: "*", operation: entity.state }];
        const grouped = /* @__PURE__ */ new Map();
        for (const fieldChange of fieldChanges) {
          const semantic = semanticForFieldChange2(entity, fieldChange);
          const key = `${semantic.kind}:${semantic.businessKind}:${semantic.propagation}`;
          const group = grouped.get(key) ?? {
            kind: semantic.kind,
            businessKind: semantic.businessKind,
            propagation: semantic.propagation,
            fields: []
          };
          if (!group.fields.includes(fieldChange.field)) group.fields.push(fieldChange.field);
          grouped.set(key, group);
        }
        return [...grouped.values()].map((group) => ({
          entity: {
            kind: entity.entityKind,
            ...entity.id !== void 0 ? { id: entity.id } : {}
          },
          kind: group.kind,
          businessKind: group.businessKind,
          propagation: group.propagation,
          fields: group.fields.sort(),
          sourceChange: {
            operation: entity.state,
            path: entity.path
          }
        }));
      });
    }
    function semanticForFieldChange2(entity, fieldChange) {
      if (entity.state === "added" || entity.state === "deleted") {
        return {
          kind: "semantic_input_changed",
          businessKind: businessKindForEntityChange2(entity.entityKind),
          propagation: entity.entityKind === "content_unit" ? "self" : "downstream_reference"
        };
      }
      if (fieldChange.jsonOperation === "reordered" || fieldChange.jsonOperation === "moved") {
        const base = semanticCategoryForFieldChange2(entity, fieldChange);
        return {
          kind: base.kind,
          businessKind: "sequence_reordered",
          propagation: base.propagation
        };
      }
      const rule = FIELD_SEMANTIC_RULES2.find((item) => item.matches(entity, fieldChange));
      if (rule) {
        return {
          kind: rule.semanticKind,
          businessKind: rule.businessKind,
          propagation: rule.propagation
        };
      }
      return semanticCategoryForFieldChange2(entity, fieldChange);
    }
    function semanticCategoryForFieldChange2(entity, fieldChange) {
      const rule = FIELD_SEMANTIC_RULES2.find((item) => item.matches(entity, fieldChange));
      if (rule) {
        return {
          kind: rule.semanticKind,
          businessKind: rule.businessKind,
          propagation: rule.propagation
        };
      }
      return {
        kind: "semantic_input_changed",
        businessKind: businessKindForEntityChange2(entity.entityKind),
        propagation: entity.entityKind === "content_unit" ? "self" : "downstream_reference"
      };
    }
    function businessKindForEntityChange2(entityKind) {
      switch (entityKind) {
        case "expression_unit":
          return "expression_unit_changed";
        case "storyboard":
          return "storyboard_changed";
        case "keyframe":
          return "keyframe_changed";
        case "content_unit":
          return "content_unit_changed";
        case "project":
        case "project_standards":
        case "script":
        case "script_version":
        case "script_block":
          return "project_context_changed";
        case "production":
        case "segment":
        case "scene_moment":
          return "production_structure_changed";
        default:
          return "domain_entity_changed";
      }
    }
    function isExpressionUnitSemanticInputField2(field) {
      return field === "*" || field === "slot_kind" || field === "slotKind" || field === "expression_kind" || field === "expressionKind" || field === "text" || field.startsWith("text.") || field === "angle" || field.startsWith("angle.") || field === "lens" || field.startsWith("lens.") || field === "camera" || field.startsWith("camera.") || field === "movement" || field.startsWith("movement.") || field === "blocking" || field.startsWith("blocking.") || field === "lighting" || field.startsWith("lighting.") || field === "sound" || field.startsWith("sound.") || field === "performance" || field.startsWith("performance.") || field === "duration" || field.startsWith("duration.") || field === "intent" || field.startsWith("intent.") || field === "visual_intent" || field.startsWith("visual_intent.") || field === "narrative_intent" || field.startsWith("narrative_intent.");
    }
    function isMetadataField2(field) {
      return /(^|\.)metadata(\.|$)/.test(field) || /(^|\.)notes?$/.test(field) || /(^|\.)title$/.test(field) || /(^|\.)description$/.test(field) || /(^|\.)created_at$/.test(field) || /(^|\.)updated_at$/.test(field) || /(^|\.)selected_at$/.test(field) || /(^|\.)selected_by$/.test(field);
    }
    function isReferenceField2(field) {
      return /(^|_|\.)refs?(\.|$)/.test(field) || field.endsWith("_ref") || field.includes("_ref.") || field.endsWith("_refs") || field.includes("_refs.");
    }
    function isSelectionField2(field) {
      return field === "lock" || field.startsWith("lock.") || field === "selection" || field.startsWith("selection.") || field === "stale_policy" || field.endsWith(".candidate_id") || field.endsWith(".resource_id");
    }
    function objectSchema22(required, properties) {
      return {
        type: "object",
        additionalProperties: true,
        required,
        properties
      };
    }
    function entitySchema2(entityKind, required, properties) {
      return objectSchema22(["schema", "kind", "id", ...required], {
        schema: { const: `movscript.${entityKind}.v1` },
        kind: { const: entityKind },
        id: { type: "string", minLength: 1 },
        title: { type: "string" },
        description: { type: "string" },
        order: { type: "number" },
        tags: { type: "array", items: { type: "string" } },
        notes: { type: "string" },
        ...properties
      });
    }
    var sourceRefSchema2 = { type: "string", minLength: 1 };
    var transitionSchema2 = objectSchema22([], {
      in: { type: "string" },
      out: { type: "string" },
      notes: { type: "string" }
    });
    var projectEntitySchema2 = {
      id: "movscript.project.v1",
      entityKind: "project",
      title: "Project",
      version: "1.0.0",
      status: "active",
      jsonSchema: objectSchema22(["schema", "kind", "project_id", "title"], {
        schema: { const: "movscript.project.v1" },
        kind: { const: "project" },
        project_id: { type: "string", minLength: 1 },
        title: { type: "string" },
        description: { type: "string" },
        project_kind: { type: "string" },
        timeline_template: {
          enum: ["film", "series", "short_video", "course"],
          description: "Optional default timeline namespace template. It is a creation/planning hint, not an instance parent tree."
        },
        timeline_namespaces: {
          type: "array",
          description: "Project-owned timeline namespace vocabulary, such as series, season, episode, act, sequence, beat.",
          items: { type: "string", minLength: 1 }
        },
        setting_namespaces: {
          type: "array",
          description: "Project-owned setting namespace vocabulary, such as character, costume, state, or voice_state.",
          items: { type: "string", minLength: 1 }
        },
        namespace_vocabulary: objectSchema22([], {
          timeline_template: {
            enum: ["film", "series", "short_video", "course"],
            description: "Optional default timeline namespace template. It suggests creation flow but never owns concrete parent relations."
          },
          timeline_namespaces: { type: "array", items: { type: "string", minLength: 1 } },
          setting_namespaces: { type: "array", items: { type: "string", minLength: 1 } }
        }),
        logline: { type: "string" },
        language: { type: "string" }
      }),
      promptSummary: "Project is the root business boundary for a complete local film workspace. It may declare namespace vocabulary and templates for UI and agent planning, but concrete parent/containment still comes from source paths or validated explicit refs.",
      examples: [{
        title: "series_demo",
        content: {
          schema: "movscript.project.v1",
          kind: "project",
          project_id: "demo",
          title: "Demo Series",
          namespace_vocabulary: {
            timeline_template: "series",
            timeline_namespaces: ["episode", "act", "beat"],
            setting_namespaces: ["character", "costume", "state"]
          }
        }
      }]
    };
    var projectStandardsEntitySchema2 = {
      id: "movscript.project_standards.v1",
      entityKind: "project_standards",
      title: "Project Standards",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("project_standards", [], {
        aspect_ratio: { type: "string" },
        shot_size_system: { type: "array", items: { type: "string" } },
        camera_language: { type: "string" },
        visual_style: { type: "string" },
        lighting_style: { type: "string" },
        color_palette: { type: "string" },
        pacing_rules: { type: "string" },
        negative_rules: { type: "array", items: { type: "string" } },
        custom_rules: { type: "array", items: { type: "object", additionalProperties: true } }
      }),
      promptSummary: "Project standards define project-wide production, visual, camera, lighting, pacing, and prompt constraints.",
      examples: [{
        title: "vertical_drama",
        content: {
          schema: "movscript.project_standards.v1",
          kind: "project_standards",
          id: "project_standards",
          aspect_ratio: "9:16",
          shot_size_system: ["wide", "medium", "close_up", "insert"]
        }
      }]
    };
    var settingEntitySchema2 = {
      id: "movscript.setting.v1",
      entityKind: "setting",
      title: "Setting",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("setting", ["title"], {
        namespace_kind: { type: "string", description: "Project vocabulary label for this setting namespace node, such as character, location, prop, costume, or voice_identity." },
        setting_namespace_kind: { type: "string", description: "Alias of namespace_kind for setting namespace vocabulary." },
        setting_kind: { enum: ["character", "location", "prop", "world_rule", "style", "other"] },
        profile: { type: "object", additionalProperties: true }
      }),
      promptSummary: "Setting is a concrete setting namespace root for a reusable film/music entity, such as a character, prop, place, instrument, costume, or voice identity. It is not a content-unit target and must not own content-unit refs; assets belong under setting states.",
      examples: [{
        title: "hero",
        content: { schema: "movscript.setting.v1", kind: "setting", id: "hero", setting_kind: "character", title: "Hero" }
      }]
    };
    var settingStateEntitySchema2 = {
      id: "movscript.setting_state.v1",
      entityKind: "setting_state",
      title: "Setting State",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("setting_state", ["title"], {
        namespace_kind: { type: "string", description: "Project vocabulary label for this setting-state namespace node, such as base_state, costume_state, emotion_state, or voice_state." },
        setting_namespace_kind: { type: "string", description: "Alias of namespace_kind for setting namespace vocabulary." },
        state_kind: { type: "string" },
        changes: { type: "object", additionalProperties: true }
      }),
      promptSummary: "Setting state is a namespace under one setting for a named condition/version of that entity, such as base look, wet hair, damaged prop, side-view variant, or calm voice. It is not a content-unit target and must not own content-unit refs.",
      examples: [{
        title: "rain_panic",
        content: { schema: "movscript.setting_state.v1", kind: "setting_state", id: "rain_panic", title: "Rain panic" }
      }]
    };
    var assetEntitySchema2 = {
      id: "movscript.asset.v1",
      entityKind: "asset",
      title: "Asset",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("asset", ["slot"], {
        setting_id: { type: "string", description: "Concrete setting id. Usually derived from the source path." },
        setting_state_id: { type: "string", description: "Owning setting_state id. Usually derived from the source path." },
        slot: { type: "string", minLength: 1 },
        asset_kind: { enum: ["image", "video", "audio", "text", "reference", "other"] },
        prompt_hint: { type: "string" },
        resource_id: { type: ["string", "number"], description: "Optional selected stable RawResource reference for imported/manual assets. Generated asset candidates should be selected through an asset_ref content unit instead." },
        asset_ref_content_unit_id: { type: "string", description: "Optional pointer to the asset_ref content unit that carries generated candidates for this asset." },
        provider_certifications: {
          type: "object",
          description: "Provider material-library certification records keyed by provider, such as seedance2. Certifications bind to the selected asset_ref RawResource, not the abstract setting.",
          additionalProperties: {
            type: "object",
            additionalProperties: true,
            properties: {
              status: { enum: ["active", "processing", "failed", "stale", "unknown"] },
              hub_asset_id: { type: "string" },
              asset_uri: { type: "string" },
              source_resource_id: { type: ["string", "number"] },
              source_candidate_id: { type: ["string", "number"] },
              source_hash: { type: "string" },
              certified_at: { type: "string" },
              updated_at: { type: "string" },
              error: { type: ["string", "object", "null"], additionalProperties: true }
            }
          }
        },
        candidates: {
          type: "array",
          deprecated: true,
          description: "Legacy inline candidate storage only. Do not create new asset candidates here; use an asset_ref content unit and backend content-unit candidate decisions.",
          items: { type: "object", additionalProperties: true }
        },
        selection: {
          type: "object",
          deprecated: true,
          description: "Legacy inline asset selection only. New generated asset selections belong to the asset_ref content unit candidate decision flow.",
          additionalProperties: true
        }
      }),
      promptSummary: "Asset is a setting-state-owned resource slot and a carrier for asset_ref content-unit generation/review. Do not write new candidates or selections into asset.json; generated asset candidates belong to the linked asset_ref content unit and backend content-unit decision flow. Provider material-library certifications, when present, bind to the selected asset_ref resource and should be marked stale if that selected resource changes. Image assets should prefer plain white or very clean backgrounds.",
      examples: [{
        title: "portrait",
        content: { schema: "movscript.asset.v1", kind: "asset", id: "base_portrait", slot: "character_base_portrait" }
      }]
    };
    var scriptEntitySchema2 = {
      id: "movscript.script.v1",
      entityKind: "script",
      title: "Script",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("script", ["title"], {
        script_kind: { type: "string" },
        source_ref: { type: "string" },
        content: { type: "string" }
      }),
      promptSummary: "Script is a screenplay or source text root. Script and production are parallel workspaces and can reference each other.",
      examples: [{
        title: "main",
        content: { schema: "movscript.script.v1", kind: "script", id: "main", title: "Main Script" }
      }]
    };
    var scriptVersionEntitySchema2 = {
      id: "movscript.script_version.v1",
      entityKind: "script_version",
      title: "Script Version",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("script_version", ["title"], {
        version_label: { type: "string" },
        source_ref: { type: "string" }
      }),
      promptSummary: "Script version is a versioned script snapshot and can own script blocks.",
      examples: [{
        title: "v1",
        content: { schema: "movscript.script_version.v1", kind: "script_version", id: "v1", title: "V1" }
      }]
    };
    var scriptBlockEntitySchema2 = {
      id: "movscript.script_block.v1",
      entityKind: "script_block",
      title: "Script Block",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("script_block", ["text"], {
        block_kind: { type: "string" },
        text: { type: "string", minLength: 1 },
        source_range: { type: "object", additionalProperties: true }
      }),
      promptSummary: "Script block is an addressable block of script text referenced by planning entities.",
      examples: [{
        title: "opening",
        content: { schema: "movscript.script_block.v1", kind: "script_block", id: "opening", text: "INT. APARTMENT - NIGHT" }
      }]
    };
    var productionEntitySchema2 = {
      id: "movscript.production.v1",
      entityKind: "production",
      title: "Production",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("production", ["title"], {
        namespace_kind: { type: "string", description: "Project vocabulary label for this timeline namespace node, such as film, episode, act, sequence, or beat." },
        timeline_namespace_kind: { type: "string", description: "Alias of namespace_kind for timeline namespace vocabulary." },
        production_kind: { type: "string" },
        transition: transitionSchema2
      }),
      promptSummary: "Production is a legacy timeline namespace source record. It contributes path-derived containment, context, and transition boundaries, but it is not directly generated; use a timeline_assembly_ref content unit when a namespace scope needs a video output.",
      examples: [{
        title: "episode",
        content: { schema: "movscript.production.v1", kind: "production", id: "p8f3", title: "Episode 1", namespace_kind: "episode" }
      }]
    };
    var segmentEntitySchema2 = {
      id: "movscript.segment.v1",
      entityKind: "segment",
      title: "Segment",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("segment", ["title", "order"], {
        namespace_kind: { type: "string", description: "Project vocabulary label for this timeline namespace node, such as act, sequence, beat, hook, lesson, or segment." },
        timeline_namespace_kind: { type: "string", description: "Alias of namespace_kind for timeline namespace vocabulary." },
        segment_kind: { enum: ["emotional_function", "rhythm_shift", "dramatic_function", "setup", "escalation", "release", "reversal", "transition"] },
        emotional_intent: { type: "string" },
        rhythm: { type: "string" },
        transition: transitionSchema2
      }),
      promptSummary: "Segment is a legacy timeline namespace node inside the path tree. Directory id and path parent define containment; namespace_kind names the user-facing layer. It is not directly generated; use scene_moment, expression_unit, or timeline_assembly_ref content units for production work.",
      examples: [{
        title: "opening",
        content: { schema: "movscript.segment.v1", kind: "segment", id: "a19d", title: "Opening pressure", namespace_kind: "beat", order: 1 }
      }]
    };
    var sceneMomentEntitySchema2 = {
      id: "movscript.scene_moment.v1",
      entityKind: "scene_moment",
      title: "Scene Moment",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("scene_moment", ["title", "order"], {
        when: { type: "string" },
        where: { type: "string" },
        action: { type: "string" },
        emotion: { type: "string" },
        transition: transitionSchema2
      }),
      promptSummary: "Scene moment is planning context. It owns only scene-level transition boundaries; storyboard ordering lives on storyboard entities and audio cues are independent objects.",
      examples: [{
        title: "call",
        content: {
          schema: "movscript.scene_moment.v1",
          kind: "scene_moment",
          id: "r72k",
          title: "Hero hears the unknown call",
          order: 1,
          transition: { out: "hold_then_cut" }
        }
      }]
    };
    var storyboardEntitySchema2 = {
      id: "movscript.storyboard.v1",
      entityKind: "storyboard",
      title: "Storyboard",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("storyboard", [], {
        asset_kind: { enum: ["image", "video", "reference", "other"] },
        slot: { type: "string", minLength: 1 },
        scene_moment_ref: sourceRefSchema2,
        expression_unit_ref: sourceRefSchema2,
        transition: transitionSchema2,
        timeline: objectSchema22([], {
          gap_after_sec: { type: "number" },
          caption: { type: "string" },
          duration_sec: { type: "number" }
        }),
        graph: objectSchema22([], {
          nodes: { type: "array", items: { type: "object", additionalProperties: true } },
          edges: { type: "array", items: { type: "object", additionalProperties: true } },
          entry_node_id: { type: "string" },
          exit_node_id: { type: "string" }
        }),
        setting_refs: {
          type: "array",
          items: objectSchema22(["setting_id"], {
            setting_id: sourceRefSchema2,
            setting_state_id: sourceRefSchema2,
            role: { type: "string" },
            notes: { type: "string" }
          })
        },
        coverage_plan: { type: "object", additionalProperties: true },
        continuity: { type: "object", additionalProperties: true },
        storyboard_panels: { type: "array", items: { type: "object", additionalProperties: true } },
        prompt_hint: { type: "string" }
      }),
      promptSummary: "Storyboard is a graph-like visual asset for a scene moment or expression unit, similar to an asset slot: storyboard.json defines refs, graph nodes/edges, panels, prompt hints, and continuity; runtime candidates and production decisions live outside storyboard.json.",
      examples: [{
        title: "main",
        content: {
          schema: "movscript.storyboard.v1",
          kind: "storyboard",
          id: "main",
          title: "Rain call storyboard",
          slot: "main",
          asset_kind: "video",
          order: 1,
          graph: {
            nodes: [{ id: "phone_glow", kind: "panel", caption: "Phone glow returns." }],
            edges: [],
            entry_node_id: "phone_glow",
            exit_node_id: "phone_glow"
          },
          timeline: { caption: "Phone glow returns.", gap_after_sec: 0.4 },
          transition: { out: "hold_then_cut" },
          setting_refs: [{ setting_id: "hero", setting_state_id: "rain_panic", role: "subject" }]
        }
      }]
    };
    var audioCueEntitySchema2 = {
      id: "movscript.audio_cue.v1",
      entityKind: "audio_cue",
      title: "Audio Cue",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("audio_cue", ["cue_kind", "title"], {
        cue_kind: { enum: ["sound_effect", "music", "ambience", "dialogue", "foley", "other"] },
        scope_ref: sourceRefSchema2,
        expression_unit_ref: sourceRefSchema2,
        storyboard_ref: sourceRefSchema2,
        timing: objectSchema22([], {
          start: { type: "string" },
          end: { type: "string" },
          offset_sec: { type: "number" },
          duration_sec: { type: "number" }
        }),
        prompt_hint: { type: "string" },
        asset_refs: { type: "array", items: sourceRefSchema2 }
      }),
      promptSummary: "Audio cue is an independent planning object for sound effects, music, ambience, dialogue cues, or foley. It can attach to a scene moment, expression unit, or storyboard through refs.",
      examples: [{
        title: "phone_vibration",
        content: {
          schema: "movscript.audio_cue.v1",
          kind: "audio_cue",
          id: "phone_vibration",
          title: "Phone vibration",
          cue_kind: "sound_effect",
          scope_ref: "productions/p8f3/segments/a19d/scene_moments/r72k",
          storyboard_ref: "productions/p8f3/segments/a19d/scene_moments/r72k/expression_units/eu_phone_closeup/storyboards/main",
          timing: { start: "after_action", duration_sec: 1.2 },
          prompt_hint: "Low, sharp phone vibration under rain ambience."
        }
      }]
    };
    var expressionUnitEntitySchema2 = {
      id: "movscript.expression_unit.v1",
      entityKind: "expression_unit",
      title: "Expression Unit",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("expression_unit", [], {
        slot_kind: { enum: ["visual", "voice", "subtitle", "audio"] },
        modality: { enum: ["visual", "verbal", "audio", "text", "interaction", "metadata"] },
        role: { type: "string", minLength: 1 },
        expression_kind: { enum: ["dialogue", "narration", "subtitle", "caption", "action", "visual_note", "shot"] },
        visual_kind: { type: "string" },
        speaker: { type: "string" },
        speaker_ref: sourceRefSchema2,
        source_expression_ref: sourceRefSchema2,
        text: { type: "string", minLength: 1 },
        intent: { type: "string" },
        content: { type: "object", additionalProperties: true },
        timing_intent: { type: "object", additionalProperties: true },
        voice_profile_ref: sourceRefSchema2,
        span: objectSchema22([], {
          storyboard_refs: { type: "array", items: sourceRefSchema2 },
          expression_refs: { type: "array", items: sourceRefSchema2 },
          from_storyboard_id: { type: "string" },
          to_storyboard_id: { type: "string" },
          start: { type: "string" },
          end: { type: "string" }
        }),
        script_block_id: sourceRefSchema2
      }),
      promptSummary: "Expression unit is the smallest form/edit slot under a scene moment. Use slot_kind=visual|voice|subtitle|audio for new records; legacy modality, role, and expression_kind fields remain compatibility hints. Content units target expression_unit_ref and declare the generated output_kind.",
      examples: [{
        title: "visual_shot_material",
        content: {
          schema: "movscript.expression_unit.v1",
          kind: "expression_unit",
          id: "eu_phone_closeup",
          slot_kind: "visual",
          modality: "visual",
          role: "shot",
          title: "Phone close-up",
          intent: "Phone lights up again while the hero freezes.",
          content: {
            shot_size: "close_up",
            camera: { movement: "slow_push_in" }
          },
          timing_intent: { duration_sec: 2.4 }
        }
      }]
    };
    var contentUnitEntitySchema2 = {
      id: "movscript.content_unit.v1",
      entityKind: "content_unit",
      title: "Content Unit",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("content_unit", ["content_unit_type", "output_kind", "title"], {
        content_unit_type: { type: "string", minLength: 1 },
        output_kind: { enum: ["image", "video", "audio", "text", "metadata"] },
        target_category: { enum: ["timeline_assembly", "system_primitive", "content_unit"] },
        target_kind: {
          enum: ["timeline_assembly", "production", "segment", "scene_moment", "expression_unit", "asset", "keyframe", "storyboard", "audio_cue", "content_unit"],
          description: "Use timeline_assembly, system primitives, or content_unit. production/segment remain legacy aliases only when paired with production_ref/segment_ref normalization. Namespace targets such as setting, setting_state, production, or segment are rejected for new generic content units."
        },
        target_ref: sourceRefSchema2,
        scope_kind: { type: "string" },
        scope_ref: sourceRefSchema2,
        generation_role: { type: "string" },
        production_ref: sourceRefSchema2,
        segment_ref: sourceRefSchema2,
        asset_ref: sourceRefSchema2,
        keyframe_ref: sourceRefSchema2,
        storyboard_ref: sourceRefSchema2,
        audio_cue_ref: sourceRefSchema2,
        scene_moment_ref: sourceRefSchema2,
        expression_unit_ref: sourceRefSchema2,
        content_unit_ref: sourceRefSchema2,
        voice_profile_ref: sourceRefSchema2,
        edit_prompt: objectSchema22([], {
          text: { type: "string" },
          negative_text: { type: "string" },
          notes: { type: "string" },
          structured: { type: "object", additionalProperties: true }
        }),
        model_intent: objectSchema22([], {
          capability: { type: "string" },
          provider: { type: "string" },
          model: { type: "string" },
          quality: { type: "string" },
          duration_sec: { type: "number" },
          aspect_ratio: { type: "string" },
          params: { type: "object", additionalProperties: true }
        })
      }),
      promptSummary: "Content unit is a project-level stable generation task. It targets a system primitive such as scene_moment, expression_unit, asset, keyframe, storyboard, or audio_cue, or it targets a timeline_assembly for rendering a namespace scope. production_ref and segment_ref are legacy aliases for timeline_assembly_ref. References written inside edit_prompt with {{type:id}} syntax are upstream inputs. Candidates copy the normalized prompt snapshot at generation time; selections and runtime candidates are stored outside content_unit.json.",
      examples: [{
        title: "expression_visual_material",
        content: {
          schema: "movscript.content_unit.v1",
          kind: "content_unit",
          id: "cu_phone_visual",
          content_unit_type: "expression_unit_ref",
          output_kind: "video",
          target_kind: "expression_unit",
          target_ref: "eu_phone_closeup",
          generation_role: "visual_material",
          title: "Phone close-up visual material",
          edit_prompt: { text: "Generate this visual expression material with selected references." }
        }
      }, {
        title: "episode_assembly",
        content: {
          schema: "movscript.content_unit.v1",
          kind: "content_unit",
          id: "cu_episode_01",
          content_unit_type: "timeline_assembly_ref",
          output_kind: "video",
          target_kind: "timeline_assembly",
          target_ref: "timeline_assembly:episode:episode_01",
          generation_role: "timeline_assembly",
          title: "Episode 01 assembly",
          edit_prompt: { text: "Assemble the selected scene moment outputs for this episode." }
        }
      }]
    };
    var keyframeEntitySchema2 = {
      id: "movscript.keyframe.v1",
      entityKind: "keyframe",
      title: "Keyframe",
      version: "1.0.0",
      status: "active",
      jsonSchema: entitySchema2("keyframe", [], {
        scene_moment_ref: sourceRefSchema2,
        expression_unit_ref: sourceRefSchema2,
        storyboard_ref: sourceRefSchema2,
        role: { type: "string" },
        visual_intent: { type: "string" },
        timing: { type: "object", additionalProperties: true },
        composition: { type: "object", additionalProperties: true },
        continuity: { type: "object", additionalProperties: true },
        reference_asset_refs: { type: "array", items: sourceRefSchema2 },
        reference_keyframe_refs: { type: "array", items: sourceRefSchema2 }
      }),
      promptSummary: "Keyframe is a visual anchor for a scene moment or expression unit. It describes timing, composition, continuity, visual intent, and reference assets. Runtime candidates and production decisions are stored outside keyframe.json.",
      examples: [{
        title: "anchor",
        content: {
          schema: "movscript.keyframe.v1",
          kind: "keyframe",
          id: "c83x",
          title: "Phone light close-up",
          visual_intent: "Phone blue light illuminates the hero face in a rainy apartment."
        }
      }]
    };
    var SEMANTIC_ENTITY_SCHEMA_REGISTRY2 = {
      [projectEntitySchema2.id]: projectEntitySchema2,
      [projectStandardsEntitySchema2.id]: projectStandardsEntitySchema2,
      [settingEntitySchema2.id]: settingEntitySchema2,
      [settingStateEntitySchema2.id]: settingStateEntitySchema2,
      [assetEntitySchema2.id]: assetEntitySchema2,
      [scriptEntitySchema2.id]: scriptEntitySchema2,
      [scriptVersionEntitySchema2.id]: scriptVersionEntitySchema2,
      [scriptBlockEntitySchema2.id]: scriptBlockEntitySchema2,
      [productionEntitySchema2.id]: productionEntitySchema2,
      [segmentEntitySchema2.id]: segmentEntitySchema2,
      [sceneMomentEntitySchema2.id]: sceneMomentEntitySchema2,
      [storyboardEntitySchema2.id]: storyboardEntitySchema2,
      [audioCueEntitySchema2.id]: audioCueEntitySchema2,
      [expressionUnitEntitySchema2.id]: expressionUnitEntitySchema2,
      [contentUnitEntitySchema2.id]: contentUnitEntitySchema2,
      [keyframeEntitySchema2.id]: keyframeEntitySchema2
    };
    var SEMANTIC_ENTITY_SCHEMA_IDS2 = Object.keys(SEMANTIC_ENTITY_SCHEMA_REGISTRY2);
    var SEMANTIC_ENTITY_KIND_VALUES2 = [
      "project",
      "project_standards",
      "setting",
      "setting_state",
      "asset",
      "script",
      "script_version",
      "script_block",
      "production",
      "segment",
      "scene_moment",
      "storyboard",
      "audio_cue",
      "expression_unit",
      "content_unit",
      "keyframe"
    ];
    function getSemanticEntitySchemaEntry2(schemaId) {
      return SEMANTIC_ENTITY_SCHEMA_REGISTRY2[schemaId] ?? null;
    }
    function listSemanticEntitySchemasByKind2(entityKind) {
      return Object.values(SEMANTIC_ENTITY_SCHEMA_REGISTRY2).filter((schema) => schema.entityKind === entityKind).sort((a, b) => b.version.localeCompare(a.version, void 0, { numeric: true }));
    }
    function getActiveSemanticEntitySchemaForKind2(entityKind) {
      const active = listSemanticEntitySchemasByKind2(entityKind).filter((schema2) => schema2.status === "active");
      const schema = active[0];
      if (!schema) throw new Error(`No active semantic entity schema for kind: ${entityKind}`);
      return schema;
    }
    function validateEditableFiles2(files) {
      const issues = [];
      for (const file of files) {
        if (file.path.endsWith(".json")) {
          try {
            JSON.parse(file.content);
          } catch (error) {
            issues.push({
              path: file.path,
              severity: "error",
              message: `invalid JSON: ${error instanceof Error ? error.message : String(error)}`
            });
          }
        }
      }
      return issues;
    }
    function validateSourceDomainGraph2(graph) {
      const issues = [];
      for (const entry of graph.records) {
        if (!entry.file.path.endsWith(".json")) continue;
        if (!isRecord272(entry.data)) continue;
        const expectedKind = entry.entityKind;
        const schemaKind = typeof entry.data.schema === "string" ? entry.data.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
        const actualKind = typeof entry.data.kind === "string" ? entry.data.kind : void 0;
        if (!expectedKind) {
          issues.push({
            path: entry.file.path,
            severity: "error",
            message: "unsupported source file path for MovScript domain entity"
          });
          continue;
        }
        if (!sourcePathMatchesEntityKind2(entry.file.relativePath, expectedKind)) {
          issues.push({
            path: entry.file.path,
            severity: "error",
            message: `source file name does not match workspace entity kind ${expectedKind}`
          });
        }
        const directoryId = stableDirectoryIdForSourceEntity2(entry.file.relativePath, expectedKind);
        const recordId = sourceEntityStableId2(entry.data, expectedKind);
        if (directoryId !== void 0 && recordId !== void 0 && String(recordId) !== directoryId) {
          issues.push({
            path: entry.file.path,
            severity: "error",
            message: `id ${String(recordId)} does not match source directory id ${directoryId}`
          });
        }
        if (!schemaKind) {
          issues.push({
            path: entry.file.path,
            severity: "error",
            message: "missing schema field"
          });
        } else if (schemaKind !== expectedKind) {
          issues.push({
            path: entry.file.path,
            severity: "error",
            message: `schema kind ${schemaKind} does not match source path entity ${expectedKind}`
          });
        } else {
          validateSemanticEntitySchema2(entry.file, entry.data, issues);
        }
        if (actualKind && actualKind !== expectedKind) {
          issues.push({
            path: entry.file.path,
            severity: "error",
            message: `kind ${actualKind} does not match source path entity ${expectedKind}`
          });
        }
        if (sourceEntityStableId2(entry.data, expectedKind) === void 0) {
          issues.push({
            path: entry.file.path,
            severity: "error",
            message: "missing stable id field"
          });
        }
        for (const diagnostic22 of assertNamespaceCannotOwnContentUnitRef2({
          entityKind: expectedKind,
          record: entry.data,
          path: entry.file.relativePath
        })) {
          issues.push({
            path: entry.file.path,
            severity: diagnostic22.severity,
            message: diagnostic22.message
          });
        }
        for (const diagnostic22 of assertNamespaceCannotOwnProductionState2({
          entityKind: expectedKind,
          record: entry.data,
          path: entry.file.relativePath
        })) {
          issues.push({
            path: entry.file.path,
            severity: diagnostic22.severity,
            message: diagnostic22.message
          });
        }
        validateExplicitPathParentRefs2(entry, graph, issues);
        if (expectedKind === "content_unit") {
          validateContentUnitRefs2(entry.file, entry.data, graph, issues);
        }
        if (expectedKind === "asset") {
          validateAssetOwnership2(entry.file, entry.data, graph, issues);
        }
        if (expectedKind === "storyboard") {
          validateStoryboardSettingRefs2(entry.file, entry.data, graph, issues);
        }
        if (expectedKind === "audio_cue") {
          validateAudioCueRefs2(entry.file, entry.data, graph, issues);
        }
        if (expectedKind === "keyframe") {
          validateKeyframeReferenceAssetRefs2(entry.file, entry.data, graph, issues);
        }
      }
      return issues;
    }
    function validateExplicitPathParentRefs2(entry, graph, issues) {
      if (!entry.entityKind || !isRecord272(entry.data)) return;
      for (const ref of explicitPathParentRefs2(entry.entityKind, entry.data)) {
        const pathParent = ref.parentKind ? nearestAncestorRecord2(graph, entry.file.relativePath, ref.parentKind) : nearestPathParentRecord2(graph, entry);
        const childId = sourceRecordStableRef2(entry);
        const parentId = pathParent ? sourceRecordStableRef2(pathParent) : void 0;
        for (const diagnostic22 of assertExplicitParentRefMatchesPath2({
          child: {
            kind: entry.entityKind,
            ...childId !== void 0 ? { id: childId } : {},
            path: entry.file.relativePath
          },
          pathParent: pathParent ? {
            kind: pathParent.entityKind ?? "unknown",
            ...parentId !== void 0 ? { id: parentId } : {},
            path: pathParent.file.relativePath
          } : void 0,
          explicitParentRef: ref.value,
          field: ref.field,
          path: entry.file.relativePath
        })) {
          issues.push({
            path: entry.file.path,
            severity: diagnostic22.severity,
            message: diagnostic22.message
          });
        }
      }
    }
    function explicitPathParentRefs2(entityKind, record) {
      const refs = [];
      for (const field of [
        "parent_ref",
        "parentRef",
        "parent_id",
        "parentId",
        "parent_scope_ref",
        "parentScopeRef",
        "parent_scope_id",
        "parentScopeId",
        "parent_namespace_ref",
        "parentNamespaceRef"
      ]) {
        const value = idField112(record[field]);
        if (value !== void 0) refs.push({ field, value });
      }
      if (classifyMovScriptEntityKind2(entityKind) === "timeline_namespace") {
        for (const field of ["scope_ref", "scopeRef"]) {
          const value = idField112(record[field]);
          if (value !== void 0) refs.push({ field, value });
        }
      }
      if (entityKind === "setting_state") {
        for (const field of ["setting_id", "settingId", "setting_ref", "settingRef"]) {
          const value = idField112(record[field]);
          if (value !== void 0) refs.push({ field, value, parentKind: "setting" });
        }
      }
      if (entityKind === "storyboard" || entityKind === "keyframe") {
        for (const field of ["scene_moment_ref", "sceneMomentRef"]) {
          const value = idField112(record[field]);
          if (value !== void 0) refs.push({ field, value, parentKind: "scene_moment" });
        }
        for (const field of ["expression_unit_ref", "expressionUnitRef"]) {
          const value = idField112(record[field]);
          if (value !== void 0) refs.push({ field, value, parentKind: "expression_unit" });
        }
      }
      return refs;
    }
    function validateSemanticEntitySchema2(file, record, issues) {
      const schemaId = typeof record.schema === "string" ? record.schema : void 0;
      const schema = schemaId ? getSemanticEntitySchemaEntry2(schemaId) : null;
      if (!schema) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `unknown semantic entity schema: ${schemaId ?? "<missing>"}`
        });
        return;
      }
      for (const message of validateJsonSchemaValue2(record, schema.jsonSchema, "$")) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `schema validation failed: ${message}`
        });
      }
    }
    function validateContentUnitRefs2(file, record, graph, issues) {
      const contentUnitType22 = typeof record.content_unit_type === "string" ? record.content_unit_type : void 0;
      const outputKind = typeof record.output_kind === "string" ? record.output_kind : void 0;
      if (!contentUnitType22) return;
      for (const diagnostic22 of contentUnitTargetValidationDiagnostics2(record)) {
        issues.push({
          path: file.path,
          severity: diagnostic22.severity,
          message: diagnostic22.message
        });
      }
      const expectedOutputKind = expectedOutputKindForContentUnitType22(contentUnitType22);
      if (expectedOutputKind && outputKind !== expectedOutputKind) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `${contentUnitType22} content_unit output_kind must be ${expectedOutputKind}`
        });
      }
      const primaryKind = primaryRefKindForContentUnitType22(contentUnitType22);
      for (const ref of parseUnsupportedContentUnitEditPromptRefs3(record.edit_prompt)) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `unsupported content_unit prompt ref kind "${ref.kind}": ${ref.raw}. Namespace vocabulary is context, not a selected-resource dependency`
        });
      }
      if (!primaryKind) return;
      const primaryRefs = primaryRefIdsForContentUnitRecord22(record, primaryKind);
      const primaryFieldName = primaryRefFieldNameForKind22(primaryKind);
      if (primaryRefs.length === 0) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `${contentUnitType22} content_unit requires ${primaryFieldName}`
        });
      }
      const uniquePrimaryRefs = [];
      for (const ref of primaryRefs) {
        if (uniquePrimaryRefs.some((primaryRef) => sameRefId22(primaryRef, ref, primaryKind))) continue;
        uniquePrimaryRefs.push(ref);
      }
      if (uniquePrimaryRefs.length > 1) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `${contentUnitType22} content_unit accepts only one ${primaryFieldName}`
        });
      }
      for (const ref of primaryRefs) {
        if (!sourceRecordByPathOrId2(graph, primaryKind, ref)) {
          issues.push({
            path: file.path,
            severity: "error",
            message: `${contentUnitType22} content_unit ${primaryFieldName} does not resolve: ${ref}`
          });
        }
      }
      const refs = parseContentUnitEditPromptRefs22(record.edit_prompt);
      for (const ref of refs) {
        if (ref.kind === primaryKind && primaryRefs.some((primaryRef) => sameRefId22(primaryRef, ref.id, primaryKind))) {
          issues.push({
            path: file.path,
            severity: "error",
            message: `${contentUnitType22} content_unit edit_prompt must not reference its own ${primaryFieldName}: ${ref.raw}`
          });
        }
        const resolved = sourceRecordByPathOrId2(graph, ref.kind, ref.id);
        if (!resolved && ref.kind !== "content_unit") {
          issues.push({
            path: file.path,
            severity: "error",
            message: `content_unit prompt ref does not resolve: ${ref.raw}`
          });
        }
      }
    }
    function validateAssetOwnership2(file, record, graph, issues) {
      const setting = nearestAncestorRecord2(graph, file.relativePath, "setting");
      const settingState = nearestAncestorRecord2(graph, file.relativePath, "setting_state");
      const pathSettingId = setting ? sourceRecordStableRef2(setting) : void 0;
      const pathStateId = settingState ? sourceRecordStableRef2(settingState) : void 0;
      const recordSettingId = idField112(record.setting_id ?? record.settingId ?? record.setting_ref ?? record.settingRef);
      const recordStateId = idField112(record.setting_state_id ?? record.settingStateId ?? record.setting_state_ref ?? record.settingStateRef);
      if (!setting) {
        issues.push({
          path: file.path,
          severity: "error",
          message: "asset source path setting parent does not resolve"
        });
      }
      if (!settingState) {
        issues.push({
          path: file.path,
          severity: "error",
          message: "asset source path setting state parent does not resolve"
        });
      }
      if (recordSettingId === void 0) {
        issues.push({
          path: file.path,
          severity: "error",
          message: "asset requires setting_id matching source path"
        });
      } else if (pathSettingId !== void 0 && !sameRefId22(String(recordSettingId), pathSettingId, "setting")) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `asset setting_id ${String(recordSettingId)} does not match source path setting ${pathSettingId}`
        });
      }
      if (recordStateId === void 0) {
        issues.push({
          path: file.path,
          severity: "error",
          message: "asset requires setting_state_id matching source path"
        });
      } else if (pathStateId !== void 0 && !sameRefId22(String(recordStateId), pathStateId, "setting_state")) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `asset setting_state_id ${String(recordStateId)} does not match source path state ${pathStateId}`
        });
      }
      if (setting && settingState && !isDescendantDir2(settingState.dir, setting.dir)) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `asset setting_state_id does not belong to setting_id: ${pathStateId}`
        });
      }
    }
    function validateAudioCueRefs2(file, record, graph, issues) {
      const scopeRef = typeof record.scope_ref === "string" ? normalizeWorkspacePath8(record.scope_ref) : void 0;
      const expressionUnitRef = typeof record.expression_unit_ref === "string" ? normalizeWorkspacePath8(record.expression_unit_ref) : void 0;
      const storyboardRef = typeof record.storyboard_ref === "string" ? normalizeWorkspacePath8(record.storyboard_ref) : void 0;
      const scope = scopeRef ? sourceRecordByPathOrId2(graph, "scene_moment", scopeRef) : void 0;
      const expressionUnit = expressionUnitRef ? sourceRecordByPathOrId2(graph, "expression_unit", expressionUnitRef) : void 0;
      const storyboard = storyboardRef ? sourceRecordByPathOrId2(graph, "storyboard", storyboardRef) : void 0;
      const cueDir = file.relativePath.replace(/\/audio_cue\.json$/, "");
      const sceneMomentDir = cueDir.replace(/\/audio_cues\/[^/]+$/, "");
      if (scopeRef && !scope) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `audio_cue scope_ref does not resolve: ${scopeRef}`
        });
      }
      if (scope && scope.dir !== sceneMomentDir) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `audio_cue scope_ref must reference the owning scene moment: ${scopeRef}`
        });
      }
      if (expressionUnitRef && !expressionUnit) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `audio_cue expression_unit_ref does not resolve: ${expressionUnitRef}`
        });
      }
      if (expressionUnit && !isDescendantDir2(expressionUnit.dir, sceneMomentDir)) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `audio_cue expression_unit_ref is not under owning scene moment: ${expressionUnitRef}`
        });
      }
      if (storyboardRef && !storyboard) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `audio_cue storyboard_ref does not resolve: ${storyboardRef}`
        });
      }
      if (storyboard && !isStoryboardUnderSceneMoment2(storyboard.dir, sceneMomentDir)) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `audio_cue storyboard_ref is not under owning scene moment: ${storyboardRef}`
        });
      }
      const assetRefs = Array.isArray(record.asset_refs) ? record.asset_refs : [];
      for (const [index, assetRef32] of assetRefs.entries()) {
        const assetId = idField112(assetRef32);
        const asset = assetId !== void 0 ? sourceRecordByPathOrId2(graph, "asset", assetId) : void 0;
        if (assetId === void 0 || !asset) {
          issues.push({
            path: file.path,
            severity: "error",
            message: `audio_cue asset_refs[${index}] does not resolve: ${String(assetRef32)}`
          });
        }
      }
    }
    function validateStoryboardSettingRefs2(file, record, graph, issues) {
      const settingRefs = Array.isArray(record.setting_refs) ? record.setting_refs.filter(isRecord272) : [];
      for (const [index, settingRef] of settingRefs.entries()) {
        const settingId = idField112(settingRef.setting_id);
        const settingStateId = idField112(settingRef.setting_state_id);
        const setting = settingId !== void 0 ? sourceRecordByPathOrId2(graph, "setting", settingId) : void 0;
        if (settingId !== void 0 && !setting) {
          issues.push({
            path: file.path,
            severity: "error",
            message: `storyboard setting_refs[${index}].setting_id does not resolve: ${String(settingId)}`
          });
        }
        if (settingStateId !== void 0) {
          const settingState = sourceRecordByPathOrId2(graph, "setting_state", settingStateId);
          if (!settingState) {
            issues.push({
              path: file.path,
              severity: "error",
              message: `storyboard setting_refs[${index}].setting_state_id does not resolve: ${String(settingStateId)}`
            });
          } else if (setting && !isDescendantDir2(settingState.dir, setting.dir)) {
            issues.push({
              path: file.path,
              severity: "error",
              message: `storyboard setting_refs[${index}].setting_state_id does not belong to setting_id: ${String(settingStateId)}`
            });
          }
        }
      }
    }
    function isStoryboardUnderSceneMoment2(storyboardDir, sceneMomentDir) {
      return storyboardDir.startsWith(`${sceneMomentDir}/storyboards/`) || /^.+\/expression_units\/[^/]+\/storyboards\/[^/]+$/.test(storyboardDir) && storyboardDir.startsWith(`${sceneMomentDir}/expression_units/`);
    }
    function validateKeyframeReferenceAssetRefs2(file, record, graph, issues) {
      const assetRefs = Array.isArray(record.reference_asset_refs) ? record.reference_asset_refs : [];
      for (const [index, assetRef32] of assetRefs.entries()) {
        const assetId = idField112(assetRef32);
        if (assetId === void 0) {
          issues.push({
            path: file.path,
            severity: "error",
            message: `keyframe reference_asset_refs[${index}] must be a stable asset id or path`
          });
          continue;
        }
        if (!sourceRecordByPathOrId2(graph, "asset", assetId)) {
          issues.push({
            path: file.path,
            severity: "error",
            message: `keyframe reference_asset_refs[${index}] does not resolve: ${String(assetId)}`
          });
        }
      }
    }
    function validateJsonSchemaValue2(value, schema, path) {
      if (!isRecord272(schema)) return [];
      const messages = [];
      if ("const" in schema && !jsonValueEquals22(value, schema.const)) {
        messages.push(`${path} must be ${JSON.stringify(schema.const)}`);
      }
      if (Array.isArray(schema.enum) && !schema.enum.some((item) => jsonValueEquals22(value, item))) {
        messages.push(`${path} must be one of ${schema.enum.map((item) => JSON.stringify(item)).join(", ")}`);
      }
      if (schema.type !== void 0 && !jsonSchemaTypeMatches2(value, schema.type)) {
        messages.push(`${path} must be ${Array.isArray(schema.type) ? schema.type.join(" or ") : String(schema.type)}`);
        return messages;
      }
      if (typeof value === "string" && typeof schema.minLength === "number" && value.length < schema.minLength) {
        messages.push(`${path} must contain at least ${schema.minLength} character${schema.minLength === 1 ? "" : "s"}`);
      }
      if (schema.type === "object" && isRecord272(value)) {
        const properties = isRecord272(schema.properties) ? schema.properties : {};
        const required = Array.isArray(schema.required) ? schema.required.filter((item) => typeof item === "string") : [];
        for (const key of required) {
          if (value[key] === void 0) messages.push(`${path}.${key} is required`);
        }
        if (schema.additionalProperties === false) {
          for (const key of Object.keys(value)) {
            if (properties[key] === void 0) messages.push(`${path}.${key} is not allowed`);
          }
        }
        for (const [key, propertySchema] of Object.entries(properties)) {
          if (value[key] !== void 0) messages.push(...validateJsonSchemaValue2(value[key], propertySchema, `${path}.${key}`));
        }
      }
      if (schema.type === "array" && Array.isArray(value) && schema.items !== void 0) {
        value.forEach((item, index) => {
          messages.push(...validateJsonSchemaValue2(item, schema.items, `${path}[${index}]`));
        });
      }
      return messages;
    }
    function jsonSchemaTypeMatches2(value, type) {
      if (Array.isArray(type)) return type.some((item) => jsonSchemaTypeMatches2(value, item));
      if (type === "object") return isRecord272(value);
      if (type === "array") return Array.isArray(value);
      if (type === "string") return typeof value === "string";
      if (type === "number") return typeof value === "number" && Number.isFinite(value);
      if (type === "boolean") return typeof value === "boolean";
      if (type === "null") return value === null;
      return true;
    }
    function jsonValueEquals22(left, right) {
      return JSON.stringify(left) === JSON.stringify(right);
    }
    function sourcePathMatchesEntityKind2(path, entityKind) {
      const normalized = normalizeWorkspacePath8(path);
      const fileName = normalized.split("/").pop();
      const fileNames = {
        project: ["project.json"],
        project_standards: ["project_standards.json"],
        setting: ["setting.json"],
        setting_state: ["setting_state.json"],
        asset: ["asset.json"],
        script: ["script.json"],
        script_version: ["script_version.json"],
        script_block: ["script_block.json"],
        content_unit: ["content_unit.json"],
        keyframe: ["keyframe.json"],
        production: ["production.json"],
        segment: ["segment.json"],
        scene_moment: ["scene_moment.json"],
        storyboard: ["storyboard.json"],
        audio_cue: ["audio_cue.json"],
        expression_unit: ["expression_unit.json"]
      };
      return fileName !== void 0 && (fileNames[entityKind] ?? []).includes(fileName);
    }
    function nearestAncestorRecord2(graph, path, entityKind) {
      const dir = normalizeWorkspacePath8(path).replace(/\/[^/]+$/, "");
      const candidates = graph.records.filter((record) => record.entityKind === entityKind && isDescendantDir2(dir, record.dir)).sort((left, right) => right.dir.length - left.dir.length);
      return candidates[0];
    }
    function nearestPathParentRecord2(graph, entry) {
      const dir = normalizeWorkspacePath8(entry.file.relativePath).replace(/\/[^/]+$/, "");
      const candidates = graph.records.filter((record) => record.file.relativePath !== entry.file.relativePath).filter((record) => record.entityKind !== void 0).filter((record) => classifyMovScriptEntityKind2(record.entityKind ?? "") !== void 0).filter((record) => isDescendantDir2(dir, record.dir)).sort((left, right) => right.dir.length - left.dir.length);
      return candidates[0];
    }
    function sourceRecordStableRef2(record) {
      if (record.id !== void 0) return String(record.id);
      return stableDirectoryIdForSourceEntity2(record.file.relativePath, record.entityKind ?? "");
    }
    function isDescendantDir2(child, parent) {
      const normalizedChild = normalizeWorkspacePath8(child);
      const normalizedParent = normalizeWorkspacePath8(parent);
      return normalizedChild === normalizedParent || normalizedChild.startsWith(`${normalizedParent}/`);
    }
    function idField112(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function sameRefId22(left, right, kind) {
      return sameEntityRef3(left, right, kind) || lastPathSegment42(left) === right || lastPathSegment42(right) === left;
    }
    function lastPathSegment42(value) {
      if (typeof value !== "string" || !value.includes("/")) return void 0;
      return value.split("/").filter(Boolean).at(-1);
    }
    function isRecord272(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function findUncoveredGitSourceFileChanges2(gitChanges, changedFiles) {
      const reviewPaths = new Set(changedFiles.flatMap((file) => {
        const path = normalizeWorkspacePath8(file.path);
        const currentPath = normalizeWorkspacePath8(file.currentPath);
        return [path, currentPath].filter((item) => item && !item.startsWith(`${MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR2}/`));
      }));
      return gitChanges.filter((change) => {
        const paths = [
          normalizeWorkspacePath8(change.path),
          ...change.previousPath ? [normalizeWorkspacePath8(change.previousPath)] : []
        ];
        return paths.every((path) => !reviewPaths.has(path));
      });
    }
    async function validateGitFileChangeCoverage2(fileRepository, baseline, changedFiles, options = {}) {
      if (baseline.source !== "git" || !baseline.checkpointHash) return [];
      const rootDir = getNodeMovScriptWorkspaceFileRepositoryRoot2(fileRepository);
      if (!rootDir) return [];
      const gitChanges = (await readNodeMovScriptGitSourceFileChanges2(rootDir, baseline.checkpointHash)).filter((change) => {
        if (options.includeContentUnitDecisionDocuments !== false) return true;
        return !isMovScriptContentUnitDecisionPath2(change.path) && (change.previousPath === void 0 || !isMovScriptContentUnitDecisionPath2(change.previousPath));
      });
      if (gitChanges.length === 0) return [];
      const missing = findUncoveredGitSourceFileChanges2(gitChanges, changedFiles);
      if (missing.length === 0) return [];
      return [{
        path: ".",
        severity: "warning",
        message: `git diff reported source file changes not present in review changedFiles: ${missing.map((change) => change.previousPath ? `${change.previousPath}->${change.path}` : change.path).join(", ")}`
      }];
    }
    async function reviewMovScriptWorkspace2(input3) {
      const now = input3.now ?? /* @__PURE__ */ new Date();
      const sourceOptions = workspaceSourceOptions3(input3);
      const source = await resolveWorkspaceSource2(input3.fileRepository, sourceOptions);
      const editFiles = source.files;
      const baseline = await loadCheckpointSourceSnapshots2(input3.fileRepository, input3.commit ?? input3.checkpointHash, sourceOptions);
      const currentFiles = baseline.files;
      const changedFiles = diffWorkspaceFiles2(editFiles, currentFiles, baseline.basePath);
      const sourceGraph = buildSourceDomainGraph2(editFiles);
      const currentGraph = buildSourceDomainGraph2(currentFiles);
      const changedEntities = changedEntitiesFromFiles2(changedFiles, sourceGraph, currentGraph);
      const issues = [
        ...validateEditableFiles2(editFiles),
        ...validateSourceDomainGraph2(sourceGraph),
        ...await validateGitFileChangeCoverage2(input3.fileRepository, baseline, changedFiles, sourceOptions)
      ];
      const semanticChanges = semanticChangesFromEntityChanges2(changedEntities);
      const sourceDocuments = editFiles.map((file) => ({
        path: file.relativePath,
        data: parseWorkspaceDocument52(file.path, file.content)
      }));
      const documents = await overlayMovScriptDecisionDocuments2(sourceDocuments, input3.decisionStore);
      const index = deriveMovScriptWorkspaceDomainIndex2(documents);
      const artifacts = deriveMovScriptWorkspaceArtifacts2({
        index,
        changedEntities,
        semanticChanges,
        interpretationId: `review_${now.toISOString().replace(/[-:.TZ]/g, "")}`,
        createdAt: now.toISOString(),
        sourceIssues: issues
      });
      const selectionValidity = artifacts.contentUnitArtifacts.map((artifact) => ({
        contentUnitId: artifact.contentUnitId,
        contentUnitPath: entityDir72(artifact.contentUnitPath),
        selected: artifact.selectionValidity.selected,
        stale: artifact.selectionValidity.stale,
        candidateId: artifact.selectionValidity.candidate_id,
        resourceId: artifact.selectionValidity.resource_id,
        staleReasons: artifact.selectionValidity.stale_reasons
      }));
      const staleSelections = selectionValidity.filter((selection) => selection.stale);
      const productionImpacts = productionImpactsFromSemanticChanges2(semanticChanges, artifacts, staleSelections);
      const businessChanges = businessChangesFromChangedEntities2(changedEntities, sourceGraph, currentGraph, semanticChanges);
      const summary = summarizeReview2(changedFiles, businessChanges, issues);
      return {
        schema: "movscript.workspace-review.v1",
        operation: "review",
        basePath: baseline.basePath,
        comparisonBase: {
          ...baseline.checkpointHash ? { from: baseline.checkpointHash } : {},
          source: baseline.source,
          workspace: {
            id: workspaceSnapshotId2(source.files),
            kind: "working_tree"
          }
        },
        sourcePath: source.rootPath,
        sourceMode: source.mode,
        createdAt: now.toISOString(),
        changedFiles,
        changedEntities,
        entityChanges: changedEntities,
        semanticChanges,
        productionImpacts,
        selectionValidity,
        staleSelections,
        productionWorkPlan: artifacts.productionWorkPlan,
        reshootTargets: [],
        businessChanges,
        issues,
        readyToInterpret: summary.errors === 0,
        summary
      };
    }
    async function inspectMovScriptWorkspace2(input3) {
      const review = await reviewMovScriptWorkspace2(input3);
      return {
        ...review,
        schema: "movscript.workspace-inspection.v1",
        operation: "inspect"
      };
    }
    function entityDir72(path) {
      return normalizeWorkspacePath8(path).replace(/\/[^/]+$/, "");
    }
    function workspaceSourceOptions3(input3) {
      return {
        includeContentUnitDecisionDocuments: input3.decisionStore ? false : true
      };
    }
    function diffWorkspaceFiles2(editFiles, currentFiles, basePath = MOVSCRIPT_INTERPRET_CURRENT_DIR2) {
      return diffMovScriptFileSnapshots2(editFiles, currentFiles, { basePath });
    }
    function parseWorkspaceDocument52(path, content) {
      if (!path.endsWith(".json")) return content;
      try {
        return JSON.parse(content);
      } catch {
        return void 0;
      }
    }
    async function overviewMovScriptWorkspace2(input3) {
      const now = input3.now ?? /* @__PURE__ */ new Date();
      const [inspection, source, latestInterpretation] = await Promise.all([
        inspectMovScriptWorkspace2({ ...input3, now }),
        resolveWorkspaceSource2(input3.fileRepository, workspaceSourceOptions22(input3)),
        loadLatestInterpretManifest2(input3.fileRepository)
      ]);
      const impactReport = await loadLatestImpactReport2(input3.fileRepository, latestInterpretation);
      const regeneration = deriveV1RegenerationPlan2({
        review: inspection,
        latestInterpretation,
        impactReport,
        createdAt: now.toISOString()
      });
      return interpretWorkspaceOverview2({
        createdAt: now.toISOString(),
        inspection,
        source,
        latestInterpretation,
        regeneration
      });
    }
    async function interpretMovScriptWorkspace2(input3) {
      const now = input3.now ?? /* @__PURE__ */ new Date();
      const review = await reviewMovScriptWorkspace2({ ...input3, now });
      if (!review.readyToInterpret) {
        return {
          schema: "movscript.workspace-interpret-result.v1",
          operation: "interpret",
          status: "failed",
          review
        };
      }
      const source = await resolveWorkspaceSource2(input3.fileRepository, workspaceSourceOptions22(input3));
      const editFiles = source.files;
      const sourceDocuments = editFiles.map((file) => ({
        path: file.relativePath,
        data: parseWorkspaceDocument62(file.path, file.content)
      }));
      const documents = await overlayMovScriptDecisionDocuments2(sourceDocuments, input3.decisionStore);
      const index = deriveMovScriptWorkspaceDomainIndex2(documents);
      const interpretationId = interpretationIdFor2(now);
      const artifacts = deriveMovScriptWorkspaceArtifacts2({
        index,
        changedEntities: review.changedEntities,
        semanticChanges: review.semanticChanges,
        sourceIssues: review.issues,
        interpretationId,
        createdAt: now.toISOString()
      });
      const impactReportPath = `.interpret/reviews/impact-report_${interpretationId}.json`;
      const manifest = {
        schema: "movscript.workspace-interpret.v1",
        interpretationId,
        interpretedAt: now.toISOString(),
        source: {
          sourcePath: source.rootPath,
          sourceMode: source.mode,
          sourceFileHashes: Object.fromEntries(editFiles.map((file) => [file.relativePath, file.hash]))
        },
        output: {
          currentPath: MOVSCRIPT_INTERPRET_CURRENT_DIR2,
          domainIndexPath: MOVSCRIPT_DOMAIN_INDEX_PATH2,
          domainTreePath: MOVSCRIPT_DOMAIN_TREE_PATH2,
          editorStatePath: MOVSCRIPT_EDITOR_STATE_PATH2,
          assetIndexPath: MOVSCRIPT_ASSET_INDEX_PATH2,
          relationGraphPath: MOVSCRIPT_RELATION_GRAPH_PATH2,
          impactReportPath
        },
        review
      };
      if (input3.debugArtifacts !== false) {
        await writeDebugArtifacts2(input3.fileRepository, artifacts, index, manifest, impactReportPath);
      }
      return {
        schema: "movscript.workspace-interpret-result.v1",
        operation: "interpret",
        status: "refreshed",
        review,
        index,
        productionWorkPlan: artifacts.productionWorkPlan,
        ...input3.debugArtifacts !== false ? { manifest } : {}
      };
    }
    async function planMovScriptWorkspaceRegeneration2(input3) {
      const now = input3.now ?? /* @__PURE__ */ new Date();
      const review = await reviewMovScriptWorkspace2({ ...input3, now });
      const latestInterpretation = await loadLatestInterpretManifest2(input3.fileRepository);
      const impactReport = await loadLatestImpactReport2(input3.fileRepository, latestInterpretation);
      return deriveV1RegenerationPlan2({
        review,
        latestInterpretation,
        impactReport,
        createdAt: now.toISOString()
      });
    }
    function parseWorkspaceDocument62(path, content) {
      if (!path.endsWith(".json")) return content;
      try {
        return JSON.parse(content);
      } catch {
        return void 0;
      }
    }
    function workspaceSourceOptions22(input3) {
      return {
        includeContentUnitDecisionDocuments: input3.decisionStore ? false : true
      };
    }
    function interpretationIdFor2(date) {
      return `interpret_${date.toISOString().replace(/[^0-9]/g, "")}`;
    }
    var PROMPT_REF_PATTERN32 = /\{\{([a-z_]+)::?([^{}:\s][^{}]*)\}\}/g;
    async function buildContentUnitBackendPromptById2(input3) {
      const contentUnit = queryMovScriptWorkspaceEntities2(input3.index, { entityKind: "content_unit" }).find((entity) => entity.id !== void 0 && sameEntityRef3(entity.id, input3.contentUnitId, "content_unit"));
      if (!contentUnit) {
        return failedPrompt2({
          contentUnitRef: `content_units/${entityPathSlug2(input3.contentUnitId, "content_unit")}`,
          contentUnitId: input3.contentUnitId,
          contentUnitType: "",
          outputKind: "metadata",
          blockers: [{
            code: "content_unit_not_found",
            content_unit_id: input3.contentUnitId,
            message: `content unit does not exist: ${String(input3.contentUnitId)}`
          }]
        });
      }
      return buildContentUnitBackendPrompt2({
        index: input3.index,
        contentUnit,
        decisionProvider: input3.decisionProvider,
        ...input3.promptText !== void 0 ? { promptText: input3.promptText } : {}
      });
    }
    async function buildContentUnitBackendPrompt2(input3) {
      const contentUnitRef = entityDir82(input3.contentUnit.path);
      const contentUnitType22 = stringField172(input3.contentUnit.record.content_unit_type) ?? "";
      const outputKind = contentUnitOutputKind32(input3.contentUnit.record.output_kind);
      const editPrompt = contentUnitEditPrompt2(input3.contentUnit.record.edit_prompt, input3.promptText);
      const refs = parseContentUnitEditPromptRefs32(editPrompt);
      const unsupportedRefs = parseUnsupportedContentUnitEditPromptRefs22(editPrompt);
      const primaryKind = primaryRefKindForContentUnitType32(contentUnitType22);
      const primaryRefs = primaryKind ? primaryContentUnitRefs22(input3.contentUnit, primaryKind) : [];
      const styleReferenceResourceIds = usesVisualStyleReferences22(outputKind) ? projectStyleReferenceResourceIds22(input3.index) : [];
      const decisionCache = /* @__PURE__ */ new Map();
      const blockers = [];
      if (primaryKind) {
        if (primaryRefs.length === 0) {
          blockers.push({
            code: "primary_ref_missing",
            content_unit_ref: contentUnitRef,
            message: `${contentUnitType22} requires a ${primaryKind} primary ref`
          });
        }
        if (hasAmbiguousPrimaryRefs22(primaryRefs, primaryKind)) {
          blockers.push({
            code: "primary_ref_ambiguous",
            content_unit_ref: contentUnitRef,
            message: `${contentUnitType22} accepts only one ${primaryKind} primary ref`
          });
        }
      }
      if (input3.contentUnit.id === void 0) {
        blockers.push({
          code: "content_unit_id_missing",
          content_unit_ref: contentUnitRef,
          message: `content unit is missing id: ${contentUnitRef}`
        });
      }
      for (const ref of unsupportedRefs) {
        blockers.push(unsupportedPromptRefBlocker2(ref));
      }
      const resolvedRefs = [];
      for (const ref of refs) {
        const role = ref.role ?? "input";
        const directRef = await resolveDirectPromptResourceRef2({
          ref,
          role,
          contentUnit: input3.contentUnit,
          contentUnitRef,
          decisionProvider: input3.decisionProvider,
          decisionCache
        });
        if (directRef) {
          if (directRef.blocker) blockers.push(directRef.blocker);
          resolvedRefs.push(directRef);
          continue;
        }
        const entity = resolvePromptRefEntity22(input3.index, ref);
        const base = {
          ...ref,
          role,
          ...entity ? {
            resolved: {
              entityKind: entity.entityKind,
              ...entity.id !== void 0 ? { id: entity.id } : {},
              path: entity.path
            }
          } : {}
        };
        if (!entity) {
          const blocker = {
            code: "ref_not_found",
            ref: ref.raw,
            message: `prompt ref does not resolve: ${ref.raw}`
          };
          blockers.push(blocker);
          resolvedRefs.push({ ...base, blocker });
          continue;
        }
        const upstream = resolveContentUnitForPromptRef22(input3.index, ref);
        if (!upstream || upstream.id === void 0) {
          const blocker = {
            code: "upstream_content_unit_not_found",
            ref: ref.raw,
            message: `prompt input has no ${ref.kind}_ref content unit: ${ref.raw}`
          };
          blockers.push(blocker);
          resolvedRefs.push({ ...base, blocker });
          continue;
        }
        const upstreamRef = entityDir82(upstream.path);
        if (upstreamRef === contentUnitRef) {
          const blocker = {
            code: "prompt_dependency_cycle",
            ref: ref.raw,
            content_unit_ref: upstreamRef,
            content_unit_id: upstream.id,
            message: `prompt input forms a dependency cycle: ${ref.raw}`
          };
          blockers.push(blocker);
          resolvedRefs.push({
            ...base,
            upstream_content_unit_ref: upstreamRef,
            upstream_content_unit_id: upstream.id,
            blocker
          });
          continue;
        }
        const decision = await decisionFor2(input3.decisionProvider, decisionCache, upstream.id, upstreamRef);
        const decisionBlocker = blockerForDecision2(ref, upstream, upstreamRef, decision);
        if (decisionBlocker) {
          blockers.push(decisionBlocker);
          resolvedRefs.push({
            ...base,
            upstream_content_unit_ref: upstreamRef,
            upstream_content_unit_id: upstream.id,
            blocker: decisionBlocker
          });
          continue;
        }
        const resourceId = promptInputResourceId2(decision);
        if (resourceId === void 0) {
          const blocker = {
            code: "upstream_resource_missing",
            ref: ref.raw,
            content_unit_ref: upstreamRef,
            content_unit_id: upstream.id,
            message: `prompt input selected content unit has no resource_id: ${ref.raw}`
          };
          blockers.push(blocker);
          resolvedRefs.push({
            ...base,
            upstream_content_unit_ref: upstreamRef,
            upstream_content_unit_id: upstream.id,
            blocker
          });
          continue;
        }
        resolvedRefs.push({
          ...base,
          upstream_content_unit_ref: upstreamRef,
          upstream_content_unit_id: upstream.id,
          resource_id: resourceId,
          replacement: resourceToken2(resourceId, ref)
        });
      }
      const replacements = resolvedRefs.flatMap((ref) => {
        if (ref.resource_id === void 0 || !ref.replacement) return [];
        return [{
          ref: ref.raw,
          field: ref.source.field,
          resource_id: ref.resource_id,
          token: ref.replacement
        }];
      });
      const structured = normalizedPromptStructured2(editPrompt?.structured, contentUnitType22, outputKind);
      const structuredText = structuredPromptText2(structured, contentUnitType22, outputKind);
      const referenceAssets = referenceAssetsForResolvedRefs2(resolvedRefs);
      const compiledText = compiledPromptTextWithStructured2({
        text: compilePromptText2(stringField172(editPrompt?.text), resolvedRefs, "edit_prompt.text"),
        structuredText: compilePromptText2(structuredText, resolvedRefs, "edit_prompt.structured")
      });
      const prompt = pruneUndefined172({
        schema: "movscript.backend_prompt.v1",
        content_unit_ref: contentUnitRef,
        content_unit_id: input3.contentUnit.id,
        content_unit_type: contentUnitType22,
        output_kind: outputKind,
        text: compiledText,
        negative_text: compilePromptText2(stringField172(editPrompt?.negative_text), resolvedRefs, "edit_prompt.negative_text"),
        notes: compilePromptText2(stringField172(editPrompt?.notes), resolvedRefs, "edit_prompt.notes"),
        structured,
        structured_text: structuredText,
        style_reference_resource_ids: styleReferenceResourceIds.length > 0 ? styleReferenceResourceIds : void 0,
        resource_ids: uniqueIds22([
          ...replacements.map((replacement) => replacement.resource_id),
          ...styleReferenceResourceIds
        ]),
        reference_assets: referenceAssets.length > 0 ? referenceAssets : void 0,
        replacements,
        refs: resolvedRefs,
        blockers: blockers.length > 0 ? dedupeBlockers22(blockers) : void 0
      });
      if (blockers.length > 0) {
        return { ok: false, prompt, blockers: dedupeBlockers22(blockers) };
      }
      return { ok: true, prompt };
    }
    function contentUnitEditPrompt2(value, promptText) {
      const base = recordField62(value);
      if (promptText === void 0) return base;
      return {
        ...base ?? {},
        text: promptText
      };
    }
    function parseContentUnitEditPromptRefs32(editPrompt) {
      const prompt = recordField62(editPrompt);
      const structuredText = structuredPromptTextFromUnknown22(prompt?.structured);
      return [
        ...parsePromptRefsFromText32(stringField172(prompt?.text), "edit_prompt.text"),
        ...parsePromptRefsFromText32(stringField172(prompt?.negative_text), "edit_prompt.negative_text"),
        ...parsePromptRefsFromText32(stringField172(prompt?.notes), "edit_prompt.notes"),
        ...parsePromptRefsFromText32(structuredText, "edit_prompt.structured")
      ];
    }
    function parseUnsupportedContentUnitEditPromptRefs22(editPrompt) {
      const prompt = recordField62(editPrompt);
      const structuredText = structuredPromptTextFromUnknown22(prompt?.structured);
      return [
        ...parseUnsupportedPromptRefsFromText22(stringField172(prompt?.text), "edit_prompt.text"),
        ...parseUnsupportedPromptRefsFromText22(stringField172(prompt?.negative_text), "edit_prompt.negative_text"),
        ...parseUnsupportedPromptRefsFromText22(stringField172(prompt?.notes), "edit_prompt.notes"),
        ...parseUnsupportedPromptRefsFromText22(structuredText, "edit_prompt.structured")
      ];
    }
    function parsePromptRefsFromText32(text, field) {
      if (!text) return [];
      const refs = [];
      for (const match of text.matchAll(PROMPT_REF_PATTERN32)) {
        const kind = promptRefKind32(match[1]);
        const payload = parsePromptRefPayload2(match[2]);
        if (!kind || !payload.id) continue;
        refs.push({
          kind,
          id: payload.id,
          raw: match[0],
          ...payload.role ? { role: payload.role } : {},
          ...payload.mediaType ? { media_type: payload.mediaType } : {},
          source: {
            field,
            start: match.index,
            end: match.index === void 0 ? void 0 : match.index + match[0].length
          }
        });
      }
      return refs;
    }
    function parseUnsupportedPromptRefsFromText22(text, field) {
      if (!text) return [];
      const refs = [];
      for (const match of text.matchAll(PROMPT_REF_PATTERN32)) {
        const kindValue = match[1]?.trim();
        const id = parsePromptRefPayload2(match[2]).id;
        if (!kindValue || !id || promptRefKind32(kindValue)) continue;
        refs.push({
          kind: kindValue,
          id,
          raw: match[0],
          source: {
            field,
            start: match.index,
            end: match.index === void 0 ? void 0 : match.index + match[0].length
          }
        });
      }
      return refs;
    }
    function parsePromptRefPayload2(value) {
      const parts = String(value ?? "").trim().split(/\s+/).filter(Boolean);
      const id = parts.shift() ?? "";
      let role = "";
      let mediaType = "";
      for (const part of parts) {
        const match = part.match(/^([a-zA-Z_][a-zA-Z0-9_-]*)=(.+)$/);
        if (!match) continue;
        const key = normalizePromptRefMetadataPart2(match[1]);
        const metadataValue = normalizePromptRefMetadataPart2(match[2]);
        if (!metadataValue) continue;
        if (key === "role") role = metadataValue;
        if (key === "media" || key === "media_type" || key === "mediatype") mediaType = metadataValue;
      }
      return {
        id,
        ...role ? { role } : {},
        ...mediaType ? { mediaType } : {}
      };
    }
    function normalizePromptRefMetadataPart2(value) {
      return String(value ?? "").trim().toLowerCase().replace(/^['"]|['"]$/g, "").replace(/[^a-z0-9_-]+/g, "_").replace(/^_+|_+$/g, "");
    }
    function normalizedPromptStructured2(value, contentUnitType22, outputKind) {
      const record = recordField62(value);
      if (!record) return void 0;
      const shotPlan = shouldCompileSceneMomentShotPlan2(contentUnitType22, outputKind) ? normalizedShotPlan2(record.shot_plan ?? record.shotPlan ?? record.shots) : [];
      const passthrough = pruneUndefined172({
        ...record,
        ...shotPlan.length > 0 ? { shot_plan: shotPlan } : {}
      });
      return Object.keys(passthrough).length > 0 ? passthrough : void 0;
    }
    function structuredPromptTextFromUnknown22(value) {
      const record = recordField62(value);
      if (!record) return void 0;
      const shotPlan = normalizedShotPlan2(record.shot_plan ?? record.shotPlan ?? record.shots);
      if (shotPlan.length > 0) return renderShotPlanText2(shotPlan);
      const json = JSON.stringify(record);
      return json === "{}" ? void 0 : json;
    }
    function structuredPromptText2(structured, contentUnitType22, outputKind) {
      if (!structured || !shouldCompileSceneMomentShotPlan2(contentUnitType22, outputKind)) return void 0;
      const shotPlan = Array.isArray(structured.shot_plan) ? normalizedShotPlan2(structured.shot_plan) : [];
      return shotPlan.length > 0 ? renderShotPlanText2(shotPlan) : void 0;
    }
    function compiledPromptTextWithStructured2(input3) {
      const parts = [input3.text, input3.structuredText].filter((part) => Boolean(part && part.trim()));
      return parts.length > 0 ? parts.join("\n\n") : void 0;
    }
    function shouldCompileSceneMomentShotPlan2(contentUnitType22, outputKind) {
      return outputKind === "video" && (contentUnitType22 === "scene_moment_ref" || contentUnitType22 === "scence_moment_ref");
    }
    function normalizedShotPlan2(value) {
      if (!Array.isArray(value)) return [];
      return value.flatMap((item, index) => {
        const record = recordField62(item);
        if (!record) return [];
        const normalized = pruneUndefined172({
          order: positiveNumber23(record.order ?? record.index ?? record.shot_number ?? record.shotNumber) ?? index + 1,
          title: stringField172(record.title ?? record.name),
          duration_sec: positiveNumber23(record.duration_sec ?? record.durationSec ?? record.duration),
          action: stringField172(record.action ?? record.description ?? record.intent),
          result: stringField172(record.result ?? record.end_state ?? record.endState),
          dialogue: stringField172(record.dialogue),
          narration: stringField172(record.narration),
          shot_size: stringField172(record.shot_size ?? record.shotSize ?? record.shot_type ?? record.shotType),
          camera_angle: stringField172(record.camera_angle ?? record.cameraAngle ?? record.angle),
          camera_motion: stringField172(record.camera_motion ?? record.cameraMotion ?? record.movement),
          lighting: stringField172(record.lighting ?? record.lighting_style ?? record.lightingStyle),
          depth_of_field: stringField172(record.depth_of_field ?? record.depthOfField ?? record.dof),
          composition: stringField172(record.composition),
          transition: stringField172(record.transition),
          notes: stringField172(record.notes ?? record.note)
        });
        const hasCreativeContent = Boolean(
          normalized.title || normalized.action || normalized.result || normalized.dialogue || normalized.narration || normalized.shot_size || normalized.camera_angle || normalized.camera_motion || normalized.lighting || normalized.depth_of_field || normalized.composition || normalized.transition || normalized.notes
        );
        return hasCreativeContent ? [normalized] : [];
      });
    }
    function renderShotPlanText2(shotPlan) {
      const lines = [
        "LOCKED SCENE MOMENT SHOT PLAN:",
        "Generate this scene moment as one continuous video while preserving the following shot order and cinematic parameters. Do not drop, merge, reorder, or contradict non-empty shot fields."
      ];
      for (const shot of shotPlan) {
        const label = `Shot ${shot.order ?? "?"}${shot.title ? ` - ${shot.title}` : ""}`;
        const fields = [
          label,
          shot.duration_sec !== void 0 ? `duration=${shot.duration_sec}s` : void 0,
          shot.action ? `action=${shot.action}` : void 0,
          shot.result ? `result=${shot.result}` : void 0,
          shot.dialogue ? `dialogue=${shot.dialogue}` : void 0,
          shot.narration ? `narration=${shot.narration}` : void 0,
          shot.shot_size ? `shot_size=${shot.shot_size}` : void 0,
          shot.camera_angle ? `camera_angle=${shot.camera_angle}` : void 0,
          shot.camera_motion ? `camera_motion=${shot.camera_motion}` : void 0,
          shot.lighting ? `lighting=${shot.lighting}` : void 0,
          shot.depth_of_field ? `depth_of_field=${shot.depth_of_field}` : void 0,
          shot.composition ? `composition=${shot.composition}` : void 0,
          shot.transition ? `transition=${shot.transition}` : void 0,
          shot.notes ? `notes=${shot.notes}` : void 0
        ].filter((field) => Boolean(field));
        lines.push(`- ${fields.join("; ")}`);
      }
      return lines.join("\n");
    }
    function failedPrompt2(input3) {
      return {
        ok: false,
        blockers: input3.blockers,
        prompt: {
          schema: "movscript.backend_prompt.v1",
          content_unit_ref: input3.contentUnitRef,
          ...input3.contentUnitId !== void 0 ? { content_unit_id: input3.contentUnitId } : {},
          content_unit_type: input3.contentUnitType,
          output_kind: input3.outputKind,
          resource_ids: [],
          replacements: [],
          refs: [],
          blockers: input3.blockers
        }
      };
    }
    function unsupportedPromptRefBlocker2(ref) {
      return {
        code: "unsupported_prompt_ref_kind",
        ref: ref.raw,
        message: `unsupported prompt ref kind "${ref.kind}": ${ref.raw}. Use system primitive, content_unit, candidate, or resource refs; namespace vocabulary is context, not a selected-resource dependency.`
      };
    }
    async function decisionFor2(provider, cache, contentUnitId, contentUnitRef) {
      const key = String(contentUnitId);
      const cached = cache.get(key);
      if (cached) return cached;
      const next = provider.getContentUnitDecision({ contentUnitId, contentUnitRef });
      cache.set(key, next);
      return next;
    }
    function blockerForDecision2(ref, upstream, upstreamRef, decision) {
      if (!decision) {
        return {
          code: "decision_context_missing",
          ref: ref.raw,
          content_unit_ref: upstreamRef,
          content_unit_id: upstream.id,
          message: `prompt input content unit has no backend decision context: ${ref.raw}`
        };
      }
      const selection = selectedDecision2(decision);
      if (!selection) {
        const candidates = Array.isArray(decision.candidates) ? decision.candidates : [];
        if (latestResourceCandidate2(candidates)) return void 0;
        return candidates.length > 0 ? {
          code: "upstream_resource_missing",
          ref: ref.raw,
          content_unit_ref: upstreamRef,
          content_unit_id: upstream.id,
          message: `prompt input latest backend candidate has no resource_id: ${ref.raw}`
        } : {
          code: "upstream_selection_missing",
          ref: ref.raw,
          content_unit_ref: upstreamRef,
          content_unit_id: upstream.id,
          message: `prompt input content unit has no selected backend candidate: ${ref.raw}`
        };
      }
      if (selection.stale === true && stalePolicy2(selection) !== "accept_stale") {
        return {
          code: "upstream_selection_stale",
          ref: ref.raw,
          content_unit_ref: upstreamRef,
          content_unit_id: upstream.id,
          message: `prompt input selected backend candidate is stale: ${ref.raw}`
        };
      }
      const candidateId = idField122(selection.candidate_id);
      if (candidateId !== void 0 && Array.isArray(decision.candidates)) {
        const candidate = decision.candidates.find((item) => sameId32(item.id, candidateId));
        if (!candidate) {
          return {
            code: "upstream_candidate_missing",
            ref: ref.raw,
            content_unit_ref: upstreamRef,
            content_unit_id: upstream.id,
            message: `prompt input selected backend candidate is missing: ${ref.raw}`
          };
        }
      }
      if (promptInputResourceId2(decision) === void 0) {
        return {
          code: "upstream_resource_missing",
          ref: ref.raw,
          content_unit_ref: upstreamRef,
          content_unit_id: upstream.id,
          message: `prompt input selected backend candidate has no resource_id: ${ref.raw}`
        };
      }
      return void 0;
    }
    async function resolveDirectPromptResourceRef2(input3) {
      const { ref, role } = input3;
      if (ref.kind === "resource") {
        const resourceId2 = resourceIdField52(ref.id);
        if (resourceId2 === void 0) {
          const blocker = {
            code: "ref_not_found",
            ref: ref.raw,
            resource_ref: ref.id,
            message: `prompt resource ref does not resolve: ${ref.raw}`
          };
          return { ...ref, role, blocker };
        }
        return {
          ...ref,
          role,
          resolved: {
            entityKind: "resource",
            id: resourceId2
          },
          resource_id: resourceId2,
          replacement: resourceToken2(resourceId2, ref)
        };
      }
      if (ref.kind !== "candidate") return void 0;
      if (input3.contentUnit.id === void 0) {
        const blocker = {
          code: "content_unit_id_missing",
          ref: ref.raw,
          content_unit_ref: input3.contentUnitRef,
          message: `content unit is missing id for candidate ref: ${ref.raw}`
        };
        return { ...ref, role, blocker };
      }
      const decision = await decisionFor2(
        input3.decisionProvider,
        input3.decisionCache,
        input3.contentUnit.id,
        input3.contentUnitRef
      );
      if (!decision) {
        const blocker = {
          code: "decision_context_missing",
          ref: ref.raw,
          content_unit_ref: input3.contentUnitRef,
          content_unit_id: input3.contentUnit.id,
          message: `prompt candidate ref has no backend decision context: ${ref.raw}`
        };
        return { ...ref, role, blocker };
      }
      const candidate = Array.isArray(decision.candidates) ? decision.candidates.find((item) => sameId32(item.id, ref.id)) : void 0;
      if (!candidate) {
        const blocker = {
          code: "upstream_candidate_missing",
          ref: ref.raw,
          content_unit_ref: input3.contentUnitRef,
          content_unit_id: input3.contentUnit.id,
          message: `prompt candidate ref is missing: ${ref.raw}`
        };
        return { ...ref, role, blocker };
      }
      const resourceId = firstCandidateResourceId22(candidate);
      if (resourceId === void 0) {
        const blocker = {
          code: "upstream_resource_missing",
          ref: ref.raw,
          content_unit_ref: input3.contentUnitRef,
          content_unit_id: input3.contentUnit.id,
          message: `prompt candidate ref has no resource_id: ${ref.raw}`
        };
        return { ...ref, role, blocker };
      }
      return {
        ...ref,
        role,
        resolved: {
          entityKind: "candidate",
          id: ref.id
        },
        upstream_content_unit_ref: input3.contentUnitRef,
        upstream_content_unit_id: input3.contentUnit.id,
        resource_id: resourceId,
        replacement: resourceToken2(resourceId, ref)
      };
    }
    function selectedDecision2(decision) {
      const selection = recordField62(decision.selection);
      return selection && Object.keys(selection).length > 0 ? selection : void 0;
    }
    function promptInputResourceId2(decision) {
      if (decision && selectedDecision2(decision)) return selectedResourceId2(decision);
      return firstCandidateResourceId22(latestResourceCandidate2(decision?.candidates));
    }
    function selectedResourceId2(decision) {
      const selection = decision ? selectedDecision2(decision) : void 0;
      const direct = resourceIdField52(selection?.resource_id);
      if (direct !== void 0) return direct;
      const candidateId = idField122(selection?.candidate_id);
      if (candidateId === void 0 || !Array.isArray(decision?.candidates)) return void 0;
      const candidate = decision.candidates.find((item) => sameId32(item.id, candidateId));
      return firstCandidateResourceId22(candidate);
    }
    function firstCandidateResourceId22(candidate) {
      const output3 = arrayField62(candidate?.outputs).filter(isRecord282)[0];
      return resourceIdField52(output3?.resource_id);
    }
    function latestResourceCandidate2(candidates) {
      const resourceCandidates = Array.isArray(candidates) ? candidates.filter((candidate) => firstCandidateResourceId22(candidate) !== void 0) : [];
      if (resourceCandidates.length === 0) return void 0;
      const withTimestamp = resourceCandidates.map((candidate, index) => ({
        candidate,
        index,
        timestamp: candidateTimestamp2(candidate)
      }));
      return withTimestamp.sort((left, right) => {
        if (left.timestamp !== right.timestamp) return right.timestamp - left.timestamp;
        return right.index - left.index;
      })[0]?.candidate;
    }
    function candidateTimestamp2(candidate) {
      const raw = stringField172(candidate.updated_at ?? candidate.updatedAt ?? candidate.created_at ?? candidate.createdAt);
      if (!raw) return 0;
      const parsed = Date.parse(raw);
      return Number.isFinite(parsed) ? parsed : 0;
    }
    function compilePromptText2(text, refs, field) {
      if (!text) return void 0;
      const replacements = refs.filter((ref) => ref.source.field === field && ref.replacement).sort((left, right) => (right.source.start ?? 0) - (left.source.start ?? 0));
      let output3 = text;
      for (const ref of replacements) {
        if (ref.source.start === void 0 || ref.source.end === void 0 || !ref.replacement) {
          output3 = output3.split(ref.raw).join(ref.replacement);
          continue;
        }
        output3 = `${output3.slice(0, ref.source.start)}${ref.replacement}${output3.slice(ref.source.end)}`;
      }
      return output3;
    }
    function resolvePromptRefEntity22(index, ref) {
      if (ref.kind === "candidate" || ref.kind === "resource") return void 0;
      if (ref.kind === "content_unit") {
        return queryMovScriptWorkspaceEntities2(index, { entityKind: "content_unit" }).find((entity) => entity.id !== void 0 && sameEntityRef3(entity.id, ref.id, "content_unit") || sameEntityRef3(entityDir82(entity.path).split("/").pop(), ref.id, "content_unit"));
      }
      return findEntityByRef32(index, ref.kind, ref.id);
    }
    function resolveContentUnitForPromptRef22(index, ref) {
      if (ref.kind === "candidate" || ref.kind === "resource") return void 0;
      if (ref.kind === "content_unit") return resolvePromptRefEntity22(index, ref);
      const kind = ref.kind;
      const expectedTypes = contentUnitTypesForPromptRefKind32(kind);
      return queryMovScriptWorkspaceEntities2(index, { entityKind: "content_unit" }).find((entity) => {
        if (!expectedTypes.includes(String(entity.record.content_unit_type ?? ""))) return false;
        return primaryContentUnitRefs22(entity, kind).some((candidate) => samePromptRefId22(candidate.id, ref.id, kind));
      });
    }
    function primaryContentUnitRefs22(contentUnit, kind) {
      return flatPrimaryRefIds2(contentUnit.record, kind).map((id) => ({ kind, id }));
    }
    function flatPrimaryRefIds2(record, kind) {
      return primaryRefIdsForContentUnitRecord4(record, kind);
    }
    function hasAmbiguousPrimaryRefs22(refs, kind) {
      const unique = [];
      for (const ref of refs) {
        if (unique.some((item) => samePromptRefId22(item.id, ref.id, kind))) continue;
        unique.push(ref);
      }
      return unique.length > 1;
    }
    function samePromptRefId22(left, right, kind) {
      return String(left) === String(right) || lastPathSegment52(left) === String(right) || lastPathSegment52(right) === String(left) || sameEntityRef3(left, right, kind);
    }
    function lastPathSegment52(value) {
      if (typeof value !== "string" || !value.includes("/")) return void 0;
      return value.split("/").filter(Boolean).at(-1);
    }
    function findEntityByRef32(index, entityKind, ref) {
      const value = idField122(ref);
      if (value === void 0) return void 0;
      const normalized = typeof value === "string" ? value.replace(/\/+$/, "") : String(value);
      return queryMovScriptWorkspaceEntities2(index, { entityKind }).find((entity) => {
        const dir = entityDir82(entity.path);
        return dir === normalized || entity.path === `${normalized}/${entityKind}.json` || sameEntityRef3(entity.id, value, entityKind);
      });
    }
    function promptRefKind32(value) {
      if (value === "candidate" || value === "resource") return value;
      return isContentUnitPromptRefKind2(value) ? value : void 0;
    }
    function primaryRefKindForContentUnitType32(contentUnitType22) {
      return primaryRefKindForContentUnitType4(contentUnitType22);
    }
    function contentUnitTypesForPromptRefKind32(kind) {
      return contentUnitTypesForPromptRefKind4(kind);
    }
    function contentUnitOutputKind32(value) {
      return outputKindForContentUnitType2("", value);
    }
    function usesVisualStyleReferences22(outputKind) {
      return outputKind === "image" || outputKind === "video";
    }
    function projectStyleReferenceResourceIds22(index) {
      const standards = queryMovScriptWorkspaceEntities2(index, { entityKind: "project_standards", limit: 1 })[0];
      if (!standards) return [];
      return uniqueIds22([
        ...resourceIdsFromValue32(standards.record.style_reference_images),
        ...resourceIdsFromValue32(standards.record.style_references),
        ...resourceIdsFromValue32(standards.record.reference_resource_ids),
        ...arrayField62(standards.record.custom_rules).filter(isRecord282).filter((rule) => rule.enabled !== false).filter((rule) => stringField172(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue32(rule.value))
      ]);
    }
    function resourceIdsFromValue32(value) {
      if (Array.isArray(value)) return value.flatMap(resourceIdsFromValue32);
      const text = stringField172(value);
      if (text) {
        const ids = [];
        const patterns = [
          /resource#([0-9]+)/gi,
          /reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi,
          /style_reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi
        ];
        for (const pattern of patterns) {
          for (const match of text.matchAll(pattern)) {
            for (const part of String(match[1] ?? "").split(",")) {
              const parsed2 = Number(part.trim());
              if (Number.isInteger(parsed2) && parsed2 > 0) ids.push(parsed2);
            }
          }
        }
        if (ids.length > 0) return ids;
        const parsed = Number(text);
        return Number.isInteger(parsed) && parsed > 0 ? [parsed] : [];
      }
      const id = resourceIdField52(value);
      if (id !== void 0) return [id];
      if (isRecord282(value)) return resourceIdsFromValue32(value.resource_id ?? value.resourceId ?? value.id);
      return [];
    }
    function referenceAssetsForResolvedRefs2(refs) {
      const seen = /* @__PURE__ */ new Set();
      const output3 = [];
      for (const ref of refs) {
        if (ref.resource_id === void 0) continue;
        const mediaType = promptRefMediaTypeForReference2(ref);
        const role = promptRefRoleForReference2(ref, mediaType);
        const key = `${String(ref.resource_id)}:${mediaType}:${role}`;
        if (seen.has(key)) continue;
        seen.add(key);
        output3.push({
          resource_id: ref.resource_id,
          media_type: mediaType,
          role,
          source_ref: ref.raw
        });
      }
      return output3;
    }
    function resourceToken2(resourceId, ref) {
      const mediaType = ref?.media_type;
      const role = ref?.role && ref.role !== "input" ? ref.role : void 0;
      return formatResourceMention2(resourceId, {
        ...mediaType ? { mediaType } : {},
        ...role ? { role } : {}
      });
    }
    function promptRefRoleForReference2(ref, mediaType) {
      if (ref.role && ref.role !== "input") return ref.role;
      if (mediaType === "video") return "reference_video";
      if (mediaType === "audio") return "reference_audio";
      return "reference_image";
    }
    function promptRefMediaTypeForReference2(ref) {
      if (ref.media_type) return ref.media_type;
      if (ref.role === "reference_video" || ref.role === "motion_reference" || ref.role === "source_video") return "video";
      if (ref.role === "reference_audio" || ref.role === "source_audio") return "audio";
      return "image";
    }
    function entityDir82(path) {
      return path.replace(/\/[^/]+$/, "");
    }
    function stalePolicy2(selection) {
      return stringField172(selection.stale_policy) ?? "strict";
    }
    function sameId32(left, right) {
      const leftId = idField122(left);
      const rightId = idField122(right);
      return leftId !== void 0 && rightId !== void 0 && String(leftId) === String(rightId);
    }
    function uniqueIds22(values) {
      const seen = /* @__PURE__ */ new Set();
      const output3 = [];
      for (const value of values) {
        const key = String(value);
        if (seen.has(key)) continue;
        seen.add(key);
        output3.push(value);
      }
      return output3;
    }
    function dedupeBlockers22(blockers) {
      const seen = /* @__PURE__ */ new Set();
      const output3 = [];
      for (const blocker of blockers) {
        const key = `${blocker.code}:${blocker.ref ?? ""}:${blocker.content_unit_ref ?? ""}:${blocker.message}`;
        if (seen.has(key)) continue;
        seen.add(key);
        output3.push(blocker);
      }
      return output3;
    }
    function recordField62(value) {
      return isRecord282(value) ? value : void 0;
    }
    function arrayField62(value) {
      return Array.isArray(value) ? value : [];
    }
    function stringField172(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function positiveNumber23(value) {
      if (typeof value === "number" && Number.isFinite(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value.trim());
        if (Number.isFinite(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function idField122(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function resourceIdField52(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function isRecord282(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function pruneUndefined172(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    function createMovScriptEngine2(options) {
      const workspaceService = options.workspaceService;
      const overviewWorkspace = options.overviewWorkspace;
      const inspectWorkspace = options.inspectWorkspace;
      const reviewWorkspace = options.reviewWorkspace;
      const interpretWorkspace = options.interpretWorkspace;
      const productionWorkPlan = options.productionWorkPlan;
      const regenerationPlan = options.regenerationPlan;
      const deriveContentUnitArtifact22 = options.deriveContentUnitArtifact;
      const buildContentUnitBackendPrompt22 = options.buildContentUnitBackendPrompt;
      const deriveArtifacts = options.deriveArtifacts;
      return {
        workspaceService,
        initProject: workspaceService.initializeProject.bind(workspaceService),
        initializeProject: workspaceService.initializeProject.bind(workspaceService),
        getModel: workspaceService.getModel.bind(workspaceService),
        loadIndex: workspaceService.loadIndex.bind(workspaceService),
        queryEntities: workspaceService.queryEntities.bind(workspaceService),
        querySettings: workspaceService.querySettings.bind(workspaceService),
        queryAssets: workspaceService.queryAssets.bind(workspaceService),
        queryProductionContext: workspaceService.queryProductionContext.bind(workspaceService),
        upsertSetting: workspaceService.upsertSetting.bind(workspaceService),
        upsertSettingState: workspaceService.upsertSettingState.bind(workspaceService),
        upsertAsset: workspaceService.upsertAsset.bind(workspaceService),
        upsertContentUnit: workspaceService.upsertContentUnit.bind(workspaceService),
        saveProductionSnapshot: workspaceService.saveProductionSnapshot.bind(workspaceService),
        deleteEntity: workspaceService.deleteEntity.bind(workspaceService),
        createSetting(input3) {
          return saveSetting2(workspaceService, input3);
        },
        updateSetting(input3) {
          return saveSetting2(workspaceService, input3);
        },
        createSettingState(input3) {
          return saveSettingState2(workspaceService, input3);
        },
        updateSettingState(input3) {
          return saveSettingState2(workspaceService, input3);
        },
        createAsset(input3) {
          return saveAsset2(workspaceService, input3);
        },
        updateAsset(input3) {
          return saveAsset2(workspaceService, input3);
        },
        writeHierarchyNode(input3) {
          return writeHierarchyNode2(workspaceService, input3);
        },
        updateEntityBasics(input3) {
          return updateEntityBasics2(workspaceService, input3);
        },
        connectSceneMomentSetting(input3) {
          return connectSceneMomentSetting2(workspaceService, input3);
        },
        listProductions(input3 = {}) {
          return workspaceService.queryEntities({ entityKind: "production", query: input3.query, limit: input3.limit });
        },
        createProduction(input3 = {}) {
          return saveProduction2(workspaceService, input3);
        },
        updateProduction(input3) {
          return saveProduction2(workspaceService, input3);
        },
        deleteProduction(input3) {
          return deletePlanningEntity2(workspaceService, "production", input3);
        },
        listSegments(input3 = {}) {
          return workspaceService.queryEntities(planningQuery2("segment", input3));
        },
        createSegment(input3) {
          return saveSegment2(workspaceService, input3);
        },
        updateSegment(input3) {
          return saveSegment2(workspaceService, input3);
        },
        deleteSegment(input3) {
          return deletePlanningEntity2(workspaceService, "segment", input3);
        },
        listSceneMoments(input3 = {}) {
          return workspaceService.queryEntities(planningQuery2("scene_moment", input3));
        },
        createSceneMoment(input3) {
          return saveSceneMoment2(workspaceService, input3);
        },
        updateSceneMoment(input3) {
          return saveSceneMoment2(workspaceService, input3);
        },
        deleteSceneMoment(input3) {
          return deletePlanningEntity2(workspaceService, "scene_moment", input3);
        },
        listStoryboards(input3 = {}) {
          return workspaceService.queryEntities(planningQuery2("storyboard", input3));
        },
        createStoryboard(input3) {
          return saveStoryboard2(workspaceService, input3);
        },
        updateStoryboard(input3) {
          return saveStoryboard2(workspaceService, input3);
        },
        deleteStoryboard(input3) {
          return deletePlanningEntity2(workspaceService, "storyboard", input3);
        },
        listKeyframes(input3 = {}) {
          return workspaceService.queryEntities(planningQuery2("keyframe", input3));
        },
        createKeyframe(input3) {
          return saveKeyframe2(workspaceService, input3);
        },
        updateKeyframe(input3) {
          return saveKeyframe2(workspaceService, input3);
        },
        deleteKeyframe(input3) {
          return deletePlanningEntity2(workspaceService, "keyframe", input3);
        },
        listAudioCues(input3 = {}) {
          return workspaceService.queryEntities(planningQuery2("audio_cue", input3));
        },
        createAudioCue(input3) {
          return saveAudioCue2(workspaceService, input3);
        },
        updateAudioCue(input3) {
          return saveAudioCue2(workspaceService, input3);
        },
        deleteAudioCue(input3) {
          return deletePlanningEntity2(workspaceService, "audio_cue", input3);
        },
        listExpressionUnits(input3 = {}) {
          return workspaceService.queryEntities(planningQuery2("expression_unit", input3));
        },
        createExpressionUnit(input3) {
          return saveExpressionUnit2(workspaceService, input3);
        },
        updateExpressionUnit(input3) {
          return saveExpressionUnit2(workspaceService, input3);
        },
        deleteExpressionUnit(input3) {
          return deletePlanningEntity2(workspaceService, "expression_unit", input3);
        },
        listContentUnits(input3 = {}) {
          return listContentUnits2(workspaceService, input3);
        },
        createContentUnit(input3) {
          return saveContentUnit2(workspaceService, input3);
        },
        updateContentUnit(input3) {
          return saveContentUnit2(workspaceService, input3);
        },
        ensureContentUnitForEntity(input3) {
          return ensureContentUnitForEntity2(workspaceService, input3);
        },
        updateContentUnitEditPrompt: workspaceService.updateContentUnitEditPrompt.bind(workspaceService),
        deleteContentUnit(input3) {
          return deletePlanningEntity2(workspaceService, "content_unit", input3);
        },
        deriveContentUnitArtifact(contentUnitId) {
          return callRequiredOperation2(
            deriveContentUnitArtifact22 ? () => deriveContentUnitArtifact22(contentUnitId) : void 0,
            "deriveContentUnitArtifact"
          );
        },
        buildContentUnitBackendPrompt(contentUnitId, options2) {
          return callRequiredOperation2(
            buildContentUnitBackendPrompt22 ? () => buildContentUnitBackendPrompt22(contentUnitId, options2) : void 0,
            "buildContentUnitBackendPrompt"
          );
        },
        deriveArtifacts(input3 = {}) {
          return callRequiredOperation2(
            deriveArtifacts ? () => deriveArtifacts(input3) : void 0,
            "deriveArtifacts"
          );
        },
        overview() {
          return callRequiredOperation2(overviewWorkspace, "overview");
        },
        inspect(input3 = {}) {
          const operation = inspectWorkspace ?? reviewWorkspace;
          return callRequiredOperation2(operation ? () => operation(input3) : void 0, "inspect");
        },
        review(input3 = {}) {
          const operation = inspectWorkspace ?? reviewWorkspace;
          return callRequiredOperation2(operation ? () => operation(input3) : void 0, "review");
        },
        interpret() {
          return callRequiredOperation2(interpretWorkspace, "interpret");
        },
        productionWorkPlan() {
          return callRequiredOperation2(productionWorkPlan, "productionWorkPlan");
        },
        regenerationPlan() {
          return callRequiredOperation2(regenerationPlan, "regenerationPlan");
        },
        publish(input3 = {}) {
          return callRequiredOperation2(
            options.publish ? () => options.publish(input3) : void 0,
            "publish"
          );
        },
        appendCandidate: workspaceService.appendCandidate.bind(workspaceService),
        createContentCandidate: workspaceService.createContentCandidate.bind(workspaceService),
        selectContentUnitCandidate: workspaceService.selectContentUnitCandidate.bind(workspaceService),
        createAssetSlotCandidate: workspaceService.createAssetSlotCandidate.bind(workspaceService),
        createKeyframeCandidate: workspaceService.createKeyframeCandidate.bind(workspaceService),
        selectCandidate: workspaceService.selectCandidate.bind(workspaceService),
        updateCandidate: workspaceService.updateCandidate.bind(workspaceService),
        unlockCandidate: workspaceService.unlockCandidate.bind(workspaceService)
      };
    }
    function planningQuery2(entityKind, input3 = {}) {
      return pruneUndefined18({
        entityKind,
        kind: input3.kind,
        query: input3.query,
        productionId: input3.productionId,
        segmentId: input3.segmentId,
        sceneMomentId: input3.sceneMomentId,
        limit: input3.limit
      });
    }
    async function saveSetting2(workspaceService, input3) {
      const id = await engineEntityId2(workspaceService, "setting", input3.id, input3.title);
      return workspaceService.upsertSetting({
        payload: pruneUndefined18({
          id,
          title: input3.title,
          setting_kind: input3.kind,
          namespace_kind: input3.namespaceKind ?? input3.settingNamespaceKind,
          setting_namespace_kind: input3.settingNamespaceKind ?? input3.namespaceKind,
          description: input3.description,
          alias: input3.alias,
          content: input3.content,
          importance: input3.importance
        })
      });
    }
    async function saveSettingState2(workspaceService, input3) {
      const id = await engineEntityId2(workspaceService, "setting_state", input3.id, input3.title);
      return workspaceService.upsertSettingState({
        payload: pruneUndefined18({
          id,
          setting_id: input3.settingId,
          title: input3.title,
          state_kind: input3.stateKind,
          namespace_kind: input3.namespaceKind ?? input3.settingNamespaceKind,
          setting_namespace_kind: input3.settingNamespaceKind ?? input3.namespaceKind,
          description: input3.description
        })
      });
    }
    async function saveAsset2(workspaceService, input3) {
      const id = await engineEntityId2(workspaceService, "asset", input3.id, input3.title ?? input3.slot);
      return workspaceService.upsertAsset({
        payload: pruneUndefined18({
          id,
          title: input3.title,
          setting_id: input3.settingId,
          setting_state_id: input3.settingStateId,
          slot: input3.slot,
          asset_kind: input3.assetKind,
          prompt_hint: input3.promptHint,
          resource_id: input3.resourceId
        })
      });
    }
    async function writeHierarchyNode2(workspaceService, input3) {
      const record = input3.record;
      const entityKind = stringValue112(record.kind) ?? entityKindFromPath22(input3.targetPath);
      if (!entityKind) throw new Error("hierarchy node kind is required");
      if (entityKind === "setting") {
        return workspaceService.upsertSetting({ entity: { path: input3.targetPath, record }, payload: record });
      }
      if (entityKind === "setting_state") {
        return workspaceService.upsertSettingState({ entity: { path: input3.targetPath, record }, payload: record });
      }
      if (entityKind === "asset") {
        return workspaceService.upsertAsset({ entity: { path: input3.targetPath, record }, payload: record });
      }
      if (entityKind === "content_unit") {
        return saveContentUnit2(workspaceService, contentUnitInputFromPatchedRecord2(record, {
          targetPath: input3.targetPath,
          record,
          title: stringValue112(record.title) ?? "Untitled",
          summary: stringValue112(record.description) ?? ""
        }));
      }
      return workspaceService.upsertSourceRecord({
        targetPath: input3.targetPath,
        record: pathPreservingHierarchyRecord2(input3.targetPath, record, entityKind)
      });
    }
    async function updateEntityBasics2(workspaceService, input3) {
      const record = input3.record ?? {};
      const entityKind = input3.entityKind ?? stringValue112(record.kind) ?? entityKindFromPath22(input3.targetPath);
      if (!entityKind) throw new Error("entityKind is required to update entity basics");
      const patched = patchEntityBasics2(record, input3);
      const entity = input3.targetPath ? { path: input3.targetPath, record } : void 0;
      if (entityKind === "setting") {
        return workspaceService.upsertSetting({ entity, record, payload: patched });
      }
      if (entityKind === "setting_state") {
        return workspaceService.upsertSettingState({ entity, record, payload: patched });
      }
      if (entityKind === "asset") {
        return workspaceService.upsertAsset({ entity, record, payload: patched });
      }
      if (entityKind === "content_unit") {
        return saveContentUnit2(workspaceService, contentUnitInputFromPatchedRecord2(patched, input3));
      }
      if (input3.targetPath) {
        return workspaceService.upsertSourceRecord({
          targetPath: input3.targetPath,
          record: pathPreservingHierarchyRecord2(input3.targetPath, patched, entityKind)
        });
      }
      if (entityKind === "production") {
        return saveProduction2(workspaceService, {
          id: input3.id ?? idValue5(record.id) ?? pathSegmentAfter42(input3.targetPath ?? "", "productions"),
          title: stringValue112(patched.title)
        });
      }
      if (entityKind === "segment") {
        return saveSegment2(workspaceService, {
          productionId: input3.productionId ?? pathSegmentAfter42(input3.targetPath ?? "", "productions"),
          id: input3.id ?? idValue5(record.id) ?? pathSegmentAfter42(input3.targetPath ?? "", "segments"),
          title: stringValue112(patched.title),
          summary: stringValue112(patched.summary ?? patched.description),
          kind: stringValue112(patched.segment_kind ?? patched.kind)
        });
      }
      if (entityKind === "scene_moment") {
        return saveSceneMoment2(workspaceService, {
          productionId: input3.productionId ?? pathSegmentAfter42(input3.targetPath ?? "", "productions"),
          segmentId: input3.segmentId ?? pathSegmentAfter42(input3.targetPath ?? "", "segments"),
          id: input3.id ?? idValue5(record.id) ?? pathSegmentAfter42(input3.targetPath ?? "", "scene_moments"),
          title: stringValue112(patched.title),
          actionText: stringValue112(patched.action_text ?? patched.action),
          description: stringValue112(patched.description),
          settings: settingRefsFromRecord3(patched)
        });
      }
      if (entityKind === "expression_unit") {
        return saveExpressionUnit2(workspaceService, {
          productionId: input3.productionId ?? pathSegmentAfter42(input3.targetPath ?? "", "productions"),
          segmentId: input3.segmentId ?? pathSegmentAfter42(input3.targetPath ?? "", "segments"),
          sceneMomentId: input3.sceneMomentId ?? idValue5(record.scene_moment_id) ?? pathSegmentAfter42(input3.targetPath ?? "", "scene_moments"),
          id: input3.id ?? idValue5(record.id) ?? pathSegmentAfter42(input3.targetPath ?? "", "expression_units"),
          title: stringValue112(patched.title),
          slotKind: stringValue112(patched.slot_kind ?? patched.slotKind),
          kind: stringValue112(patched.kind),
          text: stringValue112(patched.text ?? patched.summary ?? patched.description),
          intent: stringValue112(patched.intent)
        });
      }
      if (entityKind === "keyframe") {
        return saveKeyframe2(workspaceService, {
          productionId: input3.productionId ?? pathSegmentAfter42(input3.targetPath ?? "", "productions"),
          segmentId: input3.segmentId ?? pathSegmentAfter42(input3.targetPath ?? "", "segments"),
          sceneMomentId: input3.sceneMomentId ?? idValue5(record.scene_moment_id) ?? pathSegmentAfter42(input3.targetPath ?? "", "scene_moments"),
          expressionUnitId: input3.expressionUnitId ?? idValue5(record.expression_unit_id) ?? pathSegmentAfter42(input3.targetPath ?? "", "expression_units"),
          id: input3.id ?? idValue5(record.id) ?? pathSegmentAfter42(input3.targetPath ?? "", "keyframes"),
          title: stringValue112(patched.title),
          role: stringValue112(patched.role),
          visualIntent: stringValue112(patched.visual_intent ?? patched.description),
          timing: isRecord292(patched.timing) ? patched.timing : void 0,
          composition: isRecord292(patched.composition) ? patched.composition : void 0,
          continuity: isRecord292(patched.continuity) ? patched.continuity : void 0,
          referenceAssetRefs: Array.isArray(patched.reference_asset_refs) ? patched.reference_asset_refs : void 0,
          referenceKeyframeRefs: Array.isArray(patched.reference_keyframe_refs) ? patched.reference_keyframe_refs : void 0
        });
      }
      if (entityKind === "storyboard") {
        return saveStoryboard2(workspaceService, {
          productionId: input3.productionId ?? pathSegmentAfter42(input3.targetPath ?? "", "productions"),
          segmentId: input3.segmentId ?? pathSegmentAfter42(input3.targetPath ?? "", "segments"),
          sceneMomentId: input3.sceneMomentId ?? idValue5(record.scene_moment_id) ?? pathSegmentAfter42(input3.targetPath ?? "", "scene_moments"),
          expressionUnitId: input3.expressionUnitId ?? idValue5(record.expression_unit_id) ?? pathSegmentAfter42(input3.targetPath ?? "", "expression_units"),
          id: input3.id ?? idValue5(record.id) ?? pathSegmentAfter42(input3.targetPath ?? "", "storyboards"),
          title: stringValue112(patched.title),
          visualIntent: stringValue112(patched.visual_intent ?? patched.description),
          timeline: isRecord292(patched.timeline) ? patched.timeline : void 0,
          graph: isRecord292(patched.graph) ? patched.graph : void 0
        });
      }
      throw new Error(`Unsupported entity basics update kind: ${entityKind}`);
    }
    function connectSceneMomentSetting2(workspaceService, input3) {
      const sceneMomentRecord = input3.sceneMomentRecord ?? {};
      const sceneMomentId = requiredId3(
        input3.sceneMomentId ?? idValue5(sceneMomentRecord.id) ?? idValue5(sceneMomentRecord.client_id),
        "sceneMomentId"
      );
      const currentRefs = settingRefsFromRecord3(sceneMomentRecord);
      const settingId = requiredId3(input3.settingId, "settingId");
      const settingStateId = idValue5(input3.settingStateId);
      const alreadyLinked = currentRefs.some((ref) => String(idValue5(ref.id ?? ref.settingId) ?? "") === String(settingId) && String(idValue5(ref.settingStateId) ?? "") === String(settingStateId ?? ""));
      return saveSceneMoment2(workspaceService, {
        productionId: input3.productionId ?? pathSegmentAfter42(stringValue112(sceneMomentRecord.__workspace_path ?? sceneMomentRecord.workspace_path ?? "") ?? "", "productions"),
        segmentId: input3.segmentId ?? pathSegmentAfter42(stringValue112(sceneMomentRecord.__workspace_path ?? sceneMomentRecord.workspace_path ?? "") ?? "", "segments"),
        id: sceneMomentId,
        title: stringValue112(sceneMomentRecord.title),
        actionText: stringValue112(sceneMomentRecord.action_text ?? sceneMomentRecord.action),
        description: stringValue112(sceneMomentRecord.description),
        settings: alreadyLinked ? currentRefs : [
          ...currentRefs,
          {
            id: settingId,
            settingStateId,
            role: input3.role ?? "scene_constraint"
          }
        ]
      });
    }
    async function saveProduction2(workspaceService, input3) {
      const productionId = input3.id === void 0 && !stringValue112(input3.title) ? "main" : await engineEntityId2(workspaceService, "production", input3.id, input3.title);
      return workspaceService.saveProductionSnapshot({
        productionId,
        snapshot: {
          production: pruneUndefined18({ title: input3.title }),
          segments: []
        }
      });
    }
    async function saveSegment2(workspaceService, input3) {
      const segmentId = await engineEntityId2(workspaceService, "segment", input3.id, input3.title);
      return workspaceService.saveProductionSnapshot({
        productionId: input3.productionId ?? "main",
        snapshot: {
          segments: [pruneUndefined18({
            id: segmentId,
            title: input3.title,
            kind: input3.kind,
            summary: input3.summary,
            order: input3.order
          })]
        }
      });
    }
    async function saveSceneMoment2(workspaceService, input3) {
      const segmentId = requiredId3(input3.segmentId, "segmentId");
      const sceneMomentId = await engineEntityId2(workspaceService, "scene_moment", input3.id, input3.title);
      return workspaceService.saveProductionSnapshot({
        productionId: input3.productionId ?? "main",
        snapshot: {
          segments: [{
            id: segmentId,
            scene_moments: [pruneUndefined18({
              id: sceneMomentId,
              title: input3.title,
              storyboard_id: input3.storyboardId,
              order: input3.order,
              time_text: input3.timeText,
              scene_code: input3.sceneCode,
              location_text: input3.locationText,
              condition_text: input3.conditionText,
              action_text: input3.actionText,
              mood: input3.mood,
              description: input3.description,
              settings: input3.settings?.map(settingRefInput2)
            })]
          }]
        }
      });
    }
    async function saveStoryboard2(workspaceService, input3) {
      const segmentId = requiredId3(input3.segmentId, "segmentId");
      const sceneMomentId = requiredId3(input3.sceneMomentId, "sceneMomentId");
      const expressionUnitId = input3.expressionUnitId;
      const storyboardId = await engineEntityId2(workspaceService, "storyboard", input3.id, input3.title);
      return workspaceService.saveProductionSnapshot({
        productionId: input3.productionId ?? "main",
        snapshot: {
          segments: [{
            id: segmentId,
            scene_moments: [{
              id: sceneMomentId,
              ...expressionUnitId ? {
                expression_units: [{
                  id: expressionUnitId,
                  slotKind: "visual",
                  kind: "shot",
                  storyboards: [pruneUndefined18({
                    id: storyboardId,
                    title: input3.title,
                    visual_intent: input3.visualIntent,
                    order: input3.order,
                    timeline: input3.timeline,
                    graph: input3.graph
                  })]
                }]
              } : {
                storyboards: [pruneUndefined18({
                  id: storyboardId,
                  title: input3.title,
                  visual_intent: input3.visualIntent,
                  order: input3.order,
                  timeline: input3.timeline,
                  graph: input3.graph
                })]
              }
            }]
          }]
        }
      });
    }
    async function saveKeyframe2(workspaceService, input3) {
      const segmentId = requiredId3(input3.segmentId, "segmentId");
      const sceneMomentId = requiredId3(input3.sceneMomentId, "sceneMomentId");
      const expressionUnitId = input3.expressionUnitId;
      const keyframeId = await engineEntityId2(workspaceService, "keyframe", input3.id, input3.title);
      return workspaceService.saveProductionSnapshot({
        productionId: input3.productionId ?? "main",
        snapshot: {
          segments: [{
            id: segmentId,
            scene_moments: [{
              id: sceneMomentId,
              ...expressionUnitId ? {
                expression_units: [{
                  id: expressionUnitId,
                  slotKind: "visual",
                  kind: "shot",
                  keyframes: [pruneUndefined18({
                    id: keyframeId,
                    title: input3.title,
                    role: input3.role,
                    visual_intent: input3.visualIntent,
                    order: input3.order,
                    timing: input3.timing,
                    composition: input3.composition,
                    continuity: input3.continuity,
                    reference_asset_refs: input3.referenceAssetRefs,
                    reference_keyframe_refs: input3.referenceKeyframeRefs
                  })]
                }]
              } : {
                keyframes: [pruneUndefined18({
                  id: keyframeId,
                  title: input3.title,
                  role: input3.role,
                  visual_intent: input3.visualIntent,
                  order: input3.order,
                  timing: input3.timing,
                  composition: input3.composition,
                  continuity: input3.continuity,
                  reference_asset_refs: input3.referenceAssetRefs,
                  reference_keyframe_refs: input3.referenceKeyframeRefs
                })]
              }
            }]
          }]
        }
      });
    }
    async function saveAudioCue2(workspaceService, input3) {
      const segmentId = requiredId3(input3.segmentId, "segmentId");
      const sceneMomentId = requiredId3(input3.sceneMomentId, "sceneMomentId");
      const audioCueId = await engineEntityId2(workspaceService, "audio_cue", input3.id, input3.title);
      return workspaceService.saveProductionSnapshot({
        productionId: input3.productionId ?? "main",
        snapshot: {
          segments: [{
            id: segmentId,
            scene_moments: [{
              id: sceneMomentId,
              audio_cues: [pruneUndefined18({
                id: audioCueId,
                title: input3.title,
                kind: input3.kind,
                order: input3.order,
                storyboard_id: input3.storyboardId,
                expression_unit_ref: input3.expressionUnitId,
                prompt_hint: input3.promptHint
              })]
            }]
          }]
        }
      });
    }
    async function saveExpressionUnit2(workspaceService, input3) {
      const segmentId = requiredId3(input3.segmentId, "segmentId");
      const sceneMomentId = requiredId3(input3.sceneMomentId, "sceneMomentId");
      const expressionUnitId = await engineEntityId2(workspaceService, "expression_unit", input3.id, input3.title ?? input3.text);
      return workspaceService.saveProductionSnapshot({
        productionId: input3.productionId ?? "main",
        snapshot: {
          segments: [{
            id: segmentId,
            scene_moments: [{
              id: sceneMomentId,
              expression_units: [pruneUndefined18({
                id: expressionUnitId,
                title: input3.title,
                slot_kind: normalizeExpressionUnitSlotKind2(input3.slotKind ?? input3.kind ?? input3.role ?? input3.modality),
                modality: input3.modality,
                role: input3.role,
                kind: input3.kind,
                visual_kind: input3.visualKind,
                speaker: input3.speaker,
                speaker_ref: input3.speakerRef,
                source_expression_ref: input3.sourceExpressionRef,
                text: input3.text,
                note: input3.note,
                intent: input3.intent,
                content: input3.content,
                timing_intent: input3.timingIntent,
                voice_profile_ref: input3.voiceProfileRef,
                order: input3.order,
                span: input3.span,
                script_block_id: input3.scriptBlockId
              })]
            }]
          }]
        }
      });
    }
    async function saveContentUnit2(workspaceService, input3) {
      const id = await engineEntityId2(workspaceService, "content_unit", input3.id, input3.title);
      return workspaceService.upsertContentUnit({
        unit: pruneUndefined18({
          id,
          title: input3.title,
          content_unit_type: input3.contentUnitType ?? input3.kind ?? "storyboard_ref",
          output_kind: input3.outputKind ?? defaultContentUnitOutputKind2(input3.contentUnitType ?? input3.kind ?? "storyboard_ref"),
          target_category: input3.targetCategory,
          target_kind: input3.targetKind,
          target_ref: input3.targetRef,
          scope_kind: input3.scopeKind,
          scope_ref: input3.scopeRef,
          generation_role: input3.generationRole,
          edit_prompt: pruneUndefined18({
            text: input3.prompt?.trim() || void 0,
            negative_text: input3.negativePrompt
          }),
          model_intent: pruneUndefined18({
            ...input3.modelIntent ?? {},
            capability: input3.capability,
            provider: input3.provider,
            model: input3.model,
            quality: input3.quality,
            aspect_ratio: input3.aspectRatio,
            duration_sec: input3.durationSeconds,
            params: pruneUndefined18({
              ...input3.params ?? {},
              shot_size: input3.shotSize,
              camera_angle: input3.cameraAngle,
              camera_motion: input3.cameraMotion
            })
          }),
          description: input3.description,
          order: input3.order,
          asset_ref: input3.assetRef,
          production_ref: input3.productionId,
          segment_ref: input3.segmentId,
          scene_moment_ref: input3.sceneMomentId,
          expression_unit_ref: input3.expressionUnitRef ?? input3.expressionUnitId,
          storyboard_ref: input3.storyboardId,
          keyframe_ref: input3.keyframeId,
          audio_cue_ref: input3.audioCueId
        })
      });
    }
    async function ensureContentUnitForEntity2(workspaceService, input3) {
      const target = normalizeContentUnitTarget22(input3);
      const contentUnitType22 = input3.contentUnitType ?? contentUnitTypeForTargetKind2(input3.targetKind);
      const outputKind = input3.outputKind ?? defaultContentUnitOutputKind2(contentUnitType22);
      const existing = (await workspaceService.queryEntities({ entityKind: "content_unit" })).find((entity) => {
        const recordType = String(entity.record.content_unit_type ?? "");
        if (!contentUnitTypeMatchesTarget2(recordType, contentUnitType22, input3.targetKind, target)) return false;
        return contentUnitRecordMatchesTarget2(entity.record, input3.targetKind, target);
      });
      if (existing) {
        if (input3.prompt === void 0 && input3.negativePrompt === void 0) {
          return { contentUnitPath: existing.path, record: existing.record };
        }
        const existingContentUnitType = stringValue112(existing.record.content_unit_type);
        return saveContentUnit2(workspaceService, {
          ...contentUnitInputForTarget2(input3.targetKind, target),
          id: idValue5(existing.id) ?? idValue5(existing.record.id) ?? contentUnitIdForTarget2(input3.targetKind, target.targetRef),
          title: input3.title ?? stringValue112(existing.record.title),
          contentUnitType: input3.contentUnitType ?? existingContentUnitType ?? contentUnitType22,
          outputKind,
          prompt: input3.prompt,
          negativePrompt: input3.negativePrompt,
          description: input3.description ?? stringValue112(existing.record.description),
          order: input3.order ?? numberValue33(existing.record.order),
          modelIntent: input3.modelIntent ?? recordValue9(existing.record.model_intent)
        });
      }
      return saveContentUnit2(workspaceService, {
        ...contentUnitInputForTarget2(input3.targetKind, target),
        id: input3.id ?? contentUnitIdForTarget2(input3.targetKind, target.targetRef),
        title: input3.title ?? `${targetKindLabel2(input3.targetKind)} ${target.targetRef} \u5236\u4F5C\u9879`,
        contentUnitType: contentUnitType22,
        outputKind,
        prompt: input3.prompt,
        negativePrompt: input3.negativePrompt,
        description: input3.description ?? `\u4ECE ${targetKindLabel2(input3.targetKind)}\u300C${target.targetRef}\u300D\u521B\u5EFA\u3002`,
        order: input3.order,
        modelIntent: {
          source: "engine",
          target_kind: input3.targetKind,
          target_ref: target.targetRef,
          ...target.scopeKind && target.scopeRef ? { scope_kind: target.scopeKind, scope_ref: target.scopeRef } : {},
          ...input3.modelIntent ?? {}
        }
      });
    }
    function normalizeContentUnitTarget22(input3) {
      if (input3.targetKind !== "timeline_assembly") {
        return { targetRef: normalizeEntityRef2(input3.targetRef ?? input3.targetId, input3.targetKind) };
      }
      const scopeKind = stringValue112(input3.scopeKind);
      const scopeRef = idStringValue2(input3.scopeRef);
      const rawRef = idStringValue2(input3.targetRef ?? input3.targetId);
      const parsedRef = parseImplicitTimelineAssemblyRef2(rawRef);
      const normalizedScopeKind = scopeKind ?? parsedRef?.scopeKind;
      const normalizedScopeRef = scopeRef ?? parsedRef?.scopeRef ?? (!parsedRef && scopeKind ? rawRef : void 0);
      if (!normalizedScopeKind || !normalizedScopeRef) {
        throw new Error("timeline_assembly target requires targetRef timeline_assembly:<scopeKind>:<scopeRef> or scopeKind/scopeRef");
      }
      return {
        targetRef: implicitTimelineAssemblyRef2(normalizedScopeKind, normalizedScopeRef),
        scopeKind: normalizedScopeKind,
        scopeRef: normalizedScopeRef
      };
    }
    async function engineEntityId2(workspaceService, entityKind, explicitId, title) {
      const id = idValue5(explicitId);
      if (id !== void 0) return id;
      const entities = await workspaceService.queryEntities({ entityKind }).catch(() => []);
      return allocateMovScriptEntityId2({
        entityKind,
        title,
        existingIds: entities.flatMap((entity) => [
          idStringValue2(entity.id),
          idStringValue2(entity.record.id),
          idStringValue2(entity.record.ID)
        ]).filter((value) => Boolean(value))
      });
    }
    function contentUnitTypeMatchesTarget2(recordType, requestedType, targetKind, target) {
      if (recordType === requestedType) return true;
      if (targetKind !== "timeline_assembly" || requestedType !== "timeline_assembly_ref") return false;
      if (target.scopeKind === "production" && recordType === "production_ref") return true;
      if (target.scopeKind === "segment" && recordType === "segment_ref") return true;
      return false;
    }
    function contentUnitRecordMatchesTarget2(record, targetKind, target) {
      if (targetKind === "timeline_assembly") {
        const targetRefs = compactStrings22(record.target_ref, record.targetRef);
        if (targetRefs.some((ref) => sameEntityRef22(ref, target.targetRef))) return true;
        const recordScopeKind = stringValue112(record.scope_kind ?? record.scopeKind);
        const recordScopeRef = stringValue112(record.scope_ref ?? record.scopeRef);
        if (recordScopeKind && recordScopeRef && target.scopeKind === recordScopeKind && sameEntityRef22(recordScopeRef, target.scopeRef)) return true;
        if (target.scopeKind === "production") return compactStrings22(record.production_ref, record.productionRef).some((ref) => sameEntityRef22(ref, target.scopeRef));
        if (target.scopeKind === "segment") return compactStrings22(record.segment_ref, record.segmentRef).some((ref) => sameEntityRef22(ref, target.scopeRef));
        return false;
      }
      return compactStrings22(record[primaryRefFieldForTargetKind2(targetKind)]).some((ref) => sameEntityRef22(ref, target.targetRef));
    }
    function defaultContentUnitOutputKind2(contentUnitType22) {
      switch (contentUnitType22) {
        case "asset_ref":
        case "keyframe_ref":
        case "storyboard_ref":
          return "image";
        case "timeline_assembly_ref":
        case "scence_moment_ref":
        case "scene_moment_ref":
        case "production_ref":
        case "segment_ref":
          return "video";
        case "expression_unit_ref":
          return defaultOutputKindForExpressionUnitSlot2(void 0);
        default:
          return "metadata";
      }
    }
    function entityKindFromPath22(path) {
      if (!path) return void 0;
      if (path.endsWith("/setting.json")) return "setting";
      if (path.endsWith("/setting_state.json")) return "setting_state";
      if (path.endsWith("/asset.json")) return "asset";
      if (path.endsWith("/production.json")) return "production";
      if (path.endsWith("/segment.json")) return "segment";
      if (path.endsWith("/scene_moment.json")) return "scene_moment";
      if (path.endsWith("/expression_unit.json")) return "expression_unit";
      if (path.endsWith("/audio_cue.json")) return "audio_cue";
      if (path.endsWith("/keyframe.json")) return "keyframe";
      if (path.endsWith("/storyboard.json")) return "storyboard";
      if (path.endsWith("/shot.json")) return "shot";
      if (path.endsWith("/content_unit.json")) return "content_unit";
      return void 0;
    }
    function pathPreservingHierarchyRecord2(targetPath, record, entityKind) {
      const kind = stringValue112(record.kind) ?? entityKind;
      return pruneUndefined18({
        ...record,
        schema: stringValue112(record.schema) ?? `movscript.${kind}.v1`,
        kind,
        id: idValue5(record.id) ?? entityIdFromTargetPath2(targetPath, kind),
        title: stringValue112(record.title)
      });
    }
    function entityIdFromTargetPath2(targetPath, entityKind) {
      const collection = collectionSegmentForSourceKind2(entityKind);
      const collectionId = collection ? pathSegmentAfter42(targetPath, collection) : void 0;
      if (collectionId) return collectionId;
      const parts = normalizePath2(targetPath).split("/").filter(Boolean);
      const filename = parts.at(-1);
      if (filename?.endsWith(".json")) return parts.at(-2);
      return parts.at(-1);
    }
    function collectionSegmentForSourceKind2(entityKind) {
      switch (entityKind) {
        case "production":
          return "productions";
        case "segment":
          return "segments";
        case "scene_moment":
          return "scene_moments";
        case "expression_unit":
          return "expression_units";
        case "audio_cue":
          return "audio_cues";
        case "keyframe":
          return "keyframes";
        case "storyboard":
          return "storyboards";
        case "content_unit":
          return "content_units";
        case "setting":
          return "settings";
        case "setting_state":
          return "states";
        case "asset":
          return "assets";
        case "shot":
          return "shots";
        default:
          return void 0;
      }
    }
    function patchEntityBasics2(record, input3) {
      const next = { ...record };
      if ("title" in next || !("name" in next) && !("label" in next)) {
        next.title = input3.title;
      } else if ("name" in next) {
        next.name = input3.title;
      } else {
        next.label = input3.title;
      }
      if ("summary" in next) {
        next.summary = input3.summary;
      } else if ("description" in next || !("action_text" in next) && !("action" in next) && !("prompt" in next)) {
        next.description = input3.summary;
      } else if ("action_text" in next) {
        next.action_text = input3.summary;
      } else if ("action" in next) {
        next.action = input3.summary;
      } else if (typeof next.prompt === "string") {
        next.prompt = input3.summary;
      }
      return next;
    }
    function contentUnitInputFromPatchedRecord2(record, input3) {
      return {
        id: input3.id ?? idValue5(record.id) ?? pathSegmentAfter42(input3.targetPath ?? "", "content_units"),
        title: stringValue112(record.title),
        contentUnitType: stringValue112(record.content_unit_type ?? record.kind),
        outputKind: stringValue112(record.output_kind),
        assetRef: idValue5(record.asset_ref),
        sceneMomentId: idValue5(record.scene_moment_ref),
        expressionUnitId: idValue5(record.expression_unit_ref),
        keyframeId: idValue5(record.keyframe_ref),
        storyboardId: idValue5(record.storyboard_ref),
        audioCueId: idValue5(record.audio_cue_ref),
        prompt: stringValue112(record.edit_prompt) ?? stringValue112(record.prompt),
        description: stringValue112(record.description),
        modelIntent: recordValue9(record.model_intent)
      };
    }
    function settingRefsFromRecord3(record) {
      const refs = Array.isArray(record.setting_refs) ? record.setting_refs.filter(isRecord292) : [];
      return refs.flatMap((ref) => {
        const settingId = idValue5(ref.setting_id ?? ref.settingId ?? ref.setting_ref ?? ref.settingRef);
        if (!settingId) return [];
        return [{
          id: settingId,
          settingStateId: idValue5(ref.setting_state_id ?? ref.settingStateId ?? ref.setting_state_ref ?? ref.settingStateRef),
          role: stringValue112(ref.role),
          sourceLabel: stringValue112(ref.notes ?? ref.source_label ?? ref.sourceLabel),
          kind: stringValue112(ref.setting_kind ?? ref.kind)
        }];
      });
    }
    function settingRefInput2(input3) {
      return pruneUndefined18({
        id: input3.id ?? input3.settingId,
        setting_state_id: input3.settingStateId,
        role: input3.role,
        source_label: input3.sourceLabel,
        kind: input3.kind
      });
    }
    function contentUnitTypeForTargetKind2(kind) {
      if (kind === "timeline_assembly") return "timeline_assembly_ref";
      return `${kind}_ref`;
    }
    function primaryRefFieldForTargetKind2(kind) {
      if (kind === "timeline_assembly") return "target_ref";
      return kind === "scene_moment" ? "scene_moment_ref" : `${kind}_ref`;
    }
    function contentUnitInputForTarget2(kind, target) {
      if (kind === "timeline_assembly") {
        return {
          targetCategory: "timeline_assembly",
          targetKind: "timeline_assembly",
          targetRef: target.targetRef,
          scopeKind: target.scopeKind,
          scopeRef: target.scopeRef
        };
      }
      if (kind === "asset") return { assetRef: target.targetRef };
      if (kind === "scene_moment") return { sceneMomentId: target.targetRef };
      if (kind === "expression_unit") return { expressionUnitId: target.targetRef };
      if (kind === "keyframe") return { keyframeId: target.targetRef };
      return { storyboardId: target.targetRef };
    }
    function contentUnitIdForTarget2(kind, targetRef) {
      return `cu_${kind}_${safeToken2(targetRef)}`;
    }
    function targetKindLabel2(kind) {
      if (kind === "timeline_assembly") return "\u526A\u8F91\u805A\u5408";
      if (kind === "asset") return "\u7D20\u6750";
      if (kind === "scene_moment") return "\u60C5\u8282";
      if (kind === "expression_unit") return "\u8868\u8FBE\u5355\u5143";
      if (kind === "keyframe") return "\u5173\u952E\u5E27";
      return "\u5206\u955C\u56FE";
    }
    function normalizeEntityRef2(value, kind) {
      if (value === void 0 || value === null || String(value).trim() === "") throw new Error(`${kind} ref is required`);
      const raw = normalizePath2(String(value));
      const collection = collectionSegmentForTargetKind2(kind);
      const pathId = pathSegmentAfter42(raw, collection);
      if (pathId) return pathId;
      if (raw.endsWith(".json")) {
        const parts = raw.split("/").filter(Boolean);
        const parent = parts.at(-2);
        if (parent) return parent;
      }
      const separator = raw.indexOf(":");
      return separator > 0 ? raw.slice(separator + 1) : raw;
    }
    function collectionSegmentForTargetKind2(kind) {
      if (kind === "timeline_assembly") return "timeline_assemblies";
      if (kind === "scene_moment") return "scene_moments";
      if (kind === "expression_unit") return "expression_units";
      if (kind === "keyframe") return "keyframes";
      if (kind === "storyboard") return "storyboards";
      return "assets";
    }
    function sameEntityRef22(left, right) {
      const rightAliases = new Set(entityRefAliases22(right));
      return entityRefAliases22(left).some((alias) => rightAliases.has(alias));
    }
    function entityRefAliases22(value) {
      if (value === void 0 || value === null || String(value).trim() === "") return [];
      const raw = normalizePath2(String(value));
      const parts = raw.split("/").filter(Boolean);
      const aliases = /* @__PURE__ */ new Set([raw]);
      const tail = parts.at(-1);
      if (tail) aliases.add(tail);
      if (tail?.endsWith(".json") && parts.at(-2)) aliases.add(parts.at(-2));
      for (const marker of ["assets", "scene_moments", "expression_units", "keyframes", "storyboards", "content_units"]) {
        const segment = pathSegmentAfter42(raw, marker);
        if (segment) aliases.add(segment);
      }
      return [...aliases];
    }
    function compactStrings22(...values) {
      return values.flatMap((value) => {
        if (Array.isArray(value)) return value.filter((item) => typeof item === "string" && Boolean(item.trim())).map((item) => item.trim());
        return typeof value === "string" && value.trim() ? [value.trim()] : [];
      });
    }
    function recordValue9(value) {
      return isRecord292(value) ? value : void 0;
    }
    function numberValue33(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && /^-?\d+(\.\d+)?$/.test(value.trim())) return Number(value);
      return void 0;
    }
    function safeToken2(value) {
      return String(value).trim().replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "local";
    }
    async function listContentUnits2(workspaceService, input3) {
      const entities = await workspaceService.queryEntities({
        entityKind: "content_unit",
        query: input3.query
      });
      const filtered = entities.filter((entity) => {
        if (input3.kind && String(entity.record.content_unit_type ?? entity.record.kind) !== input3.kind) return false;
        const sceneMomentRef = stringValue112(entity.record.scene_moment_ref);
        if (input3.productionId !== void 0 && !pathSegmentMatches2(sceneMomentRef, "productions", input3.productionId, "production")) return false;
        if (input3.segmentId !== void 0 && !pathSegmentMatches2(sceneMomentRef, "segments", input3.segmentId, "segment")) return false;
        if (input3.sceneMomentId !== void 0 && !pathSegmentMatches2(sceneMomentRef, "scene_moments", input3.sceneMomentId, "scene_moment")) return false;
        return true;
      });
      return input3.limit === void 0 ? filtered : filtered.slice(0, input3.limit);
    }
    async function deletePlanningEntity2(workspaceService, entityKind, input3) {
      const entity = await findPlanningEntity2(workspaceService, entityKind, input3);
      await workspaceService.deleteEntity({ entity });
      return { deleted: true, entity };
    }
    async function findPlanningEntity2(workspaceService, entityKind, input3) {
      const id = String(input3.id);
      const entities = await workspaceService.queryEntities(planningQuery2(entityKind, input3));
      const match = entities.find((entity) => normalizePath2(entity.path) === normalizePath2(id)) ?? entities.find((entity) => sameEntityId22(entity, id, entityKind));
      if (!match) throw new Error(`${entityKind} not found: ${id}`);
      return match;
    }
    function sameEntityId22(entity, id, entityKind) {
      const entityId22 = entity.id === void 0 ? void 0 : String(entity.id);
      if (entityId22 === id) return true;
      if (entityId22 === `${entityKind}_${id}`) return true;
      if (entityId22?.startsWith(`${entityKind}_`) && entityId22.slice(entityKind.length + 1) === id) return true;
      return entity.path.includes(`/${id}/`);
    }
    function requiredId3(value, name) {
      if (value === void 0 || value === null || String(value).trim() === "") throw new Error(`${name} is required`);
      return value;
    }
    function normalizePath2(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function pathSegmentMatches2(path, marker, id, entityKind) {
      if (!path) return false;
      const segment = pathSegmentAfter42(path, marker);
      if (!segment) return false;
      return refAliases2(id, entityKind).includes(segment);
    }
    function pathSegmentAfter42(path, marker) {
      const parts = normalizePath2(path).split("/").filter(Boolean);
      const index = parts.indexOf(marker);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function refAliases2(value, entityKind) {
      const raw = String(value);
      const withoutPrefix = raw.startsWith(`${entityKind}_`) ? raw.slice(entityKind.length + 1) : raw;
      return [raw, withoutPrefix, `${entityKind}_${withoutPrefix}`];
    }
    function isRecord292(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function stringValue112(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function idValue5(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function idStringValue2(value) {
      const id = idValue5(value);
      return id === void 0 ? void 0 : String(id);
    }
    function pruneUndefined18(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0 && item !== "") output3[key] = item;
      }
      return output3;
    }
    function callRequiredOperation2(operation, operationName) {
      if (!operation) {
        return Promise.reject(new Error(`MovScript engine ${operationName} operation is not configured`));
      }
      return operation().then((result) => result);
    }
    var NodeMovScriptEngineRegistry2 = class {
      constructor(factory = createNodeMovScriptEngine2) {
        this.factory = factory;
      }
      factory;
      engines = /* @__PURE__ */ new Map();
      get(input3 = {}) {
        const key = nodeMovScriptEngineRegistryKey2(input3);
        const existing = this.engines.get(key);
        if (existing) return existing;
        const engine = this.factory(input3);
        this.engines.set(key, engine);
        return engine;
      }
      invalidate(input3 = {}) {
        this.engines.delete(typeof input3 === "string" ? input3 : nodeMovScriptEngineRegistryKey2(input3));
      }
      clear() {
        this.engines.clear();
      }
    };
    function nodeMovScriptEngineRegistryKey2(input3 = {}) {
      return input3.cacheKey ?? [
        input3.projectDir ?? ""
      ].map((part) => String(part)).join("");
    }
    function createNodeMovScriptEngine2(input3 = {}) {
      const workspaceService = createNodeMovScriptWorkspaceService2(input3);
      const fileRepository = createNodeMovScriptWorkspaceFileRepository2(workspaceService.projectDir);
      const engine = createMovScriptEngine2({
        workspaceService,
        overviewWorkspace: () => overviewMovScriptWorkspace2({
          fileRepository,
          decisionStore: input3.decisionStore,
          ...input3.now ? { now: input3.now() } : {}
        }),
        inspectWorkspace: (inspectInput3 = {}) => inspectMovScriptWorkspace2({
          fileRepository,
          decisionStore: input3.decisionStore,
          ...inspectInput3,
          ...input3.now ? { now: input3.now() } : {}
        }),
        reviewWorkspace: (inspectInput3 = {}) => reviewMovScriptWorkspace2({
          fileRepository,
          decisionStore: input3.decisionStore,
          ...inspectInput3,
          ...input3.now ? { now: input3.now() } : {}
        }),
        productionWorkPlan: async () => {
          const review = await reviewMovScriptWorkspace2({
            fileRepository,
            decisionStore: input3.decisionStore,
            ...input3.now ? { now: input3.now() } : {}
          });
          return review.productionWorkPlan;
        },
        interpretWorkspace: () => interpretMovScriptWorkspace2({
          fileRepository,
          decisionStore: input3.decisionStore,
          ...input3.now ? { now: input3.now() } : {}
        }),
        regenerationPlan: () => planMovScriptWorkspaceRegeneration2({
          fileRepository,
          decisionStore: input3.decisionStore,
          ...input3.now ? { now: input3.now() } : {}
        }),
        async deriveContentUnitArtifact(contentUnitId) {
          const index = await workspaceService.loadIndex();
          const contentUnit = index.byKind.get("content_unit")?.find((entity) => String(entity.id) === String(contentUnitId));
          if (!contentUnit) throw new Error(`content_unit not found: ${String(contentUnitId)}`);
          const now = input3.now?.() ?? /* @__PURE__ */ new Date();
          return deriveContentUnitArtifact2(index, contentUnit, { createdAt: now.toISOString() });
        },
        async buildContentUnitBackendPrompt(contentUnitId, options) {
          return buildContentUnitBackendPromptById2({
            index: await workspaceService.loadIndex(),
            contentUnitId,
            decisionProvider: input3.decisionStore ?? {
              async getContentUnitDecision() {
                return void 0;
              }
            },
            ...options?.promptText !== void 0 ? { promptText: options.promptText } : {}
          });
        },
        async deriveArtifacts(artifactInput = {}) {
          const now = input3.now?.() ?? /* @__PURE__ */ new Date();
          const createdAt = artifactInput.createdAt ?? now.toISOString();
          const interpretationId = artifactInput.interpretationId ?? `engine_${createdAt.replace(/[-:.TZ]/g, "")}`;
          return deriveMovScriptWorkspaceArtifacts2({
            index: await workspaceService.loadIndex(),
            changedEntities: [],
            interpretationId,
            createdAt
          });
        },
        ...input3.publish ? { publish: input3.publish } : {}
      });
      return {
        ...engine,
        projectDir: workspaceService.projectDir,
        workspaceService
      };
    }
    var SEMANTIC_ENTITY_KINDS2 = SEMANTIC_ENTITY_KIND_VALUES2;
    var CONTENT_UNIT_OUTPUT_KINDS = ["image", "video", "audio", "text", "metadata"];
    var INTERACTIVE_PROMPT = "movscript> ";
    function registerLangCommands(program2) {
      program2.option("--json", "Print JSON output").option("--cwd <path>", "MovScript workspace directory").showHelpAfterError().addHelpText("after", `
Examples:
  $ movscript project init --id demo --title "Demo Film"
  $ movscript project demo create --cwd ./demo
  $ movscript setting add hero --title "Hero"
  $ movscript asset add --setting hero --slot portrait --prompt "cinematic portrait"
  $ movscript language schemas
  $ movscript language schema content_unit
  $ movscript content-unit add --id cu_phone --type storyboard_ref --output-kind video --scene-moment phone_call --storyboard main
  $ movscript overview
  $ movscript inspect
  $ movscript interpret
  $ movscript regen plan
`);
      program2.command("init").description("Initialize a MovScript project workspace").option("--id <id>", "Project id written to project_id").option("--title <title>", "Project title").option("--language <language>", "Project language").option("--standard <key=value...>", "Project standard field, repeatable", collectOption, []).option("--overwrite", "Overwrite existing project files").option("--json", "Print JSON output").action(async (options, command) => {
        await initProjectFromCliOptions(options, command);
      });
      const project = program2.command("project").description("Manage the MovScript project workspace");
      project.command("init").description("Initialize a MovScript project workspace").option("--id <id>", "Project id written to project_id").option("--title <title>", "Project title").option("--language <language>", "Project language").option("--standard <key=value...>", "Project standard field, repeatable", collectOption, []).option("--overwrite", "Overwrite existing project files").option("--json", "Print JSON output").action(async (options, command) => {
        await initProjectFromCliOptions(options, command);
      });
      const projectDemo = project.command("demo").description("Create runnable demo projects");
      projectDemo.command("create").description("Create a minimal demo project with asset_ref, keyframe_ref, and storyboard_ref content units").option("--id <id>", "Project id", "demo").option("--title <title>", "Project title", "Demo Film").option("--overwrite", "Overwrite existing demo files").option("--no-interpret", "Only write demo source files; do not run interpreter interpret").option("--json", "Print JSON output").action(async (options, command) => {
        await createDemoProjectFromCliOptions(options, command);
      });
      program2.command("get-model <entityType>").description("Return the MovScript source model for one editable entity").option("--entity-id <id>", "Optional entity id used to expand editable path hints").option("--json", "Print JSON output").action((entityType, options, command) => {
        const engine = createCliEngine(mergeGlobalOptions(options, command));
        const result = engine.getModel({
          entityKind: entityType,
          ...options.entityId !== void 0 ? { entityId: options.entityId } : {}
        });
        printResult(result, mergeGlobalOptions(options, command));
      });
      const language = program2.command("language").alias("lang").description("Inspect MovScript language kinds and schemas");
      language.command("kinds").description("List semantic entity kinds supported by the language package").option("--json", "Print JSON output").action((options, command) => {
        printLanguageKinds(mergeGlobalOptions(options, command));
      });
      language.command("schemas").description("List semantic entity schemas from the language package").option("--kind <kind>", "Filter by semantic entity kind").option("--json", "Print JSON output").action((options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const schemas = listLanguageSchemas(options.kind);
        printLanguageSchemaList(schemas, merged);
      });
      language.command("schema <schemaIdOrKind>").description("Show one semantic entity schema by schema id or entity kind").option("--json", "Print JSON output").action((schemaIdOrKind, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const schema = resolveLanguageSchema(schemaIdOrKind);
        printResult(schema, merged);
      });
      program2.command("interactive").alias("i").description("Open the interactive MovScript slash shell").option("--json", "Print JSON output for write results").action(async (options, command) => {
        await runInteractiveCli(mergeGlobalOptions(options, command));
      });
      const setting = program2.command("setting").description("Manage settings");
      setting.command("list").description("List settings").option("--kind <kind>", "Filter by setting kind").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).querySettings({
          ...options.kind !== void 0 ? { kind: options.kind } : {},
          ...options.query !== void 0 ? { query: options.query } : {},
          ...options.limit !== void 0 ? { limit: parsePositiveIntegerOption(options.limit, "limit") } : {}
        });
        printSettingsTable(result, merged);
      });
      setting.command("add [id]").description("Add or update a setting").option("--id <id>", "Setting id").option("--title <title>", "Setting display title").option("--kind <kind>", "Setting kind, such as character, location, prop, world_rule, style, or other", "other").option("--description <text>", "Setting description").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).createSetting({
          id: options.id ?? id,
          title: options.title,
          kind: options.kind,
          description: options.description
        });
        printResult(result, merged);
      });
      const asset = program2.command("asset").description("Manage assets");
      asset.command("list").description("List assets").option("--setting <id>", "Filter by owning setting id").option("--state <id>", "Filter by owning setting state id").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).queryAssets({
          ...options.setting !== void 0 ? { settingId: options.setting } : {},
          ...options.state !== void 0 ? { settingStateId: options.state } : {},
          ...options.query !== void 0 ? { query: options.query } : {},
          ...options.limit !== void 0 ? { limit: parsePositiveIntegerOption(options.limit, "limit") } : {}
        });
        printAssetsTable(result.assets, merged);
      });
      asset.command("add [id]").description("Add or update an asset").option("--id <id>", "Asset id").option("--title <title>", "Asset display title").option("--setting <id>", "Owning setting id").option("--state <id>", "Owning setting state id").option("--slot <slot>", "Asset slot key").option("--kind <kind>", "Asset kind, such as image, video, audio, text, reference, or other", "image").option("--prompt <text>", "Asset prompt hint").option("--resource-id <id>", "Selected resource id").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).createAsset({
          id: options.id ?? id,
          title: options.title,
          settingId: options.setting,
          settingStateId: options.state,
          slot: options.slot,
          assetKind: options.kind,
          promptHint: options.prompt,
          resourceId: resourceIdOption(options.resourceId)
        });
        printResult(result, merged);
      });
      const production = program2.command("production").description("Manage productions");
      production.command("list").description("List productions").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("production", "Productions", options, command);
      });
      production.command("add").alias("create").description("Create or update a production").option("--id <id>", "Production id", "main").option("--title <title>", "Production title").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).createProduction({
          id: options.id ?? "main",
          title: options.title
        });
        printResult(result, merged);
      });
      production.command("modify <id>").description("Modify a production").option("--title <title>", "Production title").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).updateProduction({ id, title: options.title });
        printResult(result, merged);
      });
      production.command("delete <idOrPath>").alias("remove").description("Delete a production").option("--json", "Print JSON output").action((idOrPath, options, command) => {
        return deletePlanningEntity22("production", idOrPath, options, command);
      });
      const segment = program2.command("segment").description("Manage emotional/rhythm segments");
      segment.command("list").description("List segments").option("--production <id>", "Filter by production id").option("--kind <kind>", "Filter by segment kind").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("segment", "Segments", options, command);
      });
      segment.command("add").alias("create").description("Create or update an emotional/rhythm segment").option("--id <id>", "Segment id").option("--title <title>", "Segment title").option("--production <id>", "Production id", "main").option("--kind <kind>", "Segment kind, such as emotional_function, setup, escalation, release, or transition").option("--summary <text>", "Segment summary").option("--order <number>", "Segment order").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).createSegment({
          id: options.id,
          productionId: options.production ?? "main",
          title: options.title,
          kind: options.kind,
          summary: options.summary,
          order: parseOptionalNumberOption(options.order, "order")
        });
        printResult(result, merged);
      });
      segment.command("modify <id>").description("Modify an emotional/rhythm segment").option("--production <id>", "Production id", "main").option("--title <title>", "Segment title").option("--kind <kind>", "Segment kind").option("--summary <text>", "Segment summary").option("--order <number>", "Segment order").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).updateSegment({
          id,
          productionId: options.production ?? "main",
          title: options.title,
          kind: options.kind,
          summary: options.summary,
          order: parseOptionalNumberOption(options.order, "order")
        });
        printResult(result, merged);
      });
      segment.command("delete <idOrPath>").alias("remove").description("Delete a segment").option("--production <id>", "Filter by production id").option("--json", "Print JSON output").action((idOrPath, options, command) => {
        return deletePlanningEntity22("segment", idOrPath, options, command);
      });
      const sceneMoment = program2.command("scene-moment").alias("moment").description("Manage scene moments");
      sceneMoment.command("list").description("List scene moments").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("scene_moment", "Scene moments", options, command);
      });
      sceneMoment.command("add").alias("create").description("Create or update a scene moment under a segment").option("--id <id>", "Scene moment id").option("--title <title>", "Scene moment title").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--order <number>", "Scene moment order").option("--time <text>", "Time text").option("--scene-code <text>", "Scene code").option("--location <text>", "Location text").option("--condition <text>", "Condition text").option("--action <text>", "Action text").option("--mood <text>", "Mood/emotion text").option("--description <text>", "Scene moment description").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parsePlanningParentOptions(options);
        const result = await createCliEngine(merged).createSceneMoment({
          id: options.id ?? options.sceneMoment,
          productionId: source.productionId,
          segmentId: source.segmentId,
          title: options.title,
          order: parseOptionalNumberOption(options.order, "order"),
          timeText: options.time,
          sceneCode: options.sceneCode,
          locationText: options.location,
          conditionText: options.condition,
          actionText: options.action,
          mood: options.mood,
          description: options.description
        });
        printResult(result, merged);
      });
      sceneMoment.command("modify <id>").description("Modify a scene moment").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--title <title>", "Scene moment title").option("--order <number>", "Scene moment order").option("--time <text>", "Time text").option("--scene-code <text>", "Scene code").option("--location <text>", "Location text").option("--condition <text>", "Condition text").option("--action <text>", "Action text").option("--mood <text>", "Mood/emotion text").option("--description <text>", "Scene moment description").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parsePlanningParentOptions(options);
        const result = await createCliEngine(merged).updateSceneMoment({
          id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          title: options.title,
          order: parseOptionalNumberOption(options.order, "order"),
          timeText: options.time,
          sceneCode: options.sceneCode,
          locationText: options.location,
          conditionText: options.condition,
          actionText: options.action,
          mood: options.mood,
          description: options.description
        });
        printResult(result, merged);
      });
      sceneMoment.command("delete <idOrPath>").alias("remove").description("Delete a scene moment").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--json", "Print JSON output").action((idOrPath, options, command) => {
        return deletePlanningEntity22("scene_moment", idOrPath, options, command);
      });
      const storyboard = program2.command("storyboard").description("Manage storyboards");
      storyboard.command("list").description("List storyboards").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("storyboard", "Storyboards", options, command);
      });
      storyboard.command("add").alias("create").description("Create or update a storyboard under a scene moment or expression unit").option("--id <id>", "Storyboard id").option("--title <title>", "Storyboard title").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--expression-unit <id-or-path>", "Expression unit id or path").option("--order <number>", "Storyboard order; new storyboards append when omitted").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parseStoryboardParentOptions(options);
        const result = await createCliEngine(merged).createStoryboard({
          id: options.id ?? "main",
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          expressionUnitId: source.expressionUnitId,
          title: options.title,
          order: parseOptionalNumberOption(options.order, "order")
        });
        printResult(result, merged);
      });
      storyboard.command("modify <id>").description("Modify a storyboard").option("--title <title>", "Storyboard title").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--expression-unit <id-or-path>", "Expression unit id or path").option("--order <number>", "Storyboard order").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parseStoryboardParentOptions(options);
        const result = await createCliEngine(merged).updateStoryboard({
          id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          expressionUnitId: source.expressionUnitId,
          title: options.title,
          order: parseOptionalNumberOption(options.order, "order")
        });
        printResult(result, merged);
      });
      storyboard.command("delete <idOrPath>").alias("remove").description("Delete a storyboard").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--expression-unit <id-or-path>", "Filter by expression unit id or path").option("--json", "Print JSON output").action((idOrPath, options, command) => {
        return deletePlanningEntity22("storyboard", idOrPath, options, command);
      });
      const keyframe = program2.command("keyframe").description("Manage keyframes");
      keyframe.command("list").description("List keyframes").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--expression-unit <id-or-path>", "Filter by expression unit id or path").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("keyframe", "Keyframes", options, command);
      });
      keyframe.command("add").alias("create").description("Create or update a keyframe under a scene moment or expression unit").option("--id <id>", "Keyframe id").option("--title <title>", "Keyframe title").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--expression-unit <id-or-path>", "Expression unit id or path").option("--role <role>", "Keyframe role").option("--visual-intent <text>", "Visual intent").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parseKeyframeParentOptions(options);
        const result = await createKeyframeFromCliOptions(createCliEngine(merged), options, source);
        printResult(result, merged);
      });
      const audioCue = program2.command("audio-cue").description("Manage audio cues");
      audioCue.command("list").description("List audio cues").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("audio_cue", "Audio cues", options, command);
      });
      audioCue.command("add").alias("create").description("Create or update an audio cue under a scene moment").option("--id <id>", "Audio cue id").option("--title <title>", "Audio cue title").option("--kind <kind>", "Audio cue kind, such as sound_effect, music, ambience, dialogue, or foley", "sound_effect").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--expression-unit <id-or-path>", "Expression unit id or path").option("--storyboard <id-or-path>", "Storyboard id or path").option("--order <number>", "Audio cue order").option("--prompt <text>", "Audio prompt hint").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parseAudioCueParentOptions(options);
        const result = await createCliEngine(merged).createAudioCue({
          id: options.id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          expressionUnitId: source.expressionUnitId,
          storyboardId: source.storyboardId,
          title: options.title,
          kind: options.kind,
          order: parseOptionalNumberOption(options.order, "order"),
          promptHint: options.prompt
        });
        printResult(result, merged);
      });
      audioCue.command("modify <id>").description("Modify an audio cue").option("--title <title>", "Audio cue title").option("--kind <kind>", "Audio cue kind").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--expression-unit <id-or-path>", "Expression unit id or path").option("--storyboard <id-or-path>", "Storyboard id or path").option("--order <number>", "Audio cue order").option("--prompt <text>", "Audio prompt hint").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parseAudioCueParentOptions(options);
        const result = await createCliEngine(merged).updateAudioCue({
          id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          expressionUnitId: source.expressionUnitId,
          storyboardId: source.storyboardId,
          title: options.title,
          kind: options.kind,
          order: parseOptionalNumberOption(options.order, "order"),
          promptHint: options.prompt
        });
        printResult(result, merged);
      });
      audioCue.command("delete <idOrPath>").alias("remove").description("Delete an audio cue").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--json", "Print JSON output").action((idOrPath, options, command) => {
        return deletePlanningEntity22("audio_cue", idOrPath, options, command);
      });
      const expressionUnit = program2.command("expression-unit").alias("expr").description("Manage scene-moment expression units");
      expressionUnit.command("list").description("List expression units").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--kind <kind>", "Filter by expression kind").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("expression_unit", "Expression units", options, command);
      });
      expressionUnit.command("add").alias("create").description("Create or update an expression unit under a scene moment").option("--id <id>", "Expression unit id").option("--title <title>", "Expression unit title").option("--slot-kind <kind>", "Expression slot kind: visual, voice, subtitle, or audio").option("--kind <kind>", "Legacy expression kind, such as dialogue, narration, subtitle, caption, action, visual_note, or shot").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--speaker <text>", "Speaker label").option("--text <text>", "Expression text").option("--note <text>", "Expression note").option("--intent <text>", "Expression intent").option("--order <number>", "Expression order").option("--storyboard <id-or-path>", "Storyboard covered by this expression; repeatable", collectOption, []).option("--from-storyboard <id>", "First storyboard id covered by this expression").option("--to-storyboard <id>", "Last storyboard id covered by this expression").option("--script-block <id>", "Referenced script block id").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parseExpressionUnitParentOptions(options);
        const result = await createCliEngine(merged).createExpressionUnit({
          id: options.id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          title: options.title,
          slotKind: options.slotKind ?? options.kind ?? "visual",
          kind: options.kind,
          speaker: options.speaker,
          text: options.text,
          note: options.note,
          intent: options.intent,
          order: parseOptionalNumberOption(options.order, "order"),
          span: expressionUnitSpanFromOptions(options),
          scriptBlockId: options.scriptBlock
        });
        printResult(result, merged);
      });
      expressionUnit.command("modify <id>").description("Modify an expression unit").option("--title <title>", "Expression unit title").option("--slot-kind <kind>", "Expression slot kind: visual, voice, subtitle, or audio").option("--kind <kind>", "Legacy expression kind").option("--production <id>", "Production id").option("--segment <id-or-path>", "Segment id or path").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--speaker <text>", "Speaker label").option("--text <text>", "Expression text").option("--note <text>", "Expression note").option("--intent <text>", "Expression intent").option("--order <number>", "Expression order").option("--storyboard <id-or-path>", "Storyboard covered by this expression; repeatable", collectOption, []).option("--from-storyboard <id>", "First storyboard id covered by this expression").option("--to-storyboard <id>", "Last storyboard id covered by this expression").option("--script-block <id>", "Referenced script block id").option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const source = parseExpressionUnitParentOptions(options);
        const result = await createCliEngine(merged).updateExpressionUnit({
          id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          title: options.title,
          slotKind: options.slotKind,
          kind: options.kind,
          speaker: options.speaker,
          text: options.text,
          note: options.note,
          intent: options.intent,
          order: parseOptionalNumberOption(options.order, "order"),
          span: expressionUnitSpanFromOptions(options),
          scriptBlockId: options.scriptBlock
        });
        printResult(result, merged);
      });
      expressionUnit.command("delete <idOrPath>").alias("remove").description("Delete an expression unit").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--json", "Print JSON output").action((idOrPath, options, command) => {
        return deletePlanningEntity22("expression_unit", idOrPath, options, command);
      });
      const contentUnit = program2.command("content-unit").alias("cu").description("Manage content units");
      contentUnit.command("list").description("List content units").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action((options, command) => {
        return printPlanningEntityList("content_unit", "Content units", options, command);
      });
      contentUnit.command("status <idOrPath>").alias("panel").description("Show source and interpreted generation status for a content unit").option("--json", "Print JSON output").action(async (idOrPath, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await deriveContentUnitStatusPanel(createCliEngine(merged), idOrPath);
        printContentUnitStatusPanel(result, merged);
      });
      contentUnit.command("context <idOrPath>").description("Show the interpreted generation context for a content unit").option("--json", "Print JSON output").action(async (idOrPath, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await deriveContentUnitStatusPanel(createCliEngine(merged), idOrPath);
        printContentUnitStatusPanel(result, merged);
      });
      contentUnit.command("backend-prompt <idOrPath>").description("Build the backend-ready prompt for a content unit from prompt refs and backend decisions").option("--json", "Print JSON output").action(async (idOrPath, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const engine = createCliEngine(merged);
        const contentUnit2 = await findContentUnitForPanel(engine, idOrPath);
        const result = await engine.buildContentUnitBackendPrompt(contentUnit2.id ?? idOrPath);
        printResult(result, merged);
      });
      contentUnit.command("add").alias("create").description("Create or update a project-level content unit with prompt refs").option("--id <id>", "Content unit id").option("--title <title>", "Content unit title").option("--type <type>", "Content unit type, such as asset_ref, keyframe_ref, storyboard_ref, scence_moment_ref, expression_unit_ref, or timeline_assembly_ref").option("--content-unit-type <type>", "Content unit type field; same as --type").option("--kind <kind>", "Deprecated alias for --type").option("--output-kind <kind>", "Output kind: image, video, audio, text, or metadata").option("--timeline-assembly <ref>", "Timeline assembly target for timeline_assembly_ref, e.g. timeline_assembly:episode:episode_01 or episode_01 with --scope-kind").option("--scope-kind <kind>", "Timeline namespace scope kind for timeline_assembly_ref, e.g. episode, act, sequence, or beat").option("--scope-ref <id>", "Timeline namespace scope id/ref for timeline_assembly_ref").option("--asset <id-or-path>", "Asset id or path for asset_ref content units").option("--production <id>", "Production id; legacy assembly alias for production_ref").option("--segment <id>", "Segment id; legacy assembly alias for segment_ref").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--expression-unit <id-or-path>", "Expression unit id or path").option("--storyboard <id-or-path>", "Storyboard id or path; storyboard_ref defaults to main when omitted").option("--keyframe <id-or-path>", "Keyframe id or path for keyframe_ref content units").option("--audio-cue <id-or-path>", "Audio cue id or path for audio content units").option("--prompt <text>", "Editable generation prompt").option("--negative-prompt <text>", "Negative generation prompt").option("--description <text>", "Content unit description").option("--order <number>", "Content unit order").option("--duration <seconds>", "Expected duration in seconds").option("--aspect-ratio <value>", "Model intent aspect ratio").option("--capability <value>", "Model intent capability").option("--provider <value>", "Model provider hint").option("--model <value>", "Model name hint").option("--quality <value>", "Model quality hint").option("--shot-size <value>", "Shot size").option("--camera-angle <value>", "Camera angle").option("--camera-motion <value>", "Camera motion").option("--param <key=value...>", "Model intent param, repeatable", collectOption, []).option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await saveContentUnitFromCliOptions(createCliEngine(merged), options, { defaultType: "storyboard_ref" });
        printResult(result, merged);
      });
      contentUnit.command("modify <id>").description("Modify a content unit").option("--title <title>", "Content unit title").option("--type <type>", "Content unit type, such as asset_ref, keyframe_ref, storyboard_ref, scence_moment_ref, expression_unit_ref, or timeline_assembly_ref").option("--content-unit-type <type>", "Content unit type field; same as --type").option("--kind <kind>", "Deprecated alias for --type").option("--output-kind <kind>", "Output kind: image, video, audio, text, or metadata").option("--timeline-assembly <ref>", "Timeline assembly target for timeline_assembly_ref, e.g. timeline_assembly:episode:episode_01 or episode_01 with --scope-kind").option("--scope-kind <kind>", "Timeline namespace scope kind for timeline_assembly_ref, e.g. episode, act, sequence, or beat").option("--scope-ref <id>", "Timeline namespace scope id/ref for timeline_assembly_ref").option("--asset <id-or-path>", "Asset id or path for asset_ref content units").option("--production <id>", "Production id; legacy assembly alias for production_ref").option("--segment <id>", "Segment id; legacy assembly alias for segment_ref").option("--scene-moment <id-or-path>", "Scene moment id or path").option("--expression-unit <id-or-path>", "Expression unit id or path").option("--storyboard <id-or-path>", "Storyboard id or path").option("--keyframe <id-or-path>", "Keyframe id or path for keyframe_ref content units").option("--audio-cue <id-or-path>", "Audio cue id or path for audio content units").option("--prompt <text>", "Editable generation prompt").option("--negative-prompt <text>", "Negative generation prompt").option("--description <text>", "Content unit description").option("--order <number>", "Content unit order").option("--duration <seconds>", "Expected duration in seconds").option("--aspect-ratio <value>", "Model intent aspect ratio").option("--capability <value>", "Model intent capability").option("--provider <value>", "Model provider hint").option("--model <value>", "Model name hint").option("--quality <value>", "Model quality hint").option("--shot-size <value>", "Shot size").option("--camera-angle <value>", "Camera angle").option("--camera-motion <value>", "Camera motion").option("--param <key=value...>", "Model intent param, repeatable", collectOption, []).option("--json", "Print JSON output").action(async (id, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await saveContentUnitFromCliOptions(createCliEngine(merged), { ...options, id });
        printResult(result, merged);
      });
      contentUnit.command("delete <idOrPath>").alias("remove").description("Delete a content unit").option("--production <id>", "Filter by production id").option("--segment <id-or-path>", "Filter by segment id or path").option("--scene-moment <id-or-path>", "Filter by scene moment id or path").option("--json", "Print JSON output").action((idOrPath, options, command) => {
        return deletePlanningEntity22("content_unit", idOrPath, options, command);
      });
      const entity = program2.command("entity").description("Inspect workspace entities");
      entity.command("list [entityKind]").description("List indexed entities").option("--kind <kind>", "Filter by domain-specific kind").option("--query <text>", "Search text").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action(async (entityKind, options, command) => {
        if (entityKind !== void 0 && !isSemanticEntityKind32(entityKind)) {
          throw new Error(`unknown entity kind: ${entityKind}`);
        }
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).queryEntities({
          ...entityKind !== void 0 ? { entityKind } : {},
          ...options.kind !== void 0 ? { kind: options.kind } : {},
          ...options.query !== void 0 ? { query: options.query } : {},
          ...options.limit !== void 0 ? { limit: parsePositiveIntegerOption(options.limit, "limit") } : {}
        });
        printEntityList(result, merged, {
          title: entityKind ? `${entityKind} entities` : "Entities",
          columns: [
            { header: "Kind", value: (item) => item.entityKind },
            { header: "ID", value: (item) => item.id },
            { header: "Type", value: (item) => item.record.setting_kind ?? item.record.asset_kind ?? item.record.content_unit_type ?? item.record.cue_kind ?? item.record.kind },
            { header: "Title", value: (item) => item.record.title ?? item.record.label ?? item.id },
            { header: "Path", value: (item) => item.path, maxWidth: 52 }
          ]
        });
      });
      const interpreter = program2.command("interpreter").description("Run interpreter workflows");
      interpreter.command("overview").description("Show the workspace overview and next interpreter actions").option("--json", "Print JSON output").action((options, command) => {
        return overviewWorkspaceFromCliOptions(options, command);
      });
      interpreter.command("inspect").description("Inspect source edits, diagnostics, and predicted impact").option("--commit <ref>", "Compare current source against a specific git commit/ref").option("--json", "Print JSON output").action((options, command) => {
        return inspectWorkspaceFromCliOptions(options, command);
      });
      interpreter.command("review").description("Review source edits, diagnostics, and predicted impact").option("--commit <ref>", "Compare current source against a specific git commit/ref").option("--json", "Print JSON output").action((options, command) => {
        return inspectWorkspaceFromCliOptions(options, command);
      });
      interpreter.command("interpret").description("Validate the current source workspace and derive interpreter data in memory").option("--json", "Print JSON output").action((options, command) => {
        return interpretWorkspaceFromCliOptions(options, command);
      });
      interpreter.command("prompt <contentUnitId>").description("Derive the runtime artifact for one content unit from the current index").option("--json", "Print JSON output").action(async (contentUnitId, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).deriveContentUnitArtifact(contentUnitId);
        printResult(result, merged);
      });
      interpreter.command("artifacts").description("Derive interpreter artifacts in memory from the current index").option("--interpretation-id <id>", "Interpretation id used in derived artifacts").option("--created-at <iso>", "Creation timestamp used in derived artifacts").option("--json", "Print JSON output").action(async (options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await createCliEngine(merged).deriveArtifacts({
          ...options.interpretationId !== void 0 ? { interpretationId: options.interpretationId } : {},
          ...options.createdAt !== void 0 ? { createdAt: options.createdAt } : {}
        });
        printResult(result, merged);
      });
      interpreter.command("work-plan").description("Derive the in-memory production work plan from current source and decision state").option("--json", "Print JSON output").action((options, command) => {
        return productionWorkPlanFromCliOptions(options, command);
      });
      const interpreterRegen = interpreter.command("regen").alias("regenerate").description("Inspect regeneration work after interpret");
      interpreterRegen.command("plan").description("Show generated outputs and prompt bundles that may need regeneration").option("--json", "Print JSON output").action((options, command) => {
        return regenerationPlanFromCliOptions(options, command);
      });
      program2.command("overview").alias("status").description("Show the workspace overview and next interpreter actions").option("--json", "Print JSON output").action((options, command) => {
        return overviewWorkspaceFromCliOptions(options, command);
      });
      program2.command("inspect").description("Inspect source edits, diagnostics, and predicted impact").option("--commit <ref>", "Compare current source against a specific git commit/ref").option("--json", "Print JSON output").action((options, command) => {
        return inspectWorkspaceFromCliOptions(options, command);
      });
      program2.command("review").description("Review source edits, diagnostics, and predicted impact").option("--commit <ref>", "Compare current source against a specific git commit/ref").option("--json", "Print JSON output").action((options, command) => {
        return inspectWorkspaceFromCliOptions(options, command);
      });
      program2.command("interpret").description("Validate the current source workspace and derive interpreter data in memory").option("--json", "Print JSON output").action((options, command) => {
        return interpretWorkspaceFromCliOptions(options, command);
      });
      program2.command("work-plan").description("Derive the in-memory production work plan from current source and decision state").option("--json", "Print JSON output").action((options, command) => {
        return productionWorkPlanFromCliOptions(options, command);
      });
      const regen = program2.command("regen").alias("regenerate").description("Inspect regeneration work after interpret");
      regen.command("plan").description("Show generated outputs and prompt bundles that may need regeneration").option("--json", "Print JSON output").action((options, command) => {
        return regenerationPlanFromCliOptions(options, command);
      });
      const candidate = program2.command("candidate").description("Manage generated and external candidates");
      candidate.command("list [target]").description("List candidates for a target or across the workspace").option("--kind <kind>", "Target kind filter: asset, storyboard, keyframe, or content_unit").option("--target-kind <kind>", "Target kind filter; overrides --kind").option("--source <source>", "Filter by candidate source").option("--status <status>", "Filter by candidate status").option("--resource-id <id>", "Filter by resource id").option("--output-kind <kind>", "Filter by content candidate output kind").option("--selected", "Only show selected candidates").option("--unselected", "Only show unselected candidates").option("--stale", "Only show selected candidates whose selection is stale").option("--query <text>", "Search candidate id, target id/path, source, status, or resource id").option("--limit <number>", "Maximum rows to print").option("--json", "Print JSON output").action(async (target, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await listCandidatesFromCliOptions(createCliEngine(merged), target, options);
        printCandidateList(result, merged);
      });
      candidate.command("add <target>").description("Manually add an external resource as a content_unit candidate").option("--id <id>", "Candidate id; generated from resource id when omitted").option("--kind <kind>", "Candidate output kind or content_unit_type hint").option("--output-kind <kind>", "Candidate output kind: image, video, audio, text, or metadata").option("--target-kind <kind>", "Target kind override; defaults to content_unit for id targets").option("--resource-id <id>", "External resource id to add as a candidate").option("--source <source>", "Candidate source label", "manual").option("--notes <text>", "Candidate notes").option("--metadata <key=value...>", "Candidate metadata field, repeatable", collectOption, []).option("--json", "Print JSON output").action(async (target, options, command) => {
        if (!options.resourceId) throw new Error("--resource-id is required");
        const merged = mergeGlobalOptions(options, command);
        const result = await addCandidateFromCliOptions(createCliEngine(merged), target, options);
        printResult(result, merged);
      });
      candidate.command("select <target> <candidateId>").alias("choose").description("Select a content_unit candidate").option("--kind <kind>", "Target kind override; defaults to content_unit for id targets").option("--target-kind <kind>", "Target kind override; defaults to content_unit for id targets").option("--reason <reason>", "Selection reason").option("--json", "Print JSON output").action(async (target, candidateId, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await selectCandidateFromCliOptions(createCliEngine(merged), target, candidateId, options);
        printResult(result, merged);
      });
      program2.command("select <target> <candidateId>").description("Select a generated candidate for a target path").option("--kind <kind>", "Target kind override; defaults to content_unit for id targets").option("--target-kind <kind>", "Target kind override; defaults to content_unit for id targets").option("--reason <reason>", "Selection reason").option("--json", "Print JSON output").action(async (target, candidateId, options, command) => {
        const merged = mergeGlobalOptions(options, command);
        const result = await selectCandidateFromCliOptions(createCliEngine(merged), target, candidateId, options);
        printResult(result, merged);
      });
    }
    function mergeGlobalOptions(options, command) {
      const global = command.optsWithGlobals ? command.optsWithGlobals() : command.opts();
      return {
        ...options,
        json: options.json ?? (typeof global.json === "boolean" ? global.json : void 0),
        cwd: options.cwd ?? (typeof global.cwd === "string" ? global.cwd : void 0) ?? (typeof global.workspace === "string" ? global.workspace : void 0),
        commit: options.commit ?? (typeof global.commit === "string" ? global.commit : void 0)
      };
    }
    function createCliEngine(options) {
      const envWorkspaceDir = stringValue122(process.env.MOVSCRIPT_WORKSPACE_DIR);
      const envProjectDir = stringValue122(process.env.MOVSCRIPT_PROJECT_DIR);
      const workspaceDir2 = options.cwd ?? envWorkspaceDir;
      const projectDir = options.cwd === void 0 ? envProjectDir : void 0;
      const decisionStore = createCliBackendDecisionStore({ workspaceDir: envWorkspaceDir ?? options.cwd });
      return createNodeMovScriptEngine2({
        ...projectDir !== void 0 ? { projectDir } : workspaceDir2 !== void 0 ? { workspaceDir: workspaceDir2 } : {},
        ...decisionStore ? { decisionStore } : {}
      });
    }
    function createCliBackendDecisionStore(input3) {
      const projectId = stringValue122(process.env.MOVSCRIPT_PROJECT_ID);
      if (!projectId) return void 0;
      const session = resolveMovScriptBackendSession2({
        workspaceDir: input3.workspaceDir,
        server: stringValue122(process.env.MOVSCRIPT_DATA_SERVICE_URL),
        token: stringValue122(process.env.MOVSCRIPT_DATA_SERVICE_TOKEN),
        userId: stringValue122(process.env.MOVSCRIPT_USER_ID)
      });
      return createMovScriptBackendDecisionStore2({
        baseUrl: session.baseURL,
        projectId,
        ...session.token ? { token: session.token } : {},
        ...session.userId ? { headers: { "X-User-ID": session.userId } } : {}
      });
    }
    function createCliFileRepository(options) {
      return createCliFileRepositoryFromEngine(createCliEngine(options));
    }
    function createCliFileRepositoryFromEngine(engine) {
      return createNodeMovScriptWorkspaceFileRepository2(engine.projectDir);
    }
    async function addCandidateFromCliOptions(engine, target, options) {
      if (!options.resourceId) throw new Error("--resource-id is required");
      const resourceId = resourceIdOption(options.resourceId);
      if (resourceId === void 0) throw new Error("--resource-id is required");
      const targetKind = parseTargetKindOption(options.targetKind, target, { defaultContentUnit: true });
      if (targetKind === "content_unit") {
        const contentUnit = await resolveContentUnitTarget(engine, target);
        const contentUnitId = stringValue122(contentUnit.id ?? contentUnit.record.id);
        if (!contentUnitId) throw new Error(`content_unit missing id: ${contentUnit.path}`);
        return engine.createContentCandidate({
          contentUnitId,
          candidateId: options.id,
          source: options.source,
          outputs: [{
            kind: parseContentCandidateOutputKind(options.outputKind ?? options.kind, contentUnit.record),
            resource_id: resourceId,
            metadata: parseOptionalKeyValueOptions(options.metadata ?? [])
          }],
          promptSnapshot: options.notes !== void 0 ? { notes: options.notes } : void 0
        });
      }
      return engine.appendCandidate({
        targetPath: targetPathFromSelectionTarget(target),
        targetKind,
        payload: pruneUndefined19({
          id: options.id,
          resource_id: resourceId,
          source: options.source,
          notes: options.notes,
          metadata: parseOptionalKeyValueOptions(options.metadata ?? [])
        })
      });
    }
    async function selectCandidateFromCliOptions(engine, target, candidateId, options) {
      const targetKind = parseTargetKindOption(options.targetKind ?? options.kind, target, { defaultContentUnit: true });
      if (targetKind === "content_unit") {
        const contentUnit = await resolveContentUnitTarget(engine, target);
        const contentUnitId = stringValue122(contentUnit.id ?? contentUnit.record.id);
        if (!contentUnitId) throw new Error(`content_unit missing id: ${contentUnit.path}`);
        const candidate = await findContentUnitCandidate(engine, contentUnit, candidateId);
        return engine.selectContentUnitCandidate({
          contentUnitId,
          candidateId,
          resourceId: candidateResourceId(candidate?.record),
          reason: options.reason
        });
      }
      return engine.selectCandidate({
        targetPath: targetPathFromSelectionTarget(target),
        targetKind,
        candidateId,
        ...options.reason !== void 0 ? { reason: options.reason } : {}
      });
    }
    async function listCandidatesFromCliOptions(engine, target, options) {
      const targetKind = parseOptionalTargetKindOption(options.targetKind ?? options.kind);
      const items = target !== void 0 ? await listCandidatesForTarget(engine, target, targetKind) : await listWorkspaceCandidates(engine, targetKind);
      if (options.selected && options.unselected) throw new Error("--selected and --unselected cannot be used together");
      const query = options.query?.trim().toLowerCase();
      const filtered = items.filter((item) => candidateMatchesListOptions(item, options, query));
      const limit = options.limit !== void 0 ? parsePositiveIntegerOption(options.limit, "limit") : void 0;
      return limit === void 0 ? filtered : filtered.slice(0, limit);
    }
    async function listCandidatesForTarget(engine, target, targetKind) {
      const resolvedTargetKind = targetKind ?? parseTargetKindOption(void 0, target, { defaultContentUnit: true });
      if (resolvedTargetKind === "content_unit") {
        const contentUnit = await resolveContentUnitTarget(engine, target);
        return listContentUnitCandidateItems(engine, contentUnit);
      }
      const entity = await readInlineCandidateTarget(engine, target, resolvedTargetKind);
      return inlineCandidateItemsFromEntity(entity, resolvedTargetKind);
    }
    async function listWorkspaceCandidates(engine, targetKind) {
      const kinds = targetKind === void 0 ? ["content_unit", "asset", "storyboard", "keyframe"] : [targetKind];
      const groups = await Promise.all(kinds.map(async (kind) => {
        if (kind === "content_unit") {
          const contentUnits = await engine.queryEntities({ entityKind: "content_unit" });
          const candidates = await Promise.all(contentUnits.map((contentUnit) => listContentUnitCandidateItems(engine, contentUnit)));
          return candidates.flat();
        }
        const entities = await engine.queryEntities({ entityKind: kind });
        return entities.flatMap((entity) => inlineCandidateItemsFromEntity(entity, kind));
      }));
      return groups.flat().sort((left, right) => {
        const byTarget = left.targetPath.localeCompare(right.targetPath);
        if (byTarget !== 0) return byTarget;
        return String(left.id ?? "").localeCompare(String(right.id ?? ""));
      });
    }
    async function listContentUnitCandidateItems(engine, contentUnit) {
      const contentUnitId = stringValue122(contentUnit.id ?? contentUnit.record.id) ?? String(contentUnit.id ?? contentUnit.record.id ?? "");
      const [candidates, selectionValidity, selection] = await Promise.all([
        listContentUnitCandidates(engine, contentUnit),
        contentUnitId ? engine.workspaceService.readContentUnitSelectionValidity(contentUnitId).catch(() => void 0) : void 0,
        readContentUnitSelectionForList(engine, contentUnit)
      ]);
      const selectedCandidateId = selectionValidity?.candidate_id ?? selection?.candidate_id;
      return candidates.map((candidate) => contentUnitCandidateListItem(contentUnit, candidate, selectionValidity, selectedCandidateId));
    }
    async function readContentUnitSelectionForList(engine, contentUnit) {
      const contentUnitDir = normalizeCliPath(contentUnit.path.replace(/\/content_unit\.json$/, ""));
      const index = await engine.loadIndex().catch(() => void 0);
      const decisionContext = index?.documents.find((document) => {
        if (!isRecord302(document.data)) return false;
        return document.data.schema === "movscript.decision_context.v1" && document.data.target_kind === "content_unit" && document.data.target_ref === contentUnitDir;
      })?.data;
      const overlaySelection = recordField7(recordField7(decisionContext)?.selection);
      if (overlaySelection) return overlaySelection;
      const repository = createCliFileRepositoryFromEngine(engine);
      const file = await repository.read({ path: `${contentUnitDir}/selection.json` }).catch(() => void 0);
      if (!file) return void 0;
      const parsed = JSON.parse(file.content);
      return recordField7(parsed);
    }
    function contentUnitCandidateListItem(contentUnit, candidate, selectionValidity, selectedCandidateId) {
      const record = candidate.record;
      const outputs = arrayField72(record.outputs).filter(isRecord302);
      const id = record.id;
      const selected = selectedCandidateId !== void 0 && String(selectedCandidateId) === String(id);
      return {
        targetKind: "content_unit",
        targetId: candidateIdValue(contentUnit.id ?? contentUnit.record.id),
        targetPath: contentUnit.path,
        path: candidate.path,
        selected,
        stale: selected ? selectionValidity?.stale === true : void 0,
        id,
        source: stringValue122(record.source),
        status: stringValue122(record.status),
        resourceIds: outputs.map((output3) => output3.resource_id).filter((value) => typeof value === "string" || typeof value === "number"),
        outputs: outputs.map((output3) => `${scalarDisplayValue2(output3.kind)}:${scalarDisplayValue2(output3.resource_id)}`),
        hash: shortHash(recordField7(record.input_version ?? record.prompt_snapshot)?.hash),
        createdAt: stringValue122(record.created_at),
        notes: stringValue122(record.notes),
        record
      };
    }
    async function readInlineCandidateTarget(engine, target, targetKind) {
      const targetPath = normalizeCliPath(targetPathFromSelectionTarget(target));
      const repository = createCliFileRepositoryFromEngine(engine);
      const file = await repository.read({ path: targetPath });
      const parsed = JSON.parse(file.content);
      if (!isRecord302(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
      return {
        entityKind: targetKind,
        id: parsed.id,
        record: parsed,
        path: file.path
      };
    }
    function inlineCandidateItemsFromEntity(entity, targetKind) {
      const selection = recordField7(entity.record.lock);
      const selectedCandidateId = selection?.candidate_id;
      return arrayField72(entity.record.candidates).filter(isRecord302).map((candidate) => {
        const id = candidate.id;
        const resourceId = candidate.resource_id;
        const selected = selectedCandidateId !== void 0 && String(selectedCandidateId) === String(id);
        return {
          targetKind,
          targetId: candidateIdValue(entity.id ?? entity.record.id),
          targetPath: entity.path,
          selected,
          id,
          source: stringValue122(candidate.source),
          resourceIds: typeof resourceId === "string" || typeof resourceId === "number" ? [resourceId] : [],
          outputs: typeof resourceId === "string" || typeof resourceId === "number" ? [`resource:${resourceId}`] : [],
          notes: stringValue122(candidate.notes),
          record: candidate
        };
      });
    }
    function candidateListSearchText(item) {
      return [
        item.targetKind,
        item.targetId,
        item.targetPath,
        item.path,
        item.id,
        item.source,
        item.status,
        item.resourceIds.join(" "),
        item.outputs.join(" "),
        item.notes
      ].map((value) => scalarDisplayValue2(value).toLowerCase()).join(" ");
    }
    function candidateMatchesListOptions(item, options, query) {
      if (options.source !== void 0 && item.source !== options.source) return false;
      if (options.status !== void 0 && item.status !== options.status) return false;
      if (options.resourceId !== void 0 && !item.resourceIds.some((resourceId) => String(resourceId) === options.resourceId)) return false;
      if (options.outputKind !== void 0 && !item.outputs.some((output3) => output3.startsWith(`${options.outputKind}:`))) return false;
      if (options.selected && !item.selected) return false;
      if (options.unselected && item.selected) return false;
      if (options.stale && item.stale !== true) return false;
      if (query !== void 0 && !candidateListSearchText(item).includes(query)) return false;
      return true;
    }
    function candidateIdValue(value) {
      if (typeof value === "string" && value.trim()) return value;
      if (typeof value === "number" && Number.isFinite(value)) return value;
      return void 0;
    }
    function printCandidateList(candidates, options) {
      if (options.json) {
        printResult(candidates, options);
        return;
      }
      if (candidates.length === 0) {
        console.log("Candidates: no candidates found");
        return;
      }
      console.log(renderTable([
        { header: "Sel", value: (item) => item.selected ? item.stale ? "stale" : "*" : "" },
        { header: "Target", value: (item) => `${item.targetKind}:${scalarDisplayValue2(item.targetId)}`, maxWidth: 36 },
        { header: "Candidate", value: (item) => item.id, maxWidth: 36 },
        { header: "Source", value: (item) => item.source },
        { header: "Status", value: (item) => item.status },
        { header: "Resources", value: (item) => item.outputs.length ? item.outputs.join(", ") : item.resourceIds.join(", "), maxWidth: 40 },
        { header: "Hash", value: (item) => item.hash },
        { header: "Path", value: (item) => item.path ?? item.targetPath, maxWidth: 64 }
      ], candidates));
    }
    async function createKeyframeFromCliOptions(engine, options, source) {
      const keyframe = pruneUndefined19({
        id: options.id,
        title: options.title,
        role: options.role,
        visual_intent: options.visualIntent
      });
      return engine.createKeyframe({
        id: keyframe.id,
        title: keyframe.title,
        role: keyframe.role,
        visualIntent: keyframe.visual_intent,
        productionId: source.productionId,
        segmentId: source.segmentId,
        sceneMomentId: source.sceneMomentId,
        expressionUnitId: source.expressionUnitId
      });
    }
    async function resolveContentUnitTarget(engine, target) {
      const normalized = normalizeCliPath(targetPathFromSelectionTarget(target));
      const id = contentUnitIdFromTarget(target);
      const entities = await engine.queryEntities({ entityKind: "content_unit" });
      const match = entities.find((entity) => String(entity.id ?? "") === id) ?? entities.find((entity) => String(entity.record.id ?? "") === id) ?? entities.find((entity) => normalizeCliPath(entity.path) === normalized) ?? entities.find((entity) => normalizeCliPath(entity.path.replace(/\/content_unit\.json$/, "")) === normalized) ?? entities.find((entity) => normalizeCliPath(`content_units/${String(entity.id ?? "")}`) === normalized);
      if (!match) throw new Error(`content_unit not found: ${target}`);
      return match;
    }
    function contentUnitIdFromTarget(target) {
      const path = normalizeCliPath(targetPathFromSelectionTarget(target));
      const parts = path.split("/").filter(Boolean);
      const markerIndex = parts.indexOf("content_units");
      if (markerIndex >= 0 && parts[markerIndex + 1]) return parts[markerIndex + 1];
      const separator = target.indexOf(":");
      if (separator > 0) return target.slice(separator + 1);
      return path.replace(/\/content_unit\.json$/, "");
    }
    function parseContentCandidateOutputKind(value, contentUnitRecord) {
      const outputKind = targetKindValue(value) ? stringValue122(contentUnitRecord.output_kind) ?? outputKindFromContentUnitType(stringValue122(contentUnitRecord.content_unit_type)) : value ?? stringValue122(contentUnitRecord.output_kind) ?? outputKindFromContentUnitType(stringValue122(contentUnitRecord.content_unit_type));
      if (outputKind === "image" || outputKind === "video" || outputKind === "audio" || outputKind === "text" || outputKind === "metadata") return outputKind;
      const inferred = outputKindFromContentUnitType(outputKind);
      if (inferred) return inferred;
      throw new Error("candidate output kind must be image, video, audio, text, or metadata");
    }
    function outputKindFromContentUnitType(value) {
      if (!value) return void 0;
      if (value.includes("video")) return "video";
      if (value === "timeline_assembly_ref") return "video";
      if (value === "scence_moment_ref" || value === "scene_moment_ref") return "video";
      if (value.includes("image") || value.includes("frame") || value.includes("asset_ref")) return "image";
      if (value.includes("audio") || value.includes("sound")) return "audio";
      if (value.includes("text") || value.includes("script")) return "text";
      return void 0;
    }
    async function saveContentUnitFromCliOptions(engine, options, config = {}) {
      const requestedType = contentUnitTypeFromOptions(options, config.defaultType);
      const source = parseContentUnitSourceOptions(options, requestedType);
      const requestedOutputKind = parseOptionalContentUnitOutputKind(options.outputKind) ?? (requestedType !== void 0 ? defaultCliContentUnitOutputKind(requestedType) : void 0);
      if (requestedType !== void 0) validateContentUnitSourceForType(requestedType, source, options);
      const shouldDefaultCapability = config.defaultType !== void 0 || requestedType !== void 0 || requestedOutputKind !== void 0;
      const contentUnitType22 = requestedType ?? "storyboard_ref";
      const outputKind = requestedOutputKind ?? defaultCliContentUnitOutputKind(contentUnitType22);
      validateContentUnitSourceForType(contentUnitType22, source, options);
      return engine.createContentUnit({
        id: options.id,
        title: options.title,
        contentUnitType: contentUnitType22,
        outputKind,
        prompt: options.prompt,
        negativePrompt: options.negativePrompt,
        description: options.description,
        order: parseOptionalNumberOption(options.order, "order"),
        modelIntent: buildContentUnitModelIntent(options, outputKind, { defaultCapability: shouldDefaultCapability }),
        targetCategory: source.targetCategory,
        targetKind: source.targetKind,
        targetRef: source.targetRef,
        scopeKind: source.scopeKind,
        scopeRef: source.scopeRef,
        productionId: source.productionId,
        segmentId: source.segmentId,
        sceneMomentId: source.sceneMomentId,
        expressionUnitId: source.expressionUnitId,
        storyboardId: source.storyboardId,
        keyframeId: source.keyframeId,
        assetRef: source.assetRef,
        audioCueId: source.audioCueId
      });
    }
    function contentUnitTypeFromOptions(options, defaultType) {
      const value = options.contentUnitType ?? options.type ?? options.kind ?? (options.timelineAssembly !== void 0 || options.scopeKind !== void 0 || options.scopeRef !== void 0 ? "timeline_assembly_ref" : void 0) ?? defaultType;
      if (!value) return void 0;
      return normalizeContentUnitType(value);
    }
    function normalizeContentUnitType(value) {
      switch (value) {
        case "asset":
          return "asset_ref";
        case "keyframe":
          return "keyframe_ref";
        case "storyboard":
          return "storyboard_ref";
        case "timeline_assembly":
        case "assembly":
        case "timeline":
          return "timeline_assembly_ref";
        case "scene_moment":
        case "scene":
        case "moment":
          return "scence_moment_ref";
        case "expression_unit":
        case "expression":
        case "expr":
          return "expression_unit_ref";
        default:
          return value;
      }
    }
    function parseOptionalContentUnitOutputKind(value) {
      if (value === void 0) return void 0;
      if (typeof value !== "string") throw new Error("output kind must be image, video, audio, text, or metadata");
      if (CONTENT_UNIT_OUTPUT_KINDS.includes(value)) return value;
      throw new Error(`unknown output kind: ${value}`);
    }
    function defaultCliContentUnitOutputKind(contentUnitType22) {
      switch (contentUnitType22) {
        case "asset_ref":
        case "keyframe_ref":
          return "image";
        case "storyboard_ref":
        case "timeline_assembly_ref":
        case "expression_unit_ref":
        case "scence_moment_ref":
        case "scene_moment_ref":
          return "video";
        default:
          return "metadata";
      }
    }
    function validateContentUnitSourceForType(contentUnitType22, source, options) {
      if (contentUnitType22 === "asset_ref" && !source.assetRef) throw new Error("--asset is required for asset_ref content units");
      if (contentUnitType22 === "keyframe_ref" && !source.keyframeId) throw new Error("--keyframe is required for keyframe_ref content units");
      if (contentUnitType22 === "storyboard_ref" && !source.storyboardId) throw new Error("--storyboard is required for storyboard_ref content units");
      if (contentUnitType22 === "timeline_assembly_ref" && !source.targetRef) throw new Error("--timeline-assembly with --scope-kind, or --scope-kind/--scope-ref, is required for timeline_assembly_ref content units");
      if ((contentUnitType22 === "scence_moment_ref" || contentUnitType22 === "scene_moment_ref") && !source.sceneMomentId) throw new Error("--scene-moment is required for scence_moment_ref content units");
      if (contentUnitType22 === "expression_unit_ref" && !source.expressionUnitId) throw new Error("--expression-unit is required for expression_unit_ref content units");
      if (contentUnitType22 !== "timeline_assembly_ref" && (options.timelineAssembly !== void 0 || options.scopeKind !== void 0 || options.scopeRef !== void 0)) {
        throw new Error("--timeline-assembly, --scope-kind, and --scope-ref are only valid for timeline_assembly_ref content units");
      }
      if (contentUnitType22 === "storyboard_ref" && !options.storyboard && source.storyboardId === "main" && !source.sceneMomentId) {
        throw new Error("--scene-moment is required when storyboard_ref uses the default --storyboard main");
      }
    }
    function buildContentUnitModelIntent(options, outputKind, config = {}) {
      const params = pruneUndefined19({
        ...parseOptionalKeyValueOptions(options.param ?? []) ?? {},
        shot_size: options.shotSize,
        camera_angle: options.cameraAngle,
        camera_motion: options.cameraMotion
      });
      const modelIntent = pruneUndefined19({
        capability: options.capability ?? (config.defaultCapability ? outputKind : void 0),
        provider: options.provider,
        model: options.model,
        quality: options.quality,
        aspect_ratio: options.aspectRatio,
        duration_sec: parseOptionalNumberOption(options.duration, "duration"),
        params: Object.keys(params).length ? params : void 0
      });
      return Object.keys(modelIntent).length ? modelIntent : void 0;
    }
    function stringValue122(value) {
      return typeof value === "string" && value.trim() ? value : void 0;
    }
    function targetKindValue(value) {
      return value === "asset" || value === "storyboard" || value === "keyframe" || value === "content_unit";
    }
    async function initProjectFromCliOptions(options, command) {
      const merged = mergeGlobalOptions(options, command);
      const engine = createCliEngine(merged);
      const result = await engine.initProject({
        ...options.id !== void 0 ? { projectId: options.id } : {},
        ...options.title !== void 0 ? { title: options.title } : {},
        ...options.language !== void 0 ? { language: options.language } : {},
        standards: parseKeyValueOptions(options.standard ?? []),
        overwrite: Boolean(options.overwrite)
      });
      printResult({ projectDir: engine.projectDir, ...result }, merged);
    }
    async function createDemoProjectFromCliOptions(options, command) {
      const merged = mergeGlobalOptions(options, command);
      const engine = createCliEngine(merged);
      const repository = createNodeMovScriptWorkspaceFileRepository2(engine.projectDir);
      const writtenPaths = await writeDemoProject(repository, {
        projectId: options.id ?? "demo",
        title: options.title ?? "Demo Film",
        overwrite: Boolean(options.overwrite)
      });
      const interpretation = options.noInterpret ? void 0 : await engine.interpret();
      printResult({
        projectDir: engine.projectDir,
        projectId: options.id ?? "demo",
        writtenPaths,
        interpretation: summarizeDemoInterpretation(interpretation),
        next: [
          "cd <projectDir>",
          "movscript review",
          "movscript interpreter prompt cu_storyboard_ref",
          "movscript interpret"
        ]
      }, merged);
      if (isRecord302(interpretation) && interpretation.status === "failed") process.exitCode = 2;
    }
    function summarizeDemoInterpretation(interpretation) {
      if (!isRecord302(interpretation)) return interpretation;
      const review = isRecord302(interpretation.review) ? interpretation.review : void 0;
      const manifest = isRecord302(interpretation.manifest) ? interpretation.manifest : void 0;
      const output3 = isRecord302(manifest?.output) ? manifest.output : void 0;
      return pruneUndefined19({
        status: interpretation.status,
        readyToInterpret: review?.readyToInterpret,
        summary: review?.summary,
        editorStatePath: output3?.editorStatePath,
        impactReportPath: output3?.impactReportPath
      });
    }
    async function writeDemoProject(repository, input3) {
      const entries = demoProjectFileEntries(input3.projectId, input3.title);
      const results = [];
      for (const [path, record] of entries) {
        const existing = await repository.read({ path }).catch(() => void 0);
        if (existing && !input3.overwrite) {
          results.push({ path, status: "skipped" });
          continue;
        }
        await repository.write({
          path,
          content: typeof record === "string" ? record : `${JSON.stringify(record, null, 2)}
`
        });
        results.push({ path, status: existing ? "updated" : "created" });
      }
      return results;
    }
    function demoProjectFileEntries(projectId, title) {
      return [
        ["workspace.json", {
          schema: "movscript.workspace.v1",
          project_id: projectId,
          title
        }],
        ["project.json", {
          schema: "movscript.project.v1",
          kind: "project",
          project_id: projectId,
          title,
          language: "zh-CN"
        }],
        ["project_standards.json", {
          schema: "movscript.project_standards.v1",
          kind: "project_standards",
          id: "project_standards",
          title: "Project standards",
          visual_style: "Cold rainy suspense realism with restrained camera movement.",
          aspect_ratio: "16:9"
        }],
        ["scripts/main/script.json", {
          schema: "movscript.script.v1",
          kind: "script",
          id: "main",
          title: "Demo Script",
          source_ref: "script.md"
        }],
        ["scripts/main/script.md", "INT. APARTMENT - NIGHT\nRain hits the window. A phone lights up in the dark.\n"],
        ["settings/hero/setting.json", {
          schema: "movscript.setting.v1",
          kind: "setting",
          id: "hero",
          setting_kind: "character",
          title: "Hero",
          profile: { appearance: "young woman, exhausted, soaked by rain" }
        }],
        ["settings/hero/states/rain/setting_state.json", {
          schema: "movscript.setting_state.v1",
          kind: "setting_state",
          id: "rain",
          title: "Rain-soaked",
          description: "Wet hair, cold phone light, anxious expression."
        }],
        ["settings/hero/states/rain/assets/wet_hair/asset.json", {
          schema: "movscript.asset.v1",
          kind: "asset",
          id: "wet_hair",
          title: "Wet hair reference",
          slot: "character_state_reference",
          asset_kind: "image",
          prompt_hint: "Wet black hair clinging to the forehead under cold blue phone light."
        }],
        ["productions/p_demo/production.json", {
          schema: "movscript.production.v1",
          kind: "production",
          id: "p_demo",
          title: "Demo Episode"
        }],
        ["productions/p_demo/segments/opening/segment.json", {
          schema: "movscript.segment.v1",
          kind: "segment",
          id: "opening",
          title: "Opening",
          order: 1
        }],
        ["productions/p_demo/segments/opening/scene_moments/phone_call/scene_moment.json", {
          schema: "movscript.scene_moment.v1",
          kind: "scene_moment",
          id: "phone_call",
          title: "Phone call",
          order: 1,
          transition: { out: "hold_then_cut" },
          action: "The phone screen lights the hero face while rain taps the window."
        }],
        ["productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone/expression_unit.json", {
          schema: "movscript.expression_unit.v1",
          kind: "expression_unit",
          id: "phone",
          slot_kind: "visual",
          expression_kind: "shot",
          title: "Phone close-up",
          order: 1,
          visual_kind: "close_up",
          content: {
            shot_size: "close_up",
            camera: { movement: "slow_push_in", lens_mm: 50 },
            lighting: { key: "cold phone screen blue light" }
          }
        }],
        ["productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone/storyboards/main/storyboard.json", {
          schema: "movscript.storyboard.v1",
          kind: "storyboard",
          id: "main",
          title: "Phone close-up",
          order: 1,
          slot: "main",
          asset_kind: "video",
          expression_unit_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone",
          timeline: { caption: "Phone glow returns.", duration_sec: 4 },
          setting_refs: [{ setting_id: "hero", setting_state_id: "rain", role: "subject" }]
        }],
        ["productions/p_demo/segments/opening/scene_moments/phone_call/audio_cues/phone_vibration/audio_cue.json", {
          schema: "movscript.audio_cue.v1",
          kind: "audio_cue",
          id: "phone_vibration",
          title: "Phone vibration",
          cue_kind: "sound_effect",
          order: 1,
          scope_ref: "productions/p_demo/segments/opening/scene_moments/phone_call",
          expression_unit_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone",
          storyboard_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone/storyboards/main",
          timing: { start: "after_action", duration_sec: 1.2 },
          prompt_hint: "Rain low, phone vibration sharp."
        }],
        ["productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/caption_1/expression_unit.json", {
          schema: "movscript.expression_unit.v1",
          kind: "expression_unit",
          id: "caption_1",
          slot_kind: "subtitle",
          expression_kind: "caption",
          text: "Unknown number lights up again."
        }],
        ["content_units/cu_scene_anchor_keyframe_ref/content_unit.json", {
          schema: "movscript.content_unit.v1",
          kind: "content_unit",
          id: "cu_scene_anchor_keyframe_ref",
          title: "Scene anchor keyframe",
          content_unit_type: "keyframe_ref",
          output_kind: "image",
          scene_moment_ref: "productions/p_demo/segments/opening/scene_moments/phone_call",
          expression_unit_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone",
          storyboard_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone/storyboards/main",
          keyframe_ref: "scene_anchor",
          edit_prompt: {
            text: "Create a scene anchor keyframe for the phone close-up.",
            negative_text: "cartoon, glamour lighting"
          },
          model_intent: { capability: "image", aspect_ratio: "16:9" }
        }],
        ["productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone/keyframes/scene_anchor/keyframe.json", {
          schema: "movscript.keyframe.v1",
          kind: "keyframe",
          id: "scene_anchor",
          title: "Scene anchor",
          scene_moment_ref: "productions/p_demo/segments/opening/scene_moments/phone_call",
          expression_unit_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone",
          storyboard_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone/storyboards/main",
          role: "continuity_anchor",
          visual_intent: "Rainy apartment close-up; cold phone light on frightened face.",
          reference_asset_refs: ["wet_hair"],
          continuity: { hair: "wet and stuck to forehead", lighting: "cold phone glow" }
        }],
        ["content_units/cu_wet_hair_ref/content_unit.json", {
          schema: "movscript.content_unit.v1",
          kind: "content_unit",
          id: "cu_wet_hair_ref",
          title: "Wet hair visual reference",
          content_unit_type: "asset_ref",
          output_kind: "image",
          asset_ref: "wet_hair",
          edit_prompt: {
            text: "Create the visual reference for wet hair continuity under cold phone light.",
            negative_text: "cartoon, glamour lighting"
          },
          model_intent: { capability: "image", aspect_ratio: "1:1" }
        }],
        ["content_units/cu_storyboard_ref/content_unit.json", {
          schema: "movscript.content_unit.v1",
          kind: "content_unit",
          id: "cu_storyboard_ref",
          title: "Phone close-up video",
          content_unit_type: "storyboard_ref",
          output_kind: "video",
          scene_moment_ref: "productions/p_demo/segments/opening/scene_moments/phone_call",
          expression_unit_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone",
          storyboard_ref: "productions/p_demo/segments/opening/scene_moments/phone_call/expression_units/phone/storyboards/main",
          keyframe_refs: ["scene_anchor"],
          edit_prompt: {
            text: "Keep the push-in slow. Preserve wet hair and frightened expression continuity.",
            negative_text: "cartoon, jump cut, overacting"
          },
          model_intent: { capability: "video", duration_sec: 4, params: { camera_motion: "slow_push_in" } }
        }]
      ];
    }
    async function overviewWorkspaceFromCliOptions(options, command) {
      const merged = mergeGlobalOptions(options, command);
      const result = await overviewMovScriptWorkspace2({
        fileRepository: createCliFileRepository(merged)
      });
      printResult(result, merged);
    }
    async function inspectWorkspaceFromCliOptions(options, command) {
      const merged = mergeGlobalOptions(options, command);
      const result = await inspectMovScriptWorkspace2({
        fileRepository: createCliFileRepository(merged),
        ...merged.commit ? { commit: merged.commit } : {}
      });
      printResult(result, merged);
      if (isRecord302(result) && result.readyToInterpret === false) process.exitCode = 2;
    }
    async function interpretWorkspaceFromCliOptions(options, command) {
      const merged = mergeGlobalOptions(options, command);
      const result = await interpretMovScriptWorkspace2({
        fileRepository: createCliFileRepository(merged)
      });
      printResult(result, merged);
      if (isRecord302(result) && result.status === "failed") process.exitCode = 2;
    }
    async function regenerationPlanFromCliOptions(options, command) {
      const merged = mergeGlobalOptions(options, command);
      const result = await planMovScriptWorkspaceRegeneration2({
        fileRepository: createCliFileRepository(merged)
      });
      printResult(result, merged);
    }
    async function productionWorkPlanFromCliOptions(options, command) {
      const merged = mergeGlobalOptions(options, command);
      const result = await createCliEngine(merged).productionWorkPlan();
      printResult(result, merged);
    }
    async function runInteractiveCli(options) {
      const rl = (0, import_promises42.createInterface)({ input: import_node_process2.stdin, output: import_node_process2.stdout });
      try {
        const engine = createCliEngine(options);
        console.log("MovScript interactive");
        console.log(`Workspace: ${engine.projectDir}`);
        console.log("Type /help for commands. Type /exit to quit.");
        if (import_node_process2.stdin.isTTY) {
          while (true) {
            const line = await readInteractiveLine(rl);
            if (line === void 0) return;
            const shouldExit = await dispatchInteractiveInputLine(line, options, engine);
            if (shouldExit) return;
          }
        } else {
          for await (const line of rl) {
            const shouldExit = await dispatchInteractiveInputLine(line, options, engine);
            if (shouldExit) return;
          }
        }
      } finally {
        rl.close();
      }
    }
    async function readInteractiveLine(rl) {
      const abortController = new AbortController();
      const onSigint = () => {
        if (rl.line.length > 0) {
          clearReadlineInput(rl);
          return;
        }
        abortController.abort();
      };
      rl.on("SIGINT", onSigint);
      try {
        return await rl.question(INTERACTIVE_PROMPT, { signal: abortController.signal });
      } catch (error) {
        if (isAbortError(error)) {
          import_node_process2.stdout.write("\n");
          return void 0;
        }
        throw error;
      } finally {
        rl.off("SIGINT", onSigint);
      }
    }
    function clearReadlineInput(rl) {
      rl.write(null, { ctrl: true, name: "a" });
      rl.write(null, { ctrl: true, name: "k" });
    }
    function isAbortError(error) {
      return error instanceof Error && (error.name === "AbortError" || "code" in error && error.code === "ABORT_ERR");
    }
    async function dispatchInteractiveInputLine(line, options, engine) {
      const trimmed = line.trim();
      if (!trimmed) return false;
      try {
        return await dispatchInteractiveSlashCommand(trimmed, options, engine);
      } catch (error) {
        console.error(error instanceof Error ? error.message : String(error));
        return false;
      }
    }
    async function dispatchInteractiveSlashCommand(line, options, engine) {
      if (!line.startsWith("/")) {
        console.log("Interactive mode currently accepts slash commands. Type /help for commands.");
        return false;
      }
      const args = parseCommandLine(line.slice(1));
      const command = args.shift();
      if (!command) return false;
      if (command === "exit" || command === "quit" || command === "q") return true;
      if (command === "help" || command === "?") {
        printInteractiveHelp();
        return false;
      }
      if (command === "init") {
        await dispatchInteractiveProjectCommand(["init", ...args], options, engine);
        return false;
      }
      if (command === "project") {
        await dispatchInteractiveProjectCommand(args, options, engine);
        return false;
      }
      if (command === "setting") {
        await dispatchInteractiveSettingCommand(args, options, engine);
        return false;
      }
      if (command === "asset") {
        await dispatchInteractiveAssetCommand(args, options, engine);
        return false;
      }
      if (command === "production") {
        await dispatchInteractiveProductionCommand(args, options, engine);
        return false;
      }
      if (command === "segment") {
        await dispatchInteractiveSegmentCommand(args, options, engine);
        return false;
      }
      if (command === "scene-moment" || command === "moment") {
        await dispatchInteractiveSceneMomentCommand(args, options, engine);
        return false;
      }
      if (command === "storyboard") {
        await dispatchInteractiveStoryboardCommand(args, options, engine);
        return false;
      }
      if (command === "keyframe") {
        await dispatchInteractiveKeyframeCommand(args, options, engine);
        return false;
      }
      if (command === "audio-cue") {
        await dispatchInteractiveAudioCueCommand(args, options, engine);
        return false;
      }
      if (command === "expression-unit" || command === "expr") {
        await dispatchInteractiveExpressionUnitCommand(args, options, engine);
        return false;
      }
      if (command === "content-unit" || command === "cu") {
        await dispatchInteractiveContentUnitCommand(args, options, engine);
        return false;
      }
      if (command === "language" || command === "lang") {
        await dispatchInteractiveLanguageCommand(args, options);
        return false;
      }
      if (command === "entity") {
        await dispatchInteractiveEntityCommand(args, options, engine);
        return false;
      }
      if (command === "candidate") {
        await dispatchInteractiveCandidateCommand(args, options, engine);
        return false;
      }
      if (command === "interpreter") {
        await dispatchInteractiveInterpreterCommand(args, options, engine);
        return false;
      }
      if (command === "overview" || command === "status") {
        const result = await overviewMovScriptWorkspace2({
          fileRepository: createCliFileRepositoryFromEngine(engine)
        });
        printResult(result, options);
        return false;
      }
      if (command === "inspect") {
        const parsed = parseSlashOptions(args);
        const result = await inspectMovScriptWorkspace2({
          fileRepository: createCliFileRepositoryFromEngine(engine),
          ...parsed.options.commit ? { commit: parsed.options.commit } : {}
        });
        printResult(result, options);
        return false;
      }
      if (command === "review") {
        const parsed = parseSlashOptions(args);
        const result = await inspectMovScriptWorkspace2({
          fileRepository: createCliFileRepositoryFromEngine(engine),
          ...parsed.options.commit ? { commit: parsed.options.commit } : {}
        });
        printResult(result, options);
        return false;
      }
      if (command === "interpret") {
        const result = await engine.interpret();
        printResult(result, options);
        return false;
      }
      if (command === "regen" || command === "regenerate") {
        await dispatchInteractiveRegenerationCommand(args, options, engine);
        return false;
      }
      throw new Error(`unknown slash command: /${command}`);
    }
    async function dispatchInteractiveLanguageCommand(args, options) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "kinds" || action === void 0) {
        printLanguageKinds(options);
        return;
      }
      if (action === "schemas") {
        printLanguageSchemaList(listLanguageSchemas(parsed.options.kind), options);
        return;
      }
      if (action === "schema") {
        const schemaIdOrKind = parsed.positionals[0];
        if (!schemaIdOrKind) throw new Error("usage: /language schema <schema-id-or-kind>");
        printResult(resolveLanguageSchema(schemaIdOrKind), options);
        return;
      }
      throw new Error(`unknown /language action: ${action}`);
    }
    async function dispatchInteractiveInterpreterCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "overview") {
        const result = await overviewMovScriptWorkspace2({
          fileRepository: createCliFileRepositoryFromEngine(engine)
        });
        printResult(result, options);
        return;
      }
      if (action === "inspect" || action === "review" || action === void 0) {
        const result = await inspectMovScriptWorkspace2({
          fileRepository: createCliFileRepositoryFromEngine(engine),
          ...parsed.options.commit ? { commit: parsed.options.commit } : {}
        });
        printResult(result, options);
        return;
      }
      if (action === "interpret") {
        const result = await engine.interpret();
        printResult(result, options);
        return;
      }
      if (action === "prompt") {
        const contentUnitId = parsed.options.contentUnit ?? parsed.options["content-unit"] ?? parsed.positionals[0];
        if (!contentUnitId) throw new Error("usage: /interpreter prompt <contentUnitId>");
        const result = await engine.deriveContentUnitArtifact(contentUnitId);
        printResult(result, options);
        return;
      }
      if (action === "artifacts") {
        const result = await engine.deriveArtifacts({
          ...parsed.options.interpretationId !== void 0 ? { interpretationId: parsed.options.interpretationId } : {},
          ...parsed.options["interpretation-id"] !== void 0 ? { interpretationId: parsed.options["interpretation-id"] } : {},
          ...parsed.options.createdAt !== void 0 ? { createdAt: parsed.options.createdAt } : {},
          ...parsed.options["created-at"] !== void 0 ? { createdAt: parsed.options["created-at"] } : {}
        });
        printResult(result, options);
        return;
      }
      if (action === "regen" || action === "regenerate") {
        await dispatchInteractiveRegenerationCommand(args, options, engine);
        return;
      }
      throw new Error(`unknown /interpreter action: ${action}`);
    }
    async function dispatchInteractiveRegenerationCommand(args, options, engine) {
      const action = args.shift();
      if (action === "plan" || action === void 0) {
        const result = await planMovScriptWorkspaceRegeneration2({
          fileRepository: createCliFileRepositoryFromEngine(engine)
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /regen action: ${action}`);
    }
    async function dispatchInteractiveProjectCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "init") {
        const standard = parsed.options.standard ?? parsed.options.standards;
        const result = await engine.initProject({
          ...parsed.options.id !== void 0 || parsed.positionals[0] !== void 0 ? { projectId: parsed.options.id ?? parsed.positionals[0] } : {},
          ...parsed.options.title !== void 0 ? { title: parsed.options.title } : {},
          ...parsed.options.language !== void 0 ? { language: parsed.options.language } : {},
          standards: parseKeyValueOptions(standard === void 0 ? [] : [standard]),
          overwrite: parsed.options.overwrite === "true"
        });
        printResult({ projectDir: engine.projectDir, ...result }, options);
        return;
      }
      if (action === "demo") {
        const demoAction = args.shift();
        if (demoAction !== "create") throw new Error(`unknown /project demo action: ${demoAction ?? "<missing>"}`);
        const demoOptions = parseSlashOptions(args);
        const repository = createNodeMovScriptWorkspaceFileRepository2(engine.projectDir);
        const writtenPaths = await writeDemoProject(repository, {
          projectId: demoOptions.options.id ?? "demo",
          title: demoOptions.options.title ?? "Demo Film",
          overwrite: demoOptions.options.overwrite === "true"
        });
        const shouldInterpret = demoOptions.options.interpret !== "false" && demoOptions.options["no-interpret"] !== "true";
        const interpretation = shouldInterpret ? await engine.interpret() : void 0;
        printResult({
          projectDir: engine.projectDir,
          projectId: demoOptions.options.id ?? "demo",
          writtenPaths,
          interpretation: summarizeDemoInterpretation(interpretation)
        }, options);
        return;
      }
      throw new Error(`unknown /project action: ${action}`);
    }
    async function dispatchInteractiveSettingCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "list" || action === void 0) {
        const result = await engine.querySettings({
          ...parsed.options.kind !== void 0 ? { kind: parsed.options.kind } : {},
          ...parsed.options.query !== void 0 ? { query: parsed.options.query } : {},
          ...parsed.options.limit !== void 0 ? { limit: parsePositiveIntegerOption(parsed.options.limit, "limit") } : {}
        });
        printSettingsTable(result, options);
        return;
      }
      if (action === "add" || action === "upsert") {
        const id = parsed.options.id ?? parsed.positionals[0];
        const title = parsed.options.title;
        if (!id && !title) {
          throw new Error("usage: /setting add <id> [--title <title>] [--kind <kind>] [--description <text>]");
        }
        const result = await engine.createSetting({
          id,
          title,
          kind: parsed.options.kind ?? "other",
          description: parsed.options.description
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /setting action: ${action}`);
    }
    async function dispatchInteractiveAssetCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "list" || action === void 0) {
        const result = await engine.queryAssets({
          ...parsed.options.setting !== void 0 ? { settingId: parsed.options.setting } : {},
          ...parsed.options.state !== void 0 ? { settingStateId: parsed.options.state } : {},
          ...parsed.options.query !== void 0 ? { query: parsed.options.query } : {},
          ...parsed.options.limit !== void 0 ? { limit: parsePositiveIntegerOption(parsed.options.limit, "limit") } : {}
        });
        printAssetsTable(result.assets, options);
        return;
      }
      if (action === "add" || action === "upsert") {
        const id = parsed.options.id ?? parsed.positionals[0];
        const title = parsed.options.title;
        if (!id && !title) {
          throw new Error("usage: /asset add <id> [--title <title>] [--setting <id>] [--state <id>] [--slot <slot>] [--kind <kind>] [--prompt <text>]");
        }
        const result = await engine.createAsset({
          id,
          title,
          settingId: parsed.options.setting,
          settingStateId: parsed.options.state,
          slot: parsed.options.slot,
          assetKind: parsed.options.kind ?? "image",
          promptHint: parsed.options.prompt,
          resourceId: resourceIdOption(parsed.options.resourceId ?? parsed.options["resource-id"])
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /asset action: ${action}`);
    }
    async function dispatchInteractiveProductionCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "create" || action === "upsert") {
        const result = await engine.createProduction({
          id: parsed.options.id ?? "main",
          title: parsed.options.title ?? parsed.positionals[0]
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /production action: ${action}`);
    }
    async function dispatchInteractiveSegmentCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "create" || action === "upsert") {
        const result = await engine.createSegment({
          id: parsed.options.id,
          productionId: parsed.options.production ?? "main",
          title: parsed.options.title ?? parsed.positionals[0],
          kind: parsed.options.kind,
          summary: parsed.options.summary,
          order: parseOptionalNumberOption(parsed.options.order, "order")
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /segment action: ${action}`);
    }
    async function dispatchInteractiveSceneMomentCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "create" || action === "upsert") {
        const source = parsePlanningParentOptions({
          production: parsed.options.production,
          segment: parsed.options.segment
        });
        const result = await engine.createSceneMoment({
          id: parsed.options.id ?? parsed.options.sceneMoment ?? parsed.options["scene-moment"],
          productionId: source.productionId,
          segmentId: source.segmentId,
          title: parsed.options.title ?? parsed.positionals[0],
          order: parseOptionalNumberOption(parsed.options.order, "order"),
          timeText: parsed.options.time,
          sceneCode: parsed.options.sceneCode ?? parsed.options["scene-code"],
          locationText: parsed.options.location,
          conditionText: parsed.options.condition,
          actionText: parsed.options.action,
          mood: parsed.options.mood,
          description: parsed.options.description
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /scene-moment action: ${action}`);
    }
    async function dispatchInteractiveStoryboardCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "create" || action === "upsert") {
        const source = parseStoryboardParentOptions({
          production: parsed.options.production,
          segment: parsed.options.segment,
          sceneMoment: parsed.options.sceneMoment ?? parsed.options["scene-moment"],
          expressionUnit: parsed.options.expressionUnit ?? parsed.options["expression-unit"]
        });
        const result = await engine.createStoryboard({
          id: parsed.options.id ?? "main",
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          expressionUnitId: source.expressionUnitId,
          title: parsed.options.title ?? parsed.positionals[0],
          order: parseOptionalNumberOption(parsed.options.order, "order")
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /storyboard action: ${action}`);
    }
    async function dispatchInteractiveKeyframeCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "create" || action === "upsert") {
        const source = parseKeyframeParentOptions({
          production: parsed.options.production,
          segment: parsed.options.segment,
          sceneMoment: parsed.options.sceneMoment ?? parsed.options["scene-moment"],
          expressionUnit: parsed.options.expressionUnit ?? parsed.options["expression-unit"]
        });
        const result = await createKeyframeFromCliOptions(engine, {
          id: parsed.options.id,
          title: parsed.options.title ?? parsed.positionals[0],
          role: parsed.options.role,
          visualIntent: parsed.options.visualIntent ?? parsed.options["visual-intent"]
        }, source);
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /keyframe action: ${action}`);
    }
    async function dispatchInteractiveAudioCueCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "create" || action === "upsert") {
        const source = parseAudioCueParentOptions({
          production: parsed.options.production,
          segment: parsed.options.segment,
          sceneMoment: parsed.options.sceneMoment ?? parsed.options["scene-moment"],
          expressionUnit: parsed.options.expressionUnit ?? parsed.options["expression-unit"],
          storyboard: parsed.options.storyboard
        });
        const result = await engine.createAudioCue({
          id: parsed.options.id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          expressionUnitId: source.expressionUnitId,
          storyboardId: source.storyboardId,
          title: parsed.options.title ?? parsed.positionals[0],
          kind: parsed.options.kind ?? "sound_effect",
          order: parseOptionalNumberOption(parsed.options.order, "order"),
          promptHint: parsed.options.prompt
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /audio-cue action: ${action}`);
    }
    async function dispatchInteractiveExpressionUnitCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "create" || action === "upsert") {
        const sourceOptions = {
          id: parsed.options.id,
          title: parsed.options.title ?? parsed.positionals[0],
          production: parsed.options.production,
          segment: parsed.options.segment,
          sceneMoment: parsed.options.sceneMoment ?? parsed.options["scene-moment"],
          slotKind: parsed.options.slotKind ?? parsed.options["slot-kind"] ?? parsed.options.kind ?? "visual",
          kind: parsed.options.kind,
          speaker: parsed.options.speaker,
          text: parsed.options.text,
          note: parsed.options.note,
          intent: parsed.options.intent,
          order: parsed.options.order,
          storyboard: repeatedSlashOption(parsed.options.storyboard),
          fromStoryboard: parsed.options.fromStoryboard ?? parsed.options["from-storyboard"],
          toStoryboard: parsed.options.toStoryboard ?? parsed.options["to-storyboard"],
          scriptBlock: parsed.options.scriptBlock ?? parsed.options["script-block"]
        };
        const source = parseExpressionUnitParentOptions(sourceOptions);
        const result = await engine.createExpressionUnit({
          id: sourceOptions.id,
          productionId: source.productionId,
          segmentId: source.segmentId,
          sceneMomentId: source.sceneMomentId,
          title: sourceOptions.title,
          slotKind: sourceOptions.slotKind,
          kind: sourceOptions.kind,
          speaker: sourceOptions.speaker,
          text: sourceOptions.text,
          note: sourceOptions.note,
          intent: sourceOptions.intent,
          order: parseOptionalNumberOption(sourceOptions.order, "order"),
          span: expressionUnitSpanFromOptions(sourceOptions),
          scriptBlockId: sourceOptions.scriptBlock
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /expression-unit action: ${action}`);
    }
    async function dispatchInteractiveContentUnitCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "status" || action === "panel") {
        const idOrPath = parsed.positionals[0] ?? parsed.options.id;
        if (!idOrPath) throw new Error("usage: /content-unit status <idOrPath>");
        const result = await deriveContentUnitStatusPanel(engine, idOrPath);
        printContentUnitStatusPanel(result, options);
        return;
      }
      if (action === "backend-prompt" || action === "prompt") {
        const idOrPath = parsed.positionals[0] ?? parsed.options.id;
        if (!idOrPath) throw new Error("usage: /content-unit backend-prompt <idOrPath>");
        const contentUnit = await findContentUnitForPanel(engine, idOrPath);
        const result = await engine.buildContentUnitBackendPrompt(contentUnit.id ?? idOrPath);
        printResult(result, options);
        return;
      }
      if (action === "add" || action === "create" || action === "upsert") {
        const title = parsed.options.title ?? parsed.positionals[0];
        const sourceOptions = {
          id: parsed.options.id,
          title,
          type: parsed.options.type,
          kind: parsed.options.kind,
          contentUnitType: parsed.options.contentUnitType ?? parsed.options["content-unit-type"],
          outputKind: parsed.options.outputKind ?? parsed.options["output-kind"],
          timelineAssembly: parsed.options.timelineAssembly ?? parsed.options["timeline-assembly"],
          scopeKind: parsed.options.scopeKind ?? parsed.options["scope-kind"],
          scopeRef: parsed.options.scopeRef ?? parsed.options["scope-ref"],
          asset: parsed.options.asset,
          production: parsed.options.production,
          segment: parsed.options.segment,
          sceneMoment: parsed.options.sceneMoment ?? parsed.options["scene-moment"],
          expressionUnit: parsed.options.expressionUnit ?? parsed.options["expression-unit"],
          storyboard: parsed.options.storyboard,
          keyframe: parsed.options.keyframe,
          audioCue: parsed.options.audioCue ?? parsed.options["audio-cue"],
          prompt: parsed.options.prompt,
          negativePrompt: parsed.options.negativePrompt ?? parsed.options["negative-prompt"],
          description: parsed.options.description,
          order: parsed.options.order,
          duration: parsed.options.duration,
          aspectRatio: parsed.options.aspectRatio ?? parsed.options["aspect-ratio"],
          capability: parsed.options.capability,
          provider: parsed.options.provider,
          model: parsed.options.model,
          quality: parsed.options.quality,
          shotSize: parsed.options.shotSize ?? parsed.options["shot-size"],
          cameraAngle: parsed.options.cameraAngle ?? parsed.options["camera-angle"],
          cameraMotion: parsed.options.cameraMotion ?? parsed.options["camera-motion"],
          param: repeatedSlashOption(parsed.options.param)
        };
        const result = await saveContentUnitFromCliOptions(engine, sourceOptions, { defaultType: "storyboard_ref" });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /content-unit action: ${action}`);
    }
    async function dispatchInteractiveEntityCommand(args, options, engine) {
      const action = args.shift();
      if (action !== "list" && action !== void 0) throw new Error(`unknown /entity action: ${action}`);
      const entityKindInput = args[0]?.startsWith("--") ? void 0 : args.shift();
      if (entityKindInput !== void 0 && !isSemanticEntityKind32(entityKindInput)) {
        throw new Error(`unknown entity kind: ${entityKindInput}`);
      }
      const parsed = parseSlashOptions(args);
      const result = await engine.queryEntities({
        ...entityKindInput !== void 0 ? { entityKind: entityKindInput } : {},
        ...parsed.options.kind !== void 0 ? { kind: parsed.options.kind } : {},
        ...parsed.options.query !== void 0 ? { query: parsed.options.query } : {},
        ...parsed.options.limit !== void 0 ? { limit: parsePositiveIntegerOption(parsed.options.limit, "limit") } : {}
      });
      printEntityList(result, options, {
        title: entityKindInput ? `${entityKindInput} entities` : "Entities",
        columns: [
          { header: "Kind", value: (item) => item.entityKind },
          { header: "ID", value: (item) => item.id },
          { header: "Type", value: (item) => item.record.setting_kind ?? item.record.asset_kind ?? item.record.content_unit_type ?? item.record.cue_kind ?? item.record.kind },
          { header: "Title", value: (item) => item.record.title ?? item.record.label ?? item.id },
          { header: "Path", value: (item) => item.path, maxWidth: 52 }
        ]
      });
    }
    async function dispatchInteractiveCandidateCommand(args, options, engine) {
      const action = args.shift();
      const parsed = parseSlashOptions(args);
      if (action === "add" || action === "append") {
        const target = parsed.options.target ?? parsed.positionals[0];
        const resourceId = parsed.options.resourceId ?? parsed.options["resource-id"] ?? parsed.positionals[1];
        if (!target || !resourceId) {
          throw new Error("usage: /candidate add <content-unit> <resource-id> [--id <id>] [--kind <output-kind|content_unit_type>] [--source <source>] [--notes <text>]");
        }
        const result = await addCandidateFromCliOptions(engine, target, {
          id: parsed.options.id,
          kind: parsed.options.kind,
          targetKind: parsed.options.targetKind ?? parsed.options["target-kind"],
          outputKind: parsed.options.outputKind ?? parsed.options["output-kind"],
          resourceId,
          source: parsed.options.source ?? "manual",
          notes: parsed.options.notes
        });
        printResult(result, options);
        return;
      }
      if (action === "select" || action === "choose" || action === "lock") {
        const target = parsed.options.target ?? parsed.positionals[0];
        const candidateId = parsed.options.candidateId ?? parsed.options["candidate-id"] ?? parsed.positionals[1];
        if (!target || !candidateId) {
          throw new Error("usage: /candidate select <content-unit> <candidate-id> [--reason <text>]");
        }
        const result = await selectCandidateFromCliOptions(engine, target, candidateId, {
          kind: parsed.options.kind,
          targetKind: parsed.options.targetKind ?? parsed.options["target-kind"],
          reason: parsed.options.reason
        });
        printResult(result, options);
        return;
      }
      throw new Error(`unknown /candidate action: ${action}`);
    }
    function printInteractiveHelp() {
      console.log(`Slash commands:
  /project init [id] [--title <title>] [--language <language>] [--standard <key=value>] [--overwrite]
  /project demo create [--id <id>] [--title <title>] [--overwrite] [--no-interpret]
  /init [id] [--title <title>] [--language <language>] [--standard <key=value>] [--overwrite]
  /setting list [--kind <kind>] [--query <text>] [--limit <n>]
  /setting add <id> [--title <title>] [--kind <kind>] [--description <text>]
  /asset list [--setting <id>] [--state <id>] [--query <text>] [--limit <n>]
  /asset add <id> [--title <title>] [--setting <id>] [--state <id>] [--slot <slot>] [--kind <kind>] [--prompt <text>]
  /production add [--id <id>] [--title <title>]
  /segment add --title <title> [--production <id>] [--id <id>] [--order <n>]
  /scene-moment add --title <title> --segment <id-or-path> [--production <id>] [--id <id>] [--storyboard <id>]
  /storyboard add --scene-moment <id-or-path> [--segment <id-or-path>] [--id <id>] [--title <title>] [--order <n>]
  /audio-cue add --scene-moment <id-or-path> [--expression-unit <id-or-path>] [--storyboard <id-or-path>] [--id <id>] [--title <title>] [--kind <kind>] [--prompt <text>]
  /expression-unit add --scene-moment <id-or-path> [--id <id>] [--slot-kind <visual|voice|subtitle|audio>] [--kind <legacy-kind>] [--speaker <text>] [--text <text>] [--storyboard <id-or-path>]
  /content-unit add --title <title> --type <asset_ref|keyframe_ref|storyboard_ref|scence_moment_ref|expression_unit_ref|timeline_assembly_ref> [--timeline-assembly <timeline_assembly:scope:id>] [--scope-kind <kind>] [--scope-ref <id>] [--asset <id>] [--keyframe <id>] [--scene-moment <id>] [--expression-unit <id>] [--storyboard <id>] [--output-kind <kind>] [--prompt <text>]
  /content-unit status <id-or-path>
  /content-unit backend-prompt <id-or-path>
  /language kinds
  /language schemas [--kind <kind>]
  /language schema <schema-id-or-kind>
  /entity list [entityKind] [--kind <kind>] [--query <text>] [--limit <n>]
  /candidate add <content-unit> <resource-id> [--kind <output-kind|content_unit_type>] [--id <id>] [--source <source>] [--notes <text>]
  /candidate select <content-unit> <candidate-id> [--reason <text>]
  /overview
  /inspect [--commit <ref>]
  /interpreter overview
  /interpreter inspect [--commit <ref>]
  /interpreter interpret
  /interpreter regen plan
  /interpreter prompt <contentUnitId>
  /interpreter artifacts [--interpretation-id <id>] [--created-at <iso>]
  /review [--commit <ref>]
  /interpret
  /regen plan
  /help
  /exit`);
    }
    function printResult(result, options) {
      if (options.json) {
        console.log(JSON.stringify(result, null, 2));
        return;
      }
      console.log(JSON.stringify(result, null, 2));
    }
    function listLanguageSchemas(kind) {
      if (kind !== void 0 && !isSemanticEntityKind32(kind)) throw new Error(`unknown entity kind: ${kind}`);
      const registry = SEMANTIC_ENTITY_SCHEMA_REGISTRY2;
      return SEMANTIC_ENTITY_SCHEMA_IDS2.map((schemaId) => registry[schemaId]).filter((schema) => schema !== void 0).filter((schema) => kind === void 0 || schema.entityKind === kind);
    }
    function resolveLanguageSchema(schemaIdOrKind) {
      const byId = getSemanticEntitySchemaEntry2(schemaIdOrKind);
      if (byId) return byId;
      if (isSemanticEntityKind32(schemaIdOrKind)) return getActiveSemanticEntitySchemaForKind2(schemaIdOrKind);
      throw new Error(`unknown language schema or entity kind: ${schemaIdOrKind}`);
    }
    function printLanguageKinds(options) {
      if (options.json) {
        printResult(SEMANTIC_ENTITY_KIND_VALUES2, options);
        return;
      }
      console.log(SEMANTIC_ENTITY_KIND_VALUES2.join("\n"));
    }
    function printLanguageSchemaList(schemas, options) {
      if (options.json) {
        printResult(schemas, options);
        return;
      }
      if (schemas.length === 0) {
        console.log("Language schemas: no schemas found");
        return;
      }
      console.log(renderTable([
        { header: "Schema ID", value: (schema) => schema.id, maxWidth: 36 },
        { header: "Kind", value: (schema) => schema.entityKind },
        { header: "Version", value: (schema) => schema.version },
        { header: "Status", value: (schema) => schema.status },
        { header: "Summary", value: (schema) => schema.promptSummary, maxWidth: 72 }
      ], schemas));
    }
    async function deriveContentUnitStatusPanel(engine, idOrPath) {
      const contentUnit = await findContentUnitForPanel(engine, idOrPath);
      const contentUnitId = contentUnit.id ?? idOrPath;
      const [
        runtimePanel,
        inputVersion,
        dependencyReport,
        selectionValidity,
        candidates,
        backendPrompt
      ] = await Promise.all([
        engine.workspaceService.readContentUnitRuntimePanel(contentUnitId),
        engine.workspaceService.readContentUnitGenerationPrompt(contentUnitId),
        engine.workspaceService.readContentUnitDependencyReport(contentUnitId),
        engine.workspaceService.readContentUnitSelectionValidity(contentUnitId),
        listContentUnitCandidates(engine, contentUnit),
        engine.buildContentUnitBackendPrompt(contentUnitId)
      ]);
      return pruneUndefined19({
        contentUnit: {
          id: contentUnit.id,
          path: contentUnit.path,
          title: contentUnit.record.title,
          type: contentUnit.record.content_unit_type,
          outputKind: contentUnit.record.output_kind
        },
        source: pruneUndefined19({
          sceneMomentRef: contentUnit.record.scene_moment_ref,
          storyboardRef: contentUnit.record.storyboard_ref,
          assetRef: contentUnit.record.asset_ref,
          keyframeRef: contentUnit.record.keyframe_ref,
          keyframeRefs: contentUnit.record.keyframe_refs,
          audioCueRef: contentUnit.record.audio_cue_ref,
          editPrompt: contentUnit.record.edit_prompt,
          modelIntent: contentUnit.record.model_intent
        }),
        runtime: runtimePanel,
        inputVersion,
        dependencyReport,
        selectionValidity,
        candidates,
        backendPrompt,
        summary: summarizeContentUnitStatus2(runtimePanel, inputVersion, dependencyReport, selectionValidity, candidates, backendPrompt)
      });
    }
    async function listContentUnitCandidates(engine, contentUnit) {
      const contentUnitDir = normalizeCliPath(contentUnit.path.replace(/\/content_unit\.json$/, ""));
      const index = await engine.loadIndex().catch(() => void 0);
      const indexedCandidates = index?.documents.filter((document) => document.path.startsWith(`${contentUnitDir}/candidates/`) && document.path.endsWith("/content_candidate.json")).map((document) => {
        if (!isRecord302(document.data)) return void 0;
        return { path: document.path, record: document.data };
      }).filter((candidate) => candidate !== void 0) ?? [];
      if (indexedCandidates.length > 0) return indexedCandidates;
      const candidateRoot = `${contentUnitDir}/candidates`;
      const repository = createNodeMovScriptWorkspaceFileRepository2(engine.projectDir);
      const listed = await repository.list({ path: candidateRoot }).catch(() => void 0);
      const candidateDirs = listed?.entries.filter((entry) => entry.kind === "directory") ?? [];
      const candidates = await Promise.all(candidateDirs.map(async (entry) => {
        const path = `${entry.path}/content_candidate.json`;
        const file = await repository.read({ path }).catch(() => void 0);
        if (!file) return void 0;
        const parsed = JSON.parse(file.content);
        if (!isRecord302(parsed)) return void 0;
        return { path: file.path, record: parsed };
      }));
      return candidates.filter((candidate) => candidate !== void 0);
    }
    async function findContentUnitCandidate(engine, contentUnit, candidateId) {
      const candidates = await listContentUnitCandidates(engine, contentUnit);
      return candidates.find((candidate) => String(candidate.record.id ?? "") === candidateId);
    }
    function candidateResourceId(candidate) {
      const firstOutput = arrayField72(candidate?.outputs).filter(isRecord302)[0];
      const resourceId = firstOutput?.resource_id;
      return resourceIdOption(resourceId);
    }
    function resourceIdOption(value) {
      if (value === void 0 || value === null || value === "") return void 0;
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      throw new Error("resource_id must be a positive integer RawResource ID");
    }
    async function findContentUnitForPanel(engine, idOrPath) {
      const normalized = normalizeCliPath(idOrPath);
      const entities = await engine.queryEntities({ entityKind: "content_unit" });
      const match = entities.find((entity) => String(entity.id ?? "") === idOrPath) ?? entities.find((entity) => normalizeCliPath(entity.path) === normalized) ?? entities.find((entity) => normalizeCliPath(entity.path.replace(/\/content_unit\.json$/, "")) === normalized) ?? entities.find((entity) => normalizeCliPath(`content_units/${String(entity.id ?? "")}`) === normalized);
      if (!match) throw new Error(`content_unit not found: ${idOrPath}`);
      return match;
    }
    function summarizeContentUnitStatus2(runtimePanel, inputVersion, dependencyReport, selectionValidity, candidates = [], backendPrompt) {
      const backendPromptRecord = recordField7(backendPrompt);
      const compiledPrompt = recordField7(backendPromptRecord?.prompt);
      return pruneUndefined19({
        derived: Boolean(runtimePanel && inputVersion),
        status: runtimePanel?.status,
        inputHash: shortHash(inputVersion?.hash),
        selected: selectionValidity?.selected,
        stale: selectionValidity?.stale,
        candidateId: selectionValidity?.candidate_id,
        resourceId: selectionValidity?.resource_id,
        candidateCount: candidates.length,
        dependencyCount: arrayField72(dependencyReport?.dependencies).length,
        upstreamSelectionCount: arrayField72(dependencyReport?.upstream_selections).length,
        runtimeInputCount: arrayField72(recordField7(runtimePanel?.runtime_request)?.inputs).length,
        issueCount: arrayField72(dependencyReport?.issues).length,
        backendPromptOk: backendPromptRecord?.ok,
        backendPromptResourceCount: arrayField72(compiledPrompt?.resource_ids).length,
        backendPromptBlockerCount: arrayField72(backendPromptRecord?.blockers ?? compiledPrompt?.blockers).length
      });
    }
    function printContentUnitStatusPanel(panel, options) {
      if (options.json) {
        printResult(panel, options);
        return;
      }
      const contentUnit = recordField7(panel.contentUnit);
      const source = recordField7(panel.source);
      const runtime = recordField7(panel.runtime);
      const inputVersion = recordField7(panel.inputVersion);
      const dependencyReport = recordField7(panel.dependencyReport);
      const selectionValidity = recordField7(panel.selectionValidity);
      const selection = recordField7(panel.selection);
      const backendPrompt = recordField7(panel.backendPrompt);
      const compiledPrompt = recordField7(backendPrompt?.prompt);
      const summary = recordField7(panel.summary);
      const prompt = recordField7(runtime?.prompt);
      const runtimeRequest = recordField7(runtime?.runtime_request);
      const candidates = arrayField72(panel.candidates).filter(isRecord302);
      const dependencies = arrayField72(dependencyReport?.dependencies).filter(isRecord302);
      const upstreamSelections = arrayField72(dependencyReport?.upstream_selections).filter(isRecord302);
      const runtimeInputs = arrayField72(runtimeRequest?.inputs).filter(isRecord302);
      const issues = arrayField72(dependencyReport?.issues).filter(isRecord302);
      const backendBlockers = arrayField72(backendPrompt?.blockers ?? compiledPrompt?.blockers).filter(isRecord302);
      const lines = [
        `Content Unit: ${scalarDisplayValue2(contentUnit?.id)}  ${scalarDisplayValue2(contentUnit?.title)}`,
        `Path: ${scalarDisplayValue2(contentUnit?.path)}`,
        `Type: ${scalarDisplayValue2(contentUnit?.type)} -> ${scalarDisplayValue2(contentUnit?.outputKind)}`,
        `Derived: ${summary?.derived ? "ready" : "missing"}  Runtime: ${scalarDisplayValue2(summary?.status)}  Hash: ${scalarDisplayValue2(summary?.inputHash)}  Candidates: ${scalarDisplayValue2(summary?.candidateCount)}`,
        `Selection: ${selectionLabel(selection, selectionValidity)}  Stale: ${selectionValidity?.stale === true ? "yes" : "no"}`,
        `Accepted Hash: ${shortHash(selectionValidity?.accepted_input_hash)}  Current Hash: ${shortHash(selectionValidity?.current_input_hash)}`,
        `Backend Prompt: ${backendPrompt?.ok === true ? "ready" : backendPrompt?.ok === false ? "blocked" : "unknown"}  Resources: ${scalarDisplayValue2(summary?.backendPromptResourceCount)}  Blockers: ${scalarDisplayValue2(summary?.backendPromptBlockerCount)}`,
        "",
        "Source Refs",
        `  scene_moment: ${scalarDisplayValue2(source?.sceneMomentRef)}`,
        `  storyboard:    ${scalarDisplayValue2(source?.storyboardRef)}`,
        `  asset:         ${scalarDisplayValue2(source?.assetRef)}`,
        `  keyframe:      ${scalarDisplayValue2(source?.keyframeRef)}`,
        `  keyframes:     ${scalarDisplayValue2(source?.keyframeRefs)}`,
        `  audio_cue:     ${scalarDisplayValue2(source?.audioCueRef)}`,
        "",
        "Prompt",
        `  text:     ${formatCell(prompt?.text, 96)}`,
        `  negative: ${formatCell(prompt?.negative_text, 96)}`,
        "",
        "Generation Context",
        `  input_hash:      ${scalarDisplayValue2(inputVersion?.hash)}`,
        `  interpreter:        ${scalarDisplayValue2(inputVersion?.interpreter_version)}`,
        `  adapter:         ${scalarDisplayValue2(inputVersion?.adapter_version)}`,
        `  hash_rule:       ${scalarDisplayValue2(recordField7(dependencyReport?.hash_rule)?.rule_id)}@${scalarDisplayValue2(recordField7(dependencyReport?.hash_rule)?.rule_version)}`,
        `  included_roles:  ${scalarDisplayValue2(recordField7(dependencyReport?.hash_rule)?.included_roles)}`,
        "",
        "Runtime Inputs",
        ...runtimeInputs.length ? runtimeInputs.map((item) => {
          return `  ${scalarDisplayValue2(item.role)} ${scalarDisplayValue2(item.kind)} ref=${scalarDisplayValue2(item.ref)} resource=${scalarDisplayValue2(item.resource_id)}`;
        }) : ["  -"],
        "",
        "Backend Prompt",
        `  text:      ${formatCell(compiledPrompt?.text, 96)}`,
        `  negative:  ${formatCell(compiledPrompt?.negative_text, 96)}`,
        `  resources: ${scalarDisplayValue2(compiledPrompt?.resource_ids)}`,
        ...backendBlockers.length ? backendBlockers.map((item) => {
          return `  blocker ${scalarDisplayValue2(item.code)} ref=${scalarDisplayValue2(item.ref)} unit=${scalarDisplayValue2(item.content_unit_ref)}`;
        }) : ["  blockers: -"],
        "",
        "Upstream Selections",
        ...upstreamSelections.length ? upstreamSelections.map((item) => {
          return `  ${scalarDisplayValue2(item.content_unit_ref)} candidate=${scalarDisplayValue2(item.candidate_id)} resource=${scalarDisplayValue2(item.resource_id)} hash=${shortHash(item.accepted_input_hash)}`;
        }) : ["  -"],
        "",
        "Candidates",
        ...candidates.length ? candidates.map((item) => {
          const record = recordField7(item.record) ?? {};
          const selected = selectionValidity?.candidate_id !== void 0 && String(selectionValidity.candidate_id) === String(record.id) ? "*" : " ";
          const outputs = arrayField72(record.outputs).filter(isRecord302);
          const outputSummary = outputs.length ? outputs.map((output3) => `${scalarDisplayValue2(output3.kind)}:${scalarDisplayValue2(output3.resource_id)}`).join(", ") : "-";
          const candidateHash = shortHash(recordField7(record.input_version)?.hash);
          return ` ${selected} ${scalarDisplayValue2(record.id)} source=${scalarDisplayValue2(record.source)} status=${scalarDisplayValue2(record.status)} hash=${candidateHash} outputs=${outputSummary}`;
        }) : ["  -"],
        "",
        "Dependencies",
        ...dependencies.length ? dependencies.map((item) => {
          return `  ${scalarDisplayValue2(item.role)} ${scalarDisplayValue2(item.entityKind)}:${scalarDisplayValue2(item.id)} ${scalarDisplayValue2(item.path)}`;
        }) : ["  -"],
        "",
        "Issues",
        ...issues.length ? issues.map((item) => `  ${scalarDisplayValue2(item.severity)} ${scalarDisplayValue2(item.message)}`) : ["  -"]
      ];
      console.log(lines.join("\n"));
    }
    function selectionLabel(selection, selectionValidity) {
      if (selection?.candidate_id !== void 0) {
        return `${scalarDisplayValue2(selection.candidate_id)} resource=${scalarDisplayValue2(selection.resource_id)} accepted=${shortHash(selection.accepted_input_hash)} reason=${scalarDisplayValue2(selection.reason)}`;
      }
      if (!selectionValidity?.selected) return "none";
      return `${scalarDisplayValue2(selectionValidity.candidate_id)} resource=${scalarDisplayValue2(selectionValidity.resource_id)} accepted=${shortHash(selectionValidity.accepted_input_hash)}`;
    }
    function printEntityList(entities, options, table) {
      if (options.json) {
        printResult(entities, options);
        return;
      }
      if (entities.length === 0) {
        console.log(`${table.title}: no entities found`);
        return;
      }
      console.log(renderTable(table.columns, entities));
    }
    function printSettingsTable(settings, options) {
      printEntityList(settings, options, {
        title: "Settings",
        columns: [
          { header: "ID", value: (entity) => entity.id },
          { header: "Kind", value: (entity) => entity.record.setting_kind ?? entity.record.kind },
          { header: "Title", value: (entity) => entity.record.title ?? entity.id },
          { header: "Description", value: (entity) => entity.record.description, maxWidth: 44 },
          { header: "Path", value: (entity) => entity.path, maxWidth: 52 }
        ]
      });
    }
    function printAssetsTable(assets, options) {
      printEntityList(assets, options, {
        title: "Assets",
        columns: [
          { header: "ID", value: (entity) => entity.id },
          { header: "Kind", value: (entity) => entity.record.asset_kind ?? entity.record.kind },
          { header: "Setting", value: (entity) => entity.record.setting_id },
          { header: "State", value: (entity) => entity.record.setting_state_id },
          { header: "Slot", value: (entity) => entity.record.slot },
          { header: "Title", value: (entity) => entity.record.title ?? entity.id },
          { header: "Path", value: (entity) => entity.path, maxWidth: 52 }
        ]
      });
    }
    async function printPlanningEntityList(entityKind, title, options, command) {
      const merged = mergeGlobalOptions(options, command);
      const entities = await listPlanningEntities(createCliEngine(merged), entityKind, planningListInput(options));
      printEntityList(entities, merged, {
        title,
        columns: [
          { header: "ID", value: (entity) => entity.id },
          { header: "Kind", value: (entity) => entity.record.segment_kind ?? entity.record.cue_kind ?? entity.record.kind },
          { header: "Order", value: (entity) => entity.record.order },
          { header: "Title", value: (entity) => entity.record.title ?? entity.record.name },
          { header: "Path", value: (entity) => entity.path, maxWidth: 64 }
        ]
      });
    }
    async function deletePlanningEntity22(entityKind, idOrPath, options, command) {
      const merged = mergeGlobalOptions(options, command);
      const engine = createCliEngine(merged);
      const result = await deletePlanningEntityWithEngine(engine, entityKind, {
        id: idOrPath,
        ...planningParentInput(options)
      });
      printResult({ deleted: true, entityKind, id: result.entity.id, path: result.entity.path }, merged);
    }
    function planningListInput(options) {
      return pruneUndefined19({
        kind: options.kind,
        query: options.query,
        productionId: options.production,
        segmentId: options.segment !== void 0 ? parseSegmentRefOption(options.segment).segmentId ?? options.segment : void 0,
        sceneMomentId: options.sceneMoment !== void 0 ? parseSceneMomentRefOption(options.sceneMoment).sceneMomentId ?? options.sceneMoment : void 0,
        limit: options.limit !== void 0 ? parsePositiveIntegerOption(options.limit, "limit") : void 0
      });
    }
    function planningParentInput(options) {
      return pruneUndefined19({
        productionId: options.production,
        segmentId: options.segment !== void 0 ? parseSegmentRefOption(options.segment).segmentId ?? options.segment : void 0,
        sceneMomentId: options.sceneMoment !== void 0 ? parseSceneMomentRefOption(options.sceneMoment).sceneMomentId ?? options.sceneMoment : void 0
      });
    }
    function listPlanningEntities(engine, entityKind, input3) {
      if (entityKind === "production") return engine.listProductions(input3);
      if (entityKind === "segment") return engine.listSegments(input3);
      if (entityKind === "scene_moment") return engine.listSceneMoments(input3);
      if (entityKind === "storyboard") return engine.listStoryboards(input3);
      if (entityKind === "audio_cue") return engine.listAudioCues(input3);
      if (entityKind === "expression_unit") return engine.listExpressionUnits(input3);
      if (entityKind === "content_unit") return engine.listContentUnits(input3);
      return engine.queryEntities({ entityKind, ...input3 });
    }
    function deletePlanningEntityWithEngine(engine, entityKind, input3) {
      if (entityKind === "production") return engine.deleteProduction(input3);
      if (entityKind === "segment") return engine.deleteSegment(input3);
      if (entityKind === "scene_moment") return engine.deleteSceneMoment(input3);
      if (entityKind === "storyboard") return engine.deleteStoryboard(input3);
      if (entityKind === "audio_cue") return engine.deleteAudioCue(input3);
      if (entityKind === "expression_unit") return engine.deleteExpressionUnit(input3);
      if (entityKind === "content_unit") return engine.deleteContentUnit(input3);
      throw new Error(`delete is not supported for ${entityKind}`);
    }
    function renderTable(columns, rows) {
      const renderedRows = rows.map((row) => columns.map((column) => formatCell(column.value(row), column.maxWidth)));
      const widths = columns.map((column, index) => {
        const values = renderedRows.map((row) => row[index] ?? "");
        return Math.max(column.header.length, ...values.map((value) => value.length));
      });
      const separator = `+-${widths.map((width) => "-".repeat(width)).join("-+-")}-+`;
      const header = tableRow(columns.map((column) => column.header), widths);
      const body = renderedRows.map((row) => tableRow(row, widths));
      return [separator, header, separator, ...body, separator].join("\n");
    }
    function tableRow(cells, widths) {
      return `| ${cells.map((cell, index) => cell.padEnd(widths[index] ?? 0)).join(" | ")} |`;
    }
    function formatCell(value, maxWidth = 72) {
      const text = scalarDisplayValue2(value).replace(/\s+/g, " ").trim();
      if (text.length <= maxWidth) return text;
      return `${text.slice(0, Math.max(0, maxWidth - 3))}...`;
    }
    function scalarDisplayValue2(value) {
      if (value === void 0 || value === null || value === "") return "-";
      if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") return String(value);
      return JSON.stringify(value);
    }
    function isRecord302(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function recordField7(value) {
      return isRecord302(value) ? value : void 0;
    }
    function arrayField72(value) {
      return Array.isArray(value) ? value : [];
    }
    function shortHash(value) {
      const text = typeof value === "string" ? value : "";
      if (!text) return "-";
      return text.length > 12 ? text.slice(0, 12) : text;
    }
    function isSemanticEntityKind32(value) {
      return SEMANTIC_ENTITY_KINDS2.includes(value);
    }
    function collectOption(value, previous) {
      return [...previous, value];
    }
    function parseKeyValueOptions(values) {
      const result = {};
      for (const value of values) {
        const index = value.indexOf("=");
        if (index <= 0) throw new Error(`expected key=value option: ${value}`);
        result[value.slice(0, index)] = parseScalar2(value.slice(index + 1));
      }
      return result;
    }
    function parseOptionalKeyValueOptions(values) {
      if (values.length === 0) return void 0;
      return parseKeyValueOptions(values);
    }
    function parseScalar2(value) {
      if (value === "true") return true;
      if (value === "false") return false;
      if (value !== "" && Number.isFinite(Number(value))) return Number(value);
      return value;
    }
    function pruneUndefined19(value) {
      const output3 = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) output3[key] = item;
      }
      return output3;
    }
    function parsePositiveIntegerOption(value, optionName) {
      const parsed = Number(value);
      if (!Number.isInteger(parsed) || parsed <= 0) throw new Error(`${optionName} must be a positive integer`);
      return parsed;
    }
    function parseOptionalNumberOption(value, optionName) {
      if (value === void 0) return void 0;
      const parsed = Number(value);
      if (!Number.isFinite(parsed)) throw new Error(`${optionName} must be a number`);
      return parsed;
    }
    function parseContentUnitSourceOptions(options, contentUnitType22) {
      const asset = parseAssetRefOption(options.asset);
      const sceneMoment = parseSceneMomentRefOption(options.sceneMoment);
      const expressionUnit = parseExpressionUnitRefOption(options.expressionUnit);
      const storyboard = parseStoryboardRefOption(options.storyboard);
      const keyframe = parseKeyframeRefOption(options.keyframe);
      const audioCue = parseAudioCueRefOption(options.audioCue);
      const timelineAssembly = parseTimelineAssemblyRefOptions(options, contentUnitType22);
      const legacyProductionId = timelineAssembly.targetRef === void 0 ? options.production ?? keyframe.productionId ?? audioCue.productionId ?? storyboard.productionId ?? expressionUnit.productionId ?? sceneMoment.productionId : void 0;
      const legacySegmentId = timelineAssembly.targetRef === void 0 ? options.segment ?? keyframe.segmentId ?? audioCue.segmentId ?? storyboard.segmentId ?? expressionUnit.segmentId ?? sceneMoment.segmentId : void 0;
      return pruneUndefined19({
        targetCategory: timelineAssembly.targetCategory,
        targetKind: timelineAssembly.targetKind,
        targetRef: timelineAssembly.targetRef,
        scopeKind: timelineAssembly.scopeKind,
        scopeRef: timelineAssembly.scopeRef,
        productionId: legacyProductionId,
        segmentId: legacySegmentId,
        sceneMomentId: sceneMoment.sceneMomentId ?? keyframe.sceneMomentId ?? audioCue.sceneMomentId ?? storyboard.sceneMomentId ?? expressionUnit.sceneMomentId,
        expressionUnitId: expressionUnit.expressionUnitId ?? storyboard.expressionUnitId ?? keyframe.expressionUnitId ?? plainIdOption(options.expressionUnit),
        storyboardId: storyboard.storyboardId ?? (contentUnitType22 === void 0 || contentUnitType22 === "storyboard_ref" ? "main" : void 0),
        keyframeId: keyframe.keyframeId ?? options.keyframe,
        assetRef: asset.assetRef ?? options.asset,
        audioCueId: audioCue.audioCueId
      });
    }
    function parseTimelineAssemblyRefOptions(options, contentUnitType22) {
      const rawAssembly = stringValue122(options.timelineAssembly);
      const rawScopeKind = stringValue122(options.scopeKind);
      const rawScopeRef = stringValue122(options.scopeRef);
      if (!rawAssembly && !rawScopeKind && !rawScopeRef && contentUnitType22 !== "timeline_assembly_ref") return {};
      const parsedAssembly = parseImplicitTimelineAssemblyRefOption(rawAssembly);
      if (parsedAssembly && rawScopeKind && parsedAssembly.scopeKind !== rawScopeKind) {
        throw new Error("--scope-kind conflicts with --timeline-assembly");
      }
      if (parsedAssembly && rawScopeRef && parsedAssembly.scopeRef !== rawScopeRef) {
        throw new Error("--scope-ref conflicts with --timeline-assembly");
      }
      if (rawAssembly && !parsedAssembly && rawScopeRef && rawAssembly !== rawScopeRef) {
        throw new Error("--scope-ref conflicts with --timeline-assembly");
      }
      const productionScopeRef = !rawAssembly && !rawScopeKind && !rawScopeRef && contentUnitType22 === "timeline_assembly_ref" ? stringValue122(options.production) : void 0;
      const segmentScope = !rawAssembly && !rawScopeKind && !rawScopeRef && contentUnitType22 === "timeline_assembly_ref" && !productionScopeRef ? parseSegmentRefOption(options.segment) : {};
      const segmentScopeRef = stringValue122(segmentScope.segmentId);
      const scopeKind = rawScopeKind ?? parsedAssembly?.scopeKind ?? (productionScopeRef ? "production" : segmentScopeRef ? "segment" : void 0);
      const scopeRef = rawScopeRef ?? parsedAssembly?.scopeRef ?? (!parsedAssembly ? rawAssembly : void 0) ?? productionScopeRef ?? segmentScopeRef;
      if (!scopeKind && !scopeRef) return {};
      if (!scopeKind || !scopeRef) {
        throw new Error("--timeline-assembly requires --scope-kind, or provide both --scope-kind and --scope-ref");
      }
      return {
        targetCategory: "timeline_assembly",
        targetKind: "timeline_assembly",
        targetRef: implicitTimelineAssemblyRefOption(scopeKind, scopeRef),
        scopeKind,
        scopeRef
      };
    }
    function implicitTimelineAssemblyRefOption(scopeKind, scopeRef) {
      return `timeline_assembly:${scopeKind}:${scopeRef}`;
    }
    function parseImplicitTimelineAssemblyRefOption(value) {
      if (!value?.startsWith("timeline_assembly:")) return void 0;
      const [, scopeKind, ...scopeRefParts] = value.split(":");
      const scopeRef = scopeRefParts.join(":");
      if (!scopeKind?.trim() || !scopeRef.trim()) return void 0;
      return { scopeKind: scopeKind.trim(), scopeRef: scopeRef.trim() };
    }
    function parsePlanningParentOptions(options) {
      const segment = parseSegmentRefOption(options.segment);
      const productionId = options.production ?? segment.productionId ?? "main";
      const segmentId = segment.segmentId;
      if (!segmentId) throw new Error("--segment is required");
      return { productionId, segmentId };
    }
    function parseStoryboardParentOptions(options) {
      const segment = parseSegmentRefOption(options.segment);
      const sceneMoment = parseSceneMomentRefOption(options.sceneMoment);
      const expressionUnit = parseExpressionUnitRefOption(options.expressionUnit);
      const productionId = options.production ?? expressionUnit.productionId ?? sceneMoment.productionId ?? segment.productionId ?? "main";
      const segmentId = expressionUnit.segmentId ?? sceneMoment.segmentId ?? segment.segmentId;
      const sceneMomentId = expressionUnit.sceneMomentId ?? sceneMoment.sceneMomentId;
      const expressionUnitId = expressionUnit.expressionUnitId ?? plainIdOption(options.expressionUnit);
      if (!segmentId) throw new Error("--segment is required unless --scene-moment or --expression-unit is a path under segments");
      if (!sceneMomentId) throw new Error("--scene-moment is required unless --expression-unit is a path under scene_moments");
      return expressionUnitId === void 0 ? { productionId, segmentId, sceneMomentId } : { productionId, segmentId, sceneMomentId, expressionUnitId };
    }
    function parseKeyframeParentOptions(options) {
      return parseStoryboardParentOptions(options);
    }
    function parseAudioCueParentOptions(options) {
      const segment = parseSegmentRefOption(options.segment);
      const sceneMoment = parseSceneMomentRefOption(options.sceneMoment);
      const expressionUnit = parseExpressionUnitRefOption(options.expressionUnit);
      const storyboard = parseStoryboardRefOption(options.storyboard);
      const productionId = options.production ?? storyboard.productionId ?? expressionUnit.productionId ?? sceneMoment.productionId ?? segment.productionId ?? "main";
      const segmentId = storyboard.segmentId ?? expressionUnit.segmentId ?? sceneMoment.segmentId ?? segment.segmentId;
      const sceneMomentId = sceneMoment.sceneMomentId ?? storyboard.sceneMomentId ?? expressionUnit.sceneMomentId;
      const expressionUnitId = expressionUnit.expressionUnitId ?? storyboard.expressionUnitId;
      if (!segmentId) throw new Error("--segment is required unless --scene-moment, --expression-unit, or --storyboard is a path under segments");
      if (!sceneMomentId) throw new Error("--scene-moment is required unless --expression-unit or --storyboard is a path under scene_moments");
      return {
        productionId,
        segmentId,
        sceneMomentId,
        ...expressionUnitId !== void 0 ? { expressionUnitId } : {},
        ...storyboard.storyboardId !== void 0 ? { storyboardId: storyboard.storyboardId } : {}
      };
    }
    function parseExpressionUnitParentOptions(options) {
      const segment = parseSegmentRefOption(options.segment);
      const sceneMoment = parseSceneMomentRefOption(options.sceneMoment);
      const storyboardSource = (options.storyboard ?? []).map(parseStoryboardRefOption).find((source) => source.sceneMomentId !== void 0);
      const productionId = options.production ?? storyboardSource?.productionId ?? sceneMoment.productionId ?? segment.productionId ?? "main";
      const segmentId = storyboardSource?.segmentId ?? sceneMoment.segmentId ?? segment.segmentId;
      const sceneMomentId = sceneMoment.sceneMomentId ?? storyboardSource?.sceneMomentId;
      if (!segmentId) throw new Error("--segment is required unless --scene-moment or --storyboard is a path under segments");
      if (!sceneMomentId) throw new Error("--scene-moment is required unless --storyboard is a path under scene_moments");
      return { productionId, segmentId, sceneMomentId };
    }
    function expressionUnitSpanFromOptions(options) {
      const storyboardRefs = (options.storyboard ?? []).map((value) => targetPathFromSelectionTarget(value));
      const span = pruneUndefined19({
        storyboard_refs: storyboardRefs.length ? storyboardRefs : void 0,
        from_storyboard_id: options.fromStoryboard,
        to_storyboard_id: options.toStoryboard
      });
      return Object.keys(span).length ? span : void 0;
    }
    function repeatedSlashOption(value) {
      if (!value) return [];
      return value.split(",").map((item) => item.trim()).filter(Boolean);
    }
    function parseSegmentRefOption(value) {
      if (!value) return {};
      if (value.includes("/")) return parsePlanningSourcePath(value);
      return { segmentId: value };
    }
    function parseSceneMomentRefOption(value) {
      if (!value) return {};
      if (value.includes("/")) return parsePlanningSourcePath(value);
      return { sceneMomentId: value };
    }
    function parseExpressionUnitRefOption(value) {
      if (!value) return {};
      if (value.includes("/")) return parsePlanningSourcePath(value);
      return { expressionUnitId: value };
    }
    function parseStoryboardRefOption(value) {
      if (!value) return {};
      if (value.includes("/")) return parsePlanningSourcePath(value);
      return { storyboardId: value };
    }
    function parseKeyframeRefOption(value) {
      if (!value) return {};
      if (value.includes("/")) return parsePlanningSourcePath(value);
      return { keyframeId: value };
    }
    function parseAssetRefOption(value) {
      if (!value) return {};
      if (value.includes("/")) return parsePlanningSourcePath(value);
      return { assetRef: value };
    }
    function parseAudioCueRefOption(value) {
      if (!value) return {};
      if (value.includes("/")) return parsePlanningSourcePath(value);
      return { audioCueId: value };
    }
    function plainIdOption(value) {
      return value === void 0 || value.includes("/") ? void 0 : value;
    }
    function parsePlanningSourcePath(value) {
      const path = normalizeCliPath(targetPathFromSelectionTarget(value));
      const parts = path.split("/").filter(Boolean);
      return pruneUndefined19({
        productionId: pathSegmentAfter52(parts, "productions"),
        segmentId: pathSegmentAfter52(parts, "segments"),
        sceneMomentId: pathSegmentAfter52(parts, "scene_moments"),
        storyboardId: pathSegmentAfter52(parts, "storyboards"),
        keyframeId: pathSegmentAfter52(parts, "keyframes"),
        assetRef: pathSegmentAfter52(parts, "assets"),
        audioCueId: pathSegmentAfter52(parts, "audio_cues"),
        expressionUnitId: pathSegmentAfter52(parts, "expression_units")
      });
    }
    function pathSegmentAfter52(parts, marker) {
      const index = parts.indexOf(marker);
      const value = index >= 0 ? parts[index + 1] : void 0;
      if (!value || value.endsWith(".json")) return void 0;
      return value;
    }
    function parseSlashOptions(args) {
      const options = {};
      const positionals = [];
      for (let index = 0; index < args.length; index += 1) {
        const arg = args[index];
        if (!arg.startsWith("--")) {
          positionals.push(arg);
          continue;
        }
        const withoutPrefix = arg.slice(2);
        const equals = withoutPrefix.indexOf("=");
        if (equals >= 0) {
          options[withoutPrefix.slice(0, equals)] = withoutPrefix.slice(equals + 1);
          continue;
        }
        const next = args[index + 1];
        if (next === void 0 || next.startsWith("--")) {
          options[withoutPrefix] = "true";
          continue;
        }
        options[withoutPrefix] = next;
        index += 1;
      }
      return { options, positionals };
    }
    function parseCommandLine(inputValue) {
      const args = [];
      let current = "";
      let quote;
      let escaping = false;
      for (const char of inputValue) {
        if (escaping) {
          current += char;
          escaping = false;
          continue;
        }
        if (char === "\\") {
          escaping = true;
          continue;
        }
        if (quote) {
          if (char === quote) {
            quote = void 0;
          } else {
            current += char;
          }
          continue;
        }
        if (char === '"' || char === "'") {
          quote = char;
          continue;
        }
        if (/\s/.test(char)) {
          if (current) {
            args.push(current);
            current = "";
          }
          continue;
        }
        current += char;
      }
      if (escaping) current += "\\";
      if (quote) throw new Error("unterminated quoted string");
      if (current) args.push(current);
      return args;
    }
    function targetPathFromSelectionTarget(value) {
      const separator = value.indexOf(":");
      if (separator > 0 && value.slice(separator + 1).includes("/")) return value.slice(separator + 1);
      return value;
    }
    function normalizeCliPath(value) {
      return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function inferTargetKind(value) {
      const path = targetPathFromSelectionTarget(value);
      if (path.endsWith("/asset.json") || path.endsWith("asset.json")) return "asset";
      if (path.endsWith("/storyboard.json") || path.endsWith("storyboard.json")) return "storyboard";
      if (path.endsWith("/keyframe.json") || path.endsWith("keyframe.json")) return "keyframe";
      if (path.endsWith("/content_unit.json") || path.endsWith("content_unit.json")) return "content_unit";
      const kind = value.split(":")[0];
      if (kind === "asset" || kind === "storyboard" || kind === "keyframe" || kind === "content_unit") return kind;
      throw new Error("target kind is required for selection");
    }
    function parseTargetKindOption(kind, target, options) {
      if (kind === void 0) {
        try {
          return inferTargetKind(target);
        } catch (error) {
          if (options?.defaultContentUnit) return "content_unit";
          throw error;
        }
      }
      if (kind === "asset" || kind === "storyboard" || kind === "keyframe" || kind === "content_unit") return kind;
      if (options?.defaultContentUnit) return "content_unit";
      throw new Error("target kind must be asset, storyboard, keyframe, or content_unit");
    }
    function parseOptionalTargetKindOption(kind) {
      if (kind === void 0) return void 0;
      if (kind === "asset" || kind === "storyboard" || kind === "keyframe" || kind === "content_unit") return kind;
      throw new Error("target kind must be asset, storyboard, keyframe, or content_unit");
    }
    var import_node_readline = __toESM2(__require("readline"), 1);
    var import_node_fs7 = __require("fs");
    var import_node_path10 = __require("path");
    var MOVSCRIPT_WORKSPACE_MANIFEST_FILE_NAME32 = "manifest.json";
    var MOVSCRIPT_WORKSPACE_CONFIG_TOML_FILE_NAME22 = "config.toml";
    var MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA32 = "movscript.project-workspace.v1";
    var MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME32 = "providers";
    var MOVSCRIPT_WORKSPACE_BACKEND_DIR_NAME22 = "backend";
    var MOVSCRIPT_WORKSPACE_BIN_DIR_NAME22 = "bin";
    var MOVSCRIPT_WORKSPACE_REALMS_DIR_NAME22 = "realms";
    var import_fs7 = __require("fs");
    var import_path18 = __require("path");
    var import_crypto4 = __require("crypto");
    function resolveMovScriptWorkspaceRootPaths3(workspaceDir2) {
      const rootDir = resolveMovScriptHomeDir32(workspaceDir2);
      const controlDir = rootDir;
      const realmsDir = (0, import_path18.join)(controlDir, MOVSCRIPT_WORKSPACE_REALMS_DIR_NAME22);
      return {
        workspaceDir: rootDir,
        rootDir,
        controlDir,
        configTomlPath: (0, import_path18.join)(controlDir, MOVSCRIPT_WORKSPACE_CONFIG_TOML_FILE_NAME22),
        manifestPath: (0, import_path18.join)(controlDir, MOVSCRIPT_WORKSPACE_MANIFEST_FILE_NAME32),
        realmsDir,
        providersDir: (0, import_path18.join)(realmsDir, "local", MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME32),
        backendDir: (0, import_path18.join)(controlDir, MOVSCRIPT_WORKSPACE_BACKEND_DIR_NAME22),
        binDir: (0, import_path18.join)(controlDir, MOVSCRIPT_WORKSPACE_BIN_DIR_NAME22)
      };
    }
    function resolveMovScriptHomeDir32(workspaceDir2) {
      const input3 = workspaceDir2?.trim();
      if (input3) return (0, import_path18.resolve)(input3);
      const explicit = process.env.MOVSCRIPT_HOME?.trim();
      if (explicit) return (0, import_path18.resolve)(explicit);
      const legacy = process.env.MOVSCRIPT_WORKSPACE_DIR?.trim();
      if (legacy) return (0, import_path18.resolve)(legacy);
      return process.cwd();
    }
    function ensureMovScriptWorkspaceRoot3(paths) {
      (0, import_fs7.mkdirSync)(paths.controlDir, { recursive: true });
      (0, import_fs7.mkdirSync)(paths.rootDir, { recursive: true });
      (0, import_fs7.mkdirSync)(paths.realmsDir, { recursive: true });
      (0, import_fs7.mkdirSync)(paths.providersDir, { recursive: true });
      (0, import_fs7.mkdirSync)(paths.backendDir, { recursive: true });
      (0, import_fs7.mkdirSync)(paths.binDir, { recursive: true });
      const current = readMovScriptWorkspaceRootManifest3(paths.manifestPath);
      if (current) return current;
      const manifest = defaultMovScriptWorkspaceRootManifest3();
      writeMovScriptWorkspaceRootManifest3(paths.manifestPath, manifest);
      return manifest;
    }
    function defaultMovScriptWorkspaceRootManifest3(now = /* @__PURE__ */ new Date()) {
      const timestamp = now.toISOString();
      return {
        schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA32,
        workspaceId: (0, import_crypto4.randomUUID)(),
        createdAt: timestamp,
        updatedAt: timestamp,
        layout: {
          providerConfigRoot: MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME32
        }
      };
    }
    function readMovScriptWorkspaceRootManifest3(manifestPath) {
      const parsed = readJSON42(manifestPath);
      if (!isRecord31(parsed) || parsed.schema !== MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA32) return void 0;
      const workspaceId = stringField182(parsed.workspaceId);
      if (!workspaceId) return void 0;
      const backend = normalizeBackend22(parsed.backend);
      const activeRealm = normalizeRealmRecord22(parsed.activeRealm);
      const activeUserId = numberField52(parsed.activeUserId);
      return {
        schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA32,
        workspaceId,
        createdAt: stringField182(parsed.createdAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
        updatedAt: stringField182(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
        ...backend ? { backend } : {},
        ...activeRealm ? { activeRealm } : {},
        ...activeUserId !== void 0 ? { activeUserId } : {},
        layout: normalizeWorkspaceLayout22(parsed.layout)
      };
    }
    function writeMovScriptWorkspaceRootManifest3(manifestPath, manifest) {
      writeJSONAtomic32(manifestPath, {
        ...manifest,
        schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA32,
        updatedAt: manifest.updatedAt || (/* @__PURE__ */ new Date()).toISOString(),
        layout: normalizeWorkspaceLayout22(manifest.layout)
      });
    }
    function normalizeWorkspaceLayout22(_value) {
      return {
        providerConfigRoot: MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME32
      };
    }
    function normalizeBackend22(value) {
      if (!isRecord31(value)) return void 0;
      const kind = value.kind === "local" || value.kind === "cloud" || value.kind === "custom" ? value.kind : void 0;
      const baseURL = stringField182(value.baseURL);
      return kind || baseURL ? { ...kind ? { kind } : {}, ...baseURL ? { baseURL } : {} } : void 0;
    }
    function normalizeRealmRecord22(value) {
      if (!isRecord31(value)) return void 0;
      const kind = value.kind === "local" || value.kind === "cloud" ? value.kind : void 0;
      if (!kind) return void 0;
      if (kind === "local") return { kind, id: "local" };
      const id = stringField182(value.id);
      return id ? { kind, id: safeIdSegment22(id) } : void 0;
    }
    function safeIdSegment22(value) {
      const raw = String(value).trim();
      if (!/^[a-zA-Z0-9._-]{1,128}$/.test(raw) || raw === "." || raw === "..") {
        throw new Error(`invalid MovScript workspace id segment: ${String(value)}`);
      }
      return raw;
    }
    function readJSON42(filePath) {
      try {
        return JSON.parse((0, import_fs7.readFileSync)(filePath, "utf8"));
      } catch (error) {
        if (isNotFoundError42(error)) return void 0;
        throw error;
      }
    }
    function writeJSONAtomic32(filePath, value) {
      (0, import_fs7.mkdirSync)((0, import_path18.dirname)(filePath), { recursive: true });
      const tmpPath = `${filePath}.${process.pid}.${Date.now()}.tmp`;
      (0, import_fs7.writeFileSync)(tmpPath, `${JSON.stringify(value, null, 2)}
`, "utf8");
      (0, import_fs7.renameSync)(tmpPath, filePath);
    }
    function stringField182(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberField52(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function isRecord31(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function isNotFoundError42(error) {
      return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
    }
    var BackendHTTPError22 = class _BackendHTTPError2 extends Error {
      constructor(method, path, status, body, rawBody) {
        super(`Backend ${method} ${path} failed: HTTP ${status} ${backendErrorMessage22(body, rawBody)}`);
        this.method = method;
        this.path = path;
        this.status = status;
        this.body = body;
        this.name = "BackendHTTPError";
      }
      method;
      path;
      status;
      body;
      static async fromResponse(method, path, res) {
        const rawBody = await res.text();
        return new _BackendHTTPError2(method, path, res.status, parseJSONBody22(rawBody), rawBody);
      }
      toJSON() {
        return normalizeBackendHTTPError22(this.method, this.path, this.status, this.body);
      }
    };
    function errorData2(error) {
      if (error instanceof BackendHTTPError22) return error.toJSON();
      return void 0;
    }
    function normalizeBackendHTTPError22(method, path, status, body) {
      const bodyRecord = isRecord322(body) ? body : void 0;
      return {
        type: "backend_http_error",
        method,
        path,
        status,
        ...bodyRecord ? { body: bodyRecord } : {},
        ...bodyRecord && typeof bodyRecord.code === "string" ? { code: bodyRecord.code } : {},
        ...bodyRecord && typeof bodyRecord.field === "string" ? { field: bodyRecord.field } : {},
        ...bodyRecord && Array.isArray(bodyRecord.allowed_values) ? { allowed_values: bodyRecord.allowed_values } : {},
        ...bodyRecord && isRecord322(bodyRecord.suggested_fix) ? { suggested_fix: bodyRecord.suggested_fix } : {},
        ...bodyRecord && Number.isInteger(bodyRecord.required_min) ? { required_min: bodyRecord.required_min } : {},
        ...bodyRecord && Number.isInteger(bodyRecord.allowed_max) ? { allowed_max: bodyRecord.allowed_max } : {},
        ...bodyRecord && Number.isInteger(bodyRecord.actual_count) ? { actual_count: bodyRecord.actual_count } : {},
        ...bodyRecord && isRecord322(bodyRecord.details) ? { details: bodyRecord.details } : {}
      };
    }
    function parseJSONBody22(rawBody) {
      if (!rawBody.trim()) return void 0;
      try {
        return JSON.parse(rawBody);
      } catch {
        return rawBody;
      }
    }
    function backendErrorMessage22(body, rawBody) {
      if (isRecord322(body) && typeof body.error === "string") return body.error;
      return rawBody;
    }
    function isRecord322(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    var import_fs22 = __require("fs");
    var import_path22 = __require("path");
    var MOVSCRIPT_BACKEND_CONFIG_SCHEMA22 = "movscript.backend-config.v1";
    var MOVSCRIPT_BACKEND_AUTH_SCHEMA22 = "movscript.backend-auth.v1";
    var MOVSCRIPT_BACKEND_CONFIG_FILE_NAME22 = "config.json";
    var MOVSCRIPT_BACKEND_AUTH_FILE_NAME22 = "auth.json";
    var MOVSCRIPT_DEFAULT_BACKEND_BASE_URL22 = "http://localhost:8766";
    function resolveMovScriptBackendPaths22(workspaceDir2 = process.cwd(), realm) {
      const root = resolveMovScriptWorkspaceRootPaths3(workspaceDir2);
      const backendRealmsDir = (0, import_path22.join)(root.backendDir, "realms");
      const authRealm = normalizeRealm22(realm) ?? { kind: "local", id: "local" };
      return {
        workspaceDir: root.workspaceDir,
        controlDir: root.controlDir,
        backendDir: root.backendDir,
        backendRealmsDir,
        configPath: (0, import_path22.join)(root.backendDir, MOVSCRIPT_BACKEND_CONFIG_FILE_NAME22),
        authPath: (0, import_path22.join)(backendRealmsDir, ...backendRealmPath22(authRealm), MOVSCRIPT_BACKEND_AUTH_FILE_NAME22)
      };
    }
    function defaultMovScriptBackendConfig22(baseURL = MOVSCRIPT_DEFAULT_BACKEND_BASE_URL22) {
      return {
        schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA22,
        baseURL: normalizeBackendBaseURL22(baseURL),
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    function readMovScriptBackendConfig22(workspaceDir2 = process.cwd()) {
      const paths = resolveMovScriptBackendPaths22(workspaceDir2);
      const parsed = readJSON52(paths.configPath);
      if (!isRecord332(parsed) || parsed.schema !== MOVSCRIPT_BACKEND_CONFIG_SCHEMA22) return defaultMovScriptBackendConfig22();
      const baseURL = stringField192(parsed.baseURL) ?? MOVSCRIPT_DEFAULT_BACKEND_BASE_URL22;
      const realm = normalizeRealm22(parsed.realm);
      const activeUserId = idField132(parsed.activeUserId);
      return {
        schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA22,
        baseURL: normalizeBackendBaseURL22(baseURL),
        ...realm ? { realm } : {},
        ...activeUserId !== void 0 ? { activeUserId } : {},
        updatedAt: stringField192(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    function readMovScriptBackendAuth22(workspaceDir2 = process.cwd(), realmInput) {
      const config = readMovScriptBackendConfig22(workspaceDir2);
      const requestedRealm = normalizeRealm22(realmInput) ?? config.realm;
      const paths = resolveMovScriptBackendPaths22(workspaceDir2, requestedRealm);
      const parsed = readJSON52(paths.authPath);
      if (!isRecord332(parsed) || parsed.schema !== MOVSCRIPT_BACKEND_AUTH_SCHEMA22) return void 0;
      const token = stringField192(parsed.token);
      if (!token) return void 0;
      const tokenType = parsed.tokenType === "Bearer" ? "Bearer" : void 0;
      if (!tokenType) return void 0;
      const user = normalizeAuthUser22(parsed.user);
      const gitCredential = normalizeGitCredential22(parsed.gitCredential ?? parsed.git_credential);
      const realm = normalizeRealm22(parsed.realm) ?? requestedRealm;
      return {
        schema: MOVSCRIPT_BACKEND_AUTH_SCHEMA22,
        tokenType,
        token,
        ...realm ? { realm } : {},
        ...gitCredential ? { gitCredential } : {},
        ...user ? { user } : {},
        ...stringField192(parsed.expiresAt) ? { expiresAt: stringField192(parsed.expiresAt) } : {},
        updatedAt: stringField192(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    function resolveMovScriptBackendSession22(input3 = {}) {
      const root = resolveMovScriptBackendPaths22(input3.workspaceDir);
      const config = readMovScriptBackendConfig22(root.workspaceDir);
      const realm = normalizeRealm22(input3.realm) ?? config.realm;
      const paths = resolveMovScriptBackendPaths22(root.workspaceDir, realm);
      const auth = readMovScriptBackendAuth22(paths.workspaceDir, realm);
      const token = stringField192(input3.token) ?? stringField192(process.env.MOVCLI_TOKEN) ?? auth?.token;
      const userId = idField132(input3.userId) ?? idField132(auth?.user?.id) ?? idField132(config.activeUserId);
      const baseURL = normalizeBackendBaseURL22(stringField192(input3.server) ?? stringField192(process.env.MOVSCRIPT_API_BASE_URL) ?? config.baseURL);
      return {
        workspaceDir: paths.workspaceDir,
        baseURL,
        apiBaseURL: normalizeBackendAPIBaseURL22(baseURL),
        ...token ? { token } : {},
        tokenType: "Bearer",
        ...userId !== void 0 ? { userId: String(userId) } : {},
        ...auth?.user ? { user: auth.user } : {},
        configPath: paths.configPath,
        authPath: paths.authPath
      };
    }
    function backendRealmPath22(realm) {
      if (realm.kind === "local") return ["local"];
      return ["cloud", String(realm.id)];
    }
    function normalizeBackendBaseURL22(value) {
      const trimmed = value.trim().replace(/\/+$/, "");
      const withoutAPI = trimmed.endsWith("/api/v1") ? trimmed.slice(0, -"/api/v1".length) : trimmed;
      return withoutAPI || MOVSCRIPT_DEFAULT_BACKEND_BASE_URL22;
    }
    function normalizeBackendAPIBaseURL22(value) {
      return `${normalizeBackendBaseURL22(value)}/api/v1`;
    }
    function readJSON52(filePath) {
      try {
        return JSON.parse((0, import_fs22.readFileSync)(filePath, "utf8"));
      } catch (error) {
        if (isNotFoundError52(error)) return void 0;
        throw error;
      }
    }
    function normalizeAuthUser22(value) {
      if (!isRecord332(value)) return void 0;
      const id = idField132(value.id);
      const username = stringField192(value.username);
      const displayName22 = stringField192(value.displayName) ?? stringField192(value.display_name);
      const primaryEmail = stringField192(value.primaryEmail) ?? stringField192(value.primary_email);
      const locale = stringField192(value.locale);
      const systemRole = stringField192(value.systemRole) ?? stringField192(value.system_role);
      const user = {
        ...id !== void 0 ? { id } : {},
        ...username ? { username } : {},
        ...displayName22 ? { displayName: displayName22 } : {},
        ...primaryEmail ? { primaryEmail } : {},
        ...locale ? { locale } : {},
        ...systemRole ? { systemRole } : {}
      };
      return Object.keys(user).length > 0 ? user : void 0;
    }
    function normalizeGitCredential22(value) {
      if (!isRecord332(value)) return void 0;
      const provider = stringField192(value.provider);
      const username = stringField192(value.username);
      if (provider !== "gitea" || !username) return void 0;
      const token = stringField192(value.token);
      const maskedToken = stringField192(value.maskedToken) ?? stringField192(value.masked_token);
      const status = stringField192(value.status);
      const lastError = stringField192(value.lastError) ?? stringField192(value.last_error);
      return {
        provider,
        username,
        ...token ? { token } : {},
        ...maskedToken ? { maskedToken } : {},
        ...status ? { status } : {},
        ...lastError ? { lastError } : {}
      };
    }
    function normalizeRealm22(value) {
      if (!isRecord332(value)) return void 0;
      const kind = value.kind === "local" || value.kind === "cloud" ? value.kind : void 0;
      if (!kind) return void 0;
      if (kind === "local") return { kind, id: "local" };
      const id = idField132(value.id);
      return id ? { kind, id: String(id) } : void 0;
    }
    function stringField192(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function idField132(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function isRecord332(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function isNotFoundError52(error) {
      return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
    }
    var backendAPIBaseURL22 = normalizeBackendAPIBaseURL22(defaultMovScriptBackendBaseURL22());
    var backendRuntimeAuthToken22 = "";
    var configuredBackendDefaultWorkspaceDir22;
    function getMovScriptBackendAPIBaseURL22() {
      return backendAPIBaseURL22;
    }
    function getMovScriptBackendRuntimeAuthToken22() {
      return backendRuntimeAuthToken22;
    }
    function setMovScriptBackendDefaultWorkspaceDir22(workspaceDir2) {
      configuredBackendDefaultWorkspaceDir22 = workspaceDir2?.trim() || void 0;
    }
    function resolveMovScriptBackendDefaultWorkspaceDir22() {
      return configuredBackendDefaultWorkspaceDir22 || process.env.MOVSCRIPT_HOME || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd();
    }
    function defaultMovScriptBackendBaseURL22() {
      return typeof process !== "undefined" ? process.env.MOVSCRIPT_API_BASE_URL || MOVSCRIPT_DEFAULT_BACKEND_BASE_URL22 : MOVSCRIPT_DEFAULT_BACKEND_BASE_URL22;
    }
    async function backendGet222(path) {
      const headers = backendHeaders222();
      const res = await fetch(`${getMovScriptBackendAPIBaseURL22()}${path}`, { headers });
      if (!res.ok) {
        throw await BackendHTTPError22.fromResponse("GET", path, res);
      }
      return res.json();
    }
    async function backendGetBinary2(path, options = {}) {
      const headers = backendHeaders222();
      const res = await fetch(`${getMovScriptBackendAPIBaseURL22()}${path}`, { headers });
      if (!res.ok) {
        throw await BackendHTTPError22.fromResponse("GET", path, res);
      }
      const contentLengthHeader = res.headers.get("content-length");
      const contentLength = contentLengthHeader === null ? NaN : Number(contentLengthHeader);
      if (options.maxBytes !== void 0 && Number.isFinite(contentLength) && contentLength > options.maxBytes) {
        throw new Error(`backend GET ${path} returned content-length ${contentLength}, above maxBytes=${options.maxBytes}`);
      }
      const bytes = await readBinaryResponse2(path, res, {
        maxBytes: options.maxBytes,
        contentLength: Number.isFinite(contentLength) ? contentLength : void 0,
        onProgress: options.onProgress
      });
      if (options.maxBytes !== void 0 && bytes.length > options.maxBytes) {
        throw new Error(`backend GET ${path} returned ${bytes.length} bytes, above maxBytes=${options.maxBytes}`);
      }
      return {
        bytes,
        ...res.headers.get("content-type") ? { contentType: res.headers.get("content-type") ?? void 0 } : {},
        ...Number.isFinite(contentLength) ? { contentLength } : {}
      };
    }
    async function readBinaryResponse2(path, res, options) {
      if (!res.body) {
        const bytes2 = Buffer.from(await res.arrayBuffer());
        options.onProgress?.({
          path,
          receivedBytes: bytes2.length,
          ...options.contentLength !== void 0 ? { totalBytes: options.contentLength } : {},
          done: true
        });
        return bytes2;
      }
      const reader = res.body.getReader();
      const chunks = [];
      let receivedBytes = 0;
      for (; ; ) {
        const { done, value } = await reader.read();
        if (done) break;
        if (!value) continue;
        receivedBytes += value.byteLength;
        if (options.maxBytes !== void 0 && receivedBytes > options.maxBytes) {
          throw new Error(`backend GET ${path} returned more than maxBytes=${options.maxBytes}`);
        }
        chunks.push(value);
        options.onProgress?.({
          path,
          receivedBytes,
          ...options.contentLength !== void 0 ? { totalBytes: options.contentLength } : {},
          done: false
        });
      }
      const bytes = Buffer.concat(chunks.map((chunk) => Buffer.from(chunk)));
      options.onProgress?.({
        path,
        receivedBytes,
        ...options.contentLength !== void 0 ? { totalBytes: options.contentLength } : {},
        done: true
      });
      return bytes;
    }
    async function backendPost222(path, body, userId) {
      const headers = backendHeaders222({ json: true, userId });
      const res = await fetch(`${getMovScriptBackendAPIBaseURL22()}${path}`, {
        method: "POST",
        headers,
        body: JSON.stringify(body)
      });
      if (!res.ok) {
        throw await BackendHTTPError22.fromResponse("POST", path, res);
      }
      return res.json();
    }
    async function backendPostMultipart2(path, form, userId) {
      const headers = backendHeaders222({ userId });
      const res = await fetch(`${getMovScriptBackendAPIBaseURL22()}${path}`, {
        method: "POST",
        headers,
        body: form
      });
      if (!res.ok) {
        throw await BackendHTTPError22.fromResponse("POST", path, res);
      }
      return res.json();
    }
    async function backendList2(path) {
      const data = await backendGet222(path);
      if (Array.isArray(data)) return data;
      if (isRecord2102(data) && Array.isArray(data.items)) return data.items;
      return [];
    }
    function backendHeaders222(input3 = {}) {
      const headers = input3.json ? { "Content-Type": "application/json" } : {};
      const authToken = getMovScriptBackendRuntimeAuthToken22() || workspaceBackendAuthToken22();
      if (authToken) headers.Authorization = `Bearer ${authToken}`;
      const userId = typeof input3.userId === "number" || typeof input3.userId === "string" ? String(input3.userId) : workspaceBackendUserId22();
      if (userId) headers["X-User-ID"] = userId;
      return headers;
    }
    function workspaceBackendAuthToken22() {
      try {
        return readMovScriptBackendAuth22(resolveMovScriptBackendDefaultWorkspaceDir22())?.token ?? "";
      } catch {
        return "";
      }
    }
    function workspaceBackendUserId22() {
      try {
        return resolveMovScriptBackendSession22({ workspaceDir: resolveMovScriptBackendDefaultWorkspaceDir22() }).userId ?? "";
      } catch {
        return "";
      }
    }
    function isRecord2102(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function isRecord342(value) {
      return !!value && typeof value === "object" && !Array.isArray(value);
    }
    function stringValue132(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function toMCPJSONValue2(value) {
      if (value === null || typeof value === "string" || typeof value === "number" || typeof value === "boolean") return value;
      if (Array.isArray(value)) return value.map(toMCPJSONValue2);
      if (!isRecord342(value)) return String(value);
      const obj = {};
      for (const [key, item] of Object.entries(value)) {
        if (item !== void 0) obj[key] = toMCPJSONValue2(item);
      }
      return obj;
    }
    function renderMarkdown2(value) {
      if (value === null || value === void 0) return "\u65E0\u6570\u636E\u3002";
      if (typeof value !== "object") return String(value);
      if (Array.isArray(value)) return renderMarkdownArray2(value);
      if (isRecord342(value) && Array.isArray(value.projects)) {
        const lines = ["## \u9879\u76EE\u5217\u8868"];
        if (value.projects.length === 0) {
          lines.push("\u6CA1\u6709\u53EF\u89C1\u9879\u76EE\u3002");
        } else {
          lines.push(...value.projects.map((project, index) => formatProjectLine2(project, index)));
        }
        if (typeof value.count === "number") lines.push("", `\u5171 ${value.count} \u4E2A\u9879\u76EE\u3002`);
        return lines.join("\n");
      }
      if (isRecord342(value) && isRecord342(value.snapshot)) {
        const lines = ["## \u5F53\u524D\u4E0A\u4E0B\u6587"];
        lines.push(renderMarkdownObject2(value.snapshot));
        if (Array.isArray(value.projects)) {
          lines.push("", "## \u9879\u76EE\u5217\u8868");
          if (value.projects.length === 0) {
            lines.push(typeof value.projectsError === "string" ? `\u9879\u76EE\u5217\u8868\u4E0D\u53EF\u7528\uFF1A${value.projectsError}` : "\u6CA1\u6709\u53EF\u89C1\u9879\u76EE\u3002");
          } else {
            lines.push(...value.projects.map((project, index) => formatProjectLine2(project, index)));
          }
        }
        return lines.join("\n");
      }
      return renderMarkdownObject2(value);
    }
    function renderMarkdownArray2(items) {
      if (items.length === 0) return "\u6CA1\u6709\u6761\u76EE\u3002";
      return items.map((item, index) => `${index + 1}. ${renderInlineMarkdownValue2(item)}`).join("\n");
    }
    function renderMarkdownObject2(value) {
      const lines = [];
      for (const [key, item] of Object.entries(value)) {
        if (item === void 0) continue;
        if (Array.isArray(item)) {
          lines.push(`### ${key}`);
          lines.push(renderMarkdownArray2(item));
        } else if (isRecord342(item)) {
          lines.push(`### ${key}`);
          lines.push(renderMarkdownObject2(item));
        } else {
          lines.push(`- ${key}: ${renderInlineMarkdownValue2(item)}`);
        }
      }
      return lines.length > 0 ? lines.join("\n") : "\u65E0\u6570\u636E\u3002";
    }
    function formatProjectLine2(project, index) {
      if (!isRecord342(project)) return `${index + 1}. \u9879\u76EE${index + 1}\u7684\u540D\u5B57${String(project)}`;
      const name = typeof project.name === "string" && project.name.trim() ? project.name.trim() : `\u672A\u547D\u540D\u9879\u76EE ${index + 1}`;
      const details = [
        typeof project.description === "string" && project.description.trim() ? project.description.trim() : void 0,
        typeof project.totalEpisodes === "number" ? `\u96C6\u6570\uFF1A${project.totalEpisodes}` : void 0
      ].filter(Boolean).join("\uFF1B");
      const id = typeof project.id === "number" ? `\uFF08project#${project.id}\uFF09` : "";
      return `${index + 1}. \u9879\u76EE${index + 1}\u7684\u540D\u5B57${name}${details ? `\uFF0C${details}` : ""}${id}`;
    }
    function renderInlineMarkdownValue2(value) {
      if (value === null || value === void 0) return "\u65E0";
      if (typeof value !== "object") return String(value);
      if (Array.isArray(value)) return value.length === 0 ? "\u65E0" : value.map(renderInlineMarkdownValue2).join("\uFF1B");
      if (isRecord342(value)) {
        return Object.entries(value).filter(([, item]) => item !== void 0).map(([key, item]) => `${key}=${renderInlineMarkdownValue2(item)}`).join("\uFF0C");
      }
      return String(value);
    }
    function toolText2(value, displayValue = value) {
      return {
        content: [
          {
            type: "text",
            text: renderMarkdown2(displayValue ?? null)
          }
        ],
        data: toMCPJSONValue2(value ?? null)
      };
    }
    function resourceContent2(uri, value) {
      return {
        contents: [
          {
            uri,
            mimeType: "text/markdown",
            text: renderMarkdown2(value ?? null)
          }
        ],
        data: toMCPJSONValue2(value ?? null)
      };
    }
    function getStringParam2(params, key) {
      const obj = getObject2(params);
      const value = obj[key];
      if (typeof value !== "string" || value.length === 0) throw new Error(`${key} is required`);
      return value;
    }
    function getObjectParam2(params, key) {
      const obj = getObject2(params);
      const value = obj[key];
      return isRecord342(value) ? value : {};
    }
    function getObject2(value) {
      if (!isRecord342(value)) return {};
      return value;
    }
    function objectSchema32(properties, requiredOrOptions) {
      const options = Array.isArray(requiredOrOptions) ? { required: requiredOrOptions } : requiredOrOptions;
      return {
        type: "object",
        properties,
        ...options?.required !== void 0 ? { required: options.required } : {},
        additionalProperties: false
      };
    }
    function getOptionalString3(args, key) {
      const value = args[key];
      return textOrUndefined2(value);
    }
    function getOptionalNumeric2(args, key) {
      return numericValue2(args[key]);
    }
    function numericValue2(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        return Number.isFinite(parsed) ? parsed : void 0;
      }
      return void 0;
    }
    function numericValues2(value) {
      if (Array.isArray(value)) {
        const values = value.map(numericValue2);
        return values.every((item) => item !== void 0) ? values : void 0;
      }
      const single = numericValue2(value);
      return single === void 0 ? void 0 : [single];
    }
    function textOrUndefined2(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function clampNumber3(value, min, max) {
      return Math.max(min, Math.min(max, value));
    }
    function artifactTools2() {
      return [
        {
          name: "system_artifact_upload_export",
          description: "Upload a completed local export artifact as a neutral RawResource. This hosts an existing file only; it does not render media or write domain decisions. When resolving a completed Electron task by taskId, pass projectId for persisted workspace recovery. HLS manifests must use system_artifact_upload_hls_stream so manifest and segment files are hosted as a MediaStreamArtifact instead of one RawResource.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task output recovery." },
            project_id: { type: "string", description: "Alias for projectId." },
            outputPath: { type: "string" },
            output_path: { type: "string" },
            filePath: { type: "string" },
            file_path: { type: "string" },
            filename: { type: "string" },
            mimeType: { type: "string" },
            mime_type: { type: "string" },
            folderId: { type: ["string", "number"] },
            folder_id: { type: ["string", "number"] },
            derivative: { type: "object" },
            operation: { type: "string" },
            tool: { type: "string" },
            inputResourceIds: { type: "array", items: { type: ["string", "number"] } },
            input_resource_ids: { type: "array", items: { type: ["string", "number"] } },
            sourceResourceId: { type: ["string", "number"] },
            source_resource_id: { type: ["string", "number"] },
            sourceResourceIds: { type: "array", items: { type: ["string", "number"] } },
            source_resource_ids: { type: "array", items: { type: ["string", "number"] } },
            params: { type: "object" }
          })
        },
        {
          name: "system_artifact_upload_hls_stream",
          description: "Upload completed HLS manifest/segments as a neutral MediaStreamArtifact. This hosts an existing artifact only; it does not render media or write domain decisions. When resolving a completed Electron HLS task by taskId, pass projectId for persisted workspace output recovery.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            manifestPath: { type: "string" },
            manifest_path: { type: "string" },
            segmentPaths: { type: "array", items: { type: "string" } },
            segment_paths: { type: "array", items: { type: "string" } },
            title: { type: "string" },
            projectId: { type: ["string", "number"] },
            project_id: { type: ["string", "number"] },
            sourceResourceId: { type: ["string", "number"] },
            source_resource_id: { type: ["string", "number"] },
            sourceDerivativeId: { type: ["string", "number"] },
            source_derivative_id: { type: ["string", "number"] },
            durationMs: { type: "number" },
            duration_ms: { type: "number" },
            width: { type: "number" },
            height: { type: "number" },
            expiresAt: { type: "string" },
            expires_at: { type: "string" },
            expiresInSeconds: { type: "number" },
            expires_in_seconds: { type: "number" }
          })
        },
        {
          name: "system_artifact_get_stream",
          description: "Read MediaStreamArtifact metadata and playback URLs from backend hosting. This does not inspect or reinterpret the source timeline.",
          inputSchema: objectSchema32({
            streamId: { type: ["string", "number"] },
            stream_id: { type: ["string", "number"] },
            id: { type: ["string", "number"] }
          })
        }
      ];
    }
    function normalizeModelCapabilityAlias2(value) {
      const normalized = value?.trim().toLowerCase().replace(/-/g, "_");
      switch (normalized) {
        case "text":
        case "text_generation":
        case "reasoning":
        case "image":
        case "image_generation":
        case "image_edit":
        case "video":
        case "video_generation":
        case "video_i2v":
        case "video_v2v":
        case "audio":
        case "audio_generation":
        case "audio_tts":
        case "audio_transcribe":
        case "audio_translate":
        case "audio_music":
        case "audio_sfx":
        case "audio_chat":
        case "voice_clone":
        case "voice_design":
        case "subtitle_align":
        case "subtitle_translate":
          return normalized;
        case "tts":
          return "audio_tts";
        case "speech_translate":
          return "audio_translate";
        case "stt":
          return "audio_transcribe";
        case "music":
          return "audio_music";
        case "sfx":
          return "audio_sfx";
        default:
          return void 0;
      }
    }
    function copyFiniteNumber2(out, source, sourceKey, targetKey = sourceKey) {
      const value = source[sourceKey];
      if (typeof value === "number" && Number.isFinite(value)) out[targetKey] = value;
    }
    function isJSONScalar2(value) {
      return typeof value === "string" || typeof value === "number" || typeof value === "boolean";
    }
    function numericModelField2(source, key) {
      const value = source[key];
      const parsed = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
      return Number.isFinite(parsed) && parsed > 0 ? parsed : void 0;
    }
    function stringModelField2(source, key) {
      const value = source[key];
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function integerModelField2(source, key, min, fallback) {
      const value = source[key];
      const parsed = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
      if (!Number.isInteger(parsed) || parsed < min) return fallback;
      return parsed;
    }
    function stringArrayModelField2(value) {
      if (!Array.isArray(value)) return [];
      return Array.from(new Set(value.flatMap((item) => typeof item === "string" && item.trim() ? [item.trim()] : [])));
    }
    function summarizeInputRequirementsForAgent2(value) {
      const source = isRecord342(value) ? value : {};
      return {
        image: summarizeInputRequirementForAgent2(source.image),
        video: summarizeInputRequirementForAgent2(source.video)
      };
    }
    function summarizeInputRequirementForAgent2(value) {
      const source = isRecord342(value) ? value : {};
      const min = integerModelField2(source, "min", 0, 0);
      const max = integerModelField2(source, "max", -1, 0);
      if (max !== -1 && min > max) return { min: 0, max: 0 };
      return {
        min,
        max
      };
    }
    function summarizeSupportedParamsForAgent2(supportedParams, schema) {
      const properties = isRecord342(schema?.properties) ? schema.properties : void 0;
      const params = supportedParams.map((param) => summarizeSupportedParamDefForAgent2(param, properties)).filter((param) => !!param);
      if (params.length > 0) return params;
      if (!properties) return [];
      return Object.entries(properties).map(([key, property]) => summarizeSchemaPropertyForAgent2(key, property)).filter((param) => !!param);
    }
    function summarizeSupportedParamDefForAgent2(param, schemaProperties) {
      if (!isRecord342(param) || typeof param.key !== "string" || !param.key.trim()) return void 0;
      const out = { key: param.key.trim() };
      if (typeof param.label === "string" && param.label.trim()) out.label = param.label.trim();
      if (typeof param.type === "string" && param.type.trim()) out.type = param.type.trim();
      if (Array.isArray(param.options)) {
        const options = stringArrayModelField2(param.options);
        if (options.length > 0) out.options = options;
      }
      if (param.default !== void 0) out.default = param.default;
      copyFiniteNumber2(out, param, "min");
      copyFiniteNumber2(out, param, "max");
      copyFiniteNumber2(out, param, "step");
      copyStringArray2(out, param, "conflicts_with");
      copyConditionalEnumRules2(out, param);
      copyConditionalConstRules2(out, param);
      copyRequiresValueRules2(out, param);
      mergeSchemaPropertySummary2(out, schemaProperties?.[out.key]);
      return out;
    }
    function summarizeSchemaPropertyForAgent2(key, property) {
      const trimmedKey = key.trim();
      if (!trimmedKey || !isRecord342(property)) return void 0;
      const out = { key: trimmedKey };
      if (typeof property.type === "string" && property.type.trim()) out.type = property.type.trim();
      copySchemaEnum2(out, property);
      if (property.default !== void 0) out.default = property.default;
      copyFiniteNumber2(out, property, "minimum", "min");
      copyFiniteNumber2(out, property, "maximum", "max");
      copyFiniteNumber2(out, property, "multipleOf", "step");
      if (typeof property.description === "string" && property.description.trim()) out.description = property.description.trim();
      return out;
    }
    function mergeSchemaPropertySummary2(out, property) {
      if (!isRecord342(property)) return;
      copySchemaEnum2(out, property);
      copyFiniteNumber2(out, property, "minimum", "min");
      copyFiniteNumber2(out, property, "maximum", "max");
      copyFiniteNumber2(out, property, "multipleOf", "step");
      if (property.default !== void 0 && out.default === void 0) out.default = property.default;
      if (typeof property.description === "string" && property.description.trim()) out.description = property.description.trim();
    }
    function copySchemaEnum2(out, property) {
      if (!Array.isArray(property.enum)) return;
      const values = property.enum.filter(isJSONScalar2);
      if (values.length === 0) return;
      if (values.every((value) => typeof value === "string")) out.options = values;
      else out.enum = values;
    }
    function copyStringArray2(out, source, key) {
      const value = source[key];
      if (!Array.isArray(value)) return;
      const items = stringArrayModelField2(value);
      if (items.length > 0) out[key] = items;
    }
    function copyConditionalEnumRules2(out, source) {
      const value = source.conditional_enum;
      if (!Array.isArray(value)) return;
      const rules = value.flatMap((item) => {
        if (!isRecord342(item)) return [];
        const whenParam = typeof item.when_param === "string" ? item.when_param.trim() : "";
        const options = Array.isArray(item.options) ? item.options.filter((option) => typeof option === "string") : [];
        if (!whenParam || !isJSONScalar2(item.when_value) || options.length === 0) return [];
        return [{
          when_param: whenParam,
          when_value: item.when_value,
          options
        }];
      });
      if (rules.length > 0) out.conditional_enum = rules;
    }
    function copyConditionalConstRules2(out, source) {
      const value = source.conditional_const;
      if (!Array.isArray(value)) return;
      const rules = value.flatMap((item) => {
        if (!isRecord342(item)) return [];
        const whenParam = typeof item.when_param === "string" ? item.when_param.trim() : "";
        if (!whenParam || !isJSONScalar2(item.when_value) || !isJSONScalar2(item.value)) return [];
        return [{
          when_param: whenParam,
          when_value: item.when_value,
          value: item.value
        }];
      });
      if (rules.length > 0) out.conditional_const = rules;
    }
    function copyRequiresValueRules2(out, source) {
      const value = source.requires_value;
      if (!Array.isArray(value)) return;
      const rules = value.flatMap((item) => {
        if (!isRecord342(item)) return [];
        const param = typeof item.param === "string" ? item.param.trim() : "";
        if (!param || !isJSONScalar2(item.value)) return [];
        return [{
          param,
          value: item.value
        }];
      });
      if (rules.length > 0) out.requires_value = rules;
    }
    function summarizeModelContractForAgent2(model) {
      const source = isRecord342(model) ? model : {};
      const schema = isRecord342(source.params_schema) ? source.params_schema : void 0;
      const supportedParams = Array.isArray(source.supported_params) ? source.supported_params : [];
      const numericID = numericModelField2(source, "id") ?? numericModelField2(source, "ID");
      const supportedParamKeys = supportedParams.flatMap((param) => {
        if (!isRecord342(param) || typeof param.key !== "string" || !param.key.trim()) return [];
        return [param.key.trim()];
      });
      const propertyKeys = Object.keys(isRecord342(schema?.properties) ? schema.properties : {});
      return {
        contract_version: 1,
        model_id: stringModelField2(source, "model_id") ?? stringModelField2(source, "logical_model_id") ?? stringModelField2(source, "model_def_id") ?? (numericID ? `backend.model.${numericID}` : "default"),
        ...typeof source.display_name === "string" && source.display_name.trim() ? { display_name: source.display_name.trim() } : {},
        ...typeof source.short_name === "string" && source.short_name.trim() ? { short_name: source.short_name.trim() } : {},
        ...typeof source.logical_model_id === "string" && source.logical_model_id.trim() ? { logical_model_id: source.logical_model_id.trim() } : {},
        capabilities: stringArrayModelField2(source.capabilities),
        accepts_image_input: source.accepts_image_input === true,
        input_requirements: summarizeInputRequirementsForAgent2(source.input_requirements),
        supported_params: summarizeSupportedParamsForAgent2(supportedParams, schema),
        supported_param_keys: Array.from(new Set(supportedParamKeys.length > 0 ? supportedParamKeys : propertyKeys)).sort(),
        params_schema_loaded: !!schema,
        ...Array.isArray(schema?.["allOf"]) ? { params_schema_rule_count: schema["allOf"].length } : {}
      };
    }
    var workspaceLocator2 = {
      workspaceDir: { type: "string", description: "Optional MovScript workspace container directory. Defaults to the current MovScript workspace dir." },
      workspace_dir: { type: "string", description: "Alias for workspaceDir." },
      projectDir: { type: "string", description: "MovScript project source directory. Domain tools read and write this path directly." },
      project_dir: { type: "string", description: "Alias for projectDir." },
      projectPath: { type: "string", description: "Alias for projectDir." },
      project_path: { type: "string", description: "Alias for projectDir." },
      cwd: { type: "string", description: "Alias for projectDir when a provider passes the current project working directory." },
      projectUid: { type: "string", description: "Optional manifest project_uid used for scoped backend candidate metadata." },
      project_uid: { type: "string", description: "Alias for projectUid." }
    };
    var contentCandidateStatuses2 = ["queued", "running", "succeeded", "failed", "canceled", "imported"];
    var entityQuery2 = {
      ...workspaceLocator2,
      entityKind: { type: "string", description: "Optional semantic entity kind, for example setting, production, scene_moment, expression_unit, content_unit, storyboard, or keyframe." },
      entity_kind: { type: "string", description: "Alias for entityKind." },
      kind: { type: "string" },
      query: { type: "string" },
      q: { type: "string", description: "Alias for query." },
      productionId: { type: ["string", "number"] },
      production_id: { type: ["string", "number"] },
      segmentId: { type: ["string", "number"] },
      segment_id: { type: ["string", "number"] },
      sceneMomentId: { type: ["string", "number"] },
      scene_moment_id: { type: ["string", "number"] },
      expressionUnitId: { type: ["string", "number"] },
      expression_unit_id: { type: ["string", "number"] },
      storyboardId: { type: ["string", "number"] },
      storyboard_id: { type: ["string", "number"] },
      contentUnitId: { type: ["string", "number"] },
      content_unit_id: { type: ["string", "number"] },
      settingId: { type: ["string", "number"] },
      setting_id: { type: ["string", "number"] },
      settingStateId: { type: ["string", "number"] },
      setting_state_id: { type: ["string", "number"] },
      limit: { type: "number" }
    };
    var inspectOptions2 = {
      ...workspaceLocator2,
      commit: { type: "string", description: "Optional git commit/ref to compare current source against. Defaults to HEAD when the workspace is in git." },
      checkpointHash: { type: "string", description: "Compatibility alias for commit." },
      checkpoint_hash: { type: "string", description: "Alias for checkpointHash." }
    };
    function domainTools2() {
      return [
        {
          name: "domain_get_model",
          description: "Return the movscript-lang workspace model for one editable domain entity: concept, editable path hints, path/vocabulary semantics, context paths, schema ids, supported write APIs, and agent instructions. Call this before direct file edits. Paths are the canonical instance tree; namespace vocabulary supplies labels/templates, not a second structure source. This is project-scoped and does not write files.",
          inputSchema: projectSchema2(
            {
              ...workspaceLocator2,
              entityKind: { type: "string", description: "Domain entity kind, for example setting, asset, production, expression_unit, storyboard, content_unit, or keyframe." },
              entity_kind: { type: "string", description: "Alias for entityKind." },
              entityId: { type: ["string", "number"], description: "Optional entity id used to expand editable path hints." },
              entity_id: { type: ["string", "number"], description: "Alias for entityId." }
            },
            ["entityKind"]
          )
        },
        {
          name: "domain_query_entities",
          description: "Query indexed MovScript domain entities from source/current indexes by entity kind, ids, path context, or free text. Use this before reading many files.",
          inputSchema: projectSchema2(entityQuery2)
        },
        {
          name: "domain_query_settings",
          description: "Query MovScript setting entities: concrete film/music production entities to be made or reused, such as characters, props, places, instruments, costumes, or voice identities.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            settingId: { type: ["string", "number"] },
            setting_id: { type: ["string", "number"] },
            kind: { type: "string" },
            query: { type: "string" },
            q: { type: "string" },
            limit: { type: "number" }
          })
        },
        {
          name: "domain_query_assets",
          description: "Query MovScript setting-state-owned asset slots such as front view, side view, turnaround sheet, material reference, voice timbre, or instrument tone. Content-unit candidates are queried through domain candidate/decision APIs.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            assetId: { type: ["string", "number"] },
            asset_id: { type: ["string", "number"] },
            settingId: { type: ["string", "number"] },
            setting_id: { type: ["string", "number"] },
            settingStateId: { type: ["string", "number"] },
            setting_state_id: { type: ["string", "number"] },
            query: { type: "string" },
            q: { type: "string" },
            includeCandidates: { type: "boolean" },
            include_candidates: { type: "boolean" },
            limit: { type: "number" }
          })
        },
        {
          name: "domain_query_production_context",
          description: "Query production planning context: productions, segments, scene moments, storyboards, audio cues, expression units, content units, and candidate-bearing production slots.",
          inputSchema: projectSchema2({
            ...entityQuery2,
            include: {
              type: "array",
              items: { type: "string", enum: ["productions", "segments", "scene_moments", "storyboards", "audio_cues", "expression_units", "content_units", "keyframes"] }
            }
          })
        },
        {
          name: "domain_read_content_workspace",
          description: "Return the same content-source workspace view model used by the content workbench UI. Use this to inspect the page data source, selections, candidates, production hierarchy, preview timelines, and production work plan through the project engine.",
          inputSchema: projectSchema2(workspaceLocator2)
        },
        {
          name: "domain_read_content_workspace_snapshot",
          description: "Return the raw content-source workspace snapshot before UI normalization. Use this when debugging whether source, decision, interpreted, or preview-timeline inputs match expectations.",
          inputSchema: projectSchema2(workspaceLocator2)
        },
        {
          name: "domain_read_project_context_snapshot",
          description: "Read and compile project-wide standards plus namespace vocabulary into the stable project context snapshot agents should consult before planning, content work, or generation. This is read-only; use domain_upsert_project_standards only when the user explicitly asks to add, remove, or change standards.",
          inputSchema: projectSchema2(workspaceLocator2)
        },
        {
          name: "domain_derive_content_unit_artifact",
          description: "Derive the interpreter artifact bundle for a content unit, including runtime panel, generation prompt, dependency report, and selection validity. Use before generation or candidate selection when content-unit context may be stale.",
          inputSchema: projectSchema2({ ...workspaceLocator2, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_build_content_unit_backend_prompt",
          description: "Build a backend-ready prompt for a content unit by resolving prompt refs through backend decision selections. Rewrites selected upstream resources like {{asset:id}} to @[resource:id] and returns blockers when referenced content has not been produced or selected.",
          inputSchema: projectSchema2({ ...workspaceLocator2, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_read_preview_timeline",
          description: "Read the derived production preview timeline through the domain API. This is read-only diagnostic/artifact context, not product source.",
          inputSchema: projectSchema2({ ...workspaceLocator2, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_read_production_timeline",
          description: "Convert the production preview timeline plus selected scene_moment video candidates into a MediaEditingProject handoff. Product editing should continue through editing_* tools. Returns media_editing_project, compose_inputs, clips, and blockers for missing scene_moment output selections.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] },
            projectName: { type: "string" },
            project_name: { type: "string" },
            defaultDurationSec: { type: "number" },
            default_duration_sec: { type: "number" }
          })
        },
        {
          name: "domain_read_scene_moment_edit_plan",
          description: "Read the derived scene_moment edit plan through the domain API. The plan groups selected content-unit candidates into video, voice, subtitle, audio, image, and metadata tracks for agent-driven composition.",
          inputSchema: projectSchema2({ ...workspaceLocator2, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_read_production_edit_plan",
          description: "Read a production-level edit plan handoff from selected scene_moment outputs. This returns a MovScript edit_plan-shaped artifact for editing_project_create_from_edit_plan; it does not render or write candidates.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] },
            projectName: { type: "string" },
            project_name: { type: "string" },
            defaultDurationSec: { type: "number" },
            default_duration_sec: { type: "number" }
          })
        },
        {
          name: "domain_create_editing_project_context",
          description: "Return domain-to-editing handoff context for a scene_moment or production: selected content units, candidates, resources, provenance, blockers, and an edit_plan when available. Creation of the MediaEditingProject remains the responsibility of editing_project_create_from_edit_plan.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            sceneMomentId: { type: ["string", "number"] },
            scene_moment_id: { type: ["string", "number"] },
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] },
            projectName: { type: "string" },
            project_name: { type: "string" },
            defaultDurationSec: { type: "number" },
            default_duration_sec: { type: "number" }
          })
        },
        {
          name: "domain_read_scene_moment_timeline",
          description: "Convert a scene_moment edit plan into a MediaEditingProject handoff. Product editing should use editing_project_create_from_edit_plan for new workflows. Returns media_editing_project plus compose_inputs.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            sceneMomentId: { type: ["string", "number"] },
            scene_moment_id: { type: ["string", "number"] },
            projectName: { type: "string" },
            project_name: { type: "string" },
            sceneName: { type: "string" },
            scene_name: { type: "string" },
            defaultDurationSec: { type: "number" },
            default_duration_sec: { type: "number" }
          })
        },
        {
          name: "domain_read_content_unit_runtime_panel",
          description: "Read the derived content unit runtime panel through the domain API. This is read-only diagnostic/artifact context, not product source.",
          inputSchema: projectSchema2({ ...workspaceLocator2, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_read_content_unit_generation_prompt",
          description: "Read the derived normalized content unit generation prompt through the domain API. This is read-only diagnostic/artifact context, not product source.",
          inputSchema: projectSchema2({ ...workspaceLocator2, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_read_content_unit_input_version",
          description: "Compatibility alias for domain_read_content_unit_generation_prompt. Reads the derived normalized content unit generation prompt through the domain API.",
          inputSchema: projectSchema2({ ...workspaceLocator2, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_read_content_unit_dependency_report",
          description: "Read the derived content unit dependency report through the domain API. This is read-only diagnostic/artifact context, not product source.",
          inputSchema: projectSchema2({ ...workspaceLocator2, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_read_content_unit_selection_validity",
          description: "Read the derived content unit selection validity report through the domain API. This is read-only diagnostic/artifact context, not product source.",
          inputSchema: projectSchema2({ ...workspaceLocator2, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
        },
        {
          name: "domain_upsert_project_standards",
          description: "Create or update project-wide creative standards in source project_standards.json. Run domain_inspect, then domain_interpret when derived artifact tools need refreshed context.",
          inputSchema: projectSchema2({ ...workspaceLocator2, projectStyle: { type: "object", additionalProperties: true }, project_style: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_upsert_setting",
          description: "Create or update a MovScript setting source entity. A setting must be a concrete film/music production entity to make or reuse, not an abstract style/rule. Put the setting data to write in required payload; record/entity are optional existing-context objects only. Prefer this API over direct file edits for setting records.",
          inputSchema: projectSchema2({ ...workspaceLocator2, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } }, ["payload"])
        },
        {
          name: "domain_upsert_setting_state",
          description: "Create or update a MovScript setting_state source entity under a concrete setting. Setting states are named conditions or versions of the same setting, such as base look, wet costume, damaged prop, side view, calm voice, or angry voice. Put the state data to write in required payload.",
          inputSchema: projectSchema2({ ...workspaceLocator2, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } }, ["payload"])
        },
        {
          name: "domain_upsert_asset",
          description: "Create or update a MovScript asset slot source entity under a setting state. Assets describe one state asset such as front view, side view, turnaround sheet, material reference, voice timbre, or instrument tone; image assets should prefer plain white or very clean backgrounds. Put the asset data to write in required payload; record/entity are optional existing-context objects only. Store RawResource references by resource_id, not binaries or external URLs.",
          inputSchema: projectSchema2({ ...workspaceLocator2, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } }, ["payload"])
        },
        {
          name: "domain_certify_asset_provider",
          description: "Register the selected image RawResource with a provider asset library via the MovScript backend. Pass a concrete provider_id when known; otherwise the backend uses the enabled volcengine_ark_official provider. Official Volcengine Ark and Yunwu relay providers with private portrait support are supported. The RawResource stores provider_asset_certifications keyed by provider_id as the certification source of truth, and returned asset:// values are scoped to that provider/account boundary; asset provider_certifications may mirror the selected asset_ref use relationship.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            assetId: { type: ["string", "number"], description: "MovScript asset id or asset source path." },
            asset_id: { type: ["string", "number"], description: "Alias for assetId." },
            assetRef: { type: ["string", "number"], description: "Alias for assetId." },
            asset_ref: { type: ["string", "number"], description: "Alias for assetId." },
            provider: { type: "string", description: "Provider id or provider kind selector. Prefer a concrete provider_id such as volc-ark-main. Defaults to volcengine_ark_official." },
            resourceId: { type: "number", description: "Optional RawResource override. Defaults to the selected asset_ref resource." },
            resource_id: { type: "number", description: "Alias for resourceId." },
            model: { type: "string", description: "Target model id. Provider asset certification is model-scoped; certify separately for fast/standard/pro variants." },
            model_id: { type: "string", description: "Alias for model." },
            asset_group_id: { type: "string", description: "Explicit remote provider asset group id selected by the user." },
            assetGroupId: { type: "string", description: "Alias for asset_group_id." },
            asset_group_name: { type: "string", description: "Optional display name for the explicit remote provider asset group." },
            assetGroupName: { type: "string", description: "Alias for asset_group_name." },
            projectName: { type: "string", description: "Optional provider project label for the managed asset group. Defaults to projectId when available." },
            project_name: { type: "string", description: "Alias for projectName." },
            settingId: { type: "string", description: "Optional setting scope for the managed asset group. Defaults to the asset path setting segment when available." },
            setting_id: { type: "string", description: "Alias for settingId." },
            source_url: { type: "string", description: "Optional public image URL override. If omitted, the backend creates a short-lived signed resource URL using MOVSCRIPT_PROVIDER_ASSET_PUBLIC_BASE_URL." },
            sourceUrl: { type: "string", description: "Alias for source_url." },
            name: { type: "string", description: "Optional material asset name. Defaults to asset title/slot/id." },
            allow_private_urls: { type: "boolean", description: "Allow localhost/private source URLs for local official-API testing. Defaults to false." },
            allowPrivateUrls: { type: "boolean", description: "Alias for allow_private_urls." },
            timeout_ms: { type: "number", minimum: 1e3 },
            timeoutMs: { type: "number", minimum: 1e3 }
          })
        },
        {
          name: "domain_query_remote_asset_groups",
          description: "List remote provider asset-library groups mirrored by MovScript for a provider account. Use this before registering assets when the user must choose the target remote group.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            provider: { type: "string", description: "Provider id or provider kind selector. Prefer a concrete provider_id such as yunwu-main or volc-ark-main." },
            provider_id: { type: "string", description: "Alias for provider." },
            model: { type: "string", description: "Optional target model id for model-scoped groups." },
            model_id: { type: "string", description: "Alias for model." }
          })
        },
        {
          name: "domain_query_remote_assets",
          description: "List remote provider assets inside a selected provider asset group, including model-specific certification records.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            provider: { type: "string", description: "Provider id or provider kind selector. Prefer a concrete provider_id such as yunwu-main or volc-ark-main." },
            provider_id: { type: "string", description: "Alias for provider." },
            groupId: { type: ["string", "number"], description: "Remote provider asset group id or MovScript mirrored group id." },
            group_id: { type: ["string", "number"], description: "Alias for groupId." },
            asset_group_id: { type: ["string", "number"], description: "Alias for groupId." }
          })
        },
        {
          name: "domain_upsert_setting_tree",
          description: "Create or update one concrete setting plus multiple setting_state records and each state's asset slots in one structured write. Use this for setting -> many states -> many assets authoring. Asset generated candidates still belong to asset_ref content units; this tool only writes the source setting/state/asset structure.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            setting: { type: "object", additionalProperties: true },
            payload: { type: "object", additionalProperties: true },
            states: {
              type: "array",
              items: {
                type: "object",
                additionalProperties: true,
                properties: {
                  state: { type: "object", additionalProperties: true },
                  payload: { type: "object", additionalProperties: true },
                  assets: {
                    type: "array",
                    items: { type: "object", additionalProperties: true }
                  }
                }
              }
            }
          }, ["states"])
        },
        {
          name: "domain_upsert_script",
          description: "Create or update a script source record and script.md text. Prefer this API over hand-editing script metadata plus markdown.",
          inputSchema: projectSchema2({ ...workspaceLocator2, scriptId: { type: ["string", "number"] }, script_id: { type: ["string", "number"] }, sourceText: { type: "string" }, source_text: { type: "string" }, metadata: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_read_script_source",
          description: "Read script.md source text for a script domain entity.",
          inputSchema: projectSchema2({ ...workspaceLocator2, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_snapshot_script_version",
          description: "Create a script version and script blocks from a script Markdown source so downstream production entities can reference stable script blocks.",
          inputSchema: projectSchema2({ ...workspaceLocator2, scriptId: { type: ["string", "number"] }, script_id: { type: ["string", "number"] }, versionId: { type: ["string", "number"] }, version_id: { type: ["string", "number"] }, versionLabel: { type: "string" }, version_label: { type: "string" }, sourcePath: { type: "string" }, source_path: { type: "string" } })
        },
        {
          name: "domain_upsert_content_unit",
          description: "Create or update a project-level content unit source record. Content units are independent production tasks and never target namespace nodes directly. For namespace-scope video output, use content_unit_type=timeline_assembly_ref with target_kind=timeline_assembly plus target_ref=timeline_assembly:<scopeKind>:<scopeRef>, or pass scope_kind/scope_ref so the writer derives that target_ref. Legacy production_ref/segment_ref remain compatibility aliases for timeline assemblies; do not invent episode_ref, beat_ref, or other namespace ref types.",
          inputSchema: projectSchema2({ ...workspaceLocator2, unit: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_upsert_timeline_namespace_tree",
          description: "Merge-write a path-first timeline namespace tree under timeline/. Namespace vocabulary supplies labels/templates only; instance parent-child structure comes from targetPath and nested tree placement. Nodes are namespace labels over legacy production/segment projections and must not carry content-unit refs, candidates, selections, resources, production_ref, or segment_ref. Put scene_moments and their expression_units/storyboards/keyframes/audio_cues under namespace nodes when the tree needs production primitives. For namespace-scope video output, put explicit content_units on a namespace node; they are written as timeline_assembly_ref content units with target_kind=timeline_assembly and target_ref=timeline_assembly:<namespace_kind>:<id>.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            namespace: { type: "object", additionalProperties: true },
            root: { type: "object", additionalProperties: true },
            tree: { type: "object", additionalProperties: true },
            nodes: { type: "array", items: { type: "object", additionalProperties: true } },
            namespaces: { type: "array", items: { type: "object", additionalProperties: true } },
            timeline_namespaces: { type: "array", items: { type: "object", additionalProperties: true } },
            timelineNamespaces: { type: "array", items: { type: "object", additionalProperties: true } },
            scene_moments: { type: "array", items: { type: "object", additionalProperties: true } },
            sceneMoments: { type: "array", items: { type: "object", additionalProperties: true } }
          })
        },
        {
          name: "domain_upsert_production",
          description: "Create or update a production source record under productions/. Use this before adding segment, scene_moment, expression_unit, keyframe, or storyboard planning structure.",
          inputSchema: projectSchema2({ ...workspaceLocator2, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, production: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_upsert_production_tree",
          description: "Merge-write one legacy production-projection tree plus nested segments, scene_moments, expression_units, storyboards, keyframes, audio_cues, and content_units in one structured operation. This is an upsert/patch tree: records are matched by id and updated; omitted existing children are not deleted. Use explicit delete flows for removals. Candidate writes still belong to content-unit candidate tools. For new namespace-scope video output, prefer explicit timeline_assembly_ref content units through domain_upsert_content_unit instead of relying on legacy production_ref/segment_ref tree defaults.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] },
            production: { type: "object", additionalProperties: true },
            payload: { type: "object", additionalProperties: true },
            record: { type: "object", additionalProperties: true },
            content_units: { type: "array", items: { type: "object", additionalProperties: true } },
            contentUnits: { type: "array", items: { type: "object", additionalProperties: true } },
            segments: {
              type: "array",
              items: {
                type: "object",
                additionalProperties: true,
                properties: {
                  segment: { type: "object", additionalProperties: true },
                  payload: { type: "object", additionalProperties: true },
                  scene_moments: { type: "array", items: { type: "object", additionalProperties: true } },
                  sceneMoments: { type: "array", items: { type: "object", additionalProperties: true } },
                  content_units: { type: "array", items: { type: "object", additionalProperties: true } },
                  contentUnits: { type: "array", items: { type: "object", additionalProperties: true } }
                }
              }
            }
          }, ["segments"])
        },
        {
          name: "domain_upsert_segment",
          description: "Create or update a segment source record inside a production. Segments are rhythm or dramatic-function sections and own scene_moment children.",
          inputSchema: projectSchema2({ ...workspaceLocator2, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, segment: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_upsert_scene_moment",
          description: "Create or update a scene_moment system primitive. Prefer targetPath or namespacePath for new timeline namespace projects so the scene moment is written under timeline/** by path; productionId/segmentId are legacy compatibility inputs. Scene moments are the final expression aggregation unit for an event/beat; they own expression units and receive final composed content-unit candidates.",
          inputSchema: projectSchema2({ ...workspaceLocator2, targetPath: { type: "string" }, target_path: { type: "string" }, namespacePath: { type: "string" }, namespace_path: { type: "string" }, timelineNamespacePath: { type: "string" }, timeline_namespace_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_upsert_keyframe",
          description: "Create or update a keyframe source entity under an expression_unit(kind=shot) or directly under a scene_moment. Prefer targetPath, expressionUnitPath, or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Keyframes are visual anchors referenced by keyframe_ref content units.",
          inputSchema: projectSchema2({ ...workspaceLocator2, targetPath: { type: "string" }, target_path: { type: "string" }, expressionUnitPath: { type: "string" }, expression_unit_path: { type: "string" }, sceneMomentPath: { type: "string" }, scene_moment_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, expressionUnitId: { type: ["string", "number"] }, expression_unit_id: { type: ["string", "number"] }, keyframeId: { type: ["string", "number"] }, keyframe_id: { type: ["string", "number"] }, keyframe: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, expressionUnit: { type: "object", additionalProperties: true }, expression_unit: { type: "object", additionalProperties: true }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_upsert_storyboard",
          description: "Create or update a storyboard source record under an expression_unit(kind=shot) or directly under a scene_moment. Prefer targetPath, expressionUnitPath, or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Use expression units for shot semantics; this tool does not create standalone shot nodes.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            targetPath: { type: "string" },
            target_path: { type: "string" },
            expressionUnitPath: { type: "string" },
            expression_unit_path: { type: "string" },
            sceneMomentPath: { type: "string" },
            scene_moment_path: { type: "string" },
            parentPath: { type: "string" },
            parent_path: { type: "string" },
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] },
            segmentId: { type: ["string", "number"] },
            segment_id: { type: ["string", "number"] },
            sceneMomentId: { type: ["string", "number"] },
            scene_moment_id: { type: ["string", "number"] },
            expressionUnitId: { type: ["string", "number"] },
            expression_unit_id: { type: ["string", "number"] },
            sceneMomentTitle: { type: "string" },
            scene_moment_title: { type: "string" },
            segmentTitle: { type: "string" },
            segment_title: { type: "string" },
            storyboardId: { type: ["string", "number"] },
            storyboard_id: { type: ["string", "number"] },
            storyboard: { type: "object", additionalProperties: true },
            payload: { type: "object", additionalProperties: true },
            expressionUnit: { type: "object", additionalProperties: true },
            expression_unit: { type: "object", additionalProperties: true },
            production: { type: "object", additionalProperties: true }
          })
        },
        {
          name: "domain_upsert_audio_cue",
          description: "Legacy/auxiliary: create or update an audio_cue source entity under a scene_moment. Prefer targetPath or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Prefer expression_unit with modality=audio or verbal plus voice_profile_ref for new dialogue, sound, music, ambience, or foley planning.",
          inputSchema: projectSchema2({ ...workspaceLocator2, targetPath: { type: "string" }, target_path: { type: "string" }, sceneMomentPath: { type: "string" }, scene_moment_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, audioCueId: { type: ["string", "number"] }, audio_cue_id: { type: ["string", "number"] }, audioCue: { type: "object", additionalProperties: true }, audio_cue: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_upsert_expression_unit",
          description: "Create or update an expression_unit source entity under a scene_moment. Prefer targetPath or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Use slot_kind=visual|voice|subtitle|audio for new expression slots; expression_kind/kind are legacy compatibility hints.",
          inputSchema: projectSchema2({ ...workspaceLocator2, targetPath: { type: "string" }, target_path: { type: "string" }, sceneMomentPath: { type: "string" }, scene_moment_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, expressionUnitId: { type: ["string", "number"] }, expression_unit_id: { type: ["string", "number"] }, expressionUnit: { type: "object", additionalProperties: true }, expression_unit: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_update_content_unit_prompt",
          description: "Update a content unit edit_prompt source field. Run domain_inspect, domain_interpret, and regeneration planning when prompt changes may stale candidates.",
          inputSchema: projectSchema2({ ...workspaceLocator2, targetPath: { type: "string" }, target_path: { type: "string" }, editPrompt: { type: "object", additionalProperties: true }, edit_prompt: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_update_entity_transition",
          description: "Update an entity transition boundary on the source entity that owns transition semantics.",
          inputSchema: projectSchema2({ ...workspaceLocator2, targetPath: { type: "string" }, target_path: { type: "string" }, transition: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_update_storyboard_timeline",
          description: "Update a storyboard timeline source field. Storyboard order and timing belong on storyboard timeline source entities, not on generated artifact output.",
          inputSchema: projectSchema2({ ...workspaceLocator2, targetPath: { type: "string" }, target_path: { type: "string" }, timeline: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_append_candidate",
          description: "Append an inline candidate to an asset or keyframe source entity. Content-unit candidates are backend decision records; use domain_create_content_candidate instead. Generated resources become stable domain dependencies only after candidate/selection writes and explicit adoption/selection.",
          inputSchema: candidateSchema2()
        },
        {
          name: "domain_create_content_candidate",
          description: "Create an external content candidate record for a content unit output through the backend decision API. This does not edit workspace source files. Omit status for completed generated resources; the backend defaults it to succeeded. If status is provided, use only queued, running, succeeded, failed, canceled, or imported.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            ...contentCandidateWriteProperties2()
          })
        },
        {
          name: "domain_register_raw_resource_as_content_unit_candidate",
          description: "Register an existing MovScript RawResource as a content-unit candidate through the backend decision API. Use this when a resource already exists from upload, transform, import, or low-level generation and should enter the candidate pool without selecting it.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            contentUnitId: { type: ["string", "number"] },
            content_unit_id: { type: ["string", "number"] },
            resourceId: { type: "number", description: "Positive integer RawResource ID to register as a candidate output." },
            resource_id: { type: "number", description: "Alias for resourceId." },
            outputKind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
            output_kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
            kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
            candidateId: { type: ["string", "number"] },
            candidate_id: { type: ["string", "number"] },
            source: { type: "string" },
            status: { type: "string", enum: [...contentCandidateStatuses2] },
            mimeType: { type: "string" },
            mime_type: { type: "string" },
            width: { type: "number" },
            height: { type: "number" },
            durationSec: { type: "number" },
            duration_sec: { type: "number" },
            metadata: { type: "object", additionalProperties: true },
            producer: { type: "object", additionalProperties: true },
            promptSnapshot: { type: "object", additionalProperties: true },
            prompt_snapshot: { type: "object", additionalProperties: true }
          }, ["contentUnitId", "resourceId"])
        },
        {
          name: "domain_create_content_candidate_batch",
          description: "Create multiple external content candidate records for content unit outputs through the backend decision API. Each item accepts the same candidate fields as domain_create_content_candidate. Omit item status for completed generated resources; the backend defaults it to succeeded. Runs sequentially and returns per-item results so agents can keep partial successes.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            items: { type: "array", items: contentCandidateWriteItemSchema2() },
            continueOnError: { type: "boolean" },
            continue_on_error: { type: "boolean" }
          }, ["items"])
        },
        {
          name: "domain_create_asset_slot_candidate",
          description: "Create an asset-slot candidate using the MovScript workspace candidate service. If targetRecord carries a workspace path, this appends an inline candidate to that asset source entity.",
          inputSchema: projectSchema2(candidateWriteSchema2())
        },
        {
          name: "domain_create_keyframe_candidate",
          description: "Create a keyframe candidate using the MovScript workspace candidate service. If keyframes are represented as content units in the active model, use the content-unit candidate flow instead.",
          inputSchema: projectSchema2(candidateWriteSchema2())
        },
        {
          name: "domain_select_content_unit_candidate",
          description: "Select a content candidate for a content unit through the backend decision API. Selection is backend decision metadata, not a workspace source-file edit; run domain_inspect and domain_interpret when derived artifact tools need refreshed context.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            contentUnitId: { type: ["string", "number"] },
            content_unit_id: { type: ["string", "number"] },
            candidateId: { type: ["string", "number"] },
            candidate_id: { type: ["string", "number"] },
            resourceId: { type: "number", description: "Positive integer RawResource ID." },
            resource_id: { type: "number", description: "Positive integer RawResource ID." },
            stalePolicy: { type: "string" },
            stale_policy: { type: "string" },
            reason: { type: "string" }
          })
        },
        {
          name: "domain_select_content_unit_candidate_batch",
          description: "Select content candidates for multiple content units through the backend decision API. Each item accepts the same fields as domain_select_content_unit_candidate. Runs sequentially and returns per-item results.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            items: { type: "array", items: { type: "object", additionalProperties: true } },
            continueOnError: { type: "boolean" },
            continue_on_error: { type: "boolean" }
          }, ["items"])
        },
        {
          name: "domain_decide_content_unit_candidate",
          description: "Apply a user decision to a content-unit candidate through the backend decision API. Use decision=adopt to write selection and unblock stable downstream dependencies; use reject or defer to mark the candidate decision without selecting it, so downstream remains blocked until a candidate is adopted.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            contentUnitId: { type: ["string", "number"] },
            content_unit_id: { type: ["string", "number"] },
            candidateId: { type: ["string", "number"] },
            candidate_id: { type: ["string", "number"] },
            decision: { type: "string", enum: ["adopt", "reject", "defer"] },
            resourceId: { type: "number", description: "Positive integer RawResource ID." },
            resource_id: { type: "number", description: "Positive integer RawResource ID." },
            stalePolicy: { type: "string" },
            stale_policy: { type: "string" },
            reason: { type: "string" },
            decidedAt: { type: "string" },
            decided_at: { type: "string" },
            metadata: { type: "object", additionalProperties: true }
          })
        },
        {
          name: "domain_select_candidate",
          description: "Select and lock an inline candidate on an asset or keyframe source entity. Content-unit selections are backend decision records; use domain_select_content_unit_candidate instead.",
          inputSchema: candidateSchema2({ candidateId: { type: "string" }, candidate_id: { type: "string" }, reason: { type: "string" } })
        },
        {
          name: "domain_update_candidate",
          description: "Update an inline candidate on an asset or keyframe source entity. Content-unit candidates are backend decision records; use domain_create_content_candidate or domain_decide_content_unit_candidate instead.",
          inputSchema: candidateSchema2({ candidateId: { type: "string" }, candidate_id: { type: "string" } })
        },
        {
          name: "domain_unlock_candidate",
          description: "Remove an inline candidate lock from an asset or keyframe source entity. Content-unit selections are backend decision records; use domain_select_content_unit_candidate/domain_decide_content_unit_candidate instead.",
          inputSchema: candidateSchema2()
        },
        {
          name: "domain_delete_entity",
          description: "Delete a MovScript domain source entity file through the workspace service. Do not touch interpreter debug output for product work.",
          inputSchema: projectSchema2({ ...workspaceLocator2, entity: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
        },
        {
          name: "domain_overview",
          description: "Show MovScript source state, backend decisions, diagnostics, stale generated outputs, and recommended next actions.",
          inputSchema: projectSchema2(workspaceLocator2)
        },
        {
          name: "domain_read_production_work_plan",
          description: "Derive the current in-memory production work plan from source and decision state. This does not read or write interpreter debug artifacts and should be used by UI, CLI, and agents as the shared production todo graph.",
          inputSchema: projectSchema2(workspaceLocator2)
        },
        {
          name: "domain_production_status_summary",
          description: "Summarize current production progress for agents: prerequisites/settings/assets, storyboards, keyframes, content-unit candidate counts, selected resources, stale hints, and blocking refs. Use this before broad generation or review instead of manually stitching many tool results.",
          inputSchema: projectSchema2({
            ...workspaceLocator2,
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] }
          })
        },
        {
          name: "domain_inspect",
          description: "Inspect current source changes, diagnostics, and predicted impact without writing debug artifacts. This is the primary diagnostic entrypoint after API writes or direct source edits.",
          inputSchema: projectSchema2(inspectOptions2)
        },
        {
          name: "domain_review",
          description: "Compatibility alias for domain_inspect with review-shaped output. Prefer domain_inspect for current source diagnostics. This is diagnostic only and writes no interpreted artifacts.",
          inputSchema: projectSchema2(inspectOptions2)
        },
        {
          name: "domain_interpret",
          description: "Validate current source and refresh derived diagnostic artifacts when enabled. Does not publish, approve, commit, checkpoint user intent, or create product state.",
          inputSchema: projectSchema2(workspaceLocator2)
        },
        {
          name: "domain_regeneration_plan",
          description: "Plan downstream review targets after domain_interpret refreshes diagnostic context. Reports affected or stale content units, prompt bundles, preview timelines, and selections; it does not require regeneration by itself.",
          inputSchema: projectSchema2(workspaceLocator2)
        }
      ];
    }
    function candidateSchema2(extra = {}) {
      return projectSchema2({
        ...workspaceLocator2,
        targetPath: { type: "string" },
        target_path: { type: "string" },
        targetKind: { type: "string", enum: ["asset", "keyframe"] },
        target_kind: { type: "string", enum: ["asset", "keyframe"] },
        payload: { type: "object", additionalProperties: true },
        lock: { type: ["boolean", "object"], additionalProperties: true },
        nonce: { type: "string" },
        ...extra
      });
    }
    function candidateWriteSchema2() {
      return {
        ...workspaceLocator2,
        payload: { type: "object", additionalProperties: true },
        targetRecord: { type: "object", additionalProperties: true },
        target_record: { type: "object", additionalProperties: true },
        nonce: { type: "string" }
      };
    }
    function contentCandidateWriteProperties2() {
      return {
        contentUnitId: { type: ["string", "number"] },
        content_unit_id: { type: ["string", "number"] },
        candidateId: { type: ["string", "number"] },
        candidate_id: { type: ["string", "number"] },
        source: { type: "string" },
        status: {
          type: "string",
          enum: [...contentCandidateStatuses2],
          description: "Optional. Omit for completed generated resources; the backend defaults it to succeeded. Do not use completed, ready, done, selected, or accepted."
        },
        producer: { type: "object", additionalProperties: true },
        outputs: {
          type: "array",
          items: {
            type: "object",
            properties: {
              kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
              resource_id: { type: "number", description: "Positive integer RawResource ID." },
              artifact_ref: { type: "string", description: "Optional logical artifact reference; never use as a RawResource ID." },
              mime_type: { type: "string" },
              width: { type: "number" },
              height: { type: "number" },
              duration_sec: { type: "number" },
              metadata: { type: "object", additionalProperties: true }
            },
            required: ["kind", "resource_id"],
            additionalProperties: true
          }
        },
        promptSnapshot: { type: "object", additionalProperties: true },
        prompt_snapshot: { type: "object", additionalProperties: true }
      };
    }
    function contentCandidateWriteItemSchema2() {
      return {
        type: "object",
        properties: contentCandidateWriteProperties2(),
        additionalProperties: false
      };
    }
    function projectSchema2(properties, required) {
      return objectSchema32(properties, required);
    }
    var projectLocator2 = {
      projectId: { type: ["string", "number"], description: "MovScript project id." },
      project_id: { type: ["string", "number"], description: "Alias for projectId." }
    };
    var editingProjectInput2 = {
      editingProject: { type: "object", additionalProperties: true, description: "MediaEditingProject object." },
      editing_project: { type: "object", additionalProperties: true, description: "Alias for editingProject." },
      project: { type: "object", additionalProperties: true, description: "Alias for editingProject." }
    };
    function editingTools2() {
      return [
        {
          name: "editing_project_create",
          description: "Create and persist an empty MovScript media editing project through Editing Service. This does not render media or write content candidates.",
          inputSchema: objectSchema32({
            ...projectLocator2,
            title: { type: "string" },
            width: { type: "number" },
            height: { type: "number" },
            fps: { type: "number" },
            background: { type: "string" }
          })
        },
        {
          name: "editing_project_create_from_edit_plan",
          description: "Create and persist a MediaEditingProject from a MovScript edit_plan through Editing Service. This is the main handoff from domain planning into the dedicated editing tool family.",
          inputSchema: objectSchema32({
            ...projectLocator2,
            editPlan: { type: "object", additionalProperties: true, description: "MovScript edit_plan artifact." },
            edit_plan: { type: "object", additionalProperties: true, description: "Alias for editPlan." },
            title: { type: "string" },
            width: { type: "number" },
            height: { type: "number" },
            fps: { type: "number" },
            background: { type: "string" },
            defaultDurationMs: { type: "number" },
            default_duration_ms: { type: "number" }
          })
        },
        {
          name: "editing_project_create_from_edit_decisions",
          description: "Create and persist a MediaEditingProject from OpenMontage-style edit_decisions plus an optional asset_manifest. This only creates project data; rendering remains a separate editing task or editing_video_compose call.",
          inputSchema: objectSchema32({
            ...projectLocator2,
            editDecisions: { type: "object", additionalProperties: true, description: "Edit decision artifact with cuts, overlays, audio, subtitles, and render runtime metadata." },
            edit_decisions: { type: "object", additionalProperties: true, description: "Alias for editDecisions." },
            assetManifest: { type: "object", additionalProperties: true, description: "Asset manifest used to resolve edit_decisions source refs into MediaAssetDescriptor entries." },
            asset_manifest: { type: "object", additionalProperties: true, description: "Alias for assetManifest." },
            timelineAssembly: { type: "object", additionalProperties: true, description: "TimelineAssembly intent state used to build a repeatable CompileManifest before creating the MediaEditingProject." },
            timeline_assembly: { type: "object", additionalProperties: true, description: "Alias for timelineAssembly." },
            compileManifest: { type: "object", additionalProperties: true, description: "Optional CompileManifest from the edit desk handoff; the service revalidates the current inputs." },
            compile_manifest: { type: "object", additionalProperties: true, description: "Alias for compileManifest." },
            title: { type: "string" },
            width: { type: "number" },
            height: { type: "number" },
            fps: { type: "number" },
            background: { type: "string" },
            defaultDurationMs: { type: "number" },
            default_duration_ms: { type: "number" },
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] },
            productionPath: { type: "string" },
            production_path: { type: "string" },
            targetKind: { type: "string" },
            target_kind: { type: "string" },
            targetRef: { type: "string" },
            target_ref: { type: "string" },
            scopeKind: { type: "string" },
            scope_kind: { type: "string" },
            scopeRef: { type: ["string", "number"] },
            scope_ref: { type: ["string", "number"] }
          })
        },
        {
          name: "editing_project_get",
          description: "Read a persisted MediaEditingProject by id from Editing Service.",
          inputSchema: objectSchema32({ ...projectLocator2, editingProjectId: { type: "string" }, editing_project_id: { type: "string" } })
        },
        {
          name: "editing_project_update_settings",
          description: "Update MediaEditingProject settings such as title, fps, canvas size, background, and workspace binding. This mutates project data only.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            title: { type: "string" },
            width: { type: "number" },
            height: { type: "number" },
            fps: { type: "number" },
            background: { type: "string" },
            workspace: { type: "object", additionalProperties: true }
          })
        },
        {
          name: "editing_project_add_asset",
          description: "Add a RawResource, generated resource, local file, or bytes-backed source to a MediaEditingProject asset registry.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            asset: { type: "object", additionalProperties: true }
          })
        },
        {
          name: "editing_project_remove_asset",
          description: "Remove an unused asset reference from a MediaEditingProject asset registry. Fails if any clip still references the asset.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            assetId: { type: "string" },
            asset_id: { type: "string" }
          })
        },
        {
          name: "editing_project_save",
          description: "Persist a MediaEditingProject revision through Editing Service. Pass expectedRevision for optimistic locking against the last revision read.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            expectedRevision: { type: "number", description: "Last persisted revision observed before editing. Save returns conflict if the stored revision changed." },
            expected_revision: { type: "number", description: "Alias for expectedRevision." }
          })
        },
        {
          name: "editing_timeline_apply_commands",
          description: "Apply MovScript media timeline commands to a MediaEditingProject. This modifies the editing project only; it does not render media or create candidates.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            commands: { type: "array", items: { type: "object", additionalProperties: true } },
            command: { type: "object", additionalProperties: true }
          })
        },
        {
          name: "editing_timeline_add_track",
          description: "Add a video, image, audio, text, subtitle, or effect track to a MediaEditingProject timeline.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            track: { type: "object", additionalProperties: true },
            trackId: { type: "string" },
            track_id: { type: "string" },
            type: { type: "string" },
            trackType: { type: "string" },
            track_type: { type: "string" },
            name: { type: "string" },
            zIndex: { type: "number" },
            z_index: { type: "number" }
          })
        },
        {
          name: "editing_timeline_remove_track",
          description: "Remove an empty track from a MediaEditingProject timeline.",
          inputSchema: objectSchema32({ ...editingProjectInput2, trackId: { type: "string" }, track_id: { type: "string" } })
        },
        {
          name: "editing_timeline_add_clip",
          description: "Place an asset-backed or text clip onto a timeline track.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            trackId: { type: "string" },
            track_id: { type: "string" },
            clip: { type: "object", additionalProperties: true },
            assetId: { type: "string" },
            asset_id: { type: "string" },
            assetType: { type: "string" },
            asset_type: { type: "string" },
            timelineStartMs: { type: "number" },
            timeline_start_ms: { type: "number" },
            durationMs: { type: "number" },
            duration_ms: { type: "number" }
          })
        },
        {
          name: "editing_timeline_update_clip",
          description: "Update trim, duration, placement, volume, fit, style, or metadata for an existing timeline clip.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            clipId: { type: "string" },
            clip_id: { type: "string" },
            patch: { type: "object", additionalProperties: true }
          })
        },
        {
          name: "editing_timeline_split_clip",
          description: "Split a timeline clip at an absolute timeline timestamp.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            clipId: { type: "string" },
            clip_id: { type: "string" },
            splitTimeMs: { type: "number" },
            split_time_ms: { type: "number" },
            retainSide: { type: "string" },
            retain_side: { type: "string" }
          })
        },
        {
          name: "editing_timeline_move_clip",
          description: "Move a timeline clip to a new timestamp and optionally to another compatible track.",
          inputSchema: objectSchema32({
            ...editingProjectInput2,
            clipId: { type: "string" },
            clip_id: { type: "string" },
            targetTrackId: { type: "string" },
            target_track_id: { type: "string" },
            timelineStartMs: { type: "number" },
            timeline_start_ms: { type: "number" }
          })
        },
        {
          name: "editing_timeline_delete_clip",
          description: "Delete a clip from a MediaEditingProject timeline.",
          inputSchema: objectSchema32({ ...editingProjectInput2, clipId: { type: "string" }, clip_id: { type: "string" } })
        },
        {
          name: "editing_timeline_validate",
          description: "Validate a MediaEditingProject timeline for missing assets, invalid timing, track mismatch, and unsupported clip settings.",
          inputSchema: objectSchema32({ ...editingProjectInput2 })
        },
        {
          name: "editing_runtime_capabilities_get",
          description: "Read Electron mediaPipeline runtime capabilities such as FFmpeg availability, version, supported task types, project store, and local HLS preview support. This does not start a task.",
          inputSchema: objectSchema32({})
        },
        {
          name: "editing_video_compose",
          description: "MovScript video_compose entrypoint: resolve an editing project or edit_decisions handoff, validate the MediaEditingProject timeline, and create a Media Pipeline render/HLS task. Supports movscript_media_pipeline/ffmpeg only; Remotion, HyperFrames, and external NLE runtimes return explicit blockers instead of silent fallback.",
          inputSchema: objectSchema32({
            ...projectLocator2,
            ...editingProjectInput2,
            editingProjectId: { type: "string" },
            editing_project_id: { type: "string" },
            editDecisions: { type: "object", additionalProperties: true },
            edit_decisions: { type: "object", additionalProperties: true },
            assetManifest: { type: "object", additionalProperties: true },
            asset_manifest: { type: "object", additionalProperties: true },
            timelineAssembly: { type: "object", additionalProperties: true, description: "TimelineAssembly intent state used to build a repeatable CompileManifest before rendering." },
            timeline_assembly: { type: "object", additionalProperties: true, description: "Alias for timelineAssembly." },
            compileManifest: { type: "object", additionalProperties: true, description: "Optional CompileManifest from the edit desk handoff; the runtime still revalidates the current inputs." },
            compile_manifest: { type: "object", additionalProperties: true, description: "Alias for compileManifest." },
            renderRuntime: { type: "string" },
            render_runtime: { type: "string" },
            format: { type: "string", description: "mp4 or hls. Defaults to mp4." },
            output: { type: "object", additionalProperties: true },
            importToResource: { type: "boolean" },
            import_to_resource: { type: "boolean" },
            title: { type: "string" },
            width: { type: "number" },
            height: { type: "number" },
            fps: { type: "number" },
            background: { type: "string" },
            defaultDurationMs: { type: "number" },
            default_duration_ms: { type: "number" },
            productionId: { type: ["string", "number"] },
            production_id: { type: ["string", "number"] },
            productionPath: { type: "string" },
            production_path: { type: "string" },
            targetKind: { type: "string" },
            target_kind: { type: "string" },
            targetRef: { type: "string" },
            target_ref: { type: "string" },
            scopeKind: { type: "string" },
            scope_kind: { type: "string" },
            scopeRef: { type: ["string", "number"] },
            scope_ref: { type: ["string", "number"] }
          })
        },
        {
          name: "editing_task_render_create",
          description: "Create an Electron mediaPipeline timeline_render task for a MediaEditingProject. Rendering runs in Electron, not in the backend.",
          inputSchema: objectSchema32({ ...projectLocator2, ...editingProjectInput2, output: { type: "object", additionalProperties: true } })
        },
        {
          name: "editing_task_hls_create",
          description: "Create an Electron mediaPipeline timeline_hls task. HLS packaging, including optional output.hlsVariants adaptive renditions, runs in Electron; backend only hosts uploaded artifacts.",
          inputSchema: objectSchema32({ ...projectLocator2, ...editingProjectInput2, output: { type: "object", additionalProperties: true } })
        },
        {
          name: "editing_task_transcode_create",
          description: "Create an Electron mediaPipeline media_transcode task. Pass projectId so the task is stored in the correct Electron media workspace.",
          inputSchema: objectSchema32({ ...projectLocator2, source: { type: "object", additionalProperties: true }, output: { type: "object", additionalProperties: true } })
        },
        {
          name: "editing_task_reframe_create",
          description: "Create an Electron mediaPipeline media_reframe task for mechanical crop/contain/pad reframe. Pass projectId so the task is stored in the correct Electron media workspace.",
          inputSchema: objectSchema32({ ...projectLocator2, source: { type: "object", additionalProperties: true }, output: { type: "object", additionalProperties: true }, target: { type: "string" }, mode: { type: "string" } })
        },
        {
          name: "editing_task_get",
          description: "Read an Electron mediaPipeline task state. Pass projectId with taskId for persisted workspace recovery after app restart.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task recovery." },
            project_id: { type: "string", description: "Alias for projectId." }
          })
        },
        {
          name: "editing_task_cancel",
          description: "Cancel an Electron mediaPipeline task. Pass projectId with taskId so Electron can recover persisted task state and write a clear cancel diagnostic after app restart.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task recovery." },
            project_id: { type: "string", description: "Alias for projectId." }
          })
        },
        {
          name: "editing_task_logs_get",
          description: "Read FFmpeg/task logs for an Electron mediaPipeline task. Pass projectId with taskId for persisted workspace log recovery after app restart.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task log recovery." },
            project_id: { type: "string", description: "Alias for projectId." }
          })
        },
        {
          name: "editing_export_import_resource",
          description: "Upload a completed local editing export as a RawResource, optionally recording derivative provenance. This does not adopt or select the result. When resolving a completed Electron task by taskId, pass projectId for persisted workspace output recovery. HLS manifests must use editing_export_publish_hls so manifest and segment files are hosted as a MediaStreamArtifact instead of one RawResource.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task output recovery." },
            project_id: { type: "string", description: "Alias for projectId." },
            outputPath: { type: "string" },
            output_path: { type: "string" },
            filename: { type: "string" },
            mimeType: { type: "string" },
            mime_type: { type: "string" },
            folderId: { type: ["string", "number"] },
            folder_id: { type: ["string", "number"] },
            derivative: { type: "object", additionalProperties: true },
            operation: { type: "string" },
            tool: { type: "string" },
            inputResourceIds: { type: "array", items: { type: ["string", "number"] } },
            input_resource_ids: { type: "array", items: { type: ["string", "number"] } },
            sourceResourceId: { type: ["string", "number"] },
            source_resource_id: { type: ["string", "number"] },
            sourceResourceIds: { type: "array", items: { type: ["string", "number"] } },
            source_resource_ids: { type: "array", items: { type: ["string", "number"] } },
            params: { type: "object", additionalProperties: true }
          })
        },
        {
          name: "editing_export_save_local",
          description: "Save a completed Electron editing export to an explicit local file path, save a complete HLS bundle to a local directory, or return/validate an existing local export path. When resolving by taskId, pass projectId for persisted workspace output recovery. This does not upload or create candidates.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task output recovery." },
            project_id: { type: "string", description: "Alias for projectId." },
            outputPath: { type: "string" },
            output_path: { type: "string" },
            savePath: { type: "string" },
            save_path: { type: "string" },
            saveDirectory: { type: "string" },
            save_directory: { type: "string" },
            hlsDirectory: { type: "string" },
            hls_directory: { type: "string" },
            segmentPaths: { type: "array", items: { type: "string" } },
            segment_paths: { type: "array", items: { type: "string" } },
            filename: { type: "string" }
          })
        },
        {
          name: "editing_export_publish_hls",
          description: "Upload completed HLS manifest/segments as a MediaStreamArtifact. When resolving a completed Electron HLS task by taskId, pass projectId for persisted workspace output recovery.",
          inputSchema: objectSchema32({
            taskId: { type: "string" },
            task_id: { type: "string" },
            manifestPath: { type: "string" },
            manifest_path: { type: "string" },
            segmentPaths: { type: "array", items: { type: "string" } },
            segment_paths: { type: "array", items: { type: "string" } },
            title: { type: "string" },
            projectId: { type: ["string", "number"] },
            project_id: { type: ["string", "number"] },
            sourceResourceId: { type: ["string", "number"] },
            source_resource_id: { type: ["string", "number"] },
            sourceDerivativeId: { type: ["string", "number"] },
            source_derivative_id: { type: ["string", "number"] },
            durationMs: { type: "number" },
            duration_ms: { type: "number" },
            width: { type: "number" },
            height: { type: "number" }
          })
        },
        {
          name: "editing_export_create_candidate",
          description: "Explicitly write an exported RawResource-backed editing result as a content candidate. This is separate from render/export success. HLS MediaStreamArtifact outputs are hosted previews today; writing them as candidates requires a future domain candidate schema extension.",
          inputSchema: objectSchema32({
            ...projectLocator2,
            contentUnitId: { type: ["string", "number"] },
            content_unit_id: { type: ["string", "number"] },
            resourceId: { type: ["string", "number"] },
            resource_id: { type: ["string", "number"] },
            streamId: { type: ["string", "number"], description: "Known unsupported HLS MediaStreamArtifact ID. Current candidate outputs require resourceId/resource_id." },
            stream_id: { type: ["string", "number"], description: "Alias for streamId." },
            candidateId: { type: ["string", "number"] },
            candidate_id: { type: ["string", "number"] },
            taskId: { type: "string" },
            task_id: { type: "string" },
            editingProjectId: { type: "string" },
            editing_project_id: { type: "string" },
            outputPath: { type: "string" },
            output_path: { type: "string" },
            outputKind: { type: "string" },
            output_kind: { type: "string" },
            kind: { type: "string" },
            mimeType: { type: "string" },
            mime_type: { type: "string" },
            durationMs: { type: "number" },
            duration_ms: { type: "number" },
            durationSec: { type: "number" },
            duration_sec: { type: "number" },
            width: { type: "number" },
            height: { type: "number" },
            source: { type: "string" },
            status: { type: "string" },
            producer: { type: "object", additionalProperties: true },
            provenance: { type: "object", additionalProperties: true },
            promptSnapshot: { type: "object", additionalProperties: true },
            prompt_snapshot: { type: "object", additionalProperties: true }
          })
        }
      ];
    }
    function externalResourceTools2() {
      return [
        {
          name: "movscript_external_resource_source_list",
          description: "List configured external media search sources such as Pexels or Pixabay. Use this when choosing a source_id for external media search.",
          inputSchema: objectSchema32(
            {
              include_disabled: { type: "boolean", description: "When true, include disabled sources." },
              includeDisabled: { type: "boolean", description: "Camel-case alias for include_disabled." }
            }
          ),
          outputSchema: objectSchema32(
            {
              source: { type: "string" },
              count: { type: "number" },
              items: { type: "array", items: { type: "object", additionalProperties: true } },
              usage: { type: "string" }
            },
            ["source", "count", "items", "usage"]
          )
        },
        {
          name: "movscript_external_resource_search",
          description: "Search external image/video providers configured in MovScript. Results are external media candidates and must be imported into MovScript before they can be used as generation resource IDs.",
          inputSchema: objectSchema32(
            {
              query: { type: "string", description: "Search query, e.g. neon city street, handheld office scene, product packshot." },
              q: { type: "string", description: "Alias for query." },
              source_id: { type: "number", description: "External resource source ID. Omit to use the first enabled source." },
              sourceId: { type: "number", description: "Camel-case alias for source_id." },
              media_type: { type: "string", enum: ["image", "video"], description: "Optional media type filter." },
              mediaType: { type: "string", enum: ["image", "video"], description: "Camel-case alias for media_type." },
              orientation: { type: "string", enum: ["all", "landscape", "portrait", "square"], description: "Optional orientation filter." },
              page: { type: "number", description: "1-based page number. Defaults to 1." },
              page_size: { type: "number", description: "Page size, clamped to 1-80. Defaults to 20." },
              pageSize: { type: "number", description: "Camel-case alias for page_size." },
              limit: { type: "number", description: "Alias for page_size." }
            }
          ),
          outputSchema: objectSchema32(
            {
              source: { type: "string" },
              source_id: { type: "number" },
              query: { type: "string" },
              media_type: { type: "string" },
              orientation: { type: "string" },
              page: { type: "number" },
              pageSize: { type: "number" },
              total: { type: "number" },
              count: { type: "number" },
              provider: { type: "string" },
              source_name: { type: "string" },
              next_page: { type: "string" },
              items: { type: "array", items: { type: "object", additionalProperties: true } },
              usage: { type: "string" }
            },
            ["source", "source_id", "query", "page", "pageSize", "total", "count", "items", "usage"]
          )
        }
      ];
    }
    function focusTools2() {
      return [
        {
          name: "movscript_focus_get",
          description: "Return the current MovScript task focus: route, selected project, active production id, current user, and selected entity. This does not load project lists, scripts, workspaces, or resources.",
          inputSchema: objectSchema32({})
        }
      ];
    }
    function generationTools2() {
      return [
        generationCapabilityListTool2(),
        generationPrepareTool2(),
        generationSubmitTool2(),
        generationJobGetTool2(),
        generationJobGetBatchTool2(),
        generationResultRegisterTool2()
      ];
    }
    var GENERATION_CAPABILITIES2 = [
      "image_generation",
      "video_generation",
      "audio_generation",
      "image",
      "image_edit",
      "video",
      "video_i2v",
      "video_v2v",
      "audio_tts",
      "audio_transcribe",
      "audio_translate",
      "audio_music",
      "audio_sfx",
      "audio_chat",
      "voice_clone",
      "voice_design",
      "subtitle_align",
      "subtitle_translate"
    ];
    function generationCapabilityListTool2() {
      return {
        name: "generation_capability_list",
        description: "List MovScript generation capabilities accepted by generation_prepare and generation_submit.",
        inputSchema: objectSchema32(
          {
            include_models: { type: "boolean", description: "When true, include enabled model counts for each capability." }
          }
        ),
        outputSchema: objectSchema32(
          {
            capabilities: { type: "array", items: { type: "string", enum: [...GENERATION_CAPABILITIES2] } },
            count: { type: "number" },
            models_by_capability: { type: "object", additionalProperties: true }
          },
          ["capabilities", "count"]
        )
      };
    }
    function generationPrepareTool2() {
      return {
        name: "generation_prepare",
        description: "Prepare a MovScript generation request: validate capability/scope, list usable models, and compile content-unit prompts when scope is content_unit. Capability-family requests must include an explicit operation or generation_intent.operation.",
        inputSchema: generationRequestSchema2(["capability"]),
        outputSchema: objectSchema32(
          {
            status: { type: "string" },
            capability: { type: "string" },
            scope: { type: "string" },
            model_contracts: { type: "array", items: { type: "object", additionalProperties: true } },
            models: { type: "array", items: { type: "object", additionalProperties: true } },
            prompt: { type: "object", additionalProperties: true },
            blockers: { type: "array", items: { type: "object", additionalProperties: true } },
            message: { type: "string" }
          },
          ["status", "capability", "scope", "message"]
        )
      };
    }
    function generationSubmitTool2() {
      return {
        name: "generation_submit",
        description: "Submit any MovScript generation job through one unified capability contract. Capability-family submissions must include an explicit operation or generation_intent.operation; the tool never infers the operation from resource count. Use scope=content_unit for candidate-producing image/video generation, otherwise outputs are RawResources until explicitly registered.",
        inputSchema: generationRequestSchema2(["capability"]),
        outputSchema: objectSchema32(
          {
            status: { type: "string" },
            capability: { type: "string" },
            scope: { type: "string" },
            output_kind: { type: "string" },
            generation_mode: { type: "string" },
            candidate_policy: { type: "string" },
            jobId: { type: "number" },
            job_id: { type: "number" },
            terminal: { type: "boolean" },
            monitor: { type: "object", additionalProperties: true },
            surface: { type: "object", additionalProperties: true },
            message: { type: "string" },
            job: { type: "object", additionalProperties: true }
          },
          ["status", "capability", "scope", "jobId", "terminal", "monitor"]
        )
      };
    }
    function generationJobGetTool2() {
      return {
        name: "generation_job_get",
        description: "Fetch any MovScript generation job by id. For content-unit jobs, pass scope=content_unit, contentUnitId, and output_kind so successful terminal polls can create or refresh content-unit candidates.",
        inputSchema: objectSchema32(
          {
            jobId: { type: "number", minimum: 1 },
            job_id: { type: "number", minimum: 1 },
            capability: { type: "string", enum: [...GENERATION_CAPABILITIES2] },
            scope: { type: "string", enum: ["free", "content_unit", "asset", "storyboard", "keyframe"] },
            projectDir: { type: "string", description: "MovScript project source directory used when creating successful candidates." },
            project_dir: { type: "string", description: "Alias for projectDir." },
            cwd: { type: "string", description: "Alias for projectDir." },
            projectUid: { type: "string", description: "Optional manifest project_uid used for scoped candidate metadata." },
            project_uid: { type: "string", description: "Alias for projectUid." },
            contentUnitId: { type: ["string", "number"], description: "Target MovScript content unit id for automatic candidate creation." },
            content_unit_id: { type: ["string", "number"], description: "Alias for contentUnitId." },
            outputKind: { type: "string", enum: ["image", "video", "audio", "subtitle", "voice_profile", "json"] },
            output_kind: { type: "string", enum: ["image", "video", "audio", "subtitle", "voice_profile", "json"] },
            promptSnapshot: { type: "object", additionalProperties: true },
            prompt_snapshot: { type: "object", additionalProperties: true },
            verbosity: { type: "string", enum: ["summary", "debug"], description: "Use summary for compact polling output; debug includes the full backend job payload." }
          }
        ),
        outputSchema: objectSchema32(
          {
            status: { type: "string" },
            generation_mode: { type: "string" },
            candidate_policy: { type: "string" },
            will_auto_select: { type: "boolean" },
            requires_user_adoption: { type: "boolean" },
            jobId: { type: "number" },
            job_id: { type: "number" },
            contentUnitId: { type: ["string", "number"] },
            content_unit_id: { type: ["string", "number"] },
            terminal: { type: "boolean" },
            outputResourceIds: { type: "array", items: { type: "number" } },
            output_resource_ids: { type: "array", items: { type: "number" } },
            candidate_created: { type: "boolean" },
            candidate_count: { type: "number" },
            candidates: { type: "array", items: { type: "object", additionalProperties: true } },
            selected_candidate: { type: "object", additionalProperties: true },
            selected_raw_resource: { type: "object", additionalProperties: true },
            stale_status: { type: "string" },
            frontend: { type: "object", additionalProperties: true },
            job: { type: "object" }
          },
          ["status", "jobId", "terminal", "contentUnitId"]
        )
      };
    }
    function generationJobGetBatchTool2() {
      return {
        name: "generation_job_get_batch",
        description: "Synchronously fetch the latest state of multiple MovScript generation jobs. Items may include capability, scope, contentUnitId, and output_kind.",
        inputSchema: objectSchema32(
          {
            jobIds: { type: "array", items: { type: "number" }, description: "Generation job IDs." },
            job_ids: { type: "array", items: { type: "number" }, description: "Alias for jobIds." },
            items: { type: "array", items: { type: "object", additionalProperties: true }, description: "Optional item array. Each item may include jobId or job_id." },
            verbosity: { type: "string", enum: ["summary", "debug"], description: "Use summary for compact polling output; debug includes the full backend job payload." }
          }
        ),
        outputSchema: objectSchema32(
          {
            status: { type: "string" },
            total: { type: "number" },
            success_count: { type: "number" },
            failed_count: { type: "number" },
            terminal_count: { type: "number" },
            all_terminal: { type: "boolean" },
            output_resource_ids: { type: "array", items: { type: "number" } },
            items: { type: "array", items: { type: "object", additionalProperties: true } },
            message: { type: "string" }
          },
          ["status", "total", "success_count", "failed_count", "terminal_count", "all_terminal", "items", "message"]
        )
      };
    }
    function generationResultRegisterTool2() {
      return {
        name: "generation_result_register",
        description: "Register an existing generation RawResource as a content-unit candidate. Use this when a low-level generation result should enter domain candidate review.",
        inputSchema: objectSchema32(
          {
            contentUnitId: { type: ["string", "number"], description: "Target MovScript content unit id." },
            content_unit_id: { type: ["string", "number"], description: "Alias for contentUnitId." },
            resourceId: { type: "number", minimum: 1 },
            resource_id: { type: "number", minimum: 1 },
            outputKind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
            output_kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
            candidateId: { type: "string" },
            candidate_id: { type: "string" },
            title: { type: "string" },
            projectDir: { type: "string" },
            project_dir: { type: "string" },
            cwd: { type: "string" },
            projectUid: { type: "string" },
            project_uid: { type: "string" },
            metadata: { type: "object", additionalProperties: true }
          },
          ["contentUnitId", "resourceId"]
        ),
        outputSchema: objectSchema32(
          {
            status: { type: "string" },
            candidate: { type: "object", additionalProperties: true },
            surface: { type: "object", additionalProperties: true }
          }
        )
      };
    }
    function generationRequestSchema2(required = []) {
      return objectSchema32(
        {
          capability: { type: "string", enum: [...GENERATION_CAPABILITIES2], description: "MovScript generation capability. Prefer generation families such as image_generation, video_generation, or audio_generation plus an explicit operation." },
          scope: { type: "string", enum: ["free", "content_unit", "asset", "storyboard", "keyframe"], description: "Generation target scope. content_unit image/video jobs create candidates on successful terminal polling." },
          prompt: { type: "string", minLength: 1 },
          title: { type: "string" },
          model_id: { type: "string" },
          provider_id: { type: "string" },
          operation: {
            type: "string",
            enum: [
              "text_to_image",
              "reference_to_image",
              "image_to_image",
              "prompt_to_video",
              "reference_to_video",
              "image_to_video",
              "first_frame_to_video",
              "first_last_frame_to_video",
              "video_to_video",
              "video_edit",
              "video_extend",
              "video_inpaint",
              "object_insert",
              "object_remove",
              "motion_control",
              "lip_sync",
              "video_upscale",
              "tts",
              "stt",
              "speech_translate",
              "audio_chat",
              "voice_clone",
              "voice_design",
              "dubbing",
              "music",
              "sfx",
              "speech_enhancement"
            ],
            description: "Explicit model operation intent. Required for image_generation, video_generation, and audio_generation; agents must choose this instead of relying on resource count, route, provider, or adapter details."
          },
          model_operation: { type: "string", description: "Alias for operation." },
          generation_intent: {
            type: "object",
            additionalProperties: true,
            description: 'Explicit capability intent, e.g. {capability:"video_generation",operation:"first_last_frame_to_video",reference_assets:[...]}'
          },
          reference_assets: {
            type: "array",
            items: {
              type: "object",
              additionalProperties: true,
              properties: {
                role: { type: "string", enum: ["generic", "first_frame", "last_frame", "reference_image", "reference_video", "reference_audio"] },
                media_type: { type: "string", enum: ["image", "video", "audio"] },
                resource_id: { type: "number", minimum: 1 }
              },
              required: ["role"]
            },
            description: "Semantic roles for input/reference resources. Use reference_image/reference_video/reference_audio for omni reference video, and first_frame/last_frame for first-last video generation."
          },
          parameter_mode: { type: "string", enum: ["compatible", "strict"] },
          param_mode: { type: "string", enum: ["compatible", "strict"] },
          projectDir: { type: "string", description: "MovScript project source directory." },
          project_dir: { type: "string", description: "Alias for projectDir." },
          cwd: { type: "string", description: "Alias for projectDir." },
          projectUid: { type: "string", description: "Optional manifest project_uid used to namespace generated job metadata." },
          project_uid: { type: "string", description: "Alias for projectUid." },
          contentUnitId: { type: ["string", "number"], description: "Target content unit for scope=content_unit." },
          content_unit_id: { type: ["string", "number"], description: "Alias for contentUnitId." },
          candidateId: { type: "string" },
          candidate_id: { type: "string" },
          candidate_policy: { type: "string", enum: ["none", "auto_create", "register_existing"] },
          input_resource_ids: { type: "array", items: { type: "number" } },
          reference_resource_ids: { type: "array", items: { type: "number" } },
          negative_prompt: { type: "string" },
          aspect_ratio: { type: "string" },
          image_size: { type: "string" },
          duration: { type: "number", minimum: 0 },
          quality: { type: "string" },
          steps: { type: "number", minimum: 1 },
          seed: { type: "number" },
          fps: { type: "number", minimum: 1 },
          voice: { type: "string" },
          language: { type: "string" },
          source_language: { type: "string" },
          target_language: { type: "string" },
          model: { type: "string" },
          audio_format: { type: "string" },
          response_format: { type: "string" },
          output_format: { type: "string" },
          subtitle_format: { type: "string" },
          style: { type: "string" },
          speed: { type: "number", minimum: 0.25, maximum: 4 },
          instructions: { type: "string" },
          extra_params: { type: "object", additionalProperties: true },
          timeout_ms: { type: "number", minimum: 1 },
          poll_interval_ms: { type: "number", minimum: 1 }
        },
        required
      );
    }
    function modelTools2() {
      return [
        {
          name: "generation_model_list",
          description: "List enabled generation and speech models for an AI runtime capability and optional model operation intent. Use capability plus operation for family capabilities, e.g. video_generation + first_last_frame_to_video or audio_generation + music. The result exposes public model contracts only; provider routes, adapters, and endpoints are Admin/debug details.",
          inputSchema: objectSchema32(
            {
              capability: { type: "string", description: "Optional AI capability family such as text_generation, image_generation, video_generation, audio_generation, or an execution capability such as audio_music. Family capabilities should pair this with operation." },
              operation: { type: "string", description: "Optional model operation intent such as text_to_image, image_to_image, first_last_frame_to_video, music, sfx, tts, or stt. If capability is omitted, known operations imply image_generation, video_generation, or audio_generation." },
              model_operation: { type: "string", description: "Alias for operation." },
              reference_assets: {
                type: "array",
                description: "Optional reference asset intent used to narrow route-capable models without exposing route details.",
                items: {
                  type: "object",
                  additionalProperties: false,
                  required: ["role"],
                  properties: {
                    role: { type: "string", description: "Reference role such as generic, reference_image, reference_video, reference_audio, first_frame, or last_frame." },
                    media_type: { type: "string", enum: ["image", "video", "audio"], description: "Optional media type hint." }
                  }
                }
              },
              provider_variants: { type: "boolean", description: "Admin/debug hint for backend querying; returned records remain public model contracts without route details." },
              include_provider_variants: { type: "boolean", description: "Alias for provider_variants." }
            }
          ),
          outputSchema: objectSchema32(
            {
              count: { type: "number" },
              queries: { type: "array", items: { type: "string" } },
              model_contracts: {
                type: "array",
                items: {
                  type: "object",
                  additionalProperties: true,
                  required: ["contract_version", "model_id", "capabilities", "input_requirements", "supported_param_keys", "supported_params"],
                  properties: {
                    contract_version: { type: "number", const: 1 },
                    model_id: { type: "string", description: "Public logical model ID to pass to plugin generation tools." },
                    display_name: { type: "string" },
                    short_name: { type: "string" },
                    logical_model_id: { type: "string", description: "Legacy alias for model_id." },
                    capabilities: { type: "array", items: { type: "string" } },
                    accepts_image_input: { type: "boolean" },
                    input_requirements: {
                      type: "object",
                      required: ["image", "video"],
                      properties: {
                        image: {
                          type: "object",
                          required: ["min", "max"],
                          properties: {
                            min: { type: "number" },
                            max: { type: "number" }
                          }
                        },
                        video: {
                          type: "object",
                          required: ["min", "max"],
                          properties: {
                            min: { type: "number" },
                            max: { type: "number" }
                          }
                        }
                      }
                    },
                    supported_param_keys: { type: "array", items: { type: "string" } },
                    supported_params: {
                      type: "array",
                      items: {
                        type: "object",
                        additionalProperties: true,
                        required: ["key"],
                        properties: {
                          key: { type: "string" },
                          label: { type: "string" },
                          type: { type: "string", enum: ["select", "number", "boolean", "string"] },
                          options: { type: "array", items: { type: "string" } },
                          conflicts_with: { type: "array", items: { type: "string" } },
                          conditional_enum: { type: "array" },
                          conditional_const: { type: "array" },
                          requires_value: { type: "array" }
                        }
                      }
                    },
                    params_schema_loaded: { type: "boolean" },
                    params_schema_rule_count: { type: "number" }
                  }
                }
              },
              models: { type: "array", description: "Compatibility alias for model_contracts; contains public model contracts, not raw provider route records." }
            },
            ["count", "queries", "model_contracts", "models"]
          )
        }
      ];
    }
    function resourceLibraryTools2() {
      return [
        {
          name: "movscript_resource_library_query",
          description: "Query MovScript RawResources stored in the internal resource library. Use returned RawResource.ID values as input_resource_ids or reference_resource_ids for generation.",
          inputSchema: objectSchema32(
            {
              query: { type: "string", description: "Search resource names." },
              q: { type: "string", description: "Alias for query." },
              resource_id: { type: "number", description: "Optional RawResource ID to filter the requested page." },
              resourceId: { type: "number", description: "Camel-case alias for resource_id." },
              id: { type: "number", description: "Alias for resource_id." },
              type: { type: "string", enum: ["image", "video", "audio", "text", "file"], description: "Optional MovScript resource type filter." },
              media_type: { type: "string", enum: ["image", "video", "audio", "text", "file"], description: "Alias for type." },
              scope: { type: "string", enum: ["personal", "team"], description: "Optional library scope. Omit for the unified visible library." },
              folder_id: { type: "string", description: 'Optional folder filter. Use "root" or "0" for unfiled resources.' },
              folderId: { type: "string", description: "Camel-case alias for folder_id." },
              page: { type: "number", description: "1-based page number. Defaults to 1." },
              page_size: { type: "number", description: "Page size, clamped to 1-100. Defaults to 20." },
              pageSize: { type: "number", description: "Camel-case alias for page_size." },
              limit: { type: "number", description: "Alias for page_size." },
              include_full: { type: "boolean", description: "When true, return full backend RawResource records." },
              includeFull: { type: "boolean", description: "Camel-case alias for include_full." }
            }
          ),
          outputSchema: objectSchema32(
            {
              source: { type: "string" },
              query: { type: "string" },
              type: { type: "string" },
              scope: { type: "string" },
              folder_id: { type: "string" },
              page: { type: "number" },
              pageSize: { type: "number" },
              total: { type: "number" },
              count: { type: "number" },
              items: { type: "array", items: { type: "object", additionalProperties: true } },
              usage: { type: "string" },
              warning: { type: "string" }
            },
            ["source", "query", "page", "pageSize", "total", "count", "items", "usage"]
          )
        },
        {
          name: "movscript_resource_library_open",
          description: "Return an agent in-app browser URL for the MovScript resource library. The frontend page uses the local MCP proxy so the active agent context is forwarded without exposing a bearer token in the URL.",
          inputSchema: objectSchema32(
            {
              frontend_origin: { type: "string", description: "Optional frontend origin, such as http://127.0.0.1:5173. Defaults to MOVSCRIPT_FRONTEND_ORIGIN or the dev frontend." },
              frontendOrigin: { type: "string", description: "Camel-case alias for frontend_origin." },
              mcp_base_url: { type: "string", description: "Optional MCP server origin. Defaults to MOVSCRIPT_MCP_ENDPOINT origin." },
              mcpBaseURL: { type: "string", description: "Camel-case alias for mcp_base_url." }
            }
          ),
          outputSchema: objectSchema32(
            {
              source: { type: "string" },
              kind: { type: "string" },
              title: { type: "string" },
              url: { type: "string" },
              frontend_origin: { type: "string" },
              mcp_api_base_url: { type: "string" },
              route: { type: "string" },
              api_proxy: { type: "object", additionalProperties: true },
              backend_api_base_url: { type: "string" },
              usage: { type: "string" }
            },
            ["source", "kind", "title", "url", "frontend_origin", "mcp_api_base_url", "route", "api_proxy", "usage"]
          )
        }
      ];
    }
    function resourceMediaTools2() {
      return [
        {
          name: "movscript_resource_image_read",
          description: "Read a MovScript image RawResource and return it as MCP image content for Codex vision. Use this when the agent needs to inspect the actual pixels of an image resource.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            max_bytes: { type: "number", description: "Maximum image file size to return. Defaults to 8 MiB, hard-capped at 20 MiB." },
            maxBytes: { type: "number", description: "Camel-case alias for max_bytes." },
            mode: { type: "string", enum: ["fit", "original"], description: "Image read mode. fit validates and downsizes to max_width/max_height when needed; original validates but returns the original bytes." },
            detail: { type: "string", enum: ["high", "auto", "low", "original"], description: "Codex-style image detail hint. detail=original is equivalent to mode=original; other values use mode=fit." },
            max_width: { type: "number", description: "Maximum output width for mode=fit. Defaults to 1568, hard-capped at 4096." },
            maxWidth: { type: "number", description: "Camel-case alias for max_width." },
            max_height: { type: "number", description: "Maximum output height for mode=fit. Defaults to 1568, hard-capped at 4096." },
            maxHeight: { type: "number", description: "Camel-case alias for max_height." },
            mime_type: { type: "string", description: "Optional MIME type override when backend headers are missing." },
            mimeType: { type: "string", description: "Camel-case alias for mime_type." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              resource_id: { type: "number" },
              mode: { type: "string" },
              source_mime_type: { type: "string" },
              source_size_bytes: { type: "number" },
              source_width: { type: "number" },
              source_height: { type: "number" },
              mime_type: { type: "string" },
              size_bytes: { type: "number" },
              width: { type: "number" },
              height: { type: "number" },
              resized: { type: "boolean" },
              max_width: { type: "number" },
              max_height: { type: "number" },
              image_payload: { type: "string" }
            },
            ["status", "resource_id", "mode", "source_mime_type", "source_size_bytes", "source_width", "source_height", "mime_type", "size_bytes", "width", "height", "resized", "image_payload"]
          )
        },
        {
          name: "movscript_resource_image_transform_to_resource",
          description: "Transform a MovScript image RawResource with optional crop, resize/fit, and format conversion, then upload the result as a new image RawResource. This is a stable resource operation and does not update business shot or candidate state.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript image RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            crop_x: { type: "number", description: "Optional crop rectangle x coordinate in source pixels. Defaults to 0." },
            cropX: { type: "number", description: "Camel-case alias for crop_x." },
            crop_y: { type: "number", description: "Optional crop rectangle y coordinate in source pixels. Defaults to 0." },
            cropY: { type: "number", description: "Camel-case alias for crop_y." },
            crop_width: { type: "number", description: "Optional crop rectangle width in source pixels." },
            cropWidth: { type: "number", description: "Camel-case alias for crop_width." },
            crop_height: { type: "number", description: "Optional crop rectangle height in source pixels." },
            cropHeight: { type: "number", description: "Camel-case alias for crop_height." },
            width: { type: "number", description: "Optional exact output width. Preserves aspect ratio when height is omitted." },
            height: { type: "number", description: "Optional exact output height. Preserves aspect ratio when width is omitted." },
            max_width: { type: "number", description: "Optional maximum output width for fit resize." },
            maxWidth: { type: "number", description: "Camel-case alias for max_width." },
            max_height: { type: "number", description: "Optional maximum output height for fit resize." },
            maxHeight: { type: "number", description: "Camel-case alias for max_height." },
            output_format: { type: "string", enum: ["jpeg", "png", "webp"], description: "Output image format. Defaults from source MIME type, falling back to png." },
            outputFormat: { type: "string", enum: ["jpeg", "png", "webp"], description: "Camel-case alias for output_format." },
            image_format: { type: "string", enum: ["jpeg", "png", "webp"], description: "Alias for output_format." },
            imageFormat: { type: "string", enum: ["jpeg", "png", "webp"], description: "Camel-case alias for image_format." },
            filename: { type: "string", description: "Optional output image filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            mime_type: { type: "string", description: "Optional source MIME type override when backend headers are missing." },
            mimeType: { type: "string", description: "Camel-case alias for mime_type." },
            max_source_bytes: { type: "number", description: "Maximum source image file size to download. Defaults to 8 MiB, hard-capped at 20 MiB." },
            maxSourceBytes: { type: "number", description: "Camel-case alias for max_source_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated image upload size. Defaults to 20 MiB, hard-capped at 100 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              source_resource_id: { type: "number" },
              image_resource_id: { type: "number" },
              resource_id: { type: "number" },
              source_mime_type: { type: "string" },
              source_size_bytes: { type: "number" },
              source_width: { type: "number" },
              source_height: { type: "number" },
              mime_type: { type: "string" },
              size_bytes: { type: "number" },
              width: { type: "number" },
              height: { type: "number" },
              resource: { type: "object", additionalProperties: true },
              transform: { type: "object", additionalProperties: true },
              message: { type: "string" }
            },
            ["status", "source_resource_id", "image_resource_id", "resource_id", "mime_type", "width", "height", "message"]
          )
        },
        {
          name: "movscript_resource_video_extract_frames",
          description: "Download a MovScript video RawResource, extract representative or precise frames with ffmpeg, and return the frames as MCP image content for Codex vision. Supports overview, timestamps, range, and burst sampling. The original video is not sent to the model.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            mode: { type: "string", enum: ["overview", "timestamps", "range", "burst"], description: "Sampling mode. overview samples the full video, timestamps uses exact seconds, range samples between start/end, burst samples a window around center." },
            count: { type: "number", description: "Overview frame count. Defaults to 4." },
            frame_count: { type: "number", description: "Alias for count." },
            max_frames: { type: "number", description: "Maximum frames returned for any mode. Defaults to 12, hard-capped at 24." },
            maxFrames: { type: "number", description: "Camel-case alias for max_frames." },
            max_video_bytes: { type: "number", description: "Maximum source video file size to download for extraction. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            timestamps_sec: { type: "array", items: { type: "number" }, description: "Optional exact timestamps in seconds." },
            timestampsSec: { type: "array", items: { type: "number" }, description: "Camel-case alias for timestamps_sec." },
            start_sec: { type: "number", description: "Start timestamp for evenly-spaced sampling when timestamps_sec is omitted." },
            startSec: { type: "number", description: "Camel-case alias for start_sec." },
            end_sec: { type: "number", description: "End timestamp for range sampling." },
            endSec: { type: "number", description: "Camel-case alias for end_sec." },
            center_sec: { type: "number", description: "Center timestamp for burst sampling." },
            centerSec: { type: "number", description: "Camel-case alias for center_sec." },
            window_sec: { type: "number", description: "Window length in seconds for burst sampling. Defaults to 2." },
            windowSec: { type: "number", description: "Camel-case alias for window_sec." },
            fps: { type: "number", description: "Range/burst sampling frequency in frames per second. Defaults to 2, capped at 6." },
            interval_sec: { type: "number", description: "Sampling interval in seconds when timestamps_sec is omitted. Defaults to 3." },
            intervalSec: { type: "number", description: "Camel-case alias for interval_sec." },
            max_width: { type: "number", description: "Maximum output frame width. Defaults to 960, hard-capped at 1920." },
            maxWidth: { type: "number", description: "Camel-case alias for max_width." },
            image_format: { type: "string", description: "Output frame format: jpeg or png. Defaults to jpeg." },
            imageFormat: { type: "string", description: "Camel-case alias for image_format." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              resource_id: { type: "number" },
              source_mime_type: { type: "string" },
              source_size_bytes: { type: "number" },
              max_video_bytes: { type: "number" },
              video: { type: "object", additionalProperties: true },
              sampling: { type: "object", additionalProperties: true },
              max_width: { type: "number" },
              frames: { type: "array", items: { type: "object", additionalProperties: true } },
              warnings: { type: "array", items: { type: "string" } },
              message: { type: "string" }
            },
            ["status", "resource_id", "sampling", "frames", "message"]
          )
        },
        {
          name: "movscript_resource_video_probe",
          description: "Probe a MovScript video RawResource with ffprobe and return stable media metadata such as duration, dimensions, and fps. This is a read-only resource inspection tool.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript video RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            max_video_bytes: { type: "number", description: "Maximum source video file size to download for probing. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              resource_id: { type: "number" },
              source_mime_type: { type: "string" },
              source_size_bytes: { type: "number" },
              max_video_bytes: { type: "number" },
              video: { type: "object", additionalProperties: true },
              message: { type: "string" }
            },
            ["status", "resource_id", "video", "message"]
          )
        },
        {
          name: "movscript_resource_video_extract_frame_to_resource",
          description: "Extract one precise frame from a MovScript video RawResource and upload it as a new image RawResource. Use the returned image_resource_id as generation input_resource_ids or reference_resource_ids.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript video RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            timestamp_sec: { type: "number", description: "Frame timestamp in seconds." },
            timestampSec: { type: "number", description: "Camel-case alias for timestamp_sec." },
            max_video_bytes: { type: "number", description: "Maximum source video file size to download for extraction. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated image upload size. Defaults to 20 MiB, hard-capped at 100 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." },
            max_width: { type: "number", description: "Maximum output frame width. Defaults to 960, hard-capped at 1920." },
            maxWidth: { type: "number", description: "Camel-case alias for max_width." },
            image_format: { type: "string", enum: ["jpeg", "png"], description: "Output frame format. Defaults to jpeg." },
            imageFormat: { type: "string", enum: ["jpeg", "png"], description: "Camel-case alias for image_format." },
            filename: { type: "string", description: "Optional output image filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              source_resource_id: { type: "number" },
              image_resource_id: { type: "number" },
              resource_id: { type: "number" },
              timestamp_sec: { type: "number" },
              frame: { type: "object", additionalProperties: true },
              video: { type: "object", additionalProperties: true },
              message: { type: "string" }
            },
            ["status", "source_resource_id", "image_resource_id", "resource_id", "timestamp_sec", "message"]
          )
        },
        {
          name: "movscript_resource_video_extract_frames_to_resources",
          description: "Extract multiple frames from a MovScript video RawResource and upload each frame as a new image RawResource. Supports the same overview, timestamps, range, and burst sampling modes as movscript_resource_video_extract_frames.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript video RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            mode: { type: "string", enum: ["overview", "timestamps", "range", "burst"], description: "Sampling mode. overview samples the full video, timestamps uses exact seconds, range samples between start/end, burst samples a window around center." },
            count: { type: "number", description: "Overview frame count. Defaults to 4." },
            frame_count: { type: "number", description: "Alias for count." },
            max_frames: { type: "number", description: "Maximum frames uploaded for any mode. Defaults to 12, hard-capped at 24." },
            maxFrames: { type: "number", description: "Camel-case alias for max_frames." },
            timestamps_sec: { type: "array", items: { type: "number" }, description: "Optional exact timestamps in seconds." },
            timestampsSec: { type: "array", items: { type: "number" }, description: "Camel-case alias for timestamps_sec." },
            start_sec: { type: "number", description: "Start timestamp for range sampling." },
            startSec: { type: "number", description: "Camel-case alias for start_sec." },
            end_sec: { type: "number", description: "End timestamp for range sampling." },
            endSec: { type: "number", description: "Camel-case alias for end_sec." },
            center_sec: { type: "number", description: "Center timestamp for burst sampling." },
            centerSec: { type: "number", description: "Camel-case alias for center_sec." },
            window_sec: { type: "number", description: "Window length in seconds for burst sampling. Defaults to 2." },
            windowSec: { type: "number", description: "Camel-case alias for window_sec." },
            fps: { type: "number", description: "Range/burst sampling frequency in frames per second. Defaults to 2, capped at 6." },
            interval_sec: { type: "number", description: "Sampling interval in seconds when timestamps_sec is omitted. Defaults to 3." },
            intervalSec: { type: "number", description: "Camel-case alias for interval_sec." },
            max_video_bytes: { type: "number", description: "Maximum source video file size to download for extraction. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated image upload size per frame. Defaults to 20 MiB, hard-capped at 100 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." },
            max_width: { type: "number", description: "Maximum output frame width. Defaults to 960, hard-capped at 1920." },
            maxWidth: { type: "number", description: "Camel-case alias for max_width." },
            image_format: { type: "string", enum: ["jpeg", "png"], description: "Output frame format. Defaults to jpeg." },
            imageFormat: { type: "string", enum: ["jpeg", "png"], description: "Camel-case alias for image_format." },
            filename: { type: "string", description: "Optional base output image filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              resource_id: { type: "number" },
              source_resource_id: { type: "number" },
              video: { type: "object", additionalProperties: true },
              sampling: { type: "object", additionalProperties: true },
              count: { type: "number" },
              resource_ids: { type: "array", items: { type: "number" } },
              image_resource_ids: { type: "array", items: { type: "number" } },
              items: { type: "array", items: { type: "object", additionalProperties: true } },
              warnings: { type: "array", items: { type: "string" } },
              message: { type: "string" }
            },
            ["status", "resource_id", "source_resource_id", "sampling", "count", "resource_ids", "items", "message"]
          )
        },
        {
          name: "movscript_resource_video_trim_to_resource",
          description: "Neutral resource preparation: trim a MovScript video RawResource to a new MP4 video RawResource for references, drafts, or generation inputs. This is not the product editing path; normal timeline trim, render, export, and candidate creation should use MediaEditingProject plus editing_* tools through Electron mediaPipeline. This tool does not update any business candidate, selection, or shot state.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript video RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            start_sec: { type: "number", description: "Clip start time in seconds. Defaults to 0." },
            startSec: { type: "number", description: "Camel-case alias for start_sec." },
            end_sec: { type: "number", description: "Clip end time in seconds." },
            endSec: { type: "number", description: "Camel-case alias for end_sec." },
            duration_sec: { type: "number", description: "Clip duration in seconds. Used when end_sec is omitted." },
            durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
            mode: { type: "string", enum: ["accurate", "fast"], description: "Trim mode. accurate re-encodes to browser-friendly MP4; fast stream-copies when possible." },
            volume: { type: "number", description: "Optional audio volume percentage, 0-200. Forces accurate mode behavior." },
            muted: { type: "boolean", description: "When true, omit audio from the output." },
            filename: { type: "string", description: "Optional output MP4 filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            max_video_bytes: { type: "number", description: "Maximum source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated video upload size. Defaults to 100 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
          })
        },
        {
          name: "movscript_resource_video_compose_to_resource",
          description: "Resource-level video utility: compose video RawResources in sequence into a new MP4 video RawResource. Items are generic media clips, not business shots; each may specify start/end/duration, volume, and muted. This is not the product editing path; normal timeline editing, render, HLS packaging, transcode, reframe, and export should use MediaEditingProject plus editing_* tools through Electron mediaPipeline.",
          inputSchema: objectSchema32({
            items: {
              type: "array",
              description: "Ordered video clip items. Each item requires resource_id and may include start_sec, end_sec, duration_sec, volume, and muted.",
              items: { type: "object", additionalProperties: true }
            },
            filename: { type: "string", description: "Optional output MP4 filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            max_video_bytes: { type: "number", description: "Maximum per-source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated video upload size. Defaults to 100 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
          }, ["items"])
        },
        {
          name: "movscript_resource_video_concat_to_resource",
          description: "Resource-level video utility: alias for movscript_resource_video_compose_to_resource for simple ordered concatenation into a new MP4 video RawResource. This is not the product editing path; normal timeline editing, render, HLS packaging, transcode, reframe, and export should use MediaEditingProject plus editing_* tools through Electron mediaPipeline.",
          inputSchema: objectSchema32({
            items: {
              type: "array",
              description: "Ordered video clip items. Each item requires resource_id and may include start_sec, end_sec, duration_sec, volume, and muted.",
              items: { type: "object", additionalProperties: true }
            },
            filename: { type: "string", description: "Optional output MP4 filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            max_video_bytes: { type: "number", description: "Maximum per-source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated video upload size. Defaults to 100 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
          }, ["items"])
        },
        {
          name: "movscript_resource_video_contact_sheet_to_resource",
          description: "Create a contact sheet image from a MovScript video RawResource and upload it as a new image RawResource. This gives agents a reusable overview image resource without binding to business shot state.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript video RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            count: { type: "number", description: "Approximate number of frames to place in the sheet. Defaults to 9, capped at 64." },
            columns: { type: "number", description: "Number of sheet columns. Defaults to 3." },
            thumb_width: { type: "number", description: "Thumbnail width in pixels. Defaults to 320." },
            thumbWidth: { type: "number", description: "Camel-case alias for thumb_width." },
            interval_sec: { type: "number", description: "Frame sampling interval in seconds. Defaults from video duration/count when duration can be probed." },
            intervalSec: { type: "number", description: "Camel-case alias for interval_sec." },
            filename: { type: "string", description: "Optional output image filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            max_video_bytes: { type: "number", description: "Maximum source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated image upload size. Defaults to 20 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
          })
        },
        {
          name: "movscript_resource_video_extract_audio_to_resource",
          description: "Extract the first audio stream from a MovScript video RawResource and upload it as a new audio RawResource. Optional start/end/duration trims the extracted audio.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "MovScript video RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            start_sec: { type: "number", description: "Optional audio start time in seconds." },
            startSec: { type: "number", description: "Camel-case alias for start_sec." },
            end_sec: { type: "number", description: "Optional audio end time in seconds." },
            endSec: { type: "number", description: "Camel-case alias for end_sec." },
            duration_sec: { type: "number", description: "Optional audio duration in seconds. Used when end_sec is omitted." },
            durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
            filename: { type: "string", description: "Optional output audio filename." },
            name: { type: "string", description: "Alias for filename." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            max_video_bytes: { type: "number", description: "Maximum source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
            max_upload_bytes: { type: "number", description: "Maximum generated audio upload size. Defaults to 100 MiB." },
            maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
          })
        },
        {
          name: "movscript_resource_image_annotate",
          description: "Create a simple agent-authored visual guidance image by overlaying structured annotations on a MovScript image resource, data URL, or local artifact. Writes an SVG artifact to the cache by default and returns artifact_path metadata only; image bytes are not returned as MCP image content. Upload artifact_path with movscript_resource_upload before using it in generation.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "Optional MovScript image RawResource ID used as the annotation background." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            data_url: { type: "string", description: "Optional image data URL used as the annotation background." },
            dataUrl: { type: "string", description: "Camel-case alias for data_url." },
            local_path: { type: "string", description: "Optional local image path used as the annotation background." },
            localPath: { type: "string", description: "Camel-case alias for local_path." },
            artifact_path: { type: "string", description: "Alias for local_path, useful for annotating a previous agent artifact." },
            artifactPath: { type: "string", description: "Camel-case alias for artifact_path." },
            mime_type: { type: "string", description: "Optional source image MIME type override." },
            mimeType: { type: "string", description: "Camel-case alias for mime_type." },
            max_source_bytes: { type: "number", description: "Maximum source image size to read. Defaults to 8 MiB, hard-capped at 20 MiB." },
            maxSourceBytes: { type: "number", description: "Camel-case alias for max_source_bytes." },
            width: { type: "number", description: "SVG coordinate width. Defaults to source image width when readable." },
            height: { type: "number", description: "SVG coordinate height. Defaults to source image height when readable." },
            title: { type: "string", description: "Artifact title used in metadata and default filename." },
            note: { type: "string", description: "Optional note rendered at the bottom of the annotation image." },
            annotations: {
              type: "array",
              description: "Structured annotation shapes. Supported type values: rect, circle, line, arrow, text, highlight.",
              items: { type: "object", additionalProperties: true }
            },
            shapes: { type: "array", items: { type: "object", additionalProperties: true }, description: "Alias for annotations." },
            output_path: { type: "string", description: "Optional absolute output path for the generated SVG artifact." },
            outputPath: { type: "string", description: "Camel-case alias for output_path." },
            workspace_path: { type: "string", description: "Optional output path under the MovScript .movscript workspace root." },
            workspacePath: { type: "string", description: "Camel-case alias for workspace_path." },
            workspaceDir: { type: "string", description: "Optional MovScript workspace root directory. Defaults to the desktop workspace root." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              artifact_path: { type: "string" },
              mime_type: { type: "string" },
              size_bytes: { type: "number" },
              width: { type: "number" },
              height: { type: "number" },
              source: { type: "object", additionalProperties: true },
              annotation_count: { type: "number" },
              annotations: { type: "array", items: { type: "object", additionalProperties: true } },
              artifact_location: { type: "string" },
              cache_dir: { type: "string" },
              image_payload: { type: "string" },
              mcp_image_content: { type: "boolean" },
              message: { type: "string" }
            },
            ["status", "artifact_path", "mime_type", "size_bytes", "annotation_count", "image_payload", "mcp_image_content"]
          )
        },
        {
          name: "movscript_resource_upload",
          description: "Upload an agent-accessible image or video artifact to the MovScript RawResource library. Accepts artifact/local paths, .movscript workspace paths, data URLs, or base64 bytes. Use the returned resource_id in shot groups or generation input_resource_ids/reference_resource_ids.",
          inputSchema: objectSchema32({
            artifact_path: { type: "string", description: "Local artifact path returned by movscript_resource_image_annotate or another agent tool." },
            artifactPath: { type: "string", description: "Camel-case alias for artifact_path." },
            local_path: { type: "string", description: "Local file path to upload." },
            localPath: { type: "string", description: "Camel-case alias for local_path." },
            path: { type: "string", description: "Alias for local_path." },
            workspace_path: { type: "string", description: "Path under the MovScript .movscript workspace root." },
            workspacePath: { type: "string", description: "Camel-case alias for workspace_path." },
            workspaceDir: { type: "string", description: "Optional MovScript workspace root directory. Defaults to the desktop workspace root." },
            data_url: { type: "string", description: "Image data URL to upload." },
            dataUrl: { type: "string", description: "Camel-case alias for data_url." },
            base64: { type: "string", description: "Base64 image payload without the data URL prefix." },
            filename: { type: "string", description: "Resource filename. Defaults to the local file name or a generated guidance filename." },
            name: { type: "string", description: "Alias for filename." },
            mime_type: { type: "string", description: "Upload MIME type. Defaults from filename, including common image/video extensions." },
            mimeType: { type: "string", description: "Camel-case alias for mime_type." },
            folder_id: { type: "string", description: "Optional resource library folder ID." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            max_bytes: { type: "number", description: "Maximum upload input size. Defaults to 20 MiB, hard-capped at 100 MiB." },
            maxBytes: { type: "number", description: "Camel-case alias for max_bytes." }
          }),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              resource_id: { type: "number" },
              resource: { type: "object", additionalProperties: true },
              source: { type: "object", additionalProperties: true },
              filename: { type: "string" },
              mime_type: { type: "string" },
              size_bytes: { type: "number" },
              message: { type: "string" }
            },
            ["status", "resource", "filename", "mime_type", "size_bytes", "message"]
          )
        },
        {
          name: "movscript_resource_upload_batch",
          description: "Upload multiple agent-accessible image or video artifacts to the MovScript RawResource library. Each item accepts the same fields as movscript_resource_upload. Results are returned in input order with per-item errors so agents can keep partial successes.",
          inputSchema: objectSchema32({
            items: {
              type: "array",
              description: "Upload item array. Each item accepts artifact_path/local_path/workspace_path/data_url/base64 plus optional filename, mime_type, folder_id, and max_bytes.",
              items: { type: "object", additionalProperties: true }
            },
            folder_id: { type: "string", description: "Default resource library folder ID applied to items that omit folder_id." },
            folderId: { type: "string", description: "Camel-case alias for folder_id." },
            max_bytes: { type: "number", description: "Default maximum upload input size applied to items that omit max_bytes. Defaults to 20 MiB, hard-capped at 100 MiB." },
            maxBytes: { type: "number", description: "Camel-case alias for max_bytes." },
            workspaceDir: { type: "string", description: "Default MovScript workspace root directory applied to workspace_path items." },
            continue_on_error: { type: "boolean", description: "When true, continue after item failures. Defaults to true." },
            continueOnError: { type: "boolean", description: "Camel-case alias for continue_on_error." },
            max_concurrency: { type: "number", description: "Maximum concurrent uploads. Defaults to 3 when continue_on_error is true; forced to 1 when false." },
            maxConcurrency: { type: "number", description: "Camel-case alias for max_concurrency." }
          }, ["items"]),
          outputSchema: objectSchema32(
            {
              status: { type: "string" },
              total: { type: "number" },
              success_count: { type: "number" },
              failed_count: { type: "number" },
              items: { type: "array", items: { type: "object", additionalProperties: true } },
              resource_ids: { type: "array", items: { type: "number" } },
              message: { type: "string" }
            },
            ["status", "total", "success_count", "failed_count", "items", "resource_ids", "message"]
          )
        }
      ];
    }
    function shotLibraryTools2() {
      return [
        {
          name: "movscript_shot_library_query",
          description: "Query the MovScript shot reference library for reusable camera, composition, movement, narrative, emotion, and production patterns. Use this before generation when the user asks for a shot style, camera plan, reference shot, or reusable visual pattern.",
          inputSchema: objectSchema32(
            {
              query: { type: "string", description: "Natural-language or tag query, e.g. \u89D2\u8272\u53D1\u73B0\u771F\u76F8\u524D, slow push in, foreground obstruction, tension." },
              q: { type: "string", description: "Alias for query." },
              shot_reference_id: { type: "number", description: "Optional shot reference ID to filter the requested page." },
              shotReferenceId: { type: "number", description: "Camel-case alias for shot_reference_id." },
              id: { type: "number", description: "Alias for shot_reference_id." },
              group_id: { type: "number", description: "Optional shot reference group ID. When present, returns shots in that group." },
              groupId: { type: "number", description: "Camel-case alias for group_id." },
              page: { type: "number", description: "1-based page number. Defaults to 1." },
              page_size: { type: "number", description: "Page size, clamped to 1-100. Defaults to 20." },
              pageSize: { type: "number", description: "Camel-case alias for page_size." },
              limit: { type: "number", description: "Alias for page_size." },
              topK: { type: "number", description: "Alias for page_size." },
              include_full: { type: "boolean", description: "When true, return the full backend record instead of a compact agent summary." },
              includeFull: { type: "boolean", description: "Camel-case alias for include_full." }
            }
          ),
          outputSchema: objectSchema32(
            {
              query: { type: "string" },
              page: { type: "number" },
              pageSize: { type: "number" },
              total: { type: "number" },
              count: { type: "number" },
              items: { type: "array", items: { type: "object", additionalProperties: true } },
              warning: { type: "string" }
            },
            ["query", "page", "pageSize", "total", "count", "items"]
          )
        },
        {
          name: "movscript_shot_group_create",
          description: "Create an empty MovScript shot reference group for a video RawResource. Use this before adding detected shots when the agent needs a stable group ID for the source clip.",
          inputSchema: objectSchema32(
            {
              resource_id: { type: "number", description: "Video RawResource ID used as the source resource for the group." },
              resourceId: { type: "number", description: "Camel-case alias for resource_id." },
              title: { type: "string", description: "Optional group title. Defaults from the source filename." },
              summary: { type: "string", description: "Optional group summary." },
              cut_strategy: { type: "string", description: "Optional cut strategy label, e.g. scene_detection or manual_review." },
              cutStrategy: { type: "string", description: "Camel-case alias for cut_strategy." }
            }
          ),
          outputSchema: objectSchema32({
            status: { type: "string" },
            group_id: { type: "number" },
            group: { type: "object", additionalProperties: true },
            message: { type: "string" }
          }, ["status", "group_id", "group", "message"])
        },
        {
          name: "movscript_shot_group_get",
          description: "Read a MovScript shot reference group and its ordered shots. Use this as the agent source of truth for which shots need to be recreated.",
          inputSchema: objectSchema32(
            {
              group_id: { type: "number", description: "Shot reference group ID." },
              groupId: { type: "number", description: "Camel-case alias for group_id." },
              id: { type: "number", description: "Alias for group_id." }
            }
          ),
          outputSchema: objectSchema32({
            status: { type: "string" },
            group_id: { type: "number" },
            group: { type: "object", additionalProperties: true },
            count: { type: "number" },
            shots: { type: "array", items: { type: "object", additionalProperties: true } }
          }, ["status", "group_id", "group", "count", "shots"])
        },
        {
          name: "movscript_shot_group_add_shots",
          description: "Append one or more shot references to an existing shot reference group for a source video resource. The agent can pass detected start/end ranges plus optional manual shot metadata.",
          inputSchema: objectSchema32(
            {
              group_id: { type: "number", description: "Existing shot reference group ID." },
              groupId: { type: "number", description: "Camel-case alias for group_id." },
              resource_id: { type: "number", description: "Video RawResource ID. Defaults to the group source resource when omitted." },
              resourceId: { type: "number", description: "Camel-case alias for resource_id." },
              duration_sec: { type: "number", description: "Optional full video duration in seconds." },
              durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
              width: { type: "number", description: "Optional video width." },
              height: { type: "number", description: "Optional video height." },
              shots: {
                type: "array",
                description: "Shot metadata array. Each item may include title, summary, start_sec/startSec, end_sec/endSec, tags, and professional annotation fields.",
                items: { type: "object", additionalProperties: true }
              }
            }
          ),
          outputSchema: objectSchema32({
            status: { type: "string" },
            group_id: { type: "number" },
            count: { type: "number" },
            shots: { type: "array", items: { type: "object", additionalProperties: true } },
            message: { type: "string" }
          }, ["status", "group_id", "count", "shots", "message"])
        },
        {
          name: "movscript_video_shot_cuts_analyze",
          description: "Analyze a MovScript video RawResource with ffmpeg scene detection and return shot ranges. This only detects cuts; use movscript_shot_group_add_shots to persist the ranges.",
          inputSchema: objectSchema32({
            resource_id: { type: "number", description: "Video RawResource ID." },
            resourceId: { type: "number", description: "Camel-case alias for resource_id." },
            id: { type: "number", description: "Alias for resource_id." },
            duration_sec: { type: "number", description: "Optional known duration. If omitted, ffprobe is used when available." },
            durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
            scene_threshold: { type: "number", description: "ffmpeg scene threshold. Defaults to 0.28." },
            sceneThreshold: { type: "number", description: "Camel-case alias for scene_threshold." },
            min_shot_duration_sec: { type: "number", description: "Minimum shot duration in seconds. Defaults to 1.2." },
            minShotDurationSec: { type: "number", description: "Camel-case alias for min_shot_duration_sec." },
            max_shot_duration_sec: { type: "number", description: "Maximum shot duration in seconds. Defaults to 12." },
            maxShotDurationSec: { type: "number", description: "Camel-case alias for max_shot_duration_sec." },
            max_video_bytes: { type: "number", description: "Maximum source video size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
            maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." }
          }),
          outputSchema: objectSchema32({
            status: { type: "string" },
            resource_id: { type: "number" },
            strategy: { type: "string" },
            duration_sec: { type: "number" },
            count: { type: "number" },
            shots: { type: "array", items: { type: "object", additionalProperties: true } },
            warning: { type: "string" }
          }, ["status", "resource_id", "strategy", "count", "shots"])
        }
      ];
    }
    function projectTools2() {
      return [
        {
          name: "movscript_project_create",
          description: "Create a formal MovScript project. Use only when the user explicitly asks to create a new project or confirms the project name.",
          inputSchema: objectSchema32(
            {
              name: { type: "string" },
              description: { type: "string" },
              total_episodes: { type: "number" }
            },
            ["name"]
          )
        },
        {
          name: "movscript_project_init",
          description: "Initialize a local MovScript project in any directory, then ensure its backend project identity and project-data space.",
          inputSchema: objectSchema32(
            {
              projectDir: { type: "string", description: "Absolute or relative directory to initialize as a MovScript project." },
              project_dir: { type: "string", description: "Alias for projectDir." },
              cwd: { type: "string", description: "Alias for projectDir, useful when initializing the current folder." },
              title: { type: "string" },
              projectId: { type: "string", description: "Optional local project id written into workspace metadata." },
              project_id: { type: "string", description: "Alias for projectId." },
              language: { type: "string" },
              overwrite: { type: "boolean" }
            }
          )
        },
        {
          name: "movscript_project_open",
          description: "Open a MovScript project by directory, then ensure its backend project identity and project-data space. This does not fetch source data; business source lives in the user chosen Git repository.",
          inputSchema: objectSchema32(
            {
              projectDir: { type: "string", description: "Absolute or relative MovScript project directory to open." },
              project_dir: { type: "string", description: "Alias for projectDir." },
              cwd: { type: "string", description: "Alias for projectDir." }
            }
          )
        },
        {
          name: "movscript_project_fetch",
          description: "Compatibility alias for movscript_project_open.",
          inputSchema: objectSchema32(
            {
              projectDir: { type: "string", description: "Absolute or relative MovScript project directory to open." },
              project_dir: { type: "string", description: "Alias for projectDir." },
              cwd: { type: "string", description: "Alias for projectDir." }
            }
          )
        }
      ];
    }
    var AGENT_SURFACE_ROUTES2 = {
      resources: "/agent/resources",
      resourceDetail: "/agent/resources/:resourceId",
      contentPrompt: "/agent/content/prompt",
      contentCandidates: "/agent/content/candidates",
      generationJob: "/agent/generation/jobs/:jobId",
      previewTimeline: "/agent/preview/timeline",
      impact: "/agent/impact",
      projectStatus: "/agent/project/status"
    };
    function agentGenerationJobPath2(jobId) {
      return `/agent/generation/jobs/${encodeURIComponent(String(jobId))}`;
    }
    function createAgentBrowserSurface2(args, input3) {
      const frontendOrigin = resolveFrontendOrigin2(args);
      const proxyBaseURL = resolveMCPProxyBaseURL2(args);
      const mcpApiBaseURL = `${proxyBaseURL}/agent-api/v1`;
      const url = new URL(input3.route, frontendOrigin);
      url.searchParams.set("mcpApiBaseURL", mcpApiBaseURL);
      url.searchParams.set("source", "mcp");
      for (const [key, value] of Object.entries(input3.query ?? {})) {
        if (value === void 0) continue;
        url.searchParams.set(key, String(value));
      }
      return {
        kind: "browser_url",
        surface: input3.surface,
        title: input3.title,
        route: input3.route,
        url: url.toString(),
        frontend_origin: frontendOrigin,
        mcp_api_base_url: mcpApiBaseURL,
        api_proxy: {
          base_url: mcpApiBaseURL,
          auth: "agent_mcp_context"
        },
        ...input3.entity ? { entity: input3.entity } : {},
        intent: input3.intent,
        usage: input3.usage
      };
    }
    function createPromptSurface2(args, input3) {
      return createAgentBrowserSurface2(args, {
        route: AGENT_SURFACE_ROUTES2.contentPrompt,
        title: `Content unit prompt ${String(input3.contentUnitId)}`,
        surface: input3.mode === "inspect" ? "inspect" : "edit",
        intent: "edit_prompt",
        entity: {
          ...input3.projectId !== void 0 ? { project_id: input3.projectId } : {},
          content_unit_id: input3.contentUnitId
        },
        query: {
          ...input3.projectId !== void 0 ? { projectId: input3.projectId } : {},
          contentUnitId: input3.contentUnitId,
          mode: input3.mode ?? "edit"
        },
        usage: "Open this prompt workbench to inspect semantic refs, blockers, compiled prompt text, provider prompt text, and generation inputs."
      });
    }
    function createGenerationJobSurface2(args, input3) {
      return createAgentBrowserSurface2(args, {
        route: agentGenerationJobPath2(input3.jobId),
        title: `Generation job #${input3.jobId}`,
        surface: "inspect",
        intent: "monitor_generation",
        entity: {
          job_id: input3.jobId,
          ...input3.projectId !== void 0 ? { project_id: input3.projectId } : {},
          ...input3.contentUnitId !== void 0 ? { content_unit_id: input3.contentUnitId } : {}
        },
        query: {
          jobId: input3.jobId,
          ...input3.projectId !== void 0 ? { projectId: input3.projectId } : {},
          ...input3.contentUnitId !== void 0 ? { contentUnitId: input3.contentUnitId } : {}
        },
        usage: "Open this generation job surface to monitor status, inspect inputs, preview outputs, and continue to candidate review after success."
      });
    }
    function createContentCandidatesSurface2(args, input3) {
      return createAgentBrowserSurface2(args, {
        route: AGENT_SURFACE_ROUTES2.contentCandidates,
        title: `Content unit candidates ${String(input3.contentUnitId)}`,
        surface: "review",
        intent: "review_candidates",
        entity: {
          ...input3.projectId !== void 0 ? { project_id: input3.projectId } : {},
          content_unit_id: input3.contentUnitId,
          ...input3.candidateId ? { candidate_id: input3.candidateId } : {},
          ...input3.resourceId !== void 0 ? { resource_id: input3.resourceId } : {}
        },
        query: {
          ...input3.projectId !== void 0 ? { projectId: input3.projectId } : {},
          contentUnitId: input3.contentUnitId,
          ...input3.candidateId ? { candidateId: input3.candidateId } : {},
          ...input3.resourceId !== void 0 ? { resourceId: input3.resourceId } : {}
        },
        usage: "Open this candidate review surface to compare generated candidates, inspect prompt snapshots, and adopt, reject, or defer a content-unit candidate."
      });
    }
    function createPreviewTimelineSurface2(args, input3) {
      const focus = resolveTimelineSurfaceFocus2(input3);
      return createAgentBrowserSurface2(args, {
        route: AGENT_SURFACE_ROUTES2.previewTimeline,
        title: `Timeline preview ${focus.label}`,
        surface: "review",
        intent: "preview_timeline",
        entity: focus.entity,
        query: focus.query,
        usage: "Open this preview timeline surface to inspect selected scene-moment outputs for a timeline assembly or legacy production scope."
      });
    }
    function createImpactSurface2(args, input3) {
      return createAgentBrowserSurface2(args, {
        route: AGENT_SURFACE_ROUTES2.impact,
        title: "MovScript change impact",
        surface: "review",
        intent: "review_impact",
        entity: {
          ...input3.projectId !== void 0 ? { project_id: input3.projectId } : {}
        },
        query: {
          ...input3.projectId !== void 0 ? { projectId: input3.projectId } : {},
          ...input3.target !== void 0 ? { target: input3.target } : {},
          ...input3.source ? { source: input3.source } : {}
        },
        usage: "Open this impact surface to review affected content units, stale selections, and explicit keep, relink, re-prompt, regenerate, or accept-stale decisions."
      });
    }
    function createProjectStatusSurface2(args, input3 = {}) {
      const focus = resolveTimelineSurfaceFocus2(input3);
      return createAgentBrowserSurface2(args, {
        route: AGENT_SURFACE_ROUTES2.projectStatus,
        title: "MovScript project status",
        surface: "inspect",
        intent: "inspect_project_status",
        entity: focus.entity,
        query: focus.query,
        usage: "Open this project status surface to inspect timeline readiness, candidate coverage, selections, stale hints, and blockers."
      });
    }
    function resolveTimelineSurfaceFocus2(input3) {
      const focusInput = {
        projectId: input3.projectId,
        productionId: input3.productionId,
        scopeKind: input3.scopeKind,
        scopeRef: input3.scopeRef,
        namespaceKind: input3.namespaceKind,
        namespaceRef: input3.namespaceRef,
        namespacePath: input3.namespacePath,
        targetCategory: input3.targetCategory,
        targetKind: input3.timelineAssemblyRef !== void 0 ? "timeline_assembly" : input3.targetKind,
        targetRef: input3.timelineAssemblyRef ?? input3.targetRef
      };
      const focus = normalizeDomainFocus2(focusInput);
      const legacyProductionId = input3.productionId ?? (focus.scope?.kind === "production" ? focus.scope.ref : void 0);
      const entity = {
        ...input3.projectId !== void 0 ? { project_id: input3.projectId } : {},
        ...legacyProductionId !== void 0 ? { production_id: legacyProductionId } : {},
        ...focus.scope ? {
          timeline_scope_kind: focus.scope.kind,
          timeline_scope_ref: focus.scope.ref
        } : {},
        ...focus.target?.targetCategory ? { target_category: focus.target.targetCategory } : {},
        ...focus.target?.targetKind ? { target_kind: focus.target.targetKind } : {},
        ...focus.target?.targetRef !== void 0 ? { target_ref: focus.target.targetRef } : {},
        ...focus.target?.targetKind === "timeline_assembly" && focus.target.targetRef !== void 0 ? { timeline_assembly_ref: focus.target.targetRef } : {},
        domain_focus: focus
      };
      const query = {
        ...input3.projectId !== void 0 ? { projectId: input3.projectId } : {},
        ...legacyProductionId !== void 0 ? { productionId: legacyProductionId } : {},
        ...focus.scope ? {
          scopeKind: focus.scope.kind,
          scopeRef: focus.scope.ref
        } : {},
        ...focus.target?.targetCategory ? { targetCategory: focus.target.targetCategory } : {},
        ...focus.target?.targetKind ? { targetKind: focus.target.targetKind } : {},
        ...focus.target?.targetRef !== void 0 ? { targetRef: focus.target.targetRef } : {}
      };
      return {
        focus,
        ...legacyProductionId !== void 0 ? { legacyProductionId } : {},
        label: timelineFocusLabel2(focus, legacyProductionId),
        entity,
        query
      };
    }
    function timelineFocusLabel2(focus, legacyProductionId) {
      if (focus.scope?.kind && focus.scope.ref) return `${focus.scope.kind} ${focus.scope.ref}`;
      if (focus.target?.targetKind && focus.target.targetRef) return `${focus.target.targetKind} ${focus.target.targetRef}`;
      if (legacyProductionId !== void 0) return `production ${String(legacyProductionId)}`;
      return "project";
    }
    function projectIdFromArgs2(args) {
      const value = args.projectId ?? args.project_id;
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function candidateIdFromArgs2(args) {
      return getOptionalString22(args, "candidateId") ?? getOptionalString22(args, "candidate_id");
    }
    function resolveFrontendOrigin2(args) {
      return normalizeHTTPOrigin2(
        getOptionalString22(args, "frontend_origin") ?? getOptionalString22(args, "frontendOrigin") ?? process.env.MOVSCRIPT_FRONTEND_ORIGIN ?? process.env.VITE_DEV_SERVER_URL ?? "http://127.0.0.1:5173"
      );
    }
    function resolveMCPProxyBaseURL2(args) {
      const explicit = getOptionalString22(args, "mcp_base_url") ?? getOptionalString22(args, "mcpBaseURL");
      if (explicit) return normalizeHTTPOrigin2(explicit);
      const endpoint = process.env.MOVSCRIPT_MCP_ENDPOINT;
      if (endpoint) {
        try {
          const url = new URL(endpoint);
          return normalizeHTTPOrigin2(url.origin);
        } catch {
        }
      }
      const port = process.env.MOVSCRIPT_MCP_PORT || "28765";
      return normalizeHTTPOrigin2(`http://127.0.0.1:${port}`);
    }
    function getOptionalString22(args, key) {
      const value = args[key];
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim();
      return trimmed || void 0;
    }
    function normalizeHTTPOrigin2(value) {
      const url = new URL(value.trim());
      if (url.protocol !== "http:" && url.protocol !== "https:") {
        throw new Error(`Expected http(s) URL, got ${value}`);
      }
      return url.origin;
    }
    function completeGenerationReferenceAssets2(input3) {
      const existing = Array.isArray(input3.existing) ? input3.existing : [];
      const inputResourceIds = input3.inputResourceIds ?? [];
      if (existing.length === 0 && inputResourceIds.length === 0) return [];
      const maxLength = Math.max(existing.length, inputResourceIds.length);
      const out = [];
      for (let index = 0; index < maxLength; index += 1) {
        const ref = existing[index];
        const resourceId = positiveInteger4(ref?.resource_id) ?? inputResourceIds[index];
        if (resourceId === void 0) continue;
        const mediaType = normalizedReferenceMediaType2(ref?.media_type) ?? defaultMediaTypeForOperation2(input3.operation, input3.defaultMediaType);
        const role = normalizedReferenceRole2(ref?.role) ?? generationDefaultReferenceRoleForMediaType2(mediaType);
        if (!mediaType || !role) continue;
        out.push({
          role,
          media_type: mediaType,
          resource_id: resourceId
        });
      }
      return out;
    }
    function generationDefaultReferenceRoleForMediaType2(mediaType) {
      switch (normalizedReferenceMediaType2(mediaType)) {
        case "image":
          return "reference_image";
        case "video":
          return "reference_video";
        case "audio":
          return "reference_audio";
        default:
          return void 0;
      }
    }
    function defaultMediaTypeForOperation2(operation, fallback) {
      const normalizedFallback = normalizedReferenceMediaType2(fallback);
      if (normalizedFallback) return normalizedFallback;
      switch (String(operation ?? "").trim()) {
        case "video_to_video":
        case "motion_control":
          return "video";
        case "stt":
        case "speech_translate":
        case "audio_chat":
        case "voice_clone":
        case "speech_enhancement":
        case "dubbing":
          return "audio";
        default:
          return "image";
      }
    }
    function normalizedReferenceRole2(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function normalizedReferenceMediaType2(value) {
      const mediaType = typeof value === "string" ? value.trim() : "";
      if (!mediaType || mediaType === "any") return void 0;
      return mediaType;
    }
    function positiveInteger4(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value.trim());
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function buildContentUnitGenerationRequest2(input3) {
      const promptText = compiledContentUnitGenerationPromptText2(input3.compiledPrompt);
      const inputResourceIds = positiveIntegerIds3([
        ...compiledContentUnitGenerationPromptResourceIds2(input3.compiledPrompt),
        ...input3.additionalInputResourceIds ?? []
      ]);
      const generationIntent = requiredContentUnitGenerationIntent2(input3.generationIntent);
      const jobType = generationExecutionJobTypeForIntent2(generationIntent, input3.outputKind);
      const params = contentUnitGenerationParams2(input3.outputKind, input3.compiledPrompt, input3.params);
      const promptSnapshot = buildContentUnitGenerationPromptSnapshot2({
        contentUnitId: input3.contentUnitId,
        outputKind: input3.outputKind,
        modelId: input3.modelId,
        compiledPrompt: input3.compiledPrompt,
        resourceIds: inputResourceIds,
        paramAudit: input3.paramAudit,
        modelParams: input3.modelParams ?? params
      });
      return {
        contentUnitId: input3.contentUnitId,
        outputKind: input3.outputKind,
        promptText,
        inputResourceIds,
        jobType,
        generationIntent,
        featureKey: contentUnitGenerationFeatureKey2(input3.outputKind),
        params,
        promptSnapshot
      };
    }
    function generationExecutionJobTypeForIntent2(intent, fallbackOutputKind) {
      switch (intent?.capability?.trim()) {
        case "video_generation":
          return "video";
        case "image_generation":
          return intent.operation === "image_to_image" || intent.operation === "image_edit" ? "image_edit" : "image";
        case "audio_generation":
          switch (intent.operation?.trim()) {
            case "music":
              return "audio_music";
            case "sfx":
              return "audio_sfx";
            case "stt":
              return "audio_transcribe";
            case "speech_translate":
              return "audio_translate";
            case "audio_chat":
              return "audio_chat";
            case "voice_clone":
              return "voice_clone";
            case "voice_design":
              return "voice_design";
            default:
              return "audio_tts";
          }
        default:
          return fallbackOutputKind;
      }
    }
    function requiredContentUnitGenerationIntent2(intent) {
      if (!intent?.capability?.trim() || !intent.operation?.trim()) {
        throw new Error("generationIntent with capability and operation is required for content-unit generation");
      }
      return intent;
    }
    function buildContentUnitGenerationOutputCandidate2(input3) {
      const jobId = contentUnitGenerationJobId2(input3.job);
      const candidateId = input3.candidateId ?? contentUnitGenerationCandidateId2(input3.outputKind, jobId, input3.resourceId);
      const promptSnapshot = input3.promptSnapshot ?? {};
      const modelId = input3.modelId ?? stringField20(input3.job.model_id) ?? stringField20(promptSnapshot.model_id);
      const modelParams = nonEmptyRecord2(input3.modelParams ?? recordField8(promptSnapshot.model_params));
      return {
        contentUnitId: String(input3.contentUnitId),
        candidateId: String(candidateId),
        source: "ai_generate",
        status: "succeeded",
        producer: {
          kind: "generation",
          tool: contentUnitGenerationToolName2(input3.outputKind),
          job_id: jobId,
          ...modelId ? { model_id: modelId } : {},
          ...stringField20(input3.job.job_type) ? { job_type: stringField20(input3.job.job_type) } : {},
          ...modelParams ? { model_params: modelParams } : {}
        },
        outputs: [{
          kind: input3.outputKind,
          resource_id: input3.resourceId,
          metadata: {
            job_id: jobId,
            ...modelId ? { model_id: modelId } : {},
            ...stringField20(input3.job.job_type) ? { job_type: stringField20(input3.job.job_type) } : {},
            tool: contentUnitGenerationMonitorToolName2(input3.outputKind)
          }
        }],
        promptSnapshot,
        createdAt: stringField20(input3.job.CreatedAt) ?? stringField20(input3.job.created_at) ?? (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    function contentUnitGenerationCandidateId2(outputKind, jobId, resourceId) {
      return `gen_${outputKind}_${String(jobId)}_${String(resourceId)}`;
    }
    function compiledContentUnitGenerationPromptText2(prompt) {
      const text = stringField20(prompt.text);
      if (text) return stripResourceMentions2(text);
      throw new Error("compiled content unit prompt has no text");
    }
    function compiledContentUnitGenerationPromptResourceIds2(prompt) {
      return positiveIntegerIds3([
        ...resourceIdsFromMentions2(stringField20(prompt.text)),
        ...resourceIdsFromMentions2(stringField20(prompt.negative_text)),
        ...numericList3(prompt.resource_ids),
        ...numericList3(prompt.resourceIds),
        ...numericList3(prompt.style_reference_resource_ids),
        ...numericList3(prompt.styleReferenceResourceIds)
      ]);
    }
    function compiledContentUnitGenerationPromptReferenceAssets2(prompt) {
      const declared = referenceAssetsFromValue2(prompt.reference_assets ?? prompt.referenceAssets);
      const mentioned = [
        ...referenceAssetsFromMentions2(stringField20(prompt.text)),
        ...referenceAssetsFromMentions2(stringField20(prompt.negative_text)),
        ...referenceAssetsFromMentions2(stringField20(prompt.notes))
      ];
      const all = [...declared, ...mentioned];
      const seen = /* @__PURE__ */ new Set();
      const deduped = all.filter((asset) => {
        if (seen.has(asset.resource_id)) return false;
        seen.add(asset.resource_id);
        return true;
      });
      return completeGenerationReferenceAssets2({
        existing: deduped,
        inputResourceIds: compiledContentUnitGenerationPromptResourceIds2(prompt)
      });
    }
    function buildContentUnitGenerationPromptSnapshot2(input3) {
      const resourceIds22 = input3.resourceIds ?? compiledContentUnitGenerationPromptResourceIds2(input3.compiledPrompt);
      const modelParams = nonEmptyRecord2(input3.modelParams);
      const referenceAssets = compiledContentUnitGenerationPromptReferenceAssets2(input3.compiledPrompt);
      return {
        schema: "movscript.content_unit_generation_prompt_snapshot.v1",
        content_unit_id: input3.contentUnitId,
        output_kind: input3.outputKind,
        model_id: input3.modelId,
        compiled_prompt: input3.compiledPrompt,
        resource_ids: resourceIds22,
        ...referenceAssets.length > 0 ? { reference_assets: referenceAssets } : {},
        ...modelParams ? { model_params: modelParams } : {},
        ...Array.isArray(input3.paramAudit) && input3.paramAudit.length > 0 ? { param_audit: input3.paramAudit } : {}
      };
    }
    function contentUnitGenerationFeatureKey2(outputKind) {
      return outputKind === "image" ? "electron.generation.content_unit.image" : "electron.generation.content_unit.video";
    }
    function contentUnitGenerationMonitorToolName2(outputKind) {
      return "generation_job_get";
    }
    function contentUnitGenerationSystemMonitorToolName2(outputKind) {
      return contentUnitGenerationMonitorToolName2(outputKind);
    }
    function contentUnitGenerationToolName2(outputKind) {
      return "generation_submit";
    }
    function contentUnitGenerationParams2(outputKind, prompt, params = {}) {
      const next = { ...params };
      if (outputKind === "image") {
        if (next.aspect_ratio === void 0) next.aspect_ratio = "1:1";
        const negativePrompt = stringField20(prompt.negative_text);
        if (negativePrompt && next.negative_prompt === void 0) next.negative_prompt = negativePrompt;
      } else {
        if (next.aspect_ratio === void 0) next.aspect_ratio = "16:9";
        if (next.duration === void 0) next.duration = 5;
      }
      return next;
    }
    function contentUnitGenerationJobId2(job) {
      const id = numericId22(job.ID) ?? numericId22(job.id);
      if (id !== void 0) return id;
      throw new Error("Generation job response does not include a valid job id");
    }
    function referenceAssetsFromValue2(value) {
      if (!Array.isArray(value)) return [];
      return value.flatMap((item) => {
        const record = recordField8(item);
        if (!record) return [];
        const resourceId = numericId22(record.resource_id ?? record.resourceId ?? record.id);
        if (resourceId === void 0) return [];
        const role = stringField20(record.role);
        const mediaType = stringField20(record.media_type ?? record.mediaType);
        return [{
          resource_id: resourceId,
          ...role ? { role } : {},
          ...mediaType ? { media_type: mediaType } : {}
        }];
      });
    }
    function referenceAssetsFromMentions2(text) {
      return parseResourceMentions2(text).map((mention) => ({
        resource_id: mention.id,
        ...mention.role ? { role: mention.role } : {},
        ...mention.mediaType ? { media_type: mention.mediaType } : {}
      }));
    }
    function positiveIntegerIds3(values) {
      const ids = values.map((value) => numericId22(value)).filter((value) => value !== void 0);
      return Array.from(new Set(ids));
    }
    function numericList3(value) {
      if (!Array.isArray(value)) return [];
      return value.map((item) => numericId22(item)).filter((item) => item !== void 0);
    }
    function numericId22(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const numberValue62 = Number(value.trim());
        if (Number.isInteger(numberValue62) && numberValue62 > 0) return numberValue62;
      }
      return void 0;
    }
    function stringField20(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function recordField8(value) {
      return value && typeof value === "object" && !Array.isArray(value) ? value : void 0;
    }
    function nonEmptyRecord2(value) {
      const record = recordField8(value);
      return record && Object.keys(record).length > 0 ? record : void 0;
    }
    var DEFAULT_GENERATION_TOOLS_SETTINGS2 = {
      servers: [
        createGenerationToolServer2("comfyui", {
          id: "local-comfyui-default",
          name: "\u672C\u673A ComfyUI",
          baseURL: "http://127.0.0.1:8188",
          priority: 10
        }),
        createGenerationToolServer2("webui", {
          id: "local-webui-default",
          name: "\u672C\u673A WebUI",
          baseURL: "http://127.0.0.1:7860",
          priority: 20
        })
      ],
      defaultServerId: void 0,
      defaultServerIds: {},
      preferLocalServers: true
    };
    function createGenerationToolServer2(type, overrides = {}) {
      const id = overrides.id ?? newLocalServerId2();
      return {
        id,
        scope: overrides.scope ?? "local",
        type,
        name: overrides.name ?? (type === "comfyui" ? "ComfyUI" : "Stable Diffusion WebUI"),
        enabled: overrides.enabled ?? false,
        baseURL: overrides.baseURL ?? (type === "comfyui" ? "http://127.0.0.1:8188" : "http://127.0.0.1:7860"),
        timeoutMS: overrides.timeoutMS ?? 12e4,
        priority: overrides.priority ?? 50,
        authKind: overrides.authKind ?? "none",
        username: overrides.username ?? "",
        password: overrides.password ?? "",
        passwordSet: overrides.passwordSet ?? false,
        token: overrides.token ?? "",
        tokenSet: overrides.tokenSet ?? false,
        tags: overrides.tags ?? []
      };
    }
    function newLocalServerId2() {
      if (typeof crypto !== "undefined" && typeof crypto.randomUUID === "function") {
        return `local-${crypto.randomUUID()}`;
      }
      return `local-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
    }
    function buildContentSourceWorkspaceData2(input3) {
      const productions = input3.productions;
      const segments = input3.segments;
      const sceneMoments = input3.sceneMoments;
      const storyboards = input3.storyboards;
      const expressionUnits = input3.expressionUnits;
      const audioCues = input3.audioCues;
      const contentUnits = input3.contentUnits;
      const keyframes = input3.keyframes;
      const assets = input3.assets;
      const settings = input3.settings;
      const settingStates = input3.settingStates;
      const previewTimelines = input3.previewTimelines;
      const productionWorkPlan = normalizeProductionWorkPlanView2(input3.productionWorkPlan);
      const domainGraph = buildContentSourceWorkspaceDomainGraph2(input3);
      const ancestryIndex = buildContentSourceWorkspaceEntityAncestryIndex2(input3, domainGraph);
      const contentUnitsByPrimaryRef = groupContentUnitsByPrimaryRef22(contentUnits);
      const candidateRecordsByContentUnitId = groupContentCandidateRecordsByContentUnitId2(input3.indexDocuments);
      const selectionRecordsByContentUnitId222 = groupSelectionRecordsByContentUnitId2(input3.indexDocuments);
      const selectionByContentUnitId = buildSelectionStateByContentUnitId2(contentUnits, selectionRecordsByContentUnitId222);
      const contentUnitCandidates = buildContentUnitCandidates2({
        contentUnits,
        candidateRecordsByContentUnitId,
        selectionRecordsByContentUnitId: selectionRecordsByContentUnitId222
      });
      const previewMoments = buildPreviewMoments2({
        productions,
        segments,
        sceneMoments,
        expressionUnits,
        storyboards,
        keyframes,
        assets,
        previewTimelines,
        ancestryIndex,
        contentUnitsByPrimaryRef,
        candidateRecordsByContentUnitId,
        selectionRecordsByContentUnitId: selectionRecordsByContentUnitId222,
        selectionByContentUnitId
      });
      const expressionUnitsByMoment = buildExpressionUnitsByMoment2(expressionUnits, ancestryIndex);
      const audioCuesByMoment = buildAudioCuesByMoment2(audioCues, {
        ancestryIndex,
        contentUnitsByPrimaryRef,
        candidateRecordsByContentUnitId,
        selectionRecordsByContentUnitId: selectionRecordsByContentUnitId222,
        selectionByContentUnitId
      });
      const expressionUnitWorkspaceDetails = buildExpressionUnitWorkspaceDetails2({
        expressionUnits,
        storyboards,
        keyframes,
        assets,
        settings,
        contentUnitsByPrimaryRef,
        ancestryIndex,
        candidateRecordsByContentUnitId,
        selectionRecordsByContentUnitId: selectionRecordsByContentUnitId222,
        selectionByContentUnitId
      });
      const assetReferenceUnits = buildAssetReferenceUnits2({
        assets,
        settings,
        settingStates,
        expressionUnits,
        storyboards,
        keyframes,
        contentUnits,
        contentUnitsByPrimaryRef,
        ancestryIndex,
        candidateRecordsByContentUnitId,
        selectionRecordsByContentUnitId: selectionRecordsByContentUnitId222,
        selectionByContentUnitId
      });
      return {
        source: "workspace",
        domainGraph,
        hierarchyTree: buildHierarchyTree2({
          settings,
          settingStates,
          assets,
          productions,
          segments,
          sceneMoments,
          storyboards,
          keyframes,
          expressionUnits,
          audioCues,
          assetReferenceUnits,
          ancestryIndex
        }),
        previewMoments,
        contentUnitCandidates,
        expressionUnitsByMoment,
        audioCuesByMoment,
        expressionUnitWorkspaceDetails,
        assetReferenceUnits,
        editingTimelines: input3.editingTimelines ?? [],
        productionWorkPlan
      };
    }
    function buildContentSourceWorkspaceDomainGraph2(input3) {
      const nodes = input3.domainNodes?.length ? input3.domainNodes : deriveContentSourceWorkspaceDomainNodes2(input3);
      const edges = input3.domainEdges?.length ? input3.domainEdges : deriveContentSourceWorkspaceDomainEdges2(input3, nodes);
      return {
        namespaceVocabulary: input3.namespaceVocabulary ?? deriveContentSourceWorkspaceNamespaceVocabulary2(input3),
        nodes,
        edges,
        timelineNamespaceNodes: nodes.filter((node) => node.category === "timeline_namespace"),
        settingNamespaceNodes: nodes.filter((node) => node.category === "setting_namespace"),
        systemPrimitiveNodes: nodes.filter((node) => node.category === "system_primitive"),
        contentUnitNodes: nodes.filter((node) => node.category === "content_unit")
      };
    }
    function deriveContentSourceWorkspaceNamespaceVocabulary2(input3) {
      const projectDocument = input3.indexDocuments.find(
        (document) => normalizeWorkspacePath8(document.path) === "project.json" && isRecord352(document.data)
      );
      return normalizeNamespaceVocabulary2(projectDocument?.data);
    }
    function deriveContentSourceWorkspaceDomainNodes2(input3) {
      return sourceWorkspaceDomainEntities2(input3).flatMap((entity) => {
        const category = classifyMovScriptEntityKind2(entity.entityKind);
        if (!category) return [];
        return [pruneUndefinedRecord4({
          category,
          kind: projectMovScriptDomainNodeKind2(entity.entityKind, entity.record),
          ...entity.id !== void 0 ? { id: entity.id } : {},
          path: entity.path,
          title: stringField21(entity.record.title) ?? stringField21(entity.record.label),
          order: optionalNumberField2(entity.record.order),
          metadata: { entityKind: entity.entityKind }
        })];
      });
    }
    function deriveContentSourceWorkspaceDomainPathParentEdges2(nodes) {
      const nodeByDir = /* @__PURE__ */ new Map();
      for (const node of nodes) {
        if (node.path) nodeByDir.set(entityDir92(node.path), node);
      }
      const edges = [];
      for (const node of nodes) {
        if (!node.path) continue;
        const parentPath = nearestParentPath3(entityDir92(node.path), nodeByDir.keys());
        if (!parentPath) continue;
        const parent = nodeByDir.get(parentPath);
        const normalized = normalizePathParentEdge2(domainRefFromNode22(node), parent ? domainRefFromNode22(parent) : void 0);
        if (normalized.edge) edges.push(normalized.edge);
      }
      return dedupeDomainEdges22(edges);
    }
    function deriveContentSourceWorkspaceDomainEdges2(input3, nodes) {
      return dedupeDomainEdges22([
        ...deriveContentSourceWorkspaceDomainPathParentEdges2(nodes),
        ...deriveContentSourceWorkspaceContentUnitTargetEdges2(input3.contentUnits, nodes)
      ]);
    }
    function deriveContentSourceWorkspaceContentUnitTargetEdges2(contentUnits, nodes) {
      const nodeByPath = new Map(nodes.flatMap((node) => node.path ? [[node.path, node]] : []));
      const timelineNamespaceNodes = nodes.filter((node) => node.category === "timeline_namespace");
      const edges = [];
      for (const contentUnit of contentUnits) {
        const sourceNode = nodeByPath.get(contentUnit.path);
        if (!sourceNode) continue;
        edges.push(...normalizeContentUnitTargetEdges2({
          source: domainRefFromNode22(sourceNode),
          record: contentUnit.record,
          scopeTarget(scope) {
            return domainRefFromTimelineScope32(scope.kind, scope.ref, timelineNamespaceNodes);
          }
        }));
      }
      return edges;
    }
    function sourceWorkspaceDomainEntities2(input3) {
      return [
        ...input3.settings,
        ...input3.settingStates,
        ...input3.assets,
        ...input3.productions,
        ...input3.segments,
        ...input3.sceneMoments,
        ...input3.storyboards,
        ...input3.keyframes,
        ...input3.expressionUnits,
        ...input3.audioCues,
        ...input3.contentUnits
      ];
    }
    function buildContentSourceWorkspaceEntityAncestryIndex2(input3, domainGraph) {
      const entities = sourceWorkspaceDomainEntities2(input3);
      const entityByPath = new Map(entities.map((entity) => [entity.path, entity]));
      const entityByDir = new Map(entities.map((entity) => [entityDir92(entity.path), entity]));
      const parentPathByPath = /* @__PURE__ */ new Map();
      for (const edge of domainGraph.edges) {
        if (edge.relation !== "parent" || edge.origin !== "path") continue;
        if (!edge.source.path || !edge.target.path) continue;
        parentPathByPath.set(edge.source.path, edge.target.path);
      }
      for (const entity of entities) {
        if (parentPathByPath.has(entity.path)) continue;
        const parentDir = nearestParentPath3(entityDir92(entity.path), entityByDir.keys());
        const parent = parentDir ? entityByDir.get(parentDir) : void 0;
        if (parent) parentPathByPath.set(entity.path, parent.path);
      }
      const childrenByParentPath = /* @__PURE__ */ new Map();
      for (const [childPath, parentPath] of parentPathByPath) {
        const child = entityByPath.get(childPath);
        if (!child) continue;
        childrenByParentPath.set(parentPath, [...childrenByParentPath.get(parentPath) ?? [], child]);
      }
      return { entityByPath, parentPathByPath, childrenByParentPath };
    }
    function domainRefFromTimelineScope32(scopeKind, scopeRef, timelineNamespaceNodes) {
      const node = timelineNamespaceNodes.find(
        (candidate) => String(candidate.id ?? "") === scopeRef && (candidate.kind === scopeKind || candidate.metadata?.entityKind === scopeKind)
      );
      return node ? domainRefFromNode22(node) : { category: "timeline_namespace", kind: scopeKind, id: scopeRef };
    }
    function domainRefFromNode22(node) {
      return {
        category: node.category,
        kind: node.kind,
        ...node.id !== void 0 ? { id: node.id } : {},
        ...node.path ? { path: node.path } : {}
      };
    }
    function dedupeDomainEdges22(edges) {
      const seen = /* @__PURE__ */ new Set();
      const out = [];
      for (const edge of edges) {
        const key = JSON.stringify(edge);
        if (seen.has(key)) continue;
        seen.add(key);
        out.push(edge);
      }
      return out;
    }
    function normalizeProductionWorkPlanView2(plan) {
      if (!plan) return void 0;
      if (isProductionWorkPlanView2(plan)) {
        return {
          summary: {
            open: optionalNumberField2(plan.summary.open) ?? 0,
            blocking: optionalNumberField2(plan.summary.blocking) ?? 0,
            humanRecommended: optionalNumberField2(plan.summary.humanRecommended) ?? 0,
            agentRecommended: optionalNumberField2(plan.summary.agentRecommended) ?? 0,
            readyToGenerate: optionalNumberField2(plan.summary.readyToGenerate) ?? 0,
            staleSelections: optionalNumberField2(plan.summary.staleSelections) ?? 0
          },
          items: plan.items.map(normalizeProductionWorkItemView2).filter(isDefined42)
        };
      }
      const rawPlan = plan;
      return {
        summary: {
          open: optionalNumberField2(rawPlan.summary.open) ?? 0,
          blocking: optionalNumberField2(rawPlan.summary.blocking) ?? 0,
          humanRecommended: optionalNumberField2(rawPlan.summary.human_recommended) ?? 0,
          agentRecommended: optionalNumberField2(rawPlan.summary.agent_recommended) ?? 0,
          readyToGenerate: optionalNumberField2(rawPlan.summary.ready_to_generate) ?? 0,
          staleSelections: optionalNumberField2(rawPlan.summary.stale_selections) ?? 0
        },
        items: rawPlan.items.map((item) => ({
          id: item.id,
          kind: item.kind,
          status: item.status,
          severity: item.severity,
          priority: item.priority,
          reason: item.reason,
          targetKind: item.target.entityKind,
          targetId: item.target.id !== void 0 ? String(item.target.id) : void 0,
          targetPath: item.target.path,
          recommendedActor: item.recommended_actor,
          actionLabels: item.actions.map((action) => productionWorkActionLabel2(action.type))
        }))
      };
    }
    function isProductionWorkPlanView2(plan) {
      return "humanRecommended" in plan.summary;
    }
    function normalizeProductionWorkItemView2(item) {
      if (!item.id || !item.kind || !item.status || !item.severity || !item.reason || !item.targetKind) return void 0;
      return {
        id: item.id,
        kind: normalizeProductionWorkItemKind2(item.kind),
        status: normalizeProductionWorkItemStatus2(item.status),
        severity: normalizeProductionWorkItemSeverity2(item.severity),
        priority: optionalNumberField2(item.priority) ?? 100,
        reason: item.reason,
        targetKind: item.targetKind,
        targetId: item.targetId,
        targetPath: item.targetPath,
        recommendedActor: item.recommendedActor === "agent" || item.recommendedActor === "workflow" ? item.recommendedActor : "human",
        actionLabels: Array.isArray(item.actionLabels) ? item.actionLabels.filter((label) => typeof label === "string" && label.trim()) : []
      };
    }
    function normalizeProductionWorkItemKind2(kind) {
      switch (kind) {
        case "fix_source":
        case "edit_structure":
        case "create_content_unit":
        case "generate_candidates":
        case "select_candidate":
        case "review_stale_selection":
        case "review_affected_output":
          return kind;
        default:
          return "edit_structure";
      }
    }
    function normalizeProductionWorkItemStatus2(status) {
      if (status === "open" || status === "blocked" || status === "ready" || status === "informational") return status;
      return "open";
    }
    function normalizeProductionWorkItemSeverity2(severity) {
      if (severity === "blocking" || severity === "warning" || severity === "suggestion") return severity;
      return "warning";
    }
    function productionWorkActionLabel2(type) {
      switch (type) {
        case "open_editor":
          return "\u6253\u5F00\u7F16\u8F91\u5668";
        case "upsert_entity":
          return "\u8865\u7ED3\u6784";
        case "derive_content_unit_artifact":
          return "\u5237\u65B0\u521B\u4F5C\u7247\u6BB5";
        case "generate_candidates":
          return "\u751F\u6210\u5019\u9009";
        case "open_candidate_picker":
          return "\u6253\u5F00\u5019\u9009\u9009\u62E9";
        case "agent_review_candidates":
          return "\u8F85\u52A9\u5BA1\u9605\u5019\u9009";
        case "accept_stale":
          return "\u63A5\u53D7 stale";
        default:
          return type;
      }
    }
    function buildHierarchyTree2(input3) {
      return [
        {
          id: "settings_root",
          type: "group",
          title: "Settings",
          path: "settings/",
          children: sortEntities22(input3.settings).map((setting) => {
            const states = childEntitiesForParent2(input3.ancestryIndex, input3.settingStates, setting);
            return entityNode2(setting, "setting", {
              children: sortEntities22(states).map((state) => {
                const stateAssets = childEntitiesForParent2(input3.ancestryIndex, input3.assets, state);
                return entityNode2(state, "state", {
                  children: sortEntities22(stateAssets).map((asset) => {
                    const unit = input3.assetReferenceUnits[nodeId2(asset, "asset")];
                    return entityNode2(asset, "asset", { state: unit?.selectionState === "needs_candidate" ? "missing" : void 0 });
                  })
                });
              })
            });
          })
        },
        {
          id: "productions_group",
          type: "group",
          title: "Productions",
          path: "productions",
          children: sortEntities22(input3.productions).map((production) => {
            const segments = childEntitiesForParent2(input3.ancestryIndex, input3.segments, production);
            return entityNode2(production, "production", {
              children: sortEntities22(segments).map((segment) => {
                const sceneMoments = childEntitiesForParent2(input3.ancestryIndex, input3.sceneMoments, segment);
                return entityNode2(segment, "segment", {
                  children: sortEntities22(sceneMoments).map((sceneMoment) => {
                    const momentDir = entityDir92(sceneMoment.path);
                    const expressions = childEntitiesForParent2(input3.ancestryIndex, input3.expressionUnits, sceneMoment);
                    const momentStoryboards = childEntitiesForParent2(input3.ancestryIndex, input3.storyboards, sceneMoment);
                    const momentKeyframes = childEntitiesForParent2(input3.ancestryIndex, input3.keyframes, sceneMoment);
                    const audioCues = childEntitiesForParent2(input3.ancestryIndex, input3.audioCues, sceneMoment);
                    const momentId = idText2(sceneMoment);
                    const expressionGroup = {
                      id: `${nodeId2(sceneMoment, "scene_moment")}_expression_group`,
                      type: "group",
                      title: "Expression Units",
                      path: `${momentDir}/expression_units`,
                      momentId,
                      children: sortEntities22(expressions).map((expression) => {
                        const expressionDir = entityDir92(expression.path);
                        const expressionUnitId = idText2(expression);
                        const storyboards = childEntitiesForParent2(input3.ancestryIndex, input3.storyboards, expression);
                        const keyframes = childEntitiesForParent2(input3.ancestryIndex, input3.keyframes, expression);
                        return entityNode2(expression, "expression_unit", {
                          momentId,
                          expressionUnitId,
                          children: [
                            {
                              id: `${nodeId2(expression, "expression_unit")}_storyboards_group`,
                              type: "group",
                              title: "Storyboards",
                              path: `${expressionDir}/storyboards`,
                              momentId,
                              expressionUnitId,
                              children: sortEntities22(storyboards).map((storyboard) => entityNode2(storyboard, "storyboard", { momentId, expressionUnitId }))
                            },
                            {
                              id: `${nodeId2(expression, "expression_unit")}_keyframes_group`,
                              type: "group",
                              title: "Keyframes",
                              path: `${expressionDir}/keyframes`,
                              momentId,
                              expressionUnitId,
                              children: sortEntities22(keyframes).map((keyframe) => entityNode2(keyframe, "keyframe", { momentId, expressionUnitId }))
                            }
                          ]
                        });
                      })
                    };
                    const storyboardGroup = {
                      id: `${nodeId2(sceneMoment, "scene_moment")}_storyboards_group`,
                      type: "group",
                      title: "Storyboards",
                      path: `${momentDir}/storyboards`,
                      momentId,
                      children: sortEntities22(momentStoryboards).map((storyboard) => entityNode2(storyboard, "storyboard", { momentId }))
                    };
                    const keyframeGroup = {
                      id: `${nodeId2(sceneMoment, "scene_moment")}_keyframes_group`,
                      type: "group",
                      title: "Keyframes",
                      path: `${momentDir}/keyframes`,
                      momentId,
                      children: sortEntities22(momentKeyframes).map((keyframe) => entityNode2(keyframe, "keyframe", { momentId }))
                    };
                    const audioCueGroup = {
                      id: `${nodeId2(sceneMoment, "scene_moment")}_audio_group`,
                      type: "group",
                      title: "Audio Cues",
                      path: `${momentDir}/audio_cues`,
                      momentId,
                      children: sortEntities22(audioCues).map((audioCue) => entityNode2(audioCue, "audio_cue", { momentId }))
                    };
                    return entityNode2(sceneMoment, "scene_moment", {
                      momentId,
                      children: [expressionGroup, storyboardGroup, keyframeGroup, audioCueGroup]
                    });
                  })
                });
              })
            });
          })
        }
      ];
    }
    function buildPreviewMoments2(input3) {
      return orderedSceneMoments2(input3.sceneMoments, input3.previewTimelines).map((moment, momentIndex) => {
        const segment = ancestorEntity2(input3.ancestryIndex, moment, "segment") ?? parentByDir2(input3.segments, moment.path);
        const production = segment ? ancestorEntity2(input3.ancestryIndex, segment, "production") ?? parentByDir2(input3.productions, segment.path) : ancestorEntity2(input3.ancestryIndex, moment, "production");
        const expressionUnits = orderedChildEntitiesForTimelineParent2(input3.expressionUnits, input3.previewTimelines, timelineItemIdForEntity2(moment), "expression_unit");
        const momentExpressionUnits = (expressionUnits.length > 0 ? expressionUnits : sortEntities22(childEntitiesForParent2(input3.ancestryIndex, input3.expressionUnits, moment))).map(
          (expressionUnit, expressionUnitIndex) => previewExpressionUnit2(expressionUnit, expressionUnitIndex, moment, input3)
        );
        return {
          id: idText2(moment),
          title: titleOf2(moment, `Scene Moment ${momentIndex + 1}`),
          path: entityDir92(moment.path),
          selectionState: momentSelectionState2(momentExpressionUnits),
          priority: momentIndex < 1 ? "\u9AD8\u4F18\u5148\u7EA7" : momentIndex < 3 ? "\u4E2D\u4F18\u5148\u7EA7" : "\u4F4E\u4F18\u5148\u7EA7",
          production: production ? titleOf2(production, idText2(production)) : "",
          segment: segment ? titleOf2(segment, idText2(segment)) : "",
          settings: settingRefsForMoment2(moment, momentExpressionUnits, input3.assets),
          expressionUnits: momentExpressionUnits
        };
      });
    }
    function previewExpressionUnit2(expressionUnit, expressionUnitIndex, moment, input3) {
      const storyboards = sortEntities22(childEntitiesForParent2(input3.ancestryIndex, input3.storyboards, expressionUnit));
      const keyframes = sortEntities22(childEntitiesForParent2(input3.ancestryIndex, input3.keyframes, expressionUnit));
      const primaryStoryboard = storyboards[0];
      const primaryKeyframe = keyframes[0];
      const contentUnit = contentUnitForEntity2(input3.contentUnitsByPrimaryRef, "expression_unit", expressionUnit) ?? (primaryStoryboard ? contentUnitForEntity2(input3.contentUnitsByPrimaryRef, "storyboard", primaryStoryboard) : void 0) ?? (primaryKeyframe ? contentUnitForEntity2(input3.contentUnitsByPrimaryRef, "keyframe", primaryKeyframe) : void 0);
      return {
        id: idText2(expressionUnit),
        title: titleOf2(expressionUnit, `Expression Unit ${expressionUnitIndex + 1}`),
        kind: stringField21(expressionUnit.record.kind) ?? "expression_unit",
        camera: shotCameraText2(expressionUnit),
        duration: durationText2(recordField9(expressionUnit.record.timing_intent)?.duration_sec ?? recordField9(expressionUnit.record.timing)?.duration_sec ?? recordField9(contentUnit?.record.model_intent)?.duration_sec),
        expression: shotExpressionText2(expressionUnit),
        stillPosition: stillPositionForIndex2(expressionUnitIndex),
        path: entityDir92(expressionUnit.path),
        keyframes: keyframes.map((keyframe) => idText2(keyframe)),
        assets: shotAssets2(expressionUnit, keyframes, input3.assets),
        storyboard: primaryStoryboard ? nodeId2(primaryStoryboard, "storyboard") : "",
        contentUnit: previewContentUnit2(contentUnit, expressionUnit, moment, primaryStoryboard, keyframes, input3)
      };
    }
    function orderedSceneMoments2(sceneMoments, previewTimelines) {
      const timelineMoments = previewTimelines.flatMap(
        (timeline) => timeline.items.filter((item) => item.itemType === "scene_moment").sort((left, right) => left.order - right.order).map((item) => entityForTimelineItem2(sceneMoments, item)).filter(isDefined42)
      );
      if (timelineMoments.length === 0) return sortEntities22(sceneMoments);
      return uniqueEntities2([...timelineMoments, ...sortEntities22(sceneMoments)]);
    }
    function orderedChildEntitiesForTimelineParent2(entities, previewTimelines, parentItemId, itemType) {
      return previewTimelines.flatMap(
        (timeline) => timeline.items.filter((item) => item.itemType === itemType && item.parentId === parentItemId).sort((left, right) => left.order - right.order).map((item) => entityForTimelineItem2(entities, item)).filter(isDefined42)
      );
    }
    function entityForTimelineItem2(entities, item) {
      return entities.find(
        (entity) => item.entity.id !== void 0 && String(entity.id ?? "") === String(item.entity.id) || entity.path === item.entity.path
      );
    }
    function uniqueEntities2(entities) {
      return Array.from(new Map(entities.map((entity) => [entity.path, entity])).values());
    }
    function timelineItemIdForEntity2(entity) {
      return `${entity.entityKind}:${String(entity.id ?? entity.path)}`;
    }
    function previewContentUnit2(contentUnit, expressionUnit, moment, storyboard, keyframes, input3) {
      const type = contentUnitType2(contentUnit);
      const id = contentUnit ? idText2(contentUnit) : `cu_${idText2(expressionUnit)}`;
      const selection = input3.selectionRecordsByContentUnitId.get(id);
      return {
        id,
        type,
        outputKind: outputKindForContentUnit2(contentUnit),
        path: contentUnit?.path ?? `content_units/${id}/content_unit.json`,
        editPrompt: editPromptText2(contentUnit) ?? "",
        sceneMomentRef: `scene_moment/${idText2(moment)}`,
        expressionUnitRef: idText2(expressionUnit),
        storyboardRef: storyboard ? nodeId2(storyboard, "storyboard") : "",
        keyframeRefs: keyframes.map((keyframe) => idText2(keyframe)),
        selectionState: input3.selectionByContentUnitId.get(id) ?? selectionStateFromSourceSelection2(selection, contentUnit),
        candidates: previewCandidatesForContentUnit2(id, input3.candidateRecordsByContentUnitId.get(id) ?? [], selection)
      };
    }
    function buildExpressionUnitsByMoment2(expressionUnits, ancestryIndex) {
      const output3 = {};
      for (const expression of sortEntities22(expressionUnits)) {
        const momentId = ancestorId2(ancestryIndex, expression, "scene_moment") ?? pathSegmentAfter62(expression.path, "scene_moments") ?? "";
        const item = {
          id: idText2(expression),
          title: titleOf2(expression, stringField21(expression.record.text) ?? idText2(expression)),
          path: expression.path,
          kind: stringField21(expression.record.slot_kind ?? expression.record.expression_kind ?? expression.record.kind) ?? "visual",
          slotKind: expressionUnitSlotKindFromRecord2(expression.record),
          text: stringField21(expression.record.text) ?? "",
          summary: stringField21(expression.record.intent ?? expression.record.note ?? expression.record.text) ?? "",
          speaker: stringField21(expression.record.speaker),
          note: stringField21(expression.record.note),
          sceneMomentId: momentId
        };
        output3[momentId] = [...output3[momentId] ?? [], item];
      }
      return output3;
    }
    function buildAudioCuesByMoment2(audioCues, input3) {
      const output3 = {};
      for (const audioCue of sortEntities22(audioCues)) {
        const momentId = ancestorId2(input3.ancestryIndex, audioCue, "scene_moment") ?? pathSegmentAfter62(audioCue.path, "scene_moments") ?? "";
        const contentUnit = contentUnitForEntity2(input3.contentUnitsByPrimaryRef, "audio_cue", audioCue);
        const item = {
          id: idText2(audioCue),
          title: titleOf2(audioCue, idText2(audioCue)),
          path: audioCue.path,
          cueKind: stringField21(audioCue.record.cue_kind ?? audioCue.record.kind) ?? "sound_effect",
          promptHint: stringField21(audioCue.record.prompt_hint) ?? "",
          expressionUnitRef: stringField21(audioCue.record.expression_unit_ref),
          storyboardRef: stringField21(audioCue.record.storyboard_ref),
          timing: recordField9(audioCue.record.timing) ?? {},
          assetRefs: arrayField82(audioCue.record.asset_refs).map(String),
          sceneMomentId: momentId,
          ...contentUnit ? { contentUnit: previewAudioCueContentUnit2(contentUnit, audioCue, input3) } : {}
        };
        output3[momentId] = [...output3[momentId] ?? [], item];
      }
      return output3;
    }
    function previewAudioCueContentUnit2(contentUnit, audioCue, input3) {
      const id = idText2(contentUnit);
      const selection = input3.selectionRecordsByContentUnitId.get(id);
      return {
        id,
        type: contentUnitType2(contentUnit),
        outputKind: outputKindForContentUnit2(contentUnit),
        path: contentUnit.path,
        editPrompt: editPromptText2(contentUnit) ?? "",
        sceneMomentRef: ancestorId2(input3.ancestryIndex, audioCue, "scene_moment") ?? pathSegmentAfter62(audioCue.path, "scene_moments") ?? "",
        expressionUnitRef: stringField21(audioCue.record.expression_unit_ref) ?? "",
        storyboardRef: stringField21(audioCue.record.storyboard_ref) ?? "",
        keyframeRefs: [],
        selectionState: input3.selectionByContentUnitId.get(id) ?? selectionStateFromSourceSelection2(selection, contentUnit),
        candidates: previewCandidatesForContentUnit2(id, input3.candidateRecordsByContentUnitId.get(id) ?? [], selection)
      };
    }
    function transitionFromEntity2(entity) {
      const transition = recordField9(entity.record.transition);
      if (!transition) return void 0;
      const value = {
        in: stringField21(transition.in),
        out: stringField21(transition.out),
        notes: stringField21(transition.notes)
      };
      return Object.values(value).some(Boolean) ? value : void 0;
    }
    function storyboardTimelineFromEntity2(entity) {
      const timeline = recordField9(entity.record.timeline);
      if (!timeline) return void 0;
      const value = {
        caption: stringField21(timeline.caption),
        gapAfterSec: optionalNumberField2(timeline.gap_after_sec),
        durationSec: optionalNumberField2(timeline.duration_sec)
      };
      return Object.values(value).some((item) => item !== void 0) ? value : void 0;
    }
    function buildExpressionUnitWorkspaceDetails2(input3) {
      return Object.fromEntries(input3.expressionUnits.map((expressionUnit) => {
        const keyframes = sortEntities22(childEntitiesForParent2(input3.ancestryIndex, input3.keyframes, expressionUnit));
        const storyboards = sortEntities22(childEntitiesForParent2(input3.ancestryIndex, input3.storyboards, expressionUnit));
        const refs = shotAssets2(expressionUnit, keyframes, input3.assets);
        const assets = refs.map((ref) => ({
          id: ref.title,
          title: ref.title.replace(/^asset\//, ""),
          owner: ref.title,
          status: ref.status === "missing" ? "missing" : ref.status === "locked" ? "locked" : "current",
          summary: ref.status === "missing" ? "\u8BE5\u7D20\u6750\u5F15\u7528\u5C1A\u672A\u5728 setting/state asset \u4E2D\u89E3\u6790\u3002" : "\u6765\u81EA workspace source \u7684\u7D20\u6750\u5F15\u7528\u3002",
          downstream: [idText2(expressionUnit)]
        }));
        const settings = input3.settings.slice(0, 4).map((setting) => ({
          id: nodeId2(setting, "setting"),
          title: titleOf2(setting, idText2(setting)),
          owner: setting.path,
          status: "current",
          summary: stringField21(setting.record.summary ?? setting.record.description ?? setting.record.prompt_hint) ?? "Workspace setting context.",
          downstream: [idText2(expressionUnit)]
        }));
        return [idText2(expressionUnit), {
          settings,
          assets,
          keyframes: keyframes.map((keyframe) => expressionUnitChildOption2(keyframe, "keyframe", expressionUnit, input3)),
          storyboards: storyboards.map((storyboard) => expressionUnitChildOption2(storyboard, "storyboard", expressionUnit, input3)),
          impacts: []
        }];
      }));
    }
    function buildAssetReferenceUnits2(input3) {
      return Object.fromEntries(input3.assets.map((asset) => {
        const contentUnit = contentUnitForEntity2(input3.contentUnitsByPrimaryRef, "asset", asset);
        const contentUnitId = contentUnit ? idText2(contentUnit) : `cu_${idText2(asset)}`;
        const ownerState = ancestorEntity2(input3.ancestryIndex, asset, "setting_state") ?? parentByDir2(input3.settingStates, asset.path);
        const ownerSetting = ownerState ? ancestorEntity2(input3.ancestryIndex, ownerState, "setting") ?? parentByDir2(input3.settings, ownerState.path) : ancestorEntity2(input3.ancestryIndex, asset, "setting");
        const assetId = nodeId2(asset, "asset");
        const selection = input3.selectionRecordsByContentUnitId.get(contentUnitId);
        const selectionState = contentUnit ? input3.selectionByContentUnitId.get(contentUnitId) ?? "needs_candidate" : "ready";
        return [assetId, {
          assetId,
          title: titleOf2(asset, idText2(asset)),
          path: contentUnit?.path ?? `content_units/${contentUnitId}/content_unit.json`,
          contentUnitId,
          contentUnitType: "asset_ref",
          outputKind: "image",
          editPrompt: editPromptText2(contentUnit) ?? "",
          usage: `${titleOf2(asset, idText2(asset))} \u4F5C\u4E3A setting/state \u4E0B\u7684\u7D20\u6750\u53C2\u8003\u8F93\u5165\u3002`,
          lockPolicy: "\u9009\u62E9\u53D8\u5316\u540E\uFF0C\u4E0B\u6E38\u5F15\u7528\u8BE5 asset_ref \u7684\u521B\u4F5C\u7247\u6BB5\u9700\u8981\u91CD\u65B0\u68C0\u67E5\u3002",
          selectionState,
          upstream: [
            ...ownerSetting ? [{ id: `setting:${idText2(ownerSetting)}`, title: titleOf2(ownerSetting, idText2(ownerSetting)), kind: "setting", ownerNodeId: nodeId2(ownerSetting, "setting"), state: "current", summary: ownerSetting.path }] : [],
            ...ownerState ? [{ id: `state:${idText2(ownerState)}`, title: titleOf2(ownerState, idText2(ownerState)), kind: "state", ownerNodeId: nodeId2(ownerState, "state"), state: "current", summary: ownerState.path }] : []
          ],
          candidates: previewAssetCandidatesForContentUnit2(contentUnitId, input3.candidateRecordsByContentUnitId.get(contentUnitId) ?? [], selection),
          downstream: buildAssetDownstreamUnits2(asset, input3)
        }];
      }));
    }
    function buildAssetDownstreamUnits2(asset, input3) {
      const assetId = idText2(asset);
      const refs = /* @__PURE__ */ new Set([assetId, nodeId2(asset, "asset")]);
      return input3.contentUnits.filter((contentUnit) => {
        const promptRefs = editPromptRefs2(contentUnit);
        const referencesAsset = promptRefs.some((ref) => ref.kind === "asset" && refs.has(ref.id));
        const isOwnAssetRef = stringField21(contentUnit.record.content_unit_type) === "asset_ref" && primaryRefIdsForContentUnitRecord32(contentUnit.record, "asset").some((ref) => refs.has(ref));
        return referencesAsset && !isOwnAssetRef;
      }).map((contentUnit) => {
        const contentUnitId = idText2(contentUnit);
        const owner = primaryOwnerForContentUnit2(contentUnit, input3);
        const selection = input3.selectionRecordsByContentUnitId.get(contentUnitId);
        const selectedCandidateId = idValue22(selection?.candidate_id);
        const selectedCandidate = selectedCandidateId ? input3.candidateRecordsByContentUnitId.get(contentUnitId)?.find((candidate) => selectionCandidateMatches2(selection, idValue22(candidate.id) ?? "")) : void 0;
        const state = input3.selectionByContentUnitId.get(contentUnitId) ?? selectionStateFromSourceSelection2(selection, contentUnit);
        return {
          id: `asset:${assetId}:content_unit:${contentUnitId}`,
          title: titleOf2(contentUnit, contentUnitId),
          kind: "content_unit",
          ownerNodeId: owner?.nodeId ?? contentUnitId,
          momentId: owner?.momentId ?? "",
          expressionUnitId: owner?.expressionUnitId ?? "",
          dependencyHash: selectedCandidate ? candidateInputHash2(selectedCandidate, contentUnitId) : contentUnitId,
          state,
          action: state === "selected" ? "\u5DF2\u9009\u62E9\u5019\u9009\u5F15\u7528\u8BE5 asset" : "\u9700\u8981\u5019\u9009\u6216\u9009\u62E9\u786E\u8BA4",
          preview: `${contentUnitId} \u5728 edit_prompt \u4E2D\u5F15\u7528 ${nodeId2(asset, "asset")}\u3002`
        };
      });
    }
    function primaryOwnerForContentUnit2(contentUnit, input3) {
      const expressionUnitRef = primaryRefIdsForContentUnitRecord32(contentUnit.record, "expression_unit")[0];
      if (expressionUnitRef) {
        const expressionUnit = input3.expressionUnits.find((item) => entityMatchesRef2(item, expressionUnitRef, "expression_unit"));
        return {
          nodeId: expressionUnit ? idText2(expressionUnit) : expressionUnitRef,
          momentId: expressionUnit ? ancestorId2(input3.ancestryIndex, expressionUnit, "scene_moment") ?? pathSegmentAfter62(expressionUnit.path, "scene_moments") ?? "" : "",
          expressionUnitId: expressionUnit ? idText2(expressionUnit) : expressionUnitRef
        };
      }
      const storyboardRef = primaryRefIdsForContentUnitRecord32(contentUnit.record, "storyboard")[0];
      if (storyboardRef) {
        const storyboard = input3.storyboards.find((item) => entityMatchesRef2(item, storyboardRef, "storyboard"));
        return {
          nodeId: storyboard ? nodeId2(storyboard, "storyboard") : `storyboard/${storyboardRef}`,
          momentId: storyboard ? ancestorId2(input3.ancestryIndex, storyboard, "scene_moment") ?? pathSegmentAfter62(storyboard.path, "scene_moments") ?? "" : "",
          expressionUnitId: storyboard ? ancestorId2(input3.ancestryIndex, storyboard, "expression_unit") ?? pathSegmentAfter62(storyboard.path, "expression_units") ?? "" : ""
        };
      }
      const keyframeRef = primaryRefIdsForContentUnitRecord32(contentUnit.record, "keyframe")[0];
      if (keyframeRef) {
        const keyframe = input3.keyframes.find((item) => entityMatchesRef2(item, keyframeRef, "keyframe"));
        return {
          nodeId: keyframe ? idText2(keyframe) : keyframeRef,
          momentId: keyframe ? ancestorId2(input3.ancestryIndex, keyframe, "scene_moment") ?? pathSegmentAfter62(keyframe.path, "scene_moments") ?? "" : "",
          expressionUnitId: keyframe ? ancestorId2(input3.ancestryIndex, keyframe, "expression_unit") ?? pathSegmentAfter62(keyframe.path, "expression_units") ?? "" : ""
        };
      }
      return void 0;
    }
    function expressionUnitChildOption2(entity, primaryKind, expressionUnit, input3) {
      const contentUnit = contentUnitForEntity2(input3.contentUnitsByPrimaryRef, primaryKind, entity);
      const contentUnitId = contentUnit ? idText2(contentUnit) : "";
      const selection = contentUnitId ? input3.selectionRecordsByContentUnitId.get(contentUnitId) : void 0;
      return {
        id: idText2(entity),
        title: titleOf2(entity, idText2(entity)),
        status: contentUnit ? "candidate" : "draft",
        inputHash: contentUnitId || "source",
        summary: stringField21(entity.record.visual_intent ?? entity.record.summary ?? entity.record.description ?? entity.record.slot) ?? entity.path,
        ...contentUnit ? {
          contentUnit: {
            id: contentUnitId,
            type: primaryKind === "keyframe" ? "keyframe_ref" : "storyboard_ref",
            outputKind: outputKindForContentUnit2(contentUnit),
            path: contentUnit.path,
            editPrompt: editPromptText2(contentUnit) ?? "",
            sceneMomentRef: `scene_moment/${ancestorId2(input3.ancestryIndex, expressionUnit, "scene_moment") ?? pathSegmentAfter62(expressionUnit.path, "scene_moments") ?? ""}`,
            expressionUnitRef: idText2(expressionUnit),
            storyboardRef: primaryKind === "storyboard" ? nodeId2(entity, "storyboard") : "",
            keyframeRefs: primaryKind === "keyframe" ? [idText2(entity)] : [],
            selectionState: input3.selectionByContentUnitId.get(contentUnitId) ?? selectionStateFromSourceSelection2(selection, contentUnit),
            candidates: previewCandidatesForContentUnit2(contentUnitId, input3.candidateRecordsByContentUnitId.get(contentUnitId) ?? [], selection)
          }
        } : {}
      };
    }
    function buildContentUnitCandidates2(input3) {
      const ids = /* @__PURE__ */ new Set([
        ...input3.contentUnits.map(idText2),
        ...input3.candidateRecordsByContentUnitId.keys(),
        ...input3.selectionRecordsByContentUnitId.keys()
      ]);
      const output3 = {};
      for (const contentUnitId of ids) {
        output3[contentUnitId] = previewCandidatesForContentUnit2(
          contentUnitId,
          input3.candidateRecordsByContentUnitId.get(contentUnitId) ?? [],
          input3.selectionRecordsByContentUnitId.get(contentUnitId)
        );
      }
      return output3;
    }
    function groupContentUnitsByPrimaryRef22(contentUnits) {
      const output3 = /* @__PURE__ */ new Map();
      for (const contentUnit of contentUnits) {
        const assemblyScope = timelineAssemblyScopeFromContentUnitRecord2(contentUnit.record);
        if (assemblyScope) {
          for (const key of primaryRefKeys22(assemblyScope.kind, assemblyScope.ref)) {
            output3.set(key, [...output3.get(key) ?? [], contentUnit]);
          }
        }
        const type = stringField21(contentUnit.record.content_unit_type);
        const primaryKind = primaryKindForContentUnitType2(type);
        if (!primaryKind) continue;
        for (const ref of primaryRefIdsForContentUnitRecord32(contentUnit.record, primaryKind)) {
          for (const key of primaryRefKeys22(primaryKind, ref)) {
            output3.set(key, [...output3.get(key) ?? [], contentUnit]);
          }
        }
      }
      return output3;
    }
    function groupContentCandidateRecordsByContentUnitId2(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (document.path.endsWith("/content_candidate.json") && isContentCandidateRecord2(document.data)) {
          const contentUnitId2 = contentUnitIdForRuntimeDocument6(document.path, document.data.content_unit_ref);
          if (!contentUnitId2) continue;
          appendContentCandidateRecord3(output3, contentUnitId2, document.data);
          continue;
        }
        if (!isDecisionContextRecord2(document.data)) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument6(document.path, stringField21(document.data.target_ref));
        if (!contentUnitId) continue;
        for (const candidate of arrayField82(document.data.candidates)) {
          const record = normalizeDecisionContentCandidateRecord3(candidate, contentUnitId);
          if (record) appendContentCandidateRecord3(output3, contentUnitId, record);
        }
      }
      for (const [contentUnitId, candidates] of output3.entries()) {
        output3.set(contentUnitId, candidates.sort((left, right) => (stringField21(right.created_at) ?? "").localeCompare(stringField21(left.created_at) ?? "")));
      }
      return output3;
    }
    function appendContentCandidateRecord3(output3, contentUnitId, candidate) {
      const candidates = output3.get(contentUnitId) ?? [];
      const candidateId = idValue22(candidate.id);
      if (!candidateId) {
        output3.set(contentUnitId, [...candidates, candidate]);
        return;
      }
      const existingIndex = candidates.findIndex((existing) => idValue22(existing.id) === candidateId);
      if (existingIndex < 0) {
        output3.set(contentUnitId, [...candidates, candidate]);
        return;
      }
      output3.set(contentUnitId, [
        ...candidates.slice(0, existingIndex),
        mergeContentCandidateRecords3(candidates[existingIndex], candidate),
        ...candidates.slice(existingIndex + 1)
      ]);
    }
    function normalizeDecisionContentCandidateRecord3(value, contentUnitId) {
      if (!isRecord352(value) || !idValue22(value.id)) return void 0;
      return {
        ...value,
        content_unit_ref: stringField21(value.content_unit_ref) ?? `content_units/${contentUnitId}`
      };
    }
    function mergeContentCandidateRecords3(existing, incoming) {
      const merged = { ...existing, ...incoming };
      if (arrayField82(incoming.outputs).length === 0 && arrayField82(existing.outputs).length > 0) merged.outputs = existing.outputs;
      if (!recordField9(incoming.producer) && recordField9(existing.producer)) merged.producer = existing.producer;
      if (!recordField9(incoming.prompt_snapshot) && recordField9(existing.prompt_snapshot)) merged.prompt_snapshot = existing.prompt_snapshot;
      if (!stringField21(incoming.status) && stringField21(existing.status)) merged.status = existing.status;
      if (!stringField21(incoming.source) && stringField21(existing.source)) merged.source = existing.source;
      if (!stringField21(incoming.created_at) && stringField21(existing.created_at)) merged.created_at = existing.created_at;
      if (!stringField21(incoming.content_unit_ref) && stringField21(existing.content_unit_ref)) merged.content_unit_ref = existing.content_unit_ref;
      return merged;
    }
    function groupSelectionRecordsByContentUnitId2(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (!isDecisionContextRecord2(document.data)) continue;
        const selection = recordField9(document.data.selection);
        if (!selection) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument6(document.path, stringField21(document.data.target_ref));
        if (!contentUnitId) continue;
        output3.set(contentUnitId, normalizeContentSelectionRecord2(selection));
      }
      return output3;
    }
    function previewCandidatesForContentUnit2(contentUnitId, candidates, selection) {
      return candidates.map((candidate, index) => {
        const id = idValue22(candidate.id) ?? `candidate_${index + 1}`;
        const output3 = firstCandidateOutput6(candidate);
        return {
          id,
          title: candidateTitle2(candidate, candidateOrdinalTitle2(index)),
          model: candidateModel2(candidate),
          inputHash: candidateInputHash2(candidate, contentUnitId),
          selected: selectionCandidateMatches2(selection, id),
          note: candidateNote2(candidate),
          resourceId: resourceIdValue22(output3?.resource_id),
          resourceKind: stringField21(output3?.kind),
          artifactRef: stringField21(output3?.artifact_ref),
          status: stringField21(candidate.status),
          decisionStatus: candidateDecisionStatus2(candidate),
          decisionReason: candidateDecisionReason2(candidate),
          source: stringField21(candidate.source),
          producer: candidate.producer,
          outputs: candidate.outputs,
          promptSnapshot: candidate.prompt_snapshot,
          createdAt: stringField21(candidate.created_at)
        };
      });
    }
    function previewAssetCandidatesForContentUnit2(contentUnitId, candidates, selection) {
      return candidates.map((candidate, index) => {
        const id = idValue22(candidate.id) ?? `candidate_${index + 1}`;
        const output3 = firstCandidateOutput6(candidate);
        return {
          id,
          title: candidateTitle2(candidate, candidateOrdinalTitle2(index)),
          model: candidateModel2(candidate),
          inputHash: candidateInputHash2(candidate, contentUnitId),
          selected: selectionCandidateMatches2(selection, id),
          note: candidateNote2(candidate),
          resourceId: resourceIdValue22(output3?.resource_id),
          resourceKind: stringField21(output3?.kind),
          artifactRef: stringField21(output3?.artifact_ref),
          status: stringField21(candidate.status),
          decisionStatus: candidateDecisionStatus2(candidate),
          decisionReason: candidateDecisionReason2(candidate),
          source: stringField21(candidate.source),
          producer: candidate.producer,
          outputs: candidate.outputs,
          promptSnapshot: candidate.prompt_snapshot,
          createdAt: stringField21(candidate.created_at),
          confirmation: assetCandidateConfirmation2(candidate, selection, id)
        };
      });
    }
    function selectionStateFromSourceSelection2(selection, contentUnit) {
      if (selection?.candidate_id !== void 0) return "selected";
      return contentUnit ? "needs_candidate" : "ready";
    }
    function buildSelectionStateByContentUnitId2(contentUnits, selections) {
      const entries = contentUnits.map((contentUnit) => {
        const id = idText2(contentUnit);
        const selection = selections.get(id);
        if (selection?.candidate_id !== void 0) return [id, "selected"];
        return [id, "needs_candidate"];
      });
      return new Map(entries);
    }
    function contentUnitForEntity2(contentUnitsByPrimaryRef, entityKind, entity) {
      if (entity.id === void 0) return void 0;
      return contentUnitsByPrimaryRef.get(primaryRefKey22(entityKind, entity.id))?.[0];
    }
    function primaryKindForContentUnitType2(type) {
      return type ? primaryRefKindForContentUnitType4(type) : void 0;
    }
    function primaryRefIdsForContentUnitRecord32(record, kind) {
      return isContentUnitPromptRefKind2(kind) ? primaryRefIdsForContentUnitRecord4(record, kind) : [];
    }
    function editPromptRefs2(contentUnit) {
      const text = editPromptText2(contentUnit) ?? "";
      const refs = [];
      const pattern = /\{\{\s*([a-z_]+)\s*:\s*([^}\s]+)\s*\}\}/g;
      let match = pattern.exec(text);
      while (match) {
        refs.push({ kind: match[1] ?? "", id: match[2] ?? "" });
        match = pattern.exec(text);
      }
      return refs;
    }
    function editPromptText2(contentUnit) {
      const prompt = contentUnit?.record.edit_prompt;
      if (typeof prompt === "string") return prompt;
      if (isRecord352(prompt)) return stringField21(prompt.text);
      return void 0;
    }
    function entityNode2(entity, type, extras = {}) {
      return {
        id: nodeId2(entity, type),
        type,
        title: titleOf2(entity, idText2(entity)),
        path: entity.path,
        transition: supportsTransition2(type) ? transitionFromEntity2(entity) : void 0,
        storyboardTimeline: type === "storyboard" ? storyboardTimelineFromEntity2(entity) : void 0,
        ...extras
      };
    }
    function supportsTransition2(type) {
      return type === "production" || type === "segment" || type === "scene_moment" || type === "storyboard";
    }
    function nodeId2(entity, type) {
      if (type === "setting") return `setting/${idText2(entity)}`;
      if (type === "state") return `state/${pathSegmentAfter62(entity.path, "settings") ?? ""}/${idText2(entity)}`;
      if (type === "asset") return `asset/${idText2(entity)}`;
      if (type === "storyboard") return `storyboard/${idText2(entity)}`;
      return idText2(entity);
    }
    function titleOf2(entity, fallback) {
      return stringField21(entity.record.title ?? entity.record.name ?? entity.record.label ?? entity.record.text) ?? fallback;
    }
    function idText2(entity) {
      return String(entity.id ?? entity.record.id ?? entity.record.ID ?? entity.path);
    }
    function entityMatchesRef2(entity, ref, kind) {
      const normalized = ref.replace(/\/+$/, "");
      const dir = entity.path.replace(/\/[^/]+$/, "");
      return String(entity.id ?? "") === ref || dir === normalized || entity.path === `${normalized}/${kind}.json`;
    }
    function sortEntities22(entities) {
      return [...entities].sort((left, right) => numberField62(left.record.order) - numberField62(right.record.order) || left.path.localeCompare(right.path));
    }
    function childEntitiesForParent2(ancestryIndex, entities, parent) {
      const directChildren = new Set((ancestryIndex.childrenByParentPath.get(parent.path) ?? []).map((entity) => entity.path));
      return entities.filter((entity) => directChildren.has(entity.path));
    }
    function ancestorEntity2(ancestryIndex, entity, entityKind) {
      const seen = /* @__PURE__ */ new Set([entity.path]);
      let currentPath = entity.path;
      while (true) {
        const parentPath = ancestryIndex.parentPathByPath.get(currentPath);
        if (!parentPath || seen.has(parentPath)) return void 0;
        seen.add(parentPath);
        const parent = ancestryIndex.entityByPath.get(parentPath);
        if (!parent) return void 0;
        if (parent.entityKind === entityKind) return parent;
        currentPath = parent.path;
      }
    }
    function ancestorId2(ancestryIndex, entity, entityKind) {
      const ancestor = ancestorEntity2(ancestryIndex, entity, entityKind);
      return ancestor ? idText2(ancestor) : void 0;
    }
    function parentByDir2(entities, childPath) {
      const childDir = entityDir92(childPath);
      return entities.find((entity) => childDir.startsWith(`${entityDir92(entity.path)}/`));
    }
    function entityDir92(path) {
      return path.replace(/\/[^/]+$/, "");
    }
    function pathSegmentAfter62(path, segment) {
      const parts = path.split("/");
      const index = parts.indexOf(segment);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function primaryRefKey22(kind, id) {
      return `${kind}:${String(id ?? "")}`;
    }
    function primaryRefKeys22(kind, ref) {
      const value = String(ref);
      const keys = [primaryRefKey22(kind, value)];
      const lastSegment = value.split("/").filter(Boolean).at(-1);
      if (lastSegment && lastSegment !== value) keys.push(primaryRefKey22(kind, lastSegment));
      return keys;
    }
    function shotCameraText2(shot) {
      return [
        stringField21(shot.record.shot_size),
        stringField21(recordField9(shot.record.camera)?.movement),
        stringField21(recordField9(shot.record.camera)?.angle)
      ].filter(Boolean).join(" \xB7 ") || stringField21(shot.record.shot_kind) || "shot";
    }
    function shotExpressionText2(shot) {
      return stringField21(shot.record.description ?? shot.record.summary ?? recordField9(shot.record.expression)?.text ?? recordField9(shot.record.expression)?.intent) ?? titleOf2(shot, idText2(shot));
    }
    function durationText2(value) {
      const duration = Number(value);
      return Number.isFinite(duration) && duration > 0 ? `${duration}s` : "\u5F85\u5B9A";
    }
    function stillPositionForIndex2(index) {
      return ["0% 0%", "100% 0%", "0% 100%", "100% 100%"][index % 4] ?? "0% 0%";
    }
    function contentUnitType2(contentUnit) {
      const type = stringField21(contentUnit?.record.content_unit_type);
      if (type === "keyframe_ref" || type === "storyboard_ref" || type === "audio_cue_ref" || type === "scence_moment_ref" || type === "scene_moment_ref" || type === "expression_unit_ref") return type;
      return "expression_unit_ref";
    }
    function outputKindForContentUnit2(contentUnit) {
      const outputKind = stringField21(contentUnit?.record.output_kind);
      if (outputKind === "image" || outputKind === "video" || outputKind === "audio" || outputKind === "text" || outputKind === "metadata" || outputKind === "storyboard") return outputKind;
      const type = contentUnitType2(contentUnit);
      if (type === "keyframe_ref" || type === "storyboard_ref") return "image";
      if (type === "audio_cue_ref") return "audio";
      return "video";
    }
    function momentSelectionState2(expressionUnits) {
      if (expressionUnits.some((expressionUnit) => expressionUnit.contentUnit.selectionState === "stale")) return "stale";
      if (expressionUnits.some((expressionUnit) => expressionUnit.contentUnit.selectionState === "needs_candidate")) return "needs_candidate";
      if (expressionUnits.some((expressionUnit) => expressionUnit.contentUnit.selectionState === "selected")) return "selected";
      return "ready";
    }
    function settingRefsForMoment2(_moment, expressionUnits, assets) {
      const refs = /* @__PURE__ */ new Set();
      for (const expressionUnit of expressionUnits) {
        for (const asset of expressionUnit.assets) refs.add(asset.title);
      }
      if (refs.size === 0) {
        for (const asset of assets.slice(0, 3)) refs.add(nodeId2(asset, "asset"));
      }
      return [...refs];
    }
    function shotAssets2(shot, keyframes, assets) {
      const refs = [
        ...arrayField82(shot.record.reference_asset_refs),
        ...keyframes.flatMap((keyframe) => arrayField82(keyframe.record.reference_asset_refs))
      ].map(String);
      const uniqueRefs = [...new Set(refs)];
      return uniqueRefs.map((ref) => ({
        title: ref.startsWith("asset/") ? ref : `asset/${ref}`,
        status: assets.some((asset) => String(asset.id ?? "") === ref || entityDir92(asset.path) === ref || asset.path.startsWith(`${ref}/`)) ? "ready" : "missing"
      }));
    }
    function contentUnitIdForRuntimeDocument6(path, explicitRef) {
      const ref = explicitRef ?? entityDir92(path);
      const id = pathSegmentAfter62(ref, "content_units");
      return id;
    }
    function candidateTitle2(candidate, fallback) {
      const explicitTitle = stringField21(candidate.prompt_snapshot?.title) ?? stringField21(candidate.producer?.title) ?? stringField21(candidate.producer?.name);
      if (explicitTitle && !candidateTitleIsGeneric2(explicitTitle)) return explicitTitle;
      return readableCandidateTitleFallback2(fallback);
    }
    function candidateOrdinalTitle2(index) {
      return `\u5019\u9009 ${index + 1}`;
    }
    function candidateTitleIsGeneric2(value) {
      const normalized = value.trim().toLowerCase();
      return normalized === "queued generation" || normalized === "pending generation" || normalized === "content unit image generation" || normalized === "content unit video generation" || technicalCandidateIdPattern2.test(normalized);
    }
    function readableCandidateTitleFallback2(value) {
      return technicalCandidateIdPattern2.test(value.trim().toLowerCase()) ? "\u5019\u9009" : value;
    }
    var technicalCandidateIdPattern2 = /^(canvas|resource|content)_candidate_[\w-]+$|^resource_\d+_[\w-]+$|^gen_(image|video)_\d+_\d+$/;
    function candidateModel2(candidate) {
      return stringField21(candidate.producer?.model_id) ?? stringField21(candidate.producer?.model) ?? stringField21(candidate.producer?.kind) ?? stringField21(candidate.source) ?? "runtime";
    }
    function candidateInputHash2(candidate, contentUnitId) {
      return stringField21(candidate.prompt_snapshot?.input_hash) ?? stringField21(candidate.prompt_snapshot?.content_hash) ?? stringField21(candidate.prompt_snapshot?.hash) ?? stringField21(candidate.created_at) ?? contentUnitId;
    }
    function candidateNote2(candidate) {
      const output3 = firstCandidateOutput6(candidate);
      return stringField21(candidate.prompt_snapshot?.note) ?? stringField21(candidate.prompt_snapshot?.summary) ?? stringField21(output3?.mime_type) ?? stringField21(candidate.status) ?? "Workspace runtime candidate.";
    }
    function candidateDecisionStatus2(candidate) {
      const record = candidate;
      return stringField21(record.decision_status) ?? stringField21(record.decisionStatus);
    }
    function candidateDecisionReason2(candidate) {
      const record = candidate;
      return stringField21(record.decision_reason) ?? stringField21(record.decisionReason);
    }
    function firstCandidateOutput6(candidate) {
      return arrayField82(candidate.outputs).filter(isRecord352)[0];
    }
    function assetCandidateConfirmation2(candidate, selection, candidateId) {
      if (selectionCandidateMatches2(selection, candidateId)) return "confirmed";
      if (candidate.status === "failed" || candidate.status === "canceled") return "stale";
      return "review";
    }
    function selectionCandidateMatches2(selection, candidateId) {
      return selection?.candidate_id !== void 0 && String(selection.candidate_id) === candidateId;
    }
    function idValue22(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function recordField9(value) {
      return isRecord352(value) ? value : void 0;
    }
    function stringField21(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberField62(value) {
      const numberValue232 = Number(value);
      return Number.isFinite(numberValue232) ? numberValue232 : Number.MAX_SAFE_INTEGER;
    }
    function optionalNumberField2(value) {
      const numberValue232 = Number(value);
      return Number.isFinite(numberValue232) ? numberValue232 : void 0;
    }
    function pruneUndefinedRecord4(record) {
      const output3 = {};
      for (const [key, value] of Object.entries(record)) {
        if (value !== void 0) output3[key] = value;
      }
      return output3;
    }
    function arrayField82(value) {
      return Array.isArray(value) ? value : [];
    }
    function isRecord352(value) {
      return Boolean(value) && typeof value === "object" && !Array.isArray(value);
    }
    function isContentCandidateRecord2(value) {
      return isRecord352(value) && value.schema === "movscript.content_candidate.v1";
    }
    function isDecisionContextRecord2(value) {
      return isRecord352(value) && value.schema === "movscript.decision_context.v1" && value.target_kind === "content_unit";
    }
    function normalizeContentSelectionRecord2(value) {
      return {
        candidate_id: value.candidate_id,
        resource_id: resourceIdValue22(value.resource_id),
        artifact_ref: stringField21(value.artifact_ref),
        stale_policy: value.stale_policy,
        reason: value.reason,
        selected_at: value.selected_at
      };
    }
    function resourceIdValue22(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        if (Number.isInteger(parsed) && parsed > 0) return parsed;
      }
      return void 0;
    }
    function isDefined42(value) {
      return value !== void 0;
    }
    function buildContentSourceWorkspaceProjectTimelineStatus2(snapshot, contentUnitSummaries = contentSourceWorkspaceContentUnitStatusSummaries2(snapshot)) {
      const namespaceVocabulary = snapshot.namespaceVocabulary ?? {
        timelineTemplate: void 0,
        timelineNamespaces: [],
        settingNamespaces: [],
        diagnostics: []
      };
      const timelineNamespaceNodes = (snapshot.domainNodes ?? []).filter((node) => node.category === "timeline_namespace").sort((left, right) => (left.order ?? 0) - (right.order ?? 0) || String(left.path ?? left.id ?? "").localeCompare(String(right.path ?? right.id ?? ""), "zh-CN"));
      const timelineNamespaces = timelineNamespaceNodes.map((node) => {
        const parent = domainParentRefForNode2(snapshot, node);
        const record = domainRecordForNode2(snapshot, node);
        const productionType = stringField232(record?.production_type ?? record?.productionType ?? record?.timeline_profile ?? record?.timelineProfile);
        const nodeTimelineNamespaces = stringArrayField2(record?.timeline_namespaces ?? record?.timelineNamespaces);
        return {
          id: node.id,
          kind: node.kind,
          title: node.title,
          path: node.path,
          order: node.order,
          entity_kind: stringField232(node.metadata?.entityKind),
          ...productionType ? { production_type: productionType } : {},
          ...nodeTimelineNamespaces.length ? { timeline_namespaces: nodeTimelineNamespaces } : {},
          ...parent ? { parent } : {}
        };
      });
      const timelineAssemblies = timelineAssemblyStatusEntries2(snapshot, contentUnitSummaries);
      const hasBlockingRefs = timelineAssemblies.some((item) => arrayValue5(item.blocking_refs).length > 0);
      const hasStaleSelection = timelineAssemblies.some((item) => {
        const staleStatus = stringField232(item.stale_status);
        return staleStatus === "has_stale_selection" || staleStatus === "stale";
      });
      return {
        schema: "movscript.project_timeline_status.v1",
        status: hasBlockingRefs ? "blocked" : hasStaleSelection ? "needs_review" : "ok",
        namespace_vocabulary: {
          timeline_template: namespaceVocabulary.timelineTemplate,
          timeline_namespaces: namespaceVocabulary.timelineNamespaces,
          setting_namespaces: namespaceVocabulary.settingNamespaces,
          diagnostics: namespaceVocabulary.diagnostics
        },
        timeline_namespace_count: timelineNamespaces.length,
        timeline_namespaces: timelineNamespaces,
        timeline_assembly_count: timelineAssemblies.length,
        timeline_assemblies: timelineAssemblies,
        system_primitives: {
          scene_moments_count: snapshot.sceneMoments.length,
          expression_units_count: snapshot.expressionUnits.length,
          storyboards_count: snapshot.storyboards.length,
          keyframes_count: snapshot.keyframes.length,
          audio_cues_count: snapshot.audioCues.length,
          assets_count: snapshot.assets.length
        },
        legacy_aliases: {
          productions: "timeline_namespaces",
          segments: "timeline_namespaces",
          production_ref: "timeline_assembly_ref",
          segment_ref: "timeline_assembly_ref"
        }
      };
    }
    function contentSourceWorkspaceContentUnitStatusSummaries2(snapshot) {
      const candidatesByContentUnit = contentCandidateRecordsByContentUnitId5(snapshot.indexDocuments);
      const selectionsByContentUnit = selectionRecordsByContentUnitId5(snapshot.indexDocuments);
      return snapshot.contentUnits.map((unit) => summarizeContentUnitStatus22(unit, candidatesByContentUnit, selectionsByContentUnit));
    }
    function timelineAssemblyStatusEntries2(snapshot, contentUnitSummaries) {
      const contentUnitById = new Map(snapshot.contentUnits.map((unit) => [String(entityId2(unit)), unit]));
      return contentUnitSummaries.filter((summary) => isTimelineAssemblyContentUnitSummary2(summary)).map((summary) => {
        const contentUnitId = idField142(summary.content_unit_id);
        const unit = contentUnitById.get(String(contentUnitId));
        const scope = unit ? timelineAssemblyScopeRefForContentUnit2(snapshot, unit) : void 0;
        return {
          assembly_ref: summary.target_ref,
          content_unit_id: contentUnitId,
          title: summary.title,
          path: summary.path,
          content_unit_type: summary.content_unit_type,
          target_kind: "timeline_assembly",
          target_ref: summary.target_ref,
          output_kind: summary.output_kind,
          candidate_count: summary.candidate_count,
          candidate_ids: summary.candidate_ids,
          selected_candidate: summary.selected_candidate,
          selected_resource: summary.selected_resource,
          stale_status: summary.stale_status,
          blocking_refs: summary.blocking_refs,
          ...scope ? { scope } : {}
        };
      });
    }
    function summarizeContentUnitStatus22(unit, candidatesByContentUnit, selectionsByContentUnit) {
      const id = entityId2(unit);
      const contentUnitId = String(id);
      const candidates = candidatesByContentUnit.get(contentUnitId) ?? [];
      const selection = selectionsByContentUnit.get(contentUnitId);
      const selectedCandidate = selection?.candidate_id !== void 0 ? candidates.find((candidate) => sameId42(candidate.id, selection.candidate_id)) : void 0;
      const selectedResourceId22 = numberField222(selection?.resource_id) ?? numberField222(firstCandidateOutput22(selectedCandidate)?.resource_id);
      return {
        content_unit_id: id,
        title: stringField232(unit.record.title),
        path: unit.path,
        content_unit_type: stringField232(unit.record.content_unit_type),
        output_kind: stringField232(unit.record.output_kind),
        target_kind: stringField232(unit.record.target_kind),
        target_ref: idField142(unit.record.target_ref),
        candidate_count: candidates.length,
        candidate_ids: candidates.map((candidate) => idField142(candidate.id)).filter((value) => value !== void 0),
        selected_candidate: selection?.candidate_id,
        selected_resource: selectedResourceId22,
        stale_status: stringField232(selection?.stale_policy) === "accept_stale" ? "accepted_stale" : "ok",
        blocking_refs: selection?.candidate_id === void 0 && candidates.length > 0 ? ["selection_missing"] : []
      };
    }
    function isTimelineAssemblyContentUnitSummary2(summary) {
      const type = stringField232(summary.content_unit_type);
      const targetKind = stringField232(summary.target_kind);
      const targetRef = stringField232(summary.target_ref);
      return type === "timeline_assembly_ref" || targetKind === "timeline_assembly" || Boolean(targetRef?.startsWith("timeline_assembly:"));
    }
    function timelineAssemblyScopeRefForContentUnit2(snapshot, unit) {
      const edge = (snapshot.domainEdges ?? []).find((candidate) => candidate.relation === "scope" && candidate.target.category === "timeline_namespace" && domainRefMatchesEntity2(candidate.source, unit));
      if (!edge) return void 0;
      return {
        category: edge.target.category,
        kind: edge.target.kind,
        id: edge.target.id,
        path: edge.target.path
      };
    }
    function domainParentRefForNode2(snapshot, node) {
      const edge = (snapshot.domainEdges ?? []).find((candidate) => candidate.relation === "parent" && candidate.target.category === node.category && domainRefMatchesNode2(candidate.source, node));
      return edge ? {
        id: edge.target.id,
        kind: edge.target.kind,
        path: edge.target.path
      } : void 0;
    }
    function domainRecordForNode2(snapshot, node) {
      const entityKind = stringField232(node.metadata?.entityKind);
      const entities = entityKind === "production" ? snapshot.productions : entityKind === "segment" ? snapshot.segments : [];
      return entities.find(
        (entity) => node.path !== void 0 && entity.path === node.path || node.id !== void 0 && sameId42(idField142(entity.id ?? entity.record.id ?? entity.record.ID ?? entity.path), node.id)
      )?.record;
    }
    function domainRefMatchesNode2(ref, node) {
      return ref.category === node.category && ref.kind === node.kind && (sameId42(ref.id, node.id) || ref.path !== void 0 && ref.path === node.path);
    }
    function domainRefMatchesEntity2(ref, entity) {
      return sameId42(ref.id, entityId2(entity)) || ref.path !== void 0 && ref.path === entity.path;
    }
    function contentCandidateRecordsByContentUnitId5(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (!document.path.endsWith("/content_candidate.json") || !isRecord211(document.data)) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument22(document.path, stringField232(document.data.content_unit_ref));
        if (!contentUnitId) continue;
        output3.set(contentUnitId, [...output3.get(contentUnitId) ?? [], document.data]);
      }
      return output3;
    }
    function selectionRecordsByContentUnitId5(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (!isRecord211(document.data)) continue;
        const selection = isRecord211(document.data.selection) ? document.data.selection : void 0;
        if (!selection) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument22(document.path, stringField232(document.data.target_ref));
        if (!contentUnitId) continue;
        output3.set(contentUnitId, {
          ...idField142(selection.candidate_id) !== void 0 ? { candidate_id: idField142(selection.candidate_id) } : {},
          ...numberField222(selection.resource_id) !== void 0 ? { resource_id: numberField222(selection.resource_id) } : {},
          ...stringField232(selection.stale_policy) ? { stale_policy: stringField232(selection.stale_policy) } : {}
        });
      }
      return output3;
    }
    function entityId2(entity) {
      return idField142(entity.id ?? entity.record.id ?? entity.record.ID ?? pathSegmentAfter322(entity.path, "content_units") ?? entity.path) ?? entity.path;
    }
    function contentUnitIdForRuntimeDocument22(path, ref) {
      if (ref) return lastPathSegment62(ref) ?? ref;
      return pathSegmentAfter322(path, "content_units");
    }
    function firstCandidateOutput22(candidate) {
      const outputs = Array.isArray(candidate?.outputs) ? candidate.outputs : [];
      return outputs.find(isRecord211);
    }
    function arrayValue5(value) {
      return Array.isArray(value) ? value : [];
    }
    function stringArrayField2(value) {
      return Array.isArray(value) ? value.filter((item) => typeof item === "string" && Boolean(item.trim())).map((item) => item.trim()) : [];
    }
    function idField142(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function stringField232(value) {
      const id = idField142(value);
      return id === void 0 ? void 0 : String(id);
    }
    function numberField222(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
      return void 0;
    }
    function pathSegmentAfter322(path, segment) {
      if (!path) return void 0;
      const parts = path.split("/").filter(Boolean);
      const index = parts.indexOf(segment);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function lastPathSegment62(path) {
      return path?.split("/").filter(Boolean).at(-1);
    }
    function sameId42(left, right) {
      return left !== void 0 && right !== void 0 && String(left) === String(right);
    }
    function isRecord211(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    var import_promises52 = __require("fs/promises");
    var import_path32 = __require("path");
    function createMediaEditingProjectFromMovScriptEditPlan2(editPlan, options = {}) {
      const now = options.now ?? (/* @__PURE__ */ new Date()).toISOString();
      const projectId = options.projectId ?? `movscript_${String(editPlan.productionId)}`;
      const assets = buildMediaAssetRegistryFromEditPlan2(editPlan);
      const timeline = buildMediaTimelineRecipeFromEditPlan2(editPlan, assets, options);
      return {
        version: 1,
        id: options.id ?? `editing_project_${String(editPlan.sceneMomentId)}`,
        projectId,
        title: options.title ?? `Scene moment ${String(editPlan.sceneMomentId)}`,
        source: {
          kind: "movscript_edit_plan",
          targetKind: editPlan.target_kind ?? "scene_moment",
          targetRef: editPlan.target_ref,
          scopeKind: editPlan.scope_kind,
          scopeRef: editPlan.scope_ref === void 0 ? void 0 : String(editPlan.scope_ref),
          sceneMomentId: String(editPlan.sceneMomentId),
          productionId: String(editPlan.productionId),
          contentUnitIds: editPlan.tracks.flatMap((track) => track.items.map((item) => String(item.content_unit_id)))
        },
        timeline,
        assets,
        provenance: {
          targetKind: editPlan.target_kind ?? "scene_moment",
          targetRef: editPlan.target_ref,
          scopeKind: editPlan.scope_kind,
          scopeRef: editPlan.scope_ref === void 0 ? void 0 : String(editPlan.scope_ref),
          legacyTargetKind: editPlan.legacy_target_kind,
          legacyTargetRef: editPlan.legacy_target_ref === void 0 ? void 0 : String(editPlan.legacy_target_ref),
          productionPath: editPlan.productionPath,
          sceneMomentPath: editPlan.sceneMomentPath,
          selectedCandidateIds: editPlan.tracks.flatMap((track) => track.items).filter((item) => item.selected && item.candidate_id !== void 0).map((item) => String(item.candidate_id)),
          inputResourceIds: assets.assets.flatMap((asset) => asset.resourceId === void 0 ? [] : [asset.resourceId])
        },
        createdAt: now,
        updatedAt: now,
        revision: 1
      };
    }
    function createMediaEditingProjectFromProductionTimelineClips2(options) {
      const productionId = String(options.productionId);
      return createMediaEditingProjectFromTimelineAssemblyClips2({
        ...options,
        productionId,
        productionPath: options.productionPath,
        targetKind: options.targetKind ?? "timeline_assembly",
        targetRef: options.targetRef,
        scopeKind: options.scopeKind ?? "production",
        scopeRef: options.scopeRef ?? productionId,
        legacyTargetKind: options.targetKind === void 0 || options.targetKind === "timeline_assembly" ? "production" : void 0,
        legacyTargetRef: options.targetKind === void 0 || options.targetKind === "timeline_assembly" ? productionId : void 0
      });
    }
    function createMediaEditingProjectFromTimelineAssemblyClips2(options) {
      const now = options.now ?? (/* @__PURE__ */ new Date()).toISOString();
      const scopeKind = options.scopeKind;
      const scopeRef = String(options.scopeRef);
      const target = timelineAssemblyTarget2(options);
      const defaultDurationMs = options.defaultDurationMs ?? 4e3;
      let cursorMs = 0;
      const assets = [];
      const clips = options.clips.map((clip) => {
        const durationMs = Math.max(1, Math.round((clip.durationSec ?? defaultDurationMs / 1e3) * 1e3));
        const asset = {
          id: `movscript_resource_${clip.resourceId}`,
          sourceKind: "backend_resource",
          assetType: "video",
          resourceId: clip.resourceId,
          label: clip.title,
          metadata: {
            movscript: {
              sceneMomentId: clip.sceneMomentId,
              sceneMomentPath: clip.sceneMomentPath,
              contentUnitId: clip.contentUnitId,
              candidateId: clip.candidateId,
              resourceId: clip.resourceId,
              outputKind: "video",
              trackType: "video",
              targetKind: target.targetKind,
              targetRef: target.targetRef,
              scopeKind: target.scopeKind,
              scopeRef: target.scopeRef,
              legacyTargetKind: target.legacyTargetKind,
              legacyTargetRef: target.legacyTargetRef,
              selected: true,
              stale: false
            }
          }
        };
        assets.push(asset);
        const mediaClip = {
          id: clip.id || `assembly_clip_${safeId6(scopeKind)}_${safeId6(scopeRef)}_${assets.length}`,
          assetType: "video",
          asset,
          timelineStartMs: cursorMs,
          durationMs,
          sourceStartMs: 0,
          sourceEndMs: durationMs,
          fit: "cover",
          opacity: 1,
          muted: false,
          metadata: asset.metadata
        };
        cursorMs += durationMs;
        return mediaClip;
      });
      return {
        version: 1,
        id: options.id ?? `editing_project_${safeId6(scopeKind)}_${safeId6(scopeRef)}`,
        projectId: options.projectId ?? `movscript_${safeId6(scopeKind)}_${safeId6(scopeRef)}`,
        title: options.title ?? `Timeline assembly ${scopeKind}:${scopeRef}`,
        source: {
          kind: "movscript_edit_plan",
          targetKind: target.targetKind,
          targetRef: target.targetRef,
          scopeKind: target.scopeKind,
          scopeRef: target.scopeRef,
          ...options.productionId !== void 0 ? { productionId: String(options.productionId) } : {},
          contentUnitIds: options.clips.map((clip) => String(clip.contentUnitId))
        },
        timeline: {
          version: 1,
          id: `timeline_${safeId6(scopeKind)}_${safeId6(scopeRef)}`,
          fps: options.fps ?? 30,
          width: options.width ?? 1920,
          height: options.height ?? 1080,
          background: options.background ?? "#000000",
          durationMs: cursorMs,
          tracks: [{
            id: "track_timeline_assembly_video_0",
            name: "timeline assembly video",
            type: "video",
            zIndex: 0,
            muted: false,
            locked: false,
            clips
          }],
          metadata: {
            targetKind: target.targetKind,
            targetRef: target.targetRef,
            scopeKind: target.scopeKind,
            scopeRef: target.scopeRef,
            legacyTargetKind: target.legacyTargetKind,
            legacyTargetRef: target.legacyTargetRef,
            ...options.scopePath ? { scopePath: options.scopePath } : {},
            ...options.productionPath ? { productionPath: options.productionPath } : {}
          }
        },
        assets: { assets },
        provenance: {
          targetKind: target.targetKind,
          targetRef: target.targetRef,
          scopeKind: target.scopeKind,
          scopeRef: target.scopeRef,
          legacyTargetKind: target.legacyTargetKind,
          legacyTargetRef: target.legacyTargetRef,
          ...options.scopePath ? { scopePath: options.scopePath } : {},
          ...options.productionPath ? { productionPath: options.productionPath } : {},
          selectedCandidateIds: options.clips.flatMap((clip) => clip.candidateId === void 0 ? [] : [String(clip.candidateId)]),
          inputResourceIds: options.clips.map((clip) => clip.resourceId)
        },
        createdAt: now,
        updatedAt: now,
        revision: 1
      };
    }
    function timelineAssemblyTarget2(options) {
      const targetKind = options.targetKind ?? "timeline_assembly";
      const scopeKind = options.scopeKind;
      const scopeRef = String(options.scopeRef);
      const targetRef = options.targetRef ?? (targetKind === "timeline_assembly" ? `timeline_assembly:${scopeKind}:${scopeRef}` : scopeRef);
      return {
        targetKind,
        targetRef,
        ...scopeKind ? { scopeKind } : {},
        ...scopeRef ? { scopeRef } : {},
        ...options.legacyTargetKind && options.legacyTargetRef !== void 0 ? {
          legacyTargetKind: options.legacyTargetKind,
          legacyTargetRef: String(options.legacyTargetRef)
        } : {}
      };
    }
    function buildMediaAssetRegistryFromEditPlan2(editPlan) {
      const assetsById = /* @__PURE__ */ new Map();
      for (const item of editPlan.tracks.flatMap((track) => track.items)) {
        if (item.resource_id === void 0) continue;
        const id = mediaAssetIdForItem2(item);
        if (assetsById.has(id)) continue;
        assetsById.set(id, {
          id,
          sourceKind: "backend_resource",
          assetType: assetTypeForEditPlanItem2(item),
          resourceId: item.resource_id,
          label: String(item.content_unit_id),
          metadata: movscriptItemMetadata2(item)
        });
      }
      return { assets: [...assetsById.values()].sort((left, right) => left.id.localeCompare(right.id)) };
    }
    function buildMediaTimelineRecipeFromEditPlan2(editPlan, assets = buildMediaAssetRegistryFromEditPlan2(editPlan), options = {}) {
      const tracks = editPlan.tracks.flatMap(
        (track, index) => mediaTrackFromMovScriptTrack2({
          track,
          assets,
          index,
          defaultDurationMs: options.defaultDurationMs ?? 4e3,
          includeMissingPlaceholders: options.includeMissingPlaceholders ?? true
        })
      );
      return {
        version: 1,
        id: `timeline_scene_moment_${String(editPlan.sceneMomentId)}`,
        fps: options.fps ?? 30,
        width: options.width ?? 1080,
        height: options.height ?? 1920,
        background: options.background ?? "#000000",
        durationMs: durationFromTracks3(tracks),
        tracks,
        metadata: {
          targetRef: editPlan.target_ref,
          status: editPlan.status
        }
      };
    }
    function validateMediaEditingProjectTimeline2(project) {
      const diagnostics = [];
      const assetIds = new Set(project.assets.assets.map((asset) => asset.id));
      const seenTrackIds = /* @__PURE__ */ new Set();
      const seenClipIds = /* @__PURE__ */ new Set();
      for (const track of project.timeline.tracks) {
        if (seenTrackIds.has(track.id)) {
          diagnostics.push(diagnostic2("duplicate_track_id", "error", `Duplicate media track id: ${track.id}`, { trackId: track.id }));
        }
        seenTrackIds.add(track.id);
        const sortedClips = [...track.clips].sort((left, right) => left.timelineStartMs - right.timelineStartMs || left.id.localeCompare(right.id));
        for (const clip of track.clips) {
          if (seenClipIds.has(clip.id)) {
            diagnostics.push(diagnostic2("duplicate_clip_id", "error", `Duplicate media clip id: ${clip.id}`, { trackId: track.id, clipId: clip.id }));
          }
          seenClipIds.add(clip.id);
          if (clip.durationMs <= 0) {
            diagnostics.push(diagnostic2("invalid_duration", "error", `Media clip ${clip.id} has invalid duration.`, { trackId: track.id, clipId: clip.id }));
          }
          if (clip.timelineStartMs < 0) {
            diagnostics.push(diagnostic2("invalid_timeline_start", "error", `Media clip ${clip.id} starts before the timeline.`, { trackId: track.id, clipId: clip.id }));
          }
          if (clip.sourceStartMs !== void 0 && clip.sourceEndMs !== void 0 && clip.sourceEndMs < clip.sourceStartMs) {
            diagnostics.push(diagnostic2("invalid_source_range", "error", `Media clip ${clip.id} has an invalid source range.`, {
              trackId: track.id,
              clipId: clip.id,
              details: { sourceStartMs: clip.sourceStartMs, sourceEndMs: clip.sourceEndMs }
            }));
          }
          if (!clipFitsTrackType2(track.type, clip.assetType)) {
            diagnostics.push(diagnostic2("track_clip_type_mismatch", "error", `Media clip ${clip.id} cannot be placed on ${track.type} track ${track.id}.`, {
              trackId: track.id,
              clipId: clip.id,
              details: { trackType: track.type, clipAssetType: clip.assetType }
            }));
          }
          if (clip.asset && !assetIds.has(clip.asset.id)) {
            diagnostics.push(diagnostic2("asset_not_registered", "error", `Media clip ${clip.id} references unregistered asset ${clip.asset.id}.`, {
              trackId: track.id,
              clipId: clip.id,
              assetId: clip.asset.id
            }));
          }
          if (clip.asset && clip.asset.assetType !== clip.assetType) {
            diagnostics.push(diagnostic2("asset_type_mismatch", "error", `Media clip ${clip.id} asset type does not match its clip type.`, {
              trackId: track.id,
              clipId: clip.id,
              assetId: clip.asset.id,
              details: { clipAssetType: clip.assetType, assetType: clip.asset.assetType }
            }));
          }
          if (clip.assetType === "subtitle" && !clip.subtitle?.resourceId && !clip.asset?.resourceId && !clip.text?.content) {
            diagnostics.push(diagnostic2("subtitle_reference_missing", "error", `Subtitle clip ${clip.id} has no subtitle file, text, or resource reference.`, {
              trackId: track.id,
              clipId: clip.id
            }));
          }
          if (clip.volume !== void 0 && clip.volume > 0 && clip.volume <= 2) {
            diagnostics.push(diagnostic2("legacy_ratio_volume", "warning", `Media clip ${clip.id} uses legacy ratio volume; it will be interpreted as percent volume.`, {
              trackId: track.id,
              clipId: clip.id,
              details: { volume: clip.volume, normalizedVolume: normalizeMediaClipVolumePercent2(clip.volume) }
            }));
          }
        }
        if (!trackAllowsOverlap2(track.type)) {
          for (let index = 1; index < sortedClips.length; index += 1) {
            const previous = sortedClips[index - 1];
            const current = sortedClips[index];
            const previousEndMs = previous.timelineStartMs + previous.durationMs;
            if (previousEndMs > current.timelineStartMs) {
              diagnostics.push(diagnostic2("clip_overlap", "error", `Media clip ${current.id} overlaps ${previous.id}.`, {
                trackId: track.id,
                clipId: current.id,
                previousClipId: previous.id,
                details: { overlapMs: previousEndMs - current.timelineStartMs }
              }));
            }
          }
        }
      }
      return diagnostics;
    }
    function normalizeMediaClipVolumePercent2(volume) {
      if (volume === void 0 || !Number.isFinite(volume)) return void 0;
      const normalized = volume > 0 && volume <= 2 ? volume * 100 : volume;
      return Math.max(0, Math.min(200, normalized));
    }
    function mediaTrackFromMovScriptTrack2(input3) {
      const trackType = mediaTrackTypeForMovScriptTrack2(input3.track.type);
      if (!trackType) return [];
      let cursorMs = 0;
      const clips = input3.track.items.slice().sort((left, right) => left.order - right.order).flatMap((item) => {
        if (!input3.includeMissingPlaceholders && (item.selected !== true || item.resource_id === void 0)) return [];
        const clip = mediaClipFromMovScriptItem2({
          item,
          trackType,
          assets: input3.assets,
          fallbackStartMs: cursorMs,
          defaultDurationMs: input3.defaultDurationMs
        });
        cursorMs = clip.timelineStartMs + clip.durationMs;
        return [clip];
      });
      return [{
        id: `track_${input3.track.type}_${input3.index}`,
        type: trackType,
        zIndex: input3.index,
        name: input3.track.type,
        muted: false,
        locked: false,
        clips
      }];
    }
    function mediaClipFromMovScriptItem2(input3) {
      const timing = input3.item.timing_intent ?? {};
      const sourceStartMs = secField2(timing.trim_start_sec) ?? secField2(timing.in_sec) ?? secField2(timing.start_sec) ?? 0;
      const durationMs = secField2(timing.duration_sec) ?? durationFromInOutMs2(timing) ?? input3.defaultDurationMs;
      const timelineStartMs = secField2(timing.timeline_start_sec) ?? secField2(timing.start_time_sec) ?? input3.fallbackStartMs;
      const asset = input3.item.resource_id === void 0 ? void 0 : input3.assets.assets.find((candidate) => candidate.id === mediaAssetIdForItem2(input3.item));
      const base = {
        id: input3.item.id,
        assetType: assetTypeForEditPlanItem2(input3.item),
        asset,
        timelineStartMs,
        durationMs,
        sourceStartMs,
        sourceEndMs: sourceStartMs + durationMs,
        volume: normalizeMediaClipVolumePercent2(numberField72(timing.volume)) ?? 100,
        muted: false,
        fit: "cover",
        opacity: numberField72(timing.opacity) ?? 1,
        metadata: movscriptItemMetadata2(input3.item)
      };
      if (base.assetType === "text" || input3.trackType === "subtitle") {
        return {
          ...base,
          assetType: "text",
          text: {
            content: stringField242(timing.text) ?? String(input3.item.content_unit_id),
            fontSize: numberField72(timing.font_size) ?? 42,
            fontFamily: stringField242(timing.font_family) ?? "Inter",
            color: stringField242(timing.color) ?? "#ffffff",
            backgroundColor: stringField242(timing.background_color),
            align: "center",
            position: stringField242(timing.position) ?? "bottom_center"
          }
        };
      }
      return base;
    }
    function mediaTrackTypeForMovScriptTrack2(type) {
      switch (type) {
        case "video":
          return "video";
        case "image":
          return "image";
        case "voice":
        case "audio":
          return "audio";
        case "subtitle":
          return "subtitle";
        case "metadata":
          return void 0;
      }
    }
    function assetTypeForEditPlanItem2(item) {
      switch (item.output_kind) {
        case "video":
          return "video";
        case "image":
          return "image";
        case "audio":
          return "audio";
        case "text":
          return "text";
        case "metadata":
          return "text";
      }
    }
    function clipFitsTrackType2(trackType, assetType) {
      if (trackType === "video") return assetType === "video" || assetType === "image";
      if (trackType === "image") return assetType === "image";
      if (trackType === "audio") return assetType === "audio";
      if (trackType === "text") return assetType === "text" || assetType === "subtitle";
      if (trackType === "subtitle") return assetType === "subtitle" || assetType === "text";
      if (trackType === "effect") return true;
      return false;
    }
    function trackAllowsOverlap2(trackType) {
      return trackType === "effect" || trackType === "text" || trackType === "subtitle";
    }
    function diagnostic2(code, severity, message, input3 = {}) {
      return {
        code,
        severity,
        message,
        ...input3.trackId ? { trackId: input3.trackId, track_id: input3.trackId } : {},
        ...input3.clipId ? { clipId: input3.clipId, clip_id: input3.clipId } : {},
        ...input3.previousClipId ? { previousClipId: input3.previousClipId, previous_clip_id: input3.previousClipId } : {},
        ...input3.assetId ? { assetId: input3.assetId, asset_id: input3.assetId } : {},
        ...input3.details ? { details: input3.details } : {}
      };
    }
    function mediaAssetIdForItem2(item) {
      return `resource_${item.resource_id}`;
    }
    function movscriptItemMetadata2(item) {
      return {
        contentUnitId: item.content_unit_id,
        contentUnitRef: item.content_unit_ref,
        candidateId: item.candidate_id,
        resourceId: item.resource_id,
        outputKind: item.output_kind,
        targetKind: item.target_kind,
        targetRef: item.target_ref,
        expressionUnitRef: item.expression_unit_ref,
        expressionModality: item.expression_modality,
        expressionRole: item.expression_role,
        selected: item.selected,
        stale: item.stale
      };
    }
    function durationFromTracks3(tracks) {
      return Math.max(
        0,
        ...tracks.flatMap((track) => track.clips.map((clip) => clip.timelineStartMs + clip.durationMs))
      );
    }
    function durationFromInOutMs2(timing) {
      const startMs = secField2(timing.in_sec) ?? secField2(timing.start_sec);
      const endMs = secField2(timing.out_sec) ?? secField2(timing.end_sec);
      if (startMs === void 0 || endMs === void 0 || endMs <= startMs) return void 0;
      return endMs - startMs;
    }
    function secField2(value) {
      const seconds = numberField72(value);
      return seconds === void 0 ? void 0 : Math.round(seconds * 1e3);
    }
    function numberField72(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
      return void 0;
    }
    function stringField242(value) {
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim();
      return trimmed ? trimmed : void 0;
    }
    function safeId6(value) {
      return value.replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "item";
    }
    function createMediaEditingProjectFromEditDecisions2(editDecisions, options = {}) {
      const now = options.now ?? (/* @__PURE__ */ new Date()).toISOString();
      const projectId = options.projectId ?? composeProjectId2(options, editDecisions);
      const assets = new ComposeAssetRegistry2(options.assetManifest);
      const timeline = buildMediaTimelineRecipeFromEditDecisions2(editDecisions, assets, options);
      const inputResourceIds = [...new Set(assets.assets.flatMap((asset) => asset.resourceId === void 0 ? [] : [asset.resourceId]))].sort((left, right) => left - right);
      return {
        version: 1,
        id: options.id ?? `editing_project_compose_${safeId22(options.scopeRef ?? options.targetRef ?? options.productionId ?? "draft")}`,
        projectId,
        title: options.title ?? `Video compose ${String(options.targetRef ?? options.scopeRef ?? options.productionId ?? "draft")}`,
        source: {
          kind: "edit_decisions",
          targetKind: options.targetKind ?? "timeline_assembly",
          targetRef: options.targetRef ?? `edit_decisions:${safeId22(options.scopeKind ?? "project")}:${safeId22(options.scopeRef ?? projectId)}`,
          scopeKind: options.scopeKind,
          scopeRef: options.scopeRef === void 0 ? void 0 : String(options.scopeRef),
          ...options.productionId !== void 0 ? { productionId: String(options.productionId) } : {}
        },
        timeline,
        assets: { assets: assets.assets },
        provenance: {
          sourceHash: options.sourceHash,
          inputResourceIds,
          targetKind: options.targetKind ?? "timeline_assembly",
          targetRef: options.targetRef,
          scopeKind: options.scopeKind,
          scopeRef: options.scopeRef === void 0 ? void 0 : String(options.scopeRef),
          productionPath: options.productionPath
        },
        createdAt: now,
        updatedAt: now,
        revision: 1
      };
    }
    function buildMediaTimelineRecipeFromEditDecisions2(editDecisions, assets, options) {
      const defaultDurationMs = options.defaultDurationMs ?? 4e3;
      const tracks = new TrackRegistry2();
      let primaryCursorMs = 0;
      for (const [index, cut] of arrayValue22(editDecisions.cuts).entries()) {
        const layer = stringField252(cut.layer) ?? "primary";
        const track = videoTrackForLayer2(tracks, layer);
        const asset = assets.resolve(assetRef3(cut), inferAssetTypeFromRecord2(cut, "video"));
        const sourceStartMs = secondsToMs2(cut.source_in_seconds ?? cut.source_in_sec ?? cut.source_start_seconds ?? cut.source_start_sec ?? cut.in_seconds ?? cut.in_sec) ?? 0;
        const sourceEndMs = secondsToMs2(cut.source_out_seconds ?? cut.source_out_sec ?? cut.source_end_seconds ?? cut.source_end_sec ?? cut.out_seconds ?? cut.out_sec);
        const durationMs = secondsToMs2(cut.duration_seconds ?? cut.duration_sec) ?? durationFromRange2(sourceStartMs, sourceEndMs) ?? defaultDurationMs;
        const explicitTimelineStartMs = secondsToMs2(cut.timeline_start_seconds ?? cut.timeline_start_sec ?? cut.start_seconds ?? cut.start_sec);
        const timelineStartMs = explicitTimelineStartMs ?? (layer === "primary" ? primaryCursorMs : 0);
        const transform = recordValue22(cut.transform);
        const clip = {
          id: stringField252(cut.id) ?? `cut_${index + 1}`,
          assetType: asset?.assetType ?? inferAssetTypeFromRecord2(cut, "video"),
          asset,
          timelineStartMs,
          durationMs,
          sourceStartMs,
          sourceEndMs: sourceEndMs ?? sourceStartMs + durationMs,
          speed: numberField82(cut.speed),
          volume: normalizeMediaClipVolumePercent2(numberField82(cut.volume)) ?? 100,
          muted: false,
          fit: "cover",
          opacity: numberField82(transform?.opacity ?? cut.opacity) ?? 1,
          position: stringField252(transform?.position ?? cut.position),
          xPercent: numberField82(transform?.x_percent ?? transform?.xPercent ?? cut.x_percent ?? cut.xPercent),
          yPercent: numberField82(transform?.y_percent ?? transform?.yPercent ?? cut.y_percent ?? cut.yPercent),
          scale: numberField82(transform?.scale ?? cut.scale),
          crop: cropSpec2(transform?.crop ?? cut.crop),
          ...transitionFields2(cut),
          metadata: {
            movscript: {
              kind: "edit_decision_cut",
              layer,
              reason: stringField252(cut.reason),
              source: assetRef3(cut),
              renderRuntime: stringField252(editDecisions.render_runtime ?? editDecisions.renderRuntime)
            },
            ...cut.metadata ?? {}
          }
        };
        track.clips.push(clip);
        if (layer === "primary" && explicitTimelineStartMs === void 0) primaryCursorMs = timelineStartMs + durationMs;
      }
      const primaryDurationMs = Math.max(primaryCursorMs, durationFromTracks22(tracks.all()));
      addOverlayTracks2(editDecisions, tracks, assets, primaryDurationMs, defaultDurationMs);
      addAudioTracks2(editDecisions, tracks, assets, primaryDurationMs, defaultDurationMs);
      addSubtitleTracks2(editDecisions, tracks, assets, primaryDurationMs, defaultDurationMs);
      const finalTracks = tracks.all();
      return {
        version: 1,
        id: options.id ? `timeline_${safeId22(options.id)}` : `timeline_compose_${safeId22(options.scopeRef ?? options.targetRef ?? options.productionId ?? "draft")}`,
        fps: options.fps ?? 30,
        width: options.width ?? 1920,
        height: options.height ?? 1080,
        background: options.background ?? "#000000",
        durationMs: durationFromTracks22(finalTracks),
        tracks: finalTracks,
        metadata: {
          schema: "movscript.video_compose_timeline.v1",
          rendererFamily: stringField252(editDecisions.renderer_family ?? editDecisions.rendererFamily),
          renderRuntime: stringField252(editDecisions.render_runtime ?? editDecisions.renderRuntime),
          compositionMode: stringField252(editDecisions.composition_mode ?? editDecisions.compositionMode),
          targetKind: options.targetKind,
          targetRef: options.targetRef,
          scopeKind: options.scopeKind,
          scopeRef: options.scopeRef,
          ...editDecisions.metadata ? { editDecisionMetadata: editDecisions.metadata } : {}
        }
      };
    }
    function addOverlayTracks2(editDecisions, tracks, assets, timelineDurationMs, defaultDurationMs) {
      const overlayTrack = tracks.track("track_overlay_visual", "video", 20, "overlay");
      for (const [index, overlay] of arrayValue22(editDecisions.overlays).entries()) {
        const asset = assets.resolve(assetRef3(overlay), inferAssetTypeFromRecord2(overlay, "image"));
        const startMs = secondsToMs2(overlay.start_seconds ?? overlay.start_sec) ?? 0;
        const durationMs = secondsToMs2(overlay.duration_seconds ?? overlay.duration_sec) ?? durationFromRange2(startMs, secondsToMs2(overlay.end_seconds ?? overlay.end_sec)) ?? Math.max(defaultDurationMs, timelineDurationMs - startMs);
        overlayTrack.clips.push({
          id: stringField252(overlay.id) ?? `overlay_${index + 1}`,
          assetType: asset?.assetType ?? inferAssetTypeFromRecord2(overlay, "image"),
          asset,
          timelineStartMs: startMs,
          durationMs,
          sourceStartMs: secondsToMs2(overlay.source_start_seconds ?? overlay.source_start_sec) ?? 0,
          sourceEndMs: secondsToMs2(overlay.source_end_seconds ?? overlay.source_end_sec),
          fit: "contain",
          opacity: numberField82(overlay.opacity) ?? 1,
          position: stringField252(overlay.position),
          xPercent: numberField82(overlay.x_percent ?? overlay.xPercent),
          yPercent: numberField82(overlay.y_percent ?? overlay.yPercent),
          scale: numberField82(overlay.scale),
          metadata: {
            movscript: {
              kind: "edit_decision_overlay",
              source: assetRef3(overlay),
              animation: overlay.animation
            },
            ...overlay.metadata ?? {}
          }
        });
      }
    }
    function addAudioTracks2(editDecisions, tracks, assets, timelineDurationMs, defaultDurationMs) {
      const audio = recordValue22(editDecisions.audio);
      if (!audio) return;
      const narration = recordValue22(audio.narration);
      const narrationSegments = Array.isArray(audio.narration) ? audio.narration : Array.isArray(narration?.segments) ? narration.segments : [];
      addAudioSegments2(tracks.track("track_audio_narration", "audio", 30, "narration"), narrationSegments, assets, "narration", defaultDurationMs);
      const music = recordValue22(audio.music);
      if (music) {
        const track = tracks.track("track_audio_music", "audio", 10, "music");
        const asset = assets.resolve(assetRef3(music), "audio");
        const startMs = secondsToMs2(music.start_seconds ?? music.start_sec) ?? 0;
        const durationMs = secondsToMs2(music.duration_seconds ?? music.duration_sec) ?? durationFromRange2(startMs, secondsToMs2(music.end_seconds ?? music.end_sec)) ?? Math.max(defaultDurationMs, timelineDurationMs - startMs);
        track.clips.push(audioClipFromLayer2({
          layer: music,
          id: "music_bed",
          kind: "music",
          asset,
          startMs,
          durationMs
        }));
      }
      addAudioSegments2(
        tracks.track("track_audio_sfx", "audio", 40, "sfx"),
        arrayValue22(audio.sfx),
        assets,
        "sfx",
        defaultDurationMs
      );
    }
    function addAudioSegments2(track, segments, assets, kind, defaultDurationMs) {
      for (const [index, segment] of segments.entries()) {
        const record = recordValue22(segment);
        if (!record) continue;
        const asset = assets.resolve(assetRef3(record), "audio");
        const startMs = secondsToMs2(record.start_seconds ?? record.start_sec) ?? 0;
        const durationMs = secondsToMs2(record.duration_seconds ?? record.duration_sec) ?? durationFromRange2(startMs, secondsToMs2(record.end_seconds ?? record.end_sec)) ?? defaultDurationMs;
        track.clips.push(audioClipFromLayer2({
          layer: record,
          id: stringField252(record.id) ?? `${kind}_${index + 1}`,
          kind,
          asset,
          startMs,
          durationMs
        }));
      }
    }
    function audioClipFromLayer2(input3) {
      const sourceStartMs = secondsToMs2(input3.layer.source_start_seconds ?? input3.layer.source_start_sec) ?? 0;
      return {
        id: input3.id,
        assetType: "audio",
        asset: input3.asset,
        timelineStartMs: input3.startMs,
        durationMs: input3.durationMs,
        sourceStartMs,
        sourceEndMs: secondsToMs2(input3.layer.source_end_seconds ?? input3.layer.source_end_sec) ?? sourceStartMs + input3.durationMs,
        volume: normalizeMediaClipVolumePercent2(numberField82(input3.layer.volume)) ?? 100,
        fadeInMs: secondsToMs2(input3.layer.fade_in_seconds ?? input3.layer.fade_in ?? input3.layer.fadeIn),
        fadeOutMs: secondsToMs2(input3.layer.fade_out_seconds ?? input3.layer.fade_out ?? input3.layer.fadeOut),
        muted: false,
        metadata: {
          movscript: {
            kind: `edit_decision_${input3.kind}`,
            source: assetRef3(input3.layer),
            ducking: input3.layer.ducking
          },
          ...recordValue22(input3.layer.metadata) ?? {}
        }
      };
    }
    function addSubtitleTracks2(editDecisions, tracks, assets, timelineDurationMs, defaultDurationMs) {
      const subtitles = recordValue22(editDecisions.subtitles);
      if (!subtitles || subtitles.enabled === false) return;
      const track = tracks.track("track_subtitles", "subtitle", 50, "subtitles");
      const segments = arrayValue22(subtitles.segments).length ? arrayValue22(subtitles.segments) : arrayValue22(subtitles.captions);
      const style = subtitleTextStyle2(subtitles);
      for (const [index, item] of segments.entries()) {
        const segment = recordValue22(item);
        if (!segment) continue;
        const startMs = secondsToMs2(segment.start_seconds ?? segment.start_sec) ?? 0;
        const durationMs = secondsToMs2(segment.duration_seconds ?? segment.duration_sec) ?? durationFromRange2(startMs, secondsToMs2(segment.end_seconds ?? segment.end_sec)) ?? defaultDurationMs;
        track.clips.push({
          id: stringField252(segment.id) ?? `subtitle_${index + 1}`,
          assetType: "text",
          timelineStartMs: startMs,
          durationMs,
          text: {
            ...style,
            content: stringField252(segment.text ?? segment.content) ?? ""
          },
          subtitle: {
            burnIn: true,
            style
          },
          metadata: {
            movscript: {
              kind: "edit_decision_subtitle_segment"
            }
          }
        });
      }
      const sourceRef = assetRef3(subtitles);
      if (sourceRef !== void 0 && !isGeneratedSubtitleSource2(sourceRef)) {
        const asset = assets.resolve(sourceRef, "subtitle");
        track.clips.push({
          id: "subtitle_file",
          assetType: "subtitle",
          asset,
          timelineStartMs: 0,
          durationMs: Math.max(defaultDurationMs, timelineDurationMs),
          subtitle: {
            resourceId: asset?.resourceId,
            format: subtitleFormat2(subtitles),
            burnIn: true,
            renderer: subtitleRenderer2(subtitles),
            style
          },
          metadata: {
            movscript: {
              kind: "edit_decision_subtitle_file",
              source: sourceRef
            }
          }
        });
      }
    }
    var TrackRegistry2 = class {
      tracks = /* @__PURE__ */ new Map();
      track(id, type, zIndex, name) {
        const existing = this.tracks.get(id);
        if (existing) return existing;
        const track = {
          id,
          type,
          zIndex,
          name,
          muted: false,
          locked: false,
          clips: []
        };
        this.tracks.set(id, track);
        return track;
      }
      all() {
        return [...this.tracks.values()].map((track) => ({
          ...track,
          clips: [...track.clips].sort((left, right) => left.timelineStartMs - right.timelineStartMs || left.id.localeCompare(right.id))
        })).filter((track) => track.clips.length > 0).sort((left, right) => left.zIndex - right.zIndex || left.id.localeCompare(right.id));
      }
    };
    var ComposeAssetRegistry2 = class {
      byRef = /* @__PURE__ */ new Map();
      byId = /* @__PURE__ */ new Map();
      constructor(manifest) {
        for (const entry of arrayValue22(manifest?.assets)) {
          const asset = this.assetFromRecord(entry);
          if (!asset) continue;
          this.register(asset, assetRefsFromRecord3(entry));
        }
      }
      get assets() {
        return [...this.byId.values()].sort((left, right) => left.id.localeCompare(right.id));
      }
      resolve(ref, preferredType) {
        const key = refKey3(ref);
        if (key && this.byRef.has(key)) return this.byRef.get(key);
        const record = recordValue22(ref);
        const asset = record ? this.assetFromRecord(record, preferredType) : this.assetFromPrimitive(ref, preferredType);
        if (!asset) return void 0;
        return this.register(asset, key ? [key] : []);
      }
      register(asset, refs) {
        let next = asset;
        let index = 1;
        while (this.byId.has(next.id)) {
          const existing = this.byId.get(next.id);
          if (sameAsset2(existing, next)) {
            next = existing;
            break;
          }
          index += 1;
          next = { ...asset, id: `${asset.id}_${index}` };
        }
        this.byId.set(next.id, next);
        for (const ref of refs) this.byRef.set(ref, next);
        this.byRef.set(next.id, next);
        if (next.resourceId !== void 0) this.byRef.set(String(next.resourceId), next);
        if (next.localPath) this.byRef.set(next.localPath, next);
        return next;
      }
      assetFromRecord(record, preferredType = "video") {
        const rawPath = stringField252(record.localPath ?? record.local_path ?? record.path ?? record.file_path ?? record.filePath ?? record.src);
        const resourceId = integerField3(record.resourceId ?? record.resource_id) ?? resourceIdFromString3(rawPath ?? "");
        const localPath = localPathField2(rawPath);
        const idSource = stringField252(record.mediaAssetId ?? record.media_asset_id ?? record.id ?? record.asset_id ?? record.assetId) ?? (resourceId !== void 0 ? `resource_${resourceId}` : void 0) ?? localPath;
        if (!idSource && resourceId === void 0 && !localPath) return void 0;
        const assetType = inferAssetTypeFromRecord2(record, preferredType);
        return {
          id: `asset_${safeId22(idSource ?? `${assetType}_${this.byId.size + 1}`)}`,
          sourceKind: resourceId !== void 0 ? "backend_resource" : "local_file",
          assetType,
          ...resourceId !== void 0 ? { resourceId } : {},
          ...localPath ? { localPath } : {},
          ...stringField252(record.mimeType ?? record.mime_type) ? { mimeType: stringField252(record.mimeType ?? record.mime_type) } : {},
          ...stringField252(record.checksum) ? { checksum: stringField252(record.checksum) } : {},
          label: stringField252(record.label ?? record.title ?? record.name ?? record.id ?? record.asset_id ?? localPath),
          metadata: {
            movscript: {
              kind: "edit_decision_asset_manifest_entry",
              manifest: record
            }
          }
        };
      }
      assetFromPrimitive(ref, preferredType) {
        const resourceId = integerField3(ref);
        if (resourceId !== void 0) {
          return {
            id: `asset_resource_${resourceId}`,
            sourceKind: "backend_resource",
            assetType: preferredType,
            resourceId,
            label: `resource ${resourceId}`
          };
        }
        const value = stringField252(ref);
        if (!value) return void 0;
        const parsedResourceId = resourceIdFromString3(value);
        if (parsedResourceId !== void 0) {
          return {
            id: `asset_resource_${parsedResourceId}`,
            sourceKind: "backend_resource",
            assetType: preferredType,
            resourceId: parsedResourceId,
            label: value
          };
        }
        return {
          id: `asset_${safeId22(value)}`,
          sourceKind: "local_file",
          assetType: assetTypeFromPath2(value) ?? preferredType,
          localPath: value,
          label: value.split("/").pop() ?? value,
          metadata: {
            movscript: {
              kind: "edit_decision_inline_asset_ref",
              ref: value
            }
          }
        };
      }
    };
    function videoTrackForLayer2(tracks, layer) {
      if (layer === "background") return tracks.track("track_background_video", "video", -10, "background");
      if (layer === "overlay") return tracks.track("track_overlay_video", "video", 20, "overlay");
      return tracks.track("track_primary_video", "video", 0, "primary");
    }
    function transitionFields2(record) {
      const transitionIn = stringField252(record.transition_in ?? record.transitionIn);
      const transitionOut = stringField252(record.transition_out ?? record.transitionOut);
      const transition = stringField252(record.transition);
      const durationMs = secondsToMs2(record.transition_duration ?? record.transitionDuration);
      return {
        ...transitionIn === "fade" ? { fadeInMs: durationMs } : {},
        ...transitionOut === "fade" ? { fadeOutMs: durationMs } : {},
        ...transition || transitionIn || transitionOut ? { transition: { type: transition ?? transitionIn ?? transitionOut ?? "fade", durationMs } } : {}
      };
    }
    function cropSpec2(value) {
      const crop = recordValue22(value);
      if (!crop) return void 0;
      return {
        topPercent: numberField82(crop.topPercent ?? crop.top_percent ?? crop.top),
        rightPercent: numberField82(crop.rightPercent ?? crop.right_percent ?? crop.right),
        bottomPercent: numberField82(crop.bottomPercent ?? crop.bottom_percent ?? crop.bottom),
        leftPercent: numberField82(crop.leftPercent ?? crop.left_percent ?? crop.left)
      };
    }
    function subtitleTextStyle2(subtitles) {
      const style = recordValue22(subtitles.style) ?? {};
      return {
        content: "",
        fontSize: numberField82(style.fontSize ?? style.font_size ?? subtitles.fontSize ?? subtitles.font_size) ?? 42,
        fontFamily: stringField252(style.fontFamily ?? style.font_family ?? subtitles.font) ?? "Inter",
        color: stringField252(style.color ?? style.text_color ?? subtitles.color ?? subtitles.text_color) ?? "#ffffff",
        backgroundColor: stringField252(style.backgroundColor ?? style.background_color ?? subtitles.background_color ?? subtitles.background),
        align: textAlignValue2(style.align ?? subtitles.align) ?? "center",
        position: stringField252(style.position ?? subtitles.position) ?? "bottom"
      };
    }
    function subtitleFormat2(subtitles) {
      const format = stringField252(subtitles.format)?.toLowerCase();
      if (format === "srt" || format === "vtt" || format === "ass" || format === "ssa") return format;
      return void 0;
    }
    function subtitleRenderer2(subtitles) {
      const renderer = stringField252(subtitles.renderer)?.toLowerCase();
      if (renderer === "drawtext" || renderer === "ass" || renderer === "libass") return renderer;
      return void 0;
    }
    function textAlignValue2(value) {
      const normalized = stringField252(value)?.toLowerCase();
      if (normalized === "left" || normalized === "center" || normalized === "right") return normalized;
      return void 0;
    }
    function assetRef3(record) {
      return record.source ?? record.asset_id ?? record.assetId ?? record.resource_id ?? record.resourceId;
    }
    function assetRefsFromRecord3(record) {
      return [
        record.id,
        record.asset_id,
        record.assetId,
        record.ref,
        record.name,
        record.path,
        record.localPath,
        record.local_path,
        record.file_path,
        record.filePath,
        record.resource_id,
        record.resourceId
      ].flatMap((value) => {
        const key = refKey3(value);
        return key ? [key] : [];
      });
    }
    function inferAssetTypeFromRecord2(record, fallback) {
      const declared = stringField252(record.assetType ?? record.asset_type ?? record.type ?? record.kind ?? record.output_kind ?? record.media_type)?.toLowerCase();
      if (declared?.includes("subtitle")) return "subtitle";
      if (declared?.includes("audio") || declared?.includes("music") || declared?.includes("voice") || declared?.includes("sfx")) return "audio";
      if (declared?.includes("image") || declared?.includes("photo")) return "image";
      if (declared?.includes("text") || declared?.includes("caption")) return "text";
      if (declared?.includes("video") || declared?.includes("clip")) return "video";
      const mime = stringField252(record.mimeType ?? record.mime_type)?.toLowerCase();
      if (mime?.startsWith("audio/")) return "audio";
      if (mime?.startsWith("image/")) return "image";
      if (mime?.startsWith("text/") || mime?.includes("subrip") || mime?.includes("vtt")) return "subtitle";
      const path = localPathField2(stringField252(record.localPath ?? record.local_path ?? record.path ?? record.file_path ?? record.filePath ?? record.src));
      return assetTypeFromPath2(path) ?? fallback;
    }
    function assetTypeFromPath2(path) {
      const normalized = path?.split("?")[0]?.toLowerCase();
      if (!normalized) return void 0;
      if (/\.(wav|mp3|m4a|aac|flac|ogg)$/.test(normalized)) return "audio";
      if (/\.(png|jpg|jpeg|webp|gif|avif)$/.test(normalized)) return "image";
      if (/\.(srt|vtt|ass|ssa)$/.test(normalized)) return "subtitle";
      if (/\.(txt|md)$/.test(normalized)) return "text";
      if (/\.(mp4|mov|m4v|webm|mkv|avi)$/.test(normalized)) return "video";
      return void 0;
    }
    function composeProjectId2(options, editDecisions) {
      if (options.productionId !== void 0) return `movscript_production_${safeId22(options.productionId)}`;
      if (options.scopeKind && options.scopeRef !== void 0) return `movscript_${safeId22(options.scopeKind)}_${safeId22(options.scopeRef)}`;
      return `movscript_video_compose_${safeId22(editDecisions.version ?? "draft")}`;
    }
    function durationFromTracks22(tracks) {
      return Math.max(0, ...tracks.flatMap((track) => track.clips.map((clip) => clip.timelineStartMs + clip.durationMs)));
    }
    function durationFromRange2(startMs, endMs) {
      if (startMs === void 0 || endMs === void 0 || endMs <= startMs) return void 0;
      return endMs - startMs;
    }
    function secondsToMs2(value) {
      const seconds = numberField82(value);
      return seconds === void 0 ? void 0 : Math.max(0, Math.round(seconds * 1e3));
    }
    function numberField82(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
      return void 0;
    }
    function integerField3(value) {
      const number = numberField82(value);
      if (number === void 0 || !Number.isInteger(number) || number <= 0) return void 0;
      return number;
    }
    function resourceIdFromString3(value) {
      const match = value.match(/^(?:resource|raw_resource|backend_resource)[:_](\d+)$/i);
      if (!match) return void 0;
      return integerField3(match[1]);
    }
    function stringField252(value) {
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim();
      return trimmed ? trimmed : void 0;
    }
    function localPathField2(value) {
      if (!value) return void 0;
      if (/^(?:resource|raw_resource|backend_resource|content-unit|content_unit):/i.test(value)) return void 0;
      if (/^(?:https?:|blob:|data:)/i.test(value)) return void 0;
      return value;
    }
    function recordValue22(value) {
      if (!value || typeof value !== "object" || Array.isArray(value)) return void 0;
      return value;
    }
    function arrayValue22(value) {
      return Array.isArray(value) ? value : [];
    }
    function refKey3(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      return stringField252(value);
    }
    function safeId22(value) {
      const raw = String(value ?? "draft").trim() || "draft";
      return raw.replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "draft";
    }
    function sameAsset2(left, right) {
      if (!left) return false;
      if (left.resourceId !== void 0 && right.resourceId !== void 0) return left.resourceId === right.resourceId;
      if (left.localPath && right.localPath) return left.localPath === right.localPath;
      return left.id === right.id;
    }
    function isGeneratedSubtitleSource2(value) {
      const normalized = stringField252(value)?.toLowerCase();
      return normalized === "script" || normalized === "auto" || normalized === "captions" || normalized === "segments";
    }
    var MEDIA_EDITING_RUNTIME_ALLOW_LIST2 = /* @__PURE__ */ new Set(["movscript_media_pipeline", "ffmpeg"]);
    var BACKEND_RUNTIME_LOCKS2 = {
      hyperframes: "hyperframes",
      remotion: "remotion"
    };
    var IMPLEMENTED_BACKENDS2 = /* @__PURE__ */ new Set(["media_editing_project", "hyperframes", "remotion"]);
    function createTimelineAssemblyCompileManifest2(input3) {
      const backend = input3.backend ?? "media_editing_project";
      const renderRuntime = input3.renderRuntime ?? renderRuntimeFromEditDecisions2(input3.editDecisions);
      const runtimeLocked = input3.runtimeLocked ?? renderRuntime !== void 0;
      const renderSettings = normalizeRenderSettings2(input3);
      const timelineAssembly = input3.timelineAssembly ?? {};
      const target = targetFromTimelineAssembly2(timelineAssembly, input3.editDecisions);
      const assetIndex = assetManifestIndex2(input3.assetManifest);
      const actionSummary = summarizeEditDecisionActions2(input3.editDecisions);
      const referencedAssets = collectReferencedAssets2(input3.editDecisions);
      const unresolvedRefs = referencedAssets.filter((entry) => !assetIndex.resolvableRefs.has(entry.ref)).map((entry) => entry.ref);
      const diagnostics = [
        ...capabilityDiagnostics2({
          backend,
          renderRuntime,
          runtimeLocked,
          actionCounts: actionSummary.actionCounts,
          editDecisions: input3.editDecisions
        }),
        ...unresolvedRefs.map((ref) => ({
          code: "asset_ref_unresolved",
          severity: "error",
          message: `TimelineAssembly compile cannot resolve asset ref ${ref}.`,
          asset_ref: ref
        }))
      ];
      const timelineAssemblyHash = stableHash2(timelineAssembly);
      const assetManifestHash = stableHash2(input3.assetManifest ?? {});
      const editDecisionsHash = stableHash2(input3.editDecisions);
      const inputHash = stableHash2({
        timelineAssemblyHash,
        assetManifestHash,
        editDecisionsHash,
        backend,
        renderRuntime,
        runtimeLocked,
        renderSettings
      });
      const status = diagnostics.some((diagnostic22) => diagnostic22.severity === "error") ? "blocked" : "ready";
      return {
        schema: "movscript.timeline_assembly.compile_manifest.v1",
        id: `compile_${safeId32(target.timelineAssemblyId ?? target.targetRef ?? "timeline_assembly")}_${inputHash.slice(0, 12)}`,
        status,
        created_at: input3.now ?? (/* @__PURE__ */ new Date()).toISOString(),
        ...target.timelineAssemblyId ? { timeline_assembly_id: target.timelineAssemblyId } : {},
        ...target.targetKind ? { target_kind: target.targetKind } : {},
        ...target.targetRef ? { target_ref: target.targetRef } : {},
        ...target.scopeKind ? { scope_kind: target.scopeKind } : {},
        ...target.scopeRef ? { scope_ref: target.scopeRef } : {},
        backend: {
          target: backend,
          adapter: backend === "media_editing_project" ? "@movscript/editing:createMediaEditingProjectFromEditDecisions" : `@movscript/editing:${backend}`,
          adapter_version: input3.adapterVersion ?? "0.1.0",
          ...renderRuntime ? { render_runtime: renderRuntime } : {},
          runtime_locked: runtimeLocked,
          fallback_policy: "no_implicit_fallback"
        },
        input_hash: inputHash,
        inputs: {
          timeline_assembly_hash: timelineAssemblyHash,
          asset_manifest_hash: assetManifestHash,
          edit_decisions_hash: editDecisionsHash,
          selected_resource_ids: assetIndex.selectedResourceIds,
          selected_candidate_ids: assetIndex.selectedCandidateIds,
          referenced_asset_refs: [...new Set(referencedAssets.map((entry) => entry.ref))].sort(),
          unresolved_asset_refs: [...new Set(unresolvedRefs)].sort()
        },
        render_settings: renderSettings,
        capabilities: {
          supported_actions: actionSummary.supportedActions,
          unsupported_actions: diagnostics.filter((diagnostic22) => diagnostic22.code === "backend_adapter_not_implemented" || diagnostic22.code === "runtime_lock_backend_mismatch").flatMap((diagnostic22) => stringValue142(diagnostic22.action) ? [String(diagnostic22.action)] : []).sort(),
          action_counts: actionSummary.actionCounts
        },
        diagnostics
      };
    }
    function compileTimelineAssemblyToMediaEditingProject2(input3) {
      const compileManifest = createTimelineAssemblyCompileManifest2({
        ...input3,
        backend: "media_editing_project"
      });
      if (compileManifest.status === "blocked") {
        return {
          schema: "movscript.timeline_assembly.media_editing_compile_result.v1",
          status: "blocked",
          compile_manifest: compileManifest,
          editing_timeline_diagnostics: [],
          diagnostics: compileManifest.diagnostics
        };
      }
      const mediaEditingProject = createMediaEditingProjectFromEditDecisions2(input3.editDecisions, {
        ...input3.projectOptions ?? {},
        assetManifest: input3.assetManifest,
        sourceHash: compileManifest.input_hash,
        width: input3.projectOptions?.width ?? input3.renderSettings?.width,
        height: input3.projectOptions?.height ?? input3.renderSettings?.height,
        fps: input3.projectOptions?.fps ?? input3.renderSettings?.fps,
        background: input3.projectOptions?.background ?? input3.renderSettings?.background,
        defaultDurationMs: input3.projectOptions?.defaultDurationMs ?? input3.renderSettings?.default_duration_ms
      });
      mediaEditingProject.timeline.metadata = {
        ...mediaEditingProject.timeline.metadata ?? {},
        compileManifestId: compileManifest.id,
        compileManifestHash: compileManifest.input_hash,
        backendTarget: compileManifest.backend.target,
        runtimeLocked: compileManifest.backend.runtime_locked
      };
      const editingTimelineDiagnostics = validateMediaEditingProjectTimeline2(mediaEditingProject);
      const diagnostics = [
        ...compileManifest.diagnostics,
        ...editingTimelineDiagnostics.map((diagnostic22) => ({
          code: `media_timeline_${diagnostic22.code}`,
          severity: diagnostic22.severity,
          message: diagnostic22.message,
          details: compactRecord22({
            track_id: diagnostic22.track_id ?? diagnostic22.trackId,
            clip_id: diagnostic22.clip_id ?? diagnostic22.clipId,
            asset_id: diagnostic22.asset_id ?? diagnostic22.assetId,
            previous_clip_id: diagnostic22.previous_clip_id ?? diagnostic22.previousClipId,
            ...diagnostic22.details ? { details: diagnostic22.details } : {}
          })
        }))
      ];
      const status = diagnostics.some((diagnostic22) => diagnostic22.severity === "error") ? "blocked" : "ready";
      return {
        schema: "movscript.timeline_assembly.media_editing_compile_result.v1",
        status,
        compile_manifest: {
          ...compileManifest,
          status,
          diagnostics
        },
        media_editing_project: mediaEditingProject,
        editing_timeline_diagnostics: editingTimelineDiagnostics,
        diagnostics
      };
    }
    function renderRuntimeFromEditDecisions2(editDecisions) {
      return stringValue142(editDecisions.render_runtime ?? editDecisions.renderRuntime);
    }
    function normalizeRenderSettings2(input3) {
      return compactRecord22({
        width: positiveNumber32(input3.renderSettings?.width),
        height: positiveNumber32(input3.renderSettings?.height),
        fps: positiveNumber32(input3.renderSettings?.fps),
        background: stringValue142(input3.renderSettings?.background),
        default_duration_ms: positiveNumber32(input3.renderSettings?.default_duration_ms)
      });
    }
    function targetFromTimelineAssembly2(timelineAssembly, editDecisions) {
      const metadata = recordValue32(editDecisions.metadata);
      const editProfile = recordValue32(timelineAssembly.edit_profile ?? timelineAssembly.editProfile);
      return {
        timelineAssemblyId: stringValue142(timelineAssembly.id ?? timelineAssembly.timeline_assembly_id ?? metadata?.timeline_assembly_id),
        targetKind: stringValue142(timelineAssembly.target_kind ?? timelineAssembly.targetKind ?? metadata?.target_kind),
        targetRef: stringValue142(timelineAssembly.target_ref ?? timelineAssembly.targetRef ?? metadata?.target_ref),
        scopeKind: stringValue142(timelineAssembly.scope_kind ?? timelineAssembly.scopeKind ?? editProfile?.scope_kind),
        scopeRef: stringValue142(timelineAssembly.scope_ref ?? timelineAssembly.scopeRef ?? editProfile?.scope_ref)
      };
    }
    function capabilityDiagnostics2(input3) {
      const diagnostics = [];
      if (!IMPLEMENTED_BACKENDS2.has(input3.backend)) {
        diagnostics.push({
          code: "backend_adapter_not_implemented",
          severity: "error",
          action: `compile_to_${input3.backend}`,
          message: `TimelineAssembly backend ${input3.backend} is not implemented yet.`,
          details: { implemented_backends: [...IMPLEMENTED_BACKENDS2].sort() }
        });
      }
      if (input3.runtimeLocked && input3.renderRuntime && input3.backend === "media_editing_project" && !MEDIA_EDITING_RUNTIME_ALLOW_LIST2.has(input3.renderRuntime)) {
        diagnostics.push({
          code: "runtime_lock_backend_mismatch",
          severity: "error",
          action: `render_runtime:${input3.renderRuntime}`,
          message: `TimelineAssembly is locked to ${input3.renderRuntime}; media_editing_project cannot silently replace it.`,
          details: {
            backend: input3.backend,
            allowed_render_runtimes: [...MEDIA_EDITING_RUNTIME_ALLOW_LIST2].sort()
          }
        });
      }
      const lockedRuntime = BACKEND_RUNTIME_LOCKS2[input3.backend];
      if (input3.runtimeLocked && input3.renderRuntime && lockedRuntime && input3.renderRuntime !== lockedRuntime) {
        diagnostics.push({
          code: "runtime_lock_backend_mismatch",
          severity: "error",
          action: `render_runtime:${input3.renderRuntime}`,
          message: `TimelineAssembly is locked to ${input3.renderRuntime}; ${input3.backend} cannot silently replace it.`,
          details: {
            backend: input3.backend,
            required_render_runtime: lockedRuntime
          }
        });
      }
      if ((input3.actionCounts.cut ?? 0) === 0) {
        diagnostics.push({
          code: "primary_visual_action_missing",
          severity: "error",
          action: "cut",
          message: "TimelineAssembly compile requires at least one primary visual cut."
        });
      }
      for (const overlay of arrayValue32(input3.editDecisions.overlays)) {
        const animation = stringValue142(overlay.animation);
        if (animation && animation !== "static" && animation !== "fade-in") {
          diagnostics.push({
            code: "overlay_animation_degraded",
            severity: "warning",
            action: "overlay",
            message: `Overlay animation ${animation} will be carried as metadata unless the selected finishing backend implements it.`,
            details: { animation }
          });
        }
      }
      return diagnostics;
    }
    function summarizeEditDecisionActions2(editDecisions) {
      const audio = recordValue32(editDecisions.audio);
      const narration = recordValue32(audio?.narration);
      const narrationSegments = Array.isArray(audio?.narration) ? audio?.narration : arrayValue32(narration?.segments);
      const subtitles = recordValue32(editDecisions.subtitles);
      const actionCounts = {
        cut: arrayValue32(editDecisions.cuts).length,
        overlay: arrayValue32(editDecisions.overlays).length,
        narration_segment: narrationSegments.length,
        music_bed: recordValue32(audio?.music) ? 1 : 0,
        sfx_hit: arrayValue32(audio?.sfx).length,
        subtitle_segment: arrayValue32(subtitles?.segments).length + arrayValue32(subtitles?.captions).length
      };
      return {
        actionCounts,
        supportedActions: Object.entries(actionCounts).filter(([, count]) => count > 0).map(([action]) => action).sort()
      };
    }
    function collectReferencedAssets2(editDecisions) {
      const refs = [];
      for (const cut of arrayValue32(editDecisions.cuts)) {
        pushAssetRef2(refs, "cut", assetRef22(cut));
      }
      for (const overlay of arrayValue32(editDecisions.overlays)) {
        pushAssetRef2(refs, "overlay", assetRef22(overlay));
      }
      const audio = recordValue32(editDecisions.audio);
      const narration = recordValue32(audio?.narration);
      const narrationSegments = Array.isArray(audio?.narration) ? audio?.narration : arrayValue32(narration?.segments);
      for (const segment of narrationSegments) {
        pushAssetRef2(refs, "narration_segment", assetRef22(recordValue32(segment) ?? {}));
      }
      pushAssetRef2(refs, "music_bed", assetRef22(recordValue32(audio?.music) ?? {}));
      for (const segment of arrayValue32(audio?.sfx)) {
        pushAssetRef2(refs, "sfx_hit", assetRef22(recordValue32(segment) ?? {}));
      }
      const subtitles = recordValue32(editDecisions.subtitles);
      pushAssetRef2(refs, "subtitle_file", assetRef22(subtitles ?? {}));
      return refs;
    }
    function pushAssetRef2(output3, action, value) {
      const ref = refKey22(value);
      if (!ref || ref === "auto" || ref === "segments" || ref === "captions" || ref === "script") return;
      output3.push({ action, ref });
    }
    function assetManifestIndex2(manifest) {
      const resolvableRefs = /* @__PURE__ */ new Set();
      const selectedResourceIds = /* @__PURE__ */ new Set();
      const selectedCandidateIds = /* @__PURE__ */ new Set();
      for (const entry of arrayValue32(manifest?.assets)) {
        const refs = assetRefsFromRecord22(entry);
        const resourceId = resourceIdFromRecord2(entry);
        const candidateId = stringValue142(
          entry.candidate_id ?? entry.candidateId ?? recordValue32(entry.metadata)?.candidate_id ?? recordValue32(entry.metadata)?.candidateId ?? recordValue32(recordValue32(entry.metadata)?.intent_ref)?.candidate_id
        );
        if (resourceId !== void 0) selectedResourceIds.add(resourceId);
        if (candidateId) selectedCandidateIds.add(candidateId);
        if (assetRecordIsResolvable2(entry, resourceId)) {
          for (const ref of refs) resolvableRefs.add(ref);
          if (resourceId !== void 0) resolvableRefs.add(String(resourceId));
        }
      }
      return {
        resolvableRefs,
        selectedResourceIds: [...selectedResourceIds].sort((left, right) => left - right),
        selectedCandidateIds: [...selectedCandidateIds].sort()
      };
    }
    function assetRecordIsResolvable2(record, resourceId) {
      if (resourceId !== void 0) return true;
      return !!assetPathValue2(record.localPath ?? record.local_path ?? record.path ?? record.file_path ?? record.filePath ?? record.src);
    }
    function assetRefsFromRecord22(record) {
      return [
        record.id,
        record.asset_id,
        record.assetId,
        record.ref,
        record.name,
        record.path,
        record.localPath,
        record.local_path,
        record.file_path,
        record.filePath,
        record.resource_id,
        record.resourceId
      ].flatMap((value) => {
        const key = refKey22(value);
        return key ? [key] : [];
      });
    }
    function assetRef22(record) {
      return record.source ?? record.asset_id ?? record.assetId ?? record.resource_id ?? record.resourceId;
    }
    function resourceIdFromRecord2(record) {
      return integerField22(record.resourceId ?? record.resource_id) ?? resourceIdFromString22(stringValue142(record.path) ?? "");
    }
    function assetPathValue2(value) {
      const path = stringValue142(value);
      if (!path) return void 0;
      if (/^(?:content-unit|content_unit):/i.test(path)) return void 0;
      return path;
    }
    function resourceIdFromString22(value) {
      const match = value.match(/^(?:resource|raw_resource|backend_resource)[:_](\d+)$/i);
      const raw = match?.[1];
      return raw === void 0 ? void 0 : integerField22(raw);
    }
    function stableHash2(value) {
      const input3 = stableStringify3(value);
      let hash = 2166136261;
      for (let index = 0; index < input3.length; index += 1) {
        hash ^= input3.charCodeAt(index);
        hash = Math.imul(hash, 16777619);
      }
      return (hash >>> 0).toString(16).padStart(8, "0");
    }
    function stableStringify3(value) {
      if (value === null) return "null";
      if (value === void 0) return '"__undefined__"';
      if (typeof value === "number") return Number.isFinite(value) ? String(value) : "null";
      if (typeof value === "boolean") return value ? "true" : "false";
      if (typeof value === "string") return JSON.stringify(value);
      if (Array.isArray(value)) return `[${value.map((item) => stableStringify3(item)).join(",")}]`;
      if (typeof value === "object") {
        const record = value;
        return `{${Object.keys(record).sort().map((key) => `${JSON.stringify(key)}:${stableStringify3(record[key])}`).join(",")}}`;
      }
      return JSON.stringify(String(value));
    }
    function compactRecord22(record) {
      return Object.fromEntries(Object.entries(record).filter(([, value]) => value !== void 0));
    }
    function positiveNumber32(value) {
      const number = numberField92(value);
      return number === void 0 || number <= 0 ? void 0 : number;
    }
    function numberField92(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
      return void 0;
    }
    function integerField22(value) {
      const number = numberField92(value);
      if (number === void 0 || !Number.isInteger(number) || number <= 0) return void 0;
      return number;
    }
    function stringValue142(value) {
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim();
      return trimmed ? trimmed : void 0;
    }
    function recordValue32(value) {
      if (!value || typeof value !== "object" || Array.isArray(value)) return void 0;
      return value;
    }
    function arrayValue32(value) {
      return Array.isArray(value) ? value : [];
    }
    function refKey22(value) {
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      return stringValue142(value);
    }
    function safeId32(value) {
      const raw = String(value ?? "draft").trim() || "draft";
      return raw.replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "draft";
    }
    var defaultFetch3 = globalThis.fetch;
    var EDITING_SERVICE_NAME3 = "movscript.editing.service";
    var EDITING_SERVICE_CAPABILITIES_ENDPOINT2 = "/v1/editing/capabilities";
    var EDITING_SERVICE_PROJECT_COMMAND_ENDPOINT2 = "/v1/editing/project/command";
    var EDITING_SERVICE_TIMELINE_VIEW_ENDPOINT2 = "/v1/editing/timeline/view";
    var EDITING_SERVICE_TASK_REQUEST_ENDPOINT2 = "/v1/editing/task/request";
    var EDITING_SERVICE_TASK_ACTION_ENDPOINT2 = "/v1/editing/task/action";
    var MEDIA_PIPELINE_SERVICE_NAME2 = "movscript.media.pipeline";
    var MEDIA_PIPELINE_CAPABILITIES_ENDPOINT2 = "/v1/media-pipeline/capabilities";
    var MEDIA_PIPELINE_PROBE_ENDPOINT2 = "/v1/media-pipeline/probe";
    var MEDIA_PIPELINE_TASK_CREATE_ENDPOINT2 = "/v1/media-pipeline/task/create";
    var MEDIA_PIPELINE_TASK_ACTION_ENDPOINT2 = "/v1/media-pipeline/task/action";
    var EditingServiceClient2 = class {
      baseUrl;
      fetchImpl;
      constructor(options) {
        const baseUrl = normalizeEditingServiceBaseUrl2(options.baseUrl);
        if (!baseUrl) throw new Error("editing service baseUrl is required");
        this.baseUrl = baseUrl;
        this.fetchImpl = options.fetch ?? defaultFetch3;
      }
      async capabilities(signal) {
        return this.request("GET", EDITING_SERVICE_CAPABILITIES_ENDPOINT2, void 0, signal);
      }
      async projectCommand(request, signal) {
        return this.request("POST", EDITING_SERVICE_PROJECT_COMMAND_ENDPOINT2, {
          command: request.command,
          input: request.input ?? {}
        }, signal);
      }
      async timelineView(request, signal) {
        return this.request("POST", EDITING_SERVICE_TIMELINE_VIEW_ENDPOINT2, {
          projectDir: request.projectDir,
          kind: request.kind,
          ...request.targetKind !== void 0 ? { targetKind: request.targetKind } : {},
          ...request.targetRef !== void 0 ? { targetRef: request.targetRef } : {},
          ...request.scopeKind !== void 0 ? { scopeKind: request.scopeKind } : {},
          ...request.scopeRef !== void 0 ? { scopeRef: request.scopeRef } : {},
          ...request.productionId !== void 0 ? { productionId: request.productionId } : {},
          ...request.sceneMomentId !== void 0 ? { sceneMomentId: request.sceneMomentId } : {},
          ...request.decisionStore !== void 0 ? { decisionStore: request.decisionStore } : {},
          ...request.projectName !== void 0 ? { projectName: request.projectName } : {},
          ...request.title !== void 0 ? { title: request.title } : {},
          ...request.now !== void 0 ? { now: request.now } : {},
          ...request.defaultDurationMs !== void 0 ? { defaultDurationMs: request.defaultDurationMs } : {},
          ...request.defaultDurationSec !== void 0 ? { defaultDurationSec: request.defaultDurationSec } : {}
        }, signal);
      }
      async taskRequest(request, signal) {
        return this.request("POST", EDITING_SERVICE_TASK_REQUEST_ENDPOINT2, {
          taskType: request.taskType,
          input: request.input ?? {}
        }, signal);
      }
      async taskAction(request, signal) {
        return this.request("POST", EDITING_SERVICE_TASK_ACTION_ENDPOINT2, {
          action: request.action,
          input: request.input ?? {}
        }, signal);
      }
      async request(method, path, body, signal) {
        const response = await this.fetchImpl(`${this.baseUrl}${path}`, {
          method,
          headers: body === void 0 ? void 0 : {
            "content-type": "application/json"
          },
          body: body === void 0 ? void 0 : JSON.stringify(body),
          signal
        });
        if (!response.ok) {
          throw new EditingServiceHTTPError2(response.status, await response.text());
        }
        return response.json();
      }
    };
    var MediaPipelineServiceClient2 = class {
      baseUrl;
      fetchImpl;
      constructor(options) {
        const baseUrl = normalizeMediaPipelineServiceBaseUrl2(options.baseUrl);
        if (!baseUrl) throw new Error("media pipeline service baseUrl is required");
        this.baseUrl = baseUrl;
        this.fetchImpl = options.fetch ?? defaultFetch3;
      }
      async capabilities(signal) {
        return this.request("GET", MEDIA_PIPELINE_CAPABILITIES_ENDPOINT2, void 0, signal);
      }
      async probe(request = {}, signal) {
        return this.request("POST", MEDIA_PIPELINE_PROBE_ENDPOINT2, request, signal);
      }
      async createTask(request, signal) {
        return this.request("POST", MEDIA_PIPELINE_TASK_CREATE_ENDPOINT2, {
          request: request.request
        }, signal);
      }
      async taskAction(request, signal) {
        return this.request("POST", MEDIA_PIPELINE_TASK_ACTION_ENDPOINT2, {
          action: request.action,
          taskId: request.taskId,
          ...request.options ? { options: request.options } : {}
        }, signal);
      }
      async request(method, path, body, signal) {
        const response = await this.fetchImpl(`${this.baseUrl}${path}`, {
          method,
          headers: body === void 0 ? void 0 : {
            "content-type": "application/json"
          },
          body: body === void 0 ? void 0 : JSON.stringify(body),
          signal
        });
        if (!response.ok) {
          throw new MediaPipelineServiceHTTPError2(response.status, await response.text());
        }
        return response.json();
      }
    };
    var EditingServiceHTTPError2 = class extends Error {
      status;
      body;
      constructor(status, body) {
        super(`editing service request failed: ${status}${body ? ` ${body}` : ""}`);
        this.name = "EditingServiceHTTPError";
        this.status = status;
        this.body = body;
      }
    };
    var MediaPipelineServiceHTTPError2 = class extends Error {
      status;
      body;
      constructor(status, body) {
        super(`media pipeline service request failed: ${status}${body ? ` ${body}` : ""}`);
        this.name = "MediaPipelineServiceHTTPError";
        this.status = status;
        this.body = body;
      }
    };
    function createEditingServiceClientFromRuntime2(options = {}) {
      const baseUrl = resolveEditingServiceBaseUrl2(options);
      if (!baseUrl) {
        throw new Error("movscript.editing.service endpoint was not found; start the local runtime daemon or set MOVSCRIPT_EDITING_SERVICE_URL");
      }
      return new EditingServiceClient2({ baseUrl });
    }
    function createMediaPipelineServiceClientFromRuntime2(options = {}) {
      const baseUrl = resolveMediaPipelineServiceBaseUrl2(options);
      if (!baseUrl) {
        throw new Error("movscript.media.pipeline endpoint was not found; start the local runtime daemon or set MOVSCRIPT_MEDIA_PIPELINE_URL");
      }
      return new MediaPipelineServiceClient2({ baseUrl });
    }
    function resolveEditingServiceBaseUrl2(options = {}) {
      const env = options.env ?? process.env;
      const explicit = normalizeEditingServiceBaseUrl2(options.baseUrl) ?? normalizeEditingServiceBaseUrl2(env.MOVSCRIPT_EDITING_SERVICE_URL) ?? normalizeEditingServiceBaseUrl2(env.MOVSCRIPT_EDITING_SERVICE_BASE_URL);
      if (explicit) return explicit;
      const homeDir = options.homeDir ?? resolveMovScriptHomeDir22({ env });
      const snapshot = readRuntimeHomeSnapshot2(homeDir);
      const endpoint = findRuntimeEndpoint2(snapshot, EDITING_SERVICE_NAME3) ?? findRuntimeService2(snapshot, EDITING_SERVICE_NAME3)?.endpoint;
      return normalizeEditingServiceBaseUrl2(endpointURL22(endpoint));
    }
    function resolveMediaPipelineServiceBaseUrl2(options = {}) {
      const env = options.env ?? process.env;
      const explicit = normalizeMediaPipelineServiceBaseUrl2(options.baseUrl) ?? normalizeMediaPipelineServiceBaseUrl2(env.MOVSCRIPT_MEDIA_PIPELINE_URL) ?? normalizeMediaPipelineServiceBaseUrl2(env.MOVSCRIPT_MEDIA_PIPELINE_BASE_URL);
      if (explicit) return explicit;
      const homeDir = options.homeDir ?? resolveMovScriptHomeDir22({ env });
      const snapshot = readRuntimeHomeSnapshot2(homeDir);
      const endpoint = findRuntimeEndpoint2(snapshot, MEDIA_PIPELINE_SERVICE_NAME2) ?? findRuntimeService2(snapshot, MEDIA_PIPELINE_SERVICE_NAME2)?.endpoint;
      return normalizeMediaPipelineServiceBaseUrl2(endpointURL22(endpoint));
    }
    function normalizeEditingServiceBaseUrl2(value) {
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim().replace(/\/+$/, "");
      if (!trimmed) return void 0;
      const url = new URL(trimmed);
      if (url.protocol !== "http:" && url.protocol !== "https:") {
        throw new Error("editing service baseUrl must use http or https");
      }
      return url.toString().replace(/\/+$/, "");
    }
    function normalizeMediaPipelineServiceBaseUrl2(value) {
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim().replace(/\/+$/, "");
      if (!trimmed) return void 0;
      const url = new URL(trimmed);
      if (url.protocol !== "http:" && url.protocol !== "https:") {
        throw new Error("media pipeline service baseUrl must use http or https");
      }
      return url.toString().replace(/\/+$/, "");
    }
    function endpointURL22(endpoint) {
      if (!endpoint) return void 0;
      if (endpoint.url) return endpoint.url;
      if (endpoint.baseURL) return endpoint.baseURL;
      if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
      if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
      return void 0;
    }
    async function loadContentSourceWorkspaceSnapshotFromEngine2(engine) {
      const service22 = engine.workspaceService;
      const [index, review] = await Promise.all([
        service22.loadIndex(),
        engine.review()
      ]);
      const settings = queryMovScriptWorkspaceSettings2(index, { limit: 500 });
      const settingStates = queryMovScriptWorkspaceEntities2(index, { entityKind: "setting_state", limit: 500 });
      const assetsResult = queryMovScriptWorkspaceAssets2(index, { limit: 500 });
      const context = queryMovScriptWorkspaceProductionContext2(index, {
        include: ["productions", "segments", "scene_moments", "storyboards", "audio_cues", "expression_units", "content_units", "keyframes"],
        limit: 1e3
      });
      const productions = context.productions ?? [];
      const sceneMoments = context.scene_moments ?? [];
      const contentUnits = context.content_units ?? [];
      const productionIds = new Set(productions.map((production) => String(production.id ?? production.record.id ?? production.record.ID ?? production.path)));
      const legacyPreviewTimelines = deriveMovScriptWorkspacePreviewTimelines3(index).filter((timeline) => productionIds.has(String(timeline.productionId ?? timeline.productionPath))).filter(isDefined52);
      const timelineAssemblyScopes = timelineAssemblyScopesFromContentUnits2(contentUnits);
      const assemblyPreviewTimelines = timelineAssemblyScopes.map((scope) => deriveMovScriptWorkspaceTimelineAssemblyPreviewTimeline2(index, {
        scopeKind: scope.kind,
        scopeRef: scope.ref,
        targetRef: implicitTimelineAssemblyRef2(scope.kind, scope.ref)
      })).filter(isDefined52);
      const previewTimelines = uniquePreviewTimelines2([...legacyPreviewTimelines, ...assemblyPreviewTimelines]);
      const editingTimelines = (await Promise.all(
        sceneMoments.map(async (sceneMoment) => {
          const sceneMomentId = idField15(sceneMoment.id ?? sceneMoment.record.id ?? sceneMoment.record.ID);
          if (sceneMomentId === void 0) return void 0;
          const editPlan = await readSceneMomentEditPlanForIndexedEntity2(engine, sceneMoment).catch(() => void 0);
          if (!isRecord36(editPlan)) return void 0;
          const mediaEditingProject = createMediaEditingProjectFromMovScriptEditPlan2(editPlan, {
            projectId: String(editPlan.productionId ?? "MovScript"),
            title: String(sceneMoment.record.title ?? editPlan.sceneMomentId ?? sceneMomentId)
          });
          return {
            targetKind: "scene_moment",
            targetId: sceneMomentId,
            targetPath: sceneMoment.path,
            status: typeof editPlan.status === "string" ? editPlan.status : void 0,
            blockers: Array.isArray(editPlan.blockers) ? editPlan.blockers : void 0,
            mediaEditingProject
          };
        })
      )).filter(isDefined52);
      const productionEditingTimelines = legacyPreviewTimelines.map(
        (timeline) => productionTimelineFromPreview2({
          previewTimeline: timeline,
          contentUnits,
          documents: index.documents,
          productions
        })
      );
      const assemblyEditingTimelines = assemblyPreviewTimelines.map(
        (timeline) => timelineAssemblyTimelineFromPreview2({
          previewTimeline: timeline,
          contentUnits,
          documents: index.documents
        })
      );
      return {
        indexDocuments: index.documents,
        namespaceVocabulary: index.namespaceVocabulary,
        domainNodes: index.domainNodes,
        domainEdges: index.domainEdges,
        settings,
        settingStates,
        assets: assetsResult.assets,
        productions,
        segments: context.segments ?? [],
        sceneMoments,
        storyboards: context.storyboards ?? [],
        keyframes: context.keyframes ?? [],
        expressionUnits: context.expression_units ?? [],
        audioCues: context.audio_cues ?? [],
        contentUnits,
        previewTimelines,
        editingTimelines: [...productionEditingTimelines, ...assemblyEditingTimelines, ...editingTimelines],
        productionWorkPlan: productionWorkPlanFromReview2(review)
      };
    }
    async function readSceneMomentEditPlanForIndexedEntity2(engine, sceneMoment) {
      const sceneMomentDir = sceneMoment.path.replace(/\/scene_moment\.json$/, "");
      if (sceneMomentDir === sceneMoment.path) return void 0;
      const path = (0, import_path32.join)(engine.projectDir, MOVSCRIPT_INTERPRET_CURRENT_DIR2, sceneMomentDir, "edit_plan.json");
      const parsed = JSON.parse(await (0, import_promises52.readFile)(path, "utf8"));
      return isRecord36(parsed) ? parsed : void 0;
    }
    function productionTimelineFromPreview2(input3) {
      const candidateRecords = contentCandidateRecordsByContentUnitId22(input3.documents);
      const selections = selectionRecordsByContentUnitId22(input3.documents);
      const contentUnitsById = new Map(input3.contentUnits.map((unit) => [String(unit.id ?? pathSegmentAfter72(unit.path, "content_units") ?? unit.path), unit]));
      const blockers = [];
      const clips = input3.previewTimeline.items.filter((item) => item.itemType === "scene_moment").sort((left, right) => left.order - right.order).flatMap((item, index) => {
        const contentUnitIds = productionSceneMomentContentUnitIds3(input3.contentUnits, item);
        if (contentUnitIds.length === 0) {
          blockers.push({
            code: "scene_moment_content_unit_missing",
            scene_moment_id: item.entity.id,
            scene_moment_path: item.entity.path
          });
          return [];
        }
        for (const contentUnitId of contentUnitIds) {
          const selection = selections.get(String(contentUnitId));
          const candidate = selection?.candidate_id !== void 0 ? candidateRecords.get(String(contentUnitId))?.find((entry) => sameId52(entry.id, selection.candidate_id)) : void 0;
          const output3 = firstCandidateOutput32(candidate);
          const resourceId = numberField102(output3?.resource_id);
          if (resourceId !== void 0 && (stringField262(output3?.kind) ?? "video") === "video") {
            return [{
              id: `production_clip_${safeId42(String(item.entity.id ?? index))}_${safeId42(String(contentUnitId))}`,
              title: previewTimelineItemTitle3(item) ?? stringField262(item.entity.id) ?? `Scene ${index + 1}`,
              sceneMomentId: item.entity.id,
              sceneMomentPath: item.entity.path,
              contentUnitId,
              candidateId: selection?.candidate_id,
              resourceId,
              durationSec: numberField102(output3?.duration_sec) ?? 4
            }];
          }
          blockers.push({
            code: selection?.candidate_id === void 0 ? "scene_moment_selection_missing" : "scene_moment_resource_missing",
            scene_moment_id: item.entity.id,
            scene_moment_path: item.entity.path,
            content_unit_id: contentUnitId,
            candidate_id: selection?.candidate_id,
            output_kind: stringField262(contentUnitsById.get(String(contentUnitId))?.record.output_kind)
          });
        }
        return [];
      });
      const productionId = input3.previewTimeline.productionId ?? "unknown";
      const productionPath = stringField262(input3.previewTimeline.productionPath);
      const production = input3.productions.find(
        (item) => sameId52(item.id, productionId) || productionPath !== void 0 && item.path.startsWith(productionPath)
      );
      return {
        targetKind: "production",
        targetId: productionId,
        targetPath: production?.path ?? productionPath,
        status: blockers.length > 0 ? "blocked" : "ready_to_compose",
        blockers,
        mediaEditingProject: createMediaEditingProjectFromProductionTimelineClips2({
          productionId,
          title: stringField262(production?.record.title) ?? String(productionId),
          productionPath: production?.path ?? productionPath,
          clips
        })
      };
    }
    function timelineAssemblyTimelineFromPreview2(input3) {
      const candidateRecords = contentCandidateRecordsByContentUnitId22(input3.documents);
      const selections = selectionRecordsByContentUnitId22(input3.documents);
      const contentUnitsById = new Map(input3.contentUnits.map((unit) => [String(unit.id ?? pathSegmentAfter72(unit.path, "content_units") ?? unit.path), unit]));
      const blockers = [];
      const clips = input3.previewTimeline.items.filter((item) => item.itemType === "scene_moment").sort((left, right) => left.order - right.order).flatMap((item, index) => {
        const contentUnitIds = productionSceneMomentContentUnitIds3(input3.contentUnits, item);
        if (contentUnitIds.length === 0) {
          blockers.push({
            code: "scene_moment_content_unit_missing",
            scene_moment_id: item.entity.id,
            scene_moment_path: item.entity.path
          });
          return [];
        }
        for (const contentUnitId of contentUnitIds) {
          const selection = selections.get(String(contentUnitId));
          const candidate = selection?.candidate_id !== void 0 ? candidateRecords.get(String(contentUnitId))?.find((entry) => sameId52(entry.id, selection.candidate_id)) : void 0;
          const output3 = firstCandidateOutput32(candidate);
          const resourceId = numberField102(output3?.resource_id);
          if (resourceId !== void 0 && (stringField262(output3?.kind) ?? "video") === "video") {
            return [{
              id: `assembly_clip_${safeId42(String(item.entity.id ?? index))}_${safeId42(String(contentUnitId))}`,
              title: previewTimelineItemTitle3(item) ?? stringField262(item.entity.id) ?? `Scene ${index + 1}`,
              sceneMomentId: item.entity.id,
              sceneMomentPath: item.entity.path,
              contentUnitId,
              candidateId: selection?.candidate_id,
              resourceId,
              durationSec: numberField102(output3?.duration_sec) ?? 4
            }];
          }
          blockers.push({
            code: selection?.candidate_id === void 0 ? "scene_moment_selection_missing" : "scene_moment_resource_missing",
            scene_moment_id: item.entity.id,
            scene_moment_path: item.entity.path,
            content_unit_id: contentUnitId,
            candidate_id: selection?.candidate_id,
            output_kind: stringField262(contentUnitsById.get(String(contentUnitId))?.record.output_kind)
          });
        }
        return [];
      });
      const scopeKind = stringField262(input3.previewTimeline.scopeKind) ?? "timeline";
      const scopeRef = idField15(input3.previewTimeline.scopeRef) ?? input3.previewTimeline.scopePath ?? input3.previewTimeline.targetRef ?? "unknown";
      const targetRef = stringField262(input3.previewTimeline.targetRef) ?? implicitTimelineAssemblyRef2(scopeKind, String(scopeRef));
      const scopePath = stringField262(input3.previewTimeline.scopePath);
      return {
        targetKind: "timeline_assembly",
        targetId: targetRef,
        targetRef,
        targetPath: scopePath,
        scopeKind,
        scopeRef,
        scopePath,
        status: blockers.length > 0 ? "blocked" : "ready_to_compose",
        blockers,
        mediaEditingProject: createMediaEditingProjectFromTimelineAssemblyClips2({
          targetRef,
          scopeKind,
          scopeRef,
          scopePath,
          title: stringField262(input3.previewTimeline.scopeTitle) ?? `Timeline assembly ${scopeKind}:${String(scopeRef)}`,
          clips
        })
      };
    }
    function timelineAssemblyScopesFromContentUnits2(contentUnits) {
      const seen = /* @__PURE__ */ new Set();
      const output3 = [];
      for (const contentUnit of contentUnits) {
        const scope = timelineAssemblyScopeFromContentUnitRecord2(contentUnit.record);
        if (!scope) continue;
        const ref = String(scope.ref);
        const key = `${scope.kind}:${ref}`;
        if (seen.has(key)) continue;
        seen.add(key);
        output3.push({ kind: scope.kind, ref });
      }
      return output3;
    }
    function uniquePreviewTimelines2(timelines) {
      const seen = /* @__PURE__ */ new Set();
      const output3 = [];
      for (const timeline of timelines) {
        const key = stringField262(timeline.targetRef) ?? (timeline.productionId !== void 0 ? `production:${String(timeline.productionId)}` : void 0) ?? stringField262(timeline.scopePath) ?? JSON.stringify(timeline.items.map((item) => item.id));
        if (seen.has(key)) continue;
        seen.add(key);
        output3.push(timeline);
      }
      return output3;
    }
    function productionSceneMomentContentUnitIds3(contentUnits, item) {
      const explicit = Array.isArray(item.contentUnitIds) ? (item.contentUnitIds ?? []).filter((id) => typeof id === "string" || typeof id === "number") : [];
      const scanned = contentUnits.filter((unit) => isSceneMomentVideoContentUnit3(unit.record) && sceneMomentRefMatches3(unit.record, item)).map((unit) => unit.id ?? pathSegmentAfter72(unit.path, "content_units")).filter((id) => typeof id === "string" || typeof id === "number");
      return [...new Map([...explicit, ...scanned].map((id) => [String(id), id])).values()];
    }
    function previewTimelineItemTitle3(item) {
      return stringField262(item.title);
    }
    function isSceneMomentVideoContentUnit3(record) {
      const type = stringField262(record.content_unit_type);
      if (type !== "scene_moment_ref" && type !== "scence_moment_ref") return false;
      const outputKind = stringField262(record.output_kind);
      return outputKind === void 0 || outputKind === "video";
    }
    function sceneMomentRefMatches3(record, item) {
      const refs = [record.scene_moment_ref, record.scence_moment_ref].flatMap((value) => {
        const id = idField15(value);
        return id === void 0 ? [] : [String(id)];
      });
      return refs.some(
        (ref) => sameId52(ref, item.entity.id) || ref === item.entity.path || lastPathSegment72(ref) === lastPathSegment72(item.entity.path)
      );
    }
    function contentCandidateRecordsByContentUnitId22(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (!document.path.endsWith("/content_candidate.json") || !isRecord36(document.data)) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument32(document.path, stringField262(document.data.content_unit_ref));
        if (!contentUnitId) continue;
        output3.set(contentUnitId, [...output3.get(contentUnitId) ?? [], document.data]);
      }
      return output3;
    }
    function selectionRecordsByContentUnitId22(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (!isRecord36(document.data)) continue;
        const selection = isRecord36(document.data.selection) ? document.data.selection : void 0;
        if (!selection) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument32(document.path, stringField262(document.data.target_ref));
        if (!contentUnitId) continue;
        output3.set(contentUnitId, {
          ...idField15(selection.candidate_id) !== void 0 ? { candidate_id: idField15(selection.candidate_id) } : {}
        });
      }
      return output3;
    }
    function contentUnitIdForRuntimeDocument32(path, ref) {
      if (ref) return lastPathSegment72(ref) ?? ref;
      return pathSegmentAfter72(path, "content_units");
    }
    function firstCandidateOutput32(candidate) {
      return Array.isArray(candidate?.outputs) ? candidate.outputs.find(isRecord36) : void 0;
    }
    function productionWorkPlanFromReview2(value) {
      if (!isRecord36(value)) return void 0;
      return value.productionWorkPlan;
    }
    function isDefined52(value) {
      return value !== void 0;
    }
    function isRecord36(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function idField15(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function stringField262(value) {
      const id = idField15(value);
      return id === void 0 ? void 0 : String(id);
    }
    function numberField102(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
      return void 0;
    }
    function pathSegmentAfter72(path, segment) {
      if (!path) return void 0;
      const parts = path.split("/").filter(Boolean);
      const index = parts.indexOf(segment);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function lastPathSegment72(path) {
      return path?.split("/").filter(Boolean).at(-1);
    }
    function safeId42(value) {
      return value.replace(/[^a-zA-Z0-9_-]+/g, "_") || "item";
    }
    function sameId52(left, right) {
      return left !== void 0 && right !== void 0 && String(left) === String(right);
    }
    var defaultFetch22 = globalThis.fetch;
    var PROJECT_SERVICE_NAME3 = "movscript.project.service";
    var PROJECT_SERVICE_CAPABILITIES_ENDPOINT2 = "/v1/project/capabilities";
    var PROJECT_SERVICE_SOURCE_SNAPSHOT_ENDPOINT2 = "/v1/project/source/snapshot";
    var PROJECT_SERVICE_SOURCE_INSPECT_ENDPOINT2 = "/v1/project/source/inspect";
    var PROJECT_SERVICE_SOURCE_OVERVIEW_ENDPOINT2 = "/v1/project/source/overview";
    var PROJECT_SERVICE_READ_MODEL_ENDPOINT2 = "/v1/project/read-model";
    var PROJECT_SERVICE_HOME_READ_MODEL_ENDPOINT2 = "/v1/project/home/read-model";
    var PROJECT_SERVICE_STANDARDS_READ_MODEL_ENDPOINT2 = "/v1/project/standards/read-model";
    var PROJECT_SERVICE_CONTENT_CANVAS_READ_MODEL_ENDPOINT2 = "/v1/project/content-canvas/read-model";
    var PROJECT_SERVICE_SCRIPTS_READ_MODEL_ENDPOINT2 = "/v1/project/scripts/read-model";
    var PROJECT_SERVICE_LOCATOR_RESOLVE_ENDPOINT2 = "/v1/project/locator/resolve";
    var PROJECT_SERVICE_RESOURCE_VIEW_ENDPOINT2 = "/v1/project/resources/view";
    var PROJECT_SERVICE_LIFECYCLE_COMMAND_ENDPOINT2 = "/v1/project/lifecycle/command";
    var PROJECT_SERVICE_SOURCE_INTERPRET_ENDPOINT2 = "/v1/project/source/interpret";
    var PROJECT_SERVICE_SOURCE_REGENERATION_PLAN_ENDPOINT2 = "/v1/project/source/regeneration-plan";
    var PROJECT_SERVICE_SOURCE_COMMAND_ENDPOINT2 = "/v1/project/source/command";
    var PROJECT_SERVICE_STANDARDS_UPSERT_ENDPOINT2 = "/v1/project/standards/upsert";
    var PROJECT_SERVICE_SCRIPT_SOURCE_READ_ENDPOINT2 = "/v1/project/scripts/source/read";
    var PROJECT_SERVICE_SCRIPT_UPSERT_ENDPOINT2 = "/v1/project/scripts/upsert";
    var PROJECT_SERVICE_SCRIPT_VERSION_SNAPSHOT_ENDPOINT2 = "/v1/project/scripts/versions/snapshot";
    var PROJECT_SERVICE_SETTING_UPSERT_ENDPOINT2 = "/v1/project/settings/upsert";
    var PROJECT_SERVICE_SETTING_CREATE_ENDPOINT2 = "/v1/project/settings/create";
    var PROJECT_SERVICE_SETTING_STATE_CREATE_ENDPOINT2 = "/v1/project/settings/states/create";
    var PROJECT_SERVICE_ASSET_UPSERT_ENDPOINT2 = "/v1/project/assets/upsert";
    var PROJECT_SERVICE_ASSET_CREATE_ENDPOINT2 = "/v1/project/assets/create";
    var PROJECT_SERVICE_CONTENT_UNIT_UPSERT_ENDPOINT2 = "/v1/project/content-units/upsert";
    var PROJECT_SERVICE_CONTENT_UNIT_CREATE_ENDPOINT2 = "/v1/project/content-units/create";
    var PROJECT_SERVICE_CONTENT_UNIT_ENSURE_ENDPOINT2 = "/v1/project/content-units/ensure";
    var PROJECT_SERVICE_CONTENT_UNIT_EDIT_PROMPT_UPDATE_ENDPOINT2 = "/v1/project/content-units/edit-prompt/update";
    var PROJECT_SERVICE_PRODUCTION_CREATE_ENDPOINT2 = "/v1/project/productions/create";
    var PROJECT_SERVICE_SEGMENT_CREATE_ENDPOINT2 = "/v1/project/segments/create";
    var PROJECT_SERVICE_SCENE_MOMENT_CREATE_ENDPOINT2 = "/v1/project/scene-moments/create";
    var PROJECT_SERVICE_EXPRESSION_UNIT_CREATE_ENDPOINT2 = "/v1/project/expression-units/create";
    var PROJECT_SERVICE_KEYFRAME_CREATE_ENDPOINT2 = "/v1/project/keyframes/create";
    var PROJECT_SERVICE_STORYBOARD_CREATE_ENDPOINT2 = "/v1/project/storyboards/create";
    var PROJECT_SERVICE_STORYBOARD_TIMELINE_UPDATE_ENDPOINT2 = "/v1/project/storyboards/timeline/update";
    var PROJECT_SERVICE_AUDIO_CUE_CREATE_ENDPOINT2 = "/v1/project/audio-cues/create";
    var PROJECT_SERVICE_ENTITY_TRANSITION_UPDATE_ENDPOINT2 = "/v1/project/entities/transition/update";
    var PROJECT_SERVICE_ENTITY_DELETE_ENDPOINT2 = "/v1/project/entities/delete";
    var PROJECT_SERVICE_HIERARCHY_WRITE_ENDPOINT2 = "/v1/project/hierarchy/write";
    var PROJECT_SERVICE_NAMESPACE_WRITE_ENDPOINT2 = "/v1/project/namespaces/write";
    var PROJECT_SERVICE_WORKSPACE_CANDIDATE_SELECT_ENDPOINT2 = "/v1/project/workspace-candidates/select";
    var PROJECT_SERVICE_WORKSPACE_CANDIDATE_APPEND_ENDPOINT2 = "/v1/project/workspace-candidates/append";
    var PROJECT_SERVICE_WORKSPACE_ASSET_SLOT_CANDIDATE_CREATE_ENDPOINT2 = "/v1/project/workspace-candidates/asset-slots/create";
    var PROJECT_SERVICE_WORKSPACE_KEYFRAME_CANDIDATE_CREATE_ENDPOINT2 = "/v1/project/workspace-candidates/keyframes/create";
    var PROJECT_SERVICE_CONTENT_CANDIDATE_CREATE_ENDPOINT2 = "/v1/project/content-candidates/create";
    var PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_SELECT_ENDPOINT2 = "/v1/project/content-unit-candidates/select";
    var PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_DECIDE_ENDPOINT2 = "/v1/project/content-unit-candidates/decide";
    var PROJECT_SERVICE_CANDIDATE_COMMAND_ENDPOINT2 = "/v1/project/candidates/command";
    var PROJECT_SERVICE_CANDIDATE_VIEW_ENDPOINT2 = "/v1/project/candidates/view";
    var PROJECT_SERVICE_PROMPT_CONTEXT_ENDPOINT2 = "/v1/project/prompt/context";
    var ProjectServiceClient2 = class {
      baseUrl;
      fetchImpl;
      constructor(options) {
        const baseUrl = normalizeProjectServiceBaseUrl2(options.baseUrl);
        if (!baseUrl) throw new Error("project service baseUrl is required");
        this.baseUrl = baseUrl;
        this.fetchImpl = options.fetch ?? defaultFetch22;
      }
      async capabilities(signal) {
        return this.request("GET", PROJECT_SERVICE_CAPABILITIES_ENDPOINT2, void 0, signal);
      }
      async sourceSnapshot(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SOURCE_SNAPSHOT_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async inspectSource(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SOURCE_INSPECT_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async overviewSource(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SOURCE_OVERVIEW_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async readModel(request, signal) {
        return this.request("POST", PROJECT_SERVICE_READ_MODEL_ENDPOINT2, projectReadModelPayload2(request), signal);
      }
      async homeReadModel(request, signal) {
        return this.request("POST", PROJECT_SERVICE_HOME_READ_MODEL_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async standardsReadModel(request, signal) {
        return this.request("POST", PROJECT_SERVICE_STANDARDS_READ_MODEL_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async contentCanvasReadModel(request, signal) {
        return this.request("POST", PROJECT_SERVICE_CONTENT_CANVAS_READ_MODEL_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async scriptsReadModel(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SCRIPTS_READ_MODEL_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async interpretSource(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SOURCE_INTERPRET_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async regenerationPlan(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SOURCE_REGENERATION_PLAN_ENDPOINT2, projectSourcePayload2(request), signal);
      }
      async sourceCommand(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SOURCE_COMMAND_ENDPOINT2, {
          projectDir: request.projectDir,
          command: request.command,
          input: request.input ?? {}
        }, signal);
      }
      async upsertProjectStandards(request, signal) {
        return this.request("POST", PROJECT_SERVICE_STANDARDS_UPSERT_ENDPOINT2, projectSourceOperationPayload2(request), signal);
      }
      async readScriptSource(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SCRIPT_SOURCE_READ_ENDPOINT2, projectSourceOperationPayload2(request), signal);
      }
      async upsertScript(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SCRIPT_UPSERT_ENDPOINT2, projectSourceOperationPayload2(request), signal);
      }
      async snapshotScriptVersionFromMarkdown(request, signal) {
        return this.request("POST", PROJECT_SERVICE_SCRIPT_VERSION_SNAPSHOT_ENDPOINT2, projectSourceOperationPayload2(request), signal);
      }
      async sourceOperation(endpoint, request, signal) {
        return this.request("POST", endpoint, projectSourceOperationPayload2(request), signal);
      }
      async selectWorkspaceCandidate(request, signal) {
        return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_CANDIDATE_SELECT_ENDPOINT2, request, signal);
      }
      async appendWorkspaceCandidate(request, signal) {
        return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_CANDIDATE_APPEND_ENDPOINT2, request, signal);
      }
      async createWorkspaceAssetSlotCandidate(request, signal) {
        return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_ASSET_SLOT_CANDIDATE_CREATE_ENDPOINT2, request, signal);
      }
      async createWorkspaceKeyframeCandidate(request, signal) {
        return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_KEYFRAME_CANDIDATE_CREATE_ENDPOINT2, request, signal);
      }
      async createContentCandidate(request, signal) {
        return this.candidateAction(PROJECT_SERVICE_CONTENT_CANDIDATE_CREATE_ENDPOINT2, request, signal);
      }
      async selectContentUnitCandidate(request, signal) {
        return this.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_SELECT_ENDPOINT2, request, signal);
      }
      async decideContentUnitCandidate(request, signal) {
        return this.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_DECIDE_ENDPOINT2, request, signal);
      }
      async lifecycleCommand(request, signal) {
        return this.request("POST", PROJECT_SERVICE_LIFECYCLE_COMMAND_ENDPOINT2, {
          projectDir: request.projectDir,
          command: request.command,
          input: request.input ?? {}
        }, signal);
      }
      async resolveLocator(request, signal) {
        return this.request("POST", PROJECT_SERVICE_LOCATOR_RESOLVE_ENDPOINT2, {
          projectDir: request.projectDir,
          ...request.workspaceDir ? { workspaceDir: request.workspaceDir } : {},
          ...request.projectUid ? { projectUid: request.projectUid } : {}
        }, signal);
      }
      async resourceView(request, signal) {
        return this.request("POST", PROJECT_SERVICE_RESOURCE_VIEW_ENDPOINT2, {
          projectDir: request.projectDir,
          kind: request.kind
        }, signal);
      }
      async candidateCommand(request, signal) {
        return this.request("POST", PROJECT_SERVICE_CANDIDATE_COMMAND_ENDPOINT2, {
          projectDir: request.projectDir,
          command: request.command,
          input: request.input ?? {},
          decisionStore: request.decisionStore
        }, signal);
      }
      async candidateAction(endpoint, request, signal) {
        return this.request("POST", endpoint, {
          projectDir: request.projectDir,
          input: request.input ?? {},
          decisionStore: request.decisionStore
        }, signal);
      }
      async candidateView(request, signal) {
        return this.request("POST", PROJECT_SERVICE_CANDIDATE_VIEW_ENDPOINT2, {
          projectDir: request.projectDir,
          ...request.contentUnitId !== void 0 ? { contentUnitId: request.contentUnitId } : {},
          ...request.contentUnitIds !== void 0 ? { contentUnitIds: request.contentUnitIds } : {},
          decisionStore: request.decisionStore
        }, signal);
      }
      async promptContext(request, signal) {
        return this.request("POST", PROJECT_SERVICE_PROMPT_CONTEXT_ENDPOINT2, {
          projectDir: request.projectDir,
          ...request.contentUnitId !== void 0 ? { contentUnitId: request.contentUnitId } : {},
          ...request.contentUnitIds !== void 0 ? { contentUnitIds: request.contentUnitIds } : {},
          ...request.include !== void 0 ? { include: request.include } : {},
          ...request.promptText !== void 0 ? { promptText: request.promptText } : {},
          ...request.decisionStore ? { decisionStore: request.decisionStore } : {}
        }, signal);
      }
      async request(method, path, body, signal) {
        const response = await this.fetchImpl(`${this.baseUrl}${path}`, {
          method,
          headers: body === void 0 ? void 0 : {
            "content-type": "application/json"
          },
          body: body === void 0 ? void 0 : JSON.stringify(body),
          signal
        });
        if (!response.ok) {
          throw new ProjectServiceHTTPError2(response.status, await response.text());
        }
        return response.json();
      }
    };
    var ProjectServiceHTTPError2 = class extends Error {
      status;
      body;
      constructor(status, body) {
        super(`project service request failed: ${status}${body ? ` ${body}` : ""}`);
        this.name = "ProjectServiceHTTPError";
        this.status = status;
        this.body = body;
      }
    };
    function createProjectServiceClientFromRuntime2(options = {}) {
      const baseUrl = resolveProjectServiceBaseUrl2(options);
      if (!baseUrl) {
        throw new Error("movscript.project.service endpoint was not found; start the local runtime daemon or set MOVSCRIPT_PROJECT_SERVICE_URL");
      }
      return new ProjectServiceClient2({ baseUrl });
    }
    function resolveProjectServiceBaseUrl2(options = {}) {
      const env = options.env ?? process.env;
      const explicit = normalizeProjectServiceBaseUrl2(options.baseUrl) ?? normalizeProjectServiceBaseUrl2(env.MOVSCRIPT_PROJECT_SERVICE_URL) ?? normalizeProjectServiceBaseUrl2(env.MOVSCRIPT_PROJECT_SERVICE_BASE_URL);
      if (explicit) return explicit;
      const homeDir = options.homeDir ?? resolveMovScriptHomeDir22({ env });
      const snapshot = readRuntimeHomeSnapshot2(homeDir);
      const endpoint = findRuntimeEndpoint2(snapshot, PROJECT_SERVICE_NAME3) ?? findRuntimeService2(snapshot, PROJECT_SERVICE_NAME3)?.endpoint;
      return normalizeProjectServiceBaseUrl2(endpointURL32(endpoint));
    }
    function normalizeProjectServiceBaseUrl2(value) {
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim().replace(/\/+$/, "");
      if (!trimmed) return void 0;
      const url = new URL(trimmed);
      if (url.protocol !== "http:" && url.protocol !== "https:") {
        throw new Error("project service baseUrl must use http or https");
      }
      return url.toString().replace(/\/+$/, "");
    }
    function endpointURL32(endpoint) {
      if (!endpoint) return void 0;
      if (endpoint.url) return endpoint.url;
      if (endpoint.baseURL) return endpoint.baseURL;
      if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
      if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
      return void 0;
    }
    function projectSourcePayload2(request) {
      return {
        projectDir: request.projectDir,
        ...request.projectId !== void 0 ? { projectId: request.projectId } : {},
        ...request.projectUid ? { projectUid: request.projectUid } : {},
        ...request.includeContentUnitDecisionDocuments !== void 0 ? {
          includeContentUnitDecisionDocuments: request.includeContentUnitDecisionDocuments
        } : {},
        ...request.debugArtifacts !== void 0 ? { debugArtifacts: request.debugArtifacts } : {},
        ...request.commit ? { commit: request.commit } : {},
        ...request.checkpointHash ? { checkpointHash: request.checkpointHash } : {}
      };
    }
    function projectSourceOperationPayload2(request) {
      return {
        projectDir: request.projectDir,
        ...request.input ?? {}
      };
    }
    function projectReadModelPayload2(request) {
      return {
        ...projectSourcePayload2(request),
        ...request.includeSource !== void 0 ? { includeSource: request.includeSource } : {},
        ...request.includeInspection !== void 0 ? { includeInspection: request.includeInspection } : {}
      };
    }
    var import_path42 = __require("path");
    var import_path52 = __require("path");
    var import_child_process5 = __require("child_process");
    var import_promises62 = __require("fs/promises");
    var import_os4 = __require("os");
    var import_path62 = __require("path");
    var import_fs32 = __require("fs");
    var import_path72 = __require("path");
    var import_child_process22 = __require("child_process");
    var import_crypto22 = __require("crypto");
    var import_promises72 = __require("fs/promises");
    var import_os22 = __require("os");
    var import_path82 = __require("path");
    var import_promises82 = __require("fs/promises");
    var import_path92 = __require("path");
    var import_path102 = __require("path");
    var contextSnapshot2 = {
      route: { pathname: "/", search: "", hash: "" },
      project: null,
      user: null,
      selection: null,
      updatedAt: (/* @__PURE__ */ new Date(0)).toISOString()
    };
    var contextAuthToken2 = "";
    function getMCPContextSnapshot2() {
      return { ...contextSnapshot2 };
    }
    function getMCPFocusSnapshot2() {
      return {
        ...contextSnapshot2,
        route: sanitizeFocusRoute2(contextSnapshot2.route)
      };
    }
    function getMCPAuthToken2() {
      return contextAuthToken2;
    }
    function sanitizeFocusRoute2(route) {
      return {
        ...route,
        search: sanitizeFocusSearch2(route.search)
      };
    }
    function sanitizeFocusSearch2(search) {
      if (!search || !search.includes("workspaceId")) return search;
      const params = new URLSearchParams(search.startsWith("?") ? search.slice(1) : search);
      if (!params.has("workspaceId")) return search;
      params.delete("workspaceId");
      const next = params.toString();
      return next ? `?${next}` : "";
    }
    function createMovScriptDomainRuntime2(input3) {
      const context = normalizeRuntimeInput2(input3);
      const cacheKey = runtimeKey2(context);
      const projectCwd = projectCwdFromInput2(context);
      const decisionStoreConfig = createMCPDecisionStoreConfigResolver2(context);
      const decisionStore = createMCPDecisionStore2(decisionStoreConfig);
      const engine = movScriptDomainEngineRegistry2.get({
        cacheKey,
        projectDir: projectCwd,
        decisionStore
      });
      return createMovScriptDomainRuntimeFromEngine2(engine, projectCwd, decisionStore, decisionStoreConfig);
    }
    function invalidateMovScriptDomainRuntime2(input3) {
      movScriptDomainEngineRegistry2.invalidate(runtimeKey2(normalizeRuntimeInput2(input3)));
    }
    var movScriptDomainEngineRegistry2 = new NodeMovScriptEngineRegistry2();
    function createMovScriptDomainRuntimeFromEngine2(engine, projectCwd, decisionStore, decisionStoreConfig) {
      const projectService = createProjectServiceAccessor2(projectCwd, decisionStoreConfig);
      return {
        ...engine.workspaceService,
        ...engine,
        projectCwd,
        projectDir: projectCwd,
        decisionStore,
        upsertProjectStandards: (input3) => projectService.upsertProjectStandards(input3),
        upsertSetting: (input3) => projectService.sourceOperation(PROJECT_SERVICE_SETTING_UPSERT_ENDPOINT2, input3),
        createSetting: (input3) => projectService.sourceOperation(PROJECT_SERVICE_SETTING_CREATE_ENDPOINT2, input3),
        createSettingState: (input3) => projectService.sourceOperation(PROJECT_SERVICE_SETTING_STATE_CREATE_ENDPOINT2, input3),
        upsertAsset: (input3) => projectService.sourceOperation(PROJECT_SERVICE_ASSET_UPSERT_ENDPOINT2, input3),
        createAsset: (input3) => projectService.sourceOperation(PROJECT_SERVICE_ASSET_CREATE_ENDPOINT2, input3),
        upsertScript: (input3) => projectService.upsertScript(input3),
        snapshotScriptVersionFromMarkdown: (input3) => projectService.snapshotScriptVersionFromMarkdown(input3),
        upsertContentUnit: (input3) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_UPSERT_ENDPOINT2, input3),
        createContentUnit: (input3) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_CREATE_ENDPOINT2, input3),
        ensureContentUnitForEntity: (input3) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_ENSURE_ENDPOINT2, input3),
        writeHierarchyNode: (input3) => projectService.sourceOperation(isNamespaceHierarchyWrite2(input3) ? PROJECT_SERVICE_NAMESPACE_WRITE_ENDPOINT2 : PROJECT_SERVICE_HIERARCHY_WRITE_ENDPOINT2, input3),
        createProduction: (input3) => projectService.sourceOperation(PROJECT_SERVICE_PRODUCTION_CREATE_ENDPOINT2, input3),
        createSegment: (input3) => projectService.sourceOperation(PROJECT_SERVICE_SEGMENT_CREATE_ENDPOINT2, input3),
        createSceneMoment: (input3) => projectService.sourceOperation(PROJECT_SERVICE_SCENE_MOMENT_CREATE_ENDPOINT2, input3),
        createStoryboard: (input3) => projectService.sourceOperation(PROJECT_SERVICE_STORYBOARD_CREATE_ENDPOINT2, input3),
        createKeyframe: (input3) => projectService.sourceOperation(PROJECT_SERVICE_KEYFRAME_CREATE_ENDPOINT2, input3),
        createAudioCue: (input3) => projectService.sourceOperation(PROJECT_SERVICE_AUDIO_CUE_CREATE_ENDPOINT2, input3),
        createExpressionUnit: (input3) => projectService.sourceOperation(PROJECT_SERVICE_EXPRESSION_UNIT_CREATE_ENDPOINT2, input3),
        updateContentUnitEditPrompt: (input3) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_EDIT_PROMPT_UPDATE_ENDPOINT2, input3),
        updateEntityTransition: (input3) => projectService.sourceOperation(PROJECT_SERVICE_ENTITY_TRANSITION_UPDATE_ENDPOINT2, input3),
        updateStoryboardTimeline: (input3) => projectService.sourceOperation(PROJECT_SERVICE_STORYBOARD_TIMELINE_UPDATE_ENDPOINT2, input3),
        deleteEntity: (input3) => projectService.sourceOperation(PROJECT_SERVICE_ENTITY_DELETE_ENDPOINT2, input3).then(() => void 0),
        createContentCandidate: (input3) => projectService.candidateAction(PROJECT_SERVICE_CONTENT_CANDIDATE_CREATE_ENDPOINT2, input3),
        selectContentUnitCandidate: (input3) => projectService.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_SELECT_ENDPOINT2, input3),
        decideContentUnitCandidate: (input3) => projectService.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_DECIDE_ENDPOINT2, input3),
        readContentUnitGenerationPrompt: async (contentUnitId) => (await projectService.promptContext(contentUnitId)).generationPrompt,
        buildContentUnitBackendPrompt: async (contentUnitId) => (await projectService.promptContext(contentUnitId)).backendPrompt,
        reviewWorkspace: (inspectInput222 = {}) => projectService.inspectWorkspace(inspectInput222),
        inspectWorkspace: (inspectInput222 = {}) => projectService.inspectWorkspace(inspectInput222),
        interpretWorkspace: () => projectService.interpretWorkspace(),
        overviewWorkspace: () => projectService.overviewWorkspace(),
        productionWorkPlan: () => engine.productionWorkPlan(),
        regenerationPlan: () => projectService.regenerationPlan(),
        loadContentWorkspaceSnapshot: () => loadContentSourceWorkspaceSnapshotFromEngine2(engine),
        loadContentWorkspace: async () => buildContentSourceWorkspaceData2(await loadContentSourceWorkspaceSnapshotFromEngine2(engine))
      };
    }
    function isNamespaceHierarchyWrite2(input3) {
      const category = input3.category ?? input3.domainCategory ?? input3.domain_category;
      return category === "timeline_namespace" || category === "setting_namespace";
    }
    function createProjectServiceAccessor2(projectDir, decisionStoreConfig) {
      let client;
      const getClient = () => {
        client ??= createProjectServiceClientFromRuntime2();
        return client;
      };
      return {
        inspectWorkspace: async (input3 = {}) => (await getClient().inspectSource({ projectDir, ...input3 })).inspection,
        overviewWorkspace: async () => (await getClient().overviewSource({ projectDir })).overview,
        interpretWorkspace: async () => (await getClient().interpretSource({ projectDir })).interpretation,
        regenerationPlan: async () => (await getClient().regenerationPlan({ projectDir })).regenerationPlan,
        upsertProjectStandards: async (input3 = {}) => (await getClient().upsertProjectStandards({
          projectDir,
          input: sourceOperationInput2(input3)
        })).result,
        upsertScript: async (input3 = {}) => (await getClient().upsertScript({
          projectDir,
          input: sourceOperationInput2(input3)
        })).result,
        snapshotScriptVersionFromMarkdown: async (input3 = {}) => (await getClient().snapshotScriptVersionFromMarkdown({
          projectDir,
          input: sourceOperationInput2(input3)
        })).result,
        sourceOperation: async (endpoint, input3 = {}) => (await getClient().sourceOperation(endpoint, {
          projectDir,
          input: sourceOperationInput2(input3)
        })).result,
        candidateAction: async (endpoint, input3 = {}) => {
          const resolvedDecisionStoreConfig = await decisionStoreConfig?.();
          if (!resolvedDecisionStoreConfig) {
            throw new Error("content unit candidate action requires a scoped project data decisionStore");
          }
          return (await getClient().candidateAction(endpoint, {
            projectDir,
            input: sourceOperationInput2(input3),
            decisionStore: resolvedDecisionStoreConfig
          })).result;
        },
        promptContext: async (contentUnitId) => {
          const resolvedDecisionStoreConfig = await decisionStoreConfig?.();
          return getClient().promptContext({
            projectDir,
            contentUnitId,
            ...resolvedDecisionStoreConfig ? { decisionStore: resolvedDecisionStoreConfig } : {}
          });
        }
      };
    }
    function sourceOperationInput2(input3) {
      return input3 && typeof input3 === "object" && !Array.isArray(input3) ? input3 : {};
    }
    function normalizeRuntimeInput2(input3) {
      return {
        ...input3.workspaceDir !== void 0 ? { workspaceDir: input3.workspaceDir } : {},
        ...input3.projectDir !== void 0 ? { projectDir: input3.projectDir } : {},
        ...input3.userId !== void 0 ? { userId: input3.userId } : {},
        ...input3.orgId !== void 0 ? { orgId: input3.orgId } : {},
        ...input3.projectUid !== void 0 ? { projectUid: input3.projectUid } : {},
        ...input3.projectTitle !== void 0 ? { projectTitle: input3.projectTitle } : {}
      };
    }
    function runtimeKey2(input3) {
      const projectCwd = projectCwdFromInput2(input3);
      const session = resolveMovScriptBackendSession22({
        workspaceDir: input3.workspaceDir,
        userId: input3.userId
      });
      const token = getMCPAuthToken2() || session.token || "";
      return [
        projectCwd,
        input3.userId ?? "",
        input3.orgId ?? "",
        input3.projectUid ?? "",
        session.baseURL,
        session.userId ?? "",
        token
      ].map((part) => String(part)).join("");
    }
    function createMCPDecisionStore2(resolveConfig) {
      if (!resolveConfig) return void 0;
      let storePromise;
      const getStore = async () => {
        storePromise ??= resolveConfig().then((config) => config ? createMovScriptScopedProjectDataDecisionStore2(config) : void 0);
        return storePromise;
      };
      const requireStore = async () => {
        const store = await getStore();
        if (!store) throw new Error("content unit candidate command requires a scoped project data decisionStore");
        return store;
      };
      return {
        async getContentUnitDecision(input3) {
          return (await getStore())?.getContentUnitDecision(input3);
        },
        async getContentUnitDecisions(input3) {
          return (await getStore())?.getContentUnitDecisions?.(input3) ?? /* @__PURE__ */ new Map();
        },
        async replaceContentUnitCandidates(input3) {
          return (await requireStore()).replaceContentUnitCandidates(input3);
        },
        async upsertContentUnitCandidate(input3) {
          return (await requireStore()).upsertContentUnitCandidate(input3);
        },
        async selectContentUnitCandidate(input3) {
          return (await requireStore()).selectContentUnitCandidate(input3);
        },
        async clearContentUnitSelection(input3) {
          return (await requireStore()).clearContentUnitSelection(input3);
        }
      };
    }
    async function resolveMCPProjectDecisionStoreConfig2(input3) {
      return createMCPDecisionStoreConfigResolver2(input3)?.();
    }
    function createMCPDecisionStoreConfigResolver2(input3) {
      const session = resolveMovScriptBackendSession22({
        workspaceDir: input3.workspaceDir,
        userId: input3.userId
      });
      const token = getMCPAuthToken2() || session.token;
      if (!token) return void 0;
      const projectCwd = projectCwdFromInput2(input3);
      let configPromise;
      return async () => {
        configPromise ??= (async () => {
          const resolvedLocator = await createProjectServiceClientFromRuntime2().resolveLocator({
            projectDir: projectCwd,
            ...input3.workspaceDir ? { workspaceDir: input3.workspaceDir } : {},
            ...input3.projectUid ? { projectUid: input3.projectUid } : {}
          });
          const projectUid = resolvedLocator.locator.projectUid ?? input3.projectUid;
          const projectTitle = input3.projectTitle ?? resolvedLocator.locator.projectTitle;
          if (!projectUid) return void 0;
          const headers = {
            ...session.userId ? { "X-User-ID": session.userId } : {},
            ...input3.orgId !== void 0 ? { "X-Org-ID": String(input3.orgId) } : {}
          };
          return {
            kind: "scoped-project-data",
            baseUrl: session.baseURL,
            projectUid,
            ...projectTitle ? { title: projectTitle } : {},
            ...input3.orgId !== void 0 ? { scopeKind: "org", scopeId: input3.orgId } : {},
            token,
            ...Object.keys(headers).length > 0 ? { headers } : {}
          };
        })();
        return configPromise;
      };
    }
    function projectCwdFromInput2(input3) {
      if (input3.projectDir) return input3.projectDir;
      throw new Error("projectDir or cwd is required. The legacy projectId workspace path is no longer supported.");
    }
    var configuredMCPDefaultWorkspaceDir2;
    function setMCPDefaultWorkspaceDir2(workspaceDir2) {
      configuredMCPDefaultWorkspaceDir2 = workspaceDir2?.trim() || void 0;
      setMovScriptBackendDefaultWorkspaceDir22(configuredMCPDefaultWorkspaceDir2);
    }
    function resolveMCPDefaultWorkspaceDir2() {
      return configuredMCPDefaultWorkspaceDir2 || process.env.MOVSCRIPT_HOME || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd();
    }
    function resolveMCPProjectWorkspaceLocator2(args) {
      const workspaceDir2 = stringValue132(args.workspaceDir ?? args.workspace_dir) ?? resolveMCPDefaultWorkspaceDir2();
      const projectDir = pathValue2(args.projectDir ?? args.project_dir ?? args.projectPath ?? args.project_path ?? args.cwd);
      const projectUid = stringValue132(args.projectUid ?? args.project_uid);
      if (projectDir === void 0) throw new Error("projectDir or cwd is required for MovScript project-scoped MCP tools");
      return {
        workspaceDir: workspaceDir2,
        projectDir,
        ...projectUid !== void 0 ? { projectUid } : {}
      };
    }
    function pathValue2(value) {
      const raw = stringValue132(value);
      return raw ? (0, import_path42.resolve)(raw) : void 0;
    }
    var ensuredProjectBindings2 = /* @__PURE__ */ new Map();
    var ENSURED_PROJECT_BINDING_LIMIT2 = 500;
    async function requireMCPBackendBoundProject2(args) {
      const locator = isResolvedProjectWorkspaceLocator2(args) ? args : resolveMCPProjectWorkspaceLocator2(args);
      const resolvedLocator = await resolveMCPProjectBindingLocator2(locator);
      const projectUid = stringField272(resolvedLocator.projectUid);
      if (!projectUid) {
        throw new Error("MovScript project requires project_uid. Open or initialize the project before using project-scoped write tools.");
      }
      const projectTitle = resolvedLocator.projectTitle ?? projectTitleFromDir2(resolvedLocator.projectDir);
      const key = [
        resolvedLocator.workspaceDir,
        resolvedLocator.projectDir,
        projectUid,
        projectTitle
      ].join("");
      const cached = ensuredProjectBindings2.get(key);
      if (cached) return cached;
      const pending = ensureBackendBinding2({
        ...resolvedLocator,
        projectUid,
        ...projectTitle ? { projectTitle } : {},
        ...resolvedLocator.description ? { description: resolvedLocator.description } : {}
      }).catch((err) => {
        ensuredProjectBindings2.delete(key);
        throw err;
      });
      rememberEnsuredProjectBinding2(key, pending);
      return pending;
    }
    async function resolveMCPProjectBindingLocator2(args) {
      const locator = isResolvedProjectWorkspaceLocator2(args) ? args : resolveMCPProjectWorkspaceLocator2(args);
      return resolveProjectBindingLocator2(locator);
    }
    function isResolvedProjectWorkspaceLocator2(value) {
      return isRecord342(value) && typeof value.projectDir === "string";
    }
    async function resolveProjectBindingLocator2(locator) {
      const response = await createProjectServiceClientFromRuntime2().resolveLocator({
        projectDir: locator.projectDir,
        workspaceDir: locator.workspaceDir,
        ...locator.projectUid ? { projectUid: locator.projectUid } : {}
      });
      const projectLocator22 = response.locator;
      return {
        workspaceDir: projectLocator22.workspaceDir ?? locator.workspaceDir,
        projectDir: projectLocator22.projectDir,
        ...projectLocator22.projectUid ?? locator.projectUid ? { projectUid: projectLocator22.projectUid ?? locator.projectUid } : {},
        ...projectLocator22.projectTitle ? { projectTitle: projectLocator22.projectTitle } : {},
        ...projectLocator22.description ? { description: projectLocator22.description } : {}
      };
    }
    async function ensureBackendBinding2(input3) {
      const backendProjectResponse = await backendPost222("/projects/ensure", {
        project_uid: input3.projectUid,
        name: input3.projectTitle ?? projectTitleFromDir2(input3.projectDir),
        description: input3.description ?? input3.projectDir
      });
      const projectDataSpace = await backendPost222("/project-data/spaces", {
        project_uid: input3.projectUid,
        title: input3.projectTitle ?? projectTitleFromDir2(input3.projectDir)
      });
      return {
        ...input3,
        backendProject: isRecord342(backendProjectResponse) ? backendProjectResponse.project : void 0,
        projectDataSpace
      };
    }
    function projectTitleFromDir2(projectDir) {
      const parts = projectDir.split(/[\\/]/).filter(Boolean);
      return parts.at(-1) || "Local Project";
    }
    function stringField272(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function rememberEnsuredProjectBinding2(key, pending) {
      ensuredProjectBindings2.set(key, pending);
      if (ensuredProjectBindings2.size <= ENSURED_PROJECT_BINDING_LIMIT2) return;
      const oldest = ensuredProjectBindings2.keys().next().value;
      if (oldest) ensuredProjectBindings2.delete(oldest);
    }
    var RESOURCE_AGENT_ROUTES2 = {
      resources: "/agent/resources",
      resourceDetail: "/agent/resources/:resourceId"
    };
    function agentResourceDetailPath2(resourceId) {
      return `/agent/resources/${encodeURIComponent(String(resourceId))}`;
    }
    function createResourceLibrarySurface2(args, query) {
      return createAgentBrowserSurface2(args, {
        route: RESOURCE_AGENT_ROUTES2.resources,
        title: "MovScript resource library",
        surface: "inspect",
        intent: "open_resource_library",
        query,
        usage: "Open url in an agent in-app browser. The page uses the local MovScript MCP proxy, which forwards requests with the active agent context."
      });
    }
    function createResourceDetailSurface2(args, resourceId) {
      return createAgentBrowserSurface2(args, {
        route: agentResourceDetailPath2(resourceId),
        title: `MovScript resource #${resourceId}`,
        surface: "inspect",
        intent: "inspect_resource",
        entity: { resource_id: resourceId },
        query: { resourceId },
        usage: "Open this resource detail surface to inspect the RawResource preview, metadata, provenance, and candidate usage context."
      });
    }
    var CONTENT_CANDIDATE_STATUSES2 = /* @__PURE__ */ new Set([
      "queued",
      "running",
      "succeeded",
      "failed",
      "canceled",
      "imported"
    ]);
    var TIMELINE_PRIMITIVE_SPECS2 = {
      scene_moment: {
        entityKind: "scene_moment",
        payloadName: "scene_moment",
        collection: "scene_moments",
        filename: "scene_moment.json",
        contentUnitType: "scene_moment_ref",
        outputKind: "video",
        targetKind: "scene_moment",
        refField: "sceneMomentId"
      },
      expression_unit: {
        entityKind: "expression_unit",
        payloadName: "expression_unit",
        collection: "expression_units",
        filename: "expression_unit.json",
        contentUnitType: "expression_unit_ref",
        outputKind: "video",
        targetKind: "expression_unit",
        refField: "expressionUnitId"
      },
      storyboard: {
        entityKind: "storyboard",
        payloadName: "storyboard",
        collection: "storyboards",
        filename: "storyboard.json",
        contentUnitType: "storyboard_ref",
        outputKind: "image",
        targetKind: "storyboard",
        refField: "storyboardId"
      },
      keyframe: {
        entityKind: "keyframe",
        payloadName: "keyframe",
        collection: "keyframes",
        filename: "keyframe.json",
        contentUnitType: "keyframe_ref",
        outputKind: "image",
        targetKind: "keyframe",
        refField: "keyframeId"
      },
      audio_cue: {
        entityKind: "audio_cue",
        payloadName: "audio_cue",
        collection: "audio_cues",
        filename: "audio_cue.json",
        contentUnitType: "audio_cue_ref",
        outputKind: "audio",
        targetKind: "audio_cue",
        refField: "audioCueId"
      }
    };
    async function editingServiceTimelineView2(args, kind, input3) {
      const locator = resolveMCPProjectWorkspaceLocator2(args);
      const decisionStore = await resolveMCPProjectDecisionStoreConfig2(locator);
      const result = await createEditingServiceClientFromRuntime2().timelineView({
        projectDir: locator.projectDir,
        kind,
        ...decisionStore ? { decisionStore } : {},
        ...input3
      });
      return result.result ?? void 0;
    }
    async function domainGetModel2(args) {
      return service2(args).getModel({
        entityKind: requiredString22(args.entityKind ?? args.entity_kind, "entityKind"),
        ...args.entityId !== void 0 || args.entity_id !== void 0 ? { entityId: idValue32(args.entityId ?? args.entity_id) } : {}
      });
    }
    async function domainQueryEntities2(args) {
      return service2(args).queryEntities({
        ...semanticKind2(args.entityKind ?? args.entity_kind) ? { entityKind: semanticKind2(args.entityKind ?? args.entity_kind) } : {},
        ...stringValue132(args.kind) ? { kind: stringValue132(args.kind) } : {},
        ...stringValue132(args.query ?? args.q) ? { query: stringValue132(args.query ?? args.q) } : {},
        ...contextIds2(args),
        ...numberValue42(args.limit) !== void 0 ? { limit: numberValue42(args.limit) } : {}
      });
    }
    async function domainQuerySettings2(args) {
      return service2(args).querySettings({
        ...args.settingId !== void 0 || args.setting_id !== void 0 ? { settingId: idValue32(args.settingId ?? args.setting_id) } : {},
        ...stringValue132(args.kind) ? { kind: stringValue132(args.kind) } : {},
        ...stringValue132(args.query ?? args.q) ? { query: stringValue132(args.query ?? args.q) } : {},
        ...numberValue42(args.limit) !== void 0 ? { limit: numberValue42(args.limit) } : {}
      });
    }
    async function domainQueryAssets2(args) {
      return service2(args).queryAssets({
        ...args.assetId !== void 0 || args.asset_id !== void 0 ? { assetId: idValue32(args.assetId ?? args.asset_id) } : {},
        ...args.settingId !== void 0 || args.setting_id !== void 0 ? { settingId: idValue32(args.settingId ?? args.setting_id) } : {},
        ...args.settingStateId !== void 0 || args.setting_state_id !== void 0 ? { settingStateId: idValue32(args.settingStateId ?? args.setting_state_id) } : {},
        ...stringValue132(args.query ?? args.q) ? { query: stringValue132(args.query ?? args.q) } : {},
        ...typeof (args.includeCandidates ?? args.include_candidates) === "boolean" ? { includeCandidates: Boolean(args.includeCandidates ?? args.include_candidates) } : {},
        ...numberValue42(args.limit) !== void 0 ? { limit: numberValue42(args.limit) } : {}
      });
    }
    async function domainQueryProductionContext2(args) {
      return service2(args).queryProductionContext({
        ...contextIds2(args),
        ...stringValue132(args.query ?? args.q) ? { query: stringValue132(args.query ?? args.q) } : {},
        ...Array.isArray(args.include) ? { include: args.include.filter(isString32) } : {},
        ...numberValue42(args.limit) !== void 0 ? { limit: numberValue42(args.limit) } : {}
      });
    }
    async function domainReadContentWorkspace2(args) {
      return service2(args).loadContentWorkspace();
    }
    async function domainReadContentWorkspaceSnapshot2(args) {
      return service2(args).loadContentWorkspaceSnapshot();
    }
    async function domainReadProjectContextSnapshot2(args) {
      const locator = resolveMCPProjectWorkspaceLocator2(args);
      const response = await createProjectServiceClientFromRuntime2().resourceView({
        projectDir: locator.projectDir,
        kind: "project-context"
      });
      return response.items[0] ?? {
        schema: "movscript.project_context_snapshot.v1",
        kind: "project_context_snapshot"
      };
    }
    async function domainInterpretContentUnitArtifact2(args) {
      return service2(args).deriveContentUnitArtifact(requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
    }
    async function domainBuildContentUnitBackendPrompt2(args) {
      const contentUnitId = requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
      const result = await service2(args).buildContentUnitBackendPrompt(contentUnitId);
      return isRecord342(result) ? {
        ...result,
        surface: createPromptSurface2(args, {
          contentUnitId,
          mode: result.ok === true ? "inspect" : "edit",
          projectId: projectIdFromArgs2(args)
        })
      } : result;
    }
    async function domainReadPreviewTimeline2(args) {
      const productionId = requiredProductionScopeId2(args, "productionId");
      const result = await readProductionPreviewTimeline2(args, productionId);
      return isRecord342(result) ? {
        ...result,
        surface: createPreviewTimelineSurface2(args, {
          productionId,
          projectId: projectIdFromArgs2(args)
        })
      } : result;
    }
    async function domainReadProductionTimeline2(args) {
      const productionId = requiredProductionScopeId2(args, "productionId");
      const bundle = await productionTimelineBundle2(args, productionId);
      return {
        status: bundle.blockers.length === 0 ? "ok" : "blocked",
        production_id: productionId,
        preview_timeline: bundle.previewTimeline,
        media_editing_project: bundle.mediaEditingProject,
        compose_inputs: bundle.composeInputs,
        clips: bundle.clips,
        blockers: bundle.blockers
      };
    }
    async function domainReadSceneMomentEditPlan2(args) {
      return readSceneMomentEditPlan2(args, requiredId22(args.sceneMomentId ?? args.scene_moment_id, "sceneMomentId"));
    }
    async function domainReadProductionEditPlan2(args) {
      const productionId = requiredProductionScopeId2(args, "productionId");
      const bundle = await productionTimelineBundle2(args, productionId);
      return {
        status: bundle.blockers.length === 0 ? "ok" : "blocked",
        production_id: productionId,
        preview_timeline: bundle.previewTimeline,
        edit_plan: bundle.editPlan,
        context: bundle.context,
        blockers: bundle.blockers
      };
    }
    async function domainCreateEditingProjectContext2(args) {
      const sceneMomentId = args.sceneMomentId ?? args.scene_moment_id;
      if (sceneMomentId !== void 0) {
        const id = requiredId22(sceneMomentId, "sceneMomentId");
        const bundle2 = await sceneMomentTimelineBundle2(args, id);
        return {
          status: bundle2.status,
          target_kind: "scene_moment",
          scene_moment_id: id,
          edit_plan: bundle2.editPlan,
          context: bundle2.context,
          blockers: bundle2.blockers
        };
      }
      const productionId = requiredProductionScopeId2(args, "productionId");
      const bundle = await productionTimelineBundle2(args, productionId);
      return {
        status: bundle.blockers.length === 0 ? "ok" : "blocked",
        target_kind: "production",
        production_id: productionId,
        preview_timeline: bundle.previewTimeline,
        edit_plan: bundle.editPlan,
        context: bundle.context,
        blockers: bundle.blockers
      };
    }
    async function domainReadSceneMomentTimeline2(args) {
      const sceneMomentId = requiredId22(args.sceneMomentId ?? args.scene_moment_id, "sceneMomentId");
      const bundle = await sceneMomentTimelineBundle2(args, sceneMomentId);
      return {
        status: bundle.status,
        scene_moment_id: sceneMomentId,
        edit_plan_status: bundle.editPlan.status,
        edit_plan: bundle.editPlan,
        media_editing_project: bundle.mediaEditingProject,
        compose_inputs: bundle.composeInputs
      };
    }
    async function domainReadContentUnitRuntimePanel2(args) {
      return service2(args).readContentUnitRuntimePanel(requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
    }
    async function domainReadContentUnitGenerationPrompt2(args) {
      const contentUnitId = requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
      const result = await service2(args).readContentUnitGenerationPrompt(contentUnitId);
      return isRecord342(result) ? {
        ...result,
        surface: createPromptSurface2(args, {
          contentUnitId,
          mode: "inspect",
          projectId: projectIdFromArgs2(args)
        })
      } : result;
    }
    async function domainReadContentUnitDependencyReport2(args) {
      return service2(args).readContentUnitDependencyReport(requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
    }
    async function domainReadContentUnitSelectionValidity2(args) {
      return service2(args).readContentUnitSelectionValidity(requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
    }
    async function domainUpsertProjectStandards2(args) {
      return runtimeMutation2(args, (runtime) => runtime.upsertProjectStandards({
        record: optionalRecord2(args.record),
        projectStyle: requiredRecord2(args.projectStyle ?? args.project_style, "projectStyle")
      }));
    }
    async function domainUpsertSetting2(args) {
      const payload = upsertPayloadRecord2(args);
      return runtimeMutation2(args, (runtime) => runtime.createSetting({
        id: idValue32(payload.id ?? payload.client_id),
        title: stringValue132(payload.title),
        kind: stringValue132(payload.setting_kind ?? payload.kind),
        namespaceKind: stringValue132(payload.namespace_kind ?? payload.namespaceKind),
        settingNamespaceKind: stringValue132(payload.setting_namespace_kind ?? payload.settingNamespaceKind ?? payload.namespace_kind ?? payload.namespaceKind),
        description: stringValue132(payload.description),
        alias: stringValue132(payload.alias),
        content: payload.content,
        importance: payload.importance
      }));
    }
    async function domainUpsertSettingState2(args) {
      const payload = upsertPayloadRecord2(args);
      return runtimeMutation2(args, (runtime) => runtime.createSettingState({
        id: optionalId2(payload.id ?? payload.client_id),
        settingId: optionalId2(payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef),
        title: stringValue132(payload.title),
        stateKind: stringValue132(payload.state_kind ?? payload.kind),
        namespaceKind: stringValue132(payload.namespace_kind ?? payload.namespaceKind),
        settingNamespaceKind: stringValue132(payload.setting_namespace_kind ?? payload.settingNamespaceKind ?? payload.namespace_kind ?? payload.namespaceKind),
        description: stringValue132(payload.description)
      }));
    }
    async function domainUpsertAsset2(args) {
      const payload = upsertPayloadRecord2(args);
      return runtimeMutation2(args, (runtime) => runtime.createAsset({
        id: idValue32(payload.id ?? payload.client_id),
        title: stringValue132(payload.title),
        settingId: idValue32(payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef),
        settingStateId: idValue32(payload.setting_state_id ?? payload.settingStateId ?? payload.setting_state_ref ?? payload.settingStateRef),
        slot: stringValue132(payload.slot ?? payload.slot_key ?? payload.slotKey),
        assetKind: stringValue132(payload.asset_kind ?? payload.kind),
        promptHint: stringValue132(payload.prompt_hint ?? payload.promptHint),
        resourceId: idValue32(payload.resource_id ?? payload.resourceId)
      }));
    }
    async function domainCertifyAssetProvider2(args) {
      const provider = providerCertificationProvider2(args.provider ?? args.provider_key ?? args.providerKey);
      const runtime = service2(args);
      const index = await runtime.loadIndex();
      const assetRef32 = requiredId22(args.assetId ?? args.asset_id ?? args.assetRef ?? args.asset_ref, "assetId");
      const asset = index.entities.find((entity) => entity.entityKind === "asset" && (sameEntityRef3(entity.id, assetRef32, "asset") || sameEntityRef3(lastPathSegment82(entity.path), assetRef32, "asset") || normalizeWorkspacePath8(entity.path.replace(/\/asset\.json$/, "")) === normalizeWorkspacePath8(String(assetRef32))));
      if (!asset) throw new Error(`asset not found: ${String(assetRef32)}`);
      const selection = resolveAssetRefSelection2(index, asset);
      const sourceResourceId2 = resourceIdFromUnknown2(args.resourceId ?? args.resource_id) ?? selection.resourceId ?? resourceIdFromUnknown2(asset.record.resource_id ?? asset.record.resourceId);
      if (sourceResourceId2 === void 0) {
        throw new Error(`asset ${String(asset.id ?? assetRef32)} has no selected asset_ref resource_id; select/adopt the asset_ref candidate before certification`);
      }
      const sourceUrl = getOptionalCertificationString2(args.source_url ?? args.sourceUrl ?? args.url);
      const name = getOptionalCertificationString2(args.name) ?? stringValue132(asset.record.title) ?? stringValue132(asset.record.slot) ?? String(asset.id ?? assetRef32);
      const projectId = stringValue132(args.projectId ?? args.project_id);
      const projectName = getOptionalCertificationString2(args.projectName ?? args.project_name) ?? projectId;
      const settingId = getOptionalCertificationString2(args.settingId ?? args.setting_id) ?? stringValue132(asset.record.setting_id ?? asset.record.settingId) ?? settingIdFromAssetPath2(asset.path);
      const model = getOptionalCertificationString2(args.model ?? args.model_id ?? args.modelId ?? args.public_model_id ?? args.publicModelId);
      const assetGroupID = getOptionalCertificationString2(args.asset_group_id ?? args.assetGroupId ?? args.group_id ?? args.groupId);
      const assetGroupName = getOptionalCertificationString2(args.asset_group_name ?? args.assetGroupName ?? args.group_name ?? args.groupName);
      const backendResult = await backendPost222(`/provider-assets/providers/${encodeURIComponent(provider)}/certify`, {
        provider,
        resource_id: sourceResourceId2,
        ...selection.candidateId !== void 0 ? { source_candidate_id: String(selection.candidateId) } : {},
        ...projectId ? { project_id: projectId } : {},
        ...projectName ? { project_name: projectName } : {},
        ...settingId ? { setting_id: settingId } : {},
        ...model ? { model } : {},
        ...assetGroupID ? { asset_group_id: assetGroupID } : {},
        ...assetGroupName ? { asset_group_name: assetGroupName } : {},
        ...sourceUrl ? { source_url: sourceUrl } : {},
        name,
        ...booleanValue4(args.allow_private_urls ?? args.allowPrivateUrls) === true ? { allow_private_urls: true } : {},
        ...numberValue42(args.timeout_ms ?? args.timeoutMs) !== void 0 ? { timeout_ms: numberValue42(args.timeout_ms ?? args.timeoutMs) } : {}
      });
      const certification = isRecord342(backendResult?.certification) ? backendResult.certification : void 0;
      if (!certification) throw new Error("backend provider asset certification response did not include certification");
      const assetUri = getOptionalCertificationString2(certification.asset_uri ?? certification.assetUri);
      if (!assetUri) throw new Error("backend provider asset certification response did not include asset_uri");
      const hubAssetId = getOptionalCertificationString2(certification.hub_asset_id ?? certification.hubAssetId);
      const certifiedSourceResourceId = resourceIdFromUnknown2(certification.source_resource_id ?? certification.sourceResourceId) ?? sourceResourceId2;
      const certifiedSourceCandidateId = idFromUnknown2(certification.source_candidate_id ?? certification.sourceCandidateId) ?? selection.candidateId;
      const certifiedProvider = getOptionalCertificationString2(certification.provider_id ?? certification.providerId ?? backendResult?.provider_id ?? backendResult?.providerId ?? backendResult?.provider) ?? provider;
      const normalizedCertification = {
        ...certification,
        provider: certifiedProvider,
        provider_id: certifiedProvider,
        source_resource_id: certifiedSourceResourceId,
        ...model ? { model, public_model_id: model, provider_model_id: getOptionalCertificationString2(certification.provider_model_id ?? certification.providerModelId) ?? model } : {},
        ...certifiedSourceCandidateId !== void 0 ? { source_candidate_id: certifiedSourceCandidateId } : {}
      };
      const written = await patchAssetProviderCertification2(runtime.projectDir, asset.path, asset.record, certifiedProvider, normalizedCertification);
      invalidateMovScriptDomainRuntime2(resolveMCPProjectWorkspaceLocator2(args));
      return {
        status: "succeeded",
        provider: certifiedProvider,
        provider_id: certifiedProvider,
        asset_id: asset.id,
        asset_path: asset.path,
        source_resource_id: certifiedSourceResourceId,
        ...certifiedSourceCandidateId !== void 0 ? { source_candidate_id: certifiedSourceCandidateId } : {},
        asset_uri: assetUri,
        ...hubAssetId ? { hub_asset_id: hubAssetId } : {},
        certification: written.certification,
        path: written.path,
        backend_result: backendResult,
        message: `Certified asset ${String(asset.id ?? assetRef32)} for ${certifiedProvider}; downstream generation can resolve resource #${String(certifiedSourceResourceId)} as ${assetUri}.`
      };
    }
    async function domainQueryRemoteAssetGroups2(args) {
      const provider = providerCertificationProvider2(args.provider ?? args.provider_id ?? args.providerId ?? args.provider_key ?? args.providerKey);
      const params = new URLSearchParams();
      const model = getOptionalCertificationString2(args.model ?? args.model_id ?? args.modelId ?? args.public_model_id ?? args.publicModelId);
      const projectId = getOptionalCertificationString2(args.projectId ?? args.project_id);
      if (model) params.set("model", model);
      if (projectId) params.set("project_id", projectId);
      const suffix = params.toString();
      return backendGet222(`/provider-assets/providers/${encodeURIComponent(provider)}/groups${suffix ? `?${suffix}` : ""}`);
    }
    async function domainQueryRemoteAssets2(args) {
      const provider = providerCertificationProvider2(args.provider ?? args.provider_id ?? args.providerId ?? args.provider_key ?? args.providerKey);
      const groupRef = requiredId22(args.groupId ?? args.group_id ?? args.groupRef ?? args.group_ref ?? args.asset_group_id ?? args.assetGroupId, "groupId");
      return backendGet222(`/provider-assets/providers/${encodeURIComponent(provider)}/groups/${encodeURIComponent(String(groupRef))}/assets`);
    }
    function settingIdFromAssetPath2(path) {
      const normalized = normalizeWorkspacePath8(path ?? "");
      const parts = normalized.split("/").filter(Boolean);
      const settingsIndex = parts.indexOf("settings");
      if (settingsIndex < 0) return void 0;
      return stringValue132(parts[settingsIndex + 1]);
    }
    async function domainUpsertSettingTree2(args) {
      const settingPayload = requiredRecord2(args.setting ?? args.payload, "setting");
      const stateItems = requiredArray2(args.states ?? settingPayload.states, "states").filter(isRecord342);
      return runtimeMutation2(args, async (runtime) => {
        const setting = await runtime.createSetting({
          id: optionalId2(settingPayload.id ?? settingPayload.client_id),
          title: stringValue132(settingPayload.title),
          kind: stringValue132(settingPayload.setting_kind ?? settingPayload.kind),
          namespaceKind: stringValue132(settingPayload.namespace_kind ?? settingPayload.namespaceKind),
          settingNamespaceKind: stringValue132(settingPayload.setting_namespace_kind ?? settingPayload.settingNamespaceKind ?? settingPayload.namespace_kind ?? settingPayload.namespaceKind),
          description: stringValue132(settingPayload.description),
          alias: stringValue132(settingPayload.alias),
          content: settingPayload.content,
          importance: settingPayload.importance
        });
        const settingId = entityResultId2(setting, settingPayload, "setting");
        const states = [];
        for (const stateItem of stateItems) {
          const statePayload = requiredRecord2(stateItem.state ?? stateItem.payload ?? stateItem.record ?? stateItem.entity ?? stateItem, "state");
          const state = await runtime.createSettingState({
            id: optionalId2(statePayload.id ?? statePayload.client_id),
            settingId: optionalId2(statePayload.setting_id ?? statePayload.settingId ?? statePayload.setting_ref ?? statePayload.settingRef ?? settingId),
            title: stringValue132(statePayload.title),
            stateKind: stringValue132(statePayload.state_kind ?? statePayload.kind),
            namespaceKind: stringValue132(statePayload.namespace_kind ?? statePayload.namespaceKind),
            settingNamespaceKind: stringValue132(statePayload.setting_namespace_kind ?? statePayload.settingNamespaceKind ?? statePayload.namespace_kind ?? statePayload.namespaceKind),
            description: stringValue132(statePayload.description)
          });
          const stateId = entityResultId2(state, statePayload, "setting_state");
          const assetItems = Array.isArray(stateItem.assets) ? stateItem.assets.filter(isRecord342) : [];
          const assets = [];
          for (const assetItem of assetItems) {
            const assetPayload = requiredRecord2(assetItem.payload ?? assetItem.record ?? assetItem.entity ?? assetItem, "asset");
            assets.push(await runtime.createAsset({
              id: optionalId2(assetPayload.id ?? assetPayload.client_id),
              title: stringValue132(assetPayload.title),
              settingId: optionalId2(assetPayload.setting_id ?? assetPayload.settingId ?? assetPayload.setting_ref ?? assetPayload.settingRef ?? settingId),
              settingStateId: optionalId2(assetPayload.setting_state_id ?? assetPayload.settingStateId ?? assetPayload.setting_state_ref ?? assetPayload.settingStateRef ?? stateId),
              slot: stringValue132(assetPayload.slot ?? assetPayload.slot_key ?? assetPayload.slotKey),
              assetKind: stringValue132(assetPayload.asset_kind ?? assetPayload.kind),
              promptHint: stringValue132(assetPayload.prompt_hint ?? assetPayload.promptHint),
              resourceId: optionalId2(assetPayload.resource_id ?? assetPayload.resourceId)
            }));
          }
          states.push({ state, assets });
        }
        return { setting, states };
      });
    }
    async function domainUpsertScript2(args) {
      return runtimeMutation2(args, (runtime) => runtime.upsertScript({
        scriptId: requiredId22(args.scriptId ?? args.script_id, "scriptId"),
        record: optionalRecord2(args.record),
        sourceText: requiredString22(args.sourceText ?? args.source_text, "sourceText"),
        ...optionalRecord2(args.metadata) ? { metadata: optionalRecord2(args.metadata) } : {}
      }));
    }
    async function domainReadScriptSource2(args) {
      return service2(args).readScriptSource({
        record: requiredRecord2(args.record, "record"),
        ...optionalRecord2(args.entity) ? { entity: optionalRecord2(args.entity) } : {}
      });
    }
    async function domainSnapshotScriptVersion2(args) {
      return runtimeMutation2(args, (runtime) => runtime.snapshotScriptVersionFromMarkdown({
        scriptId: requiredId22(args.scriptId ?? args.script_id, "scriptId"),
        versionId: requiredId22(args.versionId ?? args.version_id, "versionId"),
        ...stringValue132(args.versionLabel ?? args.version_label) ? { versionLabel: stringValue132(args.versionLabel ?? args.version_label) } : {},
        ...stringValue132(args.sourcePath ?? args.source_path) ? { sourcePath: stringValue132(args.sourcePath ?? args.source_path) } : {}
      }));
    }
    async function domainUpsertContentUnit2(args) {
      return runtimeMutation2(args, (runtime) => runtime.createContentUnit(
        engineContentUnitInputFromRecord2(requiredRecord2(args.unit, "unit"))
      ));
    }
    async function domainUpsertProduction2(args) {
      const production = requiredRecord2(args.production ?? args.payload ?? args.record, "production");
      const productionId = productionIdFrom2(args, production);
      const result = await runtimeMutation2(args, (runtime) => runtime.createProduction({
        id: productionId,
        title: stringValue132(production.title)
      }));
      return productionWriteResult2("production", { productionId }, result);
    }
    async function domainUpsertProductionTree2(args) {
      const productionPayload = requiredRecord2(args.production ?? args.payload ?? args.record, "production");
      const segmentItems = requiredArray2(args.segments ?? productionPayload.segments, "segments").filter(isRecord342);
      return runtimeMutation2(args, async (runtime) => {
        const productionId = productionIdFrom2(args, productionPayload);
        const production = await runtime.createProduction({
          id: productionId,
          title: stringValue132(productionPayload.title)
        });
        const segments = [];
        for (const segmentItem of segmentItems) {
          const segmentPayload = treePayload2(segmentItem, "segment");
          const segmentId = requiredId22(segmentPayload.id ?? segmentPayload.client_id, "segment.id");
          const segment = await runtime.createSegment({
            productionId,
            id: segmentId,
            title: stringValue132(segmentPayload.title),
            kind: stringValue132(segmentPayload.segment_kind ?? segmentPayload.kind),
            summary: stringValue132(segmentPayload.summary ?? segmentPayload.description),
            order: numberValue42(segmentPayload.order)
          });
          const sceneMomentItems = arrayRecords22(segmentItem.scene_moments ?? segmentItem.sceneMoments);
          const contentUnitItems = arrayRecords22(segmentItem.content_units ?? segmentItem.contentUnits);
          const sceneMoments = [];
          const contentUnits2 = [];
          for (const contentUnitItem of contentUnitItems) {
            const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
            contentUnits2.push(await runtime.createContentUnit(treeContentUnitInput2(contentUnitPayload, {
              productionId,
              segmentId,
              contentUnitType: "segment_ref",
              outputKind: "video",
              targetKind: "segment",
              targetRef: segmentId
            })));
          }
          for (const sceneMomentItem of sceneMomentItems) {
            const sceneMomentPayload = treePayload2(sceneMomentItem, "scene_moment");
            const sceneMomentId = requiredId22(sceneMomentPayload.id ?? sceneMomentPayload.client_id, "scene_moment.id");
            const sceneMoment = await runtime.createSceneMoment({
              productionId,
              segmentId,
              id: sceneMomentId,
              title: stringValue132(sceneMomentPayload.title),
              storyboardId: optionalId2(sceneMomentPayload.storyboard_id ?? sceneMomentPayload.storyboardId),
              order: numberValue42(sceneMomentPayload.order),
              timeText: stringValue132(sceneMomentPayload.time_text ?? sceneMomentPayload.when),
              sceneCode: stringValue132(sceneMomentPayload.scene_code),
              locationText: stringValue132(sceneMomentPayload.location_text ?? sceneMomentPayload.where),
              conditionText: stringValue132(sceneMomentPayload.condition_text),
              actionText: stringValue132(sceneMomentPayload.action_text ?? sceneMomentPayload.action),
              mood: stringValue132(sceneMomentPayload.mood ?? sceneMomentPayload.emotion),
              description: stringValue132(sceneMomentPayload.description),
              settings: settingRefsFromRecord22(sceneMomentPayload)
            });
            const sceneContentUnits = [];
            for (const contentUnitItem of arrayRecords22(sceneMomentItem.content_units ?? sceneMomentItem.contentUnits)) {
              const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
              sceneContentUnits.push(await runtime.createContentUnit(treeContentUnitInput2(contentUnitPayload, {
                productionId,
                segmentId,
                sceneMomentId,
                contentUnitType: "scene_moment_ref",
                outputKind: "video",
                targetKind: "scene_moment",
                targetRef: sceneMomentId
              })));
            }
            const expressionUnits = [];
            for (const expressionUnitItem of arrayRecords22(sceneMomentItem.expression_units ?? sceneMomentItem.expressionUnits)) {
              const expressionUnitPayload = normalizeExpressionUnitPayload2(treePayload2(expressionUnitItem, "expression_unit"));
              const expressionUnitId = requiredId22(expressionUnitPayload.id ?? expressionUnitPayload.client_id, "expression_unit.id");
              const expressionUnit = await runtime.createExpressionUnit({
                productionId,
                segmentId,
                sceneMomentId,
                id: expressionUnitId,
                title: stringValue132(expressionUnitPayload.title),
                slotKind: stringValue132(expressionUnitPayload.slot_kind ?? expressionUnitPayload.slotKind),
                kind: stringValue132(expressionUnitPayload.kind),
                text: stringValue132(expressionUnitPayload.text ?? expressionUnitPayload.content),
                intent: stringValue132(expressionUnitPayload.intent ?? expressionUnitPayload.summary ?? expressionUnitPayload.description),
                speaker: stringValue132(expressionUnitPayload.speaker),
                note: stringValue132(expressionUnitPayload.note),
                order: numberValue42(expressionUnitPayload.order)
              });
              const expressionContentUnits = [];
              for (const contentUnitItem of arrayRecords22(expressionUnitItem.content_units ?? expressionUnitItem.contentUnits)) {
                const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
                expressionContentUnits.push(await runtime.createContentUnit(treeContentUnitInput2(contentUnitPayload, {
                  productionId,
                  segmentId,
                  sceneMomentId,
                  expressionUnitId,
                  contentUnitType: "expression_unit_ref",
                  outputKind: "video",
                  targetKind: "expression_unit",
                  targetRef: expressionUnitId
                })));
              }
              const storyboards2 = await upsertTreeStoryboards2(runtime, expressionUnitItem, { productionId, segmentId, sceneMomentId, expressionUnitId });
              const keyframes2 = await upsertTreeKeyframes2(runtime, expressionUnitItem, { productionId, segmentId, sceneMomentId, expressionUnitId });
              const audioCues2 = await upsertTreeAudioCues2(runtime, expressionUnitItem, { productionId, segmentId, sceneMomentId, expressionUnitId });
              expressionUnits.push({ expressionUnit, contentUnits: expressionContentUnits, storyboards: storyboards2, keyframes: keyframes2, audioCues: audioCues2 });
            }
            const storyboards = await upsertTreeStoryboards2(runtime, sceneMomentItem, { productionId, segmentId, sceneMomentId });
            const keyframes = await upsertTreeKeyframes2(runtime, sceneMomentItem, { productionId, segmentId, sceneMomentId });
            const audioCues = await upsertTreeAudioCues2(runtime, sceneMomentItem, { productionId, segmentId, sceneMomentId });
            sceneMoments.push({ sceneMoment, contentUnits: sceneContentUnits, expressionUnits, storyboards, keyframes, audioCues });
          }
          segments.push({ segment, contentUnits: contentUnits2, sceneMoments });
        }
        const contentUnits = [];
        for (const contentUnitItem of arrayRecords22(args.content_units ?? args.contentUnits ?? productionPayload.content_units ?? productionPayload.contentUnits)) {
          const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
          contentUnits.push(await runtime.createContentUnit(treeContentUnitInput2(contentUnitPayload, {
            productionId,
            contentUnitType: "production_ref",
            outputKind: "video",
            targetKind: "production",
            targetRef: productionId
          })));
        }
        return { production, contentUnits, segments };
      });
    }
    async function domainUpsertTimelineNamespaceTree2(args) {
      const namespaceItems = timelineNamespaceTreeItems2(args);
      if (namespaceItems.length === 0) throw new Error("namespace, root, tree, nodes, or timeline_namespaces is required");
      return runtimeMutation2(args, async (runtime) => {
        const collector = { namespaces: [], sceneMoments: [], primitives: [], contentUnits: [] };
        const tree = [];
        for (const item of namespaceItems) {
          tree.push(await upsertTimelineNamespaceTreeNode2(runtime, item, { depth: 0 }, collector));
        }
        return {
          schema: "movscript.timeline_namespace_tree_upsert_result.v1",
          status: "upserted",
          tree,
          namespaces: collector.namespaces,
          sceneMoments: collector.sceneMoments,
          primitives: collector.primitives,
          contentUnits: collector.contentUnits
        };
      });
    }
    async function upsertTimelineNamespaceTreeNode2(runtime, item, context, collector) {
      const payload = treePayload2(item, "namespace");
      const namespaceId = requiredId22(payload.id ?? payload.client_id, "namespace.id");
      const namespaceKind = requiredString22(
        payload.namespace_kind ?? payload.namespaceKind ?? payload.timeline_namespace_kind ?? payload.timelineNamespaceKind ?? payload.domain_kind ?? payload.domainKind ?? payload.kind,
        "namespace.namespace_kind"
      );
      const entityKind = timelineNamespaceEntityKind2(payload, context);
      const targetPath = timelineNamespaceTargetPath2(item, payload, context, entityKind, namespaceId);
      const record = timelineNamespaceRecord2(payload, {
        entityKind,
        namespaceId,
        namespaceKind
      });
      const node = await runtime.writeHierarchyNode({
        category: "timeline_namespace",
        namespaceKind,
        targetPath,
        record
      });
      const namespaceSummary = {
        id: namespaceId,
        namespace_kind: namespaceKind,
        entity_kind: entityKind,
        targetPath,
        node
      };
      collector.namespaces.push(namespaceSummary);
      const contentUnits = [];
      for (const contentUnitItem of arrayRecords22(item.content_units ?? item.contentUnits ?? payload.content_units ?? payload.contentUnits)) {
        const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
        const contentUnit = await runtime.createContentUnit(timelineNamespaceAssemblyContentUnitInput2(contentUnitPayload, {
          namespaceId,
          namespaceKind
        }));
        contentUnits.push(contentUnit);
        collector.contentUnits.push(contentUnit);
      }
      const sceneMoments = [];
      for (const sceneMomentItem of timelineNamespaceSceneMomentItems2(item, payload)) {
        sceneMoments.push(await upsertTimelinePrimitiveNode2(runtime, sceneMomentItem, {
          spec: TIMELINE_PRIMITIVE_SPECS2.scene_moment,
          parentDir: timelineNamespaceNodeDir2(targetPath)
        }, collector));
      }
      const children = [];
      for (const child of timelineNamespaceChildItems2(item, payload)) {
        children.push(await upsertTimelineNamespaceTreeNode2(runtime, child, {
          depth: context.depth + 1,
          parentTargetPath: targetPath
        }, collector));
      }
      return {
        ...namespaceSummary,
        contentUnits,
        sceneMoments,
        children
      };
    }
    async function domainUpsertSegment2(args) {
      const segment = requiredRecord2(args.segment ?? args.payload, "segment");
      const production = optionalRecord2(args.production);
      const productionId = productionIdFrom2(args, production);
      const segmentId = requiredId22(args.segmentId ?? args.segment_id ?? segment.id ?? segment.client_id, "segmentId");
      const result = await runtimeMutation2(args, (runtime) => runtime.createSegment({
        productionId,
        id: segmentId,
        title: stringValue132(segment.title),
        kind: stringValue132(segment.segment_kind ?? segment.kind),
        summary: stringValue132(segment.summary ?? segment.description),
        order: numberValue42(segment.order)
      }));
      return productionWriteResult2("segment", { productionId, segmentId }, result);
    }
    async function domainUpsertSceneMoment2(args) {
      const sceneMoment = requiredRecord2(args.sceneMoment ?? args.scene_moment ?? args.payload, "sceneMoment");
      const sceneMomentId = requiredId22(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment.id ?? sceneMoment.client_id, "sceneMomentId");
      const pathTarget = sceneMomentPathTarget2(args, sceneMoment, sceneMomentId);
      if (pathTarget) {
        const result2 = await runtimeMutation2(args, (runtime) => runtime.writeHierarchyNode({
          targetPath: pathTarget,
          record: sceneMomentHierarchyRecord2(sceneMoment, sceneMomentId)
        }));
        return {
          status: "upserted",
          entityKind: "scene_moment",
          sceneMomentId,
          targetPath: pathTarget,
          result: result2
        };
      }
      const production = optionalRecord2(args.production);
      const segment = optionalRecord2(args.segment);
      const productionId = productionIdFrom2(args, production);
      const segmentId = requiredId22(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
      const result = await runtimeMutation2(args, (runtime) => runtime.createSceneMoment({
        productionId,
        segmentId,
        id: sceneMomentId,
        title: stringValue132(sceneMoment.title),
        storyboardId: idValue32(sceneMoment.storyboard_id ?? sceneMoment.storyboardId),
        order: numberValue42(sceneMoment.order),
        timeText: stringValue132(sceneMoment.time_text ?? sceneMoment.when),
        sceneCode: stringValue132(sceneMoment.scene_code),
        locationText: stringValue132(sceneMoment.location_text ?? sceneMoment.where),
        conditionText: stringValue132(sceneMoment.condition_text),
        actionText: stringValue132(sceneMoment.action_text ?? sceneMoment.action),
        mood: stringValue132(sceneMoment.mood ?? sceneMoment.emotion),
        description: stringValue132(sceneMoment.description),
        settings: settingRefsFromRecord22(sceneMoment)
      }));
      return productionWriteResult2("scene_moment", { productionId, segmentId, sceneMomentId }, result);
    }
    async function domainUpsertKeyframe2(args) {
      const keyframe = normalizeKeyframePayload2(requiredRecord2(args.keyframe ?? args.payload, "keyframe"));
      const expressionUnit = optionalRecord2(args.expressionUnit ?? args.expression_unit);
      const keyframeId = requiredId22(args.keyframeId ?? args.keyframe_id ?? keyframe.id ?? keyframe.client_id, "keyframeId");
      const pathTarget = timelinePrimitivePathTarget2(args, keyframe, {
        spec: TIMELINE_PRIMITIVE_SPECS2.keyframe,
        id: keyframeId
      });
      if (pathTarget) {
        return writeTimelinePrimitivePathTarget2(args, TIMELINE_PRIMITIVE_SPECS2.keyframe, keyframe, keyframeId, pathTarget);
      }
      const production = optionalRecord2(args.production);
      const segment = optionalRecord2(args.segment);
      const sceneMoment = optionalRecord2(args.sceneMoment ?? args.scene_moment);
      const productionId = productionIdFrom2(args, production);
      const segmentId = requiredId22(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
      const sceneMomentId = requiredId22(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment?.id ?? sceneMoment?.client_id, "sceneMomentId");
      const expressionUnitId = idValue32(args.expressionUnitId ?? args.expression_unit_id ?? expressionUnit?.id ?? expressionUnit?.client_id);
      const result = await runtimeMutation2(args, (runtime) => runtime.createKeyframe({
        productionId,
        segmentId,
        sceneMomentId,
        ...expressionUnitId ? { expressionUnitId } : {},
        id: keyframeId,
        title: stringValue132(keyframe.title),
        role: stringValue132(keyframe.role ?? keyframe.status),
        visualIntent: stringValue132(keyframe.visual_intent ?? keyframe.visualIntent ?? keyframe.prompt_hint ?? keyframe.description),
        order: numberValue42(keyframe.order),
        timing: optionalRecord2(keyframe.timing),
        composition: optionalRecord2(keyframe.composition),
        continuity: optionalRecord2(keyframe.continuity),
        referenceAssetRefs: Array.isArray(keyframe.reference_asset_refs) ? keyframe.reference_asset_refs : void 0,
        referenceKeyframeRefs: Array.isArray(keyframe.reference_keyframe_refs) ? keyframe.reference_keyframe_refs : void 0
      }));
      return productionWriteResult2("keyframe", { productionId, segmentId, sceneMomentId, ...expressionUnitId ? { expressionUnitId } : {}, keyframeId }, result);
    }
    async function domainUpsertStoryboard2(args) {
      const storyboard = requiredRecord2(args.storyboard ?? args.payload, "storyboard");
      const expressionUnit = optionalRecord2(args.expressionUnit ?? args.expression_unit);
      const storyboardId = idValue32(args.storyboardId ?? args.storyboard_id ?? storyboard.id ?? storyboard.client_id ?? "main") ?? "main";
      const pathTarget = timelinePrimitivePathTarget2(args, storyboard, {
        spec: TIMELINE_PRIMITIVE_SPECS2.storyboard,
        id: storyboardId
      });
      if (pathTarget) {
        const pathResult = await writeTimelinePrimitivePathTarget2(args, TIMELINE_PRIMITIVE_SPECS2.storyboard, storyboard, storyboardId, pathTarget);
        return {
          ...pathResult,
          storyboardId,
          storyboardPath: pathTarget
        };
      }
      const productionId = requiredId22(args.productionId ?? args.production_id, "productionId");
      const segmentId = requiredId22(args.segmentId ?? args.segment_id, "segmentId");
      const sceneMomentId = requiredId22(args.sceneMomentId ?? args.scene_moment_id, "sceneMomentId");
      const expressionUnitId = idValue32(args.expressionUnitId ?? args.expression_unit_id ?? storyboard.expression_unit_id ?? expressionUnit?.id ?? expressionUnit?.client_id);
      const result = await runtimeMutation2(args, (runtime) => runtime.createStoryboard({
        productionId,
        segmentId,
        sceneMomentId,
        ...expressionUnitId ? { expressionUnitId } : {},
        id: storyboardId ?? "main",
        title: stringValue132(storyboard.title),
        visualIntent: stringValue132(storyboard.visual_intent ?? storyboard.visualIntent ?? storyboard.prompt_hint ?? storyboard.description),
        order: numberValue42(storyboard.order),
        timeline: optionalRecord2(storyboard.timeline),
        graph: optionalRecord2(storyboard.graph)
      }));
      return {
        status: "upserted",
        productionId,
        segmentId,
        sceneMomentId,
        ...expressionUnitId ? { expressionUnitId } : {},
        storyboardId,
        writtenPaths: result.writtenPaths,
        storyboardPath: result.writtenPaths.find((path) => path.endsWith("/storyboard.json"))
      };
    }
    async function domainUpsertAudioCue2(args) {
      const audioCue = normalizeAudioCuePayload2(requiredRecord2(args.audioCue ?? args.audio_cue ?? args.payload, "audioCue"));
      const audioCueId = requiredId22(args.audioCueId ?? args.audio_cue_id ?? audioCue.id ?? audioCue.client_id, "audioCueId");
      const pathTarget = timelinePrimitivePathTarget2(args, audioCue, {
        spec: TIMELINE_PRIMITIVE_SPECS2.audio_cue,
        id: audioCueId
      });
      if (pathTarget) {
        return writeTimelinePrimitivePathTarget2(args, TIMELINE_PRIMITIVE_SPECS2.audio_cue, audioCue, audioCueId, pathTarget);
      }
      const production = optionalRecord2(args.production);
      const segment = optionalRecord2(args.segment);
      const sceneMoment = optionalRecord2(args.sceneMoment ?? args.scene_moment);
      const productionId = productionIdFrom2(args, production);
      const segmentId = requiredId22(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
      const sceneMomentId = requiredId22(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment?.id ?? sceneMoment?.client_id, "sceneMomentId");
      const result = await runtimeMutation2(args, (runtime) => runtime.createAudioCue({
        productionId,
        segmentId,
        sceneMomentId,
        id: audioCueId,
        title: stringValue132(audioCue.title),
        kind: stringValue132(audioCue.cue_kind ?? audioCue.kind),
        storyboardId: idValue32(audioCue.storyboard_id ?? audioCue.storyboardId),
        expressionUnitId: idValue32(audioCue.expression_unit_ref ?? audioCue.expressionUnitRef ?? audioCue.expression_unit_id ?? audioCue.expressionUnitId),
        promptHint: stringValue132(audioCue.prompt_hint ?? audioCue.promptHint)
      }));
      return productionWriteResult2("audio_cue", { productionId, segmentId, sceneMomentId, audioCueId }, result);
    }
    async function domainUpsertExpressionUnit2(args) {
      const expressionUnit = normalizeExpressionUnitPayload2(requiredRecord2(args.expressionUnit ?? args.expression_unit ?? args.payload, "expressionUnit"));
      const expressionUnitId = requiredId22(args.expressionUnitId ?? args.expression_unit_id ?? expressionUnit.id ?? expressionUnit.client_id, "expressionUnitId");
      const pathTarget = timelinePrimitivePathTarget2(args, expressionUnit, {
        spec: TIMELINE_PRIMITIVE_SPECS2.expression_unit,
        id: expressionUnitId
      });
      if (pathTarget) {
        return writeTimelinePrimitivePathTarget2(args, TIMELINE_PRIMITIVE_SPECS2.expression_unit, expressionUnit, expressionUnitId, pathTarget);
      }
      const production = optionalRecord2(args.production);
      const segment = optionalRecord2(args.segment);
      const sceneMoment = optionalRecord2(args.sceneMoment ?? args.scene_moment);
      const productionId = productionIdFrom2(args, production);
      const segmentId = requiredId22(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
      const sceneMomentId = requiredId22(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment?.id ?? sceneMoment?.client_id, "sceneMomentId");
      const result = await runtimeMutation2(args, (runtime) => runtime.createExpressionUnit({
        productionId,
        segmentId,
        sceneMomentId,
        id: expressionUnitId,
        title: stringValue132(expressionUnit.title),
        slotKind: stringValue132(expressionUnit.slot_kind ?? expressionUnit.slotKind),
        kind: stringValue132(expressionUnit.kind),
        text: stringValue132(expressionUnit.text ?? expressionUnit.content),
        intent: stringValue132(expressionUnit.intent ?? expressionUnit.summary ?? expressionUnit.description),
        speaker: stringValue132(expressionUnit.speaker),
        note: stringValue132(expressionUnit.note),
        order: numberValue42(expressionUnit.order)
      }));
      return productionWriteResult2("expression_unit", { productionId, segmentId, sceneMomentId, expressionUnitId }, result);
    }
    async function domainUpdateContentUnitPrompt2(args) {
      const result = await runtimeMutation2(args, (runtime) => runtime.updateContentUnitEditPrompt({
        targetPath: requiredString22(args.targetPath ?? args.target_path, "targetPath"),
        editPrompt: requiredRecord2(args.editPrompt ?? args.edit_prompt, "editPrompt")
      }));
      const contentUnitId = args.contentUnitId ?? args.content_unit_id;
      return {
        status: "updated",
        result,
        ...contentUnitId !== void 0 ? {
          contentUnitId,
          content_unit_id: contentUnitId,
          surface: createPromptSurface2(args, {
            contentUnitId: idValue32(contentUnitId),
            mode: "edit",
            projectId: projectIdFromArgs2(args)
          })
        } : {},
        secondary_surfaces: [
          createImpactSurface2(args, {
            projectId: projectIdFromArgs2(args),
            target: contentUnitId === void 0 ? void 0 : String(contentUnitId),
            source: "domain_update_content_unit_prompt"
          })
        ]
      };
    }
    async function domainUpdateEntityTransition2(args) {
      return runtimeMutation2(args, (runtime) => runtime.updateEntityTransition({
        targetPath: requiredString22(args.targetPath ?? args.target_path, "targetPath"),
        ...optionalRecord2(args.transition) ? { transition: optionalRecord2(args.transition) } : {}
      }));
    }
    async function domainUpdateStoryboardTimeline2(args) {
      return runtimeMutation2(args, (runtime) => runtime.updateStoryboardTimeline({
        targetPath: requiredString22(args.targetPath ?? args.target_path, "targetPath"),
        ...optionalRecord2(args.timeline) ? { timeline: optionalRecord2(args.timeline) } : {}
      }));
    }
    async function domainAppendCandidate2(args) {
      return runtimeMutation2(args, (runtime) => runtime.appendCandidate({
        targetPath: requiredString22(args.targetPath ?? args.target_path, "targetPath"),
        targetKind: requiredTargetKind2(args),
        payload: requiredRecord2(args.payload, "payload"),
        ...args.lock !== void 0 ? { lock: args.lock } : {},
        ...stringValue132(args.nonce) ? { nonce: stringValue132(args.nonce) } : {}
      }));
    }
    async function domainCreateContentCandidate2(args) {
      const status = contentCandidateStatus2(args.status);
      const contentUnitId = requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
      const result = await runtimeMutation2(args, (runtime) => runtime.createContentCandidate({
        contentUnitId,
        ...args.candidateId !== void 0 || args.candidate_id !== void 0 ? { candidateId: idValue32(args.candidateId ?? args.candidate_id) } : {},
        ...stringValue132(args.source) ? { source: stringValue132(args.source) } : {},
        ...status ? { status } : {},
        ...optionalRecord2(args.producer) ? { producer: optionalRecord2(args.producer) } : {},
        outputs: requiredArray2(args.outputs, "outputs").filter(isRecord342),
        ...optionalRecord2(args.promptSnapshot ?? args.prompt_snapshot) ? { promptSnapshot: optionalRecord2(args.promptSnapshot ?? args.prompt_snapshot) } : {}
      }));
      const visibility = await withContentUnitCandidateVisibility2(args, contentUnitId, result, {
        candidate_created: true,
        generation_mode: "content_unit_candidate",
        will_auto_select: false,
        requires_user_adoption: true
      });
      return {
        ...visibility,
        surface: createContentCandidatesSurface2(args, {
          contentUnitId,
          ...candidateIdFromArgs2(args) ? { candidateId: candidateIdFromArgs2(args) } : {},
          ...firstOutputResourceId2(args.outputs) !== void 0 ? { resourceId: firstOutputResourceId2(args.outputs) } : {},
          projectId: projectIdFromArgs2(args)
        })
      };
    }
    async function domainCreateContentCandidateBatch2(args) {
      return runDomainBatch2(args, domainCreateContentCandidate2);
    }
    async function domainRegisterRawResourceAsContentUnitCandidate2(args) {
      const resourceId = requiredResourceId52(args.resourceId ?? args.resource_id);
      const outputKind = contentCandidateOutputKind2(args.outputKind ?? args.output_kind ?? args.kind);
      const contentUnitId = requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
      const candidateId = args.candidateId ?? args.candidate_id ?? `raw_${outputKind}_${resourceId}`;
      return domainCreateContentCandidate2({
        ...args,
        contentUnitId,
        content_unit_id: contentUnitId,
        candidateId,
        candidate_id: candidateId,
        source: stringValue132(args.source) ?? "manual",
        status: args.status ?? "imported",
        producer: optionalRecord2(args.producer) ?? { kind: "raw_resource_registration" },
        outputs: [{
          kind: outputKind,
          resource_id: resourceId,
          ...stringValue132(args.mimeType ?? args.mime_type) ? { mime_type: stringValue132(args.mimeType ?? args.mime_type) } : {},
          ...numberValue42(args.width) !== void 0 ? { width: numberValue42(args.width) } : {},
          ...numberValue42(args.height) !== void 0 ? { height: numberValue42(args.height) } : {},
          ...numberValue42(args.durationSec ?? args.duration_sec) !== void 0 ? { duration_sec: numberValue42(args.durationSec ?? args.duration_sec) } : {},
          ...optionalRecord2(args.metadata) ? { metadata: optionalRecord2(args.metadata) } : {}
        }]
      });
    }
    async function domainCreateAssetSlotCandidate2(args) {
      return runtimeMutation2(args, (runtime) => runtime.createAssetSlotCandidate({
        payload: requiredRecord2(args.payload, "payload"),
        ...optionalRecord2(args.targetRecord ?? args.target_record) ? { targetRecord: optionalRecord2(args.targetRecord ?? args.target_record) } : {},
        ...stringValue132(args.nonce) ? { nonce: stringValue132(args.nonce) } : {}
      }));
    }
    async function domainCreateKeyframeCandidate2(args) {
      return runtimeMutation2(args, (runtime) => runtime.createKeyframeCandidate({
        payload: requiredRecord2(args.payload, "payload"),
        ...optionalRecord2(args.targetRecord ?? args.target_record) ? { targetRecord: optionalRecord2(args.targetRecord ?? args.target_record) } : {},
        ...stringValue132(args.nonce) ? { nonce: stringValue132(args.nonce) } : {}
      }));
    }
    async function domainSelectContentUnitCandidate2(args) {
      const contentUnitId = requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
      const result = await runtimeMutation2(args, (runtime) => runtime.selectContentUnitCandidate({
        contentUnitId,
        candidateId: requiredId22(args.candidateId ?? args.candidate_id, "candidateId"),
        ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId52(args.resourceId ?? args.resource_id) } : {},
        ...stringValue132(args.stalePolicy ?? args.stale_policy) ? { stalePolicy: stringValue132(args.stalePolicy ?? args.stale_policy) } : {},
        ...stringValue132(args.reason) ? { reason: stringValue132(args.reason) } : {}
      }));
      const visibility = await withContentUnitCandidateVisibility2(args, contentUnitId, result, {
        adoption: "selection",
        requires_user_adoption: false
      });
      return {
        ...visibility,
        surface: createContentCandidatesSurface2(args, {
          contentUnitId,
          candidateId: String(requiredId22(args.candidateId ?? args.candidate_id, "candidateId")),
          ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId52(args.resourceId ?? args.resource_id) } : {},
          projectId: projectIdFromArgs2(args)
        }),
        secondary_surfaces: [
          createImpactSurface2(args, {
            projectId: projectIdFromArgs2(args),
            target: String(contentUnitId),
            source: "domain_select_content_unit_candidate"
          })
        ]
      };
    }
    async function domainSelectContentUnitCandidateBatch2(args) {
      return runDomainBatch2(args, domainSelectContentUnitCandidate2);
    }
    async function domainDecideContentUnitCandidate2(args) {
      const contentUnitId = requiredId22(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
      const decision = requiredDecision2(args.decision);
      const result = await runtimeMutation2(args, (runtime) => runtime.decideContentUnitCandidate({
        contentUnitId,
        candidateId: requiredId22(args.candidateId ?? args.candidate_id, "candidateId"),
        decision,
        ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId52(args.resourceId ?? args.resource_id) } : {},
        ...stringValue132(args.stalePolicy ?? args.stale_policy) ? { stalePolicy: stringValue132(args.stalePolicy ?? args.stale_policy) } : {},
        ...stringValue132(args.reason) ? { reason: stringValue132(args.reason) } : {},
        ...stringValue132(args.decidedAt ?? args.decided_at) ? { decidedAt: stringValue132(args.decidedAt ?? args.decided_at) } : {},
        ...optionalRecord2(args.metadata) ? { metadata: optionalRecord2(args.metadata) } : {}
      }));
      const visibility = await withContentUnitCandidateVisibility2(args, contentUnitId, result, {
        adoption: decision,
        requires_user_adoption: decision !== "adopt"
      });
      return {
        ...visibility,
        surface: createContentCandidatesSurface2(args, {
          contentUnitId,
          candidateId: String(requiredId22(args.candidateId ?? args.candidate_id, "candidateId")),
          ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId52(args.resourceId ?? args.resource_id) } : {},
          projectId: projectIdFromArgs2(args)
        }),
        ...decision === "adopt" ? {
          secondary_surfaces: [
            createImpactSurface2(args, {
              projectId: projectIdFromArgs2(args),
              target: String(contentUnitId),
              source: "domain_decide_content_unit_candidate"
            })
          ]
        } : {}
      };
    }
    async function domainSelectCandidate2(args) {
      return runtimeMutation2(args, (runtime) => runtime.selectCandidate({
        targetPath: requiredString22(args.targetPath ?? args.target_path, "targetPath"),
        targetKind: requiredTargetKind2(args),
        candidateId: requiredString22(args.candidateId ?? args.candidate_id, "candidateId"),
        ...stringValue132(args.reason) ? { reason: stringValue132(args.reason) } : {}
      }));
    }
    async function domainUpdateCandidate2(args) {
      return runtimeMutation2(args, (runtime) => runtime.updateCandidate({
        targetPath: requiredString22(args.targetPath ?? args.target_path, "targetPath"),
        targetKind: requiredTargetKind2(args),
        candidateId: requiredString22(args.candidateId ?? args.candidate_id, "candidateId"),
        payload: requiredRecord2(args.payload, "payload")
      }));
    }
    async function domainUnlockCandidate2(args) {
      return runtimeMutation2(args, (runtime) => runtime.unlockCandidate({
        targetPath: requiredString22(args.targetPath ?? args.target_path, "targetPath"),
        targetKind: requiredTargetKind2(args)
      }));
    }
    async function domainDeleteEntity2(args) {
      await runtimeMutation2(args, (runtime) => runtime.deleteEntity({
        entity: optionalRecord2(args.entity),
        record: optionalRecord2(args.record) ?? {}
      }));
      return { status: "deleted" };
    }
    async function domainReview2(args) {
      return service2(args).reviewWorkspace(inspectInput2(args));
    }
    async function domainInspect2(args) {
      return service2(args).inspectWorkspace(inspectInput2(args));
    }
    async function domainOverview2(args) {
      return service2(args).overviewWorkspace();
    }
    async function domainReadProductionWorkPlan2(args) {
      return service2(args).productionWorkPlan();
    }
    async function domainProductionStatusSummary2(args) {
      const snapshot = await service2(args).loadContentWorkspaceSnapshot();
      const requestedProductionId = args.productionId ?? args.production_id;
      const productionIds = requestedProductionId !== void 0 ? [String(requiredId22(requestedProductionId, "productionId"))] : snapshot.productions.map((item) => String(idValue32(item.id ?? item.record.id ?? item.record.ID ?? item.path)));
      const contentUnitSummaries = contentSourceWorkspaceContentUnitStatusSummaries2(snapshot);
      const editingByProduction = new Map((snapshot.editingTimelines ?? []).filter((item) => item.targetKind === "production").map((item) => [String(item.targetId), item]));
      const projectTimelineStatus = buildContentSourceWorkspaceProjectTimelineStatus2(snapshot, contentUnitSummaries);
      return {
        schema: "movscript.production_status_summary.v1",
        status: "ok",
        legacy_alias: true,
        preferred_schema: "movscript.project_timeline_status.v1",
        namespace_vocabulary: projectTimelineStatus.namespace_vocabulary,
        project_timeline_status: projectTimelineStatus,
        timeline_namespaces: projectTimelineStatus.timeline_namespaces,
        timeline_assemblies: projectTimelineStatus.timeline_assemblies,
        production_count: productionIds.length,
        productions: productionIds.map((productionId) => {
          const production = snapshot.productions.find((item) => sameId62(item.id ?? item.record.id ?? item.record.ID, productionId));
          const editing = editingByProduction.get(productionId);
          return {
            production_id: productionId,
            title: stringValue132(production?.record.title),
            path: production?.path,
            prerequisites: {
              settings_count: snapshot.settings.length,
              setting_states_count: snapshot.settingStates.length,
              assets_count: snapshot.assets.length
            },
            storyboards: snapshot.storyboards.map((item) => entityStatusLine2(item)),
            keyframes: snapshot.keyframes.map((item) => entityStatusLine2(item)),
            content_units: contentUnitSummaries,
            shots_videos: contentUnitSummaries.filter((item) => item.output_kind === "video" || item.content_unit_type === "scene_moment_ref" || item.content_unit_type === "scence_moment_ref"),
            job_status: "not_tracked_in_domain_summary",
            blocking_refs: Array.isArray(editing?.blockers) ? editing.blockers : [],
            stale_status: contentUnitSummaries.some((item) => item.stale_status === "stale") ? "has_stale_selection" : "ok"
          };
        }),
        surface: createProjectStatusSurface2(args, {
          projectId: projectIdFromArgs2(args),
          ...productionIds.length === 1 ? { productionId: productionIds[0] } : {}
        })
      };
    }
    async function domainInterpret2(args) {
      return runtimeMutation2(args, (runtime) => runtime.interpretWorkspace());
    }
    async function domainRegenerationPlan2(args) {
      const result = await service2(args).regenerationPlan();
      return isRecord342(result) ? {
        ...result,
        surface: createImpactSurface2(args, {
          projectId: projectIdFromArgs2(args),
          target: stringValue132(args.target ?? args.contentUnitId ?? args.content_unit_id),
          source: "domain_regeneration_plan"
        })
      } : result;
    }
    function service2(args) {
      return createMovScriptDomainRuntime2(resolveMCPProjectWorkspaceLocator2(args));
    }
    function resolveAssetRefSelection2(index, asset) {
      const assetRef32 = asset.id ?? lastPathSegment82(asset.path);
      const contentUnit = index.entities.find((entity) => entity.entityKind === "content_unit" && entity.record.content_unit_type === "asset_ref" && sameEntityRef3(entity.record.asset_ref, assetRef32, "asset"));
      if (!contentUnit) return {};
      const contentUnitPath = contentUnit.path.replace(/\/content_unit\.json$/, "");
      const selection = index.documents.find((document) => {
        if (!isRecord342(document.data)) return false;
        return document.data.schema === "movscript.decision_context.v1" && document.data.target_kind === "content_unit" && document.data.target_ref === contentUnitPath;
      })?.data;
      const selectionRecord22 = isRecord342(selection) && isRecord342(selection.selection) ? selection.selection : void 0;
      return {
        contentUnitId: contentUnit.id,
        contentUnitPath,
        candidateId: idFromUnknown2(selectionRecord22?.candidate_id ?? selectionRecord22?.candidateId),
        resourceId: resourceIdFromUnknown2(selectionRecord22?.resource_id ?? selectionRecord22?.resourceId)
      };
    }
    async function patchAssetProviderCertification2(projectDir, assetPath22, fallbackRecord, provider, certification) {
      const fileRepository = createNodeMovScriptWorkspaceFileRepository2(projectDir);
      const path = normalizeWorkspacePath8(assetPath22);
      const current = await fileRepository.read({ path }).then((file) => JSON.parse(file.content)).catch(() => fallbackRecord);
      if (!isRecord342(current)) throw new Error(`asset source is not a JSON object: ${path}`);
      const providerCertifications = isRecord342(current.provider_certifications) ? { ...current.provider_certifications } : {};
      providerCertifications[providerCertificationStorageKey2(provider, certification)] = certification;
      const next = {
        ...current,
        provider_certifications: providerCertifications
      };
      await fileRepository.write({ path, content: `${JSON.stringify(next, null, 2)}
` });
      return {
        path,
        certification
      };
    }
    function providerCertificationStorageKey2(provider, certification) {
      const model = getOptionalCertificationString2(certification.model ?? certification.public_model_id ?? certification.publicModelId ?? certification.provider_model_id ?? certification.providerModelId);
      return model ? `${provider}::model:${model}` : provider;
    }
    function providerCertificationProvider2(value) {
      const provider = getOptionalCertificationString2(value) ?? "volcengine_ark_official";
      const normalized = provider.toLowerCase();
      if (normalized === "jimeng" || normalized === "jimeng2" || normalized === "seedance" || normalized === "seedance2") return "volcengine_ark_official";
      if (normalized === "yunwu" || normalized === "yunwu_gateway") return "yunwu_gateway";
      return provider;
    }
    function getOptionalCertificationString2(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function idFromUnknown2(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      return void 0;
    }
    function resourceIdFromUnknown2(value) {
      const number = numberValue42(value);
      return number !== void 0 && Number.isInteger(number) && number > 0 ? number : void 0;
    }
    async function runtimeMutation2(args, action) {
      const locator = await requireMCPBackendBoundProject2(args);
      try {
        return await action(createMovScriptDomainRuntime2(locator));
      } finally {
        invalidateMovScriptDomainRuntime2(locator);
      }
    }
    async function runDomainBatch2(args, action) {
      const items = requiredArray2(args.items, "items");
      if (items.length === 0) throw new Error("items must contain at least one item");
      const continueOnError = booleanValue4(args.continueOnError ?? args.continue_on_error) ?? true;
      const defaults = batchDefaults3(args, /* @__PURE__ */ new Set(["items", "continueOnError", "continue_on_error"]));
      const results = [];
      for (let index = 0; index < items.length; index += 1) {
        const item = items[index];
        if (!isRecord342(item)) {
          results.push({ index, status: "error", error: `items[${index}] must be an object` });
          if (!continueOnError) break;
          continue;
        }
        try {
          results.push({ index, status: "ok", result: await action({ ...defaults, ...item }) });
        } catch (error) {
          results.push({ index, status: "error", error: errorMessage22(error) });
          if (!continueOnError) break;
        }
      }
      for (let index = results.length; index < items.length; index += 1) {
        results.push({
          index,
          status: "skipped",
          error: "Skipped because an earlier item failed and continue_on_error is false."
        });
      }
      const successCount = results.filter((item) => item.status === "ok").length;
      const failedCount = results.filter((item) => item.status === "error").length;
      return {
        status: failedCount === 0 ? "completed" : successCount > 0 ? "partial_error" : "error",
        total: items.length,
        success_count: successCount,
        failed_count: failedCount,
        items: results,
        message: `${successCount}/${items.length} domain batch item(s) completed.`
      };
    }
    function batchDefaults3(args, excluded) {
      const defaults = {};
      for (const [key, value] of Object.entries(args)) {
        if (!excluded.has(key) && value !== void 0) defaults[key] = value;
      }
      return defaults;
    }
    function contextIds2(args) {
      return {
        ...args.productionId !== void 0 || args.production_id !== void 0 ? { productionId: idValue32(args.productionId ?? args.production_id) } : {},
        ...args.segmentId !== void 0 || args.segment_id !== void 0 ? { segmentId: idValue32(args.segmentId ?? args.segment_id) } : {},
        ...args.sceneMomentId !== void 0 || args.scene_moment_id !== void 0 ? { sceneMomentId: idValue32(args.sceneMomentId ?? args.scene_moment_id) } : {},
        ...args.storyboardId !== void 0 || args.storyboard_id !== void 0 ? { storyboardId: idValue32(args.storyboardId ?? args.storyboard_id) } : {},
        ...args.contentUnitId !== void 0 || args.content_unit_id !== void 0 ? { contentUnitId: idValue32(args.contentUnitId ?? args.content_unit_id) } : {},
        ...args.settingId !== void 0 || args.setting_id !== void 0 ? { settingId: idValue32(args.settingId ?? args.setting_id) } : {},
        ...args.settingStateId !== void 0 || args.setting_state_id !== void 0 ? { settingStateId: idValue32(args.settingStateId ?? args.setting_state_id) } : {}
      };
    }
    function productionIdFrom2(args, production) {
      return idValue32(args.productionId ?? args.production_id ?? production?.id ?? production?.client_id ?? "main");
    }
    function requiredProductionScopeId2(args, field) {
      if (args.productionId !== void 0 || args.production_id !== void 0) {
        return requiredId22(args.productionId ?? args.production_id, field);
      }
      const focus = normalizeDomainFocus2(args);
      if (focus.scope?.kind === "production" && focus.scope.ref) return focus.scope.ref;
      throw new Error(`Missing required argument: ${field}`);
    }
    function productionWriteResult2(entityKind, ids, result) {
      return {
        status: "upserted",
        entityKind,
        ...ids,
        productionPath: result.productionPath,
        writtenPaths: result.writtenPaths,
        snapshot: result.snapshot
      };
    }
    function timelineNamespaceTreeItems2(args) {
      const arrayValue222 = args.namespaces ?? args.timeline_namespaces ?? args.timelineNamespaces ?? args.nodes;
      const arrayItems = arrayRecords22(arrayValue222);
      if (arrayItems.length > 0) return arrayItems;
      const root = optionalRecord2(args.namespace ?? args.root ?? args.tree ?? args.payload ?? args.record);
      return root ? [root] : [];
    }
    function timelineNamespaceEntityKind2(payload, context) {
      const explicit = stringValue132(payload.entity_kind ?? payload.entityKind ?? payload.source_kind ?? payload.sourceKind);
      if (explicit === "production" || explicit === "segment") return explicit;
      const targetPath = stringValue132(payload.target_path ?? payload.targetPath);
      if (targetPath?.endsWith("/production.json")) return "production";
      if (targetPath?.endsWith("/segment.json")) return "segment";
      return context.depth === 0 ? "production" : "segment";
    }
    function timelineNamespaceTargetPath2(item, payload, context, entityKind, namespaceId) {
      const explicit = stringValue132(payload.target_path ?? payload.targetPath ?? item.target_path ?? item.targetPath);
      if (explicit) return explicit.replace(/^\/+/, "");
      const idToken = pathToken2(namespaceId);
      if (entityKind === "production" || !context.parentTargetPath) {
        return entityKind === "production" ? `timeline/${idToken}/production.json` : `timeline/${idToken}/segment.json`;
      }
      return `${timelineNamespaceNodeDir2(context.parentTargetPath)}/segments/${idToken}/segment.json`;
    }
    function timelineNamespaceNodeDir2(targetPath) {
      return targetPath.replace(/\/(?:production|segment)\.json$/, "");
    }
    function normalizedSourcePath2(value) {
      return value.trim().replace(/^\/+/, "").replace(/\/+$/, "");
    }
    function sourceNodeDir2(path) {
      const normalized = normalizedSourcePath2(path);
      const namespaceDir = timelineNamespaceNodeDir2(normalized);
      if (namespaceDir !== normalized) return namespaceDir;
      return normalized.endsWith(".json") ? normalized.replace(/\/[^/]+\.json$/, "") : normalized;
    }
    function timelineNamespaceRecord2(payload, input3) {
      const sanitized = stripNamespaceRecordFields2(payload);
      return pruneUndefinedRecord22({
        ...sanitized,
        schema: `movscript.${input3.entityKind}.v1`,
        kind: input3.entityKind,
        id: input3.namespaceId,
        title: stringValue132(payload.title),
        order: numberValue42(payload.order),
        intent: stringValue132(payload.intent ?? payload.summary ?? payload.description),
        namespace_kind: input3.namespaceKind,
        timeline_namespace_kind: input3.namespaceKind
      });
    }
    function timelineNamespaceChildItems2(item, payload) {
      return arrayRecords22(
        item.children ?? item.namespaces ?? item.timeline_namespaces ?? item.timelineNamespaces ?? item.segments ?? payload.children ?? payload.namespaces ?? payload.timeline_namespaces ?? payload.timelineNamespaces ?? payload.segments
      );
    }
    function timelineNamespaceSceneMomentItems2(item, payload) {
      return arrayRecords22(item.scene_moments ?? item.sceneMoments ?? payload.scene_moments ?? payload.sceneMoments);
    }
    function sceneMomentPathTarget2(args, record, sceneMomentId) {
      const explicit = stringValue132(args.targetPath ?? args.target_path ?? record.targetPath ?? record.target_path);
      if (explicit) return normalizedSourcePath2(explicit);
      const namespacePath = stringValue132(
        args.namespacePath ?? args.namespace_path ?? args.timelineNamespacePath ?? args.timeline_namespace_path ?? args.parentPath ?? args.parent_path ?? record.namespacePath ?? record.namespace_path ?? record.timelineNamespacePath ?? record.timeline_namespace_path ?? record.parentPath ?? record.parent_path
      );
      if (!namespacePath) return void 0;
      return `${sourceNodeDir2(namespacePath)}/scene_moments/${pathToken2(sceneMomentId)}/scene_moment.json`;
    }
    function sceneMomentHierarchyRecord2(payload, sceneMomentId) {
      return pruneUndefinedRecord22({
        ...timelinePrimitiveRecord2(payload, TIMELINE_PRIMITIVE_SPECS2.scene_moment, sceneMomentId),
        storyboard_id: optionalId2(payload.storyboard_id ?? payload.storyboardId),
        time_text: stringValue132(payload.time_text ?? payload.timeText ?? payload.when),
        scene_code: stringValue132(payload.scene_code ?? payload.sceneCode),
        location_text: stringValue132(payload.location_text ?? payload.locationText ?? payload.where),
        condition_text: stringValue132(payload.condition_text ?? payload.conditionText),
        action_text: stringValue132(payload.action_text ?? payload.actionText ?? payload.action),
        mood: stringValue132(payload.mood ?? payload.emotion),
        description: stringValue132(payload.description),
        settings: Array.isArray(payload.settings) ? payload.settings : void 0,
        setting_refs: Array.isArray(payload.setting_refs ?? payload.settingRefs) ? payload.setting_refs ?? payload.settingRefs : void 0
      });
    }
    function timelinePrimitivePathTarget2(args, record, input3) {
      const explicit = stringValue132(args.targetPath ?? args.target_path ?? record.targetPath ?? record.target_path);
      if (explicit) return normalizedSourcePath2(explicit);
      const parentPath = timelinePrimitiveParentPath2(args, record, input3.spec);
      if (!parentPath) return void 0;
      return `${sourceNodeDir2(parentPath)}/${input3.spec.collection}/${pathToken2(input3.id)}/${input3.spec.filename}`;
    }
    function timelinePrimitiveParentPath2(args, record, spec) {
      const explicitParent = stringValue132(args.parentPath ?? args.parent_path ?? record.parentPath ?? record.parent_path);
      if (explicitParent) return explicitParent;
      const expressionUnitPath = stringValue132(
        args.expressionUnitPath ?? args.expression_unit_path ?? record.expressionUnitPath ?? record.expression_unit_path
      );
      if ((spec.entityKind === "keyframe" || spec.entityKind === "storyboard") && expressionUnitPath) return expressionUnitPath;
      return stringValue132(
        args.sceneMomentPath ?? args.scene_moment_path ?? record.sceneMomentPath ?? record.scene_moment_path
      );
    }
    async function writeTimelinePrimitivePathTarget2(args, spec, payload, id, targetPath) {
      const result = await runtimeMutation2(args, (runtime) => runtime.writeHierarchyNode({
        targetPath,
        record: timelinePrimitiveRecord2(payload, spec, id)
      }));
      return {
        status: "upserted",
        entityKind: spec.entityKind,
        [`${camelEntityKind22(spec.entityKind)}Id`]: id,
        targetPath,
        result
      };
    }
    async function upsertTimelinePrimitiveNode2(runtime, item, input3, collector) {
      const payload = treePayload2(item, input3.spec.payloadName);
      const primitiveId = requiredId22(payload.id ?? payload.client_id, `${input3.spec.payloadName}.id`);
      const targetPath = timelinePrimitiveTargetPath2(item, payload, input3.parentDir, input3.spec, primitiveId);
      const targetRef = timelinePrimitiveRef2(targetPath, input3.spec);
      const record = timelinePrimitiveRecord2(payload, input3.spec, primitiveId);
      const node = await runtime.writeHierarchyNode({ targetPath, record });
      const summary = {
        id: primitiveId,
        entity_kind: input3.spec.entityKind,
        targetPath,
        targetRef,
        node
      };
      if (input3.spec.entityKind === "scene_moment") {
        collector.sceneMoments.push(summary);
      } else {
        collector.primitives.push(summary);
      }
      const contentUnits = [];
      for (const contentUnitItem of arrayRecords22(item.content_units ?? item.contentUnits ?? payload.content_units ?? payload.contentUnits)) {
        const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
        const contentUnit = await runtime.createContentUnit(timelinePrimitiveContentUnitInput2(contentUnitPayload, input3.spec, targetRef));
        contentUnits.push(contentUnit);
        collector.contentUnits.push(contentUnit);
      }
      const parentDir = timelinePrimitiveNodeDir2(targetPath, input3.spec);
      const expressionUnits = input3.spec.entityKind === "scene_moment" ? await upsertTimelinePrimitiveChildren2(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS2.expression_unit, collector) : [];
      const storyboards = timelinePrimitiveSupportsSceneChildren2(input3.spec) ? await upsertTimelinePrimitiveChildren2(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS2.storyboard, collector) : [];
      const keyframes = timelinePrimitiveSupportsSceneChildren2(input3.spec) ? await upsertTimelinePrimitiveChildren2(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS2.keyframe, collector) : [];
      const audioCues = timelinePrimitiveSupportsSceneChildren2(input3.spec) ? await upsertTimelinePrimitiveChildren2(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS2.audio_cue, collector) : [];
      return {
        ...summary,
        contentUnits,
        ...expressionUnits.length > 0 ? { expressionUnits } : {},
        ...storyboards.length > 0 ? { storyboards } : {},
        ...keyframes.length > 0 ? { keyframes } : {},
        ...audioCues.length > 0 ? { audioCues } : {}
      };
    }
    async function upsertTimelinePrimitiveChildren2(runtime, item, payload, parentDir, spec, collector) {
      const children = [];
      for (const child of timelinePrimitiveChildItems2(item, payload, spec)) {
        children.push(await upsertTimelinePrimitiveNode2(runtime, child, { spec, parentDir }, collector));
      }
      return children;
    }
    function timelinePrimitiveChildItems2(item, payload, spec) {
      const camel = camelCollectionName2(spec.collection);
      return arrayRecords22(item[spec.collection] ?? item[camel] ?? payload[spec.collection] ?? payload[camel]);
    }
    function timelinePrimitiveSupportsSceneChildren2(spec) {
      return spec.entityKind === "scene_moment" || spec.entityKind === "expression_unit";
    }
    function timelinePrimitiveTargetPath2(item, payload, parentDir, spec, primitiveId) {
      const explicit = stringValue132(payload.target_path ?? payload.targetPath ?? item.target_path ?? item.targetPath);
      if (explicit) return explicit.replace(/^\/+/, "");
      return `${parentDir}/${spec.collection}/${pathToken2(primitiveId)}/${spec.filename}`;
    }
    function timelinePrimitiveRef2(targetPath, spec) {
      return targetPath.replace(new RegExp(`/${escapeRegExp22(spec.filename)}$`), "");
    }
    function timelinePrimitiveNodeDir2(targetPath, spec) {
      return timelinePrimitiveRef2(targetPath, spec);
    }
    function timelinePrimitiveRecord2(payload, spec, primitiveId) {
      const sanitized = stripTimelinePrimitiveRecordFields2(payload);
      return pruneUndefinedRecord22({
        ...sanitized,
        schema: `movscript.${spec.entityKind}.v1`,
        kind: spec.entityKind,
        id: primitiveId,
        title: stringValue132(payload.title),
        order: numberValue42(payload.order),
        ...spec.entityKind === "scene_moment" ? {
          storyboard_id: optionalId2(payload.storyboard_id ?? payload.storyboardId),
          time_text: stringValue132(payload.time_text ?? payload.timeText ?? payload.when),
          scene_code: stringValue132(payload.scene_code ?? payload.sceneCode),
          location_text: stringValue132(payload.location_text ?? payload.locationText ?? payload.where),
          condition_text: stringValue132(payload.condition_text ?? payload.conditionText),
          action_text: stringValue132(payload.action_text ?? payload.actionText ?? payload.action),
          mood: stringValue132(payload.mood ?? payload.emotion),
          description: stringValue132(payload.description)
        } : {},
        ...spec.entityKind === "expression_unit" ? {
          slot_kind: stringValue132(payload.slot_kind ?? payload.slotKind),
          expression_kind: stringValue132(payload.expression_kind ?? payload.expressionKind ?? nonSystemKind2(payload.kind, spec.entityKind)),
          visual_kind: stringValue132(payload.visual_kind ?? payload.visualKind),
          speaker_ref: optionalId2(payload.speaker_ref ?? payload.speakerRef),
          source_expression_ref: optionalId2(payload.source_expression_ref ?? payload.sourceExpressionRef),
          text: stringValue132(payload.text ?? payload.content),
          note: stringValue132(payload.note),
          intent: stringValue132(payload.intent ?? payload.summary ?? payload.description),
          content: optionalRecord2(payload.content),
          timing_intent: optionalRecord2(payload.timing_intent ?? payload.timingIntent),
          voice_profile_ref: optionalId2(payload.voice_profile_ref ?? payload.voiceProfileRef),
          span: optionalRecord2(payload.span),
          script_block_id: optionalId2(payload.script_block_id ?? payload.scriptBlockId)
        } : {},
        ...spec.entityKind === "storyboard" ? {
          visual_intent: stringValue132(payload.visual_intent ?? payload.visualIntent ?? payload.prompt_hint ?? payload.promptHint ?? payload.description),
          timeline: optionalRecord2(payload.timeline),
          graph: optionalRecord2(payload.graph)
        } : {},
        ...spec.entityKind === "keyframe" ? {
          role: stringValue132(payload.role ?? payload.status),
          visual_intent: stringValue132(payload.visual_intent ?? payload.visualIntent ?? payload.prompt_hint ?? payload.promptHint ?? payload.description),
          timing: optionalRecord2(payload.timing),
          composition: optionalRecord2(payload.composition),
          continuity: optionalRecord2(payload.continuity),
          reference_asset_refs: Array.isArray(payload.reference_asset_refs ?? payload.referenceAssetRefs) ? payload.reference_asset_refs ?? payload.referenceAssetRefs : void 0,
          reference_keyframe_refs: Array.isArray(payload.reference_keyframe_refs ?? payload.referenceKeyframeRefs) ? payload.reference_keyframe_refs ?? payload.referenceKeyframeRefs : void 0
        } : {},
        ...spec.entityKind === "audio_cue" ? {
          cue_kind: stringValue132(payload.cue_kind ?? payload.cueKind ?? nonSystemKind2(payload.kind, spec.entityKind)),
          storyboard_id: optionalId2(payload.storyboard_id ?? payload.storyboardId),
          expression_unit_ref: optionalId2(payload.expression_unit_ref ?? payload.expressionUnitRef ?? payload.expression_unit_id ?? payload.expressionUnitId),
          prompt_hint: stringValue132(payload.prompt_hint ?? payload.promptHint),
          asset_refs: Array.isArray(payload.asset_refs ?? payload.assetRefs) ? payload.asset_refs ?? payload.assetRefs : void 0
        } : {}
      });
    }
    function timelinePrimitiveContentUnitInput2(record, spec, targetRef) {
      const input3 = treeContentUnitInput2(record, {
        contentUnitType: spec.contentUnitType,
        outputKind: spec.outputKind,
        targetKind: spec.targetKind,
        targetRef,
        [spec.refField]: targetRef
      });
      if (input3.productionId !== void 0 || input3.segmentId !== void 0) {
        throw new Error("timeline namespace primitive content_units must not use production_ref, production_id, segment_ref, or segment_id");
      }
      if (input3.contentUnitType === "production_ref" || input3.contentUnitType === "segment_ref") {
        throw new Error("timeline namespace primitive content_units must not use content_unit_type=production_ref or segment_ref");
      }
      return {
        ...input3,
        productionId: void 0,
        segmentId: void 0
      };
    }
    function stripTimelinePrimitiveRecordFields2(record) {
      const forbidden = /* @__PURE__ */ new Set([
        "content_units",
        "contentUnits",
        "content_unit_type",
        "contentUnitType",
        "expression_units",
        "expressionUnits",
        "storyboards",
        "keyframes",
        "audio_cues",
        "audioCues",
        "target_path",
        "targetPath",
        "namespace_path",
        "namespacePath",
        "timeline_namespace_path",
        "timelineNamespacePath",
        "scene_moment_path",
        "sceneMomentPath",
        "expression_unit_path",
        "expressionUnitPath",
        "parent_path",
        "parentPath",
        "target_category",
        "targetCategory",
        "target_kind",
        "targetKind",
        "target_ref",
        "targetRef",
        "output_kind",
        "outputKind",
        "production_ref",
        "productionRef",
        "production_id",
        "productionId",
        "segment_ref",
        "segmentRef",
        "segment_id",
        "segmentId",
        "content_unit_ref",
        "contentUnitRef",
        "content_unit_refs",
        "contentUnitRefs",
        "content_unit_id",
        "contentUnitId",
        "main_content_unit_id",
        "mainContentUnitId",
        "candidate",
        "candidates",
        "selection",
        "selections",
        "selected_candidate_id",
        "selectedCandidateId",
        "selected_resource_id",
        "selectedResourceId",
        "resource_id",
        "resourceId",
        "client_id",
        "clientId",
        "storyboardId",
        "expression_unit_id",
        "expressionUnitId",
        "expression_unit_ref",
        "expressionUnitRef",
        "timeText",
        "when",
        "sceneCode",
        "locationText",
        "where",
        "conditionText",
        "actionText",
        "action",
        "emotion",
        "expressionKind",
        "visualKind",
        "speakerRef",
        "sourceExpressionRef",
        "timingIntent",
        "voiceProfileRef",
        "scriptBlockId",
        "cueKind",
        "visualIntent",
        "promptHint",
        "referenceAssetRefs",
        "referenceKeyframeRefs",
        "assetRefs"
      ]);
      return Object.fromEntries(Object.entries(record).filter(([key, value]) => !forbidden.has(key) && value !== void 0));
    }
    function nonSystemKind2(value, entityKind) {
      const kind = stringValue132(value);
      return kind && kind !== entityKind ? kind : void 0;
    }
    function camelCollectionName2(collection) {
      return collection.replace(/_([a-z])/g, (_, char) => char.toUpperCase());
    }
    function camelEntityKind22(entityKind) {
      return entityKind.replace(/_([a-z])/g, (_, char) => char.toUpperCase());
    }
    function escapeRegExp22(value) {
      return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    }
    function timelineNamespaceAssemblyContentUnitInput2(record, scope) {
      const explicit = engineContentUnitInputFromRecord2(record);
      if (explicit.contentUnitType && explicit.contentUnitType !== "timeline_assembly_ref") {
        throw new Error("timeline namespace content_units must use content_unit_type=timeline_assembly_ref");
      }
      if (explicit.targetKind && explicit.targetKind !== "timeline_assembly") {
        throw new Error("timeline namespace content_units must use target_kind=timeline_assembly");
      }
      if (explicit.productionId !== void 0 || explicit.segmentId !== void 0) {
        throw new Error("timeline namespace content_units must not use production_ref, production_id, segment_ref, or segment_id");
      }
      const targetRef = explicit.targetRef ?? `timeline_assembly:${scope.namespaceKind}:${scope.namespaceId}`;
      return pruneUndefinedRecord22({
        ...explicit,
        contentUnitType: "timeline_assembly_ref",
        outputKind: explicit.outputKind ?? "video",
        targetCategory: "timeline_assembly",
        targetKind: "timeline_assembly",
        targetRef,
        scopeKind: explicit.scopeKind ?? scope.namespaceKind,
        scopeRef: explicit.scopeRef ?? scope.namespaceId,
        productionId: void 0,
        segmentId: void 0
      });
    }
    function stripNamespaceRecordFields2(record) {
      const forbidden = /* @__PURE__ */ new Set([
        "children",
        "namespaces",
        "timeline_namespaces",
        "timelineNamespaces",
        "segments",
        "content_units",
        "contentUnits",
        "content_unit_ref",
        "contentUnitRef",
        "content_unit_refs",
        "contentUnitRefs",
        "content_unit_id",
        "contentUnitId",
        "main_content_unit_id",
        "mainContentUnitId",
        "content_unit_type",
        "contentUnitType",
        "target_path",
        "targetPath",
        "target_category",
        "targetCategory",
        "target_kind",
        "targetKind",
        "target_ref",
        "targetRef",
        "output_kind",
        "outputKind",
        "production_ref",
        "productionRef",
        "production_id",
        "productionId",
        "segment_ref",
        "segmentRef",
        "segment_id",
        "segmentId",
        "scene_moment_ref",
        "sceneMomentRef",
        "asset_ref",
        "assetRef",
        "client_id",
        "clientId",
        "namespaceKind",
        "timelineNamespaceKind",
        "domain_kind",
        "domainKind",
        "entity_kind",
        "entityKind",
        "source_kind",
        "sourceKind",
        "candidate",
        "candidates",
        "selection",
        "selections",
        "selected_candidate_id",
        "selectedCandidateId",
        "selected_resource_id",
        "selectedResourceId",
        "resource_id",
        "resourceId"
      ]);
      return Object.fromEntries(Object.entries(record).filter(([key, value]) => !forbidden.has(key) && value !== void 0));
    }
    function pathToken2(value) {
      const token = String(value).trim().replace(/[\\/#?]+/g, "_").replace(/\s+/g, "_");
      return token || "node";
    }
    function treePayload2(item, name) {
      return requiredRecord2(item[name] ?? item.payload ?? item.record ?? item.entity ?? item, name);
    }
    function arrayRecords22(value) {
      return Array.isArray(value) ? value.filter(isRecord342) : [];
    }
    function treeContentUnitInput2(record, defaults) {
      const explicitInput = engineContentUnitInputFromRecord2(record);
      if (isExplicitTimelineAssemblyContentUnit2(explicitInput)) {
        return {
          ...defaults,
          ...explicitInput,
          productionId: explicitInput.productionId,
          segmentId: explicitInput.segmentId,
          sceneMomentId: explicitInput.sceneMomentId,
          expressionUnitId: explicitInput.expressionUnitId,
          storyboardId: explicitInput.storyboardId,
          keyframeId: explicitInput.keyframeId,
          audioCueId: explicitInput.audioCueId,
          assetRef: explicitInput.assetRef
        };
      }
      return {
        ...defaults,
        ...explicitInput
      };
    }
    function isExplicitTimelineAssemblyContentUnit2(input3) {
      return input3.contentUnitType === "timeline_assembly_ref" || input3.targetCategory === "timeline_assembly" || input3.targetKind === "timeline_assembly";
    }
    async function upsertTreeStoryboards2(runtime, parent, context) {
      const output3 = [];
      for (const item of arrayRecords22(parent.storyboards)) {
        const storyboard = treePayload2(item, "storyboard");
        const storyboardId = optionalId2(storyboard.id ?? storyboard.client_id) ?? "main";
        output3.push(await runtime.createStoryboard({
          productionId: context.productionId,
          segmentId: requiredId22(context.segmentId, "segmentId"),
          sceneMomentId: requiredId22(context.sceneMomentId, "sceneMomentId"),
          ...context.expressionUnitId ? { expressionUnitId: context.expressionUnitId } : {},
          id: storyboardId,
          title: stringValue132(storyboard.title),
          visualIntent: stringValue132(storyboard.visual_intent ?? storyboard.visualIntent ?? storyboard.prompt_hint ?? storyboard.description),
          order: numberValue42(storyboard.order),
          timeline: optionalRecord2(storyboard.timeline),
          graph: optionalRecord2(storyboard.graph)
        }));
        for (const contentUnitItem of arrayRecords22(item.content_units ?? item.contentUnits)) {
          const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
          output3.push(await runtime.createContentUnit(treeContentUnitInput2(contentUnitPayload, {
            productionId: context.productionId,
            segmentId: context.segmentId,
            sceneMomentId: context.sceneMomentId,
            expressionUnitId: context.expressionUnitId,
            storyboardId,
            contentUnitType: "storyboard_ref",
            outputKind: "image",
            targetKind: "storyboard",
            targetRef: storyboardId
          })));
        }
      }
      return output3;
    }
    async function upsertTreeKeyframes2(runtime, parent, context) {
      const output3 = [];
      for (const item of arrayRecords22(parent.keyframes)) {
        const keyframe = normalizeKeyframePayload2(treePayload2(item, "keyframe"));
        const keyframeId = requiredId22(keyframe.id ?? keyframe.client_id, "keyframe.id");
        output3.push(await runtime.createKeyframe({
          productionId: context.productionId,
          segmentId: requiredId22(context.segmentId, "segmentId"),
          sceneMomentId: requiredId22(context.sceneMomentId, "sceneMomentId"),
          ...context.expressionUnitId ? { expressionUnitId: context.expressionUnitId } : {},
          id: keyframeId,
          title: stringValue132(keyframe.title),
          role: stringValue132(keyframe.role ?? keyframe.status),
          visualIntent: stringValue132(keyframe.visual_intent ?? keyframe.visualIntent ?? keyframe.prompt_hint ?? keyframe.description),
          order: numberValue42(keyframe.order),
          timing: optionalRecord2(keyframe.timing),
          composition: optionalRecord2(keyframe.composition),
          continuity: optionalRecord2(keyframe.continuity),
          referenceAssetRefs: Array.isArray(keyframe.reference_asset_refs) ? keyframe.reference_asset_refs : void 0,
          referenceKeyframeRefs: Array.isArray(keyframe.reference_keyframe_refs) ? keyframe.reference_keyframe_refs : void 0
        }));
        for (const contentUnitItem of arrayRecords22(item.content_units ?? item.contentUnits)) {
          const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
          output3.push(await runtime.createContentUnit(treeContentUnitInput2(contentUnitPayload, {
            productionId: context.productionId,
            segmentId: context.segmentId,
            sceneMomentId: context.sceneMomentId,
            expressionUnitId: context.expressionUnitId,
            keyframeId,
            contentUnitType: "keyframe_ref",
            outputKind: "image",
            targetKind: "keyframe",
            targetRef: keyframeId
          })));
        }
      }
      return output3;
    }
    async function upsertTreeAudioCues2(runtime, parent, context) {
      const output3 = [];
      for (const item of arrayRecords22(parent.audio_cues ?? parent.audioCues)) {
        const audioCue = normalizeAudioCuePayload2(treePayload2(item, "audio_cue"));
        const audioCueId = requiredId22(audioCue.id ?? audioCue.client_id, "audio_cue.id");
        output3.push(await runtime.createAudioCue({
          productionId: context.productionId,
          segmentId: requiredId22(context.segmentId, "segmentId"),
          sceneMomentId: requiredId22(context.sceneMomentId, "sceneMomentId"),
          ...context.expressionUnitId ? { expressionUnitId: context.expressionUnitId } : {},
          id: audioCueId,
          title: stringValue132(audioCue.title),
          kind: stringValue132(audioCue.cue_kind ?? audioCue.kind),
          storyboardId: optionalId2(audioCue.storyboard_id ?? audioCue.storyboardId),
          promptHint: stringValue132(audioCue.prompt_hint ?? audioCue.promptHint)
        }));
        for (const contentUnitItem of arrayRecords22(item.content_units ?? item.contentUnits)) {
          const contentUnitPayload = treePayload2(contentUnitItem, "content_unit");
          output3.push(await runtime.createContentUnit(treeContentUnitInput2(contentUnitPayload, {
            productionId: context.productionId,
            segmentId: context.segmentId,
            sceneMomentId: context.sceneMomentId,
            expressionUnitId: context.expressionUnitId,
            audioCueId,
            contentUnitType: "expression_unit_ref",
            outputKind: "audio",
            targetKind: context.expressionUnitId ? "expression_unit" : "scene_moment",
            targetRef: context.expressionUnitId ?? context.sceneMomentId
          })));
        }
      }
      return output3;
    }
    function normalizeKeyframePayload2(record) {
      return pruneUndefinedRecord22({
        ...record,
        visual_intent: record.visual_intent ?? record.visualIntent,
        reference_asset_refs: record.reference_asset_refs ?? record.referenceAssetRefs,
        reference_keyframe_refs: record.reference_keyframe_refs ?? record.referenceKeyframeRefs
      });
    }
    function normalizeAudioCuePayload2(record) {
      return pruneUndefinedRecord22({
        ...record,
        cue_kind: record.cue_kind ?? record.cueKind ?? record.kind,
        storyboard_id: record.storyboard_id ?? record.storyboardId,
        storyboard_ref: record.storyboard_ref ?? record.storyboardRef,
        prompt_hint: record.prompt_hint ?? record.promptHint,
        asset_refs: record.asset_refs ?? record.assetRefs
      });
    }
    function normalizeExpressionUnitPayload2(record) {
      return pruneUndefinedRecord22({
        ...record,
        slot_kind: record.slot_kind ?? record.slotKind,
        kind: record.kind ?? record.expression_kind ?? record.expressionKind,
        script_block_id: record.script_block_id ?? record.scriptBlockId
      });
    }
    function pruneUndefinedRecord22(record) {
      return Object.fromEntries(Object.entries(record).filter(([, value]) => value !== void 0));
    }
    function engineContentUnitInputFromRecord2(record) {
      const editPrompt = optionalRecord2(record.edit_prompt ?? record.editPrompt);
      const modelIntent = optionalRecord2(record.model_intent ?? record.modelIntent);
      return pruneUndefinedRecord22({
        id: record.id ?? record.ID ?? record.client_id,
        title: record.title,
        kind: record.kind,
        contentUnitType: record.content_unit_type ?? record.contentUnitType,
        outputKind: record.output_kind ?? record.outputKind,
        targetCategory: record.target_category ?? record.targetCategory,
        targetKind: record.target_kind ?? record.targetKind,
        targetRef: record.target_ref ?? record.targetRef,
        scopeKind: record.scope_kind ?? record.scopeKind,
        scopeRef: record.scope_ref ?? record.scopeRef,
        generationRole: record.generation_role ?? record.generationRole,
        assetRef: record.asset_ref ?? record.assetRef,
        productionId: record.production_ref ?? record.productionRef ?? record.productionId ?? record.production_id,
        segmentId: record.segment_ref ?? record.segmentRef ?? record.segmentId ?? record.segment_id,
        sceneMomentId: record.scene_moment_ref ?? record.sceneMomentRef ?? record.sceneMomentId ?? record.scene_moment_id,
        expressionUnitId: record.expression_unit_ref ?? record.expressionUnitRef ?? record.expressionUnitId ?? record.expression_unit_id,
        storyboardId: record.storyboard_ref ?? record.storyboardRef ?? record.storyboardId ?? record.storyboard_id,
        keyframeId: record.keyframe_ref ?? record.keyframeRef ?? record.keyframeId ?? record.keyframe_id,
        audioCueId: record.audio_cue_ref ?? record.audioCueRef ?? record.audioCueId ?? record.audio_cue_id,
        prompt: record.prompt ?? editPrompt?.text,
        negativePrompt: record.negative_prompt ?? record.negativePrompt ?? editPrompt?.negative_text,
        description: record.description,
        order: numberValue42(record.order),
        modelIntent
      });
    }
    function settingRefsFromRecord22(record) {
      const refs = Array.isArray(record.setting_refs) ? record.setting_refs.filter(isRecord342) : [];
      return refs.flatMap((ref) => {
        const settingId = ref.setting_id ?? ref.settingId ?? ref.setting_ref ?? ref.settingRef;
        if (settingId === void 0 || settingId === null || String(settingId).trim() === "") return [];
        return [{
          id: idValue32(settingId),
          ...ref.setting_state_id !== void 0 || ref.settingStateId !== void 0 || ref.setting_state_ref !== void 0 || ref.settingStateRef !== void 0 ? { settingStateId: idValue32(ref.setting_state_id ?? ref.settingStateId ?? ref.setting_state_ref ?? ref.settingStateRef) } : {},
          ...stringValue132(ref.role) ? { role: stringValue132(ref.role) } : {},
          ...stringValue132(ref.notes ?? ref.source_label ?? ref.sourceLabel) ? { sourceLabel: stringValue132(ref.notes ?? ref.source_label ?? ref.sourceLabel) } : {},
          ...stringValue132(ref.setting_kind ?? ref.kind) ? { kind: stringValue132(ref.setting_kind ?? ref.kind) } : {}
        }];
      });
    }
    function requiredTargetKind2(args) {
      const value = requiredString22(args.targetKind ?? args.target_kind, "targetKind");
      if (value === "asset" || value === "keyframe") return value;
      if (value === "content_unit") {
        throw new Error("content_unit candidates are backend decision records; use domain_create_content_candidate/domain_select_content_unit_candidate");
      }
      throw new Error("targetKind must be asset or keyframe");
    }
    function semanticKind2(value) {
      return stringValue132(value);
    }
    function inspectInput2(args) {
      const commit = stringValue132(args.commit);
      const checkpointHash = stringValue132(args.checkpointHash ?? args.checkpoint_hash);
      return {
        ...commit ? { commit } : {},
        ...checkpointHash ? { checkpointHash } : {}
      };
    }
    function contentCandidateStatus2(value) {
      const status = stringValue132(value);
      if (!status) return void 0;
      if (CONTENT_CANDIDATE_STATUSES2.has(status)) return status;
      throw new Error("status must be queued, running, succeeded, failed, canceled, or imported");
    }
    function requiredDecision2(value) {
      const decision = stringValue132(value);
      if (decision === "adopt" || decision === "reject" || decision === "defer") return decision;
      throw new Error("decision must be adopt, reject, or defer");
    }
    function optionalRecord2(value) {
      return isRecord342(value) ? value : void 0;
    }
    function requiredRecord2(value, name) {
      if (isRecord342(value)) return value;
      throw new Error(`${name} is required`);
    }
    function upsertPayloadRecord2(args) {
      return requiredRecord2(args.payload ?? args.record ?? args.entity, "payload");
    }
    function requiredArray2(value, name) {
      if (Array.isArray(value)) return value;
      throw new Error(`${name} is required`);
    }
    function requiredString22(value, name) {
      const next = stringValue132(value);
      if (!next) throw new Error(`${name} is required`);
      return next;
    }
    function requiredId22(value, name) {
      if (value === void 0 || value === null || String(value).trim() === "") throw new Error(`${name} is required`);
      return idValue32(value);
    }
    function optionalId2(value) {
      if (value === void 0 || value === null || String(value).trim() === "") return void 0;
      return idValue32(value);
    }
    function entityResultId2(result, fallback, name) {
      const resultRecord = isRecord342(result) && isRecord342(result.record) ? result.record : {};
      return requiredId22(resultRecord.id ?? resultRecord.ID ?? fallback.id ?? fallback.client_id, `${name}.id`);
    }
    function idValue32(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      return String(value);
    }
    function requiredResourceId52(value) {
      if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
      throw new Error("resource_id must be a positive integer RawResource ID");
    }
    function numberValue42(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
      return void 0;
    }
    function msValue3(value) {
      const number = numberValue42(value);
      return number === void 0 ? void 0 : Math.max(1, number);
    }
    function secToMs3(value) {
      return value === void 0 ? void 0 : Math.max(1, Math.round(value * 1e3));
    }
    function booleanValue4(value) {
      if (typeof value === "boolean") return value;
      if (typeof value !== "string") return void 0;
      if (value === "true") return true;
      if (value === "false") return false;
      return void 0;
    }
    async function readSceneMomentEditPlan2(args, sceneMomentId) {
      return editingServiceTimelineView2(args, "sceneMomentEditPlan", { sceneMomentId });
    }
    async function readProductionPreviewTimeline2(args, productionId) {
      const previewTimeline = await editingServiceTimelineView2(args, "previewTimeline", { productionId });
      return isRecord342(previewTimeline) ? previewTimeline : void 0;
    }
    async function sceneMomentTimelineBundle2(args, sceneMomentId) {
      const result = await editingServiceTimelineView2(args, "sceneMomentTimelineBundle", {
        sceneMomentId,
        id: stringValue132(args.editingProjectId ?? args.editing_project_id),
        projectId: stringValue132(args.timelineProjectId ?? args.timeline_project_id),
        title: stringValue132(args.projectName ?? args.project_name ?? args.sceneName ?? args.scene_name),
        defaultDurationMs: msValue3(args.defaultDurationMs ?? args.default_duration_ms) ?? secToMs3(numberValue42(args.defaultDurationSec ?? args.default_duration_sec))
      });
      if (!isRecord342(result)) throw new Error("editing service did not return scene moment timeline bundle");
      const mediaEditingProject = isRecord342(result.media_editing_project) ? result.media_editing_project : isRecord342(result.mediaEditingProject) ? result.mediaEditingProject : void 0;
      if (!mediaEditingProject) throw new Error("editing service scene moment timeline bundle did not include media_editing_project");
      const editPlan = isRecord342(result.edit_plan) ? result.edit_plan : isRecord342(result.editPlan) ? result.editPlan : void 0;
      if (!editPlan) throw new Error("editing service scene moment timeline bundle did not include edit_plan");
      const context = isRecord342(result.context) ? result.context : {};
      const composeInputs = Array.isArray(result.compose_inputs) ? result.compose_inputs.filter(isRecord342) : Array.isArray(result.composeInputs) ? result.composeInputs.filter(isRecord342) : [];
      const blockers = Array.isArray(result.blockers) ? result.blockers.filter(isRecord342) : [];
      const status = stringValue132(result.status) ?? (blockers.length === 0 ? "ok" : "blocked");
      return { status, mediaEditingProject, editPlan, context, composeInputs, blockers };
    }
    async function productionTimelineBundle2(args, productionId) {
      const result = await editingServiceTimelineView2(args, "productionTimelineBundle", {
        productionId,
        title: stringValue132(args.projectName ?? args.project_name),
        now: stringValue132(args.now),
        defaultDurationMs: msValue3(args.defaultDurationMs ?? args.default_duration_ms) ?? secToMs3(numberValue42(args.defaultDurationSec ?? args.default_duration_sec)) ?? 4e3
      });
      if (!isRecord342(result)) throw new Error("editing service did not return production timeline bundle");
      const mediaEditingProject = isRecord342(result.media_editing_project) ? result.media_editing_project : isRecord342(result.mediaEditingProject) ? result.mediaEditingProject : void 0;
      if (!mediaEditingProject) throw new Error("editing service production timeline bundle did not include media_editing_project");
      const editPlan = isRecord342(result.edit_plan) ? result.edit_plan : isRecord342(result.editPlan) ? result.editPlan : void 0;
      if (!editPlan) throw new Error("editing service production timeline bundle did not include edit_plan");
      const previewTimeline = isRecord342(result.preview_timeline) ? result.preview_timeline : isRecord342(result.previewTimeline) ? result.previewTimeline : void 0;
      const context = isRecord342(result.context) ? result.context : {};
      const composeInputs = Array.isArray(result.compose_inputs) ? result.compose_inputs.filter(isRecord342) : Array.isArray(result.composeInputs) ? result.composeInputs.filter(isRecord342) : [];
      const clips = Array.isArray(result.clips) ? result.clips : [];
      const blockers = Array.isArray(result.blockers) ? result.blockers.filter(isRecord342) : [];
      return { previewTimeline, mediaEditingProject, editPlan, context, composeInputs, clips, blockers };
    }
    function contentCandidateRecordsByContentUnitId32(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (document.path.endsWith("/content_candidate.json") && isRecord342(document.data)) {
          const contentUnitId2 = contentUnitIdForRuntimeDocument42(document.path, stringValue132(document.data.content_unit_ref));
          if (!contentUnitId2) continue;
          appendContentCandidateRecord22(output3, contentUnitId2, document.data);
          continue;
        }
        if (!isContentUnitDecisionContextRecord2(document.data)) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument42(document.path, stringValue132(document.data.target_ref));
        if (!contentUnitId) continue;
        for (const candidate of Array.isArray(document.data.candidates) ? document.data.candidates : []) {
          const record = normalizeDecisionContentCandidateRecord22(candidate, contentUnitId);
          if (record) appendContentCandidateRecord22(output3, contentUnitId, record);
        }
      }
      return output3;
    }
    function appendContentCandidateRecord22(output3, contentUnitId, candidate) {
      const candidates = output3.get(contentUnitId) ?? [];
      const candidateId = idFromUnknown2(candidate.id);
      if (candidateId === void 0) {
        output3.set(contentUnitId, [...candidates, candidate]);
        return;
      }
      const existingIndex = candidates.findIndex((existing) => sameId62(existing.id, candidateId));
      if (existingIndex < 0) {
        output3.set(contentUnitId, [...candidates, candidate]);
        return;
      }
      output3.set(contentUnitId, [
        ...candidates.slice(0, existingIndex),
        mergeContentCandidateRecords22(candidates[existingIndex], candidate),
        ...candidates.slice(existingIndex + 1)
      ]);
    }
    function normalizeDecisionContentCandidateRecord22(candidate, contentUnitId) {
      if (!isRecord342(candidate) || idFromUnknown2(candidate.id) === void 0) return void 0;
      return {
        ...candidate,
        content_unit_ref: stringValue132(candidate.content_unit_ref) ?? `content_units/${contentUnitId}`
      };
    }
    function mergeContentCandidateRecords22(existing, incoming) {
      const merged = { ...existing, ...incoming };
      if (!Array.isArray(incoming.outputs) && Array.isArray(existing.outputs)) merged.outputs = existing.outputs;
      if (!isRecord342(incoming.producer) && isRecord342(existing.producer)) merged.producer = existing.producer;
      if (!isRecord342(incoming.prompt_snapshot) && isRecord342(existing.prompt_snapshot)) merged.prompt_snapshot = existing.prompt_snapshot;
      if (!stringValue132(incoming.status) && stringValue132(existing.status)) merged.status = existing.status;
      if (!stringValue132(incoming.source) && stringValue132(existing.source)) merged.source = existing.source;
      if (!stringValue132(incoming.created_at) && stringValue132(existing.created_at)) merged.created_at = existing.created_at;
      if (!stringValue132(incoming.content_unit_ref) && stringValue132(existing.content_unit_ref)) merged.content_unit_ref = existing.content_unit_ref;
      return merged;
    }
    function isContentUnitDecisionContextRecord2(value) {
      return isRecord342(value) && value.schema === "movscript.decision_context.v1" && value.target_kind === "content_unit";
    }
    async function withContentUnitCandidateVisibility2(args, contentUnitId, result, extra = {}) {
      const status = await readContentUnitCandidateVisibility2(args, contentUnitId).catch((error) => ({
        visibility_error: errorMessage22(error),
        content_unit_candidates: [],
        selected_candidate: void 0,
        selected_raw_resource: void 0,
        stale_status: "unknown",
        frontend: { visible_in_panel: false, refresh_required: true }
      }));
      return {
        status: "ok",
        contentUnitId,
        content_unit_id: contentUnitId,
        result,
        ...status,
        ...extra
      };
    }
    async function readContentUnitCandidateVisibility2(args, contentUnitId) {
      const snapshot = await service2(args).loadContentWorkspaceSnapshot();
      const candidates = contentCandidateRecordsByContentUnitId32(snapshot.indexDocuments).get(String(contentUnitId)) ?? [];
      const selection = selectionRecordsByContentUnitId32(snapshot.indexDocuments).get(String(contentUnitId));
      const selectedCandidate = selection?.candidate_id !== void 0 ? candidates.find((candidate) => sameId62(candidate.id, selection.candidate_id)) : void 0;
      const selectedResourceId22 = numberValue42(selection?.resource_id) ?? numberValue42(firstCandidateOutput42(selectedCandidate)?.resource_id);
      return {
        content_unit_candidates: candidates,
        candidate_count: candidates.length,
        selected_candidate: selectedCandidate,
        selected_raw_resource: selectedResourceId22 === void 0 ? void 0 : { resource_id: selectedResourceId22 },
        stale_status: stringValue132(selection?.stale_policy) === "accept_stale" ? "accepted_stale" : "ok",
        frontend: {
          visible_in_panel: candidates.length > 0,
          refresh_required: true
        }
      };
    }
    function selectionRecordsByContentUnitId32(documents) {
      const output3 = /* @__PURE__ */ new Map();
      for (const document of documents) {
        if (!isRecord342(document.data)) continue;
        const selection = optionalRecord2(document.data.selection);
        if (!selection) continue;
        const contentUnitId = contentUnitIdForRuntimeDocument42(document.path, stringValue132(document.data.target_ref));
        if (!contentUnitId) continue;
        output3.set(contentUnitId, selection);
      }
      return output3;
    }
    function contentUnitIdForRuntimeDocument42(path, ref) {
      if (ref) return lastPathSegment82(ref) ?? ref;
      return pathSegmentAfter82(path, "content_units");
    }
    function entityStatusLine2(entity) {
      return {
        id: idValue32(entity.id ?? entity.record.id ?? entity.record.ID ?? entity.path),
        title: stringValue132(entity.record.title),
        path: entity.path
      };
    }
    function contentCandidateOutputKind2(value) {
      const kind = stringValue132(value);
      if (kind === "image" || kind === "video" || kind === "audio" || kind === "text" || kind === "metadata") return kind;
      return "image";
    }
    function firstCandidateOutput42(candidate) {
      const outputs = Array.isArray(candidate?.outputs) ? candidate.outputs : [];
      return outputs.find(isRecord342);
    }
    function firstOutputResourceId2(outputs) {
      const items = Array.isArray(outputs) ? outputs : [];
      for (const item of items) {
        if (!isRecord342(item)) continue;
        const resourceId = numberValue42(item.resource_id ?? item.resourceId);
        if (resourceId !== void 0 && Number.isInteger(resourceId) && resourceId > 0) return resourceId;
      }
      return void 0;
    }
    function pathSegmentAfter82(path, segment) {
      if (!path) return void 0;
      const parts = path.split("/").filter(Boolean);
      const index = parts.indexOf(segment);
      return index >= 0 ? parts[index + 1] : void 0;
    }
    function lastPathSegment82(path) {
      return path?.split("/").filter(Boolean).at(-1);
    }
    function sameId62(left, right) {
      return left !== void 0 && right !== void 0 && String(left) === String(right);
    }
    function errorMessage22(error) {
      return error instanceof Error ? error.message : String(error);
    }
    function isString32(value) {
      return typeof value === "string";
    }
    function makeResult2(id, result) {
      return { jsonrpc: "2.0", id, result };
    }
    function makeError2(id, code, message, data) {
      return {
        jsonrpc: "2.0",
        id,
        error: {
          code,
          message,
          ...data === void 0 ? {} : { data }
        }
      };
    }
    function listFocusResources2() {
      return [
        {
          uri: "movscript://ui/current-route",
          name: "Current route",
          description: "Current MovScript route in the Electron renderer.",
          mimeType: "text/markdown"
        },
        {
          uri: "movscript://ui/current-selection",
          name: "Current selection",
          description: "Current selected entity, when a page has reported one.",
          mimeType: "text/markdown"
        },
        {
          uri: "movscript://project/current",
          name: "Current project",
          description: "Current MovScript project summary.",
          mimeType: "text/markdown"
        }
      ];
    }
    var focusResourceReaders2 = [
      readFocusResource2
    ];
    function readFocusResource2(uri) {
      const snapshot = getMCPContextSnapshot2();
      if (uri === "movscript://ui/current-route") return resourceContent2(uri, snapshot.route);
      if (uri === "movscript://ui/current-selection") return resourceContent2(uri, snapshot.selection);
      if (uri === "movscript://project/current") return resourceContent2(uri, snapshot.project);
      return null;
    }
    function summarizeProject2(item) {
      if (!item || typeof item !== "object") return item;
      const id = typeof item.id === "number" ? item.id : typeof item.ID === "number" ? item.ID : void 0;
      return {
        ...id !== void 0 ? { id } : {},
        ...typeof item.name === "string" ? { name: truncateLongText2(item.name) } : {},
        ...typeof item.description === "string" ? { description: truncateLongText2(item.description) } : {},
        ...typeof item.total_episodes === "number" ? { totalEpisodes: item.total_episodes } : typeof item.totalEpisodes === "number" ? { totalEpisodes: item.totalEpisodes } : {},
        ...typeof item.CreatedAt === "string" ? { CreatedAt: item.CreatedAt } : {},
        ...typeof item.UpdatedAt === "string" ? { UpdatedAt: item.UpdatedAt } : {}
      };
    }
    function summarizeResource2(data) {
      if (!Array.isArray(data)) return data;
      return data.map(summarizeEntity2);
    }
    function summarizeEntity2(item) {
      if (!item || typeof item !== "object") return item;
      const summary = {};
      for (const key of [
        "ID",
        "id",
        "project_id",
        "entityKind",
        "path",
        "category",
        "kind",
        "domainCategory",
        "domainKind",
        "resourceKind",
        "legacyAlias",
        "preferredResourceKind",
        "timelineTemplate",
        "timelineNamespaces",
        "settingNamespaces",
        "namespaces",
        "order",
        "title",
        "name",
        "type",
        "script_type",
        "number",
        "status",
        "review_status",
        "description",
        "summary",
        "synopsis",
        "location",
        "time_of_day",
        "prompt",
        "final_prompt",
        "is_approved",
        "CreatedAt",
        "UpdatedAt"
      ]) {
        if (item[key] !== void 0) summary[key] = truncateLongText2(item[key]);
      }
      return summary;
    }
    function truncateLongText2(value) {
      if (typeof value !== "string") return value;
      return value.length > 1200 ? `${value.slice(0, 1200)}...` : value;
    }
    async function listProjects2(args) {
      const limit = getOptionalNumeric2(args, "limit") ?? 100;
      const projects = await backendList2("/projects");
      return {
        count: projects.length,
        projects: projects.slice(0, limit).map(summarizeProject2)
      };
    }
    async function createProject3(args) {
      const name = getOptionalString3(args, "name") ?? "";
      if (!name) throw new Error("name is required");
      const payload = { name };
      const description = getOptionalString3(args, "description") ?? "";
      const totalEpisodes = getOptionalNumeric2(args, "total_episodes");
      if (description) payload.description = description;
      if (totalEpisodes !== void 0) payload.total_episodes = totalEpisodes;
      const project = await backendPost222("/projects", payload);
      const summary = summarizeProject2(project);
      return {
        status: "created",
        project: summary,
        message: isRecord342(summary) && typeof summary.id === "number" ? `\u9879\u76EE\u300C${name}\u300D\u5DF2\u521B\u5EFA\uFF08project#${summary.id}\uFF09\u3002` : `\u9879\u76EE\u300C${name}\u300D\u5DF2\u521B\u5EFA\u3002`
      };
    }
    async function initLocalProject2(args) {
      const result = await runProjectLifecycleCommand2("createProject", args);
      const backendBinding = await requireBackendBindingForLifecycleResult2(result);
      return {
        ...result,
        backendProject: backendBinding.backendProject,
        projectDataSpace: backendBinding.projectDataSpace,
        message: `MovScript \u9879\u76EE\u5DF2\u521D\u59CB\u5316\uFF1A${stringField282(result.projectDir)}`
      };
    }
    async function fetchLocalProject2(args) {
      const result = await runProjectLifecycleCommand2("openProject", args);
      const backendBinding = stringField282(result.projectUid) ? await requireBackendBindingForLifecycleResult2(result) : void 0;
      return {
        ...result,
        ...backendBinding ? {
          backendProject: backendBinding.backendProject,
          projectDataSpace: backendBinding.projectDataSpace
        } : {},
        message: stringField282(result.projectUid) ? `MovScript \u9879\u76EE\u5DF2\u6253\u5F00\uFF1A${stringField282(result.projectDir)}` : `\u76EE\u5F55\u5DF2\u6253\u5F00\u4F46\u6CA1\u6709\u627E\u5230 MovScript \u9879\u76EE\u5143\u6570\u636E\uFF1A${stringField282(result.projectDir)}`
      };
    }
    async function runProjectLifecycleCommand2(command, args) {
      const projectDir = normalizeProjectDir2(args);
      const response = await createProjectServiceClientFromRuntime2().lifecycleCommand({
        projectDir,
        command,
        input: lifecycleInputFromArgs2(args)
      });
      if (!isRecord342(response.result)) {
        throw new Error(`Project Service lifecycle command ${command} returned an invalid result`);
      }
      return response.result;
    }
    function lifecycleInputFromArgs2(args) {
      return {
        ...getOptionalString3(args, "title") ? { title: getOptionalString3(args, "title") } : {},
        ...getOptionalString3(args, "projectId") ? { projectId: getOptionalString3(args, "projectId") } : {},
        ...getOptionalString3(args, "project_id") ? { project_id: getOptionalString3(args, "project_id") } : {},
        ...getOptionalString3(args, "projectUid") ? { projectUid: getOptionalString3(args, "projectUid") } : {},
        ...getOptionalString3(args, "project_uid") ? { project_uid: getOptionalString3(args, "project_uid") } : {},
        ...getOptionalString3(args, "language") ? { language: getOptionalString3(args, "language") } : {},
        ...isRecord342(args.standards) ? { standards: args.standards } : {},
        ...typeof args.overwrite === "boolean" ? { overwrite: args.overwrite } : {}
      };
    }
    function normalizeProjectDir2(args) {
      const projectDir = getOptionalString3(args, "projectDir") ?? getOptionalString3(args, "project_dir") ?? getOptionalString3(args, "cwd");
      if (!projectDir) throw new Error("projectDir is required");
      return (0, import_path52.resolve)(projectDir);
    }
    async function requireBackendBindingForLifecycleResult2(result) {
      const projectDir = stringField282(result.projectDir);
      const projectUid = stringField282(result.projectUid);
      if (!projectDir || !projectUid) {
        throw new Error("Project Service lifecycle result must include projectDir and projectUid for MCP backend binding");
      }
      return requireMCPBackendBoundProject2({ projectDir, projectUid });
    }
    function stringField282(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function listProjectResources2() {
      const snapshot = getMCPContextSnapshot2();
      const resources = [
        {
          uri: "movscript://projects",
          name: "Projects",
          description: "All visible MovScript projects.",
          mimeType: "text/markdown"
        }
      ];
      if (snapshot.project) {
        const id = snapshot.project.id;
        resources.push(
          resource2(`movscript://project/${id}/summary`, "Project summary"),
          resource2(`movscript://project/${id}/scripts`, "Scripts"),
          resource2(`movscript://project/${id}/settings`, "Settings", "Legacy setting source records with namespace projection fields."),
          resource2(`movscript://project/${id}/setting-states`, "Setting states", "Legacy setting-state alias; prefer setting-namespaces for namespace-aware work."),
          resource2(`movscript://project/${id}/assets`, "Assets", "System primitive asset records."),
          resource2(`movscript://project/${id}/project-context`, "Project context", "Project standards, namespace vocabulary, hash, and agent guidance from Project Service."),
          resource2(`movscript://project/${id}/namespace-vocabulary`, "Namespace vocabulary", "Project namespace vocabulary, templates, aliases, and diagnostics."),
          resource2(`movscript://project/${id}/timeline-namespaces`, "Timeline namespaces", "Canonical timeline namespace nodes using project vocabulary."),
          resource2(`movscript://project/${id}/setting-namespaces`, "Setting namespaces", "Canonical setting namespace nodes using project vocabulary."),
          resource2(`movscript://project/${id}/system-primitives`, "System primitives", "Scene moments, expression units, storyboards, keyframes, audio cues, assets, and assemblies."),
          resource2(`movscript://project/${id}/domain-nodes`, "Domain nodes", "All normalized MovScript domain nodes."),
          resource2(`movscript://project/${id}/domain-edges`, "Domain edges", "Normalized parent, scope, target, uses, and selection edges."),
          resource2(`movscript://project/${id}/episodes`, "Episodes", "Legacy production alias; prefer timeline-namespaces for new namespace-aware work."),
          resource2(`movscript://project/${id}/scenes`, "Scenes", "Legacy segment alias; prefer timeline-namespaces and system-primitives for new work."),
          resource2(`movscript://project/${id}/storyboards`, "Storyboards", "System primitive storyboard records."),
          resource2(`movscript://project/${id}/content-units`, "Content units", "Production tasks, candidates, and generation target refs.")
        );
      }
      return resources;
    }
    var projectResourceReaders2 = [
      readProjectResource2
    ];
    async function readProjectResource2(uri) {
      if (uri === "movscript://projects") return resourceContent2(uri, await listProjects2({}));
      const projectResource = parseProjectResourceURI2(uri);
      if (!projectResource) return null;
      const data = await readWorkspaceProjectResource2(projectResource.projectId, projectResource.kind);
      return resourceContent2(uri, summarizeResource2(data));
    }
    function resource2(uri, name, description) {
      return { uri, name, ...description ? { description } : {}, mimeType: "text/markdown" };
    }
    async function readWorkspaceProjectResource2(projectId, kind) {
      const snapshot = getMCPContextSnapshot2();
      const projectDir = snapshot.project && snapshot.project.id === projectId ? snapshot.project.projectDir ?? snapshot.project.projectPath ?? snapshot.project.workspacePath ?? snapshot.project.project_path ?? snapshot.project.workspace_path : void 0;
      const locator = resolveMCPProjectWorkspaceLocator2({ projectDir });
      const response = await createProjectServiceClientFromRuntime2().resourceView({
        projectDir: locator.projectDir,
        kind: projectResourceViewKind2(kind)
      });
      return response.items;
    }
    function projectResourceViewKind2(kind) {
      switch (kind) {
        case "summary":
          return "summary";
        case "assets":
        case "assests":
          return "assets";
        case "namespace-vocabulary":
          return "namespace-vocabulary";
        case "project-context":
          return "project-context";
        case "timeline-namespaces":
          return "timeline-namespaces";
        case "setting-namespaces":
          return "setting-namespaces";
        case "system-primitives":
          return "system-primitives";
        case "domain-nodes":
          return "domain-nodes";
        case "domain-edges":
          return "domain-edges";
        case "episodes":
        case "productions":
          return "episodes";
        case "scenes":
        case "segments":
          return "scenes";
        case "storyboards":
          return "storyboards";
        case "content-units":
          return "content-units";
        case "settings":
          return "settings";
        case "setting-states":
        case "states":
          return "setting-states";
        case "scripts":
          return "scripts";
        default:
          throw new Error(`Unsupported project resource kind: ${kind}`);
      }
    }
    function parseProjectResourceURI2(uri) {
      const match = uri.match(/^movscript:\/\/project\/(\d+)\/([a-z-]+)$/);
      if (!match) return null;
      return {
        projectId: Number(match[1]),
        kind: match[2] ?? ""
      };
    }
    function ffmpegBinaryName2(platform = process.platform) {
      return platform === "win32" ? "ffmpeg.exe" : "ffmpeg";
    }
    function resolveFFmpegPath2(options = {}) {
      const envPath = process.env.FFMPEG_PATH?.trim() || process.env.MOVSCRIPT_FFMPEG_PATH?.trim();
      if (envPath && (0, import_fs32.existsSync)(envPath)) return envPath;
      const platform = options.platform ?? process.platform;
      const arch = options.arch ?? process.arch;
      const resourcesPath = options.resourcesPath ?? process.resourcesPath;
      const cwd = options.cwd ?? process.cwd();
      const binary = ffmpegBinaryName2(platform);
      const candidates = [
        (0, import_path72.join)(resourcesPath || "", "ffmpeg", platform, arch, binary),
        (0, import_path72.join)(resourcesPath || "", "ffmpeg", platform, binary),
        (0, import_path72.join)(resourcesPath || "", "ffmpeg", binary),
        (0, import_path72.join)(resourcesPath || "", "bin", binary),
        (0, import_path72.resolve)(cwd, "vendor/ffmpeg", platform, arch, binary),
        (0, import_path72.resolve)(cwd, "vendor/ffmpeg", platform, binary),
        (0, import_path72.resolve)(cwd, "../../apps/desktop/vendor/ffmpeg", platform, arch, binary),
        (0, import_path72.resolve)(cwd, "../../apps/desktop/vendor/ffmpeg", platform, binary),
        binary
      ];
      return candidates.find((candidate) => candidate === binary || (0, import_fs32.existsSync)(candidate));
    }
    var DEFAULT_SCENE_THRESHOLD2 = 0.28;
    var DEFAULT_MIN_SHOT_DURATION_SEC2 = 1.2;
    var DEFAULT_MAX_SHOT_DURATION_SEC2 = 12;
    var DEFAULT_TARGET_SHOT_DURATION_SEC2 = 6;
    var DEFAULT_MAX_VIDEO_BYTES3 = 200 * 1024 * 1024;
    var ABSOLUTE_MAX_VIDEO_BYTES3 = 1024 * 1024 * 1024;
    async function queryShotLibrary2(args) {
      const id = getOptionalNumeric2(args, "shot_reference_id") ?? getOptionalNumeric2(args, "shotReferenceId") ?? getOptionalNumeric2(args, "id");
      const includeFull = booleanParam5(args.include_full) ?? booleanParam5(args.includeFull) ?? false;
      const query = getOptionalString3(args, "query") ?? getOptionalString3(args, "q") ?? "";
      const groupId = positiveIntegerParam2(args, "group_id") ?? positiveIntegerParam2(args, "groupId");
      const page = Math.floor(clampNumber3(getOptionalNumeric2(args, "page") ?? 1, 1, 1e4));
      const pageSize = Math.floor(clampNumber3(
        getOptionalNumeric2(args, "page_size") ?? getOptionalNumeric2(args, "pageSize") ?? getOptionalNumeric2(args, "limit") ?? getOptionalNumeric2(args, "topK") ?? 20,
        1,
        100
      ));
      const pageData = await fetchShotLibraryPage2({ query, page, pageSize, groupId });
      const items = Array.isArray(pageData.items) ? pageData.items : [];
      const summarized = items.map((item) => summarizeShotReference2(item, { includeFull }));
      const filtered = id === void 0 ? summarized : summarized.filter((item) => shotReferenceId2(item) === id);
      return {
        query,
        page: numberField11(pageData, "page") ?? page,
        pageSize: numberField11(pageData, "page_size") ?? pageSize,
        total: numberField11(pageData, "total") ?? filtered.length,
        count: filtered.length,
        items: filtered,
        ...id !== void 0 && filtered.length === 0 ? { warning: `shot_reference_id ${id} was not found on the requested page` } : {}
      };
    }
    async function createShotGroup2(args) {
      const resourceId = requiredPositiveInteger2(args, ["resource_id", "resourceId"], "resource_id");
      const body = {
        resource_id: resourceId
      };
      const title = getOptionalString3(args, "title");
      const summary = getOptionalString3(args, "summary");
      const cutStrategy = getOptionalString3(args, "cut_strategy") ?? getOptionalString3(args, "cutStrategy");
      if (title) body.title = title;
      if (summary) body.summary = summary;
      if (cutStrategy) body.cut_strategy = cutStrategy;
      const group = await backendPost222("/shot-reference-groups", body);
      const groupId = idField16(group);
      if (groupId === void 0) throw new Error("shot group create response did not include a valid group ID");
      return {
        status: "created",
        group_id: groupId,
        group,
        message: `Shot group #${groupId} created for resource #${resourceId}.`
      };
    }
    async function getShotGroup2(args) {
      const groupId = requiredPositiveInteger2(args, ["group_id", "groupId", "id"], "group_id");
      const detail = await backendGet222(`/shot-reference-groups/${encodeURIComponent(String(groupId))}`);
      const group = isRecord342(detail) && isRecord342(detail.group) ? detail.group : {};
      const shots = isRecord342(detail) && Array.isArray(detail.shots) ? detail.shots : [];
      return {
        status: "loaded",
        group_id: groupId,
        group,
        count: shots.length,
        shots: shots.map((shot) => summarizeShotReference2(shot, { includeFull: true }))
      };
    }
    async function addShotsToGroup2(args) {
      const groupId = requiredPositiveInteger2(args, ["group_id", "groupId"], "group_id");
      const rawShots = Array.isArray(args.shots) ? args.shots : [];
      if (rawShots.length === 0) throw new Error("shots must contain at least one shot range or metadata object");
      const groupDetail = await backendGet222(`/shot-reference-groups/${encodeURIComponent(String(groupId))}`);
      const group = isRecord342(groupDetail) && isRecord342(groupDetail.group) ? groupDetail.group : {};
      const groupResourceId = positiveIntegerValue2(group.source_resource_id) ?? positiveIntegerValue2(group.sourceResourceId);
      const resourceId = positiveIntegerParam2(args, "resource_id") ?? positiveIntegerParam2(args, "resourceId") ?? groupResourceId;
      if (resourceId === void 0) throw new Error("resource_id is required when the shot group response has no source_resource_id");
      const body = {
        resource_id: resourceId,
        group_id: groupId,
        shots: rawShots.map(normalizeShotInput2)
      };
      const durationSec = getOptionalNumeric2(args, "duration_sec") ?? getOptionalNumeric2(args, "durationSec");
      const width = getOptionalNumeric2(args, "width");
      const height = getOptionalNumeric2(args, "height");
      if (durationSec !== void 0) body.duration_sec = durationSec;
      if (width !== void 0) body.width = Math.floor(width);
      if (height !== void 0) body.height = Math.floor(height);
      const result = await backendPost222("/shot-references/from-resource", body);
      const shots = isRecord342(result) && Array.isArray(result.items) ? result.items : [];
      return {
        status: "created",
        group_id: groupId,
        count: shots.length,
        shots: shots.map((shot) => summarizeShotReference2(shot, { includeFull: true })),
        message: `${shots.length} shot reference(s) added to group #${groupId}.`
      };
    }
    async function analyzeVideoShotCuts2(args) {
      const resourceId = requiredPositiveInteger2(args, ["resource_id", "resourceId", "id"], "resource_id");
      const maxVideoBytes = Math.floor(clampNumber3(getOptionalNumeric2(args, "max_video_bytes") ?? getOptionalNumeric2(args, "maxVideoBytes") ?? DEFAULT_MAX_VIDEO_BYTES3, 1, ABSOLUTE_MAX_VIDEO_BYTES3));
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_video_shot_cuts_analyze but was not found");
      const source = await backendGetBinary2(`/resources/${encodeURIComponent(String(resourceId))}/file`, { maxBytes: maxVideoBytes });
      const dir = (0, import_path62.join)((0, import_os4.tmpdir)(), `movscript-shot-cuts-${Date.now()}-${Math.random().toString(16).slice(2)}`);
      const inputPath = (0, import_path62.join)(dir, `resource-${resourceId}.video`);
      await (0, import_promises62.mkdir)(dir, { recursive: true });
      try {
        await (0, import_promises62.writeFile)(inputPath, source.bytes);
        const probed = await probeVideo2(inputPath, ffmpeg).catch(() => ({}));
        const durationSec = positiveNumber42(getOptionalNumeric2(args, "duration_sec") ?? getOptionalNumeric2(args, "durationSec")) ?? probed.durationSec;
        if (!durationSec) throw new Error("duration_sec is required when ffprobe cannot read video duration");
        const sceneThreshold = positiveNumber42(getOptionalNumeric2(args, "scene_threshold") ?? getOptionalNumeric2(args, "sceneThreshold")) ?? DEFAULT_SCENE_THRESHOLD2;
        const sceneOutput = await runFFmpeg3(ffmpeg, [
          "-hide_banner",
          "-i",
          inputPath,
          "-filter:v",
          `select='gt(scene,${sceneThreshold})',showinfo`,
          "-an",
          "-f",
          "null",
          "-"
        ]).catch((error) => {
          throw error instanceof Error ? error : new Error(String(error));
        });
        const sceneTimes = parseSceneDetectTimes2(sceneOutput);
        const shots = buildSceneShotSegments2(durationSec, sceneTimes, {
          minShotDurationSec: getOptionalNumeric2(args, "min_shot_duration_sec") ?? getOptionalNumeric2(args, "minShotDurationSec"),
          maxShotDurationSec: getOptionalNumeric2(args, "max_shot_duration_sec") ?? getOptionalNumeric2(args, "maxShotDurationSec")
        });
        const finalShots = shots.length > 0 ? shots : buildEvenShotSegments2(durationSec);
        return {
          status: "analyzed",
          resource_id: resourceId,
          strategy: shots.length > 0 ? "scene_detection" : "even",
          duration_sec: roundTime3(durationSec),
          count: finalShots.length,
          shots: finalShots.map((shot, index) => ({
            index: index + 1,
            start_sec: shot.startSec,
            end_sec: shot.endSec,
            startSec: shot.startSec,
            endSec: shot.endSec
          })),
          ...sceneTimes.length === 0 ? { warning: "No scene boundaries were detected; returned even fallback segments." } : {}
        };
      } finally {
        await (0, import_promises62.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function readShotLibrary2(uri) {
      const parsed = parseShotLibraryURI2(uri);
      if (!parsed) throw new Error(`Unsupported shot library resource URI: ${uri}`);
      return queryShotLibrary2({
        ...parsed.id !== void 0 ? { shot_reference_id: parsed.id, limit: 100 } : {},
        ...parsed.query ? { query: parsed.query } : {},
        ...parsed.page !== void 0 ? { page: parsed.page } : {},
        ...parsed.pageSize !== void 0 ? { page_size: parsed.pageSize } : {},
        include_full: true
      });
    }
    function isShotLibraryURI2(uri) {
      return parseShotLibraryURI2(uri) !== null;
    }
    function parseShotLibraryURI2(uri) {
      const match = uri.match(/^movscript:\/\/shot-library(?:\/(\d+))?(?:\?(.*))?$/);
      if (!match) return null;
      const params = new URLSearchParams(match[2] ?? "");
      const page = numericParam5(params.get("page"));
      const pageSize = numericParam5(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit"));
      return {
        ...match[1] ? { id: Number(match[1]) } : {},
        ...params.get("query") || params.get("q") ? { query: params.get("query") ?? params.get("q") ?? "" } : {},
        ...page !== void 0 ? { page } : {},
        ...pageSize !== void 0 ? { pageSize } : {}
      };
    }
    async function fetchShotLibraryPage2(input3) {
      const params = new URLSearchParams();
      params.set("page", String(input3.page ?? 1));
      params.set("page_size", String(input3.pageSize ?? 20));
      if (input3.query?.trim()) params.set("q", input3.query.trim());
      if (input3.groupId !== void 0) params.set("group_id", String(input3.groupId));
      const data = await backendGet222(`/shot-references?${params.toString()}`);
      return isRecord342(data) ? data : { items: [] };
    }
    function summarizeShotReference2(item, options) {
      if (!isRecord342(item)) return item;
      if (options.includeFull) return item;
      const resource22 = isRecord342(item.resource) ? item.resource : void 0;
      const group = isRecord342(item.group) ? item.group : void 0;
      return {
        ...picked4(item, ["ID", "id", "title", "summary", "analysis_status", "analysis_source", "intent", "pattern", "shot_function", "visual_preference", "emotional_effect", "start_sec", "end_sec", "retrieval_text", "CreatedAt", "UpdatedAt"]),
        resource: resource22 ? picked4(resource22, ["ID", "id", "name", "mime_type", "type", "url", "size"]) : void 0,
        group: group ? picked4(group, ["ID", "id", "title", "summary", "analysis_status", "cut_strategy"]) : void 0,
        execution_details: isRecord342(item.execution_details) ? picked4(item.execution_details, ["duration_sec", "resolution", "aspect_ratio", "coverage_role", "difficulty", "blocking", "requirements"]) : void 0,
        search_index: isRecord342(item.search_index) ? picked4(item.search_index, ["natural_language_queries", "tags", "visual_facets", "narrative_facets", "emotion_facets", "pattern_facets", "production_facets"]) : void 0
      };
    }
    function picked4(record, keys) {
      const result = {};
      for (const key of keys) {
        if (record[key] !== void 0) result[key] = truncateText3(record[key]);
      }
      return result;
    }
    function truncateText3(value) {
      if (typeof value !== "string") return value;
      return value.length > 1200 ? `${value.slice(0, 1200)}...` : value;
    }
    function shotReferenceId2(item) {
      if (!isRecord342(item)) return void 0;
      return typeof item.ID === "number" ? item.ID : typeof item.id === "number" ? item.id : void 0;
    }
    function numberField11(record, key) {
      return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
    }
    function numericParam5(value) {
      if (!value?.trim()) return void 0;
      const parsed = Number(value);
      return Number.isFinite(parsed) ? parsed : void 0;
    }
    function booleanParam5(value) {
      if (typeof value === "boolean") return value;
      if (typeof value !== "string") return void 0;
      if (value === "true") return true;
      if (value === "false") return false;
      return void 0;
    }
    function normalizeShotInput2(value) {
      if (!isRecord342(value)) throw new Error("each shot must be an object");
      const shot = {};
      copyString2(value, shot, "title");
      copyString2(value, shot, "summary");
      copyArray2(value, shot, "intent");
      copyArray2(value, shot, "pattern");
      copyArray2(value, shot, "shot_function", "shotFunction");
      copyArray2(value, shot, "visual_preference", "visualPreference");
      copyArray2(value, shot, "emotional_effect", "emotionalEffect");
      copyObject2(value, shot, "execution_details", "executionDetails");
      copyObject2(value, shot, "visual_analysis", "visualAnalysis");
      copyObject2(value, shot, "scene_semantics", "sceneSemantics");
      copyObject2(value, shot, "narrative_function", "narrativeFunction");
      copyObject2(value, shot, "emotional_profile", "emotionalProfile");
      copyObject2(value, shot, "reusable_pattern", "reusablePattern");
      const startSec = numberValue222(value.start_sec) ?? numberValue222(value.startSec);
      const endSec = numberValue222(value.end_sec) ?? numberValue222(value.endSec);
      if (startSec !== void 0) {
        shot.start_sec = startSec;
        shot.start_sec_set = true;
      }
      if (endSec !== void 0) {
        shot.end_sec = endSec;
        shot.end_sec_set = true;
      }
      return shot;
    }
    function copyString2(input3, out, key, alias) {
      const value = stringValue222(input3[key]) ?? (alias ? stringValue222(input3[alias]) : void 0);
      if (value !== void 0) out[key] = value;
    }
    function copyArray2(input3, out, key, alias) {
      const value = arrayValue42(input3[key]) ?? (alias ? arrayValue42(input3[alias]) : void 0);
      if (value !== void 0) out[key] = value;
    }
    function copyObject2(input3, out, key, alias) {
      const value = isRecord342(input3[key]) ? input3[key] : alias && isRecord342(input3[alias]) ? input3[alias] : void 0;
      if (value !== void 0) out[key] = value;
    }
    function requiredPositiveInteger2(args, keys, label) {
      for (const key of keys) {
        const value = positiveIntegerParam2(args, key);
        if (value !== void 0) return value;
      }
      throw new Error(`${label} must be a positive integer`);
    }
    function positiveIntegerParam2(args, key) {
      return positiveIntegerValue2(args[key]);
    }
    function positiveIntegerValue2(value) {
      const number = numberValue222(value);
      return number !== void 0 && Number.isInteger(number) && number > 0 ? number : void 0;
    }
    function idField16(value) {
      if (!isRecord342(value)) return void 0;
      return positiveIntegerValue2(value.ID) ?? positiveIntegerValue2(value.id);
    }
    function numberValue222(value) {
      const number = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : NaN;
      return Number.isFinite(number) ? number : void 0;
    }
    function positiveNumber42(value) {
      const number = numberValue222(value);
      return number !== void 0 && number > 0 ? number : void 0;
    }
    function stringValue222(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function arrayValue42(value) {
      return Array.isArray(value) ? value : void 0;
    }
    function parseSceneDetectTimes2(output3) {
      const times = /* @__PURE__ */ new Set();
      for (const match of output3.matchAll(/\bpts_time:([0-9]+(?:\.[0-9]+)?)/g)) {
        const value = Number(match[1]);
        if (Number.isFinite(value) && value > 0) times.add(roundTime3(value));
      }
      return Array.from(times).sort((a, b) => a - b);
    }
    function buildSceneShotSegments2(durationSec, sceneTimes, input3 = {}) {
      const minDuration = positiveNumber42(input3.minShotDurationSec) ?? DEFAULT_MIN_SHOT_DURATION_SEC2;
      const maxDuration = positiveNumber42(input3.maxShotDurationSec) ?? DEFAULT_MAX_SHOT_DURATION_SEC2;
      const boundaries = normalizeSceneBoundaries2(sceneTimes, durationSec, minDuration, maxDuration);
      return rangesFromBoundaries2(durationSec, boundaries);
    }
    function buildEvenShotSegments2(durationSec) {
      const segmentCount = Math.max(1, Math.ceil(durationSec / DEFAULT_TARGET_SHOT_DURATION_SEC2));
      const segmentLength = durationSec / segmentCount;
      return Array.from({ length: segmentCount }, (_item, index) => ({
        startSec: roundTime3(index * segmentLength),
        endSec: roundTime3(index === segmentCount - 1 ? durationSec : (index + 1) * segmentLength)
      }));
    }
    function normalizeSceneBoundaries2(sceneTimes, durationSec, minDurationSec, maxDurationSec) {
      const boundaries = [];
      let previous = 0;
      const candidates = sceneTimes.map(roundTime3).filter((time) => time > 0 && time < durationSec).sort((a, b) => a - b);
      for (const candidate of candidates) {
        addForcedBoundaries2(boundaries, previous, candidate, maxDurationSec);
        previous = boundaries[boundaries.length - 1] ?? previous;
        if (candidate - previous >= minDurationSec && durationSec - candidate >= minDurationSec) {
          boundaries.push(candidate);
          previous = candidate;
        }
      }
      addForcedBoundaries2(boundaries, previous, durationSec, maxDurationSec);
      return boundaries;
    }
    function addForcedBoundaries2(boundaries, startSec, endSec, maxDurationSec) {
      let cursor = startSec;
      while (maxDurationSec > 0 && endSec - cursor > maxDurationSec) {
        cursor = roundTime3(cursor + maxDurationSec);
        if (cursor < endSec) boundaries.push(cursor);
      }
    }
    function rangesFromBoundaries2(durationSec, boundaries) {
      const points = [0, ...boundaries, durationSec];
      const result = [];
      for (let index = 0; index < points.length - 1; index += 1) {
        const startSec = roundTime3(points[index] ?? 0);
        const endSec = roundTime3(points[index + 1] ?? 0);
        if (endSec > startSec) result.push({ startSec, endSec });
      }
      return result;
    }
    async function probeVideo2(inputPath, ffmpeg) {
      const ffprobe = ffmpeg.replace(/ffmpeg(?:\.exe)?$/i, process.platform === "win32" ? "ffprobe.exe" : "ffprobe");
      const output3 = await runCommand3(ffprobe, [
        "-v",
        "error",
        "-show_entries",
        "format=duration",
        "-of",
        "json",
        inputPath
      ]);
      const parsed = JSON.parse(output3);
      const durationSec = isRecord342(parsed) && isRecord342(parsed.format) ? positiveNumber42(parsed.format.duration) : void 0;
      return durationSec ? { durationSec } : {};
    }
    function runFFmpeg3(command, args) {
      return runCommand3(command, args, true);
    }
    function runCommand3(command, args, acceptNonZero = false) {
      return new Promise((resolve62, reject) => {
        const child = (0, import_child_process5.spawn)(command, args, { stdio: ["ignore", "pipe", "pipe"] });
        let stdout = "";
        let stderr = "";
        const appendStdout = (chunk) => {
          stdout += String(chunk);
        };
        const appendStderr = (chunk) => {
          stderr += String(chunk);
          if (stderr.length > 1024 * 1024) stderr = stderr.slice(stderr.length - 1024 * 1024);
        };
        child.stdout?.on("data", appendStdout);
        child.stderr?.on("data", appendStderr);
        child.on("error", (error) => reject(error instanceof Error ? error : new Error(String(error))));
        child.on("exit", (code) => {
          if (code === 0 || acceptNonZero) {
            resolve62(`${stdout}
${stderr}`);
            return;
          }
          reject(new Error(stderr.trim() || `command exited with code ${code ?? "unknown"}`));
        });
      });
    }
    function roundTime3(value) {
      return Math.round(value * 10) / 10;
    }
    function listShotLibraryResources2() {
      return [
        {
          uri: "movscript://shot-library",
          name: "Shot reference library",
          description: "Searchable MovScript shot reference library for reusable camera, composition, movement, narrative, emotion, and production patterns.",
          mimeType: "text/markdown"
        }
      ];
    }
    var shotLibraryResourceReaders2 = [
      readShotLibraryResource2
    ];
    async function readShotLibraryResource2(uri) {
      if (!isShotLibraryURI2(uri)) return null;
      return resourceContent2(uri, await readShotLibrary2(uri));
    }
    async function queryResourceLibrary2(args) {
      const id = getOptionalNumeric2(args, "resource_id") ?? getOptionalNumeric2(args, "resourceId") ?? getOptionalNumeric2(args, "id");
      const includeFull = booleanParam22(args.include_full) ?? booleanParam22(args.includeFull) ?? false;
      const query = getOptionalString3(args, "query") ?? getOptionalString3(args, "q") ?? "";
      const type = getOptionalString3(args, "type") ?? getOptionalString3(args, "media_type");
      const scope = getOptionalString3(args, "scope");
      const folderId = getOptionalString3(args, "folder_id") ?? getOptionalString3(args, "folderId");
      const page = Math.floor(clampNumber3(getOptionalNumeric2(args, "page") ?? 1, 1, 1e4));
      const pageSize = Math.floor(clampNumber3(
        getOptionalNumeric2(args, "page_size") ?? getOptionalNumeric2(args, "pageSize") ?? getOptionalNumeric2(args, "limit") ?? 20,
        1,
        100
      ));
      const pageData = await fetchResourceLibraryPage2({ query, type, scope, folderId, page, pageSize });
      const items = resourceItems2(pageData);
      const summarized = items.map((item) => summarizeRawResource2(item, { includeFull }));
      const filtered = id === void 0 ? summarized : summarized.filter((item) => rawResourceId2(item) === id);
      return {
        source: "movscript_resource_library",
        query,
        ...type ? { type } : {},
        ...scope ? { scope } : {},
        ...folderId ? { folder_id: folderId } : {},
        page: numberField232(pageData, "page") ?? page,
        pageSize: numberField232(pageData, "page_size") ?? pageSize,
        total: numberField232(pageData, "total") ?? filtered.length,
        count: filtered.length,
        items: filtered,
        usage: "Use RawResource.ID values as input_resource_ids or reference_resource_ids for MovScript generation tools.",
        ...id !== void 0 && filtered.length > 0 ? { surface: createResourceDetailSurface2(args, id) } : {},
        ...id !== void 0 && filtered.length === 0 ? { warning: `resource_id ${id} was not found on the requested page` } : {}
      };
    }
    function openResourceLibrary2(args) {
      const surface = createResourceLibrarySurface2(args);
      return {
        source: "movscript_resource_library",
        ...surface,
        backend_api_base_url: getMovScriptBackendAPIBaseURL22()
      };
    }
    async function readResourceLibrary2(uri) {
      const parsed = parseResourceLibraryURI2(uri);
      if (!parsed) throw new Error(`Unsupported MovScript resource library URI: ${uri}`);
      return queryResourceLibrary2({
        ...parsed,
        include_full: true
      });
    }
    function isResourceLibraryURI2(uri) {
      return parseResourceLibraryURI2(uri) !== null;
    }
    function parseResourceLibraryURI2(uri) {
      const match = uri.match(/^movscript:\/\/resource-library(?:\/(\d+))?(?:\?(.*))?$/);
      if (!match) return null;
      const params = new URLSearchParams(match[2] ?? "");
      return {
        ...match[1] ? { resource_id: Number(match[1]) } : {},
        ...params.get("query") || params.get("q") ? { query: params.get("query") ?? params.get("q") ?? "" } : {},
        ...params.get("type") || params.get("media_type") ? { type: params.get("type") ?? params.get("media_type") ?? "" } : {},
        ...params.get("scope") ? { scope: params.get("scope") ?? "" } : {},
        ...params.get("folder_id") || params.get("folderId") ? { folder_id: params.get("folder_id") ?? params.get("folderId") ?? "" } : {},
        ...numericParam22(params.get("page")) !== void 0 ? { page: numericParam22(params.get("page")) } : {},
        ...numericParam22(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) !== void 0 ? { page_size: numericParam22(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) } : {}
      };
    }
    async function fetchResourceLibraryPage2(input3) {
      const params = new URLSearchParams();
      params.set("page", String(input3.page ?? 1));
      params.set("page_size", String(input3.pageSize ?? 20));
      if (input3.query?.trim()) params.set("q", input3.query.trim());
      if (input3.type?.trim()) params.set("type", input3.type.trim());
      if (input3.scope?.trim()) params.set("scope", input3.scope.trim());
      if (input3.folderId?.trim()) params.set("folder_id", input3.folderId.trim());
      const data = await backendGet222(`/resources?${params.toString()}`);
      return isRecord342(data) || Array.isArray(data) ? data : { items: [] };
    }
    function summarizeRawResource2(item, options) {
      if (!isRecord342(item)) return item;
      if (options.includeFull) return item;
      return picked22(item, ["ID", "id", "name", "type", "mime_type", "size", "url", "direct_url", "folder_id", "owner_id", "org_id", "CreatedAt", "UpdatedAt"]);
    }
    function resourceItems2(pageData) {
      if (Array.isArray(pageData)) return pageData;
      return Array.isArray(pageData.items) ? pageData.items : [];
    }
    function picked22(record, keys) {
      const result = {};
      for (const key of keys) {
        if (record[key] !== void 0) result[key] = record[key];
      }
      return result;
    }
    function rawResourceId2(item) {
      if (!isRecord342(item)) return void 0;
      return typeof item.ID === "number" ? item.ID : typeof item.id === "number" ? item.id : void 0;
    }
    function numberField232(record, key) {
      if (!isRecord342(record)) return void 0;
      return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
    }
    function numericParam22(value) {
      if (!value?.trim()) return void 0;
      const parsed = Number(value);
      return Number.isFinite(parsed) ? parsed : void 0;
    }
    function booleanParam22(value) {
      if (typeof value === "boolean") return value;
      if (typeof value !== "string") return void 0;
      if (value === "true") return true;
      if (value === "false") return false;
      return void 0;
    }
    function listResourceLibraryResources2() {
      return [
        {
          uri: "movscript://resource-library",
          name: "MovScript resource library",
          description: "Internal MovScript RawResource library. Use the movscript_resource_library_query tool for search and generation-ready resource IDs.",
          mimeType: "text/markdown"
        },
        {
          uri: "movscript://resource-library/open",
          name: "Open MovScript resource library",
          description: "Agent in-app browser URL for the MovScript resource library. Read this resource or call system_resource_library_open to get the current URL.",
          mimeType: "text/markdown"
        }
      ];
    }
    var resourceLibraryResourceReaders2 = [
      readResourceLibraryResource2
    ];
    async function readResourceLibraryResource2(uri) {
      if (uri === "movscript://resource-library/open") return resourceContent2(uri, openResourceLibrary2({}));
      if (!isResourceLibraryURI2(uri)) return null;
      return resourceContent2(uri, await readResourceLibrary2(uri));
    }
    var DEFAULT_MAX_IMAGE_BYTES2 = 8 * 1024 * 1024;
    var ABSOLUTE_MAX_IMAGE_BYTES2 = 20 * 1024 * 1024;
    var DEFAULT_MAX_IMAGE_WIDTH2 = 1568;
    var DEFAULT_MAX_IMAGE_HEIGHT2 = 1568;
    var ABSOLUTE_MAX_IMAGE_DIMENSION2 = 4096;
    var DEFAULT_MAX_UPLOAD_BYTES2 = 20 * 1024 * 1024;
    var ABSOLUTE_MAX_UPLOAD_BYTES2 = 100 * 1024 * 1024;
    var DEFAULT_MAX_VIDEO_BYTES22 = 200 * 1024 * 1024;
    var ABSOLUTE_MAX_VIDEO_BYTES22 = 1024 * 1024 * 1024;
    var DEFAULT_VIDEO_FRAME_COUNT2 = 4;
    var DEFAULT_VIDEO_FRAME_INTERVAL_SEC2 = 3;
    var DEFAULT_MAX_WIDTH2 = 960;
    var DEFAULT_MAX_FRAMES2 = 12;
    var ABSOLUTE_MAX_VIDEO_FRAMES2 = 24;
    var DEFAULT_RANGE_FPS2 = 2;
    var MAX_FPS2 = 6;
    var DEFAULT_BURST_WINDOW_SEC2 = 2;
    var DEFAULT_ANNOTATION_WIDTH2 = 1024;
    var DEFAULT_ANNOTATION_HEIGHT2 = 768;
    async function readResourceImageForVision2(args) {
      const resourceId = resourceIdParam2(args);
      const maxBytes = clampInteger2(numberParam2(args.max_bytes) ?? numberParam2(args.maxBytes) ?? DEFAULT_MAX_IMAGE_BYTES2, 1, ABSOLUTE_MAX_IMAGE_BYTES2);
      const mode = resourceImageReadModeParam2(args);
      const maxWidth = clampInteger2(numberParam2(args.max_width) ?? numberParam2(args.maxWidth) ?? DEFAULT_MAX_IMAGE_WIDTH2, 1, ABSOLUTE_MAX_IMAGE_DIMENSION2);
      const maxHeight = clampInteger2(numberParam2(args.max_height) ?? numberParam2(args.maxHeight) ?? DEFAULT_MAX_IMAGE_HEIGHT2, 1, ABSOLUTE_MAX_IMAGE_DIMENSION2);
      const file = await downloadResourceFile2(resourceId, { maxBytes });
      const sourceMimeType = normalizeImageMimeType2(stringParam3(args.mime_type) ?? stringParam3(args.mimeType) ?? file.contentType);
      if (!sourceMimeType.startsWith("image/")) {
        throw new Error(`resource ${resourceId} is not an image resource; content-type=${file.contentType ?? "unknown"}`);
      }
      if (file.bytes.length > maxBytes) {
        throw new Error(`resource ${resourceId} image is ${file.bytes.length} bytes, above max_bytes=${maxBytes}`);
      }
      const processed = await processResourceImageForVision2(resourceId, file.bytes, sourceMimeType, {
        mode,
        maxWidth,
        maxHeight
      });
      const metadata = {
        status: "image_read",
        resource_id: resourceId,
        mode,
        source_mime_type: sourceMimeType,
        source_size_bytes: file.bytes.length,
        source_width: processed.source.width,
        source_height: processed.source.height,
        mime_type: processed.mimeType,
        size_bytes: processed.bytes.length,
        width: processed.width,
        height: processed.height,
        resized: processed.resized,
        ...mode === "fit" ? { max_width: maxWidth, max_height: maxHeight } : {},
        image_payload: "sent_as_mcp_image_content"
      };
      return mcpToolResultWithImages2(metadata, [{
        data: processed.bytes.toString("base64"),
        mimeType: processed.mimeType
      }], { includeText: false });
    }
    async function extractResourceVideoFramesForVision2(args) {
      const resourceId = resourceIdParam2(args);
      const maxVideoBytes = clampInteger2(numberParam2(args.max_video_bytes) ?? numberParam2(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES22, 1, ABSOLUTE_MAX_VIDEO_BYTES22);
      const maxWidth = clampInteger2(numberParam2(args.max_width) ?? numberParam2(args.maxWidth) ?? DEFAULT_MAX_WIDTH2, 128, 1920);
      const imageFormat = stringParam3(args.image_format) === "png" || stringParam3(args.imageFormat) === "png" ? "png" : "jpeg";
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_extract_frames but was not found");
      const file = await downloadResourceFile2(resourceId, { maxBytes: maxVideoBytes });
      const dir = await mkdtempStable2("movscript-mcp-video-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}.video`);
      const extension = imageFormat === "png" ? "png" : "jpg";
      const mimeType = imageFormat === "png" ? "image/png" : "image/jpeg";
      try {
        await (0, import_promises72.writeFile)(inputPath, file.bytes);
        const video = await probeVideoMetadata2(inputPath, ffmpeg).catch(() => ({}));
        const sampling = buildMCPFrameSamplingPlan2({
          mode: modeParam2(args.mode),
          count: numberParam2(args.count) ?? numberParam2(args.frame_count),
          maxFrames: numberParam2(args.max_frames) ?? numberParam2(args.maxFrames),
          timestampsSec: timestampsParam2(args.timestamps_sec ?? args.timestampsSec),
          startSec: numberParam2(args.start_sec) ?? numberParam2(args.startSec),
          endSec: numberParam2(args.end_sec) ?? numberParam2(args.endSec),
          centerSec: numberParam2(args.center_sec) ?? numberParam2(args.centerSec),
          windowSec: numberParam2(args.window_sec) ?? numberParam2(args.windowSec),
          fps: numberParam2(args.fps),
          intervalSec: numberParam2(args.interval_sec) ?? numberParam2(args.intervalSec)
        }, video);
        const frames = [];
        const images = [];
        for (let index = 0; index < sampling.timestampsSec.length; index += 1) {
          const timestamp = sampling.timestampsSec[index] ?? 0;
          const outputPath = (0, import_path82.join)(dir, `frame-${String(index + 1).padStart(3, "0")}.${extension}`);
          await runFFmpeg22(ffmpeg, [
            "-hide_banner",
            "-loglevel",
            "error",
            "-y",
            "-ss",
            timestamp.toFixed(3),
            "-i",
            inputPath,
            "-frames:v",
            "1",
            "-vf",
            `scale=${maxWidth}:-2:force_original_aspect_ratio=decrease`,
            ...imageFormat === "jpeg" ? ["-q:v", "3"] : [],
            outputPath
          ]);
          const bytes = await (0, import_promises72.readFile)(outputPath);
          frames.push({
            index: index + 1,
            timestamp_sec: timestamp,
            mime_type: mimeType,
            size_bytes: bytes.length,
            image_payload: "sent_as_mcp_image_content"
          });
          images.push({
            label: `frame ${index + 1}, timestamp_sec=${timestamp}`,
            data: bytes.toString("base64"),
            mimeType
          });
        }
        const metadata = {
          status: "frames_extracted",
          resource_id: resourceId,
          source_mime_type: file.contentType ?? "video/unknown",
          source_size_bytes: file.bytes.length,
          max_video_bytes: maxVideoBytes,
          ...Object.keys(video).length > 0 ? { video: publicVideoMetadata2(video) } : {},
          sampling: publicSamplingPlan2({ ...sampling, returnedFrameCount: frames.length }),
          max_width: maxWidth,
          frames,
          ...sampling.warnings.length > 0 ? { warnings: sampling.warnings } : {},
          message: "Video frames were extracted by MovScript MCP and returned as MCP image content. The original video was not sent to the model."
        };
        return mcpToolResultWithImages2(metadata, images);
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function probeResourceVideo2(args) {
      const resourceId = resourceIdParam2(args);
      const maxVideoBytes = clampInteger2(numberParam2(args.max_video_bytes) ?? numberParam2(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES22, 1, ABSOLUTE_MAX_VIDEO_BYTES22);
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_probe but was not found");
      const file = await downloadResourceFile2(resourceId, { maxBytes: maxVideoBytes });
      const dir = await mkdtempStable2("movscript-mcp-video-probe-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}.video`);
      try {
        await (0, import_promises72.writeFile)(inputPath, file.bytes);
        const video = await probeVideoMetadata2(inputPath, ffmpeg);
        return {
          status: "probed",
          resource_id: resourceId,
          source_mime_type: file.contentType ?? "video/unknown",
          source_size_bytes: file.bytes.length,
          max_video_bytes: maxVideoBytes,
          video: publicVideoMetadata2(video),
          message: `Video resource #${resourceId} probed.`
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function extractResourceVideoFrameToResource2(args) {
      const result = await extractResourceVideoFramesToResources2({
        ...args,
        mode: "timestamps",
        timestamps_sec: [numberParam2(args.timestamp_sec) ?? numberParam2(args.timestampSec) ?? 0],
        max_frames: 1
      });
      const item = Array.isArray(result.items) ? result.items[0] : void 0;
      if (!isRecord2122(item)) throw new Error("frame extraction did not create an image resource");
      return {
        status: "created",
        source_resource_id: result.resource_id,
        image_resource_id: item.resource_id,
        resource_id: item.resource_id,
        timestamp_sec: item.timestamp_sec,
        frame: item,
        video: result.video,
        message: `Extracted frame at ${item.timestamp_sec}s to image resource #${item.resource_id}.`
      };
    }
    async function extractResourceVideoFramesToResources2(args) {
      const resourceId = resourceIdParam2(args);
      const maxVideoBytes = clampInteger2(numberParam2(args.max_video_bytes) ?? numberParam2(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES22, 1, ABSOLUTE_MAX_VIDEO_BYTES22);
      const maxWidth = clampInteger2(numberParam2(args.max_width) ?? numberParam2(args.maxWidth) ?? DEFAULT_MAX_WIDTH2, 128, 1920);
      const imageFormat = stringParam3(args.image_format) === "png" || stringParam3(args.imageFormat) === "png" ? "png" : "jpeg";
      const maxUploadBytes = clampInteger2(numberParam2(args.max_upload_bytes) ?? numberParam2(args.maxUploadBytes) ?? DEFAULT_MAX_UPLOAD_BYTES2, 1, ABSOLUTE_MAX_UPLOAD_BYTES2);
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_extract_frames_to_resources but was not found");
      const file = await downloadResourceFile2(resourceId, { maxBytes: maxVideoBytes });
      const dir = await mkdtempStable2("movscript-mcp-video-frames-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}.video`);
      const extension = imageFormat === "png" ? "png" : "jpg";
      const mimeType = imageFormat === "png" ? "image/png" : "image/jpeg";
      try {
        await (0, import_promises72.writeFile)(inputPath, file.bytes);
        const video = await probeVideoMetadata2(inputPath, ffmpeg).catch(() => ({}));
        const sampling = buildMCPFrameSamplingPlan2({
          mode: modeParam2(args.mode),
          count: numberParam2(args.count) ?? numberParam2(args.frame_count),
          maxFrames: numberParam2(args.max_frames) ?? numberParam2(args.maxFrames),
          timestampsSec: timestampsParam2(args.timestamps_sec ?? args.timestampsSec),
          startSec: numberParam2(args.start_sec) ?? numberParam2(args.startSec),
          endSec: numberParam2(args.end_sec) ?? numberParam2(args.endSec),
          centerSec: numberParam2(args.center_sec) ?? numberParam2(args.centerSec),
          windowSec: numberParam2(args.window_sec) ?? numberParam2(args.windowSec),
          fps: numberParam2(args.fps),
          intervalSec: numberParam2(args.interval_sec) ?? numberParam2(args.intervalSec)
        }, video);
        const items = [];
        for (let index = 0; index < sampling.timestampsSec.length; index += 1) {
          const timestamp = sampling.timestampsSec[index] ?? 0;
          const outputPath = (0, import_path82.join)(dir, `frame-${String(index + 1).padStart(3, "0")}.${extension}`);
          await extractVideoFrame2(ffmpeg, inputPath, outputPath, {
            timestampSec: timestamp,
            maxWidth,
            imageFormat
          });
          const bytes = await (0, import_promises72.readFile)(outputPath);
          const filename = frameOutputFilename2(args, resourceId, timestamp, extension, index);
          const uploaded = await uploadGeneratedResourceBytes2({
            bytes,
            mimeType,
            filename,
            folderId: stringParam3(args.folder_id) ?? stringParam3(args.folderId),
            maxBytes: maxUploadBytes,
            derivative: {
              operation: "video_extract_frame",
              tool: "movscript_resource_video_extract_frames_to_resources",
              inputResourceIds: [resourceId],
              params: {
                timestamp_sec: timestamp,
                max_width: maxWidth,
                image_format: imageFormat
              }
            }
          });
          items.push({
            index: index + 1,
            timestamp_sec: timestamp,
            resource_id: uploaded.resource_id,
            image_resource_id: uploaded.resource_id,
            mime_type: mimeType,
            size_bytes: bytes.length,
            resource: uploaded.resource,
            source: {
              operation: "video_extract_frame",
              source_resource_id: resourceId,
              timestamp_sec: timestamp,
              max_width: maxWidth,
              image_format: imageFormat
            }
          });
        }
        const resourceIds22 = items.map((item) => numberParam2(item.resource_id)).filter((item) => item !== void 0);
        return {
          status: "created",
          resource_id: resourceId,
          source_resource_id: resourceId,
          source_mime_type: file.contentType ?? "video/unknown",
          source_size_bytes: file.bytes.length,
          ...Object.keys(video).length > 0 ? { video: publicVideoMetadata2(video) } : {},
          sampling: publicSamplingPlan2({ ...sampling, returnedFrameCount: items.length }),
          max_width: maxWidth,
          image_format: imageFormat,
          count: items.length,
          resource_ids: resourceIds22,
          image_resource_ids: resourceIds22,
          items,
          ...sampling.warnings.length > 0 ? { warnings: sampling.warnings } : {},
          message: `${items.length} frame resource(s) created from video resource #${resourceId}.`
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function trimResourceVideoToResource2(args) {
      const resourceId = resourceIdParam2(args);
      const maxVideoBytes = clampInteger2(numberParam2(args.max_video_bytes) ?? numberParam2(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES22, 1, ABSOLUTE_MAX_VIDEO_BYTES22);
      const maxUploadBytes = clampInteger2(numberParam2(args.max_upload_bytes) ?? numberParam2(args.maxUploadBytes) ?? ABSOLUTE_MAX_UPLOAD_BYTES2, 1, ABSOLUTE_MAX_UPLOAD_BYTES2);
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_trim_to_resource but was not found");
      const file = await downloadResourceFile2(resourceId, { maxBytes: maxVideoBytes });
      const dir = await mkdtempStable2("movscript-mcp-video-trim-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}.video`);
      const outputPath = (0, import_path82.join)(dir, "trimmed.mp4");
      try {
        await (0, import_promises72.writeFile)(inputPath, file.bytes);
        const video = await probeVideoMetadata2(inputPath, ffmpeg).catch(() => ({}));
        const range = clipRangeFromArgs2(args, video.durationSec);
        await trimVideoFile2(ffmpeg, inputPath, outputPath, range, {
          mode: stringParam3(args.mode) === "fast" ? "fast" : "accurate",
          volume: numberParam2(args.volume),
          muted: booleanParam32(args.muted)
        });
        const bytes = await (0, import_promises72.readFile)(outputPath);
        const filename = videoOutputFilename2(args, `resource-${resourceId}-trim-${range.startSec}-${range.endSec}`);
        const uploaded = await uploadGeneratedResourceBytes2({
          bytes,
          mimeType: "video/mp4",
          filename,
          folderId: stringParam3(args.folder_id) ?? stringParam3(args.folderId),
          maxBytes: maxUploadBytes,
          derivative: {
            operation: "video_trim",
            tool: "movscript_resource_video_trim_to_resource",
            inputResourceIds: [resourceId],
            params: {
              start_sec: range.startSec,
              end_sec: range.endSec,
              mode: stringParam3(args.mode) === "fast" ? "fast" : "accurate",
              ...numberParam2(args.volume) !== void 0 ? { volume: numberParam2(args.volume) } : {},
              ...booleanParam32(args.muted) !== void 0 ? { muted: booleanParam32(args.muted) } : {}
            }
          }
        });
        return {
          status: "created",
          compatibility: neutralVideoResourcePreparationCompatibility2(),
          source_resource_id: resourceId,
          video_resource_id: uploaded.resource_id,
          resource_id: uploaded.resource_id,
          mime_type: "video/mp4",
          size_bytes: bytes.length,
          duration_sec: roundTime22(range.endSec - range.startSec),
          start_sec: range.startSec,
          end_sec: range.endSec,
          resource: uploaded.resource,
          source: {
            operation: "video_trim",
            source_resource_id: resourceId,
            start_sec: range.startSec,
            end_sec: range.endSec
          },
          message: `Trimmed video resource #${resourceId} to video resource #${uploaded.resource_id}.`
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function composeResourceVideosToResource2(args) {
      const items = videoComposeItems2(args.items);
      const maxVideoBytes = clampInteger2(numberParam2(args.max_video_bytes) ?? numberParam2(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES22, 1, ABSOLUTE_MAX_VIDEO_BYTES22);
      const maxUploadBytes = clampInteger2(numberParam2(args.max_upload_bytes) ?? numberParam2(args.maxUploadBytes) ?? ABSOLUTE_MAX_UPLOAD_BYTES2, 1, ABSOLUTE_MAX_UPLOAD_BYTES2);
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_compose_to_resource but was not found");
      const dir = await mkdtempStable2("movscript-mcp-video-compose-");
      try {
        const segmentPaths = [];
        const segments = [];
        for (let index = 0; index < items.length; index += 1) {
          const item = items[index];
          const file = await downloadResourceFile2(item.resourceId, { maxBytes: maxVideoBytes });
          const inputPath = (0, import_path82.join)(dir, `input-${String(index + 1).padStart(3, "0")}.video`);
          const segmentPath = (0, import_path82.join)(dir, `segment-${String(index + 1).padStart(3, "0")}.mp4`);
          await (0, import_promises72.writeFile)(inputPath, file.bytes);
          const video = await probeVideoMetadata2(inputPath, ffmpeg).catch(() => ({}));
          const range = clipRangeFromItem2(item, video.durationSec);
          await trimVideoFile2(ffmpeg, inputPath, segmentPath, range, {
            mode: "accurate",
            volume: item.volume,
            muted: item.muted
          });
          segmentPaths.push(segmentPath);
          segments.push({
            index: index + 1,
            source_resource_id: item.resourceId,
            start_sec: range.startSec,
            end_sec: range.endSec,
            duration_sec: roundTime22(range.endSec - range.startSec),
            ...Object.keys(video).length > 0 ? { video: publicVideoMetadata2(video) } : {}
          });
        }
        const listPath = (0, import_path82.join)(dir, "concat-list.txt");
        await (0, import_promises72.writeFile)(listPath, segmentPaths.map((path) => `file '${(0, import_path82.basename)(path)}'`).join("\n"), "utf8");
        const outputPath = (0, import_path82.join)(dir, "composed.mp4");
        await runFFmpeg22(ffmpeg, [
          "-hide_banner",
          "-loglevel",
          "error",
          "-y",
          "-f",
          "concat",
          "-safe",
          "0",
          "-i",
          listPath,
          "-c",
          "copy",
          "-movflags",
          "+faststart",
          outputPath
        ]);
        const bytes = await (0, import_promises72.readFile)(outputPath);
        const filename = videoOutputFilename2(args, `composed-video-${(0, import_crypto22.randomUUID)().slice(0, 8)}`);
        const uploaded = await uploadGeneratedResourceBytes2({
          bytes,
          mimeType: "video/mp4",
          filename,
          folderId: stringParam3(args.folder_id) ?? stringParam3(args.folderId),
          maxBytes: maxUploadBytes,
          derivative: {
            operation: "video_compose",
            tool: "movscript_resource_video_compose_to_resource",
            inputResourceIds: items.map((item) => item.resourceId),
            params: { segments }
          }
        });
        const durationSec = segments.reduce((total, item) => total + (numberParam2(item.duration_sec) ?? 0), 0);
        return {
          status: "created",
          compatibility: resourceLevelVideoUtility2(),
          video_resource_id: uploaded.resource_id,
          resource_id: uploaded.resource_id,
          mime_type: "video/mp4",
          size_bytes: bytes.length,
          duration_sec: roundTime22(durationSec),
          count: segments.length,
          input_resource_ids: items.map((item) => item.resourceId),
          segments,
          resource: uploaded.resource,
          source: {
            operation: "video_compose",
            input_resource_ids: items.map((item) => item.resourceId),
            segments
          },
          message: `Composed ${segments.length} video segment(s) to video resource #${uploaded.resource_id}.`
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function createResourceVideoContactSheetToResource2(args) {
      const resourceId = resourceIdParam2(args);
      const maxVideoBytes = clampInteger2(numberParam2(args.max_video_bytes) ?? numberParam2(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES22, 1, ABSOLUTE_MAX_VIDEO_BYTES22);
      const maxUploadBytes = clampInteger2(numberParam2(args.max_upload_bytes) ?? numberParam2(args.maxUploadBytes) ?? DEFAULT_MAX_UPLOAD_BYTES2, 1, ABSOLUTE_MAX_UPLOAD_BYTES2);
      const columns = clampInteger2(numberParam2(args.columns) ?? 3, 1, 8);
      const count = clampInteger2(numberParam2(args.count) ?? 9, 1, 64);
      const rows = Math.max(1, Math.ceil(count / columns));
      const thumbWidth = clampInteger2(numberParam2(args.thumb_width) ?? numberParam2(args.thumbWidth) ?? 320, 64, 960);
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_contact_sheet_to_resource but was not found");
      const file = await downloadResourceFile2(resourceId, { maxBytes: maxVideoBytes });
      const dir = await mkdtempStable2("movscript-mcp-video-sheet-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}.video`);
      const outputPath = (0, import_path82.join)(dir, "contact-sheet.jpg");
      try {
        await (0, import_promises72.writeFile)(inputPath, file.bytes);
        const video = await probeVideoMetadata2(inputPath, ffmpeg).catch(() => ({}));
        const durationSec = video.durationSec;
        const intervalSec = positiveNumber222(numberParam2(args.interval_sec) ?? numberParam2(args.intervalSec)) ?? (durationSec ? Math.max(0.1, durationSec / count) : DEFAULT_VIDEO_FRAME_INTERVAL_SEC2);
        await runFFmpeg22(ffmpeg, [
          "-hide_banner",
          "-loglevel",
          "error",
          "-y",
          "-i",
          inputPath,
          "-vf",
          `fps=1/${intervalSec.toFixed(3)},scale=${thumbWidth}:-2:force_original_aspect_ratio=decrease,tile=${columns}x${rows}:padding=8:margin=8`,
          "-frames:v",
          "1",
          "-q:v",
          "3",
          outputPath
        ]);
        const bytes = await (0, import_promises72.readFile)(outputPath);
        const filename = ensureExtension2(stringParam3(args.filename) ?? stringParam3(args.name) ?? `resource-${resourceId}-contact-sheet`, "jpg");
        const uploaded = await uploadGeneratedResourceBytes2({
          bytes,
          mimeType: "image/jpeg",
          filename,
          folderId: stringParam3(args.folder_id) ?? stringParam3(args.folderId),
          maxBytes: maxUploadBytes,
          derivative: {
            operation: "video_contact_sheet",
            tool: "movscript_resource_video_contact_sheet_to_resource",
            inputResourceIds: [resourceId],
            params: {
              columns,
              rows,
              count,
              thumb_width: thumbWidth,
              interval_sec: roundTime22(intervalSec)
            }
          }
        });
        return {
          status: "created",
          source_resource_id: resourceId,
          image_resource_id: uploaded.resource_id,
          resource_id: uploaded.resource_id,
          mime_type: "image/jpeg",
          size_bytes: bytes.length,
          video: publicVideoMetadata2(video),
          sheet: {
            columns,
            rows,
            count,
            thumb_width: thumbWidth,
            interval_sec: roundTime22(intervalSec)
          },
          resource: uploaded.resource,
          source: {
            operation: "video_contact_sheet",
            source_resource_id: resourceId,
            columns,
            rows,
            count,
            thumb_width: thumbWidth,
            interval_sec: roundTime22(intervalSec)
          },
          message: `Created contact sheet image resource #${uploaded.resource_id} from video resource #${resourceId}.`
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function extractResourceVideoAudioToResource2(args) {
      const resourceId = resourceIdParam2(args);
      const maxVideoBytes = clampInteger2(numberParam2(args.max_video_bytes) ?? numberParam2(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES22, 1, ABSOLUTE_MAX_VIDEO_BYTES22);
      const maxUploadBytes = clampInteger2(numberParam2(args.max_upload_bytes) ?? numberParam2(args.maxUploadBytes) ?? ABSOLUTE_MAX_UPLOAD_BYTES2, 1, ABSOLUTE_MAX_UPLOAD_BYTES2);
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_extract_audio_to_resource but was not found");
      const file = await downloadResourceFile2(resourceId, { maxBytes: maxVideoBytes });
      const dir = await mkdtempStable2("movscript-mcp-video-audio-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}.video`);
      const outputPath = (0, import_path82.join)(dir, "audio.m4a");
      try {
        await (0, import_promises72.writeFile)(inputPath, file.bytes);
        const range = optionalAudioRangeFromArgs2(args);
        await runFFmpeg22(ffmpeg, [
          "-hide_banner",
          "-loglevel",
          "error",
          "-y",
          ...range ? ["-ss", range.startSec.toFixed(3)] : [],
          "-i",
          inputPath,
          ...range ? ["-t", (range.endSec - range.startSec).toFixed(3)] : [],
          "-vn",
          "-map",
          "0:a:0?",
          "-c:a",
          "aac",
          "-b:a",
          "128k",
          outputPath
        ]);
        const bytes = await (0, import_promises72.readFile)(outputPath);
        const filename = ensureExtension2(stringParam3(args.filename) ?? stringParam3(args.name) ?? `resource-${resourceId}-audio`, "m4a");
        const uploaded = await uploadGeneratedResourceBytes2({
          bytes,
          mimeType: "audio/mp4",
          filename,
          folderId: stringParam3(args.folder_id) ?? stringParam3(args.folderId),
          maxBytes: maxUploadBytes,
          derivative: {
            operation: "video_extract_audio",
            tool: "movscript_resource_video_extract_audio_to_resource",
            inputResourceIds: [resourceId],
            params: {
              ...range ? { start_sec: range.startSec, end_sec: range.endSec } : {}
            }
          }
        });
        return {
          status: "created",
          source_resource_id: resourceId,
          audio_resource_id: uploaded.resource_id,
          resource_id: uploaded.resource_id,
          mime_type: "audio/mp4",
          size_bytes: bytes.length,
          ...range ? { start_sec: range.startSec, end_sec: range.endSec, duration_sec: roundTime22(range.endSec - range.startSec) } : {},
          resource: uploaded.resource,
          source: {
            operation: "video_extract_audio",
            source_resource_id: resourceId,
            ...range ? { start_sec: range.startSec, end_sec: range.endSec } : {}
          },
          message: `Extracted audio resource #${uploaded.resource_id} from video resource #${resourceId}.`
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function transformResourceImageToResource2(args) {
      const resourceId = resourceIdParam2(args);
      const maxSourceBytes = clampInteger2(numberParam2(args.max_source_bytes) ?? numberParam2(args.maxSourceBytes) ?? DEFAULT_MAX_IMAGE_BYTES2, 1, ABSOLUTE_MAX_IMAGE_BYTES2);
      const maxUploadBytes = clampInteger2(numberParam2(args.max_upload_bytes) ?? numberParam2(args.maxUploadBytes) ?? DEFAULT_MAX_UPLOAD_BYTES2, 1, ABSOLUTE_MAX_UPLOAD_BYTES2);
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_image_transform_to_resource but was not found");
      const file = await downloadResourceFile2(resourceId, { maxBytes: maxSourceBytes });
      const sourceMimeType = normalizeImageMimeType2(stringParam3(args.mime_type) ?? stringParam3(args.mimeType) ?? file.contentType);
      const outputFormat = imageOutputFormat2(args, sourceMimeType);
      const outputMimeType = imageMimeTypeForFormat2(outputFormat);
      const outputExtension = imageExtensionForFormat2(outputFormat);
      const dir = await mkdtempStable2("movscript-mcp-image-transform-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}${extensionForMimeType2(sourceMimeType) || ".image"}`);
      const outputPath = (0, import_path82.join)(dir, `transformed.${outputExtension}`);
      try {
        await (0, import_promises72.writeFile)(inputPath, file.bytes);
        const source = await probeImageMetadata2(inputPath, ffmpeg);
        await decodeImageFrame2(inputPath, ffmpeg);
        const transform = imageTransformSpec2(args, source);
        const filters = imageTransformFilters2(transform);
        await runFFmpeg22(ffmpeg, [
          "-hide_banner",
          "-loglevel",
          "error",
          "-y",
          "-i",
          inputPath,
          "-frames:v",
          "1",
          ...filters.length > 0 ? ["-vf", filters.join(",")] : [],
          ...outputFormat === "jpeg" ? ["-q:v", "2"] : [],
          ...outputFormat === "webp" ? ["-lossless", "1"] : [],
          outputPath
        ]);
        const bytes = await (0, import_promises72.readFile)(outputPath);
        const filename = ensureExtension2(stringParam3(args.filename) ?? stringParam3(args.name) ?? `resource-${resourceId}-transform`, outputExtension);
        const uploaded = await uploadGeneratedResourceBytes2({
          bytes,
          mimeType: outputMimeType,
          filename,
          folderId: stringParam3(args.folder_id) ?? stringParam3(args.folderId),
          maxBytes: maxUploadBytes,
          derivative: {
            operation: "image_transform",
            tool: "movscript_resource_image_transform_to_resource",
            inputResourceIds: [resourceId],
            params: { ...transform.public, output_format: outputFormat }
          }
        });
        return {
          status: "created",
          source_resource_id: resourceId,
          image_resource_id: uploaded.resource_id,
          resource_id: uploaded.resource_id,
          source_mime_type: sourceMimeType,
          source_size_bytes: file.bytes.length,
          source_width: source.width,
          source_height: source.height,
          mime_type: outputMimeType,
          size_bytes: bytes.length,
          width: transform.output.width,
          height: transform.output.height,
          resource: uploaded.resource,
          transform: { ...transform.public, output_format: outputFormat },
          message: `Transformed image resource #${resourceId} to image resource #${uploaded.resource_id}.`
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function annotateResourceImage2(args) {
      const source = await loadImageAnnotationSource2(args);
      const natural = parseImageSize2(source.bytes, source.mimeType);
      const width = clampInteger2(numberParam2(args.width) ?? natural?.width ?? DEFAULT_ANNOTATION_WIDTH2, 1, 16384);
      const height = clampInteger2(numberParam2(args.height) ?? natural?.height ?? DEFAULT_ANNOTATION_HEIGHT2, 1, 16384);
      const shapes = annotationShapes2(args.annotations ?? args.shapes);
      if (shapes.length === 0) throw new Error("annotations must contain at least one shape");
      const title = stringParam3(args.title) ?? "MovScript agent annotation";
      const svg = renderAnnotationSVG2({
        source,
        width,
        height,
        title,
        shapes,
        note: stringParam3(args.note)
      });
      const output3 = await resolveAnnotationOutputPath2(args, title);
      const outputPath = output3.path;
      await (0, import_promises72.mkdir)((0, import_path82.dirname)(outputPath), { recursive: true });
      await (0, import_promises72.writeFile)(outputPath, svg, "utf8");
      const bytes = Buffer.from(svg, "utf8");
      const metadata = {
        status: "annotated",
        artifact_path: outputPath,
        mime_type: "image/svg+xml",
        size_bytes: bytes.length,
        width,
        height,
        source: source.public,
        annotation_count: shapes.length,
        annotations: shapes.map(publicAnnotationShape2),
        artifact_location: output3.location,
        ...output3.cacheDir ? { cache_dir: output3.cacheDir } : {},
        image_payload: "stored_as_local_artifact",
        mcp_image_content: false,
        message: "Annotated guidance image was rendered as SVG and stored at artifact_path. The image bytes were not returned as MCP image content. Upload artifact_path with movscript_resource_upload to store it as a RawResource for generation."
      };
      return mcpToolResultMetadata2(metadata);
    }
    async function uploadAgentImageResource2(args) {
      const input3 = await loadUploadInput2(args);
      const maxBytes = clampInteger2(numberParam2(args.max_bytes) ?? numberParam2(args.maxBytes) ?? DEFAULT_MAX_UPLOAD_BYTES2, 1, ABSOLUTE_MAX_UPLOAD_BYTES2);
      if (input3.bytes.length > maxBytes) {
        throw new Error(`upload input is ${input3.bytes.length} bytes, above max_bytes=${maxBytes}`);
      }
      const filename = uploadFilename2(args, input3.mimeType, input3.filename);
      const form = new FormData();
      const blob = new Blob([new Uint8Array(input3.bytes)], { type: input3.mimeType });
      form.append("file", blob, filename);
      const folderId = stringParam3(args.folder_id) ?? stringParam3(args.folderId);
      if (folderId) form.append("folder_id", folderId);
      const resource22 = await backendPostMultipart2("/resources/upload", form);
      const resourceId = numericResourceId3(resource22);
      if (resourceId === void 0) throw new Error("resource upload response did not include a valid resource ID");
      return {
        status: "uploaded",
        resource_id: resourceId,
        resource: resource22,
        source: input3.public,
        filename,
        mime_type: input3.mimeType,
        size_bytes: input3.bytes.length,
        message: "Agent-created image was uploaded to MovScript RawResource library. Use resource_id in generation input_resource_ids/reference_resource_ids."
      };
    }
    async function uploadAgentImageResources2(args) {
      const items = batchItems2(args.items, "items");
      const continueOnError = booleanParam32(args.continue_on_error) ?? booleanParam32(args.continueOnError) ?? true;
      const maxConcurrency = continueOnError ? clampInteger2(numberParam2(args.max_concurrency) ?? numberParam2(args.maxConcurrency) ?? 3, 1, 8) : 1;
      const defaults = batchDefaults22(args, /* @__PURE__ */ new Set(["items", "continue_on_error", "continueOnError", "max_concurrency", "maxConcurrency"]));
      const results = await runBatch2(items, maxConcurrency, async (item, index) => {
        const result = await uploadAgentImageResource2({ ...defaults, ...item });
        return {
          index,
          status: "uploaded",
          resource_id: result.resource_id,
          resource: result.resource,
          source: result.source,
          filename: result.filename,
          mime_type: result.mime_type,
          size_bytes: result.size_bytes,
          message: result.message
        };
      }, continueOnError);
      const successItems = results.filter((item) => item.status !== "error" && item.status !== "skipped");
      const failedItems = results.filter((item) => item.status === "error");
      const resourceIds22 = successItems.map((item) => numberParam2(item.resource_id)).filter((value) => value !== void 0);
      return {
        status: batchStatus2(successItems.length, failedItems.length),
        total: items.length,
        success_count: successItems.length,
        failed_count: failedItems.length,
        items: results,
        resource_ids: resourceIds22,
        message: `${successItems.length}/${items.length} resource upload(s) completed.`
      };
    }
    async function readResourceFileBlob2(resourceId, options = {}) {
      const maxBytes = clampInteger2(options.maxBytes ?? DEFAULT_MAX_IMAGE_BYTES2, 1, ABSOLUTE_MAX_IMAGE_BYTES2);
      const file = await downloadResourceFile2(resourceId, { maxBytes });
      if (file.bytes.length > maxBytes) {
        throw new Error(`resource ${resourceId} is ${file.bytes.length} bytes, above maxBytes=${maxBytes}`);
      }
      return {
        mimeType: file.contentType ?? "application/octet-stream",
        blob: file.bytes.toString("base64"),
        sizeBytes: file.bytes.length
      };
    }
    function buildMCPFrameSamplingPlan2(input3, video = {}) {
      const warnings = [];
      const maxFrames = clampInteger2(input3.maxFrames ?? DEFAULT_MAX_FRAMES2, 1, ABSOLUTE_MAX_VIDEO_FRAMES2);
      const mode = resolveFrameMode2(input3);
      const durationSec = video.durationSec;
      const requested = requestedFrameCount2(input3, mode, durationSec);
      let timestampsSec = [];
      if (mode === "timestamps") {
        timestampsSec = normalizeExplicitTimestamps2(input3.timestampsSec, maxFrames, durationSec, warnings);
      } else if (mode === "range") {
        const range = normalizeRange2(input3.startSec, input3.endSec, durationSec, warnings);
        timestampsSec = timestampsForRange2(range.startSec, range.endSec, input3, maxFrames, warnings);
        return {
          mode,
          timestampsSec,
          requestedFrameCount: requested,
          returnedFrameCount: timestampsSec.length,
          maxFrames,
          startSec: range.startSec,
          endSec: range.endSec,
          ...samplingFps2(input3) !== void 0 ? { fps: samplingFps2(input3) } : {},
          ...samplingIntervalSec2(input3) !== void 0 ? { intervalSec: samplingIntervalSec2(input3) } : {},
          warnings
        };
      } else if (mode === "burst") {
        const burst = normalizeBurst2(input3.centerSec, input3.windowSec, durationSec, warnings);
        timestampsSec = timestampsForRange2(burst.startSec, burst.endSec, input3, maxFrames, warnings);
        return {
          mode,
          timestampsSec,
          requestedFrameCount: requested,
          returnedFrameCount: timestampsSec.length,
          maxFrames,
          startSec: burst.startSec,
          endSec: burst.endSec,
          centerSec: burst.centerSec,
          windowSec: burst.windowSec,
          ...samplingFps2(input3) !== void 0 ? { fps: samplingFps2(input3) } : {},
          ...samplingIntervalSec2(input3) !== void 0 ? { intervalSec: samplingIntervalSec2(input3) } : {},
          warnings
        };
      } else {
        timestampsSec = overviewTimestamps2(input3.count ?? DEFAULT_VIDEO_FRAME_COUNT2, maxFrames, durationSec);
      }
      if (timestampsSec.length === 0) timestampsSec = durationSec && durationSec > 0 ? [roundTime22(durationSec / 2)] : [0];
      return {
        mode,
        timestampsSec,
        requestedFrameCount: requested,
        returnedFrameCount: timestampsSec.length,
        maxFrames,
        warnings
      };
    }
    async function extractVideoFrame2(ffmpeg, inputPath, outputPath, input3) {
      await runFFmpeg22(ffmpeg, [
        "-hide_banner",
        "-loglevel",
        "error",
        "-y",
        "-ss",
        input3.timestampSec.toFixed(3),
        "-i",
        inputPath,
        "-frames:v",
        "1",
        "-vf",
        `scale=${input3.maxWidth}:-2:force_original_aspect_ratio=decrease`,
        ...input3.imageFormat === "jpeg" ? ["-q:v", "3"] : [],
        outputPath
      ]);
    }
    async function trimVideoFile2(ffmpeg, inputPath, outputPath, range, options) {
      const durationSec = Math.max(0.1, range.endSec - range.startSec);
      if (options.mode === "fast" && !options.muted && options.volume === void 0) {
        await runFFmpeg22(ffmpeg, [
          "-hide_banner",
          "-loglevel",
          "error",
          "-y",
          "-ss",
          range.startSec.toFixed(3),
          "-i",
          inputPath,
          "-t",
          durationSec.toFixed(3),
          "-map",
          "0:v:0",
          "-map",
          "0:a?",
          "-c",
          "copy",
          "-movflags",
          "+faststart",
          outputPath
        ]);
        return;
      }
      const args = [
        "-hide_banner",
        "-loglevel",
        "error",
        "-y",
        "-i",
        inputPath,
        "-ss",
        range.startSec.toFixed(3),
        "-t",
        durationSec.toFixed(3),
        "-map",
        "0:v:0",
        "-vf",
        "setsar=1",
        "-c:v",
        "libx264",
        "-pix_fmt",
        "yuv420p",
        "-preset",
        "veryfast",
        "-movflags",
        "+faststart"
      ];
      const volume = options.volume === void 0 ? 100 : clampNumber22(options.volume, 0, 200);
      if (options.muted || volume <= 0) {
        args.push("-an");
      } else {
        args.push("-map", "0:a?");
        if (volume !== 100) args.push("-filter:a", `volume=${(volume / 100).toFixed(2)}`);
        args.push("-c:a", "aac", "-b:a", "128k");
      }
      args.push(outputPath);
      await runFFmpeg22(ffmpeg, args);
    }
    function clipRangeFromArgs2(args, durationSec) {
      return normalizeClipRange2({
        startSec: numberParam2(args.start_sec) ?? numberParam2(args.startSec) ?? 0,
        endSec: numberParam2(args.end_sec) ?? numberParam2(args.endSec),
        durationSec: numberParam2(args.duration_sec) ?? numberParam2(args.durationSec),
        sourceDurationSec: durationSec
      });
    }
    function optionalAudioRangeFromArgs2(args) {
      const startSec = numberParam2(args.start_sec) ?? numberParam2(args.startSec);
      const endSec = numberParam2(args.end_sec) ?? numberParam2(args.endSec);
      const durationSec = numberParam2(args.duration_sec) ?? numberParam2(args.durationSec);
      if (startSec === void 0 && endSec === void 0 && durationSec === void 0) return void 0;
      return normalizeClipRange2({
        startSec: startSec ?? 0,
        endSec,
        durationSec
      });
    }
    function clipRangeFromItem2(item, durationSec) {
      return normalizeClipRange2({
        startSec: item.startSec ?? 0,
        endSec: item.endSec,
        durationSec: item.durationSec,
        sourceDurationSec: durationSec
      });
    }
    function normalizeClipRange2(input3) {
      const startSec = roundTime22(Math.max(0, input3.startSec));
      const endCandidate = input3.endSec ?? (input3.durationSec !== void 0 ? startSec + Math.max(0, input3.durationSec) : input3.sourceDurationSec);
      if (endCandidate === void 0) throw new Error("end_sec or duration_sec is required when video duration cannot be probed");
      const endSec = roundTime22(clampToDuration2(Math.max(0, endCandidate), input3.sourceDurationSec));
      if (endSec <= startSec) throw new Error("video clip range must have end_sec greater than start_sec");
      return { startSec, endSec };
    }
    function videoComposeItems2(value) {
      if (!Array.isArray(value) || value.length === 0) throw new Error("items must contain at least one video resource item");
      return value.map((item, index) => {
        if (!isRecord2122(item)) throw new Error(`items[${index}] must be an object`);
        const resourceId = optionalResourceIdParam2(item);
        if (resourceId === void 0) throw new Error(`items[${index}].resource_id is required`);
        const startSec = numberParam2(item.start_sec) ?? numberParam2(item.startSec);
        const endSec = numberParam2(item.end_sec) ?? numberParam2(item.endSec);
        const durationSec = numberParam2(item.duration_sec) ?? numberParam2(item.durationSec);
        const volume = numberParam2(item.volume);
        const muted = booleanParam32(item.muted);
        const normalized = {
          resourceId
        };
        if (startSec !== void 0) normalized.startSec = startSec;
        if (endSec !== void 0) normalized.endSec = endSec;
        if (durationSec !== void 0) normalized.durationSec = durationSec;
        if (volume !== void 0) normalized.volume = volume;
        if (muted !== void 0) normalized.muted = muted;
        return normalized;
      });
    }
    async function uploadGeneratedResourceBytes2(input3) {
      if (input3.bytes.length > input3.maxBytes) {
        throw new Error(`generated resource is ${input3.bytes.length} bytes, above max_upload_bytes=${input3.maxBytes}`);
      }
      const form = new FormData();
      const blob = new Blob([new Uint8Array(input3.bytes)], { type: input3.mimeType });
      form.append("file", blob, input3.filename);
      if (input3.folderId) form.append("folder_id", input3.folderId);
      if (input3.derivative) {
        form.append("derivative", JSON.stringify({
          operation: input3.derivative.operation,
          ...input3.derivative.tool ? { tool: input3.derivative.tool } : {},
          input_resource_ids: input3.derivative.inputResourceIds ?? [],
          params: input3.derivative.params ?? {}
        }));
      }
      const resource22 = await backendPostMultipart2("/resources/upload", form);
      const resourceId = numericResourceId3(resource22);
      if (resourceId === void 0) throw new Error("resource upload response did not include a valid resource ID");
      return { resource_id: resourceId, resource: resource22 };
    }
    function frameOutputFilename2(args, resourceId, timestampSec, extension, index) {
      const explicit = stringParam3(args.filename) ?? stringParam3(args.name);
      if (explicit) {
        if (index === 0) return ensureExtension2(explicit, extension);
        return ensureExtension2(`${safeFilename2((0, import_path82.basename)(explicit, (0, import_path82.extname)(explicit)))}-${String(index + 1).padStart(3, "0")}`, extension);
      }
      return `resource-${resourceId}-frame-${timestampSec.toFixed(3).replace(".", "_")}.${extension}`;
    }
    function videoOutputFilename2(args, fallbackBase) {
      const explicit = stringParam3(args.filename) ?? stringParam3(args.name);
      return ensureExtension2(explicit ?? safeFilename2(fallbackBase), "mp4");
    }
    function ensureExtension2(value, extension) {
      const normalizedExtension = extension.startsWith(".") ? extension.slice(1) : extension;
      const currentExtension = (0, import_path82.extname)(value);
      if (currentExtension.toLowerCase() === `.${normalizedExtension.toLowerCase()}`) return value;
      const base = currentExtension ? value.slice(0, -currentExtension.length) : value;
      return `${safeFilename2(base)}.${normalizedExtension}`;
    }
    function imageOutputFormat2(args, sourceMimeType) {
      const explicit = stringParam3(args.output_format) ?? stringParam3(args.outputFormat) ?? stringParam3(args.image_format) ?? stringParam3(args.imageFormat);
      switch ((explicit ?? "").toLowerCase()) {
        case "jpg":
        case "jpeg":
          return "jpeg";
        case "webp":
          return "webp";
        case "png":
          return "png";
        case "":
          if (sourceMimeType === "image/jpeg" || sourceMimeType === "image/jpg") return "jpeg";
          if (sourceMimeType === "image/webp") return "webp";
          return "png";
        default:
          throw new Error("output_format must be one of jpeg, png, or webp");
      }
    }
    function imageMimeTypeForFormat2(format) {
      switch (format) {
        case "jpeg":
          return "image/jpeg";
        case "webp":
          return "image/webp";
        case "png":
        default:
          return "image/png";
      }
    }
    function imageExtensionForFormat2(format) {
      return format === "jpeg" ? "jpg" : format;
    }
    function imageTransformSpec2(args, source) {
      const cropX = clampInteger2(numberParam2(args.crop_x) ?? numberParam2(args.cropX) ?? 0, 0, source.width);
      const cropY = clampInteger2(numberParam2(args.crop_y) ?? numberParam2(args.cropY) ?? 0, 0, source.height);
      const cropWidth = positiveInteger22(numberParam2(args.crop_width) ?? numberParam2(args.cropWidth));
      const cropHeight = positiveInteger22(numberParam2(args.crop_height) ?? numberParam2(args.cropHeight));
      const crop = cropWidth !== void 0 || cropHeight !== void 0 ? {
        x: cropX,
        y: cropY,
        width: Math.min(cropWidth ?? source.width - cropX, source.width - cropX),
        height: Math.min(cropHeight ?? source.height - cropY, source.height - cropY)
      } : void 0;
      if (crop && (crop.width <= 0 || crop.height <= 0)) throw new Error("crop rectangle must overlap the source image");
      const base = crop ? { width: crop.width, height: crop.height } : source;
      const width = positiveInteger22(numberParam2(args.width));
      const height = positiveInteger22(numberParam2(args.height));
      const maxWidth = positiveInteger22(numberParam2(args.max_width) ?? numberParam2(args.maxWidth));
      const maxHeight = positiveInteger22(numberParam2(args.max_height) ?? numberParam2(args.maxHeight));
      const resize = width !== void 0 || height !== void 0 || maxWidth !== void 0 || maxHeight !== void 0 ? { width, height, maxWidth, maxHeight } : void 0;
      const output3 = computeImageTransformOutputDimensions2(base, resize);
      const publicSpec = {
        source_width: source.width,
        source_height: source.height,
        output_width: output3.width,
        output_height: output3.height
      };
      if (crop) publicSpec.crop = { x: crop.x, y: crop.y, width: crop.width, height: crop.height };
      if (resize) {
        publicSpec.resize = {
          ...width !== void 0 ? { width } : {},
          ...height !== void 0 ? { height } : {},
          ...maxWidth !== void 0 ? { max_width: maxWidth } : {},
          ...maxHeight !== void 0 ? { max_height: maxHeight } : {}
        };
      }
      return { crop, resize, output: output3, public: publicSpec };
    }
    function computeImageTransformOutputDimensions2(source, resize) {
      if (!resize) return source;
      if (resize.width !== void 0 && resize.height !== void 0) {
        return { width: resize.width, height: resize.height };
      }
      if (resize.width !== void 0) {
        return { width: resize.width, height: Math.max(1, Math.round(source.height * (resize.width / source.width))) };
      }
      if (resize.height !== void 0) {
        return { width: Math.max(1, Math.round(source.width * (resize.height / source.height))), height: resize.height };
      }
      return fitDimensions2(source, resize.maxWidth ?? source.width, resize.maxHeight ?? source.height);
    }
    function imageTransformFilters2(transform) {
      const filters = [];
      if (transform.crop) {
        filters.push(`crop=${transform.crop.width}:${transform.crop.height}:${transform.crop.x}:${transform.crop.y}`);
      }
      if (transform.resize) {
        const resize = transform.resize;
        if (resize.width !== void 0 && resize.height !== void 0) {
          filters.push(`scale=${resize.width}:${resize.height}`);
        } else if (resize.width !== void 0) {
          filters.push(`scale=${resize.width}:-2`);
        } else if (resize.height !== void 0) {
          filters.push(`scale=-2:${resize.height}`);
        } else {
          filters.push(`scale=${resize.maxWidth ?? -2}:${resize.maxHeight ?? -2}:force_original_aspect_ratio=decrease`);
        }
      }
      return filters;
    }
    async function downloadResourceFile2(resourceId, options = {}) {
      return backendGetBinary2(`/resources/${encodeURIComponent(String(resourceId))}/file`, options);
    }
    async function processResourceImageForVision2(resourceId, bytes, mimeType, options) {
      const ffmpeg = resolveFFmpegPath2();
      if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_image_read image validation and resizing but was not found");
      const dir = await mkdtempStable2("movscript-mcp-image-");
      const inputPath = (0, import_path82.join)(dir, `resource-${resourceId}${extensionForMimeType2(mimeType) || ".image"}`);
      try {
        await (0, import_promises72.writeFile)(inputPath, bytes);
        const source = await probeImageMetadata2(inputPath, ffmpeg);
        await decodeImageFrame2(inputPath, ffmpeg);
        if (options.mode === "original") {
          return {
            bytes,
            mimeType,
            width: source.width,
            height: source.height,
            resized: false,
            source
          };
        }
        const outputDimensions = fitDimensions2(source, options.maxWidth, options.maxHeight);
        if (outputDimensions.width === source.width && outputDimensions.height === source.height) {
          return {
            bytes,
            mimeType,
            width: source.width,
            height: source.height,
            resized: false,
            source
          };
        }
        const outputPath = (0, import_path82.join)(dir, "image-fit.png");
        await runFFmpeg22(ffmpeg, [
          "-hide_banner",
          "-loglevel",
          "error",
          "-y",
          "-i",
          inputPath,
          "-frames:v",
          "1",
          "-vf",
          `scale=${options.maxWidth}:${options.maxHeight}:force_original_aspect_ratio=decrease`,
          outputPath
        ]);
        const outputBytes = await (0, import_promises72.readFile)(outputPath);
        return {
          bytes: outputBytes,
          mimeType: "image/png",
          width: outputDimensions.width,
          height: outputDimensions.height,
          resized: true,
          source
        };
      } finally {
        await (0, import_promises72.rm)(dir, { recursive: true, force: true }).catch(() => void 0);
      }
    }
    async function probeImageMetadata2(path, ffmpeg) {
      const ffprobe = ffprobePath2(ffmpeg);
      const result = await runCommand22(ffprobe, [
        "-v",
        "error",
        "-select_streams",
        "v:0",
        "-show_entries",
        "stream=width,height",
        "-of",
        "json",
        path
      ]).catch((error) => {
        throw new Error(`unable to read image dimensions: ${error instanceof Error ? error.message : String(error)}`);
      });
      const parsed = JSON.parse(result.stdout);
      const stream = parsed.streams?.find((item) => positiveInteger22(item.width) !== void 0 && positiveInteger22(item.height) !== void 0);
      const width = positiveInteger22(stream?.width);
      const height = positiveInteger22(stream?.height);
      if (width === void 0 || height === void 0) throw new Error("unable to read image dimensions");
      return { width, height };
    }
    async function decodeImageFrame2(path, ffmpeg) {
      await runFFmpeg22(ffmpeg, [
        "-hide_banner",
        "-loglevel",
        "error",
        "-i",
        path,
        "-frames:v",
        "1",
        "-f",
        "null",
        "-"
      ]).catch((error) => {
        throw new Error(`unable to decode image bytes: ${error instanceof Error ? error.message : String(error)}`);
      });
    }
    function fitDimensions2(source, maxWidth, maxHeight) {
      if (source.width <= maxWidth && source.height <= maxHeight) return source;
      const scale = Math.min(maxWidth / source.width, maxHeight / source.height);
      return {
        width: Math.max(1, Math.floor(source.width * scale)),
        height: Math.max(1, Math.floor(source.height * scale))
      };
    }
    function mcpToolResultWithImages2(metadata, images, options = {}) {
      const includeText = options.includeText ?? true;
      return {
        content: [
          ...includeText ? [{
            type: "text",
            text: JSON.stringify(metadata, null, 2)
          }] : [],
          ...images.flatMap((image) => [
            ...includeText && image.label ? [{ type: "text", text: image.label }] : [],
            { type: "image", data: image.data, mimeType: image.mimeType }
          ])
        ],
        data: metadata
      };
    }
    function mcpToolResultMetadata2(metadata) {
      return {
        content: [
          {
            type: "text",
            text: JSON.stringify(metadata, null, 2)
          }
        ],
        data: metadata
      };
    }
    function runFFmpeg22(ffmpeg, args) {
      return new Promise((resolveRun, reject) => {
        const child = (0, import_child_process22.spawn)(ffmpeg, args, { stdio: ["ignore", "ignore", "pipe"] });
        let stderr = "";
        child.stderr?.on("data", (chunk) => {
          stderr += String(chunk);
        });
        child.on("error", (error) => reject(error instanceof Error ? error : new Error(String(error))));
        child.on("exit", (code) => {
          if (code === 0) {
            resolveRun();
            return;
          }
          reject(new Error(stderr.trim() || `ffmpeg exited with code ${code ?? "unknown"}`));
        });
      });
    }
    function runCommand22(command, args) {
      return new Promise((resolveRun, reject) => {
        const child = (0, import_child_process22.spawn)(command, args, { stdio: ["ignore", "pipe", "pipe"] });
        let stdout = "";
        let stderr = "";
        child.stdout?.on("data", (chunk) => {
          stdout += String(chunk);
        });
        child.stderr?.on("data", (chunk) => {
          stderr += String(chunk);
        });
        child.on("error", (error) => reject(error instanceof Error ? error : new Error(String(error))));
        child.on("exit", (code) => {
          if (code === 0) {
            resolveRun({ stdout });
            return;
          }
          reject(new Error(stderr.trim() || `${command} exited with code ${code ?? "unknown"}`));
        });
      });
    }
    async function probeVideoMetadata2(path, ffmpeg) {
      const ffprobe = ffprobePath2(ffmpeg);
      const result = await runCommand22(ffprobe, [
        "-v",
        "error",
        "-show_entries",
        "format=duration:stream=width,height,avg_frame_rate,r_frame_rate",
        "-of",
        "json",
        path
      ]);
      const parsed = JSON.parse(result.stdout);
      const stream = parsed.streams?.find((item) => typeof item.width === "number" && typeof item.height === "number") ?? parsed.streams?.[0];
      const durationSec = positiveNumber222(Number(parsed.format?.duration));
      const fps = parseFrameRate2(stream?.avg_frame_rate) ?? parseFrameRate2(stream?.r_frame_rate);
      return {
        ...durationSec !== void 0 ? { durationSec } : {},
        ...positiveInteger22(stream?.width) !== void 0 ? { width: positiveInteger22(stream?.width) } : {},
        ...positiveInteger22(stream?.height) !== void 0 ? { height: positiveInteger22(stream?.height) } : {},
        ...fps !== void 0 ? { fps } : {}
      };
    }
    function ffprobePath2(ffmpeg) {
      if (ffmpeg === "ffmpeg") return "ffprobe";
      const suffix = process.platform === "win32" ? "ffmpeg.exe" : "ffmpeg";
      const probeSuffix = process.platform === "win32" ? "ffprobe.exe" : "ffprobe";
      return ffmpeg.endsWith(suffix) ? `${ffmpeg.slice(0, -suffix.length)}${probeSuffix}` : "ffprobe";
    }
    function resolveFrameMode2(input3) {
      if (input3.mode) return input3.mode;
      if (input3.timestampsSec && input3.timestampsSec.length > 0) return "timestamps";
      if (input3.centerSec !== void 0) return "burst";
      if (input3.startSec !== void 0 || input3.endSec !== void 0) return "range";
      return "overview";
    }
    function requestedFrameCount2(input3, mode, durationSec) {
      if (mode === "timestamps") return input3.timestampsSec?.length ?? 0;
      if (mode === "range" || mode === "burst") {
        const startSec = mode === "burst" ? Math.max(0, (input3.centerSec ?? 0) - (input3.windowSec ?? DEFAULT_BURST_WINDOW_SEC2) / 2) : input3.startSec ?? 0;
        const endSec = mode === "burst" ? (input3.centerSec ?? 0) + (input3.windowSec ?? DEFAULT_BURST_WINDOW_SEC2) / 2 : input3.endSec ?? durationSec ?? startSec;
        const interval = samplingIntervalSec2(input3) ?? 1 / (samplingFps2(input3) ?? DEFAULT_RANGE_FPS2);
        return Math.max(1, Math.floor(Math.max(0, endSec - startSec) / interval) + 1);
      }
      return input3.count ?? DEFAULT_VIDEO_FRAME_COUNT2;
    }
    function normalizeExplicitTimestamps2(timestampsSec, maxFrames, durationSec, warnings) {
      const explicit = (timestampsSec ?? []).filter((value) => Number.isFinite(value) && value >= 0).map((value) => clampToDuration2(value, durationSec)).map(roundTime22);
      if (explicit.length > maxFrames) {
        warnings.push(`Requested ${explicit.length} timestamps; returned the first ${maxFrames} due to max_frames.`);
      }
      return uniqueSorted22(explicit).slice(0, maxFrames);
    }
    function overviewTimestamps2(count, maxFrames, durationSec) {
      const frameCount = clampInteger2(count, 1, maxFrames);
      if (!durationSec || durationSec <= 0) {
        const interval = DEFAULT_VIDEO_FRAME_INTERVAL_SEC2;
        return Array.from({ length: frameCount }, (_unused, index) => roundTime22(index * interval));
      }
      if (frameCount <= 1) return [roundTime22(durationSec / 2)];
      const margin = Math.min(1, Math.max(0.1, durationSec * 0.08));
      const start = Math.min(durationSec, margin);
      const end = Math.max(start, durationSec - margin);
      const step = frameCount === 1 ? 0 : (end - start) / (frameCount - 1);
      return uniqueSorted22(Array.from({ length: frameCount }, (_unused, index) => roundTime22(start + step * index)));
    }
    function normalizeRange2(start, end, durationSec, warnings) {
      const normalizedStart = clampToDuration2(Math.max(0, start ?? 0), durationSec);
      const fallbackEnd = durationSec ?? normalizedStart;
      let normalizedEnd = clampToDuration2(Math.max(0, end ?? fallbackEnd), durationSec);
      if (normalizedEnd < normalizedStart) {
        warnings.push("end_sec was before start_sec; the range was normalized to a single timestamp.");
        normalizedEnd = normalizedStart;
      }
      return { startSec: roundTime22(normalizedStart), endSec: roundTime22(normalizedEnd) };
    }
    function normalizeBurst2(center, windowSec, durationSec, warnings) {
      const normalizedWindow = Math.max(0, windowSec ?? DEFAULT_BURST_WINDOW_SEC2);
      const normalizedCenter = clampToDuration2(Math.max(0, center ?? 0), durationSec);
      const startSec = clampToDuration2(Math.max(0, normalizedCenter - normalizedWindow / 2), durationSec);
      let endSec = clampToDuration2(normalizedCenter + normalizedWindow / 2, durationSec);
      if (endSec < startSec) {
        warnings.push("burst window could not be applied; the range was normalized to a single timestamp.");
        endSec = startSec;
      }
      return { startSec: roundTime22(startSec), endSec: roundTime22(endSec), centerSec: roundTime22(normalizedCenter), windowSec: roundTime22(normalizedWindow) };
    }
    function timestampsForRange2(startSec, endSec, input3, maxFrames, warnings) {
      const interval = samplingIntervalSec2(input3) ?? 1 / (samplingFps2(input3) ?? DEFAULT_RANGE_FPS2);
      const timestamps = [];
      for (let timestamp = startSec; timestamp <= endSec + 1e-4; timestamp += interval) {
        timestamps.push(roundTime22(timestamp));
      }
      if (timestamps.length === 0) timestamps.push(roundTime22(startSec));
      if (timestamps.length > maxFrames) {
        warnings.push(`Requested ${timestamps.length} frames for the time range; downsampled to ${maxFrames} due to max_frames.`);
        return downsample2(timestamps, maxFrames);
      }
      return uniqueSorted22(timestamps);
    }
    function samplingFps2(input3) {
      const fps = positiveNumber222(input3.fps);
      return fps === void 0 ? void 0 : Math.min(fps, MAX_FPS2);
    }
    function samplingIntervalSec2(input3) {
      return positiveNumber222(input3.intervalSec);
    }
    function downsample2(values, maxItems) {
      if (values.length <= maxItems) return uniqueSorted22(values);
      if (maxItems <= 1) return [values[0] ?? 0];
      const step = (values.length - 1) / (maxItems - 1);
      return uniqueSorted22(Array.from({ length: maxItems }, (_unused, index) => values[Math.round(index * step)] ?? values[values.length - 1] ?? 0));
    }
    function publicSamplingPlan2(sampling) {
      return {
        mode: sampling.mode,
        timestamps_sec: sampling.timestampsSec,
        requested_frame_count: sampling.requestedFrameCount,
        returned_frame_count: sampling.returnedFrameCount,
        max_frames: sampling.maxFrames,
        ...sampling.startSec !== void 0 ? { start_sec: sampling.startSec } : {},
        ...sampling.endSec !== void 0 ? { end_sec: sampling.endSec } : {},
        ...sampling.centerSec !== void 0 ? { center_sec: sampling.centerSec } : {},
        ...sampling.windowSec !== void 0 ? { window_sec: sampling.windowSec } : {},
        ...sampling.fps !== void 0 ? { fps: sampling.fps } : {},
        ...sampling.intervalSec !== void 0 ? { interval_sec: sampling.intervalSec } : {},
        ...sampling.warnings.length > 0 ? { warnings: sampling.warnings } : {}
      };
    }
    function publicVideoMetadata2(video) {
      return {
        ...video.durationSec !== void 0 ? { duration_sec: video.durationSec } : {},
        ...video.width !== void 0 ? { width: video.width } : {},
        ...video.height !== void 0 ? { height: video.height } : {},
        ...video.fps !== void 0 ? { fps: video.fps } : {}
      };
    }
    async function loadImageAnnotationSource2(args) {
      const resourceId = optionalResourceIdParam2(args);
      if (resourceId !== void 0) {
        const maxBytes = clampInteger2(numberParam2(args.max_source_bytes) ?? numberParam2(args.maxSourceBytes) ?? DEFAULT_MAX_IMAGE_BYTES2, 1, ABSOLUTE_MAX_IMAGE_BYTES2);
        const file = await downloadResourceFile2(resourceId, { maxBytes });
        const mimeType = normalizeImageMimeType2(stringParam3(args.mime_type) ?? stringParam3(args.mimeType) ?? file.contentType);
        return {
          bytes: file.bytes,
          mimeType,
          public: { kind: "backend_resource", resource_id: resourceId, mime_type: mimeType, size_bytes: file.bytes.length }
        };
      }
      const dataUrl = stringParam3(args.data_url) ?? stringParam3(args.dataUrl);
      if (dataUrl) {
        const decoded = decodeDataURL2(dataUrl);
        return {
          bytes: decoded.bytes,
          mimeType: normalizeImageMimeType2(decoded.mimeType),
          public: { kind: "data_url", mime_type: decoded.mimeType, size_bytes: decoded.bytes.length }
        };
      }
      const localPath = stringParam3(args.local_path) ?? stringParam3(args.localPath) ?? stringParam3(args.artifact_path) ?? stringParam3(args.artifactPath);
      if (localPath) {
        const bytes = await (0, import_promises72.readFile)(localPath);
        const mimeType = normalizeImageMimeType2(stringParam3(args.mime_type) ?? stringParam3(args.mimeType) ?? mimeTypeFromFilename2(localPath));
        return {
          bytes,
          mimeType,
          public: { kind: "local_path", path: localPath, mime_type: mimeType, size_bytes: bytes.length }
        };
      }
      throw new Error("annotate requires resource_id, data_url, or local_path");
    }
    function annotationShapes2(value) {
      if (!Array.isArray(value)) return [];
      return value.map(annotationShape2).filter((item) => item !== void 0);
    }
    function annotationShape2(value) {
      if (!isRecord2122(value)) return void 0;
      const type = shapeType2(value.type);
      if (!type) return void 0;
      const color = stringParam3(value.color) ?? (type === "highlight" ? "#facc15" : "#ef4444");
      const strokeWidth = clampNumber22(numberParam2(value.stroke_width) ?? numberParam2(value.strokeWidth) ?? 5, 1, 64);
      const opacity = clampNumber22(numberParam2(value.opacity) ?? (type === "highlight" ? 0.32 : 0.95), 0.05, 1);
      const fontSize = clampNumber22(numberParam2(value.font_size) ?? numberParam2(value.fontSize) ?? 32, 8, 240);
      return {
        type,
        ...numberParam2(value.x) !== void 0 ? { x: numberParam2(value.x) } : {},
        ...numberParam2(value.y) !== void 0 ? { y: numberParam2(value.y) } : {},
        ...numberParam2(value.width) !== void 0 ? { width: numberParam2(value.width) } : {},
        ...numberParam2(value.height) !== void 0 ? { height: numberParam2(value.height) } : {},
        ...numberParam2(value.cx) !== void 0 ? { cx: numberParam2(value.cx) } : {},
        ...numberParam2(value.cy) !== void 0 ? { cy: numberParam2(value.cy) } : {},
        ...numberParam2(value.r) !== void 0 ? { r: numberParam2(value.r) } : {},
        ...numberParam2(value.x1) !== void 0 ? { x1: numberParam2(value.x1) } : {},
        ...numberParam2(value.y1) !== void 0 ? { y1: numberParam2(value.y1) } : {},
        ...numberParam2(value.x2) !== void 0 ? { x2: numberParam2(value.x2) } : {},
        ...numberParam2(value.y2) !== void 0 ? { y2: numberParam2(value.y2) } : {},
        ...stringParam3(value.text) ? { text: stringParam3(value.text) } : {},
        ...stringParam3(value.fill) ? { fill: stringParam3(value.fill) } : {},
        ...stringParam3(value.label) ? { label: stringParam3(value.label) } : {},
        color,
        strokeWidth,
        opacity,
        fontSize
      };
    }
    function renderAnnotationSVG2(input3) {
      const sourceHref = `data:${input3.source.mimeType};base64,${input3.source.bytes.toString("base64")}`;
      const shapeSVG = input3.shapes.map(renderAnnotationShape2).join("\n  ");
      const noteSVG = input3.note ? `<text x="24" y="${Math.max(36, input3.height - 24)}" font-family="Arial, sans-serif" font-size="24" fill="#111827" stroke="#ffffff" stroke-width="5" paint-order="stroke">${escapeXML2(input3.note)}</text>` : "";
      return [
        `<svg xmlns="http://www.w3.org/2000/svg" width="${input3.width}" height="${input3.height}" viewBox="0 0 ${input3.width} ${input3.height}">`,
        `  <title>${escapeXML2(input3.title)}</title>`,
        "  <defs>",
        '    <marker id="arrow-head" markerWidth="16" markerHeight="16" refX="12" refY="8" orient="auto" markerUnits="strokeWidth">',
        '      <path d="M 0 0 L 16 8 L 0 16 z" fill="context-stroke" />',
        "    </marker>",
        "  </defs>",
        `  <image href="${sourceHref}" x="0" y="0" width="${input3.width}" height="${input3.height}" preserveAspectRatio="xMidYMid meet" />`,
        `  ${shapeSVG}`,
        noteSVG ? `  ${noteSVG}` : "",
        "</svg>"
      ].filter(Boolean).join("\n");
    }
    function renderAnnotationShape2(shape) {
      const common = `stroke="${escapeXML2(shape.color)}" stroke-width="${shape.strokeWidth}" opacity="${shape.opacity}"`;
      if (shape.type === "rect" || shape.type === "highlight") {
        const x = shape.x ?? 0;
        const y = shape.y ?? 0;
        const width = shape.width ?? 1;
        const height = shape.height ?? 1;
        const fill = shape.type === "highlight" ? shape.color : shape.fill ?? "none";
        return `<rect x="${x}" y="${y}" width="${width}" height="${height}" fill="${escapeXML2(fill)}" ${common} />`;
      }
      if (shape.type === "circle") {
        return `<circle cx="${shape.cx ?? shape.x ?? 0}" cy="${shape.cy ?? shape.y ?? 0}" r="${shape.r ?? 24}" fill="${escapeXML2(shape.fill ?? "none")}" ${common} />`;
      }
      if (shape.type === "line" || shape.type === "arrow") {
        const marker = shape.type === "arrow" ? ' marker-end="url(#arrow-head)"' : "";
        return `<line x1="${shape.x1 ?? shape.x ?? 0}" y1="${shape.y1 ?? shape.y ?? 0}" x2="${shape.x2 ?? 0}" y2="${shape.y2 ?? 0}" fill="none" ${common}${marker} />`;
      }
      if (shape.type === "text") {
        return `<text x="${shape.x ?? 0}" y="${shape.y ?? shape.fontSize}" font-family="Arial, sans-serif" font-size="${shape.fontSize}" fill="${escapeXML2(shape.color)}" stroke="#ffffff" stroke-width="${Math.max(2, Math.round(shape.strokeWidth / 2))}" paint-order="stroke" opacity="${shape.opacity}">${escapeXML2(shape.text ?? shape.label ?? "")}</text>`;
      }
      return "";
    }
    function publicAnnotationShape2(shape) {
      return {
        type: shape.type,
        ...shape.x !== void 0 ? { x: shape.x } : {},
        ...shape.y !== void 0 ? { y: shape.y } : {},
        ...shape.width !== void 0 ? { width: shape.width } : {},
        ...shape.height !== void 0 ? { height: shape.height } : {},
        ...shape.cx !== void 0 ? { cx: shape.cx } : {},
        ...shape.cy !== void 0 ? { cy: shape.cy } : {},
        ...shape.r !== void 0 ? { r: shape.r } : {},
        ...shape.x1 !== void 0 ? { x1: shape.x1 } : {},
        ...shape.y1 !== void 0 ? { y1: shape.y1 } : {},
        ...shape.x2 !== void 0 ? { x2: shape.x2 } : {},
        ...shape.y2 !== void 0 ? { y2: shape.y2 } : {},
        ...shape.text !== void 0 ? { text: shape.text } : {},
        color: shape.color,
        stroke_width: shape.strokeWidth,
        opacity: shape.opacity
      };
    }
    async function resolveAnnotationOutputPath2(args, title) {
      const outputPath = stringParam3(args.output_path) ?? stringParam3(args.outputPath);
      if (outputPath) return { path: outputPath, location: "explicit_path" };
      const workspacePath52 = stringParam3(args.workspace_path) ?? stringParam3(args.workspacePath);
      if (workspacePath52) {
        return {
          path: await resolveWorkspaceFilePath2(stringParam3(args.workspaceDir), workspacePath52),
          location: "workspace_path"
        };
      }
      const dir = (0, import_path82.join)((0, import_os22.tmpdir)(), "movscript-mcp-artifacts");
      const filename = `${safeFilename2(title || "annotation")}-${(0, import_crypto22.randomUUID)().slice(0, 8)}.svg`;
      return { path: (0, import_path82.join)(dir, filename), location: "cache", cacheDir: dir };
    }
    async function loadUploadInput2(args) {
      const dataUrl = stringParam3(args.data_url) ?? stringParam3(args.dataUrl);
      if (dataUrl) {
        const decoded = decodeDataURL2(dataUrl);
        return {
          bytes: decoded.bytes,
          mimeType: decoded.mimeType,
          public: { kind: "data_url", mime_type: decoded.mimeType, size_bytes: decoded.bytes.length }
        };
      }
      const base64 = stringParam3(args.base64);
      if (base64) {
        const mimeType = normalizeImageMimeType2(stringParam3(args.mime_type) ?? stringParam3(args.mimeType));
        const bytes = Buffer.from(base64, "base64");
        return {
          bytes,
          mimeType,
          public: { kind: "base64", mime_type: mimeType, size_bytes: bytes.length }
        };
      }
      const workspacePath52 = stringParam3(args.workspace_path) ?? stringParam3(args.workspacePath);
      if (workspacePath52) {
        const path = await resolveWorkspaceFilePath2(stringParam3(args.workspaceDir), workspacePath52);
        const bytes = await (0, import_promises72.readFile)(path);
        const mimeType = normalizeImageMimeType2(stringParam3(args.mime_type) ?? stringParam3(args.mimeType) ?? mimeTypeFromFilename2(path));
        return {
          bytes,
          mimeType,
          filename: (0, import_path82.basename)(path),
          public: { kind: "workspace_path", path: workspacePath52, mime_type: mimeType, size_bytes: bytes.length }
        };
      }
      const localPath = stringParam3(args.local_path) ?? stringParam3(args.localPath) ?? stringParam3(args.artifact_path) ?? stringParam3(args.artifactPath) ?? stringParam3(args.path);
      if (localPath) {
        const fileStat = await (0, import_promises72.stat)(localPath);
        if (!fileStat.isFile()) throw new Error("local_path must point to a file");
        const bytes = await (0, import_promises72.readFile)(localPath);
        const mimeType = normalizeImageMimeType2(stringParam3(args.mime_type) ?? stringParam3(args.mimeType) ?? mimeTypeFromFilename2(localPath));
        return {
          bytes,
          mimeType,
          filename: (0, import_path82.basename)(localPath),
          public: { kind: "local_path", path: localPath, mime_type: mimeType, size_bytes: bytes.length }
        };
      }
      throw new Error("upload requires data_url, base64, workspace_path, artifact_path, or local_path");
    }
    async function resolveWorkspaceFilePath2(workspaceDir2, workspacePath52) {
      const rootDir = workspaceDir2?.trim() || await resolveDefaultMovScriptWorkspaceDir2();
      const workspaceRoot = resolveMovScriptWorkspaceRootPaths3(rootDir);
      ensureMovScriptWorkspaceRoot3(workspaceRoot);
      const rootPath = workspaceRoot.controlDir;
      const normalizedRelativePath = workspacePath52.replace(/^[/\\]+/, "");
      const absolutePath = (0, import_path82.resolve)(rootPath, normalizedRelativePath);
      const rootRelativePath = (0, import_path82.relative)(rootPath, absolutePath);
      if (rootRelativePath === "" || !rootRelativePath.startsWith(`..${import_path82.sep}`) && rootRelativePath !== ".." && !(0, import_path82.isAbsolute)(rootRelativePath)) {
        return absolutePath;
      }
      throw new Error("workspace_path must stay inside the MovScript workspace directory");
    }
    async function resolveDefaultMovScriptWorkspaceDir2() {
      return resolveMCPDefaultWorkspaceDir2();
    }
    function uploadFilename2(args, mimeType, sourceFilename) {
      const explicit = stringParam3(args.filename) ?? stringParam3(args.name);
      if (explicit) return explicit;
      const ext = (0, import_path82.extname)(sourceFilename ?? "") || extensionForMimeType2(mimeType);
      const base = sourceFilename ? safeFilename2((0, import_path82.basename)(sourceFilename, (0, import_path82.extname)(sourceFilename))) : `agent-guidance-${(0, import_crypto22.randomUUID)().slice(0, 8)}`;
      return `${base}${ext || ".png"}`;
    }
    function decodeDataURL2(value) {
      const match = value.match(/^data:([^;,]+)?(;base64)?,(.*)$/s);
      if (!match) throw new Error("data_url must be a valid data URL");
      const mimeType = match[1] || "image/png";
      const isBase64 = !!match[2];
      const payload = match[3] ?? "";
      return {
        mimeType,
        bytes: isBase64 ? Buffer.from(payload, "base64") : Buffer.from(decodeURIComponent(payload), "utf8")
      };
    }
    function parseImageSize2(bytes, mimeType) {
      if (mimeType === "image/png" && bytes.length >= 24 && bytes.toString("ascii", 1, 4) === "PNG") {
        return { width: bytes.readUInt32BE(16), height: bytes.readUInt32BE(20) };
      }
      if ((mimeType === "image/jpeg" || mimeType === "image/jpg") && bytes.length > 4) {
        return parseJPEGSize2(bytes);
      }
      if (mimeType === "image/gif" && bytes.length >= 10) {
        return { width: bytes.readUInt16LE(6), height: bytes.readUInt16LE(8) };
      }
      return void 0;
    }
    function parseJPEGSize2(bytes) {
      let offset = 2;
      while (offset + 9 < bytes.length) {
        if (bytes[offset] !== 255) return void 0;
        const marker = bytes[offset + 1];
        const length = bytes.readUInt16BE(offset + 2);
        if (marker !== void 0 && marker >= 192 && marker <= 195) {
          return { height: bytes.readUInt16BE(offset + 5), width: bytes.readUInt16BE(offset + 7) };
        }
        offset += 2 + length;
      }
      return void 0;
    }
    function resourceIdParam2(args) {
      const value = optionalResourceIdParam2(args);
      if (value === void 0) throw new Error("resource_id is required");
      return value;
    }
    function optionalResourceIdParam2(args) {
      const value = numberParam2(args.resource_id) ?? numberParam2(args.resourceId) ?? numberParam2(args.id);
      return value !== void 0 && value >= 1 ? Math.floor(value) : void 0;
    }
    function timestampsParam2(value) {
      if (!Array.isArray(value)) return [];
      return value.map(numberParam2).filter((item) => item !== void 0 && item >= 0).map(roundTime22);
    }
    function modeParam2(value) {
      return value === "overview" || value === "timestamps" || value === "range" || value === "burst" ? value : void 0;
    }
    function resourceImageReadModeParam2(args) {
      const mode = stringParam3(args.mode);
      if (mode === "original" || mode === "fit") return mode;
      if (mode) throw new Error('mode must be "fit" or "original"');
      const detail = stringParam3(args.detail);
      if (detail === "original") return "original";
      if (detail === void 0 || detail === "high" || detail === "auto" || detail === "low") return "fit";
      throw new Error('detail must be "original", "high", "auto", or "low"');
    }
    function shapeType2(value) {
      return value === "rect" || value === "circle" || value === "line" || value === "arrow" || value === "text" || value === "highlight" ? value : void 0;
    }
    function normalizeImageMimeType2(value) {
      if (!value) return "image/png";
      return value.split(";")[0]?.trim().toLowerCase() || "image/png";
    }
    function mimeTypeFromFilename2(filename) {
      switch ((0, import_path82.extname)(filename).toLowerCase()) {
        case ".jpg":
        case ".jpeg":
          return "image/jpeg";
        case ".svg":
          return "image/svg+xml";
        case ".webp":
          return "image/webp";
        case ".gif":
          return "image/gif";
        case ".mp4":
        case ".m4v":
          return "video/mp4";
        case ".mov":
          return "video/quicktime";
        case ".webm":
          return "video/webm";
        case ".mkv":
          return "video/x-matroska";
        case ".png":
        default:
          return "image/png";
      }
    }
    function extensionForMimeType2(mimeType) {
      switch (mimeType) {
        case "image/jpeg":
          return ".jpg";
        case "image/svg+xml":
          return ".svg";
        case "image/webp":
          return ".webp";
        case "image/gif":
          return ".gif";
        case "video/mp4":
          return ".mp4";
        case "video/quicktime":
          return ".mov";
        case "video/webm":
          return ".webm";
        case "video/x-matroska":
          return ".mkv";
        case "image/png":
        default:
          return ".png";
      }
    }
    function numericResourceId3(resource22) {
      if (!isRecord2122(resource22)) return void 0;
      return numberParam2(resource22.ID) ?? numberParam2(resource22.id);
    }
    function stringParam3(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberParam2(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) {
        const parsed = Number(value);
        return Number.isFinite(parsed) ? parsed : void 0;
      }
      return void 0;
    }
    function booleanParam32(value) {
      if (typeof value === "boolean") return value;
      if (typeof value !== "string") return void 0;
      if (value === "true") return true;
      if (value === "false") return false;
      return void 0;
    }
    function batchItems2(value, name) {
      if (!Array.isArray(value) || value.length === 0) throw new Error(`${name} must contain at least one item`);
      return value.map((item, index) => {
        if (!isRecord2122(item)) throw new Error(`${name}[${index}] must be an object`);
        return item;
      });
    }
    function batchDefaults22(args, excluded) {
      const defaults = {};
      for (const [key, value] of Object.entries(args)) {
        if (!excluded.has(key) && value !== void 0) defaults[key] = value;
      }
      return defaults;
    }
    async function runBatch2(items, concurrency, worker, continueOnError) {
      const results = new Array(items.length);
      let nextIndex = 0;
      let stopped = false;
      async function runWorker() {
        while (!stopped) {
          const index = nextIndex;
          nextIndex += 1;
          if (index >= items.length) return;
          const item = items[index];
          if (!item) return;
          try {
            results[index] = await worker(item, index);
          } catch (error) {
            results[index] = {
              index,
              status: "error",
              error: errorMessage33(error)
            };
            if (!continueOnError) stopped = true;
          }
        }
      }
      await Promise.all(Array.from({ length: Math.min(concurrency, items.length) }, runWorker));
      return results.map((result, index) => result ?? {
        index,
        status: "skipped",
        error: "Skipped because an earlier item failed and continue_on_error is false."
      });
    }
    function batchStatus2(successCount, failedCount) {
      if (failedCount === 0) return "completed";
      return successCount > 0 ? "partial_error" : "error";
    }
    function errorMessage33(error) {
      return error instanceof Error ? error.message : String(error);
    }
    function neutralVideoResourcePreparationCompatibility2() {
      return {
        kind: "neutral_resource_preparation",
        message: "This tool derives a reusable RawResource for references, drafts, or generation inputs. It is not the product editing path and does not update MediaEditingProject, candidates, selections, or shot state.",
        recommended_workflow: "For timeline trim, render, export, and candidate creation use MediaEditingProject plus editing_* through Electron mediaPipeline.",
        recommended_tool_family: "editing_*"
      };
    }
    function resourceLevelVideoUtility2() {
      return {
        kind: "resource_level_video_utility",
        message: "This tool composes generic video RawResources into another RawResource. It is not the product editing path.",
        recommended_workflow: "For product timeline editing, stitching, render, HLS packaging, export, and candidate creation use MediaEditingProject plus editing_* through Electron mediaPipeline.",
        recommended_tool_family: "editing_*"
      };
    }
    function clampInteger2(value, min, max) {
      if (!Number.isFinite(value)) return min;
      return Math.min(max, Math.max(min, Math.floor(value)));
    }
    function clampNumber22(value, min, max) {
      if (!Number.isFinite(value)) return min;
      return Math.min(max, Math.max(min, value));
    }
    function roundTime22(value) {
      return Math.round(value * 1e3) / 1e3;
    }
    function clampToDuration2(value, durationSec) {
      if (!durationSec || durationSec <= 0) return value;
      return Math.min(value, durationSec);
    }
    function uniqueSorted22(values) {
      return [...new Set(values.map(roundTime22))].sort((left, right) => left - right);
    }
    function positiveInteger22(value) {
      return typeof value === "number" && Number.isInteger(value) && value > 0 ? value : void 0;
    }
    function positiveNumber222(value) {
      return typeof value === "number" && Number.isFinite(value) && value > 0 ? value : void 0;
    }
    function parseFrameRate2(value) {
      if (!value || value === "0/0") return void 0;
      const [num, den] = value.split("/").map(Number);
      if (!Number.isFinite(num) || !Number.isFinite(den) || !num || !den) return void 0;
      return Math.round(num / den * 1e3) / 1e3;
    }
    function safeFilename2(value) {
      const normalized = value.trim().replace(/\.[^.]+$/, "") || "agent-guidance";
      return normalized.replace(/[^a-zA-Z0-9._-]+/g, "-").replace(/^-+|-+$/g, "") || "agent-guidance";
    }
    function escapeXML2(value) {
      return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&apos;");
    }
    function isRecord2122(value) {
      return !!value && typeof value === "object" && !Array.isArray(value);
    }
    async function mkdtempStable2(prefix) {
      const dir = (0, import_path82.join)((0, import_os22.tmpdir)(), `${prefix}${(0, import_crypto22.randomUUID)().slice(0, 8)}`);
      await (0, import_promises72.mkdir)(dir, { recursive: true });
      return dir;
    }
    function listResourceMediaResources2() {
      return [
        {
          uri: "movscript://resource-file/{resource_id}",
          name: "MovScript resource file",
          description: "Dynamic binary RawResource reader. Replace {resource_id} with an ID, for example movscript://resource-file/42?maxBytes=8388608. Use image/video media tools for Codex vision workflows.",
          mimeType: "application/octet-stream"
        }
      ];
    }
    var resourceMediaResourceReaders2 = [
      readResourceMediaResource2
    ];
    async function readResourceMediaResource2(uri) {
      const resourceFile = parseResourceFileURI2(uri);
      if (!resourceFile) return null;
      const file = await readResourceFileBlob2(resourceFile.resourceId, { maxBytes: resourceFile.maxBytes });
      return {
        contents: [
          {
            uri,
            mimeType: file.mimeType,
            blob: file.blob
          }
        ],
        data: {
          resource_id: resourceFile.resourceId,
          mime_type: file.mimeType,
          size_bytes: file.sizeBytes
        }
      };
    }
    function parseResourceFileURI2(uri) {
      const match = uri.match(/^movscript:\/\/resource-file\/(\d+)(?:\?(.*))?$/);
      if (!match) return null;
      const params = new URLSearchParams(match[2] ?? "");
      const maxBytes = numericParam32(params.get("maxBytes") ?? params.get("max_bytes"));
      return {
        resourceId: Number(match[1]),
        ...maxBytes !== void 0 ? { maxBytes } : {}
      };
    }
    function numericParam32(value) {
      if (!value?.trim()) return void 0;
      const parsed = Number(value);
      return Number.isFinite(parsed) ? parsed : void 0;
    }
    async function listExternalResourceSources2(args) {
      const includeDisabled = booleanParam42(args.include_disabled) ?? booleanParam42(args.includeDisabled) ?? false;
      const sources = await fetchExternalResourceSources2();
      const filtered = includeDisabled ? sources : sources.filter((source) => source.is_enabled !== false);
      return {
        source: "movscript_external_resource_sources",
        count: filtered.length,
        items: filtered.map(summarizeExternalResourceSource2),
        usage: "Pass an enabled source ID to movscript_external_resource_search.source_id, or omit source_id to use the first enabled source."
      };
    }
    async function searchExternalResources2(args) {
      const sourceId = getOptionalNumeric2(args, "source_id") ?? getOptionalNumeric2(args, "sourceId") ?? await defaultExternalResourceSourceId2();
      const query = getOptionalString3(args, "query") ?? getOptionalString3(args, "q");
      if (!query) throw new Error("query is required");
      if (!sourceId) throw new Error("No enabled external resource source is configured");
      const mediaType = getOptionalString3(args, "media_type") ?? getOptionalString3(args, "mediaType");
      const orientation = getOptionalString3(args, "orientation");
      const page = Math.floor(clampNumber3(getOptionalNumeric2(args, "page") ?? 1, 1, 1e4));
      const pageSize = Math.floor(clampNumber3(
        getOptionalNumeric2(args, "page_size") ?? getOptionalNumeric2(args, "pageSize") ?? getOptionalNumeric2(args, "limit") ?? 20,
        1,
        80
      ));
      const result = await fetchExternalResourceSearch2({ sourceId, query, mediaType, orientation, page, pageSize });
      const items = Array.isArray(result.items) ? result.items : [];
      return {
        source: "movscript_external_resources",
        source_id: sourceId,
        query,
        ...mediaType ? { media_type: mediaType } : {},
        ...orientation ? { orientation } : {},
        page: numberField322(result, "page") ?? page,
        pageSize: numberField322(result, "page_size") ?? pageSize,
        total: numberField322(result, "total") ?? items.length,
        count: items.length,
        provider: typeof result.provider === "string" ? result.provider : void 0,
        source_name: typeof result.source_name === "string" ? result.source_name : void 0,
        next_page: typeof result.next_page === "string" ? result.next_page : void 0,
        items: items.map(summarizeExternalResourceItem2),
        usage: "These are external search results, not MovScript RawResources. Import through MovScript before using them as generation input_resource_ids."
      };
    }
    async function readExternalResources2(uri) {
      const parsed = parseExternalResourcesURI2(uri);
      if (!parsed) throw new Error(`Unsupported external resource URI: ${uri}`);
      if (!parsed.query) return listExternalResourceSources2({});
      return searchExternalResources2(parsed);
    }
    function isExternalResourcesURI2(uri) {
      return parseExternalResourcesURI2(uri) !== null;
    }
    function parseExternalResourcesURI2(uri) {
      const match = uri.match(/^movscript:\/\/external-resources(?:\?(.*))?$/);
      if (!match) return null;
      const params = new URLSearchParams(match[1] ?? "");
      return {
        ...params.get("query") || params.get("q") ? { query: params.get("query") ?? params.get("q") ?? "" } : {},
        ...numericParam42(params.get("source_id") ?? params.get("sourceId")) !== void 0 ? { source_id: numericParam42(params.get("source_id") ?? params.get("sourceId")) } : {},
        ...params.get("media_type") || params.get("mediaType") ? { media_type: params.get("media_type") ?? params.get("mediaType") ?? "" } : {},
        ...params.get("orientation") ? { orientation: params.get("orientation") ?? "" } : {},
        ...numericParam42(params.get("page")) !== void 0 ? { page: numericParam42(params.get("page")) } : {},
        ...numericParam42(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) !== void 0 ? { page_size: numericParam42(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) } : {}
      };
    }
    async function defaultExternalResourceSourceId2() {
      const sources = await fetchExternalResourceSources2();
      const enabled = sources.find((source) => source.is_enabled !== false);
      return numericField2(enabled, "ID") ?? numericField2(enabled, "id");
    }
    async function fetchExternalResourceSources2() {
      const data = await backendGet222("/external-resource-sources");
      return Array.isArray(data) ? data.filter(isRecord342) : [];
    }
    async function fetchExternalResourceSearch2(input3) {
      const params = new URLSearchParams();
      params.set("source_id", String(input3.sourceId));
      params.set("q", input3.query);
      params.set("page", String(input3.page));
      params.set("page_size", String(input3.pageSize));
      if (input3.mediaType?.trim()) params.set("media_type", input3.mediaType.trim());
      if (input3.orientation?.trim() && input3.orientation !== "all") params.set("orientation", input3.orientation.trim());
      const data = await backendGet222(`/external-resources/search?${params.toString()}`);
      return isRecord342(data) ? data : { items: [] };
    }
    function summarizeExternalResourceSource2(item) {
      return picked32(item, ["ID", "id", "name", "provider_key", "priority", "is_enabled", "CreatedAt", "UpdatedAt"]);
    }
    function summarizeExternalResourceItem2(item) {
      if (!isRecord342(item)) return item;
      return picked32(item, ["provider_key", "external_id", "media_type", "title", "description", "thumbnail_url", "preview_url", "source_url", "width", "height", "duration_seconds", "author_name", "author_url", "attribution_text", "license_label"]);
    }
    function picked32(record, keys) {
      const result = {};
      for (const key of keys) {
        if (record[key] !== void 0) result[key] = truncateText22(record[key]);
      }
      return result;
    }
    function truncateText22(value) {
      if (typeof value !== "string") return value;
      return value.length > 1200 ? `${value.slice(0, 1200)}...` : value;
    }
    function numericField2(record, key) {
      if (!record) return void 0;
      return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
    }
    function numberField322(record, key) {
      return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
    }
    function numericParam42(value) {
      if (!value?.trim()) return void 0;
      const parsed = Number(value);
      return Number.isFinite(parsed) ? parsed : void 0;
    }
    function booleanParam42(value) {
      if (typeof value === "boolean") return value;
      if (typeof value !== "string") return void 0;
      if (value === "true") return true;
      if (value === "false") return false;
      return void 0;
    }
    function listExternalResourceResources2() {
      return [
        {
          uri: "movscript://external-resources",
          name: "External media search sources",
          description: "Configured external image/video providers. Use movscript_external_resource_search for provider search; import results before generation.",
          mimeType: "text/markdown"
        }
      ];
    }
    var externalResourceReaders2 = [
      readExternalResourceResource2
    ];
    async function readExternalResourceResource2(uri) {
      if (!isExternalResourcesURI2(uri)) return null;
      return resourceContent2(uri, await readExternalResources2(uri));
    }
    var resourceReaders2 = [
      ...focusResourceReaders2,
      ...projectResourceReaders2,
      ...shotLibraryResourceReaders2,
      ...resourceLibraryResourceReaders2,
      ...resourceMediaResourceReaders2,
      ...externalResourceReaders2
    ];
    function listResources2() {
      return [
        ...listFocusResources2(),
        ...listProjectResources2(),
        ...listShotLibraryResources2(),
        ...listResourceLibraryResources2(),
        ...listResourceMediaResources2(),
        ...listExternalResourceResources2()
      ];
    }
    async function readResource2(uri) {
      for (const reader of resourceReaders2) {
        const result = await reader(uri);
        if (result) return result;
      }
      throw new Error(`Unsupported resource URI: ${uri}`);
    }
    function getFocus2() {
      const startedAt = Date.now();
      const focusMs = Date.now() - startedAt;
      return {
        focus: getMCPFocusSnapshot2(),
        timings: {
          totalMs: focusMs,
          focusMs
        }
      };
    }
    var currentEditingRuntimePort2;
    function getEditingRuntimePort2() {
      return currentEditingRuntimePort2;
    }
    async function artifactUploadExport2(args) {
      const taskResolution = await resolveExportUploadTask2(args);
      if ("diagnostic" in taskResolution) return taskResolution.diagnostic;
      const request = await exportUploadRequest2(args, taskResolution.task);
      if (isHlsManifestExportRequest2(request)) {
        return {
          status: "unsupported_output",
          code: "USE_SYSTEM_ARTIFACT_UPLOAD_HLS_STREAM",
          message: "Output is an HLS manifest. Use system_artifact_upload_hls_stream for HLS artifacts instead of uploading it as a RawResource.",
          outputPath: request.outputPath,
          output_path: request.outputPath,
          filename: request.filename,
          mimeType: request.mimeType,
          mime_type: request.mimeType
        };
      }
      const info = await (0, import_promises82.stat)(request.outputPath);
      if (!info.isFile()) throw new Error(`ARTIFACT_EXPORT_FILE_INVALID: ${request.outputPath} is not a file.`);
      const bytes = await (0, import_promises82.readFile)(request.outputPath);
      const form = new FormData();
      form.append("file", new Blob([new Uint8Array(bytes)], { type: request.mimeType }), request.filename);
      appendFormValue2(form, "folder_id", request.folderId);
      const derivative = exportDerivativePayload3(args);
      if (derivative) form.append("derivative", JSON.stringify(derivative));
      const response = await backendPostMultipart2("/resources/upload", form);
      const resourceId = numericResourceId22(response);
      if (resourceId === void 0) {
        throw new Error("ARTIFACT_EXPORT_UPLOAD_RESPONSE_INVALID: resource upload response did not include a valid resource ID.");
      }
      return {
        status: "ok",
        resourceId,
        resource_id: resourceId,
        resource: response,
        outputPath: request.outputPath,
        output_path: request.outputPath,
        filename: request.filename,
        mimeType: request.mimeType,
        mime_type: request.mimeType,
        ...request.folderId !== void 0 ? { folderId: request.folderId, folder_id: request.folderId } : {}
      };
    }
    async function artifactUploadHlsStream2(args) {
      const taskId = stringValue322(args.taskId ?? args.task_id);
      const runtime = getEditingRuntimePort2();
      if (taskId && runtime?.publishHlsStream) {
        const task = runtime.getTask ? await runtime.getTask(taskId, { projectId: stringValue322(args.projectId ?? args.project_id) }) : void 0;
        const hasExplicitManifest = !!stringValue322(args.manifestPath ?? args.manifest_path);
        const hasExplicitSegments = !!stringList3(args.segmentPaths ?? args.segment_paths)?.length;
        if (!task && !hasExplicitManifest) {
          return {
            status: "not_found",
            task_id: taskId,
            message: "No Electron mediaPipeline task was found for taskId. Pass projectId with taskId for persisted workspace recovery, or provide manifestPath and segmentPaths explicitly."
          };
        }
        if (isRecord37(task) && !hasExplicitManifest && !hasExplicitSegments) {
          const manifestPath = stringValue322(task.hlsManifestPath ?? task.hls_manifest_path ?? task.outputPath);
          const segmentPaths = stringList3(task.hlsSegmentPaths ?? task.hls_segment_paths);
          if (!manifestPath || !segmentPaths?.length) {
            return {
              status: "pending_output",
              task_id: taskId,
              message: "The Electron mediaPipeline task does not have a complete HLS manifest/segment output yet.",
              task
            };
          }
        }
        const request2 = hlsPublishRequest2(args, task);
        return runtime.publishHlsStream(request2);
      }
      const request = hlsPublishRequest2(args);
      const form = new FormData();
      const manifest = await (0, import_promises82.readFile)(request.manifestPath);
      form.append("manifest", new Blob([new Uint8Array(manifest)], { type: "application/vnd.apple.mpegurl" }), (0, import_path92.basename)(request.manifestPath));
      for (const segmentPath of request.segmentPaths ?? []) {
        const bytes = await (0, import_promises82.readFile)(segmentPath);
        form.append("segments", new Blob([new Uint8Array(bytes)], { type: mimeTypeForFilename2(segmentPath) }), (0, import_path92.basename)(segmentPath));
      }
      appendFormValue2(form, "title", request.title);
      appendFormValue2(form, "task_id", request.task_id ?? request.taskId);
      appendFormValue2(form, "project_id", request.project_id ?? request.projectId);
      appendFormValue2(form, "source_resource_id", request.source_resource_id ?? request.sourceResourceId);
      appendFormValue2(form, "source_derivative_id", request.source_derivative_id ?? request.sourceDerivativeId);
      appendFormValue2(form, "duration_ms", request.duration_ms ?? request.durationMs);
      appendFormValue2(form, "width", request.width);
      appendFormValue2(form, "height", request.height);
      appendFormValue2(form, "expires_at", stringValue322(args.expiresAt ?? args.expires_at));
      appendFormValue2(form, "expires_in_seconds", numberValue322(args.expiresInSeconds ?? args.expires_in_seconds));
      const response = await backendPostMultipart2("/media/streams/uploads", form);
      return hlsUploadResult2(response);
    }
    async function artifactGetStream2(args) {
      const streamId = stringOrNumberValue4(args.streamId ?? args.stream_id ?? args.id);
      if (streamId === void 0) throw new Error("streamId is required");
      const response = await backendGet222(`/media/streams/${encodeURIComponent(String(streamId))}`);
      const record = response && typeof response === "object" ? response : {};
      const stream = record.stream ?? record.media_stream ?? response;
      return {
        status: "ok",
        streamId: numericId32(response) ?? numericId32(stream) ?? Number(streamId),
        stream_id: numericId32(response) ?? numericId32(stream) ?? Number(streamId),
        stream,
        media_stream: stream,
        manifestUrl: stringValue322(record.manifest_url ?? record.manifestUrl),
        manifest_url: stringValue322(record.manifest_url ?? record.manifestUrl),
        presignedManifestUrl: stringValue322(record.presigned_manifest_url ?? record.presignedManifestUrl),
        presigned_manifest_url: stringValue322(record.presigned_manifest_url ?? record.presignedManifestUrl),
        segmentBaseUrl: stringValue322(record.segment_base_url ?? record.segmentBaseUrl),
        segment_base_url: stringValue322(record.segment_base_url ?? record.segmentBaseUrl),
        segments: record.segments
      };
    }
    async function resolveExportUploadTask2(args) {
      const taskId = stringValue322(args.taskId ?? args.task_id);
      const explicitOutputPath = stringValue322(args.outputPath ?? args.output_path ?? args.filePath ?? args.file_path);
      if (!taskId || explicitOutputPath) return {};
      const runtime = getEditingRuntimePort2();
      if (!runtime?.getTask) {
        return {
          diagnostic: {
            status: "unsupported_runtime",
            code: "ELECTRON_EDITING_RUNTIME_REQUIRED",
            task_id: taskId,
            message: "Resolving an Electron mediaPipeline task output requires the Electron runtime. Provide outputPath/filePath explicitly, or run this task inside MovScript Desktop with projectId and taskId."
          }
        };
      }
      const task = await runtime.getTask(taskId, { projectId: stringValue322(args.projectId ?? args.project_id) });
      if (!isRecord37(task)) {
        return {
          diagnostic: {
            status: "not_found",
            task_id: taskId,
            message: "No Electron mediaPipeline task was found for taskId. Pass projectId with taskId for persisted workspace recovery, or provide outputPath/filePath explicitly."
          }
        };
      }
      const outputPath = stringValue322(task.outputPath ?? task.output_path);
      if (!outputPath) {
        return {
          diagnostic: {
            status: "pending_output",
            task_id: taskId,
            message: "The Electron mediaPipeline task does not have a local export output path yet.",
            task
          }
        };
      }
      return { task };
    }
    async function exportUploadRequest2(args, task) {
      const outputPath = stringValue322(args.outputPath ?? args.output_path ?? args.filePath ?? args.file_path) ?? stringValue322(task?.outputPath ?? task?.output_path);
      if (!outputPath) throw new Error("outputPath is required");
      const filename = stringValue322(args.filename) ?? stringValue322(task?.filename) ?? (0, import_path92.basename)(outputPath);
      const mimeType = stringValue322(args.mimeType ?? args.mime_type) ?? stringValue322(task?.mimeType ?? task?.mime_type) ?? mimeTypeForFilename2(filename);
      return {
        outputPath,
        filename,
        mimeType,
        ...stringOrNumberValue4(args.folderId ?? args.folder_id) !== void 0 ? { folderId: stringOrNumberValue4(args.folderId ?? args.folder_id) } : {}
      };
    }
    function exportDerivativePayload3(args) {
      const explicit = args.derivative;
      if (isRecord37(explicit)) {
        const operation2 = stringValue322(explicit.operation);
        if (!operation2) return void 0;
        const inputIds2 = numericIdList3(explicit.input_resource_ids ?? explicit.inputResourceIds);
        return {
          operation: operation2,
          ...stringValue322(explicit.tool) ? { tool: stringValue322(explicit.tool) } : {},
          ...inputIds2.length ? { input_resource_ids: inputIds2 } : {},
          ...isRecord37(explicit.params) ? { params: explicit.params } : {}
        };
      }
      const operation = stringValue322(args.operation);
      const inputIds = [
        ...numericIdList3(args.inputResourceIds ?? args.input_resource_ids),
        ...numericIdList3(args.sourceResourceIds ?? args.source_resource_ids),
        ...numericIdList3(args.sourceResourceId ?? args.source_resource_id)
      ];
      if (!operation && !inputIds.length && !isRecord37(args.params)) return void 0;
      return {
        operation: operation ?? "artifact_export",
        ...stringValue322(args.tool) ? { tool: stringValue322(args.tool) } : { tool: "system_artifact_upload_export" },
        ...inputIds.length ? { input_resource_ids: Array.from(new Set(inputIds)) } : {},
        ...isRecord37(args.params) ? { params: args.params } : {}
      };
    }
    function hlsPublishRequest2(args, taskValue) {
      const task = isRecord37(taskValue) ? taskValue : void 0;
      const manifestPath = stringValue322(args.manifestPath ?? args.manifest_path) ?? stringValue322(task?.hlsManifestPath ?? task?.hls_manifest_path ?? task?.outputPath);
      const segmentPaths = stringList3(args.segmentPaths ?? args.segment_paths) ?? stringList3(task?.hlsSegmentPaths ?? task?.hls_segment_paths);
      if (!manifestPath) throw new Error("manifestPath is required");
      if (!segmentPaths?.length) throw new Error("segmentPaths is required");
      const request = {
        manifestPath,
        manifest_path: manifestPath,
        segmentPaths,
        segment_paths: segmentPaths
      };
      const taskId = stringValue322(args.taskId ?? args.task_id);
      const title = stringValue322(args.title);
      const projectId = stringOrNumberValue4(args.projectId ?? args.project_id);
      const sourceResourceId2 = stringOrNumberValue4(args.sourceResourceId ?? args.source_resource_id);
      const sourceDerivativeId = stringOrNumberValue4(args.sourceDerivativeId ?? args.source_derivative_id);
      const durationMs = numberValue322(args.durationMs ?? args.duration_ms);
      const width = numberValue322(args.width);
      const height = numberValue322(args.height);
      if (taskId) {
        request.taskId = taskId;
        request.task_id = taskId;
      }
      if (title) request.title = title;
      if (projectId !== void 0) {
        request.projectId = projectId;
        request.project_id = projectId;
      }
      if (sourceResourceId2 !== void 0) {
        request.sourceResourceId = sourceResourceId2;
        request.source_resource_id = sourceResourceId2;
      }
      if (sourceDerivativeId !== void 0) {
        request.sourceDerivativeId = sourceDerivativeId;
        request.source_derivative_id = sourceDerivativeId;
      }
      if (durationMs !== void 0) {
        request.durationMs = durationMs;
        request.duration_ms = durationMs;
      }
      if (width !== void 0) request.width = width;
      if (height !== void 0) request.height = height;
      return request;
    }
    function hlsUploadResult2(response) {
      const record = response && typeof response === "object" ? response : {};
      const stream = record.stream ?? record.media_stream ?? response;
      const streamId = numericId32(response) ?? numericId32(stream);
      if (streamId === void 0) {
        throw new Error("HLS_STREAM_UPLOAD_RESPONSE_INVALID: response did not include a valid stream ID.");
      }
      const manifestUrl = stringValue322(record.manifest_url ?? record.manifestUrl);
      const presignedManifestUrl = stringValue322(record.presigned_manifest_url ?? record.presignedManifestUrl);
      const segmentBaseUrl = stringValue322(record.segment_base_url ?? record.segmentBaseUrl);
      return {
        status: "ok",
        streamId,
        stream_id: streamId,
        stream,
        media_stream: stream,
        ...manifestUrl ? { manifestUrl, manifest_url: manifestUrl } : {},
        ...presignedManifestUrl ? { presignedManifestUrl, presigned_manifest_url: presignedManifestUrl } : {},
        ...segmentBaseUrl ? { segmentBaseUrl, segment_base_url: segmentBaseUrl } : {},
        segments: record.segments
      };
    }
    function mimeTypeForFilename2(filename) {
      const lower = filename.toLowerCase();
      if (lower.endsWith(".m3u8")) return "application/vnd.apple.mpegurl";
      if (lower.endsWith(".mp4")) return "video/mp4";
      if (lower.endsWith(".m4s")) return "video/iso.segment";
      if (lower.endsWith(".ts")) return "video/mp2t";
      if (lower.endsWith(".aac")) return "audio/aac";
      if (lower.endsWith(".m4v")) return "video/mp4";
      if (lower.endsWith(".mov")) return "video/quicktime";
      if (lower.endsWith(".webm")) return "video/webm";
      if (lower.endsWith(".mp3")) return "audio/mpeg";
      if (lower.endsWith(".wav")) return "audio/wav";
      if (lower.endsWith(".vtt")) return "text/vtt";
      if (lower.endsWith(".srt")) return "application/x-subrip";
      if (lower.endsWith(".ass")) return "text/x-ssa";
      return "application/octet-stream";
    }
    function isHlsManifestExportRequest2(request) {
      const outputPath = request.outputPath.toLowerCase();
      const filename = request.filename.toLowerCase();
      const mimeType = request.mimeType.toLowerCase();
      return outputPath.endsWith(".m3u8") || filename.endsWith(".m3u8") || mimeType === "application/vnd.apple.mpegurl" || mimeType === "application/x-mpegurl";
    }
    function appendFormValue2(form, key, value) {
      if (value === void 0 || value === null || String(value).trim() === "") return;
      form.append(key, String(value));
    }
    function stringValue322(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function stringOrNumberValue4(value) {
      if (typeof value === "string" && value.trim()) return value.trim();
      if (typeof value === "number" && Number.isFinite(value)) return value;
      return void 0;
    }
    function numberValue322(value) {
      const n = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
      return Number.isFinite(n) ? n : void 0;
    }
    function stringList3(value) {
      if (!Array.isArray(value)) return void 0;
      const out = value.flatMap((item) => typeof item === "string" && item.trim() ? [item.trim()] : []);
      return out.length ? out : void 0;
    }
    function numericId32(value) {
      if (!value || typeof value !== "object") return void 0;
      const record = value;
      const raw = record.ID ?? record.id ?? record.stream_id ?? record.streamId;
      const n = typeof raw === "number" ? raw : typeof raw === "string" ? Number(raw) : NaN;
      return Number.isInteger(n) && n > 0 ? n : void 0;
    }
    function numericResourceId22(value) {
      if (!value || typeof value !== "object") return void 0;
      const record = value;
      const raw = record.ID ?? record.id ?? record.resource_id ?? record.resourceId;
      const n = typeof raw === "number" ? raw : typeof raw === "string" ? Number(raw) : NaN;
      return Number.isInteger(n) && n > 0 ? n : void 0;
    }
    function numericIdList3(value) {
      const values = Array.isArray(value) ? value : value === void 0 || value === null ? [] : [value];
      return values.flatMap((item) => {
        const n = typeof item === "number" ? item : typeof item === "string" ? Number(item) : NaN;
        return Number.isInteger(n) && n > 0 ? [n] : [];
      });
    }
    function isRecord37(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    var STANDALONE_EDITING_PROJECT_ID2 = "standalone";
    async function editingServiceProjectCommand2(command, input3) {
      return (await createEditingServiceClientFromRuntime2().projectCommand({ command, input: input3 })).result;
    }
    async function editingServiceTaskRequest2(taskType, input3) {
      const response = await createEditingServiceClientFromRuntime2().taskRequest({ taskType, input: input3 });
      if (!isRecord342(response.request)) throw new Error("editing service did not return a task request");
      return response.request;
    }
    async function editingServiceTaskAction2(action, input3) {
      const response = await createEditingServiceClientFromRuntime2().taskAction({ action, input: input3 });
      return {
        status: response.status,
        result: response.result,
        request: isRecord342(response.request) ? response.request : void 0
      };
    }
    async function editingServiceRuntimeTaskAction2(action, input3) {
      const envelope = await editingServiceTaskAction2(action, input3);
      const request = envelope.request;
      if (!request) throw new Error("editing service did not return a task action request");
      const taskId = stringValue422(request.taskId ?? request.task_id);
      if (!taskId) throw new Error("editing service task action request did not include taskId");
      const options = isRecord342(request.options) ? request.options : {};
      return {
        taskId,
        options: {
          ...stringValue422(options.projectId ?? options.project_id) ? { projectId: stringValue422(options.projectId ?? options.project_id) } : {}
        }
      };
    }
    async function editingProjectCreate2(args) {
      const result = await editingServiceProjectCommand2("createProject", args);
      return persistCreatedEditingProject2(editingProjectFromServiceResult2(result), result);
    }
    async function persistCreatedEditingProject2(editingProject, fallbackResult) {
      const saved = await editingServiceProjectCommand2("saveProject", {
        editingProject
      });
      if (isRecord342(saved)) {
        const savedProject = saved.editingProject ?? saved.editing_project ?? editingProject;
        return {
          ...saved,
          editingProject: savedProject,
          editing_project: savedProject
        };
      }
      return fallbackResult;
    }
    async function editingProjectCreateFromEditPlan2(args) {
      const result = await editingServiceProjectCommand2("createProjectFromEditPlan", args);
      return persistCreatedEditingProject2(editingProjectFromServiceResult2(result), result);
    }
    async function editingProjectCreateFromEditDecisions2(args) {
      const result = await editingServiceProjectCommand2("createProjectFromEditDecisions", args);
      return persistCreatedEditingProject2(editingProjectFromServiceResult2(result), result);
    }
    async function editingVideoCompose2(args) {
      const inputEditDecisions = objectArg2(args, "editDecisions") ?? objectArg2(args, "edit_decisions");
      const renderRuntime = normalizeRenderRuntime2(args.renderRuntime ?? args.render_runtime ?? inputEditDecisions?.render_runtime ?? inputEditDecisions?.renderRuntime);
      if (!renderRuntime.supported) {
        return {
          status: "unsupported_runtime",
          code: "VIDEO_COMPOSE_RENDER_RUNTIME_UNSUPPORTED",
          message: `MovScript video compose currently supports movscript_media_pipeline/ffmpeg only. Requested runtime ${renderRuntime.value} must be handled by an explicit future adapter; no silent fallback was performed.`,
          render_runtime: renderRuntime.value,
          supported_render_runtimes: ["movscript_media_pipeline", "ffmpeg"]
        };
      }
      const composed = await composeEditingProjectWithCompile2(args, renderRuntime.value);
      const editingProject = composed.editingProject;
      const compileResult = composed.compileResult;
      if (!editingProject) {
        return {
          status: "blocked",
          code: "VIDEO_COMPOSE_COMPILE_BLOCKED",
          message: "Video compose did not start render because TimelineAssembly compile has blockers.",
          render_runtime: renderRuntime.value,
          ...compileResult ? {
            compile_manifest: compileResult.compile_manifest,
            compile_result: compileResult,
            diagnostics: compileResult.diagnostics
          } : {}
        };
      }
      const validation = await editingServiceProjectCommand2("validateTimeline", {
        editingProject
      });
      if (timelineValidationHasErrors2(validation)) {
        return {
          status: "blocked",
          code: "VIDEO_COMPOSE_TIMELINE_INVALID",
          message: "Video compose did not start render because the MediaEditingProject timeline has validation errors.",
          render_runtime: renderRuntime.value,
          editing_project: editingProject,
          ...compileResult ? { compile_manifest: compileResult.compile_manifest, compile_result: compileResult } : {},
          validation
        };
      }
      const output3 = isRecord342(args.output) ? args.output : {};
      const format = stringValue422(output3.format ?? args.format) === "hls" ? "hls" : "mp4";
      const task = await mediaPipelineTaskCreate2(format === "hls" ? "timeline_hls" : "timeline_render", {
        ...args,
        editingProject,
        projectId: projectIdValue3(args) ?? editingProject.projectId,
        output: {
          ...output3,
          format,
          ...output3.importToResource !== void 0 || output3.import_to_resource !== void 0 ? {} : booleanValue22(args.importToResource ?? args.import_to_resource) === void 0 ? {} : { importToResource: booleanValue22(args.importToResource ?? args.import_to_resource) }
        }
      });
      const taskRecord = isRecord342(task) ? task : {};
      const mediaTask = isRecord342(taskRecord.task) ? taskRecord.task : void 0;
      return {
        ...task,
        render_runtime: renderRuntime.value,
        render_runtime_used: renderRuntime.value === "ffmpeg" ? "movscript_media_pipeline_ffmpeg" : renderRuntime.value,
        format,
        editing_project: editingProject,
        ...compileResult ? { compile_manifest: compileResult.compile_manifest, compile_result: compileResult } : {},
        validation,
        render_report: {
          schema: "movscript.render_report.v1",
          status: stringValue422(task.status) ?? "unknown",
          render_runtime: renderRuntime.value,
          render_runtime_used: renderRuntime.value === "ffmpeg" ? "movscript_media_pipeline_ffmpeg" : renderRuntime.value,
          format,
          project_id: editingProject.projectId,
          editing_project_id: editingProject.id,
          compile_manifest_id: compileResult?.compile_manifest.id,
          compile_input_hash: compileResult?.compile_manifest.input_hash,
          task_id: stringValue422(mediaTask?.taskId ?? mediaTask?.task_id),
          output_path: stringValue422(mediaTask?.outputPath ?? mediaTask?.output_path),
          output_resource_id: optionalNumber3(mediaTask?.outputResourceId ?? mediaTask?.output_resource_id),
          candidate_created: false,
          adopted: false,
          selected: false
        },
        candidate_created: false,
        adopted: false,
        selected: false
      };
    }
    async function editingProjectAddAsset2(args) {
      return editingServiceProjectCommand2("addAsset", args);
    }
    async function editingProjectRemoveAsset2(args) {
      return editingServiceProjectCommand2("removeAsset", args);
    }
    async function editingProjectSave2(args) {
      const project = editingProjectArg3(args);
      const expectedRevision = getOptionalNumeric2(args, "expectedRevision") ?? getOptionalNumeric2(args, "expected_revision");
      return editingServiceProjectCommand2("saveProject", {
        editingProject: project,
        ...expectedRevision !== void 0 ? { expectedRevision } : {}
      });
    }
    async function editingProjectGet2(args) {
      const projectId = projectIdValue3(args) ?? STANDALONE_EDITING_PROJECT_ID2;
      const editingProjectId = editingProjectIdValue3(args);
      return editingServiceProjectCommand2("getProject", { projectId, editingProjectId });
    }
    async function editingProjectUpdateSettings2(args) {
      return editingServiceProjectCommand2("updateProjectSettings", args);
    }
    async function editingTimelineApplyCommands2(args) {
      return editingServiceProjectCommand2("applyTimelineCommands", args);
    }
    async function editingTimelineValidate2(args) {
      return editingServiceProjectCommand2("validateTimeline", args);
    }
    async function editingTimelineAddTrack2(args) {
      return editingServiceProjectCommand2("addTrack", args);
    }
    async function editingTimelineRemoveTrack2(args) {
      return editingServiceProjectCommand2("removeTrack", args);
    }
    async function editingTimelineAddClip2(args) {
      return editingServiceProjectCommand2("addClip", args);
    }
    async function editingTimelineUpdateClip2(args) {
      return editingServiceProjectCommand2("updateClip", args);
    }
    async function editingTimelineSplitClip2(args) {
      return editingServiceProjectCommand2("splitClip", args);
    }
    async function editingTimelineMoveClip2(args) {
      return editingServiceProjectCommand2("moveClip", args);
    }
    async function editingTimelineDeleteClip2(args) {
      return editingServiceProjectCommand2("deleteClip", args);
    }
    async function editingRuntimeCapabilitiesGet2(args) {
      const runtime = getEditingRuntimePort2();
      if (!runtime?.getCapabilities) return editingRuntimeRequired2(args);
      return runtime.getCapabilities();
    }
    async function editingTaskRenderCreate2(args) {
      return mediaPipelineTaskCreate2("timeline_render", args);
    }
    async function editingTaskHlsCreate2(args) {
      return mediaPipelineTaskCreate2("timeline_hls", args);
    }
    async function editingTaskTranscodeCreate2(args) {
      return editingTaskSourceCreate2(args, "media_transcode");
    }
    async function editingTaskReframeCreate2(args) {
      return editingTaskSourceCreate2(args, "media_reframe");
    }
    async function editingTaskGet2(args) {
      const action = await editingServiceRuntimeTaskAction2("getTask", args);
      const serviceResponse = await mediaPipelineTaskAction2("getTask", action);
      if (serviceResponse) {
        const task2 = serviceResponse.task;
        if (!task2) {
          return {
            status: "not_found",
            task_id: action.taskId
          };
        }
        return taskResult2(task2);
      }
      const runtime = getEditingRuntimePort2();
      if (!runtime) return editingRuntimeRequired2(args);
      const task = await runtime.getTask(action.taskId, action.options);
      if (!task) {
        return {
          status: "not_found",
          task_id: action.taskId
        };
      }
      return taskResult2(task);
    }
    async function editingTaskCancel2(args) {
      const action = await editingServiceRuntimeTaskAction2("cancelTask", args);
      const serviceResponse = await mediaPipelineTaskAction2("cancelTask", action);
      if (serviceResponse?.task) return taskResult2(serviceResponse.task);
      const runtime = getEditingRuntimePort2();
      if (!runtime) return editingRuntimeRequired2(args);
      return taskResult2(await runtime.cancelTask(action.taskId, action.options));
    }
    async function editingTaskLogsGet2(args) {
      const action = await editingServiceRuntimeTaskAction2("getTaskLogs", args);
      const serviceResponse = await mediaPipelineTaskAction2("getTaskLogs", action);
      if (serviceResponse?.logs) {
        return {
          ...serviceResponse.logs,
          task_id: serviceResponse.logs.taskId
        };
      }
      const runtime = getEditingRuntimePort2();
      if (!runtime) return editingRuntimeRequired2(args);
      if (!runtime.getTaskLogs) {
        return {
          status: "unsupported_runtime",
          code: "EDITING_TASK_LOGS_UNAVAILABLE",
          message: "The registered Electron editing runtime does not expose task logs yet.",
          task_id: action.taskId
        };
      }
      const logs = await runtime.getTaskLogs(action.taskId, action.options);
      return {
        ...logs,
        task_id: logs.taskId
      };
    }
    async function editingExportImportResource2(args) {
      const runtime = getEditingRuntimePort2();
      if (!runtime?.importExportResource) return editingRuntimeRequired2(args);
      const task = await editingRuntimeTaskSnapshot2(args, runtime);
      const envelope = await editingServiceTaskAction2("importExportResource", {
        ...args,
        ...task ? { task } : {}
      });
      if (envelope.result !== void 0) return envelope.result;
      if (!envelope.request) throw new Error("editing service did not return an import export resource request");
      return runtime.importExportResource(envelope.request);
    }
    async function editingExportSaveLocal2(args) {
      const runtime = getEditingRuntimePort2();
      const taskId = stringValue422(args.taskId ?? args.task_id);
      const explicitOutputPath = stringValue422(args.outputPath ?? args.output_path);
      if (taskId && !explicitOutputPath && !runtime?.getTask) return editingRuntimeRequired2(args);
      const task = await editingRuntimeTaskSnapshot2(args, runtime);
      const envelope = await editingServiceTaskAction2("saveLocalExport", {
        ...args,
        ...task ? { task } : {}
      });
      if (envelope.result !== void 0) return envelope.result;
      if (!envelope.request) throw new Error("editing service did not return a save local export request");
      if (!runtime?.saveLocalExport) return editingRuntimeRequired2(args);
      const saved = await runtime.saveLocalExport(envelope.request);
      return {
        ...saved,
        task_id: stringValue422(envelope.request.taskId ?? envelope.request.task_id),
        persisted: true,
        uploaded: false,
        candidate_created: false,
        ...task ? { task } : {}
      };
    }
    async function editingExportPublishHls2(args) {
      const runtime = getEditingRuntimePort2();
      if (!runtime?.publishHlsStream) return editingRuntimeRequired2(args);
      const task = await editingRuntimeTaskSnapshot2(args, runtime);
      const envelope = await editingServiceTaskAction2("publishHlsStream", {
        ...args,
        ...task ? { task } : {}
      });
      if (envelope.result !== void 0) return envelope.result;
      if (!envelope.request) throw new Error("editing service did not return an HLS publish request");
      return runtime.publishHlsStream(envelope.request);
    }
    async function editingExportCreateCandidate2(args) {
      const contentUnitId = stringOrNumberValue22(args.contentUnitId ?? args.content_unit_id);
      if (contentUnitId === void 0) throw new Error("contentUnitId is required");
      const streamId = stringOrNumberValue22(args.streamId ?? args.stream_id);
      if (streamId !== void 0 && args.resourceId === void 0 && args.resource_id === void 0) {
        return {
          status: "unsupported_output",
          code: "HLS_STREAM_CANDIDATE_UNSUPPORTED",
          message: "editing_export_create_candidate currently writes RawResource-backed candidates only. HLS MediaStreamArtifact candidate outputs require a future domain candidate schema extension; keep the stream as a hosted preview or create a RawResource export before writing a candidate.",
          content_unit_id: contentUnitId,
          streamId,
          stream_id: streamId
        };
      }
      const resourceId = requiredResourceIdValue2(args.resourceId ?? args.resource_id);
      const candidateId = stringOrNumberValue22(args.candidateId ?? args.candidate_id);
      const outputKind = stringValue422(args.outputKind ?? args.output_kind ?? args.kind) ?? "video";
      const mimeType = stringValue422(args.mimeType ?? args.mime_type) ?? defaultMimeTypeForOutputKind2(outputKind);
      const durationMs = optionalNumber3(args.durationMs ?? args.duration_ms);
      const durationSec = optionalNumber3(args.durationSec ?? args.duration_sec) ?? (durationMs !== void 0 ? durationMs / 1e3 : void 0);
      const width = optionalNumber3(args.width);
      const height = optionalNumber3(args.height);
      const taskId = stringValue422(args.taskId ?? args.task_id);
      const outputPath = stringValue422(args.outputPath ?? args.output_path);
      const editingProjectId = stringValue422(args.editingProjectId ?? args.editing_project_id);
      const provenance = objectArg2(args, "provenance");
      const promptSnapshotRaw = args.promptSnapshot ?? args.prompt_snapshot;
      const promptSnapshotInput = isRecord342(promptSnapshotRaw) ? { ...promptSnapshotRaw } : {};
      const outputMetadata = {
        operation: "editing_export_create_candidate",
        tool: "editing_export_create_candidate",
        ...taskId ? { task_id: taskId } : {},
        ...outputPath ? { output_path: outputPath } : {},
        ...editingProjectId ? { editing_project_id: editingProjectId } : {},
        ...provenance ? { provenance } : {}
      };
      const candidate = await domainCreateContentCandidate2({
        ...args,
        contentUnitId,
        ...candidateId !== void 0 ? { candidateId } : {},
        source: stringValue422(args.source) ?? "editing_export",
        status: stringValue422(args.status) ?? "imported",
        producer: {
          ...isRecord342(args.producer) ? args.producer : {},
          kind: stringValue422(args.producer?.kind) ?? "editing",
          tool: "editing_export_create_candidate",
          ...taskId ? { task_id: taskId } : {},
          ...editingProjectId ? { editing_project_id: editingProjectId } : {}
        },
        outputs: [{
          kind: outputKind,
          resource_id: resourceId,
          mime_type: mimeType,
          ...durationSec !== void 0 ? { duration_sec: durationSec } : {},
          ...width !== void 0 ? { width } : {},
          ...height !== void 0 ? { height } : {},
          metadata: outputMetadata
        }],
        promptSnapshot: {
          ...promptSnapshotInput,
          schema: "movscript.editing_export_candidate.v1",
          content_unit_id: contentUnitId,
          resource_id: resourceId,
          ...candidateId !== void 0 ? { candidate_id: candidateId } : {},
          ...taskId ? { task_id: taskId } : {},
          ...outputPath ? { output_path: outputPath } : {},
          ...editingProjectId ? { editing_project_id: editingProjectId } : {}
        }
      });
      return {
        status: "created",
        candidate_created: true,
        adopted: false,
        selected: false,
        content_unit_id: contentUnitId,
        resource_id: resourceId,
        ...candidateId !== void 0 ? { candidate_id: candidateId } : {},
        candidate
      };
    }
    async function editingRuntimeRequired2(args) {
      return {
        status: "unsupported_runtime",
        code: "ELECTRON_EDITING_RUNTIME_REQUIRED",
        message: "This editing tool requires the Electron mediaPipeline runtime. The MCP contract is registered, but runtime IPC is not connected in this process yet.",
        received: Object.keys(args).sort()
      };
    }
    async function editingTaskSourceCreate2(args, taskType) {
      return mediaPipelineTaskCreate2(taskType, args);
    }
    async function mediaPipelineTaskCreate2(taskType, args) {
      const request = await editingServiceTaskRequest2(taskType, args);
      const service22 = optionalMediaPipelineServiceClient2();
      if (service22) {
        const response = await service22.createTask({ request });
        return taskResult2(response.task);
      }
      const runtime = getEditingRuntimePort2();
      if (!runtime) return editingRuntimeRequired2(args);
      const task = await runtime.createTask(request);
      return taskResult2(task);
    }
    async function mediaPipelineTaskAction2(action, request) {
      const service22 = optionalMediaPipelineServiceClient2();
      if (!service22) return void 0;
      return service22.taskAction({
        action,
        taskId: request.taskId,
        options: request.options
      });
    }
    function optionalMediaPipelineServiceClient2() {
      try {
        return createMediaPipelineServiceClientFromRuntime2();
      } catch (error) {
        if (error instanceof Error && error.message.includes("movscript.media.pipeline endpoint was not found")) {
          return void 0;
        }
        throw error;
      }
    }
    async function editingRuntimeTaskSnapshot2(args, runtime) {
      if (!stringValue422(args.taskId ?? args.task_id) || !runtime?.getTask) return void 0;
      const action = await editingServiceRuntimeTaskAction2("getTask", args);
      return runtime.getTask(action.taskId, action.options);
    }
    async function composeEditingProjectWithCompile2(args, renderRuntime) {
      const explicitProject = objectArg2(args, "editingProject") ?? objectArg2(args, "editing_project") ?? objectArg2(args, "project");
      if (explicitProject) return { editingProject: editingProjectArg3(args) };
      const editingProjectId = stringValue422(args.editingProjectId ?? args.editing_project_id);
      if (editingProjectId && !objectArg2(args, "editDecisions") && !objectArg2(args, "edit_decisions")) {
        const result = await editingProjectGet2({
          projectId: projectIdValue3(args) ?? STANDALONE_EDITING_PROJECT_ID2,
          editingProjectId
        });
        return { editingProject: editingProjectFromServiceResult2(result) };
      }
      const editDecisions = objectArg2(args, "editDecisions") ?? objectArg2(args, "edit_decisions");
      if (editDecisions) {
        const assetManifest = objectArg2(args, "assetManifest") ?? objectArg2(args, "asset_manifest");
        const timelineAssembly = objectArg2(args, "timelineAssembly") ?? objectArg2(args, "timeline_assembly");
        const compileResult = compileTimelineAssemblyToMediaEditingProject2({
          timelineAssembly,
          assetManifest,
          editDecisions,
          renderRuntime,
          runtimeLocked: true,
          renderSettings: {
            width: optionalNumber3(args.width),
            height: optionalNumber3(args.height),
            fps: optionalNumber3(args.fps),
            background: stringValue422(args.background),
            default_duration_ms: optionalNumber3(args.defaultDurationMs ?? args.default_duration_ms)
          },
          projectOptions: {
            id: stringValue422(args.id ?? args.editingProjectId ?? args.editing_project_id),
            projectId: projectIdValue3(args) ?? STANDALONE_EDITING_PROJECT_ID2,
            title: stringValue422(args.title),
            width: optionalNumber3(args.width),
            height: optionalNumber3(args.height),
            fps: optionalNumber3(args.fps),
            background: stringValue422(args.background),
            defaultDurationMs: optionalNumber3(args.defaultDurationMs ?? args.default_duration_ms),
            productionId: stringOrNumberValue22(args.productionId ?? args.production_id),
            productionPath: stringValue422(args.productionPath ?? args.production_path),
            targetKind: stringValue422(args.targetKind ?? args.target_kind),
            targetRef: stringValue422(args.targetRef ?? args.target_ref),
            scopeKind: stringValue422(args.scopeKind ?? args.scope_kind),
            scopeRef: stringOrNumberValue22(args.scopeRef ?? args.scope_ref)
          }
        });
        if (compileResult.status === "blocked" || !compileResult.media_editing_project) {
          return { compileResult };
        }
        const saved = await persistCreatedEditingProject2(compileResult.media_editing_project, {
          status: "ok",
          editing_project: compileResult.media_editing_project
        });
        const editingProject = editingProjectFromServiceResult2(saved);
        return {
          editingProject,
          compileResult: {
            ...compileResult,
            media_editing_project: editingProject
          }
        };
      }
      throw new Error("editingProject, editingProjectId, or editDecisions is required");
    }
    function normalizeRenderRuntime2(value) {
      const runtime = stringValue422(value) ?? "movscript_media_pipeline";
      return {
        value: runtime,
        supported: runtime === "movscript_media_pipeline" || runtime === "ffmpeg"
      };
    }
    function timelineValidationHasErrors2(validation) {
      if (!isRecord342(validation)) return true;
      if (validation.valid === false) return true;
      const diagnostics = Array.isArray(validation.diagnostics) ? validation.diagnostics : [];
      return diagnostics.some((diagnostic22) => isRecord342(diagnostic22) && diagnostic22.severity === "error");
    }
    function editingProjectArg3(args) {
      const project = objectArg2(args, "editingProject") ?? objectArg2(args, "editing_project") ?? objectArg2(args, "project");
      if (!project) throw new Error("editingProject is required");
      assertMediaEditingProjectEnvelope3(project);
      return project;
    }
    function assertMediaEditingProjectEnvelope3(project) {
      if (project.version !== 1 || !stringValue422(project.id) || !stringValue422(project.projectId ?? project.project_id)) {
        throw new Error("editingProject must be a MediaEditingProject v1 object");
      }
      if (!isRecord342(project.timeline) || project.timeline.version !== 1 || !Array.isArray(project.timeline.tracks)) {
        throw new Error("editingProject.timeline must be a MediaTimelineRecipe v1 object");
      }
      if (!isRecord342(project.assets) || !Array.isArray(project.assets.assets)) {
        throw new Error("editingProject.assets must contain an assets array");
      }
    }
    function editingProjectFromServiceResult2(result) {
      const record = isRecord342(result) ? result : void 0;
      const project = record && (isRecord342(record.editing_project) ? record.editing_project : isRecord342(record.editingProject) ? record.editingProject : void 0);
      if (!project) throw new Error("editing service did not return editing_project");
      assertMediaEditingProjectEnvelope3(project);
      return project;
    }
    function projectIdValue3(args) {
      const value = args.projectId ?? args.project_id;
      if (typeof value === "number" && Number.isFinite(value)) return String(value);
      return stringValue422(value);
    }
    function editingProjectIdValue3(args) {
      const editingProjectId = stringValue422(args.editingProjectId ?? args.editing_project_id);
      if (!editingProjectId) throw new Error("editingProjectId is required");
      return editingProjectId;
    }
    function requiredResourceIdValue2(value) {
      const resourceId = optionalNumber3(value);
      if (resourceId === void 0 || !Number.isInteger(resourceId) || resourceId <= 0) throw new Error("resourceId is required");
      return resourceId;
    }
    function defaultMimeTypeForOutputKind2(kind) {
      if (kind === "audio") return "audio/mpeg";
      if (kind === "image") return "image/png";
      if (kind === "subtitle" || kind === "text") return "text/plain";
      return "video/mp4";
    }
    function taskResult2(task) {
      return {
        status: "ok",
        task,
        media_pipeline_task: task
      };
    }
    function objectArg2(args, key) {
      const direct = args[key];
      if (isRecord342(direct)) return direct;
      return void 0;
    }
    function optionalNumber3(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
      return void 0;
    }
    function booleanValue22(value) {
      if (typeof value === "boolean") return value;
      if (typeof value === "string") {
        if (value === "true") return true;
        if (value === "false") return false;
      }
      return void 0;
    }
    function stringValue422(value) {
      if (typeof value !== "string") return void 0;
      const trimmed = value.trim();
      return trimmed ? trimmed : void 0;
    }
    function stringOrNumberValue22(value) {
      if (typeof value === "number" && Number.isFinite(value)) return value;
      return stringValue422(value);
    }
    async function listModels2(args) {
      const rawCapability = getOptionalString3(args, "capability");
      const operation = getOptionalString3(args, "operation") ?? getOptionalString3(args, "model_operation");
      const normalizedCapability = normalizeModelCapabilityAlias2(rawCapability);
      const capability = normalizedCapability ?? (rawCapability ? void 0 : capabilityForModelOperation2(operation));
      const referenceAssets = modelReferenceAssetsArg2(args.reference_assets ?? args.referenceAssets);
      const providerVariants = args.provider_variants === true || args.include_provider_variants === true;
      const defaultCapabilities = ["text_generation", "image_generation", "video_generation", "audio_generation", "text", "image", "image_edit", "video", "video_i2v", "video_v2v", "audio_tts", "audio_transcribe", "audio_translate", "audio_music", "audio_sfx", "audio_chat", "voice_clone", "voice_design", "subtitle_align", "subtitle_translate"];
      if (rawCapability && !capability) {
        return {
          count: 0,
          queries: [],
          model_contracts: [],
          models: []
        };
      }
      if (operation && !capability) {
        return {
          count: 0,
          queries: [],
          model_contracts: [],
          models: []
        };
      }
      const queries = capability ? [modelListQuery2(capability, { operation, providerVariants, referenceAssets })] : defaultCapabilities.map((item) => ({
        label: `capability:${item}`,
        path: modelListPath2(item, { providerVariants })
      }));
      const byId = /* @__PURE__ */ new Map();
      for (const query of queries) {
        const models = await backendList2(query.path);
        for (const model of models) {
          const id = Number(model?.id ?? model?.ID);
          const contract = summarizeModelContractForAgent2(model);
          const key = typeof contract.model_id === "string" && contract.model_id.trim() ? contract.model_id.trim() : Number.isFinite(id) && id > 0 ? `backend.model.${id}` : `model.${byId.size}`;
          if (!byId.has(key)) byId.set(key, contract);
        }
      }
      const modelContracts = Array.from(byId.values());
      return {
        count: byId.size,
        queries: queries.map((query) => query.label),
        model_contracts: modelContracts,
        models: modelContracts
      };
    }
    function modelListQuery2(capability, options) {
      const parts = [`capability:${capability}`];
      if (options.operation) parts.push(`operation:${options.operation}`);
      if (options.referenceAssets && options.referenceAssets.length > 0) {
        parts.push(`reference_assets:${options.referenceAssets.length}`);
      }
      return {
        label: parts.join(":"),
        path: modelListPath2(capability, options)
      };
    }
    function modelListPath2(capability, options = {}) {
      const params = new URLSearchParams();
      params.set("capability", capability);
      if (options.operation) params.set("operation", options.operation);
      if (options.providerVariants) params.set("provider_variants", "true");
      if (options.referenceAssets && options.referenceAssets.length > 0) {
        params.set("reference_assets", JSON.stringify(options.referenceAssets));
      }
      return `/models?${params.toString()}`;
    }
    function capabilityForModelOperation2(operation) {
      const normalized = operation?.trim().toLowerCase().replace(/-/g, "_");
      switch (normalized) {
        case "text_to_image":
        case "image_to_image":
          return "image_generation";
        case "prompt_to_video":
        case "text_to_video":
        case "image_to_video":
        case "first_frame_to_video":
        case "first_last_frame_to_video":
        case "reference_to_video":
        case "video_to_video":
          return "video_generation";
        case "tts":
        case "stt":
        case "speech_translate":
        case "audio_chat":
        case "voice_clone":
        case "voice_design":
        case "dubbing":
        case "music":
        case "sfx":
        case "speech_enhancement":
          return "audio_generation";
        default:
          return void 0;
      }
    }
    function modelReferenceAssetsArg2(value) {
      const raw = Array.isArray(value) ? value : [];
      const refs = raw.filter(isRecord342).map((item) => {
        const role = getOptionalString3(item, "role") ?? "generic";
        const mediaType = getOptionalString3(item, "media_type") ?? getOptionalString3(item, "mediaType");
        return {
          role,
          ...mediaType ? { media_type: mediaType } : {}
        };
      }).filter((item) => item.role.trim());
      return refs.length > 0 ? refs : void 0;
    }
    var generationCapabilities2 = [
      "image_generation",
      "video_generation",
      "audio_generation",
      "image",
      "image_edit",
      "video",
      "video_i2v",
      "video_v2v",
      "audio_tts",
      "audio_transcribe",
      "audio_translate",
      "audio_music",
      "audio_sfx",
      "audio_chat",
      "voice_clone",
      "voice_design",
      "subtitle_align",
      "subtitle_translate"
    ];
    async function generateImage2(args) {
      const built = buildImageRequest2(args);
      const selection = await resolveModelSelection2(args, built.generationIntent?.capability ?? built.jobType, built.jobType === "image_edit" ? "image" : "image_edit", built.generationIntent?.operation);
      const submitted = await submitGenerationJob2(args, selection, built, "electron.generation.image");
      return withGenerationJobSurface2(args, generationSubmitResult2("image", submitted.job, "generation_job_get", submitted.paramAudit));
    }
    async function listGenerationCapabilities2(args) {
      const includeModels = args.include_models === true;
      const modelsByCapability = {};
      if (includeModels) {
        for (const capability of generationCapabilities2) {
          const models = await modelsForCapability2(capability);
          modelsByCapability[capability] = {
            count: models.length,
            models
          };
        }
      }
      return {
        capabilities: generationCapabilities2,
        count: generationCapabilities2.length,
        ...includeModels ? { models_by_capability: modelsByCapability } : {}
      };
    }
    async function prepareGeneration2(args) {
      const capability = requiredGenerationCapability2(args);
      const scope = generationScope2(args);
      const operation = generationOperationArg2(args);
      if ((isImageGenerationCapability2(capability) || isVideoGenerationCapability2(capability) || isAudioGenerationCapability2(capability)) && !operation) {
        throw new Error(`${capability} operation is required for generation_prepare; choose an explicit operation instead of relying on input resources`);
      }
      const referenceAssets = generationReferenceAssetsForModelList2(args);
      const models = await listModels2({
        capability,
        operation,
        ...referenceAssets.length > 0 ? { reference_assets: referenceAssets } : {},
        provider_variants: args.provider_variants,
        include_provider_variants: args.include_provider_variants
      });
      if (scope === "content_unit" && (isImageGenerationCapability2(capability) || isVideoGenerationCapability2(capability))) {
        const contentUnitId = requiredContentUnitId3(args);
        const compiled = await compiledContentUnitPrompt2(args, contentUnitId);
        const blockers = Array.isArray(compiled.blockers) ? compiled.blockers : compiled.prompt?.blockers ?? [];
        return {
          status: compiled.ok === true ? "ready" : "blocked",
          capability,
          scope,
          contentUnitId,
          content_unit_id: contentUnitId,
          prompt: compiled.prompt,
          blockers,
          ...isRecord342(models) ? models : { models },
          message: compiled.ok === true ? `Content unit ${String(contentUnitId)} is ready for ${capability} generation.` : `Content unit ${String(contentUnitId)} has unresolved prompt blockers.`
        };
      }
      return {
        status: "ready",
        capability,
        scope,
        ...isRecord342(models) ? models : { models },
        message: `${capability} generation is ready to submit.`
      };
    }
    async function submitUnifiedGeneration2(args) {
      const capability = requiredGenerationCapability2(args);
      const scope = generationScope2(args);
      const candidatePolicy = getOptionalString3(args, "candidate_policy") ?? (scope === "content_unit" ? "auto_create" : "none");
      if (scope === "content_unit") {
        if (isImageGenerationCapability2(capability)) {
          return generationV2Result2(await generateContentUnitImage2(args), capability, scope, "image", "content_unit_candidate", candidatePolicy);
        }
        if (isVideoGenerationCapability2(capability)) {
          return generationV2Result2(await generateContentUnitVideo2(args), capability, scope, "video", "content_unit_candidate", candidatePolicy);
        }
        throw new Error(`scope=content_unit is currently supported by image and video generation capabilities; got ${capability}`);
      }
      switch (capability) {
        case "image_generation":
        case "image":
        case "image_edit":
          return generationV2Result2(await generateImage2(args), capability, scope, "image", "raw_resource", candidatePolicy);
        case "video_generation":
        case "video":
        case "video_i2v":
        case "video_v2v":
          return generationV2Result2(await generateVideo2(args), capability, scope, "video", "raw_resource", candidatePolicy);
        case "audio_generation":
          return submitUnifiedAudioGeneration2(args, capability, scope, candidatePolicy);
        case "audio_tts":
          return generationV2Result2(await generateVoiceover2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "audio_transcribe":
          return generationV2Result2(await generateSubtitle2(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
        case "audio_translate":
          return generationV2Result2(await generateAudioTranslate2(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
        case "audio_music":
          return generationV2Result2(await generateMusic2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "audio_sfx":
          return generationV2Result2(await generateSfx2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "audio_chat":
          return generationV2Result2(await generateAudioChat2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "voice_clone":
          return generationV2Result2(await generateVoiceClone2(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
        case "voice_design":
          return generationV2Result2(await generateVoiceDesign2(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
        case "subtitle_align":
          return generationV2Result2(await alignSubtitle2(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
        case "subtitle_translate":
          return generationV2Result2(await translateSubtitle2(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
        default:
          return assertNeverGenerationCapability2(capability);
      }
    }
    async function submitUnifiedAudioGeneration2(args, capability, scope, candidatePolicy) {
      const operation = generationOperationArg2(args);
      if (!operation) {
        throw new Error("audio_generation operation is required; choose a model operation such as tts, stt, music, or sfx");
      }
      switch (operation) {
        case "tts":
          return generationV2Result2(await generateVoiceover2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "stt":
          return generationV2Result2(await generateSubtitle2(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
        case "speech_translate":
          return generationV2Result2(await generateAudioTranslate2(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
        case "music":
          return generationV2Result2(await generateMusic2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "sfx":
          return generationV2Result2(await generateSfx2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "audio_chat":
          return generationV2Result2(await generateAudioChat2(args), capability, scope, "audio", "raw_resource", candidatePolicy);
        case "voice_clone":
          return generationV2Result2(await generateVoiceClone2(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
        case "voice_design":
          return generationV2Result2(await generateVoiceDesign2(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
        default:
          throw new Error(`unsupported audio_generation operation: ${operation}`);
      }
    }
    async function getUnifiedGenerationJob2(args) {
      const capability = optionalGenerationCapability2(args);
      const scope = generationScope2(args);
      const outputKind = outputKindArg2(args, capability);
      if (scope === "content_unit" && (outputKind === "image" || outputKind === "video")) {
        const result2 = outputKind === "image" ? await getContentUnitImageGenerationJob2({ ...args, outputKind }) : await getContentUnitVideoGenerationJob2({ ...args, outputKind });
        return generationV2Result2(result2, capability ?? outputKind, scope, outputKind, "content_unit_candidate", getOptionalString3(args, "candidate_policy") ?? "auto_create");
      }
      const kind = generationOutputJobGroup2(outputKind);
      const result = withGenerationJobSurface2(args, generationJobGetResult2(kind, await getGenerationJob2(normalizedJobId2(args)), verbosityArg2(args)));
      return generationV2Result2(result, capability ?? outputKind, scope, outputKind, "raw_resource", getOptionalString3(args, "candidate_policy") ?? "none");
    }
    async function getUnifiedGenerationJobs2(args) {
      const jobIds = normalizedJobIds2(args);
      const items = [];
      const rawItems = Array.isArray(args.items) ? args.items : [];
      for (let index = 0; index < jobIds.length; index += 1) {
        const jobId = jobIds[index];
        const itemArgs = isRecord342(rawItems[index]) ? { ...args, ...rawItems[index], jobId } : { ...args, jobId };
        try {
          const result = await getUnifiedGenerationJob2(itemArgs);
          items.push({
            index,
            status: "loaded",
            jobId,
            job_id: jobId,
            terminal: isRecord342(result) ? result.terminal : void 0,
            outputResourceIds: isRecord342(result) ? result.outputResourceIds : void 0,
            output_resource_ids: isRecord342(result) ? result.output_resource_ids : void 0,
            result
          });
        } catch (error) {
          items.push({
            index,
            status: "error",
            jobId,
            job_id: jobId,
            terminal: true,
            error: errorMessage43(error)
          });
        }
      }
      const successItems = items.filter((item) => item.status !== "error");
      const failedItems = items.filter((item) => item.status === "error");
      const terminalCount = items.filter((item) => item.terminal === true).length;
      const outputResourceIds = Array.from(new Set(successItems.flatMap((item) => numericList22(item.output_resource_ids))));
      return {
        status: failedItems.length === 0 ? "loaded" : successItems.length > 0 ? "partial_error" : "error",
        total: jobIds.length,
        success_count: successItems.length,
        failed_count: failedItems.length,
        terminal_count: terminalCount,
        all_terminal: terminalCount === jobIds.length,
        output_resource_ids: outputResourceIds,
        outputResourceIds,
        items,
        message: `${successItems.length}/${jobIds.length} generation job(s) loaded.`
      };
    }
    async function registerGenerationResult2(args) {
      const result = await domainRegisterRawResourceAsContentUnitCandidate2(args);
      return {
        status: "registered",
        candidate: result,
        message: "Generation result registered as a content-unit candidate."
      };
    }
    async function generateContentUnitImage2(args) {
      return generateContentUnitVisual2(args, "image");
    }
    async function getContentUnitImageGenerationJob2(args) {
      return getContentUnitVisualGenerationJob2(args, "image");
    }
    async function generateVideo2(args) {
      const built = buildVideoRequest2(args);
      const selection = await resolveModelSelection2(args, built.generationIntent?.capability ?? built.jobType, "video", built.generationIntent?.operation);
      const submitted = await submitGenerationJob2(args, selection, built, "electron.generation.video");
      return withGenerationJobSurface2(args, generationSubmitResult2("video", submitted.job, "generation_job_get", submitted.paramAudit));
    }
    async function generateContentUnitVideo2(args) {
      return generateContentUnitVisual2(args, "video");
    }
    async function getContentUnitVideoGenerationJob2(args) {
      return getContentUnitVisualGenerationJob2(args, "video");
    }
    async function generateVoiceover2(args) {
      return generateAudioLike2(args, "audio_tts", "audio_tts", "electron.generation.voiceover");
    }
    async function generateMusic2(args) {
      return generateAudioLike2(args, "audio_music", "audio_music", "electron.generation.music");
    }
    async function generateSfx2(args) {
      return generateAudioLike2(args, "audio_sfx", "audio_sfx", "electron.generation.sfx");
    }
    async function generateAudioChat2(args) {
      return generateAudioLike2(args, "audio_chat", "audio_chat", "electron.generation.audio_chat");
    }
    async function generateSubtitle2(args) {
      return generateAudioLike2(args, "audio_transcribe", "audio_transcribe", "electron.generation.subtitle");
    }
    async function alignSubtitle2(args) {
      return generateAudioLike2(args, "subtitle_align", "subtitle_align", "electron.generation.subtitle_align");
    }
    async function translateSubtitle2(args) {
      return generateAudioLike2(args, "subtitle_translate", "subtitle_translate", "electron.generation.subtitle_translate");
    }
    async function generateAudioTranslate2(args) {
      return generateAudioLike2(args, "audio_translate", "audio_translate", "electron.generation.audio_translate");
    }
    async function generateVoiceClone2(args) {
      return generateAudioLike2(args, "voice_clone", "voice_clone", "electron.generation.voice_clone");
    }
    async function generateVoiceDesign2(args) {
      return generateAudioLike2(args, "voice_design", "voice_design", "electron.generation.voice_design");
    }
    async function generateAudioLike2(args, jobType, fallbackCapability, featureKey) {
      const built = buildAudioRequest2(args, jobType);
      const selection = await resolveModelSelection2(args, built.generationIntent?.capability ?? built.jobType, fallbackCapability, built.generationIntent?.operation);
      const submitted = await submitGenerationJob2(args, selection, built, featureKey);
      return withGenerationJobSurface2(args, generationSubmitResult2("audio", submitted.job, "generation_job_get", submitted.paramAudit));
    }
    async function generateContentUnitVisual2(args, kind) {
      const contentUnitId = requiredContentUnitId3(args);
      const compiled = await compiledContentUnitPrompt2(args, contentUnitId);
      if (compiled.ok !== true) {
        return {
          status: "blocked",
          terminal: true,
          contentUnitId,
          content_unit_id: contentUnitId,
          prompt: compiled.prompt,
          blockers: Array.isArray(compiled.blockers) ? compiled.blockers : compiled.prompt?.blockers ?? [],
          surface: createPromptSurface2(args, { contentUnitId, mode: "edit", projectId: projectIdFromArgs2(args) }),
          message: `Content unit ${String(contentUnitId)} ${kind} generation is blocked by unresolved prompt inputs.`
        };
      }
      const prompt = compiledContentUnitGenerationPromptText2(compiled.prompt);
      const providerPrompt = stripResourceMentions2(prompt);
      const compiledRefIds = positiveIntegerIds22([
        ...compiledContentUnitGenerationPromptResourceIds2(compiled.prompt),
        ...resourceIds2(args.input_resource_ids) ?? [],
        ...resourceIds2(args.reference_resource_ids) ?? []
      ]);
      const nextArgs = {
        ...args,
        prompt,
        input_resource_ids: compiledRefIds
      };
      if (kind === "image" && getOptionalString3(nextArgs, "negative_prompt") === void 0) {
        const negative = typeof compiled.prompt.negative_text === "string" ? compiled.prompt.negative_text.trim() : "";
        if (negative) nextArgs.negative_prompt = negative;
      }
      const built = kind === "image" ? buildImageRequest2(nextArgs) : buildVideoRequest2(nextArgs);
      const selection = kind === "image" ? await resolveModelSelection2(nextArgs, built.generationIntent?.capability ?? built.jobType, built.jobType === "image_edit" ? "image" : "image_edit", built.generationIntent?.operation) : await resolveModelSelection2(nextArgs, built.generationIntent?.capability ?? built.jobType, "video", built.generationIntent?.operation);
      const projectScope = await resolveGenerationProjectScope2(args, { required: true });
      const sharedRequest = buildContentUnitGenerationRequest2({
        contentUnitId,
        outputKind: kind,
        compiledPrompt: compiled.prompt,
        modelId: selection.modelId,
        additionalInputResourceIds: [
          ...resourceIds2(args.input_resource_ids) ?? [],
          ...resourceIds2(args.reference_resource_ids) ?? []
        ],
        generationIntent: built.generationIntent,
        paramAudit: []
      });
      const promptSnapshot = {
        ...sharedRequest.promptSnapshot,
        ...buildContentUnitGenerationPromptSnapshot2({
          contentUnitId,
          outputKind: kind,
          modelId: selection.modelId,
          compiledPrompt: compiled.prompt,
          resourceIds: sharedRequest.inputResourceIds,
          paramAudit: [],
          modelParams: {}
        })
      };
      const prepared = prepareGenerationParams2(built, selection.model, parameterModeArg2(nextArgs));
      const modelParams = submittedModelParams2(prepared);
      Object.assign(promptSnapshot, buildContentUnitGenerationPromptSnapshot2({
        contentUnitId,
        outputKind: kind,
        modelId: selection.modelId,
        compiledPrompt: compiled.prompt,
        resourceIds: sharedRequest.inputResourceIds,
        paramAudit: prepared.audit,
        modelParams
      }));
      const submitted = await submitGenerationJob2({
        ...args,
        title: getOptionalString3(args, "title") ?? `Content unit ${kind} generation`
      }, selection, built, contentUnitGenerationFeatureKey2(kind), {
        candidate_id: getOptionalString3(args, "candidate_id") ?? getOptionalString3(args, "candidateId"),
        output_kind: kind,
        prompt_snapshot: promptSnapshot,
        content_unit_id: contentUnitId
      });
      const monitorTool = contentUnitGenerationSystemMonitorToolName2(kind);
      const result = withGenerationJobSurface2(args, generationSubmitResult2(kind, submitted.job, monitorTool, prepared.audit), contentUnitId);
      const jobId = idField222(result.job_id);
      const candidateId = getOptionalString3(args, "candidate_id") ?? getOptionalString3(args, "candidateId");
      return {
        ...result,
        generation_mode: "content_unit_candidate",
        candidate_policy: "auto_create_on_success",
        will_auto_select: false,
        requires_user_adoption: true,
        contentUnitId,
        content_unit_id: contentUnitId,
        prompt: compiled.prompt,
        compiled_prompt_text: prompt,
        provider_prompt_text: providerPrompt,
        input_resource_ids: sharedRequest.inputResourceIds,
        inputResourceIds: sharedRequest.inputResourceIds,
        semantic_ref_replacements: semanticRefReplacements2(compiled.prompt),
        provider_prompt_note: "Provider prompt text intentionally strips MovScript resource tokens; resources are passed separately through input_resource_ids.",
        monitor: {
          tool: monitorTool,
          args: {
            jobId,
            contentUnitId,
            content_unit_id: contentUnitId,
            ...candidateId ? { candidateId, candidate_id: candidateId } : {},
            outputKind: kind,
            output_kind: kind,
            projectDir: projectScope.projectDir,
            project_dir: projectScope.projectDir,
            ...projectScope.projectUid ? { projectUid: projectScope.projectUid, project_uid: projectScope.projectUid } : {},
            promptSnapshot,
            prompt_snapshot: promptSnapshot
          }
        },
        secondary_surfaces: [
          createPromptSurface2(args, { contentUnitId, mode: "inspect", projectId: projectIdFromArgs2(args) })
        ],
        ...candidateId ? { candidateId, candidate_id: candidateId } : {},
        message: `Content unit ${String(contentUnitId)} ${kind} generation candidate job submitted (Job #${String(jobId)}). Candidate will be refreshed automatically when the job succeeds.`
      };
    }
    async function getContentUnitVisualGenerationJob2(args, kind) {
      const contentUnitId = requiredContentUnitId3(args);
      const job = await getGenerationJob2(normalizedJobId2(args));
      const base = generationJobGetResult2(kind, job, verbosityArg2(args));
      const status = stringField332(base.status) ?? "";
      if (!isSuccessfulStatus2(status)) {
        return {
          ...base,
          generation_mode: "content_unit_candidate",
          candidate_policy: "auto_create_on_success",
          will_auto_select: false,
          requires_user_adoption: true,
          contentUnitId,
          content_unit_id: contentUnitId,
          candidate_created: false,
          surface: createGenerationJobSurface2(args, {
            jobId: normalizedJobId2(args),
            contentUnitId,
            projectId: projectIdFromArgs2(args)
          }),
          message: `${base.message}. Candidate will be created after a successful terminal result.`
        };
      }
      const outputResourceIds = numericList22(base.output_resource_ids);
      if (outputResourceIds.length === 0) {
        return {
          ...base,
          generation_mode: "content_unit_candidate",
          candidate_policy: "auto_create_on_success",
          will_auto_select: false,
          requires_user_adoption: true,
          contentUnitId,
          content_unit_id: contentUnitId,
          candidate_created: false,
          surface: createGenerationJobSurface2(args, {
            jobId: normalizedJobId2(args),
            contentUnitId,
            projectId: projectIdFromArgs2(args)
          }),
          message: `${base.message}. No output resource is available for candidate creation yet.`
        };
      }
      const candidates = [];
      for (const resourceId of outputResourceIds) {
        const promptSnapshotValue = args.promptSnapshot ?? args.prompt_snapshot;
        const promptSnapshot = isRecord342(promptSnapshotValue) ? promptSnapshotValue : void 0;
        const requestedCandidateId = getOptionalString3(args, "candidateId") ?? getOptionalString3(args, "candidate_id");
        const candidateId = requestedCandidateId ?? contentUnitGenerationCandidateId2(kind, idField222(base.job_id) ?? normalizedJobId2(args), resourceId);
        const plan = buildContentUnitGenerationOutputCandidate2({
          contentUnitId,
          outputKind: kind,
          job,
          resourceId,
          candidateId,
          ...promptSnapshot ? { promptSnapshot } : {}
        });
        const candidate = await domainCreateContentCandidate2({
          ...args,
          ...plan
        });
        candidates.push({
          candidateId,
          candidate_id: candidateId,
          resourceId,
          resource_id: resourceId,
          result: candidate
        });
      }
      const visibility = await readContentUnitCandidateVisibility2(args, contentUnitId).catch(() => void 0);
      const firstCandidate = candidates.length === 1 ? candidates[0] : void 0;
      const firstCandidateId = firstCandidate ? stringField332(firstCandidate.candidate_id ?? firstCandidate.candidateId) : void 0;
      const firstResourceId = firstCandidate ? idField222(firstCandidate.resource_id ?? firstCandidate.resourceId) : void 0;
      return {
        ...base,
        generation_mode: "content_unit_candidate",
        candidate_policy: "auto_create_on_success",
        will_auto_select: false,
        requires_user_adoption: true,
        contentUnitId,
        content_unit_id: contentUnitId,
        candidate_created: true,
        candidate_count: candidates.length,
        candidates,
        surface: createContentCandidatesSurface2(args, {
          contentUnitId,
          ...firstCandidateId ?? candidateIdFromArgs2(args) ? { candidateId: firstCandidateId ?? candidateIdFromArgs2(args) } : {},
          ...firstResourceId !== void 0 ? { resourceId: firstResourceId } : {},
          projectId: projectIdFromArgs2(args)
        }),
        secondary_surfaces: [
          createGenerationJobSurface2(args, {
            jobId: normalizedJobId2(args),
            contentUnitId,
            projectId: projectIdFromArgs2(args)
          })
        ],
        ...visibility ?? {},
        message: `${base.message}. Created or refreshed ${candidates.length} content-unit candidate(s).`
      };
    }
    function buildImageRequest2(args) {
      const { prompt, refIds } = promptAndResourceIds2(args);
      const generationIntent = generationIntentArg2(args, "image", refIds);
      const params = extraParamsArg2(args.extra_params);
      const explicitParamKeys = new Set(Object.keys(params));
      const defaultParamKeys = /* @__PURE__ */ new Set();
      assignStringParam2(args, params, explicitParamKeys, "image_size");
      assignDefaultParam2(params, defaultParamKeys, "image_size", "1024x1024");
      assignStringParam2(args, params, explicitParamKeys, "aspect_ratio");
      assignDefaultParam2(params, defaultParamKeys, "aspect_ratio", "1:1");
      const quality = getOptionalString3(args, "quality");
      if (quality) {
        params.quality = quality;
        explicitParamKeys.add("quality");
      }
      const negativePrompt = getOptionalString3(args, "negative_prompt");
      if (negativePrompt) {
        params.negative_prompt = negativePrompt;
        explicitParamKeys.add("negative_prompt");
      }
      const steps = getOptionalNumeric2(args, "steps");
      if (steps !== void 0) {
        params.steps = steps;
        explicitParamKeys.add("steps");
      }
      const seed = getOptionalNumeric2(args, "seed");
      if (seed !== void 0) {
        params.seed = seed;
        explicitParamKeys.add("seed");
      }
      return {
        prompt,
        refIds,
        jobType: generationExecutionJobTypeForIntent2(generationIntent, "image"),
        generationIntent,
        timeoutMs: getOptionalNumeric2(args, "timeout_ms") ?? 18e4,
        params,
        explicitParamKeys,
        defaultParamKeys
      };
    }
    function buildVideoRequest2(args) {
      const { prompt, refIds } = promptAndResourceIds2(args);
      const generationIntent = generationIntentArg2(args, "video", refIds);
      const params = { ...extraParamsArg2(args.extra_params) };
      const explicitParamKeys = new Set(Object.keys(params));
      const defaultParamKeys = /* @__PURE__ */ new Set();
      assignStringParam2(args, params, explicitParamKeys, "aspect_ratio");
      assignDefaultParam2(params, defaultParamKeys, "aspect_ratio", "16:9");
      const duration = getOptionalNumeric2(args, "duration");
      if (duration !== void 0) {
        params.duration = duration;
        explicitParamKeys.add("duration");
      } else {
        assignDefaultParam2(params, defaultParamKeys, "duration", 5);
      }
      const quality = getOptionalString3(args, "quality");
      if (quality) {
        params.quality = quality;
        explicitParamKeys.add("quality");
      }
      const fps = getOptionalNumeric2(args, "fps");
      if (fps !== void 0) {
        params.fps = fps;
        explicitParamKeys.add("fps");
      }
      const seed = getOptionalNumeric2(args, "seed");
      if (seed !== void 0) {
        params.seed = seed;
        explicitParamKeys.add("seed");
      }
      return {
        prompt,
        refIds,
        jobType: generationExecutionJobTypeForIntent2(generationIntent, "video"),
        generationIntent,
        timeoutMs: getOptionalNumeric2(args, "timeout_ms") ?? 6e5,
        params,
        explicitParamKeys,
        defaultParamKeys
      };
    }
    function generationIntentArg2(args, outputKind, refIds) {
      const explicit = args.generation_intent ?? args.generationIntent;
      const topLevelOperation = getOptionalString3(args, "operation") ?? getOptionalString3(args, "model_operation");
      if (isRecord342(explicit)) {
        const capability = getOptionalString3(explicit, "capability") ?? (outputKind === "image" ? "image_generation" : "video_generation");
        const operation2 = getOptionalString3(explicit, "operation") ?? topLevelOperation;
        if (capability && operation2) {
          return {
            capability,
            operation: operation2,
            ...generationReferenceAssetsPayload2(explicit.reference_assets ?? explicit.referenceAssets, refIds)
          };
        }
      }
      const operation = topLevelOperation;
      if (!operation) {
        throw new Error(`${outputKind}_generation operation is required; choose an explicit operation instead of relying on input resources`);
      }
      return {
        capability: outputKind === "image" ? "image_generation" : "video_generation",
        operation,
        ...generationReferenceAssetsPayload2(args.reference_assets ?? args.referenceAssets, refIds)
      };
    }
    function audioGenerationIntentArg2(args, jobType, refIds) {
      const explicit = args.generation_intent ?? args.generationIntent;
      const topLevelOperation = getOptionalString3(args, "operation") ?? getOptionalString3(args, "model_operation");
      if (isRecord342(explicit)) {
        const capability = getOptionalString3(explicit, "capability") ?? "audio_generation";
        const operation2 = getOptionalString3(explicit, "operation") ?? topLevelOperation ?? audioOperationForJobType2(jobType);
        if (capability && operation2) {
          return {
            capability,
            operation: operation2,
            ...generationReferenceAssetsPayload2(explicit.reference_assets ?? explicit.referenceAssets, refIds)
          };
        }
      }
      const operation = topLevelOperation ?? audioOperationForJobType2(jobType);
      if (!operation) return void 0;
      return {
        capability: "audio_generation",
        operation,
        ...generationReferenceAssetsPayload2(args.reference_assets ?? args.referenceAssets, refIds)
      };
    }
    function audioOperationForJobType2(jobType) {
      switch (jobType) {
        case "audio_tts":
          return "tts";
        case "audio_transcribe":
          return "stt";
        case "audio_translate":
          return "speech_translate";
        case "audio_music":
          return "music";
        case "audio_sfx":
          return "sfx";
        case "audio_chat":
          return "audio_chat";
        case "voice_clone":
          return "voice_clone";
        case "voice_design":
          return "voice_design";
        default:
          return void 0;
      }
    }
    function generationReferenceAssetsPayload2(value, refIds) {
      const explicit = Array.isArray(value) ? value.filter(isRecord342) : [];
      const source = explicit.length > 0 ? explicit.map((item, index) => ({
        role: getOptionalString3(item, "role") ?? "generic",
        media_type: getOptionalString3(item, "media_type") ?? getOptionalString3(item, "mediaType"),
        resource_id: idField222(item.resource_id ?? item.resourceId) ?? refIds[index]
      })) : refIds.map((resourceId) => ({ role: "generic", resource_id: resourceId }));
      const complete = source.filter((item) => item.role.trim() && item.resource_id !== void 0);
      if (complete.some((item) => !item.media_type?.trim())) {
        throw new Error("reference_assets media_type is required for every input resource; pass typed reference_assets instead of bare reference_resource_ids");
      }
      const referenceAssets = complete.map((item) => ({
        role: item.role.trim(),
        media_type: item.media_type.trim(),
        resource_id: item.resource_id
      }));
      return referenceAssets.length > 0 ? { reference_assets: referenceAssets } : {};
    }
    function generationOperationArg2(args) {
      const explicit = args.generation_intent ?? args.generationIntent;
      if (isRecord342(explicit)) {
        const operation = getOptionalString3(explicit, "operation");
        if (operation) return operation;
      }
      return getOptionalString3(args, "operation") ?? getOptionalString3(args, "model_operation");
    }
    function generationReferenceAssetsForModelList2(args) {
      const explicit = args.generation_intent ?? args.generationIntent;
      const raw = isRecord342(explicit) && (explicit.reference_assets !== void 0 || explicit.referenceAssets !== void 0) ? explicit.reference_assets ?? explicit.referenceAssets : args.reference_assets ?? args.referenceAssets;
      const refs = Array.isArray(raw) ? raw.filter(isRecord342) : [];
      if (refs.length === 0) return [];
      return refs.map((item) => {
        const role = getOptionalString3(item, "role") ?? "generic";
        const mediaType = getOptionalString3(item, "media_type") ?? getOptionalString3(item, "mediaType");
        return {
          role,
          ...mediaType ? { media_type: mediaType } : {}
        };
      }).filter((item) => item.role.trim());
    }
    function buildAudioRequest2(args, jobType = "audio_tts") {
      const { prompt, refIds } = promptAndResourceIds2(args);
      const generationIntent = audioGenerationIntentArg2(args, jobType, refIds);
      const params = { ...extraParamsArg2(args.extra_params) };
      const explicitParamKeys = new Set(Object.keys(params));
      const defaultParamKeys = /* @__PURE__ */ new Set();
      assignStringParam2(args, params, explicitParamKeys, "voice");
      assignStringParam2(args, params, explicitParamKeys, "language");
      assignStringParam2(args, params, explicitParamKeys, "model");
      assignStringParam2(args, params, explicitParamKeys, "audio_format");
      assignStringParam2(args, params, explicitParamKeys, "response_format");
      assignStringParam2(args, params, explicitParamKeys, "output_format");
      assignStringParam2(args, params, explicitParamKeys, "instructions");
      assignStringParam2(args, params, explicitParamKeys, "target_language");
      assignStringParam2(args, params, explicitParamKeys, "source_language");
      assignStringParam2(args, params, explicitParamKeys, "subtitle_format");
      assignStringParam2(args, params, explicitParamKeys, "style");
      const speed = getOptionalNumeric2(args, "speed");
      if (speed !== void 0) {
        params.speed = speed;
        explicitParamKeys.add("speed");
      }
      return {
        prompt,
        refIds,
        jobType,
        generationIntent,
        timeoutMs: getOptionalNumeric2(args, "timeout_ms") ?? 18e4,
        params,
        explicitParamKeys,
        defaultParamKeys
      };
    }
    async function resolveModelSelection2(args, primaryCapability, fallbackCapability, operation) {
      const explicit = getOptionalString3(args, "model_id");
      if (explicit) {
        const models = await modelsForCapabilities2([primaryCapability, fallbackCapability], operation);
        const model2 = models.find((model3) => modelMatchesPublicId2(model3, explicit));
        return { modelId: explicit, model: model2 };
      }
      const primary = await modelsForCapability2(primaryCapability, operation);
      const fallback = primary.length > 0 ? primary : await modelsForCapability2(fallbackCapability, operation);
      const modelId = modelPublicId2(fallback[0]);
      if (!modelId) throw new Error(`No enabled generation model is configured for ${primaryCapability}`);
      const model = isRecord342(fallback[0]) ? fallback[0] : void 0;
      return { modelId, model };
    }
    async function modelsForCapability2(capability, operation) {
      const result = await listModels2({ capability, operation });
      return isRecord342(result) && Array.isArray(result.models) ? result.models : [];
    }
    async function modelsForCapabilities2(capabilities, operation) {
      const byId = /* @__PURE__ */ new Map();
      for (const capability of Array.from(new Set(capabilities))) {
        for (const model of await modelsForCapability2(capability, operation)) {
          if (!isRecord342(model)) continue;
          const key = String(idField222(model.id) ?? idField222(model.ID) ?? modelPublicId2(model) ?? byId.size);
          if (!byId.has(key)) byId.set(key, model);
        }
      }
      return Array.from(byId.values());
    }
    async function submitGenerationJob2(args, selection, built, featureKey, contentUnitCandidate) {
      const prepared = prepareGenerationParams2(built, selection.model, parameterModeArg2(args));
      const modelParams = submittedModelParams2(prepared);
      const projectScope = await resolveGenerationProjectScope2(args, { required: contentUnitCandidate !== void 0 });
      const body = {
        model_id: selection.modelId,
        job_type: built.jobType,
        generation_intent: built.generationIntent,
        feature_key: featureKey,
        prompt: built.prompt,
        input_resource_ids: built.refIds,
        extra_params: JSON.stringify(prepared.extraParams)
      };
      if (projectScope.projectUid) body.project_uid = projectScope.projectUid;
      if (projectScope.projectTitle) body.project_title = projectScope.projectTitle;
      if (projectScope.projectDir) body.project_dir = projectScope.projectDir;
      if (prepared.aspectRatio !== void 0) body.aspect_ratio = prepared.aspectRatio;
      if (prepared.duration !== void 0) body.duration = prepared.duration;
      const title = getOptionalString3(args, "title");
      if (title) body.title = title;
      if (contentUnitCandidate) {
        body.content_unit_candidate = {
          ...contentUnitCandidate,
          ...projectScope.projectUid ? { project_uid: projectScope.projectUid } : {}
        };
      }
      const projectContext = projectScope.projectDir || projectScope.projectUid || projectScope.projectTitle ? {
        ...projectScope.projectDir ? { dir: projectScope.projectDir } : {},
        ...projectScope.projectUid ? { uid: projectScope.projectUid } : {},
        ...projectScope.projectTitle ? { title: projectScope.projectTitle } : {}
      } : void 0;
      body.request_context = JSON.stringify({
        ...projectContext ? { project: projectContext } : {},
        ...contentUnitCandidate ? { content_unit_candidate: contentUnitCandidate } : {}
      });
      const job = await backendPost222("/jobs", body);
      if (!isRecord342(job)) throw new Error("Generation job create returned an invalid response");
      return { job: normalizeJob2(job), paramAudit: prepared.audit, modelParams };
    }
    async function getGenerationJob2(jobId) {
      const job = await backendGet222(`/jobs/${jobId}`);
      if (!isRecord342(job)) throw new Error("Generation job get returned an invalid response");
      return normalizeJob2(job);
    }
    function submittedModelParams2(prepared) {
      return {
        ...prepared.extraParams,
        ...prepared.aspectRatio !== void 0 ? { aspect_ratio: prepared.aspectRatio } : {},
        ...prepared.duration !== void 0 ? { duration: prepared.duration } : {}
      };
    }
    async function resolveGenerationProjectScope2(args, options = {}) {
      const rawDir = getOptionalString3(args, "projectDir") ?? getOptionalString3(args, "project_dir") ?? getOptionalString3(args, "projectPath") ?? getOptionalString3(args, "project_path") ?? getOptionalString3(args, "cwd");
      const explicitProjectUid = getOptionalString3(args, "projectUid") ?? getOptionalString3(args, "project_uid");
      const explicitProjectTitle = getOptionalString3(args, "projectTitle") ?? getOptionalString3(args, "project_title");
      if (!rawDir) {
        if (options.required) throw new Error("projectDir or cwd is required for project-scoped generation tools");
        return {
          ...explicitProjectUid ? { projectUid: explicitProjectUid } : {},
          ...explicitProjectTitle ? { projectTitle: explicitProjectTitle } : {}
        };
      }
      const projectDir = (0, import_path102.resolve)(rawDir);
      if (options.required) {
        const binding = await requireMCPBackendBoundProject2({ projectDir, ...explicitProjectUid ? { projectUid: explicitProjectUid } : {} });
        return {
          projectDir,
          projectUid: binding.projectUid,
          projectTitle: explicitProjectTitle ?? binding.projectTitle
        };
      }
      const locator = await resolveMCPProjectBindingLocator2({
        projectDir,
        ...explicitProjectUid ? { projectUid: explicitProjectUid } : {}
      }).catch(() => void 0);
      return {
        projectDir: locator?.projectDir ?? projectDir,
        projectUid: locator?.projectUid ?? explicitProjectUid,
        projectTitle: explicitProjectTitle ?? locator?.projectTitle
      };
    }
    function generationSubmitResult2(kind, job, monitorTool, paramAudit = []) {
      const jobId = idField222(job.id) ?? idField222(job.ID);
      if (jobId === void 0) throw new Error("Generation job create did not return a valid job id");
      const kindLabel = generationKindLabel2(kind);
      return {
        status: "submitted",
        terminal: false,
        jobId,
        job_id: jobId,
        monitor: { tool: monitorTool, args: { jobId } },
        message: `${kindLabel} generation job submitted (Job #${jobId})`,
        job,
        ...paramAudit.length > 0 ? { param_audit: paramAudit, paramAudit } : {}
      };
    }
    function generationJobGetResult2(kind, job, verbosity = "debug") {
      const jobId = idField222(job.id) ?? idField222(job.ID);
      if (jobId === void 0) throw new Error("Generation job response does not include a valid job id");
      const status = stringField332(job.status) ?? "unknown";
      const outputResourceIds = outputResourceIdsFromJob2(job);
      const result = {
        status,
        terminal: isTerminalStatus2(status),
        jobId,
        job_id: jobId,
        provider_status: stringField332(job.provider_status ?? job.providerStatus ?? job.external_status ?? job.externalStatus),
        outputResourceIds,
        output_resource_ids: outputResourceIds,
        ...outputResourceIds[0] ? { output_resource_id: outputResourceIds[0], outputResourceId: outputResourceIds[0] } : {},
        next_run_at: stringField332(job.next_run_at ?? job.nextRunAt),
        error_summary: errorSummary2(job),
        message: `${generationKindLabel2(kind)} generation job #${jobId} status: ${status}`
      };
      if (verbosity !== "summary") result.job = job;
      return result;
    }
    function withGenerationJobSurface2(args, result, contentUnitId) {
      const jobId = idField222(result.job_id ?? result.jobId);
      if (jobId === void 0) return result;
      return {
        ...result,
        surface: createGenerationJobSurface2(args, {
          jobId,
          ...contentUnitId !== void 0 ? { contentUnitId } : {},
          projectId: projectIdFromArgs2(args)
        })
      };
    }
    function generationKindLabel2(kind) {
      if (kind === "image") return "Image";
      if (kind === "video") return "Video";
      return "Audio";
    }
    function normalizeJob2(job) {
      const outputResourceIds = outputResourceIdsFromJob2(job);
      return {
        ...job,
        ...outputResourceIds.length > 0 ? { outputResourceIds, output_resource_ids: outputResourceIds } : {}
      };
    }
    function outputResourceIdsFromJob2(job) {
      const ids = [];
      appendId2(ids, job.output_resource_id);
      appendId2(ids, job.outputResourceId);
      appendId2(ids, isRecord342(job.output_resource) ? job.output_resource.id ?? job.output_resource.ID : void 0);
      appendId2(ids, isRecord342(job.outputResource) ? job.outputResource.id ?? job.outputResource.ID : void 0);
      appendIds2(ids, job.output_resource_ids);
      appendIds2(ids, job.outputResourceIds);
      return Array.from(new Set(ids));
    }
    function requiredGenerationCapability2(args) {
      const capability = optionalGenerationCapability2(args);
      if (!capability) throw new Error("capability is required");
      return capability;
    }
    function optionalGenerationCapability2(args) {
      const raw = getOptionalString3(args, "capability") ?? getOptionalString3(args, "job_type") ?? getOptionalString3(args, "jobType");
      if (!raw) return void 0;
      const normalized = normalizeGenerationCapability2(raw);
      if (!normalized) throw new Error(`unsupported generation capability: ${raw}`);
      return normalized;
    }
    function normalizeGenerationCapability2(value) {
      const raw = value.trim();
      const aliases = {
        image_generation: "image_generation",
        video_generation: "video_generation",
        audio: "audio_tts",
        tts: "audio_tts",
        voiceover: "audio_tts",
        stt: "audio_transcribe",
        transcription: "audio_transcribe",
        audio_translation: "audio_translate",
        voice_chat: "audio_chat",
        speech_chat: "audio_chat",
        omni: "audio_chat",
        music: "audio_music",
        sfx: "audio_sfx",
        sound_effect: "audio_sfx",
        sound_effects: "audio_sfx",
        voice_cloning: "voice_clone",
        voice_profile: "voice_design"
      };
      const normalized = aliases[raw] ?? raw;
      return generationCapabilities2.includes(normalized) ? normalized : void 0;
    }
    function generationScope2(args) {
      const raw = getOptionalString3(args, "scope");
      if (raw === "content_unit" || raw === "asset" || raw === "storyboard" || raw === "keyframe") return raw;
      return "free";
    }
    function outputKindArg2(args, capability) {
      const raw = getOptionalString3(args, "outputKind") ?? getOptionalString3(args, "output_kind");
      if (raw === "image" || raw === "video" || raw === "audio" || raw === "subtitle" || raw === "voice_profile" || raw === "json") return raw;
      return generationCapabilityOutputKind2(capability);
    }
    function generationCapabilityOutputKind2(capability) {
      if (isImageGenerationCapability2(capability)) return "image";
      if (isVideoGenerationCapability2(capability)) return "video";
      if (capability === "audio_transcribe" || capability === "audio_translate" || capability === "subtitle_align" || capability === "subtitle_translate") return "subtitle";
      if (capability === "voice_clone" || capability === "voice_design") return "voice_profile";
      return "audio";
    }
    function isImageGenerationCapability2(capability) {
      return capability === "image_generation" || capability === "image" || capability === "image_edit";
    }
    function isVideoGenerationCapability2(capability) {
      return capability === "video_generation" || capability === "video" || capability === "video_i2v" || capability === "video_v2v";
    }
    function isAudioGenerationCapability2(capability) {
      return capability === "audio_generation";
    }
    function generationOutputJobGroup2(outputKind) {
      if (outputKind === "image") return "image";
      if (outputKind === "video") return "video";
      return "audio";
    }
    function generationV2Result2(rawResult, capability, scope, outputKind, generationMode, candidatePolicy) {
      if (!isRecord342(rawResult)) {
        return {
          status: "unknown",
          capability,
          scope,
          output_kind: outputKind,
          outputKind,
          generation_mode: generationMode,
          candidate_policy: candidatePolicy,
          result: rawResult
        };
      }
      const jobId = idField222(rawResult.job_id ?? rawResult.jobId);
      const monitorArgs = {
        ...isRecord342(rawResult.monitor) && isRecord342(rawResult.monitor.args) ? rawResult.monitor.args : {},
        ...jobId !== void 0 ? { jobId, job_id: jobId } : {},
        capability,
        scope,
        outputKind,
        output_kind: outputKind
      };
      return {
        ...rawResult,
        capability,
        scope,
        output_kind: outputKind,
        outputKind,
        generation_mode: stringField332(rawResult.generation_mode) ?? generationMode,
        candidate_policy: stringField332(rawResult.candidate_policy) ?? candidatePolicy,
        monitor: jobId !== void 0 ? {
          tool: "generation_job_get",
          args: monitorArgs
        } : rawResult.monitor
      };
    }
    function assertNeverGenerationCapability2(value) {
      throw new Error(`unsupported generation capability: ${String(value)}`);
    }
    function promptArg2(args) {
      const prompt = getOptionalString3(args, "prompt");
      if (!prompt) throw new Error("prompt is required");
      return prompt;
    }
    function promptAndResourceIds2(args) {
      const rawPrompt = promptArg2(args);
      return {
        prompt: stripResourceMentions2(rawPrompt),
        refIds: positiveIntegerIds22([
          ...resourceIdsFromMentions2(rawPrompt),
          ...resourceIds2(args.input_resource_ids) ?? [],
          ...resourceIds2(args.reference_resource_ids) ?? []
        ])
      };
    }
    function requiredContentUnitId3(args) {
      const value = args.contentUnitId ?? args.content_unit_id;
      if (typeof value === "number" && Number.isFinite(value)) return value;
      if (typeof value === "string" && value.trim()) return value.trim();
      throw new Error("contentUnitId is required");
    }
    async function compiledContentUnitPrompt2(args, contentUnitId) {
      const result = await domainBuildContentUnitBackendPrompt2({ ...args, contentUnitId });
      if (!isRecord342(result)) throw new Error("content unit prompt compiler returned an invalid response");
      const prompt = isRecord342(result.prompt) ? result.prompt : void 0;
      if (!prompt) throw new Error("content unit prompt compiler did not return prompt data");
      return {
        ok: result.ok,
        prompt,
        ...Array.isArray(result.blockers) ? { blockers: result.blockers } : {}
      };
    }
    function resourceIds2(value) {
      if (typeof value === "string") {
        const ids = value.split(",").map((item) => Number(item.trim()));
        return positiveIntegerIds22(ids);
      }
      const values = numericValues2(value);
      return values ? positiveIntegerIds22(values) : void 0;
    }
    function positiveIntegerIds22(values) {
      return Array.from(new Set(values.filter((id) => Number.isFinite(id) && id > 0).map((id) => Math.floor(id))));
    }
    function extraParamsArg2(value) {
      if (isRecord342(value)) return { ...value };
      if (typeof value === "string" && value.trim()) {
        const parsed = JSON.parse(value);
        if (!isRecord342(parsed)) throw new Error("extra_params must be an object");
        return { ...parsed };
      }
      return {};
    }
    function assignStringParam2(args, params, explicitParamKeys, key) {
      const value = getOptionalString3(args, key);
      if (!value) return;
      params[key] = value;
      explicitParamKeys.add(key);
    }
    function assignDefaultParam2(params, defaultParamKeys, key, value) {
      if (params[key] !== void 0 && params[key] !== null && params[key] !== "") return;
      params[key] = value;
      defaultParamKeys.add(key);
    }
    function parameterModeArg2(args) {
      const raw = getOptionalString3(args, "parameter_mode") ?? getOptionalString3(args, "param_mode");
      return raw === "strict" ? "strict" : "compatible";
    }
    function verbosityArg2(args) {
      const raw = getOptionalString3(args, "verbosity");
      return raw === "summary" ? "summary" : "debug";
    }
    function semanticRefReplacements2(prompt) {
      const replacements = prompt.replacements ?? prompt.semantic_ref_replacements ?? prompt.ref_replacements;
      return Array.isArray(replacements) ? replacements : [];
    }
    function errorSummary2(job) {
      const direct = stringField332(job.error_summary ?? job.errorSummary ?? job.error ?? job.error_message ?? job.errorMessage);
      if (direct) return direct;
      const error = isRecord342(job.error) ? job.error : void 0;
      return stringField332(error?.message);
    }
    function prepareGenerationParams2(built, model, mode) {
      const supported = supportedParamMap2(model);
      const hasContract = supported !== void 0;
      const output3 = {};
      const audit = [];
      const pending = { ...built.params };
      if (hasContract && mode === "compatible" && !supported.has("aspect_ratio") && supported.has("image_size") && pending.aspect_ratio !== void 0 && (pending.image_size === void 0 || built.defaultParamKeys.has("image_size"))) {
        const mapped = aspectRatioToImageSize2(String(pending.aspect_ratio), supported.get("image_size"));
        if (mapped) {
          pending.image_size = mapped;
          built.defaultParamKeys.delete("image_size");
          audit.push({
            key: "aspect_ratio",
            value: pending.aspect_ratio,
            reason: "mapped_unsupported_aspect_ratio_to_image_size",
            source: paramSource2(built, "aspect_ratio"),
            mapped_to: "image_size",
            mapped_value: mapped
          });
        }
      }
      for (const [key, value] of Object.entries(pending)) {
        const source = paramSource2(built, key);
        if (value === void 0 || value === null || value === "") continue;
        const paramDef = hasContract ? supported.get(key) : void 0;
        if (hasContract && !paramDef) {
          if (mode === "strict" && source === "explicit") {
            throw unsupportedMCPParamError2(key, model, supported);
          }
          audit.push({ key, value, reason: "dropped_unsupported_parameter", source });
          continue;
        }
        const normalized = normalizeParamValue2(value, paramDef);
        const checked = hasContract && paramDef ? compatibleParamValue2(key, normalized, paramDef, source, mode, model, supported, audit) : normalized;
        if (checked === void 0) continue;
        output3[key] = checked;
      }
      const { aspect_ratio, duration, ...extraParams } = output3;
      return {
        aspectRatio: typeof aspect_ratio === "string" ? aspect_ratio : void 0,
        duration: numericDuration2(duration),
        extraParams,
        audit
      };
    }
    function compatibleParamValue2(key, value, paramDef, source, mode, model, supported, audit) {
      const options = Array.isArray(paramDef.options) ? paramDef.options.filter((item) => typeof item === "string") : [];
      if (options.length > 0 && typeof value === "string" && !options.includes(value)) {
        if (mode === "strict" && source === "explicit") {
          throw invalidMCPParamOptionError2(key, value, model, options);
        }
        const fallback = typeof paramDef.default === "string" && options.includes(paramDef.default) ? paramDef.default : options[0];
        audit.push({ key, value, reason: "replaced_invalid_option", source, mapped_value: fallback });
        return fallback;
      }
      const type = typeof paramDef.type === "string" ? paramDef.type : "";
      if (type === "number" && typeof value === "number") {
        const min = typeof paramDef.min === "number" ? paramDef.min : void 0;
        const max = typeof paramDef.max === "number" ? paramDef.max : void 0;
        const clamped = Math.min(max ?? value, Math.max(min ?? value, value));
        if (clamped !== value) {
          if (mode === "strict" && source === "explicit") {
            throw new Error(`parameter "${key}" is outside the supported range for model "${modelDisplay2(model)}"`);
          }
          audit.push({ key, value, reason: "clamped_numeric_range", source, mapped_value: clamped });
        }
        return clamped;
      }
      return value;
    }
    function supportedParamMap2(model) {
      if (!model) return void 0;
      const params = Array.isArray(model.supported_params) ? model.supported_params : void 0;
      if (params) {
        const out = /* @__PURE__ */ new Map();
        for (const item of params) {
          if (!isRecord342(item)) continue;
          const key = stringField332(item.key);
          if (key) out.set(key, item);
        }
        return out;
      }
      if (Array.isArray(model.supported_param_keys)) {
        return new Map(model.supported_param_keys.flatMap((item) => {
          const key = stringField332(item);
          return key ? [[key, { key }]] : [];
        }));
      }
      const schema = isRecord342(model.params_schema) ? model.params_schema : void 0;
      const properties = isRecord342(schema?.properties) ? schema.properties : void 0;
      if (properties) {
        return new Map(Object.keys(properties).map((key) => [key, { key, ...isRecord342(properties[key]) ? properties[key] : {} }]));
      }
      return void 0;
    }
    function aspectRatioToImageSize2(aspectRatio, imageSizeParam) {
      const options = Array.isArray(imageSizeParam?.options) ? imageSizeParam.options.filter((item) => typeof item === "string") : [];
      const byRatio = {
        "1:1": ["2048x2048", "1024x1024", "4096x4096"],
        "4:3": ["2304x1728"],
        "3:4": ["1728x2304"],
        "16:9": ["2848x1600", "1792x1024", "1536x1024", "1280x720"],
        "9:16": ["1600x2848", "1024x1792", "1024x1536", "720x1280"]
      };
      for (const candidate of byRatio[aspectRatio] ?? []) {
        if (options.includes(candidate)) return candidate;
      }
      return void 0;
    }
    function normalizeParamValue2(value, paramDef) {
      if (!paramDef) return value;
      if (paramDef.type === "number" && typeof value === "string" && value.trim()) {
        const number = Number(value);
        return Number.isFinite(number) ? number : value;
      }
      if (paramDef.type === "boolean" && typeof value === "string") {
        if (value === "true") return true;
        if (value === "false") return false;
      }
      return value;
    }
    function numericDuration2(value) {
      if (value === void 0 || value === null || value === "") return void 0;
      const number = typeof value === "number" ? value : Number(value);
      return Number.isFinite(number) ? number : void 0;
    }
    function paramSource2(built, key) {
      if (built.explicitParamKeys.has(key)) return "explicit";
      if (built.defaultParamKeys.has(key)) return "default";
      return "compatible";
    }
    function unsupportedMCPParamError2(key, model, supported) {
      const allowed = Array.from(supported.keys()).sort();
      const err = new Error(`parameter "${key}" is not supported by model "${modelDisplay2(model)}"; supported parameters: ${allowed.join(", ") || "(none)"}`);
      Object.assign(err, {
        code: "UNSUPPORTED_PARAMETER",
        field: key,
        supported_params: allowed
      });
      return err;
    }
    function invalidMCPParamOptionError2(key, value, model, options) {
      const err = new Error(`parameter "${key}" value ${JSON.stringify(value)} is not supported by model "${modelDisplay2(model)}"; allowed values: ${options.join(", ")}`);
      Object.assign(err, {
        code: "INVALID_PARAMETER_OPTION",
        field: key,
        allowed_values: options
      });
      return err;
    }
    function normalizedJobId2(args) {
      const jobId = getOptionalNumeric2(args, "jobId") ?? getOptionalNumeric2(args, "job_id");
      if (jobId === void 0 || !Number.isInteger(jobId) || jobId <= 0) throw new Error("jobId must be a positive integer");
      return jobId;
    }
    function normalizedJobIds2(args) {
      const rawIds = Array.isArray(args.jobIds) ? args.jobIds : Array.isArray(args.job_ids) ? args.job_ids : void 0;
      const ids = rawIds ? rawIds.map((value) => idField222(value)).filter((value) => value !== void 0) : Array.isArray(args.items) ? args.items.map((item) => isRecord342(item) ? normalizedJobId2(item) : void 0).filter((value) => value !== void 0) : [];
      const unique = Array.from(new Set(ids));
      if (unique.length === 0) throw new Error("jobIds must contain at least one positive integer");
      return unique;
    }
    function modelPublicId2(value) {
      if (!isRecord342(value)) return void 0;
      return stringField332(value.model_id) ?? stringField332(value.logical_model_id) ?? stringField332(value.model_def_id);
    }
    function modelMatchesPublicId2(model, publicId) {
      const ids = [
        modelPublicId2(model),
        stringField332(model.model_id),
        stringField332(model.logical_model_id),
        stringField332(model.model_def_id),
        idField222(model.id) !== void 0 ? `backend.model.${idField222(model.id)}` : void 0,
        idField222(model.ID) !== void 0 ? `backend.model.${idField222(model.ID)}` : void 0
      ];
      return ids.includes(publicId);
    }
    function modelDisplay2(model) {
      return stringField332(model?.display_name) ?? stringField332(model?.short_name) ?? (model ? modelPublicId2(model) : void 0) ?? "selected model";
    }
    function stringField332(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function idField222(value) {
      const number = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
      return Number.isInteger(number) && number > 0 ? number : void 0;
    }
    function appendId2(ids, value) {
      const id = idField222(value);
      if (id !== void 0) ids.push(id);
    }
    function appendIds2(ids, value) {
      if (!Array.isArray(value)) return;
      for (const item of value) appendId2(ids, item);
    }
    function numericList22(value) {
      if (!Array.isArray(value)) return [];
      return value.map(idField222).filter((item) => item !== void 0);
    }
    function errorMessage43(error) {
      return error instanceof Error ? error.message : String(error);
    }
    function isTerminalStatus2(status) {
      return TERMINAL_STATUSES2.has(status.trim().toLowerCase());
    }
    function isSuccessfulStatus2(status) {
      return SUCCESS_STATUSES2.has(status.trim().toLowerCase());
    }
    var TERMINAL_STATUSES2 = /* @__PURE__ */ new Set(["succeeded", "succeed", "success", "completed", "complete", "done", "finished", "failed", "failure", "error", "cancelled", "canceled"]);
    var SUCCESS_STATUSES2 = /* @__PURE__ */ new Set(["succeeded", "succeed", "success", "completed", "complete", "done", "finished"]);
    function summarizeWorkspaceInterpretForAgent2(value) {
      if (!isRecord342(value)) return { result: value };
      const review = isRecord342(value.review) ? value.review : {};
      const summary = isRecord342(review.summary) ? review.summary : void 0;
      const changedEntities = Array.isArray(review.changedEntities) ? review.changedEntities : [];
      const semanticChanges = Array.isArray(review.semanticChanges) ? review.semanticChanges : [];
      const issues = Array.isArray(review.issues) ? review.issues : [];
      return {
        schema: "movscript.workspace-interpret-refresh-agent-summary.v1",
        operation: value.operation,
        status: value.status,
        ...summary ? { summary } : {},
        changedEntities: changedEntities.map(summarizeChangedEntity2).filter(Boolean),
        semanticChanges: semanticChanges.map(summarizeSemanticChange2).filter(Boolean),
        issues: issues.map(summarizeIssue2).filter(Boolean),
        fullResult: "available in result.data"
      };
    }
    function summarizeChangedEntity2(value) {
      if (!isRecord342(value)) return void 0;
      const fieldChanges = Array.isArray(value.fieldChanges) ? value.fieldChanges : [];
      const fields = fieldChanges.map((item) => isRecord342(item) && typeof item.field === "string" ? item.field : void 0).filter((item) => !!item);
      return {
        entityKind: value.entityKind,
        ...value.id !== void 0 ? { id: value.id } : {},
        ...typeof value.clientId === "string" ? { clientId: value.clientId } : {},
        state: value.state,
        ...fields.length > 0 ? { fields } : {}
      };
    }
    function summarizeSemanticChange2(value) {
      if (!isRecord342(value)) return void 0;
      return {
        ...isRecord342(value.entity) ? { entity: pick2(value.entity, ["kind", "id"]) } : {},
        kind: value.kind,
        businessKind: value.businessKind,
        propagation: value.propagation,
        ...Array.isArray(value.fields) ? { fields: value.fields } : {}
      };
    }
    function summarizeIssue2(value) {
      if (!isRecord342(value)) return void 0;
      return pick2(value, ["severity", "message"]);
    }
    function pick2(value, keys) {
      const result = {};
      for (const key of keys) {
        if (value[key] !== void 0) result[key] = value[key];
      }
      return result;
    }
    async function callTool2(params) {
      const name = getStringParam2(params, "name");
      const args = getObjectParam2(params, "arguments");
      switch (name) {
        case "movscript_focus_get":
          return toolText2(getFocus2());
        case "system_model_list":
        case "generation_model_list":
        case "movscript_model_list":
          return toolText2(await listModels2(args));
        case "generation_capability_list":
          return toolText2(await listGenerationCapabilities2(args));
        case "generation_prepare":
          return toolText2(await prepareGeneration2(args));
        case "generation_submit":
          return toolText2(await submitUnifiedGeneration2(args));
        case "generation_job_get":
          return toolText2(await getUnifiedGenerationJob2(args));
        case "generation_job_get_batch":
          return toolText2(await getUnifiedGenerationJobs2(args));
        case "generation_result_register":
          return toolText2(await registerGenerationResult2(args));
        case "system_resource_library_query":
        case "movscript_resource_library_query":
          return toolText2(await queryResourceLibrary2(args));
        case "system_resource_library_open":
        case "movscript_resource_library_open":
          return toolText2(openResourceLibrary2(args));
        case "system_resource_image_read":
        case "movscript_resource_image_read":
          return await readResourceImageForVision2(args);
        case "system_resource_image_transform_to_resource":
        case "movscript_resource_image_transform_to_resource":
          return toolText2(await transformResourceImageToResource2(args));
        case "system_resource_video_extract_frames":
        case "movscript_resource_video_extract_frames":
          return await extractResourceVideoFramesForVision2(args);
        case "system_resource_video_probe":
        case "movscript_resource_video_probe":
          return toolText2(await probeResourceVideo2(args));
        case "system_resource_video_extract_frame_to_resource":
        case "movscript_resource_video_extract_frame_to_resource":
          return toolText2(await extractResourceVideoFrameToResource2(args));
        case "system_resource_video_extract_frames_to_resources":
        case "movscript_resource_video_extract_frames_to_resources":
          return toolText2(await extractResourceVideoFramesToResources2(args));
        case "system_resource_video_trim_to_resource":
        case "movscript_resource_video_trim_to_resource":
          return toolText2(await trimResourceVideoToResource2(args));
        case "system_resource_video_compose_to_resource":
        case "system_resource_video_concat_to_resource":
        case "movscript_resource_video_compose_to_resource":
        case "movscript_resource_video_concat_to_resource":
          return toolText2(await composeResourceVideosToResource2(args));
        case "system_resource_video_contact_sheet_to_resource":
        case "movscript_resource_video_contact_sheet_to_resource":
          return toolText2(await createResourceVideoContactSheetToResource2(args));
        case "system_resource_video_extract_audio_to_resource":
        case "movscript_resource_video_extract_audio_to_resource":
          return toolText2(await extractResourceVideoAudioToResource2(args));
        case "system_resource_image_annotate":
        case "movscript_resource_image_annotate":
          return await annotateResourceImage2(args);
        case "system_resource_upload":
        case "movscript_resource_upload":
          return toolText2(await uploadAgentImageResource2(args));
        case "system_resource_upload_batch":
        case "movscript_resource_upload_batch":
          return toolText2(await uploadAgentImageResources2(args));
        case "system_shot_library_query":
        case "movscript_shot_library_query":
          return toolText2(await queryShotLibrary2(args));
        case "system_shot_group_create":
        case "movscript_shot_group_create":
          return toolText2(await createShotGroup2(args));
        case "system_shot_group_get":
        case "movscript_shot_group_get":
          return toolText2(await getShotGroup2(args));
        case "system_shot_group_add_shots":
        case "movscript_shot_group_add_shots":
          return toolText2(await addShotsToGroup2(args));
        case "system_video_shot_cuts_analyze":
        case "movscript_video_shot_cuts_analyze":
          return toolText2(await analyzeVideoShotCuts2(args));
        case "system_external_resource_source_list":
        case "movscript_external_resource_source_list":
          return toolText2(await listExternalResourceSources2(args));
        case "system_external_resource_search":
        case "movscript_external_resource_search":
          return toolText2(await searchExternalResources2(args));
        case "system_artifact_upload_export":
          return toolText2(await artifactUploadExport2(args));
        case "system_artifact_upload_hls_stream":
          return toolText2(await artifactUploadHlsStream2(args));
        case "system_artifact_get_stream":
          return toolText2(await artifactGetStream2(args));
        case "domain_get_model":
          return toolText2(await domainGetModel2(args));
        case "domain_query_entities":
          return toolText2(await domainQueryEntities2(args));
        case "domain_query_settings":
          return toolText2(await domainQuerySettings2(args));
        case "domain_query_assets":
          return toolText2(await domainQueryAssets2(args));
        case "domain_query_production_context":
          return toolText2(await domainQueryProductionContext2(args));
        case "domain_read_content_workspace":
          return toolText2(await domainReadContentWorkspace2(args));
        case "domain_read_content_workspace_snapshot":
          return toolText2(await domainReadContentWorkspaceSnapshot2(args));
        case "domain_read_project_context_snapshot":
          return toolText2(await domainReadProjectContextSnapshot2(args));
        case "domain_derive_content_unit_artifact":
          return toolText2(await domainInterpretContentUnitArtifact2(args));
        case "domain_build_content_unit_backend_prompt":
          return toolText2(await domainBuildContentUnitBackendPrompt2(args));
        case "domain_read_preview_timeline":
          return toolText2(await domainReadPreviewTimeline2(args));
        case "domain_read_production_timeline":
          return toolText2(await domainReadProductionTimeline2(args));
        case "domain_read_scene_moment_edit_plan":
          return toolText2(await domainReadSceneMomentEditPlan2(args));
        case "domain_read_production_edit_plan":
          return toolText2(await domainReadProductionEditPlan2(args));
        case "domain_create_editing_project_context":
          return toolText2(await domainCreateEditingProjectContext2(args));
        case "domain_read_scene_moment_timeline":
          return toolText2(await domainReadSceneMomentTimeline2(args));
        case "domain_read_content_unit_runtime_panel":
          return toolText2(await domainReadContentUnitRuntimePanel2(args));
        case "domain_read_content_unit_generation_prompt":
        case "domain_read_content_unit_input_version":
          return toolText2(await domainReadContentUnitGenerationPrompt2(args));
        case "domain_read_content_unit_dependency_report":
          return toolText2(await domainReadContentUnitDependencyReport2(args));
        case "domain_read_content_unit_selection_validity":
          return toolText2(await domainReadContentUnitSelectionValidity2(args));
        case "domain_upsert_project_standards":
          return toolText2(await domainUpsertProjectStandards2(args));
        case "domain_upsert_setting":
          return toolText2(await domainUpsertSetting2(args));
        case "domain_upsert_setting_state":
          return toolText2(await domainUpsertSettingState2(args));
        case "domain_upsert_setting_tree":
          return toolText2(await domainUpsertSettingTree2(args));
        case "domain_upsert_asset":
          return toolText2(await domainUpsertAsset2(args));
        case "domain_certify_asset_provider":
        case "domain_certify_asset_seedance2":
          return toolText2(await domainCertifyAssetProvider2(args));
        case "domain_query_remote_asset_groups":
          return toolText2(await domainQueryRemoteAssetGroups2(args));
        case "domain_query_remote_assets":
          return toolText2(await domainQueryRemoteAssets2(args));
        case "domain_upsert_script":
          return toolText2(await domainUpsertScript2(args));
        case "domain_read_script_source":
          return toolText2(await domainReadScriptSource2(args));
        case "domain_snapshot_script_version":
          return toolText2(await domainSnapshotScriptVersion2(args));
        case "domain_upsert_content_unit":
          return toolText2(await domainUpsertContentUnit2(args));
        case "domain_upsert_production":
          return toolText2(await domainUpsertProduction2(args));
        case "domain_upsert_production_tree":
          return toolText2(await domainUpsertProductionTree2(args));
        case "domain_upsert_timeline_namespace_tree":
          return toolText2(await domainUpsertTimelineNamespaceTree2(args));
        case "domain_upsert_segment":
          return toolText2(await domainUpsertSegment2(args));
        case "domain_upsert_scene_moment":
          return toolText2(await domainUpsertSceneMoment2(args));
        case "domain_upsert_keyframe":
          return toolText2(await domainUpsertKeyframe2(args));
        case "domain_upsert_storyboard":
          return toolText2(await domainUpsertStoryboard2(args));
        case "domain_upsert_audio_cue":
          return toolText2(await domainUpsertAudioCue2(args));
        case "domain_upsert_expression_unit":
          return toolText2(await domainUpsertExpressionUnit2(args));
        case "domain_update_content_unit_prompt":
          return toolText2(await domainUpdateContentUnitPrompt2(args));
        case "domain_update_entity_transition":
          return toolText2(await domainUpdateEntityTransition2(args));
        case "domain_update_storyboard_timeline":
          return toolText2(await domainUpdateStoryboardTimeline2(args));
        case "domain_append_candidate":
          return toolText2(await domainAppendCandidate2(args));
        case "domain_create_content_candidate":
          return toolText2(await domainCreateContentCandidate2(args));
        case "domain_register_raw_resource_as_content_unit_candidate":
          return toolText2(await domainRegisterRawResourceAsContentUnitCandidate2(args));
        case "domain_create_content_candidate_batch":
          return toolText2(await domainCreateContentCandidateBatch2(args));
        case "domain_create_asset_slot_candidate":
          return toolText2(await domainCreateAssetSlotCandidate2(args));
        case "domain_create_keyframe_candidate":
          return toolText2(await domainCreateKeyframeCandidate2(args));
        case "domain_select_content_unit_candidate":
          return toolText2(await domainSelectContentUnitCandidate2(args));
        case "domain_select_content_unit_candidate_batch":
          return toolText2(await domainSelectContentUnitCandidateBatch2(args));
        case "domain_decide_content_unit_candidate":
          return toolText2(await domainDecideContentUnitCandidate2(args));
        case "domain_select_candidate":
          return toolText2(await domainSelectCandidate2(args));
        case "domain_update_candidate":
          return toolText2(await domainUpdateCandidate2(args));
        case "domain_unlock_candidate":
          return toolText2(await domainUnlockCandidate2(args));
        case "domain_delete_entity":
          return toolText2(await domainDeleteEntity2(args));
        case "domain_overview":
          return toolText2(await domainOverview2(args));
        case "domain_read_production_work_plan":
          return toolText2(await domainReadProductionWorkPlan2(args));
        case "domain_production_status_summary":
          return toolText2(await domainProductionStatusSummary2(args));
        case "domain_inspect":
          return toolText2(await domainInspect2(args));
        case "domain_review":
          return toolText2(await domainReview2(args));
        case "domain_interpret":
          return interpretToolText2(await domainInterpret2(args));
        case "domain_regeneration_plan":
          return toolText2(await domainRegenerationPlan2(args));
        case "editing_project_create":
          return toolText2(await editingProjectCreate2(args));
        case "editing_project_create_from_edit_plan":
          return toolText2(await editingProjectCreateFromEditPlan2(args));
        case "editing_project_create_from_edit_decisions":
          return toolText2(await editingProjectCreateFromEditDecisions2(args));
        case "editing_video_compose":
          return toolText2(await editingVideoCompose2(args));
        case "editing_project_add_asset":
          return toolText2(await editingProjectAddAsset2(args));
        case "editing_project_remove_asset":
          return toolText2(await editingProjectRemoveAsset2(args));
        case "editing_project_get":
          return toolText2(await editingProjectGet2(args));
        case "editing_project_update_settings":
          return toolText2(await editingProjectUpdateSettings2(args));
        case "editing_project_save":
          return toolText2(await editingProjectSave2(args));
        case "editing_timeline_apply_commands":
          return toolText2(await editingTimelineApplyCommands2(args));
        case "editing_timeline_add_track":
          return toolText2(await editingTimelineAddTrack2(args));
        case "editing_timeline_remove_track":
          return toolText2(await editingTimelineRemoveTrack2(args));
        case "editing_timeline_add_clip":
          return toolText2(await editingTimelineAddClip2(args));
        case "editing_timeline_update_clip":
          return toolText2(await editingTimelineUpdateClip2(args));
        case "editing_timeline_split_clip":
          return toolText2(await editingTimelineSplitClip2(args));
        case "editing_timeline_move_clip":
          return toolText2(await editingTimelineMoveClip2(args));
        case "editing_timeline_delete_clip":
          return toolText2(await editingTimelineDeleteClip2(args));
        case "editing_timeline_validate":
          return toolText2(await editingTimelineValidate2(args));
        case "editing_runtime_capabilities_get":
          return toolText2(await editingRuntimeCapabilitiesGet2(args));
        case "editing_task_render_create":
          return toolText2(await editingTaskRenderCreate2(args));
        case "editing_task_hls_create":
          return toolText2(await editingTaskHlsCreate2(args));
        case "editing_task_transcode_create":
          return toolText2(await editingTaskTranscodeCreate2(args));
        case "editing_task_reframe_create":
          return toolText2(await editingTaskReframeCreate2(args));
        case "editing_task_get":
          return toolText2(await editingTaskGet2(args));
        case "editing_task_cancel":
          return toolText2(await editingTaskCancel2(args));
        case "editing_task_logs_get":
          return toolText2(await editingTaskLogsGet2(args));
        case "editing_export_import_resource":
          return toolText2(await editingExportImportResource2(args));
        case "editing_export_save_local":
          return toolText2(await editingExportSaveLocal2(args));
        case "editing_export_publish_hls":
          return toolText2(await editingExportPublishHls2(args));
        case "editing_export_create_candidate":
          return toolText2(await editingExportCreateCandidate2(args));
        case "system_project_create":
        case "movscript_project_create":
          return toolText2(await createProject3(args));
        case "system_project_init":
        case "movscript_project_init":
          return toolText2(await initLocalProject2(args));
        case "system_project_fetch":
        case "system_project_open":
        case "movscript_project_open":
        case "movscript_project_fetch":
          return toolText2(await fetchLocalProject2(args));
        default:
          throw new Error(`Unknown tool: ${name}`);
      }
    }
    function interpretToolText2(value) {
      return toolText2(value, summarizeWorkspaceInterpretForAgent2(value));
    }
    function listTools2() {
      const baseProjectTools = projectTools2();
      const baseGenerationTools = generationTools2();
      const baseResourceLibraryTools = resourceLibraryTools2();
      const baseResourceMediaTools = resourceMediaTools2();
      const baseShotLibraryTools = shotLibraryTools2();
      const baseExternalResourceTools = externalResourceTools2();
      return [
        ...systemTools2({
          models: modelTools2(),
          generation: baseGenerationTools,
          resourceLibrary: baseResourceLibraryTools,
          resourceMedia: baseResourceMediaTools,
          shotLibrary: baseShotLibraryTools,
          externalResources: baseExternalResourceTools,
          project: baseProjectTools
        }),
        ...domainTools2(),
        ...editingTools2(),
        ...artifactTools2(),
        ...focusTools2(),
        ...modelTools2(),
        ...baseResourceLibraryTools,
        ...baseResourceMediaTools,
        ...baseShotLibraryTools,
        ...baseExternalResourceTools,
        ...baseGenerationTools,
        ...baseProjectTools
      ];
    }
    function systemTools2(input3) {
      return [
        ...renameTools2(input3.project, {
          movscript_project_create: "system_project_create",
          movscript_project_init: "system_project_init",
          movscript_project_open: "system_project_open",
          movscript_project_fetch: "system_project_fetch"
        }),
        ...renameTools2(input3.models, { generation_model_list: "system_model_list" }),
        ...renameTools2(input3.resourceLibrary, {
          movscript_resource_library_query: "system_resource_library_query",
          movscript_resource_library_open: "system_resource_library_open"
        }),
        ...renameTools2(input3.resourceMedia, {
          movscript_resource_image_read: "system_resource_image_read",
          movscript_resource_image_transform_to_resource: "system_resource_image_transform_to_resource",
          movscript_resource_video_extract_frames: "system_resource_video_extract_frames",
          movscript_resource_video_probe: "system_resource_video_probe",
          movscript_resource_video_extract_frame_to_resource: "system_resource_video_extract_frame_to_resource",
          movscript_resource_video_extract_frames_to_resources: "system_resource_video_extract_frames_to_resources",
          movscript_resource_video_trim_to_resource: "system_resource_video_trim_to_resource",
          movscript_resource_video_compose_to_resource: "system_resource_video_compose_to_resource",
          movscript_resource_video_concat_to_resource: "system_resource_video_concat_to_resource",
          movscript_resource_video_contact_sheet_to_resource: "system_resource_video_contact_sheet_to_resource",
          movscript_resource_video_extract_audio_to_resource: "system_resource_video_extract_audio_to_resource",
          movscript_resource_image_annotate: "system_resource_image_annotate",
          movscript_resource_upload: "system_resource_upload",
          movscript_resource_upload_batch: "system_resource_upload_batch"
        }),
        ...renameTools2(input3.shotLibrary, {
          movscript_shot_library_query: "system_shot_library_query",
          movscript_shot_group_create: "system_shot_group_create",
          movscript_shot_group_get: "system_shot_group_get",
          movscript_shot_group_add_shots: "system_shot_group_add_shots",
          movscript_video_shot_cuts_analyze: "system_video_shot_cuts_analyze"
        }),
        ...renameTools2(input3.externalResources, {
          movscript_external_resource_source_list: "system_external_resource_source_list",
          movscript_external_resource_search: "system_external_resource_search"
        })
      ];
    }
    function renameTools2(tools, names) {
      return tools.filter((tool) => names[tool.name]).map((tool) => ({
        ...tool,
        name: names[tool.name],
        description: `${tool.description} New system-level alias for ${tool.name}.`
      }));
    }
    var MCP_DEBUG3 = process.env.MOVSCRIPT_MCP_DEBUG === "1";
    async function handleJSONRPC2(req, httpRequestId) {
      const startedAt = Date.now();
      const isNotification = !Object.prototype.hasOwnProperty.call(req, "id");
      const id = isNotification ? null : req.id ?? null;
      if (MCP_DEBUG3) {
        console.info(`[mcp] rpc start httpRequestId=${httpRequestId ?? "n/a"} rpcId=${String(id)} method=${req.method ?? ""}`);
      }
      if (req.jsonrpc !== "2.0" || !req.method) {
        if (isNotification) return void 0;
        return makeError2(id, -32600, "Invalid Request");
      }
      try {
        switch (req.method) {
          case "initialized":
          case "notifications/cancelled":
          case "notifications/progress":
            return void 0;
          case "initialize":
            return makeResult2(id, {
              protocolVersion: "2025-06-18",
              serverInfo: { name: "movscript-core-mcp", version: "0.1.0" },
              capabilities: {
                resources: {},
                tools: {}
              }
            });
          case "resources/list":
            return makeResult2(id, { resources: listResources2() });
          case "resources/read":
            return makeResult2(id, await readResource2(getStringParam2(req.params, "uri")));
          case "tools/list":
            return makeResult2(id, { tools: listTools2() });
          case "tools/call":
            return makeResult2(id, await callTool2(req.params));
          default:
            if (isNotification) return void 0;
            return makeError2(id, -32601, `Method not found: ${req.method}`);
        }
      } catch (error) {
        console.error(`[mcp] rpc error httpRequestId=${httpRequestId ?? "n/a"} rpcId=${String(id)} method=${req.method} elapsedMs=${Date.now() - startedAt}`, error);
        if (isNotification) return void 0;
        return makeError2(id, -32e3, error instanceof Error ? error.message : String(error), errorData2(error));
      } finally {
        if (MCP_DEBUG3) {
          console.info(`[mcp] rpc finish httpRequestId=${httpRequestId ?? "n/a"} rpcId=${String(id)} method=${req.method ?? ""} elapsedMs=${Date.now() - startedAt}`);
        }
      }
    }
    var MCP_DEBUG22 = process.env.MOVSCRIPT_MCP_DEBUG === "1";
    var import_child_process32 = __require("child_process");
    var import_fs42 = __require("fs");
    var import_path112 = __require("path");
    var LOCAL_RUNTIME_DAEMON_APP_ID2 = "movscript.local-node";
    var LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE2 = "movscript.local-node.control";
    var LOCAL_RUNTIME_DAEMON_GATEWAY_SERVICE2 = "movscript.local-node.gateway";
    var REQUIRED_LOCAL_RUNTIME_DAEMON_SERVICES2 = [
      LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE2,
      LOCAL_RUNTIME_DAEMON_GATEWAY_SERVICE2,
      "movscript.project.service",
      "movscript.editing.service",
      "movscript.canvas.service",
      "movscript.local-surface.host",
      "movscript.media.pipeline"
    ];
    var LOCAL_DATA_SERVICE2 = "movscript.data.service";
    async function ensureLocalRuntimeDaemon2(options) {
      const startupTimeoutMs = options.startupTimeoutMs ?? 15e3;
      const stopTimeoutMs = options.stopTimeoutMs ?? 5e3;
      const initial = await probeLocalRuntimeDaemon2(options.homeDir);
      if (!options.forceRestart && initial.available && localRuntimeServicesReady2(initial) && localRuntimeMatchesRequest2(initial, options)) {
        await localRuntimeControlRequest2(options.homeDir, "POST", "/touch").catch(() => void 0);
        return { status: "ready", reused: true, ...initial };
      }
      const lock = await acquireLocalRuntimeStartupLock2(options.homeDir, startupTimeoutMs);
      if (!lock) {
        const reused = await waitForLocalRuntimeReady2(options.homeDir, startupTimeoutMs, options);
        if (reused) return { status: "ready", reused: true, ...reused };
        const lastProbe = await probeLocalRuntimeDaemon2(options.homeDir);
        throw new Error(`MovScript local runtime daemon did not become ready while another startup was in progress; ${localRuntimeReadinessSummary2(lastProbe, options)}`);
      }
      try {
        const probe = await probeLocalRuntimeDaemon2(options.homeDir);
        if (probe.available) {
          if (!options.forceRestart && localRuntimeServicesReady2(probe) && localRuntimeMatchesRequest2(probe, options)) {
            await localRuntimeControlRequest2(options.homeDir, "POST", "/touch").catch(() => void 0);
            return { status: "ready", reused: true, ...probe };
          }
          await stopAvailableLocalRuntimeDaemon2(options.homeDir, stopTimeoutMs);
        }
        const child = (0, import_child_process32.spawn)(process.execPath, [options.entrypoint, ...options.runArgs ?? ["daemon", "run"]], {
          cwd: options.cwd,
          env: {
            ...process.env,
            ...options.env ?? {},
            MOVSCRIPT_HOME: options.homeDir
          },
          detached: true,
          stdio: "ignore"
        });
        child.unref();
        const ready = await waitForLocalRuntimeReady2(options.homeDir, startupTimeoutMs, options);
        if (ready) return { status: "ready", reused: false, launcherPid: child.pid, ...ready };
        const lastProbe = await probeLocalRuntimeDaemon2(options.homeDir);
        throw new Error(`MovScript local runtime daemon did not become ready within ${startupTimeoutMs}ms; ${localRuntimeReadinessSummary2(lastProbe, options)}`);
      } finally {
        lock.release();
      }
    }
    async function probeLocalRuntimeDaemon2(homeDir) {
      const snapshot = readRuntimeHomeSnapshot2(homeDir);
      const app = findRuntimeApp2(snapshot, LOCAL_RUNTIME_DAEMON_APP_ID2) ?? snapshot.apps.find((record) => record.applicationId === LOCAL_RUNTIME_DAEMON_APP_ID2);
      const appProbeDetails = localRuntimeAppProbeDetails2(app);
      const endpoint = endpointURL42(findRuntimeEndpoint2(snapshot, LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE2));
      if (!endpoint) return { available: false, ...appProbeDetails };
      try {
        const healthResponse = await fetch(`${endpoint}/health`, { signal: AbortSignal.timeout(1e3) });
        const status = healthResponse.ok ? await fetch(`${endpoint}/status`, { signal: AbortSignal.timeout(1e3) }).then((item) => item.ok ? item.json() : {}).catch(() => ({})) : {};
        return {
          available: healthResponse.ok,
          endpoint,
          ...healthResponse.ok ? await healthResponse.json() : { error: `HTTP ${healthResponse.status}` },
          ...status
        };
      } catch (error) {
        return { available: false, endpoint, ...appProbeDetails, error: errorMessage52(error) };
      }
    }
    async function stopLocalRuntimeDaemon2(homeDir, options = {}) {
      const probe = await probeLocalRuntimeDaemon2(homeDir);
      if (probe.available) return await localRuntimeControlRequest2(homeDir, "POST", "/shutdown");
      if (!options.force) return { status: "not_running", ...probe };
      const app = findRuntimeApp2(readRuntimeHomeSnapshot2(homeDir), LOCAL_RUNTIME_DAEMON_APP_ID2);
      if (app?.pid && app.pid !== process.pid) {
        try {
          process.kill(app.pid, "SIGTERM");
          return { status: "stopping", forced: true, pid: app.pid };
        } catch (error) {
          return { status: "error", forced: true, pid: app.pid, error: errorMessage52(error) };
        }
      }
      return { status: "not_running", forced: true };
    }
    async function localRuntimeControlRequest2(homeDir, method, path) {
      const snapshot = readRuntimeHomeSnapshot2(homeDir);
      const endpoint = endpointURL42(findRuntimeEndpoint2(snapshot, LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE2));
      if (!endpoint) throw new Error("MovScript local runtime daemon control endpoint was not found");
      const response = await fetch(`${endpoint}${path}`, { method, signal: AbortSignal.timeout(3e3) });
      const payload = await response.json().catch(() => ({}));
      if (!response.ok) throw new Error(`local runtime daemon ${path} failed with HTTP ${response.status}: ${JSON.stringify(payload)}`);
      return payload;
    }
    function localRuntimeServicesReady2(status) {
      return missingLocalRuntimeServices2(status).length === 0;
    }
    function missingLocalRuntimeServices2(status) {
      const requiredServices = status.dataPlane === "local" ? [...REQUIRED_LOCAL_RUNTIME_DAEMON_SERVICES2, LOCAL_DATA_SERVICE2] : REQUIRED_LOCAL_RUNTIME_DAEMON_SERVICES2;
      if (!Array.isArray(status.services)) return [...requiredServices];
      const readyServices = new Set(status.services.flatMap((item) => {
        if (!item || typeof item !== "object") return [];
        const record = item;
        return record.ready === true && typeof record.serviceName === "string" ? [record.serviceName] : [];
      }));
      return requiredServices.filter((serviceName) => !readyServices.has(serviceName));
    }
    function localRuntimeReadinessSummary2(status, options) {
      const details = [];
      if (status.endpoint) details.push(`controlEndpoint=${status.endpoint}`);
      details.push(`available=${status.available}`);
      if (typeof status.status === "string") details.push(`status=${status.status}`);
      if (typeof status.error === "string") details.push(`error=${status.error}`);
      if (typeof status.pid === "number") details.push(`pid=${status.pid}`);
      if (typeof status.dataPlane === "string") details.push(`dataPlane=${status.dataPlane}`);
      if (localRuntimeHasIdentityDetails2(status) && !localRuntimeMatchesIdentity2(status, options.identity)) {
        details.push(`identityMismatch=${JSON.stringify({
          expected: options.identity ?? {},
          actual: {
            pluginVersion: status.pluginVersion,
            pluginRoot: status.pluginRoot,
            runtimeVersion: status.runtimeVersion,
            runtimeRoot: status.runtimeRoot
          }
        })}`);
      }
      if (!localRuntimeMatchesRequestedDataPlane2(status, options.env)) {
        details.push(`dataPlaneMismatch=expected ${requestedLocalRuntimeDataPlane2(options.env) ?? "any"}`);
      }
      if (!localRuntimeMatchesRequestedDataServiceURL2(status, options.env)) {
        details.push("dataServiceURLMismatch=true");
      }
      const missingServices = missingLocalRuntimeServices2(status);
      if (missingServices.length > 0) details.push(`missingServices=${missingServices.join(",")}`);
      return details.join("; ") || "last probe was empty";
    }
    function localRuntimeHasIdentityDetails2(status) {
      return typeof status.pluginVersion === "string" || typeof status.pluginRoot === "string" || typeof status.runtimeVersion === "string" || typeof status.runtimeRoot === "string";
    }
    function localRuntimeAppProbeDetails2(app) {
      if (!app) return {};
      const metadata = app.raw.metadata && typeof app.raw.metadata === "object" ? app.raw.metadata : {};
      return {
        status: app.status,
        pid: app.pid,
        pluginVersion: metadata.pluginVersion,
        pluginRoot: metadata.pluginRoot,
        runtimeVersion: metadata.runtimeVersion,
        runtimeRoot: metadata.runtimeRoot,
        dataPlane: metadata.dataPlane,
        dataServiceURL: metadata.dataServiceURL,
        ...typeof metadata.error === "string" ? { error: metadata.error } : {}
      };
    }
    function localRuntimeMatchesIdentity2(status, identity) {
      if (!identity) return true;
      if (identity.pluginVersion && status.pluginVersion !== identity.pluginVersion) return false;
      if (identity.pluginRoot && status.pluginRoot !== identity.pluginRoot) return false;
      if (identity.runtimeVersion && status.runtimeVersion !== identity.runtimeVersion) return false;
      if (identity.runtimeRoot && status.runtimeRoot !== identity.runtimeRoot) return false;
      return true;
    }
    function localRuntimeMatchesRequestedDataPlane2(status, env) {
      const expected = requestedLocalRuntimeDataPlane2(env);
      if (!expected) return true;
      return status.dataPlane === expected;
    }
    function localRuntimeMatchesRequestedDataServiceURL2(status, env) {
      const expected = normalizeDataServiceURL3(env?.MOVSCRIPT_DATA_SERVICE_URL);
      if (!expected) return true;
      return normalizeDataServiceURL3(status.dataServiceURL) === expected;
    }
    function localRuntimeMatchesRequest2(status, options) {
      return localRuntimeMatchesIdentity2(status, options.identity) && localRuntimeMatchesRequestedDataPlane2(status, options.env) && localRuntimeMatchesRequestedDataServiceURL2(status, options.env);
    }
    function requestedLocalRuntimeDataPlane2(env) {
      const explicit = (env?.MOVSCRIPT_LOCAL_DAEMON_DATA_PLANE ?? env?.MOVSCRIPT_LOCAL_NODE_DATA_PLANE ?? "").trim().toLowerCase();
      if (explicit === "local" || explicit === "cloud" || explicit === "external") return explicit;
      return void 0;
    }
    function normalizeDataServiceURL3(value) {
      if (typeof value !== "string") return void 0;
      const raw = value.trim();
      if (!raw) return void 0;
      try {
        const url = new URL(raw);
        url.hash = "";
        url.pathname = url.pathname.replace(/\/+$/, "") || "/";
        return url.toString().replace(/\/$/, "");
      } catch {
        return raw.replace(/\/+$/, "");
      }
    }
    async function waitForLocalRuntimeStop2(homeDir, timeoutMs) {
      const startedAt = Date.now();
      while (Date.now() - startedAt < timeoutMs) {
        const probe = await probeLocalRuntimeDaemon2(homeDir);
        if (!probe.available) return;
        await delay3(150);
      }
    }
    async function stopAvailableLocalRuntimeDaemon2(homeDir, timeoutMs) {
      await stopLocalRuntimeDaemon2(homeDir).catch(() => void 0);
      await waitForLocalRuntimeStop2(homeDir, timeoutMs);
      const probe = await probeLocalRuntimeDaemon2(homeDir);
      if (probe.available) {
        throw new Error("MovScript local runtime daemon is still reachable after shutdown; refusing to start another daemon");
      }
    }
    async function waitForLocalRuntimeReady2(homeDir, timeoutMs, options) {
      const startedAt = Date.now();
      while (Date.now() - startedAt < timeoutMs) {
        const probe = await probeLocalRuntimeDaemon2(homeDir);
        if (probe.available && localRuntimeServicesReady2(probe) && localRuntimeMatchesRequest2(probe, options)) return probe;
        await delay3(150);
      }
      return void 0;
    }
    async function acquireLocalRuntimeStartupLock2(homeDir, timeoutMs) {
      const lockDir = (0, import_path112.join)(homeDir, "runtime", "locks", `${LOCAL_RUNTIME_DAEMON_APP_ID2}.startup.lock`);
      const metadataPath = (0, import_path112.join)(lockDir, "owner.json");
      const startedAt = Date.now();
      while (Date.now() - startedAt < timeoutMs) {
        (0, import_fs42.mkdirSync)((0, import_path112.dirname)(lockDir), { recursive: true });
        try {
          (0, import_fs42.mkdirSync)(lockDir);
          (0, import_fs42.writeFileSync)(metadataPath, JSON.stringify({
            pid: process.pid,
            createdAt: (/* @__PURE__ */ new Date()).toISOString()
          }), "utf8");
          return {
            release: () => {
              (0, import_fs42.rmSync)(lockDir, { recursive: true, force: true });
            }
          };
        } catch (error) {
          const code = typeof error === "object" && error ? error.code : void 0;
          if (code !== "EEXIST") throw error;
          if (isStaleStartupLock2(metadataPath)) {
            (0, import_fs42.rmSync)(lockDir, { recursive: true, force: true });
            continue;
          }
          await delay3(150);
        }
      }
      return void 0;
    }
    function isStaleStartupLock2(metadataPath) {
      if (!(0, import_fs42.existsSync)(metadataPath)) return false;
      try {
        const metadata = JSON.parse((0, import_fs42.readFileSync)(metadataPath, "utf8"));
        const pid = typeof metadata.pid === "number" ? metadata.pid : void 0;
        return !pidIsAlive2(pid);
      } catch {
        return false;
      }
    }
    function endpointURL42(endpoint) {
      if (!endpoint) return void 0;
      if (endpoint.url) return endpoint.url;
      if (endpoint.baseURL) return endpoint.baseURL;
      if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
      if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
      return void 0;
    }
    function delay3(ms) {
      return new Promise((resolveDelay) => setTimeout(resolveDelay, ms));
    }
    function errorMessage52(error) {
      return error instanceof Error ? error.message : String(error);
    }
    var DEFAULT_LOCAL_BACKEND2 = "http://localhost:8766";
    var MCP_HOST_DEBUG2 = process.env.MOVSCRIPT_MCP_HOST_DEBUG === "1";
    var LOCAL_NODE_CONTROL_SERVICE3 = LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE2;
    var LOCAL_NODE_GATEWAY_SERVICE22 = LOCAL_RUNTIME_DAEMON_GATEWAY_SERVICE2;
    var DATA_SERVICE22 = "movscript.data.service";
    var PROJECT_SERVICE2 = "movscript.project.service";
    var EDITING_SERVICE2 = "movscript.editing.service";
    var LOCAL_SURFACE_HOST_SERVICE2 = "movscript.local-surface.host";
    var MEDIA_PIPELINE_SERVICE2 = "movscript.media.pipeline";
    var adminTools2 = adminCommandSpecs2.map((tool) => ({
      name: tool.mcpToolName,
      description: tool.description,
      inputSchema: tool.inputSchema
    }));
    var hostTools2 = [
      ...adminTools2,
      {
        name: "movscript_runtime_status",
        description: "Detect MovScript local/cloud backend availability, current project source, Desktop enhancement status, and the recommended runtime mode. This tool is read-only.",
        inputSchema: objectSchema42({
          workspaceDir: { type: "string", description: "Optional MovScript workspace or project directory to inspect." },
          workspace_dir: { type: "string", description: "Alias for workspaceDir." },
          homeDir: { type: "string", description: "Optional MovScript Home directory for runtime discovery. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
          home_dir: { type: "string", description: "Alias for homeDir." },
          movscriptHome: { type: "string", description: "Alias for homeDir." },
          movscript_home: { type: "string", description: "Alias for homeDir." },
          projectDir: { type: "string", description: "Optional project source directory to inspect." },
          project_dir: { type: "string", description: "Alias for projectDir." },
          projectId: { type: "string", description: "Optional project id to use when constructing Local Surface Host URLs." },
          project_id: { type: "string", description: "Alias for projectId." },
          productionId: { type: "string", description: "Optional legacy production-scope id to include in project surface URLs. Namespace focus should use scopeKind/scopeRef or targetKind/targetRef." },
          production_id: { type: "string", description: "Alias for productionId." },
          scopeKind: { type: "string", description: "Optional timeline namespace scope kind to include in project surface URLs, for example episode or beat." },
          scope_kind: { type: "string", description: "Alias for scopeKind." },
          scopeRef: { type: "string", description: "Optional timeline namespace scope ref to include in project surface URLs." },
          scope_ref: { type: "string", description: "Alias for scopeRef." },
          targetKind: { type: "string", description: "Optional normalized target kind to include in project surface URLs, such as timeline_assembly." },
          target_kind: { type: "string", description: "Alias for targetKind." },
          targetRef: { type: "string", description: "Optional normalized target ref to include in project surface URLs." },
          target_ref: { type: "string", description: "Alias for targetRef." },
          timelineAssemblyRef: { type: "string", description: "Optional timeline assembly target ref to include in project surface URLs." },
          timeline_assembly_ref: { type: "string", description: "Alias for timelineAssemblyRef." },
          localBackendURL: { type: "string", description: "Optional local backend URL to probe." },
          local_backend_url: { type: "string", description: "Alias for localBackendURL." },
          timeoutMs: { type: "number", description: "Probe timeout in milliseconds. Defaults to 750." },
          timeout_ms: { type: "number", description: "Alias for timeoutMs." }
        })
      },
      {
        name: "movscript_runtime_configure",
        description: "Configure the MovScript MCP host backend mode, backend URL, auth token, or project directory. This does not create or initialize a business project.",
        inputSchema: objectSchema42({
          backendMode: { type: "string", enum: ["local", "cloud"], description: "Preferred backend mode." },
          backend_mode: { type: "string", enum: ["local", "cloud"], description: "Alias for backendMode." },
          backendBaseURL: { type: "string", description: "Backend base URL such as http://localhost:8766 or https://api.example." },
          backend_base_url: { type: "string", description: "Alias for backendBaseURL." },
          token: { type: "string", description: "Bearer token for the selected backend. Prefer environment variables or movcli auth for persistent secrets." },
          projectDir: { type: "string", description: "Project source directory to use as default workspace/project context." },
          project_dir: { type: "string", description: "Alias for projectDir." },
          workspaceDir: { type: "string", description: "Workspace directory to persist backend config under." },
          workspace_dir: { type: "string", description: "Alias for workspaceDir." },
          remember: { type: "boolean", description: "When true, persist backendBaseURL to .movscript/backend/config.json." },
          clearToken: { type: "boolean", description: "When true, clear persisted workspace auth for the selected workspace." },
          clear_token: { type: "boolean", description: "Alias for clearToken." }
        })
      },
      {
        name: "runtime_daemon_configure",
        description: "Canonical runtime tool for configuring the MovScript MCP host backend mode, backend URL, auth token, or project directory.",
        inputSchema: objectSchema42({
          backendMode: { type: "string", enum: ["local", "cloud"], description: "Preferred backend mode." },
          backend_mode: { type: "string", enum: ["local", "cloud"], description: "Alias for backendMode." },
          backendBaseURL: { type: "string", description: "Backend base URL such as http://localhost:8766 or https://api.example." },
          backend_base_url: { type: "string", description: "Alias for backendBaseURL." },
          token: { type: "string", description: "Bearer token for the selected backend. Prefer environment variables or movcli auth for persistent secrets." },
          projectDir: { type: "string", description: "Project source directory to use as default workspace/project context." },
          project_dir: { type: "string", description: "Alias for projectDir." },
          workspaceDir: { type: "string", description: "Workspace directory to persist backend config under." },
          workspace_dir: { type: "string", description: "Alias for workspaceDir." },
          remember: { type: "boolean", description: "When true, persist backendBaseURL to .movscript/backend/config.json." },
          clearToken: { type: "boolean", description: "When true, clear persisted workspace auth for the selected workspace." },
          clear_token: { type: "boolean", description: "Alias for clearToken." }
        })
      },
      {
        name: "runtime_daemon_discover",
        description: "Read MovScript Home runtime records and discover daemon, gateway, service, and surface endpoints without starting or probing services.",
        inputSchema: runtimeReadinessSchema2()
      },
      {
        name: "runtime_local_daemon_status",
        description: "Return the persistent MovScript local runtime daemon status. The daemon is the per-user owner for local Data/Project/Editing/Canvas/Surface/Media services and is independent from MCP sessions.",
        inputSchema: objectSchema42({
          homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
          home_dir: { type: "string", description: "Alias for homeDir." },
          movscriptHome: { type: "string", description: "Alias for homeDir." },
          movscript_home: { type: "string", description: "Alias for homeDir." }
        })
      },
      {
        name: "runtime_local_daemon_ensure",
        description: "Ensure the persistent MovScript local runtime daemon is running. This is a bootstrap tool: it starts the daemon when missing and reuses it when already ready.",
        inputSchema: localDaemonBootstrapSchema2()
      },
      {
        name: "runtime_local_daemon_start",
        description: "Start the persistent MovScript local runtime daemon, reusing an already ready daemon unless forceRestart is set.",
        inputSchema: localDaemonBootstrapSchema2()
      },
      {
        name: "runtime_daemon_ensure",
        description: "Canonical alias for runtime_local_daemon_ensure.",
        inputSchema: localDaemonBootstrapSchema2()
      },
      {
        name: "runtime_daemon_start",
        description: "Canonical alias for runtime_local_daemon_start.",
        inputSchema: localDaemonBootstrapSchema2()
      },
      {
        name: "runtime_daemon_status",
        description: "Canonical alias for runtime_local_daemon_status.",
        inputSchema: localDaemonControlSchema2()
      },
      {
        name: "runtime_daemon_stop",
        description: "Canonical alias for runtime_local_daemon_stop.",
        inputSchema: localDaemonControlSchema2()
      },
      {
        name: "runtime_daemon_restart",
        description: "Canonical alias for runtime_local_daemon_restart.",
        inputSchema: localDaemonControlSchema2()
      },
      {
        name: "runtime_descriptor_get",
        description: "Return the canonical runtime descriptor for agents: selected data plane, MCP endpoint, service readiness, surfaces, and missing capabilities.",
        inputSchema: runtimeReadinessSchema2()
      },
      {
        name: "runtime_preflight_check",
        description: "Run read-only runtime preflight checks and return blockers/warnings before project, generation, admin, or editing work.",
        inputSchema: runtimeReadinessSchema2({
          requireProject: { type: "boolean", description: "When false, do not treat a missing project source as a blocker." },
          require_project: { type: "boolean", description: "Alias for requireProject." }
        })
      },
      {
        name: "runtime_local_daemon_stop",
        description: "Gracefully stop the persistent MovScript local runtime daemon so services can be upgraded, ports can be released, or local resources can be freed.",
        inputSchema: localDaemonControlSchema2()
      },
      {
        name: "runtime_local_daemon_restart",
        description: "Gracefully restart the persistent MovScript local runtime daemon and its local services.",
        inputSchema: localDaemonControlSchema2()
      },
      {
        name: "runtime_local_node_status",
        description: "Compatibility alias for runtime_local_daemon_status.",
        inputSchema: objectSchema42({
          homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
          home_dir: { type: "string", description: "Alias for homeDir." },
          movscriptHome: { type: "string", description: "Alias for homeDir." },
          movscript_home: { type: "string", description: "Alias for homeDir." }
        })
      },
      {
        name: "runtime_local_node_stop",
        description: "Compatibility alias for runtime_local_daemon_stop.",
        inputSchema: objectSchema42({
          homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
          home_dir: { type: "string", description: "Alias for homeDir." },
          movscriptHome: { type: "string", description: "Alias for homeDir." },
          movscript_home: { type: "string", description: "Alias for homeDir." }
        })
      },
      {
        name: "runtime_local_node_restart",
        description: "Compatibility alias for runtime_local_daemon_restart.",
        inputSchema: objectSchema42({
          homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
          home_dir: { type: "string", description: "Alias for homeDir." },
          movscriptHome: { type: "string", description: "Alias for homeDir." },
          movscript_home: { type: "string", description: "Alias for homeDir." }
        })
      }
    ];
    async function startMCPStdioHost2() {
      const rl = import_node_readline.default.createInterface({
        input: process.stdin,
        crlfDelay: Infinity
      });
      const pending = /* @__PURE__ */ new Set();
      rl.on("line", (line) => {
        const trimmed = line.trim();
        if (!trimmed) return;
        const task = (async () => {
          try {
            const payload = JSON.parse(trimmed);
            const response = Array.isArray(payload) ? (await Promise.all(payload.map((item) => handleMCPHostJSONRPC2(item)))).filter((item) => item !== void 0) : await handleMCPHostJSONRPC2(payload);
            if (Array.isArray(response)) {
              if (response.length > 0) writeMessage2(response);
            } else if (response !== void 0) {
              writeMessage2(response);
            }
          } catch (error) {
            writeMessage2(makeError2(null, -32700, "Parse error", errorMessage62(error)));
          }
        })();
        pending.add(task);
        task.finally(() => pending.delete(task));
      });
      await new Promise((resolve12) => {
        rl.on("close", async () => {
          await Promise.allSettled([...pending]);
          resolve12();
        });
      });
    }
    async function handleMCPHostJSONRPC2(req) {
      const isNotification = !Object.prototype.hasOwnProperty.call(req, "id");
      const id = isNotification ? null : req.id ?? null;
      if (MCP_HOST_DEBUG2) {
        process.stderr.write(`[movscript-mcp-host] method=${req?.method ?? ""} id=${String(id)}
`);
      }
      if (req.jsonrpc !== "2.0" || !req.method) {
        if (isNotification) return void 0;
        return makeError2(id, -32600, "Invalid Request");
      }
      try {
        switch (req.method) {
          case "initialize":
            return makeResult2(id, {
              protocolVersion: "2025-06-18",
              serverInfo: { name: "movscript-mcp-host", version: "0.1.28" },
              capabilities: { resources: {}, tools: {} }
            });
          case "initialized":
          case "notifications/initialized":
          case "notifications/cancelled":
          case "notifications/progress":
            return void 0;
          case "ping":
            return makeResult2(id, {});
          case "tools/list":
            return makeResult2(id, { tools: mergeTools2(hostTools2, listTools2()) });
          case "tools/call":
            await touchLocalNode2().catch(() => void 0);
            return makeResult2(id, await callMCPHostTool2(req.params));
          case "resources/list":
            return makeResult2(id, { resources: listResources2() });
          case "resources/read":
            return makeResult2(id, await readResource2(stringParam22(req.params, "uri") ?? ""));
          default:
            return handleJSONRPC2(req);
        }
      } catch (error) {
        if (isNotification) return void 0;
        return makeError2(id, -32e3, errorMessage62(error));
      }
    }
    async function callMCPHostTool2(params) {
      const name = stringParam22(params, "name");
      const args = objectParam2(params, "arguments");
      if (name === "movscript_runtime_status") return await runtimeStatus2(args);
      if (name === "movscript_runtime_configure") return runtimeConfigure2(args);
      if (name === "runtime_daemon_configure") return runtimeConfigure2(args);
      if (name === "runtime_daemon_discover") return runtimeDaemonDiscover2(args);
      if (name === "runtime_daemon_ensure") return await localDaemonEnsure2(args);
      if (name === "runtime_daemon_start") return await localDaemonEnsure2(args);
      if (name === "runtime_daemon_status") return await localNodeControl2(args, "GET", "/status");
      if (name === "runtime_daemon_stop") return await localNodeControl2(args, "POST", "/shutdown");
      if (name === "runtime_daemon_restart") return await localNodeControl2(args, "POST", "/restart");
      if (name === "runtime_descriptor_get") return await runtimeDescriptorGet2(args);
      if (name === "runtime_preflight_check") return await runtimePreflightCheck2(args);
      if (name === "runtime_local_daemon_ensure") return await localDaemonEnsure2(args);
      if (name === "runtime_local_daemon_start") return await localDaemonEnsure2(args);
      if (name === "runtime_local_daemon_status") return await localNodeControl2(args, "GET", "/status");
      if (name === "runtime_local_daemon_stop") return await localNodeControl2(args, "POST", "/shutdown");
      if (name === "runtime_local_daemon_restart") return await localNodeControl2(args, "POST", "/restart");
      if (name === "runtime_local_node_status") return await localNodeControl2(args, "GET", "/status");
      if (name === "runtime_local_node_stop") return await localNodeControl2(args, "POST", "/shutdown");
      if (name === "runtime_local_node_restart") return await localNodeControl2(args, "POST", "/restart");
      if (isAdminMCPToolName2(name)) {
        return unwrapCommandDataWithDebug2(await runMovScriptAdminCommand2(name, args));
      }
      bindBackendRuntimeForCoreTools2(args);
      return callTool2(params);
    }
    async function localDaemonEnsure2(args) {
      const homeDir = resolveRuntimeHomeArg22(args);
      const entrypoint = resolveLocalDaemonEntrypoint2(args);
      const cwd = (0, import_node_path10.resolve)(stringValue15(args.cwd) || process.cwd());
      const startupTimeoutMs = numberValue52(args.startupTimeoutMs ?? args.startup_timeout_ms);
      const stopTimeoutMs = numberValue52(args.stopTimeoutMs ?? args.stop_timeout_ms);
      const result = await ensureLocalRuntimeDaemon2({
        homeDir,
        entrypoint,
        runArgs: ["daemon", "run"],
        cwd,
        env: localDaemonBootstrapEnv2(args),
        forceRestart: args.forceRestart === true || args.force_restart === true,
        ...startupTimeoutMs ? { startupTimeoutMs } : {},
        ...stopTimeoutMs ? { stopTimeoutMs } : {}
      });
      return {
        bootstrap: "local_daemon",
        homeDir,
        entrypoint,
        cwd,
        ...result
      };
    }
    function resolveLocalDaemonEntrypoint2(args) {
      const explicit = stringValue15(args.entrypoint ?? args.entry_point) ?? process.env.MOVSCRIPT_DAEMON_ENTRYPOINT ?? process.env.MOVSCRIPT_AGENT_MCP_ENTRYPOINT ?? process.argv[1];
      if (!explicit) {
        throw new Error("runtime daemon startup requires an entrypoint; pass entrypoint or run through the MovScript Agent Plugin MCP entrypoint");
      }
      return (0, import_node_path10.resolve)(explicit);
    }
    function localDaemonBootstrapEnv2(args) {
      const env = {};
      const dataPlane = stringValue15(args.dataPlane ?? args.data_plane);
      if (dataPlane) {
        if (!["local", "cloud", "external"].includes(dataPlane)) {
          throw new Error(`invalid runtime daemon dataPlane: ${dataPlane}`);
        }
        env.MOVSCRIPT_LOCAL_DAEMON_DATA_PLANE = dataPlane;
      }
      const dataServiceURL = stringValue15(args.dataServiceURL ?? args.data_service_url);
      if (dataServiceURL) env.MOVSCRIPT_DATA_SERVICE_URL = dataServiceURL;
      const idleTimeout = stringValue15(args.idleTimeout ?? args.idle_timeout);
      if (idleTimeout) {
        env.MOVSCRIPT_LOCAL_DAEMON_IDLE_TIMEOUT = idleTimeout;
        env.MOVSCRIPT_LOCAL_NODE_IDLE_TIMEOUT = idleTimeout;
      }
      return Object.keys(env).length > 0 ? env : void 0;
    }
    async function runtimeStatus2(args = {}) {
      const workspaceDir2 = (0, import_node_path10.resolve)(stringValue15(args.workspaceDir ?? args.workspace_dir) || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd());
      const projectDir = (0, import_node_path10.resolve)(stringValue15(args.projectDir ?? args.project_dir) || workspaceDir2);
      const homeDir = resolveRuntimeHomeArg22(args);
      const runtimeHome = readRuntimeHomeSnapshot2(homeDir);
      const timeoutMs = numberValue52(args.timeoutMs ?? args.timeout_ms) ?? 750;
      const homeGatewayEndpoint = endpointURL52(
        findRuntimeEndpoint2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22) ?? findRuntimeService2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22)?.endpoint
      );
      const homeDataEndpoint = endpointURL52(
        findRuntimeEndpoint2(runtimeHome, "movscript.data.service") ?? findRuntimeService2(runtimeHome, "movscript.data.service")?.endpoint
      );
      const localBackendURL = normalizeBaseURL3(stringValue15(args.localBackendURL ?? args.local_backend_url) || homeGatewayEndpoint || homeDataEndpoint || DEFAULT_LOCAL_BACKEND2);
      const configuredSession = resolveMovScriptBackendSession2({
        workspaceDir: workspaceDir2,
        server: process.env.MOVSCRIPT_DATA_SERVICE_URL,
        token: process.env.MOVSCRIPT_API_TOKEN ?? process.env.MOVSCRIPT_DATA_SERVICE_TOKEN
      });
      const configuredBaseURL = normalizeBaseURL3(configuredSession.baseURL);
      const configuredIsLocal = isLocalBackendURL2(configuredBaseURL);
      const localProbe = await probeBackend2(localBackendURL, timeoutMs);
      const configuredProbe = configuredBaseURL === localBackendURL ? localProbe : await probeBackend2(configuredBaseURL, timeoutMs);
      const cloudAuth = findCloudAuth2(workspaceDir2);
      const cloudBaseURL = configuredIsLocal ? cloudAuth.baseURL : configuredBaseURL;
      const cloudProbe = cloudBaseURL && cloudBaseURL !== localBackendURL && cloudBaseURL !== configuredBaseURL ? await probeBackend2(cloudBaseURL, timeoutMs) : configuredIsLocal ? { available: false } : configuredProbe;
      const project = inspectProjectSource2(projectDir);
      const mediaPipeline = mediaPipelineRuntimeStatus2(runtimeHome);
      const localNode = localNodeRuntimeStatus2(runtimeHome);
      const surfaceHost = surfaceHostRuntimeStatus2(runtimeHome);
      const desktop = await probeDesktop2(timeoutMs, runtimeHome);
      const localAvailable = localProbe.available;
      const cloudConfigured = Boolean(cloudBaseURL || cloudAuth.authenticated || !configuredIsLocal && configuredSession.token);
      const cloudAvailable = Boolean(cloudBaseURL && isRecord38(cloudProbe) && cloudProbe.available === true);
      const selected = selectedBackendMode2({
        configuredIsLocal,
        localAvailable,
        cloudAvailable,
        projectAvailable: project.isMovScriptProject
      });
      const requiresUserChoice = shouldRequireUserChoice2({
        localAvailable,
        cloudAvailable,
        projectAvailable: project.isMovScriptProject
      });
      const missing = missingItems2({
        localAvailable,
        cloudAvailable,
        projectAvailable: project.isMovScriptProject
      });
      const runtimeOwner = runtimeOwnerStatus2({
        desktopAvailable: desktop.available === true,
        localDaemonAvailable: localNode.available === true,
        localAvailable,
        cloudAvailable,
        selected
      });
      const surfaces = localSurfaceURLs2({
        surfaceHost,
        project,
        projectDir,
        projectId: stringValue15(args.projectId ?? args.project_id),
        productionId: stringValue15(args.productionId ?? args.production_id),
        focusQuery: runtimeFocusQuery2(args),
        runtimeOwner
      });
      return {
        status: "ok",
        home: runtimeHomeSummary2(runtimeHome),
        backend: {
          local: {
            available: localAvailable,
            baseURL: localBackendURL,
            discoveredFromHome: Boolean(homeGatewayEndpoint || homeDataEndpoint),
            ...homeGatewayEndpoint ? { gatewayBaseURL: homeGatewayEndpoint } : {},
            ...homeDataEndpoint ? { dataServiceBaseURL: homeDataEndpoint } : {},
            authenticated: configuredIsLocal && Boolean(configuredSession.token),
            ...localProbe.error ? { error: localProbe.error } : {}
          },
          cloud: {
            available: cloudAvailable,
            configured: cloudConfigured,
            ...cloudBaseURL ? { baseURL: cloudBaseURL } : {},
            authenticated: Boolean(cloudAuth.authenticated || !configuredIsLocal && configuredSession.token),
            ...isRecord38(cloudProbe) && typeof cloudProbe.error === "string" ? { error: cloudProbe.error } : {}
          },
          selected
        },
        workspace: {
          cwd: process.cwd(),
          workspaceDir: workspaceDir2,
          projectDir,
          ...project
        },
        desktop,
        localDaemon: localNode,
        localNode,
        surfaceHost,
        surfaces,
        ...surfaces.primary ? { surface: surfaces.primary } : {},
        ...surfaces.secondary.length > 0 ? { secondary_surfaces: surfaces.secondary } : {},
        mediaPipeline,
        runtimeOwner,
        recommendedMode: recommendedMode2(selected, project.isMovScriptProject),
        requiresUserChoice,
        missing
      };
    }
    function runtimeDaemonDiscover2(args = {}) {
      const homeDir = resolveRuntimeHomeArg22(args);
      const runtimeHome = readRuntimeHomeSnapshot2(homeDir);
      const endpoints = runtimeDiscoveredEndpoints2(runtimeHome);
      const daemonAvailable = Boolean(endpoints.control || endpoints.gateway);
      return {
        schema: "movscript.runtime_daemon_discovery.v1",
        status: daemonAvailable ? "ready" : "not_running",
        homeDir,
        daemon: {
          available: daemonAvailable,
          applicationId: LOCAL_RUNTIME_DAEMON_APP_ID2,
          ...endpoints.control ? { controlEndpoint: endpoints.control } : {},
          ...endpoints.gateway ? { gatewayEndpoint: endpoints.gateway } : {},
          ...endpoints.gateway ? { mcpEndpoint: runtimeMcpEndpoint2(endpoints.gateway) } : {}
        },
        endpoints,
        home: runtimeHomeSummary2(runtimeHome),
        recommendedNextTool: daemonAvailable ? "runtime_descriptor_get" : "runtime_daemon_ensure"
      };
    }
    async function runtimeDescriptorGet2(args = {}) {
      const status = await runtimeStatus2(args);
      return runtimeDescriptorFromStatus2(status);
    }
    async function runtimePreflightCheck2(args = {}) {
      const status = await runtimeStatus2(args);
      const descriptor = runtimeDescriptorFromStatus2(status);
      const checks = runtimePreflightChecks2(status, args);
      const blockers = checks.filter((check) => check.status === "blocked");
      const warnings = checks.filter((check) => check.status === "warning");
      return {
        schema: "movscript.runtime_preflight.v1",
        status: blockers.length > 0 ? "blocked" : warnings.length > 0 ? "degraded" : "ready",
        ready: blockers.length === 0,
        blockers,
        warnings,
        checks,
        descriptor
      };
    }
    function runtimeDescriptorFromStatus2(status) {
      const backend = recordValue42(status.backend);
      const localBackend = recordValue42(backend.local);
      const cloudBackend = recordValue42(backend.cloud);
      const workspace = recordValue42(status.workspace);
      const runtimeOwner = recordValue42(status.runtimeOwner);
      const localDaemon = recordValue42(status.localDaemon ?? status.localNode);
      const mediaPipeline = recordValue42(status.mediaPipeline);
      const surfaceHost = recordValue42(status.surfaceHost);
      const surfaces = recordValue42(status.surfaces);
      const gatewayEndpoint = stringValue15(localBackend.gatewayBaseURL);
      const dataServiceEndpoint = stringValue15(localBackend.dataServiceBaseURL) ?? (localBackend.discoveredFromHome === true ? stringValue15(localBackend.baseURL) : void 0);
      const selected = stringValue15(backend.selected);
      const endpointMap = {};
      if (gatewayEndpoint) {
        endpointMap.gateway = gatewayEndpoint;
        endpointMap.mcp = runtimeMcpEndpoint2(gatewayEndpoint);
        endpointMap.mcp_http = runtimeMcpEndpoint2(gatewayEndpoint);
      }
      if (dataServiceEndpoint) endpointMap.dataService = dataServiceEndpoint;
      if (stringValue15(localDaemon.endpoint)) endpointMap.daemonControl = stringValue15(localDaemon.endpoint);
      if (stringValue15(mediaPipeline.endpoint)) endpointMap.mediaPipeline = stringValue15(mediaPipeline.endpoint);
      if (stringValue15(surfaceHost.endpoint)) endpointMap.surfaceHost = stringValue15(surfaceHost.endpoint);
      return {
        schema: "movscript.runtime_descriptor.v1",
        status: runtimeDescriptorStatus2(status),
        owner: runtimeOwner,
        dataPlane: selected ?? (cloudBackend.available === true ? "cloud" : localBackend.available === true ? "local" : "unknown"),
        endpoints: endpointMap,
        services: {
          backend: runtimeServiceDescriptor2("backend", localBackend.available === true || cloudBackend.available === true, stringValue15(localBackend.baseURL ?? cloudBackend.baseURL), selected),
          daemon: runtimeServiceDescriptor2("daemon", localDaemon.available === true, stringValue15(localDaemon.endpoint), stringValue15(runtimeOwner.kind)),
          dataService: runtimeServiceDescriptor2(DATA_SERVICE22, localBackend.available === true, dataServiceEndpoint, selected),
          projectService: runtimeServiceDescriptor2(PROJECT_SERVICE2, Boolean(workspace.isMovScriptProject), stringValue15(workspace.projectDir), "project_source"),
          editingService: runtimeServiceDescriptor2(EDITING_SERVICE2, true, void 0, "not_probed"),
          surfaceHost: runtimeServiceDescriptor2(LOCAL_SURFACE_HOST_SERVICE2, surfaceHost.available === true, stringValue15(surfaceHost.endpoint), stringValue15(surfaceHost.mode)),
          mediaPipeline: runtimeServiceDescriptor2(MEDIA_PIPELINE_SERVICE2, mediaPipeline.available === true, stringValue15(mediaPipeline.endpoint), "render")
        },
        workspace,
        surfaces,
        recommendedMode: status.recommendedMode,
        requiresUserChoice: status.requiresUserChoice,
        missing: Array.isArray(status.missing) ? status.missing : []
      };
    }
    function runtimeDescriptorStatus2(status) {
      const missing = Array.isArray(status.missing) ? status.missing : [];
      if (missing.includes("backend")) return "missing_backend";
      if (missing.length > 0) return "degraded";
      return "ready";
    }
    function runtimeServiceDescriptor2(name, ready, endpoint, mode) {
      return {
        name,
        ready,
        status: ready ? "ready" : "missing",
        ...endpoint ? { endpoint } : {},
        ...mode ? { mode } : {}
      };
    }
    function runtimePreflightChecks2(status, args) {
      const backend = recordValue42(status.backend);
      const localBackend = recordValue42(backend.local);
      const cloudBackend = recordValue42(backend.cloud);
      const workspace = recordValue42(status.workspace);
      const localDaemon = recordValue42(status.localDaemon ?? status.localNode);
      const mediaPipeline = recordValue42(status.mediaPipeline);
      const surfaceHost = recordValue42(status.surfaceHost);
      const requireProject = args.requireProject === false || args.require_project === false ? false : true;
      const checks = [];
      checks.push(runtimeCheck2({
        id: "backend",
        ok: localBackend.available === true || cloudBackend.available === true,
        blocker: true,
        message: "Data backend or daemon gateway is reachable.",
        missingMessage: "No reachable Data Service, daemon gateway, or cloud backend was found.",
        owner: "runtime"
      }));
      checks.push(runtimeCheck2({
        id: "daemon",
        ok: localDaemon.available === true,
        blocker: false,
        message: "Local daemon control endpoint is registered.",
        missingMessage: "Local daemon control endpoint is not registered; external/cloud runtimes may still work.",
        owner: "runtime"
      }));
      checks.push(runtimeCheck2({
        id: "project_source",
        ok: workspace.isMovScriptProject === true,
        blocker: requireProject,
        message: "MovScript project source is available.",
        missingMessage: "MovScript project source was not found at the selected projectDir/workspaceDir.",
        owner: "project"
      }));
      checks.push(runtimeCheck2({
        id: "surface_host",
        ok: surfaceHost.available === true,
        blocker: false,
        message: "Local surface host is available for human review.",
        missingMessage: "Local surface host is not available; browser review URLs may be unavailable.",
        owner: "surface"
      }));
      checks.push(runtimeCheck2({
        id: "media_pipeline",
        ok: mediaPipeline.available === true,
        blocker: false,
        message: "Media Pipeline endpoint is registered.",
        missingMessage: "Media Pipeline endpoint is not registered; render/transcode/HLS may be unavailable.",
        owner: "media_pipeline"
      }));
      return checks;
    }
    function runtimeCheck2(input3) {
      if (input3.ok) {
        return {
          id: input3.id,
          status: "ready",
          owner: input3.owner,
          message: input3.message
        };
      }
      return {
        id: input3.id,
        status: input3.blocker ? "blocked" : "warning",
        owner: input3.owner,
        message: input3.missingMessage
      };
    }
    function runtimeDiscoveredEndpoints2(runtimeHome) {
      const endpoints = {};
      setEndpoint2(endpoints, "control", runtimeHome, LOCAL_NODE_CONTROL_SERVICE3);
      setEndpoint2(endpoints, "gateway", runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22);
      setEndpoint2(endpoints, "dataService", runtimeHome, DATA_SERVICE22);
      setEndpoint2(endpoints, "projectService", runtimeHome, PROJECT_SERVICE2);
      setEndpoint2(endpoints, "editingService", runtimeHome, EDITING_SERVICE2);
      setEndpoint2(endpoints, "surfaceHost", runtimeHome, LOCAL_SURFACE_HOST_SERVICE2);
      setEndpoint2(endpoints, "mediaPipeline", runtimeHome, MEDIA_PIPELINE_SERVICE2);
      if (endpoints.gateway) endpoints.mcp = runtimeMcpEndpoint2(endpoints.gateway);
      return endpoints;
    }
    function setEndpoint2(output3, key, runtimeHome, serviceName) {
      const endpoint = endpointURL52(
        findRuntimeEndpoint2(runtimeHome, serviceName) ?? findRuntimeService2(runtimeHome, serviceName)?.endpoint
      );
      if (endpoint) output3[key] = endpoint;
    }
    function runtimeMcpEndpoint2(baseURL) {
      const normalized = normalizeBaseURL3(baseURL);
      return `${normalized}/v1/mcp`;
    }
    async function localNodeControl2(args, method, path) {
      const homeDir = resolveRuntimeHomeArg22(args);
      const endpoint = endpointURL52(findRuntimeEndpoint2(readRuntimeHomeSnapshot2(homeDir), LOCAL_NODE_CONTROL_SERVICE3));
      if (!endpoint) return { status: "not_running", homeDir };
      try {
        const response = await fetch(`${endpoint}${path}`, { method, signal: AbortSignal.timeout(3e3) });
        const payload = await response.json().catch(() => ({}));
        return {
          status: response.ok ? payload.status ?? "ok" : "error",
          homeDir,
          endpoint,
          ...payload,
          ...response.ok ? {} : { httpStatus: response.status }
        };
      } catch (error) {
        return { status: "error", homeDir, endpoint, error: errorMessage62(error) };
      }
    }
    async function touchLocalNode2() {
      const homeDir = resolveMovScriptHomeDir22();
      const endpoint = endpointURL52(findRuntimeEndpoint2(readRuntimeHomeSnapshot2(homeDir), LOCAL_NODE_CONTROL_SERVICE3));
      if (!endpoint) return;
      await fetch(`${endpoint}/touch`, { method: "POST", signal: AbortSignal.timeout(750) });
    }
    function runtimeConfigure2(args = {}) {
      const backendBaseURL = stringValue15(args.backendBaseURL ?? args.backend_base_url);
      const backendMode = stringValue15(args.backendMode ?? args.backend_mode);
      const workspaceDir2 = (0, import_node_path10.resolve)(stringValue15(args.workspaceDir ?? args.workspace_dir) || stringValue15(args.projectDir ?? args.project_dir) || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd());
      const projectDir = stringValue15(args.projectDir ?? args.project_dir);
      const token = stringValue15(args.token);
      const remember = args.remember === true;
      const clearToken = args.clearToken === true || args.clear_token === true;
      if (projectDir) setMCPDefaultWorkspaceDir2((0, import_node_path10.resolve)(projectDir));
      else setMCPDefaultWorkspaceDir2(workspaceDir2);
      if (backendBaseURL) setMovScriptBackendAPIBaseURL2(backendBaseURL);
      if (token) setMovScriptBackendRuntimeAuthToken2(token);
      if (clearToken) {
        clearMovScriptBackendAuth2(workspaceDir2);
        setMovScriptBackendRuntimeAuthToken2(void 0);
      }
      const persisted = backendBaseURL && remember ? writeMovScriptBackendConfig2(workspaceDir2, {
        baseURL: backendBaseURL,
        ...backendMode === "local" || backendMode === "cloud" ? { realm: backendMode === "local" ? { kind: "local", id: "local" } : { kind: "cloud", id: "default" } } : {}
      }) : void 0;
      return {
        status: "configured",
        workspaceDir: workspaceDir2,
        ...projectDir ? { projectDir: (0, import_node_path10.resolve)(projectDir) } : {},
        ...backendMode ? { backendMode } : {},
        ...backendBaseURL ? { backendBaseURL: normalizeBaseURL3(backendBaseURL) } : {},
        remembered: Boolean(persisted),
        tokenConfigured: Boolean(token),
        tokenCleared: clearToken
      };
    }
    function inspectProjectSource2(projectDir) {
      const workspacePath52 = (0, import_node_path10.resolve)(projectDir, "workspace.json");
      const projectPath = (0, import_node_path10.resolve)(projectDir, "project.json");
      const metadataPath = (0, import_node_fs7.existsSync)(workspacePath52) ? workspacePath52 : (0, import_node_fs7.existsSync)(projectPath) ? projectPath : void 0;
      const metadata = metadataPath ? readJSON6(metadataPath) : void 0;
      const sourceCollections = Array.from(MOVSCRIPT_SOURCE_COLLECTION_DIRS2).filter((name) => (0, import_node_fs7.existsSync)((0, import_node_path10.resolve)(projectDir, name))).sort();
      const sourceRootFiles = Array.from(MOVSCRIPT_SOURCE_ROOT_FILES2).filter((name) => (0, import_node_fs7.existsSync)((0, import_node_path10.resolve)(projectDir, name))).sort();
      const hasSourceDirs = sourceCollections.length > 0;
      const hasSourceRootFiles = sourceRootFiles.length > 0;
      const projectUid = isRecord38(metadata) ? stringValue15(metadata.project_uid ?? metadata.projectUid) : void 0;
      const projectTitle = isRecord38(metadata) ? stringValue15(metadata.title ?? metadata.name) : void 0;
      return {
        isMovScriptProject: Boolean(metadataPath || hasSourceDirs || hasSourceRootFiles),
        hasMetadata: Boolean(metadataPath),
        hasSourceDirs,
        hasSourceRootFiles,
        sourceCollections,
        sourceRootFiles,
        ...metadataPath ? { metadataPath } : {},
        ...projectUid ? { projectUid } : {},
        ...projectTitle ? { projectTitle } : {}
      };
    }
    async function probeBackend2(baseURL, timeoutMs) {
      const controller = new AbortController();
      const timeout = setTimeout(() => controller.abort(), Math.max(250, Math.min(timeoutMs, 5e3)));
      try {
        const response = await fetch(`${baseURL.replace(/\/+$/, "")}/health`, { signal: controller.signal });
        return { available: response.ok, status: response.status, ...response.ok ? {} : { error: `HTTP ${response.status}` } };
      } catch (error) {
        return { available: false, error: errorMessage62(error) };
      } finally {
        clearTimeout(timeout);
      }
    }
    async function probeDesktop2(timeoutMs, runtimeHome) {
      const desktopApp = findRuntimeApp2(runtimeHome, "movscript.desktop") ?? findRuntimeApp2(runtimeHome, "movscript.desktop.app");
      const mediaPipeline = mediaPipelineRuntimeStatus2(runtimeHome);
      const homeEndpoint = endpointURL52(desktopApp?.endpoint) ?? endpointURL52(findRuntimeEndpoint2(runtimeHome, "movscript.mcp.host"));
      const endpoint = process.env.MOVSCRIPT_MCP_ENDPOINT || homeEndpoint || "http://127.0.0.1:18765/mcp";
      const controller = new AbortController();
      const timeout = setTimeout(() => controller.abort(), Math.max(250, Math.min(timeoutMs, 3e3)));
      try {
        const response = await fetch(endpoint, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ jsonrpc: "2.0", id: "runtime-status-desktop-probe", method: "initialize" }),
          signal: controller.signal
        });
        return {
          available: response.ok,
          endpoint,
          discoveredFromHome: Boolean(homeEndpoint),
          applicationId: desktopApp?.applicationId,
          mediaPipeline: mediaPipeline.available,
          ...mediaPipeline.endpoint ? { mediaPipelineEndpoint: mediaPipeline.endpoint } : {},
          ...response.ok ? {} : { error: `HTTP ${response.status}` }
        };
      } catch (error) {
        return {
          available: false,
          endpoint,
          discoveredFromHome: Boolean(homeEndpoint),
          applicationId: desktopApp?.applicationId,
          mediaPipeline: mediaPipeline.available,
          ...mediaPipeline.endpoint ? { mediaPipelineEndpoint: mediaPipeline.endpoint } : {},
          error: errorMessage62(error)
        };
      } finally {
        clearTimeout(timeout);
      }
    }
    function mediaPipelineRuntimeStatus2(runtimeHome) {
      const endpoint = endpointURL52(
        findRuntimeEndpoint2(runtimeHome, "movscript.media.pipeline") ?? findRuntimeService2(runtimeHome, "movscript.media.pipeline")?.endpoint
      );
      return {
        available: Boolean(endpoint),
        ...endpoint ? { endpoint } : {}
      };
    }
    function localNodeRuntimeStatus2(runtimeHome) {
      const endpoint = endpointURL52(findRuntimeEndpoint2(runtimeHome, LOCAL_NODE_CONTROL_SERVICE3));
      return {
        available: Boolean(endpoint),
        ...endpoint ? { endpoint } : {}
      };
    }
    function surfaceHostRuntimeStatus2(runtimeHome) {
      const gatewayEndpointRecord = findRuntimeEndpoint2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22);
      const gatewayServiceRecord = findRuntimeService2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22);
      const surfaceEndpointRecord = findRuntimeEndpoint2(runtimeHome, LOCAL_SURFACE_HOST_SERVICE2);
      const surfaceServiceRecord = findRuntimeService2(runtimeHome, LOCAL_SURFACE_HOST_SERVICE2);
      const endpointRecord = gatewayEndpointRecord ?? surfaceEndpointRecord;
      const serviceRecord = gatewayServiceRecord ?? surfaceServiceRecord;
      const endpoint = endpointURL52(endpointRecord ?? serviceRecord?.endpoint);
      const ownerApplicationId = endpointRecord?.applicationId ?? serviceRecord?.ownerApplicationId;
      const serviceName = endpointRecord?.serviceName ?? serviceRecord?.serviceName ?? LOCAL_SURFACE_HOST_SERVICE2;
      return {
        available: Boolean(endpoint),
        serviceName,
        surfaceHostServiceName: LOCAL_SURFACE_HOST_SERVICE2,
        ...endpoint ? { endpoint } : {},
        ...ownerApplicationId ? { ownerApplicationId } : {},
        ...ownerApplicationId ? { mode: surfaceHostMode2(ownerApplicationId) } : {}
      };
    }
    function surfaceHostMode2(ownerApplicationId) {
      if (ownerApplicationId === "movscript.agent-plugin") return "agent-plugin-session";
      if (ownerApplicationId === LOCAL_RUNTIME_DAEMON_APP_ID2) return "local-daemon";
      if (ownerApplicationId === "movscript.desktop") return "desktop-owned";
      return "external";
    }
    function localSurfaceURLs2(input3) {
      const startupAllowed = input3.runtimeOwner.surfaceHostStartupAllowed === true;
      if (!input3.surfaceHost.endpoint) {
        return {
          available: false,
          openable: false,
          reason: startupAllowed ? "local_surface_host_not_ready_startup_allowed" : "local_surface_host_not_ready",
          secondary: [],
          urls: {},
          startupAllowed
        };
      }
      const baseURL = normalizeBaseURL3(input3.surfaceHost.endpoint);
      const projectId = input3.projectId ?? stringValue15(input3.project.projectUid) ?? safeProjectIdFromDir2(input3.projectDir);
      const commonQuery = {
        source: "runtime-status",
        projectId,
        projectDir: input3.projectDir
      };
      if (input3.productionId) commonQuery.productionId = input3.productionId;
      for (const [key, value] of Object.entries(input3.focusQuery ?? {})) {
        commonQuery[key] = value;
      }
      const home = localSurfaceURL2(baseURL, "/", commonQuery);
      const projectOverview = localSurfaceURL2(baseURL, `/studio/${encodeURIComponent(projectId)}/overview`, commonQuery);
      const projectContent = localSurfaceURL2(baseURL, `/studio/${encodeURIComponent(projectId)}/content`, commonQuery);
      const projectTimeline = localSurfaceURL2(baseURL, `/studio/${encodeURIComponent(projectId)}/timeline`, commonQuery);
      const canvas = localSurfaceURL2(baseURL, "/canvases", { source: "runtime-status" });
      const editing = localSurfaceURL2(baseURL, "/editing", commonQuery);
      const admin = localSurfaceURL2(baseURL, "/admin/overview", { source: "runtime-status" });
      const primary = input3.project.isMovScriptProject ? runtimeSurfaceLink2({
        title: "MovScript project overview",
        surface: "project.overview",
        route: `/studio/${encodeURIComponent(projectId)}/overview`,
        url: projectOverview,
        usage: "Open this URL in the Codex/in-app browser when the user needs to inspect or operate the MovScript project UI."
      }) : runtimeSurfaceLink2({
        title: "MovScript Local Surface Host",
        surface: "local-surface-host",
        route: "/",
        url: home,
        usage: "Open this URL in the Codex/in-app browser to choose a MovScript local surface."
      });
      const secondary = [
        runtimeSurfaceLink2({
          title: "MovScript project content",
          surface: "project.content",
          route: `/studio/${encodeURIComponent(projectId)}/content`,
          url: projectContent,
          usage: "Open this URL to inspect content units and project content state."
        }),
        runtimeSurfaceLink2({
          title: "MovScript project timeline",
          surface: "project.timeline",
          route: `/studio/${encodeURIComponent(projectId)}/timeline`,
          url: projectTimeline,
          usage: "Open this URL to inspect the project timeline surface."
        }),
        runtimeSurfaceLink2({
          title: "MovScript canvases",
          surface: "canvas",
          route: "/canvases",
          url: canvas,
          usage: "Open this URL when the user needs to inspect or edit canvas surfaces."
        }),
        runtimeSurfaceLink2({
          title: "MovScript editing",
          surface: "editing",
          route: "/editing",
          url: editing,
          usage: "Open this URL when the user needs to inspect or edit media editing projects."
        }),
        runtimeSurfaceLink2({
          title: "MovScript local admin",
          surface: "admin.overview",
          route: "/admin/overview",
          url: admin,
          usage: "Open this URL when the user needs local admin/provider/job controls."
        })
      ];
      return {
        available: true,
        openable: true,
        reason: "local_surface_host_ready",
        primary,
        secondary,
        urls: {
          home,
          projectOverview,
          projectContent,
          projectTimeline,
          canvas,
          editing,
          admin
        },
        startupAllowed
      };
    }
    function safeProjectIdFromDir2(projectDir) {
      return (0, import_node_path10.basename)(projectDir) || "sample-project";
    }
    function localSurfaceURL2(baseURL, pathname, query) {
      const url = new URL(`${baseURL.replace(/\/+$/, "")}/`);
      const basePath = url.pathname.replace(/\/+$/, "");
      const routePath = pathname.startsWith("/") ? pathname : `/${pathname}`;
      url.pathname = `${basePath}${routePath}` || "/";
      for (const [key, value] of Object.entries(query)) {
        if (value) url.searchParams.set(key, value);
      }
      return url.toString();
    }
    function runtimeFocusQuery2(args) {
      const output3 = {};
      setQueryValue2(output3, "scopeKind", args.scopeKind ?? args.scope_kind);
      setQueryValue2(output3, "scopeRef", args.scopeRef ?? args.scope_ref);
      setQueryValue2(output3, "targetKind", args.targetKind ?? args.target_kind);
      setQueryValue2(output3, "targetRef", args.targetRef ?? args.target_ref);
      setQueryValue2(output3, "timeline_assembly_ref", args.timelineAssemblyRef ?? args.timeline_assembly_ref);
      return output3;
    }
    function setQueryValue2(output3, key, value) {
      const normalized = stringValue15(value);
      if (normalized) output3[key] = normalized;
    }
    function runtimeSurfaceLink2(input3) {
      return {
        kind: "browser_url",
        title: input3.title,
        surface: input3.surface,
        route: input3.route,
        url: input3.url,
        usage: input3.usage
      };
    }
    function resolveRuntimeHomeArg22(args) {
      const homeDir = stringValue15(args.homeDir ?? args.home_dir ?? args.movscriptHome ?? args.movscript_home);
      return homeDir ? (0, import_node_path10.resolve)(homeDir) : resolveMovScriptHomeDir22();
    }
    function bindBackendRuntimeForCoreTools2(args) {
      const homeDir = resolveRuntimeHomeArg22(args);
      const runtimeHome = readRuntimeHomeSnapshot2(homeDir);
      const gatewayEndpoint = endpointURL52(
        findRuntimeEndpoint2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22) ?? findRuntimeService2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22)?.endpoint
      );
      const dataEndpoint = endpointURL52(
        findRuntimeEndpoint2(runtimeHome, "movscript.data.service") ?? findRuntimeService2(runtimeHome, "movscript.data.service")?.endpoint
      );
      const backendEndpoint = gatewayEndpoint ?? dataEndpoint;
      if (backendEndpoint) setMovScriptBackendAPIBaseURL2(backendEndpoint);
      if (backendEndpoint && args.mcp_base_url === void 0 && args.mcpBaseURL === void 0) {
        args.mcp_base_url = backendEndpoint;
      }
      const agentSurfaceEndpoint = endpointURL52(
        findRuntimeEndpoint2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22) ?? findRuntimeService2(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE22)?.endpoint ?? findRuntimeEndpoint2(runtimeHome, LOCAL_SURFACE_HOST_SERVICE2) ?? findRuntimeService2(runtimeHome, LOCAL_SURFACE_HOST_SERVICE2)?.endpoint
      );
      if (agentSurfaceEndpoint && args.frontend_origin === void 0 && args.frontendOrigin === void 0) {
        args.frontend_origin = agentSurfaceEndpoint;
      }
      const workspaceDir2 = stringValue15(args.workspaceDir ?? args.workspace_dir) ?? stringValue15(args.projectDir ?? args.project_dir) ?? stringValue15(args.projectPath ?? args.project_path) ?? stringValue15(args.cwd) ?? process.env.MOVSCRIPT_WORKSPACE_DIR;
      if (workspaceDir2) setMCPDefaultWorkspaceDir2((0, import_node_path10.resolve)(workspaceDir2));
    }
    function endpointURL52(endpoint) {
      if (!endpoint) return void 0;
      if (endpoint.protocol && endpoint.protocol !== "http" && endpoint.protocol !== "https") return void 0;
      if (endpoint.url) return endpoint.url;
      if (endpoint.baseURL) return endpoint.baseURL;
      if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
      if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
      return void 0;
    }
    function runtimeHomeSummary2(snapshot) {
      const apps = activeAppRecords2(snapshot).map((record) => ({
        applicationId: record.applicationId,
        status: record.status,
        ready: record.ready,
        ...record.profile ? { profile: record.profile } : {},
        ...record.owner ? { owner: record.owner } : {},
        ...record.endpoint ? { endpoint: endpointURL52(record.endpoint) } : {}
      }));
      const services = activeServiceRecords2(snapshot).map((record) => ({
        serviceName: record.serviceName,
        instanceId: record.instanceId,
        status: record.status,
        ready: record.ready,
        ...record.profile ? { profile: record.profile } : {},
        ...record.ownerApplicationId ? { ownerApplicationId: record.ownerApplicationId } : {},
        ...record.endpoint ? { endpoint: endpointURL52(record.endpoint) } : {}
      }));
      const endpoints = activeEndpointRecords2(snapshot).map((record) => ({
        ...record.serviceName ? { serviceName: record.serviceName } : {},
        ...record.applicationId ? { applicationId: record.applicationId } : {},
        status: record.status,
        ready: record.ready,
        ...endpointURL52(record) ? { endpoint: endpointURL52(record) } : {}
      }));
      return {
        homeDir: snapshot.homeDir,
        apps,
        services,
        endpoints
      };
    }
    function runtimeOwnerStatus2(input3) {
      if (input3.localDaemonAvailable) {
        return {
          kind: "local_daemon",
          applicationId: LOCAL_RUNTIME_DAEMON_APP_ID2,
          reason: "local_runtime_daemon_ready",
          businessSidecarStartupAllowed: false,
          surfaceHostStartupAllowed: false,
          sidecarStartupAllowed: false
        };
      }
      if (input3.desktopAvailable) {
        return {
          kind: "desktop_legacy_owner",
          applicationId: "movscript.desktop",
          reason: "desktop_full_runtime_ready_without_local_daemon",
          businessSidecarStartupAllowed: false,
          surfaceHostStartupAllowed: false,
          sidecarStartupAllowed: false
        };
      }
      if (input3.selected === "cloud" && input3.cloudAvailable) {
        return {
          kind: "cloud",
          reason: "cloud_backend_ready",
          businessSidecarStartupAllowed: false,
          surfaceHostStartupAllowed: false,
          sidecarStartupAllowed: false
        };
      }
      if (input3.selected === "local" && input3.localAvailable) {
        return {
          kind: "external_local",
          reason: "local_backend_ready_without_desktop",
          businessSidecarStartupAllowed: true,
          surfaceHostStartupAllowed: true,
          sidecarStartupAllowed: true
        };
      }
      return {
        kind: "none",
        reason: "no_ready_runtime",
        businessSidecarStartupAllowed: true,
        surfaceHostStartupAllowed: true,
        sidecarStartupAllowed: true
      };
    }
    function findCloudAuth2(workspaceDir2) {
      try {
        const paths = resolveMovScriptBackendPaths2(workspaceDir2);
        const cloudRoot = (0, import_node_path10.resolve)(paths.backendRealmsDir, "cloud");
        if (!(0, import_node_fs7.existsSync)(cloudRoot)) return { authenticated: false };
        for (const realmId of (0, import_node_fs7.readdirSync)(cloudRoot)) {
          const authPath = (0, import_node_path10.resolve)(cloudRoot, realmId, "auth.json");
          const auth = readJSON6(authPath);
          if (isRecord38(auth) && stringValue15(auth.token)) return { authenticated: true };
        }
      } catch {
      }
      return { authenticated: false };
    }
    function selectedBackendMode2(input3) {
      if (input3.localAvailable && input3.cloudAvailable) return input3.configuredIsLocal ? "local" : "cloud";
      if (input3.localAvailable) return "local";
      if (input3.cloudAvailable) return "cloud";
      return void 0;
    }
    function shouldRequireUserChoice2(input3) {
      return input3.localAvailable && input3.cloudAvailable && input3.projectAvailable || input3.localAvailable && !input3.projectAvailable || input3.cloudAvailable && !input3.projectAvailable;
    }
    function missingItems2(input3) {
      const missing = [];
      if (!input3.localAvailable && !input3.cloudAvailable) missing.push("backend");
      if (!input3.projectAvailable) missing.push("project_source");
      return missing;
    }
    function recommendedMode2(selected, hasProject) {
      if (selected === "local" && hasProject) return "local_backend_local_source";
      if (selected === "cloud" && hasProject) return "cloud_backend_local_source";
      if (selected === "cloud") return "cloud_backend_cloud_source";
      return void 0;
    }
    function runtimeReadinessSchema2(extra = {}) {
      return objectSchema42({
        homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
        home_dir: { type: "string", description: "Alias for homeDir." },
        movscriptHome: { type: "string", description: "Alias for homeDir." },
        movscript_home: { type: "string", description: "Alias for homeDir." },
        workspaceDir: { type: "string", description: "Optional MovScript workspace or project directory to inspect." },
        workspace_dir: { type: "string", description: "Alias for workspaceDir." },
        projectDir: { type: "string", description: "Optional project source directory to inspect." },
        project_dir: { type: "string", description: "Alias for projectDir." },
        projectId: { type: "string", description: "Optional project id to use when constructing surface URLs." },
        project_id: { type: "string", description: "Alias for projectId." },
        productionId: { type: "string", description: "Optional legacy production id to include in surface URLs." },
        production_id: { type: "string", description: "Alias for productionId." },
        scopeKind: { type: "string", description: "Optional timeline namespace scope kind." },
        scope_kind: { type: "string", description: "Alias for scopeKind." },
        scopeRef: { type: "string", description: "Optional timeline namespace scope ref." },
        scope_ref: { type: "string", description: "Alias for scopeRef." },
        targetKind: { type: "string", description: "Optional normalized target kind." },
        target_kind: { type: "string", description: "Alias for targetKind." },
        targetRef: { type: "string", description: "Optional normalized target ref." },
        target_ref: { type: "string", description: "Alias for targetRef." },
        timelineAssemblyRef: { type: "string", description: "Optional timeline assembly target ref." },
        timeline_assembly_ref: { type: "string", description: "Alias for timelineAssemblyRef." },
        localBackendURL: { type: "string", description: "Optional local backend URL to probe." },
        local_backend_url: { type: "string", description: "Alias for localBackendURL." },
        timeoutMs: { type: "number", description: "Probe timeout in milliseconds. Defaults to 750." },
        timeout_ms: { type: "number", description: "Alias for timeoutMs." },
        ...extra
      });
    }
    function objectSchema42(properties, required = []) {
      return {
        type: "object",
        properties,
        ...required.length > 0 ? { required } : {},
        additionalProperties: false
      };
    }
    function localDaemonControlSchema2() {
      return objectSchema42({
        homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
        home_dir: { type: "string", description: "Alias for homeDir." },
        movscriptHome: { type: "string", description: "Alias for homeDir." },
        movscript_home: { type: "string", description: "Alias for homeDir." }
      });
    }
    function localDaemonBootstrapSchema2() {
      return objectSchema42({
        homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
        home_dir: { type: "string", description: "Alias for homeDir." },
        movscriptHome: { type: "string", description: "Alias for homeDir." },
        movscript_home: { type: "string", description: "Alias for homeDir." },
        entrypoint: { type: "string", description: "Optional MovScript CLI entrypoint capable of running daemon run. Defaults to the current process entrypoint." },
        entry_point: { type: "string", description: "Alias for entrypoint." },
        cwd: { type: "string", description: "Optional working directory for the daemon launcher. Defaults to the current working directory." },
        dataPlane: { type: "string", enum: ["local", "cloud", "external"], description: "Optional daemon data plane." },
        data_plane: { type: "string", enum: ["local", "cloud", "external"], description: "Alias for dataPlane." },
        dataServiceURL: { type: "string", description: "Optional cloud or external Data Service URL." },
        data_service_url: { type: "string", description: "Alias for dataServiceURL." },
        idleTimeout: { type: "string", description: "Optional idle shutdown timeout, for example 30m or never." },
        idle_timeout: { type: "string", description: "Alias for idleTimeout." },
        startupTimeoutMs: { type: "number", description: "Optional daemon startup timeout in milliseconds." },
        startup_timeout_ms: { type: "number", description: "Alias for startupTimeoutMs." },
        stopTimeoutMs: { type: "number", description: "Optional timeout for stopping a stale daemon before restart, in milliseconds." },
        stop_timeout_ms: { type: "number", description: "Alias for stopTimeoutMs." },
        forceRestart: { type: "boolean", description: "When true, restart even if a matching daemon is already ready." },
        force_restart: { type: "boolean", description: "Alias for forceRestart." }
      });
    }
    function mergeTools2(primary, secondary) {
      const seen = /* @__PURE__ */ new Set();
      const tools = [];
      for (const tool of [...primary, ...secondary]) {
        if (seen.has(tool.name)) continue;
        seen.add(tool.name);
        tools.push(tool);
      }
      return tools;
    }
    function objectParam2(value, key) {
      if (!isRecord38(value)) return {};
      const candidate = value[key];
      return isRecord38(candidate) ? candidate : {};
    }
    function stringParam22(value, key) {
      if (!isRecord38(value)) return void 0;
      return stringValue15(value[key]);
    }
    function readJSON6(path) {
      try {
        return JSON.parse((0, import_node_fs7.readFileSync)(path, "utf8"));
      } catch {
        return void 0;
      }
    }
    function normalizeBaseURL3(value) {
      return value.trim().replace(/\/+$/, "").replace(/\/api\/v1$/, "") || DEFAULT_LOCAL_BACKEND2;
    }
    function isLocalBackendURL2(value) {
      try {
        const url = new URL(value);
        return ["localhost", "127.0.0.1", "::1", "[::1]"].includes(url.hostname);
      } catch {
        return false;
      }
    }
    function stringValue15(value) {
      return typeof value === "string" && value.trim() ? value.trim() : void 0;
    }
    function numberValue52(value) {
      return typeof value === "number" && Number.isFinite(value) ? value : void 0;
    }
    function recordValue42(value) {
      return isRecord38(value) ? value : {};
    }
    function isRecord38(value) {
      return typeof value === "object" && value !== null && !Array.isArray(value);
    }
    function writeMessage2(message) {
      process.stdout.write(`${JSON.stringify(message)}
`);
    }
    function errorMessage62(error) {
      return error instanceof Error ? error.message : String(error);
    }
    var MCP_HOST_HTTP_DEBUG = process.env.MOVSCRIPT_MCP_HOST_HTTP_DEBUG === "1";
    function registerMCPCommands(program2) {
      const mcp = program2.command("mcp").description("Run MovScript MCP protocol adapters");
      mcp.command("stdio").description("Run the MovScript MCP host over stdio").action(async () => {
        await startMCPStdioHost2();
      });
    }
    function registerRuntimeCommands(program2) {
      const runtime = program2.command("runtime").description("Discover, configure, and control the MovScript runtime daemon");
      addRuntimeContextOptions(runtime.command("status").description("Show runtime owner, backend, workspace, surface, and media readiness")).action(async (options, command) => {
        await runRuntimeTool("movscript_runtime_status", () => runtimeContextArgs(options, command), options, command);
      });
      addRuntimeConfigureOptions(runtime.command("configure").description("Configure the runtime backend binding for this CLI/MCP process")).action(async (options, command) => {
        await runRuntimeTool("movscript_runtime_configure", () => runtimeConfigureArgs(options, command), options, command);
      });
      const daemon = runtime.command("daemon").description("Discover and control the local MovScript daemon");
      addRuntimeContextOptions(daemon.command("discover").description("Read daemon endpoints from MovScript Home without starting anything")).action(async (options, command) => {
        await runRuntimeTool("runtime_daemon_discover", () => runtimeContextArgs(options, command), options, command);
      });
      addDaemonStartOptions(daemon.command("ensure").description("Start the local daemon if needed, otherwise reuse the current one")).action(async (options, command) => {
        await runRuntimeTool("runtime_daemon_ensure", () => daemonStartArgs(options, command), options, command);
      });
      addDaemonStartOptions(daemon.command("start").description("Explicitly start the local daemon if it is not ready")).action(async (options, command) => {
        await runRuntimeTool("runtime_daemon_start", () => daemonStartArgs(options, command), options, command);
      });
      addRuntimeContextOptions(daemon.command("status").description("Call the local daemon control endpoint status API")).action(async (options, command) => {
        await runRuntimeTool("runtime_daemon_status", () => runtimeContextArgs(options, command), options, command);
      });
      addConfirmedRuntimeOptions(daemon.command("stop").description("Stop the local daemon; requires --yes")).action(async (options, command) => {
        if (!options.yes) {
          printError("runtime_daemon_stop requires --yes", options);
          process.exitCode = 1;
          return;
        }
        await runRuntimeTool("runtime_daemon_stop", () => runtimeContextArgs(options, command), options, command);
      });
      addConfirmedRuntimeOptions(daemon.command("restart").description("Restart the local daemon; requires --yes")).action(async (options, command) => {
        if (!options.yes) {
          printError("runtime_daemon_restart requires --yes", options);
          process.exitCode = 1;
          return;
        }
        await runRuntimeTool("runtime_daemon_restart", () => runtimeContextArgs(options, command), options, command);
      });
      addRuntimeConfigureOptions(daemon.command("configure").description("Alias for runtime configure, kept near daemon control commands")).action(async (options, command) => {
        await runRuntimeTool("runtime_daemon_configure", () => runtimeConfigureArgs(options, command), options, command);
      });
      const descriptor = runtime.command("descriptor").description("Read runtime descriptor contracts");
      addRuntimeContextOptions(descriptor.command("get").description("Return canonical daemon/runtime service endpoints and readiness")).action(async (options, command) => {
        await runRuntimeTool("runtime_descriptor_get", () => runtimeContextArgs(options, command), options, command);
      });
      const preflight = runtime.command("preflight").description("Validate runtime prerequisites for project and production work");
      addRuntimeContextOptions(preflight.command("check").description("Return blocking and degraded runtime checks")).option("--require-project", "Block when the selected project source is missing", true).option("--no-require-project", "Treat missing project source as a warning").action(async (options, command) => {
        const result = await runRuntimeTool("runtime_preflight_check", () => ({
          ...runtimeContextArgs(options, command),
          requireProject: options.requireProject !== false
        }), options, command);
        if (isRecord39(result) && result.ready === false && process.exitCode === void 0) {
          process.exitCode = 2;
        }
      });
    }
    function addRuntimeContextOptions(command) {
      return command.option("--home-dir <dir>", "MovScript Home directory").option("--workspace <dir>", "Workspace root directory").option("--project-dir <dir>", "MovScript project source directory").option("--cwd <dir>", "Working directory for runtime operations").option("--timeout-ms <ms>", "Probe timeout in milliseconds").option("--json", "Print JSON output");
    }
    function addDaemonStartOptions(command) {
      return addRuntimeContextOptions(command).option("--entrypoint <path>", "Daemon bootstrap entrypoint").option("--data-plane <local|cloud|external>", "Runtime data plane for daemon bootstrap").option("--data-service-url <url>", "External/cloud Data Service URL for daemon bootstrap").option("--idle-timeout <duration>", "Daemon idle timeout passed to the local runtime").option("--startup-timeout-ms <ms>", "Startup wait timeout in milliseconds").option("--stop-timeout-ms <ms>", "Stop wait timeout in milliseconds").option("--force-restart", "Stop and restart an existing daemon before waiting for readiness");
    }
    function addConfirmedRuntimeOptions(command) {
      return addRuntimeContextOptions(command).option("--yes", "Confirm the daemon control action");
    }
    function addRuntimeConfigureOptions(command) {
      return addRuntimeContextOptions(command).option("--backend-mode <local|cloud|external>", "Configured backend mode").option("--backend-base-url <url>", "Backend or daemon gateway base URL").option("--token <token>", "Runtime backend token").option("--remember", "Persist backend base URL in the workspace .movscript config").option("--clear-token", "Clear persisted runtime/backend token state");
    }
    async function runRuntimeTool(name, argsInput, options, command) {
      try {
        const args = typeof argsInput === "function" ? argsInput() : argsInput;
        const result = await callMCPHostTool2({
          name,
          arguments: args
        });
        printResult2(result, options, command);
        if (isRecord39(result) && result.status === "error" && process.exitCode === void 0) {
          process.exitCode = 1;
        }
        return result;
      } catch (error) {
        printError(errorMessage72(error), options);
        process.exitCode = 1;
        return void 0;
      }
    }
    function runtimeContextArgs(options, command) {
      const global = commandGlobalOptions3(command);
      return compactRecord32({
        homeDir: options.homeDir,
        workspaceDir: options.workspace ?? global.workspace,
        projectDir: options.projectDir,
        cwd: options.cwd,
        timeoutMs: numberOption(options.timeoutMs)
      });
    }
    function daemonStartArgs(options, command) {
      return compactRecord32({
        ...runtimeContextArgs(options, command),
        entrypoint: options.entrypoint,
        dataPlane: options.dataPlane,
        dataServiceURL: options.dataServiceUrl,
        idleTimeout: options.idleTimeout,
        startupTimeoutMs: numberOption(options.startupTimeoutMs),
        stopTimeoutMs: numberOption(options.stopTimeoutMs),
        forceRestart: options.forceRestart === true ? true : void 0
      });
    }
    function runtimeConfigureArgs(options, command) {
      return compactRecord32({
        ...runtimeContextArgs(options, command),
        backendMode: options.backendMode,
        backendBaseURL: options.backendBaseUrl,
        token: options.token,
        remember: options.remember === true ? true : void 0,
        clearToken: options.clearToken === true ? true : void 0
      });
    }
    function commandGlobalOptions3(command) {
      const root = rootCommand2(command);
      const options = root.opts();
      return {
        workspace: typeof options.workspace === "string" ? options.workspace : void 0
      };
    }
    function rootCommand2(command) {
      let current = command;
      while (current.parent) current = current.parent;
      return current;
    }
    function numberOption(value) {
      if (value === void 0) return void 0;
      const parsed = Number(value);
      if (!Number.isFinite(parsed) || parsed <= 0) {
        throw new Error(`expected a positive number, received ${value}`);
      }
      return parsed;
    }
    function compactRecord32(input3) {
      return Object.fromEntries(Object.entries(input3).filter(([, value]) => value !== void 0));
    }
    function printResult2(result, _options, _command) {
      console.log(JSON.stringify(result, null, 2));
    }
    function printError(message, _options) {
      console.log(JSON.stringify({
        status: "error",
        error: {
          code: "runtime_command_failed",
          message
        }
      }, null, 2));
    }
    function isRecord39(value) {
      return Boolean(value && typeof value === "object" && !Array.isArray(value));
    }
    function errorMessage72(error) {
      return error instanceof Error ? error.message : String(error);
    }
    function registerWorkspaceCommands(program2) {
      const workspace = program2.command("workspace").alias("ws").description("Inspect, review, and interpret MovScript project workspaces");
      workspace.command("get-model <entityType>").description("Return the domain workspace model for one editable entity").option("--entity-id <id>", "Optional entity id used to expand editable path hints").option("--workspace <dir>", "Project workspace Git repository root").option("--cwd <dir>", "Alias for --workspace").option("--project-dir <dir>", "Alias for --workspace").option("--user <id>", "Workspace user id").option("--json", "Print JSON output").action(async (entityType, options) => {
        const result = getMovScriptWorkspaceModel2({
          entityKind: entityType,
          ...options.entityId !== void 0 ? { entityId: options.entityId } : {}
        });
        printResult3(result, options);
      });
      workspace.command("review").description("Review current MovScript source files and diagnostics without publishing product state").option("--workspace <dir>", "Project workspace Git repository root").option("--cwd <dir>", "Alias for --workspace").option("--project-dir <dir>", "Alias for --workspace").option("--user <id>", "Workspace user id").option("--org <id>", "Workspace organization id").option("--commit <ref>", "Compare current source against a specific git commit/ref").option("--json", "Print JSON output").action(async (options, command) => {
        const result = await workspaceEngine(options, command).review(inspectInput22(options));
        printResult3(result, options);
        if (!result.readyToInterpret) process.exitCode = 2;
      });
      workspace.command("interpret").description("Validate current MovScript source files and refresh interpreter debug artifacts").option("--workspace <dir>", "Project workspace Git repository root").option("--cwd <dir>", "Alias for --workspace").option("--project-dir <dir>", "Alias for --workspace").option("--user <id>", "Workspace user id").option("--org <id>", "Workspace organization id").option("--json", "Print JSON output").action(async (options, command) => {
        const result = await workspaceEngine(options, command).interpret();
        printResult3(result, options);
        if (result.status === "failed") process.exitCode = 2;
      });
    }
    function workspaceDir(options, command) {
      const global = commandGlobalOptions4(command);
      return options.projectDir ?? options.cwd ?? options.workspace ?? global.cwd ?? global.workspace;
    }
    function workspaceEngine(options, command) {
      const projectDir = workspaceDir(options, command);
      return createNodeMovScriptEngine2({ projectDir });
    }
    function inspectInput22(options) {
      return {
        ...options.commit ? { commit: options.commit } : {}
      };
    }
    function commandGlobalOptions4(command) {
      const root = command.parent?.parent ?? command.parent ?? command;
      const options = root.optsWithGlobals ? root.optsWithGlobals() : root.opts();
      return {
        workspace: typeof options.workspace === "string" ? options.workspace : void 0,
        cwd: typeof options.cwd === "string" ? options.cwd : void 0
      };
    }
    function printResult3(result, options) {
      if (options.json) {
        console.log(JSON.stringify(result, null, 2));
        return;
      }
      console.log(JSON.stringify(result, null, 2));
    }
    function createMovcliProgram(name = "movscript") {
      const program2 = new Command();
      program2.name(name).description("MovScript command line").version("0.2.0").option("--server <url>", "MovScript server URL", "http://localhost:8766").option("--token <token>", "API token (or set MOVSCRIPT_DATA_SERVICE_TOKEN env)").option("--workspace <dir>", "MovScript workspace root directory");
      registerAuthCommands(program2);
      registerRuntimeCommands(program2);
      registerMCPCommands(program2);
      registerAdminCommands(program2);
      registerLangCommands(program2);
      registerWorkspaceCommands(program2);
      configureCommandHelp(program2);
      return program2;
    }
    async function runMovcli2(argv = process.argv) {
      argv = normalizeMovcliArgv(argv);
      const program2 = createMovcliProgram(programNameFromArgv(argv));
      if (argv.length <= 2) {
        program2.outputHelp();
        return;
      }
      await program2.parseAsync(argv);
    }
    async function main2() {
      await runMovcli2(process.argv);
    }
    function normalizeMovcliArgv(argv) {
      const maybeShimPath = argv[2];
      if (maybeShimPath === "--") return [argv[0], argv[1], ...argv.slice(3)];
      if (!maybeShimPath || !["movcli.mjs", "movscript.mjs"].includes((0, import_node_path11.basename)(maybeShimPath))) return argv;
      return [argv[0], argv[1], ...argv.slice(3)];
    }
    function programNameFromArgv(argv) {
      const invoked = argv[1];
      if (!invoked) return "movscript";
      const name = (0, import_node_path11.basename)(invoked);
      return name.startsWith("movcli") ? "movcli" : "movscript";
    }
    function configureCommandHelp(command) {
      command.showHelpAfterError();
      command.showSuggestionAfterError();
      for (const child of command.commands) {
        configureCommandHelp(child);
        if (child.commands.length > 0) {
          child.action(() => {
            child.outputHelp();
          });
        }
      }
    }
    function isDirectMovcliInvocation() {
      const invoked = process.argv[1];
      if (!invoked) return false;
      const invokedName = (0, import_node_path11.basename)(invoked);
      if (invokedName === "movcli" || invokedName === "movcli.cmd" || invokedName === "movcli.mjs") return true;
      return invokedName === "index.cjs" || invokedName === "index.js" || invokedName === "index.ts";
    }
    if (isDirectMovcliInvocation()) {
      main2().catch((error) => {
        console.error(error instanceof Error ? error.message : String(error));
        process.exit(1);
      });
    }
  }
});

// src/agent-mcp.ts
import { spawn as spawn4 } from "child_process";
import { randomBytes } from "crypto";
import { createReadStream, existsSync as existsSync5, mkdirSync as mkdirSync5, readFileSync as readFileSync6, statSync, writeFileSync as writeFileSync5 } from "fs";
import { createServer as createServer5 } from "http";
import { basename as basename5, dirname as dirname8, extname as extname3, join as join10, resolve as resolve11 } from "path";

// ../../packages/runtime-contracts/dist/index.js
import { existsSync, mkdirSync, readFileSync, readdirSync, renameSync, writeFileSync } from "fs";
import { homedir } from "os";
import { join, resolve } from "path";
var MOVSCRIPT_DEFAULT_HOME_DIR_NAME = ".movscript";
var MOVSCRIPT_RUNTIME_DIR_NAME = "runtime";
var MOVSCRIPT_RUNTIME_APPS_DIR_NAME = "apps";
var MOVSCRIPT_RUNTIME_SERVICES_DIR_NAME = "services";
var MOVSCRIPT_RUNTIME_ENDPOINTS_DIR_NAME = "endpoints";
var MOVSCRIPT_APPLICATION_MANIFEST_SCHEMA = "movscript.application.v1";
var MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA = "movscript.program.v1";
var MOVSCRIPT_SCENARIO_POLICY_SCHEMA = "movscript.scenario-policy.v1";
function validateApplicationManifest(value) {
  const raw = asRecord(value);
  const errors = [];
  if (!raw) return { ok: false, errors: ["manifest must be an object"] };
  if (raw.schema !== MOVSCRIPT_APPLICATION_MANIFEST_SCHEMA) errors.push(`schema must be ${MOVSCRIPT_APPLICATION_MANIFEST_SCHEMA}`);
  const applicationId = stringValue(raw.applicationId);
  const name = stringValue(raw.name);
  const owner = ownerKindValue(raw.owner);
  const programs = stringArrayValue(raw.programs);
  if (!applicationId) errors.push("applicationId is required");
  if (!name) errors.push("name is required");
  if (!owner) errors.push("owner must be a supported application owner kind");
  if (raw.programs !== void 0 && !programs) errors.push("programs must be an array of strings");
  if (errors.length > 0 || !applicationId || !name || !owner) return { ok: false, errors };
  return {
    ok: true,
    errors: [],
    manifest: {
      schema: MOVSCRIPT_APPLICATION_MANIFEST_SCHEMA,
      applicationId,
      name,
      owner,
      ...programs ? { programs } : {}
    }
  };
}
function validateProgramManifest(value) {
  const raw = asRecord(value);
  const errors = [];
  if (!raw) return { ok: false, errors: ["manifest must be an object"] };
  if (raw.schema !== MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA) errors.push(`schema must be ${MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA}`);
  const programId = stringValue(raw.programId);
  const serviceName = stringValue(raw.serviceName);
  const kind = programKindValue(raw.kind);
  const name = stringValue(raw.name);
  const profiles = stringArrayValue(raw.profiles);
  const entry = programEntryValue(raw.entry);
  const transport = programTransportValue(raw.transport);
  const health = programHealthValue(raw.health);
  const dependsOn = stringArrayValue(raw.dependsOn);
  const provides = stringArrayValue(raw.provides);
  if (!programId) errors.push("programId is required");
  if (!serviceName) errors.push("serviceName is required");
  if (!kind) errors.push("kind must be a supported program kind");
  if (raw.profiles !== void 0 && !profiles) errors.push("profiles must be an array of strings");
  if (raw.entry !== void 0 && !entry) errors.push("entry must define a command and optional string args/cwd");
  if (raw.transport !== void 0 && !transport) errors.push("transport must be a supported transport kind");
  if (raw.health !== void 0 && !health) errors.push("health must define a supported kind and optional target");
  if (raw.dependsOn !== void 0 && !dependsOn) errors.push("dependsOn must be an array of strings");
  if (raw.provides !== void 0 && !provides) errors.push("provides must be an array of strings");
  if (errors.length > 0 || !programId || !serviceName || !kind) return { ok: false, errors };
  return {
    ok: true,
    errors: [],
    manifest: {
      schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
      programId,
      serviceName,
      kind,
      ...name ? { name } : {},
      ...profiles ? { profiles } : {},
      ...entry ? { entry } : {},
      ...transport ? { transport } : {},
      ...health ? { health } : {},
      ...dependsOn ? { dependsOn } : {},
      ...provides ? { provides } : {}
    }
  };
}
function validateScenarioPolicyManifest(value) {
  const raw = asRecord(value);
  const errors = [];
  if (!raw) return { ok: false, errors: ["manifest must be an object"] };
  if (raw.schema !== MOVSCRIPT_SCENARIO_POLICY_SCHEMA) errors.push(`schema must be ${MOVSCRIPT_SCENARIO_POLICY_SCHEMA}`);
  const scenarioId = stringValue(raw.scenarioId);
  const applicationId = stringValue(raw.applicationId);
  const programRecords = Array.isArray(raw.programs) ? raw.programs : void 0;
  if (!scenarioId) errors.push("scenarioId is required");
  if (!applicationId) errors.push("applicationId is required");
  if (!programRecords) errors.push("programs must be an array");
  const programs = (programRecords ?? []).flatMap((item, index) => {
    const record = asRecord(item);
    const serviceName = record ? stringValue(record.serviceName) : void 0;
    if (!record || !serviceName) {
      errors.push(`programs[${index}].serviceName is required`);
      return [];
    }
    return [{
      serviceName,
      ...typeof record.required === "boolean" ? { required: record.required } : {},
      ...stringValue(record.profile) ? { profile: stringValue(record.profile) } : {}
    }];
  });
  if (errors.length > 0 || !scenarioId || !applicationId || !programRecords) return { ok: false, errors };
  return {
    ok: true,
    errors: [],
    manifest: {
      schema: MOVSCRIPT_SCENARIO_POLICY_SCHEMA,
      scenarioId,
      applicationId,
      programs
    }
  };
}
function resolveMovScriptHomeDir(options = {}) {
  const env = options.env ?? process.env;
  const explicit = stringValue(env.MOVSCRIPT_HOME);
  if (explicit) return resolve(explicit);
  const legacy = stringValue(env.MOVSCRIPT_WORKSPACE_DIR);
  if (legacy) return resolve(legacy);
  return resolve(options.userHomeDir ?? homedir(), MOVSCRIPT_DEFAULT_HOME_DIR_NAME);
}
function resolveRuntimeHomePaths(homeDir = resolveMovScriptHomeDir()) {
  const runtimeDir = join(homeDir, MOVSCRIPT_RUNTIME_DIR_NAME);
  return {
    homeDir,
    runtimeDir,
    appsDir: join(runtimeDir, MOVSCRIPT_RUNTIME_APPS_DIR_NAME),
    servicesDir: join(runtimeDir, MOVSCRIPT_RUNTIME_SERVICES_DIR_NAME),
    endpointsDir: join(runtimeDir, MOVSCRIPT_RUNTIME_ENDPOINTS_DIR_NAME)
  };
}
function readRuntimeHomeSnapshot(homeDir = resolveMovScriptHomeDir()) {
  const paths = resolveRuntimeHomePaths(homeDir);
  return {
    homeDir: paths.homeDir,
    apps: readAppRecords(paths.appsDir),
    services: readServiceRecords(paths.servicesDir),
    endpoints: readEndpointRecords(paths.endpointsDir)
  };
}
function writeRuntimeAppRecord(homeDir, input) {
  const paths = resolveRuntimeHomePaths(homeDir);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const path = join(paths.appsDir, `${safeRecordFileName(input.applicationId)}.json`);
  writeJSONAtomic(path, compactRecord({
    applicationId: input.applicationId,
    owner: input.owner,
    profile: input.profile,
    pid: input.pid,
    status: input.status ?? "ready",
    ready: input.ready ?? (input.status === void 0 || input.status === "ready"),
    endpoint: input.endpoint ? compactRecord({
      ...input.endpoint,
      status: input.endpoint.status ?? input.status ?? "ready",
      ready: input.endpoint.ready ?? input.ready ?? (input.status === void 0 || input.status === "ready"),
      updatedAt: input.endpoint.updatedAt ?? input.updatedAt ?? now
    }) : void 0,
    updatedAt: input.updatedAt ?? now,
    metadata: input.metadata
  }));
  return path;
}
function writeRuntimeServiceRecord(homeDir, input) {
  const paths = resolveRuntimeHomePaths(homeDir);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const serviceDir = join(paths.servicesDir, safeRecordFileName(input.serviceName));
  const path = join(serviceDir, `${safeRecordFileName(input.instanceId)}.json`);
  writeJSONAtomic(path, compactRecord({
    serviceName: input.serviceName,
    instanceId: input.instanceId,
    ownerApplicationId: input.ownerApplicationId,
    profile: input.profile,
    pid: input.pid,
    status: input.status ?? "ready",
    ready: input.ready ?? (input.status === void 0 || input.status === "ready"),
    endpoint: input.endpoint ? compactRecord({
      ...input.endpoint,
      serviceName: input.endpoint.serviceName ?? input.serviceName,
      instanceId: input.endpoint.instanceId ?? input.instanceId,
      status: input.endpoint.status ?? input.status ?? "ready",
      ready: input.endpoint.ready ?? input.ready ?? (input.status === void 0 || input.status === "ready"),
      updatedAt: input.endpoint.updatedAt ?? input.updatedAt ?? now
    }) : void 0,
    updatedAt: input.updatedAt ?? now,
    metadata: input.metadata
  }));
  return path;
}
function writeRuntimeEndpointRecord(homeDir, input) {
  const paths = resolveRuntimeHomePaths(homeDir);
  const id = input.serviceName ?? input.applicationId ?? input.instanceId;
  if (!id) throw new Error("Runtime endpoint record requires serviceName, applicationId, or instanceId");
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const path = join(paths.endpointsDir, `${safeRecordFileName(id)}.json`);
  writeJSONAtomic(path, compactRecord({
    ...input,
    status: input.status ?? "ready",
    ready: input.ready ?? (input.status === void 0 || input.status === "ready"),
    updatedAt: input.updatedAt ?? now
  }));
  return path;
}
function activeAppRecords(snapshot) {
  return snapshot.apps.filter((record) => record.ready && pidIsAlive(record.pid));
}
function activeServiceRecords(snapshot) {
  return snapshot.services.filter((record) => record.ready && pidIsAlive(record.pid));
}
function activeEndpointRecords(snapshot) {
  return snapshot.endpoints.filter((record) => record.ready && pidIsAlive(record.pid));
}
function findRuntimeApp(snapshot, applicationId) {
  return activeAppRecords(snapshot).find((record) => record.applicationId === applicationId);
}
function findRuntimeService(snapshot, serviceName) {
  return activeServiceRecords(snapshot).find((record) => record.serviceName === serviceName);
}
function findRuntimeEndpoint(snapshot, serviceName) {
  return activeEndpointRecords(snapshot).find((record) => record.serviceName === serviceName);
}
function pidIsAlive(pid) {
  if (pid === void 0) return true;
  if (!Number.isInteger(pid) || pid <= 0) return false;
  try {
    process.kill(pid, 0);
    return true;
  } catch {
    return false;
  }
}
function readAppRecords(appsDir) {
  return readJSONFiles(appsDir).flatMap(({ path, value }) => {
    const raw = asRecord(value);
    if (!raw) return [];
    const applicationId = stringValue(raw.applicationId ?? raw.appId ?? raw.id) ?? fileStem(path);
    if (!applicationId) return [];
    const endpoint = endpointRecordFrom(raw.endpoint, path, { applicationId });
    return [{
      applicationId,
      owner: stringValue(raw.owner ?? raw.runtimeOwner),
      profile: stringValue(raw.profile),
      pid: numberValue(raw.pid),
      status: statusValue(raw.status),
      ready: readyValue(raw),
      ...endpoint ? { endpoint } : {},
      updatedAt: stringValue(raw.updatedAt ?? raw.startedAt ?? raw.createdAt),
      raw,
      path
    }];
  });
}
function readServiceRecords(servicesDir) {
  if (!existsSync(servicesDir)) return [];
  const records = [];
  for (const serviceName of safeReaddir(servicesDir)) {
    const serviceDir = join(servicesDir, serviceName);
    for (const { path, value } of readJSONFiles(serviceDir)) {
      const raw = asRecord(value);
      if (!raw) continue;
      const instanceId = stringValue(raw.instanceId ?? raw.id) ?? fileStem(path);
      if (!instanceId) continue;
      const endpoint = endpointRecordFrom(raw.endpoint, path, { serviceName, instanceId });
      records.push({
        serviceName: stringValue(raw.serviceName) ?? serviceName,
        instanceId,
        ownerApplicationId: stringValue(raw.ownerApplicationId ?? raw.applicationId ?? raw.appId),
        profile: stringValue(raw.profile),
        pid: numberValue(raw.pid),
        status: statusValue(raw.status),
        ready: readyValue(raw),
        ...endpoint ? { endpoint } : {},
        updatedAt: stringValue(raw.updatedAt ?? raw.startedAt ?? raw.createdAt),
        raw,
        path
      });
    }
  }
  return records;
}
function readEndpointRecords(endpointsDir) {
  return readJSONFiles(endpointsDir).flatMap(({ path, value }) => {
    const record = endpointRecordFrom(value, path, { serviceName: fileStem(path) });
    return record ? [record] : [];
  });
}
function endpointRecordFrom(value, path, defaults) {
  const raw = asRecord(value);
  if (!raw) return void 0;
  const serviceName = stringValue(raw.serviceName ?? raw.service ?? defaults.serviceName);
  const applicationId = stringValue(raw.applicationId ?? raw.appId ?? defaults.applicationId);
  const instanceId = stringValue(raw.instanceId ?? raw.id ?? defaults.instanceId);
  const baseURL = stringValue(raw.baseURL ?? raw.baseUrl ?? raw.url);
  const url = stringValue(raw.url ?? raw.endpointURL ?? raw.endpointUrl ?? raw.mcpURL ?? raw.mcpUrl);
  const healthURL = stringValue(raw.healthURL ?? raw.healthUrl ?? raw.healthEndpoint);
  const port = numberValue(raw.port);
  const pid = numberValue(raw.pid);
  const protocol = stringValue(raw.protocol);
  if (!serviceName && !applicationId && !baseURL && !url && !healthURL && !port && !pid) return void 0;
  return {
    ...serviceName ? { serviceName } : {},
    ...applicationId ? { applicationId } : {},
    ...instanceId ? { instanceId } : {},
    ...baseURL ? { baseURL } : {},
    ...url ? { url } : {},
    ...healthURL ? { healthURL } : {},
    ...protocol ? { protocol } : {},
    ...pid !== void 0 ? { pid } : {},
    ...port !== void 0 ? { port } : {},
    status: statusValue(raw.status),
    ready: readyValue(raw),
    updatedAt: stringValue(raw.updatedAt ?? raw.startedAt ?? raw.createdAt),
    raw,
    path
  };
}
function readJSONFiles(dir) {
  if (!existsSync(dir)) return [];
  return safeReaddir(dir).filter((name) => name.endsWith(".json")).map((name) => {
    const path = join(dir, name);
    return { path, value: readJSON(path) };
  });
}
function readJSON(path) {
  try {
    return JSON.parse(readFileSync(path, "utf8"));
  } catch {
    return void 0;
  }
}
function writeJSONAtomic(path, value) {
  mkdirSync(path.replace(/[\\/][^\\/]+$/, ""), { recursive: true });
  const tmpPath = `${path}.${process.pid}.${Date.now()}.tmp`;
  writeFileSync(tmpPath, `${JSON.stringify(value, null, 2)}
`, "utf8");
  renameSync(tmpPath, path);
}
function safeRecordFileName(value) {
  return value.trim().replace(/[^A-Za-z0-9._-]+/g, "_") || "unknown";
}
function compactRecord(record) {
  const out = {};
  for (const [key, value] of Object.entries(record)) {
    if (value !== void 0) out[key] = value;
  }
  return out;
}
function safeReaddir(dir) {
  try {
    return readdirSync(dir);
  } catch {
    return [];
  }
}
function fileStem(path) {
  const name = path.split(/[\\/]/).pop();
  if (!name) return void 0;
  return name.endsWith(".json") ? name.slice(0, -5) : name;
}
function statusValue(value) {
  if (value === "starting" || value === "ready" || value === "stopping" || value === "stopped" || value === "error") return value;
  return "unknown";
}
function readyValue(raw) {
  if (typeof raw.ready === "boolean") return raw.ready;
  const status = statusValue(raw.status);
  return status === "ready" || status === "unknown";
}
function stringValue(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberValue(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function stringArrayValue(value) {
  if (!Array.isArray(value)) return void 0;
  const items = value.map((item) => stringValue(item));
  return items.every((item) => Boolean(item)) ? items : void 0;
}
function programEntryValue(value) {
  const raw = asRecord(value);
  if (!raw) return void 0;
  const command = stringValue(raw.command);
  const args = stringArrayValue(raw.args);
  const cwd = stringValue(raw.cwd);
  if (!command) return void 0;
  if (raw.args !== void 0 && !args) return void 0;
  return {
    command,
    ...args ? { args } : {},
    ...cwd ? { cwd } : {}
  };
}
function programHealthValue(value) {
  const raw = asRecord(value);
  if (!raw) return void 0;
  const kind = programHealthKindValue(raw.kind);
  const target = stringValue(raw.target);
  if (!kind) return void 0;
  return {
    kind,
    ...target ? { target } : {}
  };
}
function ownerKindValue(value) {
  if (value === "electron" || value === "agent-provider" || value === "cloud-orchestrator" || value === "cli" || value === "test") return value;
  return void 0;
}
function programKindValue(value) {
  if (value === "service" || value === "mcp-endpoint" || value === "web" || value === "cli" || value === "worker" || value === "desktop-shell") return value;
  return void 0;
}
function programTransportValue(value) {
  if (value === "http" || value === "stdio" || value === "ipc" || value === "embedded" || value === "none") return value;
  return void 0;
}
function programHealthKindValue(value) {
  if (value === "http" || value === "process" || value === "stdio_tool" || value === "none") return value;
  return void 0;
}
function asRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value) ? value : void 0;
}

// ../../packages/app-runner/dist/index.js
var ApplicationRunner = class {
  homeDir;
  application;
  programs;
  profile;
  pid;
  state = "idle";
  logFn;
  abortController;
  started = [];
  constructor(options) {
    const appValidation = validateApplicationManifest(options.application);
    if (!appValidation.ok || !appValidation.manifest) {
      throw new Error(`Invalid application manifest:
${appValidation.errors.join("\n")}`);
    }
    for (const program of options.programs ?? []) {
      const programValidation = validateProgramManifest(program.manifest);
      if (!programValidation.ok) {
        throw new Error(`Invalid program manifest for ${program.manifest?.programId ?? "unknown"}:
${programValidation.errors.join("\n")}`);
      }
    }
    this.homeDir = options.homeDir;
    this.application = appValidation.manifest;
    this.programs = options.programs ?? [];
    this.profile = options.profile;
    this.pid = options.pid ?? process.pid;
    this.logFn = options.log ?? (() => void 0);
    this.abortController = options.abortController ?? new AbortController();
  }
  async start() {
    if (this.state !== "idle" && this.state !== "stopped") {
      throw new Error(`ApplicationRunner cannot start from state ${this.state}`);
    }
    this.state = "starting";
    this.writeApplicationRecord("starting", false);
    try {
      await this.runProgramPhase("prepare");
      await this.runProgramPhase("configure");
      for (const adapter of this.programs) {
        const context = this.createProgramContext(adapter);
        this.writeServiceRecord(context, "starting", false, {});
        const runtime = await adapter.start?.(context) ?? {};
        const health = adapter.health ? await adapter.health(context, runtime) : { ready: true, status: "ready", endpoint: runtime.endpoint };
        const ready = health.ready;
        const status = health.status ?? (ready ? "ready" : "error");
        const endpoint = health.endpoint ?? runtime.endpoint;
        this.writeServiceRecord(context, status, ready, runtime, endpoint);
        if (endpoint && ready) writeRuntimeEndpointRecord(this.homeDir, normalizeEndpoint(endpoint, context, runtime));
        if (!ready) throw new Error(health.message || `${context.program.serviceName} did not become ready`);
        this.started.push({ adapter, context, runtime });
      }
      this.state = "ready";
      this.writeApplicationRecord("ready", true);
    } catch (error) {
      this.state = "error";
      this.writeApplicationRecord("error", false, { error: errorMessage(error) });
      await this.shutdownStarted("error");
      throw error;
    }
  }
  async shutdown() {
    if (this.state === "stopping" || this.state === "stopped") return;
    this.state = "stopping";
    this.writeApplicationRecord("stopping", false);
    await this.shutdownStarted("stopped");
    this.state = "stopped";
    this.writeApplicationRecord("stopped", false);
  }
  async runProgramPhase(phase) {
    for (const adapter of this.programs) {
      const context = this.createProgramContext(adapter);
      if (phase === "prepare") await adapter.prepare?.(context);
      else await adapter.configure?.(context);
    }
  }
  async shutdownStarted(finalStatus) {
    const programs = [...this.started].reverse();
    this.started.length = 0;
    for (const item of programs) {
      try {
        await item.adapter.stop?.(item.context, item.runtime);
        this.writeServiceRecord(item.context, finalStatus, false, item.runtime);
      } catch (error) {
        this.writeServiceRecord(item.context, "error", false, item.runtime, item.runtime.endpoint, { error: errorMessage(error) });
      }
    }
  }
  createProgramContext(adapter) {
    const instanceId = adapter.instanceId ?? `${adapter.manifest.programId}-${this.pid}`;
    return {
      homeDir: this.homeDir,
      application: this.application,
      program: adapter.manifest,
      instanceId,
      profile: adapter.profile ?? this.profile,
      signal: this.abortController.signal,
      log: (message, metadata) => this.log(message, {
        programId: adapter.manifest.programId,
        serviceName: adapter.manifest.serviceName,
        ...metadata
      })
    };
  }
  writeApplicationRecord(status, ready, metadata) {
    writeRuntimeAppRecord(this.homeDir, {
      applicationId: this.application.applicationId,
      owner: this.application.owner,
      profile: this.profile,
      pid: this.pid,
      status,
      ready,
      metadata
    });
  }
  writeServiceRecord(context, status, ready, runtime, endpoint, metadata) {
    writeRuntimeServiceRecord(this.homeDir, {
      serviceName: context.program.serviceName,
      instanceId: context.instanceId,
      ownerApplicationId: this.application.applicationId,
      profile: context.profile,
      pid: runtime.pid,
      status,
      ready,
      endpoint: endpoint || runtime.endpoint ? normalizeEndpoint(endpoint ?? runtime.endpoint, context, runtime) : void 0,
      metadata: {
        ...runtime.metadata ?? {},
        ...metadata ?? {}
      }
    });
  }
  log(message, metadata) {
    this.logFn(message, metadata);
  }
};
function createApplicationRunner(options) {
  return new ApplicationRunner(options);
}
function resolveScenarioProgramAdapters(options) {
  const appValidation = validateApplicationManifest(options.application);
  if (!appValidation.ok || !appValidation.manifest) {
    throw new Error(`Invalid application manifest:
${appValidation.errors.join("\n")}`);
  }
  const scenarioValidation = validateScenarioPolicyManifest(options.scenario);
  if (!scenarioValidation.ok || !scenarioValidation.manifest) {
    throw new Error(`Invalid scenario policy manifest:
${scenarioValidation.errors.join("\n")}`);
  }
  const application = appValidation.manifest;
  const scenario = scenarioValidation.manifest;
  if (scenario.applicationId !== application.applicationId) {
    throw new Error(`Scenario ${scenario.scenarioId} belongs to ${scenario.applicationId}, not ${application.applicationId}`);
  }
  const applicationPrograms = new Set(application.programs ?? []);
  const adaptersByServiceName = new Map(options.programs.map((adapter) => [adapter.manifest.serviceName, adapter]));
  const resolved = [];
  for (const policy of scenario.programs) {
    if (applicationPrograms.size > 0 && !applicationPrograms.has(policy.serviceName)) {
      throw new Error(`Scenario ${scenario.scenarioId} references ${policy.serviceName}, but the application manifest does not declare it`);
    }
    const adapter = adaptersByServiceName.get(policy.serviceName);
    if (!adapter) {
      if (policy.required !== false) {
        throw new Error(`Scenario ${scenario.scenarioId} requires ${policy.serviceName}, but no ProgramAdapter was provided`);
      }
      continue;
    }
    resolved.push({
      ...adapter,
      profile: policy.profile ?? adapter.profile
    });
  }
  return resolved;
}
function createScenarioApplicationRunner(options) {
  return createApplicationRunner({
    ...options,
    profile: options.profile ?? options.scenario.scenarioId,
    programs: resolveScenarioProgramAdapters({
      application: options.application,
      scenario: options.scenario,
      programs: options.programs
    })
  });
}
function normalizeEndpoint(endpoint, context, runtime) {
  return {
    ...endpoint,
    serviceName: endpoint.serviceName ?? context.program.serviceName,
    applicationId: endpoint.applicationId ?? context.application.applicationId,
    instanceId: endpoint.instanceId ?? context.instanceId,
    pid: endpoint.pid ?? runtime.pid
  };
}
function errorMessage(error) {
  return error instanceof Error ? error.message : String(error);
}

// ../../services/media-pipeline/src/server.mjs
import { createServer } from "http";

// ../../packages/editing/dist/chunk-7KPHPMO7.js
var MediaEditingProjectService = class {
  project;
  generatedId = 0;
  now;
  idFactory;
  constructor(project, options = {}) {
    this.project = clone(project);
    this.now = options.now ?? (() => (/* @__PURE__ */ new Date()).toISOString());
    this.idFactory = options.idFactory;
  }
  getProject() {
    return clone(this.project);
  }
  applyCommand(command) {
    switch (command.type) {
      case "add_track":
        this.addTrack(command.track);
        break;
      case "remove_track":
        this.removeTrack(command.trackId);
        break;
      case "add_clip":
        this.addClip(command.trackId, command.clip);
        break;
      case "update_clip":
        this.updateClip(command.clipId, command.patch);
        break;
      case "move_clip":
        this.moveClip(command.clipId, command.targetTrackId, command.timelineStartMs);
        break;
      case "split_clip":
        this.splitClip(command.clipId, command.splitTimeMs, command.retainSide ?? "both");
        break;
      case "delete_clip":
        this.deleteClip(command.clipId);
        break;
      default:
        assertNever(command);
    }
    this.refreshProject();
    return this.getProject();
  }
  addTrack(track) {
    if (this.project.timeline.tracks.some((candidate) => candidate.id === track.id)) {
      throw new Error(`Media track already exists: ${track.id}`);
    }
    this.project.timeline.tracks.push(clone(track));
    this.sortTracks();
  }
  removeTrack(trackId) {
    const track = this.trackById(trackId);
    if (track.clips.length > 0) throw new Error(`Cannot remove non-empty media track: ${trackId}`);
    this.project.timeline.tracks = this.project.timeline.tracks.filter((candidate) => candidate.id !== trackId);
  }
  addClip(trackId, clip) {
    const track = this.trackById(trackId);
    assertClipFitsTrack(track, clip);
    if (this.findClip(clip.id)) throw new Error(`Media clip already exists: ${clip.id}`);
    track.clips.push(clone(clip));
    sortClips(track);
  }
  updateClip(clipId, patch) {
    const found = this.requiredClip(clipId);
    found.track.clips[found.index] = {
      ...found.clip,
      ...clone(patch)
    };
    sortClips(found.track);
  }
  moveClip(clipId, targetTrackId, timelineStartMs) {
    const found = this.requiredClip(clipId);
    const moved = {
      ...found.clip,
      timelineStartMs
    };
    if (!targetTrackId || targetTrackId === found.track.id) {
      found.track.clips[found.index] = moved;
      sortClips(found.track);
      return;
    }
    const targetTrack = this.trackById(targetTrackId);
    assertClipFitsTrack(targetTrack, moved);
    found.track.clips.splice(found.index, 1);
    targetTrack.clips.push(moved);
    sortClips(found.track);
    sortClips(targetTrack);
  }
  splitClip(clipId, splitTimeMs, retainSide) {
    const found = this.requiredClip(clipId);
    const clipEndMs = found.clip.timelineStartMs + found.clip.durationMs;
    if (splitTimeMs <= found.clip.timelineStartMs || splitTimeMs >= clipEndMs) {
      throw new Error(`Split time ${splitTimeMs} is outside media clip ${clipId}`);
    }
    const leftDurationMs = splitTimeMs - found.clip.timelineStartMs;
    const rightDurationMs = found.clip.durationMs - leftDurationMs;
    const sourceStartMs = found.clip.sourceStartMs;
    const left = {
      ...found.clip,
      durationMs: leftDurationMs,
      sourceEndMs: sourceStartMs !== void 0 ? sourceStartMs + leftDurationMs : found.clip.sourceEndMs
    };
    const right = {
      ...found.clip,
      id: this.makeId(`${found.clip.id}_right`),
      timelineStartMs: splitTimeMs,
      durationMs: rightDurationMs,
      sourceStartMs: sourceStartMs !== void 0 ? sourceStartMs + leftDurationMs : found.clip.sourceStartMs
    };
    const replacement = retainSide === "left" ? [left] : retainSide === "right" ? [right] : [left, right];
    found.track.clips.splice(found.index, 1, ...replacement);
    sortClips(found.track);
  }
  deleteClip(clipId) {
    const found = this.requiredClip(clipId);
    found.track.clips.splice(found.index, 1);
  }
  refreshProject() {
    this.project.updatedAt = this.now();
    this.project.revision += 1;
    this.project.timeline.durationMs = durationFromTracks(this.project.timeline.tracks);
  }
  sortTracks() {
    this.project.timeline.tracks.sort((left, right) => {
      if (left.zIndex !== right.zIndex) return left.zIndex - right.zIndex;
      return left.id.localeCompare(right.id);
    });
  }
  trackById(trackId) {
    const track = this.project.timeline.tracks.find((candidate) => candidate.id === trackId);
    if (!track) throw new Error(`Media track not found: ${trackId}`);
    return track;
  }
  requiredClip(clipId) {
    const found = this.findClip(clipId);
    if (!found) throw new Error(`Media clip not found: ${clipId}`);
    return found;
  }
  findClip(clipId) {
    for (const track of this.project.timeline.tracks) {
      const index = track.clips.findIndex((candidate) => candidate.id === clipId);
      const clip = index >= 0 ? track.clips[index] : void 0;
      if (clip) return { track, clip, index };
    }
    return void 0;
  }
  makeId(prefix) {
    if (this.idFactory) return this.idFactory(prefix);
    this.generatedId += 1;
    return `${prefix}_${this.generatedId}`;
  }
};
function createMediaEditingProjectFromMovScriptEditPlan(editPlan, options = {}) {
  const now = options.now ?? (/* @__PURE__ */ new Date()).toISOString();
  const projectId = options.projectId ?? `movscript_${String(editPlan.productionId)}`;
  const assets = buildMediaAssetRegistryFromEditPlan(editPlan);
  const timeline = buildMediaTimelineRecipeFromEditPlan(editPlan, assets, options);
  return {
    version: 1,
    id: options.id ?? `editing_project_${String(editPlan.sceneMomentId)}`,
    projectId,
    title: options.title ?? `Scene moment ${String(editPlan.sceneMomentId)}`,
    source: {
      kind: "movscript_edit_plan",
      targetKind: editPlan.target_kind ?? "scene_moment",
      targetRef: editPlan.target_ref,
      scopeKind: editPlan.scope_kind,
      scopeRef: editPlan.scope_ref === void 0 ? void 0 : String(editPlan.scope_ref),
      sceneMomentId: String(editPlan.sceneMomentId),
      productionId: String(editPlan.productionId),
      contentUnitIds: editPlan.tracks.flatMap((track) => track.items.map((item) => String(item.content_unit_id)))
    },
    timeline,
    assets,
    provenance: {
      targetKind: editPlan.target_kind ?? "scene_moment",
      targetRef: editPlan.target_ref,
      scopeKind: editPlan.scope_kind,
      scopeRef: editPlan.scope_ref === void 0 ? void 0 : String(editPlan.scope_ref),
      legacyTargetKind: editPlan.legacy_target_kind,
      legacyTargetRef: editPlan.legacy_target_ref === void 0 ? void 0 : String(editPlan.legacy_target_ref),
      productionPath: editPlan.productionPath,
      sceneMomentPath: editPlan.sceneMomentPath,
      selectedCandidateIds: editPlan.tracks.flatMap((track) => track.items).filter((item) => item.selected && item.candidate_id !== void 0).map((item) => String(item.candidate_id)),
      inputResourceIds: assets.assets.flatMap((asset) => asset.resourceId === void 0 ? [] : [asset.resourceId])
    },
    createdAt: now,
    updatedAt: now,
    revision: 1
  };
}
function createMediaEditingProjectFromProductionTimelineClips(options) {
  const productionId = String(options.productionId);
  return createMediaEditingProjectFromTimelineAssemblyClips({
    ...options,
    productionId,
    productionPath: options.productionPath,
    targetKind: options.targetKind ?? "timeline_assembly",
    targetRef: options.targetRef,
    scopeKind: options.scopeKind ?? "production",
    scopeRef: options.scopeRef ?? productionId,
    legacyTargetKind: options.targetKind === void 0 || options.targetKind === "timeline_assembly" ? "production" : void 0,
    legacyTargetRef: options.targetKind === void 0 || options.targetKind === "timeline_assembly" ? productionId : void 0
  });
}
function createMediaEditingProjectFromTimelineAssemblyClips(options) {
  const now = options.now ?? (/* @__PURE__ */ new Date()).toISOString();
  const scopeKind = options.scopeKind;
  const scopeRef = String(options.scopeRef);
  const target = timelineAssemblyTarget(options);
  const defaultDurationMs = options.defaultDurationMs ?? 4e3;
  let cursorMs = 0;
  const assets = [];
  const clips = options.clips.map((clip) => {
    const durationMs = Math.max(1, Math.round((clip.durationSec ?? defaultDurationMs / 1e3) * 1e3));
    const asset = {
      id: `movscript_resource_${clip.resourceId}`,
      sourceKind: "backend_resource",
      assetType: "video",
      resourceId: clip.resourceId,
      label: clip.title,
      metadata: {
        movscript: {
          sceneMomentId: clip.sceneMomentId,
          sceneMomentPath: clip.sceneMomentPath,
          contentUnitId: clip.contentUnitId,
          candidateId: clip.candidateId,
          resourceId: clip.resourceId,
          outputKind: "video",
          trackType: "video",
          targetKind: target.targetKind,
          targetRef: target.targetRef,
          scopeKind: target.scopeKind,
          scopeRef: target.scopeRef,
          legacyTargetKind: target.legacyTargetKind,
          legacyTargetRef: target.legacyTargetRef,
          selected: true,
          stale: false
        }
      }
    };
    assets.push(asset);
    const mediaClip = {
      id: clip.id || `assembly_clip_${safeId(scopeKind)}_${safeId(scopeRef)}_${assets.length}`,
      assetType: "video",
      asset,
      timelineStartMs: cursorMs,
      durationMs,
      sourceStartMs: 0,
      sourceEndMs: durationMs,
      fit: "cover",
      opacity: 1,
      muted: false,
      metadata: asset.metadata
    };
    cursorMs += durationMs;
    return mediaClip;
  });
  return {
    version: 1,
    id: options.id ?? `editing_project_${safeId(scopeKind)}_${safeId(scopeRef)}`,
    projectId: options.projectId ?? `movscript_${safeId(scopeKind)}_${safeId(scopeRef)}`,
    title: options.title ?? `Timeline assembly ${scopeKind}:${scopeRef}`,
    source: {
      kind: "movscript_edit_plan",
      targetKind: target.targetKind,
      targetRef: target.targetRef,
      scopeKind: target.scopeKind,
      scopeRef: target.scopeRef,
      ...options.productionId !== void 0 ? { productionId: String(options.productionId) } : {},
      contentUnitIds: options.clips.map((clip) => String(clip.contentUnitId))
    },
    timeline: {
      version: 1,
      id: `timeline_${safeId(scopeKind)}_${safeId(scopeRef)}`,
      fps: options.fps ?? 30,
      width: options.width ?? 1920,
      height: options.height ?? 1080,
      background: options.background ?? "#000000",
      durationMs: cursorMs,
      tracks: [{
        id: "track_timeline_assembly_video_0",
        name: "timeline assembly video",
        type: "video",
        zIndex: 0,
        muted: false,
        locked: false,
        clips
      }],
      metadata: {
        targetKind: target.targetKind,
        targetRef: target.targetRef,
        scopeKind: target.scopeKind,
        scopeRef: target.scopeRef,
        legacyTargetKind: target.legacyTargetKind,
        legacyTargetRef: target.legacyTargetRef,
        ...options.scopePath ? { scopePath: options.scopePath } : {},
        ...options.productionPath ? { productionPath: options.productionPath } : {}
      }
    },
    assets: { assets },
    provenance: {
      targetKind: target.targetKind,
      targetRef: target.targetRef,
      scopeKind: target.scopeKind,
      scopeRef: target.scopeRef,
      legacyTargetKind: target.legacyTargetKind,
      legacyTargetRef: target.legacyTargetRef,
      ...options.scopePath ? { scopePath: options.scopePath } : {},
      ...options.productionPath ? { productionPath: options.productionPath } : {},
      selectedCandidateIds: options.clips.flatMap((clip) => clip.candidateId === void 0 ? [] : [String(clip.candidateId)]),
      inputResourceIds: options.clips.map((clip) => clip.resourceId)
    },
    createdAt: now,
    updatedAt: now,
    revision: 1
  };
}
function timelineAssemblyTarget(options) {
  const targetKind = options.targetKind ?? "timeline_assembly";
  const scopeKind = options.scopeKind;
  const scopeRef = String(options.scopeRef);
  const targetRef = options.targetRef ?? (targetKind === "timeline_assembly" ? `timeline_assembly:${scopeKind}:${scopeRef}` : scopeRef);
  return {
    targetKind,
    targetRef,
    ...scopeKind ? { scopeKind } : {},
    ...scopeRef ? { scopeRef } : {},
    ...options.legacyTargetKind && options.legacyTargetRef !== void 0 ? {
      legacyTargetKind: options.legacyTargetKind,
      legacyTargetRef: String(options.legacyTargetRef)
    } : {}
  };
}
function buildMediaAssetRegistryFromEditPlan(editPlan) {
  const assetsById = /* @__PURE__ */ new Map();
  for (const item of editPlan.tracks.flatMap((track) => track.items)) {
    if (item.resource_id === void 0) continue;
    const id = mediaAssetIdForItem(item);
    if (assetsById.has(id)) continue;
    assetsById.set(id, {
      id,
      sourceKind: "backend_resource",
      assetType: assetTypeForEditPlanItem(item),
      resourceId: item.resource_id,
      label: String(item.content_unit_id),
      metadata: movscriptItemMetadata(item)
    });
  }
  return { assets: [...assetsById.values()].sort((left, right) => left.id.localeCompare(right.id)) };
}
function buildMediaTimelineRecipeFromEditPlan(editPlan, assets = buildMediaAssetRegistryFromEditPlan(editPlan), options = {}) {
  const tracks = editPlan.tracks.flatMap(
    (track, index) => mediaTrackFromMovScriptTrack({
      track,
      assets,
      index,
      defaultDurationMs: options.defaultDurationMs ?? 4e3,
      includeMissingPlaceholders: options.includeMissingPlaceholders ?? true
    })
  );
  return {
    version: 1,
    id: `timeline_scene_moment_${String(editPlan.sceneMomentId)}`,
    fps: options.fps ?? 30,
    width: options.width ?? 1080,
    height: options.height ?? 1920,
    background: options.background ?? "#000000",
    durationMs: durationFromTracks(tracks),
    tracks,
    metadata: {
      targetRef: editPlan.target_ref,
      status: editPlan.status
    }
  };
}
function createMediaEditingProjectService(project, options) {
  return new MediaEditingProjectService(project, options);
}
function validateMediaEditingProjectTimeline(project) {
  const diagnostics = [];
  const assetIds = new Set(project.assets.assets.map((asset) => asset.id));
  const seenTrackIds = /* @__PURE__ */ new Set();
  const seenClipIds = /* @__PURE__ */ new Set();
  for (const track of project.timeline.tracks) {
    if (seenTrackIds.has(track.id)) {
      diagnostics.push(diagnostic("duplicate_track_id", "error", `Duplicate media track id: ${track.id}`, { trackId: track.id }));
    }
    seenTrackIds.add(track.id);
    const sortedClips = [...track.clips].sort((left, right) => left.timelineStartMs - right.timelineStartMs || left.id.localeCompare(right.id));
    for (const clip of track.clips) {
      if (seenClipIds.has(clip.id)) {
        diagnostics.push(diagnostic("duplicate_clip_id", "error", `Duplicate media clip id: ${clip.id}`, { trackId: track.id, clipId: clip.id }));
      }
      seenClipIds.add(clip.id);
      if (clip.durationMs <= 0) {
        diagnostics.push(diagnostic("invalid_duration", "error", `Media clip ${clip.id} has invalid duration.`, { trackId: track.id, clipId: clip.id }));
      }
      if (clip.timelineStartMs < 0) {
        diagnostics.push(diagnostic("invalid_timeline_start", "error", `Media clip ${clip.id} starts before the timeline.`, { trackId: track.id, clipId: clip.id }));
      }
      if (clip.sourceStartMs !== void 0 && clip.sourceEndMs !== void 0 && clip.sourceEndMs < clip.sourceStartMs) {
        diagnostics.push(diagnostic("invalid_source_range", "error", `Media clip ${clip.id} has an invalid source range.`, {
          trackId: track.id,
          clipId: clip.id,
          details: { sourceStartMs: clip.sourceStartMs, sourceEndMs: clip.sourceEndMs }
        }));
      }
      if (!clipFitsTrackType(track.type, clip.assetType)) {
        diagnostics.push(diagnostic("track_clip_type_mismatch", "error", `Media clip ${clip.id} cannot be placed on ${track.type} track ${track.id}.`, {
          trackId: track.id,
          clipId: clip.id,
          details: { trackType: track.type, clipAssetType: clip.assetType }
        }));
      }
      if (clip.asset && !assetIds.has(clip.asset.id)) {
        diagnostics.push(diagnostic("asset_not_registered", "error", `Media clip ${clip.id} references unregistered asset ${clip.asset.id}.`, {
          trackId: track.id,
          clipId: clip.id,
          assetId: clip.asset.id
        }));
      }
      if (clip.asset && clip.asset.assetType !== clip.assetType) {
        diagnostics.push(diagnostic("asset_type_mismatch", "error", `Media clip ${clip.id} asset type does not match its clip type.`, {
          trackId: track.id,
          clipId: clip.id,
          assetId: clip.asset.id,
          details: { clipAssetType: clip.assetType, assetType: clip.asset.assetType }
        }));
      }
      if (clip.assetType === "subtitle" && !clip.subtitle?.resourceId && !clip.asset?.resourceId && !clip.text?.content) {
        diagnostics.push(diagnostic("subtitle_reference_missing", "error", `Subtitle clip ${clip.id} has no subtitle file, text, or resource reference.`, {
          trackId: track.id,
          clipId: clip.id
        }));
      }
      if (clip.volume !== void 0 && clip.volume > 0 && clip.volume <= 2) {
        diagnostics.push(diagnostic("legacy_ratio_volume", "warning", `Media clip ${clip.id} uses legacy ratio volume; it will be interpreted as percent volume.`, {
          trackId: track.id,
          clipId: clip.id,
          details: { volume: clip.volume, normalizedVolume: normalizeMediaClipVolumePercent(clip.volume) }
        }));
      }
    }
    if (!trackAllowsOverlap(track.type)) {
      for (let index = 1; index < sortedClips.length; index += 1) {
        const previous = sortedClips[index - 1];
        const current = sortedClips[index];
        const previousEndMs = previous.timelineStartMs + previous.durationMs;
        if (previousEndMs > current.timelineStartMs) {
          diagnostics.push(diagnostic("clip_overlap", "error", `Media clip ${current.id} overlaps ${previous.id}.`, {
            trackId: track.id,
            clipId: current.id,
            previousClipId: previous.id,
            details: { overlapMs: previousEndMs - current.timelineStartMs }
          }));
        }
      }
    }
  }
  return diagnostics;
}
function normalizeMediaClipVolumePercent(volume) {
  if (volume === void 0 || !Number.isFinite(volume)) return void 0;
  const normalized = volume > 0 && volume <= 2 ? volume * 100 : volume;
  return Math.max(0, Math.min(200, normalized));
}
function mediaTrackFromMovScriptTrack(input) {
  const trackType = mediaTrackTypeForMovScriptTrack(input.track.type);
  if (!trackType) return [];
  let cursorMs = 0;
  const clips = input.track.items.slice().sort((left, right) => left.order - right.order).flatMap((item) => {
    if (!input.includeMissingPlaceholders && (item.selected !== true || item.resource_id === void 0)) return [];
    const clip = mediaClipFromMovScriptItem({
      item,
      trackType,
      assets: input.assets,
      fallbackStartMs: cursorMs,
      defaultDurationMs: input.defaultDurationMs
    });
    cursorMs = clip.timelineStartMs + clip.durationMs;
    return [clip];
  });
  return [{
    id: `track_${input.track.type}_${input.index}`,
    type: trackType,
    zIndex: input.index,
    name: input.track.type,
    muted: false,
    locked: false,
    clips
  }];
}
function mediaClipFromMovScriptItem(input) {
  const timing = input.item.timing_intent ?? {};
  const sourceStartMs = secField(timing.trim_start_sec) ?? secField(timing.in_sec) ?? secField(timing.start_sec) ?? 0;
  const durationMs = secField(timing.duration_sec) ?? durationFromInOutMs(timing) ?? input.defaultDurationMs;
  const timelineStartMs = secField(timing.timeline_start_sec) ?? secField(timing.start_time_sec) ?? input.fallbackStartMs;
  const asset = input.item.resource_id === void 0 ? void 0 : input.assets.assets.find((candidate) => candidate.id === mediaAssetIdForItem(input.item));
  const base = {
    id: input.item.id,
    assetType: assetTypeForEditPlanItem(input.item),
    asset,
    timelineStartMs,
    durationMs,
    sourceStartMs,
    sourceEndMs: sourceStartMs + durationMs,
    volume: normalizeMediaClipVolumePercent(numberField(timing.volume)) ?? 100,
    muted: false,
    fit: "cover",
    opacity: numberField(timing.opacity) ?? 1,
    metadata: movscriptItemMetadata(input.item)
  };
  if (base.assetType === "text" || input.trackType === "subtitle") {
    return {
      ...base,
      assetType: "text",
      text: {
        content: stringField(timing.text) ?? String(input.item.content_unit_id),
        fontSize: numberField(timing.font_size) ?? 42,
        fontFamily: stringField(timing.font_family) ?? "Inter",
        color: stringField(timing.color) ?? "#ffffff",
        backgroundColor: stringField(timing.background_color),
        align: "center",
        position: stringField(timing.position) ?? "bottom_center"
      }
    };
  }
  return base;
}
function mediaTrackTypeForMovScriptTrack(type) {
  switch (type) {
    case "video":
      return "video";
    case "image":
      return "image";
    case "voice":
    case "audio":
      return "audio";
    case "subtitle":
      return "subtitle";
    case "metadata":
      return void 0;
  }
}
function assetTypeForEditPlanItem(item) {
  switch (item.output_kind) {
    case "video":
      return "video";
    case "image":
      return "image";
    case "audio":
      return "audio";
    case "text":
      return "text";
    case "metadata":
      return "text";
  }
}
function assertClipFitsTrack(track, clip) {
  if (clipFitsTrackType(track.type, clip.assetType)) return;
  throw new Error(`Media clip type ${clip.assetType} cannot be placed on ${track.type} track ${track.id}`);
}
function clipFitsTrackType(trackType, assetType) {
  if (trackType === "video") return assetType === "video" || assetType === "image";
  if (trackType === "image") return assetType === "image";
  if (trackType === "audio") return assetType === "audio";
  if (trackType === "text") return assetType === "text" || assetType === "subtitle";
  if (trackType === "subtitle") return assetType === "subtitle" || assetType === "text";
  if (trackType === "effect") return true;
  return false;
}
function trackAllowsOverlap(trackType) {
  return trackType === "effect" || trackType === "text" || trackType === "subtitle";
}
function diagnostic(code, severity, message, input = {}) {
  return {
    code,
    severity,
    message,
    ...input.trackId ? { trackId: input.trackId, track_id: input.trackId } : {},
    ...input.clipId ? { clipId: input.clipId, clip_id: input.clipId } : {},
    ...input.previousClipId ? { previousClipId: input.previousClipId, previous_clip_id: input.previousClipId } : {},
    ...input.assetId ? { assetId: input.assetId, asset_id: input.assetId } : {},
    ...input.details ? { details: input.details } : {}
  };
}
function mediaAssetIdForItem(item) {
  return `resource_${item.resource_id}`;
}
function movscriptItemMetadata(item) {
  return {
    contentUnitId: item.content_unit_id,
    contentUnitRef: item.content_unit_ref,
    candidateId: item.candidate_id,
    resourceId: item.resource_id,
    outputKind: item.output_kind,
    targetKind: item.target_kind,
    targetRef: item.target_ref,
    expressionUnitRef: item.expression_unit_ref,
    expressionModality: item.expression_modality,
    expressionRole: item.expression_role,
    selected: item.selected,
    stale: item.stale
  };
}
function durationFromTracks(tracks) {
  return Math.max(
    0,
    ...tracks.flatMap((track) => track.clips.map((clip) => clip.timelineStartMs + clip.durationMs))
  );
}
function durationFromInOutMs(timing) {
  const startMs = secField(timing.in_sec) ?? secField(timing.start_sec);
  const endMs = secField(timing.out_sec) ?? secField(timing.end_sec);
  if (startMs === void 0 || endMs === void 0 || endMs <= startMs) return void 0;
  return endMs - startMs;
}
function secField(value) {
  const seconds = numberField(value);
  return seconds === void 0 ? void 0 : Math.round(seconds * 1e3);
}
function numberField(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function stringField(value) {
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim();
  return trimmed ? trimmed : void 0;
}
function safeId(value) {
  return value.replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "item";
}
function sortClips(track) {
  track.clips.sort((left, right) => {
    if (left.timelineStartMs !== right.timelineStartMs) return left.timelineStartMs - right.timelineStartMs;
    return left.id.localeCompare(right.id);
  });
}
function clone(value) {
  return structuredClone(value);
}
function assertNever(value) {
  throw new Error(`Unexpected media editing value: ${JSON.stringify(value)}`);
}
function createMediaEditingProjectFromEditDecisions(editDecisions, options = {}) {
  const now = options.now ?? (/* @__PURE__ */ new Date()).toISOString();
  const projectId = options.projectId ?? composeProjectId(options, editDecisions);
  const assets = new ComposeAssetRegistry(options.assetManifest);
  const timeline = buildMediaTimelineRecipeFromEditDecisions(editDecisions, assets, options);
  const inputResourceIds = [...new Set(assets.assets.flatMap((asset) => asset.resourceId === void 0 ? [] : [asset.resourceId]))].sort((left, right) => left - right);
  return {
    version: 1,
    id: options.id ?? `editing_project_compose_${safeId2(options.scopeRef ?? options.targetRef ?? options.productionId ?? "draft")}`,
    projectId,
    title: options.title ?? `Video compose ${String(options.targetRef ?? options.scopeRef ?? options.productionId ?? "draft")}`,
    source: {
      kind: "edit_decisions",
      targetKind: options.targetKind ?? "timeline_assembly",
      targetRef: options.targetRef ?? `edit_decisions:${safeId2(options.scopeKind ?? "project")}:${safeId2(options.scopeRef ?? projectId)}`,
      scopeKind: options.scopeKind,
      scopeRef: options.scopeRef === void 0 ? void 0 : String(options.scopeRef),
      ...options.productionId !== void 0 ? { productionId: String(options.productionId) } : {}
    },
    timeline,
    assets: { assets: assets.assets },
    provenance: {
      sourceHash: options.sourceHash,
      inputResourceIds,
      targetKind: options.targetKind ?? "timeline_assembly",
      targetRef: options.targetRef,
      scopeKind: options.scopeKind,
      scopeRef: options.scopeRef === void 0 ? void 0 : String(options.scopeRef),
      productionPath: options.productionPath
    },
    createdAt: now,
    updatedAt: now,
    revision: 1
  };
}
function buildMediaTimelineRecipeFromEditDecisions(editDecisions, assets, options) {
  const defaultDurationMs = options.defaultDurationMs ?? 4e3;
  const tracks = new TrackRegistry();
  let primaryCursorMs = 0;
  for (const [index, cut] of arrayValue(editDecisions.cuts).entries()) {
    const layer = stringField2(cut.layer) ?? "primary";
    const track = videoTrackForLayer(tracks, layer);
    const asset = assets.resolve(assetRef(cut), inferAssetTypeFromRecord(cut, "video"));
    const sourceStartMs = secondsToMs(cut.source_in_seconds ?? cut.source_in_sec ?? cut.source_start_seconds ?? cut.source_start_sec ?? cut.in_seconds ?? cut.in_sec) ?? 0;
    const sourceEndMs = secondsToMs(cut.source_out_seconds ?? cut.source_out_sec ?? cut.source_end_seconds ?? cut.source_end_sec ?? cut.out_seconds ?? cut.out_sec);
    const durationMs = secondsToMs(cut.duration_seconds ?? cut.duration_sec) ?? durationFromRange(sourceStartMs, sourceEndMs) ?? defaultDurationMs;
    const explicitTimelineStartMs = secondsToMs(cut.timeline_start_seconds ?? cut.timeline_start_sec ?? cut.start_seconds ?? cut.start_sec);
    const timelineStartMs = explicitTimelineStartMs ?? (layer === "primary" ? primaryCursorMs : 0);
    const transform = recordValue(cut.transform);
    const clip = {
      id: stringField2(cut.id) ?? `cut_${index + 1}`,
      assetType: asset?.assetType ?? inferAssetTypeFromRecord(cut, "video"),
      asset,
      timelineStartMs,
      durationMs,
      sourceStartMs,
      sourceEndMs: sourceEndMs ?? sourceStartMs + durationMs,
      speed: numberField2(cut.speed),
      volume: normalizeMediaClipVolumePercent(numberField2(cut.volume)) ?? 100,
      muted: false,
      fit: "cover",
      opacity: numberField2(transform?.opacity ?? cut.opacity) ?? 1,
      position: stringField2(transform?.position ?? cut.position),
      xPercent: numberField2(transform?.x_percent ?? transform?.xPercent ?? cut.x_percent ?? cut.xPercent),
      yPercent: numberField2(transform?.y_percent ?? transform?.yPercent ?? cut.y_percent ?? cut.yPercent),
      scale: numberField2(transform?.scale ?? cut.scale),
      crop: cropSpec(transform?.crop ?? cut.crop),
      ...transitionFields(cut),
      metadata: {
        movscript: {
          kind: "edit_decision_cut",
          layer,
          reason: stringField2(cut.reason),
          source: assetRef(cut),
          renderRuntime: stringField2(editDecisions.render_runtime ?? editDecisions.renderRuntime)
        },
        ...cut.metadata ?? {}
      }
    };
    track.clips.push(clip);
    if (layer === "primary" && explicitTimelineStartMs === void 0) primaryCursorMs = timelineStartMs + durationMs;
  }
  const primaryDurationMs = Math.max(primaryCursorMs, durationFromTracks2(tracks.all()));
  addOverlayTracks(editDecisions, tracks, assets, primaryDurationMs, defaultDurationMs);
  addAudioTracks(editDecisions, tracks, assets, primaryDurationMs, defaultDurationMs);
  addSubtitleTracks(editDecisions, tracks, assets, primaryDurationMs, defaultDurationMs);
  const finalTracks = tracks.all();
  return {
    version: 1,
    id: options.id ? `timeline_${safeId2(options.id)}` : `timeline_compose_${safeId2(options.scopeRef ?? options.targetRef ?? options.productionId ?? "draft")}`,
    fps: options.fps ?? 30,
    width: options.width ?? 1920,
    height: options.height ?? 1080,
    background: options.background ?? "#000000",
    durationMs: durationFromTracks2(finalTracks),
    tracks: finalTracks,
    metadata: {
      schema: "movscript.video_compose_timeline.v1",
      rendererFamily: stringField2(editDecisions.renderer_family ?? editDecisions.rendererFamily),
      renderRuntime: stringField2(editDecisions.render_runtime ?? editDecisions.renderRuntime),
      compositionMode: stringField2(editDecisions.composition_mode ?? editDecisions.compositionMode),
      targetKind: options.targetKind,
      targetRef: options.targetRef,
      scopeKind: options.scopeKind,
      scopeRef: options.scopeRef,
      ...editDecisions.metadata ? { editDecisionMetadata: editDecisions.metadata } : {}
    }
  };
}
function addOverlayTracks(editDecisions, tracks, assets, timelineDurationMs, defaultDurationMs) {
  const overlayTrack = tracks.track("track_overlay_visual", "video", 20, "overlay");
  for (const [index, overlay] of arrayValue(editDecisions.overlays).entries()) {
    const asset = assets.resolve(assetRef(overlay), inferAssetTypeFromRecord(overlay, "image"));
    const startMs = secondsToMs(overlay.start_seconds ?? overlay.start_sec) ?? 0;
    const durationMs = secondsToMs(overlay.duration_seconds ?? overlay.duration_sec) ?? durationFromRange(startMs, secondsToMs(overlay.end_seconds ?? overlay.end_sec)) ?? Math.max(defaultDurationMs, timelineDurationMs - startMs);
    overlayTrack.clips.push({
      id: stringField2(overlay.id) ?? `overlay_${index + 1}`,
      assetType: asset?.assetType ?? inferAssetTypeFromRecord(overlay, "image"),
      asset,
      timelineStartMs: startMs,
      durationMs,
      sourceStartMs: secondsToMs(overlay.source_start_seconds ?? overlay.source_start_sec) ?? 0,
      sourceEndMs: secondsToMs(overlay.source_end_seconds ?? overlay.source_end_sec),
      fit: "contain",
      opacity: numberField2(overlay.opacity) ?? 1,
      position: stringField2(overlay.position),
      xPercent: numberField2(overlay.x_percent ?? overlay.xPercent),
      yPercent: numberField2(overlay.y_percent ?? overlay.yPercent),
      scale: numberField2(overlay.scale),
      metadata: {
        movscript: {
          kind: "edit_decision_overlay",
          source: assetRef(overlay),
          animation: overlay.animation
        },
        ...overlay.metadata ?? {}
      }
    });
  }
}
function addAudioTracks(editDecisions, tracks, assets, timelineDurationMs, defaultDurationMs) {
  const audio = recordValue(editDecisions.audio);
  if (!audio) return;
  const narration = recordValue(audio.narration);
  const narrationSegments = Array.isArray(audio.narration) ? audio.narration : Array.isArray(narration?.segments) ? narration.segments : [];
  addAudioSegments(tracks.track("track_audio_narration", "audio", 30, "narration"), narrationSegments, assets, "narration", defaultDurationMs);
  const music = recordValue(audio.music);
  if (music) {
    const track = tracks.track("track_audio_music", "audio", 10, "music");
    const asset = assets.resolve(assetRef(music), "audio");
    const startMs = secondsToMs(music.start_seconds ?? music.start_sec) ?? 0;
    const durationMs = secondsToMs(music.duration_seconds ?? music.duration_sec) ?? durationFromRange(startMs, secondsToMs(music.end_seconds ?? music.end_sec)) ?? Math.max(defaultDurationMs, timelineDurationMs - startMs);
    track.clips.push(audioClipFromLayer({
      layer: music,
      id: "music_bed",
      kind: "music",
      asset,
      startMs,
      durationMs
    }));
  }
  addAudioSegments(
    tracks.track("track_audio_sfx", "audio", 40, "sfx"),
    arrayValue(audio.sfx),
    assets,
    "sfx",
    defaultDurationMs
  );
}
function addAudioSegments(track, segments, assets, kind, defaultDurationMs) {
  for (const [index, segment] of segments.entries()) {
    const record = recordValue(segment);
    if (!record) continue;
    const asset = assets.resolve(assetRef(record), "audio");
    const startMs = secondsToMs(record.start_seconds ?? record.start_sec) ?? 0;
    const durationMs = secondsToMs(record.duration_seconds ?? record.duration_sec) ?? durationFromRange(startMs, secondsToMs(record.end_seconds ?? record.end_sec)) ?? defaultDurationMs;
    track.clips.push(audioClipFromLayer({
      layer: record,
      id: stringField2(record.id) ?? `${kind}_${index + 1}`,
      kind,
      asset,
      startMs,
      durationMs
    }));
  }
}
function audioClipFromLayer(input) {
  const sourceStartMs = secondsToMs(input.layer.source_start_seconds ?? input.layer.source_start_sec) ?? 0;
  return {
    id: input.id,
    assetType: "audio",
    asset: input.asset,
    timelineStartMs: input.startMs,
    durationMs: input.durationMs,
    sourceStartMs,
    sourceEndMs: secondsToMs(input.layer.source_end_seconds ?? input.layer.source_end_sec) ?? sourceStartMs + input.durationMs,
    volume: normalizeMediaClipVolumePercent(numberField2(input.layer.volume)) ?? 100,
    fadeInMs: secondsToMs(input.layer.fade_in_seconds ?? input.layer.fade_in ?? input.layer.fadeIn),
    fadeOutMs: secondsToMs(input.layer.fade_out_seconds ?? input.layer.fade_out ?? input.layer.fadeOut),
    muted: false,
    metadata: {
      movscript: {
        kind: `edit_decision_${input.kind}`,
        source: assetRef(input.layer),
        ducking: input.layer.ducking
      },
      ...recordValue(input.layer.metadata) ?? {}
    }
  };
}
function addSubtitleTracks(editDecisions, tracks, assets, timelineDurationMs, defaultDurationMs) {
  const subtitles = recordValue(editDecisions.subtitles);
  if (!subtitles || subtitles.enabled === false) return;
  const track = tracks.track("track_subtitles", "subtitle", 50, "subtitles");
  const segments = arrayValue(subtitles.segments).length ? arrayValue(subtitles.segments) : arrayValue(subtitles.captions);
  const style = subtitleTextStyle(subtitles);
  for (const [index, item] of segments.entries()) {
    const segment = recordValue(item);
    if (!segment) continue;
    const startMs = secondsToMs(segment.start_seconds ?? segment.start_sec) ?? 0;
    const durationMs = secondsToMs(segment.duration_seconds ?? segment.duration_sec) ?? durationFromRange(startMs, secondsToMs(segment.end_seconds ?? segment.end_sec)) ?? defaultDurationMs;
    track.clips.push({
      id: stringField2(segment.id) ?? `subtitle_${index + 1}`,
      assetType: "text",
      timelineStartMs: startMs,
      durationMs,
      text: {
        ...style,
        content: stringField2(segment.text ?? segment.content) ?? ""
      },
      subtitle: {
        burnIn: true,
        style
      },
      metadata: {
        movscript: {
          kind: "edit_decision_subtitle_segment"
        }
      }
    });
  }
  const sourceRef = assetRef(subtitles);
  if (sourceRef !== void 0 && !isGeneratedSubtitleSource(sourceRef)) {
    const asset = assets.resolve(sourceRef, "subtitle");
    track.clips.push({
      id: "subtitle_file",
      assetType: "subtitle",
      asset,
      timelineStartMs: 0,
      durationMs: Math.max(defaultDurationMs, timelineDurationMs),
      subtitle: {
        resourceId: asset?.resourceId,
        format: subtitleFormat(subtitles),
        burnIn: true,
        renderer: subtitleRenderer(subtitles),
        style
      },
      metadata: {
        movscript: {
          kind: "edit_decision_subtitle_file",
          source: sourceRef
        }
      }
    });
  }
}
var TrackRegistry = class {
  tracks = /* @__PURE__ */ new Map();
  track(id, type, zIndex, name) {
    const existing = this.tracks.get(id);
    if (existing) return existing;
    const track = {
      id,
      type,
      zIndex,
      name,
      muted: false,
      locked: false,
      clips: []
    };
    this.tracks.set(id, track);
    return track;
  }
  all() {
    return [...this.tracks.values()].map((track) => ({
      ...track,
      clips: [...track.clips].sort((left, right) => left.timelineStartMs - right.timelineStartMs || left.id.localeCompare(right.id))
    })).filter((track) => track.clips.length > 0).sort((left, right) => left.zIndex - right.zIndex || left.id.localeCompare(right.id));
  }
};
var ComposeAssetRegistry = class {
  byRef = /* @__PURE__ */ new Map();
  byId = /* @__PURE__ */ new Map();
  constructor(manifest) {
    for (const entry of arrayValue(manifest?.assets)) {
      const asset = this.assetFromRecord(entry);
      if (!asset) continue;
      this.register(asset, assetRefsFromRecord(entry));
    }
  }
  get assets() {
    return [...this.byId.values()].sort((left, right) => left.id.localeCompare(right.id));
  }
  resolve(ref, preferredType) {
    const key = refKey(ref);
    if (key && this.byRef.has(key)) return this.byRef.get(key);
    const record = recordValue(ref);
    const asset = record ? this.assetFromRecord(record, preferredType) : this.assetFromPrimitive(ref, preferredType);
    if (!asset) return void 0;
    return this.register(asset, key ? [key] : []);
  }
  register(asset, refs) {
    let next = asset;
    let index = 1;
    while (this.byId.has(next.id)) {
      const existing = this.byId.get(next.id);
      if (sameAsset(existing, next)) {
        next = existing;
        break;
      }
      index += 1;
      next = { ...asset, id: `${asset.id}_${index}` };
    }
    this.byId.set(next.id, next);
    for (const ref of refs) this.byRef.set(ref, next);
    this.byRef.set(next.id, next);
    if (next.resourceId !== void 0) this.byRef.set(String(next.resourceId), next);
    if (next.localPath) this.byRef.set(next.localPath, next);
    return next;
  }
  assetFromRecord(record, preferredType = "video") {
    const rawPath = stringField2(record.localPath ?? record.local_path ?? record.path ?? record.file_path ?? record.filePath ?? record.src);
    const resourceId = integerField(record.resourceId ?? record.resource_id) ?? resourceIdFromString(rawPath ?? "");
    const localPath = localPathField(rawPath);
    const idSource = stringField2(record.mediaAssetId ?? record.media_asset_id ?? record.id ?? record.asset_id ?? record.assetId) ?? (resourceId !== void 0 ? `resource_${resourceId}` : void 0) ?? localPath;
    if (!idSource && resourceId === void 0 && !localPath) return void 0;
    const assetType = inferAssetTypeFromRecord(record, preferredType);
    return {
      id: `asset_${safeId2(idSource ?? `${assetType}_${this.byId.size + 1}`)}`,
      sourceKind: resourceId !== void 0 ? "backend_resource" : "local_file",
      assetType,
      ...resourceId !== void 0 ? { resourceId } : {},
      ...localPath ? { localPath } : {},
      ...stringField2(record.mimeType ?? record.mime_type) ? { mimeType: stringField2(record.mimeType ?? record.mime_type) } : {},
      ...stringField2(record.checksum) ? { checksum: stringField2(record.checksum) } : {},
      label: stringField2(record.label ?? record.title ?? record.name ?? record.id ?? record.asset_id ?? localPath),
      metadata: {
        movscript: {
          kind: "edit_decision_asset_manifest_entry",
          manifest: record
        }
      }
    };
  }
  assetFromPrimitive(ref, preferredType) {
    const resourceId = integerField(ref);
    if (resourceId !== void 0) {
      return {
        id: `asset_resource_${resourceId}`,
        sourceKind: "backend_resource",
        assetType: preferredType,
        resourceId,
        label: `resource ${resourceId}`
      };
    }
    const value = stringField2(ref);
    if (!value) return void 0;
    const parsedResourceId = resourceIdFromString(value);
    if (parsedResourceId !== void 0) {
      return {
        id: `asset_resource_${parsedResourceId}`,
        sourceKind: "backend_resource",
        assetType: preferredType,
        resourceId: parsedResourceId,
        label: value
      };
    }
    return {
      id: `asset_${safeId2(value)}`,
      sourceKind: "local_file",
      assetType: assetTypeFromPath(value) ?? preferredType,
      localPath: value,
      label: value.split("/").pop() ?? value,
      metadata: {
        movscript: {
          kind: "edit_decision_inline_asset_ref",
          ref: value
        }
      }
    };
  }
};
function videoTrackForLayer(tracks, layer) {
  if (layer === "background") return tracks.track("track_background_video", "video", -10, "background");
  if (layer === "overlay") return tracks.track("track_overlay_video", "video", 20, "overlay");
  return tracks.track("track_primary_video", "video", 0, "primary");
}
function transitionFields(record) {
  const transitionIn = stringField2(record.transition_in ?? record.transitionIn);
  const transitionOut = stringField2(record.transition_out ?? record.transitionOut);
  const transition = stringField2(record.transition);
  const durationMs = secondsToMs(record.transition_duration ?? record.transitionDuration);
  return {
    ...transitionIn === "fade" ? { fadeInMs: durationMs } : {},
    ...transitionOut === "fade" ? { fadeOutMs: durationMs } : {},
    ...transition || transitionIn || transitionOut ? { transition: { type: transition ?? transitionIn ?? transitionOut ?? "fade", durationMs } } : {}
  };
}
function cropSpec(value) {
  const crop = recordValue(value);
  if (!crop) return void 0;
  return {
    topPercent: numberField2(crop.topPercent ?? crop.top_percent ?? crop.top),
    rightPercent: numberField2(crop.rightPercent ?? crop.right_percent ?? crop.right),
    bottomPercent: numberField2(crop.bottomPercent ?? crop.bottom_percent ?? crop.bottom),
    leftPercent: numberField2(crop.leftPercent ?? crop.left_percent ?? crop.left)
  };
}
function subtitleTextStyle(subtitles) {
  const style = recordValue(subtitles.style) ?? {};
  return {
    content: "",
    fontSize: numberField2(style.fontSize ?? style.font_size ?? subtitles.fontSize ?? subtitles.font_size) ?? 42,
    fontFamily: stringField2(style.fontFamily ?? style.font_family ?? subtitles.font) ?? "Inter",
    color: stringField2(style.color ?? style.text_color ?? subtitles.color ?? subtitles.text_color) ?? "#ffffff",
    backgroundColor: stringField2(style.backgroundColor ?? style.background_color ?? subtitles.background_color ?? subtitles.background),
    align: textAlignValue(style.align ?? subtitles.align) ?? "center",
    position: stringField2(style.position ?? subtitles.position) ?? "bottom"
  };
}
function subtitleFormat(subtitles) {
  const format = stringField2(subtitles.format)?.toLowerCase();
  if (format === "srt" || format === "vtt" || format === "ass" || format === "ssa") return format;
  return void 0;
}
function subtitleRenderer(subtitles) {
  const renderer = stringField2(subtitles.renderer)?.toLowerCase();
  if (renderer === "drawtext" || renderer === "ass" || renderer === "libass") return renderer;
  return void 0;
}
function textAlignValue(value) {
  const normalized = stringField2(value)?.toLowerCase();
  if (normalized === "left" || normalized === "center" || normalized === "right") return normalized;
  return void 0;
}
function assetRef(record) {
  return record.source ?? record.asset_id ?? record.assetId ?? record.resource_id ?? record.resourceId;
}
function assetRefsFromRecord(record) {
  return [
    record.id,
    record.asset_id,
    record.assetId,
    record.ref,
    record.name,
    record.path,
    record.localPath,
    record.local_path,
    record.file_path,
    record.filePath,
    record.resource_id,
    record.resourceId
  ].flatMap((value) => {
    const key = refKey(value);
    return key ? [key] : [];
  });
}
function inferAssetTypeFromRecord(record, fallback) {
  const declared = stringField2(record.assetType ?? record.asset_type ?? record.type ?? record.kind ?? record.output_kind ?? record.media_type)?.toLowerCase();
  if (declared?.includes("subtitle")) return "subtitle";
  if (declared?.includes("audio") || declared?.includes("music") || declared?.includes("voice") || declared?.includes("sfx")) return "audio";
  if (declared?.includes("image") || declared?.includes("photo")) return "image";
  if (declared?.includes("text") || declared?.includes("caption")) return "text";
  if (declared?.includes("video") || declared?.includes("clip")) return "video";
  const mime = stringField2(record.mimeType ?? record.mime_type)?.toLowerCase();
  if (mime?.startsWith("audio/")) return "audio";
  if (mime?.startsWith("image/")) return "image";
  if (mime?.startsWith("text/") || mime?.includes("subrip") || mime?.includes("vtt")) return "subtitle";
  const path = localPathField(stringField2(record.localPath ?? record.local_path ?? record.path ?? record.file_path ?? record.filePath ?? record.src));
  return assetTypeFromPath(path) ?? fallback;
}
function assetTypeFromPath(path) {
  const normalized = path?.split("?")[0]?.toLowerCase();
  if (!normalized) return void 0;
  if (/\.(wav|mp3|m4a|aac|flac|ogg)$/.test(normalized)) return "audio";
  if (/\.(png|jpg|jpeg|webp|gif|avif)$/.test(normalized)) return "image";
  if (/\.(srt|vtt|ass|ssa)$/.test(normalized)) return "subtitle";
  if (/\.(txt|md)$/.test(normalized)) return "text";
  if (/\.(mp4|mov|m4v|webm|mkv|avi)$/.test(normalized)) return "video";
  return void 0;
}
function composeProjectId(options, editDecisions) {
  if (options.productionId !== void 0) return `movscript_production_${safeId2(options.productionId)}`;
  if (options.scopeKind && options.scopeRef !== void 0) return `movscript_${safeId2(options.scopeKind)}_${safeId2(options.scopeRef)}`;
  return `movscript_video_compose_${safeId2(editDecisions.version ?? "draft")}`;
}
function durationFromTracks2(tracks) {
  return Math.max(0, ...tracks.flatMap((track) => track.clips.map((clip) => clip.timelineStartMs + clip.durationMs)));
}
function durationFromRange(startMs, endMs) {
  if (startMs === void 0 || endMs === void 0 || endMs <= startMs) return void 0;
  return endMs - startMs;
}
function secondsToMs(value) {
  const seconds = numberField2(value);
  return seconds === void 0 ? void 0 : Math.max(0, Math.round(seconds * 1e3));
}
function numberField2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function integerField(value) {
  const number = numberField2(value);
  if (number === void 0 || !Number.isInteger(number) || number <= 0) return void 0;
  return number;
}
function resourceIdFromString(value) {
  const match = value.match(/^(?:resource|raw_resource|backend_resource)[:_](\d+)$/i);
  if (!match) return void 0;
  return integerField(match[1]);
}
function stringField2(value) {
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim();
  return trimmed ? trimmed : void 0;
}
function localPathField(value) {
  if (!value) return void 0;
  if (/^(?:resource|raw_resource|backend_resource|content-unit|content_unit):/i.test(value)) return void 0;
  if (/^(?:https?:|blob:|data:)/i.test(value)) return void 0;
  return value;
}
function recordValue(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return void 0;
  return value;
}
function arrayValue(value) {
  return Array.isArray(value) ? value : [];
}
function refKey(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return stringField2(value);
}
function safeId2(value) {
  const raw = String(value ?? "draft").trim() || "draft";
  return raw.replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "draft";
}
function sameAsset(left, right) {
  if (!left) return false;
  if (left.resourceId !== void 0 && right.resourceId !== void 0) return left.resourceId === right.resourceId;
  if (left.localPath && right.localPath) return left.localPath === right.localPath;
  return left.id === right.id;
}
function isGeneratedSubtitleSource(value) {
  const normalized = stringField2(value)?.toLowerCase();
  return normalized === "script" || normalized === "auto" || normalized === "captions" || normalized === "segments";
}
var MEDIA_EDITING_RUNTIME_ALLOW_LIST = /* @__PURE__ */ new Set(["movscript_media_pipeline", "ffmpeg"]);
var BACKEND_RUNTIME_LOCKS = {
  hyperframes: "hyperframes",
  remotion: "remotion"
};
var IMPLEMENTED_BACKENDS = /* @__PURE__ */ new Set(["media_editing_project", "hyperframes", "remotion"]);
function createTimelineAssemblyCompileManifest(input) {
  const backend = input.backend ?? "media_editing_project";
  const renderRuntime = input.renderRuntime ?? renderRuntimeFromEditDecisions(input.editDecisions);
  const runtimeLocked = input.runtimeLocked ?? renderRuntime !== void 0;
  const renderSettings = normalizeRenderSettings(input);
  const timelineAssembly = input.timelineAssembly ?? {};
  const target = targetFromTimelineAssembly(timelineAssembly, input.editDecisions);
  const assetIndex = assetManifestIndex(input.assetManifest);
  const actionSummary = summarizeEditDecisionActions(input.editDecisions);
  const referencedAssets = collectReferencedAssets(input.editDecisions);
  const unresolvedRefs = referencedAssets.filter((entry) => !assetIndex.resolvableRefs.has(entry.ref)).map((entry) => entry.ref);
  const diagnostics = [
    ...capabilityDiagnostics({
      backend,
      renderRuntime,
      runtimeLocked,
      actionCounts: actionSummary.actionCounts,
      editDecisions: input.editDecisions
    }),
    ...unresolvedRefs.map((ref) => ({
      code: "asset_ref_unresolved",
      severity: "error",
      message: `TimelineAssembly compile cannot resolve asset ref ${ref}.`,
      asset_ref: ref
    }))
  ];
  const timelineAssemblyHash = stableHash(timelineAssembly);
  const assetManifestHash = stableHash(input.assetManifest ?? {});
  const editDecisionsHash = stableHash(input.editDecisions);
  const inputHash = stableHash({
    timelineAssemblyHash,
    assetManifestHash,
    editDecisionsHash,
    backend,
    renderRuntime,
    runtimeLocked,
    renderSettings
  });
  const status = diagnostics.some((diagnostic2) => diagnostic2.severity === "error") ? "blocked" : "ready";
  return {
    schema: "movscript.timeline_assembly.compile_manifest.v1",
    id: `compile_${safeId3(target.timelineAssemblyId ?? target.targetRef ?? "timeline_assembly")}_${inputHash.slice(0, 12)}`,
    status,
    created_at: input.now ?? (/* @__PURE__ */ new Date()).toISOString(),
    ...target.timelineAssemblyId ? { timeline_assembly_id: target.timelineAssemblyId } : {},
    ...target.targetKind ? { target_kind: target.targetKind } : {},
    ...target.targetRef ? { target_ref: target.targetRef } : {},
    ...target.scopeKind ? { scope_kind: target.scopeKind } : {},
    ...target.scopeRef ? { scope_ref: target.scopeRef } : {},
    backend: {
      target: backend,
      adapter: backend === "media_editing_project" ? "@movscript/editing:createMediaEditingProjectFromEditDecisions" : `@movscript/editing:${backend}`,
      adapter_version: input.adapterVersion ?? "0.1.0",
      ...renderRuntime ? { render_runtime: renderRuntime } : {},
      runtime_locked: runtimeLocked,
      fallback_policy: "no_implicit_fallback"
    },
    input_hash: inputHash,
    inputs: {
      timeline_assembly_hash: timelineAssemblyHash,
      asset_manifest_hash: assetManifestHash,
      edit_decisions_hash: editDecisionsHash,
      selected_resource_ids: assetIndex.selectedResourceIds,
      selected_candidate_ids: assetIndex.selectedCandidateIds,
      referenced_asset_refs: [...new Set(referencedAssets.map((entry) => entry.ref))].sort(),
      unresolved_asset_refs: [...new Set(unresolvedRefs)].sort()
    },
    render_settings: renderSettings,
    capabilities: {
      supported_actions: actionSummary.supportedActions,
      unsupported_actions: diagnostics.filter((diagnostic2) => diagnostic2.code === "backend_adapter_not_implemented" || diagnostic2.code === "runtime_lock_backend_mismatch").flatMap((diagnostic2) => stringValue2(diagnostic2.action) ? [String(diagnostic2.action)] : []).sort(),
      action_counts: actionSummary.actionCounts
    },
    diagnostics
  };
}
function compileTimelineAssemblyToMediaEditingProject(input) {
  const compileManifest = createTimelineAssemblyCompileManifest({
    ...input,
    backend: "media_editing_project"
  });
  if (compileManifest.status === "blocked") {
    return {
      schema: "movscript.timeline_assembly.media_editing_compile_result.v1",
      status: "blocked",
      compile_manifest: compileManifest,
      editing_timeline_diagnostics: [],
      diagnostics: compileManifest.diagnostics
    };
  }
  const mediaEditingProject = createMediaEditingProjectFromEditDecisions(input.editDecisions, {
    ...input.projectOptions ?? {},
    assetManifest: input.assetManifest,
    sourceHash: compileManifest.input_hash,
    width: input.projectOptions?.width ?? input.renderSettings?.width,
    height: input.projectOptions?.height ?? input.renderSettings?.height,
    fps: input.projectOptions?.fps ?? input.renderSettings?.fps,
    background: input.projectOptions?.background ?? input.renderSettings?.background,
    defaultDurationMs: input.projectOptions?.defaultDurationMs ?? input.renderSettings?.default_duration_ms
  });
  mediaEditingProject.timeline.metadata = {
    ...mediaEditingProject.timeline.metadata ?? {},
    compileManifestId: compileManifest.id,
    compileManifestHash: compileManifest.input_hash,
    backendTarget: compileManifest.backend.target,
    runtimeLocked: compileManifest.backend.runtime_locked
  };
  const editingTimelineDiagnostics = validateMediaEditingProjectTimeline(mediaEditingProject);
  const diagnostics = [
    ...compileManifest.diagnostics,
    ...editingTimelineDiagnostics.map((diagnostic2) => ({
      code: `media_timeline_${diagnostic2.code}`,
      severity: diagnostic2.severity,
      message: diagnostic2.message,
      details: compactRecord2({
        track_id: diagnostic2.track_id ?? diagnostic2.trackId,
        clip_id: diagnostic2.clip_id ?? diagnostic2.clipId,
        asset_id: diagnostic2.asset_id ?? diagnostic2.assetId,
        previous_clip_id: diagnostic2.previous_clip_id ?? diagnostic2.previousClipId,
        ...diagnostic2.details ? { details: diagnostic2.details } : {}
      })
    }))
  ];
  const status = diagnostics.some((diagnostic2) => diagnostic2.severity === "error") ? "blocked" : "ready";
  return {
    schema: "movscript.timeline_assembly.media_editing_compile_result.v1",
    status,
    compile_manifest: {
      ...compileManifest,
      status,
      diagnostics
    },
    media_editing_project: mediaEditingProject,
    editing_timeline_diagnostics: editingTimelineDiagnostics,
    diagnostics
  };
}
function renderRuntimeFromEditDecisions(editDecisions) {
  return stringValue2(editDecisions.render_runtime ?? editDecisions.renderRuntime);
}
function normalizeRenderSettings(input) {
  return compactRecord2({
    width: positiveNumber(input.renderSettings?.width),
    height: positiveNumber(input.renderSettings?.height),
    fps: positiveNumber(input.renderSettings?.fps),
    background: stringValue2(input.renderSettings?.background),
    default_duration_ms: positiveNumber(input.renderSettings?.default_duration_ms)
  });
}
function targetFromTimelineAssembly(timelineAssembly, editDecisions) {
  const metadata = recordValue2(editDecisions.metadata);
  const editProfile = recordValue2(timelineAssembly.edit_profile ?? timelineAssembly.editProfile);
  return {
    timelineAssemblyId: stringValue2(timelineAssembly.id ?? timelineAssembly.timeline_assembly_id ?? metadata?.timeline_assembly_id),
    targetKind: stringValue2(timelineAssembly.target_kind ?? timelineAssembly.targetKind ?? metadata?.target_kind),
    targetRef: stringValue2(timelineAssembly.target_ref ?? timelineAssembly.targetRef ?? metadata?.target_ref),
    scopeKind: stringValue2(timelineAssembly.scope_kind ?? timelineAssembly.scopeKind ?? editProfile?.scope_kind),
    scopeRef: stringValue2(timelineAssembly.scope_ref ?? timelineAssembly.scopeRef ?? editProfile?.scope_ref)
  };
}
function capabilityDiagnostics(input) {
  const diagnostics = [];
  if (!IMPLEMENTED_BACKENDS.has(input.backend)) {
    diagnostics.push({
      code: "backend_adapter_not_implemented",
      severity: "error",
      action: `compile_to_${input.backend}`,
      message: `TimelineAssembly backend ${input.backend} is not implemented yet.`,
      details: { implemented_backends: [...IMPLEMENTED_BACKENDS].sort() }
    });
  }
  if (input.runtimeLocked && input.renderRuntime && input.backend === "media_editing_project" && !MEDIA_EDITING_RUNTIME_ALLOW_LIST.has(input.renderRuntime)) {
    diagnostics.push({
      code: "runtime_lock_backend_mismatch",
      severity: "error",
      action: `render_runtime:${input.renderRuntime}`,
      message: `TimelineAssembly is locked to ${input.renderRuntime}; media_editing_project cannot silently replace it.`,
      details: {
        backend: input.backend,
        allowed_render_runtimes: [...MEDIA_EDITING_RUNTIME_ALLOW_LIST].sort()
      }
    });
  }
  const lockedRuntime = BACKEND_RUNTIME_LOCKS[input.backend];
  if (input.runtimeLocked && input.renderRuntime && lockedRuntime && input.renderRuntime !== lockedRuntime) {
    diagnostics.push({
      code: "runtime_lock_backend_mismatch",
      severity: "error",
      action: `render_runtime:${input.renderRuntime}`,
      message: `TimelineAssembly is locked to ${input.renderRuntime}; ${input.backend} cannot silently replace it.`,
      details: {
        backend: input.backend,
        required_render_runtime: lockedRuntime
      }
    });
  }
  if ((input.actionCounts.cut ?? 0) === 0) {
    diagnostics.push({
      code: "primary_visual_action_missing",
      severity: "error",
      action: "cut",
      message: "TimelineAssembly compile requires at least one primary visual cut."
    });
  }
  for (const overlay of arrayValue2(input.editDecisions.overlays)) {
    const animation = stringValue2(overlay.animation);
    if (animation && animation !== "static" && animation !== "fade-in") {
      diagnostics.push({
        code: "overlay_animation_degraded",
        severity: "warning",
        action: "overlay",
        message: `Overlay animation ${animation} will be carried as metadata unless the selected finishing backend implements it.`,
        details: { animation }
      });
    }
  }
  return diagnostics;
}
function summarizeEditDecisionActions(editDecisions) {
  const audio = recordValue2(editDecisions.audio);
  const narration = recordValue2(audio?.narration);
  const narrationSegments = Array.isArray(audio?.narration) ? audio?.narration : arrayValue2(narration?.segments);
  const subtitles = recordValue2(editDecisions.subtitles);
  const actionCounts = {
    cut: arrayValue2(editDecisions.cuts).length,
    overlay: arrayValue2(editDecisions.overlays).length,
    narration_segment: narrationSegments.length,
    music_bed: recordValue2(audio?.music) ? 1 : 0,
    sfx_hit: arrayValue2(audio?.sfx).length,
    subtitle_segment: arrayValue2(subtitles?.segments).length + arrayValue2(subtitles?.captions).length
  };
  return {
    actionCounts,
    supportedActions: Object.entries(actionCounts).filter(([, count]) => count > 0).map(([action]) => action).sort()
  };
}
function collectReferencedAssets(editDecisions) {
  const refs = [];
  for (const cut of arrayValue2(editDecisions.cuts)) {
    pushAssetRef(refs, "cut", assetRef2(cut));
  }
  for (const overlay of arrayValue2(editDecisions.overlays)) {
    pushAssetRef(refs, "overlay", assetRef2(overlay));
  }
  const audio = recordValue2(editDecisions.audio);
  const narration = recordValue2(audio?.narration);
  const narrationSegments = Array.isArray(audio?.narration) ? audio?.narration : arrayValue2(narration?.segments);
  for (const segment of narrationSegments) {
    pushAssetRef(refs, "narration_segment", assetRef2(recordValue2(segment) ?? {}));
  }
  pushAssetRef(refs, "music_bed", assetRef2(recordValue2(audio?.music) ?? {}));
  for (const segment of arrayValue2(audio?.sfx)) {
    pushAssetRef(refs, "sfx_hit", assetRef2(recordValue2(segment) ?? {}));
  }
  const subtitles = recordValue2(editDecisions.subtitles);
  pushAssetRef(refs, "subtitle_file", assetRef2(subtitles ?? {}));
  return refs;
}
function pushAssetRef(output, action, value) {
  const ref = refKey2(value);
  if (!ref || ref === "auto" || ref === "segments" || ref === "captions" || ref === "script") return;
  output.push({ action, ref });
}
function assetManifestIndex(manifest) {
  const resolvableRefs = /* @__PURE__ */ new Set();
  const selectedResourceIds = /* @__PURE__ */ new Set();
  const selectedCandidateIds = /* @__PURE__ */ new Set();
  for (const entry of arrayValue2(manifest?.assets)) {
    const refs = assetRefsFromRecord2(entry);
    const resourceId = resourceIdFromRecord(entry);
    const candidateId = stringValue2(
      entry.candidate_id ?? entry.candidateId ?? recordValue2(entry.metadata)?.candidate_id ?? recordValue2(entry.metadata)?.candidateId ?? recordValue2(recordValue2(entry.metadata)?.intent_ref)?.candidate_id
    );
    if (resourceId !== void 0) selectedResourceIds.add(resourceId);
    if (candidateId) selectedCandidateIds.add(candidateId);
    if (assetRecordIsResolvable(entry, resourceId)) {
      for (const ref of refs) resolvableRefs.add(ref);
      if (resourceId !== void 0) resolvableRefs.add(String(resourceId));
    }
  }
  return {
    resolvableRefs,
    selectedResourceIds: [...selectedResourceIds].sort((left, right) => left - right),
    selectedCandidateIds: [...selectedCandidateIds].sort()
  };
}
function assetRecordIsResolvable(record, resourceId) {
  if (resourceId !== void 0) return true;
  return !!assetPathValue(record.localPath ?? record.local_path ?? record.path ?? record.file_path ?? record.filePath ?? record.src);
}
function assetRefsFromRecord2(record) {
  return [
    record.id,
    record.asset_id,
    record.assetId,
    record.ref,
    record.name,
    record.path,
    record.localPath,
    record.local_path,
    record.file_path,
    record.filePath,
    record.resource_id,
    record.resourceId
  ].flatMap((value) => {
    const key = refKey2(value);
    return key ? [key] : [];
  });
}
function assetRef2(record) {
  return record.source ?? record.asset_id ?? record.assetId ?? record.resource_id ?? record.resourceId;
}
function resourceIdFromRecord(record) {
  return integerField2(record.resourceId ?? record.resource_id) ?? resourceIdFromString2(stringValue2(record.path) ?? "");
}
function assetPathValue(value) {
  const path = stringValue2(value);
  if (!path) return void 0;
  if (/^(?:content-unit|content_unit):/i.test(path)) return void 0;
  return path;
}
function resourceIdFromString2(value) {
  const match = value.match(/^(?:resource|raw_resource|backend_resource)[:_](\d+)$/i);
  const raw = match?.[1];
  return raw === void 0 ? void 0 : integerField2(raw);
}
function stableHash(value) {
  const input = stableStringify(value);
  let hash = 2166136261;
  for (let index = 0; index < input.length; index += 1) {
    hash ^= input.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(16).padStart(8, "0");
}
function stableStringify(value) {
  if (value === null) return "null";
  if (value === void 0) return '"__undefined__"';
  if (typeof value === "number") return Number.isFinite(value) ? String(value) : "null";
  if (typeof value === "boolean") return value ? "true" : "false";
  if (typeof value === "string") return JSON.stringify(value);
  if (Array.isArray(value)) return `[${value.map((item) => stableStringify(item)).join(",")}]`;
  if (typeof value === "object") {
    const record = value;
    return `{${Object.keys(record).sort().map((key) => `${JSON.stringify(key)}:${stableStringify(record[key])}`).join(",")}}`;
  }
  return JSON.stringify(String(value));
}
function compactRecord2(record) {
  return Object.fromEntries(Object.entries(record).filter(([, value]) => value !== void 0));
}
function positiveNumber(value) {
  const number = numberField3(value);
  return number === void 0 || number <= 0 ? void 0 : number;
}
function numberField3(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function integerField2(value) {
  const number = numberField3(value);
  if (number === void 0 || !Number.isInteger(number) || number <= 0) return void 0;
  return number;
}
function stringValue2(value) {
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim();
  return trimmed ? trimmed : void 0;
}
function recordValue2(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) return void 0;
  return value;
}
function arrayValue2(value) {
  return Array.isArray(value) ? value : [];
}
function refKey2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return stringValue2(value);
}
function safeId3(value) {
  const raw = String(value ?? "draft").trim() || "draft";
  return raw.replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "draft";
}

// ../../packages/editing/dist/index.js
var defaultFetch = globalThis.fetch;
var EDITING_SERVICE_NAME = "movscript.editing.service";
var EDITING_SERVICE_CAPABILITIES_ENDPOINT = "/v1/editing/capabilities";
var EDITING_SERVICE_PROJECT_COMMAND_ENDPOINT = "/v1/editing/project/command";
var EDITING_SERVICE_TIMELINE_VIEW_ENDPOINT = "/v1/editing/timeline/view";
var EDITING_SERVICE_TASK_REQUEST_ENDPOINT = "/v1/editing/task/request";
var EDITING_SERVICE_TASK_ACTION_ENDPOINT = "/v1/editing/task/action";
var MEDIA_PIPELINE_SERVICE_NAME = "movscript.media.pipeline";
var MEDIA_PIPELINE_CAPABILITIES_ENDPOINT = "/v1/media-pipeline/capabilities";
var MEDIA_PIPELINE_PROBE_ENDPOINT = "/v1/media-pipeline/probe";
var MEDIA_PIPELINE_TASK_CREATE_ENDPOINT = "/v1/media-pipeline/task/create";
var MEDIA_PIPELINE_TASK_ACTION_ENDPOINT = "/v1/media-pipeline/task/action";
var EditingServiceClient = class {
  baseUrl;
  fetchImpl;
  constructor(options) {
    const baseUrl = normalizeEditingServiceBaseUrl(options.baseUrl);
    if (!baseUrl) throw new Error("editing service baseUrl is required");
    this.baseUrl = baseUrl;
    this.fetchImpl = options.fetch ?? defaultFetch;
  }
  async capabilities(signal) {
    return this.request("GET", EDITING_SERVICE_CAPABILITIES_ENDPOINT, void 0, signal);
  }
  async projectCommand(request, signal) {
    return this.request("POST", EDITING_SERVICE_PROJECT_COMMAND_ENDPOINT, {
      command: request.command,
      input: request.input ?? {}
    }, signal);
  }
  async timelineView(request, signal) {
    return this.request("POST", EDITING_SERVICE_TIMELINE_VIEW_ENDPOINT, {
      projectDir: request.projectDir,
      kind: request.kind,
      ...request.targetKind !== void 0 ? { targetKind: request.targetKind } : {},
      ...request.targetRef !== void 0 ? { targetRef: request.targetRef } : {},
      ...request.scopeKind !== void 0 ? { scopeKind: request.scopeKind } : {},
      ...request.scopeRef !== void 0 ? { scopeRef: request.scopeRef } : {},
      ...request.productionId !== void 0 ? { productionId: request.productionId } : {},
      ...request.sceneMomentId !== void 0 ? { sceneMomentId: request.sceneMomentId } : {},
      ...request.decisionStore !== void 0 ? { decisionStore: request.decisionStore } : {},
      ...request.projectName !== void 0 ? { projectName: request.projectName } : {},
      ...request.title !== void 0 ? { title: request.title } : {},
      ...request.now !== void 0 ? { now: request.now } : {},
      ...request.defaultDurationMs !== void 0 ? { defaultDurationMs: request.defaultDurationMs } : {},
      ...request.defaultDurationSec !== void 0 ? { defaultDurationSec: request.defaultDurationSec } : {}
    }, signal);
  }
  async taskRequest(request, signal) {
    return this.request("POST", EDITING_SERVICE_TASK_REQUEST_ENDPOINT, {
      taskType: request.taskType,
      input: request.input ?? {}
    }, signal);
  }
  async taskAction(request, signal) {
    return this.request("POST", EDITING_SERVICE_TASK_ACTION_ENDPOINT, {
      action: request.action,
      input: request.input ?? {}
    }, signal);
  }
  async request(method, path, body, signal) {
    const response = await this.fetchImpl(`${this.baseUrl}${path}`, {
      method,
      headers: body === void 0 ? void 0 : {
        "content-type": "application/json"
      },
      body: body === void 0 ? void 0 : JSON.stringify(body),
      signal
    });
    if (!response.ok) {
      throw new EditingServiceHTTPError(response.status, await response.text());
    }
    return response.json();
  }
};
var MediaPipelineServiceClient = class {
  baseUrl;
  fetchImpl;
  constructor(options) {
    const baseUrl = normalizeMediaPipelineServiceBaseUrl(options.baseUrl);
    if (!baseUrl) throw new Error("media pipeline service baseUrl is required");
    this.baseUrl = baseUrl;
    this.fetchImpl = options.fetch ?? defaultFetch;
  }
  async capabilities(signal) {
    return this.request("GET", MEDIA_PIPELINE_CAPABILITIES_ENDPOINT, void 0, signal);
  }
  async probe(request = {}, signal) {
    return this.request("POST", MEDIA_PIPELINE_PROBE_ENDPOINT, request, signal);
  }
  async createTask(request, signal) {
    return this.request("POST", MEDIA_PIPELINE_TASK_CREATE_ENDPOINT, {
      request: request.request
    }, signal);
  }
  async taskAction(request, signal) {
    return this.request("POST", MEDIA_PIPELINE_TASK_ACTION_ENDPOINT, {
      action: request.action,
      taskId: request.taskId,
      ...request.options ? { options: request.options } : {}
    }, signal);
  }
  async request(method, path, body, signal) {
    const response = await this.fetchImpl(`${this.baseUrl}${path}`, {
      method,
      headers: body === void 0 ? void 0 : {
        "content-type": "application/json"
      },
      body: body === void 0 ? void 0 : JSON.stringify(body),
      signal
    });
    if (!response.ok) {
      throw new MediaPipelineServiceHTTPError(response.status, await response.text());
    }
    return response.json();
  }
};
var EditingServiceHTTPError = class extends Error {
  status;
  body;
  constructor(status, body) {
    super(`editing service request failed: ${status}${body ? ` ${body}` : ""}`);
    this.name = "EditingServiceHTTPError";
    this.status = status;
    this.body = body;
  }
};
var MediaPipelineServiceHTTPError = class extends Error {
  status;
  body;
  constructor(status, body) {
    super(`media pipeline service request failed: ${status}${body ? ` ${body}` : ""}`);
    this.name = "MediaPipelineServiceHTTPError";
    this.status = status;
    this.body = body;
  }
};
function createEditingServiceClientFromRuntime(options = {}) {
  const baseUrl = resolveEditingServiceBaseUrl(options);
  if (!baseUrl) {
    throw new Error("movscript.editing.service endpoint was not found; start the local runtime daemon or set MOVSCRIPT_EDITING_SERVICE_URL");
  }
  return new EditingServiceClient({ baseUrl });
}
function createMediaPipelineServiceClientFromRuntime(options = {}) {
  const baseUrl = resolveMediaPipelineServiceBaseUrl(options);
  if (!baseUrl) {
    throw new Error("movscript.media.pipeline endpoint was not found; start the local runtime daemon or set MOVSCRIPT_MEDIA_PIPELINE_URL");
  }
  return new MediaPipelineServiceClient({ baseUrl });
}
function resolveEditingServiceBaseUrl(options = {}) {
  const env = options.env ?? process.env;
  const explicit = normalizeEditingServiceBaseUrl(options.baseUrl) ?? normalizeEditingServiceBaseUrl(env.MOVSCRIPT_EDITING_SERVICE_URL) ?? normalizeEditingServiceBaseUrl(env.MOVSCRIPT_EDITING_SERVICE_BASE_URL);
  if (explicit) return explicit;
  const homeDir = options.homeDir ?? resolveMovScriptHomeDir({ env });
  const snapshot = readRuntimeHomeSnapshot(homeDir);
  const endpoint = findRuntimeEndpoint(snapshot, EDITING_SERVICE_NAME) ?? findRuntimeService(snapshot, EDITING_SERVICE_NAME)?.endpoint;
  return normalizeEditingServiceBaseUrl(endpointURL(endpoint));
}
function resolveMediaPipelineServiceBaseUrl(options = {}) {
  const env = options.env ?? process.env;
  const explicit = normalizeMediaPipelineServiceBaseUrl(options.baseUrl) ?? normalizeMediaPipelineServiceBaseUrl(env.MOVSCRIPT_MEDIA_PIPELINE_URL) ?? normalizeMediaPipelineServiceBaseUrl(env.MOVSCRIPT_MEDIA_PIPELINE_BASE_URL);
  if (explicit) return explicit;
  const homeDir = options.homeDir ?? resolveMovScriptHomeDir({ env });
  const snapshot = readRuntimeHomeSnapshot(homeDir);
  const endpoint = findRuntimeEndpoint(snapshot, MEDIA_PIPELINE_SERVICE_NAME) ?? findRuntimeService(snapshot, MEDIA_PIPELINE_SERVICE_NAME)?.endpoint;
  return normalizeMediaPipelineServiceBaseUrl(endpointURL(endpoint));
}
function normalizeEditingServiceBaseUrl(value) {
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim().replace(/\/+$/, "");
  if (!trimmed) return void 0;
  const url = new URL(trimmed);
  if (url.protocol !== "http:" && url.protocol !== "https:") {
    throw new Error("editing service baseUrl must use http or https");
  }
  return url.toString().replace(/\/+$/, "");
}
function normalizeMediaPipelineServiceBaseUrl(value) {
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim().replace(/\/+$/, "");
  if (!trimmed) return void 0;
  const url = new URL(trimmed);
  if (url.protocol !== "http:" && url.protocol !== "https:") {
    throw new Error("media pipeline service baseUrl must use http or https");
  }
  return url.toString().replace(/\/+$/, "");
}
function endpointURL(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}

// ../../services/media-pipeline/src/headlessRuntime.mjs
import { spawn } from "child_process";
import { mkdir, readdir, stat, writeFile } from "fs/promises";
import { basename, dirname, extname, join as join2, resolve as resolve2 } from "path";
import { tmpdir } from "os";
var HEADLESS_SUPPORTED_TASK_TYPES = Object.freeze(["timeline_render", "timeline_hls", "media_transcode", "media_reframe"]);
var HEADLESS_SUPPORTED_OUTPUTS = Object.freeze(["mp4", "hls"]);
function createHeadlessMediaPipelineRuntimePort(options = {}) {
  const env = options.env ?? process.env;
  const tasks = /* @__PURE__ */ new Map();
  let nextTaskId = 0;
  return {
    async getCapabilities() {
      const ffmpeg = await probeFFmpeg(env);
      return {
        status: "ok",
        runtime: "headless_media_pipeline",
        available: ffmpeg.available,
        ffmpeg,
        supportedTaskTypes: [...HEADLESS_SUPPORTED_TASK_TYPES],
        supported_task_types: [...HEADLESS_SUPPORTED_TASK_TYPES],
        supportedOutputs: [...HEADLESS_SUPPORTED_OUTPUTS],
        supported_outputs: [...HEADLESS_SUPPORTED_OUTPUTS],
        localHlsPreview: false,
        local_hls_preview: false,
        projectStore: false,
        project_store: false
      };
    },
    async createTask(request) {
      const now = (/* @__PURE__ */ new Date()).toISOString();
      const taskType = stringValue3(request?.taskType);
      const projectId = stringValue3(request?.projectId) ?? "default";
      const taskId = `${taskType || "media_task"}_headless_${++nextTaskId}`;
      const task = {
        taskId,
        projectId,
        taskType,
        status: "queued",
        progressPercent: 0,
        currentStep: "queued",
        createdAt: now,
        updatedAt: now
      };
      const entry = { task, logs: [`${now} queued ${taskId}`], child: void 0 };
      tasks.set(taskId, entry);
      runHeadlessTask({ request, entry, env }).catch((error) => {
        failTask(entry, "HEADLESS_MEDIA_PIPELINE_ERROR", errorMessage2(error));
      });
      return { ...task };
    },
    async getTask(taskId) {
      return cloneTask(tasks.get(taskId)?.task) ?? null;
    },
    async cancelTask(taskId) {
      const entry = tasks.get(taskId);
      if (!entry) return notFoundTask(taskId);
      if (entry.child && entry.task.status === "running") {
        entry.child.kill("SIGTERM");
      }
      updateTask(entry, {
        status: "canceled",
        progressPercent: 100,
        currentStep: "canceled"
      });
      entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} canceled ${taskId}`);
      return cloneTask(entry.task);
    },
    async getTaskLogs(taskId) {
      const entry = tasks.get(taskId);
      if (!entry) {
        return {
          status: "not_found",
          taskId,
          logs: []
        };
      }
      return {
        status: "ok",
        taskId,
        logs: [...entry.logs],
        text: entry.logs.join("\n")
      };
    }
  };
}
async function runHeadlessTask({ request, entry, env }) {
  const capabilities = await probeFFmpeg(env);
  if (!capabilities.available) {
    failTask(entry, capabilities.code ?? "FFMPEG_UNAVAILABLE", capabilities.error ?? "ffmpeg is unavailable");
    return;
  }
  const taskType = stringValue3(request?.taskType);
  if (!HEADLESS_SUPPORTED_TASK_TYPES.includes(taskType)) {
    failTask(entry, "HEADLESS_TASK_TYPE_UNSUPPORTED", `headless media pipeline does not support task type: ${taskType}`);
    return;
  }
  const source = taskType === "timeline_render" || taskType === "timeline_hls" ? timelineSource(request?.timeline ?? request?.editingProject?.timeline) : request?.source;
  const resolvedSource = await resolveSourceLocalPath({
    source,
    request,
    entry,
    env
  });
  const sourcePath = resolvedSource.localPath;
  if (!sourcePath) {
    failTask(entry, "SOURCE_REQUIRED", `${taskType} requires a local file source or a resolvable resource_id source`);
    return;
  }
  try {
    const sourceStat = await stat(sourcePath);
    if (!sourceStat.isFile()) {
      failTask(entry, "SOURCE_NOT_FILE", `source is not a file: ${sourcePath}`);
      return;
    }
  } catch {
    failTask(entry, "SOURCE_NOT_FOUND", `source file was not found: ${sourcePath}`);
    return;
  }
  const outputPath = await resolveOutputPath({ request, task: entry.task, env });
  const variants = taskType === "timeline_hls" ? hlsVariants(request?.output) : [];
  if (variants.length > 0) {
    await runHeadlessHlsVariantTask({ capabilities, sourcePath, outputPath, request, entry, variants });
    return;
  }
  const args = ffmpegArgsForTask({ taskType, sourcePath, outputPath, request });
  updateTask(entry, {
    status: "running",
    progressPercent: 5,
    currentStep: "ffmpeg",
    outputPath,
    outputName: outputPath.split("/").at(-1),
    ...resolvedSource.resourceId !== void 0 ? { sourceResourceId: resolvedSource.resourceId, source_resource_id: resolvedSource.resourceId } : {},
    ...resolvedSource.downloaded ? { sourceDownloaded: true, source_downloaded: true } : {}
  });
  entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} ffmpeg ${args.join(" ")}`);
  const result = await runProcess(capabilities.path, args, entry);
  if (result.code === 0) {
    updateTask(entry, {
      status: "succeeded",
      progressPercent: 100,
      currentStep: "completed",
      outputPath,
      outputName: outputPath.split("/").at(-1),
      ...taskType === "timeline_hls" ? {
        hlsManifestPath: outputPath,
        hls_manifest_path: outputPath,
        hlsDirectory: outputPath.split("/").slice(0, -1).join("/"),
        hls_directory: outputPath.split("/").slice(0, -1).join("/")
      } : {}
    });
    entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} completed ${entry.task.taskId}`);
    return;
  }
  failTask(entry, "FFMPEG_FAILED", `ffmpeg exited with code ${result.code}`);
}
async function runHeadlessHlsVariantTask({ capabilities, sourcePath, outputPath, request, entry, variants }) {
  updateTask(entry, {
    status: "running",
    progressPercent: 5,
    currentStep: "hls-variants",
    outputPath,
    outputName: outputPath.split("/").at(-1),
    hlsManifestPath: outputPath,
    hls_manifest_path: outputPath,
    hlsDirectory: outputPath.split("/").slice(0, -1).join("/"),
    hls_directory: outputPath.split("/").slice(0, -1).join("/")
  });
  const hlsDirectory = outputPath.split("/").slice(0, -1).join("/");
  const variantStates = [];
  for (const [index, variant] of variants.entries()) {
    const state = normalizeHlsVariant(variant, index);
    variantStates.push(state);
    const playlistPath = join2(hlsDirectory, `${state.name}.m3u8`);
    const segmentPattern = join2(hlsDirectory, `${state.name}-segment-%05d.ts`);
    const args = ffmpegArgsForHlsVariant({ sourcePath, playlistPath, segmentPattern, variant: state, request });
    entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} ffmpeg ${args.join(" ")}`);
    const result = await runProcess(capabilities.path, args, entry);
    if (result.code !== 0) {
      failTask(entry, "FFMPEG_FAILED", `ffmpeg exited with code ${result.code}`);
      return;
    }
  }
  await writeFile(outputPath, masterHlsManifest(variantStates), "utf8");
  const segmentPaths = await hlsOutputPaths(hlsDirectory, outputPath);
  updateTask(entry, {
    status: "succeeded",
    progressPercent: 100,
    currentStep: "completed",
    hlsSegmentPaths: segmentPaths,
    hls_segment_paths: segmentPaths,
    hlsVariants: variantStates,
    hls_variants: variantStates
  });
  entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} completed ${entry.task.taskId}`);
}
async function resolveOutputPath({ request, task, env }) {
  const homeDir = stringValue3(env.MOVSCRIPT_HOME);
  const root = stringValue3(env.MOVSCRIPT_MEDIA_PIPELINE_WORK_DIR) ?? (homeDir ? join2(homeDir, "runtime", "media-pipeline", "tasks") : join2(tmpdir(), "movscript-media-pipeline"));
  const taskDir = join2(root, task.projectId, task.taskId);
  await mkdir(taskDir, { recursive: true });
  const filename = stringValue3(request?.output?.filename) ?? `${task.taskId}.${request?.output?.format === "hls" ? "m3u8" : "mp4"}`;
  return resolve2(taskDir, filename);
}
function ffmpegArgsForTask({ taskType, sourcePath, outputPath, request }) {
  const args = ["-y", "-i", sourcePath];
  if (taskType === "timeline_hls") {
    args.push("-f", "hls", "-hls_time", "4", "-hls_playlist_type", "vod");
  }
  if (taskType === "media_reframe") {
    const width = positiveInteger(request?.reframe?.width);
    const height = positiveInteger(request?.reframe?.height);
    if (width && height) {
      const mode = stringValue3(request?.reframe?.mode) ?? "contain";
      const background = stringValue3(request?.reframe?.background) ?? "black";
      if (mode === "stretch") {
        args.push("-vf", `scale=${width}:${height}`);
      } else if (mode === "crop" || mode === "cover") {
        args.push("-vf", `scale=${width}:${height}:force_original_aspect_ratio=increase,crop=${width}:${height}`);
      } else {
        args.push("-vf", `scale=${width}:${height}:force_original_aspect_ratio=decrease,pad=${width}:${height}:(ow-iw)/2:(oh-ih)/2:${background}`);
      }
    }
  }
  const videoCodec = stringValue3(request?.transcode?.videoCodec ?? request?.transcode?.video_codec);
  const audioCodec = stringValue3(request?.transcode?.audioCodec ?? request?.transcode?.audio_codec);
  if (videoCodec) args.push("-c:v", videoCodec);
  if (audioCodec) args.push("-c:a", audioCodec);
  args.push(outputPath);
  return args;
}
function ffmpegArgsForHlsVariant({ sourcePath, playlistPath, segmentPattern, variant, request }) {
  const args = ["-y", "-i", sourcePath];
  if (variant.width && variant.height) {
    args.push("-vf", `scale=${variant.width}:${variant.height}:force_original_aspect_ratio=decrease,pad=${variant.width}:${variant.height}:(ow-iw)/2:(oh-ih)/2:black`);
  }
  if (variant.videoBitrateKbps) args.push("-b:v", `${variant.videoBitrateKbps}k`);
  if (variant.audioBitrateKbps) args.push("-b:a", `${variant.audioBitrateKbps}k`);
  const videoCodec = stringValue3(request?.transcode?.videoCodec ?? request?.transcode?.video_codec);
  const audioCodec = stringValue3(request?.transcode?.audioCodec ?? request?.transcode?.audio_codec);
  if (videoCodec) args.push("-c:v", videoCodec);
  if (audioCodec) args.push("-c:a", audioCodec);
  args.push("-f", "hls", "-hls_time", "4", "-hls_playlist_type", "vod", "-hls_segment_filename", segmentPattern, playlistPath);
  return args;
}
function hlsVariants(output) {
  const variants = Array.isArray(output?.hlsVariants) ? output.hlsVariants : Array.isArray(output?.hls_variants) ? output.hls_variants : [];
  return variants.filter((variant) => variant && typeof variant === "object" && !Array.isArray(variant));
}
function normalizeHlsVariant(variant, index) {
  const width = positiveInteger(variant.width);
  const height = positiveInteger(variant.height);
  const videoBitrateKbps = positiveInteger(variant.videoBitrateKbps ?? variant.video_bitrate_kbps) ?? 900;
  const audioBitrateKbps = positiveInteger(variant.audioBitrateKbps ?? variant.audio_bitrate_kbps) ?? 128;
  const name = safeVariantName(stringValue3(variant.name) ?? (height ? `${height}p` : `variant-${index + 1}`));
  const bandwidth = (videoBitrateKbps + audioBitrateKbps) * 1e3;
  return {
    name,
    bandwidth,
    videoBitrateKbps,
    video_bitrate_kbps: videoBitrateKbps,
    audioBitrateKbps,
    audio_bitrate_kbps: audioBitrateKbps,
    ...width ? { width } : {},
    ...height ? { height } : {}
  };
}
function masterHlsManifest(variants) {
  return [
    "#EXTM3U",
    "#EXT-X-VERSION:7",
    ...variants.flatMap((variant) => {
      const attributes = [`BANDWIDTH=${variant.bandwidth}`];
      if (variant.width && variant.height) attributes.push(`RESOLUTION=${variant.width}x${variant.height}`);
      return [`#EXT-X-STREAM-INF:${attributes.join(",")}`, `${variant.name}.m3u8`];
    }),
    ""
  ].join("\n");
}
async function hlsOutputPaths(hlsDirectory, masterPath) {
  const names = await readdir(hlsDirectory);
  return names.filter((name) => name !== masterPath.split("/").at(-1)).sort().map((name) => join2(hlsDirectory, name));
}
function safeVariantName(name) {
  return name.replace(/[^a-zA-Z0-9_-]+/g, "-").replace(/^-+|-+$/g, "") || "variant";
}
async function probeFFmpeg(env) {
  const candidate = stringValue3(env.MOVSCRIPT_FFMPEG_PATH) ?? stringValue3(env.FFMPEG_PATH) ?? "ffmpeg";
  try {
    const result = await runProcess(candidate, ["-version"]);
    if (result.code !== 0) {
      return {
        available: false,
        path: candidate,
        code: "FFMPEG_PROBE_FAILED",
        error: result.stderr || `ffmpeg exited with code ${result.code}`
      };
    }
    return {
      available: true,
      path: candidate,
      version: firstLine(result.stdout),
      platform: process.platform,
      arch: process.arch
    };
  } catch (error) {
    return {
      available: false,
      path: candidate,
      code: "FFMPEG_NOT_FOUND",
      error: errorMessage2(error),
      platform: process.platform,
      arch: process.arch
    };
  }
}
function runProcess(command, args, entry) {
  return new Promise((resolveProcess, rejectProcess) => {
    const child = spawn(command, args, { stdio: ["ignore", "pipe", "pipe"] });
    if (entry) entry.child = child;
    let stderr = "";
    let stdout = "";
    child.stdout?.setEncoding("utf8");
    child.stderr?.setEncoding("utf8");
    child.stdout?.on("data", (chunk) => {
      stdout += String(chunk);
    });
    child.stderr?.on("data", (chunk) => {
      stderr += String(chunk);
      if (entry) entry.logs.push(String(chunk).trimEnd());
    });
    child.once("error", rejectProcess);
    child.once("close", (code) => {
      if (entry) entry.child = void 0;
      resolveProcess({ code: code ?? 0, stdout, stderr });
    });
  });
}
function updateTask(entry, patch) {
  entry.task = {
    ...entry.task,
    ...patch,
    updatedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
}
function failTask(entry, errorCode, errorMessageValue) {
  updateTask(entry, {
    status: "failed",
    progressPercent: 100,
    currentStep: "failed",
    errorCode,
    errorMessage: errorMessageValue
  });
  entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} failed ${entry.task.taskId}: ${errorCode} ${errorMessageValue}`);
}
function notFoundTask(taskId) {
  const now = (/* @__PURE__ */ new Date()).toISOString();
  return {
    taskId,
    projectId: "unknown",
    taskType: "media_transcode",
    status: "failed",
    progressPercent: 100,
    currentStep: "not_found",
    errorCode: "TASK_NOT_FOUND",
    errorMessage: `media pipeline task was not found: ${taskId}`,
    createdAt: now,
    updatedAt: now
  };
}
function cloneTask(task) {
  return task ? { ...task } : void 0;
}
function sourceLocalPath(source) {
  if (!source || typeof source !== "object" || Array.isArray(source)) return void 0;
  return stringValue3(source.localPath ?? source.local_path ?? source.path);
}
function timelineSource(timeline) {
  if (!timeline || typeof timeline !== "object" || Array.isArray(timeline)) return void 0;
  const tracks = Array.isArray(timeline.tracks) ? timeline.tracks : [];
  const sortedClips = tracks.flatMap((track) => Array.isArray(track?.clips) ? track.clips : []).filter((clip) => clip && typeof clip === "object").sort((left, right) => Number(left.timelineStartMs ?? left.timeline_start_ms ?? 0) - Number(right.timelineStartMs ?? right.timeline_start_ms ?? 0));
  for (const clip of sortedClips) {
    if (clip.asset && typeof clip.asset === "object" && !Array.isArray(clip.asset)) return clip.asset;
  }
  return void 0;
}
async function resolveSourceLocalPath({ source, request, entry, env }) {
  const localPath = sourceLocalPath(source);
  if (localPath) return { localPath };
  const resourceId = sourceResourceId(source);
  if (resourceId === void 0) return {};
  const configuredPath = sourceResourceLocalPath(resourceId, request);
  if (configuredPath) return { localPath: configuredPath, resourceId };
  const resourceDownload = recordValue3(request?.resourceDownload) ?? recordValue3(request?.resource_download) ?? recordValue3(request?.output?.resourceDownload) ?? recordValue3(request?.output?.resource_download);
  const url = resourceDownloadURL(resourceId, resourceDownload);
  if (!url) return { resourceId };
  const cachePath = await resourceDownloadCachePath({
    resourceId,
    responseURL: url,
    resourceDownload,
    request,
    env
  });
  try {
    const cached = await stat(cachePath);
    if (cached.isFile() && cached.size > 0) {
      entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} resource ${resourceId} cache hit ${cachePath}`);
      return { localPath: cachePath, resourceId, downloaded: true };
    }
  } catch {
  }
  entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} downloading resource ${resourceId} ${url}`);
  const response = await fetch(url, { headers: resourceDownloadHeaders(resourceDownload) });
  if (!response.ok) {
    throw new Error(`resource ${resourceId} download failed with HTTP ${response.status}`);
  }
  const bytes = Buffer.from(await response.arrayBuffer());
  if (bytes.byteLength === 0) throw new Error(`resource ${resourceId} download returned an empty body`);
  await mkdir(dirname(cachePath), { recursive: true });
  await writeFile(cachePath, bytes);
  entry.logs.push(`${(/* @__PURE__ */ new Date()).toISOString()} downloaded resource ${resourceId} ${bytes.byteLength} bytes to ${cachePath}`);
  return { localPath: cachePath, resourceId, downloaded: true };
}
function sourceResourceId(source) {
  if (!source || typeof source !== "object" || Array.isArray(source)) return void 0;
  return positiveInteger(source.resourceId ?? source.resource_id ?? source.backendResourceId ?? source.backend_resource_id);
}
function sourceResourceLocalPath(resourceId, request) {
  const resourceCache = recordValue3(request?.resourceCache) ?? recordValue3(request?.resource_cache) ?? recordValue3(request?.output?.resourceCache) ?? recordValue3(request?.output?.resource_cache);
  const localFiles = recordValue3(resourceCache?.localFiles) ?? recordValue3(resourceCache?.local_files) ?? recordValue3(resourceCache?.files);
  const value = localFiles?.[String(resourceId)] ?? localFiles?.[resourceId];
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function resourceDownloadURL(resourceId, resourceDownload) {
  if (!resourceDownload) return void 0;
  const urlTemplate = stringValue3(resourceDownload.urlTemplate ?? resourceDownload.url_template);
  if (urlTemplate) {
    return urlTemplate.replaceAll("{resourceId}", encodeURIComponent(String(resourceId))).replaceAll("{resource_id}", encodeURIComponent(String(resourceId))).replaceAll(":id", encodeURIComponent(String(resourceId)));
  }
  const baseURL = stringValue3(resourceDownload.baseUrl ?? resourceDownload.base_url ?? resourceDownload.apiBaseUrl ?? resourceDownload.api_base_url);
  if (!baseURL) return void 0;
  const base = baseURL.replace(/\/+$/, "");
  return `${base}/api/v1/resources/${encodeURIComponent(String(resourceId))}/file`;
}
function resourceDownloadHeaders(resourceDownload) {
  const headers = {};
  const configured = recordValue3(resourceDownload?.headers);
  if (configured) {
    for (const [key, value] of Object.entries(configured)) {
      const string = stringValue3(value);
      if (string) headers[key] = string;
    }
  }
  const authorization = stringValue3(resourceDownload?.authorization);
  const bearerToken = stringValue3(resourceDownload?.bearerToken ?? resourceDownload?.bearer_token);
  if (authorization) headers.Authorization = authorization;
  if (bearerToken) headers.Authorization = `Bearer ${bearerToken}`;
  return headers;
}
async function resourceDownloadCachePath({ resourceId, responseURL, resourceDownload, request, env }) {
  const cacheDir = stringValue3(resourceDownload?.cacheDir ?? resourceDownload?.cache_dir) ?? stringValue3(request?.resourceCache?.cacheDir ?? request?.resourceCache?.cache_dir) ?? stringValue3(request?.resource_cache?.cacheDir ?? request?.resource_cache?.cache_dir) ?? stringValue3(env.MOVSCRIPT_MEDIA_PIPELINE_RESOURCE_CACHE_DIR) ?? defaultResourceCacheDir(env);
  await mkdir(cacheDir, { recursive: true });
  const extension = resourceExtension(resourceId, responseURL, resourceDownload);
  return join2(cacheDir, `resource-${resourceId}${extension}`);
}
function defaultResourceCacheDir(env) {
  const homeDir = stringValue3(env.MOVSCRIPT_HOME);
  return homeDir ? join2(homeDir, "runtime", "media-pipeline", "resource-cache") : join2(tmpdir(), "movscript-media-pipeline", "resource-cache");
}
function resourceExtension(resourceId, responseURL, resourceDownload) {
  const explicit = stringValue3(resourceDownload?.extension);
  if (explicit) return explicit.startsWith(".") ? explicit : `.${explicit}`;
  const filename = stringValue3(resourceDownload?.filename);
  const filenameExtension = filename ? extname(filename) : "";
  if (filenameExtension) return filenameExtension;
  try {
    const parsed = new URL(responseURL);
    const pathExtension = extname(basename(parsed.pathname));
    if (pathExtension) return pathExtension;
  } catch {
    const pathExtension = extname(basename(responseURL));
    if (pathExtension) return pathExtension;
  }
  return `.resource-${resourceId}`;
}
function recordValue3(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : void 0;
}
function stringValue3(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function positiveInteger(value) {
  const number = Number(value);
  return Number.isInteger(number) && number > 0 ? number : void 0;
}
function firstLine(value) {
  return stringValue3(String(value ?? "").split(/\r?\n/)[0]);
}
function errorMessage2(error) {
  return error instanceof Error ? error.message : String(error);
}

// ../../services/media-pipeline/src/server.mjs
var MEDIA_PIPELINE_SERVICE_CAPABILITIES = Object.freeze([
  "probe",
  "thumbnail",
  "waveform",
  "transcode",
  "render",
  "timeline-materialization"
]);
var MEDIA_PIPELINE_SUPPORTED_TASK_TYPES = Object.freeze([
  "timeline_render",
  "timeline_hls",
  "media_transcode",
  "media_reframe"
]);
var MEDIA_PIPELINE_SUPPORTED_OUTPUTS = Object.freeze(["mp4", "hls"]);
function createMediaPipelineServiceHandler(options = {}) {
  const serviceName = options.serviceName ?? MEDIA_PIPELINE_SERVICE_NAME;
  const capabilities = options.capabilities ?? MEDIA_PIPELINE_SERVICE_CAPABILITIES;
  const supportedTaskTypes = options.supportedTaskTypes ?? MEDIA_PIPELINE_SUPPORTED_TASK_TYPES;
  const supportedOutputs = options.supportedOutputs ?? MEDIA_PIPELINE_SUPPORTED_OUTPUTS;
  const probe = options.probe ?? defaultProbe;
  const runtimePort = options.runtimePort;
  return async (request, response) => {
    const url = new URL(request.url ?? "/", "http://127.0.0.1");
    try {
      writeCORSHeaders(response, request.headers.origin);
      if (request.method === "OPTIONS") {
        response.writeHead(204);
        response.end();
        return;
      }
      if (request.method === "GET" && url.pathname === "/health") {
        writeJSON(response, 200, {
          status: "ok",
          serviceName,
          capabilities
        });
        return;
      }
      if (request.method === "GET" && url.pathname === MEDIA_PIPELINE_CAPABILITIES_ENDPOINT) {
        writeJSON(response, 200, mediaPipelineCapabilities({
          serviceName,
          capabilities,
          supportedTaskTypes,
          supportedOutputs
        }));
        return;
      }
      if (request.method === "POST" && url.pathname === MEDIA_PIPELINE_PROBE_ENDPOINT) {
        const body = await readJSONBody(request);
        const taskType = stringValue4(body.taskType ?? body.task_type);
        if (taskType && !supportedTaskTypes.includes(taskType)) {
          throw httpError(400, "media_pipeline_task_type_unsupported", `unsupported media pipeline task type: ${taskType}`);
        }
        const result = await probe({
          input: body,
          taskType,
          feature: stringValue4(body.feature),
          serviceName,
          capabilities,
          supportedTaskTypes,
          supportedOutputs,
          runtimePort
        });
        writeJSON(response, 200, normalizeProbeResult(result, {
          serviceName,
          capabilities,
          supportedTaskTypes,
          supportedOutputs,
          taskType,
          feature: stringValue4(body.feature)
        }));
        return;
      }
      if (request.method === "POST" && url.pathname === MEDIA_PIPELINE_TASK_CREATE_ENDPOINT) {
        const body = await readJSONBody(request);
        const taskRequest = recordValue4(body.request);
        if (!taskRequest) {
          throw httpError(400, "media_pipeline_task_request_required", "media pipeline task create requires request");
        }
        const taskType = stringValue4(taskRequest.taskType);
        if (!taskType) {
          throw httpError(400, "media_pipeline_task_type_required", "media pipeline task request requires taskType");
        }
        if (!supportedTaskTypes.includes(taskType)) {
          throw httpError(400, "media_pipeline_task_type_unsupported", `unsupported media pipeline task type: ${taskType}`);
        }
        assertRuntimeMethod(runtimePort, "createTask");
        const task = await runtimePort.createTask(taskRequest);
        writeJSON(response, 200, {
          schema: "movscript.media-pipeline-task-create.v1",
          task
        });
        return;
      }
      if (request.method === "POST" && url.pathname === MEDIA_PIPELINE_TASK_ACTION_ENDPOINT) {
        const body = await readJSONBody(request);
        const action = stringValue4(body.action);
        if (!action) {
          throw httpError(400, "media_pipeline_task_action_required", "media pipeline task action requires action");
        }
        const taskId = stringValue4(body.taskId);
        if (!taskId) {
          throw httpError(400, "media_pipeline_task_id_required", "media pipeline task action requires taskId");
        }
        const runtimeOptions = mediaPipelineTaskOptions(body.options);
        if (action === "getTask") {
          assertRuntimeMethod(runtimePort, "getTask");
          writeJSON(response, 200, {
            schema: "movscript.media-pipeline-task-action.v1",
            action,
            task: await runtimePort.getTask(taskId, runtimeOptions) ?? null
          });
          return;
        }
        if (action === "cancelTask") {
          assertRuntimeMethod(runtimePort, "cancelTask");
          writeJSON(response, 200, {
            schema: "movscript.media-pipeline-task-action.v1",
            action,
            task: await runtimePort.cancelTask(taskId, runtimeOptions)
          });
          return;
        }
        if (action === "getTaskLogs") {
          assertRuntimeMethod(runtimePort, "getTaskLogs");
          writeJSON(response, 200, {
            schema: "movscript.media-pipeline-task-action.v1",
            action,
            logs: await runtimePort.getTaskLogs(taskId, runtimeOptions)
          });
          return;
        }
        throw httpError(400, "media_pipeline_task_action_unsupported", `unsupported media pipeline task action: ${action}`);
      }
      writeJSON(response, 404, {
        error: "not_found"
      });
    } catch (error) {
      writeMediaPipelineServiceError(response, error);
    }
  };
}
function mediaPipelineCapabilities({ serviceName, capabilities, supportedTaskTypes, supportedOutputs }) {
  return {
    serviceName,
    capabilities,
    runtimeContract: "EditingRuntimePort",
    supportedTaskTypes,
    supportedOutputs
  };
}
async function defaultProbe(context = {}) {
  const runtimePort = context.runtimePort;
  if (runtimePort?.getCapabilities) {
    try {
      const runtimeCapabilities = await runtimePort.getCapabilities();
      return {
        status: runtimeCapabilities.available ? "available" : "unavailable",
        available: runtimeCapabilities.available,
        ffmpeg: runtimeCapabilities.ffmpeg
      };
    } catch (error) {
      return {
        status: "unavailable",
        available: false,
        reason: "media_pipeline_probe_failed",
        ffmpeg: {
          available: false,
          code: "FFMPEG_RUNTIME_PROBE_FAILED",
          error: stringValue4(error?.message)
        }
      };
    }
  }
  return {
    status: "unavailable",
    available: false,
    reason: "media_pipeline_execution_not_configured",
    ffmpeg: {
      available: false,
      code: "FFMPEG_RUNTIME_NOT_CONFIGURED"
    }
  };
}
function normalizeProbeResult(result, context) {
  const available = result?.available === true || result?.status === "available";
  return {
    schema: "movscript.media-pipeline-probe.v1",
    serviceName: context.serviceName,
    status: available ? "available" : "unavailable",
    available,
    runtimeContract: "EditingRuntimePort",
    capabilities: context.capabilities,
    supportedTaskTypes: context.supportedTaskTypes,
    supportedOutputs: context.supportedOutputs,
    ...context.taskType ? { requestedTaskType: context.taskType } : {},
    ...context.feature ? { requestedFeature: context.feature } : {},
    ...stringValue4(result?.reason) ? { reason: stringValue4(result.reason) } : {},
    ...recordValue4(result?.ffmpeg) ? { ffmpeg: result.ffmpeg } : {}
  };
}
function startMediaPipelineService(options = {}) {
  const host = options.host ?? "127.0.0.1";
  const port = Number(options.port ?? 0);
  const server = createServer(createMediaPipelineServiceHandler(options));
  return new Promise((resolve12, reject) => {
    server.once("error", reject);
    server.listen(port, host, () => {
      server.off("error", reject);
      const address = server.address();
      const actualPort = typeof address === "object" && address ? address.port : port;
      resolve12({
        server,
        host,
        port: actualPort,
        url: `http://${host}:${actualPort}`,
        close: () => new Promise((closeResolve, closeReject) => {
          server.close((error) => error ? closeReject(error) : closeResolve());
        })
      });
    });
  });
}
async function runMediaPipelineServiceCLI(argv = [], env = process.env) {
  const command = argv[0] ?? "serve";
  if (command !== "serve") {
    throw new Error(`unsupported media-pipeline command: ${command}`);
  }
  const host = env.MOVSCRIPT_MEDIA_PIPELINE_HOST || "127.0.0.1";
  const port = Number(env.MOVSCRIPT_MEDIA_PIPELINE_PORT || env.PORT || 0);
  const runtimePort = env.MOVSCRIPT_MEDIA_PIPELINE_RUNTIME === "none" ? void 0 : createHeadlessMediaPipelineRuntimePort({ env });
  const runtime = await startMediaPipelineService({ host, port, runtimePort });
  process.stdout.write(JSON.stringify({
    serviceName: MEDIA_PIPELINE_SERVICE_NAME,
    url: runtime.url
  }) + "\n");
  await waitForShutdown(runtime);
}
function writeJSON(response, statusCode, payload) {
  response.writeHead(statusCode, {
    "content-type": "application/json; charset=utf-8"
  });
  response.end(JSON.stringify(payload));
}
function writeCORSHeaders(response, origin) {
  response.setHeader("Access-Control-Allow-Origin", typeof origin === "string" && origin ? origin : "*");
  response.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
  response.setHeader("Access-Control-Allow-Headers", "Content-Type, Authorization");
  response.setHeader("Access-Control-Allow-Credentials", "false");
  response.setHeader("Vary", "Origin");
}
async function readJSONBody(request) {
  let raw = "";
  for await (const chunk of request) raw += chunk;
  if (!raw.trim()) return {};
  try {
    return JSON.parse(raw);
  } catch {
    throw httpError(400, "invalid_json", "request body must be valid JSON");
  }
}
function writeMediaPipelineServiceError(response, error) {
  const statusCode = Number(error?.statusCode ?? 500);
  writeJSON(response, statusCode, {
    error: stringValue4(error?.code) ?? "media_pipeline_service_error",
    message: stringValue4(error?.message) ?? "media pipeline service error"
  });
}
function httpError(statusCode, code, message) {
  const error = new Error(message);
  error.statusCode = statusCode;
  error.code = code;
  return error;
}
function stringValue4(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function recordValue4(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : void 0;
}
function mediaPipelineTaskOptions(value) {
  const options = recordValue4(value);
  const projectId = stringValue4(options?.projectId);
  return projectId ? { projectId } : void 0;
}
function assertRuntimeMethod(runtimePort, method) {
  if (!runtimePort || typeof runtimePort[method] !== "function") {
    throw httpError(503, "media_pipeline_runtime_unavailable", `media pipeline runtime method is unavailable: ${method}`);
  }
}
async function waitForShutdown(runtime) {
  await new Promise((resolve12) => {
    const stop = async () => {
      process.off("SIGINT", stop);
      process.off("SIGTERM", stop);
      await runtime.close();
      resolve12();
    };
    process.on("SIGINT", stop);
    process.on("SIGTERM", stop);
  });
}

// ../../packages/local-runtime/dist/index.js
import { spawn as spawn2 } from "child_process";
import { existsSync as existsSync2, mkdirSync as mkdirSync2, readFileSync as readFileSync2, rmSync, writeFileSync as writeFileSync2 } from "fs";
import { dirname as dirname2, join as join3 } from "path";
var LOCAL_RUNTIME_DAEMON_APP_ID = "movscript.local-node";
var LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE = "movscript.local-node.control";
var LOCAL_RUNTIME_DAEMON_GATEWAY_SERVICE = "movscript.local-node.gateway";
var REQUIRED_LOCAL_RUNTIME_DAEMON_SERVICES = [
  LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE,
  LOCAL_RUNTIME_DAEMON_GATEWAY_SERVICE,
  "movscript.project.service",
  "movscript.editing.service",
  "movscript.canvas.service",
  "movscript.local-surface.host",
  "movscript.media.pipeline"
];
var LOCAL_DATA_SERVICE = "movscript.data.service";
async function ensureLocalRuntimeDaemon(options) {
  const startupTimeoutMs = options.startupTimeoutMs ?? 15e3;
  const stopTimeoutMs = options.stopTimeoutMs ?? 5e3;
  const initial = await probeLocalRuntimeDaemon(options.homeDir);
  if (!options.forceRestart && initial.available && localRuntimeServicesReady(initial) && localRuntimeMatchesRequest(initial, options)) {
    await localRuntimeControlRequest(options.homeDir, "POST", "/touch").catch(() => void 0);
    return { status: "ready", reused: true, ...initial };
  }
  const lock = await acquireLocalRuntimeStartupLock(options.homeDir, startupTimeoutMs);
  if (!lock) {
    const reused = await waitForLocalRuntimeReady(options.homeDir, startupTimeoutMs, options);
    if (reused) return { status: "ready", reused: true, ...reused };
    const lastProbe = await probeLocalRuntimeDaemon(options.homeDir);
    throw new Error(`MovScript local runtime daemon did not become ready while another startup was in progress; ${localRuntimeReadinessSummary(lastProbe, options)}`);
  }
  try {
    const probe = await probeLocalRuntimeDaemon(options.homeDir);
    if (probe.available) {
      if (!options.forceRestart && localRuntimeServicesReady(probe) && localRuntimeMatchesRequest(probe, options)) {
        await localRuntimeControlRequest(options.homeDir, "POST", "/touch").catch(() => void 0);
        return { status: "ready", reused: true, ...probe };
      }
      await stopAvailableLocalRuntimeDaemon(options.homeDir, stopTimeoutMs);
    }
    const child = spawn2(process.execPath, [options.entrypoint, ...options.runArgs ?? ["daemon", "run"]], {
      cwd: options.cwd,
      env: {
        ...process.env,
        ...options.env ?? {},
        MOVSCRIPT_HOME: options.homeDir
      },
      detached: true,
      stdio: "ignore"
    });
    child.unref();
    const ready = await waitForLocalRuntimeReady(options.homeDir, startupTimeoutMs, options);
    if (ready) return { status: "ready", reused: false, launcherPid: child.pid, ...ready };
    const lastProbe = await probeLocalRuntimeDaemon(options.homeDir);
    throw new Error(`MovScript local runtime daemon did not become ready within ${startupTimeoutMs}ms; ${localRuntimeReadinessSummary(lastProbe, options)}`);
  } finally {
    lock.release();
  }
}
async function probeLocalRuntimeDaemon(homeDir) {
  const snapshot = readRuntimeHomeSnapshot(homeDir);
  const app = findRuntimeApp(snapshot, LOCAL_RUNTIME_DAEMON_APP_ID) ?? snapshot.apps.find((record) => record.applicationId === LOCAL_RUNTIME_DAEMON_APP_ID);
  const appProbeDetails = localRuntimeAppProbeDetails(app);
  const endpoint = endpointURL2(findRuntimeEndpoint(snapshot, LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE));
  if (!endpoint) return { available: false, ...appProbeDetails };
  try {
    const healthResponse = await fetch(`${endpoint}/health`, { signal: AbortSignal.timeout(1e3) });
    const status = healthResponse.ok ? await fetch(`${endpoint}/status`, { signal: AbortSignal.timeout(1e3) }).then((item) => item.ok ? item.json() : {}).catch(() => ({})) : {};
    return {
      available: healthResponse.ok,
      endpoint,
      ...healthResponse.ok ? await healthResponse.json() : { error: `HTTP ${healthResponse.status}` },
      ...status
    };
  } catch (error) {
    return { available: false, endpoint, ...appProbeDetails, error: errorMessage3(error) };
  }
}
async function stopLocalRuntimeDaemon(homeDir, options = {}) {
  const probe = await probeLocalRuntimeDaemon(homeDir);
  if (probe.available) return await localRuntimeControlRequest(homeDir, "POST", "/shutdown");
  if (!options.force) return { status: "not_running", ...probe };
  const app = findRuntimeApp(readRuntimeHomeSnapshot(homeDir), LOCAL_RUNTIME_DAEMON_APP_ID);
  if (app?.pid && app.pid !== process.pid) {
    try {
      process.kill(app.pid, "SIGTERM");
      return { status: "stopping", forced: true, pid: app.pid };
    } catch (error) {
      return { status: "error", forced: true, pid: app.pid, error: errorMessage3(error) };
    }
  }
  return { status: "not_running", forced: true };
}
async function localRuntimeControlRequest(homeDir, method, path) {
  const snapshot = readRuntimeHomeSnapshot(homeDir);
  const endpoint = endpointURL2(findRuntimeEndpoint(snapshot, LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE));
  if (!endpoint) throw new Error("MovScript local runtime daemon control endpoint was not found");
  const response = await fetch(`${endpoint}${path}`, { method, signal: AbortSignal.timeout(3e3) });
  const payload = await response.json().catch(() => ({}));
  if (!response.ok) throw new Error(`local runtime daemon ${path} failed with HTTP ${response.status}: ${JSON.stringify(payload)}`);
  return payload;
}
function localRuntimeServicesReady(status) {
  return missingLocalRuntimeServices(status).length === 0;
}
function missingLocalRuntimeServices(status) {
  const requiredServices = status.dataPlane === "local" ? [...REQUIRED_LOCAL_RUNTIME_DAEMON_SERVICES, LOCAL_DATA_SERVICE] : REQUIRED_LOCAL_RUNTIME_DAEMON_SERVICES;
  if (!Array.isArray(status.services)) return [...requiredServices];
  const readyServices = new Set(status.services.flatMap((item) => {
    if (!item || typeof item !== "object") return [];
    const record = item;
    return record.ready === true && typeof record.serviceName === "string" ? [record.serviceName] : [];
  }));
  return requiredServices.filter((serviceName) => !readyServices.has(serviceName));
}
function localRuntimeReadinessSummary(status, options) {
  const details = [];
  if (status.endpoint) details.push(`controlEndpoint=${status.endpoint}`);
  details.push(`available=${status.available}`);
  if (typeof status.status === "string") details.push(`status=${status.status}`);
  if (typeof status.error === "string") details.push(`error=${status.error}`);
  if (typeof status.pid === "number") details.push(`pid=${status.pid}`);
  if (typeof status.dataPlane === "string") details.push(`dataPlane=${status.dataPlane}`);
  if (localRuntimeHasIdentityDetails(status) && !localRuntimeMatchesIdentity(status, options.identity)) {
    details.push(`identityMismatch=${JSON.stringify({
      expected: options.identity ?? {},
      actual: {
        pluginVersion: status.pluginVersion,
        pluginRoot: status.pluginRoot,
        runtimeVersion: status.runtimeVersion,
        runtimeRoot: status.runtimeRoot
      }
    })}`);
  }
  if (!localRuntimeMatchesRequestedDataPlane(status, options.env)) {
    details.push(`dataPlaneMismatch=expected ${requestedLocalRuntimeDataPlane(options.env) ?? "any"}`);
  }
  if (!localRuntimeMatchesRequestedDataServiceURL(status, options.env)) {
    details.push("dataServiceURLMismatch=true");
  }
  const missingServices = missingLocalRuntimeServices(status);
  if (missingServices.length > 0) details.push(`missingServices=${missingServices.join(",")}`);
  return details.join("; ") || "last probe was empty";
}
function localRuntimeHasIdentityDetails(status) {
  return typeof status.pluginVersion === "string" || typeof status.pluginRoot === "string" || typeof status.runtimeVersion === "string" || typeof status.runtimeRoot === "string";
}
function localRuntimeAppProbeDetails(app) {
  if (!app) return {};
  const metadata = app.raw.metadata && typeof app.raw.metadata === "object" ? app.raw.metadata : {};
  return {
    status: app.status,
    pid: app.pid,
    pluginVersion: metadata.pluginVersion,
    pluginRoot: metadata.pluginRoot,
    runtimeVersion: metadata.runtimeVersion,
    runtimeRoot: metadata.runtimeRoot,
    dataPlane: metadata.dataPlane,
    dataServiceURL: metadata.dataServiceURL,
    ...typeof metadata.error === "string" ? { error: metadata.error } : {}
  };
}
function localRuntimeMatchesIdentity(status, identity) {
  if (!identity) return true;
  if (identity.pluginVersion && status.pluginVersion !== identity.pluginVersion) return false;
  if (identity.pluginRoot && status.pluginRoot !== identity.pluginRoot) return false;
  if (identity.runtimeVersion && status.runtimeVersion !== identity.runtimeVersion) return false;
  if (identity.runtimeRoot && status.runtimeRoot !== identity.runtimeRoot) return false;
  return true;
}
function localRuntimeMatchesRequestedDataPlane(status, env) {
  const expected = requestedLocalRuntimeDataPlane(env);
  if (!expected) return true;
  return status.dataPlane === expected;
}
function localRuntimeMatchesRequestedDataServiceURL(status, env) {
  const expected = normalizeDataServiceURL(env?.MOVSCRIPT_DATA_SERVICE_URL);
  if (!expected) return true;
  return normalizeDataServiceURL(status.dataServiceURL) === expected;
}
function localRuntimeMatchesRequest(status, options) {
  return localRuntimeMatchesIdentity(status, options.identity) && localRuntimeMatchesRequestedDataPlane(status, options.env) && localRuntimeMatchesRequestedDataServiceURL(status, options.env);
}
function requestedLocalRuntimeDataPlane(env) {
  const explicit = (env?.MOVSCRIPT_LOCAL_DAEMON_DATA_PLANE ?? env?.MOVSCRIPT_LOCAL_NODE_DATA_PLANE ?? "").trim().toLowerCase();
  if (explicit === "local" || explicit === "cloud" || explicit === "external") return explicit;
  return void 0;
}
function normalizeDataServiceURL(value) {
  if (typeof value !== "string") return void 0;
  const raw = value.trim();
  if (!raw) return void 0;
  try {
    const url = new URL(raw);
    url.hash = "";
    url.pathname = url.pathname.replace(/\/+$/, "") || "/";
    return url.toString().replace(/\/$/, "");
  } catch {
    return raw.replace(/\/+$/, "");
  }
}
async function waitForLocalRuntimeStop(homeDir, timeoutMs) {
  const startedAt = Date.now();
  while (Date.now() - startedAt < timeoutMs) {
    const probe = await probeLocalRuntimeDaemon(homeDir);
    if (!probe.available) return;
    await delay(150);
  }
}
async function stopAvailableLocalRuntimeDaemon(homeDir, timeoutMs) {
  await stopLocalRuntimeDaemon(homeDir).catch(() => void 0);
  await waitForLocalRuntimeStop(homeDir, timeoutMs);
  const probe = await probeLocalRuntimeDaemon(homeDir);
  if (probe.available) {
    throw new Error("MovScript local runtime daemon is still reachable after shutdown; refusing to start another daemon");
  }
}
async function waitForLocalRuntimeReady(homeDir, timeoutMs, options) {
  const startedAt = Date.now();
  while (Date.now() - startedAt < timeoutMs) {
    const probe = await probeLocalRuntimeDaemon(homeDir);
    if (probe.available && localRuntimeServicesReady(probe) && localRuntimeMatchesRequest(probe, options)) return probe;
    await delay(150);
  }
  return void 0;
}
async function acquireLocalRuntimeStartupLock(homeDir, timeoutMs) {
  const lockDir = join3(homeDir, "runtime", "locks", `${LOCAL_RUNTIME_DAEMON_APP_ID}.startup.lock`);
  const metadataPath = join3(lockDir, "owner.json");
  const startedAt = Date.now();
  while (Date.now() - startedAt < timeoutMs) {
    mkdirSync2(dirname2(lockDir), { recursive: true });
    try {
      mkdirSync2(lockDir);
      writeFileSync2(metadataPath, JSON.stringify({
        pid: process.pid,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      }), "utf8");
      return {
        release: () => {
          rmSync(lockDir, { recursive: true, force: true });
        }
      };
    } catch (error) {
      const code = typeof error === "object" && error ? error.code : void 0;
      if (code !== "EEXIST") throw error;
      if (isStaleStartupLock(metadataPath)) {
        rmSync(lockDir, { recursive: true, force: true });
        continue;
      }
      await delay(150);
    }
  }
  return void 0;
}
function isStaleStartupLock(metadataPath) {
  if (!existsSync2(metadataPath)) return false;
  try {
    const metadata = JSON.parse(readFileSync2(metadataPath, "utf8"));
    const pid = typeof metadata.pid === "number" ? metadata.pid : void 0;
    return !pidIsAlive(pid);
  } catch {
    return false;
  }
}
function endpointURL2(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}
function delay(ms) {
  return new Promise((resolveDelay) => setTimeout(resolveDelay, ms));
}
function errorMessage3(error) {
  return error instanceof Error ? error.message : String(error);
}

// ../../packages/mcp-host/dist/programManifest.js
var mcpHostProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "mcp-host",
  serviceName: "movscript.mcp.host",
  kind: "mcp-endpoint",
  name: "MovScript MCP Host",
  profiles: ["desktop", "plugin", "cloud", "test"],
  entry: {
    command: "movscript-mcp-host",
    args: ["--stdio"]
  },
  transport: "stdio",
  health: {
    kind: "stdio_tool",
    target: "movscript_runtime_status"
  },
  provides: ["mcp-tools", "runtime-status", "capability-gating"]
};

// ../../packages/mcp-host/dist/stdio.js
import readline from "readline";
import { existsSync as existsSync4, readdirSync as readdirSync2, readFileSync as readFileSync5 } from "fs";
import { basename as basename3, resolve as resolve8 } from "path";

// ../../packages/core/dist/chunk-HMBFHB33.js
var MOVSCRIPT_WORKSPACE_MANIFEST_FILE_NAME = "manifest.json";
var MOVSCRIPT_WORKSPACE_CONFIG_TOML_FILE_NAME = "config.toml";
var MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA = "movscript.project-workspace.v1";
var MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME = "providers";
var MOVSCRIPT_WORKSPACE_BACKEND_DIR_NAME = "backend";
var MOVSCRIPT_WORKSPACE_BIN_DIR_NAME = "bin";
var MOVSCRIPT_WORKSPACE_REALMS_DIR_NAME = "realms";

// ../../packages/core/dist/chunk-IW7ZDFIY.js
import { mkdirSync as mkdirSync3, readFileSync as readFileSync3, renameSync as renameSync2, writeFileSync as writeFileSync3 } from "fs";
import { dirname as dirname3, join as join4, resolve as resolve3, win32 as pathWin32 } from "path";
import { randomUUID } from "crypto";
function resolveMovScriptWorkspaceRootPaths(workspaceDir) {
  const rootDir = resolveMovScriptHomeDir2(workspaceDir);
  const controlDir = rootDir;
  const realmsDir = join4(controlDir, MOVSCRIPT_WORKSPACE_REALMS_DIR_NAME);
  return {
    workspaceDir: rootDir,
    rootDir,
    controlDir,
    configTomlPath: join4(controlDir, MOVSCRIPT_WORKSPACE_CONFIG_TOML_FILE_NAME),
    manifestPath: join4(controlDir, MOVSCRIPT_WORKSPACE_MANIFEST_FILE_NAME),
    realmsDir,
    providersDir: join4(realmsDir, "local", MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME),
    backendDir: join4(controlDir, MOVSCRIPT_WORKSPACE_BACKEND_DIR_NAME),
    binDir: join4(controlDir, MOVSCRIPT_WORKSPACE_BIN_DIR_NAME)
  };
}
function resolveMovScriptHomeDir2(workspaceDir) {
  const input = workspaceDir?.trim();
  if (input) return resolve3(input);
  const explicit = process.env.MOVSCRIPT_HOME?.trim();
  if (explicit) return resolve3(explicit);
  const legacy = process.env.MOVSCRIPT_WORKSPACE_DIR?.trim();
  if (legacy) return resolve3(legacy);
  return process.cwd();
}
function ensureMovScriptWorkspaceRoot(paths) {
  mkdirSync3(paths.controlDir, { recursive: true });
  mkdirSync3(paths.rootDir, { recursive: true });
  mkdirSync3(paths.realmsDir, { recursive: true });
  mkdirSync3(paths.providersDir, { recursive: true });
  mkdirSync3(paths.backendDir, { recursive: true });
  mkdirSync3(paths.binDir, { recursive: true });
  const current = readMovScriptWorkspaceRootManifest(paths.manifestPath);
  if (current) return current;
  const manifest = defaultMovScriptWorkspaceRootManifest();
  writeMovScriptWorkspaceRootManifest(paths.manifestPath, manifest);
  return manifest;
}
function defaultMovScriptWorkspaceRootManifest(now = /* @__PURE__ */ new Date()) {
  const timestamp = now.toISOString();
  return {
    schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA,
    workspaceId: randomUUID(),
    createdAt: timestamp,
    updatedAt: timestamp,
    layout: {
      providerConfigRoot: MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME
    }
  };
}
function readMovScriptWorkspaceRootManifest(manifestPath) {
  const parsed = readJSON2(manifestPath);
  if (!isRecord(parsed) || parsed.schema !== MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA) return void 0;
  const workspaceId = stringField3(parsed.workspaceId);
  if (!workspaceId) return void 0;
  const backend = normalizeBackend(parsed.backend);
  const activeRealm = normalizeRealmRecord(parsed.activeRealm);
  const activeUserId = numberField4(parsed.activeUserId);
  return {
    schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA,
    workspaceId,
    createdAt: stringField3(parsed.createdAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
    updatedAt: stringField3(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString(),
    ...backend ? { backend } : {},
    ...activeRealm ? { activeRealm } : {},
    ...activeUserId !== void 0 ? { activeUserId } : {},
    layout: normalizeWorkspaceLayout(parsed.layout)
  };
}
function writeMovScriptWorkspaceRootManifest(manifestPath, manifest) {
  writeJSONAtomic2(manifestPath, {
    ...manifest,
    schema: MOVSCRIPT_WORKSPACE_MANIFEST_SCHEMA,
    updatedAt: manifest.updatedAt || (/* @__PURE__ */ new Date()).toISOString(),
    layout: normalizeWorkspaceLayout(manifest.layout)
  });
}
function normalizeWorkspaceLayout(_value) {
  return {
    providerConfigRoot: MOVSCRIPT_WORKSPACE_PROVIDER_CONFIGS_DIR_NAME
  };
}
function normalizeBackend(value) {
  if (!isRecord(value)) return void 0;
  const kind = value.kind === "local" || value.kind === "cloud" || value.kind === "custom" ? value.kind : void 0;
  const baseURL = stringField3(value.baseURL);
  return kind || baseURL ? { ...kind ? { kind } : {}, ...baseURL ? { baseURL } : {} } : void 0;
}
function normalizeRealmRecord(value) {
  if (!isRecord(value)) return void 0;
  const kind = value.kind === "local" || value.kind === "cloud" ? value.kind : void 0;
  if (!kind) return void 0;
  if (kind === "local") return { kind, id: "local" };
  const id = stringField3(value.id);
  return id ? { kind, id: safeIdSegment(id) } : void 0;
}
function safeIdSegment(value) {
  const raw = String(value).trim();
  if (!/^[a-zA-Z0-9._-]{1,128}$/.test(raw) || raw === "." || raw === "..") {
    throw new Error(`invalid MovScript workspace id segment: ${String(value)}`);
  }
  return raw;
}
function readJSON2(filePath) {
  try {
    return JSON.parse(readFileSync3(filePath, "utf8"));
  } catch (error) {
    if (isNotFoundError(error)) return void 0;
    throw error;
  }
}
function writeJSONAtomic2(filePath, value) {
  mkdirSync3(dirname3(filePath), { recursive: true });
  const tmpPath = `${filePath}.${process.pid}.${Date.now()}.tmp`;
  writeFileSync3(tmpPath, `${JSON.stringify(value, null, 2)}
`, "utf8");
  renameSync2(tmpPath, filePath);
}
function stringField3(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberField4(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function isRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function isNotFoundError(error) {
  return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}

// ../../packages/core/dist/chunk-YOOKICW5.js
var BackendHTTPError = class _BackendHTTPError extends Error {
  constructor(method, path, status, body, rawBody) {
    super(`Backend ${method} ${path} failed: HTTP ${status} ${backendErrorMessage(body, rawBody)}`);
    this.method = method;
    this.path = path;
    this.status = status;
    this.body = body;
    this.name = "BackendHTTPError";
  }
  method;
  path;
  status;
  body;
  static async fromResponse(method, path, res) {
    const rawBody = await res.text();
    return new _BackendHTTPError(method, path, res.status, parseJSONBody(rawBody), rawBody);
  }
  toJSON() {
    return normalizeBackendHTTPError(this.method, this.path, this.status, this.body);
  }
};
function errorData(error) {
  if (error instanceof BackendHTTPError) return error.toJSON();
  return void 0;
}
function normalizeBackendHTTPError(method, path, status, body) {
  const bodyRecord = isRecord2(body) ? body : void 0;
  return {
    type: "backend_http_error",
    method,
    path,
    status,
    ...bodyRecord ? { body: bodyRecord } : {},
    ...bodyRecord && typeof bodyRecord.code === "string" ? { code: bodyRecord.code } : {},
    ...bodyRecord && typeof bodyRecord.field === "string" ? { field: bodyRecord.field } : {},
    ...bodyRecord && Array.isArray(bodyRecord.allowed_values) ? { allowed_values: bodyRecord.allowed_values } : {},
    ...bodyRecord && isRecord2(bodyRecord.suggested_fix) ? { suggested_fix: bodyRecord.suggested_fix } : {},
    ...bodyRecord && Number.isInteger(bodyRecord.required_min) ? { required_min: bodyRecord.required_min } : {},
    ...bodyRecord && Number.isInteger(bodyRecord.allowed_max) ? { allowed_max: bodyRecord.allowed_max } : {},
    ...bodyRecord && Number.isInteger(bodyRecord.actual_count) ? { actual_count: bodyRecord.actual_count } : {},
    ...bodyRecord && isRecord2(bodyRecord.details) ? { details: bodyRecord.details } : {}
  };
}
function parseJSONBody(rawBody) {
  if (!rawBody.trim()) return void 0;
  try {
    return JSON.parse(rawBody);
  } catch {
    return rawBody;
  }
}
function backendErrorMessage(body, rawBody) {
  if (isRecord2(body) && typeof body.error === "string") return body.error;
  return rawBody;
}
function isRecord2(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/core/dist/chunk-OZEEBCLM.js
import { mkdirSync as mkdirSync4, readFileSync as readFileSync4, renameSync as renameSync3, rmSync as rmSync2, writeFileSync as writeFileSync4 } from "fs";
import { dirname as dirname4, join as join5 } from "path";
var MOVSCRIPT_BACKEND_CONFIG_SCHEMA = "movscript.backend-config.v1";
var MOVSCRIPT_BACKEND_AUTH_SCHEMA = "movscript.backend-auth.v1";
var MOVSCRIPT_BACKEND_CONFIG_FILE_NAME = "config.json";
var MOVSCRIPT_BACKEND_AUTH_FILE_NAME = "auth.json";
var MOVSCRIPT_DEFAULT_BACKEND_BASE_URL = "http://localhost:8766";
function resolveMovScriptBackendPaths(workspaceDir = process.cwd(), realm) {
  const root = resolveMovScriptWorkspaceRootPaths(workspaceDir);
  const backendRealmsDir = join5(root.backendDir, "realms");
  const authRealm = normalizeRealm(realm) ?? { kind: "local", id: "local" };
  return {
    workspaceDir: root.workspaceDir,
    controlDir: root.controlDir,
    backendDir: root.backendDir,
    backendRealmsDir,
    configPath: join5(root.backendDir, MOVSCRIPT_BACKEND_CONFIG_FILE_NAME),
    authPath: join5(backendRealmsDir, ...backendRealmPath(authRealm), MOVSCRIPT_BACKEND_AUTH_FILE_NAME)
  };
}
function defaultMovScriptBackendConfig(baseURL = MOVSCRIPT_DEFAULT_BACKEND_BASE_URL) {
  return {
    schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA,
    baseURL: normalizeBackendBaseURL(baseURL),
    updatedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
}
function readMovScriptBackendConfig(workspaceDir = process.cwd()) {
  const paths = resolveMovScriptBackendPaths(workspaceDir);
  const parsed = readJSON3(paths.configPath);
  if (!isRecord3(parsed) || parsed.schema !== MOVSCRIPT_BACKEND_CONFIG_SCHEMA) return defaultMovScriptBackendConfig();
  const baseURL = stringField4(parsed.baseURL) ?? MOVSCRIPT_DEFAULT_BACKEND_BASE_URL;
  const realm = normalizeRealm(parsed.realm);
  const activeUserId = idField(parsed.activeUserId);
  return {
    schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA,
    baseURL: normalizeBackendBaseURL(baseURL),
    ...realm ? { realm } : {},
    ...activeUserId !== void 0 ? { activeUserId } : {},
    updatedAt: stringField4(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString()
  };
}
function writeMovScriptBackendConfig(workspaceDir, config) {
  const paths = resolveMovScriptBackendPaths(workspaceDir);
  ensureMovScriptWorkspaceRoot(resolveMovScriptWorkspaceRootPaths(paths.workspaceDir));
  const next = {
    schema: MOVSCRIPT_BACKEND_CONFIG_SCHEMA,
    baseURL: normalizeBackendBaseURL(config.baseURL),
    ...normalizeRealm(config.realm) ? { realm: normalizeRealm(config.realm) } : {},
    ...config.activeUserId !== void 0 ? { activeUserId: config.activeUserId } : {},
    updatedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
  writeJSONAtomic3(paths.configPath, next);
  return next;
}
function readMovScriptBackendAuth(workspaceDir = process.cwd(), realmInput) {
  const config = readMovScriptBackendConfig(workspaceDir);
  const requestedRealm = normalizeRealm(realmInput) ?? config.realm;
  const paths = resolveMovScriptBackendPaths(workspaceDir, requestedRealm);
  const parsed = readJSON3(paths.authPath);
  if (!isRecord3(parsed) || parsed.schema !== MOVSCRIPT_BACKEND_AUTH_SCHEMA) return void 0;
  const token = stringField4(parsed.token);
  if (!token) return void 0;
  const tokenType = parsed.tokenType === "Bearer" ? "Bearer" : void 0;
  if (!tokenType) return void 0;
  const user = normalizeAuthUser(parsed.user);
  const gitCredential = normalizeGitCredential(parsed.gitCredential ?? parsed.git_credential);
  const realm = normalizeRealm(parsed.realm) ?? requestedRealm;
  return {
    schema: MOVSCRIPT_BACKEND_AUTH_SCHEMA,
    tokenType,
    token,
    ...realm ? { realm } : {},
    ...gitCredential ? { gitCredential } : {},
    ...user ? { user } : {},
    ...stringField4(parsed.expiresAt) ? { expiresAt: stringField4(parsed.expiresAt) } : {},
    updatedAt: stringField4(parsed.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString()
  };
}
function clearMovScriptBackendAuth(workspaceDir = process.cwd()) {
  const paths = resolveMovScriptBackendPaths(workspaceDir, readMovScriptBackendConfig(workspaceDir).realm);
  rmSync2(paths.authPath, { force: true });
}
function resolveMovScriptBackendSession(input = {}) {
  const root = resolveMovScriptBackendPaths(input.workspaceDir);
  const config = readMovScriptBackendConfig(root.workspaceDir);
  const realm = normalizeRealm(input.realm) ?? config.realm;
  const paths = resolveMovScriptBackendPaths(root.workspaceDir, realm);
  const auth = readMovScriptBackendAuth(paths.workspaceDir, realm);
  const token = stringField4(input.token) ?? stringField4(process.env.MOVCLI_TOKEN) ?? auth?.token;
  const userId = idField(input.userId) ?? idField(auth?.user?.id) ?? idField(config.activeUserId);
  const baseURL = normalizeBackendBaseURL(stringField4(input.server) ?? stringField4(process.env.MOVSCRIPT_API_BASE_URL) ?? config.baseURL);
  return {
    workspaceDir: paths.workspaceDir,
    baseURL,
    apiBaseURL: normalizeBackendAPIBaseURL(baseURL),
    ...token ? { token } : {},
    tokenType: "Bearer",
    ...userId !== void 0 ? { userId: String(userId) } : {},
    ...auth?.user ? { user: auth.user } : {},
    configPath: paths.configPath,
    authPath: paths.authPath
  };
}
function backendRealmPath(realm) {
  if (realm.kind === "local") return ["local"];
  return ["cloud", String(realm.id)];
}
function normalizeBackendBaseURL(value) {
  const trimmed = value.trim().replace(/\/+$/, "");
  const withoutAPI = trimmed.endsWith("/api/v1") ? trimmed.slice(0, -"/api/v1".length) : trimmed;
  return withoutAPI || MOVSCRIPT_DEFAULT_BACKEND_BASE_URL;
}
function normalizeBackendAPIBaseURL(value) {
  return `${normalizeBackendBaseURL(value)}/api/v1`;
}
function readJSON3(filePath) {
  try {
    return JSON.parse(readFileSync4(filePath, "utf8"));
  } catch (error) {
    if (isNotFoundError2(error)) return void 0;
    throw error;
  }
}
function writeJSONAtomic3(filePath, value, mode) {
  mkdirSync4(dirname4(filePath), { recursive: true });
  const tmpPath = `${filePath}.${process.pid}.${Date.now()}.tmp`;
  writeFileSync4(tmpPath, `${JSON.stringify(value, null, 2)}
`, { encoding: "utf8", mode });
  renameSync3(tmpPath, filePath);
}
function normalizeAuthUser(value) {
  if (!isRecord3(value)) return void 0;
  const id = idField(value.id);
  const username = stringField4(value.username);
  const displayName2 = stringField4(value.displayName) ?? stringField4(value.display_name);
  const primaryEmail = stringField4(value.primaryEmail) ?? stringField4(value.primary_email);
  const locale = stringField4(value.locale);
  const systemRole = stringField4(value.systemRole) ?? stringField4(value.system_role);
  const user = {
    ...id !== void 0 ? { id } : {},
    ...username ? { username } : {},
    ...displayName2 ? { displayName: displayName2 } : {},
    ...primaryEmail ? { primaryEmail } : {},
    ...locale ? { locale } : {},
    ...systemRole ? { systemRole } : {}
  };
  return Object.keys(user).length > 0 ? user : void 0;
}
function normalizeGitCredential(value) {
  if (!isRecord3(value)) return void 0;
  const provider = stringField4(value.provider);
  const username = stringField4(value.username);
  if (provider !== "gitea" || !username) return void 0;
  const token = stringField4(value.token);
  const maskedToken = stringField4(value.maskedToken) ?? stringField4(value.masked_token);
  const status = stringField4(value.status);
  const lastError = stringField4(value.lastError) ?? stringField4(value.last_error);
  return {
    provider,
    username,
    ...token ? { token } : {},
    ...maskedToken ? { maskedToken } : {},
    ...status ? { status } : {},
    ...lastError ? { lastError } : {}
  };
}
function normalizeRealm(value) {
  if (!isRecord3(value)) return void 0;
  const kind = value.kind === "local" || value.kind === "cloud" ? value.kind : void 0;
  if (!kind) return void 0;
  if (kind === "local") return { kind, id: "local" };
  const id = idField(value.id);
  return id ? { kind, id: String(id) } : void 0;
}
function stringField4(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function idField(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function isRecord3(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function isNotFoundError2(error) {
  return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
var backendAPIBaseURL = normalizeBackendAPIBaseURL(defaultMovScriptBackendBaseURL());
var backendRuntimeAuthToken = "";
var configuredBackendDefaultWorkspaceDir;
function setMovScriptBackendAPIBaseURL(next) {
  backendAPIBaseURL = normalizeBackendAPIBaseURL(next);
}
function getMovScriptBackendAPIBaseURL() {
  return backendAPIBaseURL;
}
function setMovScriptBackendRuntimeAuthToken(token) {
  backendRuntimeAuthToken = token?.trim() ?? "";
}
function getMovScriptBackendRuntimeAuthToken() {
  return backendRuntimeAuthToken;
}
function setMovScriptBackendDefaultWorkspaceDir(workspaceDir) {
  configuredBackendDefaultWorkspaceDir = workspaceDir?.trim() || void 0;
}
function resolveMovScriptBackendDefaultWorkspaceDir() {
  return configuredBackendDefaultWorkspaceDir || process.env.MOVSCRIPT_HOME || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd();
}
function defaultMovScriptBackendBaseURL() {
  return typeof process !== "undefined" ? process.env.MOVSCRIPT_API_BASE_URL || MOVSCRIPT_DEFAULT_BACKEND_BASE_URL : MOVSCRIPT_DEFAULT_BACKEND_BASE_URL;
}
async function backendGet2(path) {
  const headers = backendHeaders2();
  const res = await fetch(`${getMovScriptBackendAPIBaseURL()}${path}`, { headers });
  if (!res.ok) {
    throw await BackendHTTPError.fromResponse("GET", path, res);
  }
  return res.json();
}
async function backendGetBinary(path, options = {}) {
  const headers = backendHeaders2();
  const res = await fetch(`${getMovScriptBackendAPIBaseURL()}${path}`, { headers });
  if (!res.ok) {
    throw await BackendHTTPError.fromResponse("GET", path, res);
  }
  const contentLengthHeader = res.headers.get("content-length");
  const contentLength = contentLengthHeader === null ? NaN : Number(contentLengthHeader);
  if (options.maxBytes !== void 0 && Number.isFinite(contentLength) && contentLength > options.maxBytes) {
    throw new Error(`backend GET ${path} returned content-length ${contentLength}, above maxBytes=${options.maxBytes}`);
  }
  const bytes = await readBinaryResponse(path, res, {
    maxBytes: options.maxBytes,
    contentLength: Number.isFinite(contentLength) ? contentLength : void 0,
    onProgress: options.onProgress
  });
  if (options.maxBytes !== void 0 && bytes.length > options.maxBytes) {
    throw new Error(`backend GET ${path} returned ${bytes.length} bytes, above maxBytes=${options.maxBytes}`);
  }
  return {
    bytes,
    ...res.headers.get("content-type") ? { contentType: res.headers.get("content-type") ?? void 0 } : {},
    ...Number.isFinite(contentLength) ? { contentLength } : {}
  };
}
async function readBinaryResponse(path, res, options) {
  if (!res.body) {
    const bytes2 = Buffer.from(await res.arrayBuffer());
    options.onProgress?.({
      path,
      receivedBytes: bytes2.length,
      ...options.contentLength !== void 0 ? { totalBytes: options.contentLength } : {},
      done: true
    });
    return bytes2;
  }
  const reader = res.body.getReader();
  const chunks = [];
  let receivedBytes = 0;
  for (; ; ) {
    const { done, value } = await reader.read();
    if (done) break;
    if (!value) continue;
    receivedBytes += value.byteLength;
    if (options.maxBytes !== void 0 && receivedBytes > options.maxBytes) {
      throw new Error(`backend GET ${path} returned more than maxBytes=${options.maxBytes}`);
    }
    chunks.push(value);
    options.onProgress?.({
      path,
      receivedBytes,
      ...options.contentLength !== void 0 ? { totalBytes: options.contentLength } : {},
      done: false
    });
  }
  const bytes = Buffer.concat(chunks.map((chunk) => Buffer.from(chunk)));
  options.onProgress?.({
    path,
    receivedBytes,
    ...options.contentLength !== void 0 ? { totalBytes: options.contentLength } : {},
    done: true
  });
  return bytes;
}
async function backendPost2(path, body, userId) {
  const headers = backendHeaders2({ json: true, userId });
  const res = await fetch(`${getMovScriptBackendAPIBaseURL()}${path}`, {
    method: "POST",
    headers,
    body: JSON.stringify(body)
  });
  if (!res.ok) {
    throw await BackendHTTPError.fromResponse("POST", path, res);
  }
  return res.json();
}
async function backendPut(path, body, userId) {
  const headers = backendHeaders2({ json: true, userId });
  const res = await fetch(`${getMovScriptBackendAPIBaseURL()}${path}`, {
    method: "PUT",
    headers,
    body: JSON.stringify(body)
  });
  if (!res.ok) {
    throw await BackendHTTPError.fromResponse("PUT", path, res);
  }
  const text = await res.text();
  return text.trim() ? JSON.parse(text) : null;
}
async function backendPostMultipart(path, form, userId) {
  const headers = backendHeaders2({ userId });
  const res = await fetch(`${getMovScriptBackendAPIBaseURL()}${path}`, {
    method: "POST",
    headers,
    body: form
  });
  if (!res.ok) {
    throw await BackendHTTPError.fromResponse("POST", path, res);
  }
  return res.json();
}
async function backendPatch(path, body, userId) {
  const headers = backendHeaders2({ json: true, userId });
  const res = await fetch(`${getMovScriptBackendAPIBaseURL()}${path}`, {
    method: "PATCH",
    headers,
    body: JSON.stringify(body)
  });
  if (!res.ok) {
    throw await BackendHTTPError.fromResponse("PATCH", path, res);
  }
  const text = await res.text();
  return text.trim() ? JSON.parse(text) : null;
}
async function backendDelete(path, userId) {
  const headers = backendHeaders2({ userId });
  const res = await fetch(`${getMovScriptBackendAPIBaseURL()}${path}`, {
    method: "DELETE",
    headers
  });
  if (!res.ok) {
    throw await BackendHTTPError.fromResponse("DELETE", path, res);
  }
  const text = await res.text();
  return text.trim() ? JSON.parse(text) : null;
}
async function backendList(path) {
  const data = await backendGet2(path);
  if (Array.isArray(data)) return data;
  if (isRecord22(data) && Array.isArray(data.items)) return data.items;
  return [];
}
function backendHeaders2(input = {}) {
  const headers = input.json ? { "Content-Type": "application/json" } : {};
  const authToken = getMovScriptBackendRuntimeAuthToken() || workspaceBackendAuthToken();
  if (authToken) headers.Authorization = `Bearer ${authToken}`;
  const userId = typeof input.userId === "number" || typeof input.userId === "string" ? String(input.userId) : workspaceBackendUserId();
  if (userId) headers["X-User-ID"] = userId;
  return headers;
}
function workspaceBackendAuthToken() {
  try {
    return readMovScriptBackendAuth(resolveMovScriptBackendDefaultWorkspaceDir())?.token ?? "";
  } catch {
    return "";
  }
}
function workspaceBackendUserId() {
  try {
    return resolveMovScriptBackendSession({ workspaceDir: resolveMovScriptBackendDefaultWorkspaceDir() }).userId ?? "";
  } catch {
    return "";
  }
}
function isRecord22(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/core/dist/chunk-EFWC2V2I.js
function isRecord4(value) {
  return !!value && typeof value === "object" && !Array.isArray(value);
}
function stringValue5(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function toMCPJSONValue(value) {
  if (value === null || typeof value === "string" || typeof value === "number" || typeof value === "boolean") return value;
  if (Array.isArray(value)) return value.map(toMCPJSONValue);
  if (!isRecord4(value)) return String(value);
  const obj = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) obj[key] = toMCPJSONValue(item);
  }
  return obj;
}
function renderMarkdown(value) {
  if (value === null || value === void 0) return "\u65E0\u6570\u636E\u3002";
  if (typeof value !== "object") return String(value);
  if (Array.isArray(value)) return renderMarkdownArray(value);
  if (isRecord4(value) && Array.isArray(value.projects)) {
    const lines = ["## \u9879\u76EE\u5217\u8868"];
    if (value.projects.length === 0) {
      lines.push("\u6CA1\u6709\u53EF\u89C1\u9879\u76EE\u3002");
    } else {
      lines.push(...value.projects.map((project, index) => formatProjectLine(project, index)));
    }
    if (typeof value.count === "number") lines.push("", `\u5171 ${value.count} \u4E2A\u9879\u76EE\u3002`);
    return lines.join("\n");
  }
  if (isRecord4(value) && isRecord4(value.snapshot)) {
    const lines = ["## \u5F53\u524D\u4E0A\u4E0B\u6587"];
    lines.push(renderMarkdownObject(value.snapshot));
    if (Array.isArray(value.projects)) {
      lines.push("", "## \u9879\u76EE\u5217\u8868");
      if (value.projects.length === 0) {
        lines.push(typeof value.projectsError === "string" ? `\u9879\u76EE\u5217\u8868\u4E0D\u53EF\u7528\uFF1A${value.projectsError}` : "\u6CA1\u6709\u53EF\u89C1\u9879\u76EE\u3002");
      } else {
        lines.push(...value.projects.map((project, index) => formatProjectLine(project, index)));
      }
    }
    return lines.join("\n");
  }
  return renderMarkdownObject(value);
}
function renderMarkdownArray(items) {
  if (items.length === 0) return "\u6CA1\u6709\u6761\u76EE\u3002";
  return items.map((item, index) => `${index + 1}. ${renderInlineMarkdownValue(item)}`).join("\n");
}
function renderMarkdownObject(value) {
  const lines = [];
  for (const [key, item] of Object.entries(value)) {
    if (item === void 0) continue;
    if (Array.isArray(item)) {
      lines.push(`### ${key}`);
      lines.push(renderMarkdownArray(item));
    } else if (isRecord4(item)) {
      lines.push(`### ${key}`);
      lines.push(renderMarkdownObject(item));
    } else {
      lines.push(`- ${key}: ${renderInlineMarkdownValue(item)}`);
    }
  }
  return lines.length > 0 ? lines.join("\n") : "\u65E0\u6570\u636E\u3002";
}
function formatProjectLine(project, index) {
  if (!isRecord4(project)) return `${index + 1}. \u9879\u76EE${index + 1}\u7684\u540D\u5B57${String(project)}`;
  const name = typeof project.name === "string" && project.name.trim() ? project.name.trim() : `\u672A\u547D\u540D\u9879\u76EE ${index + 1}`;
  const details = [
    typeof project.description === "string" && project.description.trim() ? project.description.trim() : void 0,
    typeof project.totalEpisodes === "number" ? `\u96C6\u6570\uFF1A${project.totalEpisodes}` : void 0
  ].filter(Boolean).join("\uFF1B");
  const id = typeof project.id === "number" ? `\uFF08project#${project.id}\uFF09` : "";
  return `${index + 1}. \u9879\u76EE${index + 1}\u7684\u540D\u5B57${name}${details ? `\uFF0C${details}` : ""}${id}`;
}
function renderInlineMarkdownValue(value) {
  if (value === null || value === void 0) return "\u65E0";
  if (typeof value !== "object") return String(value);
  if (Array.isArray(value)) return value.length === 0 ? "\u65E0" : value.map(renderInlineMarkdownValue).join("\uFF1B");
  if (isRecord4(value)) {
    return Object.entries(value).filter(([, item]) => item !== void 0).map(([key, item]) => `${key}=${renderInlineMarkdownValue(item)}`).join("\uFF0C");
  }
  return String(value);
}
function toolText(value, displayValue = value) {
  return {
    content: [
      {
        type: "text",
        text: renderMarkdown(displayValue ?? null)
      }
    ],
    data: toMCPJSONValue(value ?? null)
  };
}
function resourceContent(uri, value) {
  return {
    contents: [
      {
        uri,
        mimeType: "text/markdown",
        text: renderMarkdown(value ?? null)
      }
    ],
    data: toMCPJSONValue(value ?? null)
  };
}
function getStringParam(params, key) {
  const obj = getObject(params);
  const value = obj[key];
  if (typeof value !== "string" || value.length === 0) throw new Error(`${key} is required`);
  return value;
}
function getObjectParam(params, key) {
  const obj = getObject(params);
  const value = obj[key];
  return isRecord4(value) ? value : {};
}
function getObject(value) {
  if (!isRecord4(value)) return {};
  return value;
}
function objectSchema(properties, requiredOrOptions) {
  const options = Array.isArray(requiredOrOptions) ? { required: requiredOrOptions } : requiredOrOptions;
  return {
    type: "object",
    properties,
    ...options?.required !== void 0 ? { required: options.required } : {},
    additionalProperties: false
  };
}
function getOptionalString(args, key) {
  const value = args[key];
  return textOrUndefined(value);
}
function getOptionalNumeric(args, key) {
  return numericValue(args[key]);
}
function numericValue(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    return Number.isFinite(parsed) ? parsed : void 0;
  }
  return void 0;
}
function numericValues(value) {
  if (Array.isArray(value)) {
    const values = value.map(numericValue);
    return values.every((item) => item !== void 0) ? values : void 0;
  }
  const single = numericValue(value);
  return single === void 0 ? void 0 : [single];
}
function textOrUndefined(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function clampNumber(value, min, max) {
  return Math.max(min, Math.min(max, value));
}
function artifactTools() {
  return [
    {
      name: "system_artifact_upload_export",
      description: "Upload a completed local export artifact as a neutral RawResource. This hosts an existing file only; it does not render media or write domain decisions. When resolving a completed Electron task by taskId, pass projectId for persisted workspace recovery. HLS manifests must use system_artifact_upload_hls_stream so manifest and segment files are hosted as a MediaStreamArtifact instead of one RawResource.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task output recovery." },
        project_id: { type: "string", description: "Alias for projectId." },
        outputPath: { type: "string" },
        output_path: { type: "string" },
        filePath: { type: "string" },
        file_path: { type: "string" },
        filename: { type: "string" },
        mimeType: { type: "string" },
        mime_type: { type: "string" },
        folderId: { type: ["string", "number"] },
        folder_id: { type: ["string", "number"] },
        derivative: { type: "object" },
        operation: { type: "string" },
        tool: { type: "string" },
        inputResourceIds: { type: "array", items: { type: ["string", "number"] } },
        input_resource_ids: { type: "array", items: { type: ["string", "number"] } },
        sourceResourceId: { type: ["string", "number"] },
        source_resource_id: { type: ["string", "number"] },
        sourceResourceIds: { type: "array", items: { type: ["string", "number"] } },
        source_resource_ids: { type: "array", items: { type: ["string", "number"] } },
        params: { type: "object" }
      })
    },
    {
      name: "system_artifact_upload_hls_stream",
      description: "Upload completed HLS manifest/segments as a neutral MediaStreamArtifact. This hosts an existing artifact only; it does not render media or write domain decisions. When resolving a completed Electron HLS task by taskId, pass projectId for persisted workspace output recovery.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        manifestPath: { type: "string" },
        manifest_path: { type: "string" },
        segmentPaths: { type: "array", items: { type: "string" } },
        segment_paths: { type: "array", items: { type: "string" } },
        title: { type: "string" },
        projectId: { type: ["string", "number"] },
        project_id: { type: ["string", "number"] },
        sourceResourceId: { type: ["string", "number"] },
        source_resource_id: { type: ["string", "number"] },
        sourceDerivativeId: { type: ["string", "number"] },
        source_derivative_id: { type: ["string", "number"] },
        durationMs: { type: "number" },
        duration_ms: { type: "number" },
        width: { type: "number" },
        height: { type: "number" },
        expiresAt: { type: "string" },
        expires_at: { type: "string" },
        expiresInSeconds: { type: "number" },
        expires_in_seconds: { type: "number" }
      })
    },
    {
      name: "system_artifact_get_stream",
      description: "Read MediaStreamArtifact metadata and playback URLs from backend hosting. This does not inspect or reinterpret the source timeline.",
      inputSchema: objectSchema({
        streamId: { type: ["string", "number"] },
        stream_id: { type: ["string", "number"] },
        id: { type: ["string", "number"] }
      })
    }
  ];
}
function normalizeModelCapabilityAlias(value) {
  const normalized = value?.trim().toLowerCase().replace(/-/g, "_");
  switch (normalized) {
    case "text":
    case "text_generation":
    case "reasoning":
    case "image":
    case "image_generation":
    case "image_edit":
    case "video":
    case "video_generation":
    case "video_i2v":
    case "video_v2v":
    case "audio":
    case "audio_generation":
    case "audio_tts":
    case "audio_transcribe":
    case "audio_translate":
    case "audio_music":
    case "audio_sfx":
    case "audio_chat":
    case "voice_clone":
    case "voice_design":
    case "subtitle_align":
    case "subtitle_translate":
      return normalized;
    case "tts":
      return "audio_tts";
    case "speech_translate":
      return "audio_translate";
    case "stt":
      return "audio_transcribe";
    case "music":
      return "audio_music";
    case "sfx":
      return "audio_sfx";
    default:
      return void 0;
  }
}
function copyFiniteNumber(out, source, sourceKey, targetKey = sourceKey) {
  const value = source[sourceKey];
  if (typeof value === "number" && Number.isFinite(value)) out[targetKey] = value;
}
function isJSONScalar(value) {
  return typeof value === "string" || typeof value === "number" || typeof value === "boolean";
}
function numericModelField(source, key) {
  const value = source[key];
  const parsed = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
  return Number.isFinite(parsed) && parsed > 0 ? parsed : void 0;
}
function stringModelField(source, key) {
  const value = source[key];
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function integerModelField(source, key, min, fallback) {
  const value = source[key];
  const parsed = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
  if (!Number.isInteger(parsed) || parsed < min) return fallback;
  return parsed;
}
function stringArrayModelField(value) {
  if (!Array.isArray(value)) return [];
  return Array.from(new Set(value.flatMap((item) => typeof item === "string" && item.trim() ? [item.trim()] : [])));
}
function summarizeInputRequirementsForAgent(value) {
  const source = isRecord4(value) ? value : {};
  return {
    image: summarizeInputRequirementForAgent(source.image),
    video: summarizeInputRequirementForAgent(source.video)
  };
}
function summarizeInputRequirementForAgent(value) {
  const source = isRecord4(value) ? value : {};
  const min = integerModelField(source, "min", 0, 0);
  const max = integerModelField(source, "max", -1, 0);
  if (max !== -1 && min > max) return { min: 0, max: 0 };
  return {
    min,
    max
  };
}
function summarizeSupportedParamsForAgent(supportedParams, schema) {
  const properties = isRecord4(schema?.properties) ? schema.properties : void 0;
  const params = supportedParams.map((param) => summarizeSupportedParamDefForAgent(param, properties)).filter((param) => !!param);
  if (params.length > 0) return params;
  if (!properties) return [];
  return Object.entries(properties).map(([key, property]) => summarizeSchemaPropertyForAgent(key, property)).filter((param) => !!param);
}
function summarizeSupportedParamDefForAgent(param, schemaProperties) {
  if (!isRecord4(param) || typeof param.key !== "string" || !param.key.trim()) return void 0;
  const out = { key: param.key.trim() };
  if (typeof param.label === "string" && param.label.trim()) out.label = param.label.trim();
  if (typeof param.type === "string" && param.type.trim()) out.type = param.type.trim();
  if (Array.isArray(param.options)) {
    const options = stringArrayModelField(param.options);
    if (options.length > 0) out.options = options;
  }
  if (param.default !== void 0) out.default = param.default;
  copyFiniteNumber(out, param, "min");
  copyFiniteNumber(out, param, "max");
  copyFiniteNumber(out, param, "step");
  copyStringArray(out, param, "conflicts_with");
  copyConditionalEnumRules(out, param);
  copyConditionalConstRules(out, param);
  copyRequiresValueRules(out, param);
  mergeSchemaPropertySummary(out, schemaProperties?.[out.key]);
  return out;
}
function summarizeSchemaPropertyForAgent(key, property) {
  const trimmedKey = key.trim();
  if (!trimmedKey || !isRecord4(property)) return void 0;
  const out = { key: trimmedKey };
  if (typeof property.type === "string" && property.type.trim()) out.type = property.type.trim();
  copySchemaEnum(out, property);
  if (property.default !== void 0) out.default = property.default;
  copyFiniteNumber(out, property, "minimum", "min");
  copyFiniteNumber(out, property, "maximum", "max");
  copyFiniteNumber(out, property, "multipleOf", "step");
  if (typeof property.description === "string" && property.description.trim()) out.description = property.description.trim();
  return out;
}
function mergeSchemaPropertySummary(out, property) {
  if (!isRecord4(property)) return;
  copySchemaEnum(out, property);
  copyFiniteNumber(out, property, "minimum", "min");
  copyFiniteNumber(out, property, "maximum", "max");
  copyFiniteNumber(out, property, "multipleOf", "step");
  if (property.default !== void 0 && out.default === void 0) out.default = property.default;
  if (typeof property.description === "string" && property.description.trim()) out.description = property.description.trim();
}
function copySchemaEnum(out, property) {
  if (!Array.isArray(property.enum)) return;
  const values = property.enum.filter(isJSONScalar);
  if (values.length === 0) return;
  if (values.every((value) => typeof value === "string")) out.options = values;
  else out.enum = values;
}
function copyStringArray(out, source, key) {
  const value = source[key];
  if (!Array.isArray(value)) return;
  const items = stringArrayModelField(value);
  if (items.length > 0) out[key] = items;
}
function copyConditionalEnumRules(out, source) {
  const value = source.conditional_enum;
  if (!Array.isArray(value)) return;
  const rules = value.flatMap((item) => {
    if (!isRecord4(item)) return [];
    const whenParam = typeof item.when_param === "string" ? item.when_param.trim() : "";
    const options = Array.isArray(item.options) ? item.options.filter((option) => typeof option === "string") : [];
    if (!whenParam || !isJSONScalar(item.when_value) || options.length === 0) return [];
    return [{
      when_param: whenParam,
      when_value: item.when_value,
      options
    }];
  });
  if (rules.length > 0) out.conditional_enum = rules;
}
function copyConditionalConstRules(out, source) {
  const value = source.conditional_const;
  if (!Array.isArray(value)) return;
  const rules = value.flatMap((item) => {
    if (!isRecord4(item)) return [];
    const whenParam = typeof item.when_param === "string" ? item.when_param.trim() : "";
    if (!whenParam || !isJSONScalar(item.when_value) || !isJSONScalar(item.value)) return [];
    return [{
      when_param: whenParam,
      when_value: item.when_value,
      value: item.value
    }];
  });
  if (rules.length > 0) out.conditional_const = rules;
}
function copyRequiresValueRules(out, source) {
  const value = source.requires_value;
  if (!Array.isArray(value)) return;
  const rules = value.flatMap((item) => {
    if (!isRecord4(item)) return [];
    const param = typeof item.param === "string" ? item.param.trim() : "";
    if (!param || !isJSONScalar(item.value)) return [];
    return [{
      param,
      value: item.value
    }];
  });
  if (rules.length > 0) out.requires_value = rules;
}
function summarizeModelContractForAgent(model) {
  const source = isRecord4(model) ? model : {};
  const schema = isRecord4(source.params_schema) ? source.params_schema : void 0;
  const supportedParams = Array.isArray(source.supported_params) ? source.supported_params : [];
  const numericID = numericModelField(source, "id") ?? numericModelField(source, "ID");
  const supportedParamKeys = supportedParams.flatMap((param) => {
    if (!isRecord4(param) || typeof param.key !== "string" || !param.key.trim()) return [];
    return [param.key.trim()];
  });
  const propertyKeys = Object.keys(isRecord4(schema?.properties) ? schema.properties : {});
  return {
    contract_version: 1,
    model_id: stringModelField(source, "model_id") ?? stringModelField(source, "logical_model_id") ?? stringModelField(source, "model_def_id") ?? (numericID ? `backend.model.${numericID}` : "default"),
    ...typeof source.display_name === "string" && source.display_name.trim() ? { display_name: source.display_name.trim() } : {},
    ...typeof source.short_name === "string" && source.short_name.trim() ? { short_name: source.short_name.trim() } : {},
    ...typeof source.logical_model_id === "string" && source.logical_model_id.trim() ? { logical_model_id: source.logical_model_id.trim() } : {},
    capabilities: stringArrayModelField(source.capabilities),
    accepts_image_input: source.accepts_image_input === true,
    input_requirements: summarizeInputRequirementsForAgent(source.input_requirements),
    supported_params: summarizeSupportedParamsForAgent(supportedParams, schema),
    supported_param_keys: Array.from(new Set(supportedParamKeys.length > 0 ? supportedParamKeys : propertyKeys)).sort(),
    params_schema_loaded: !!schema,
    ...Array.isArray(schema?.["allOf"]) ? { params_schema_rule_count: schema["allOf"].length } : {}
  };
}
var workspaceLocator = {
  workspaceDir: { type: "string", description: "Optional MovScript workspace container directory. Defaults to the current MovScript workspace dir." },
  workspace_dir: { type: "string", description: "Alias for workspaceDir." },
  projectDir: { type: "string", description: "MovScript project source directory. Domain tools read and write this path directly." },
  project_dir: { type: "string", description: "Alias for projectDir." },
  projectPath: { type: "string", description: "Alias for projectDir." },
  project_path: { type: "string", description: "Alias for projectDir." },
  cwd: { type: "string", description: "Alias for projectDir when a provider passes the current project working directory." },
  projectUid: { type: "string", description: "Optional manifest project_uid used for scoped backend candidate metadata." },
  project_uid: { type: "string", description: "Alias for projectUid." }
};
var contentCandidateStatuses = ["queued", "running", "succeeded", "failed", "canceled", "imported"];
var entityQuery = {
  ...workspaceLocator,
  entityKind: { type: "string", description: "Optional semantic entity kind, for example setting, production, scene_moment, expression_unit, content_unit, storyboard, or keyframe." },
  entity_kind: { type: "string", description: "Alias for entityKind." },
  kind: { type: "string" },
  query: { type: "string" },
  q: { type: "string", description: "Alias for query." },
  productionId: { type: ["string", "number"] },
  production_id: { type: ["string", "number"] },
  segmentId: { type: ["string", "number"] },
  segment_id: { type: ["string", "number"] },
  sceneMomentId: { type: ["string", "number"] },
  scene_moment_id: { type: ["string", "number"] },
  expressionUnitId: { type: ["string", "number"] },
  expression_unit_id: { type: ["string", "number"] },
  storyboardId: { type: ["string", "number"] },
  storyboard_id: { type: ["string", "number"] },
  contentUnitId: { type: ["string", "number"] },
  content_unit_id: { type: ["string", "number"] },
  settingId: { type: ["string", "number"] },
  setting_id: { type: ["string", "number"] },
  settingStateId: { type: ["string", "number"] },
  setting_state_id: { type: ["string", "number"] },
  limit: { type: "number" }
};
var inspectOptions = {
  ...workspaceLocator,
  commit: { type: "string", description: "Optional git commit/ref to compare current source against. Defaults to HEAD when the workspace is in git." },
  checkpointHash: { type: "string", description: "Compatibility alias for commit." },
  checkpoint_hash: { type: "string", description: "Alias for checkpointHash." }
};
function domainTools() {
  return [
    {
      name: "domain_get_model",
      description: "Return the movscript-lang workspace model for one editable domain entity: concept, editable path hints, path/vocabulary semantics, context paths, schema ids, supported write APIs, and agent instructions. Call this before direct file edits. Paths are the canonical instance tree; namespace vocabulary supplies labels/templates, not a second structure source. This is project-scoped and does not write files.",
      inputSchema: projectSchema(
        {
          ...workspaceLocator,
          entityKind: { type: "string", description: "Domain entity kind, for example setting, asset, production, expression_unit, storyboard, content_unit, or keyframe." },
          entity_kind: { type: "string", description: "Alias for entityKind." },
          entityId: { type: ["string", "number"], description: "Optional entity id used to expand editable path hints." },
          entity_id: { type: ["string", "number"], description: "Alias for entityId." }
        },
        ["entityKind"]
      )
    },
    {
      name: "domain_query_entities",
      description: "Query indexed MovScript domain entities from source/current indexes by entity kind, ids, path context, or free text. Use this before reading many files.",
      inputSchema: projectSchema(entityQuery)
    },
    {
      name: "domain_query_settings",
      description: "Query MovScript setting entities: concrete film/music production entities to be made or reused, such as characters, props, places, instruments, costumes, or voice identities.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        settingId: { type: ["string", "number"] },
        setting_id: { type: ["string", "number"] },
        kind: { type: "string" },
        query: { type: "string" },
        q: { type: "string" },
        limit: { type: "number" }
      })
    },
    {
      name: "domain_query_assets",
      description: "Query MovScript setting-state-owned asset slots such as front view, side view, turnaround sheet, material reference, voice timbre, or instrument tone. Content-unit candidates are queried through domain candidate/decision APIs.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        assetId: { type: ["string", "number"] },
        asset_id: { type: ["string", "number"] },
        settingId: { type: ["string", "number"] },
        setting_id: { type: ["string", "number"] },
        settingStateId: { type: ["string", "number"] },
        setting_state_id: { type: ["string", "number"] },
        query: { type: "string" },
        q: { type: "string" },
        includeCandidates: { type: "boolean" },
        include_candidates: { type: "boolean" },
        limit: { type: "number" }
      })
    },
    {
      name: "domain_query_production_context",
      description: "Query production planning context: productions, segments, scene moments, storyboards, audio cues, expression units, content units, and candidate-bearing production slots.",
      inputSchema: projectSchema({
        ...entityQuery,
        include: {
          type: "array",
          items: { type: "string", enum: ["productions", "segments", "scene_moments", "storyboards", "audio_cues", "expression_units", "content_units", "keyframes"] }
        }
      })
    },
    {
      name: "domain_read_content_workspace",
      description: "Return the same content-source workspace view model used by the content workbench UI. Use this to inspect the page data source, selections, candidates, production hierarchy, preview timelines, and production work plan through the project engine.",
      inputSchema: projectSchema(workspaceLocator)
    },
    {
      name: "domain_read_content_workspace_snapshot",
      description: "Return the raw content-source workspace snapshot before UI normalization. Use this when debugging whether source, decision, interpreted, or preview-timeline inputs match expectations.",
      inputSchema: projectSchema(workspaceLocator)
    },
    {
      name: "domain_read_project_context_snapshot",
      description: "Read and compile project-wide standards plus namespace vocabulary into the stable project context snapshot agents should consult before planning, content work, or generation. This is read-only; use domain_upsert_project_standards only when the user explicitly asks to add, remove, or change standards.",
      inputSchema: projectSchema(workspaceLocator)
    },
    {
      name: "domain_derive_content_unit_artifact",
      description: "Derive the interpreter artifact bundle for a content unit, including runtime panel, generation prompt, dependency report, and selection validity. Use before generation or candidate selection when content-unit context may be stale.",
      inputSchema: projectSchema({ ...workspaceLocator, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_build_content_unit_backend_prompt",
      description: "Build a backend-ready prompt for a content unit by resolving prompt refs through backend decision selections. Rewrites selected upstream resources like {{asset:id}} to @[resource:id] and returns blockers when referenced content has not been produced or selected.",
      inputSchema: projectSchema({ ...workspaceLocator, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_read_preview_timeline",
      description: "Read the derived production preview timeline through the domain API. This is read-only diagnostic/artifact context, not product source.",
      inputSchema: projectSchema({ ...workspaceLocator, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_read_production_timeline",
      description: "Convert the production preview timeline plus selected scene_moment video candidates into a MediaEditingProject handoff. Product editing should continue through editing_* tools. Returns media_editing_project, compose_inputs, clips, and blockers for missing scene_moment output selections.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] },
        projectName: { type: "string" },
        project_name: { type: "string" },
        defaultDurationSec: { type: "number" },
        default_duration_sec: { type: "number" }
      })
    },
    {
      name: "domain_read_scene_moment_edit_plan",
      description: "Read the derived scene_moment edit plan through the domain API. The plan groups selected content-unit candidates into video, voice, subtitle, audio, image, and metadata tracks for agent-driven composition.",
      inputSchema: projectSchema({ ...workspaceLocator, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_read_production_edit_plan",
      description: "Read a production-level edit plan handoff from selected scene_moment outputs. This returns a MovScript edit_plan-shaped artifact for editing_project_create_from_edit_plan; it does not render or write candidates.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] },
        projectName: { type: "string" },
        project_name: { type: "string" },
        defaultDurationSec: { type: "number" },
        default_duration_sec: { type: "number" }
      })
    },
    {
      name: "domain_create_editing_project_context",
      description: "Return domain-to-editing handoff context for a scene_moment or production: selected content units, candidates, resources, provenance, blockers, and an edit_plan when available. Creation of the MediaEditingProject remains the responsibility of editing_project_create_from_edit_plan.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        sceneMomentId: { type: ["string", "number"] },
        scene_moment_id: { type: ["string", "number"] },
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] },
        projectName: { type: "string" },
        project_name: { type: "string" },
        defaultDurationSec: { type: "number" },
        default_duration_sec: { type: "number" }
      })
    },
    {
      name: "domain_read_scene_moment_timeline",
      description: "Convert a scene_moment edit plan into a MediaEditingProject handoff. Product editing should use editing_project_create_from_edit_plan for new workflows. Returns media_editing_project plus compose_inputs.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        sceneMomentId: { type: ["string", "number"] },
        scene_moment_id: { type: ["string", "number"] },
        projectName: { type: "string" },
        project_name: { type: "string" },
        sceneName: { type: "string" },
        scene_name: { type: "string" },
        defaultDurationSec: { type: "number" },
        default_duration_sec: { type: "number" }
      })
    },
    {
      name: "domain_read_content_unit_runtime_panel",
      description: "Read the derived content unit runtime panel through the domain API. This is read-only diagnostic/artifact context, not product source.",
      inputSchema: projectSchema({ ...workspaceLocator, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_read_content_unit_generation_prompt",
      description: "Read the derived normalized content unit generation prompt through the domain API. This is read-only diagnostic/artifact context, not product source.",
      inputSchema: projectSchema({ ...workspaceLocator, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_read_content_unit_input_version",
      description: "Compatibility alias for domain_read_content_unit_generation_prompt. Reads the derived normalized content unit generation prompt through the domain API.",
      inputSchema: projectSchema({ ...workspaceLocator, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_read_content_unit_dependency_report",
      description: "Read the derived content unit dependency report through the domain API. This is read-only diagnostic/artifact context, not product source.",
      inputSchema: projectSchema({ ...workspaceLocator, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_read_content_unit_selection_validity",
      description: "Read the derived content unit selection validity report through the domain API. This is read-only diagnostic/artifact context, not product source.",
      inputSchema: projectSchema({ ...workspaceLocator, contentUnitId: { type: ["string", "number"] }, content_unit_id: { type: ["string", "number"] } })
    },
    {
      name: "domain_upsert_project_standards",
      description: "Create or update project-wide creative standards in source project_standards.json. Run domain_inspect, then domain_interpret when derived artifact tools need refreshed context.",
      inputSchema: projectSchema({ ...workspaceLocator, projectStyle: { type: "object", additionalProperties: true }, project_style: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_upsert_setting",
      description: "Create or update a MovScript setting source entity. A setting must be a concrete film/music production entity to make or reuse, not an abstract style/rule. Put the setting data to write in required payload; record/entity are optional existing-context objects only. Prefer this API over direct file edits for setting records.",
      inputSchema: projectSchema({ ...workspaceLocator, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } }, ["payload"])
    },
    {
      name: "domain_upsert_setting_state",
      description: "Create or update a MovScript setting_state source entity under a concrete setting. Setting states are named conditions or versions of the same setting, such as base look, wet costume, damaged prop, side view, calm voice, or angry voice. Put the state data to write in required payload.",
      inputSchema: projectSchema({ ...workspaceLocator, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } }, ["payload"])
    },
    {
      name: "domain_upsert_asset",
      description: "Create or update a MovScript asset slot source entity under a setting state. Assets describe one state asset such as front view, side view, turnaround sheet, material reference, voice timbre, or instrument tone; image assets should prefer plain white or very clean backgrounds. Put the asset data to write in required payload; record/entity are optional existing-context objects only. Store RawResource references by resource_id, not binaries or external URLs.",
      inputSchema: projectSchema({ ...workspaceLocator, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } }, ["payload"])
    },
    {
      name: "domain_certify_asset_provider",
      description: "Register the selected image RawResource with a provider asset library via the MovScript backend. Pass a concrete provider_id when known; otherwise the backend uses the enabled volcengine_ark_official provider. Official Volcengine Ark and Yunwu relay providers with private portrait support are supported. The RawResource stores provider_asset_certifications keyed by provider_id as the certification source of truth, and returned asset:// values are scoped to that provider/account boundary; asset provider_certifications may mirror the selected asset_ref use relationship.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        assetId: { type: ["string", "number"], description: "MovScript asset id or asset source path." },
        asset_id: { type: ["string", "number"], description: "Alias for assetId." },
        assetRef: { type: ["string", "number"], description: "Alias for assetId." },
        asset_ref: { type: ["string", "number"], description: "Alias for assetId." },
        provider: { type: "string", description: "Provider id or provider kind selector. Prefer a concrete provider_id such as volc-ark-main. Defaults to volcengine_ark_official." },
        resourceId: { type: "number", description: "Optional RawResource override. Defaults to the selected asset_ref resource." },
        resource_id: { type: "number", description: "Alias for resourceId." },
        model: { type: "string", description: "Target model id. Provider asset certification is model-scoped; certify separately for fast/standard/pro variants." },
        model_id: { type: "string", description: "Alias for model." },
        asset_group_id: { type: "string", description: "Explicit remote provider asset group id selected by the user." },
        assetGroupId: { type: "string", description: "Alias for asset_group_id." },
        asset_group_name: { type: "string", description: "Optional display name for the explicit remote provider asset group." },
        assetGroupName: { type: "string", description: "Alias for asset_group_name." },
        projectName: { type: "string", description: "Optional provider project label for the managed asset group. Defaults to projectId when available." },
        project_name: { type: "string", description: "Alias for projectName." },
        settingId: { type: "string", description: "Optional setting scope for the managed asset group. Defaults to the asset path setting segment when available." },
        setting_id: { type: "string", description: "Alias for settingId." },
        source_url: { type: "string", description: "Optional public image URL override. If omitted, the backend creates a short-lived signed resource URL using MOVSCRIPT_PROVIDER_ASSET_PUBLIC_BASE_URL." },
        sourceUrl: { type: "string", description: "Alias for source_url." },
        name: { type: "string", description: "Optional material asset name. Defaults to asset title/slot/id." },
        allow_private_urls: { type: "boolean", description: "Allow localhost/private source URLs for local official-API testing. Defaults to false." },
        allowPrivateUrls: { type: "boolean", description: "Alias for allow_private_urls." },
        timeout_ms: { type: "number", minimum: 1e3 },
        timeoutMs: { type: "number", minimum: 1e3 }
      })
    },
    {
      name: "domain_query_remote_asset_groups",
      description: "List remote provider asset-library groups mirrored by MovScript for a provider account. Use this before registering assets when the user must choose the target remote group.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        provider: { type: "string", description: "Provider id or provider kind selector. Prefer a concrete provider_id such as yunwu-main or volc-ark-main." },
        provider_id: { type: "string", description: "Alias for provider." },
        model: { type: "string", description: "Optional target model id for model-scoped groups." },
        model_id: { type: "string", description: "Alias for model." }
      })
    },
    {
      name: "domain_query_remote_assets",
      description: "List remote provider assets inside a selected provider asset group, including model-specific certification records.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        provider: { type: "string", description: "Provider id or provider kind selector. Prefer a concrete provider_id such as yunwu-main or volc-ark-main." },
        provider_id: { type: "string", description: "Alias for provider." },
        groupId: { type: ["string", "number"], description: "Remote provider asset group id or MovScript mirrored group id." },
        group_id: { type: ["string", "number"], description: "Alias for groupId." },
        asset_group_id: { type: ["string", "number"], description: "Alias for groupId." }
      })
    },
    {
      name: "domain_upsert_setting_tree",
      description: "Create or update one concrete setting plus multiple setting_state records and each state's asset slots in one structured write. Use this for setting -> many states -> many assets authoring. Asset generated candidates still belong to asset_ref content units; this tool only writes the source setting/state/asset structure.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        setting: { type: "object", additionalProperties: true },
        payload: { type: "object", additionalProperties: true },
        states: {
          type: "array",
          items: {
            type: "object",
            additionalProperties: true,
            properties: {
              state: { type: "object", additionalProperties: true },
              payload: { type: "object", additionalProperties: true },
              assets: {
                type: "array",
                items: { type: "object", additionalProperties: true }
              }
            }
          }
        }
      }, ["states"])
    },
    {
      name: "domain_upsert_script",
      description: "Create or update a script source record and script.md text. Prefer this API over hand-editing script metadata plus markdown.",
      inputSchema: projectSchema({ ...workspaceLocator, scriptId: { type: ["string", "number"] }, script_id: { type: ["string", "number"] }, sourceText: { type: "string" }, source_text: { type: "string" }, metadata: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_read_script_source",
      description: "Read script.md source text for a script domain entity.",
      inputSchema: projectSchema({ ...workspaceLocator, record: { type: "object", additionalProperties: true }, entity: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_snapshot_script_version",
      description: "Create a script version and script blocks from a script Markdown source so downstream production entities can reference stable script blocks.",
      inputSchema: projectSchema({ ...workspaceLocator, scriptId: { type: ["string", "number"] }, script_id: { type: ["string", "number"] }, versionId: { type: ["string", "number"] }, version_id: { type: ["string", "number"] }, versionLabel: { type: "string" }, version_label: { type: "string" }, sourcePath: { type: "string" }, source_path: { type: "string" } })
    },
    {
      name: "domain_upsert_content_unit",
      description: "Create or update a project-level content unit source record. Content units are independent production tasks and never target namespace nodes directly. For namespace-scope video output, use content_unit_type=timeline_assembly_ref with target_kind=timeline_assembly plus target_ref=timeline_assembly:<scopeKind>:<scopeRef>, or pass scope_kind/scope_ref so the writer derives that target_ref. Legacy production_ref/segment_ref remain compatibility aliases for timeline assemblies; do not invent episode_ref, beat_ref, or other namespace ref types.",
      inputSchema: projectSchema({ ...workspaceLocator, unit: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_upsert_timeline_namespace_tree",
      description: "Merge-write a path-first timeline namespace tree under timeline/. Namespace vocabulary supplies labels/templates only; instance parent-child structure comes from targetPath and nested tree placement. Nodes are namespace labels over legacy production/segment projections and must not carry content-unit refs, candidates, selections, resources, production_ref, or segment_ref. Put scene_moments and their expression_units/storyboards/keyframes/audio_cues under namespace nodes when the tree needs production primitives. For namespace-scope video output, put explicit content_units on a namespace node; they are written as timeline_assembly_ref content units with target_kind=timeline_assembly and target_ref=timeline_assembly:<namespace_kind>:<id>.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        namespace: { type: "object", additionalProperties: true },
        root: { type: "object", additionalProperties: true },
        tree: { type: "object", additionalProperties: true },
        nodes: { type: "array", items: { type: "object", additionalProperties: true } },
        namespaces: { type: "array", items: { type: "object", additionalProperties: true } },
        timeline_namespaces: { type: "array", items: { type: "object", additionalProperties: true } },
        timelineNamespaces: { type: "array", items: { type: "object", additionalProperties: true } },
        scene_moments: { type: "array", items: { type: "object", additionalProperties: true } },
        sceneMoments: { type: "array", items: { type: "object", additionalProperties: true } }
      })
    },
    {
      name: "domain_upsert_production",
      description: "Create or update a production source record under productions/. Use this before adding segment, scene_moment, expression_unit, keyframe, or storyboard planning structure.",
      inputSchema: projectSchema({ ...workspaceLocator, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, production: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_upsert_production_tree",
      description: "Merge-write one legacy production-projection tree plus nested segments, scene_moments, expression_units, storyboards, keyframes, audio_cues, and content_units in one structured operation. This is an upsert/patch tree: records are matched by id and updated; omitted existing children are not deleted. Use explicit delete flows for removals. Candidate writes still belong to content-unit candidate tools. For new namespace-scope video output, prefer explicit timeline_assembly_ref content units through domain_upsert_content_unit instead of relying on legacy production_ref/segment_ref tree defaults.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] },
        production: { type: "object", additionalProperties: true },
        payload: { type: "object", additionalProperties: true },
        record: { type: "object", additionalProperties: true },
        content_units: { type: "array", items: { type: "object", additionalProperties: true } },
        contentUnits: { type: "array", items: { type: "object", additionalProperties: true } },
        segments: {
          type: "array",
          items: {
            type: "object",
            additionalProperties: true,
            properties: {
              segment: { type: "object", additionalProperties: true },
              payload: { type: "object", additionalProperties: true },
              scene_moments: { type: "array", items: { type: "object", additionalProperties: true } },
              sceneMoments: { type: "array", items: { type: "object", additionalProperties: true } },
              content_units: { type: "array", items: { type: "object", additionalProperties: true } },
              contentUnits: { type: "array", items: { type: "object", additionalProperties: true } }
            }
          }
        }
      }, ["segments"])
    },
    {
      name: "domain_upsert_segment",
      description: "Create or update a segment source record inside a production. Segments are rhythm or dramatic-function sections and own scene_moment children.",
      inputSchema: projectSchema({ ...workspaceLocator, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, segment: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_upsert_scene_moment",
      description: "Create or update a scene_moment system primitive. Prefer targetPath or namespacePath for new timeline namespace projects so the scene moment is written under timeline/** by path; productionId/segmentId are legacy compatibility inputs. Scene moments are the final expression aggregation unit for an event/beat; they own expression units and receive final composed content-unit candidates.",
      inputSchema: projectSchema({ ...workspaceLocator, targetPath: { type: "string" }, target_path: { type: "string" }, namespacePath: { type: "string" }, namespace_path: { type: "string" }, timelineNamespacePath: { type: "string" }, timeline_namespace_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_upsert_keyframe",
      description: "Create or update a keyframe source entity under an expression_unit(kind=shot) or directly under a scene_moment. Prefer targetPath, expressionUnitPath, or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Keyframes are visual anchors referenced by keyframe_ref content units.",
      inputSchema: projectSchema({ ...workspaceLocator, targetPath: { type: "string" }, target_path: { type: "string" }, expressionUnitPath: { type: "string" }, expression_unit_path: { type: "string" }, sceneMomentPath: { type: "string" }, scene_moment_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, expressionUnitId: { type: ["string", "number"] }, expression_unit_id: { type: ["string", "number"] }, keyframeId: { type: ["string", "number"] }, keyframe_id: { type: ["string", "number"] }, keyframe: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, expressionUnit: { type: "object", additionalProperties: true }, expression_unit: { type: "object", additionalProperties: true }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_upsert_storyboard",
      description: "Create or update a storyboard source record under an expression_unit(kind=shot) or directly under a scene_moment. Prefer targetPath, expressionUnitPath, or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Use expression units for shot semantics; this tool does not create standalone shot nodes.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        targetPath: { type: "string" },
        target_path: { type: "string" },
        expressionUnitPath: { type: "string" },
        expression_unit_path: { type: "string" },
        sceneMomentPath: { type: "string" },
        scene_moment_path: { type: "string" },
        parentPath: { type: "string" },
        parent_path: { type: "string" },
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] },
        segmentId: { type: ["string", "number"] },
        segment_id: { type: ["string", "number"] },
        sceneMomentId: { type: ["string", "number"] },
        scene_moment_id: { type: ["string", "number"] },
        expressionUnitId: { type: ["string", "number"] },
        expression_unit_id: { type: ["string", "number"] },
        sceneMomentTitle: { type: "string" },
        scene_moment_title: { type: "string" },
        segmentTitle: { type: "string" },
        segment_title: { type: "string" },
        storyboardId: { type: ["string", "number"] },
        storyboard_id: { type: ["string", "number"] },
        storyboard: { type: "object", additionalProperties: true },
        payload: { type: "object", additionalProperties: true },
        expressionUnit: { type: "object", additionalProperties: true },
        expression_unit: { type: "object", additionalProperties: true },
        production: { type: "object", additionalProperties: true }
      })
    },
    {
      name: "domain_upsert_audio_cue",
      description: "Legacy/auxiliary: create or update an audio_cue source entity under a scene_moment. Prefer targetPath or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Prefer expression_unit with modality=audio or verbal plus voice_profile_ref for new dialogue, sound, music, ambience, or foley planning.",
      inputSchema: projectSchema({ ...workspaceLocator, targetPath: { type: "string" }, target_path: { type: "string" }, sceneMomentPath: { type: "string" }, scene_moment_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, audioCueId: { type: ["string", "number"] }, audio_cue_id: { type: ["string", "number"] }, audioCue: { type: "object", additionalProperties: true }, audio_cue: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_upsert_expression_unit",
      description: "Create or update an expression_unit source entity under a scene_moment. Prefer targetPath or sceneMomentPath for new timeline namespace projects; productionId/segmentId are legacy compatibility inputs. Use slot_kind=visual|voice|subtitle|audio for new expression slots; expression_kind/kind are legacy compatibility hints.",
      inputSchema: projectSchema({ ...workspaceLocator, targetPath: { type: "string" }, target_path: { type: "string" }, sceneMomentPath: { type: "string" }, scene_moment_path: { type: "string" }, parentPath: { type: "string" }, parent_path: { type: "string" }, productionId: { type: ["string", "number"] }, production_id: { type: ["string", "number"] }, segmentId: { type: ["string", "number"] }, segment_id: { type: ["string", "number"] }, sceneMomentId: { type: ["string", "number"] }, scene_moment_id: { type: ["string", "number"] }, expressionUnitId: { type: ["string", "number"] }, expression_unit_id: { type: ["string", "number"] }, expressionUnit: { type: "object", additionalProperties: true }, expression_unit: { type: "object", additionalProperties: true }, payload: { type: "object", additionalProperties: true }, sceneMoment: { type: "object", additionalProperties: true }, scene_moment: { type: "object", additionalProperties: true }, segment: { type: "object", additionalProperties: true }, production: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_update_content_unit_prompt",
      description: "Update a content unit edit_prompt source field. Run domain_inspect, domain_interpret, and regeneration planning when prompt changes may stale candidates.",
      inputSchema: projectSchema({ ...workspaceLocator, targetPath: { type: "string" }, target_path: { type: "string" }, editPrompt: { type: "object", additionalProperties: true }, edit_prompt: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_update_entity_transition",
      description: "Update an entity transition boundary on the source entity that owns transition semantics.",
      inputSchema: projectSchema({ ...workspaceLocator, targetPath: { type: "string" }, target_path: { type: "string" }, transition: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_update_storyboard_timeline",
      description: "Update a storyboard timeline source field. Storyboard order and timing belong on storyboard timeline source entities, not on generated artifact output.",
      inputSchema: projectSchema({ ...workspaceLocator, targetPath: { type: "string" }, target_path: { type: "string" }, timeline: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_append_candidate",
      description: "Append an inline candidate to an asset or keyframe source entity. Content-unit candidates are backend decision records; use domain_create_content_candidate instead. Generated resources become stable domain dependencies only after candidate/selection writes and explicit adoption/selection.",
      inputSchema: candidateSchema()
    },
    {
      name: "domain_create_content_candidate",
      description: "Create an external content candidate record for a content unit output through the backend decision API. This does not edit workspace source files. Omit status for completed generated resources; the backend defaults it to succeeded. If status is provided, use only queued, running, succeeded, failed, canceled, or imported.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        ...contentCandidateWriteProperties()
      })
    },
    {
      name: "domain_register_raw_resource_as_content_unit_candidate",
      description: "Register an existing MovScript RawResource as a content-unit candidate through the backend decision API. Use this when a resource already exists from upload, transform, import, or low-level generation and should enter the candidate pool without selecting it.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        contentUnitId: { type: ["string", "number"] },
        content_unit_id: { type: ["string", "number"] },
        resourceId: { type: "number", description: "Positive integer RawResource ID to register as a candidate output." },
        resource_id: { type: "number", description: "Alias for resourceId." },
        outputKind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
        output_kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
        kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
        candidateId: { type: ["string", "number"] },
        candidate_id: { type: ["string", "number"] },
        source: { type: "string" },
        status: { type: "string", enum: [...contentCandidateStatuses] },
        mimeType: { type: "string" },
        mime_type: { type: "string" },
        width: { type: "number" },
        height: { type: "number" },
        durationSec: { type: "number" },
        duration_sec: { type: "number" },
        metadata: { type: "object", additionalProperties: true },
        producer: { type: "object", additionalProperties: true },
        promptSnapshot: { type: "object", additionalProperties: true },
        prompt_snapshot: { type: "object", additionalProperties: true }
      }, ["contentUnitId", "resourceId"])
    },
    {
      name: "domain_create_content_candidate_batch",
      description: "Create multiple external content candidate records for content unit outputs through the backend decision API. Each item accepts the same candidate fields as domain_create_content_candidate. Omit item status for completed generated resources; the backend defaults it to succeeded. Runs sequentially and returns per-item results so agents can keep partial successes.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        items: { type: "array", items: contentCandidateWriteItemSchema() },
        continueOnError: { type: "boolean" },
        continue_on_error: { type: "boolean" }
      }, ["items"])
    },
    {
      name: "domain_create_asset_slot_candidate",
      description: "Create an asset-slot candidate using the MovScript workspace candidate service. If targetRecord carries a workspace path, this appends an inline candidate to that asset source entity.",
      inputSchema: projectSchema(candidateWriteSchema())
    },
    {
      name: "domain_create_keyframe_candidate",
      description: "Create a keyframe candidate using the MovScript workspace candidate service. If keyframes are represented as content units in the active model, use the content-unit candidate flow instead.",
      inputSchema: projectSchema(candidateWriteSchema())
    },
    {
      name: "domain_select_content_unit_candidate",
      description: "Select a content candidate for a content unit through the backend decision API. Selection is backend decision metadata, not a workspace source-file edit; run domain_inspect and domain_interpret when derived artifact tools need refreshed context.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        contentUnitId: { type: ["string", "number"] },
        content_unit_id: { type: ["string", "number"] },
        candidateId: { type: ["string", "number"] },
        candidate_id: { type: ["string", "number"] },
        resourceId: { type: "number", description: "Positive integer RawResource ID." },
        resource_id: { type: "number", description: "Positive integer RawResource ID." },
        stalePolicy: { type: "string" },
        stale_policy: { type: "string" },
        reason: { type: "string" }
      })
    },
    {
      name: "domain_select_content_unit_candidate_batch",
      description: "Select content candidates for multiple content units through the backend decision API. Each item accepts the same fields as domain_select_content_unit_candidate. Runs sequentially and returns per-item results.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        items: { type: "array", items: { type: "object", additionalProperties: true } },
        continueOnError: { type: "boolean" },
        continue_on_error: { type: "boolean" }
      }, ["items"])
    },
    {
      name: "domain_decide_content_unit_candidate",
      description: "Apply a user decision to a content-unit candidate through the backend decision API. Use decision=adopt to write selection and unblock stable downstream dependencies; use reject or defer to mark the candidate decision without selecting it, so downstream remains blocked until a candidate is adopted.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        contentUnitId: { type: ["string", "number"] },
        content_unit_id: { type: ["string", "number"] },
        candidateId: { type: ["string", "number"] },
        candidate_id: { type: ["string", "number"] },
        decision: { type: "string", enum: ["adopt", "reject", "defer"] },
        resourceId: { type: "number", description: "Positive integer RawResource ID." },
        resource_id: { type: "number", description: "Positive integer RawResource ID." },
        stalePolicy: { type: "string" },
        stale_policy: { type: "string" },
        reason: { type: "string" },
        decidedAt: { type: "string" },
        decided_at: { type: "string" },
        metadata: { type: "object", additionalProperties: true }
      })
    },
    {
      name: "domain_select_candidate",
      description: "Select and lock an inline candidate on an asset or keyframe source entity. Content-unit selections are backend decision records; use domain_select_content_unit_candidate instead.",
      inputSchema: candidateSchema({ candidateId: { type: "string" }, candidate_id: { type: "string" }, reason: { type: "string" } })
    },
    {
      name: "domain_update_candidate",
      description: "Update an inline candidate on an asset or keyframe source entity. Content-unit candidates are backend decision records; use domain_create_content_candidate or domain_decide_content_unit_candidate instead.",
      inputSchema: candidateSchema({ candidateId: { type: "string" }, candidate_id: { type: "string" } })
    },
    {
      name: "domain_unlock_candidate",
      description: "Remove an inline candidate lock from an asset or keyframe source entity. Content-unit selections are backend decision records; use domain_select_content_unit_candidate/domain_decide_content_unit_candidate instead.",
      inputSchema: candidateSchema()
    },
    {
      name: "domain_delete_entity",
      description: "Delete a MovScript domain source entity file through the workspace service. Do not touch interpreter debug output for product work.",
      inputSchema: projectSchema({ ...workspaceLocator, entity: { type: "object", additionalProperties: true }, record: { type: "object", additionalProperties: true } })
    },
    {
      name: "domain_overview",
      description: "Show MovScript source state, backend decisions, diagnostics, stale generated outputs, and recommended next actions.",
      inputSchema: projectSchema(workspaceLocator)
    },
    {
      name: "domain_read_production_work_plan",
      description: "Derive the current in-memory production work plan from source and decision state. This does not read or write interpreter debug artifacts and should be used by UI, CLI, and agents as the shared production todo graph.",
      inputSchema: projectSchema(workspaceLocator)
    },
    {
      name: "domain_production_status_summary",
      description: "Summarize current production progress for agents: prerequisites/settings/assets, storyboards, keyframes, content-unit candidate counts, selected resources, stale hints, and blocking refs. Use this before broad generation or review instead of manually stitching many tool results.",
      inputSchema: projectSchema({
        ...workspaceLocator,
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] }
      })
    },
    {
      name: "domain_inspect",
      description: "Inspect current source changes, diagnostics, and predicted impact without writing debug artifacts. This is the primary diagnostic entrypoint after API writes or direct source edits.",
      inputSchema: projectSchema(inspectOptions)
    },
    {
      name: "domain_review",
      description: "Compatibility alias for domain_inspect with review-shaped output. Prefer domain_inspect for current source diagnostics. This is diagnostic only and writes no interpreted artifacts.",
      inputSchema: projectSchema(inspectOptions)
    },
    {
      name: "domain_interpret",
      description: "Validate current source and refresh derived diagnostic artifacts when enabled. Does not publish, approve, commit, checkpoint user intent, or create product state.",
      inputSchema: projectSchema(workspaceLocator)
    },
    {
      name: "domain_regeneration_plan",
      description: "Plan downstream review targets after domain_interpret refreshes diagnostic context. Reports affected or stale content units, prompt bundles, preview timelines, and selections; it does not require regeneration by itself.",
      inputSchema: projectSchema(workspaceLocator)
    }
  ];
}
function candidateSchema(extra = {}) {
  return projectSchema({
    ...workspaceLocator,
    targetPath: { type: "string" },
    target_path: { type: "string" },
    targetKind: { type: "string", enum: ["asset", "keyframe"] },
    target_kind: { type: "string", enum: ["asset", "keyframe"] },
    payload: { type: "object", additionalProperties: true },
    lock: { type: ["boolean", "object"], additionalProperties: true },
    nonce: { type: "string" },
    ...extra
  });
}
function candidateWriteSchema() {
  return {
    ...workspaceLocator,
    payload: { type: "object", additionalProperties: true },
    targetRecord: { type: "object", additionalProperties: true },
    target_record: { type: "object", additionalProperties: true },
    nonce: { type: "string" }
  };
}
function contentCandidateWriteProperties() {
  return {
    contentUnitId: { type: ["string", "number"] },
    content_unit_id: { type: ["string", "number"] },
    candidateId: { type: ["string", "number"] },
    candidate_id: { type: ["string", "number"] },
    source: { type: "string" },
    status: {
      type: "string",
      enum: [...contentCandidateStatuses],
      description: "Optional. Omit for completed generated resources; the backend defaults it to succeeded. Do not use completed, ready, done, selected, or accepted."
    },
    producer: { type: "object", additionalProperties: true },
    outputs: {
      type: "array",
      items: {
        type: "object",
        properties: {
          kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
          resource_id: { type: "number", description: "Positive integer RawResource ID." },
          artifact_ref: { type: "string", description: "Optional logical artifact reference; never use as a RawResource ID." },
          mime_type: { type: "string" },
          width: { type: "number" },
          height: { type: "number" },
          duration_sec: { type: "number" },
          metadata: { type: "object", additionalProperties: true }
        },
        required: ["kind", "resource_id"],
        additionalProperties: true
      }
    },
    promptSnapshot: { type: "object", additionalProperties: true },
    prompt_snapshot: { type: "object", additionalProperties: true }
  };
}
function contentCandidateWriteItemSchema() {
  return {
    type: "object",
    properties: contentCandidateWriteProperties(),
    additionalProperties: false
  };
}
function projectSchema(properties, required) {
  return objectSchema(properties, required);
}
var projectLocator = {
  projectId: { type: ["string", "number"], description: "MovScript project id." },
  project_id: { type: ["string", "number"], description: "Alias for projectId." }
};
var editingProjectInput = {
  editingProject: { type: "object", additionalProperties: true, description: "MediaEditingProject object." },
  editing_project: { type: "object", additionalProperties: true, description: "Alias for editingProject." },
  project: { type: "object", additionalProperties: true, description: "Alias for editingProject." }
};
function editingTools() {
  return [
    {
      name: "editing_project_create",
      description: "Create and persist an empty MovScript media editing project through Editing Service. This does not render media or write content candidates.",
      inputSchema: objectSchema({
        ...projectLocator,
        title: { type: "string" },
        width: { type: "number" },
        height: { type: "number" },
        fps: { type: "number" },
        background: { type: "string" }
      })
    },
    {
      name: "editing_project_create_from_edit_plan",
      description: "Create and persist a MediaEditingProject from a MovScript edit_plan through Editing Service. This is the main handoff from domain planning into the dedicated editing tool family.",
      inputSchema: objectSchema({
        ...projectLocator,
        editPlan: { type: "object", additionalProperties: true, description: "MovScript edit_plan artifact." },
        edit_plan: { type: "object", additionalProperties: true, description: "Alias for editPlan." },
        title: { type: "string" },
        width: { type: "number" },
        height: { type: "number" },
        fps: { type: "number" },
        background: { type: "string" },
        defaultDurationMs: { type: "number" },
        default_duration_ms: { type: "number" }
      })
    },
    {
      name: "editing_project_create_from_edit_decisions",
      description: "Create and persist a MediaEditingProject from OpenMontage-style edit_decisions plus an optional asset_manifest. This only creates project data; rendering remains a separate editing task or editing_video_compose call.",
      inputSchema: objectSchema({
        ...projectLocator,
        editDecisions: { type: "object", additionalProperties: true, description: "Edit decision artifact with cuts, overlays, audio, subtitles, and render runtime metadata." },
        edit_decisions: { type: "object", additionalProperties: true, description: "Alias for editDecisions." },
        assetManifest: { type: "object", additionalProperties: true, description: "Asset manifest used to resolve edit_decisions source refs into MediaAssetDescriptor entries." },
        asset_manifest: { type: "object", additionalProperties: true, description: "Alias for assetManifest." },
        timelineAssembly: { type: "object", additionalProperties: true, description: "TimelineAssembly intent state used to build a repeatable CompileManifest before creating the MediaEditingProject." },
        timeline_assembly: { type: "object", additionalProperties: true, description: "Alias for timelineAssembly." },
        compileManifest: { type: "object", additionalProperties: true, description: "Optional CompileManifest from the edit desk handoff; the service revalidates the current inputs." },
        compile_manifest: { type: "object", additionalProperties: true, description: "Alias for compileManifest." },
        title: { type: "string" },
        width: { type: "number" },
        height: { type: "number" },
        fps: { type: "number" },
        background: { type: "string" },
        defaultDurationMs: { type: "number" },
        default_duration_ms: { type: "number" },
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] },
        productionPath: { type: "string" },
        production_path: { type: "string" },
        targetKind: { type: "string" },
        target_kind: { type: "string" },
        targetRef: { type: "string" },
        target_ref: { type: "string" },
        scopeKind: { type: "string" },
        scope_kind: { type: "string" },
        scopeRef: { type: ["string", "number"] },
        scope_ref: { type: ["string", "number"] }
      })
    },
    {
      name: "editing_project_get",
      description: "Read a persisted MediaEditingProject by id from Editing Service.",
      inputSchema: objectSchema({ ...projectLocator, editingProjectId: { type: "string" }, editing_project_id: { type: "string" } })
    },
    {
      name: "editing_project_update_settings",
      description: "Update MediaEditingProject settings such as title, fps, canvas size, background, and workspace binding. This mutates project data only.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        title: { type: "string" },
        width: { type: "number" },
        height: { type: "number" },
        fps: { type: "number" },
        background: { type: "string" },
        workspace: { type: "object", additionalProperties: true }
      })
    },
    {
      name: "editing_project_add_asset",
      description: "Add a RawResource, generated resource, local file, or bytes-backed source to a MediaEditingProject asset registry.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        asset: { type: "object", additionalProperties: true }
      })
    },
    {
      name: "editing_project_remove_asset",
      description: "Remove an unused asset reference from a MediaEditingProject asset registry. Fails if any clip still references the asset.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        assetId: { type: "string" },
        asset_id: { type: "string" }
      })
    },
    {
      name: "editing_project_save",
      description: "Persist a MediaEditingProject revision through Editing Service. Pass expectedRevision for optimistic locking against the last revision read.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        expectedRevision: { type: "number", description: "Last persisted revision observed before editing. Save returns conflict if the stored revision changed." },
        expected_revision: { type: "number", description: "Alias for expectedRevision." }
      })
    },
    {
      name: "editing_timeline_apply_commands",
      description: "Apply MovScript media timeline commands to a MediaEditingProject. This modifies the editing project only; it does not render media or create candidates.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        commands: { type: "array", items: { type: "object", additionalProperties: true } },
        command: { type: "object", additionalProperties: true }
      })
    },
    {
      name: "editing_timeline_add_track",
      description: "Add a video, image, audio, text, subtitle, or effect track to a MediaEditingProject timeline.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        track: { type: "object", additionalProperties: true },
        trackId: { type: "string" },
        track_id: { type: "string" },
        type: { type: "string" },
        trackType: { type: "string" },
        track_type: { type: "string" },
        name: { type: "string" },
        zIndex: { type: "number" },
        z_index: { type: "number" }
      })
    },
    {
      name: "editing_timeline_remove_track",
      description: "Remove an empty track from a MediaEditingProject timeline.",
      inputSchema: objectSchema({ ...editingProjectInput, trackId: { type: "string" }, track_id: { type: "string" } })
    },
    {
      name: "editing_timeline_add_clip",
      description: "Place an asset-backed or text clip onto a timeline track.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        trackId: { type: "string" },
        track_id: { type: "string" },
        clip: { type: "object", additionalProperties: true },
        assetId: { type: "string" },
        asset_id: { type: "string" },
        assetType: { type: "string" },
        asset_type: { type: "string" },
        timelineStartMs: { type: "number" },
        timeline_start_ms: { type: "number" },
        durationMs: { type: "number" },
        duration_ms: { type: "number" }
      })
    },
    {
      name: "editing_timeline_update_clip",
      description: "Update trim, duration, placement, volume, fit, style, or metadata for an existing timeline clip.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        clipId: { type: "string" },
        clip_id: { type: "string" },
        patch: { type: "object", additionalProperties: true }
      })
    },
    {
      name: "editing_timeline_split_clip",
      description: "Split a timeline clip at an absolute timeline timestamp.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        clipId: { type: "string" },
        clip_id: { type: "string" },
        splitTimeMs: { type: "number" },
        split_time_ms: { type: "number" },
        retainSide: { type: "string" },
        retain_side: { type: "string" }
      })
    },
    {
      name: "editing_timeline_move_clip",
      description: "Move a timeline clip to a new timestamp and optionally to another compatible track.",
      inputSchema: objectSchema({
        ...editingProjectInput,
        clipId: { type: "string" },
        clip_id: { type: "string" },
        targetTrackId: { type: "string" },
        target_track_id: { type: "string" },
        timelineStartMs: { type: "number" },
        timeline_start_ms: { type: "number" }
      })
    },
    {
      name: "editing_timeline_delete_clip",
      description: "Delete a clip from a MediaEditingProject timeline.",
      inputSchema: objectSchema({ ...editingProjectInput, clipId: { type: "string" }, clip_id: { type: "string" } })
    },
    {
      name: "editing_timeline_validate",
      description: "Validate a MediaEditingProject timeline for missing assets, invalid timing, track mismatch, and unsupported clip settings.",
      inputSchema: objectSchema({ ...editingProjectInput })
    },
    {
      name: "editing_runtime_capabilities_get",
      description: "Read Electron mediaPipeline runtime capabilities such as FFmpeg availability, version, supported task types, project store, and local HLS preview support. This does not start a task.",
      inputSchema: objectSchema({})
    },
    {
      name: "editing_video_compose",
      description: "MovScript video_compose entrypoint: resolve an editing project or edit_decisions handoff, validate the MediaEditingProject timeline, and create a Media Pipeline render/HLS task. Supports movscript_media_pipeline/ffmpeg only; Remotion, HyperFrames, and external NLE runtimes return explicit blockers instead of silent fallback.",
      inputSchema: objectSchema({
        ...projectLocator,
        ...editingProjectInput,
        editingProjectId: { type: "string" },
        editing_project_id: { type: "string" },
        editDecisions: { type: "object", additionalProperties: true },
        edit_decisions: { type: "object", additionalProperties: true },
        assetManifest: { type: "object", additionalProperties: true },
        asset_manifest: { type: "object", additionalProperties: true },
        timelineAssembly: { type: "object", additionalProperties: true, description: "TimelineAssembly intent state used to build a repeatable CompileManifest before rendering." },
        timeline_assembly: { type: "object", additionalProperties: true, description: "Alias for timelineAssembly." },
        compileManifest: { type: "object", additionalProperties: true, description: "Optional CompileManifest from the edit desk handoff; the runtime still revalidates the current inputs." },
        compile_manifest: { type: "object", additionalProperties: true, description: "Alias for compileManifest." },
        renderRuntime: { type: "string" },
        render_runtime: { type: "string" },
        format: { type: "string", description: "mp4 or hls. Defaults to mp4." },
        output: { type: "object", additionalProperties: true },
        importToResource: { type: "boolean" },
        import_to_resource: { type: "boolean" },
        title: { type: "string" },
        width: { type: "number" },
        height: { type: "number" },
        fps: { type: "number" },
        background: { type: "string" },
        defaultDurationMs: { type: "number" },
        default_duration_ms: { type: "number" },
        productionId: { type: ["string", "number"] },
        production_id: { type: ["string", "number"] },
        productionPath: { type: "string" },
        production_path: { type: "string" },
        targetKind: { type: "string" },
        target_kind: { type: "string" },
        targetRef: { type: "string" },
        target_ref: { type: "string" },
        scopeKind: { type: "string" },
        scope_kind: { type: "string" },
        scopeRef: { type: ["string", "number"] },
        scope_ref: { type: ["string", "number"] }
      })
    },
    {
      name: "editing_task_render_create",
      description: "Create an Electron mediaPipeline timeline_render task for a MediaEditingProject. Rendering runs in Electron, not in the backend.",
      inputSchema: objectSchema({ ...projectLocator, ...editingProjectInput, output: { type: "object", additionalProperties: true } })
    },
    {
      name: "editing_task_hls_create",
      description: "Create an Electron mediaPipeline timeline_hls task. HLS packaging, including optional output.hlsVariants adaptive renditions, runs in Electron; backend only hosts uploaded artifacts.",
      inputSchema: objectSchema({ ...projectLocator, ...editingProjectInput, output: { type: "object", additionalProperties: true } })
    },
    {
      name: "editing_task_transcode_create",
      description: "Create an Electron mediaPipeline media_transcode task. Pass projectId so the task is stored in the correct Electron media workspace.",
      inputSchema: objectSchema({ ...projectLocator, source: { type: "object", additionalProperties: true }, output: { type: "object", additionalProperties: true } })
    },
    {
      name: "editing_task_reframe_create",
      description: "Create an Electron mediaPipeline media_reframe task for mechanical crop/contain/pad reframe. Pass projectId so the task is stored in the correct Electron media workspace.",
      inputSchema: objectSchema({ ...projectLocator, source: { type: "object", additionalProperties: true }, output: { type: "object", additionalProperties: true }, target: { type: "string" }, mode: { type: "string" } })
    },
    {
      name: "editing_task_get",
      description: "Read an Electron mediaPipeline task state. Pass projectId with taskId for persisted workspace recovery after app restart.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task recovery." },
        project_id: { type: "string", description: "Alias for projectId." }
      })
    },
    {
      name: "editing_task_cancel",
      description: "Cancel an Electron mediaPipeline task. Pass projectId with taskId so Electron can recover persisted task state and write a clear cancel diagnostic after app restart.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task recovery." },
        project_id: { type: "string", description: "Alias for projectId." }
      })
    },
    {
      name: "editing_task_logs_get",
      description: "Read FFmpeg/task logs for an Electron mediaPipeline task. Pass projectId with taskId for persisted workspace log recovery after app restart.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task log recovery." },
        project_id: { type: "string", description: "Alias for projectId." }
      })
    },
    {
      name: "editing_export_import_resource",
      description: "Upload a completed local editing export as a RawResource, optionally recording derivative provenance. This does not adopt or select the result. When resolving a completed Electron task by taskId, pass projectId for persisted workspace output recovery. HLS manifests must use editing_export_publish_hls so manifest and segment files are hosted as a MediaStreamArtifact instead of one RawResource.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task output recovery." },
        project_id: { type: "string", description: "Alias for projectId." },
        outputPath: { type: "string" },
        output_path: { type: "string" },
        filename: { type: "string" },
        mimeType: { type: "string" },
        mime_type: { type: "string" },
        folderId: { type: ["string", "number"] },
        folder_id: { type: ["string", "number"] },
        derivative: { type: "object", additionalProperties: true },
        operation: { type: "string" },
        tool: { type: "string" },
        inputResourceIds: { type: "array", items: { type: ["string", "number"] } },
        input_resource_ids: { type: "array", items: { type: ["string", "number"] } },
        sourceResourceId: { type: ["string", "number"] },
        source_resource_id: { type: ["string", "number"] },
        sourceResourceIds: { type: "array", items: { type: ["string", "number"] } },
        source_resource_ids: { type: "array", items: { type: ["string", "number"] } },
        params: { type: "object", additionalProperties: true }
      })
    },
    {
      name: "editing_export_save_local",
      description: "Save a completed Electron editing export to an explicit local file path, save a complete HLS bundle to a local directory, or return/validate an existing local export path. When resolving by taskId, pass projectId for persisted workspace output recovery. This does not upload or create candidates.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        projectId: { type: "string", description: "Media workspace project id used with taskId for persisted task output recovery." },
        project_id: { type: "string", description: "Alias for projectId." },
        outputPath: { type: "string" },
        output_path: { type: "string" },
        savePath: { type: "string" },
        save_path: { type: "string" },
        saveDirectory: { type: "string" },
        save_directory: { type: "string" },
        hlsDirectory: { type: "string" },
        hls_directory: { type: "string" },
        segmentPaths: { type: "array", items: { type: "string" } },
        segment_paths: { type: "array", items: { type: "string" } },
        filename: { type: "string" }
      })
    },
    {
      name: "editing_export_publish_hls",
      description: "Upload completed HLS manifest/segments as a MediaStreamArtifact. When resolving a completed Electron HLS task by taskId, pass projectId for persisted workspace output recovery.",
      inputSchema: objectSchema({
        taskId: { type: "string" },
        task_id: { type: "string" },
        manifestPath: { type: "string" },
        manifest_path: { type: "string" },
        segmentPaths: { type: "array", items: { type: "string" } },
        segment_paths: { type: "array", items: { type: "string" } },
        title: { type: "string" },
        projectId: { type: ["string", "number"] },
        project_id: { type: ["string", "number"] },
        sourceResourceId: { type: ["string", "number"] },
        source_resource_id: { type: ["string", "number"] },
        sourceDerivativeId: { type: ["string", "number"] },
        source_derivative_id: { type: ["string", "number"] },
        durationMs: { type: "number" },
        duration_ms: { type: "number" },
        width: { type: "number" },
        height: { type: "number" }
      })
    },
    {
      name: "editing_export_create_candidate",
      description: "Explicitly write an exported RawResource-backed editing result as a content candidate. This is separate from render/export success. HLS MediaStreamArtifact outputs are hosted previews today; writing them as candidates requires a future domain candidate schema extension.",
      inputSchema: objectSchema({
        ...projectLocator,
        contentUnitId: { type: ["string", "number"] },
        content_unit_id: { type: ["string", "number"] },
        resourceId: { type: ["string", "number"] },
        resource_id: { type: ["string", "number"] },
        streamId: { type: ["string", "number"], description: "Known unsupported HLS MediaStreamArtifact ID. Current candidate outputs require resourceId/resource_id." },
        stream_id: { type: ["string", "number"], description: "Alias for streamId." },
        candidateId: { type: ["string", "number"] },
        candidate_id: { type: ["string", "number"] },
        taskId: { type: "string" },
        task_id: { type: "string" },
        editingProjectId: { type: "string" },
        editing_project_id: { type: "string" },
        outputPath: { type: "string" },
        output_path: { type: "string" },
        outputKind: { type: "string" },
        output_kind: { type: "string" },
        kind: { type: "string" },
        mimeType: { type: "string" },
        mime_type: { type: "string" },
        durationMs: { type: "number" },
        duration_ms: { type: "number" },
        durationSec: { type: "number" },
        duration_sec: { type: "number" },
        width: { type: "number" },
        height: { type: "number" },
        source: { type: "string" },
        status: { type: "string" },
        producer: { type: "object", additionalProperties: true },
        provenance: { type: "object", additionalProperties: true },
        promptSnapshot: { type: "object", additionalProperties: true },
        prompt_snapshot: { type: "object", additionalProperties: true }
      })
    }
  ];
}
function externalResourceTools() {
  return [
    {
      name: "movscript_external_resource_source_list",
      description: "List configured external media search sources such as Pexels or Pixabay. Use this when choosing a source_id for external media search.",
      inputSchema: objectSchema(
        {
          include_disabled: { type: "boolean", description: "When true, include disabled sources." },
          includeDisabled: { type: "boolean", description: "Camel-case alias for include_disabled." }
        }
      ),
      outputSchema: objectSchema(
        {
          source: { type: "string" },
          count: { type: "number" },
          items: { type: "array", items: { type: "object", additionalProperties: true } },
          usage: { type: "string" }
        },
        ["source", "count", "items", "usage"]
      )
    },
    {
      name: "movscript_external_resource_search",
      description: "Search external image/video providers configured in MovScript. Results are external media candidates and must be imported into MovScript before they can be used as generation resource IDs.",
      inputSchema: objectSchema(
        {
          query: { type: "string", description: "Search query, e.g. neon city street, handheld office scene, product packshot." },
          q: { type: "string", description: "Alias for query." },
          source_id: { type: "number", description: "External resource source ID. Omit to use the first enabled source." },
          sourceId: { type: "number", description: "Camel-case alias for source_id." },
          media_type: { type: "string", enum: ["image", "video"], description: "Optional media type filter." },
          mediaType: { type: "string", enum: ["image", "video"], description: "Camel-case alias for media_type." },
          orientation: { type: "string", enum: ["all", "landscape", "portrait", "square"], description: "Optional orientation filter." },
          page: { type: "number", description: "1-based page number. Defaults to 1." },
          page_size: { type: "number", description: "Page size, clamped to 1-80. Defaults to 20." },
          pageSize: { type: "number", description: "Camel-case alias for page_size." },
          limit: { type: "number", description: "Alias for page_size." }
        }
      ),
      outputSchema: objectSchema(
        {
          source: { type: "string" },
          source_id: { type: "number" },
          query: { type: "string" },
          media_type: { type: "string" },
          orientation: { type: "string" },
          page: { type: "number" },
          pageSize: { type: "number" },
          total: { type: "number" },
          count: { type: "number" },
          provider: { type: "string" },
          source_name: { type: "string" },
          next_page: { type: "string" },
          items: { type: "array", items: { type: "object", additionalProperties: true } },
          usage: { type: "string" }
        },
        ["source", "source_id", "query", "page", "pageSize", "total", "count", "items", "usage"]
      )
    }
  ];
}
function focusTools() {
  return [
    {
      name: "movscript_focus_get",
      description: "Return the current MovScript task focus: route, selected project, active production id, current user, and selected entity. This does not load project lists, scripts, workspaces, or resources.",
      inputSchema: objectSchema({})
    }
  ];
}
function generationTools() {
  return [
    generationCapabilityListTool(),
    generationPrepareTool(),
    generationSubmitTool(),
    generationJobGetTool(),
    generationJobGetBatchTool(),
    generationResultRegisterTool()
  ];
}
var GENERATION_CAPABILITIES = [
  "image_generation",
  "video_generation",
  "audio_generation",
  "image",
  "image_edit",
  "video",
  "video_i2v",
  "video_v2v",
  "audio_tts",
  "audio_transcribe",
  "audio_translate",
  "audio_music",
  "audio_sfx",
  "audio_chat",
  "voice_clone",
  "voice_design",
  "subtitle_align",
  "subtitle_translate"
];
function generationCapabilityListTool() {
  return {
    name: "generation_capability_list",
    description: "List MovScript generation capabilities accepted by generation_prepare and generation_submit.",
    inputSchema: objectSchema(
      {
        include_models: { type: "boolean", description: "When true, include enabled model counts for each capability." }
      }
    ),
    outputSchema: objectSchema(
      {
        capabilities: { type: "array", items: { type: "string", enum: [...GENERATION_CAPABILITIES] } },
        count: { type: "number" },
        models_by_capability: { type: "object", additionalProperties: true }
      },
      ["capabilities", "count"]
    )
  };
}
function generationPrepareTool() {
  return {
    name: "generation_prepare",
    description: "Prepare a MovScript generation request: validate capability/scope, list usable models, and compile content-unit prompts when scope is content_unit. Capability-family requests must include an explicit operation or generation_intent.operation.",
    inputSchema: generationRequestSchema(["capability"]),
    outputSchema: objectSchema(
      {
        status: { type: "string" },
        capability: { type: "string" },
        scope: { type: "string" },
        model_contracts: { type: "array", items: { type: "object", additionalProperties: true } },
        models: { type: "array", items: { type: "object", additionalProperties: true } },
        prompt: { type: "object", additionalProperties: true },
        blockers: { type: "array", items: { type: "object", additionalProperties: true } },
        message: { type: "string" }
      },
      ["status", "capability", "scope", "message"]
    )
  };
}
function generationSubmitTool() {
  return {
    name: "generation_submit",
    description: "Submit any MovScript generation job through one unified capability contract. Capability-family submissions must include an explicit operation or generation_intent.operation; the tool never infers the operation from resource count. Use scope=content_unit for candidate-producing image/video generation, otherwise outputs are RawResources until explicitly registered.",
    inputSchema: generationRequestSchema(["capability"]),
    outputSchema: objectSchema(
      {
        status: { type: "string" },
        capability: { type: "string" },
        scope: { type: "string" },
        output_kind: { type: "string" },
        generation_mode: { type: "string" },
        candidate_policy: { type: "string" },
        jobId: { type: "number" },
        job_id: { type: "number" },
        terminal: { type: "boolean" },
        monitor: { type: "object", additionalProperties: true },
        surface: { type: "object", additionalProperties: true },
        message: { type: "string" },
        job: { type: "object", additionalProperties: true }
      },
      ["status", "capability", "scope", "jobId", "terminal", "monitor"]
    )
  };
}
function generationJobGetTool() {
  return {
    name: "generation_job_get",
    description: "Fetch any MovScript generation job by id. For content-unit jobs, pass scope=content_unit, contentUnitId, and output_kind so successful terminal polls can create or refresh content-unit candidates.",
    inputSchema: objectSchema(
      {
        jobId: { type: "number", minimum: 1 },
        job_id: { type: "number", minimum: 1 },
        capability: { type: "string", enum: [...GENERATION_CAPABILITIES] },
        scope: { type: "string", enum: ["free", "content_unit", "asset", "storyboard", "keyframe"] },
        projectDir: { type: "string", description: "MovScript project source directory used when creating successful candidates." },
        project_dir: { type: "string", description: "Alias for projectDir." },
        cwd: { type: "string", description: "Alias for projectDir." },
        projectUid: { type: "string", description: "Optional manifest project_uid used for scoped candidate metadata." },
        project_uid: { type: "string", description: "Alias for projectUid." },
        contentUnitId: { type: ["string", "number"], description: "Target MovScript content unit id for automatic candidate creation." },
        content_unit_id: { type: ["string", "number"], description: "Alias for contentUnitId." },
        outputKind: { type: "string", enum: ["image", "video", "audio", "subtitle", "voice_profile", "json"] },
        output_kind: { type: "string", enum: ["image", "video", "audio", "subtitle", "voice_profile", "json"] },
        promptSnapshot: { type: "object", additionalProperties: true },
        prompt_snapshot: { type: "object", additionalProperties: true },
        verbosity: { type: "string", enum: ["summary", "debug"], description: "Use summary for compact polling output; debug includes the full backend job payload." }
      }
    ),
    outputSchema: objectSchema(
      {
        status: { type: "string" },
        generation_mode: { type: "string" },
        candidate_policy: { type: "string" },
        will_auto_select: { type: "boolean" },
        requires_user_adoption: { type: "boolean" },
        jobId: { type: "number" },
        job_id: { type: "number" },
        contentUnitId: { type: ["string", "number"] },
        content_unit_id: { type: ["string", "number"] },
        terminal: { type: "boolean" },
        outputResourceIds: { type: "array", items: { type: "number" } },
        output_resource_ids: { type: "array", items: { type: "number" } },
        candidate_created: { type: "boolean" },
        candidate_count: { type: "number" },
        candidates: { type: "array", items: { type: "object", additionalProperties: true } },
        selected_candidate: { type: "object", additionalProperties: true },
        selected_raw_resource: { type: "object", additionalProperties: true },
        stale_status: { type: "string" },
        frontend: { type: "object", additionalProperties: true },
        job: { type: "object" }
      },
      ["status", "jobId", "terminal", "contentUnitId"]
    )
  };
}
function generationJobGetBatchTool() {
  return {
    name: "generation_job_get_batch",
    description: "Synchronously fetch the latest state of multiple MovScript generation jobs. Items may include capability, scope, contentUnitId, and output_kind.",
    inputSchema: objectSchema(
      {
        jobIds: { type: "array", items: { type: "number" }, description: "Generation job IDs." },
        job_ids: { type: "array", items: { type: "number" }, description: "Alias for jobIds." },
        items: { type: "array", items: { type: "object", additionalProperties: true }, description: "Optional item array. Each item may include jobId or job_id." },
        verbosity: { type: "string", enum: ["summary", "debug"], description: "Use summary for compact polling output; debug includes the full backend job payload." }
      }
    ),
    outputSchema: objectSchema(
      {
        status: { type: "string" },
        total: { type: "number" },
        success_count: { type: "number" },
        failed_count: { type: "number" },
        terminal_count: { type: "number" },
        all_terminal: { type: "boolean" },
        output_resource_ids: { type: "array", items: { type: "number" } },
        items: { type: "array", items: { type: "object", additionalProperties: true } },
        message: { type: "string" }
      },
      ["status", "total", "success_count", "failed_count", "terminal_count", "all_terminal", "items", "message"]
    )
  };
}
function generationResultRegisterTool() {
  return {
    name: "generation_result_register",
    description: "Register an existing generation RawResource as a content-unit candidate. Use this when a low-level generation result should enter domain candidate review.",
    inputSchema: objectSchema(
      {
        contentUnitId: { type: ["string", "number"], description: "Target MovScript content unit id." },
        content_unit_id: { type: ["string", "number"], description: "Alias for contentUnitId." },
        resourceId: { type: "number", minimum: 1 },
        resource_id: { type: "number", minimum: 1 },
        outputKind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
        output_kind: { type: "string", enum: ["image", "video", "audio", "text", "metadata"] },
        candidateId: { type: "string" },
        candidate_id: { type: "string" },
        title: { type: "string" },
        projectDir: { type: "string" },
        project_dir: { type: "string" },
        cwd: { type: "string" },
        projectUid: { type: "string" },
        project_uid: { type: "string" },
        metadata: { type: "object", additionalProperties: true }
      },
      ["contentUnitId", "resourceId"]
    ),
    outputSchema: objectSchema(
      {
        status: { type: "string" },
        candidate: { type: "object", additionalProperties: true },
        surface: { type: "object", additionalProperties: true }
      }
    )
  };
}
function generationRequestSchema(required = []) {
  return objectSchema(
    {
      capability: { type: "string", enum: [...GENERATION_CAPABILITIES], description: "MovScript generation capability. Prefer generation families such as image_generation, video_generation, or audio_generation plus an explicit operation." },
      scope: { type: "string", enum: ["free", "content_unit", "asset", "storyboard", "keyframe"], description: "Generation target scope. content_unit image/video jobs create candidates on successful terminal polling." },
      prompt: { type: "string", minLength: 1 },
      title: { type: "string" },
      model_id: { type: "string" },
      provider_id: { type: "string" },
      operation: {
        type: "string",
        enum: [
          "text_to_image",
          "reference_to_image",
          "image_to_image",
          "prompt_to_video",
          "reference_to_video",
          "image_to_video",
          "first_frame_to_video",
          "first_last_frame_to_video",
          "video_to_video",
          "video_edit",
          "video_extend",
          "video_inpaint",
          "object_insert",
          "object_remove",
          "motion_control",
          "lip_sync",
          "video_upscale",
          "tts",
          "stt",
          "speech_translate",
          "audio_chat",
          "voice_clone",
          "voice_design",
          "dubbing",
          "music",
          "sfx",
          "speech_enhancement"
        ],
        description: "Explicit model operation intent. Required for image_generation, video_generation, and audio_generation; agents must choose this instead of relying on resource count, route, provider, or adapter details."
      },
      model_operation: { type: "string", description: "Alias for operation." },
      generation_intent: {
        type: "object",
        additionalProperties: true,
        description: 'Explicit capability intent, e.g. {capability:"video_generation",operation:"first_last_frame_to_video",reference_assets:[...]}'
      },
      reference_assets: {
        type: "array",
        items: {
          type: "object",
          additionalProperties: true,
          properties: {
            role: { type: "string", enum: ["generic", "first_frame", "last_frame", "reference_image", "reference_video", "reference_audio"] },
            media_type: { type: "string", enum: ["image", "video", "audio"] },
            resource_id: { type: "number", minimum: 1 }
          },
          required: ["role"]
        },
        description: "Semantic roles for input/reference resources. Use reference_image/reference_video/reference_audio for omni reference video, and first_frame/last_frame for first-last video generation."
      },
      parameter_mode: { type: "string", enum: ["compatible", "strict"] },
      param_mode: { type: "string", enum: ["compatible", "strict"] },
      projectDir: { type: "string", description: "MovScript project source directory." },
      project_dir: { type: "string", description: "Alias for projectDir." },
      cwd: { type: "string", description: "Alias for projectDir." },
      projectUid: { type: "string", description: "Optional manifest project_uid used to namespace generated job metadata." },
      project_uid: { type: "string", description: "Alias for projectUid." },
      contentUnitId: { type: ["string", "number"], description: "Target content unit for scope=content_unit." },
      content_unit_id: { type: ["string", "number"], description: "Alias for contentUnitId." },
      candidateId: { type: "string" },
      candidate_id: { type: "string" },
      candidate_policy: { type: "string", enum: ["none", "auto_create", "register_existing"] },
      input_resource_ids: { type: "array", items: { type: "number" } },
      reference_resource_ids: { type: "array", items: { type: "number" } },
      negative_prompt: { type: "string" },
      aspect_ratio: { type: "string" },
      image_size: { type: "string" },
      duration: { type: "number", minimum: 0 },
      quality: { type: "string" },
      steps: { type: "number", minimum: 1 },
      seed: { type: "number" },
      fps: { type: "number", minimum: 1 },
      voice: { type: "string" },
      language: { type: "string" },
      source_language: { type: "string" },
      target_language: { type: "string" },
      model: { type: "string" },
      audio_format: { type: "string" },
      response_format: { type: "string" },
      output_format: { type: "string" },
      subtitle_format: { type: "string" },
      style: { type: "string" },
      speed: { type: "number", minimum: 0.25, maximum: 4 },
      instructions: { type: "string" },
      extra_params: { type: "object", additionalProperties: true },
      timeout_ms: { type: "number", minimum: 1 },
      poll_interval_ms: { type: "number", minimum: 1 }
    },
    required
  );
}
function modelTools() {
  return [
    {
      name: "generation_model_list",
      description: "List enabled generation and speech models for an AI runtime capability and optional model operation intent. Use capability plus operation for family capabilities, e.g. video_generation + first_last_frame_to_video or audio_generation + music. The result exposes public model contracts only; provider routes, adapters, and endpoints are Admin/debug details.",
      inputSchema: objectSchema(
        {
          capability: { type: "string", description: "Optional AI capability family such as text_generation, image_generation, video_generation, audio_generation, or an execution capability such as audio_music. Family capabilities should pair this with operation." },
          operation: { type: "string", description: "Optional model operation intent such as text_to_image, image_to_image, first_last_frame_to_video, music, sfx, tts, or stt. If capability is omitted, known operations imply image_generation, video_generation, or audio_generation." },
          model_operation: { type: "string", description: "Alias for operation." },
          reference_assets: {
            type: "array",
            description: "Optional reference asset intent used to narrow route-capable models without exposing route details.",
            items: {
              type: "object",
              additionalProperties: false,
              required: ["role"],
              properties: {
                role: { type: "string", description: "Reference role such as generic, reference_image, reference_video, reference_audio, first_frame, or last_frame." },
                media_type: { type: "string", enum: ["image", "video", "audio"], description: "Optional media type hint." }
              }
            }
          },
          provider_variants: { type: "boolean", description: "Admin/debug hint for backend querying; returned records remain public model contracts without route details." },
          include_provider_variants: { type: "boolean", description: "Alias for provider_variants." }
        }
      ),
      outputSchema: objectSchema(
        {
          count: { type: "number" },
          queries: { type: "array", items: { type: "string" } },
          model_contracts: {
            type: "array",
            items: {
              type: "object",
              additionalProperties: true,
              required: ["contract_version", "model_id", "capabilities", "input_requirements", "supported_param_keys", "supported_params"],
              properties: {
                contract_version: { type: "number", const: 1 },
                model_id: { type: "string", description: "Public logical model ID to pass to plugin generation tools." },
                display_name: { type: "string" },
                short_name: { type: "string" },
                logical_model_id: { type: "string", description: "Legacy alias for model_id." },
                capabilities: { type: "array", items: { type: "string" } },
                accepts_image_input: { type: "boolean" },
                input_requirements: {
                  type: "object",
                  required: ["image", "video"],
                  properties: {
                    image: {
                      type: "object",
                      required: ["min", "max"],
                      properties: {
                        min: { type: "number" },
                        max: { type: "number" }
                      }
                    },
                    video: {
                      type: "object",
                      required: ["min", "max"],
                      properties: {
                        min: { type: "number" },
                        max: { type: "number" }
                      }
                    }
                  }
                },
                supported_param_keys: { type: "array", items: { type: "string" } },
                supported_params: {
                  type: "array",
                  items: {
                    type: "object",
                    additionalProperties: true,
                    required: ["key"],
                    properties: {
                      key: { type: "string" },
                      label: { type: "string" },
                      type: { type: "string", enum: ["select", "number", "boolean", "string"] },
                      options: { type: "array", items: { type: "string" } },
                      conflicts_with: { type: "array", items: { type: "string" } },
                      conditional_enum: { type: "array" },
                      conditional_const: { type: "array" },
                      requires_value: { type: "array" }
                    }
                  }
                },
                params_schema_loaded: { type: "boolean" },
                params_schema_rule_count: { type: "number" }
              }
            }
          },
          models: { type: "array", description: "Compatibility alias for model_contracts; contains public model contracts, not raw provider route records." }
        },
        ["count", "queries", "model_contracts", "models"]
      )
    }
  ];
}
function resourceLibraryTools() {
  return [
    {
      name: "movscript_resource_library_query",
      description: "Query MovScript RawResources stored in the internal resource library. Use returned RawResource.ID values as input_resource_ids or reference_resource_ids for generation.",
      inputSchema: objectSchema(
        {
          query: { type: "string", description: "Search resource names." },
          q: { type: "string", description: "Alias for query." },
          resource_id: { type: "number", description: "Optional RawResource ID to filter the requested page." },
          resourceId: { type: "number", description: "Camel-case alias for resource_id." },
          id: { type: "number", description: "Alias for resource_id." },
          type: { type: "string", enum: ["image", "video", "audio", "text", "file"], description: "Optional MovScript resource type filter." },
          media_type: { type: "string", enum: ["image", "video", "audio", "text", "file"], description: "Alias for type." },
          scope: { type: "string", enum: ["personal", "team"], description: "Optional library scope. Omit for the unified visible library." },
          folder_id: { type: "string", description: 'Optional folder filter. Use "root" or "0" for unfiled resources.' },
          folderId: { type: "string", description: "Camel-case alias for folder_id." },
          page: { type: "number", description: "1-based page number. Defaults to 1." },
          page_size: { type: "number", description: "Page size, clamped to 1-100. Defaults to 20." },
          pageSize: { type: "number", description: "Camel-case alias for page_size." },
          limit: { type: "number", description: "Alias for page_size." },
          include_full: { type: "boolean", description: "When true, return full backend RawResource records." },
          includeFull: { type: "boolean", description: "Camel-case alias for include_full." }
        }
      ),
      outputSchema: objectSchema(
        {
          source: { type: "string" },
          query: { type: "string" },
          type: { type: "string" },
          scope: { type: "string" },
          folder_id: { type: "string" },
          page: { type: "number" },
          pageSize: { type: "number" },
          total: { type: "number" },
          count: { type: "number" },
          items: { type: "array", items: { type: "object", additionalProperties: true } },
          usage: { type: "string" },
          warning: { type: "string" }
        },
        ["source", "query", "page", "pageSize", "total", "count", "items", "usage"]
      )
    },
    {
      name: "movscript_resource_library_open",
      description: "Return an agent in-app browser URL for the MovScript resource library. The frontend page uses the local MCP proxy so the active agent context is forwarded without exposing a bearer token in the URL.",
      inputSchema: objectSchema(
        {
          frontend_origin: { type: "string", description: "Optional frontend origin, such as http://127.0.0.1:5173. Defaults to MOVSCRIPT_FRONTEND_ORIGIN or the dev frontend." },
          frontendOrigin: { type: "string", description: "Camel-case alias for frontend_origin." },
          mcp_base_url: { type: "string", description: "Optional MCP server origin. Defaults to MOVSCRIPT_MCP_ENDPOINT origin." },
          mcpBaseURL: { type: "string", description: "Camel-case alias for mcp_base_url." }
        }
      ),
      outputSchema: objectSchema(
        {
          source: { type: "string" },
          kind: { type: "string" },
          title: { type: "string" },
          url: { type: "string" },
          frontend_origin: { type: "string" },
          mcp_api_base_url: { type: "string" },
          route: { type: "string" },
          api_proxy: { type: "object", additionalProperties: true },
          backend_api_base_url: { type: "string" },
          usage: { type: "string" }
        },
        ["source", "kind", "title", "url", "frontend_origin", "mcp_api_base_url", "route", "api_proxy", "usage"]
      )
    }
  ];
}
function resourceMediaTools() {
  return [
    {
      name: "movscript_resource_image_read",
      description: "Read a MovScript image RawResource and return it as MCP image content for Codex vision. Use this when the agent needs to inspect the actual pixels of an image resource.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        max_bytes: { type: "number", description: "Maximum image file size to return. Defaults to 8 MiB, hard-capped at 20 MiB." },
        maxBytes: { type: "number", description: "Camel-case alias for max_bytes." },
        mode: { type: "string", enum: ["fit", "original"], description: "Image read mode. fit validates and downsizes to max_width/max_height when needed; original validates but returns the original bytes." },
        detail: { type: "string", enum: ["high", "auto", "low", "original"], description: "Codex-style image detail hint. detail=original is equivalent to mode=original; other values use mode=fit." },
        max_width: { type: "number", description: "Maximum output width for mode=fit. Defaults to 1568, hard-capped at 4096." },
        maxWidth: { type: "number", description: "Camel-case alias for max_width." },
        max_height: { type: "number", description: "Maximum output height for mode=fit. Defaults to 1568, hard-capped at 4096." },
        maxHeight: { type: "number", description: "Camel-case alias for max_height." },
        mime_type: { type: "string", description: "Optional MIME type override when backend headers are missing." },
        mimeType: { type: "string", description: "Camel-case alias for mime_type." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          resource_id: { type: "number" },
          mode: { type: "string" },
          source_mime_type: { type: "string" },
          source_size_bytes: { type: "number" },
          source_width: { type: "number" },
          source_height: { type: "number" },
          mime_type: { type: "string" },
          size_bytes: { type: "number" },
          width: { type: "number" },
          height: { type: "number" },
          resized: { type: "boolean" },
          max_width: { type: "number" },
          max_height: { type: "number" },
          image_payload: { type: "string" }
        },
        ["status", "resource_id", "mode", "source_mime_type", "source_size_bytes", "source_width", "source_height", "mime_type", "size_bytes", "width", "height", "resized", "image_payload"]
      )
    },
    {
      name: "movscript_resource_image_transform_to_resource",
      description: "Transform a MovScript image RawResource with optional crop, resize/fit, and format conversion, then upload the result as a new image RawResource. This is a stable resource operation and does not update business shot or candidate state.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript image RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        crop_x: { type: "number", description: "Optional crop rectangle x coordinate in source pixels. Defaults to 0." },
        cropX: { type: "number", description: "Camel-case alias for crop_x." },
        crop_y: { type: "number", description: "Optional crop rectangle y coordinate in source pixels. Defaults to 0." },
        cropY: { type: "number", description: "Camel-case alias for crop_y." },
        crop_width: { type: "number", description: "Optional crop rectangle width in source pixels." },
        cropWidth: { type: "number", description: "Camel-case alias for crop_width." },
        crop_height: { type: "number", description: "Optional crop rectangle height in source pixels." },
        cropHeight: { type: "number", description: "Camel-case alias for crop_height." },
        width: { type: "number", description: "Optional exact output width. Preserves aspect ratio when height is omitted." },
        height: { type: "number", description: "Optional exact output height. Preserves aspect ratio when width is omitted." },
        max_width: { type: "number", description: "Optional maximum output width for fit resize." },
        maxWidth: { type: "number", description: "Camel-case alias for max_width." },
        max_height: { type: "number", description: "Optional maximum output height for fit resize." },
        maxHeight: { type: "number", description: "Camel-case alias for max_height." },
        output_format: { type: "string", enum: ["jpeg", "png", "webp"], description: "Output image format. Defaults from source MIME type, falling back to png." },
        outputFormat: { type: "string", enum: ["jpeg", "png", "webp"], description: "Camel-case alias for output_format." },
        image_format: { type: "string", enum: ["jpeg", "png", "webp"], description: "Alias for output_format." },
        imageFormat: { type: "string", enum: ["jpeg", "png", "webp"], description: "Camel-case alias for image_format." },
        filename: { type: "string", description: "Optional output image filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        mime_type: { type: "string", description: "Optional source MIME type override when backend headers are missing." },
        mimeType: { type: "string", description: "Camel-case alias for mime_type." },
        max_source_bytes: { type: "number", description: "Maximum source image file size to download. Defaults to 8 MiB, hard-capped at 20 MiB." },
        maxSourceBytes: { type: "number", description: "Camel-case alias for max_source_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated image upload size. Defaults to 20 MiB, hard-capped at 100 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          source_resource_id: { type: "number" },
          image_resource_id: { type: "number" },
          resource_id: { type: "number" },
          source_mime_type: { type: "string" },
          source_size_bytes: { type: "number" },
          source_width: { type: "number" },
          source_height: { type: "number" },
          mime_type: { type: "string" },
          size_bytes: { type: "number" },
          width: { type: "number" },
          height: { type: "number" },
          resource: { type: "object", additionalProperties: true },
          transform: { type: "object", additionalProperties: true },
          message: { type: "string" }
        },
        ["status", "source_resource_id", "image_resource_id", "resource_id", "mime_type", "width", "height", "message"]
      )
    },
    {
      name: "movscript_resource_video_extract_frames",
      description: "Download a MovScript video RawResource, extract representative or precise frames with ffmpeg, and return the frames as MCP image content for Codex vision. Supports overview, timestamps, range, and burst sampling. The original video is not sent to the model.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        mode: { type: "string", enum: ["overview", "timestamps", "range", "burst"], description: "Sampling mode. overview samples the full video, timestamps uses exact seconds, range samples between start/end, burst samples a window around center." },
        count: { type: "number", description: "Overview frame count. Defaults to 4." },
        frame_count: { type: "number", description: "Alias for count." },
        max_frames: { type: "number", description: "Maximum frames returned for any mode. Defaults to 12, hard-capped at 24." },
        maxFrames: { type: "number", description: "Camel-case alias for max_frames." },
        max_video_bytes: { type: "number", description: "Maximum source video file size to download for extraction. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        timestamps_sec: { type: "array", items: { type: "number" }, description: "Optional exact timestamps in seconds." },
        timestampsSec: { type: "array", items: { type: "number" }, description: "Camel-case alias for timestamps_sec." },
        start_sec: { type: "number", description: "Start timestamp for evenly-spaced sampling when timestamps_sec is omitted." },
        startSec: { type: "number", description: "Camel-case alias for start_sec." },
        end_sec: { type: "number", description: "End timestamp for range sampling." },
        endSec: { type: "number", description: "Camel-case alias for end_sec." },
        center_sec: { type: "number", description: "Center timestamp for burst sampling." },
        centerSec: { type: "number", description: "Camel-case alias for center_sec." },
        window_sec: { type: "number", description: "Window length in seconds for burst sampling. Defaults to 2." },
        windowSec: { type: "number", description: "Camel-case alias for window_sec." },
        fps: { type: "number", description: "Range/burst sampling frequency in frames per second. Defaults to 2, capped at 6." },
        interval_sec: { type: "number", description: "Sampling interval in seconds when timestamps_sec is omitted. Defaults to 3." },
        intervalSec: { type: "number", description: "Camel-case alias for interval_sec." },
        max_width: { type: "number", description: "Maximum output frame width. Defaults to 960, hard-capped at 1920." },
        maxWidth: { type: "number", description: "Camel-case alias for max_width." },
        image_format: { type: "string", description: "Output frame format: jpeg or png. Defaults to jpeg." },
        imageFormat: { type: "string", description: "Camel-case alias for image_format." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          resource_id: { type: "number" },
          source_mime_type: { type: "string" },
          source_size_bytes: { type: "number" },
          max_video_bytes: { type: "number" },
          video: { type: "object", additionalProperties: true },
          sampling: { type: "object", additionalProperties: true },
          max_width: { type: "number" },
          frames: { type: "array", items: { type: "object", additionalProperties: true } },
          warnings: { type: "array", items: { type: "string" } },
          message: { type: "string" }
        },
        ["status", "resource_id", "sampling", "frames", "message"]
      )
    },
    {
      name: "movscript_resource_video_probe",
      description: "Probe a MovScript video RawResource with ffprobe and return stable media metadata such as duration, dimensions, and fps. This is a read-only resource inspection tool.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript video RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        max_video_bytes: { type: "number", description: "Maximum source video file size to download for probing. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          resource_id: { type: "number" },
          source_mime_type: { type: "string" },
          source_size_bytes: { type: "number" },
          max_video_bytes: { type: "number" },
          video: { type: "object", additionalProperties: true },
          message: { type: "string" }
        },
        ["status", "resource_id", "video", "message"]
      )
    },
    {
      name: "movscript_resource_video_extract_frame_to_resource",
      description: "Extract one precise frame from a MovScript video RawResource and upload it as a new image RawResource. Use the returned image_resource_id as generation input_resource_ids or reference_resource_ids.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript video RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        timestamp_sec: { type: "number", description: "Frame timestamp in seconds." },
        timestampSec: { type: "number", description: "Camel-case alias for timestamp_sec." },
        max_video_bytes: { type: "number", description: "Maximum source video file size to download for extraction. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated image upload size. Defaults to 20 MiB, hard-capped at 100 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." },
        max_width: { type: "number", description: "Maximum output frame width. Defaults to 960, hard-capped at 1920." },
        maxWidth: { type: "number", description: "Camel-case alias for max_width." },
        image_format: { type: "string", enum: ["jpeg", "png"], description: "Output frame format. Defaults to jpeg." },
        imageFormat: { type: "string", enum: ["jpeg", "png"], description: "Camel-case alias for image_format." },
        filename: { type: "string", description: "Optional output image filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          source_resource_id: { type: "number" },
          image_resource_id: { type: "number" },
          resource_id: { type: "number" },
          timestamp_sec: { type: "number" },
          frame: { type: "object", additionalProperties: true },
          video: { type: "object", additionalProperties: true },
          message: { type: "string" }
        },
        ["status", "source_resource_id", "image_resource_id", "resource_id", "timestamp_sec", "message"]
      )
    },
    {
      name: "movscript_resource_video_extract_frames_to_resources",
      description: "Extract multiple frames from a MovScript video RawResource and upload each frame as a new image RawResource. Supports the same overview, timestamps, range, and burst sampling modes as movscript_resource_video_extract_frames.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript video RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        mode: { type: "string", enum: ["overview", "timestamps", "range", "burst"], description: "Sampling mode. overview samples the full video, timestamps uses exact seconds, range samples between start/end, burst samples a window around center." },
        count: { type: "number", description: "Overview frame count. Defaults to 4." },
        frame_count: { type: "number", description: "Alias for count." },
        max_frames: { type: "number", description: "Maximum frames uploaded for any mode. Defaults to 12, hard-capped at 24." },
        maxFrames: { type: "number", description: "Camel-case alias for max_frames." },
        timestamps_sec: { type: "array", items: { type: "number" }, description: "Optional exact timestamps in seconds." },
        timestampsSec: { type: "array", items: { type: "number" }, description: "Camel-case alias for timestamps_sec." },
        start_sec: { type: "number", description: "Start timestamp for range sampling." },
        startSec: { type: "number", description: "Camel-case alias for start_sec." },
        end_sec: { type: "number", description: "End timestamp for range sampling." },
        endSec: { type: "number", description: "Camel-case alias for end_sec." },
        center_sec: { type: "number", description: "Center timestamp for burst sampling." },
        centerSec: { type: "number", description: "Camel-case alias for center_sec." },
        window_sec: { type: "number", description: "Window length in seconds for burst sampling. Defaults to 2." },
        windowSec: { type: "number", description: "Camel-case alias for window_sec." },
        fps: { type: "number", description: "Range/burst sampling frequency in frames per second. Defaults to 2, capped at 6." },
        interval_sec: { type: "number", description: "Sampling interval in seconds when timestamps_sec is omitted. Defaults to 3." },
        intervalSec: { type: "number", description: "Camel-case alias for interval_sec." },
        max_video_bytes: { type: "number", description: "Maximum source video file size to download for extraction. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated image upload size per frame. Defaults to 20 MiB, hard-capped at 100 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." },
        max_width: { type: "number", description: "Maximum output frame width. Defaults to 960, hard-capped at 1920." },
        maxWidth: { type: "number", description: "Camel-case alias for max_width." },
        image_format: { type: "string", enum: ["jpeg", "png"], description: "Output frame format. Defaults to jpeg." },
        imageFormat: { type: "string", enum: ["jpeg", "png"], description: "Camel-case alias for image_format." },
        filename: { type: "string", description: "Optional base output image filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          resource_id: { type: "number" },
          source_resource_id: { type: "number" },
          video: { type: "object", additionalProperties: true },
          sampling: { type: "object", additionalProperties: true },
          count: { type: "number" },
          resource_ids: { type: "array", items: { type: "number" } },
          image_resource_ids: { type: "array", items: { type: "number" } },
          items: { type: "array", items: { type: "object", additionalProperties: true } },
          warnings: { type: "array", items: { type: "string" } },
          message: { type: "string" }
        },
        ["status", "resource_id", "source_resource_id", "sampling", "count", "resource_ids", "items", "message"]
      )
    },
    {
      name: "movscript_resource_video_trim_to_resource",
      description: "Neutral resource preparation: trim a MovScript video RawResource to a new MP4 video RawResource for references, drafts, or generation inputs. This is not the product editing path; normal timeline trim, render, export, and candidate creation should use MediaEditingProject plus editing_* tools through Electron mediaPipeline. This tool does not update any business candidate, selection, or shot state.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript video RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        start_sec: { type: "number", description: "Clip start time in seconds. Defaults to 0." },
        startSec: { type: "number", description: "Camel-case alias for start_sec." },
        end_sec: { type: "number", description: "Clip end time in seconds." },
        endSec: { type: "number", description: "Camel-case alias for end_sec." },
        duration_sec: { type: "number", description: "Clip duration in seconds. Used when end_sec is omitted." },
        durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
        mode: { type: "string", enum: ["accurate", "fast"], description: "Trim mode. accurate re-encodes to browser-friendly MP4; fast stream-copies when possible." },
        volume: { type: "number", description: "Optional audio volume percentage, 0-200. Forces accurate mode behavior." },
        muted: { type: "boolean", description: "When true, omit audio from the output." },
        filename: { type: "string", description: "Optional output MP4 filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        max_video_bytes: { type: "number", description: "Maximum source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated video upload size. Defaults to 100 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
      })
    },
    {
      name: "movscript_resource_video_compose_to_resource",
      description: "Resource-level video utility: compose video RawResources in sequence into a new MP4 video RawResource. Items are generic media clips, not business shots; each may specify start/end/duration, volume, and muted. This is not the product editing path; normal timeline editing, render, HLS packaging, transcode, reframe, and export should use MediaEditingProject plus editing_* tools through Electron mediaPipeline.",
      inputSchema: objectSchema({
        items: {
          type: "array",
          description: "Ordered video clip items. Each item requires resource_id and may include start_sec, end_sec, duration_sec, volume, and muted.",
          items: { type: "object", additionalProperties: true }
        },
        filename: { type: "string", description: "Optional output MP4 filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        max_video_bytes: { type: "number", description: "Maximum per-source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated video upload size. Defaults to 100 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
      }, ["items"])
    },
    {
      name: "movscript_resource_video_concat_to_resource",
      description: "Resource-level video utility: alias for movscript_resource_video_compose_to_resource for simple ordered concatenation into a new MP4 video RawResource. This is not the product editing path; normal timeline editing, render, HLS packaging, transcode, reframe, and export should use MediaEditingProject plus editing_* tools through Electron mediaPipeline.",
      inputSchema: objectSchema({
        items: {
          type: "array",
          description: "Ordered video clip items. Each item requires resource_id and may include start_sec, end_sec, duration_sec, volume, and muted.",
          items: { type: "object", additionalProperties: true }
        },
        filename: { type: "string", description: "Optional output MP4 filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        max_video_bytes: { type: "number", description: "Maximum per-source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated video upload size. Defaults to 100 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
      }, ["items"])
    },
    {
      name: "movscript_resource_video_contact_sheet_to_resource",
      description: "Create a contact sheet image from a MovScript video RawResource and upload it as a new image RawResource. This gives agents a reusable overview image resource without binding to business shot state.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript video RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        count: { type: "number", description: "Approximate number of frames to place in the sheet. Defaults to 9, capped at 64." },
        columns: { type: "number", description: "Number of sheet columns. Defaults to 3." },
        thumb_width: { type: "number", description: "Thumbnail width in pixels. Defaults to 320." },
        thumbWidth: { type: "number", description: "Camel-case alias for thumb_width." },
        interval_sec: { type: "number", description: "Frame sampling interval in seconds. Defaults from video duration/count when duration can be probed." },
        intervalSec: { type: "number", description: "Camel-case alias for interval_sec." },
        filename: { type: "string", description: "Optional output image filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        max_video_bytes: { type: "number", description: "Maximum source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated image upload size. Defaults to 20 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
      })
    },
    {
      name: "movscript_resource_video_extract_audio_to_resource",
      description: "Extract the first audio stream from a MovScript video RawResource and upload it as a new audio RawResource. Optional start/end/duration trims the extracted audio.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "MovScript video RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        start_sec: { type: "number", description: "Optional audio start time in seconds." },
        startSec: { type: "number", description: "Camel-case alias for start_sec." },
        end_sec: { type: "number", description: "Optional audio end time in seconds." },
        endSec: { type: "number", description: "Camel-case alias for end_sec." },
        duration_sec: { type: "number", description: "Optional audio duration in seconds. Used when end_sec is omitted." },
        durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
        filename: { type: "string", description: "Optional output audio filename." },
        name: { type: "string", description: "Alias for filename." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        max_video_bytes: { type: "number", description: "Maximum source video file size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." },
        max_upload_bytes: { type: "number", description: "Maximum generated audio upload size. Defaults to 100 MiB." },
        maxUploadBytes: { type: "number", description: "Camel-case alias for max_upload_bytes." }
      })
    },
    {
      name: "movscript_resource_image_annotate",
      description: "Create a simple agent-authored visual guidance image by overlaying structured annotations on a MovScript image resource, data URL, or local artifact. Writes an SVG artifact to the cache by default and returns artifact_path metadata only; image bytes are not returned as MCP image content. Upload artifact_path with movscript_resource_upload before using it in generation.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "Optional MovScript image RawResource ID used as the annotation background." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        data_url: { type: "string", description: "Optional image data URL used as the annotation background." },
        dataUrl: { type: "string", description: "Camel-case alias for data_url." },
        local_path: { type: "string", description: "Optional local image path used as the annotation background." },
        localPath: { type: "string", description: "Camel-case alias for local_path." },
        artifact_path: { type: "string", description: "Alias for local_path, useful for annotating a previous agent artifact." },
        artifactPath: { type: "string", description: "Camel-case alias for artifact_path." },
        mime_type: { type: "string", description: "Optional source image MIME type override." },
        mimeType: { type: "string", description: "Camel-case alias for mime_type." },
        max_source_bytes: { type: "number", description: "Maximum source image size to read. Defaults to 8 MiB, hard-capped at 20 MiB." },
        maxSourceBytes: { type: "number", description: "Camel-case alias for max_source_bytes." },
        width: { type: "number", description: "SVG coordinate width. Defaults to source image width when readable." },
        height: { type: "number", description: "SVG coordinate height. Defaults to source image height when readable." },
        title: { type: "string", description: "Artifact title used in metadata and default filename." },
        note: { type: "string", description: "Optional note rendered at the bottom of the annotation image." },
        annotations: {
          type: "array",
          description: "Structured annotation shapes. Supported type values: rect, circle, line, arrow, text, highlight.",
          items: { type: "object", additionalProperties: true }
        },
        shapes: { type: "array", items: { type: "object", additionalProperties: true }, description: "Alias for annotations." },
        output_path: { type: "string", description: "Optional absolute output path for the generated SVG artifact." },
        outputPath: { type: "string", description: "Camel-case alias for output_path." },
        workspace_path: { type: "string", description: "Optional output path under the MovScript .movscript workspace root." },
        workspacePath: { type: "string", description: "Camel-case alias for workspace_path." },
        workspaceDir: { type: "string", description: "Optional MovScript workspace root directory. Defaults to the desktop workspace root." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          artifact_path: { type: "string" },
          mime_type: { type: "string" },
          size_bytes: { type: "number" },
          width: { type: "number" },
          height: { type: "number" },
          source: { type: "object", additionalProperties: true },
          annotation_count: { type: "number" },
          annotations: { type: "array", items: { type: "object", additionalProperties: true } },
          artifact_location: { type: "string" },
          cache_dir: { type: "string" },
          image_payload: { type: "string" },
          mcp_image_content: { type: "boolean" },
          message: { type: "string" }
        },
        ["status", "artifact_path", "mime_type", "size_bytes", "annotation_count", "image_payload", "mcp_image_content"]
      )
    },
    {
      name: "movscript_resource_upload",
      description: "Upload an agent-accessible image or video artifact to the MovScript RawResource library. Accepts artifact/local paths, .movscript workspace paths, data URLs, or base64 bytes. Use the returned resource_id in shot groups or generation input_resource_ids/reference_resource_ids.",
      inputSchema: objectSchema({
        artifact_path: { type: "string", description: "Local artifact path returned by movscript_resource_image_annotate or another agent tool." },
        artifactPath: { type: "string", description: "Camel-case alias for artifact_path." },
        local_path: { type: "string", description: "Local file path to upload." },
        localPath: { type: "string", description: "Camel-case alias for local_path." },
        path: { type: "string", description: "Alias for local_path." },
        workspace_path: { type: "string", description: "Path under the MovScript .movscript workspace root." },
        workspacePath: { type: "string", description: "Camel-case alias for workspace_path." },
        workspaceDir: { type: "string", description: "Optional MovScript workspace root directory. Defaults to the desktop workspace root." },
        data_url: { type: "string", description: "Image data URL to upload." },
        dataUrl: { type: "string", description: "Camel-case alias for data_url." },
        base64: { type: "string", description: "Base64 image payload without the data URL prefix." },
        filename: { type: "string", description: "Resource filename. Defaults to the local file name or a generated guidance filename." },
        name: { type: "string", description: "Alias for filename." },
        mime_type: { type: "string", description: "Upload MIME type. Defaults from filename, including common image/video extensions." },
        mimeType: { type: "string", description: "Camel-case alias for mime_type." },
        folder_id: { type: "string", description: "Optional resource library folder ID." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        max_bytes: { type: "number", description: "Maximum upload input size. Defaults to 20 MiB, hard-capped at 100 MiB." },
        maxBytes: { type: "number", description: "Camel-case alias for max_bytes." }
      }),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          resource_id: { type: "number" },
          resource: { type: "object", additionalProperties: true },
          source: { type: "object", additionalProperties: true },
          filename: { type: "string" },
          mime_type: { type: "string" },
          size_bytes: { type: "number" },
          message: { type: "string" }
        },
        ["status", "resource", "filename", "mime_type", "size_bytes", "message"]
      )
    },
    {
      name: "movscript_resource_upload_batch",
      description: "Upload multiple agent-accessible image or video artifacts to the MovScript RawResource library. Each item accepts the same fields as movscript_resource_upload. Results are returned in input order with per-item errors so agents can keep partial successes.",
      inputSchema: objectSchema({
        items: {
          type: "array",
          description: "Upload item array. Each item accepts artifact_path/local_path/workspace_path/data_url/base64 plus optional filename, mime_type, folder_id, and max_bytes.",
          items: { type: "object", additionalProperties: true }
        },
        folder_id: { type: "string", description: "Default resource library folder ID applied to items that omit folder_id." },
        folderId: { type: "string", description: "Camel-case alias for folder_id." },
        max_bytes: { type: "number", description: "Default maximum upload input size applied to items that omit max_bytes. Defaults to 20 MiB, hard-capped at 100 MiB." },
        maxBytes: { type: "number", description: "Camel-case alias for max_bytes." },
        workspaceDir: { type: "string", description: "Default MovScript workspace root directory applied to workspace_path items." },
        continue_on_error: { type: "boolean", description: "When true, continue after item failures. Defaults to true." },
        continueOnError: { type: "boolean", description: "Camel-case alias for continue_on_error." },
        max_concurrency: { type: "number", description: "Maximum concurrent uploads. Defaults to 3 when continue_on_error is true; forced to 1 when false." },
        maxConcurrency: { type: "number", description: "Camel-case alias for max_concurrency." }
      }, ["items"]),
      outputSchema: objectSchema(
        {
          status: { type: "string" },
          total: { type: "number" },
          success_count: { type: "number" },
          failed_count: { type: "number" },
          items: { type: "array", items: { type: "object", additionalProperties: true } },
          resource_ids: { type: "array", items: { type: "number" } },
          message: { type: "string" }
        },
        ["status", "total", "success_count", "failed_count", "items", "resource_ids", "message"]
      )
    }
  ];
}
function shotLibraryTools() {
  return [
    {
      name: "movscript_shot_library_query",
      description: "Query the MovScript shot reference library for reusable camera, composition, movement, narrative, emotion, and production patterns. Use this before generation when the user asks for a shot style, camera plan, reference shot, or reusable visual pattern.",
      inputSchema: objectSchema(
        {
          query: { type: "string", description: "Natural-language or tag query, e.g. \u89D2\u8272\u53D1\u73B0\u771F\u76F8\u524D, slow push in, foreground obstruction, tension." },
          q: { type: "string", description: "Alias for query." },
          shot_reference_id: { type: "number", description: "Optional shot reference ID to filter the requested page." },
          shotReferenceId: { type: "number", description: "Camel-case alias for shot_reference_id." },
          id: { type: "number", description: "Alias for shot_reference_id." },
          group_id: { type: "number", description: "Optional shot reference group ID. When present, returns shots in that group." },
          groupId: { type: "number", description: "Camel-case alias for group_id." },
          page: { type: "number", description: "1-based page number. Defaults to 1." },
          page_size: { type: "number", description: "Page size, clamped to 1-100. Defaults to 20." },
          pageSize: { type: "number", description: "Camel-case alias for page_size." },
          limit: { type: "number", description: "Alias for page_size." },
          topK: { type: "number", description: "Alias for page_size." },
          include_full: { type: "boolean", description: "When true, return the full backend record instead of a compact agent summary." },
          includeFull: { type: "boolean", description: "Camel-case alias for include_full." }
        }
      ),
      outputSchema: objectSchema(
        {
          query: { type: "string" },
          page: { type: "number" },
          pageSize: { type: "number" },
          total: { type: "number" },
          count: { type: "number" },
          items: { type: "array", items: { type: "object", additionalProperties: true } },
          warning: { type: "string" }
        },
        ["query", "page", "pageSize", "total", "count", "items"]
      )
    },
    {
      name: "movscript_shot_group_create",
      description: "Create an empty MovScript shot reference group for a video RawResource. Use this before adding detected shots when the agent needs a stable group ID for the source clip.",
      inputSchema: objectSchema(
        {
          resource_id: { type: "number", description: "Video RawResource ID used as the source resource for the group." },
          resourceId: { type: "number", description: "Camel-case alias for resource_id." },
          title: { type: "string", description: "Optional group title. Defaults from the source filename." },
          summary: { type: "string", description: "Optional group summary." },
          cut_strategy: { type: "string", description: "Optional cut strategy label, e.g. scene_detection or manual_review." },
          cutStrategy: { type: "string", description: "Camel-case alias for cut_strategy." }
        }
      ),
      outputSchema: objectSchema({
        status: { type: "string" },
        group_id: { type: "number" },
        group: { type: "object", additionalProperties: true },
        message: { type: "string" }
      }, ["status", "group_id", "group", "message"])
    },
    {
      name: "movscript_shot_group_get",
      description: "Read a MovScript shot reference group and its ordered shots. Use this as the agent source of truth for which shots need to be recreated.",
      inputSchema: objectSchema(
        {
          group_id: { type: "number", description: "Shot reference group ID." },
          groupId: { type: "number", description: "Camel-case alias for group_id." },
          id: { type: "number", description: "Alias for group_id." }
        }
      ),
      outputSchema: objectSchema({
        status: { type: "string" },
        group_id: { type: "number" },
        group: { type: "object", additionalProperties: true },
        count: { type: "number" },
        shots: { type: "array", items: { type: "object", additionalProperties: true } }
      }, ["status", "group_id", "group", "count", "shots"])
    },
    {
      name: "movscript_shot_group_add_shots",
      description: "Append one or more shot references to an existing shot reference group for a source video resource. The agent can pass detected start/end ranges plus optional manual shot metadata.",
      inputSchema: objectSchema(
        {
          group_id: { type: "number", description: "Existing shot reference group ID." },
          groupId: { type: "number", description: "Camel-case alias for group_id." },
          resource_id: { type: "number", description: "Video RawResource ID. Defaults to the group source resource when omitted." },
          resourceId: { type: "number", description: "Camel-case alias for resource_id." },
          duration_sec: { type: "number", description: "Optional full video duration in seconds." },
          durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
          width: { type: "number", description: "Optional video width." },
          height: { type: "number", description: "Optional video height." },
          shots: {
            type: "array",
            description: "Shot metadata array. Each item may include title, summary, start_sec/startSec, end_sec/endSec, tags, and professional annotation fields.",
            items: { type: "object", additionalProperties: true }
          }
        }
      ),
      outputSchema: objectSchema({
        status: { type: "string" },
        group_id: { type: "number" },
        count: { type: "number" },
        shots: { type: "array", items: { type: "object", additionalProperties: true } },
        message: { type: "string" }
      }, ["status", "group_id", "count", "shots", "message"])
    },
    {
      name: "movscript_video_shot_cuts_analyze",
      description: "Analyze a MovScript video RawResource with ffmpeg scene detection and return shot ranges. This only detects cuts; use movscript_shot_group_add_shots to persist the ranges.",
      inputSchema: objectSchema({
        resource_id: { type: "number", description: "Video RawResource ID." },
        resourceId: { type: "number", description: "Camel-case alias for resource_id." },
        id: { type: "number", description: "Alias for resource_id." },
        duration_sec: { type: "number", description: "Optional known duration. If omitted, ffprobe is used when available." },
        durationSec: { type: "number", description: "Camel-case alias for duration_sec." },
        scene_threshold: { type: "number", description: "ffmpeg scene threshold. Defaults to 0.28." },
        sceneThreshold: { type: "number", description: "Camel-case alias for scene_threshold." },
        min_shot_duration_sec: { type: "number", description: "Minimum shot duration in seconds. Defaults to 1.2." },
        minShotDurationSec: { type: "number", description: "Camel-case alias for min_shot_duration_sec." },
        max_shot_duration_sec: { type: "number", description: "Maximum shot duration in seconds. Defaults to 12." },
        maxShotDurationSec: { type: "number", description: "Camel-case alias for max_shot_duration_sec." },
        max_video_bytes: { type: "number", description: "Maximum source video size to download. Defaults to 200 MiB, hard-capped at 1 GiB." },
        maxVideoBytes: { type: "number", description: "Camel-case alias for max_video_bytes." }
      }),
      outputSchema: objectSchema({
        status: { type: "string" },
        resource_id: { type: "number" },
        strategy: { type: "string" },
        duration_sec: { type: "number" },
        count: { type: "number" },
        shots: { type: "array", items: { type: "object", additionalProperties: true } },
        warning: { type: "string" }
      }, ["status", "resource_id", "strategy", "count", "shots"])
    }
  ];
}
function projectTools() {
  return [
    {
      name: "movscript_project_create",
      description: "Create a formal MovScript project. Use only when the user explicitly asks to create a new project or confirms the project name.",
      inputSchema: objectSchema(
        {
          name: { type: "string" },
          description: { type: "string" },
          total_episodes: { type: "number" }
        },
        ["name"]
      )
    },
    {
      name: "movscript_project_init",
      description: "Initialize a local MovScript project in any directory, then ensure its backend project identity and project-data space.",
      inputSchema: objectSchema(
        {
          projectDir: { type: "string", description: "Absolute or relative directory to initialize as a MovScript project." },
          project_dir: { type: "string", description: "Alias for projectDir." },
          cwd: { type: "string", description: "Alias for projectDir, useful when initializing the current folder." },
          title: { type: "string" },
          projectId: { type: "string", description: "Optional local project id written into workspace metadata." },
          project_id: { type: "string", description: "Alias for projectId." },
          language: { type: "string" },
          overwrite: { type: "boolean" }
        }
      )
    },
    {
      name: "movscript_project_open",
      description: "Open a MovScript project by directory, then ensure its backend project identity and project-data space. This does not fetch source data; business source lives in the user chosen Git repository.",
      inputSchema: objectSchema(
        {
          projectDir: { type: "string", description: "Absolute or relative MovScript project directory to open." },
          project_dir: { type: "string", description: "Alias for projectDir." },
          cwd: { type: "string", description: "Alias for projectDir." }
        }
      )
    },
    {
      name: "movscript_project_fetch",
      description: "Compatibility alias for movscript_project_open.",
      inputSchema: objectSchema(
        {
          projectDir: { type: "string", description: "Absolute or relative MovScript project directory to open." },
          project_dir: { type: "string", description: "Alias for projectDir." },
          cwd: { type: "string", description: "Alias for projectDir." }
        }
      )
    }
  ];
}

// ../../packages/domain/dist/index.js
var MOVSCRIPT_TIMELINE_NAMESPACE_ENTITY_KINDS = ["production", "segment"];
var MOVSCRIPT_SETTING_NAMESPACE_ENTITY_KINDS = ["setting", "setting_state", "state"];
var MOVSCRIPT_SYSTEM_PRIMITIVE_KINDS = [
  "scene_moment",
  "expression_unit",
  "storyboard",
  "keyframe",
  "audio_cue",
  "asset",
  "timeline_assembly"
];
var TIMELINE_NAMESPACE_KINDS = new Set(MOVSCRIPT_TIMELINE_NAMESPACE_ENTITY_KINDS);
var SETTING_NAMESPACE_KINDS = new Set(MOVSCRIPT_SETTING_NAMESPACE_ENTITY_KINDS);
var SYSTEM_PRIMITIVE_KINDS = new Set(MOVSCRIPT_SYSTEM_PRIMITIVE_KINDS);
function classifyMovScriptEntityKind(kind) {
  if (TIMELINE_NAMESPACE_KINDS.has(kind)) return "timeline_namespace";
  if (SETTING_NAMESPACE_KINDS.has(kind)) return "setting_namespace";
  if (SYSTEM_PRIMITIVE_KINDS.has(kind)) return "system_primitive";
  if (kind === "content_unit") return "content_unit";
  if (kind === "candidate" || kind === "selection" || kind === "raw_resource" || kind === "resource") return "resource_state";
  return void 0;
}
function projectMovScriptDomainNodeKind(entityKind, record = {}) {
  const category = classifyMovScriptEntityKind(entityKind);
  if (category === "timeline_namespace") {
    return stringField5(record.namespace_kind) ?? stringField5(record.timeline_namespace_kind) ?? stringField5(record.timelineNamespaceKind) ?? stringField5(record.kind) ?? entityKind;
  }
  if (category === "setting_namespace") {
    return stringField5(record.namespace_kind) ?? stringField5(record.setting_namespace_kind) ?? stringField5(record.settingNamespaceKind) ?? stringField5(record.setting_kind) ?? stringField5(record.settingKind) ?? stringField5(record.kind) ?? entityKind;
  }
  return entityKind;
}
function isMovScriptNamespaceCategory(category) {
  return category === "timeline_namespace" || category === "setting_namespace";
}
function isMovScriptSystemPrimitiveKind(kind) {
  return kind !== void 0 && SYSTEM_PRIMITIVE_KINDS.has(kind);
}
function isMovScriptTimelineNamespaceKind(kind) {
  return kind !== void 0 && TIMELINE_NAMESPACE_KINDS.has(kind);
}
function isMovScriptSettingNamespaceKind(kind) {
  return kind !== void 0 && SETTING_NAMESPACE_KINDS.has(kind);
}
function isMovScriptNamespaceKind(kind) {
  if (kind === "timeline_namespace" || kind === "setting_namespace") return true;
  return isMovScriptTimelineNamespaceKind(kind) || isMovScriptSettingNamespaceKind(kind);
}
function stringField5(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
var MOVSCRIPT_SPECIALIZED_CONTENT_UNIT_TYPES = [
  "timeline_assembly_ref",
  "production_ref",
  "segment_ref",
  "asset_ref",
  "keyframe_ref",
  "storyboard_ref",
  "audio_cue_ref",
  "scence_moment_ref",
  "scene_moment_ref",
  "expression_unit_ref"
];
var CONTENT_UNIT_TARGET_ADAPTERS = {
  timeline_assembly_ref: {
    contentUnitType: "timeline_assembly_ref",
    targetCategory: "timeline_assembly",
    targetKind: "timeline_assembly",
    outputKind: "video",
    primaryRefField: "target_ref"
  },
  production_ref: {
    contentUnitType: "production_ref",
    targetCategory: "timeline_assembly",
    targetKind: "timeline_assembly",
    outputKind: "video",
    primaryRefKind: "production",
    primaryRefField: "production_ref",
    legacyNamespaceKind: "production"
  },
  segment_ref: {
    contentUnitType: "segment_ref",
    targetCategory: "timeline_assembly",
    targetKind: "timeline_assembly",
    outputKind: "video",
    primaryRefKind: "segment",
    primaryRefField: "segment_ref",
    legacyNamespaceKind: "segment"
  },
  asset_ref: primitiveAdapter("asset_ref", "asset", "image"),
  keyframe_ref: primitiveAdapter("keyframe_ref", "keyframe", "image"),
  storyboard_ref: primitiveAdapter("storyboard_ref", "storyboard", "image"),
  audio_cue_ref: primitiveAdapter("audio_cue_ref", "audio_cue", "audio"),
  scence_moment_ref: primitiveAdapter("scence_moment_ref", "scene_moment", "video", "scene_moment", "scene_moment_ref"),
  scene_moment_ref: primitiveAdapter("scene_moment_ref", "scene_moment", "video"),
  expression_unit_ref: primitiveAdapter("expression_unit_ref", "expression_unit", void 0)
};
var PROMPT_REF_KINDS = /* @__PURE__ */ new Set([
  "production",
  "segment",
  "asset",
  "keyframe",
  "storyboard",
  "audio_cue",
  "scene_moment",
  "expression_unit",
  "content_unit"
]);
function primitiveAdapter(contentUnitType2, targetKind, outputKind, primaryRefKind = targetKind, primaryRefField = `${primaryRefKind}_ref`) {
  return {
    contentUnitType: contentUnitType2,
    targetCategory: "system_primitive",
    targetKind,
    outputKind,
    primaryRefKind,
    primaryRefField
  };
}
function hasSpecializedContentUnitType(contentUnitType2) {
  return typeof contentUnitType2 === "string" && isMovScriptSpecializedContentUnitType(contentUnitType2);
}
function isMovScriptSpecializedContentUnitType(value) {
  return value !== void 0 && MOVSCRIPT_SPECIALIZED_CONTENT_UNIT_TYPES.includes(value);
}
function contentUnitTargetAdapterFor(contentUnitType2) {
  return isMovScriptSpecializedContentUnitType(contentUnitType2) ? CONTENT_UNIT_TARGET_ADAPTERS[contentUnitType2] : void 0;
}
function isContentUnitPromptRefKind(value) {
  return value !== void 0 && PROMPT_REF_KINDS.has(value);
}
function primaryRefKindForContentUnitType(contentUnitType2) {
  return contentUnitTargetAdapterFor(contentUnitType2)?.primaryRefKind;
}
function primaryRefFieldNameForKind(kind) {
  return kind === "scene_moment" ? "scene_moment_ref" : `${kind}_ref`;
}
function contentUnitTypesForPromptRefKind(kind) {
  if (kind === "production") return ["production_ref", "timeline_assembly_ref"];
  if (kind === "segment") return ["segment_ref", "timeline_assembly_ref"];
  if (kind === "scene_moment") return ["scence_moment_ref", "scene_moment_ref"];
  if (kind === "expression_unit") return ["expression_unit_ref"];
  return [`${kind}_ref`];
}
function primaryRefIdsForContentUnitRecord(record, kind) {
  switch (kind) {
    case "asset":
      return compactStrings(record.asset_ref);
    case "keyframe":
      return compactStrings(record.keyframe_ref);
    case "storyboard":
      return compactStrings(record.storyboard_ref);
    case "audio_cue":
      return compactStrings(record.target_kind === "audio_cue" ? record.target_ref : void 0, record.audio_cue_ref);
    case "production":
      return compactStrings(
        record.production_ref,
        record.target_kind === "production" ? record.target_ref : void 0,
        timelineAssemblyScopeRefFor(record, "production")
      );
    case "segment":
      return compactStrings(
        record.segment_ref,
        record.target_kind === "segment" ? record.target_ref : void 0,
        timelineAssemblyScopeRefFor(record, "segment")
      );
    case "scene_moment":
      return compactStrings(record.target_kind === "scene_moment" ? record.target_ref : void 0, record.scene_moment_ref, record.scence_moment_ref);
    case "expression_unit":
      return compactStrings(record.target_kind === "expression_unit" ? record.target_ref : void 0, record.expression_unit_ref);
    case "content_unit":
      return compactStrings(record.content_unit_ref);
    default:
      return [];
  }
}
function outputKindForContentUnitType(contentUnitType2, explicitOutputKind) {
  const explicit = contentUnitOutputKind(explicitOutputKind);
  if (explicit !== "metadata") return explicit;
  return contentUnitTargetAdapterFor(contentUnitType2)?.outputKind ?? "metadata";
}
function expectedOutputKindForContentUnitType(contentUnitType2) {
  return contentUnitTargetAdapterFor(contentUnitType2)?.outputKind;
}
function normalizeContentUnitTarget(record) {
  const contentUnitType2 = stringField22(record.content_unit_type ?? record.contentUnitType) ?? "";
  const adapter = contentUnitType2 ? contentUnitTargetAdapterFor(contentUnitType2) : void 0;
  const explicitTargetCategory = stringField22(record.target_category ?? record.targetCategory);
  const explicitTargetKind = stringField22(record.target_kind ?? record.targetKind);
  const explicitTargetRef = idString(record.target_ref ?? record.targetRef);
  const explicitScopeKind = stringField22(record.scope_kind ?? record.scopeKind);
  const explicitScopeRef = idString(record.scope_ref ?? record.scopeRef);
  const explicitScope = explicitScopeKind && explicitScopeRef ? { scopeKind: explicitScopeKind, scopeRef: explicitScopeRef } : void 0;
  const diagnostics = [];
  const outputKind = outputKindForContentUnitType(contentUnitType2, record.output_kind ?? record.outputKind);
  const primaryRefKind = adapter?.primaryRefKind;
  const primaryRefField = adapter?.primaryRefField ?? (primaryRefKind ? primaryRefFieldNameForKind(primaryRefKind) : void 0);
  const primaryRefs = primaryRefKind ? primaryRefIdsForContentUnitRecord(record, primaryRefKind) : [];
  if (!adapter) {
    const targetCategory = explicitWorkTargetCategory(explicitTargetCategory) ?? (explicitTargetKind ? workTargetCategoryForKind(explicitTargetKind) : void 0);
    if (explicitTargetCategory && isMovScriptNamespaceCategory(explicitTargetCategory)) {
      diagnostics.push(namespaceTargetDiagnostic(explicitTargetCategory, "target_category"));
    }
    if (explicitTargetKind && isMovScriptNamespaceKind(explicitTargetKind)) {
      diagnostics.push(namespaceTargetDiagnostic(explicitTargetKind, "target_kind"));
    }
    return {
      contentUnitType: contentUnitType2,
      outputKind,
      ...targetCategory && explicitTargetKind ? {
        target: {
          targetCategory,
          targetKind: explicitTargetKind,
          ...explicitTargetRef ? { targetRef: explicitTargetRef } : {}
        }
      } : {},
      primaryRefs,
      diagnostics
    };
  }
  if (adapter.contentUnitType === "timeline_assembly_ref") {
    if (explicitTargetCategory && isMovScriptNamespaceCategory(explicitTargetCategory)) {
      diagnostics.push(namespaceTargetDiagnostic(explicitTargetCategory, "target_category"));
    }
    if (explicitTargetKind && isMovScriptNamespaceKind(explicitTargetKind)) {
      diagnostics.push(namespaceTargetDiagnostic(explicitTargetKind, "target_kind"));
    }
    if (explicitTargetKind && explicitTargetKind !== "timeline_assembly") {
      diagnostics.push({
        severity: "error",
        code: "content_unit_target_kind_mismatch",
        message: `${contentUnitType2} must target timeline_assembly, not ${explicitTargetKind}`,
        field: "target_kind"
      });
    }
    const targetRef2 = explicitTargetKind === "timeline_assembly" && explicitTargetRef ? explicitTargetRef : explicitScope ? implicitTimelineAssemblyRef(explicitScope.scopeKind, explicitScope.scopeRef) : void 0;
    const parsedScope = parseImplicitTimelineAssemblyRef(targetRef2);
    if (!targetRef2) {
      diagnostics.push({
        severity: "error",
        code: "content_unit_target_ref_missing",
        message: `${contentUnitType2} requires target_ref or scope_kind/scope_ref`,
        field: "target_ref"
      });
    } else if (!parsedScope) {
      diagnostics.push({
        severity: "error",
        code: "content_unit_scope_ref_invalid",
        message: `${contentUnitType2} target_ref must use timeline_assembly:<scopeKind>:<scopeRef>`,
        field: "target_ref"
      });
    }
    return {
      contentUnitType: contentUnitType2,
      outputKind,
      target: {
        targetCategory: "timeline_assembly",
        targetKind: "timeline_assembly",
        ...targetRef2 ? { targetRef: targetRef2 } : {}
      },
      primaryRefField: adapter.primaryRefField,
      primaryRefs,
      ...parsedScope ? {
        scope: {
          category: "timeline_namespace",
          kind: parsedScope.scopeKind,
          ref: parsedScope.scopeRef,
          field: explicitTargetRef ? "target_ref" : "scope_ref"
        }
      } : {},
      diagnostics
    };
  }
  if (adapter.legacyNamespaceKind) {
    if (explicitTargetCategory && isMovScriptNamespaceCategory(explicitTargetCategory)) {
      diagnostics.push(namespaceTargetDiagnostic(explicitTargetCategory, "target_category"));
    }
    const scopeRef = firstUnique(primaryRefs) ?? (explicitTargetKind === adapter.legacyNamespaceKind ? explicitTargetRef : void 0);
    if (!scopeRef) {
      diagnostics.push({
        severity: "error",
        code: "content_unit_scope_ref_missing",
        message: `${contentUnitType2} requires ${adapter.primaryRefField}`,
        field: adapter.primaryRefField
      });
    }
    if (explicitTargetKind && explicitTargetKind !== adapter.legacyNamespaceKind && explicitTargetKind !== "timeline_assembly") {
      diagnostics.push({
        severity: "error",
        code: "content_unit_target_kind_mismatch",
        message: `${contentUnitType2} cannot target ${explicitTargetKind}`,
        field: "target_kind"
      });
    }
    const targetRef2 = explicitTargetKind === "timeline_assembly" && explicitTargetRef ? explicitTargetRef : scopeRef ? implicitTimelineAssemblyRef(adapter.legacyNamespaceKind, scopeRef) : void 0;
    return {
      contentUnitType: contentUnitType2,
      outputKind,
      target: {
        targetCategory: "timeline_assembly",
        targetKind: "timeline_assembly",
        ...targetRef2 ? { targetRef: targetRef2 } : {}
      },
      primaryRefKind,
      primaryRefField,
      primaryRefs,
      ...scopeRef ? {
        scope: {
          category: "timeline_namespace",
          kind: adapter.legacyNamespaceKind,
          ref: scopeRef,
          field: adapter.primaryRefField
        }
      } : {},
      legacyAlias: {
        contentUnitType: contentUnitType2,
        namespaceKind: adapter.legacyNamespaceKind
      },
      diagnostics
    };
  }
  if (explicitTargetCategory && isMovScriptNamespaceCategory(explicitTargetCategory)) {
    diagnostics.push(namespaceTargetDiagnostic(explicitTargetCategory, "target_category"));
  }
  if (explicitTargetKind && isMovScriptNamespaceKind(explicitTargetKind)) {
    diagnostics.push(namespaceTargetDiagnostic(explicitTargetKind, "target_kind"));
  }
  if (explicitTargetKind && explicitTargetKind !== adapter.targetKind) {
    diagnostics.push({
      severity: "error",
      code: "content_unit_target_kind_mismatch",
      message: `${contentUnitType2} must target ${adapter.targetKind}, not ${explicitTargetKind}`,
      field: "target_kind"
    });
  }
  const targetRef = explicitTargetKind === adapter.targetKind && explicitTargetRef ? explicitTargetRef : firstUnique(primaryRefs);
  if (!targetRef) {
    diagnostics.push({
      severity: "error",
      code: "content_unit_target_ref_missing",
      message: `${contentUnitType2} requires ${primaryRefField ?? "target_ref"}`,
      field: primaryRefField
    });
  }
  return {
    contentUnitType: contentUnitType2,
    outputKind,
    target: {
      targetCategory: adapter.targetCategory,
      targetKind: adapter.targetKind,
      ...targetRef ? { targetRef } : {}
    },
    primaryRefKind,
    primaryRefField,
    primaryRefs,
    diagnostics
  };
}
function timelineAssemblyScopeFromContentUnitRecord(record) {
  return normalizeContentUnitTarget(record).scope;
}
function contentUnitTargetValidationDiagnostics(record) {
  const contentUnitType2 = stringField22(record.content_unit_type ?? record.contentUnitType);
  return normalizeContentUnitTarget(record).diagnostics.filter(
    (diagnostic2) => isContentUnitTargetValidationDiagnostic(diagnostic2, contentUnitType2)
  );
}
function isContentUnitTargetValidationDiagnostic(diagnostic2, contentUnitType2) {
  if (diagnostic2.code === "content_unit_namespace_target") return true;
  if (diagnostic2.code === "content_unit_target_kind_mismatch") return true;
  if (contentUnitType2 !== "timeline_assembly_ref") return false;
  return diagnostic2.code === "content_unit_target_ref_missing" || diagnostic2.code === "content_unit_scope_ref_invalid";
}
function normalizeContentUnitTargetEdges(input) {
  const normalized = normalizeContentUnitTarget(input.record);
  const origin = normalized.legacyAlias ? "legacy_alias" : "explicit_ref";
  const edges = [];
  if (normalized.target?.targetRef) {
    edges.push({
      source: input.source,
      target: {
        category: normalized.target.targetCategory,
        kind: normalized.target.targetKind,
        id: normalized.target.targetRef
      },
      relation: "target",
      origin,
      field: contentUnitTargetEdgeField(input.record, normalized.primaryRefField, normalized.scope?.field, normalized.target.targetRef)
    });
  }
  if (normalized.scope) {
    edges.push({
      source: input.source,
      target: input.scopeTarget?.(normalized.scope) ?? {
        category: "timeline_namespace",
        kind: normalized.scope.kind,
        id: normalized.scope.ref
      },
      relation: "scope",
      origin,
      field: normalized.scope.field
    });
  }
  return edges;
}
function implicitTimelineAssemblyRef(scopeKind, scopeRef) {
  return `timeline_assembly:${scopeKind}:${scopeRef}`;
}
function parseImplicitTimelineAssemblyRef(value) {
  if (!value?.startsWith("timeline_assembly:")) return void 0;
  const [, scopeKind, ...scopeRefParts] = value.split(":");
  const scopeRef = scopeRefParts.join(":");
  if (!scopeKind?.trim() || !scopeRef.trim()) return void 0;
  return { scopeKind: scopeKind.trim(), scopeRef: scopeRef.trim() };
}
function workTargetCategoryForKind(kind) {
  if (kind === "content_unit") return "content_unit";
  if (kind === "timeline_assembly") return "timeline_assembly";
  if (isMovScriptSystemPrimitiveKind(kind)) return "system_primitive";
  return void 0;
}
function explicitWorkTargetCategory(category) {
  if (category === "content_unit" || category === "timeline_assembly" || category === "system_primitive") return category;
  return void 0;
}
function timelineAssemblyScopeRefFor(record, scopeKind) {
  const explicitScopeKind = stringField22(record.scope_kind ?? record.scopeKind);
  const explicitScopeRef = idString(record.scope_ref ?? record.scopeRef);
  if (explicitScopeKind === scopeKind && explicitScopeRef) return explicitScopeRef;
  if (record.target_kind !== "timeline_assembly" && record.targetKind !== "timeline_assembly") return void 0;
  const parsedScope = parseImplicitTimelineAssemblyRef(idString(record.target_ref ?? record.targetRef));
  return parsedScope?.scopeKind === scopeKind ? parsedScope.scopeRef : void 0;
}
function namespaceTargetDiagnostic(kind, field) {
  return {
    severity: "error",
    code: "content_unit_namespace_target",
    message: `namespace ${kind} cannot be a content unit target; use a system primitive or timeline assembly`,
    field
  };
}
function contentUnitTargetEdgeField(record, primaryRefField, scopeField, targetRef) {
  const explicitTargetRef = stringField22(record.target_ref ?? record.targetRef);
  if (explicitTargetRef === targetRef) return "target_ref";
  return primaryRefField ?? scopeField ?? "target_ref";
}
function contentUnitOutputKind(value) {
  if (value === "image" || value === "video" || value === "audio" || value === "text" || value === "metadata") return value;
  return "metadata";
}
function compactStrings(...values) {
  return values.flatMap((value) => {
    const id = idString(value);
    return id === void 0 ? [] : [id];
  });
}
function idString(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function stringField22(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function firstUnique(values) {
  const seen = /* @__PURE__ */ new Set();
  for (const value of values) {
    if (seen.has(value)) continue;
    seen.add(value);
    return value;
  }
  return void 0;
}
var MOVSCRIPT_EXPRESSION_UNIT_SLOT_KINDS = [
  "visual",
  "voice",
  "subtitle",
  "audio"
];
var SLOT_KIND_SET = new Set(MOVSCRIPT_EXPRESSION_UNIT_SLOT_KINDS);
function isMovScriptExpressionUnitSlotKind(value) {
  return typeof value === "string" && SLOT_KIND_SET.has(value);
}
function normalizeExpressionUnitSlotKind(value, fallback = "visual") {
  const text = normalizedToken(value);
  if (!text) return fallback;
  if (isMovScriptExpressionUnitSlotKind(text)) return text;
  if (text === "dialogue" || text === "narration" || text === "voiceover" || text === "voice_over" || text === "speech" || text === "verbal") return "voice";
  if (text === "caption" || text === "caption_card" || text === "text") return "subtitle";
  if (text === "sound" || text === "sound_effect" || text === "sfx" || text === "music" || text === "music_beat" || text === "ambience" || text === "ambient" || text === "foley") return "audio";
  if (text === "shot" || text === "action" || text === "visual_note" || text === "image" || text === "video" || text === "visual") return "visual";
  return fallback;
}
function expressionUnitSlotKindFromRecord(record) {
  if (!record) return "visual";
  return normalizeExpressionUnitSlotKind(
    record.slot_kind ?? record.slotKind ?? record.expression_kind ?? record.expressionKind ?? record.kind ?? record.role ?? record.modality
  );
}
function defaultOutputKindForExpressionUnitSlot(slotKind) {
  const normalized = normalizeExpressionUnitSlotKind(slotKind);
  if (normalized === "voice" || normalized === "audio") return "audio";
  if (normalized === "subtitle") return "text";
  return "video";
}
function normalizedToken(value) {
  return String(value ?? "").trim().toLowerCase();
}
function normalizePathParentEdge(child, parent) {
  if (!parent) {
    return {
      diagnostics: [{
        severity: "warning",
        code: "path_parent_missing",
        message: `no path parent found for ${child.kind}`
      }]
    };
  }
  return {
    edge: {
      source: child,
      target: parent,
      relation: "parent",
      origin: "path"
    },
    diagnostics: []
  };
}
function entityDir(path) {
  return path.replace(/\/[^/]+$/, "");
}
function nearestParentPath(path, candidates) {
  const candidateSet = new Set(candidates);
  const parts = path.split("/").filter(Boolean);
  for (let index = parts.length - 1; index > 0; index -= 1) {
    const candidate = parts.slice(0, index).join("/");
    if (candidateSet.has(candidate)) return candidate;
  }
  return void 0;
}
function assertExplicitParentRefMatchesPath(input) {
  const explicitParentRef = idString2(input.explicitParentRef);
  if (!explicitParentRef || !input.pathParent) return [];
  if (domainRefMatches(input.pathParent, explicitParentRef)) return [];
  const pathParentLabel = [
    input.pathParent.kind,
    input.pathParent.id !== void 0 ? String(input.pathParent.id) : void 0,
    input.pathParent.path
  ].filter(Boolean).join(" ");
  return [{
    severity: "error",
    code: "path_parent_ref_conflict",
    field: input.field,
    ...input.path ? { path: input.path } : {},
    message: `${input.child.kind} ${input.field} ${explicitParentRef} conflicts with path parent ${pathParentLabel}; source path is the canonical instance parent`
  }];
}
function domainRefMatches(ref, value) {
  const normalizedValue = normalizePathLike(value);
  if (ref.id !== void 0 && String(ref.id) === value) return true;
  if (ref.path) {
    const normalizedPath = normalizePathLike(ref.path);
    const normalizedDir = domainRefDir(normalizedPath);
    if (normalizedPath === normalizedValue) return true;
    if (normalizedDir === normalizedValue) return true;
  }
  const pathTail = ref.path ? domainRefDir(normalizePathLike(ref.path)).split("/").filter(Boolean).at(-1) : void 0;
  return pathTail !== void 0 && pathTail === value;
}
function idString2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function normalizePathLike(value) {
  return value.replace(/\\/g, "/").replace(/^\/+|\/+$/g, "");
}
function domainRefDir(path) {
  return path.endsWith(".json") ? entityDir(path) : path;
}
var MOVSCRIPT_DOMAIN_PATH_SEMANTICS = {
  structureSource: "source_path",
  vocabularyRole: "labels_templates_and_aliases",
  rules: [
    "The source path is the canonical instance tree: parent/child structure is derived from the actual entity file location.",
    "project.namespace_vocabulary and namespace_kind fields name, template, alias, and project vocabulary; they do not create a second instance tree.",
    "Default path templates are writer hints and legacy compatibility paths. Custom namespace layouts are valid when the entity is placed under the intended parent path and uses the canonical entity filename.",
    "Namespace nodes are organizational scope only. They cannot directly own content-unit refs or become content-unit targets; use timeline_assembly_ref for namespace-scope output."
  ],
  diagnostics: []
};
var MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES = {
  video: ["hook", "proof", "demo", "cta"],
  film: ["act", "sequence", "beat"],
  episode: ["act", "sequence", "beat"],
  lesson: ["segment"],
  custom: []
};
var MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATES = {
  ...MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES,
  short_video: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.video,
  series: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.episode,
  course: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.lesson
};
var MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATE_STRATEGIES = {
  video: {
    productionType: "video",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.video,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  },
  film: {
    productionType: "film",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.film,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  },
  episode: {
    productionType: "episode",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.episode,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  },
  lesson: {
    productionType: "lesson",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.lesson,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  },
  custom: {
    productionType: "custom",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.custom,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  },
  short_video: {
    productionType: "video",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.video,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  },
  series: {
    productionType: "episode",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.episode,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  },
  course: {
    productionType: "lesson",
    namespaces: MOVSCRIPT_PRODUCTION_TYPE_TEMPLATES.lesson,
    initialNamespaces: ["production"],
    defaultPreviewKind: "production"
  }
};
function normalizeNamespaceVocabulary(input) {
  const record = isRecord5(input) ? input : {};
  const nested = isRecord5(record.namespace_vocabulary) ? record.namespace_vocabulary : isRecord5(record.namespaceVocabulary) ? record.namespaceVocabulary : {};
  const timelineTemplate = stringField32(
    nested.timeline_template ?? nested.timelineTemplate ?? record.timeline_template ?? record.timelineTemplate
  );
  const templateNamespaces = timelineTemplate && isTimelineNamespaceTemplate(timelineTemplate) ? [...MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATES[timelineTemplate]] : [];
  const timelineNamespaces = uniqueStrings([
    ...templateNamespaces,
    ...stringArray(nested.timeline_namespaces ?? nested.timelineNamespaces),
    ...stringArray(record.timeline_namespaces ?? record.timelineNamespaces)
  ]);
  const settingNamespaces = uniqueStrings([
    ...stringArray(nested.setting_namespaces ?? nested.settingNamespaces),
    ...stringArray(record.setting_namespaces ?? record.settingNamespaces)
  ]);
  const diagnostics = [];
  if (timelineTemplate && !isTimelineNamespaceTemplate(timelineTemplate)) {
    diagnostics.push({
      severity: "warning",
      code: "namespace_template_unknown",
      message: `unknown timeline namespace template: ${timelineTemplate}`,
      field: "timeline_template"
    });
  }
  return {
    timelineNamespaces,
    settingNamespaces,
    ...timelineTemplate ? { timelineTemplate } : {},
    diagnostics
  };
}
function isTimelineNamespaceTemplate(value) {
  return Object.prototype.hasOwnProperty.call(MOVSCRIPT_TIMELINE_NAMESPACE_TEMPLATES, value);
}
function stringArray(value) {
  return Array.isArray(value) ? value.filter((item) => typeof item === "string" && item.trim().length > 0).map((item) => item.trim()) : [];
}
function uniqueStrings(values) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const value of values) {
    if (seen.has(value)) continue;
    seen.add(value);
    out.push(value);
  }
  return out;
}
function stringField32(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function isRecord5(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function normalizeDomainFocus(input) {
  const diagnostics = [];
  const projectId = idString3(input.projectId ?? input.project_id);
  const productionId = idString3(input.productionId ?? input.production_id);
  const targetCategory = stringField42(input.targetCategory ?? input.target_category ?? input.domainTargetCategory ?? input.domain_target_category);
  const timelineAssemblyRef = idString3(input.timelineAssemblyRef ?? input.timeline_assembly_ref);
  const targetKind = timelineAssemblyRef ? "timeline_assembly" : stringField42(input.targetKind ?? input.target_kind ?? input.domainTargetKind ?? input.domain_target_kind);
  const targetRef = timelineAssemblyRef ?? idString3(input.targetRef ?? input.target_ref ?? input.domainTargetRef ?? input.domain_target_ref);
  const entityKind = stringField42(input.entityKind ?? input.entity_kind);
  const entityId2 = idString3(input.entityId ?? input.entity_id);
  const productionPath = stringField42(input.productionPath ?? input.production_path);
  const explicitScopeKind = stringField42(input.scopeKind ?? input.scope_kind ?? input.namespaceKind ?? input.namespace_kind);
  const explicitScopeRef = idString3(input.scopeRef ?? input.scope_ref ?? input.namespaceRef ?? input.namespace_ref ?? input.namespacePath ?? input.namespace_path);
  const parsedAssemblyScope = targetKind === "timeline_assembly" ? parseImplicitTimelineAssemblyRef(targetRef) : void 0;
  const scopeKind = explicitScopeKind ?? parsedAssemblyScope?.scopeKind ?? (productionId ? "production" : void 0);
  const scopeRef = explicitScopeRef ?? parsedAssemblyScope?.scopeRef ?? productionPath ?? productionId;
  const scopeField = explicitScopeRef ? "scopeRef" : parsedAssemblyScope ? "targetRef" : productionPath ? "productionPath" : productionId ? "productionId" : void 0;
  if (targetCategory && isMovScriptNamespaceCategory(targetCategory)) {
    diagnostics.push({
      severity: "error",
      code: "focus_namespace_target",
      message: `namespace category ${targetCategory} cannot be normalized as a work target`,
      field: "targetCategory"
    });
  }
  if (targetKind && isMovScriptNamespaceKind(targetKind)) {
    diagnostics.push({
      severity: "error",
      code: "focus_namespace_target",
      message: `namespace ${targetKind} cannot be normalized as a work target`,
      field: "targetKind"
    });
  }
  const explicitTargetCategory = workTargetCategoryForCategory(targetCategory) ?? (targetKind ? workTargetCategoryForKind2(targetKind) : void 0);
  const explicitTarget = explicitTargetCategory && targetKind ? {
    targetCategory: explicitTargetCategory,
    targetKind,
    ...targetRef ? { targetRef } : scopeKind && scopeRef && targetKind === "timeline_assembly" ? { targetRef: implicitTimelineAssemblyRef(scopeKind, scopeRef) } : {}
  } : void 0;
  const timelineScope = scopeKind && scopeRef ? {
    category: "timeline_namespace",
    kind: scopeKind,
    ref: scopeRef,
    ...scopeField ? { field: scopeField } : {}
  } : void 0;
  return {
    ...projectId ? { projectId } : {},
    target: explicitTarget ?? (timelineScope ? {
      targetCategory: "timeline_assembly",
      targetKind: "timeline_assembly",
      targetRef: implicitTimelineAssemblyRef(timelineScope.kind, timelineScope.ref)
    } : void 0),
    ...timelineScope ? { scope: timelineScope } : {},
    ...entityKind ? {
      entity: {
        kind: entityKind,
        ...classifyMovScriptEntityKind(entityKind) ? { category: classifyMovScriptEntityKind(entityKind) } : {},
        ...entityId2 !== void 0 ? { id: entityId2 } : {},
        ...input.path ? { path: input.path } : {}
      }
    } : {},
    diagnostics
  };
}
function workTargetCategoryForKind2(kind) {
  if (kind === "content_unit") return "content_unit";
  if (kind === "timeline_assembly") return "timeline_assembly";
  if (isMovScriptSystemPrimitiveKind(kind)) return "system_primitive";
  return void 0;
}
function workTargetCategoryForCategory(category) {
  if (category === "content_unit" || category === "timeline_assembly" || category === "system_primitive") return category;
  return void 0;
}
function stringField42(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function idString3(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
var MOVSCRIPT_NAMESPACE_FORBIDDEN_CONTENT_UNIT_FIELDS = [
  "content_unit_ref",
  "contentUnitRef",
  "content_unit_refs",
  "contentUnitRefs",
  "main_content_unit_id",
  "mainContentUnitId"
];
var MOVSCRIPT_NAMESPACE_FORBIDDEN_PRODUCTION_STATE_FIELDS = [
  "candidate",
  "candidates",
  "candidate_ref",
  "candidateRef",
  "candidate_refs",
  "candidateRefs",
  "selection",
  "selections",
  "selected_candidate_id",
  "selectedCandidateId",
  "selected_candidate_ref",
  "selectedCandidateRef",
  "selected_resource_id",
  "selectedResourceId",
  "selected_resource_ref",
  "selectedResourceRef",
  "resource_id",
  "resourceId"
];
function assertNamespaceCannotOwnContentUnitRef(input) {
  const record = input.record ?? {};
  const namespaceKind = namespaceKindForSourceEntity(input, record);
  if (!namespaceKind) return [];
  const diagnostics = [];
  for (const field of MOVSCRIPT_NAMESPACE_FORBIDDEN_CONTENT_UNIT_FIELDS) {
    if (!Object.prototype.hasOwnProperty.call(record, field)) continue;
    diagnostics.push({
      severity: "error",
      code: "namespace_content_unit_ref_forbidden",
      message: `namespace ${namespaceKind} must not own content unit ref field ${field}; create a content_unit targeting a system primitive or timeline assembly instead`,
      field,
      ...input.path ? { path: input.path } : {}
    });
  }
  return diagnostics;
}
function assertNamespaceCannotOwnProductionState(input) {
  const record = input.record ?? {};
  const namespaceKind = namespaceKindForSourceEntity(input, record);
  if (!namespaceKind) return [];
  const diagnostics = [];
  for (const field of MOVSCRIPT_NAMESPACE_FORBIDDEN_PRODUCTION_STATE_FIELDS) {
    if (!Object.prototype.hasOwnProperty.call(record, field)) continue;
    diagnostics.push({
      severity: "error",
      code: "namespace_production_state_forbidden",
      message: `namespace ${namespaceKind} must not own production state field ${field}; create/select candidates through a content_unit targeting a system primitive or timeline assembly instead`,
      field,
      ...input.path ? { path: input.path } : {}
    });
  }
  return diagnostics;
}
function namespaceKindForSourceEntity(input, record) {
  const entityKind = input.entityKind ?? stringField52(record.kind);
  const category = input.category ?? (entityKind ? classifyMovScriptEntityKind(entityKind) : void 0);
  if (!isMovScriptNamespaceCategory(category)) return void 0;
  return entityKind ? projectMovScriptDomainNodeKind(entityKind, record) : category;
}
function stringField52(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
var ENTITY_KIND_FALLBACK_PREFIXES = {
  audio_cue: "audio",
  content_unit: "cu",
  expression_unit: "expression",
  keyframe: "keyframe",
  production: "production",
  scene_moment: "scene",
  segment: "segment",
  setting: "setting",
  setting_state: "state",
  state: "state",
  storyboard: "storyboard",
  asset: "asset"
};
function suggestMovScriptEntityId(input) {
  const fallbackPrefix = normalizedEntityIdToken(
    input.fallbackPrefix ?? (input.entityKind ? ENTITY_KIND_FALLBACK_PREFIXES[input.entityKind] : void 0) ?? input.entityKind ?? "item"
  ) || "item";
  const title = stringField6(input.title);
  const titleToken = title ? normalizedEntityIdToken(title) : void 0;
  if (titleToken) return titleToken;
  if (title) return `${fallbackPrefix}_${stableShortHash(title)}`;
  return fallbackPrefix;
}
function allocateMovScriptEntityId(input) {
  const base = suggestMovScriptEntityId(input);
  const existingIds = new Set(
    [...input.existingIds ?? []].map((id) => stringField6(id)?.toLowerCase()).filter((id) => Boolean(id))
  );
  if (!existingIds.has(base.toLowerCase())) return base;
  for (let index = 2; index < 1e4; index += 1) {
    const candidate = `${base}_${index}`;
    if (!existingIds.has(candidate.toLowerCase())) return candidate;
  }
  return `${base}_${stableShortHash([...existingIds].join("\n"))}`;
}
function normalizedEntityIdToken(value) {
  const text = stringField6(value);
  if (!text) return "";
  return text.normalize("NFKD").replace(/[\u0300-\u036f]/g, "").replace(/['’]/g, "").replace(/&/g, " and ").toLowerCase().replace(/[^a-z0-9_-]+/g, "_").replace(/_+/g, "_").replace(/^_+|_+$/g, "");
}
function stableShortHash(value) {
  let hash = 2166136261;
  for (let index = 0; index < value.length; index += 1) {
    hash ^= value.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(36).slice(0, 6);
}
function stringField6(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  if (typeof value !== "string") return void 0;
  const text = value.trim();
  return text || void 0;
}

// ../../packages/core/dist/chunk-NRNAVDJZ.js
var AGENT_SURFACE_ROUTES = {
  resources: "/agent/resources",
  resourceDetail: "/agent/resources/:resourceId",
  contentPrompt: "/agent/content/prompt",
  contentCandidates: "/agent/content/candidates",
  generationJob: "/agent/generation/jobs/:jobId",
  previewTimeline: "/agent/preview/timeline",
  impact: "/agent/impact",
  projectStatus: "/agent/project/status"
};
function agentGenerationJobPath(jobId) {
  return `/agent/generation/jobs/${encodeURIComponent(String(jobId))}`;
}
function createAgentBrowserSurface(args, input) {
  const frontendOrigin = resolveFrontendOrigin(args);
  const proxyBaseURL = resolveMCPProxyBaseURL(args);
  const mcpApiBaseURL = `${proxyBaseURL}/agent-api/v1`;
  const url = new URL(input.route, frontendOrigin);
  url.searchParams.set("mcpApiBaseURL", mcpApiBaseURL);
  url.searchParams.set("source", "mcp");
  for (const [key, value] of Object.entries(input.query ?? {})) {
    if (value === void 0) continue;
    url.searchParams.set(key, String(value));
  }
  return {
    kind: "browser_url",
    surface: input.surface,
    title: input.title,
    route: input.route,
    url: url.toString(),
    frontend_origin: frontendOrigin,
    mcp_api_base_url: mcpApiBaseURL,
    api_proxy: {
      base_url: mcpApiBaseURL,
      auth: "agent_mcp_context"
    },
    ...input.entity ? { entity: input.entity } : {},
    intent: input.intent,
    usage: input.usage
  };
}
function createPromptSurface(args, input) {
  return createAgentBrowserSurface(args, {
    route: AGENT_SURFACE_ROUTES.contentPrompt,
    title: `Content unit prompt ${String(input.contentUnitId)}`,
    surface: input.mode === "inspect" ? "inspect" : "edit",
    intent: "edit_prompt",
    entity: {
      ...input.projectId !== void 0 ? { project_id: input.projectId } : {},
      content_unit_id: input.contentUnitId
    },
    query: {
      ...input.projectId !== void 0 ? { projectId: input.projectId } : {},
      contentUnitId: input.contentUnitId,
      mode: input.mode ?? "edit"
    },
    usage: "Open this prompt workbench to inspect semantic refs, blockers, compiled prompt text, provider prompt text, and generation inputs."
  });
}
function createGenerationJobSurface(args, input) {
  return createAgentBrowserSurface(args, {
    route: agentGenerationJobPath(input.jobId),
    title: `Generation job #${input.jobId}`,
    surface: "inspect",
    intent: "monitor_generation",
    entity: {
      job_id: input.jobId,
      ...input.projectId !== void 0 ? { project_id: input.projectId } : {},
      ...input.contentUnitId !== void 0 ? { content_unit_id: input.contentUnitId } : {}
    },
    query: {
      jobId: input.jobId,
      ...input.projectId !== void 0 ? { projectId: input.projectId } : {},
      ...input.contentUnitId !== void 0 ? { contentUnitId: input.contentUnitId } : {}
    },
    usage: "Open this generation job surface to monitor status, inspect inputs, preview outputs, and continue to candidate review after success."
  });
}
function createContentCandidatesSurface(args, input) {
  return createAgentBrowserSurface(args, {
    route: AGENT_SURFACE_ROUTES.contentCandidates,
    title: `Content unit candidates ${String(input.contentUnitId)}`,
    surface: "review",
    intent: "review_candidates",
    entity: {
      ...input.projectId !== void 0 ? { project_id: input.projectId } : {},
      content_unit_id: input.contentUnitId,
      ...input.candidateId ? { candidate_id: input.candidateId } : {},
      ...input.resourceId !== void 0 ? { resource_id: input.resourceId } : {}
    },
    query: {
      ...input.projectId !== void 0 ? { projectId: input.projectId } : {},
      contentUnitId: input.contentUnitId,
      ...input.candidateId ? { candidateId: input.candidateId } : {},
      ...input.resourceId !== void 0 ? { resourceId: input.resourceId } : {}
    },
    usage: "Open this candidate review surface to compare generated candidates, inspect prompt snapshots, and adopt, reject, or defer a content-unit candidate."
  });
}
function createPreviewTimelineSurface(args, input) {
  const focus = resolveTimelineSurfaceFocus(input);
  return createAgentBrowserSurface(args, {
    route: AGENT_SURFACE_ROUTES.previewTimeline,
    title: `Timeline preview ${focus.label}`,
    surface: "review",
    intent: "preview_timeline",
    entity: focus.entity,
    query: focus.query,
    usage: "Open this preview timeline surface to inspect selected scene-moment outputs for a timeline assembly or legacy production scope."
  });
}
function createImpactSurface(args, input) {
  return createAgentBrowserSurface(args, {
    route: AGENT_SURFACE_ROUTES.impact,
    title: "MovScript change impact",
    surface: "review",
    intent: "review_impact",
    entity: {
      ...input.projectId !== void 0 ? { project_id: input.projectId } : {}
    },
    query: {
      ...input.projectId !== void 0 ? { projectId: input.projectId } : {},
      ...input.target !== void 0 ? { target: input.target } : {},
      ...input.source ? { source: input.source } : {}
    },
    usage: "Open this impact surface to review affected content units, stale selections, and explicit keep, relink, re-prompt, regenerate, or accept-stale decisions."
  });
}
function createProjectStatusSurface(args, input = {}) {
  const focus = resolveTimelineSurfaceFocus(input);
  return createAgentBrowserSurface(args, {
    route: AGENT_SURFACE_ROUTES.projectStatus,
    title: "MovScript project status",
    surface: "inspect",
    intent: "inspect_project_status",
    entity: focus.entity,
    query: focus.query,
    usage: "Open this project status surface to inspect timeline readiness, candidate coverage, selections, stale hints, and blockers."
  });
}
function resolveTimelineSurfaceFocus(input) {
  const focusInput = {
    projectId: input.projectId,
    productionId: input.productionId,
    scopeKind: input.scopeKind,
    scopeRef: input.scopeRef,
    namespaceKind: input.namespaceKind,
    namespaceRef: input.namespaceRef,
    namespacePath: input.namespacePath,
    targetCategory: input.targetCategory,
    targetKind: input.timelineAssemblyRef !== void 0 ? "timeline_assembly" : input.targetKind,
    targetRef: input.timelineAssemblyRef ?? input.targetRef
  };
  const focus = normalizeDomainFocus(focusInput);
  const legacyProductionId = input.productionId ?? (focus.scope?.kind === "production" ? focus.scope.ref : void 0);
  const entity = {
    ...input.projectId !== void 0 ? { project_id: input.projectId } : {},
    ...legacyProductionId !== void 0 ? { production_id: legacyProductionId } : {},
    ...focus.scope ? {
      timeline_scope_kind: focus.scope.kind,
      timeline_scope_ref: focus.scope.ref
    } : {},
    ...focus.target?.targetCategory ? { target_category: focus.target.targetCategory } : {},
    ...focus.target?.targetKind ? { target_kind: focus.target.targetKind } : {},
    ...focus.target?.targetRef !== void 0 ? { target_ref: focus.target.targetRef } : {},
    ...focus.target?.targetKind === "timeline_assembly" && focus.target.targetRef !== void 0 ? { timeline_assembly_ref: focus.target.targetRef } : {},
    domain_focus: focus
  };
  const query = {
    ...input.projectId !== void 0 ? { projectId: input.projectId } : {},
    ...legacyProductionId !== void 0 ? { productionId: legacyProductionId } : {},
    ...focus.scope ? {
      scopeKind: focus.scope.kind,
      scopeRef: focus.scope.ref
    } : {},
    ...focus.target?.targetCategory ? { targetCategory: focus.target.targetCategory } : {},
    ...focus.target?.targetKind ? { targetKind: focus.target.targetKind } : {},
    ...focus.target?.targetRef !== void 0 ? { targetRef: focus.target.targetRef } : {}
  };
  return {
    focus,
    ...legacyProductionId !== void 0 ? { legacyProductionId } : {},
    label: timelineFocusLabel(focus, legacyProductionId),
    entity,
    query
  };
}
function timelineFocusLabel(focus, legacyProductionId) {
  if (focus.scope?.kind && focus.scope.ref) return `${focus.scope.kind} ${focus.scope.ref}`;
  if (focus.target?.targetKind && focus.target.targetRef) return `${focus.target.targetKind} ${focus.target.targetRef}`;
  if (legacyProductionId !== void 0) return `production ${String(legacyProductionId)}`;
  return "project";
}
function projectIdFromArgs(args) {
  const value = args.projectId ?? args.project_id;
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function candidateIdFromArgs(args) {
  return getOptionalString2(args, "candidateId") ?? getOptionalString2(args, "candidate_id");
}
function resolveFrontendOrigin(args) {
  return normalizeHTTPOrigin(
    getOptionalString2(args, "frontend_origin") ?? getOptionalString2(args, "frontendOrigin") ?? process.env.MOVSCRIPT_FRONTEND_ORIGIN ?? process.env.VITE_DEV_SERVER_URL ?? "http://127.0.0.1:5173"
  );
}
function resolveMCPProxyBaseURL(args) {
  const explicit = getOptionalString2(args, "mcp_base_url") ?? getOptionalString2(args, "mcpBaseURL");
  if (explicit) return normalizeHTTPOrigin(explicit);
  const endpoint = process.env.MOVSCRIPT_MCP_ENDPOINT;
  if (endpoint) {
    try {
      const url = new URL(endpoint);
      return normalizeHTTPOrigin(url.origin);
    } catch {
    }
  }
  const port = process.env.MOVSCRIPT_MCP_PORT || "28765";
  return normalizeHTTPOrigin(`http://127.0.0.1:${port}`);
}
function getOptionalString2(args, key) {
  const value = args[key];
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim();
  return trimmed || void 0;
}
function normalizeHTTPOrigin(value) {
  const url = new URL(value.trim());
  if (url.protocol !== "http:" && url.protocol !== "https:") {
    throw new Error(`Expected http(s) URL, got ${value}`);
  }
  return url.origin;
}

// ../../packages/workspace/dist/chunk-VIT4JJRQ.js
var MOVSCRIPT_INTERPRET_CURRENT_DIR = ".interpret/current";
var MOVSCRIPT_INTERPRET_MANIFESTS_DIR = ".interpret/manifests";
var MOVSCRIPT_DOMAIN_TREE_PATH = ".interpret/current/domain-tree.json";
var MOVSCRIPT_EDITOR_STATE_PATH = ".interpret/current/editor-state.json";
var MOVSCRIPT_DOMAIN_INDEX_PATH = ".interpret/current/domain-index.json";
var MOVSCRIPT_ASSET_INDEX_PATH = ".interpret/current/asset-index.json";
var MOVSCRIPT_RELATION_GRAPH_PATH = ".interpret/current/relation-graph.json";
function normalizeWorkspacePath(path) {
  return path.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+$/, "");
}
function safeWorkspacePathToken(value) {
  return String(value).trim().replace(/[^a-zA-Z0-9_-]+/g, "_");
}
function semanticEntityId(value, entityKind, fallback = "local") {
  void entityKind;
  return safeWorkspacePathToken(rawIdentityValue(value, fallback));
}
function entityPathSlug(value, entityKind, fallback = "local") {
  void entityKind;
  return safeWorkspacePathToken(rawIdentityValue(value, fallback));
}
function entityIdentity(value, entityKind, fallback = "local") {
  const id = semanticEntityId(value, entityKind, fallback);
  return {
    id,
    slug: entityPathSlug(id, entityKind, fallback)
  };
}
function entityRefAliases(value, entityKind) {
  if (value === void 0 || value === null || String(value).trim() === "") return [];
  const raw = String(value).trim();
  const normalized = normalizeRefPath(raw);
  const aliases = /* @__PURE__ */ new Set([raw, normalized]);
  if (entityKind) {
    aliases.add(semanticEntityId(raw, entityKind));
    aliases.add(entityPathSlug(raw, entityKind));
    aliases.add(semanticEntityId(normalized, entityKind));
    aliases.add(entityPathSlug(normalized, entityKind));
  }
  return [...aliases].filter(Boolean);
}
function sameEntityRef(left, right, entityKind) {
  const leftAliases = new Set(entityRefAliases(left, entityKind));
  return entityRefAliases(right, entityKind).some((alias) => leftAliases.has(alias));
}
function displayEntityId(value, entityKind) {
  void entityKind;
  return value;
}
function rawIdentityValue(value, fallback) {
  if (value === void 0 || value === null || String(value).trim() === "") return fallback;
  const raw = String(value).trim();
  return raw.startsWith("-") ? `local_${raw.replace(/^-+/, "")}` : raw;
}
function normalizeRefPath(value) {
  return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
}
function isMovScriptSourcePath(path) {
  const normalized = normalizeWorkspacePath(path);
  if (MOVSCRIPT_SOURCE_ROOT_FILES.has(normalized)) return true;
  const [first] = normalized.split("/");
  const fileName = normalized.split("/").pop();
  return first !== void 0 && MOVSCRIPT_SOURCE_COLLECTION_DIRS.has(first) && fileName !== void 0 && MOVSCRIPT_SOURCE_ENTITY_FILES.has(fileName) && isMovScriptSourceDocumentPath(normalized);
}
function isMovScriptSourceDocumentPath(path) {
  return path.endsWith(".json") || path.endsWith(".md");
}
function isMovScriptContentUnitDecisionPath(path) {
  const normalized = normalizeWorkspacePath(path);
  return isLegacyMovScriptContentUnitSelectionPath(normalized) || isMovScriptContentUnitCandidatePath(normalized);
}
function isMovScriptContentUnitCandidatePath(path) {
  const normalized = normalizeWorkspacePath(path);
  return /^content_units\/[^/]+\/candidates\/[^/]+\/content_candidate\.json$/.test(normalized);
}
function isLegacyMovScriptContentUnitSelectionPath(path) {
  const normalized = normalizeWorkspacePath(path);
  return /^content_units\/[^/]+\/selection\.json$/.test(normalized);
}
function isMovScriptNonSourceRootDirectory(path) {
  const normalized = normalizeWorkspacePath(path);
  if (normalized.includes("/")) return false;
  return normalized === ".movscript" || normalized === ".git" || normalized === ".base" || normalized === ".update" || normalized === "runtime" || normalized === "tmp" || normalized.startsWith(".");
}
var MOVSCRIPT_SOURCE_COLLECTION_DIRS = /* @__PURE__ */ new Set([
  "settings",
  "scripts",
  "content_units",
  "productions",
  "timeline",
  "project_standards"
]);
var MOVSCRIPT_SOURCE_ROOT_FILES = /* @__PURE__ */ new Set([
  "project.json",
  "project_standards.json"
]);
var MOVSCRIPT_SOURCE_ENTITY_FILES = /* @__PURE__ */ new Set([
  "setting.json",
  "setting_state.json",
  "asset.json",
  "script.json",
  "script.md",
  "script_version.json",
  "script_block.json",
  "content_unit.json",
  "keyframe.json",
  "production.json",
  "segment.json",
  "scene_moment.json",
  "shot.json",
  "storyboard.json",
  "audio_cue.json",
  "expression_unit.json"
]);

// ../../packages/core/dist/chunk-5MIG2CYA.js
function buildContentSourceWorkspaceData(input) {
  const productions = input.productions;
  const segments = input.segments;
  const sceneMoments = input.sceneMoments;
  const storyboards = input.storyboards;
  const expressionUnits = input.expressionUnits;
  const audioCues = input.audioCues;
  const contentUnits = input.contentUnits;
  const keyframes = input.keyframes;
  const assets = input.assets;
  const settings = input.settings;
  const settingStates = input.settingStates;
  const previewTimelines = input.previewTimelines;
  const productionWorkPlan = normalizeProductionWorkPlanView(input.productionWorkPlan);
  const domainGraph = buildContentSourceWorkspaceDomainGraph(input);
  const ancestryIndex = buildContentSourceWorkspaceEntityAncestryIndex(input, domainGraph);
  const contentUnitsByPrimaryRef = groupContentUnitsByPrimaryRef(contentUnits);
  const candidateRecordsByContentUnitId = groupContentCandidateRecordsByContentUnitId(input.indexDocuments);
  const selectionRecordsByContentUnitId22 = groupSelectionRecordsByContentUnitId(input.indexDocuments);
  const selectionByContentUnitId = buildSelectionStateByContentUnitId(contentUnits, selectionRecordsByContentUnitId22);
  const contentUnitCandidates = buildContentUnitCandidates({
    contentUnits,
    candidateRecordsByContentUnitId,
    selectionRecordsByContentUnitId: selectionRecordsByContentUnitId22
  });
  const previewMoments = buildPreviewMoments({
    productions,
    segments,
    sceneMoments,
    expressionUnits,
    storyboards,
    keyframes,
    assets,
    previewTimelines,
    ancestryIndex,
    contentUnitsByPrimaryRef,
    candidateRecordsByContentUnitId,
    selectionRecordsByContentUnitId: selectionRecordsByContentUnitId22,
    selectionByContentUnitId
  });
  const expressionUnitsByMoment = buildExpressionUnitsByMoment(expressionUnits, ancestryIndex);
  const audioCuesByMoment = buildAudioCuesByMoment(audioCues, {
    ancestryIndex,
    contentUnitsByPrimaryRef,
    candidateRecordsByContentUnitId,
    selectionRecordsByContentUnitId: selectionRecordsByContentUnitId22,
    selectionByContentUnitId
  });
  const expressionUnitWorkspaceDetails = buildExpressionUnitWorkspaceDetails({
    expressionUnits,
    storyboards,
    keyframes,
    assets,
    settings,
    contentUnitsByPrimaryRef,
    ancestryIndex,
    candidateRecordsByContentUnitId,
    selectionRecordsByContentUnitId: selectionRecordsByContentUnitId22,
    selectionByContentUnitId
  });
  const assetReferenceUnits = buildAssetReferenceUnits({
    assets,
    settings,
    settingStates,
    expressionUnits,
    storyboards,
    keyframes,
    contentUnits,
    contentUnitsByPrimaryRef,
    ancestryIndex,
    candidateRecordsByContentUnitId,
    selectionRecordsByContentUnitId: selectionRecordsByContentUnitId22,
    selectionByContentUnitId
  });
  return {
    source: "workspace",
    domainGraph,
    hierarchyTree: buildHierarchyTree({
      settings,
      settingStates,
      assets,
      productions,
      segments,
      sceneMoments,
      storyboards,
      keyframes,
      expressionUnits,
      audioCues,
      assetReferenceUnits,
      ancestryIndex
    }),
    previewMoments,
    contentUnitCandidates,
    expressionUnitsByMoment,
    audioCuesByMoment,
    expressionUnitWorkspaceDetails,
    assetReferenceUnits,
    editingTimelines: input.editingTimelines ?? [],
    productionWorkPlan
  };
}
function buildContentSourceWorkspaceDomainGraph(input) {
  const nodes = input.domainNodes?.length ? input.domainNodes : deriveContentSourceWorkspaceDomainNodes(input);
  const edges = input.domainEdges?.length ? input.domainEdges : deriveContentSourceWorkspaceDomainEdges(input, nodes);
  return {
    namespaceVocabulary: input.namespaceVocabulary ?? deriveContentSourceWorkspaceNamespaceVocabulary(input),
    nodes,
    edges,
    timelineNamespaceNodes: nodes.filter((node) => node.category === "timeline_namespace"),
    settingNamespaceNodes: nodes.filter((node) => node.category === "setting_namespace"),
    systemPrimitiveNodes: nodes.filter((node) => node.category === "system_primitive"),
    contentUnitNodes: nodes.filter((node) => node.category === "content_unit")
  };
}
function deriveContentSourceWorkspaceNamespaceVocabulary(input) {
  const projectDocument = input.indexDocuments.find(
    (document) => normalizeWorkspacePath(document.path) === "project.json" && isRecord6(document.data)
  );
  return normalizeNamespaceVocabulary(projectDocument?.data);
}
function deriveContentSourceWorkspaceDomainNodes(input) {
  return sourceWorkspaceDomainEntities(input).flatMap((entity) => {
    const category = classifyMovScriptEntityKind(entity.entityKind);
    if (!category) return [];
    return [pruneUndefinedRecord({
      category,
      kind: projectMovScriptDomainNodeKind(entity.entityKind, entity.record),
      ...entity.id !== void 0 ? { id: entity.id } : {},
      path: entity.path,
      title: stringField7(entity.record.title) ?? stringField7(entity.record.label),
      order: optionalNumberField(entity.record.order),
      metadata: { entityKind: entity.entityKind }
    })];
  });
}
function deriveContentSourceWorkspaceDomainPathParentEdges(nodes) {
  const nodeByDir = /* @__PURE__ */ new Map();
  for (const node of nodes) {
    if (node.path) nodeByDir.set(entityDir2(node.path), node);
  }
  const edges = [];
  for (const node of nodes) {
    if (!node.path) continue;
    const parentPath = nearestParentPath(entityDir2(node.path), nodeByDir.keys());
    if (!parentPath) continue;
    const parent = nodeByDir.get(parentPath);
    const normalized = normalizePathParentEdge(domainRefFromNode(node), parent ? domainRefFromNode(parent) : void 0);
    if (normalized.edge) edges.push(normalized.edge);
  }
  return dedupeDomainEdges(edges);
}
function deriveContentSourceWorkspaceDomainEdges(input, nodes) {
  return dedupeDomainEdges([
    ...deriveContentSourceWorkspaceDomainPathParentEdges(nodes),
    ...deriveContentSourceWorkspaceContentUnitTargetEdges(input.contentUnits, nodes)
  ]);
}
function deriveContentSourceWorkspaceContentUnitTargetEdges(contentUnits, nodes) {
  const nodeByPath = new Map(nodes.flatMap((node) => node.path ? [[node.path, node]] : []));
  const timelineNamespaceNodes = nodes.filter((node) => node.category === "timeline_namespace");
  const edges = [];
  for (const contentUnit of contentUnits) {
    const sourceNode = nodeByPath.get(contentUnit.path);
    if (!sourceNode) continue;
    edges.push(...normalizeContentUnitTargetEdges({
      source: domainRefFromNode(sourceNode),
      record: contentUnit.record,
      scopeTarget(scope) {
        return domainRefFromTimelineScope(scope.kind, scope.ref, timelineNamespaceNodes);
      }
    }));
  }
  return edges;
}
function sourceWorkspaceDomainEntities(input) {
  return [
    ...input.settings,
    ...input.settingStates,
    ...input.assets,
    ...input.productions,
    ...input.segments,
    ...input.sceneMoments,
    ...input.storyboards,
    ...input.keyframes,
    ...input.expressionUnits,
    ...input.audioCues,
    ...input.contentUnits
  ];
}
function buildContentSourceWorkspaceEntityAncestryIndex(input, domainGraph) {
  const entities = sourceWorkspaceDomainEntities(input);
  const entityByPath = new Map(entities.map((entity) => [entity.path, entity]));
  const entityByDir = new Map(entities.map((entity) => [entityDir2(entity.path), entity]));
  const parentPathByPath = /* @__PURE__ */ new Map();
  for (const edge of domainGraph.edges) {
    if (edge.relation !== "parent" || edge.origin !== "path") continue;
    if (!edge.source.path || !edge.target.path) continue;
    parentPathByPath.set(edge.source.path, edge.target.path);
  }
  for (const entity of entities) {
    if (parentPathByPath.has(entity.path)) continue;
    const parentDir = nearestParentPath(entityDir2(entity.path), entityByDir.keys());
    const parent = parentDir ? entityByDir.get(parentDir) : void 0;
    if (parent) parentPathByPath.set(entity.path, parent.path);
  }
  const childrenByParentPath = /* @__PURE__ */ new Map();
  for (const [childPath, parentPath] of parentPathByPath) {
    const child = entityByPath.get(childPath);
    if (!child) continue;
    childrenByParentPath.set(parentPath, [...childrenByParentPath.get(parentPath) ?? [], child]);
  }
  return { entityByPath, parentPathByPath, childrenByParentPath };
}
function domainRefFromTimelineScope(scopeKind, scopeRef, timelineNamespaceNodes) {
  const node = timelineNamespaceNodes.find(
    (candidate) => String(candidate.id ?? "") === scopeRef && (candidate.kind === scopeKind || candidate.metadata?.entityKind === scopeKind)
  );
  return node ? domainRefFromNode(node) : { category: "timeline_namespace", kind: scopeKind, id: scopeRef };
}
function domainRefFromNode(node) {
  return {
    category: node.category,
    kind: node.kind,
    ...node.id !== void 0 ? { id: node.id } : {},
    ...node.path ? { path: node.path } : {}
  };
}
function dedupeDomainEdges(edges) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const edge of edges) {
    const key = JSON.stringify(edge);
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(edge);
  }
  return out;
}
function normalizeProductionWorkPlanView(plan) {
  if (!plan) return void 0;
  if (isProductionWorkPlanView(plan)) {
    return {
      summary: {
        open: optionalNumberField(plan.summary.open) ?? 0,
        blocking: optionalNumberField(plan.summary.blocking) ?? 0,
        humanRecommended: optionalNumberField(plan.summary.humanRecommended) ?? 0,
        agentRecommended: optionalNumberField(plan.summary.agentRecommended) ?? 0,
        readyToGenerate: optionalNumberField(plan.summary.readyToGenerate) ?? 0,
        staleSelections: optionalNumberField(plan.summary.staleSelections) ?? 0
      },
      items: plan.items.map(normalizeProductionWorkItemView).filter(isDefined)
    };
  }
  const rawPlan = plan;
  return {
    summary: {
      open: optionalNumberField(rawPlan.summary.open) ?? 0,
      blocking: optionalNumberField(rawPlan.summary.blocking) ?? 0,
      humanRecommended: optionalNumberField(rawPlan.summary.human_recommended) ?? 0,
      agentRecommended: optionalNumberField(rawPlan.summary.agent_recommended) ?? 0,
      readyToGenerate: optionalNumberField(rawPlan.summary.ready_to_generate) ?? 0,
      staleSelections: optionalNumberField(rawPlan.summary.stale_selections) ?? 0
    },
    items: rawPlan.items.map((item) => ({
      id: item.id,
      kind: item.kind,
      status: item.status,
      severity: item.severity,
      priority: item.priority,
      reason: item.reason,
      targetKind: item.target.entityKind,
      targetId: item.target.id !== void 0 ? String(item.target.id) : void 0,
      targetPath: item.target.path,
      recommendedActor: item.recommended_actor,
      actionLabels: item.actions.map((action) => productionWorkActionLabel(action.type))
    }))
  };
}
function isProductionWorkPlanView(plan) {
  return "humanRecommended" in plan.summary;
}
function normalizeProductionWorkItemView(item) {
  if (!item.id || !item.kind || !item.status || !item.severity || !item.reason || !item.targetKind) return void 0;
  return {
    id: item.id,
    kind: normalizeProductionWorkItemKind(item.kind),
    status: normalizeProductionWorkItemStatus(item.status),
    severity: normalizeProductionWorkItemSeverity(item.severity),
    priority: optionalNumberField(item.priority) ?? 100,
    reason: item.reason,
    targetKind: item.targetKind,
    targetId: item.targetId,
    targetPath: item.targetPath,
    recommendedActor: item.recommendedActor === "agent" || item.recommendedActor === "workflow" ? item.recommendedActor : "human",
    actionLabels: Array.isArray(item.actionLabels) ? item.actionLabels.filter((label) => typeof label === "string" && label.trim()) : []
  };
}
function normalizeProductionWorkItemKind(kind) {
  switch (kind) {
    case "fix_source":
    case "edit_structure":
    case "create_content_unit":
    case "generate_candidates":
    case "select_candidate":
    case "review_stale_selection":
    case "review_affected_output":
      return kind;
    default:
      return "edit_structure";
  }
}
function normalizeProductionWorkItemStatus(status) {
  if (status === "open" || status === "blocked" || status === "ready" || status === "informational") return status;
  return "open";
}
function normalizeProductionWorkItemSeverity(severity) {
  if (severity === "blocking" || severity === "warning" || severity === "suggestion") return severity;
  return "warning";
}
function productionWorkActionLabel(type) {
  switch (type) {
    case "open_editor":
      return "\u6253\u5F00\u7F16\u8F91\u5668";
    case "upsert_entity":
      return "\u8865\u7ED3\u6784";
    case "derive_content_unit_artifact":
      return "\u5237\u65B0\u521B\u4F5C\u7247\u6BB5";
    case "generate_candidates":
      return "\u751F\u6210\u5019\u9009";
    case "open_candidate_picker":
      return "\u6253\u5F00\u5019\u9009\u9009\u62E9";
    case "agent_review_candidates":
      return "\u8F85\u52A9\u5BA1\u9605\u5019\u9009";
    case "accept_stale":
      return "\u63A5\u53D7 stale";
    default:
      return type;
  }
}
function buildHierarchyTree(input) {
  return [
    {
      id: "settings_root",
      type: "group",
      title: "Settings",
      path: "settings/",
      children: sortEntities(input.settings).map((setting) => {
        const states = childEntitiesForParent(input.ancestryIndex, input.settingStates, setting);
        return entityNode(setting, "setting", {
          children: sortEntities(states).map((state) => {
            const stateAssets = childEntitiesForParent(input.ancestryIndex, input.assets, state);
            return entityNode(state, "state", {
              children: sortEntities(stateAssets).map((asset) => {
                const unit = input.assetReferenceUnits[nodeId(asset, "asset")];
                return entityNode(asset, "asset", { state: unit?.selectionState === "needs_candidate" ? "missing" : void 0 });
              })
            });
          })
        });
      })
    },
    {
      id: "productions_group",
      type: "group",
      title: "Productions",
      path: "productions",
      children: sortEntities(input.productions).map((production) => {
        const segments = childEntitiesForParent(input.ancestryIndex, input.segments, production);
        return entityNode(production, "production", {
          children: sortEntities(segments).map((segment) => {
            const sceneMoments = childEntitiesForParent(input.ancestryIndex, input.sceneMoments, segment);
            return entityNode(segment, "segment", {
              children: sortEntities(sceneMoments).map((sceneMoment) => {
                const momentDir = entityDir2(sceneMoment.path);
                const expressions = childEntitiesForParent(input.ancestryIndex, input.expressionUnits, sceneMoment);
                const momentStoryboards = childEntitiesForParent(input.ancestryIndex, input.storyboards, sceneMoment);
                const momentKeyframes = childEntitiesForParent(input.ancestryIndex, input.keyframes, sceneMoment);
                const audioCues = childEntitiesForParent(input.ancestryIndex, input.audioCues, sceneMoment);
                const momentId = idText(sceneMoment);
                const expressionGroup = {
                  id: `${nodeId(sceneMoment, "scene_moment")}_expression_group`,
                  type: "group",
                  title: "Expression Units",
                  path: `${momentDir}/expression_units`,
                  momentId,
                  children: sortEntities(expressions).map((expression) => {
                    const expressionDir = entityDir2(expression.path);
                    const expressionUnitId = idText(expression);
                    const storyboards = childEntitiesForParent(input.ancestryIndex, input.storyboards, expression);
                    const keyframes = childEntitiesForParent(input.ancestryIndex, input.keyframes, expression);
                    return entityNode(expression, "expression_unit", {
                      momentId,
                      expressionUnitId,
                      children: [
                        {
                          id: `${nodeId(expression, "expression_unit")}_storyboards_group`,
                          type: "group",
                          title: "Storyboards",
                          path: `${expressionDir}/storyboards`,
                          momentId,
                          expressionUnitId,
                          children: sortEntities(storyboards).map((storyboard) => entityNode(storyboard, "storyboard", { momentId, expressionUnitId }))
                        },
                        {
                          id: `${nodeId(expression, "expression_unit")}_keyframes_group`,
                          type: "group",
                          title: "Keyframes",
                          path: `${expressionDir}/keyframes`,
                          momentId,
                          expressionUnitId,
                          children: sortEntities(keyframes).map((keyframe) => entityNode(keyframe, "keyframe", { momentId, expressionUnitId }))
                        }
                      ]
                    });
                  })
                };
                const storyboardGroup = {
                  id: `${nodeId(sceneMoment, "scene_moment")}_storyboards_group`,
                  type: "group",
                  title: "Storyboards",
                  path: `${momentDir}/storyboards`,
                  momentId,
                  children: sortEntities(momentStoryboards).map((storyboard) => entityNode(storyboard, "storyboard", { momentId }))
                };
                const keyframeGroup = {
                  id: `${nodeId(sceneMoment, "scene_moment")}_keyframes_group`,
                  type: "group",
                  title: "Keyframes",
                  path: `${momentDir}/keyframes`,
                  momentId,
                  children: sortEntities(momentKeyframes).map((keyframe) => entityNode(keyframe, "keyframe", { momentId }))
                };
                const audioCueGroup = {
                  id: `${nodeId(sceneMoment, "scene_moment")}_audio_group`,
                  type: "group",
                  title: "Audio Cues",
                  path: `${momentDir}/audio_cues`,
                  momentId,
                  children: sortEntities(audioCues).map((audioCue) => entityNode(audioCue, "audio_cue", { momentId }))
                };
                return entityNode(sceneMoment, "scene_moment", {
                  momentId,
                  children: [expressionGroup, storyboardGroup, keyframeGroup, audioCueGroup]
                });
              })
            });
          })
        });
      })
    }
  ];
}
function buildPreviewMoments(input) {
  return orderedSceneMoments(input.sceneMoments, input.previewTimelines).map((moment, momentIndex) => {
    const segment = ancestorEntity(input.ancestryIndex, moment, "segment") ?? parentByDir(input.segments, moment.path);
    const production = segment ? ancestorEntity(input.ancestryIndex, segment, "production") ?? parentByDir(input.productions, segment.path) : ancestorEntity(input.ancestryIndex, moment, "production");
    const expressionUnits = orderedChildEntitiesForTimelineParent(input.expressionUnits, input.previewTimelines, timelineItemIdForEntity(moment), "expression_unit");
    const momentExpressionUnits = (expressionUnits.length > 0 ? expressionUnits : sortEntities(childEntitiesForParent(input.ancestryIndex, input.expressionUnits, moment))).map(
      (expressionUnit, expressionUnitIndex) => previewExpressionUnit(expressionUnit, expressionUnitIndex, moment, input)
    );
    return {
      id: idText(moment),
      title: titleOf(moment, `Scene Moment ${momentIndex + 1}`),
      path: entityDir2(moment.path),
      selectionState: momentSelectionState(momentExpressionUnits),
      priority: momentIndex < 1 ? "\u9AD8\u4F18\u5148\u7EA7" : momentIndex < 3 ? "\u4E2D\u4F18\u5148\u7EA7" : "\u4F4E\u4F18\u5148\u7EA7",
      production: production ? titleOf(production, idText(production)) : "",
      segment: segment ? titleOf(segment, idText(segment)) : "",
      settings: settingRefsForMoment(moment, momentExpressionUnits, input.assets),
      expressionUnits: momentExpressionUnits
    };
  });
}
function previewExpressionUnit(expressionUnit, expressionUnitIndex, moment, input) {
  const storyboards = sortEntities(childEntitiesForParent(input.ancestryIndex, input.storyboards, expressionUnit));
  const keyframes = sortEntities(childEntitiesForParent(input.ancestryIndex, input.keyframes, expressionUnit));
  const primaryStoryboard = storyboards[0];
  const primaryKeyframe = keyframes[0];
  const contentUnit = contentUnitForEntity(input.contentUnitsByPrimaryRef, "expression_unit", expressionUnit) ?? (primaryStoryboard ? contentUnitForEntity(input.contentUnitsByPrimaryRef, "storyboard", primaryStoryboard) : void 0) ?? (primaryKeyframe ? contentUnitForEntity(input.contentUnitsByPrimaryRef, "keyframe", primaryKeyframe) : void 0);
  return {
    id: idText(expressionUnit),
    title: titleOf(expressionUnit, `Expression Unit ${expressionUnitIndex + 1}`),
    kind: stringField7(expressionUnit.record.kind) ?? "expression_unit",
    camera: shotCameraText(expressionUnit),
    duration: durationText(recordField(expressionUnit.record.timing_intent)?.duration_sec ?? recordField(expressionUnit.record.timing)?.duration_sec ?? recordField(contentUnit?.record.model_intent)?.duration_sec),
    expression: shotExpressionText(expressionUnit),
    stillPosition: stillPositionForIndex(expressionUnitIndex),
    path: entityDir2(expressionUnit.path),
    keyframes: keyframes.map((keyframe) => idText(keyframe)),
    assets: shotAssets(expressionUnit, keyframes, input.assets),
    storyboard: primaryStoryboard ? nodeId(primaryStoryboard, "storyboard") : "",
    contentUnit: previewContentUnit(contentUnit, expressionUnit, moment, primaryStoryboard, keyframes, input)
  };
}
function orderedSceneMoments(sceneMoments, previewTimelines) {
  const timelineMoments = previewTimelines.flatMap(
    (timeline) => timeline.items.filter((item) => item.itemType === "scene_moment").sort((left, right) => left.order - right.order).map((item) => entityForTimelineItem(sceneMoments, item)).filter(isDefined)
  );
  if (timelineMoments.length === 0) return sortEntities(sceneMoments);
  return uniqueEntities([...timelineMoments, ...sortEntities(sceneMoments)]);
}
function orderedChildEntitiesForTimelineParent(entities, previewTimelines, parentItemId, itemType) {
  return previewTimelines.flatMap(
    (timeline) => timeline.items.filter((item) => item.itemType === itemType && item.parentId === parentItemId).sort((left, right) => left.order - right.order).map((item) => entityForTimelineItem(entities, item)).filter(isDefined)
  );
}
function entityForTimelineItem(entities, item) {
  return entities.find(
    (entity) => item.entity.id !== void 0 && String(entity.id ?? "") === String(item.entity.id) || entity.path === item.entity.path
  );
}
function uniqueEntities(entities) {
  return Array.from(new Map(entities.map((entity) => [entity.path, entity])).values());
}
function timelineItemIdForEntity(entity) {
  return `${entity.entityKind}:${String(entity.id ?? entity.path)}`;
}
function previewContentUnit(contentUnit, expressionUnit, moment, storyboard, keyframes, input) {
  const type = contentUnitType(contentUnit);
  const id = contentUnit ? idText(contentUnit) : `cu_${idText(expressionUnit)}`;
  const selection = input.selectionRecordsByContentUnitId.get(id);
  return {
    id,
    type,
    outputKind: outputKindForContentUnit(contentUnit),
    path: contentUnit?.path ?? `content_units/${id}/content_unit.json`,
    editPrompt: editPromptText(contentUnit) ?? "",
    sceneMomentRef: `scene_moment/${idText(moment)}`,
    expressionUnitRef: idText(expressionUnit),
    storyboardRef: storyboard ? nodeId(storyboard, "storyboard") : "",
    keyframeRefs: keyframes.map((keyframe) => idText(keyframe)),
    selectionState: input.selectionByContentUnitId.get(id) ?? selectionStateFromSourceSelection(selection, contentUnit),
    candidates: previewCandidatesForContentUnit(id, input.candidateRecordsByContentUnitId.get(id) ?? [], selection)
  };
}
function buildExpressionUnitsByMoment(expressionUnits, ancestryIndex) {
  const output = {};
  for (const expression of sortEntities(expressionUnits)) {
    const momentId = ancestorId(ancestryIndex, expression, "scene_moment") ?? pathSegmentAfter(expression.path, "scene_moments") ?? "";
    const item = {
      id: idText(expression),
      title: titleOf(expression, stringField7(expression.record.text) ?? idText(expression)),
      path: expression.path,
      kind: stringField7(expression.record.slot_kind ?? expression.record.expression_kind ?? expression.record.kind) ?? "visual",
      slotKind: expressionUnitSlotKindFromRecord(expression.record),
      text: stringField7(expression.record.text) ?? "",
      summary: stringField7(expression.record.intent ?? expression.record.note ?? expression.record.text) ?? "",
      speaker: stringField7(expression.record.speaker),
      note: stringField7(expression.record.note),
      sceneMomentId: momentId
    };
    output[momentId] = [...output[momentId] ?? [], item];
  }
  return output;
}
function buildAudioCuesByMoment(audioCues, input) {
  const output = {};
  for (const audioCue of sortEntities(audioCues)) {
    const momentId = ancestorId(input.ancestryIndex, audioCue, "scene_moment") ?? pathSegmentAfter(audioCue.path, "scene_moments") ?? "";
    const contentUnit = contentUnitForEntity(input.contentUnitsByPrimaryRef, "audio_cue", audioCue);
    const item = {
      id: idText(audioCue),
      title: titleOf(audioCue, idText(audioCue)),
      path: audioCue.path,
      cueKind: stringField7(audioCue.record.cue_kind ?? audioCue.record.kind) ?? "sound_effect",
      promptHint: stringField7(audioCue.record.prompt_hint) ?? "",
      expressionUnitRef: stringField7(audioCue.record.expression_unit_ref),
      storyboardRef: stringField7(audioCue.record.storyboard_ref),
      timing: recordField(audioCue.record.timing) ?? {},
      assetRefs: arrayField(audioCue.record.asset_refs).map(String),
      sceneMomentId: momentId,
      ...contentUnit ? { contentUnit: previewAudioCueContentUnit(contentUnit, audioCue, input) } : {}
    };
    output[momentId] = [...output[momentId] ?? [], item];
  }
  return output;
}
function previewAudioCueContentUnit(contentUnit, audioCue, input) {
  const id = idText(contentUnit);
  const selection = input.selectionRecordsByContentUnitId.get(id);
  return {
    id,
    type: contentUnitType(contentUnit),
    outputKind: outputKindForContentUnit(contentUnit),
    path: contentUnit.path,
    editPrompt: editPromptText(contentUnit) ?? "",
    sceneMomentRef: ancestorId(input.ancestryIndex, audioCue, "scene_moment") ?? pathSegmentAfter(audioCue.path, "scene_moments") ?? "",
    expressionUnitRef: stringField7(audioCue.record.expression_unit_ref) ?? "",
    storyboardRef: stringField7(audioCue.record.storyboard_ref) ?? "",
    keyframeRefs: [],
    selectionState: input.selectionByContentUnitId.get(id) ?? selectionStateFromSourceSelection(selection, contentUnit),
    candidates: previewCandidatesForContentUnit(id, input.candidateRecordsByContentUnitId.get(id) ?? [], selection)
  };
}
function transitionFromEntity(entity) {
  const transition = recordField(entity.record.transition);
  if (!transition) return void 0;
  const value = {
    in: stringField7(transition.in),
    out: stringField7(transition.out),
    notes: stringField7(transition.notes)
  };
  return Object.values(value).some(Boolean) ? value : void 0;
}
function storyboardTimelineFromEntity(entity) {
  const timeline = recordField(entity.record.timeline);
  if (!timeline) return void 0;
  const value = {
    caption: stringField7(timeline.caption),
    gapAfterSec: optionalNumberField(timeline.gap_after_sec),
    durationSec: optionalNumberField(timeline.duration_sec)
  };
  return Object.values(value).some((item) => item !== void 0) ? value : void 0;
}
function buildExpressionUnitWorkspaceDetails(input) {
  return Object.fromEntries(input.expressionUnits.map((expressionUnit) => {
    const keyframes = sortEntities(childEntitiesForParent(input.ancestryIndex, input.keyframes, expressionUnit));
    const storyboards = sortEntities(childEntitiesForParent(input.ancestryIndex, input.storyboards, expressionUnit));
    const refs = shotAssets(expressionUnit, keyframes, input.assets);
    const assets = refs.map((ref) => ({
      id: ref.title,
      title: ref.title.replace(/^asset\//, ""),
      owner: ref.title,
      status: ref.status === "missing" ? "missing" : ref.status === "locked" ? "locked" : "current",
      summary: ref.status === "missing" ? "\u8BE5\u7D20\u6750\u5F15\u7528\u5C1A\u672A\u5728 setting/state asset \u4E2D\u89E3\u6790\u3002" : "\u6765\u81EA workspace source \u7684\u7D20\u6750\u5F15\u7528\u3002",
      downstream: [idText(expressionUnit)]
    }));
    const settings = input.settings.slice(0, 4).map((setting) => ({
      id: nodeId(setting, "setting"),
      title: titleOf(setting, idText(setting)),
      owner: setting.path,
      status: "current",
      summary: stringField7(setting.record.summary ?? setting.record.description ?? setting.record.prompt_hint) ?? "Workspace setting context.",
      downstream: [idText(expressionUnit)]
    }));
    return [idText(expressionUnit), {
      settings,
      assets,
      keyframes: keyframes.map((keyframe) => expressionUnitChildOption(keyframe, "keyframe", expressionUnit, input)),
      storyboards: storyboards.map((storyboard) => expressionUnitChildOption(storyboard, "storyboard", expressionUnit, input)),
      impacts: []
    }];
  }));
}
function buildAssetReferenceUnits(input) {
  return Object.fromEntries(input.assets.map((asset) => {
    const contentUnit = contentUnitForEntity(input.contentUnitsByPrimaryRef, "asset", asset);
    const contentUnitId = contentUnit ? idText(contentUnit) : `cu_${idText(asset)}`;
    const ownerState = ancestorEntity(input.ancestryIndex, asset, "setting_state") ?? parentByDir(input.settingStates, asset.path);
    const ownerSetting = ownerState ? ancestorEntity(input.ancestryIndex, ownerState, "setting") ?? parentByDir(input.settings, ownerState.path) : ancestorEntity(input.ancestryIndex, asset, "setting");
    const assetId = nodeId(asset, "asset");
    const selection = input.selectionRecordsByContentUnitId.get(contentUnitId);
    const selectionState = contentUnit ? input.selectionByContentUnitId.get(contentUnitId) ?? "needs_candidate" : "ready";
    return [assetId, {
      assetId,
      title: titleOf(asset, idText(asset)),
      path: contentUnit?.path ?? `content_units/${contentUnitId}/content_unit.json`,
      contentUnitId,
      contentUnitType: "asset_ref",
      outputKind: "image",
      editPrompt: editPromptText(contentUnit) ?? "",
      usage: `${titleOf(asset, idText(asset))} \u4F5C\u4E3A setting/state \u4E0B\u7684\u7D20\u6750\u53C2\u8003\u8F93\u5165\u3002`,
      lockPolicy: "\u9009\u62E9\u53D8\u5316\u540E\uFF0C\u4E0B\u6E38\u5F15\u7528\u8BE5 asset_ref \u7684\u521B\u4F5C\u7247\u6BB5\u9700\u8981\u91CD\u65B0\u68C0\u67E5\u3002",
      selectionState,
      upstream: [
        ...ownerSetting ? [{ id: `setting:${idText(ownerSetting)}`, title: titleOf(ownerSetting, idText(ownerSetting)), kind: "setting", ownerNodeId: nodeId(ownerSetting, "setting"), state: "current", summary: ownerSetting.path }] : [],
        ...ownerState ? [{ id: `state:${idText(ownerState)}`, title: titleOf(ownerState, idText(ownerState)), kind: "state", ownerNodeId: nodeId(ownerState, "state"), state: "current", summary: ownerState.path }] : []
      ],
      candidates: previewAssetCandidatesForContentUnit(contentUnitId, input.candidateRecordsByContentUnitId.get(contentUnitId) ?? [], selection),
      downstream: buildAssetDownstreamUnits(asset, input)
    }];
  }));
}
function buildAssetDownstreamUnits(asset, input) {
  const assetId = idText(asset);
  const refs = /* @__PURE__ */ new Set([assetId, nodeId(asset, "asset")]);
  return input.contentUnits.filter((contentUnit) => {
    const promptRefs = editPromptRefs(contentUnit);
    const referencesAsset = promptRefs.some((ref) => ref.kind === "asset" && refs.has(ref.id));
    const isOwnAssetRef = stringField7(contentUnit.record.content_unit_type) === "asset_ref" && primaryRefIdsForContentUnitRecord2(contentUnit.record, "asset").some((ref) => refs.has(ref));
    return referencesAsset && !isOwnAssetRef;
  }).map((contentUnit) => {
    const contentUnitId = idText(contentUnit);
    const owner = primaryOwnerForContentUnit(contentUnit, input);
    const selection = input.selectionRecordsByContentUnitId.get(contentUnitId);
    const selectedCandidateId = idValue(selection?.candidate_id);
    const selectedCandidate = selectedCandidateId ? input.candidateRecordsByContentUnitId.get(contentUnitId)?.find((candidate) => selectionCandidateMatches(selection, idValue(candidate.id) ?? "")) : void 0;
    const state = input.selectionByContentUnitId.get(contentUnitId) ?? selectionStateFromSourceSelection(selection, contentUnit);
    return {
      id: `asset:${assetId}:content_unit:${contentUnitId}`,
      title: titleOf(contentUnit, contentUnitId),
      kind: "content_unit",
      ownerNodeId: owner?.nodeId ?? contentUnitId,
      momentId: owner?.momentId ?? "",
      expressionUnitId: owner?.expressionUnitId ?? "",
      dependencyHash: selectedCandidate ? candidateInputHash(selectedCandidate, contentUnitId) : contentUnitId,
      state,
      action: state === "selected" ? "\u5DF2\u9009\u62E9\u5019\u9009\u5F15\u7528\u8BE5 asset" : "\u9700\u8981\u5019\u9009\u6216\u9009\u62E9\u786E\u8BA4",
      preview: `${contentUnitId} \u5728 edit_prompt \u4E2D\u5F15\u7528 ${nodeId(asset, "asset")}\u3002`
    };
  });
}
function primaryOwnerForContentUnit(contentUnit, input) {
  const expressionUnitRef = primaryRefIdsForContentUnitRecord2(contentUnit.record, "expression_unit")[0];
  if (expressionUnitRef) {
    const expressionUnit = input.expressionUnits.find((item) => entityMatchesRef(item, expressionUnitRef, "expression_unit"));
    return {
      nodeId: expressionUnit ? idText(expressionUnit) : expressionUnitRef,
      momentId: expressionUnit ? ancestorId(input.ancestryIndex, expressionUnit, "scene_moment") ?? pathSegmentAfter(expressionUnit.path, "scene_moments") ?? "" : "",
      expressionUnitId: expressionUnit ? idText(expressionUnit) : expressionUnitRef
    };
  }
  const storyboardRef = primaryRefIdsForContentUnitRecord2(contentUnit.record, "storyboard")[0];
  if (storyboardRef) {
    const storyboard = input.storyboards.find((item) => entityMatchesRef(item, storyboardRef, "storyboard"));
    return {
      nodeId: storyboard ? nodeId(storyboard, "storyboard") : `storyboard/${storyboardRef}`,
      momentId: storyboard ? ancestorId(input.ancestryIndex, storyboard, "scene_moment") ?? pathSegmentAfter(storyboard.path, "scene_moments") ?? "" : "",
      expressionUnitId: storyboard ? ancestorId(input.ancestryIndex, storyboard, "expression_unit") ?? pathSegmentAfter(storyboard.path, "expression_units") ?? "" : ""
    };
  }
  const keyframeRef = primaryRefIdsForContentUnitRecord2(contentUnit.record, "keyframe")[0];
  if (keyframeRef) {
    const keyframe = input.keyframes.find((item) => entityMatchesRef(item, keyframeRef, "keyframe"));
    return {
      nodeId: keyframe ? idText(keyframe) : keyframeRef,
      momentId: keyframe ? ancestorId(input.ancestryIndex, keyframe, "scene_moment") ?? pathSegmentAfter(keyframe.path, "scene_moments") ?? "" : "",
      expressionUnitId: keyframe ? ancestorId(input.ancestryIndex, keyframe, "expression_unit") ?? pathSegmentAfter(keyframe.path, "expression_units") ?? "" : ""
    };
  }
  return void 0;
}
function expressionUnitChildOption(entity, primaryKind, expressionUnit, input) {
  const contentUnit = contentUnitForEntity(input.contentUnitsByPrimaryRef, primaryKind, entity);
  const contentUnitId = contentUnit ? idText(contentUnit) : "";
  const selection = contentUnitId ? input.selectionRecordsByContentUnitId.get(contentUnitId) : void 0;
  return {
    id: idText(entity),
    title: titleOf(entity, idText(entity)),
    status: contentUnit ? "candidate" : "draft",
    inputHash: contentUnitId || "source",
    summary: stringField7(entity.record.visual_intent ?? entity.record.summary ?? entity.record.description ?? entity.record.slot) ?? entity.path,
    ...contentUnit ? {
      contentUnit: {
        id: contentUnitId,
        type: primaryKind === "keyframe" ? "keyframe_ref" : "storyboard_ref",
        outputKind: outputKindForContentUnit(contentUnit),
        path: contentUnit.path,
        editPrompt: editPromptText(contentUnit) ?? "",
        sceneMomentRef: `scene_moment/${ancestorId(input.ancestryIndex, expressionUnit, "scene_moment") ?? pathSegmentAfter(expressionUnit.path, "scene_moments") ?? ""}`,
        expressionUnitRef: idText(expressionUnit),
        storyboardRef: primaryKind === "storyboard" ? nodeId(entity, "storyboard") : "",
        keyframeRefs: primaryKind === "keyframe" ? [idText(entity)] : [],
        selectionState: input.selectionByContentUnitId.get(contentUnitId) ?? selectionStateFromSourceSelection(selection, contentUnit),
        candidates: previewCandidatesForContentUnit(contentUnitId, input.candidateRecordsByContentUnitId.get(contentUnitId) ?? [], selection)
      }
    } : {}
  };
}
function buildContentUnitCandidates(input) {
  const ids = /* @__PURE__ */ new Set([
    ...input.contentUnits.map(idText),
    ...input.candidateRecordsByContentUnitId.keys(),
    ...input.selectionRecordsByContentUnitId.keys()
  ]);
  const output = {};
  for (const contentUnitId of ids) {
    output[contentUnitId] = previewCandidatesForContentUnit(
      contentUnitId,
      input.candidateRecordsByContentUnitId.get(contentUnitId) ?? [],
      input.selectionRecordsByContentUnitId.get(contentUnitId)
    );
  }
  return output;
}
function groupContentUnitsByPrimaryRef(contentUnits) {
  const output = /* @__PURE__ */ new Map();
  for (const contentUnit of contentUnits) {
    const assemblyScope = timelineAssemblyScopeFromContentUnitRecord(contentUnit.record);
    if (assemblyScope) {
      for (const key of primaryRefKeys(assemblyScope.kind, assemblyScope.ref)) {
        output.set(key, [...output.get(key) ?? [], contentUnit]);
      }
    }
    const type = stringField7(contentUnit.record.content_unit_type);
    const primaryKind = primaryKindForContentUnitType(type);
    if (!primaryKind) continue;
    for (const ref of primaryRefIdsForContentUnitRecord2(contentUnit.record, primaryKind)) {
      for (const key of primaryRefKeys(primaryKind, ref)) {
        output.set(key, [...output.get(key) ?? [], contentUnit]);
      }
    }
  }
  return output;
}
function groupContentCandidateRecordsByContentUnitId(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (document.path.endsWith("/content_candidate.json") && isContentCandidateRecord(document.data)) {
      const contentUnitId2 = contentUnitIdForRuntimeDocument(document.path, document.data.content_unit_ref);
      if (!contentUnitId2) continue;
      appendContentCandidateRecord(output, contentUnitId2, document.data);
      continue;
    }
    if (!isDecisionContextRecord(document.data)) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument(document.path, stringField7(document.data.target_ref));
    if (!contentUnitId) continue;
    for (const candidate of arrayField(document.data.candidates)) {
      const record = normalizeDecisionContentCandidateRecord(candidate, contentUnitId);
      if (record) appendContentCandidateRecord(output, contentUnitId, record);
    }
  }
  for (const [contentUnitId, candidates] of output.entries()) {
    output.set(contentUnitId, candidates.sort((left, right) => (stringField7(right.created_at) ?? "").localeCompare(stringField7(left.created_at) ?? "")));
  }
  return output;
}
function appendContentCandidateRecord(output, contentUnitId, candidate) {
  const candidates = output.get(contentUnitId) ?? [];
  const candidateId = idValue(candidate.id);
  if (!candidateId) {
    output.set(contentUnitId, [...candidates, candidate]);
    return;
  }
  const existingIndex = candidates.findIndex((existing) => idValue(existing.id) === candidateId);
  if (existingIndex < 0) {
    output.set(contentUnitId, [...candidates, candidate]);
    return;
  }
  output.set(contentUnitId, [
    ...candidates.slice(0, existingIndex),
    mergeContentCandidateRecords(candidates[existingIndex], candidate),
    ...candidates.slice(existingIndex + 1)
  ]);
}
function normalizeDecisionContentCandidateRecord(value, contentUnitId) {
  if (!isRecord6(value) || !idValue(value.id)) return void 0;
  return {
    ...value,
    content_unit_ref: stringField7(value.content_unit_ref) ?? `content_units/${contentUnitId}`
  };
}
function mergeContentCandidateRecords(existing, incoming) {
  const merged = { ...existing, ...incoming };
  if (arrayField(incoming.outputs).length === 0 && arrayField(existing.outputs).length > 0) merged.outputs = existing.outputs;
  if (!recordField(incoming.producer) && recordField(existing.producer)) merged.producer = existing.producer;
  if (!recordField(incoming.prompt_snapshot) && recordField(existing.prompt_snapshot)) merged.prompt_snapshot = existing.prompt_snapshot;
  if (!stringField7(incoming.status) && stringField7(existing.status)) merged.status = existing.status;
  if (!stringField7(incoming.source) && stringField7(existing.source)) merged.source = existing.source;
  if (!stringField7(incoming.created_at) && stringField7(existing.created_at)) merged.created_at = existing.created_at;
  if (!stringField7(incoming.content_unit_ref) && stringField7(existing.content_unit_ref)) merged.content_unit_ref = existing.content_unit_ref;
  return merged;
}
function groupSelectionRecordsByContentUnitId(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!isDecisionContextRecord(document.data)) continue;
    const selection = recordField(document.data.selection);
    if (!selection) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument(document.path, stringField7(document.data.target_ref));
    if (!contentUnitId) continue;
    output.set(contentUnitId, normalizeContentSelectionRecord(selection));
  }
  return output;
}
function previewCandidatesForContentUnit(contentUnitId, candidates, selection) {
  return candidates.map((candidate, index) => {
    const id = idValue(candidate.id) ?? `candidate_${index + 1}`;
    const output = firstCandidateOutput(candidate);
    return {
      id,
      title: candidateTitle(candidate, candidateOrdinalTitle(index)),
      model: candidateModel(candidate),
      inputHash: candidateInputHash(candidate, contentUnitId),
      selected: selectionCandidateMatches(selection, id),
      note: candidateNote(candidate),
      resourceId: resourceIdValue(output?.resource_id),
      resourceKind: stringField7(output?.kind),
      artifactRef: stringField7(output?.artifact_ref),
      status: stringField7(candidate.status),
      decisionStatus: candidateDecisionStatus(candidate),
      decisionReason: candidateDecisionReason(candidate),
      source: stringField7(candidate.source),
      producer: candidate.producer,
      outputs: candidate.outputs,
      promptSnapshot: candidate.prompt_snapshot,
      createdAt: stringField7(candidate.created_at)
    };
  });
}
function previewAssetCandidatesForContentUnit(contentUnitId, candidates, selection) {
  return candidates.map((candidate, index) => {
    const id = idValue(candidate.id) ?? `candidate_${index + 1}`;
    const output = firstCandidateOutput(candidate);
    return {
      id,
      title: candidateTitle(candidate, candidateOrdinalTitle(index)),
      model: candidateModel(candidate),
      inputHash: candidateInputHash(candidate, contentUnitId),
      selected: selectionCandidateMatches(selection, id),
      note: candidateNote(candidate),
      resourceId: resourceIdValue(output?.resource_id),
      resourceKind: stringField7(output?.kind),
      artifactRef: stringField7(output?.artifact_ref),
      status: stringField7(candidate.status),
      decisionStatus: candidateDecisionStatus(candidate),
      decisionReason: candidateDecisionReason(candidate),
      source: stringField7(candidate.source),
      producer: candidate.producer,
      outputs: candidate.outputs,
      promptSnapshot: candidate.prompt_snapshot,
      createdAt: stringField7(candidate.created_at),
      confirmation: assetCandidateConfirmation(candidate, selection, id)
    };
  });
}
function selectionStateFromSourceSelection(selection, contentUnit) {
  if (selection?.candidate_id !== void 0) return "selected";
  return contentUnit ? "needs_candidate" : "ready";
}
function buildSelectionStateByContentUnitId(contentUnits, selections) {
  const entries = contentUnits.map((contentUnit) => {
    const id = idText(contentUnit);
    const selection = selections.get(id);
    if (selection?.candidate_id !== void 0) return [id, "selected"];
    return [id, "needs_candidate"];
  });
  return new Map(entries);
}
function contentUnitForEntity(contentUnitsByPrimaryRef, entityKind, entity) {
  if (entity.id === void 0) return void 0;
  return contentUnitsByPrimaryRef.get(primaryRefKey(entityKind, entity.id))?.[0];
}
function primaryKindForContentUnitType(type) {
  return type ? primaryRefKindForContentUnitType(type) : void 0;
}
function primaryRefIdsForContentUnitRecord2(record, kind) {
  return isContentUnitPromptRefKind(kind) ? primaryRefIdsForContentUnitRecord(record, kind) : [];
}
function editPromptRefs(contentUnit) {
  const text = editPromptText(contentUnit) ?? "";
  const refs = [];
  const pattern = /\{\{\s*([a-z_]+)\s*:\s*([^}\s]+)\s*\}\}/g;
  let match = pattern.exec(text);
  while (match) {
    refs.push({ kind: match[1] ?? "", id: match[2] ?? "" });
    match = pattern.exec(text);
  }
  return refs;
}
function editPromptText(contentUnit) {
  const prompt = contentUnit?.record.edit_prompt;
  if (typeof prompt === "string") return prompt;
  if (isRecord6(prompt)) return stringField7(prompt.text);
  return void 0;
}
function entityNode(entity, type, extras = {}) {
  return {
    id: nodeId(entity, type),
    type,
    title: titleOf(entity, idText(entity)),
    path: entity.path,
    transition: supportsTransition(type) ? transitionFromEntity(entity) : void 0,
    storyboardTimeline: type === "storyboard" ? storyboardTimelineFromEntity(entity) : void 0,
    ...extras
  };
}
function supportsTransition(type) {
  return type === "production" || type === "segment" || type === "scene_moment" || type === "storyboard";
}
function nodeId(entity, type) {
  if (type === "setting") return `setting/${idText(entity)}`;
  if (type === "state") return `state/${pathSegmentAfter(entity.path, "settings") ?? ""}/${idText(entity)}`;
  if (type === "asset") return `asset/${idText(entity)}`;
  if (type === "storyboard") return `storyboard/${idText(entity)}`;
  return idText(entity);
}
function titleOf(entity, fallback) {
  return stringField7(entity.record.title ?? entity.record.name ?? entity.record.label ?? entity.record.text) ?? fallback;
}
function idText(entity) {
  return String(entity.id ?? entity.record.id ?? entity.record.ID ?? entity.path);
}
function entityMatchesRef(entity, ref, kind) {
  const normalized = ref.replace(/\/+$/, "");
  const dir = entity.path.replace(/\/[^/]+$/, "");
  return String(entity.id ?? "") === ref || dir === normalized || entity.path === `${normalized}/${kind}.json`;
}
function sortEntities(entities) {
  return [...entities].sort((left, right) => numberField5(left.record.order) - numberField5(right.record.order) || left.path.localeCompare(right.path));
}
function childEntitiesForParent(ancestryIndex, entities, parent) {
  const directChildren = new Set((ancestryIndex.childrenByParentPath.get(parent.path) ?? []).map((entity) => entity.path));
  return entities.filter((entity) => directChildren.has(entity.path));
}
function ancestorEntity(ancestryIndex, entity, entityKind) {
  const seen = /* @__PURE__ */ new Set([entity.path]);
  let currentPath = entity.path;
  while (true) {
    const parentPath = ancestryIndex.parentPathByPath.get(currentPath);
    if (!parentPath || seen.has(parentPath)) return void 0;
    seen.add(parentPath);
    const parent = ancestryIndex.entityByPath.get(parentPath);
    if (!parent) return void 0;
    if (parent.entityKind === entityKind) return parent;
    currentPath = parent.path;
  }
}
function ancestorId(ancestryIndex, entity, entityKind) {
  const ancestor = ancestorEntity(ancestryIndex, entity, entityKind);
  return ancestor ? idText(ancestor) : void 0;
}
function parentByDir(entities, childPath) {
  const childDir = entityDir2(childPath);
  return entities.find((entity) => childDir.startsWith(`${entityDir2(entity.path)}/`));
}
function entityDir2(path) {
  return path.replace(/\/[^/]+$/, "");
}
function pathSegmentAfter(path, segment) {
  const parts = path.split("/");
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function primaryRefKey(kind, id) {
  return `${kind}:${String(id ?? "")}`;
}
function primaryRefKeys(kind, ref) {
  const value = String(ref);
  const keys = [primaryRefKey(kind, value)];
  const lastSegment = value.split("/").filter(Boolean).at(-1);
  if (lastSegment && lastSegment !== value) keys.push(primaryRefKey(kind, lastSegment));
  return keys;
}
function shotCameraText(shot) {
  return [
    stringField7(shot.record.shot_size),
    stringField7(recordField(shot.record.camera)?.movement),
    stringField7(recordField(shot.record.camera)?.angle)
  ].filter(Boolean).join(" \xB7 ") || stringField7(shot.record.shot_kind) || "shot";
}
function shotExpressionText(shot) {
  return stringField7(shot.record.description ?? shot.record.summary ?? recordField(shot.record.expression)?.text ?? recordField(shot.record.expression)?.intent) ?? titleOf(shot, idText(shot));
}
function durationText(value) {
  const duration = Number(value);
  return Number.isFinite(duration) && duration > 0 ? `${duration}s` : "\u5F85\u5B9A";
}
function stillPositionForIndex(index) {
  return ["0% 0%", "100% 0%", "0% 100%", "100% 100%"][index % 4] ?? "0% 0%";
}
function contentUnitType(contentUnit) {
  const type = stringField7(contentUnit?.record.content_unit_type);
  if (type === "keyframe_ref" || type === "storyboard_ref" || type === "audio_cue_ref" || type === "scence_moment_ref" || type === "scene_moment_ref" || type === "expression_unit_ref") return type;
  return "expression_unit_ref";
}
function outputKindForContentUnit(contentUnit) {
  const outputKind = stringField7(contentUnit?.record.output_kind);
  if (outputKind === "image" || outputKind === "video" || outputKind === "audio" || outputKind === "text" || outputKind === "metadata" || outputKind === "storyboard") return outputKind;
  const type = contentUnitType(contentUnit);
  if (type === "keyframe_ref" || type === "storyboard_ref") return "image";
  if (type === "audio_cue_ref") return "audio";
  return "video";
}
function momentSelectionState(expressionUnits) {
  if (expressionUnits.some((expressionUnit) => expressionUnit.contentUnit.selectionState === "stale")) return "stale";
  if (expressionUnits.some((expressionUnit) => expressionUnit.contentUnit.selectionState === "needs_candidate")) return "needs_candidate";
  if (expressionUnits.some((expressionUnit) => expressionUnit.contentUnit.selectionState === "selected")) return "selected";
  return "ready";
}
function settingRefsForMoment(_moment, expressionUnits, assets) {
  const refs = /* @__PURE__ */ new Set();
  for (const expressionUnit of expressionUnits) {
    for (const asset of expressionUnit.assets) refs.add(asset.title);
  }
  if (refs.size === 0) {
    for (const asset of assets.slice(0, 3)) refs.add(nodeId(asset, "asset"));
  }
  return [...refs];
}
function shotAssets(shot, keyframes, assets) {
  const refs = [
    ...arrayField(shot.record.reference_asset_refs),
    ...keyframes.flatMap((keyframe) => arrayField(keyframe.record.reference_asset_refs))
  ].map(String);
  const uniqueRefs = [...new Set(refs)];
  return uniqueRefs.map((ref) => ({
    title: ref.startsWith("asset/") ? ref : `asset/${ref}`,
    status: assets.some((asset) => String(asset.id ?? "") === ref || entityDir2(asset.path) === ref || asset.path.startsWith(`${ref}/`)) ? "ready" : "missing"
  }));
}
function contentUnitIdForRuntimeDocument(path, explicitRef) {
  const ref = explicitRef ?? entityDir2(path);
  const id = pathSegmentAfter(ref, "content_units");
  return id;
}
function candidateTitle(candidate, fallback) {
  const explicitTitle = stringField7(candidate.prompt_snapshot?.title) ?? stringField7(candidate.producer?.title) ?? stringField7(candidate.producer?.name);
  if (explicitTitle && !candidateTitleIsGeneric(explicitTitle)) return explicitTitle;
  return readableCandidateTitleFallback(fallback);
}
function candidateOrdinalTitle(index) {
  return `\u5019\u9009 ${index + 1}`;
}
function candidateTitleIsGeneric(value) {
  const normalized = value.trim().toLowerCase();
  return normalized === "queued generation" || normalized === "pending generation" || normalized === "content unit image generation" || normalized === "content unit video generation" || technicalCandidateIdPattern.test(normalized);
}
function readableCandidateTitleFallback(value) {
  return technicalCandidateIdPattern.test(value.trim().toLowerCase()) ? "\u5019\u9009" : value;
}
var technicalCandidateIdPattern = /^(canvas|resource|content)_candidate_[\w-]+$|^resource_\d+_[\w-]+$|^gen_(image|video)_\d+_\d+$/;
function candidateModel(candidate) {
  return stringField7(candidate.producer?.model_id) ?? stringField7(candidate.producer?.model) ?? stringField7(candidate.producer?.kind) ?? stringField7(candidate.source) ?? "runtime";
}
function candidateInputHash(candidate, contentUnitId) {
  return stringField7(candidate.prompt_snapshot?.input_hash) ?? stringField7(candidate.prompt_snapshot?.content_hash) ?? stringField7(candidate.prompt_snapshot?.hash) ?? stringField7(candidate.created_at) ?? contentUnitId;
}
function candidateNote(candidate) {
  const output = firstCandidateOutput(candidate);
  return stringField7(candidate.prompt_snapshot?.note) ?? stringField7(candidate.prompt_snapshot?.summary) ?? stringField7(output?.mime_type) ?? stringField7(candidate.status) ?? "Workspace runtime candidate.";
}
function candidateDecisionStatus(candidate) {
  const record = candidate;
  return stringField7(record.decision_status) ?? stringField7(record.decisionStatus);
}
function candidateDecisionReason(candidate) {
  const record = candidate;
  return stringField7(record.decision_reason) ?? stringField7(record.decisionReason);
}
function firstCandidateOutput(candidate) {
  return arrayField(candidate.outputs).filter(isRecord6)[0];
}
function assetCandidateConfirmation(candidate, selection, candidateId) {
  if (selectionCandidateMatches(selection, candidateId)) return "confirmed";
  if (candidate.status === "failed" || candidate.status === "canceled") return "stale";
  return "review";
}
function selectionCandidateMatches(selection, candidateId) {
  return selection?.candidate_id !== void 0 && String(selection.candidate_id) === candidateId;
}
function idValue(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function recordField(value) {
  return isRecord6(value) ? value : void 0;
}
function stringField7(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberField5(value) {
  const numberValue23 = Number(value);
  return Number.isFinite(numberValue23) ? numberValue23 : Number.MAX_SAFE_INTEGER;
}
function optionalNumberField(value) {
  const numberValue23 = Number(value);
  return Number.isFinite(numberValue23) ? numberValue23 : void 0;
}
function pruneUndefinedRecord(record) {
  const output = {};
  for (const [key, value] of Object.entries(record)) {
    if (value !== void 0) output[key] = value;
  }
  return output;
}
function arrayField(value) {
  return Array.isArray(value) ? value : [];
}
function isRecord6(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function isContentCandidateRecord(value) {
  return isRecord6(value) && value.schema === "movscript.content_candidate.v1";
}
function isDecisionContextRecord(value) {
  return isRecord6(value) && value.schema === "movscript.decision_context.v1" && value.target_kind === "content_unit";
}
function normalizeContentSelectionRecord(value) {
  return {
    candidate_id: value.candidate_id,
    resource_id: resourceIdValue(value.resource_id),
    artifact_ref: stringField7(value.artifact_ref),
    stale_policy: value.stale_policy,
    reason: value.reason,
    selected_at: value.selected_at
  };
}
function resourceIdValue(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function isDefined(value) {
  return value !== void 0;
}
function buildContentSourceWorkspaceProjectTimelineStatus(snapshot, contentUnitSummaries = contentSourceWorkspaceContentUnitStatusSummaries(snapshot)) {
  const namespaceVocabulary = snapshot.namespaceVocabulary ?? {
    timelineTemplate: void 0,
    timelineNamespaces: [],
    settingNamespaces: [],
    diagnostics: []
  };
  const timelineNamespaceNodes = (snapshot.domainNodes ?? []).filter((node) => node.category === "timeline_namespace").sort((left, right) => (left.order ?? 0) - (right.order ?? 0) || String(left.path ?? left.id ?? "").localeCompare(String(right.path ?? right.id ?? ""), "zh-CN"));
  const timelineNamespaces = timelineNamespaceNodes.map((node) => {
    const parent = domainParentRefForNode(snapshot, node);
    const record = domainRecordForNode(snapshot, node);
    const productionType = stringField23(record?.production_type ?? record?.productionType ?? record?.timeline_profile ?? record?.timelineProfile);
    const nodeTimelineNamespaces = stringArrayField(record?.timeline_namespaces ?? record?.timelineNamespaces);
    return {
      id: node.id,
      kind: node.kind,
      title: node.title,
      path: node.path,
      order: node.order,
      entity_kind: stringField23(node.metadata?.entityKind),
      ...productionType ? { production_type: productionType } : {},
      ...nodeTimelineNamespaces.length ? { timeline_namespaces: nodeTimelineNamespaces } : {},
      ...parent ? { parent } : {}
    };
  });
  const timelineAssemblies = timelineAssemblyStatusEntries(snapshot, contentUnitSummaries);
  const hasBlockingRefs = timelineAssemblies.some((item) => arrayValue3(item.blocking_refs).length > 0);
  const hasStaleSelection = timelineAssemblies.some((item) => {
    const staleStatus = stringField23(item.stale_status);
    return staleStatus === "has_stale_selection" || staleStatus === "stale";
  });
  return {
    schema: "movscript.project_timeline_status.v1",
    status: hasBlockingRefs ? "blocked" : hasStaleSelection ? "needs_review" : "ok",
    namespace_vocabulary: {
      timeline_template: namespaceVocabulary.timelineTemplate,
      timeline_namespaces: namespaceVocabulary.timelineNamespaces,
      setting_namespaces: namespaceVocabulary.settingNamespaces,
      diagnostics: namespaceVocabulary.diagnostics
    },
    timeline_namespace_count: timelineNamespaces.length,
    timeline_namespaces: timelineNamespaces,
    timeline_assembly_count: timelineAssemblies.length,
    timeline_assemblies: timelineAssemblies,
    system_primitives: {
      scene_moments_count: snapshot.sceneMoments.length,
      expression_units_count: snapshot.expressionUnits.length,
      storyboards_count: snapshot.storyboards.length,
      keyframes_count: snapshot.keyframes.length,
      audio_cues_count: snapshot.audioCues.length,
      assets_count: snapshot.assets.length
    },
    legacy_aliases: {
      productions: "timeline_namespaces",
      segments: "timeline_namespaces",
      production_ref: "timeline_assembly_ref",
      segment_ref: "timeline_assembly_ref"
    }
  };
}
function contentSourceWorkspaceContentUnitStatusSummaries(snapshot) {
  const candidatesByContentUnit = contentCandidateRecordsByContentUnitId(snapshot.indexDocuments);
  const selectionsByContentUnit = selectionRecordsByContentUnitId(snapshot.indexDocuments);
  return snapshot.contentUnits.map((unit) => summarizeContentUnitStatus(unit, candidatesByContentUnit, selectionsByContentUnit));
}
function timelineAssemblyStatusEntries(snapshot, contentUnitSummaries) {
  const contentUnitById = new Map(snapshot.contentUnits.map((unit) => [String(entityId(unit)), unit]));
  return contentUnitSummaries.filter((summary) => isTimelineAssemblyContentUnitSummary(summary)).map((summary) => {
    const contentUnitId = idField2(summary.content_unit_id);
    const unit = contentUnitById.get(String(contentUnitId));
    const scope = unit ? timelineAssemblyScopeRefForContentUnit(snapshot, unit) : void 0;
    return {
      assembly_ref: summary.target_ref,
      content_unit_id: contentUnitId,
      title: summary.title,
      path: summary.path,
      content_unit_type: summary.content_unit_type,
      target_kind: "timeline_assembly",
      target_ref: summary.target_ref,
      output_kind: summary.output_kind,
      candidate_count: summary.candidate_count,
      candidate_ids: summary.candidate_ids,
      selected_candidate: summary.selected_candidate,
      selected_resource: summary.selected_resource,
      stale_status: summary.stale_status,
      blocking_refs: summary.blocking_refs,
      ...scope ? { scope } : {}
    };
  });
}
function summarizeContentUnitStatus(unit, candidatesByContentUnit, selectionsByContentUnit) {
  const id = entityId(unit);
  const contentUnitId = String(id);
  const candidates = candidatesByContentUnit.get(contentUnitId) ?? [];
  const selection = selectionsByContentUnit.get(contentUnitId);
  const selectedCandidate = selection?.candidate_id !== void 0 ? candidates.find((candidate) => sameId(candidate.id, selection.candidate_id)) : void 0;
  const selectedResourceId2 = numberField22(selection?.resource_id) ?? numberField22(firstCandidateOutput2(selectedCandidate)?.resource_id);
  return {
    content_unit_id: id,
    title: stringField23(unit.record.title),
    path: unit.path,
    content_unit_type: stringField23(unit.record.content_unit_type),
    output_kind: stringField23(unit.record.output_kind),
    target_kind: stringField23(unit.record.target_kind),
    target_ref: idField2(unit.record.target_ref),
    candidate_count: candidates.length,
    candidate_ids: candidates.map((candidate) => idField2(candidate.id)).filter((value) => value !== void 0),
    selected_candidate: selection?.candidate_id,
    selected_resource: selectedResourceId2,
    stale_status: stringField23(selection?.stale_policy) === "accept_stale" ? "accepted_stale" : "ok",
    blocking_refs: selection?.candidate_id === void 0 && candidates.length > 0 ? ["selection_missing"] : []
  };
}
function isTimelineAssemblyContentUnitSummary(summary) {
  const type = stringField23(summary.content_unit_type);
  const targetKind = stringField23(summary.target_kind);
  const targetRef = stringField23(summary.target_ref);
  return type === "timeline_assembly_ref" || targetKind === "timeline_assembly" || Boolean(targetRef?.startsWith("timeline_assembly:"));
}
function timelineAssemblyScopeRefForContentUnit(snapshot, unit) {
  const edge = (snapshot.domainEdges ?? []).find((candidate) => candidate.relation === "scope" && candidate.target.category === "timeline_namespace" && domainRefMatchesEntity(candidate.source, unit));
  if (!edge) return void 0;
  return {
    category: edge.target.category,
    kind: edge.target.kind,
    id: edge.target.id,
    path: edge.target.path
  };
}
function domainParentRefForNode(snapshot, node) {
  const edge = (snapshot.domainEdges ?? []).find((candidate) => candidate.relation === "parent" && candidate.target.category === node.category && domainRefMatchesNode(candidate.source, node));
  return edge ? {
    id: edge.target.id,
    kind: edge.target.kind,
    path: edge.target.path
  } : void 0;
}
function domainRecordForNode(snapshot, node) {
  const entityKind = stringField23(node.metadata?.entityKind);
  const entities = entityKind === "production" ? snapshot.productions : entityKind === "segment" ? snapshot.segments : [];
  return entities.find(
    (entity) => node.path !== void 0 && entity.path === node.path || node.id !== void 0 && sameId(idField2(entity.id ?? entity.record.id ?? entity.record.ID ?? entity.path), node.id)
  )?.record;
}
function domainRefMatchesNode(ref, node) {
  return ref.category === node.category && ref.kind === node.kind && (sameId(ref.id, node.id) || ref.path !== void 0 && ref.path === node.path);
}
function domainRefMatchesEntity(ref, entity) {
  return sameId(ref.id, entityId(entity)) || ref.path !== void 0 && ref.path === entity.path;
}
function contentCandidateRecordsByContentUnitId(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!document.path.endsWith("/content_candidate.json") || !isRecord23(document.data)) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument2(document.path, stringField23(document.data.content_unit_ref));
    if (!contentUnitId) continue;
    output.set(contentUnitId, [...output.get(contentUnitId) ?? [], document.data]);
  }
  return output;
}
function selectionRecordsByContentUnitId(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!isRecord23(document.data)) continue;
    const selection = isRecord23(document.data.selection) ? document.data.selection : void 0;
    if (!selection) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument2(document.path, stringField23(document.data.target_ref));
    if (!contentUnitId) continue;
    output.set(contentUnitId, {
      ...idField2(selection.candidate_id) !== void 0 ? { candidate_id: idField2(selection.candidate_id) } : {},
      ...numberField22(selection.resource_id) !== void 0 ? { resource_id: numberField22(selection.resource_id) } : {},
      ...stringField23(selection.stale_policy) ? { stale_policy: stringField23(selection.stale_policy) } : {}
    });
  }
  return output;
}
function entityId(entity) {
  return idField2(entity.id ?? entity.record.id ?? entity.record.ID ?? pathSegmentAfter3(entity.path, "content_units") ?? entity.path) ?? entity.path;
}
function contentUnitIdForRuntimeDocument2(path, ref) {
  if (ref) return lastPathSegment(ref) ?? ref;
  return pathSegmentAfter3(path, "content_units");
}
function firstCandidateOutput2(candidate) {
  const outputs = Array.isArray(candidate?.outputs) ? candidate.outputs : [];
  return outputs.find(isRecord23);
}
function arrayValue3(value) {
  return Array.isArray(value) ? value : [];
}
function stringArrayField(value) {
  return Array.isArray(value) ? value.filter((item) => typeof item === "string" && Boolean(item.trim())).map((item) => item.trim()) : [];
}
function idField2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function stringField23(value) {
  const id = idField2(value);
  return id === void 0 ? void 0 : String(id);
}
function numberField22(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function pathSegmentAfter3(path, segment) {
  if (!path) return void 0;
  const parts = path.split("/").filter(Boolean);
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function lastPathSegment(path) {
  return path?.split("/").filter(Boolean).at(-1);
}
function sameId(left, right) {
  return left !== void 0 && right !== void 0 && String(left) === String(right);
}
function isRecord23(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
var PROJECT_CONTEXT_STYLE_REFERENCE_RULE_KEY = "style_reference_images";
var PROJECT_CONTEXT_CORE_STANDARDS = [
  { key: "aspect_ratio", label: "\u753B\u5E45\u6BD4\u4F8B", promptRole: "context" },
  { key: "visual_style", label: "\u89C6\u89C9\u98CE\u683C", promptRole: "style" },
  { key: "shot_size_system", label: "\u955C\u5934\u5927\u5C0F\u4F53\u7CFB", promptRole: "style" },
  { key: "camera_language", label: "\u955C\u5934\u8BED\u8A00", promptRole: "style" },
  { key: "lighting_style", label: "\u706F\u5149\u89C4\u5219", promptRole: "style" },
  { key: "color_palette", label: "\u8272\u5F69\u89C4\u5219", promptRole: "style" },
  { key: "pacing_rules", label: "\u8282\u594F\u89C4\u5219", promptRole: "constraint" },
  { key: "negative_rules", label: "\u8D1F\u9762\u89C4\u5219", promptRole: "negative" }
];
function buildProjectContextSnapshot(input) {
  const entity = input.standardsEntity;
  const standards = entity?.record ?? {};
  const namespaceVocabulary = input.namespaceVocabulary ?? {
    timelineTemplate: void 0,
    timelineNamespaces: [],
    settingNamespaces: [],
    diagnostics: []
  };
  const projectStyle = parseProjectStyle(standards.project_style);
  const core = PROJECT_CONTEXT_CORE_STANDARDS.map((item) => {
    const value = projectContextFieldText(item.key === "aspect_ratio" ? standards.aspect_ratio ?? projectStyle.aspect_ratio : item.key === "visual_style" ? standards.visual_style ?? projectStyle.visual_style : projectStyle[item.key] ?? standards[item.key]);
    return {
      key: item.key,
      label: item.label,
      prompt_role: item.promptRole,
      required: true,
      value,
      filled: Boolean(value)
    };
  });
  const customRules = normalizeProjectContextRules(projectStyle.custom_rules);
  const enabledRules = customRules.filter((rule) => rule.enabled && rule.value);
  const promptPreview = buildProjectContextPromptPreview(core, enabledRules);
  const styleReferenceResourceIds = Array.from(new Set(
    enabledRules.flatMap((rule) => rule.key === PROJECT_CONTEXT_STYLE_REFERENCE_RULE_KEY ? extractProjectContextResourceIds(rule.value) : [])
  ));
  return {
    schema: "movscript.project_context_snapshot.v1",
    kind: "project_context_snapshot",
    source: {
      entity_kind: "project_standards",
      entity_id: entity?.id ?? standards.id ?? "project_standards",
      path: entity?.path ?? "project_standards.json",
      updated_at: standards.updated_at
    },
    namespace_vocabulary: {
      timeline_template: namespaceVocabulary.timelineTemplate,
      timeline_namespaces: namespaceVocabulary.timelineNamespaces,
      setting_namespaces: namespaceVocabulary.settingNamespaces,
      diagnostics: namespaceVocabulary.diagnostics
    },
    standards_hash: stableProjectContextHash({
      aspect_ratio: standards.aspect_ratio,
      visual_style: standards.visual_style,
      project_style: projectStyle,
      namespace_vocabulary: namespaceVocabulary
    }),
    core_standards: core,
    missing_core_keys: core.filter((item) => !item.filled).map((item) => item.key),
    missing_core_labels: core.filter((item) => !item.filled).map((item) => item.label),
    custom_rules: customRules,
    enabled_rules: enabledRules,
    style_reference_resource_ids: styleReferenceResourceIds,
    prompt_preview: promptPreview,
    agent_guidance: [
      "Read this snapshot before planning, content-unit work, or generation that depends on project house style or constraints.",
      "Use namespace_vocabulary for project-specific timeline/setting terms; do not infer concrete parent relationships from vocabulary templates.",
      "Do not modify project standards just because fields are missing; only use domain_upsert_project_standards when the user asks to add, remove, or change standards.",
      "When visual generation supports reference images, pass style_reference_resource_ids as house-style reference_resource_ids."
    ]
  };
}
function parseProjectStyle(value) {
  if (isRecord32(value)) return value;
  if (typeof value !== "string" || !value.trim()) return {};
  try {
    const parsed = JSON.parse(value);
    return isRecord32(parsed) ? parsed : {};
  } catch {
    return {};
  }
}
function projectContextFieldText(value) {
  if (value === void 0 || value === null) return "";
  if (typeof value === "string") return value.trim();
  if (typeof value === "number" || typeof value === "boolean") return String(value);
  if (Array.isArray(value)) return value.map((item) => projectContextFieldText(item)).filter(Boolean).join("\uFF1B");
  if (isRecord32(value)) {
    try {
      return JSON.stringify(value);
    } catch {
      return String(value);
    }
  }
  return String(value);
}
function normalizeProjectContextRules(value) {
  const rules = Array.isArray(value) ? value.filter(isRecord32) : [];
  return rules.map((rule, index) => {
    const key = projectContextFieldText(rule.key).toLowerCase().replace(/\s+/g, "_");
    const label = projectContextFieldText(rule.label ?? rule.name ?? rule.key) || `\u6269\u5C55\u89C4\u8303 ${index + 1}`;
    const ruleValue = projectContextFieldText(rule.value ?? rule.content ?? rule.description);
    return {
      id: projectContextFieldText(rule.id) || `rule_${key || index}_${index}`,
      key: key || label.toLowerCase().replace(/\s+/g, "_"),
      label,
      category: projectContextFieldText(rule.category) || "\u901A\u7528",
      value: ruleValue,
      prompt_role: normalizeProjectContextPromptRole(rule.prompt_role ?? rule.promptRole ?? rule.role),
      enabled: typeof rule.enabled === "boolean" ? rule.enabled : true,
      required: typeof rule.required === "boolean" ? rule.required : false,
      order: numberValue2(rule.order) ?? (index + 1) * 10
    };
  }).filter((rule) => rule.key || rule.label || rule.value).sort((left, right) => left.order - right.order || left.label.localeCompare(right.label));
}
function normalizeProjectContextPromptRole(value) {
  if (value === "context" || value === "style" || value === "constraint" || value === "negative" || value === "quality_gate") return value;
  return "constraint";
}
function buildProjectContextPromptPreview(core, rules) {
  const items = [
    ...core.filter((item) => item.value),
    ...rules.filter((item) => item.value)
  ];
  const sections = [
    { role: "context", title: "\u9879\u76EE\u80CC\u666F\u89C4\u8303" },
    { role: "style", title: "\u89C6\u89C9\u4E0E\u8868\u8FBE\u89C4\u8303" },
    { role: "constraint", title: "\u5FC5\u987B\u9075\u5B88" },
    { role: "negative", title: "\u7981\u6B62\u51FA\u73B0" },
    { role: "quality_gate", title: "\u8D28\u68C0\u53E3\u5F84" }
  ].flatMap((section) => {
    const sectionItems = items.filter((item) => item.prompt_role === section.role);
    if (sectionItems.length === 0) return [];
    return [`${section.title}\uFF1A`, ...sectionItems.map((item) => `- ${item.label}\uFF1A${item.value}`)];
  });
  return sections.length > 0 ? `\u9879\u76EE\u89C4\u8303\uFF1A
${sections.join("\n")}` : "\u9879\u76EE\u89C4\u8303\uFF1A\n- \u6682\u65E0\u5DF2\u542F\u7528\u89C4\u8303\u3002";
}
function extractProjectContextResourceIds(value) {
  const ids = /* @__PURE__ */ new Set();
  for (const pattern of [/resource#(\d+)/gi, /reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi]) {
    for (const match of value.matchAll(pattern)) {
      const idText2 = match[1] ?? "";
      for (const part of idText2.split(",")) {
        const id = Number(part.trim());
        if (Number.isInteger(id) && id > 0) ids.add(id);
      }
    }
  }
  return Array.from(ids);
}
function stableProjectContextHash(value) {
  const text = stableStringify2(value);
  let hash = 2166136261;
  for (let index = 0; index < text.length; index += 1) {
    hash ^= text.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(16).padStart(8, "0");
}
function stableStringify2(value) {
  if (Array.isArray(value)) return `[${value.map((item) => stableStringify2(item)).join(",")}]`;
  if (isRecord32(value)) {
    return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${stableStringify2(value[key])}`).join(",")}}`;
  }
  return JSON.stringify(value);
}
function isRecord32(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function numberValue2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value !== "string" || !value.trim()) return void 0;
  const number = Number(value);
  return Number.isFinite(number) ? number : void 0;
}

// ../../packages/core/dist/chunk-G2ZRFCTC.js
import { readFile } from "fs/promises";
import { join as join6 } from "path";

// ../../packages/workspace/dist/chunk-LJ7J62DV.js
function deriveMovScriptWorkspaceDomainIndex(documents) {
  const entities = documents.flatMap((document) => indexedEntitiesFromDocument(document));
  const byKind = /* @__PURE__ */ new Map();
  for (const entity of entities) {
    byKind.set(entity.entityKind, [...byKind.get(entity.entityKind) ?? [], entity]);
  }
  const namespaceVocabulary = normalizeNamespaceVocabulary(byKind.get("project")?.[0]?.record);
  const domainNodes = deriveMovScriptDomainNodes(entities);
  const domainEdges = deriveMovScriptDomainEdges(entities, domainNodes);
  return { documents, entities, byKind, namespaceVocabulary, domainNodes, domainEdges };
}
function queryMovScriptWorkspaceEntities(index, query = {}) {
  const source = query.entityKind ? index.byKind.get(query.entityKind) ?? [] : index.entities;
  const queryContext = createEntityQueryContext(index);
  const out = source.filter((entity) => {
    const record = entity.record;
    if (query.kind && !entityKindMatches(entity, query.kind)) return false;
    if (query.productionId !== void 0 && !entityMatchesScope(entity, "production", query.productionId, queryContext)) return false;
    if (query.segmentId !== void 0 && !entityMatchesScope(entity, "segment", query.segmentId, queryContext)) return false;
    if (query.sceneMomentId !== void 0 && !entityMatchesScope(entity, "scene_moment", query.sceneMomentId, queryContext)) return false;
    if (query.storyboardId !== void 0 && !entityMatchesStoryboard(entity, query.storyboardId, queryContext)) return false;
    if (query.contentUnitId !== void 0 && !entityPathMatchesContentUnit(entity.path, query.contentUnitId)) return false;
    if (query.settingId !== void 0 && !entityMatchesScope(entity, "setting", query.settingId, queryContext)) return false;
    if (query.settingStateId !== void 0 && !entityMatchesScope(entity, "setting_state", query.settingStateId, queryContext)) return false;
    if (query.query && !recordMatchesQuery(record, query.query)) return false;
    return !isDeletedWorkspaceRecord(record);
  });
  return limitEntities(out, query.limit);
}
function queryMovScriptWorkspaceSettings(index, query = {}) {
  const references = queryMovScriptWorkspaceEntities(index, {
    entityKind: "setting",
    kind: query.kind,
    query: query.query,
    limit: query.limit
  });
  if (query.settingId === void 0) return references;
  return references.filter((entity) => sameId2(entity.id, query.settingId));
}
function queryMovScriptWorkspaceAssets(index, query = {}) {
  const assets = queryMovScriptWorkspaceEntities(index, {
    entityKind: "asset",
    query: query.query,
    settingId: query.settingId,
    settingStateId: query.settingStateId,
    limit: query.limit
  }).filter((entity) => query.assetId === void 0 || sameId2(entity.id, query.assetId));
  if (!query.includeCandidates) return { assets };
  const candidates = assets.flatMap((entity) => {
    const rows = entity.record.candidates;
    return Array.isArray(rows) ? rows.filter(isRecord7) : [];
  });
  return { assets, candidates };
}
function queryMovScriptWorkspaceProductionContext(index, query = {}) {
  const include = new Set(query.include ?? ["productions", "segments", "scene_moments", "storyboards", "audio_cues", "expression_units", "content_units", "keyframes"]);
  const result = {};
  if (include.has("productions")) {
    result.productions = queryMovScriptWorkspaceEntities(index, {
      entityKind: "production",
      query: query.query,
      limit: query.limit
    }).filter((entity) => query.productionId === void 0 || sameId2(entity.id, query.productionId));
  }
  if (include.has("segments")) {
    result.segments = queryMovScriptWorkspaceEntities(index, {
      entityKind: "segment",
      productionId: query.productionId,
      query: query.query,
      limit: query.limit
    }).filter((entity) => query.segmentId === void 0 || sameId2(entity.id, query.segmentId));
  }
  if (include.has("scene_moments")) {
    result.scene_moments = queryMovScriptWorkspaceEntities(index, {
      entityKind: "scene_moment",
      productionId: query.productionId,
      segmentId: query.segmentId,
      query: query.query,
      limit: query.limit
    }).filter((entity) => query.sceneMomentId === void 0 || sameId2(entity.id, query.sceneMomentId));
  }
  if (include.has("storyboards")) {
    result.storyboards = queryMovScriptWorkspaceEntities(index, {
      entityKind: "storyboard",
      productionId: query.productionId,
      segmentId: query.segmentId,
      sceneMomentId: query.sceneMomentId,
      query: query.query,
      limit: query.limit
    }).filter((entity) => query.storyboardId === void 0 || sameId2(entity.id, query.storyboardId));
  }
  if (include.has("audio_cues")) {
    result.audio_cues = queryMovScriptWorkspaceEntities(index, {
      entityKind: "audio_cue",
      productionId: query.productionId,
      segmentId: query.segmentId,
      sceneMomentId: query.sceneMomentId,
      storyboardId: query.storyboardId,
      query: query.query,
      limit: query.limit
    });
  }
  if (include.has("expression_units")) {
    result.expression_units = queryMovScriptWorkspaceEntities(index, {
      entityKind: "expression_unit",
      productionId: query.productionId,
      segmentId: query.segmentId,
      sceneMomentId: query.sceneMomentId,
      query: query.query,
      limit: query.limit
    });
  }
  if (include.has("content_units")) {
    result.content_units = queryMovScriptWorkspaceEntities(index, {
      entityKind: "content_unit",
      contentUnitId: query.contentUnitId,
      query: query.query,
      limit: query.limit
    });
  }
  if (include.has("keyframes")) {
    result.keyframes = queryMovScriptWorkspaceEntities(index, {
      entityKind: "keyframe",
      sceneMomentId: query.sceneMomentId,
      contentUnitId: query.contentUnitId,
      query: query.query,
      limit: query.limit
    });
  }
  return result;
}
function indexedEntitiesFromDocument(document) {
  const path = normalizeIndexedDocumentPath(document.path);
  if (!isRecord7(document.data)) return [];
  const entityKind = entityKindFromSchema(stringField8(document.data.schema)) ?? entityKindFromPath(path);
  return entityKind ? [indexedEntity(entityKind, workspaceRecordWithDocumentMetadata(document.data, document), path, 0)] : [];
}
function indexedEntity(entityKind, record, path, index) {
  const schema = stringField8(record.schema);
  const id = entityIdField(entityKind, record);
  const clientId = stringField8(record.client_id ?? record.clientId);
  return {
    entityKind,
    record,
    path,
    index,
    ...id !== void 0 ? { id } : {},
    ...clientId ? { clientId } : {},
    ...schema ? { schema } : {}
  };
}
function deriveMovScriptDomainNodes(entities) {
  return entities.flatMap((entity) => {
    const category = classifyMovScriptEntityKind(entity.entityKind);
    if (!category) return [];
    return [pruneUndefined({
      category,
      kind: projectMovScriptDomainNodeKind(entity.entityKind, entity.record),
      ...entity.id !== void 0 ? { id: entity.id } : {},
      path: entity.path,
      title: stringField8(entity.record.title) ?? stringField8(entity.record.label),
      order: finiteNumber(entity.record.order),
      metadata: {
        entityKind: entity.entityKind
      }
    })];
  });
}
function deriveMovScriptDomainPathParentEdges(nodes) {
  const nodeByDir = /* @__PURE__ */ new Map();
  for (const node of nodes) {
    if (!node.path) continue;
    nodeByDir.set(entityDir(node.path), node);
  }
  const edges = [];
  for (const node of nodes) {
    if (!node.path) continue;
    const parentPath = nearestParentPath(entityDir(node.path), nodeByDir.keys());
    if (!parentPath) continue;
    const parent = nodeByDir.get(parentPath);
    const normalized = normalizePathParentEdge(domainRefFromNode2(node), parent ? domainRefFromNode2(parent) : void 0);
    if (normalized.edge) edges.push(normalized.edge);
  }
  return dedupeDomainEdges2(edges);
}
function deriveMovScriptDomainEdges(entities, nodes) {
  return dedupeDomainEdges2([
    ...deriveMovScriptDomainPathParentEdges(nodes),
    ...deriveMovScriptContentUnitTargetEdges(entities, nodes)
  ]);
}
function deriveMovScriptContentUnitTargetEdges(entities, nodes) {
  const nodeByPath = new Map(nodes.flatMap((node) => node.path ? [[node.path, node]] : []));
  const timelineNamespaceNodes = nodes.filter((node) => node.category === "timeline_namespace");
  const edges = [];
  for (const entity of entities) {
    if (entity.entityKind !== "content_unit") continue;
    const sourceNode = nodeByPath.get(entity.path);
    if (!sourceNode) continue;
    edges.push(...normalizeContentUnitTargetEdges({
      source: domainRefFromNode2(sourceNode),
      record: entity.record,
      scopeTarget(scope) {
        return domainRefFromTimelineScope2(scope.kind, scope.ref, timelineNamespaceNodes);
      }
    }));
  }
  return edges;
}
function domainRefFromTimelineScope2(scopeKind, scopeRef, timelineNamespaceNodes) {
  const node = timelineNamespaceNodes.find(
    (candidate) => String(candidate.id ?? "") === scopeRef && (candidate.kind === scopeKind || candidate.metadata?.entityKind === scopeKind)
  );
  return node ? domainRefFromNode2(node) : { category: "timeline_namespace", kind: scopeKind, id: scopeRef };
}
function domainRefFromNode2(node) {
  return {
    category: node.category,
    kind: node.kind,
    ...node.id !== void 0 ? { id: node.id } : {},
    ...node.path ? { path: node.path } : {}
  };
}
function workspaceRecordWithDocumentMetadata(record, document) {
  return {
    ...record,
    __workspace_path: document.path,
    ...document.version !== void 0 ? { __workspace_version: document.version } : {},
    ...document.updatedAt !== void 0 ? { __workspace_updated_at: document.updatedAt } : {}
  };
}
function entityKindFromSchema(schema) {
  if (!schema) return void 0;
  const normalized = schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "");
  return schemaEntityKinds[normalized];
}
function entityKindFromPath(path) {
  const name = path.split("/").pop() ?? path;
  if (name === "project.json") return "project";
  if (name === "project_standards.json") return "project_standards";
  if (name === "script.meta.json" || name === "script.json") return "script";
  if (name === "setting.json") return "setting";
  if (name === "setting_state.json") return "setting_state";
  if (name === "asset.json") return "asset";
  if (name === "script_version.json") return "script_version";
  if (name === "script_block.json") return "script_block";
  if (name === "production.json") return "production";
  if (name === "segment.json") return "segment";
  if (name === "scene_moment.json") return "scene_moment";
  if (name === "storyboard.json") return "storyboard";
  if (name === "audio_cue.json") return "audio_cue";
  if (name === "expression_unit.json") return "expression_unit";
  if (name === "content_unit.json") return "content_unit";
  if (name === "keyframe.json") return "keyframe";
  return void 0;
}
var schemaEntityKinds = {
  project: "project",
  project_standards: "project_standards",
  script: "script",
  script_version: "script_version",
  script_block: "script_block",
  production: "production",
  segment: "segment",
  scene_moment: "scene_moment",
  storyboard: "storyboard",
  audio_cue: "audio_cue",
  expression_unit: "expression_unit",
  content_unit: "content_unit",
  keyframe: "keyframe",
  setting: "setting",
  setting_state: "setting_state",
  asset: "asset"
};
function entityPathMatchesProduction(path, productionId) {
  return pathSegmentAfter2(path, "productions") !== void 0 && sameEntityRef(pathSegmentAfter2(path, "productions"), productionId, "production");
}
function entityPathMatchesSegment(path, segmentId) {
  return pathSegmentAfter2(path, "segments") !== void 0 && sameEntityRef(pathSegmentAfter2(path, "segments"), segmentId, "segment");
}
function entityPathMatchesSceneMoment(path, sceneMomentId) {
  return pathSegmentAfter2(path, "scene_moments") !== void 0 && sameEntityRef(pathSegmentAfter2(path, "scene_moments"), sceneMomentId, "scene_moment");
}
function entityPathMatchesStoryboard(path, storyboardId) {
  return pathSegmentAfter2(path, "storyboards") !== void 0 && sameEntityRef(pathSegmentAfter2(path, "storyboards"), storyboardId, "storyboard");
}
function entityMatchesStoryboard(entity, storyboardId, context) {
  if (entityMatchesScope(entity, "storyboard", storyboardId, context)) return true;
  if (entityPathMatchesStoryboard(entity.path, storyboardId)) return true;
  const storyboardRef = stringField8(entity.record.storyboard_ref);
  return storyboardRef !== void 0 && (entityPathMatchesStoryboard(storyboardRef, storyboardId) || sameNormalizedEntityPath(storyboardRef, storyboardId, "storyboard"));
}
function entityPathMatchesContentUnit(path, contentUnitId) {
  return pathSegmentAfter2(path, "content_units") !== void 0 && sameEntityRef(pathSegmentAfter2(path, "content_units"), contentUnitId, "content_unit");
}
function entityPathMatchesSetting(path, settingId) {
  return pathSegmentAfter2(path, "settings") !== void 0 && sameEntityRef(pathSegmentAfter2(path, "settings"), settingId, "setting");
}
function entityPathMatchesSettingState(path, stateId) {
  return pathSegmentAfter2(path, "states") !== void 0 && sameEntityRef(pathSegmentAfter2(path, "states"), stateId, "setting_state");
}
function createEntityQueryContext(index) {
  const entityByPath = new Map(index.entities.map((entity) => [entity.path, entity]));
  const entityByDir = new Map(index.entities.map((entity) => [entityDir(entity.path), entity]));
  const parentPathByPath = /* @__PURE__ */ new Map();
  for (const edge of index.domainEdges) {
    if (edge.relation !== "parent" || edge.origin !== "path") continue;
    if (!edge.source.path || !edge.target.path) continue;
    parentPathByPath.set(edge.source.path, edge.target.path);
  }
  for (const entity of index.entities) {
    if (parentPathByPath.has(entity.path)) continue;
    const parentDir = nearestParentPath(entityDir(entity.path), entityByDir.keys());
    const parent = parentDir ? entityByDir.get(parentDir) : void 0;
    if (parent) parentPathByPath.set(entity.path, parent.path);
  }
  return { entityByPath, parentPathByPath };
}
function entityMatchesScope(entity, scopeKind, scopeRef, context) {
  if (entityRefMatchesScope(entity, scopeKind, scopeRef)) return true;
  for (const ancestor of ancestorEntities(entity, context)) {
    if (entityRefMatchesScope(ancestor, scopeKind, scopeRef)) return true;
  }
  return legacyEntityPathMatchesScope(entity.path, scopeKind, scopeRef);
}
function ancestorEntities(entity, context) {
  const ancestors = [];
  const seen = /* @__PURE__ */ new Set([entity.path]);
  let currentPath = entity.path;
  while (true) {
    const parentPath = context.parentPathByPath.get(currentPath);
    if (!parentPath || seen.has(parentPath)) break;
    seen.add(parentPath);
    const parent = context.entityByPath.get(parentPath);
    if (!parent) break;
    ancestors.push(parent);
    currentPath = parent.path;
  }
  return ancestors;
}
function entityRefMatchesScope(entity, scopeKind, scopeRef) {
  if (entity.entityKind !== scopeKind) return false;
  if (entity.id !== void 0 && sameEntityRef(entity.id, scopeRef, scopeKind)) return true;
  return sameNormalizedEntityPath(entity.path, scopeRef, scopeKind) || sameNormalizedEntityPath(entityDir(entity.path), scopeRef, scopeKind);
}
function legacyEntityPathMatchesScope(path, scopeKind, scopeRef) {
  if (scopeKind === "production") return entityPathMatchesProduction(path, scopeRef);
  if (scopeKind === "segment") return entityPathMatchesSegment(path, scopeRef);
  if (scopeKind === "scene_moment") return entityPathMatchesSceneMoment(path, scopeRef);
  if (scopeKind === "storyboard") return entityPathMatchesStoryboard(path, scopeRef);
  if (scopeKind === "setting") return entityPathMatchesSetting(path, scopeRef);
  if (scopeKind === "setting_state") return entityPathMatchesSettingState(path, scopeRef);
  return false;
}
function recordMatchesQuery(record, query) {
  const needle = query.trim().toLowerCase();
  if (!needle) return true;
  return ["id", "title", "label", "alias", "description", "content", "summary", "prompt", "notes"].some((key) => String(record[key] ?? "").toLowerCase().includes(needle));
}
function entityKindMatches(entity, kind) {
  return projectMovScriptDomainNodeKind(entity.entityKind, entity.record) === kind || recordKindMatches(entity.record, kind);
}
function recordKindMatches(record, kind) {
  return [
    record.kind,
    record.namespace_kind,
    record.namespaceKind,
    record.timeline_namespace_kind,
    record.timelineNamespaceKind,
    record.setting_namespace_kind,
    record.settingNamespaceKind,
    record.setting_kind,
    record.settingKind,
    record.content_unit_type,
    record.contentUnitType,
    record.asset_kind,
    record.assetKind,
    record.shot_kind,
    record.shotKind,
    record.segment_kind,
    record.segmentKind,
    record.slot_kind,
    record.slotKind,
    record.expression_kind,
    record.expressionKind,
    record.cue_kind,
    record.cueKind
  ].some((value) => stringField8(value) === kind);
}
function isDeletedWorkspaceRecord(record) {
  return record.__delete === true || record.deleted === true;
}
function limitEntities(items, limit) {
  if (limit === void 0) return items;
  if (!Number.isFinite(limit) || limit <= 0) return [];
  return items.slice(0, Math.floor(limit));
}
function sameId2(left, right) {
  const leftId = idField3(left);
  const rightId = idField3(right);
  return leftId !== void 0 && rightId !== void 0 && String(leftId) === String(rightId);
}
function entityIdField(entityKind, record) {
  if (entityKind === "project") return idField3(record.project_id ?? record.ID ?? record.id);
  return idField3(record.ID ?? record.id);
}
function idField3(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function stringField8(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function finiteNumber(value) {
  const next = Number(value);
  return Number.isFinite(next) ? next : void 0;
}
function pathSegmentAfter2(path, segment) {
  const parts = path.split("/");
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function sameNormalizedEntityPath(pathOrDir, ref, entityKind) {
  const left = normalizeWorkspacePath(pathOrDir);
  const right = normalizeWorkspacePath(String(ref));
  if (!left || !right) return false;
  if (left === right) return true;
  if (left.endsWith(".json") && entityDir(left) === right) return true;
  if (right.endsWith(".json") && left === entityDir(right)) return true;
  return sameEntityRef(left, right, entityKind);
}
function normalizeIndexedDocumentPath(path) {
  return normalizeWorkspacePath(path).replace(/^\.movscript\//, "");
}
function isRecord7(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function pruneUndefined(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
function dedupeDomainEdges2(edges) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const edge of edges) {
    const key = JSON.stringify(edge);
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(edge);
  }
  return out;
}

// ../../packages/decision/dist/index.js
var DEFAULT_SELECTION_FIELD = "lock";
function buildMovScriptCandidateSelectionRecord(candidate, input = {}) {
  const candidateId = idField4(candidate.id);
  const resourceId = resourceIdField(candidate.resource_id ?? candidate.resourceId);
  const artifactRef = stringField9(candidate.artifact_ref ?? candidate.artifactRef);
  if (candidateId === void 0 && resourceId === void 0) {
    throw new Error("candidate selection requires candidate id or resource_id");
  }
  return pruneUndefined2({
    candidate_id: candidateId,
    resource_id: resourceId,
    artifact_ref: artifactRef,
    reason: input.reason ?? "selected",
    selected_at: input.selectedAt,
    selected_by: input.selectedBy,
    metadata: input.metadata
  });
}
function selectMovScriptCandidateRecord(record, input) {
  const candidate = findCandidate(record, input.candidateId);
  if (!candidate) throw new Error(`candidate not found: ${String(input.candidateId)}`);
  const selection = buildMovScriptCandidateSelectionRecord(candidate, input);
  return {
    candidate,
    selection,
    record: {
      ...record,
      [selectionField(input)]: selection
    }
  };
}
function clearMovScriptCandidateSelectionRecord(record, options = {}) {
  const field = selectionField(options);
  const { [field]: _selection, ...nextRecord } = record;
  return nextRecord;
}
function findCandidate(record, candidateId) {
  return arrayField2(record.candidates).filter(isRecord8).find((item) => sameId3(item.id, candidateId));
}
function selectionField(options) {
  return options.selectionField?.trim() || DEFAULT_SELECTION_FIELD;
}
function sameId3(left, right) {
  if (left === void 0 || right === void 0) return false;
  return String(left) === String(right);
}
function idField4(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function resourceIdField(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function stringField9(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function arrayField2(value) {
  return Array.isArray(value) ? value : [];
}
function isRecord8(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined2(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}

// ../../packages/workspace/dist/chunk-OLW7PAL3.js
function createMovScriptWorkspaceDomainRepository(options) {
  const includeFile = options.includeFile ?? defaultWorkspaceDomainFileFilter;
  return {
    async loadDocuments(input = {}) {
      return loadWorkspaceDocuments(options.fileRepository, input.path ?? "", includeFile);
    },
    async loadIndex(input = {}) {
      const documents = await loadWorkspaceDocuments(options.fileRepository, input.path ?? "", includeFile);
      return deriveMovScriptWorkspaceDomainIndex(documents);
    }
  };
}
async function loadWorkspaceDocuments(fileRepository, rootPath, includeFile) {
  const out = [];
  await collectWorkspaceDocuments(fileRepository, normalizeWorkspacePath(rootPath), includeFile, out);
  return out.sort((left, right) => left.path.localeCompare(right.path));
}
async function collectWorkspaceDocuments(fileRepository, path, includeFile, out) {
  const listed = await fileRepository.list({ path });
  for (const entry of listed.entries) {
    if (entry.kind === "directory") {
      if (!path && isMovScriptNonSourceRootDirectory(entry.path)) continue;
      await collectWorkspaceDocuments(fileRepository, entry.path, includeFile, out);
      continue;
    }
    if (!includeFile(entry.path)) continue;
    const file = await fileRepository.read({ path: entry.path });
    out.push({
      path: file.path,
      data: parseWorkspaceDocument(file.path, file.content),
      ...file.version !== void 0 ? { version: file.version } : {},
      ...file.updatedAt !== void 0 ? { updatedAt: file.updatedAt } : {}
    });
  }
}
function parseWorkspaceDocument(path, content) {
  if (path.endsWith(".json")) {
    try {
      return JSON.parse(content);
    } catch {
      return void 0;
    }
  }
  return content;
}
function defaultWorkspaceDomainFileFilter(path) {
  return isMovScriptSourcePath(path);
}
async function appendMovScriptInlineCandidate(input) {
  const targetPath = normalizeWorkspacePath2(input.targetPath);
  const current = await readTargetRecord(input.fileRepository, targetPath);
  validateTargetKind(input.targetKind, current);
  const candidate = buildInlineCandidate(input.payload, input.nonce);
  const candidates = arrayField3(current.candidates).filter(isRecord9);
  if (candidates.some((item) => String(item.id) === String(candidate.id))) {
    throw new Error(`candidate already exists: ${String(candidate.id)}`);
  }
  const record = {
    ...current,
    candidates: [...candidates, candidate]
  };
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord(record) });
  return { path: targetPath, targetKind: input.targetKind, candidate, record };
}
async function selectMovScriptInlineCandidate(input) {
  const targetPath = normalizeWorkspacePath2(input.targetPath);
  const current = await readTargetRecord(input.fileRepository, targetPath);
  validateTargetKind(input.targetKind, current);
  const { candidate, record } = selectMovScriptCandidateRecord(current, {
    candidateId: input.candidateId,
    reason: input.reason
  });
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord(record) });
  return { path: targetPath, targetKind: input.targetKind, candidate, record };
}
async function updateMovScriptInlineCandidate(input) {
  const targetPath = normalizeWorkspacePath2(input.targetPath ?? input.targetEntity?.path ?? workspacePath(input.targetRecord));
  const current = await readTargetRecord(input.fileRepository, targetPath);
  validateTargetKind(input.targetKind, current);
  const candidates = arrayField3(current.candidates).filter(isRecord9);
  const candidateIndex = candidates.findIndex((item) => String(item.id) === input.candidateId);
  if (candidateIndex < 0) throw new Error(`candidate not found: ${input.candidateId}`);
  const currentCandidate = candidates[candidateIndex];
  const candidate = pruneUndefined3({
    ...currentCandidate,
    ...input.payload.resource_id !== void 0 || input.payload.resourceId !== void 0 ? { resource_id: requiredResourceId(input.payload.resource_id ?? input.payload.resourceId) } : {},
    ...input.payload.artifact_ref !== void 0 || input.payload.artifactRef !== void 0 ? { artifact_ref: stringField10(input.payload.artifact_ref ?? input.payload.artifactRef) } : {},
    ...input.payload.source !== void 0 ? { source: input.payload.source } : {},
    ...input.payload.notes !== void 0 ? { notes: input.payload.notes } : {},
    ...input.payload.metadata !== void 0 ? { metadata: input.payload.metadata } : {}
  });
  const nextCandidates = [...candidates];
  nextCandidates[candidateIndex] = candidate;
  const record = {
    ...current,
    candidates: nextCandidates
  };
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord(record) });
  return { path: targetPath, targetKind: input.targetKind, candidate, record };
}
function workspacePath(record) {
  const value = record?.__workspace_path ?? record?.workspace_path ?? record?.path;
  if (typeof value === "string" && value.trim()) return value;
  throw new Error("candidate target path is required");
}
async function unlockMovScriptInlineCandidate(input) {
  const targetPath = normalizeWorkspacePath2(input.targetPath);
  const current = await readTargetRecord(input.fileRepository, targetPath);
  validateTargetKind(input.targetKind, current);
  const record = clearMovScriptCandidateSelectionRecord(current);
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord(record) });
  return { path: targetPath, targetKind: input.targetKind, record };
}
function readTargetRecord(fileRepository, targetPath) {
  return fileRepository.read({ path: targetPath }).then((file) => {
    const parsed = JSON.parse(file.content);
    if (!isRecord9(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
    return parsed;
  });
}
function validateTargetKind(targetKind, record) {
  if (targetKind === "content_unit") {
    throw new Error("content_unit candidates are backend decision records; use createContentCandidate/selectContentUnitCandidate");
  }
  if (record.kind === targetKind) return;
  const schemaKind = typeof record.schema === "string" ? record.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
  if (schemaKind === targetKind) return;
  throw new Error(`target kind mismatch: expected ${targetKind}`);
}
function buildInlineCandidate(payload, nonce) {
  const resourceId = requiredResourceId(payload.resource_id ?? payload.resourceId);
  if (resourceId === void 0) {
    throw new Error("resource_id required");
  }
  const id = payload.id ?? `candidate_${safeFileToken(String(resourceId))}_${safeFileToken(nonce ?? randomNonce())}`;
  return pruneUndefined3({
    id,
    resource_id: resourceId,
    artifact_ref: stringField10(payload.artifact_ref ?? payload.artifactRef),
    source: payload.source ?? "manual",
    notes: payload.notes,
    metadata: payload.metadata
  });
}
function serializeWorkspaceRecord(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function normalizeWorkspacePath2(value) {
  return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
}
function safeFileToken(value) {
  return value.trim().replace(/[^a-zA-Z0-9_-]+/g, "_");
}
function requiredResourceId(value) {
  if (value === void 0 || value === null || value === "") return void 0;
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  throw new Error("resource_id must be a positive integer RawResource ID");
}
function stringField10(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function randomNonce() {
  const random = globalThis.crypto;
  if (random?.randomUUID) return random.randomUUID();
  return `${Date.now()}_${Math.random().toString(36).slice(2)}`;
}
function arrayField3(value) {
  return Array.isArray(value) ? value : [];
}
function isRecord9(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined3(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
async function updateMovScriptContentUnitEditPrompt(input) {
  const targetPath = normalizeWorkspacePath3(input.targetPath);
  const current = await readContentUnitRecord(input.fileRepository, targetPath);
  const editPrompt = pruneUndefined22({
    text: stringValue6(input.editPrompt.text),
    negative_text: stringValue6(input.editPrompt.negative_text),
    notes: stringValue6(input.editPrompt.notes),
    structured: isRecord24(input.editPrompt.structured) ? input.editPrompt.structured : void 0
  });
  const record = {
    ...current,
    edit_prompt: editPrompt
  };
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord2(record) });
  return { path: targetPath, record };
}
function readContentUnitRecord(fileRepository, targetPath) {
  return fileRepository.read({ path: targetPath }).then((file) => {
    const parsed = JSON.parse(file.content);
    if (!isRecord24(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
    const schemaKind = typeof parsed.schema === "string" ? parsed.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
    if (parsed.kind !== "content_unit" && schemaKind !== "content_unit") {
      throw new Error("target kind mismatch: expected content_unit");
    }
    return parsed;
  });
}
function normalizeWorkspacePath3(value) {
  return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
}
function serializeWorkspaceRecord2(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function stringValue6(value) {
  return typeof value === "string" ? value : void 0;
}
function isRecord24(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined22(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
async function snapshotMovScriptVersionFromMarkdown(input) {
  const scriptId = semanticEntityId(input.scriptId, "script");
  const versionId = semanticEntityId(input.versionId, "script_version");
  const scriptDir = `scripts/${entityPathSlug(scriptId, "script")}`;
  const scriptPath = normalizeWorkspacePath4(input.sourcePath ?? `${scriptDir}/script.md`);
  const markdown = await input.fileRepository.read({ path: scriptPath });
  const blocks = splitMarkdownBlocks(markdown.content);
  const versionDir = `${scriptDir}/versions/${entityPathSlug(versionId, "script_version")}`;
  const versionPath = `${versionDir}/script_version.json`;
  const createdAt = (input.now ?? /* @__PURE__ */ new Date()).toISOString();
  const versionRecord = {
    schema: "movscript.script_version.v1",
    kind: "script_version",
    id: versionId,
    title: input.versionLabel ?? String(versionId),
    version_label: input.versionLabel ?? String(versionId),
    source_ref: scriptPath.replace(`${scriptDir}/`, ""),
    source_text_hash: hashText(markdown.content),
    block_count: blocks.length,
    created_at: createdAt
  };
  await input.fileRepository.write({
    path: versionPath,
    content: serializeWorkspaceRecord3(versionRecord)
  });
  const blockPaths = [];
  for (const [index, block] of blocks.entries()) {
    const blockId = String(index + 1).padStart(3, "0");
    const blockPath = `${versionDir}/blocks/${entityPathSlug(blockId, "script_block")}/script_block.json`;
    blockPaths.push(blockPath);
    await input.fileRepository.write({
      path: blockPath,
      content: serializeWorkspaceRecord3({
        schema: "movscript.script_block.v1",
        kind: "script_block",
        id: blockId,
        order: index + 1,
        text: block.text,
        block_kind: block.kind,
        source_range: block.sourceRange
      })
    });
  }
  return {
    scriptId,
    versionId,
    scriptPath,
    versionPath,
    blockPaths,
    blockCount: blockPaths.length
  };
}
function splitMarkdownBlocks(markdown) {
  const lines = markdown.replace(/\r\n/g, "\n").split("\n");
  const blocks = [];
  let startLine = 1;
  let current = [];
  function flush(endLine) {
    const text = current.join("\n").trim();
    if (!text) return;
    blocks.push({
      text,
      kind: inferScriptBlockKind(text),
      sourceRange: { start_line: startLine, end_line: endLine }
    });
    current = [];
  }
  for (const [index, line] of lines.entries()) {
    const lineNumber = index + 1;
    if (line.trim() === "") {
      flush(lineNumber - 1);
      startLine = lineNumber + 1;
      continue;
    }
    if (current.length === 0) startLine = lineNumber;
    current.push(line);
  }
  flush(lines.length);
  return blocks;
}
function inferScriptBlockKind(text) {
  const firstLine2 = text.split("\n")[0]?.trim() ?? "";
  if (/^(INT\.|EXT\.|内景|外景)/i.test(firstLine2)) return "scene_heading";
  if (/^[A-Z][A-Z0-9 _-]{1,32}$/.test(firstLine2)) return "character";
  if (/^（.*）$|^\(.*\)$/.test(firstLine2)) return "parenthetical";
  return void 0;
}
function normalizeWorkspacePath4(value) {
  return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
}
function serializeWorkspaceRecord3(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function hashText(value) {
  let hash = 5381;
  for (let index = 0; index < value.length; index += 1) {
    hash = (hash << 5) + hash ^ value.charCodeAt(index);
  }
  return `djb2_${(hash >>> 0).toString(16)}`;
}
async function upsertMovScriptSourceRecord(input) {
  const targetPath = normalizeWorkspacePath5(input.targetPath);
  const current = await readOptionalWorkspaceRecord(input.fileRepository, targetPath);
  const incoming = stripWorkspacePrivateFields({
    ...input.record,
    ...input.payload ?? {}
  });
  const expectedKind = stringValue22(incoming.kind);
  if (expectedKind !== void 0) assertRecordKind(current, expectedKind, targetPath);
  const record = pruneUndefined32({
    ...current,
    ...incoming
  });
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord4(record) });
  return { path: targetPath, record };
}
async function updateMovScriptEntityTransition(input) {
  const targetPath = normalizeWorkspacePath5(input.targetPath);
  const current = await readWorkspaceRecord(input.fileRepository, targetPath);
  const record = pruneUndefined32({
    ...current,
    transition: normalizeTransition(input.transition)
  });
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord4(record) });
  return { path: targetPath, record };
}
async function updateMovScriptStoryboardTimeline(input) {
  const targetPath = normalizeWorkspacePath5(input.targetPath);
  const current = await readWorkspaceRecord(input.fileRepository, targetPath, "storyboard");
  const record = pruneUndefined32({
    ...current,
    timeline: normalizeTimeline(input.timeline)
  });
  await input.fileRepository.write({ path: targetPath, content: serializeWorkspaceRecord4(record) });
  return { path: targetPath, record };
}
async function readWorkspaceRecord(fileRepository, targetPath, expectedKind) {
  const file = await fileRepository.read({ path: targetPath });
  const parsed = JSON.parse(file.content);
  if (!isRecord33(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
  const schemaKind = typeof parsed.schema === "string" ? parsed.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
  if (expectedKind !== void 0 && parsed.kind !== expectedKind && schemaKind !== expectedKind) {
    throw new Error(`target kind mismatch: expected ${expectedKind}`);
  }
  return parsed;
}
async function readOptionalWorkspaceRecord(fileRepository, targetPath) {
  const file = await fileRepository.read({ path: targetPath }).catch(() => void 0);
  if (!file) return {};
  const parsed = JSON.parse(file.content);
  if (!isRecord33(parsed)) throw new Error(`target JSON must be an object: ${targetPath}`);
  return parsed;
}
function assertRecordKind(current, expectedKind, targetPath) {
  const currentKind = typeof current.kind === "string" ? current.kind : typeof current.schema === "string" ? current.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
  if (currentKind !== void 0 && currentKind !== expectedKind) {
    throw new Error(`target kind mismatch: expected ${expectedKind} at ${targetPath}`);
  }
}
function normalizeTransition(transition) {
  if (!transition) return void 0;
  return pruneUndefined32({
    in: stringValue22(transition.in),
    out: stringValue22(transition.out),
    notes: stringValue22(transition.notes)
  });
}
function normalizeTimeline(timeline) {
  if (!timeline) return void 0;
  return pruneUndefined32({
    gap_after_sec: finiteNumber2(timeline.gap_after_sec),
    caption: stringValue22(timeline.caption),
    duration_sec: finiteNumber2(timeline.duration_sec)
  });
}
function serializeWorkspaceRecord4(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function normalizeWorkspacePath5(value) {
  return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
}
function stringValue22(value) {
  return typeof value === "string" && value.trim() ? value : void 0;
}
function finiteNumber2(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function isRecord33(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function stripWorkspacePrivateFields(record) {
  return Object.fromEntries(
    Object.entries(record).filter(([key]) => !key.startsWith("__workspace_"))
  );
}
function pruneUndefined32(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
var CANDIDATE_SCHEMA = "movscript.candidate.v1";
async function createMovScriptWorkspaceAssetSlotCandidate(input) {
  const targetPath = workspacePath2(input.targetRecord);
  if (targetPath) {
    const result = await appendMovScriptInlineCandidate({
      fileRepository: input.fileRepository,
      targetPath,
      targetKind: "asset",
      nonce: input.nonce,
      payload: inlineCandidatePayload(input.payload)
    });
    return {
      path: result.path,
      entityType: "candidate",
      record: workspaceCandidateRecordFromInline({
        payload: input.payload,
        targetKind: "asset",
        targetRecord: result.record,
        candidate: result.candidate,
        path: result.path
      }),
      candidate: result.candidate,
      targetRecord: result.record
    };
  }
  const record = createMovScriptWorkspaceAssetSlotCandidateRecord(input);
  const parentSlotId = positiveNumber2(record.asset_slot_id);
  if (!parentSlotId) throw new Error("\u5019\u9009\u7F3A\u5C11\u7236\u7D20\u6750\u9700\u6C42");
  const path = `${normalizeProjectPath(input.projectPath)}/assets/asset_slot_${parentSlotId}.candidates/candidate_${workspaceCandidateFileKey(record)}.json`;
  await input.fileRepository.write({ path, content: serializeWorkspaceRecord5(record) });
  return { path, entityType: "candidate", record };
}
async function createMovScriptWorkspaceKeyframeCandidate(input) {
  const targetPath = workspacePath2(input.targetRecord) ?? stringValue32(input.payload.target_path ?? input.payload.targetPath);
  if (targetPath) {
    const result = await appendMovScriptInlineCandidate({
      fileRepository: input.fileRepository,
      targetPath,
      targetKind: "keyframe",
      nonce: input.nonce,
      payload: inlineCandidatePayload(input.payload)
    });
    return {
      path: result.path,
      entityType: "candidate",
      record: workspaceCandidateRecordFromInline({
        payload: input.payload,
        targetKind: "keyframe",
        targetRecord: result.record,
        candidate: result.candidate,
        path: result.path
      }),
      candidate: result.candidate,
      targetRecord: result.record
    };
  }
  const record = createMovScriptWorkspaceKeyframeCandidateRecord(input);
  const productionId = positiveNumber2(record.production_id);
  if (!productionId) throw new Error("\u5019\u9009\u7F3A\u5C11\u5236\u4F5C ID");
  const targetKeyframeId = positiveNumber2(record.keyframe_id);
  if (!targetKeyframeId) throw new Error("\u5019\u9009\u7F3A\u5C11\u76EE\u6807\u5173\u952E\u5E27");
  const directory = `${normalizeProjectPath(input.projectPath)}/productions/${entityPathSlug(productionId, "production")}/keyframes/${entityPathSlug(targetKeyframeId, "keyframe")}.candidates`;
  const path = `${directory}/candidate_${workspaceCandidateFileKey(record)}.json`;
  await input.fileRepository.write({ path, content: serializeWorkspaceRecord5(record) });
  return { path, entityType: "candidate", record };
}
function createMovScriptWorkspaceAssetSlotCandidateRecord(input) {
  const parentSlotId = positiveNumber2(input.payload.asset_slot_id ?? input.payload.assetSlotId ?? input.targetRecord?.ID ?? input.targetRecord?.id);
  if (!parentSlotId) throw new Error("\u5019\u9009\u7F3A\u5C11\u7236\u7D20\u6750\u9700\u6C42");
  const resourceId = positiveNumber2(input.payload.resource_id ?? input.payload.resourceId);
  if (!resourceId) throw new Error("resource_id required");
  const clientId = localClientId("asset_slot_candidate", parentSlotId, resourceId, input.nonce);
  const note = stringValue32(input.payload.note ?? input.payload.notes);
  const sourceType = stringValue32(input.payload.source_type ?? input.payload.sourceType ?? input.payload.source) ?? "manual";
  const sourceId = positiveNumber2(input.payload.source_id ?? input.payload.sourceId);
  return pruneUndefined4({
    schema: CANDIDATE_SCHEMA,
    ID: -stablePositiveHash(clientId),
    id: -stablePositiveHash(clientId),
    client_id: clientId,
    target: { type: "asset_slot", id: parentSlotId },
    asset_slot_id: parentSlotId,
    kind: stringValue32(input.payload.kind) ?? stringValue32(input.targetRecord?.kind ?? input.targetRecord?.asset_kind) ?? "image",
    title: note ?? `\u5019\u9009\u7D20\u6750 #${resourceId}`,
    description: note,
    priority: "normal",
    resource_id: resourceId,
    source_type: sourceType,
    ...sourceId ? { source_id: sourceId } : {},
    metadata_json: JSON.stringify(pruneUndefined4({
      source: "workspace_asset_slot_candidate",
      asset_slot_id: parentSlotId,
      resource_id: resourceId,
      source_type: sourceType,
      source_id: sourceId,
      score: numberValue3(input.payload.score),
      note
    }))
  });
}
function createMovScriptWorkspaceKeyframeCandidateRecord(input) {
  const resourceId = positiveNumber2(input.payload.resource_id ?? input.payload.resourceId);
  if (!resourceId) throw new Error("resource_id required");
  const targetKeyframeId = keyframeCandidateTargetId(input.payload);
  if (!targetKeyframeId) throw new Error("\u5019\u9009\u7F3A\u5C11\u76EE\u6807\u5173\u952E\u5E27");
  const clientId = localClientId("keyframe_candidate", targetKeyframeId, resourceId, input.nonce);
  return pruneUndefined4({
    ...input.payload,
    schema: CANDIDATE_SCHEMA,
    ID: -stablePositiveHash(clientId),
    id: -stablePositiveHash(clientId),
    client_id: clientId,
    target: { type: "keyframe", id: targetKeyframeId },
    keyframe_id: targetKeyframeId,
    resource_id: resourceId,
    production_id: numberValue3(input.payload.production_id ?? input.payload.productionId),
    scene_moment_id: numberValue3(input.payload.scene_moment_id ?? input.payload.sceneMomentId),
    content_unit_id: numberValue3(input.payload.content_unit_id ?? input.payload.contentUnitId),
    canvas_id: numberValue3(input.payload.canvas_id ?? input.payload.canvasId),
    title: stringValue32(input.payload.title) ?? `\u5019\u9009\u753B\u9762 #${resourceId}`,
    description: stringValue32(input.payload.description) ?? "",
    prompt: stringValue32(input.payload.prompt) ?? "",
    order: numberValue3(input.payload.order ?? input.payload.sort_order ?? input.payload.sortOrder) ?? 0
  });
}
function inlineCandidatePayload(payload) {
  const resourceId = payload.resource_id ?? payload.resourceId;
  return pruneUndefined4({
    id: stringValue32(payload.id ?? payload.client_id ?? payload.clientId),
    resource_id: resourceId === void 0 ? void 0 : requiredResourceId2(resourceId),
    artifact_ref: stringValue32(payload.artifact_ref ?? payload.artifactRef),
    source: stringValue32(payload.source ?? payload.source_type ?? payload.sourceType),
    notes: stringValue32(payload.notes ?? payload.note ?? payload.description),
    metadata: pruneUndefined4({
      source_id: payload.source_id ?? payload.sourceId,
      score: payload.score,
      raw: payload
    })
  });
}
function workspaceCandidateRecordFromInline(input) {
  const targetId = input.targetRecord.ID ?? input.targetRecord.id;
  const candidateId = stringValue32(input.candidate.id) ?? localClientId(`${input.targetKind}_candidate`, String(targetId ?? "target"), Number(input.candidate.resource_id) || 0);
  const numericCandidateId = -stablePositiveHash(candidateId);
  return pruneUndefined4({
    schema: CANDIDATE_SCHEMA,
    ID: numericCandidateId,
    id: numericCandidateId,
    client_id: candidateId,
    target: { type: input.targetKind, id: targetId },
    asset_slot_id: input.targetKind === "asset" ? numericId(targetId) : void 0,
    keyframe_id: input.targetKind === "keyframe" ? numericId(targetId) : void 0,
    resource_id: resourceIdValue2(input.candidate.resource_id),
    artifact_ref: stringValue32(input.candidate.artifact_ref),
    source_type: input.candidate.source,
    note: input.candidate.notes,
    metadata_json: JSON.stringify(pruneUndefined4({
      source: "workspace_inline_candidate",
      target_path: input.path,
      target_kind: input.targetKind,
      candidate_id: input.candidate.id,
      payload: input.payload
    }))
  });
}
function keyframeCandidateTargetId(payload) {
  const metadata = parseMetadata(payload.metadata_json);
  return positiveNumber2(payload.keyframe_id ?? payload.keyframeId ?? metadata?.target_keyframe_id);
}
function workspacePath2(record) {
  return stringValue32(record?.__workspace_path ?? record?.workspace_path ?? record?.path);
}
function parseMetadata(value) {
  if (isRecord42(value)) return value;
  if (typeof value !== "string" || !value.trim()) return void 0;
  try {
    const parsed = JSON.parse(value);
    return isRecord42(parsed) ? parsed : void 0;
  } catch {
    return void 0;
  }
}
function serializeWorkspaceRecord5(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function workspaceCandidateFileKey(record) {
  return safeFileToken2(stringValue32(record.client_id) ?? String(record.id ?? record.ID ?? Date.now()));
}
function localClientId(prefix, targetId, resourceId, nonce) {
  return `${prefix}_${targetId}_${resourceId}_${nonce ?? randomNonce2()}`;
}
function randomNonce2() {
  const random = globalThis.crypto;
  if (random?.randomUUID) return random.randomUUID();
  return `${Date.now()}_${Math.random().toString(36).slice(2)}`;
}
function normalizeProjectPath(value) {
  return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
}
function safeFileToken2(value) {
  return value.replace(/[^a-zA-Z0-9_-]+/g, "_");
}
function positiveNumber2(value) {
  const number = numberValue3(value);
  return number !== void 0 && number > 0 ? number : void 0;
}
function numberValue3(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) {
    const number = Number(value);
    if (Number.isFinite(number)) return number;
  }
  return void 0;
}
function numericId(value) {
  return numberValue3(value);
}
function resourceIdValue2(value) {
  if (value === void 0) return void 0;
  return requiredResourceId2(value);
}
function requiredResourceId2(value) {
  const id = positiveNumber2(value);
  if (id) return id;
  throw new Error("resource_id must be a positive integer RawResource ID");
}
function stringValue32(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function isRecord42(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined4(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
function stablePositiveHash(value) {
  let hash = 0;
  for (let index = 0; index < value.length; index += 1) {
    hash = hash * 31 + value.charCodeAt(index) >>> 0;
  }
  return hash % 2e9 + 1;
}
async function upsertMovScriptWorkspaceSetting(input) {
  const sourcePath = input.entity?.path ?? workspacePath3(input.record ?? {});
  const current = stripEntityPrivateFields(input.entity?.record ?? input.record ?? {});
  const payload = stripEntityPrivateFields(input.payload);
  const identity = entityIdentity(entityIdentityValue(current, payload, input.now), "setting");
  const title = stringValue42(payload.title ?? current.title) ?? displayName(identity.id);
  const path = sourcePath ?? `settings/${identity.slug}/setting.json`;
  const record = pruneUndefined5({
    ...current,
    ...payload,
    schema: "movscript.setting.v1",
    kind: "setting",
    id: identity.id,
    title,
    setting_kind: normalizeSettingKind(payload.setting_kind ?? payload.kind ?? current.setting_kind ?? current.kind),
    profile: normalizeProfile(payload, current)
  });
  await input.fileRepository.write({ path, content: serializeWorkspaceRecord6(record) });
  return { path, entityKind: "setting", record };
}
async function upsertMovScriptWorkspaceAsset(input) {
  const sourcePath = input.entity?.path ?? workspacePath3(input.record ?? {});
  const current = stripEntityPrivateFields(input.entity?.record ?? input.record ?? {});
  const payload = stripEntityPrivateFields(input.payload);
  const identity = entityIdentity(entityIdentityValue(current, payload, input.now), "asset");
  const sourcePathSettingId = sourcePath ? pathSegmentAfter4(sourcePath, "settings") : void 0;
  const sourcePathSettingStateId = sourcePath ? pathSegmentAfter4(sourcePath, "states") : void 0;
  if (sourcePath && !sourcePathSettingStateId) {
    throw new Error("asset source path must be under a setting state");
  }
  const settingId = entityRef(
    payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef ?? current.setting_id ?? current.settingId ?? current.setting_ref ?? current.settingRef,
    "setting"
  ) ?? sourcePathSettingId;
  const settingStateId = entityRef(
    payload.setting_state_id ?? payload.settingStateId ?? payload.setting_state_ref ?? payload.settingStateRef ?? current.setting_state_id ?? current.settingStateId ?? current.setting_state_ref ?? current.settingStateRef,
    "setting_state"
  ) ?? sourcePathSettingStateId;
  if (!settingId) {
    throw new Error("asset requires setting_id; assets must be stored under settings/{setting}/states/{state}/assets/{asset}/asset.json");
  }
  if (!settingStateId) {
    throw new Error("asset requires setting_state_id; assets must be stored under settings/{setting}/states/{state}/assets/{asset}/asset.json");
  }
  const title = stringValue42(payload.title ?? current.title) ?? displayName(identity.id);
  const path = sourcePath ?? assetPath({ id: identity.id, settingId, settingStateId });
  const record = pruneUndefined5({
    ...stripAssetOwnershipAliasFields(current),
    ...stripAssetOwnershipAliasFields(payload),
    schema: "movscript.asset.v1",
    kind: "asset",
    id: identity.id,
    title,
    slot: stringValue42(payload.slot ?? payload.slot_key ?? payload.slotKey ?? current.slot ?? current.slot_key ?? current.slotKey) ?? identity.slug,
    asset_kind: normalizeAssetKind(payload.asset_kind ?? payload.kind ?? current.asset_kind ?? current.kind),
    prompt_hint: stringValue42(payload.prompt_hint ?? payload.promptHint ?? current.prompt_hint ?? current.promptHint),
    setting_id: settingId,
    setting_state_id: settingStateId
  });
  await input.fileRepository.write({ path, content: serializeWorkspaceRecord6(record) });
  return { path, entityKind: "asset", record };
}
async function upsertMovScriptWorkspaceSettingState(input) {
  const sourcePath = input.entity?.path ?? workspacePath3(input.record ?? {});
  const current = stripEntityPrivateFields(input.entity?.record ?? input.record ?? {});
  const payload = stripEntityPrivateFields(input.payload);
  const identity = entityIdentity(entityIdentityValue(current, payload, input.now), "setting_state");
  const sourcePathSettingId = sourcePath ? pathSegmentAfter4(sourcePath, "settings") : void 0;
  const settingId = entityRef(
    payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef ?? current.setting_id ?? current.settingId ?? current.setting_ref ?? current.settingRef,
    "setting"
  ) ?? sourcePathSettingId;
  if (!settingId) {
    throw new Error("setting state requires setting_id; states must be stored under settings/{setting}/states/{state}/setting_state.json");
  }
  const title = stringValue42(payload.title ?? current.title) ?? displayName(identity.id);
  const path = sourcePath ?? `settings/${entityPathSlug(settingId, "setting")}/states/${identity.slug}/setting_state.json`;
  const record = pruneUndefined5({
    ...current,
    ...payload,
    schema: "movscript.setting_state.v1",
    kind: "setting_state",
    id: identity.id,
    title,
    setting_id: settingId,
    state_kind: stringValue42(payload.state_kind ?? payload.stateKind ?? payload.kind ?? current.state_kind ?? current.stateKind ?? current.kind),
    description: stringValue42(payload.description ?? current.description)
  });
  await input.fileRepository.write({ path, content: serializeWorkspaceRecord6(record) });
  return { path, entityKind: "setting_state", record };
}
async function deleteMovScriptWorkspaceEntity(input) {
  const path = input.entity?.path ?? workspacePath3(input.record ?? {});
  if (!path) throw new Error("workspace entity path is required");
  await input.fileRepository.delete({ path });
}
function movScriptWorkspaceAssetPath(input) {
  const settingSlug = entityPathSlug(input.settingId, "setting");
  const assetSlug = entityPathSlug(input.id, "asset");
  return `settings/${settingSlug}/states/${entityPathSlug(input.settingStateId, "setting_state")}/assets/${assetSlug}/asset.json`;
}
var assetPath = movScriptWorkspaceAssetPath;
function entityIdentityValue(current, payload, now) {
  return payload.workspace_slug ?? payload.workspaceSlug ?? current.workspace_slug ?? current.workspaceSlug ?? payload.workspace_entity_id ?? payload.workspaceEntityId ?? current.workspace_entity_id ?? current.workspaceEntityId ?? payload.id ?? payload.ID ?? current.id ?? current.ID ?? current.client_id ?? current.clientId ?? `local_${(now ?? /* @__PURE__ */ new Date()).getTime()}`;
}
function entityRef(value, entityKind) {
  if (value === void 0 || value === null || String(value).trim() === "") return void 0;
  return semanticEntityId(value, entityKind);
}
function stripAssetOwnershipAliasFields(record) {
  const output = {};
  for (const [key, value] of Object.entries(record)) {
    if (key === "setting_ref" || key === "settingRef" || key === "setting_state_ref" || key === "settingStateRef") continue;
    output[key] = value;
  }
  return output;
}
function normalizeSettingKind(value) {
  const kind = stringValue42(value);
  if (kind === "person" || kind === "character") return "character";
  if (kind === "place" || kind === "location") return "location";
  if (kind === "prop" || kind === "world_rule" || kind === "style" || kind === "other") return kind;
  return "other";
}
function normalizeAssetKind(value) {
  const kind = stringValue42(value);
  if (kind === "image" || kind === "video" || kind === "audio" || kind === "text" || kind === "reference" || kind === "other") return kind;
  return "other";
}
function normalizeProfile(payload, current) {
  const existing = isRecord52(current.profile) ? current.profile : {};
  const profile = pruneUndefined5({
    ...existing,
    alias: payload.alias ?? current.alias,
    description: payload.description ?? current.description,
    content: payload.content ?? current.content,
    importance: payload.importance ?? current.importance
  });
  return Object.keys(profile).length > 0 ? profile : void 0;
}
function workspacePath3(record) {
  return stringValue42(record.__workspace_path ?? record.workspace_path ?? record.path);
}
function pathSegmentAfter4(path, segment) {
  const parts = path.split("/");
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function stripEntityPrivateFields(record) {
  const output = {};
  for (const [key, value] of Object.entries(record)) {
    if (key.startsWith("__workspace_")) continue;
    if (key === "name") continue;
    output[key] = value;
  }
  return output;
}
function serializeWorkspaceRecord6(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function stringValue42(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function displayName(value) {
  const text = stringValue42(value);
  return text ?? "Untitled";
}
function isRecord52(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined5(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0 && item !== "") output[key] = item;
  }
  return output;
}
async function upsertMovScriptWorkspaceScript(input) {
  const current = stripWorkspacePrivateFields2(input.record ?? {});
  const scriptId = stableScriptId(input.scriptId);
  const scriptDir = `scripts/${entityPathSlug(scriptId, "script")}`;
  const scriptPath = `${scriptDir}/script.json`;
  const sourcePath = `${scriptDir}/script.md`;
  const record = pruneUndefined6({
    ...current,
    ...input.metadata,
    schema: "movscript.script.v1",
    kind: "script",
    id: scriptId,
    title: stringValue52(input.metadata?.title ?? current.title) ?? `Script ${scriptId}`,
    description: stringValue52(input.metadata?.description ?? current.description),
    script_kind: stringValue52(input.metadata?.script_kind ?? input.metadata?.script_type ?? current.script_kind ?? current.script_type) ?? "uncategorized",
    source_ref: "script.md",
    content: input.sourceText,
    updated_at: (input.now ?? /* @__PURE__ */ new Date()).toISOString(),
    created_at: stringValue52(current.created_at ?? current.CreatedAt) ?? (input.now ?? /* @__PURE__ */ new Date()).toISOString()
  });
  await input.fileRepository.write({ path: sourcePath, content: input.sourceText });
  await input.fileRepository.write({ path: scriptPath, content: serializeWorkspaceRecord7(record) });
  return { scriptId, scriptPath, sourcePath, record, sourceText: input.sourceText };
}
async function readMovScriptWorkspaceScriptSource(input) {
  const record = input.entity?.record ?? input.record;
  const scriptPath = stringValue52(input.entity?.path ?? workspacePath4(record));
  const sourceRef = stringValue52(record.source_ref) ?? "script.md";
  if (!scriptPath) return stringValue52(input.record.content) ?? "";
  const sourcePath = `${scriptPath.replace(/\/script\.json$/, "")}/${sourceRef}`.replace(/\/+/g, "/");
  return input.fileRepository.read({ path: sourcePath }).then((file) => file.content).catch(() => stringValue52(record.content) ?? "");
}
function workspacePath4(record) {
  return stringValue52(record.__workspace_path ?? record.workspace_path ?? record.path);
}
function stableScriptId(value) {
  return semanticEntityId(value, "script");
}
function stripWorkspacePrivateFields2(record) {
  const output = {};
  for (const [key, value] of Object.entries(record)) {
    if (key.startsWith("__workspace_")) continue;
    output[key] = value;
  }
  return output;
}
function serializeWorkspaceRecord7(value) {
  return `${JSON.stringify(value, null, 2)}
`;
}
function stringValue52(value) {
  return typeof value === "string" ? value : void 0;
}
function pruneUndefined6(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0 && item !== "") output[key] = item;
  }
  return output;
}
async function saveMovScriptProductionWorkspaceSnapshot(input) {
  const productionId = stableId(input.productionId, "production");
  const productionSlug = slugId(productionId, "production");
  const productionPath = `productions/${productionSlug}/production.json`;
  const writtenPaths = [];
  const production = await readOptionalRecord(input.fileRepository, productionPath);
  await writeRecord(input.fileRepository, productionPath, pruneUndefined7({
    ...stripWorkspacePrivateFields3(production),
    schema: "movscript.production.v1",
    kind: "production",
    id: productionId,
    title: stringValue62(input.snapshot.production?.title ?? production.title) ?? `Production ${displayId(productionId, "production")}`,
    transition: normalizeTransition2(input.snapshot.production?.transition ?? production.transition),
    updated_at: (input.now ?? /* @__PURE__ */ new Date()).toISOString()
  }));
  writtenPaths.push(productionPath);
  for (const segment of input.snapshot.segments) {
    const segmentId = stableId(segment.id ?? segment.client_id ?? writtenPaths.length + 1, "segment");
    const segmentSlug = slugId(segmentId, "segment");
    const segmentPath = `productions/${productionSlug}/segments/${segmentSlug}/segment.json`;
    const existingSegment = await readOptionalRecord(input.fileRepository, segmentPath);
    await writeRecord(input.fileRepository, segmentPath, pruneUndefined7({
      ...stripWorkspacePrivateFields3(existingSegment),
      schema: "movscript.segment.v1",
      kind: "segment",
      id: segmentId,
      title: stringValue62(segment.title) ?? stringValue62(existingSegment.title) ?? `Segment ${displayId(segmentId, "segment")}`,
      segment_kind: stringValue62(segment.kind ?? existingSegment.segment_kind),
      summary: stringValue62(segment.summary ?? existingSegment.summary),
      order: finiteNumber22(segment.order) ?? finiteNumber22(existingSegment.order),
      transition: normalizeTransition2(segment.transition ?? existingSegment.transition),
      script_block_id: nullableRef(segment.script_block_id ?? existingSegment.script_block_id, "script_block"),
      ...segment.__delete === true ? { __delete: true } : {}
    }));
    writtenPaths.push(segmentPath);
    for (const moment of segment.scene_moments ?? []) {
      const momentId = stableId(moment.id ?? moment.client_id ?? `${segmentId}_${writtenPaths.length + 1}`, "scene_moment");
      const momentSlug = slugId(momentId, "scene_moment");
      const momentDir = `productions/${productionSlug}/segments/${segmentSlug}/scene_moments/${momentSlug}`;
      const momentPath = `${momentDir}/scene_moment.json`;
      const existingMoment = await readOptionalRecord(input.fileRepository, momentPath);
      await writeRecord(input.fileRepository, momentPath, pruneUndefined7({
        ...stripWorkspacePrivateFields3(existingMoment),
        schema: "movscript.scene_moment.v1",
        kind: "scene_moment",
        id: momentId,
        title: stringValue62(moment.title) ?? stringValue62(existingMoment.title) ?? `Scene Moment ${displayId(momentId, "scene_moment")}`,
        scene_code: stringValue62(moment.scene_code ?? existingMoment.scene_code),
        when: stringValue62(moment.time_text ?? existingMoment.when),
        where: stringValue62(moment.location_text ?? existingMoment.where),
        condition_text: stringValue62(moment.condition_text ?? existingMoment.condition_text),
        action: stringValue62(moment.action_text ?? existingMoment.action),
        emotion: stringValue62(moment.mood ?? existingMoment.emotion),
        description: stringValue62(moment.description ?? existingMoment.description),
        order: finiteNumber22(moment.order) ?? finiteNumber22(existingMoment.order),
        transition: normalizeTransition2(moment.transition ?? existingMoment.transition),
        script_block_id: nullableRef(moment.script_block_id ?? existingMoment.script_block_id, "script_block"),
        setting_refs: normalizeSettingRefs(moment.settings, existingMoment.setting_refs),
        ...moment.__delete === true ? { __delete: true } : {}
      }));
      writtenPaths.push(momentPath);
      await writeKeyframeNodes({
        fileRepository: input.fileRepository,
        writtenPaths,
        ownerDir: momentDir,
        ownerKind: "scene_moment",
        sceneMomentDir: momentDir,
        ownerTitle: stringValue62(moment.title ?? existingMoment.title) ?? displayId(momentId, "scene_moment"),
        nodes: moment.keyframes ?? []
      });
      await writeStoryboardNodes({
        fileRepository: input.fileRepository,
        writtenPaths,
        ownerDir: momentDir,
        ownerKind: "scene_moment",
        sceneMomentDir: momentDir,
        ownerTitle: stringValue62(moment.title ?? existingMoment.title) ?? displayId(momentId, "scene_moment"),
        nodes: moment.storyboards ?? [],
        settings: moment.settings
      });
      for (const audioCue of moment.audio_cues ?? []) {
        const audioCueId = stableId(audioCue.id ?? audioCue.client_id ?? `${momentId}_${writtenPaths.length + 1}`, "audio_cue");
        const audioCuePath = `${momentDir}/audio_cues/${slugId(audioCueId, "audio_cue")}/audio_cue.json`;
        const existingAudioCue = await readOptionalRecord(input.fileRepository, audioCuePath);
        await writeRecord(input.fileRepository, audioCuePath, pruneUndefined7({
          ...stripWorkspacePrivateFields3(existingAudioCue),
          schema: "movscript.audio_cue.v1",
          kind: "audio_cue",
          id: audioCueId,
          title: stringValue62(audioCue.title ?? existingAudioCue.title) ?? `Audio Cue ${displayId(audioCueId, "audio_cue")}`,
          cue_kind: normalizeAudioCueKind(audioCue.cue_kind ?? audioCue.kind ?? existingAudioCue.cue_kind),
          order: finiteNumber22(audioCue.order) ?? finiteNumber22(existingAudioCue.order),
          scope_ref: momentDir,
          expression_unit_ref: stringValue62(audioCue.expression_unit_ref ?? existingAudioCue.expression_unit_ref),
          storyboard_ref: normalizeStoryboardRef(audioCue.storyboard_ref ?? audioCue.storyboard_id ?? existingAudioCue.storyboard_ref),
          timing: isRecord62(audioCue.timing) ? audioCue.timing : isRecord62(existingAudioCue.timing) ? existingAudioCue.timing : void 0,
          prompt_hint: stringValue62(audioCue.prompt_hint ?? existingAudioCue.prompt_hint),
          asset_refs: Array.isArray(audioCue.asset_refs) ? audioCue.asset_refs.filter(isString) : existingAudioCue.asset_refs,
          ...audioCue.__delete === true ? { __delete: true } : {}
        }));
        writtenPaths.push(audioCuePath);
      }
      for (const expression of moment.expression_units ?? []) {
        const expressionId = stableId(expression.id ?? expression.client_id ?? `${momentId}_${writtenPaths.length + 1}`, "expression_unit");
        const expressionPath = `${momentDir}/expression_units/${slugId(expressionId, "expression_unit")}/expression_unit.json`;
        const existingExpression = await readOptionalRecord(input.fileRepository, expressionPath);
        await writeRecord(input.fileRepository, expressionPath, pruneUndefined7({
          ...stripWorkspacePrivateFields3(existingExpression),
          schema: "movscript.expression_unit.v1",
          kind: "expression_unit",
          id: expressionId,
          title: stringValue62(expression.title ?? existingExpression.title) ?? stringValue62(expression.text) ?? `Expression Unit ${displayId(expressionId, "expression_unit")}`,
          slot_kind: normalizeExpressionUnitSlotKind(expression.slot_kind ?? expression.slotKind ?? existingExpression.slot_kind ?? existingExpression.slotKind ?? expression.kind ?? existingExpression.expression_kind),
          modality: stringValue62(expression.modality ?? existingExpression.modality),
          role: stringValue62(expression.role ?? existingExpression.role),
          expression_kind: normalizeExpressionKind(expression.kind ?? existingExpression.expression_kind),
          visual_kind: stringValue62(expression.visual_kind ?? existingExpression.visual_kind),
          speaker: stringValue62(expression.speaker ?? existingExpression.speaker),
          speaker_ref: stringValue62(expression.speaker_ref ?? existingExpression.speaker_ref),
          source_expression_ref: stringValue62(expression.source_expression_ref ?? existingExpression.source_expression_ref),
          text: stringValue62(expression.text ?? existingExpression.text) ?? "",
          note: stringValue62(expression.note ?? existingExpression.note),
          intent: stringValue62(expression.intent ?? existingExpression.intent),
          content: isRecord62(expression.content) ? expression.content : isRecord62(existingExpression.content) ? existingExpression.content : void 0,
          timing_intent: isRecord62(expression.timing_intent) ? expression.timing_intent : isRecord62(existingExpression.timing_intent) ? existingExpression.timing_intent : void 0,
          voice_profile_ref: stringValue62(expression.voice_profile_ref ?? existingExpression.voice_profile_ref),
          order: finiteNumber22(expression.order) ?? finiteNumber22(existingExpression.order),
          span: isRecord62(expression.span) ? expression.span : isRecord62(existingExpression.span) ? existingExpression.span : void 0,
          script_block_id: nullableRef(expression.script_block_id ?? existingExpression.script_block_id, "script_block"),
          ...expression.__delete === true ? { __delete: true } : {}
        }));
        writtenPaths.push(expressionPath);
        await writeKeyframeNodes({
          fileRepository: input.fileRepository,
          writtenPaths,
          ownerDir: `${momentDir}/expression_units/${slugId(expressionId, "expression_unit")}`,
          ownerKind: "expression_unit",
          sceneMomentDir: momentDir,
          ownerTitle: stringValue62(expression.title ?? existingExpression.title) ?? displayId(expressionId, "expression_unit"),
          nodes: expression.keyframes ?? []
        });
        await writeStoryboardNodes({
          fileRepository: input.fileRepository,
          writtenPaths,
          ownerDir: `${momentDir}/expression_units/${slugId(expressionId, "expression_unit")}`,
          ownerKind: "expression_unit",
          sceneMomentDir: momentDir,
          ownerTitle: stringValue62(expression.title ?? existingExpression.title) ?? displayId(expressionId, "expression_unit"),
          nodes: expression.storyboards ?? [],
          settings: moment.settings
        });
      }
    }
  }
  return { productionPath, snapshot: input.snapshot, writtenPaths };
}
async function writeKeyframeNodes({
  fileRepository,
  writtenPaths,
  ownerDir,
  ownerKind,
  sceneMomentDir,
  ownerTitle,
  nodes
}) {
  for (const keyframe of nodes) {
    const keyframeId = stableId(keyframe.id ?? keyframe.client_id ?? `${ownerTitle}_${writtenPaths.length + 1}`, "keyframe");
    const keyframePath = `${ownerDir}/keyframes/${slugId(keyframeId, "keyframe")}/keyframe.json`;
    const existingKeyframe = await readOptionalRecord(fileRepository, keyframePath);
    await writeRecord(fileRepository, keyframePath, pruneUndefined7({
      ...stripWorkspacePrivateFields3(existingKeyframe),
      schema: "movscript.keyframe.v1",
      kind: "keyframe",
      id: keyframeId,
      title: stringValue62(keyframe.title ?? existingKeyframe.title) ?? `Keyframe ${displayId(keyframeId, "keyframe")}`,
      scene_moment_ref: sceneMomentDir,
      ...ownerKind === "expression_unit" ? { expression_unit_ref: ownerDir } : {},
      role: stringValue62(keyframe.role ?? existingKeyframe.role),
      visual_intent: stringValue62(keyframe.visual_intent ?? existingKeyframe.visual_intent),
      timing: isRecord62(keyframe.timing) ? keyframe.timing : isRecord62(existingKeyframe.timing) ? existingKeyframe.timing : void 0,
      composition: isRecord62(keyframe.composition) ? keyframe.composition : isRecord62(existingKeyframe.composition) ? existingKeyframe.composition : void 0,
      continuity: isRecord62(keyframe.continuity) ? keyframe.continuity : isRecord62(existingKeyframe.continuity) ? existingKeyframe.continuity : void 0,
      reference_asset_refs: Array.isArray(keyframe.reference_asset_refs) ? keyframe.reference_asset_refs.filter(isString) : existingKeyframe.reference_asset_refs,
      reference_keyframe_refs: Array.isArray(keyframe.reference_keyframe_refs) ? keyframe.reference_keyframe_refs.filter(isString) : existingKeyframe.reference_keyframe_refs,
      ...keyframe.__delete === true ? { __delete: true } : {}
    }));
    writtenPaths.push(keyframePath);
  }
}
async function writeStoryboardNodes({
  fileRepository,
  writtenPaths,
  ownerDir,
  ownerKind,
  sceneMomentDir,
  ownerTitle,
  nodes,
  settings
}) {
  for (const [storyboardIndex, storyboard] of nodes.entries()) {
    const storyboardId = stableId(storyboard.id ?? storyboard.client_id ?? "main", "storyboard");
    const storyboardPath = `${ownerDir}/storyboards/${slugId(storyboardId, "storyboard")}/storyboard.json`;
    const existingStoryboard = await readOptionalRecord(fileRepository, storyboardPath);
    await writeRecord(fileRepository, storyboardPath, pruneUndefined7({
      ...stripWorkspacePrivateFields3(existingStoryboard),
      schema: "movscript.storyboard.v1",
      kind: "storyboard",
      id: storyboardId,
      title: stringValue62(storyboard.title ?? existingStoryboard.title) ?? `${ownerTitle} storyboard`,
      order: finiteNumber22(storyboard.order) ?? finiteNumber22(existingStoryboard.order) ?? storyboardIndex + 1,
      slot: stringValue62(storyboard.slot ?? existingStoryboard.slot) ?? String(storyboardId),
      asset_kind: stringValue62(storyboard.asset_kind ?? existingStoryboard.asset_kind),
      scene_moment_ref: sceneMomentDir,
      ...ownerKind === "expression_unit" ? { expression_unit_ref: ownerDir } : {},
      transition: normalizeTransition2(storyboard.transition ?? existingStoryboard.transition),
      timeline: normalizeTimeline2(storyboard.timeline ?? {
        gap_after_sec: storyboard.gap_after_sec,
        caption: storyboard.caption,
        duration_sec: storyboard.duration_sec
      }, existingStoryboard.timeline),
      graph: isRecord62(storyboard.graph) ? storyboard.graph : isRecord62(existingStoryboard.graph) ? existingStoryboard.graph : void 0,
      setting_refs: normalizeSettingRefs(storyboard.settings ?? settings, existingStoryboard.setting_refs),
      ...storyboard.__delete === true ? { __delete: true } : {}
    }));
    writtenPaths.push(storyboardPath);
  }
}
function normalizeSettingRefs(refs, fallback) {
  if (!refs) return Array.isArray(fallback) ? fallback.filter(isRecord62) : void 0;
  return refs.map((ref) => pruneUndefined7({
    setting_id: stableId(ref.id ?? ref.client_id ?? "unassigned", "setting"),
    setting_state_id: ref.setting_state_id ?? ref.setting_state_ref,
    role: stringValue62(ref.role),
    notes: stringValue62(ref.source_label),
    setting_kind: stringValue62(ref.kind),
    state: isRecord62(ref.state) ? ref.state : void 0,
    ...ref.__delete === true ? { __delete: true } : {}
  }));
}
function normalizeExpressionKind(value) {
  const kind = stringValue62(value);
  if (kind === "shot") return "shot";
  if (kind === "dialogue" || kind === "narration" || kind === "subtitle" || kind === "caption" || kind === "action" || kind === "shot") return kind;
  if (kind === "visual" || kind === "visual_note") return "visual_note";
  if (kind === "voice" || kind === "verbal" || kind === "voiceover") return "dialogue";
  if (kind === "audio" || kind === "sound" || kind === "sound_effect" || kind === "sfx" || kind === "music" || kind === "ambience") return "action";
  return "visual_note";
}
function normalizeTransition2(value) {
  if (!isRecord62(value)) return void 0;
  return pruneUndefined7({
    in: stringValue62(value.in),
    out: stringValue62(value.out),
    notes: stringValue62(value.notes)
  });
}
function normalizeTimeline2(value, fallback) {
  const source = isRecord62(value) ? value : isRecord62(fallback) ? fallback : {};
  const timeline = pruneUndefined7({
    gap_after_sec: finiteNumber22(source.gap_after_sec),
    caption: stringValue62(source.caption),
    duration_sec: finiteNumber22(source.duration_sec)
  });
  return Object.keys(timeline).length ? timeline : void 0;
}
function normalizeAudioCueKind(value) {
  const kind = stringValue62(value);
  if (kind === "sound_effect" || kind === "music" || kind === "ambience" || kind === "dialogue" || kind === "foley" || kind === "other") return kind;
  if (kind === "sound") return "sound_effect";
  if (kind === "music_beat") return "music";
  return "sound_effect";
}
function normalizeStoryboardRef(value) {
  return stringValue62(value);
}
async function readRecord(fileRepository, path) {
  const file = await fileRepository.read({ path });
  const parsed = JSON.parse(file.content);
  return isRecord62(parsed) ? parsed : {};
}
async function readOptionalRecord(fileRepository, path) {
  return readRecord(fileRepository, path).catch(() => ({}));
}
async function writeRecord(fileRepository, path, record) {
  await fileRepository.write({ path, content: `${JSON.stringify(record, null, 2)}
` });
}
function stableId(value, prefix) {
  return semanticEntityId(value, prefix);
}
function slugId(value, prefix) {
  return entityPathSlug(value, prefix);
}
function nullableRef(value, prefix) {
  if (value === null) return null;
  if (value === void 0 || String(value).trim() === "") return void 0;
  return stableId(value, prefix);
}
function displayId(value, prefix) {
  return displayEntityId(value, prefix);
}
function finiteNumber22(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function stringValue62(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function isRecord62(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function isString(value) {
  return typeof value === "string";
}
function stripWorkspacePrivateFields3(record) {
  const output = {};
  for (const [key, value] of Object.entries(record)) {
    if (key.startsWith("__workspace_")) continue;
    output[key] = value;
  }
  return output;
}
function pruneUndefined7(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0 && item !== "") output[key] = item;
  }
  return output;
}
async function upsertMovScriptContentUnit(input) {
  const contentUnitId = stableEntityId(input.unit.ID ?? input.unit.id ?? input.unit.client_id, "content_unit");
  const contentUnitPath = movScriptContentUnitPath(input.unit);
  const current = await readOptionalRecord2(input.fileRepository, contentUnitPath);
  const record = normalizeContentUnitRecord(input.unit, current, contentUnitId);
  validateContentUnitTarget(record);
  validateContentUnitPrimaryPromptRefs(record);
  await writeRecord2(input.fileRepository, contentUnitPath, record);
  return { contentUnitPath, record };
}
function movScriptContentUnitPath(unit) {
  const id = stableEntityId(unit.ID ?? unit.id ?? unit.client_id, "content_unit");
  return `${contentUnitDirectory(id)}/content_unit.json`;
}
function normalizeContentUnitRecord(unit, current, id) {
  const contentUnitType2 = stringValue7(unit.content_unit_type ?? unit.contentUnitType ?? current.content_unit_type) ?? "storyboard_ref";
  const outputKind = stringValue7(unit.output_kind ?? unit.outputKind ?? current.output_kind) ?? defaultOutputKind(contentUnitType2);
  const scopeKind = stringValue7(unit.scope_kind ?? unit.scopeKind ?? current.scope_kind);
  const scopeRef = stringValue7(unit.scope_ref ?? unit.scopeRef ?? current.scope_ref);
  const targetKind = stringValue7(unit.target_kind ?? unit.targetKind ?? current.target_kind) ?? (contentUnitType2 === "timeline_assembly_ref" ? "timeline_assembly" : void 0);
  const targetRef = stringValue7(unit.target_ref ?? unit.targetRef ?? current.target_ref) ?? (contentUnitType2 === "timeline_assembly_ref" && scopeKind && scopeRef ? implicitTimelineAssemblyRef(scopeKind, scopeRef) : void 0);
  return pruneUndefined8({
    ...stripWorkspacePrivateFields4(current),
    schema: "movscript.content_unit.v1",
    kind: "content_unit",
    id,
    title: stringValue7(unit.title ?? current.title) ?? "Untitled content unit",
    content_unit_type: contentUnitType2,
    output_kind: outputKind,
    target_category: stringValue7(unit.target_category ?? unit.targetCategory ?? current.target_category),
    target_kind: targetKind,
    target_ref: targetRef,
    scope_kind: scopeKind,
    scope_ref: scopeRef,
    generation_role: stringValue7(unit.generation_role ?? unit.generationRole ?? current.generation_role),
    order: finiteNumber3(unit.order) ?? finiteNumber3(current.order),
    description: stringValue7(unit.description ?? current.description) ?? "",
    asset_ref: stringValue7(unit.asset_ref ?? unit.assetRef ?? current.asset_ref),
    production_ref: stringValue7(unit.production_ref ?? unit.productionRef ?? current.production_ref),
    segment_ref: stringValue7(unit.segment_ref ?? unit.segmentRef ?? current.segment_ref),
    keyframe_ref: stringValue7(unit.keyframe_ref ?? unit.keyframeRef ?? current.keyframe_ref),
    storyboard_ref: stringValue7(unit.storyboard_ref ?? unit.storyboardRef ?? current.storyboard_ref),
    scene_moment_ref: stringValue7(unit.scene_moment_ref ?? unit.sceneMomentRef ?? current.scene_moment_ref),
    expression_unit_ref: stringValue7(unit.expression_unit_ref ?? unit.expressionUnitRef ?? current.expression_unit_ref),
    content_unit_ref: stringValue7(unit.content_unit_ref ?? unit.contentUnitRef ?? current.content_unit_ref),
    voice_profile_ref: stringValue7(unit.voice_profile_ref ?? unit.voiceProfileRef ?? current.voice_profile_ref),
    edit_prompt: normalizeEditPrompt(unit.edit_prompt ?? unit.editPrompt ?? unit.prompt ?? current.edit_prompt),
    model_intent: isRecord72(unit.model_intent ?? unit.modelIntent) ? unit.model_intent ?? unit.modelIntent : current.model_intent,
    ...unit.__delete === true ? { __delete: true } : {}
  });
}
function contentUnitDirectory(id) {
  return `content_units/${entityPathSlug(id, "content_unit")}`;
}
function defaultOutputKind(contentUnitType2) {
  return outputKindForContentUnitType(contentUnitType2, void 0);
}
function validateContentUnitTarget(record) {
  for (const diagnostic2 of contentUnitTargetValidationDiagnostics(record)) {
    throw new Error(diagnostic2.message);
  }
}
function validateContentUnitPrimaryPromptRefs(record) {
  const contentUnitType2 = stringValue7(record.content_unit_type);
  if (!contentUnitType2) return;
  const primaryKind = primaryRefKindForContentUnitType(contentUnitType2);
  if (!primaryKind) return;
  const primaryRefs = primaryRefIdsForContentUnitRecord(record, primaryKind);
  if (primaryRefs.length === 0) return;
  const promptRefs = parseContentUnitEditPromptRefs(record.edit_prompt);
  for (const promptRef of promptRefs) {
    if (promptRef.kind !== primaryKind) continue;
    if (!primaryRefs.some((primaryRef) => sameRefId(primaryRef, promptRef.id, primaryKind))) continue;
    throw new Error(`${contentUnitType2} content_unit edit_prompt must not reference its own ${primaryRefFieldNameForKind(primaryKind)}: ${promptRef.raw}`);
  }
}
var PROMPT_REF_PATTERN = /\{\{([a-z_]+):([^{}:\s][^{}]*)\}\}/g;
function parseContentUnitEditPromptRefs(value) {
  const editPrompt = isRecord72(value) ? value : {};
  return [
    ...parsePromptRefsFromText(stringValue7(editPrompt.text)),
    ...parsePromptRefsFromText(stringValue7(editPrompt.negative_text)),
    ...parsePromptRefsFromText(stringValue7(editPrompt.notes))
  ];
}
function parsePromptRefsFromText(text) {
  if (!text) return [];
  const refs = [];
  for (const match of text.matchAll(PROMPT_REF_PATTERN)) {
    const kind = promptRefKind(match[1]);
    const id = match[2]?.trim();
    if (!kind || !id) continue;
    refs.push({ kind, id, raw: match[0] });
  }
  return refs;
}
function promptRefKind(value) {
  return isContentUnitPromptRefKind(value) ? value : void 0;
}
function sameRefId(left, right, kind) {
  return sameEntityRef(left, right, kind) || lastPathSegment2(left) === right || lastPathSegment2(right) === left;
}
function lastPathSegment2(value) {
  if (typeof value !== "string" || !value.includes("/")) return void 0;
  return value.split("/").filter(Boolean).at(-1);
}
function stableEntityId(value, prefix) {
  return semanticEntityId(value, prefix);
}
async function readOptionalRecord2(fileRepository, path) {
  return fileRepository.read({ path }).then((file) => {
    const parsed = JSON.parse(file.content);
    return isRecord72(parsed) ? parsed : {};
  }).catch(() => ({}));
}
async function writeRecord2(fileRepository, path, record) {
  await fileRepository.write({ path, content: `${JSON.stringify(record, null, 2)}
` });
}
function stripWorkspacePrivateFields4(record) {
  const output = {};
  for (const [key, value] of Object.entries(record)) {
    if (key.startsWith("__workspace_")) continue;
    output[key] = value;
  }
  return output;
}
function stringValue7(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function finiteNumber3(value) {
  const next = Number(value);
  return Number.isFinite(next) ? next : void 0;
}
function normalizeEditPrompt(value) {
  if (typeof value === "string") return { text: value };
  if (isRecord72(value)) return value;
  return void 0;
}
function isRecord72(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined8(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0 && item !== "") output[key] = item;
  }
  return output;
}
function createMovScriptScopedProjectDataDecisionStore(options) {
  const baseUrl = options.baseUrl.replace(/\/+$/, "");
  const fetchImpl = options.fetch ?? globalThis.fetch;
  if (!fetchImpl) throw new Error("fetch is required for scoped project data decision store");
  const headers = () => ({
    "content-type": "application/json",
    ...options.token ? { authorization: `Bearer ${options.token}` } : {},
    ...options.headers ?? {}
  });
  const scopePayload = () => pruneUndefined9({
    scope_kind: options.scopeKind,
    scope_id: options.scopeId === void 0 ? void 0 : String(options.scopeId),
    project_uid: options.projectUid,
    title: options.title
  });
  const targetQuery = (targetRef) => {
    const query = new URLSearchParams();
    query.set("project_uid", options.projectUid);
    query.set("target_kind", "content_unit");
    query.set("target_ref", targetRef);
    if (options.scopeKind) query.set("scope_kind", options.scopeKind);
    if (options.scopeId !== void 0) query.set("scope_id", String(options.scopeId));
    return query.toString();
  };
  const request = async (path, init = {}) => {
    const response = await fetchImpl(`${baseUrl}/api/v1/project-data${path}`, {
      ...init,
      headers: {
        ...headers(),
        ...init.headers ?? {}
      }
    });
    if (response.status === 404) {
      console.info("[movscript-decision-store] scoped project data request not found", {
        projectUid: options.projectUid,
        scopeKind: options.scopeKind,
        scopeId: options.scopeId,
        method: init.method ?? "GET",
        path,
        status: response.status
      });
      return void 0;
    }
    if (!response.ok) {
      const body = await response.text().catch(() => "");
      console.warn("[movscript-decision-store] scoped project data request failed", {
        projectUid: options.projectUid,
        scopeKind: options.scopeKind,
        scopeId: options.scopeId,
        method: init.method ?? "GET",
        path,
        status: response.status,
        body
      });
      throw new Error(`scoped project data decision request failed: ${response.status}${body ? ` ${body}` : ""}`);
    }
    if (response.status === 204) return void 0;
    return await response.json();
  };
  return {
    async getContentUnitDecision(input) {
      const targetRef = contentUnitDecisionTargetRef(input.contentUnitId);
      const context = await request(`/decisions?${targetQuery(targetRef)}`);
      if (isMovScriptDecisionStoreDebugEnabled()) {
        console.info("[movscript-decision-store] get scoped content unit decision", {
          projectUid: options.projectUid,
          contentUnitId: input.contentUnitId,
          targetRef,
          found: Boolean(context),
          candidateCount: context?.candidates.length ?? 0
        });
      }
      return context;
    },
    async getContentUnitDecisions(input) {
      const ids = uniqueContentUnitIds(input.contentUnitIds);
      if (ids.length === 0) return /* @__PURE__ */ new Map();
      const contexts = await request("/decisions/query", {
        method: "POST",
        body: JSON.stringify({
          ...scopePayload(),
          target_kind: "content_unit",
          target_refs: ids.map(contentUnitDecisionTargetRef)
        })
      }) ?? [];
      if (isMovScriptDecisionStoreDebugEnabled()) {
        console.info("[movscript-decision-store] get scoped content unit decisions", {
          projectUid: options.projectUid,
          requestedCount: ids.length,
          foundCount: contexts.length
        });
      }
      const byTargetRef = new Map(contexts.map((context) => [context.target_ref, context]));
      const out = /* @__PURE__ */ new Map();
      for (const id of ids) {
        const context = byTargetRef.get(contentUnitDecisionTargetRef(id));
        if (context) out.set(String(id), context);
      }
      return out;
    },
    async replaceContentUnitCandidates(input) {
      const targetRef = contentUnitDecisionTargetRef(input.contentUnitId);
      const context = await requiredDecisionContext(request("/decisions/candidates", {
        method: "PUT",
        body: JSON.stringify({
          ...scopePayload(),
          target_kind: "content_unit",
          target_ref: targetRef,
          candidates: input.candidates
        })
      }));
      console.info("[movscript-decision-store] replace scoped content unit candidates", {
        projectUid: options.projectUid,
        contentUnitId: input.contentUnitId,
        targetRef,
        candidateCount: context.candidates.length
      });
      return context;
    },
    async upsertContentUnitCandidate(input) {
      const targetRef = contentUnitDecisionTargetRef(input.contentUnitId);
      const context = await requiredDecisionContext(request("/decisions/candidates", {
        method: "POST",
        body: JSON.stringify({
          ...scopePayload(),
          target_kind: "content_unit",
          target_ref: targetRef,
          candidate: input.candidate
        })
      }));
      console.info("[movscript-decision-store] upsert scoped content unit candidate", {
        projectUid: options.projectUid,
        contentUnitId: input.contentUnitId,
        targetRef,
        candidateId: idField5(input.candidate.id),
        candidateCount: context.candidates.length
      });
      return context;
    },
    async selectContentUnitCandidate(input) {
      const targetRef = contentUnitDecisionTargetRef(input.contentUnitId);
      const context = await requiredDecisionContext(request("/decisions/selection", {
        method: "PUT",
        body: JSON.stringify({
          ...scopePayload(),
          target_kind: "content_unit",
          target_ref: targetRef,
          candidate_id: stringIdField(input.candidateId),
          resource_id: input.resourceId === void 0 ? void 0 : requiredResourceId3(input.resourceId),
          stale_policy: input.stalePolicy,
          reason: input.reason,
          selected_at: input.selectedAt,
          metadata: input.metadata
        })
      }));
      console.info("[movscript-decision-store] select scoped content unit candidate", {
        projectUid: options.projectUid,
        contentUnitId: input.contentUnitId,
        targetRef,
        candidateId: stringIdField(input.candidateId),
        candidateCount: context.candidates.length,
        hasSelection: Boolean(context.selection)
      });
      return context;
    },
    clearContentUnitSelection(input) {
      const targetRef = contentUnitDecisionTargetRef(input.contentUnitId);
      return requiredDecisionContext(request(`/decisions/selection?${targetQuery(targetRef)}`, {
        method: "DELETE"
      }));
    }
  };
}
async function overlayMovScriptDecisionDocuments(documents, decisionStore) {
  if (!decisionStore) return documents;
  const sourceDocuments = documents.filter((document) => !isMovScriptContentUnitDecisionPath(document.path));
  const baseIndex = deriveMovScriptWorkspaceDomainIndex(sourceDocuments);
  const contentUnits = baseIndex.byKind.get("content_unit") ?? [];
  const decisionsByContentUnitId = await getOverlayDecisionContexts(decisionStore, contentUnits);
  const overlays = [];
  const rows = [];
  for (const contentUnit of contentUnits) {
    if (contentUnit.id === void 0) continue;
    const context = decisionsByContentUnitId.get(String(contentUnit.id));
    if (!context) continue;
    const contentUnitRef = entityDir3(contentUnit.path);
    rows.push({
      contentUnitId: contentUnit.id,
      targetRef: context.target_ref,
      candidateCount: context.candidates.length,
      candidateIds: decisionCandidateIds(context.candidates)
    });
    overlays.push({
      path: contentUnitDecisionContextPath(contentUnit.id),
      data: normalizeDecisionContext(context, contentUnitRef)
    });
    for (const candidate of context.candidates) {
      const candidateId = idField5(candidate.id);
      if (candidateId === void 0) continue;
      overlays.push({
        path: `${contentUnitRef}/candidates/${entityPathSlug(candidateId, "candidate")}/content_candidate.json`,
        data: normalizeContentUnitCandidate(candidate, contentUnitRef)
      });
    }
  }
  if (rows.length > 0 && isMovScriptDecisionStoreDebugEnabled()) {
    console.info("[movscript-decision-store] overlay content unit decisions", {
      contentUnitCount: contentUnits.length,
      rows
    });
  }
  const overlayPaths = new Set(overlays.map((document) => document.path));
  return [
    ...sourceDocuments.filter((document) => !overlayPaths.has(document.path)),
    ...overlays
  ].sort((left, right) => left.path.localeCompare(right.path));
}
async function getOverlayDecisionContexts(decisionStore, contentUnits) {
  const contentUnitIds = uniqueContentUnitIds(contentUnits.map((unit) => unit.id).filter((id) => id !== void 0));
  if (contentUnitIds.length === 0) return /* @__PURE__ */ new Map();
  if (decisionStore.getContentUnitDecisions) {
    return decisionStore.getContentUnitDecisions({ contentUnitIds });
  }
  const out = /* @__PURE__ */ new Map();
  for (const contentUnitId of contentUnitIds) {
    const context = await decisionStore.getContentUnitDecision({ contentUnitId });
    if (context) out.set(String(contentUnitId), context);
  }
  return out;
}
function uniqueContentUnitIds(values) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const value of values) {
    const key = String(value);
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(value);
  }
  return out;
}
function isMovScriptDecisionStoreDebugEnabled() {
  return typeof process !== "undefined" && process.env?.MOVSCRIPT_DECISION_STORE_DEBUG === "1";
}
function contentUnitDecisionTargetRef(contentUnitId) {
  return `content_units/${entityPathSlug(contentUnitId, "content_unit")}`;
}
function contentUnitDecisionContextPath(contentUnitId) {
  return `.movscript/decisions/content_units/${entityPathSlug(contentUnitId, "content_unit")}/decision_context.json`;
}
function normalizeDecisionContext(context, targetRef = context.target_ref) {
  return pruneUndefined9({
    ...context,
    schema: "movscript.decision_context.v1",
    target_kind: context.target_kind,
    target_ref: targetRef,
    candidates: context.candidates.map((candidate) => normalizeContentUnitCandidate(candidate, targetRef)),
    selection: isRecord82(context.selection) ? normalizeContentUnitDecisionSelection(context.selection) : void 0
  });
}
function normalizeContentUnitCandidate(candidate, contentUnitRef) {
  return pruneUndefined9({
    ...candidate,
    schema: candidate.schema ?? "movscript.content_candidate.v1",
    content_unit_ref: candidate.content_unit_ref ?? contentUnitRef
  });
}
function normalizeContentUnitDecisionSelection(selection) {
  return pruneUndefined9({
    candidate_id: selection.candidate_id,
    resource_id: selection.resource_id,
    stale_policy: selection.stale_policy === "accept_stale" ? "accept_stale" : "strict",
    reason: selection.reason,
    selected_at: selection.selected_at,
    selected_by: selection.selected_by,
    metadata: isRecord82(selection.metadata) ? selection.metadata : void 0
  });
}
async function requiredDecisionContext(promise) {
  const context = await promise;
  if (!context) throw new Error("backend decision context not found");
  return context;
}
function entityDir3(path) {
  return path.replace(/\/[^/]+$/, "");
}
function idField5(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function decisionCandidateIds(candidates) {
  return candidates.map((candidate) => idField5(candidate.id)).filter((id) => id !== void 0);
}
function stringIdField(value) {
  const id = idField5(value);
  return id === void 0 ? void 0 : String(id);
}
function requiredResourceId3(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  throw new Error("resource_id must be a positive integer RawResource ID");
}
function isRecord82(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined9(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
function buildMovScriptContentCandidate(input) {
  const contentUnitId = stableEntityId2(input.contentUnitId, "content_unit");
  const candidateId = stableEntityId2(input.candidateId ?? `candidate_${Date.now()}`, "candidate");
  const path = `${contentUnitDirectory2(contentUnitId)}/candidates/${entityPathSlug(candidateId, "candidate")}/content_candidate.json`;
  const record = pruneUndefined10({
    schema: "movscript.content_candidate.v1",
    id: candidateId,
    content_unit_ref: contentUnitDirectory2(contentUnitId),
    source: input.source ?? "ai_generate",
    status: input.status ?? "succeeded",
    producer: input.producer ?? { kind: "runtime" },
    outputs: normalizeOutputs(input.outputs),
    prompt_snapshot: input.promptSnapshot,
    created_at: input.createdAt ?? (/* @__PURE__ */ new Date()).toISOString()
  });
  return { path, record };
}
function contentUnitDirectory2(id) {
  return `content_units/${entityPathSlug(id, "content_unit")}`;
}
function stableEntityId2(value, prefix) {
  return semanticEntityId(value, prefix);
}
function normalizeOutputs(outputs) {
  return outputs.map((output, index) => {
    const resourceId = requiredResourceId4(output.resource_id, `outputs[${index}].resource_id`);
    return pruneUndefined10({
      ...output,
      resource_id: resourceId,
      artifact_ref: stringField24(output.artifact_ref)
    });
  });
}
function requiredResourceId4(value, name) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  throw new Error(`${name} must be a positive integer RawResource ID`);
}
function stringField24(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function pruneUndefined10(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
async function upsertMovScriptProjectStandards(input) {
  const current = stripWorkspacePrivateFields5(input.record ?? {});
  const projectStyle = {
    ...parseProjectStyle2(current.project_style),
    ...input.projectStyle
  };
  const record = pruneUndefined11({
    ...current,
    schema: "movscript.project_standards.v1",
    kind: "project_standards",
    id: "project_standards",
    title: stringValue8(current.title) ?? "Project standards",
    aspect_ratio: stringValue8(projectStyle.aspect_ratio ?? current.aspect_ratio),
    visual_style: stringValue8(projectStyle.visual_style ?? current.visual_style),
    project_style: JSON.stringify(projectStyle),
    updated_at: (input.now ?? /* @__PURE__ */ new Date()).toISOString()
  });
  const path = "project_standards.json";
  await input.fileRepository.write({ path, content: `${JSON.stringify(record, null, 2)}
` });
  return { path, record };
}
function parseProjectStyle2(value) {
  if (isRecord92(value)) return value;
  if (typeof value !== "string" || !value.trim()) return {};
  try {
    const parsed = JSON.parse(value);
    return isRecord92(parsed) ? parsed : {};
  } catch {
    return {};
  }
}
function stripWorkspacePrivateFields5(record) {
  const output = {};
  for (const [key, value] of Object.entries(record)) {
    if (key.startsWith("__workspace_")) continue;
    output[key] = value;
  }
  return output;
}
function stringValue8(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function isRecord92(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined11(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0 && item !== "") output[key] = item;
  }
  return output;
}
var MOVSCRIPT_PROJECT_STANDARD_SKILL_PATHS = [
  ".codex/skills/plugins/movscript_project-standards/project-standards/SKILL.md",
  ".claude/skills/plugins/movscript_project-standards/project-standards/SKILL.md",
  ".mova/skills/plugins/movscript_project-standards/project-standards/SKILL.md"
];
async function syncMovScriptProjectStandardSkills(input) {
  const content = renderMovScriptProjectStandardSkill(input.standards);
  const results = [];
  for (const path of MOVSCRIPT_PROJECT_STANDARD_SKILL_PATHS) {
    await input.fileRepository.write({ path, content });
    results.push({ path, content });
  }
  return results;
}
function renderMovScriptProjectStandardSkill(standards) {
  const projectStyle = parseProjectStyle22(standards.project_style);
  const coreSections = [
    fieldSection("Aspect ratio", standards.aspect_ratio ?? projectStyle.aspect_ratio),
    fieldSection("Visual style", standards.visual_style ?? projectStyle.visual_style),
    fieldSection("Lighting style", standards.lighting_style ?? projectStyle.lighting_style),
    fieldSection("Camera language", standards.camera_language ?? projectStyle.camera_language),
    fieldSection("Color palette", standards.color_palette ?? projectStyle.color_palette),
    fieldSection("Pacing rules", standards.pacing_rules ?? projectStyle.pacing_rules),
    listSection("Shot size system", standards.shot_size_system ?? projectStyle.shot_size_system),
    listSection("Negative rules", standards.negative_rules ?? projectStyle.negative_rules),
    customRulesSection(standards.custom_rules ?? projectStyle.custom_rules),
    styleReferenceSection(standards, projectStyle)
  ].filter(Boolean);
  return [
    "---",
    "name: project-standards",
    "description: Apply the current MovScript project standards from project_standards.json when planning, coding, prompting, or generating project-scoped work.",
    "---",
    "",
    "# MovScript Project Standards",
    "",
    "Generated from `project_standards.json`. Do not edit this file manually; update project standards through MovScript source or domain tools.",
    "",
    "Use this skill whenever work depends on this MovScript project, especially planning, content-unit prompts, generation, style-sensitive code, or provider behavior.",
    "",
    "## Rules",
    "",
    "- Treat `project_standards.json` as the source of truth.",
    "- Apply these standards as project-wide house style and constraints.",
    "- Do not change project standards unless the user explicitly asks to add, remove, replace, or refine them.",
    "- For visual generation that supports references, pass listed style reference resource ids as house-style references.",
    "",
    ...coreSections.length ? ["## Current Standards", "", ...coreSections] : ["## Current Standards", "", "- No project standards are currently filled.", ""]
  ].join("\n");
}
function fieldSection(label, value) {
  const text = textValue(value);
  return text ? `### ${label}

${text}
` : void 0;
}
function listSection(label, value) {
  const items = arrayValues(value);
  if (!items.length) return void 0;
  return `### ${label}

${items.map((item) => `- ${item}`).join("\n")}
`;
}
function customRulesSection(value) {
  const rules = arrayRecords(value).filter((rule) => rule.enabled !== false).map((rule) => {
    const key = textValue(rule.key) ?? "custom_rule";
    const ruleValue = rule.value ?? rule.text ?? rule.description;
    const text = textValue(ruleValue) ?? JSON.stringify(ruleValue);
    return text ? `- ${key}: ${text}` : void 0;
  }).filter((rule) => Boolean(rule));
  if (!rules.length) return void 0;
  return `### Custom rules

${rules.join("\n")}
`;
}
function styleReferenceSection(standards, projectStyle) {
  const ids = uniqueNumbers([
    ...resourceIdsFromValue(standards.style_reference_images),
    ...resourceIdsFromValue(standards.style_references),
    ...resourceIdsFromValue(standards.reference_resource_ids),
    ...resourceIdsFromValue(projectStyle.style_reference_images),
    ...resourceIdsFromValue(projectStyle.style_references),
    ...resourceIdsFromValue(projectStyle.reference_resource_ids),
    ...arrayRecords(standards.custom_rules).filter((rule) => rule.enabled !== false).filter((rule) => textValue(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue(rule.value)),
    ...arrayRecords(projectStyle.custom_rules).filter((rule) => rule.enabled !== false).filter((rule) => textValue(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue(rule.value))
  ]);
  if (!ids.length) return void 0;
  return `### Style reference resource ids

${ids.map((id) => `- ${id}`).join("\n")}
`;
}
function resourceIdsFromValue(value) {
  if (Array.isArray(value)) return value.flatMap(resourceIdsFromValue);
  if (typeof value === "number" && Number.isInteger(value)) return [value];
  if (isRecord10(value)) {
    return [
      ...resourceIdsFromValue(value.resource_id),
      ...resourceIdsFromValue(value.resource_ids),
      ...resourceIdsFromValue(value.id)
    ];
  }
  const text = textValue(value);
  if (!text) return [];
  return Array.from(text.matchAll(/\b(?:resource[_-]?id|resource)[_:#\s-]*(\d+)\b/gi)).map((match) => Number(match[1])).filter((id) => Number.isInteger(id));
}
function parseProjectStyle22(value) {
  if (isRecord10(value)) return value;
  const text = textValue(value);
  if (!text) return {};
  try {
    const parsed = JSON.parse(text);
    return isRecord10(parsed) ? parsed : {};
  } catch {
    return {};
  }
}
function arrayValues(value) {
  if (Array.isArray(value)) return value.map(textValue).filter((item) => Boolean(item));
  const text = textValue(value);
  return text ? [text] : [];
}
function arrayRecords(value) {
  return Array.isArray(value) ? value.filter(isRecord10) : [];
}
function uniqueNumbers(values) {
  return Array.from(new Set(values)).sort((left, right) => left - right);
}
function textValue(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function isRecord10(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}

// ../../packages/workspace/dist/chunk-OGANVRDU.js
var WORKSPACE_ENTITY_KIND = {
  project_workspace: "project",
  project_standards_workspace: "project_standards",
  setting_workspace: "setting",
  setting_state_workspace: "setting_state",
  asset_workspace: "asset",
  script_workspace: "script",
  script_version_workspace: "script_version",
  script_block_workspace: "script_block",
  production_workspace: "production",
  segment_workspace: "segment",
  scene_moment_workspace: "scene_moment",
  storyboard_workspace: "storyboard",
  audio_cue_workspace: "audio_cue",
  expression_unit_workspace: "expression_unit",
  content_unit_workspace: "content_unit",
  keyframe_workspace: "keyframe"
};
var MOVSCRIPT_DOMAIN_WORKSPACE_MODELS = {
  project_workspace: {
    kind: "project_workspace",
    title: "Project workspace",
    entityKinds: ["project"],
    editablePathPatterns: ["project.json"],
    contextPathPatterns: [],
    schemaIds: ["movscript.project.v1"],
    instructions: ["Edit the project root entity in project.json. Interpret validates the source tree and writes .derived artifacts."]
  },
  project_standards_workspace: {
    kind: "project_standards_workspace",
    title: "Project standards workspace",
    entityKinds: ["project_standards"],
    editablePathPatterns: ["project_standards.json"],
    contextPathPatterns: ["project.json"],
    schemaIds: ["movscript.project_standards.v1"],
    instructions: ["Edit project-wide creative standards in project_standards.json. Interpret uses these standards when deriving generation context."]
  },
  setting_workspace: {
    kind: "setting_workspace",
    title: "Setting workspace",
    entityKinds: ["setting"],
    editablePathPatterns: ["settings/{settingSlug}/setting.json"],
    contextPathPatterns: ["project.json", "project_standards.json"],
    schemaIds: ["movscript.setting.v1"],
    instructions: ["Use setting workspaces for concrete film/music production entities to be made or reused, such as characters, props, places, instruments, costumes, or voice identities. Do not use settings for abstract styles or rules; put project-wide style/rules in project standards. Child setting namespaces/states are organized by their actual source path; namespace_kind only labels that path level. Assets belong under setting states."]
  },
  setting_state_workspace: {
    kind: "setting_state_workspace",
    title: "Setting state workspace",
    entityKinds: ["setting_state"],
    editablePathPatterns: ["settings/{settingSlug}/states/{settingStateSlug}/setting_state.json"],
    contextPathPatterns: ["settings/{settingSlug}/setting.json", "project_standards.json"],
    schemaIds: ["movscript.setting_state.v1"],
    instructions: ["Use setting state workspaces as namespaces under a setting for named conditions or versions of the same entity, such as base look, wet hair, damaged prop, side-view variant, or calm voice. The default states/ path is a legacy hint; custom setting namespace paths are valid when setting_state.json sits under the intended setting parent."]
  },
  asset_workspace: {
    kind: "asset_workspace",
    title: "Asset workspace",
    entityKinds: ["asset"],
    editablePathPatterns: [
      "settings/{settingSlug}/states/{settingStateSlug}/assets/{assetSlug}/asset.json"
    ],
    contextPathPatterns: ["settings/{settingSlug}/setting.json", "settings/{settingSlug}/states/{settingStateSlug}/setting_state.json", "project_standards.json"],
    schemaIds: ["movscript.asset.v1"],
    instructions: ["Assets are setting-state-owned resource slots and carriers for asset_ref content-unit generation/review. The owning setting state is the nearest setting_state ancestor in the source path, not a fixed states/ directory. Do not write new candidates or selections into asset.json; generated asset candidates belong to the linked asset_ref content unit and backend content-unit decision flow. Existing inline candidates/selection in asset.json are legacy compatibility data only. Downstream content-unit prompts can reference selected asset outputs with {{asset::id}} when dependency tracking matters; loose raw-resource guidance can use direct resource inputs. Image assets should prefer plain white or very clean backgrounds."]
  },
  script_workspace: {
    kind: "script_workspace",
    title: "Script workspace",
    entityKinds: ["script"],
    editablePathPatterns: ["scripts/{scriptSlug}/script.json", "scripts/{scriptSlug}/script.md"],
    contextPathPatterns: ["project.json"],
    schemaIds: ["movscript.script.v1"],
    instructions: ["Use script workspaces for screenplay roots and source text references."]
  },
  script_version_workspace: {
    kind: "script_version_workspace",
    title: "Script version workspace",
    entityKinds: ["script_version"],
    editablePathPatterns: ["scripts/{scriptSlug}/versions/{scriptVersionSlug}/script_version.json"],
    contextPathPatterns: ["scripts/{scriptSlug}/script.json"],
    schemaIds: ["movscript.script_version.v1"],
    instructions: ["Use script version workspaces for versioned script snapshots and block grouping."]
  },
  script_block_workspace: {
    kind: "script_block_workspace",
    title: "Script block workspace",
    entityKinds: ["script_block"],
    editablePathPatterns: ["scripts/{scriptSlug}/versions/{scriptVersionSlug}/blocks/{scriptBlockSlug}/script_block.json"],
    contextPathPatterns: ["scripts/{scriptSlug}/versions/{scriptVersionSlug}/script_version.json"],
    schemaIds: ["movscript.script_block.v1"],
    instructions: ["Use script block workspaces for addressable text blocks referenced by planning entities."]
  },
  production_workspace: {
    kind: "production_workspace",
    title: "Production workspace",
    entityKinds: ["production"],
    editablePathPatterns: ["productions/{productionSlug}/production.json"],
    contextPathPatterns: ["project.json", "project_standards.json", "settings/**", "scripts/**"],
    schemaIds: ["movscript.production.v1"],
    instructions: ["Production records are legacy/default timeline namespace nodes. Edit existing custom timeline namespace nodes in place; for new custom layouts, put production.json under the intended timeline path and set namespace_kind to the user-facing namespace label. Segment and scene moment children hold planning structure through path ancestry."]
  },
  segment_workspace: {
    kind: "segment_workspace",
    title: "Segment workspace",
    entityKinds: ["segment"],
    editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/segment.json"],
    contextPathPatterns: ["productions/{productionSlug}/production.json"],
    schemaIds: ["movscript.segment.v1"],
    instructions: ["Segment records are legacy/default timeline namespace nodes for rhythm sections. Edit existing custom namespace nodes in place; for new custom layouts, put segment.json under the intended timeline parent path and set namespace_kind to the user-facing label. Order lives in segment.json, not the directory name."]
  },
  scene_moment_workspace: {
    kind: "scene_moment_workspace",
    title: "Scene moment workspace",
    entityKinds: ["scene_moment"],
    editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/scene_moment.json"],
    contextPathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/segment.json", "settings/**"],
    schemaIds: ["movscript.scene_moment.v1"],
    instructions: ["Scene moments are stable production primitives and should remain under the intended timeline namespace path. They describe planning context and their own transition boundaries. Shot semantics live as expression_unit records with kind=shot; storyboards and keyframes may be direct scene moment children when they are scene-scoped."]
  },
  storyboard_workspace: {
    kind: "storyboard_workspace",
    title: "Storyboard workspace",
    entityKinds: ["storyboard"],
    editablePathPatterns: [
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/{expressionUnitSlug}/storyboards/{storyboardSlug}/storyboard.json",
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/storyboards/{storyboardSlug}/storyboard.json"
    ],
    contextPathPatterns: ["productions/**", "settings/**", "project_standards.json"],
    schemaIds: ["movscript.storyboard.v1"],
    instructions: ["Storyboards are production primitives under expression_unit(kind=shot) or directly under scene_moment. Runtime candidates and production decisions are stored outside storyboard.json. Downstream prompts can reference selected storyboard outputs with {{storyboard::id}} when dependency tracking matters; loose raw-resource guidance can use direct resource inputs."]
  },
  audio_cue_workspace: {
    kind: "audio_cue_workspace",
    title: "Audio cue workspace",
    entityKinds: ["audio_cue"],
    editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/audio_cues/{audioCueSlug}/audio_cue.json"],
    contextPathPatterns: [
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/scene_moment.json",
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/**",
      "settings/**",
      "project_standards.json"
    ],
    schemaIds: ["movscript.audio_cue.v1"],
    instructions: ["Audio cues are production primitives for sound, music, ambience, dialogue, or foley planning, normally parented by source path under a scene moment and optionally attached by refs to scene moments, storyboards, or shot plans."]
  },
  expression_unit_workspace: {
    kind: "expression_unit_workspace",
    title: "Expression unit workspace",
    entityKinds: ["expression_unit"],
    editablePathPatterns: ["productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/{expressionUnitSlug}/expression_unit.json"],
    contextPathPatterns: [
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/scene_moment.json",
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/**"
    ],
    schemaIds: ["movscript.expression_unit.v1"],
    instructions: ["Expression units are scene-moment-owned form/edit slots. Their owning scene moment is the nearest scene_moment ancestor in the source path. Use slot_kind=visual|voice|subtitle|audio for new records; expression_kind/kind are legacy compatibility hints. Content units target expression_unit_ref and declare output_kind."]
  },
  content_unit_workspace: {
    kind: "content_unit_workspace",
    title: "Content unit workspace",
    entityKinds: ["content_unit"],
    editablePathPatterns: ["content_units/{contentUnitSlug}/content_unit.json"],
    contextPathPatterns: ["project_standards.json", "settings/**", "productions/**"],
    schemaIds: ["movscript.content_unit.v1"],
    instructions: ["Content units are project-level stable production units. They declare content_unit_type, output_kind, flat business refs, edit_prompt, and model_intent. Specialized content_unit_type adapters perform dependency and regeneration checks; unknown types are valid but untracked for regeneration. Use expression_unit_ref for shot expression units, keyframe_ref/storyboard_ref for visual anchors, audio_cue_ref for sound cues, and timeline_assembly_ref for namespace-scope assembly. In edit_prompt, use semantic refs such as {{asset::id}}, {{storyboard::id}}, {{keyframe::id}}, {{audio_cue::id}}, {{candidate::id}}, or {{resource::123}}; prompt compilation resolves selected backend candidates into resource mentions and resource_ids for generation."]
  },
  keyframe_workspace: {
    kind: "keyframe_workspace",
    title: "Keyframe workspace",
    entityKinds: ["keyframe"],
    editablePathPatterns: [
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/expression_units/{expressionUnitSlug}/keyframes/{keyframeSlug}/keyframe.json",
      "productions/{productionSlug}/segments/{segmentSlug}/scene_moments/{sceneMomentSlug}/keyframes/{keyframeSlug}/keyframe.json"
    ],
    contextPathPatterns: ["settings/**", "project_standards.json", "productions/**"],
    schemaIds: ["movscript.keyframe.v1"],
    instructions: ["Keyframes are production primitives and visual anchors under expression_unit(kind=shot) or directly under scene_moment. Content units reference keyframes through keyframe_ref or keyframe_refs."]
  }
};
var ENTITY_WORKSPACE_KIND = Object.entries(WORKSPACE_ENTITY_KIND).reduce((out, [workspaceKind, entityKind]) => {
  out[entityKind] = workspaceKind;
  return out;
}, {});
function getMovScriptDomainWorkspaceModel(kind) {
  const model = MOVSCRIPT_DOMAIN_WORKSPACE_MODELS[kind];
  if (!model) throw new Error(`Unsupported MovScript workspace kind: ${kind}`);
  return model;
}
function resolveMovScriptDomainWorkspaceKindForEntity(entityKind) {
  const normalized = normalizeEntityKind(entityKind);
  return isSemanticEntityKind(normalized) ? ENTITY_WORKSPACE_KIND[normalized] : void 0;
}
function getMovScriptWorkspaceModel(input) {
  const entityKind = normalizeEntityKind(input.entityKind);
  if (!isSemanticEntityKind(entityKind)) throw new Error(`Unsupported MovScript workspace entity kind: ${input.entityKind}`);
  const workspaceKind = resolveMovScriptDomainWorkspaceKindForEntity(entityKind);
  if (!workspaceKind) throw new Error(`Unsupported MovScript workspace entity kind: ${input.entityKind}`);
  const model = getMovScriptDomainWorkspaceModel(workspaceKind);
  return {
    workspaceKind,
    entityKind,
    ...input.entityId !== void 0 ? { entityId: input.entityId } : {},
    editablePaths: expandPathPatterns(model.editablePathPatterns, input),
    contextPaths: model.contextPathPatterns,
    pathSemantics: MOVSCRIPT_DOMAIN_PATH_SEMANTICS,
    schemaIds: model.schemaIds,
    instructions: model.instructions
  };
}
function expandPathPatterns(patterns, input) {
  const slug = input.entityId === void 0 ? void 0 : entityPathSlug(input.entityId, normalizeEntityKind(input.entityKind));
  return patterns.map((pattern) => pattern.replaceAll("{id}", slug ?? "{id}").replaceAll(`{${camelEntityKind(input.entityKind)}Slug}`, slug ?? `{${camelEntityKind(input.entityKind)}Slug}`));
}
function camelEntityKind(entityKind) {
  return normalizeEntityKind(entityKind).replace(/_([a-z])/g, (_match, char) => char.toUpperCase());
}
function normalizeEntityKind(entityKind) {
  return entityKind.trim().replace(/^movscript\./, "").replace(/\.v\d+$/, "");
}
function isSemanticEntityKind(value) {
  return Object.values(WORKSPACE_ENTITY_KIND).some((kind) => kind === value);
}
var RESOURCE_MENTION_RE = /@\[resource:([^\]\s]+)\]|\[\[resource::(\d+)\]\]|\{\{\s*resource:{1,2}\s*([^}]+?)\s*\}\}/g;
var RESOURCE_MEDIA_TYPES = /* @__PURE__ */ new Set(["image", "video", "audio", "text", "any"]);
function formatResourceMention(resourceId, options = {}) {
  const id = String(resourceId);
  const mediaType = normalizeMentionPart(options.mediaType);
  const role = normalizeMentionPart(options.role);
  if (mediaType && role) return `@[resource:${mediaType}:${role}:${id}]`;
  if (mediaType) return `@[resource:${mediaType}:${id}]`;
  if (role) return `@[resource:${role}:${id}]`;
  return `@[resource:${id}]`;
}
function parseResourceMentions(text) {
  const mentions = [];
  for (const match of (text ?? "").matchAll(RESOURCE_MENTION_RE)) {
    const index = match.index;
    if (index === void 0) continue;
    const parsed = match[1] ? parseResourceMentionPayload(match[1]) : match[3] ? parseLegacyResourceMentionPayload(match[3]) : { id: Number(match[2]) };
    const id = parsed.id;
    if (!Number.isInteger(id) || id <= 0) continue;
    mentions.push({
      id,
      token: match[0],
      index,
      ...parsed.mediaType ? { mediaType: parsed.mediaType } : {},
      ...parsed.role ? { role: parsed.role } : {}
    });
  }
  return mentions;
}
function resourceIdsFromMentions(text) {
  const ids = [];
  const seen = /* @__PURE__ */ new Set();
  for (const mention of parseResourceMentions(text)) {
    if (seen.has(mention.id)) continue;
    seen.add(mention.id);
    ids.push(mention.id);
  }
  return ids;
}
function stripResourceMentions(text) {
  return text.replace(RESOURCE_MENTION_RE, "").replace(/[ \t]{2,}/g, " ").replace(/[ \t]+\n/g, "\n").replace(/\n{3,}/g, "\n\n").trim();
}
function parseResourceMentionPayload(payload) {
  const parts = payload.split(":").map((part) => normalizeMentionPart(part)).filter((part) => Boolean(part));
  if (parts.length === 0) return { id: 0 };
  const id = Number(parts[parts.length - 1]);
  if (!Number.isInteger(id) || id <= 0) return { id: 0 };
  const descriptors = parts.slice(0, -1);
  let mediaType = "";
  let role = "";
  if (descriptors.length === 1) {
    const first = descriptors[0] ?? "";
    if (RESOURCE_MEDIA_TYPES.has(first)) {
      mediaType = first;
    } else {
      role = first;
    }
  } else if (descriptors.length > 1) {
    const first = descriptors[0] ?? "";
    if (RESOURCE_MEDIA_TYPES.has(first)) {
      mediaType = first;
      role = descriptors.slice(1).join(":");
    } else {
      role = descriptors.join(":");
    }
  }
  return {
    id,
    ...mediaType ? { mediaType } : {},
    ...role ? { role } : {}
  };
}
function parseLegacyResourceMentionPayload(payload) {
  const parts = payload.trim().split(/\s+/).filter(Boolean);
  const id = Number(parts.shift());
  if (!Number.isInteger(id) || id <= 0) return { id: 0 };
  let mediaType = "";
  let role = "";
  for (const part of parts) {
    const match = part.match(/^([a-zA-Z_][a-zA-Z0-9_-]*)=(.+)$/);
    if (!match) continue;
    const key = normalizeMentionPart(match[1]);
    const value = normalizeMentionPart(match[2]);
    if (!value) continue;
    if (key === "role") role = value;
    if (key === "media" || key === "media_type" || key === "mediatype") mediaType = value;
  }
  return {
    id,
    ...mediaType ? { mediaType } : {},
    ...role ? { role } : {}
  };
}
function normalizeMentionPart(value) {
  return (value ?? "").trim().toLowerCase().replace(/[^a-z0-9_-]+/g, "_").replace(/^_+|_+$/g, "");
}
function deriveMovScriptWorkspacePreviewTimelines(index) {
  const sourceEntities = canonicalEntities(index);
  const contentUnitsByPrimaryRef = groupContentUnitsByPrimaryRef2(index);
  return sourceEntities.filter(isProductionWithId).map((production) => {
    const productionDir = entityDir4(production.path);
    const segments = childEntities(index, productionDir, "segment");
    const items = [];
    let order = 0;
    for (const segment of sortEntities2(segments)) {
      const segmentItemId = timelineItemId(segment);
      items.push({
        ...timelineItem(segmentItemId, "segment", segment, order++),
        transition: recordField2(segment.record.transition)
      });
      const sceneMoments = childEntities(index, entityDir4(segment.path), "scene_moment");
      for (const sceneMoment of sortEntities2(sceneMoments)) {
        const sceneMomentItemId = timelineItemId(sceneMoment);
        const sceneMomentContentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "scene_moment", sceneMoment);
        items.push({
          ...timelineItem(sceneMomentItemId, "scene_moment", sceneMoment, order++),
          parentId: segmentItemId,
          transition: recordField2(sceneMoment.record.transition),
          contentUnitIds: sceneMomentContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
        });
        for (const contentUnit of sortEntities2(sceneMomentContentUnits)) {
          items.push({
            ...timelineItem(timelineItemId(contentUnit), "content_unit", contentUnit, order++),
            parentId: sceneMomentItemId
          });
        }
        for (const storyboard of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "storyboard"))) {
          const storyboardItemId = timelineItemId(storyboard);
          const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "storyboard", storyboard);
          const timeline = recordField2(storyboard.record.timeline);
          items.push({
            ...timelineItem(storyboardItemId, "storyboard", storyboard, order++),
            parentId: sceneMomentItemId,
            caption: stringField11(timeline?.caption),
            gapAfterSec: numberField6(timeline?.gap_after_sec),
            timing: timeline,
            transition: recordField2(storyboard.record.transition),
            contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
          });
          for (const contentUnit of sortEntities2(contentUnits)) {
            items.push({
              ...timelineItem(timelineItemId(contentUnit), "content_unit", contentUnit, order++),
              parentId: storyboardItemId
            });
          }
        }
        for (const keyframe of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "keyframe"))) {
          const keyframeItemId = timelineItemId(keyframe);
          const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "keyframe", keyframe);
          items.push({
            ...timelineItem(keyframeItemId, "keyframe", keyframe, order++),
            parentId: sceneMomentItemId,
            timing: recordField2(keyframe.record.timing),
            contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
          });
          for (const contentUnit of sortEntities2(contentUnits)) {
            items.push({
              ...timelineItem(timelineItemId(contentUnit), "content_unit", contentUnit, order++),
              parentId: keyframeItemId
            });
          }
        }
        for (const expressionUnit of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "expression_unit"))) {
          const expressionUnitItemId = timelineItemId(expressionUnit);
          const expressionUnitContentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "expression_unit", expressionUnit);
          items.push({
            ...timelineItem(expressionUnitItemId, "expression_unit", expressionUnit, order++),
            parentId: sceneMomentItemId,
            contentUnitIds: expressionUnitContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
          });
          for (const contentUnit of sortEntities2(expressionUnitContentUnits)) {
            items.push({
              ...timelineItem(timelineItemId(contentUnit), "content_unit", contentUnit, order++),
              parentId: expressionUnitItemId
            });
          }
          for (const storyboard of sortEntities2(childEntities(index, entityDir4(expressionUnit.path), "storyboard"))) {
            const storyboardItemId = timelineItemId(storyboard);
            const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "storyboard", storyboard);
            const timeline = recordField2(storyboard.record.timeline);
            items.push({
              ...timelineItem(storyboardItemId, "storyboard", storyboard, order++),
              parentId: expressionUnitItemId,
              caption: stringField11(timeline?.caption),
              gapAfterSec: numberField6(timeline?.gap_after_sec),
              timing: timeline,
              transition: recordField2(storyboard.record.transition),
              contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
            });
            for (const contentUnit of sortEntities2(contentUnits)) {
              items.push({
                ...timelineItem(timelineItemId(contentUnit), "content_unit", contentUnit, order++),
                parentId: storyboardItemId
              });
            }
          }
          for (const keyframe of sortEntities2(childEntities(index, entityDir4(expressionUnit.path), "keyframe"))) {
            const keyframeItemId = timelineItemId(keyframe);
            const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "keyframe", keyframe);
            items.push({
              ...timelineItem(keyframeItemId, "keyframe", keyframe, order++),
              parentId: expressionUnitItemId,
              timing: recordField2(keyframe.record.timing),
              contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
            });
            for (const contentUnit of sortEntities2(contentUnits)) {
              items.push({
                ...timelineItem(timelineItemId(contentUnit), "content_unit", contentUnit, order++),
                parentId: keyframeItemId
              });
            }
          }
        }
        for (const audioCue of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "audio_cue"))) {
          items.push({
            ...timelineItem(timelineItemId(audioCue), "audio_cue", audioCue, order++),
            parentId: sceneMomentItemId,
            cueKind: stringField11(audioCue.record.cue_kind),
            timing: recordField2(audioCue.record.timing)
          });
        }
      }
    }
    return {
      schema: "movscript.preview_timeline.v1",
      productionId: production.id,
      productionPath: productionDir,
      items
    };
  });
}
function deriveMovScriptWorkspaceTimelineAssemblyPreviewTimeline(index, scope) {
  const parsedTarget = parseImplicitTimelineAssemblyRef(scope.targetRef);
  const scopeKind = parsedTarget?.scopeKind ?? scope.scopeKind;
  const scopeRef = parsedTarget?.scopeRef ?? String(scope.scopeRef);
  const scopeNode = timelineNamespaceNodeForScope(index, scopeKind, scopeRef);
  if (!scopeNode?.path) return void 0;
  const scopeEntity = canonicalEntities(index).find((entity) => entity.path === scopeNode.path);
  const scopeDir = entityDir4(scopeNode.path);
  const contentUnitsByPrimaryRef = groupContentUnitsByPrimaryRef2(index);
  const items = [];
  let order = 0;
  const appendTimelineNamespace = (namespace, parentId) => {
    const namespaceItemId = timelineNamespaceItemId(namespace);
    items.push({
      ...timelineItem(namespaceItemId, "timeline_namespace", namespace, order++),
      ...parentId ? { parentId } : {}
    });
    appendNamespaceChildren(entityDir4(namespace.path), namespaceItemId);
  };
  const appendSceneMoment = (sceneMoment, parentId) => {
    const sceneMomentItemId = timelineItemId(sceneMoment);
    const sceneMomentContentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "scene_moment", sceneMoment);
    items.push({
      ...timelineItem(sceneMomentItemId, "scene_moment", sceneMoment, order++),
      ...parentId ? { parentId } : {},
      transition: recordField2(sceneMoment.record.transition),
      contentUnitIds: sceneMomentContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
    });
    appendContentUnits(sceneMomentContentUnits, sceneMomentItemId);
    appendSceneMomentChildren(sceneMoment, sceneMomentItemId);
  };
  const appendContentUnits = (contentUnits, parentId) => {
    for (const contentUnit of sortEntities2(contentUnits)) {
      items.push({
        ...timelineItem(timelineItemId(contentUnit), "content_unit", contentUnit, order++),
        parentId
      });
    }
  };
  const appendSceneMomentChildren = (sceneMoment, sceneMomentItemId) => {
    for (const storyboard of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "storyboard"))) {
      const storyboardItemId = timelineItemId(storyboard);
      const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "storyboard", storyboard);
      const timeline = recordField2(storyboard.record.timeline);
      items.push({
        ...timelineItem(storyboardItemId, "storyboard", storyboard, order++),
        parentId: sceneMomentItemId,
        caption: stringField11(timeline?.caption),
        gapAfterSec: numberField6(timeline?.gap_after_sec),
        timing: timeline,
        transition: recordField2(storyboard.record.transition),
        contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
      });
      appendContentUnits(contentUnits, storyboardItemId);
    }
    for (const keyframe of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "keyframe"))) {
      const keyframeItemId = timelineItemId(keyframe);
      const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "keyframe", keyframe);
      items.push({
        ...timelineItem(keyframeItemId, "keyframe", keyframe, order++),
        parentId: sceneMomentItemId,
        timing: recordField2(keyframe.record.timing),
        contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
      });
      appendContentUnits(contentUnits, keyframeItemId);
    }
    for (const expressionUnit of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "expression_unit"))) {
      const expressionUnitItemId = timelineItemId(expressionUnit);
      const expressionUnitContentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "expression_unit", expressionUnit);
      items.push({
        ...timelineItem(expressionUnitItemId, "expression_unit", expressionUnit, order++),
        parentId: sceneMomentItemId,
        contentUnitIds: expressionUnitContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
      });
      appendContentUnits(expressionUnitContentUnits, expressionUnitItemId);
      for (const storyboard of sortEntities2(childEntities(index, entityDir4(expressionUnit.path), "storyboard"))) {
        const storyboardItemId = timelineItemId(storyboard);
        const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "storyboard", storyboard);
        const timeline = recordField2(storyboard.record.timeline);
        items.push({
          ...timelineItem(storyboardItemId, "storyboard", storyboard, order++),
          parentId: expressionUnitItemId,
          caption: stringField11(timeline?.caption),
          gapAfterSec: numberField6(timeline?.gap_after_sec),
          timing: timeline,
          transition: recordField2(storyboard.record.transition),
          contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
        });
        appendContentUnits(contentUnits, storyboardItemId);
      }
      for (const keyframe of sortEntities2(childEntities(index, entityDir4(expressionUnit.path), "keyframe"))) {
        const keyframeItemId = timelineItemId(keyframe);
        const contentUnits = contentUnitsForEntity(contentUnitsByPrimaryRef, "keyframe", keyframe);
        items.push({
          ...timelineItem(keyframeItemId, "keyframe", keyframe, order++),
          parentId: expressionUnitItemId,
          timing: recordField2(keyframe.record.timing),
          contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined2)
        });
        appendContentUnits(contentUnits, keyframeItemId);
      }
    }
    for (const audioCue of sortEntities2(childEntities(index, entityDir4(sceneMoment.path), "audio_cue"))) {
      items.push({
        ...timelineItem(timelineItemId(audioCue), "audio_cue", audioCue, order++),
        parentId: sceneMomentItemId,
        cueKind: stringField11(audioCue.record.cue_kind),
        timing: recordField2(audioCue.record.timing)
      });
    }
  };
  const appendNamespaceChildren = (parentDir, parentId) => {
    for (const namespace of sortEntities2(childEntities(index, parentDir, "segment"))) {
      appendTimelineNamespace(namespace, parentId);
    }
    for (const sceneMoment of sortEntities2(childEntities(index, parentDir, "scene_moment"))) {
      appendSceneMoment(sceneMoment, parentId);
    }
  };
  appendNamespaceChildren(scopeDir);
  return {
    schema: "movscript.preview_timeline.v1",
    targetKind: "timeline_assembly",
    targetRef: scope.targetRef ?? implicitTimelineAssemblyRef(scopeKind, scopeRef),
    scopeKind,
    scopeRef,
    scopePath: scopeDir,
    scopeTitle: scopeNode.title,
    ...scopeEntity?.entityKind === "production" && scopeEntity.id !== void 0 ? {
      productionId: scopeEntity.id,
      productionPath: scopeDir
    } : {},
    items
  };
}
function groupContentUnitsByPrimaryRef2(index) {
  const out = /* @__PURE__ */ new Map();
  for (const entity of canonicalEntities(index)) {
    if (entity.entityKind !== "content_unit") continue;
    const contentUnitType2 = stringField11(entity.record.content_unit_type);
    if (!contentUnitType2 || !hasSpecializedContentUnitType(contentUnitType2)) continue;
    const assemblyScope = timelineAssemblyScopeFromContentUnitRecord(entity.record);
    if (assemblyScope) {
      for (const key of primaryRefKeys2(assemblyScope.kind, assemblyScope.ref)) {
        out.set(key, [...out.get(key) ?? [], entity]);
      }
    }
    const primaryKind = primaryRefKindForContentUnitType(contentUnitType2);
    if (!primaryKind) continue;
    const primaryRefs = primaryRefIdsForContentUnitRecord(entity.record, primaryKind);
    if (primaryRefs.length !== 1) continue;
    const primaryRef = primaryRefs[0];
    if (!primaryRef) continue;
    for (const key of primaryRefKeys2(primaryKind, primaryRef)) {
      out.set(key, [...out.get(key) ?? [], entity]);
    }
  }
  return out;
}
function contentUnitsForEntity(contentUnitsByPrimaryRef, entityKind, entity) {
  if (entity.id === void 0) return [];
  return contentUnitsByPrimaryRef.get(primaryRefKey2(entityKind, entity.id)) ?? [];
}
function primaryRefKey2(kind, id) {
  return `${kind}:${String(id)}`;
}
function primaryRefKeys2(kind, ref) {
  const value = String(ref);
  const keys = [primaryRefKey2(kind, value)];
  const lastSegment = value.split("/").filter(Boolean).at(-1);
  if (lastSegment && lastSegment !== value) keys.push(primaryRefKey2(kind, lastSegment));
  return keys;
}
function timelineItem(id, itemType, entity, order) {
  return {
    id,
    itemType,
    entity: entityRef2(entity),
    order,
    title: stringField11(entity.record.title) ?? String(entity.id ?? entity.path)
  };
}
function timelineItemId(entity) {
  return `${entity.entityKind}:${String(entity.id ?? entity.path)}`;
}
function timelineNamespaceItemId(entity) {
  return `timeline_namespace:${String(entity.id ?? entity.path)}`;
}
function entityRef2(entity) {
  return {
    entityKind: entity.entityKind,
    ...entity.id !== void 0 ? { id: entity.id } : {},
    path: entity.path
  };
}
function childEntities(index, parentDir, entityKind) {
  const collectionName = collectionDirForEntityKind(entityKind);
  if (!collectionName) return [];
  return canonicalEntities(index).filter((entity) => entity.entityKind === entityKind && entity.path.startsWith(`${parentDir}/${collectionName}/`) && entityDir4(entity.path).replace(`${parentDir}/${collectionName}/`, "").split("/").length === 1);
}
function collectionDirForEntityKind(entityKind) {
  if (entityKind === "segment") return "segments";
  if (entityKind === "scene_moment") return "scene_moments";
  if (entityKind === "storyboard") return "storyboards";
  if (entityKind === "keyframe") return "keyframes";
  if (entityKind === "audio_cue") return "audio_cues";
  if (entityKind === "expression_unit") return "expression_units";
  return void 0;
}
function canonicalEntities(index) {
  return index.entities;
}
function isProductionWithId(entity) {
  return entity.entityKind === "production" && entity.id !== void 0;
}
function timelineNamespaceNodeForScope(index, scopeKind, scopeRef) {
  const normalizedRef = normalizeRef(scopeRef);
  return index.domainNodes.find((node) => {
    if (node.category !== "timeline_namespace") return false;
    if (node.kind !== scopeKind && node.metadata?.entityKind !== scopeKind) return false;
    const nodePath = node.path ? normalizeRef(node.path) : void 0;
    const nodeDir = node.path ? normalizeRef(entityDir4(node.path)) : void 0;
    return normalizeRef(node.id) === normalizedRef || nodePath === normalizedRef || nodeDir === normalizedRef;
  });
}
function normalizeRef(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  if (typeof value !== "string" || !value.trim()) return void 0;
  return value.trim().replace(/^\/+|\/+$/g, "");
}
function sortEntities2(entities) {
  return [...entities].sort((left, right) => {
    const leftOrder = numberField6(left.record.order) ?? Number.MAX_SAFE_INTEGER;
    const rightOrder = numberField6(right.record.order) ?? Number.MAX_SAFE_INTEGER;
    if (leftOrder !== rightOrder) return leftOrder - rightOrder;
    return left.path.localeCompare(right.path);
  });
}
function entityDir4(path) {
  return path.replace(/\/[^/]+$/, "");
}
function recordField2(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value) ? value : void 0;
}
function stringField11(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberField6(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function isDefined2(value) {
  return value !== void 0;
}
function createMovScriptWorkspaceService(options) {
  const domainRepository = createMovScriptWorkspaceDomainRepository({
    fileRepository: options.fileRepository
  });
  const indexCacheTtlMs = options.indexCacheTtlMs ?? 2e3;
  const indexCache = /* @__PURE__ */ new Map();
  const invalidateIndexCache = () => {
    indexCache.clear();
  };
  const loadIndex = async (input) => {
    const cacheKey = normalizeWorkspacePath(input?.path ?? "");
    const nowMs = Date.now();
    const cached = indexCache.get(cacheKey);
    if (cached?.index && cached.expiresAt > nowMs) return cached.index;
    if (cached?.promise) return cached.promise;
    const promise = (async () => {
      const documents = await domainRepository.loadDocuments(input);
      const decisionDocuments = await overlayMovScriptDecisionDocuments(documents, options.decisionStore);
      return deriveMovScriptWorkspaceDomainIndex(decisionDocuments);
    })();
    indexCache.set(cacheKey, { expiresAt: 0, promise });
    try {
      const index = await promise;
      indexCache.set(cacheKey, {
        expiresAt: Date.now() + Math.max(0, indexCacheTtlMs),
        index
      });
      return index;
    } catch (error) {
      if (indexCache.get(cacheKey)?.promise === promise) indexCache.delete(cacheKey);
      throw error;
    }
  };
  const invalidateAfter = async (operation) => {
    const result = await operation();
    invalidateIndexCache();
    return result;
  };
  return {
    async initializeProject(input = {}) {
      const now = options.now?.() ?? /* @__PURE__ */ new Date();
      const createdAt = now.toISOString();
      const existingWorkspace = await readJSONArtifact(options.fileRepository, "workspace.json");
      const title = stringField25(input.title) ?? "MovScript Project";
      const projectId = stringField25(input.projectId) ?? stringField25(existingWorkspace?.project_id) ?? safeProjectSlug(title);
      const projectUid = stringField25(input.projectUid) ?? stringField25(existingWorkspace?.project_uid) ?? stringField25(existingWorkspace?.projectUid) ?? createMovScriptProjectUid();
      const standardsRecord = {
        schema: "movscript.project_standards.v1",
        kind: "project_standards",
        id: "project_standards",
        project_id: projectId,
        title: "Project standards",
        ...input.standards ?? {},
        updated_at: createdAt
      };
      const files = [
        await ensureMovScriptGitignore(options.fileRepository),
        await writeJSONDocument(options.fileRepository, ".movscript/config.json", {
          schema: "movscript.local_project_config.v1",
          project_uid: projectUid,
          remotes: {},
          created_at: createdAt,
          updated_at: createdAt
        }, Boolean(input.overwrite)),
        await writeJSONDocument(options.fileRepository, "workspace.json", {
          schema: "movscript.workspace.v2",
          project_uid: projectUid,
          project_id: projectId,
          title,
          created_at: stringField25(existingWorkspace?.created_at) ?? createdAt,
          updated_at: createdAt
        }, Boolean(input.overwrite)),
        await writeJSONDocument(options.fileRepository, "project.json", {
          schema: "movscript.project.v1",
          kind: "project",
          project_id: projectId,
          title,
          language: stringField25(input.language),
          created_at: createdAt,
          updated_at: createdAt
        }, Boolean(input.overwrite)),
        await writeJSONDocument(options.fileRepository, "project_standards.json", standardsRecord, Boolean(input.overwrite))
      ];
      const standardSkillFiles = await syncMovScriptProjectStandardSkills({
        fileRepository: options.fileRepository,
        standards: standardsRecord
      });
      invalidateIndexCache();
      return { projectId, projectUid, files, standardSkillFiles };
    },
    getModel: getMovScriptWorkspaceModel,
    loadIndex,
    async queryEntities(query = {}) {
      return queryMovScriptWorkspaceEntities(await loadIndex(), query);
    },
    async querySettings(query = {}) {
      return queryMovScriptWorkspaceSettings(await loadIndex(), query);
    },
    async queryAssets(query = {}) {
      return queryMovScriptWorkspaceAssets(await loadIndex(), query);
    },
    async queryProductionContext(query = {}) {
      return queryMovScriptWorkspaceProductionContext(await loadIndex(), query);
    },
    readEditorState() {
      return readJSONArtifact(options.fileRepository, MOVSCRIPT_EDITOR_STATE_PATH);
    },
    async readPreviewTimeline(productionId) {
      const timelines = deriveMovScriptWorkspacePreviewTimelines(await loadIndex());
      return timelines.find((timeline) => samePreviewTimelineProduction(timeline.productionId, productionId));
    },
    async readTimelineAssemblyPreviewTimeline(scope) {
      return deriveMovScriptWorkspaceTimelineAssemblyPreviewTimeline(await loadIndex(), scope);
    },
    async readSceneMomentEditPlan(sceneMomentId) {
      const sceneMoment = queryMovScriptWorkspaceEntities(await loadIndex(), { entityKind: "scene_moment" }).find((entity) => sameEntityId(entity.id, sceneMomentId) || entity.path.includes(`/scene_moments/${entityPathSlug(sceneMomentId, "scene_moment")}/`));
      if (!sceneMoment) return void 0;
      return readJSONArtifact(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${sceneMoment.path.replace(/\/scene_moment\.json$/, "")}/edit_plan.json`);
    },
    readContentUnitRuntimePanel(contentUnitId) {
      return readJSONArtifact(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR}/content_units/${entityPathSlug(contentUnitId, "content_unit")}/runtime_panel.json`);
    },
    readContentUnitGenerationPrompt(contentUnitId) {
      return readJSONArtifact(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR}/content_units/${entityPathSlug(contentUnitId, "content_unit")}/generation_prompt.json`);
    },
    readContentUnitDependencyReport(contentUnitId) {
      return readJSONArtifact(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR}/content_units/${entityPathSlug(contentUnitId, "content_unit")}/dependency_report.json`);
    },
    readContentUnitSelectionValidity(contentUnitId) {
      return readJSONArtifact(options.fileRepository, `${MOVSCRIPT_INTERPRET_CURRENT_DIR}/content_units/${entityPathSlug(contentUnitId, "content_unit")}/selection_validity.json`);
    },
    async upsertSetting(input) {
      const payload = await writePayloadWithGeneratedEntityId(loadIndex, "setting", input);
      return invalidateAfter(() => upsertMovScriptWorkspaceSetting({
        fileRepository: options.fileRepository,
        now: options.now?.(),
        ...input,
        payload
      }));
    },
    async upsertSettingState(input) {
      const payload = await writePayloadWithGeneratedEntityId(loadIndex, "setting_state", input);
      return invalidateAfter(() => upsertMovScriptWorkspaceSettingState({
        fileRepository: options.fileRepository,
        now: options.now?.(),
        ...input,
        payload
      }));
    },
    async upsertAsset(input) {
      const payload = await writePayloadWithGeneratedEntityId(loadIndex, "asset", input);
      return invalidateAfter(() => upsertMovScriptWorkspaceAsset({
        fileRepository: options.fileRepository,
        now: options.now?.(),
        ...input,
        payload
      }));
    },
    upsertScript(input) {
      return invalidateAfter(() => upsertMovScriptWorkspaceScript({
        fileRepository: options.fileRepository,
        now: options.now?.(),
        ...input
      }));
    },
    readScriptSource(input) {
      return readMovScriptWorkspaceScriptSource({
        fileRepository: options.fileRepository,
        ...input
      });
    },
    saveProductionSnapshot(input) {
      return invalidateAfter(() => saveMovScriptProductionWorkspaceSnapshot({
        fileRepository: options.fileRepository,
        now: options.now?.(),
        ...input
      }));
    },
    deleteEntity(input) {
      return invalidateAfter(() => deleteMovScriptWorkspaceEntity({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    snapshotScriptVersionFromMarkdown(input) {
      return invalidateAfter(() => snapshotMovScriptVersionFromMarkdown({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    updateContentUnitEditPrompt(input) {
      return invalidateAfter(() => updateMovScriptContentUnitEditPrompt({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    async upsertContentUnit(input) {
      const unit = await contentUnitWithGeneratedEntityId(loadIndex, input.unit);
      return invalidateAfter(() => upsertMovScriptContentUnit({
        fileRepository: options.fileRepository,
        ...input,
        unit
      }));
    },
    async upsertProjectStandards(input) {
      const result = await invalidateAfter(() => upsertMovScriptProjectStandards({
        fileRepository: options.fileRepository,
        now: options.now?.(),
        ...input
      }));
      const standardSkillFiles = await syncMovScriptProjectStandardSkills({
        fileRepository: options.fileRepository,
        standards: result.record
      });
      invalidateIndexCache();
      return { ...result, standardSkillFiles };
    },
    updateEntityTransition(input) {
      return invalidateAfter(() => updateMovScriptEntityTransition({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    updateStoryboardTimeline(input) {
      return invalidateAfter(() => updateMovScriptStoryboardTimeline({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    upsertSourceRecord(input) {
      return invalidateAfter(() => upsertMovScriptSourceRecord({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    updateExpressionUnitSource(input) {
      return invalidateAfter(() => updateMovScriptExpressionUnit(options.fileRepository, input));
    },
    updateAudioCueSource(input) {
      return invalidateAfter(() => updateMovScriptAudioCue(options.fileRepository, input));
    },
    appendCandidate(input) {
      return invalidateAfter(() => appendMovScriptInlineCandidate({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    async createContentCandidate(input) {
      const promptSnapshot = mergePromptSnapshots(
        await readContentUnitGenerationPromptArtifact(options.fileRepository, input.contentUnitId),
        input.promptSnapshot
      );
      if (options.decisionStore) {
        const result = buildMovScriptContentCandidate({
          ...input,
          ...promptSnapshot !== void 0 ? { promptSnapshot } : {}
        });
        await options.decisionStore.upsertContentUnitCandidate({
          contentUnitId: input.contentUnitId,
          candidate: result.record
        });
        invalidateIndexCache();
        return result;
      }
      throw new Error("content unit candidate creation requires a backend scoped project data decisionStore");
    },
    async selectContentUnitCandidate(input) {
      const decisionStore = options.decisionStore;
      if (!decisionStore) {
        throw new Error("content unit candidate selection requires a backend scoped project data decisionStore");
      }
      const candidate = await readBackendContentCandidateRecord(decisionStore, input.contentUnitId, input.candidateId);
      const resourceId = input.resourceId ?? firstCandidateResourceId(candidate);
      const context = await decisionStore.selectContentUnitCandidate({
        contentUnitId: input.contentUnitId,
        candidateId: input.candidateId,
        ...resourceId !== void 0 ? { resourceId } : {},
        stalePolicy: input.stalePolicy,
        reason: input.reason,
        selectedAt: input.selectedAt
      });
      invalidateIndexCache();
      return {
        path: contentUnitDecisionContextPath(input.contentUnitId),
        record: normalizeDecisionContext(context),
        context
      };
    },
    async decideContentUnitCandidate(input) {
      const decisionStore = options.decisionStore;
      if (!decisionStore) {
        throw new Error("content unit candidate decision requires a backend scoped project data decisionStore");
      }
      if (input.decision === "adopt") {
        const candidate2 = await readBackendContentCandidateRecord(decisionStore, input.contentUnitId, input.candidateId);
        const resourceId = input.resourceId ?? firstCandidateResourceId(candidate2);
        const context2 = await decisionStore.selectContentUnitCandidate({
          contentUnitId: input.contentUnitId,
          candidateId: input.candidateId,
          ...resourceId !== void 0 ? { resourceId } : {},
          stalePolicy: input.stalePolicy,
          reason: input.reason,
          selectedAt: input.decidedAt,
          metadata: input.metadata
        });
        invalidateIndexCache();
        return {
          path: contentUnitDecisionContextPath(input.contentUnitId),
          record: normalizeDecisionContext(context2),
          context: context2
        };
      }
      const candidate = await readBackendContentCandidateRecord(decisionStore, input.contentUnitId, input.candidateId);
      if (!candidate) throw new Error(`candidate not found: ${String(input.candidateId)}`);
      const decidedAt = input.decidedAt ?? options.now?.().toISOString();
      const context = await decisionStore.upsertContentUnitCandidate({
        contentUnitId: input.contentUnitId,
        candidate: pruneUndefined12({
          ...candidate,
          decision_status: input.decision,
          decision_reason: input.reason,
          decided_at: decidedAt,
          decision_metadata: input.metadata
        })
      });
      invalidateIndexCache();
      return {
        path: contentUnitDecisionContextPath(input.contentUnitId),
        record: normalizeDecisionContext(context),
        context
      };
    },
    createAssetSlotCandidate(input) {
      return invalidateAfter(() => createMovScriptWorkspaceAssetSlotCandidate({
        fileRepository: options.fileRepository,
        projectPath: input.projectPath ?? "",
        ...input
      }));
    },
    createKeyframeCandidate(input) {
      return invalidateAfter(() => createMovScriptWorkspaceKeyframeCandidate({
        fileRepository: options.fileRepository,
        projectPath: input.projectPath ?? "",
        ...input
      }));
    },
    selectCandidate(input) {
      return invalidateAfter(() => selectMovScriptInlineCandidate({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    updateCandidate(input) {
      return invalidateAfter(() => updateMovScriptInlineCandidate({
        fileRepository: options.fileRepository,
        ...input
      }));
    },
    unlockCandidate(input) {
      return invalidateAfter(() => unlockMovScriptInlineCandidate({
        fileRepository: options.fileRepository,
        ...input
      }));
    }
  };
}
async function readJSONArtifact(fileRepository, path) {
  const file = await fileRepository.read({ path: normalizeWorkspacePath(path) }).catch(() => void 0);
  if (!file) return void 0;
  const parsed = JSON.parse(file.content);
  return isRecord11(parsed) ? parsed : void 0;
}
async function updateMovScriptExpressionUnit(fileRepository, input) {
  const normalizedPath = normalizeWorkspacePath(input.targetPath);
  const existing = await readJSONArtifact(fileRepository, normalizedPath);
  if (!existing) throw new Error(`Expression unit source not found: ${normalizedPath}`);
  if (existing.kind !== "expression_unit") throw new Error(`Target is not an expression_unit: ${normalizedPath}`);
  const record = pruneUndefined12({
    ...existing,
    schema: stringField25(existing.schema) ?? "movscript.expression_unit.v1",
    kind: "expression_unit",
    title: input.patch.title !== void 0 ? input.patch.title : existing.title,
    slot_kind: input.patch.slotKind !== void 0 ? normalizeExpressionUnitSlotKind(input.patch.slotKind) : normalizeExpressionUnitSlotKind(existing.slot_kind ?? existing.slotKind ?? existing.expression_kind ?? existing.kind),
    expression_kind: input.patch.expressionKind !== void 0 ? legacyExpressionKind(input.patch.expressionKind, stringField25(existing.expression_kind)) : existing.expression_kind,
    speaker: input.patch.speaker !== void 0 ? input.patch.speaker : existing.speaker,
    text: input.patch.text !== void 0 ? input.patch.text : existing.text,
    note: input.patch.note !== void 0 ? input.patch.note : existing.note,
    intent: input.patch.intent !== void 0 ? input.patch.intent : existing.intent
  });
  await fileRepository.write({
    path: normalizedPath,
    content: `${JSON.stringify(record, null, 2)}
`
  });
  return { path: normalizedPath, record };
}
function legacyExpressionKind(value, fallback) {
  const text = String(value ?? "").trim().toLowerCase();
  if (text === "dialogue" || text === "narration" || text === "subtitle" || text === "caption" || text === "action" || text === "visual_note" || text === "shot") return text;
  if (text === "voice" || text === "verbal" || text === "voiceover") return "dialogue";
  if (text === "visual" || text === "image" || text === "video") return "visual_note";
  if (text === "audio" || text === "sound" || text === "sound_effect" || text === "sfx" || text === "music" || text === "ambience") return "action";
  return fallback ?? "visual_note";
}
async function updateMovScriptAudioCue(fileRepository, input) {
  const normalizedPath = normalizeWorkspacePath(input.targetPath);
  const existing = await readJSONArtifact(fileRepository, normalizedPath);
  if (!existing) throw new Error(`Audio cue source not found: ${normalizedPath}`);
  if (existing.kind !== "audio_cue") throw new Error(`Target is not an audio_cue: ${normalizedPath}`);
  const record = pruneUndefined12({
    ...existing,
    schema: stringField25(existing.schema) ?? "movscript.audio_cue.v1",
    kind: "audio_cue",
    title: input.patch.title !== void 0 ? input.patch.title : existing.title,
    cue_kind: input.patch.cueKind !== void 0 ? input.patch.cueKind : existing.cue_kind,
    expression_unit_ref: input.patch.expressionUnitRef !== void 0 ? input.patch.expressionUnitRef : existing.expression_unit_ref,
    storyboard_ref: input.patch.storyboardRef !== void 0 ? input.patch.storyboardRef : existing.storyboard_ref,
    timing: input.patch.timing !== void 0 ? input.patch.timing : existing.timing,
    prompt_hint: input.patch.promptHint !== void 0 ? input.patch.promptHint : existing.prompt_hint,
    asset_refs: input.patch.assetRefs !== void 0 ? input.patch.assetRefs : existing.asset_refs
  });
  await fileRepository.write({
    path: normalizedPath,
    content: `${JSON.stringify(record, null, 2)}
`
  });
  return { path: normalizedPath, record };
}
function samePreviewTimelineProduction(left, right) {
  return String(left) === String(right) || entityPathSlug(left, "production") === entityPathSlug(right, "production");
}
function contentUnitDerivedArtifactPath(contentUnitId, filename) {
  return `${MOVSCRIPT_INTERPRET_CURRENT_DIR}/content_units/${entityPathSlug(contentUnitId, "content_unit")}/${filename}`;
}
async function readContentUnitGenerationPromptArtifact(fileRepository, contentUnitId) {
  return readJSONArtifact(fileRepository, contentUnitDerivedArtifactPath(contentUnitId, "generation_prompt.json"));
}
async function readBackendContentCandidateRecord(decisionStore, contentUnitId, candidateId) {
  const context = await decisionStore.getContentUnitDecision({ contentUnitId });
  return context?.candidates.find((candidate) => String(candidate.id) === String(candidateId));
}
function mergePromptSnapshots(runtimePrompt, promptSnapshot) {
  if (!runtimePrompt) return promptSnapshot;
  if (!promptSnapshot) return runtimePrompt;
  return pruneUndefined12({ ...runtimePrompt, ...promptSnapshot });
}
function firstCandidateResourceId(candidate) {
  const firstOutput = arrayField4(candidate?.outputs).filter(isRecord11)[0];
  const resourceId = firstOutput?.resource_id;
  return resourceIdField2(resourceId);
}
function resourceIdField2(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function arrayField4(value) {
  return Array.isArray(value) ? value : [];
}
var MOVSCRIPT_GITIGNORE_PATH = ".gitignore";
var MOVSCRIPT_INTERPRET_GITIGNORE_ENTRY = ".interpret/";
var MOVSCRIPT_LOCAL_CONTROL_GITIGNORE_ENTRY = ".movscript/";
var MOVSCRIPT_INTERPRET_GITIGNORE_BLOCK = [
  "# MovScript generated artifacts",
  MOVSCRIPT_INTERPRET_GITIGNORE_ENTRY,
  MOVSCRIPT_LOCAL_CONTROL_GITIGNORE_ENTRY,
  ""
].join("\n");
async function ensureMovScriptGitignore(fileRepository) {
  const normalizedPath = normalizeWorkspacePath(MOVSCRIPT_GITIGNORE_PATH);
  const existingFile = await fileRepository.read({ path: normalizedPath }).catch(() => void 0);
  const existingContent = existingFile?.content;
  if (existingContent !== void 0 && gitignoreContainsBuildEntry(existingContent)) {
    return { path: normalizedPath, status: "skipped", content: existingContent };
  }
  const content = appendGitignoreBlock(existingContent, MOVSCRIPT_INTERPRET_GITIGNORE_BLOCK);
  await fileRepository.write({ path: normalizedPath, content });
  return {
    path: normalizedPath,
    status: existingContent === void 0 ? "created" : "updated",
    content
  };
}
function gitignoreContainsBuildEntry(content) {
  const entries = new Set(content.split(/\r?\n/).map((line) => line.trim()).filter(Boolean));
  return (entries.has(".interpret") || entries.has(MOVSCRIPT_INTERPRET_GITIGNORE_ENTRY)) && (entries.has(".movscript") || entries.has(MOVSCRIPT_LOCAL_CONTROL_GITIGNORE_ENTRY));
}
function appendGitignoreBlock(existingContent, block) {
  if (!existingContent) return block;
  const separator = existingContent.endsWith("\n") ? "\n" : "\n\n";
  return `${existingContent}${separator}${block}`;
}
async function writeJSONDocument(fileRepository, path, record, overwrite) {
  const normalizedPath = normalizeWorkspacePath(path);
  if (!overwrite) {
    const existing2 = await readJSONArtifact(fileRepository, normalizedPath);
    if (existing2) {
      return { path: normalizedPath, status: "skipped", record: existing2 };
    }
  }
  const existing = await readJSONArtifact(fileRepository, normalizedPath);
  await fileRepository.write({
    path: normalizedPath,
    content: `${JSON.stringify(pruneUndefined12(record), null, 2)}
`
  });
  return {
    path: normalizedPath,
    status: existing ? "updated" : "created",
    record: pruneUndefined12(record)
  };
}
async function writePayloadWithGeneratedEntityId(loadIndex, entityKind, input) {
  const current = {
    ...isRecord11(input.record) ? input.record : {},
    ...isRecord11(input.entity?.record) ? input.entity?.record : {}
  };
  const payload = isRecord11(input.payload) ? input.payload : {};
  if (hasEntityIdentity(payload) || hasEntityIdentity(current)) return payload;
  const existingIds = await existingWorkspaceEntityIds(loadIndex, entityKind);
  return {
    ...payload,
    id: allocateMovScriptEntityId({
      entityKind,
      title: entityTitleSeed(payload, current),
      existingIds
    })
  };
}
async function contentUnitWithGeneratedEntityId(loadIndex, unit) {
  if (hasEntityIdentity(unit)) return unit;
  const existingIds = await existingWorkspaceEntityIds(loadIndex, "content_unit");
  return {
    ...unit,
    id: allocateMovScriptEntityId({
      entityKind: "content_unit",
      title: entityTitleSeed(unit, {}),
      existingIds
    })
  };
}
async function existingWorkspaceEntityIds(loadIndex, entityKind) {
  const entities = queryMovScriptWorkspaceEntities(await loadIndex(), { entityKind });
  return entities.flatMap((entity) => [
    stringField25(entity.id),
    stringField25(entity.record.id),
    stringField25(entity.record.ID)
  ]).filter((id) => Boolean(id));
}
function hasEntityIdentity(record) {
  return [
    record.workspace_slug,
    record.workspaceSlug,
    record.workspace_entity_id,
    record.workspaceEntityId,
    record.id,
    record.ID,
    record.client_id,
    record.clientId
  ].some((value) => stringField25(value) !== void 0);
}
function entityTitleSeed(payload, current) {
  return payload.title ?? payload.name ?? payload.slot ?? payload.slot_key ?? payload.slotKey ?? payload.text ?? payload.description ?? current.title ?? current.name ?? current.slot ?? current.slot_key ?? current.slotKey ?? current.text ?? current.description;
}
function stringField25(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function safeProjectSlug(value) {
  const slug = value.trim().toLowerCase().replace(/[^a-z0-9._-]+/g, "_").replace(/^_+|_+$/g, "");
  return slug || "movscript_project";
}
function createMovScriptProjectUid() {
  const randomUUID3 = globalThis.crypto?.randomUUID?.();
  if (randomUUID3) return `prj_${randomUUID3.replace(/-/g, "")}`;
  const random = Math.random().toString(36).slice(2);
  return `prj_${Date.now().toString(36)}${random}`;
}
function sameEntityId(left, right) {
  return String(left ?? "") === String(right ?? "");
}
function isRecord11(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function pruneUndefined12(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}

// ../../packages/core/dist/chunk-G2ZRFCTC.js
async function loadContentSourceWorkspaceSnapshotFromEngine(engine) {
  const service2 = engine.workspaceService;
  const [index, review] = await Promise.all([
    service2.loadIndex(),
    engine.review()
  ]);
  const settings = queryMovScriptWorkspaceSettings(index, { limit: 500 });
  const settingStates = queryMovScriptWorkspaceEntities(index, { entityKind: "setting_state", limit: 500 });
  const assetsResult = queryMovScriptWorkspaceAssets(index, { limit: 500 });
  const context = queryMovScriptWorkspaceProductionContext(index, {
    include: ["productions", "segments", "scene_moments", "storyboards", "audio_cues", "expression_units", "content_units", "keyframes"],
    limit: 1e3
  });
  const productions = context.productions ?? [];
  const sceneMoments = context.scene_moments ?? [];
  const contentUnits = context.content_units ?? [];
  const productionIds = new Set(productions.map((production) => String(production.id ?? production.record.id ?? production.record.ID ?? production.path)));
  const legacyPreviewTimelines = deriveMovScriptWorkspacePreviewTimelines(index).filter((timeline) => productionIds.has(String(timeline.productionId ?? timeline.productionPath))).filter(isDefined3);
  const timelineAssemblyScopes = timelineAssemblyScopesFromContentUnits(contentUnits);
  const assemblyPreviewTimelines = timelineAssemblyScopes.map((scope) => deriveMovScriptWorkspaceTimelineAssemblyPreviewTimeline(index, {
    scopeKind: scope.kind,
    scopeRef: scope.ref,
    targetRef: implicitTimelineAssemblyRef(scope.kind, scope.ref)
  })).filter(isDefined3);
  const previewTimelines = uniquePreviewTimelines([...legacyPreviewTimelines, ...assemblyPreviewTimelines]);
  const editingTimelines = (await Promise.all(
    sceneMoments.map(async (sceneMoment) => {
      const sceneMomentId = idField6(sceneMoment.id ?? sceneMoment.record.id ?? sceneMoment.record.ID);
      if (sceneMomentId === void 0) return void 0;
      const editPlan = await readSceneMomentEditPlanForIndexedEntity(engine, sceneMoment).catch(() => void 0);
      if (!isRecord12(editPlan)) return void 0;
      const mediaEditingProject = createMediaEditingProjectFromMovScriptEditPlan(editPlan, {
        projectId: String(editPlan.productionId ?? "MovScript"),
        title: String(sceneMoment.record.title ?? editPlan.sceneMomentId ?? sceneMomentId)
      });
      return {
        targetKind: "scene_moment",
        targetId: sceneMomentId,
        targetPath: sceneMoment.path,
        status: typeof editPlan.status === "string" ? editPlan.status : void 0,
        blockers: Array.isArray(editPlan.blockers) ? editPlan.blockers : void 0,
        mediaEditingProject
      };
    })
  )).filter(isDefined3);
  const productionEditingTimelines = legacyPreviewTimelines.map(
    (timeline) => productionTimelineFromPreview({
      previewTimeline: timeline,
      contentUnits,
      documents: index.documents,
      productions
    })
  );
  const assemblyEditingTimelines = assemblyPreviewTimelines.map(
    (timeline) => timelineAssemblyTimelineFromPreview({
      previewTimeline: timeline,
      contentUnits,
      documents: index.documents
    })
  );
  return {
    indexDocuments: index.documents,
    namespaceVocabulary: index.namespaceVocabulary,
    domainNodes: index.domainNodes,
    domainEdges: index.domainEdges,
    settings,
    settingStates,
    assets: assetsResult.assets,
    productions,
    segments: context.segments ?? [],
    sceneMoments,
    storyboards: context.storyboards ?? [],
    keyframes: context.keyframes ?? [],
    expressionUnits: context.expression_units ?? [],
    audioCues: context.audio_cues ?? [],
    contentUnits,
    previewTimelines,
    editingTimelines: [...productionEditingTimelines, ...assemblyEditingTimelines, ...editingTimelines],
    productionWorkPlan: productionWorkPlanFromReview(review)
  };
}
async function readSceneMomentEditPlanForIndexedEntity(engine, sceneMoment) {
  const sceneMomentDir = sceneMoment.path.replace(/\/scene_moment\.json$/, "");
  if (sceneMomentDir === sceneMoment.path) return void 0;
  const path = join6(engine.projectDir, MOVSCRIPT_INTERPRET_CURRENT_DIR, sceneMomentDir, "edit_plan.json");
  const parsed = JSON.parse(await readFile(path, "utf8"));
  return isRecord12(parsed) ? parsed : void 0;
}
function productionTimelineFromPreview(input) {
  const candidateRecords = contentCandidateRecordsByContentUnitId2(input.documents);
  const selections = selectionRecordsByContentUnitId2(input.documents);
  const contentUnitsById = new Map(input.contentUnits.map((unit) => [String(unit.id ?? pathSegmentAfter5(unit.path, "content_units") ?? unit.path), unit]));
  const blockers = [];
  const clips = input.previewTimeline.items.filter((item) => item.itemType === "scene_moment").sort((left, right) => left.order - right.order).flatMap((item, index) => {
    const contentUnitIds = productionSceneMomentContentUnitIds(input.contentUnits, item);
    if (contentUnitIds.length === 0) {
      blockers.push({
        code: "scene_moment_content_unit_missing",
        scene_moment_id: item.entity.id,
        scene_moment_path: item.entity.path
      });
      return [];
    }
    for (const contentUnitId of contentUnitIds) {
      const selection = selections.get(String(contentUnitId));
      const candidate = selection?.candidate_id !== void 0 ? candidateRecords.get(String(contentUnitId))?.find((entry) => sameId4(entry.id, selection.candidate_id)) : void 0;
      const output = firstCandidateOutput3(candidate);
      const resourceId = numberField7(output?.resource_id);
      if (resourceId !== void 0 && (stringField12(output?.kind) ?? "video") === "video") {
        return [{
          id: `production_clip_${safeId4(String(item.entity.id ?? index))}_${safeId4(String(contentUnitId))}`,
          title: previewTimelineItemTitle(item) ?? stringField12(item.entity.id) ?? `Scene ${index + 1}`,
          sceneMomentId: item.entity.id,
          sceneMomentPath: item.entity.path,
          contentUnitId,
          candidateId: selection?.candidate_id,
          resourceId,
          durationSec: numberField7(output?.duration_sec) ?? 4
        }];
      }
      blockers.push({
        code: selection?.candidate_id === void 0 ? "scene_moment_selection_missing" : "scene_moment_resource_missing",
        scene_moment_id: item.entity.id,
        scene_moment_path: item.entity.path,
        content_unit_id: contentUnitId,
        candidate_id: selection?.candidate_id,
        output_kind: stringField12(contentUnitsById.get(String(contentUnitId))?.record.output_kind)
      });
    }
    return [];
  });
  const productionId = input.previewTimeline.productionId ?? "unknown";
  const productionPath = stringField12(input.previewTimeline.productionPath);
  const production = input.productions.find(
    (item) => sameId4(item.id, productionId) || productionPath !== void 0 && item.path.startsWith(productionPath)
  );
  return {
    targetKind: "production",
    targetId: productionId,
    targetPath: production?.path ?? productionPath,
    status: blockers.length > 0 ? "blocked" : "ready_to_compose",
    blockers,
    mediaEditingProject: createMediaEditingProjectFromProductionTimelineClips({
      productionId,
      title: stringField12(production?.record.title) ?? String(productionId),
      productionPath: production?.path ?? productionPath,
      clips
    })
  };
}
function timelineAssemblyTimelineFromPreview(input) {
  const candidateRecords = contentCandidateRecordsByContentUnitId2(input.documents);
  const selections = selectionRecordsByContentUnitId2(input.documents);
  const contentUnitsById = new Map(input.contentUnits.map((unit) => [String(unit.id ?? pathSegmentAfter5(unit.path, "content_units") ?? unit.path), unit]));
  const blockers = [];
  const clips = input.previewTimeline.items.filter((item) => item.itemType === "scene_moment").sort((left, right) => left.order - right.order).flatMap((item, index) => {
    const contentUnitIds = productionSceneMomentContentUnitIds(input.contentUnits, item);
    if (contentUnitIds.length === 0) {
      blockers.push({
        code: "scene_moment_content_unit_missing",
        scene_moment_id: item.entity.id,
        scene_moment_path: item.entity.path
      });
      return [];
    }
    for (const contentUnitId of contentUnitIds) {
      const selection = selections.get(String(contentUnitId));
      const candidate = selection?.candidate_id !== void 0 ? candidateRecords.get(String(contentUnitId))?.find((entry) => sameId4(entry.id, selection.candidate_id)) : void 0;
      const output = firstCandidateOutput3(candidate);
      const resourceId = numberField7(output?.resource_id);
      if (resourceId !== void 0 && (stringField12(output?.kind) ?? "video") === "video") {
        return [{
          id: `assembly_clip_${safeId4(String(item.entity.id ?? index))}_${safeId4(String(contentUnitId))}`,
          title: previewTimelineItemTitle(item) ?? stringField12(item.entity.id) ?? `Scene ${index + 1}`,
          sceneMomentId: item.entity.id,
          sceneMomentPath: item.entity.path,
          contentUnitId,
          candidateId: selection?.candidate_id,
          resourceId,
          durationSec: numberField7(output?.duration_sec) ?? 4
        }];
      }
      blockers.push({
        code: selection?.candidate_id === void 0 ? "scene_moment_selection_missing" : "scene_moment_resource_missing",
        scene_moment_id: item.entity.id,
        scene_moment_path: item.entity.path,
        content_unit_id: contentUnitId,
        candidate_id: selection?.candidate_id,
        output_kind: stringField12(contentUnitsById.get(String(contentUnitId))?.record.output_kind)
      });
    }
    return [];
  });
  const scopeKind = stringField12(input.previewTimeline.scopeKind) ?? "timeline";
  const scopeRef = idField6(input.previewTimeline.scopeRef) ?? input.previewTimeline.scopePath ?? input.previewTimeline.targetRef ?? "unknown";
  const targetRef = stringField12(input.previewTimeline.targetRef) ?? implicitTimelineAssemblyRef(scopeKind, String(scopeRef));
  const scopePath = stringField12(input.previewTimeline.scopePath);
  return {
    targetKind: "timeline_assembly",
    targetId: targetRef,
    targetRef,
    targetPath: scopePath,
    scopeKind,
    scopeRef,
    scopePath,
    status: blockers.length > 0 ? "blocked" : "ready_to_compose",
    blockers,
    mediaEditingProject: createMediaEditingProjectFromTimelineAssemblyClips({
      targetRef,
      scopeKind,
      scopeRef,
      scopePath,
      title: stringField12(input.previewTimeline.scopeTitle) ?? `Timeline assembly ${scopeKind}:${String(scopeRef)}`,
      clips
    })
  };
}
function timelineAssemblyScopesFromContentUnits(contentUnits) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const contentUnit of contentUnits) {
    const scope = timelineAssemblyScopeFromContentUnitRecord(contentUnit.record);
    if (!scope) continue;
    const ref = String(scope.ref);
    const key = `${scope.kind}:${ref}`;
    if (seen.has(key)) continue;
    seen.add(key);
    output.push({ kind: scope.kind, ref });
  }
  return output;
}
function uniquePreviewTimelines(timelines) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const timeline of timelines) {
    const key = stringField12(timeline.targetRef) ?? (timeline.productionId !== void 0 ? `production:${String(timeline.productionId)}` : void 0) ?? stringField12(timeline.scopePath) ?? JSON.stringify(timeline.items.map((item) => item.id));
    if (seen.has(key)) continue;
    seen.add(key);
    output.push(timeline);
  }
  return output;
}
function productionSceneMomentContentUnitIds(contentUnits, item) {
  const explicit = Array.isArray(item.contentUnitIds) ? (item.contentUnitIds ?? []).filter((id) => typeof id === "string" || typeof id === "number") : [];
  const scanned = contentUnits.filter((unit) => isSceneMomentVideoContentUnit(unit.record) && sceneMomentRefMatches(unit.record, item)).map((unit) => unit.id ?? pathSegmentAfter5(unit.path, "content_units")).filter((id) => typeof id === "string" || typeof id === "number");
  return [...new Map([...explicit, ...scanned].map((id) => [String(id), id])).values()];
}
function previewTimelineItemTitle(item) {
  return stringField12(item.title);
}
function isSceneMomentVideoContentUnit(record) {
  const type = stringField12(record.content_unit_type);
  if (type !== "scene_moment_ref" && type !== "scence_moment_ref") return false;
  const outputKind = stringField12(record.output_kind);
  return outputKind === void 0 || outputKind === "video";
}
function sceneMomentRefMatches(record, item) {
  const refs = [record.scene_moment_ref, record.scence_moment_ref].flatMap((value) => {
    const id = idField6(value);
    return id === void 0 ? [] : [String(id)];
  });
  return refs.some(
    (ref) => sameId4(ref, item.entity.id) || ref === item.entity.path || lastPathSegment3(ref) === lastPathSegment3(item.entity.path)
  );
}
function contentCandidateRecordsByContentUnitId2(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!document.path.endsWith("/content_candidate.json") || !isRecord12(document.data)) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument3(document.path, stringField12(document.data.content_unit_ref));
    if (!contentUnitId) continue;
    output.set(contentUnitId, [...output.get(contentUnitId) ?? [], document.data]);
  }
  return output;
}
function selectionRecordsByContentUnitId2(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!isRecord12(document.data)) continue;
    const selection = isRecord12(document.data.selection) ? document.data.selection : void 0;
    if (!selection) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument3(document.path, stringField12(document.data.target_ref));
    if (!contentUnitId) continue;
    output.set(contentUnitId, {
      ...idField6(selection.candidate_id) !== void 0 ? { candidate_id: idField6(selection.candidate_id) } : {}
    });
  }
  return output;
}
function contentUnitIdForRuntimeDocument3(path, ref) {
  if (ref) return lastPathSegment3(ref) ?? ref;
  return pathSegmentAfter5(path, "content_units");
}
function firstCandidateOutput3(candidate) {
  return Array.isArray(candidate?.outputs) ? candidate.outputs.find(isRecord12) : void 0;
}
function productionWorkPlanFromReview(value) {
  if (!isRecord12(value)) return void 0;
  return value.productionWorkPlan;
}
function isDefined3(value) {
  return value !== void 0;
}
function isRecord12(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function idField6(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function stringField12(value) {
  const id = idField6(value);
  return id === void 0 ? void 0 : String(id);
}
function numberField7(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function pathSegmentAfter5(path, segment) {
  if (!path) return void 0;
  const parts = path.split("/").filter(Boolean);
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function lastPathSegment3(path) {
  return path?.split("/").filter(Boolean).at(-1);
}
function safeId4(value) {
  return value.replace(/[^a-zA-Z0-9_-]+/g, "_") || "item";
}
function sameId4(left, right) {
  return left !== void 0 && right !== void 0 && String(left) === String(right);
}

// ../../packages/core/dist/chunk-DOFYYYVC.js
function completeGenerationReferenceAssets(input) {
  const existing = Array.isArray(input.existing) ? input.existing : [];
  const inputResourceIds = input.inputResourceIds ?? [];
  if (existing.length === 0 && inputResourceIds.length === 0) return [];
  const maxLength = Math.max(existing.length, inputResourceIds.length);
  const out = [];
  for (let index = 0; index < maxLength; index += 1) {
    const ref = existing[index];
    const resourceId = positiveInteger2(ref?.resource_id) ?? inputResourceIds[index];
    if (resourceId === void 0) continue;
    const mediaType = normalizedReferenceMediaType(ref?.media_type) ?? defaultMediaTypeForOperation(input.operation, input.defaultMediaType);
    const role = normalizedReferenceRole(ref?.role) ?? generationDefaultReferenceRoleForMediaType(mediaType);
    if (!mediaType || !role) continue;
    out.push({
      role,
      media_type: mediaType,
      resource_id: resourceId
    });
  }
  return out;
}
function generationDefaultReferenceRoleForMediaType(mediaType) {
  switch (normalizedReferenceMediaType(mediaType)) {
    case "image":
      return "reference_image";
    case "video":
      return "reference_video";
    case "audio":
      return "reference_audio";
    default:
      return void 0;
  }
}
function defaultMediaTypeForOperation(operation, fallback) {
  const normalizedFallback = normalizedReferenceMediaType(fallback);
  if (normalizedFallback) return normalizedFallback;
  switch (String(operation ?? "").trim()) {
    case "video_to_video":
    case "motion_control":
      return "video";
    case "stt":
    case "speech_translate":
    case "audio_chat":
    case "voice_clone":
    case "speech_enhancement":
    case "dubbing":
      return "audio";
    default:
      return "image";
  }
}
function normalizedReferenceRole(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function normalizedReferenceMediaType(value) {
  const mediaType = typeof value === "string" ? value.trim() : "";
  if (!mediaType || mediaType === "any") return void 0;
  return mediaType;
}
function positiveInteger2(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value.trim());
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function buildContentUnitGenerationRequest(input) {
  const promptText = compiledContentUnitGenerationPromptText(input.compiledPrompt);
  const inputResourceIds = positiveIntegerIds([
    ...compiledContentUnitGenerationPromptResourceIds(input.compiledPrompt),
    ...input.additionalInputResourceIds ?? []
  ]);
  const generationIntent = requiredContentUnitGenerationIntent(input.generationIntent);
  const jobType = generationExecutionJobTypeForIntent(generationIntent, input.outputKind);
  const params = contentUnitGenerationParams(input.outputKind, input.compiledPrompt, input.params);
  const promptSnapshot = buildContentUnitGenerationPromptSnapshot({
    contentUnitId: input.contentUnitId,
    outputKind: input.outputKind,
    modelId: input.modelId,
    compiledPrompt: input.compiledPrompt,
    resourceIds: inputResourceIds,
    paramAudit: input.paramAudit,
    modelParams: input.modelParams ?? params
  });
  return {
    contentUnitId: input.contentUnitId,
    outputKind: input.outputKind,
    promptText,
    inputResourceIds,
    jobType,
    generationIntent,
    featureKey: contentUnitGenerationFeatureKey(input.outputKind),
    params,
    promptSnapshot
  };
}
function generationExecutionJobTypeForIntent(intent, fallbackOutputKind) {
  switch (intent?.capability?.trim()) {
    case "video_generation":
      return "video";
    case "image_generation":
      return intent.operation === "image_to_image" || intent.operation === "image_edit" ? "image_edit" : "image";
    case "audio_generation":
      switch (intent.operation?.trim()) {
        case "music":
          return "audio_music";
        case "sfx":
          return "audio_sfx";
        case "stt":
          return "audio_transcribe";
        case "speech_translate":
          return "audio_translate";
        case "audio_chat":
          return "audio_chat";
        case "voice_clone":
          return "voice_clone";
        case "voice_design":
          return "voice_design";
        default:
          return "audio_tts";
      }
    default:
      return fallbackOutputKind;
  }
}
function requiredContentUnitGenerationIntent(intent) {
  if (!intent?.capability?.trim() || !intent.operation?.trim()) {
    throw new Error("generationIntent with capability and operation is required for content-unit generation");
  }
  return intent;
}
function buildContentUnitGenerationOutputCandidate(input) {
  const jobId = contentUnitGenerationJobId(input.job);
  const candidateId = input.candidateId ?? contentUnitGenerationCandidateId(input.outputKind, jobId, input.resourceId);
  const promptSnapshot = input.promptSnapshot ?? {};
  const modelId = input.modelId ?? stringField13(input.job.model_id) ?? stringField13(promptSnapshot.model_id);
  const modelParams = nonEmptyRecord(input.modelParams ?? recordField3(promptSnapshot.model_params));
  return {
    contentUnitId: String(input.contentUnitId),
    candidateId: String(candidateId),
    source: "ai_generate",
    status: "succeeded",
    producer: {
      kind: "generation",
      tool: contentUnitGenerationToolName(input.outputKind),
      job_id: jobId,
      ...modelId ? { model_id: modelId } : {},
      ...stringField13(input.job.job_type) ? { job_type: stringField13(input.job.job_type) } : {},
      ...modelParams ? { model_params: modelParams } : {}
    },
    outputs: [{
      kind: input.outputKind,
      resource_id: input.resourceId,
      metadata: {
        job_id: jobId,
        ...modelId ? { model_id: modelId } : {},
        ...stringField13(input.job.job_type) ? { job_type: stringField13(input.job.job_type) } : {},
        tool: contentUnitGenerationMonitorToolName(input.outputKind)
      }
    }],
    promptSnapshot,
    createdAt: stringField13(input.job.CreatedAt) ?? stringField13(input.job.created_at) ?? (/* @__PURE__ */ new Date()).toISOString()
  };
}
function contentUnitGenerationCandidateId(outputKind, jobId, resourceId) {
  return `gen_${outputKind}_${String(jobId)}_${String(resourceId)}`;
}
function compiledContentUnitGenerationPromptText(prompt) {
  const text = stringField13(prompt.text);
  if (text) return stripResourceMentions(text);
  throw new Error("compiled content unit prompt has no text");
}
function compiledContentUnitGenerationPromptResourceIds(prompt) {
  return positiveIntegerIds([
    ...resourceIdsFromMentions(stringField13(prompt.text)),
    ...resourceIdsFromMentions(stringField13(prompt.negative_text)),
    ...numericList(prompt.resource_ids),
    ...numericList(prompt.resourceIds),
    ...numericList(prompt.style_reference_resource_ids),
    ...numericList(prompt.styleReferenceResourceIds)
  ]);
}
function compiledContentUnitGenerationPromptReferenceAssets(prompt) {
  const declared = referenceAssetsFromValue(prompt.reference_assets ?? prompt.referenceAssets);
  const mentioned = [
    ...referenceAssetsFromMentions(stringField13(prompt.text)),
    ...referenceAssetsFromMentions(stringField13(prompt.negative_text)),
    ...referenceAssetsFromMentions(stringField13(prompt.notes))
  ];
  const all = [...declared, ...mentioned];
  const seen = /* @__PURE__ */ new Set();
  const deduped = all.filter((asset) => {
    if (seen.has(asset.resource_id)) return false;
    seen.add(asset.resource_id);
    return true;
  });
  return completeGenerationReferenceAssets({
    existing: deduped,
    inputResourceIds: compiledContentUnitGenerationPromptResourceIds(prompt)
  });
}
function buildContentUnitGenerationPromptSnapshot(input) {
  const resourceIds2 = input.resourceIds ?? compiledContentUnitGenerationPromptResourceIds(input.compiledPrompt);
  const modelParams = nonEmptyRecord(input.modelParams);
  const referenceAssets = compiledContentUnitGenerationPromptReferenceAssets(input.compiledPrompt);
  return {
    schema: "movscript.content_unit_generation_prompt_snapshot.v1",
    content_unit_id: input.contentUnitId,
    output_kind: input.outputKind,
    model_id: input.modelId,
    compiled_prompt: input.compiledPrompt,
    resource_ids: resourceIds2,
    ...referenceAssets.length > 0 ? { reference_assets: referenceAssets } : {},
    ...modelParams ? { model_params: modelParams } : {},
    ...Array.isArray(input.paramAudit) && input.paramAudit.length > 0 ? { param_audit: input.paramAudit } : {}
  };
}
function contentUnitGenerationFeatureKey(outputKind) {
  return outputKind === "image" ? "electron.generation.content_unit.image" : "electron.generation.content_unit.video";
}
function contentUnitGenerationMonitorToolName(outputKind) {
  return "generation_job_get";
}
function contentUnitGenerationSystemMonitorToolName(outputKind) {
  return contentUnitGenerationMonitorToolName(outputKind);
}
function contentUnitGenerationToolName(outputKind) {
  return "generation_submit";
}
function contentUnitGenerationParams(outputKind, prompt, params = {}) {
  const next = { ...params };
  if (outputKind === "image") {
    if (next.aspect_ratio === void 0) next.aspect_ratio = "1:1";
    const negativePrompt = stringField13(prompt.negative_text);
    if (negativePrompt && next.negative_prompt === void 0) next.negative_prompt = negativePrompt;
  } else {
    if (next.aspect_ratio === void 0) next.aspect_ratio = "16:9";
    if (next.duration === void 0) next.duration = 5;
  }
  return next;
}
function contentUnitGenerationJobId(job) {
  const id = numericId2(job.ID) ?? numericId2(job.id);
  if (id !== void 0) return id;
  throw new Error("Generation job response does not include a valid job id");
}
function referenceAssetsFromValue(value) {
  if (!Array.isArray(value)) return [];
  return value.flatMap((item) => {
    const record = recordField3(item);
    if (!record) return [];
    const resourceId = numericId2(record.resource_id ?? record.resourceId ?? record.id);
    if (resourceId === void 0) return [];
    const role = stringField13(record.role);
    const mediaType = stringField13(record.media_type ?? record.mediaType);
    return [{
      resource_id: resourceId,
      ...role ? { role } : {},
      ...mediaType ? { media_type: mediaType } : {}
    }];
  });
}
function referenceAssetsFromMentions(text) {
  return parseResourceMentions(text).map((mention) => ({
    resource_id: mention.id,
    ...mention.role ? { role: mention.role } : {},
    ...mention.mediaType ? { media_type: mention.mediaType } : {}
  }));
}
function positiveIntegerIds(values) {
  const ids = values.map((value) => numericId2(value)).filter((value) => value !== void 0);
  return Array.from(new Set(ids));
}
function numericList(value) {
  if (!Array.isArray(value)) return [];
  return value.map((item) => numericId2(item)).filter((item) => item !== void 0);
}
function numericId2(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const numberValue8 = Number(value.trim());
    if (Number.isInteger(numberValue8) && numberValue8 > 0) return numberValue8;
  }
  return void 0;
}
function stringField13(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function recordField3(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : void 0;
}
function nonEmptyRecord(value) {
  const record = recordField3(value);
  return record && Object.keys(record).length > 0 ? record : void 0;
}
var DEFAULT_GENERATION_TOOLS_SETTINGS = {
  servers: [
    createGenerationToolServer("comfyui", {
      id: "local-comfyui-default",
      name: "\u672C\u673A ComfyUI",
      baseURL: "http://127.0.0.1:8188",
      priority: 10
    }),
    createGenerationToolServer("webui", {
      id: "local-webui-default",
      name: "\u672C\u673A WebUI",
      baseURL: "http://127.0.0.1:7860",
      priority: 20
    })
  ],
  defaultServerId: void 0,
  defaultServerIds: {},
  preferLocalServers: true
};
function createGenerationToolServer(type, overrides = {}) {
  const id = overrides.id ?? newLocalServerId();
  return {
    id,
    scope: overrides.scope ?? "local",
    type,
    name: overrides.name ?? (type === "comfyui" ? "ComfyUI" : "Stable Diffusion WebUI"),
    enabled: overrides.enabled ?? false,
    baseURL: overrides.baseURL ?? (type === "comfyui" ? "http://127.0.0.1:8188" : "http://127.0.0.1:7860"),
    timeoutMS: overrides.timeoutMS ?? 12e4,
    priority: overrides.priority ?? 50,
    authKind: overrides.authKind ?? "none",
    username: overrides.username ?? "",
    password: overrides.password ?? "",
    passwordSet: overrides.passwordSet ?? false,
    token: overrides.token ?? "",
    tokenSet: overrides.tokenSet ?? false,
    tags: overrides.tags ?? []
  };
}
function newLocalServerId() {
  if (typeof crypto !== "undefined" && typeof crypto.randomUUID === "function") {
    return `local-${crypto.randomUUID()}`;
  }
  return `local-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
}

// ../../packages/project/dist/index.js
var defaultFetch2 = globalThis.fetch;
var PROJECT_SERVICE_NAME = "movscript.project.service";
var PROJECT_SERVICE_CAPABILITIES_ENDPOINT = "/v1/project/capabilities";
var PROJECT_SERVICE_SOURCE_SNAPSHOT_ENDPOINT = "/v1/project/source/snapshot";
var PROJECT_SERVICE_SOURCE_INSPECT_ENDPOINT = "/v1/project/source/inspect";
var PROJECT_SERVICE_SOURCE_OVERVIEW_ENDPOINT = "/v1/project/source/overview";
var PROJECT_SERVICE_READ_MODEL_ENDPOINT = "/v1/project/read-model";
var PROJECT_SERVICE_HOME_READ_MODEL_ENDPOINT = "/v1/project/home/read-model";
var PROJECT_SERVICE_STANDARDS_READ_MODEL_ENDPOINT = "/v1/project/standards/read-model";
var PROJECT_SERVICE_CONTENT_CANVAS_READ_MODEL_ENDPOINT = "/v1/project/content-canvas/read-model";
var PROJECT_SERVICE_SCRIPTS_READ_MODEL_ENDPOINT = "/v1/project/scripts/read-model";
var PROJECT_SERVICE_CONTENT_UNITS_READ_MODEL_ENDPOINT = "/v1/project/content-units/read-model";
var PROJECT_SERVICE_LOCATOR_RESOLVE_ENDPOINT = "/v1/project/locator/resolve";
var PROJECT_SERVICE_RESOURCE_VIEW_ENDPOINT = "/v1/project/resources/view";
var PROJECT_SERVICE_LIFECYCLE_COMMAND_ENDPOINT = "/v1/project/lifecycle/command";
var PROJECT_SERVICE_SOURCE_INTERPRET_ENDPOINT = "/v1/project/source/interpret";
var PROJECT_SERVICE_SOURCE_REGENERATION_PLAN_ENDPOINT = "/v1/project/source/regeneration-plan";
var PROJECT_SERVICE_SOURCE_COMMAND_ENDPOINT = "/v1/project/source/command";
var PROJECT_SERVICE_ENTITIES_QUERY_ENDPOINT = "/v1/project/entities/query";
var PROJECT_SERVICE_SETTINGS_QUERY_ENDPOINT = "/v1/project/settings/query";
var PROJECT_SERVICE_ASSETS_QUERY_ENDPOINT = "/v1/project/assets/query";
var PROJECT_SERVICE_CONTENT_WORKSPACE_SNAPSHOT_ENDPOINT = "/v1/project/content-workspace/snapshot";
var PROJECT_SERVICE_CONTENT_WORKSPACE_READ_ENDPOINT = "/v1/project/content-workspace/read";
var PROJECT_SERVICE_STANDARDS_UPSERT_ENDPOINT = "/v1/project/standards/upsert";
var PROJECT_SERVICE_SCRIPT_SOURCE_READ_ENDPOINT = "/v1/project/scripts/source/read";
var PROJECT_SERVICE_SCRIPT_UPSERT_ENDPOINT = "/v1/project/scripts/upsert";
var PROJECT_SERVICE_SCRIPT_VERSION_SNAPSHOT_ENDPOINT = "/v1/project/scripts/versions/snapshot";
var PROJECT_SERVICE_SETTING_UPSERT_ENDPOINT = "/v1/project/settings/upsert";
var PROJECT_SERVICE_SETTING_CREATE_ENDPOINT = "/v1/project/settings/create";
var PROJECT_SERVICE_SETTING_STATE_CREATE_ENDPOINT = "/v1/project/settings/states/create";
var PROJECT_SERVICE_ASSET_UPSERT_ENDPOINT = "/v1/project/assets/upsert";
var PROJECT_SERVICE_ASSET_CREATE_ENDPOINT = "/v1/project/assets/create";
var PROJECT_SERVICE_PRODUCTION_SNAPSHOT_SAVE_ENDPOINT = "/v1/project/productions/snapshot/save";
var PROJECT_SERVICE_CONTENT_UNIT_UPSERT_ENDPOINT = "/v1/project/content-units/upsert";
var PROJECT_SERVICE_CONTENT_UNIT_CREATE_ENDPOINT = "/v1/project/content-units/create";
var PROJECT_SERVICE_CONTENT_UNIT_ENSURE_ENDPOINT = "/v1/project/content-units/ensure";
var PROJECT_SERVICE_TIMELINE_ASSEMBLY_CONTENT_UNIT_ENSURE_ENDPOINT = "/v1/project/timeline-assemblies/content-unit/ensure";
var PROJECT_SERVICE_CONTENT_UNIT_EDIT_PROMPT_UPDATE_ENDPOINT = "/v1/project/content-units/edit-prompt/update";
var PROJECT_SERVICE_PRODUCTION_CREATE_ENDPOINT = "/v1/project/productions/create";
var PROJECT_SERVICE_SEGMENT_CREATE_ENDPOINT = "/v1/project/segments/create";
var PROJECT_SERVICE_SCENE_MOMENT_CREATE_ENDPOINT = "/v1/project/scene-moments/create";
var PROJECT_SERVICE_SCENE_MOMENT_SETTING_CONNECT_ENDPOINT = "/v1/project/scene-moments/settings/connect";
var PROJECT_SERVICE_EXPRESSION_UNIT_CREATE_ENDPOINT = "/v1/project/expression-units/create";
var PROJECT_SERVICE_EXPRESSION_UNIT_UPDATE_ENDPOINT = "/v1/project/expression-units/update";
var PROJECT_SERVICE_KEYFRAME_CREATE_ENDPOINT = "/v1/project/keyframes/create";
var PROJECT_SERVICE_STORYBOARD_CREATE_ENDPOINT = "/v1/project/storyboards/create";
var PROJECT_SERVICE_STORYBOARD_TIMELINE_UPDATE_ENDPOINT = "/v1/project/storyboards/timeline/update";
var PROJECT_SERVICE_AUDIO_CUE_CREATE_ENDPOINT = "/v1/project/audio-cues/create";
var PROJECT_SERVICE_AUDIO_CUE_UPDATE_ENDPOINT = "/v1/project/audio-cues/update";
var PROJECT_SERVICE_ENTITY_BASICS_UPDATE_ENDPOINT = "/v1/project/entities/basics/update";
var PROJECT_SERVICE_ENTITY_TRANSITION_UPDATE_ENDPOINT = "/v1/project/entities/transition/update";
var PROJECT_SERVICE_ENTITY_DELETE_ENDPOINT = "/v1/project/entities/delete";
var PROJECT_SERVICE_HIERARCHY_WRITE_ENDPOINT = "/v1/project/hierarchy/write";
var PROJECT_SERVICE_NAMESPACE_WRITE_ENDPOINT = "/v1/project/namespaces/write";
var PROJECT_SERVICE_CONTENT_CANVASES_LIST_ENDPOINT = "/v1/project/content-canvases/list";
var PROJECT_SERVICE_CONTENT_CANVAS_WRITE_ENDPOINT = "/v1/project/content-canvases/write";
var PROJECT_SERVICE_CONTENT_CANVAS_RENAME_ENDPOINT = "/v1/project/content-canvases/rename";
var PROJECT_SERVICE_CONTENT_CANVAS_RUN_ENDPOINT = "/v1/project/content-canvases/run";
var PROJECT_SERVICE_CONTENT_CANVAS_DELETE_ENDPOINT = "/v1/project/content-canvases/delete";
var PROJECT_SERVICE_WORKSPACE_CANDIDATE_SELECT_ENDPOINT = "/v1/project/workspace-candidates/select";
var PROJECT_SERVICE_WORKSPACE_CANDIDATE_APPEND_ENDPOINT = "/v1/project/workspace-candidates/append";
var PROJECT_SERVICE_WORKSPACE_ASSET_SLOT_CANDIDATE_CREATE_ENDPOINT = "/v1/project/workspace-candidates/asset-slots/create";
var PROJECT_SERVICE_WORKSPACE_KEYFRAME_CANDIDATE_CREATE_ENDPOINT = "/v1/project/workspace-candidates/keyframes/create";
var PROJECT_SERVICE_CONTENT_CANDIDATE_CREATE_ENDPOINT = "/v1/project/content-candidates/create";
var PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_SELECT_ENDPOINT = "/v1/project/content-unit-candidates/select";
var PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_DECIDE_ENDPOINT = "/v1/project/content-unit-candidates/decide";
var PROJECT_SERVICE_CANDIDATE_COMMAND_ENDPOINT = "/v1/project/candidates/command";
var PROJECT_SERVICE_CANDIDATE_VIEW_ENDPOINT = "/v1/project/candidates/view";
var PROJECT_SERVICE_PROMPT_CONTEXT_ENDPOINT = "/v1/project/prompt/context";
var ProjectServiceClient = class {
  baseUrl;
  fetchImpl;
  constructor(options) {
    const baseUrl = normalizeProjectServiceBaseUrl(options.baseUrl);
    if (!baseUrl) throw new Error("project service baseUrl is required");
    this.baseUrl = baseUrl;
    this.fetchImpl = options.fetch ?? defaultFetch2;
  }
  async capabilities(signal) {
    return this.request("GET", PROJECT_SERVICE_CAPABILITIES_ENDPOINT, void 0, signal);
  }
  async sourceSnapshot(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SOURCE_SNAPSHOT_ENDPOINT, projectSourcePayload(request), signal);
  }
  async inspectSource(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SOURCE_INSPECT_ENDPOINT, projectSourcePayload(request), signal);
  }
  async overviewSource(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SOURCE_OVERVIEW_ENDPOINT, projectSourcePayload(request), signal);
  }
  async readModel(request, signal) {
    return this.request("POST", PROJECT_SERVICE_READ_MODEL_ENDPOINT, projectReadModelPayload(request), signal);
  }
  async homeReadModel(request, signal) {
    return this.request("POST", PROJECT_SERVICE_HOME_READ_MODEL_ENDPOINT, projectSourcePayload(request), signal);
  }
  async standardsReadModel(request, signal) {
    return this.request("POST", PROJECT_SERVICE_STANDARDS_READ_MODEL_ENDPOINT, projectSourcePayload(request), signal);
  }
  async contentCanvasReadModel(request, signal) {
    return this.request("POST", PROJECT_SERVICE_CONTENT_CANVAS_READ_MODEL_ENDPOINT, projectSourcePayload(request), signal);
  }
  async scriptsReadModel(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SCRIPTS_READ_MODEL_ENDPOINT, projectSourcePayload(request), signal);
  }
  async contentUnitsReadModel(request, signal) {
    return this.request("POST", PROJECT_SERVICE_CONTENT_UNITS_READ_MODEL_ENDPOINT, {
      ...projectSourcePayload(request),
      contentUnitIds: request.contentUnitIds,
      ...request.decisionStore ? { decisionStore: request.decisionStore } : {}
    }, signal);
  }
  async interpretSource(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SOURCE_INTERPRET_ENDPOINT, projectSourcePayload(request), signal);
  }
  async regenerationPlan(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SOURCE_REGENERATION_PLAN_ENDPOINT, projectSourcePayload(request), signal);
  }
  async sourceCommand(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SOURCE_COMMAND_ENDPOINT, {
      projectDir: request.projectDir,
      command: request.command,
      input: request.input ?? {}
    }, signal);
  }
  async upsertProjectStandards(request, signal) {
    return this.request("POST", PROJECT_SERVICE_STANDARDS_UPSERT_ENDPOINT, projectSourceOperationPayload(request), signal);
  }
  async readScriptSource(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SCRIPT_SOURCE_READ_ENDPOINT, projectSourceOperationPayload(request), signal);
  }
  async upsertScript(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SCRIPT_UPSERT_ENDPOINT, projectSourceOperationPayload(request), signal);
  }
  async snapshotScriptVersionFromMarkdown(request, signal) {
    return this.request("POST", PROJECT_SERVICE_SCRIPT_VERSION_SNAPSHOT_ENDPOINT, projectSourceOperationPayload(request), signal);
  }
  async sourceOperation(endpoint, request, signal) {
    return this.request("POST", endpoint, projectSourceOperationPayload(request), signal);
  }
  async selectWorkspaceCandidate(request, signal) {
    return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_CANDIDATE_SELECT_ENDPOINT, request, signal);
  }
  async appendWorkspaceCandidate(request, signal) {
    return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_CANDIDATE_APPEND_ENDPOINT, request, signal);
  }
  async createWorkspaceAssetSlotCandidate(request, signal) {
    return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_ASSET_SLOT_CANDIDATE_CREATE_ENDPOINT, request, signal);
  }
  async createWorkspaceKeyframeCandidate(request, signal) {
    return this.sourceOperation(PROJECT_SERVICE_WORKSPACE_KEYFRAME_CANDIDATE_CREATE_ENDPOINT, request, signal);
  }
  async createContentCandidate(request, signal) {
    return this.candidateAction(PROJECT_SERVICE_CONTENT_CANDIDATE_CREATE_ENDPOINT, request, signal);
  }
  async selectContentUnitCandidate(request, signal) {
    return this.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_SELECT_ENDPOINT, request, signal);
  }
  async decideContentUnitCandidate(request, signal) {
    return this.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_DECIDE_ENDPOINT, request, signal);
  }
  async lifecycleCommand(request, signal) {
    return this.request("POST", PROJECT_SERVICE_LIFECYCLE_COMMAND_ENDPOINT, {
      projectDir: request.projectDir,
      command: request.command,
      input: request.input ?? {}
    }, signal);
  }
  async resolveLocator(request, signal) {
    return this.request("POST", PROJECT_SERVICE_LOCATOR_RESOLVE_ENDPOINT, {
      projectDir: request.projectDir,
      ...request.workspaceDir ? { workspaceDir: request.workspaceDir } : {},
      ...request.projectUid ? { projectUid: request.projectUid } : {}
    }, signal);
  }
  async resourceView(request, signal) {
    return this.request("POST", PROJECT_SERVICE_RESOURCE_VIEW_ENDPOINT, {
      projectDir: request.projectDir,
      kind: request.kind
    }, signal);
  }
  async candidateCommand(request, signal) {
    return this.request("POST", PROJECT_SERVICE_CANDIDATE_COMMAND_ENDPOINT, {
      projectDir: request.projectDir,
      command: request.command,
      input: request.input ?? {},
      decisionStore: request.decisionStore
    }, signal);
  }
  async candidateAction(endpoint, request, signal) {
    return this.request("POST", endpoint, {
      projectDir: request.projectDir,
      input: request.input ?? {},
      decisionStore: request.decisionStore
    }, signal);
  }
  async candidateView(request, signal) {
    return this.request("POST", PROJECT_SERVICE_CANDIDATE_VIEW_ENDPOINT, {
      projectDir: request.projectDir,
      ...request.contentUnitId !== void 0 ? { contentUnitId: request.contentUnitId } : {},
      ...request.contentUnitIds !== void 0 ? { contentUnitIds: request.contentUnitIds } : {},
      decisionStore: request.decisionStore
    }, signal);
  }
  async promptContext(request, signal) {
    return this.request("POST", PROJECT_SERVICE_PROMPT_CONTEXT_ENDPOINT, {
      projectDir: request.projectDir,
      ...request.contentUnitId !== void 0 ? { contentUnitId: request.contentUnitId } : {},
      ...request.contentUnitIds !== void 0 ? { contentUnitIds: request.contentUnitIds } : {},
      ...request.include !== void 0 ? { include: request.include } : {},
      ...request.promptText !== void 0 ? { promptText: request.promptText } : {},
      ...request.decisionStore ? { decisionStore: request.decisionStore } : {}
    }, signal);
  }
  async request(method, path, body, signal) {
    const response = await this.fetchImpl(`${this.baseUrl}${path}`, {
      method,
      headers: body === void 0 ? void 0 : {
        "content-type": "application/json"
      },
      body: body === void 0 ? void 0 : JSON.stringify(body),
      signal
    });
    if (!response.ok) {
      throw new ProjectServiceHTTPError(response.status, await response.text());
    }
    return response.json();
  }
};
var ProjectServiceHTTPError = class extends Error {
  status;
  body;
  constructor(status, body) {
    super(`project service request failed: ${status}${body ? ` ${body}` : ""}`);
    this.name = "ProjectServiceHTTPError";
    this.status = status;
    this.body = body;
  }
};
function createProjectServiceClientFromRuntime(options = {}) {
  const baseUrl = resolveProjectServiceBaseUrl(options);
  if (!baseUrl) {
    throw new Error("movscript.project.service endpoint was not found; start the local runtime daemon or set MOVSCRIPT_PROJECT_SERVICE_URL");
  }
  return new ProjectServiceClient({ baseUrl });
}
function resolveProjectServiceBaseUrl(options = {}) {
  const env = options.env ?? process.env;
  const explicit = normalizeProjectServiceBaseUrl(options.baseUrl) ?? normalizeProjectServiceBaseUrl(env.MOVSCRIPT_PROJECT_SERVICE_URL) ?? normalizeProjectServiceBaseUrl(env.MOVSCRIPT_PROJECT_SERVICE_BASE_URL);
  if (explicit) return explicit;
  const homeDir = options.homeDir ?? resolveMovScriptHomeDir({ env });
  const snapshot = readRuntimeHomeSnapshot(homeDir);
  const endpoint = findRuntimeEndpoint(snapshot, PROJECT_SERVICE_NAME) ?? findRuntimeService(snapshot, PROJECT_SERVICE_NAME)?.endpoint;
  return normalizeProjectServiceBaseUrl(endpointURL3(endpoint));
}
function normalizeProjectServiceBaseUrl(value) {
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim().replace(/\/+$/, "");
  if (!trimmed) return void 0;
  const url = new URL(trimmed);
  if (url.protocol !== "http:" && url.protocol !== "https:") {
    throw new Error("project service baseUrl must use http or https");
  }
  return url.toString().replace(/\/+$/, "");
}
function endpointURL3(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}
function projectSourcePayload(request) {
  return {
    projectDir: request.projectDir,
    ...request.projectId !== void 0 ? { projectId: request.projectId } : {},
    ...request.projectUid ? { projectUid: request.projectUid } : {},
    ...request.includeContentUnitDecisionDocuments !== void 0 ? {
      includeContentUnitDecisionDocuments: request.includeContentUnitDecisionDocuments
    } : {},
    ...request.debugArtifacts !== void 0 ? { debugArtifacts: request.debugArtifacts } : {},
    ...request.commit ? { commit: request.commit } : {},
    ...request.checkpointHash ? { checkpointHash: request.checkpointHash } : {}
  };
}
function projectSourceOperationPayload(request) {
  return {
    projectDir: request.projectDir,
    ...request.input ?? {}
  };
}
function projectReadModelPayload(request) {
  return {
    ...projectSourcePayload(request),
    ...request.includeSource !== void 0 ? { includeSource: request.includeSource } : {},
    ...request.includeInspection !== void 0 ? { includeInspection: request.includeInspection } : {}
  };
}

// ../../packages/workspace/dist/node.js
import { mkdir as mkdir2, readdir as readdir2, readFile as readFile2, rename, rm, stat as stat2, writeFile as writeFile2 } from "fs/promises";
import { dirname as dirname5, join as join7, relative, resolve as resolve4 } from "path";
import { execFile } from "child_process";
import { resolve as resolve22 } from "path";
import { promisify } from "util";
import { resolve as resolve5 } from "path";
function createNodeMovScriptWorkspaceFileRepository(rootDir = process.cwd()) {
  const root = resolve4(rootDir);
  const repository = {
    async list(input = {}) {
      const path = normalizeWorkspacePath(input.path ?? "");
      const absolutePath = resolveWorkspacePath(root, path);
      const entries = await readdir2(absolutePath, { withFileTypes: true }).catch(() => []);
      return {
        path,
        entries: entries.map((entry) => ({
          path: normalizeWorkspacePath(join7(path, entry.name)),
          kind: entry.isDirectory() ? "directory" : "file"
        })).sort((left, right) => {
          if (left.kind !== right.kind) return left.kind === "directory" ? -1 : 1;
          return left.path.localeCompare(right.path);
        })
      };
    },
    async read(input) {
      const path = normalizeWorkspacePath(input.path);
      const absolutePath = resolveWorkspacePath(root, path);
      const content = await readFile2(absolutePath, "utf8");
      const fileStat = await stat2(absolutePath);
      const version = fileVersion(fileStat.mtimeMs, fileStat.size);
      return { path, content, size: content.length, updatedAt: fileStat.mtime.toISOString(), version };
    },
    async write(input) {
      const path = normalizeWorkspacePath(input.path);
      const absolutePath = resolveWorkspacePath(root, path);
      await mkdir2(dirname5(absolutePath), { recursive: true });
      await assertExpectedFileVersion(absolutePath, input.expectedVersion);
      await writeTextFileAtomic(absolutePath, input.content);
      const fileStat = await stat2(absolutePath);
      const version = fileVersion(fileStat.mtimeMs, fileStat.size);
      return { path, content: input.content, size: input.content.length, updatedAt: fileStat.mtime.toISOString(), version };
    },
    async delete(input) {
      await rm(resolveWorkspacePath(root, normalizeWorkspacePath(input.path)), { force: true });
    }
  };
  Object.defineProperty(repository, "rootDir", {
    value: root,
    enumerable: false,
    configurable: false
  });
  return repository;
}
function getNodeMovScriptWorkspaceFileRepositoryRoot(fileRepository) {
  const rootDir = fileRepository.rootDir;
  return typeof rootDir === "string" && rootDir ? rootDir : void 0;
}
function resolveWorkspacePath(root, path) {
  const absolutePath = resolve4(root, path);
  const relativePath = relative(root, absolutePath);
  if (relativePath === "" || !relativePath.startsWith("..") && !relativePath.startsWith("/")) return absolutePath;
  throw new Error("workspace path must stay inside the project workspace root");
}
async function writeTextFileAtomic(filePath, content) {
  const tmpPath = join7(dirname5(filePath), `.${Date.now()}-${Math.random().toString(36).slice(2)}.tmp`);
  try {
    await writeFile2(tmpPath, content, "utf8");
    await rename(tmpPath, filePath);
  } catch (error) {
    await rm(tmpPath, { force: true }).catch(() => void 0);
    throw error;
  }
}
async function assertExpectedFileVersion(filePath, expectedVersion) {
  if (expectedVersion === void 0) return;
  const fileStat = await stat2(filePath).catch((error) => {
    if (isNotFoundError3(error)) return void 0;
    throw error;
  });
  const currentVersion = fileStat ? fileVersion(fileStat.mtimeMs, fileStat.size) : null;
  if (currentVersion !== expectedVersion) {
    throw new Error(`workspace file changed: ${filePath}`);
  }
}
function fileVersion(mtimeMs, size) {
  return `${Math.trunc(mtimeMs)}:${size}`;
}
function isNotFoundError3(error) {
  return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
var execFileAsync = promisify(execFile);
var MOVSCRIPT_GIT_SOURCE_PATHS = [
  "project.json",
  "project_standards.json",
  "settings",
  "scripts",
  "content_units",
  "productions"
];
async function inspectNodeMovScriptGitWorkspace(rootDir) {
  const root = resolve22(rootDir);
  if (!await isGitAvailable()) {
    return { rootDir: root, available: false, insideWorkTree: false, hasHead: false };
  }
  const repoRoot = await gitStdout(root, ["rev-parse", "--show-toplevel"]).catch(() => void 0);
  if (!repoRoot) {
    return { rootDir: root, available: true, insideWorkTree: false, hasHead: false };
  }
  const head = await currentNodeMovScriptGitHead(root);
  return {
    rootDir: root,
    available: true,
    insideWorkTree: true,
    hasHead: Boolean(head),
    repoRoot: repoRoot.trim(),
    ...head ? { head } : {}
  };
}
async function currentNodeMovScriptGitHead(rootDir) {
  const head = await gitStdout(rootDir, ["rev-parse", "--verify", "HEAD"]).catch(() => void 0);
  return head?.trim() || void 0;
}
async function readNodeMovScriptGitSourceFiles(rootDir, gitRef, pathspecs = MOVSCRIPT_GIT_SOURCE_PATHS) {
  const stdout = await gitStdout(rootDir, [
    "ls-tree",
    "-r",
    "--name-only",
    gitRef,
    "--",
    ...pathspecs
  ]).catch(() => "");
  const paths = stdout.split("\n").map((path) => normalizeWorkspacePath(path)).filter(Boolean);
  const files = [];
  for (const path of paths) {
    try {
      const content = await gitStdout(rootDir, ["show", `${gitRef}:${path}`], 1024 * 1024 * 20);
      files.push({ path, content });
    } catch {
    }
  }
  return files.sort((left, right) => left.path.localeCompare(right.path));
}
async function readNodeMovScriptGitSourceFileChanges(rootDir, gitRef, pathspecs = MOVSCRIPT_GIT_SOURCE_PATHS) {
  const trackedStdout = await gitStdout(rootDir, [
    "diff",
    "--name-status",
    "--find-renames",
    gitRef,
    "--",
    ...pathspecs
  ]).catch(() => "");
  const changes = trackedStdout.split("\n").map(parseGitNameStatusLine).filter((change) => change !== void 0);
  const seenPaths = new Set(changes.map((change) => change.path));
  const untrackedStdout = await gitStdout(rootDir, [
    "ls-files",
    "--others",
    "--exclude-standard",
    "--",
    ...pathspecs
  ]).catch(() => "");
  for (const path of untrackedStdout.split("\n").map((item) => normalizeWorkspacePath(item)).filter(Boolean)) {
    if (seenPaths.has(path)) continue;
    changes.push({
      path,
      state: "added",
      statusCode: "??"
    });
    seenPaths.add(path);
  }
  return changes.sort((left, right) => {
    return left.path.localeCompare(right.path) || String(left.previousPath ?? "").localeCompare(String(right.previousPath ?? "")) || left.statusCode.localeCompare(right.statusCode);
  });
}
async function isGitAvailable() {
  try {
    await execFileAsync("git", ["--version"]);
    return true;
  } catch {
    return false;
  }
}
async function gitStdout(rootDir, args, maxBuffer) {
  const { stdout } = await execFileAsync("git", ["-C", rootDir, ...args], {
    ...maxBuffer ? { maxBuffer } : {}
  });
  return stdout;
}
function parseGitNameStatusLine(line) {
  const parts = line.split("	").filter(Boolean);
  const statusCode = parts[0];
  if (!statusCode) return void 0;
  const status = statusCode[0];
  if (status === "R") {
    const previousPath = parts[1] ? normalizeWorkspacePath(parts[1]) : void 0;
    const path2 = parts[2] ? normalizeWorkspacePath(parts[2]) : void 0;
    if (!previousPath || !path2) return void 0;
    return {
      path: path2,
      previousPath,
      state: "moved",
      statusCode
    };
  }
  const path = parts[1] ? normalizeWorkspacePath(parts[1]) : void 0;
  if (!path) return void 0;
  if (status === "A") return { path, state: "added", statusCode };
  if (status === "D") return { path, state: "deleted", statusCode };
  return { path, state: "modified", statusCode };
}
function createNodeMovScriptWorkspaceService(input = {}) {
  const projectDir = resolve5(input.projectDir ?? input.workspaceDir ?? process.cwd());
  const fileRepository = createNodeMovScriptWorkspaceFileRepository(projectDir);
  const service2 = createMovScriptWorkspaceService({
    fileRepository,
    decisionStore: input.decisionStore,
    now: input.now
  });
  return {
    ...service2,
    projectDir
  };
}

// ../../packages/engine/dist/chunk-PVMLBOBJ.js
function createMovScriptEngine(options) {
  const workspaceService = options.workspaceService;
  const overviewWorkspace = options.overviewWorkspace;
  const inspectWorkspace = options.inspectWorkspace;
  const reviewWorkspace = options.reviewWorkspace;
  const interpretWorkspace = options.interpretWorkspace;
  const productionWorkPlan = options.productionWorkPlan;
  const regenerationPlan = options.regenerationPlan;
  const deriveContentUnitArtifact2 = options.deriveContentUnitArtifact;
  const buildContentUnitBackendPrompt2 = options.buildContentUnitBackendPrompt;
  const deriveArtifacts = options.deriveArtifacts;
  return {
    workspaceService,
    initProject: workspaceService.initializeProject.bind(workspaceService),
    initializeProject: workspaceService.initializeProject.bind(workspaceService),
    getModel: workspaceService.getModel.bind(workspaceService),
    loadIndex: workspaceService.loadIndex.bind(workspaceService),
    queryEntities: workspaceService.queryEntities.bind(workspaceService),
    querySettings: workspaceService.querySettings.bind(workspaceService),
    queryAssets: workspaceService.queryAssets.bind(workspaceService),
    queryProductionContext: workspaceService.queryProductionContext.bind(workspaceService),
    upsertSetting: workspaceService.upsertSetting.bind(workspaceService),
    upsertSettingState: workspaceService.upsertSettingState.bind(workspaceService),
    upsertAsset: workspaceService.upsertAsset.bind(workspaceService),
    upsertContentUnit: workspaceService.upsertContentUnit.bind(workspaceService),
    saveProductionSnapshot: workspaceService.saveProductionSnapshot.bind(workspaceService),
    deleteEntity: workspaceService.deleteEntity.bind(workspaceService),
    createSetting(input) {
      return saveSetting(workspaceService, input);
    },
    updateSetting(input) {
      return saveSetting(workspaceService, input);
    },
    createSettingState(input) {
      return saveSettingState(workspaceService, input);
    },
    updateSettingState(input) {
      return saveSettingState(workspaceService, input);
    },
    createAsset(input) {
      return saveAsset(workspaceService, input);
    },
    updateAsset(input) {
      return saveAsset(workspaceService, input);
    },
    writeHierarchyNode(input) {
      return writeHierarchyNode(workspaceService, input);
    },
    updateEntityBasics(input) {
      return updateEntityBasics(workspaceService, input);
    },
    connectSceneMomentSetting(input) {
      return connectSceneMomentSetting(workspaceService, input);
    },
    listProductions(input = {}) {
      return workspaceService.queryEntities({ entityKind: "production", query: input.query, limit: input.limit });
    },
    createProduction(input = {}) {
      return saveProduction(workspaceService, input);
    },
    updateProduction(input) {
      return saveProduction(workspaceService, input);
    },
    deleteProduction(input) {
      return deletePlanningEntity(workspaceService, "production", input);
    },
    listSegments(input = {}) {
      return workspaceService.queryEntities(planningQuery("segment", input));
    },
    createSegment(input) {
      return saveSegment(workspaceService, input);
    },
    updateSegment(input) {
      return saveSegment(workspaceService, input);
    },
    deleteSegment(input) {
      return deletePlanningEntity(workspaceService, "segment", input);
    },
    listSceneMoments(input = {}) {
      return workspaceService.queryEntities(planningQuery("scene_moment", input));
    },
    createSceneMoment(input) {
      return saveSceneMoment(workspaceService, input);
    },
    updateSceneMoment(input) {
      return saveSceneMoment(workspaceService, input);
    },
    deleteSceneMoment(input) {
      return deletePlanningEntity(workspaceService, "scene_moment", input);
    },
    listStoryboards(input = {}) {
      return workspaceService.queryEntities(planningQuery("storyboard", input));
    },
    createStoryboard(input) {
      return saveStoryboard(workspaceService, input);
    },
    updateStoryboard(input) {
      return saveStoryboard(workspaceService, input);
    },
    deleteStoryboard(input) {
      return deletePlanningEntity(workspaceService, "storyboard", input);
    },
    listKeyframes(input = {}) {
      return workspaceService.queryEntities(planningQuery("keyframe", input));
    },
    createKeyframe(input) {
      return saveKeyframe(workspaceService, input);
    },
    updateKeyframe(input) {
      return saveKeyframe(workspaceService, input);
    },
    deleteKeyframe(input) {
      return deletePlanningEntity(workspaceService, "keyframe", input);
    },
    listAudioCues(input = {}) {
      return workspaceService.queryEntities(planningQuery("audio_cue", input));
    },
    createAudioCue(input) {
      return saveAudioCue(workspaceService, input);
    },
    updateAudioCue(input) {
      return saveAudioCue(workspaceService, input);
    },
    deleteAudioCue(input) {
      return deletePlanningEntity(workspaceService, "audio_cue", input);
    },
    listExpressionUnits(input = {}) {
      return workspaceService.queryEntities(planningQuery("expression_unit", input));
    },
    createExpressionUnit(input) {
      return saveExpressionUnit(workspaceService, input);
    },
    updateExpressionUnit(input) {
      return saveExpressionUnit(workspaceService, input);
    },
    deleteExpressionUnit(input) {
      return deletePlanningEntity(workspaceService, "expression_unit", input);
    },
    listContentUnits(input = {}) {
      return listContentUnits(workspaceService, input);
    },
    createContentUnit(input) {
      return saveContentUnit(workspaceService, input);
    },
    updateContentUnit(input) {
      return saveContentUnit(workspaceService, input);
    },
    ensureContentUnitForEntity(input) {
      return ensureContentUnitForEntity(workspaceService, input);
    },
    updateContentUnitEditPrompt: workspaceService.updateContentUnitEditPrompt.bind(workspaceService),
    deleteContentUnit(input) {
      return deletePlanningEntity(workspaceService, "content_unit", input);
    },
    deriveContentUnitArtifact(contentUnitId) {
      return callRequiredOperation(
        deriveContentUnitArtifact2 ? () => deriveContentUnitArtifact2(contentUnitId) : void 0,
        "deriveContentUnitArtifact"
      );
    },
    buildContentUnitBackendPrompt(contentUnitId, options2) {
      return callRequiredOperation(
        buildContentUnitBackendPrompt2 ? () => buildContentUnitBackendPrompt2(contentUnitId, options2) : void 0,
        "buildContentUnitBackendPrompt"
      );
    },
    deriveArtifacts(input = {}) {
      return callRequiredOperation(
        deriveArtifacts ? () => deriveArtifacts(input) : void 0,
        "deriveArtifacts"
      );
    },
    overview() {
      return callRequiredOperation(overviewWorkspace, "overview");
    },
    inspect(input = {}) {
      const operation = inspectWorkspace ?? reviewWorkspace;
      return callRequiredOperation(operation ? () => operation(input) : void 0, "inspect");
    },
    review(input = {}) {
      const operation = inspectWorkspace ?? reviewWorkspace;
      return callRequiredOperation(operation ? () => operation(input) : void 0, "review");
    },
    interpret() {
      return callRequiredOperation(interpretWorkspace, "interpret");
    },
    productionWorkPlan() {
      return callRequiredOperation(productionWorkPlan, "productionWorkPlan");
    },
    regenerationPlan() {
      return callRequiredOperation(regenerationPlan, "regenerationPlan");
    },
    publish(input = {}) {
      return callRequiredOperation(
        options.publish ? () => options.publish(input) : void 0,
        "publish"
      );
    },
    appendCandidate: workspaceService.appendCandidate.bind(workspaceService),
    createContentCandidate: workspaceService.createContentCandidate.bind(workspaceService),
    selectContentUnitCandidate: workspaceService.selectContentUnitCandidate.bind(workspaceService),
    createAssetSlotCandidate: workspaceService.createAssetSlotCandidate.bind(workspaceService),
    createKeyframeCandidate: workspaceService.createKeyframeCandidate.bind(workspaceService),
    selectCandidate: workspaceService.selectCandidate.bind(workspaceService),
    updateCandidate: workspaceService.updateCandidate.bind(workspaceService),
    unlockCandidate: workspaceService.unlockCandidate.bind(workspaceService)
  };
}
function planningQuery(entityKind, input = {}) {
  return pruneUndefined13({
    entityKind,
    kind: input.kind,
    query: input.query,
    productionId: input.productionId,
    segmentId: input.segmentId,
    sceneMomentId: input.sceneMomentId,
    limit: input.limit
  });
}
async function saveSetting(workspaceService, input) {
  const id = await engineEntityId(workspaceService, "setting", input.id, input.title);
  return workspaceService.upsertSetting({
    payload: pruneUndefined13({
      id,
      title: input.title,
      setting_kind: input.kind,
      namespace_kind: input.namespaceKind ?? input.settingNamespaceKind,
      setting_namespace_kind: input.settingNamespaceKind ?? input.namespaceKind,
      description: input.description,
      alias: input.alias,
      content: input.content,
      importance: input.importance
    })
  });
}
async function saveSettingState(workspaceService, input) {
  const id = await engineEntityId(workspaceService, "setting_state", input.id, input.title);
  return workspaceService.upsertSettingState({
    payload: pruneUndefined13({
      id,
      setting_id: input.settingId,
      title: input.title,
      state_kind: input.stateKind,
      namespace_kind: input.namespaceKind ?? input.settingNamespaceKind,
      setting_namespace_kind: input.settingNamespaceKind ?? input.namespaceKind,
      description: input.description
    })
  });
}
async function saveAsset(workspaceService, input) {
  const id = await engineEntityId(workspaceService, "asset", input.id, input.title ?? input.slot);
  return workspaceService.upsertAsset({
    payload: pruneUndefined13({
      id,
      title: input.title,
      setting_id: input.settingId,
      setting_state_id: input.settingStateId,
      slot: input.slot,
      asset_kind: input.assetKind,
      prompt_hint: input.promptHint,
      resource_id: input.resourceId
    })
  });
}
async function writeHierarchyNode(workspaceService, input) {
  const record = input.record;
  const entityKind = stringValue9(record.kind) ?? entityKindFromPath2(input.targetPath);
  if (!entityKind) throw new Error("hierarchy node kind is required");
  if (entityKind === "setting") {
    return workspaceService.upsertSetting({ entity: { path: input.targetPath, record }, payload: record });
  }
  if (entityKind === "setting_state") {
    return workspaceService.upsertSettingState({ entity: { path: input.targetPath, record }, payload: record });
  }
  if (entityKind === "asset") {
    return workspaceService.upsertAsset({ entity: { path: input.targetPath, record }, payload: record });
  }
  if (entityKind === "content_unit") {
    return saveContentUnit(workspaceService, contentUnitInputFromPatchedRecord(record, {
      targetPath: input.targetPath,
      record,
      title: stringValue9(record.title) ?? "Untitled",
      summary: stringValue9(record.description) ?? ""
    }));
  }
  return workspaceService.upsertSourceRecord({
    targetPath: input.targetPath,
    record: pathPreservingHierarchyRecord(input.targetPath, record, entityKind)
  });
}
async function updateEntityBasics(workspaceService, input) {
  const record = input.record ?? {};
  const entityKind = input.entityKind ?? stringValue9(record.kind) ?? entityKindFromPath2(input.targetPath);
  if (!entityKind) throw new Error("entityKind is required to update entity basics");
  const patched = patchEntityBasics(record, input);
  const entity = input.targetPath ? { path: input.targetPath, record } : void 0;
  if (entityKind === "setting") {
    return workspaceService.upsertSetting({ entity, record, payload: patched });
  }
  if (entityKind === "setting_state") {
    return workspaceService.upsertSettingState({ entity, record, payload: patched });
  }
  if (entityKind === "asset") {
    return workspaceService.upsertAsset({ entity, record, payload: patched });
  }
  if (entityKind === "content_unit") {
    return saveContentUnit(workspaceService, contentUnitInputFromPatchedRecord(patched, input));
  }
  if (input.targetPath) {
    return workspaceService.upsertSourceRecord({
      targetPath: input.targetPath,
      record: pathPreservingHierarchyRecord(input.targetPath, patched, entityKind)
    });
  }
  if (entityKind === "production") {
    return saveProduction(workspaceService, {
      id: input.id ?? idValue2(record.id) ?? pathSegmentAfter6(input.targetPath ?? "", "productions"),
      title: stringValue9(patched.title)
    });
  }
  if (entityKind === "segment") {
    return saveSegment(workspaceService, {
      productionId: input.productionId ?? pathSegmentAfter6(input.targetPath ?? "", "productions"),
      id: input.id ?? idValue2(record.id) ?? pathSegmentAfter6(input.targetPath ?? "", "segments"),
      title: stringValue9(patched.title),
      summary: stringValue9(patched.summary ?? patched.description),
      kind: stringValue9(patched.segment_kind ?? patched.kind)
    });
  }
  if (entityKind === "scene_moment") {
    return saveSceneMoment(workspaceService, {
      productionId: input.productionId ?? pathSegmentAfter6(input.targetPath ?? "", "productions"),
      segmentId: input.segmentId ?? pathSegmentAfter6(input.targetPath ?? "", "segments"),
      id: input.id ?? idValue2(record.id) ?? pathSegmentAfter6(input.targetPath ?? "", "scene_moments"),
      title: stringValue9(patched.title),
      actionText: stringValue9(patched.action_text ?? patched.action),
      description: stringValue9(patched.description),
      settings: settingRefsFromRecord(patched)
    });
  }
  if (entityKind === "expression_unit") {
    return saveExpressionUnit(workspaceService, {
      productionId: input.productionId ?? pathSegmentAfter6(input.targetPath ?? "", "productions"),
      segmentId: input.segmentId ?? pathSegmentAfter6(input.targetPath ?? "", "segments"),
      sceneMomentId: input.sceneMomentId ?? idValue2(record.scene_moment_id) ?? pathSegmentAfter6(input.targetPath ?? "", "scene_moments"),
      id: input.id ?? idValue2(record.id) ?? pathSegmentAfter6(input.targetPath ?? "", "expression_units"),
      title: stringValue9(patched.title),
      slotKind: stringValue9(patched.slot_kind ?? patched.slotKind),
      kind: stringValue9(patched.kind),
      text: stringValue9(patched.text ?? patched.summary ?? patched.description),
      intent: stringValue9(patched.intent)
    });
  }
  if (entityKind === "keyframe") {
    return saveKeyframe(workspaceService, {
      productionId: input.productionId ?? pathSegmentAfter6(input.targetPath ?? "", "productions"),
      segmentId: input.segmentId ?? pathSegmentAfter6(input.targetPath ?? "", "segments"),
      sceneMomentId: input.sceneMomentId ?? idValue2(record.scene_moment_id) ?? pathSegmentAfter6(input.targetPath ?? "", "scene_moments"),
      expressionUnitId: input.expressionUnitId ?? idValue2(record.expression_unit_id) ?? pathSegmentAfter6(input.targetPath ?? "", "expression_units"),
      id: input.id ?? idValue2(record.id) ?? pathSegmentAfter6(input.targetPath ?? "", "keyframes"),
      title: stringValue9(patched.title),
      role: stringValue9(patched.role),
      visualIntent: stringValue9(patched.visual_intent ?? patched.description),
      timing: isRecord13(patched.timing) ? patched.timing : void 0,
      composition: isRecord13(patched.composition) ? patched.composition : void 0,
      continuity: isRecord13(patched.continuity) ? patched.continuity : void 0,
      referenceAssetRefs: Array.isArray(patched.reference_asset_refs) ? patched.reference_asset_refs : void 0,
      referenceKeyframeRefs: Array.isArray(patched.reference_keyframe_refs) ? patched.reference_keyframe_refs : void 0
    });
  }
  if (entityKind === "storyboard") {
    return saveStoryboard(workspaceService, {
      productionId: input.productionId ?? pathSegmentAfter6(input.targetPath ?? "", "productions"),
      segmentId: input.segmentId ?? pathSegmentAfter6(input.targetPath ?? "", "segments"),
      sceneMomentId: input.sceneMomentId ?? idValue2(record.scene_moment_id) ?? pathSegmentAfter6(input.targetPath ?? "", "scene_moments"),
      expressionUnitId: input.expressionUnitId ?? idValue2(record.expression_unit_id) ?? pathSegmentAfter6(input.targetPath ?? "", "expression_units"),
      id: input.id ?? idValue2(record.id) ?? pathSegmentAfter6(input.targetPath ?? "", "storyboards"),
      title: stringValue9(patched.title),
      visualIntent: stringValue9(patched.visual_intent ?? patched.description),
      timeline: isRecord13(patched.timeline) ? patched.timeline : void 0,
      graph: isRecord13(patched.graph) ? patched.graph : void 0
    });
  }
  throw new Error(`Unsupported entity basics update kind: ${entityKind}`);
}
function connectSceneMomentSetting(workspaceService, input) {
  const sceneMomentRecord = input.sceneMomentRecord ?? {};
  const sceneMomentId = requiredId(
    input.sceneMomentId ?? idValue2(sceneMomentRecord.id) ?? idValue2(sceneMomentRecord.client_id),
    "sceneMomentId"
  );
  const currentRefs = settingRefsFromRecord(sceneMomentRecord);
  const settingId = requiredId(input.settingId, "settingId");
  const settingStateId = idValue2(input.settingStateId);
  const alreadyLinked = currentRefs.some((ref) => String(idValue2(ref.id ?? ref.settingId) ?? "") === String(settingId) && String(idValue2(ref.settingStateId) ?? "") === String(settingStateId ?? ""));
  return saveSceneMoment(workspaceService, {
    productionId: input.productionId ?? pathSegmentAfter6(stringValue9(sceneMomentRecord.__workspace_path ?? sceneMomentRecord.workspace_path ?? "") ?? "", "productions"),
    segmentId: input.segmentId ?? pathSegmentAfter6(stringValue9(sceneMomentRecord.__workspace_path ?? sceneMomentRecord.workspace_path ?? "") ?? "", "segments"),
    id: sceneMomentId,
    title: stringValue9(sceneMomentRecord.title),
    actionText: stringValue9(sceneMomentRecord.action_text ?? sceneMomentRecord.action),
    description: stringValue9(sceneMomentRecord.description),
    settings: alreadyLinked ? currentRefs : [
      ...currentRefs,
      {
        id: settingId,
        settingStateId,
        role: input.role ?? "scene_constraint"
      }
    ]
  });
}
async function saveProduction(workspaceService, input) {
  const productionId = input.id === void 0 && !stringValue9(input.title) ? "main" : await engineEntityId(workspaceService, "production", input.id, input.title);
  return workspaceService.saveProductionSnapshot({
    productionId,
    snapshot: {
      production: pruneUndefined13({ title: input.title }),
      segments: []
    }
  });
}
async function saveSegment(workspaceService, input) {
  const segmentId = await engineEntityId(workspaceService, "segment", input.id, input.title);
  return workspaceService.saveProductionSnapshot({
    productionId: input.productionId ?? "main",
    snapshot: {
      segments: [pruneUndefined13({
        id: segmentId,
        title: input.title,
        kind: input.kind,
        summary: input.summary,
        order: input.order
      })]
    }
  });
}
async function saveSceneMoment(workspaceService, input) {
  const segmentId = requiredId(input.segmentId, "segmentId");
  const sceneMomentId = await engineEntityId(workspaceService, "scene_moment", input.id, input.title);
  return workspaceService.saveProductionSnapshot({
    productionId: input.productionId ?? "main",
    snapshot: {
      segments: [{
        id: segmentId,
        scene_moments: [pruneUndefined13({
          id: sceneMomentId,
          title: input.title,
          storyboard_id: input.storyboardId,
          order: input.order,
          time_text: input.timeText,
          scene_code: input.sceneCode,
          location_text: input.locationText,
          condition_text: input.conditionText,
          action_text: input.actionText,
          mood: input.mood,
          description: input.description,
          settings: input.settings?.map(settingRefInput)
        })]
      }]
    }
  });
}
async function saveStoryboard(workspaceService, input) {
  const segmentId = requiredId(input.segmentId, "segmentId");
  const sceneMomentId = requiredId(input.sceneMomentId, "sceneMomentId");
  const expressionUnitId = input.expressionUnitId;
  const storyboardId = await engineEntityId(workspaceService, "storyboard", input.id, input.title);
  return workspaceService.saveProductionSnapshot({
    productionId: input.productionId ?? "main",
    snapshot: {
      segments: [{
        id: segmentId,
        scene_moments: [{
          id: sceneMomentId,
          ...expressionUnitId ? {
            expression_units: [{
              id: expressionUnitId,
              slotKind: "visual",
              kind: "shot",
              storyboards: [pruneUndefined13({
                id: storyboardId,
                title: input.title,
                visual_intent: input.visualIntent,
                order: input.order,
                timeline: input.timeline,
                graph: input.graph
              })]
            }]
          } : {
            storyboards: [pruneUndefined13({
              id: storyboardId,
              title: input.title,
              visual_intent: input.visualIntent,
              order: input.order,
              timeline: input.timeline,
              graph: input.graph
            })]
          }
        }]
      }]
    }
  });
}
async function saveKeyframe(workspaceService, input) {
  const segmentId = requiredId(input.segmentId, "segmentId");
  const sceneMomentId = requiredId(input.sceneMomentId, "sceneMomentId");
  const expressionUnitId = input.expressionUnitId;
  const keyframeId = await engineEntityId(workspaceService, "keyframe", input.id, input.title);
  return workspaceService.saveProductionSnapshot({
    productionId: input.productionId ?? "main",
    snapshot: {
      segments: [{
        id: segmentId,
        scene_moments: [{
          id: sceneMomentId,
          ...expressionUnitId ? {
            expression_units: [{
              id: expressionUnitId,
              slotKind: "visual",
              kind: "shot",
              keyframes: [pruneUndefined13({
                id: keyframeId,
                title: input.title,
                role: input.role,
                visual_intent: input.visualIntent,
                order: input.order,
                timing: input.timing,
                composition: input.composition,
                continuity: input.continuity,
                reference_asset_refs: input.referenceAssetRefs,
                reference_keyframe_refs: input.referenceKeyframeRefs
              })]
            }]
          } : {
            keyframes: [pruneUndefined13({
              id: keyframeId,
              title: input.title,
              role: input.role,
              visual_intent: input.visualIntent,
              order: input.order,
              timing: input.timing,
              composition: input.composition,
              continuity: input.continuity,
              reference_asset_refs: input.referenceAssetRefs,
              reference_keyframe_refs: input.referenceKeyframeRefs
            })]
          }
        }]
      }]
    }
  });
}
async function saveAudioCue(workspaceService, input) {
  const segmentId = requiredId(input.segmentId, "segmentId");
  const sceneMomentId = requiredId(input.sceneMomentId, "sceneMomentId");
  const audioCueId = await engineEntityId(workspaceService, "audio_cue", input.id, input.title);
  return workspaceService.saveProductionSnapshot({
    productionId: input.productionId ?? "main",
    snapshot: {
      segments: [{
        id: segmentId,
        scene_moments: [{
          id: sceneMomentId,
          audio_cues: [pruneUndefined13({
            id: audioCueId,
            title: input.title,
            kind: input.kind,
            order: input.order,
            storyboard_id: input.storyboardId,
            expression_unit_ref: input.expressionUnitId,
            prompt_hint: input.promptHint
          })]
        }]
      }]
    }
  });
}
async function saveExpressionUnit(workspaceService, input) {
  const segmentId = requiredId(input.segmentId, "segmentId");
  const sceneMomentId = requiredId(input.sceneMomentId, "sceneMomentId");
  const expressionUnitId = await engineEntityId(workspaceService, "expression_unit", input.id, input.title ?? input.text);
  return workspaceService.saveProductionSnapshot({
    productionId: input.productionId ?? "main",
    snapshot: {
      segments: [{
        id: segmentId,
        scene_moments: [{
          id: sceneMomentId,
          expression_units: [pruneUndefined13({
            id: expressionUnitId,
            title: input.title,
            slot_kind: normalizeExpressionUnitSlotKind(input.slotKind ?? input.kind ?? input.role ?? input.modality),
            modality: input.modality,
            role: input.role,
            kind: input.kind,
            visual_kind: input.visualKind,
            speaker: input.speaker,
            speaker_ref: input.speakerRef,
            source_expression_ref: input.sourceExpressionRef,
            text: input.text,
            note: input.note,
            intent: input.intent,
            content: input.content,
            timing_intent: input.timingIntent,
            voice_profile_ref: input.voiceProfileRef,
            order: input.order,
            span: input.span,
            script_block_id: input.scriptBlockId
          })]
        }]
      }]
    }
  });
}
async function saveContentUnit(workspaceService, input) {
  const id = await engineEntityId(workspaceService, "content_unit", input.id, input.title);
  return workspaceService.upsertContentUnit({
    unit: pruneUndefined13({
      id,
      title: input.title,
      content_unit_type: input.contentUnitType ?? input.kind ?? "storyboard_ref",
      output_kind: input.outputKind ?? defaultContentUnitOutputKind(input.contentUnitType ?? input.kind ?? "storyboard_ref"),
      target_category: input.targetCategory,
      target_kind: input.targetKind,
      target_ref: input.targetRef,
      scope_kind: input.scopeKind,
      scope_ref: input.scopeRef,
      generation_role: input.generationRole,
      edit_prompt: pruneUndefined13({
        text: input.prompt?.trim() || void 0,
        negative_text: input.negativePrompt
      }),
      model_intent: pruneUndefined13({
        ...input.modelIntent ?? {},
        capability: input.capability,
        provider: input.provider,
        model: input.model,
        quality: input.quality,
        aspect_ratio: input.aspectRatio,
        duration_sec: input.durationSeconds,
        params: pruneUndefined13({
          ...input.params ?? {},
          shot_size: input.shotSize,
          camera_angle: input.cameraAngle,
          camera_motion: input.cameraMotion
        })
      }),
      description: input.description,
      order: input.order,
      asset_ref: input.assetRef,
      production_ref: input.productionId,
      segment_ref: input.segmentId,
      scene_moment_ref: input.sceneMomentId,
      expression_unit_ref: input.expressionUnitRef ?? input.expressionUnitId,
      storyboard_ref: input.storyboardId,
      keyframe_ref: input.keyframeId,
      audio_cue_ref: input.audioCueId
    })
  });
}
async function ensureContentUnitForEntity(workspaceService, input) {
  const target = normalizeContentUnitTarget2(input);
  const contentUnitType2 = input.contentUnitType ?? contentUnitTypeForTargetKind(input.targetKind);
  const outputKind = input.outputKind ?? defaultContentUnitOutputKind(contentUnitType2);
  const existing = (await workspaceService.queryEntities({ entityKind: "content_unit" })).find((entity) => {
    const recordType = String(entity.record.content_unit_type ?? "");
    if (!contentUnitTypeMatchesTarget(recordType, contentUnitType2, input.targetKind, target)) return false;
    return contentUnitRecordMatchesTarget(entity.record, input.targetKind, target);
  });
  if (existing) {
    if (input.prompt === void 0 && input.negativePrompt === void 0) {
      return { contentUnitPath: existing.path, record: existing.record };
    }
    const existingContentUnitType = stringValue9(existing.record.content_unit_type);
    return saveContentUnit(workspaceService, {
      ...contentUnitInputForTarget(input.targetKind, target),
      id: idValue2(existing.id) ?? idValue2(existing.record.id) ?? contentUnitIdForTarget(input.targetKind, target.targetRef),
      title: input.title ?? stringValue9(existing.record.title),
      contentUnitType: input.contentUnitType ?? existingContentUnitType ?? contentUnitType2,
      outputKind,
      prompt: input.prompt,
      negativePrompt: input.negativePrompt,
      description: input.description ?? stringValue9(existing.record.description),
      order: input.order ?? numberValue4(existing.record.order),
      modelIntent: input.modelIntent ?? recordValue5(existing.record.model_intent)
    });
  }
  return saveContentUnit(workspaceService, {
    ...contentUnitInputForTarget(input.targetKind, target),
    id: input.id ?? contentUnitIdForTarget(input.targetKind, target.targetRef),
    title: input.title ?? `${targetKindLabel(input.targetKind)} ${target.targetRef} \u5236\u4F5C\u9879`,
    contentUnitType: contentUnitType2,
    outputKind,
    prompt: input.prompt,
    negativePrompt: input.negativePrompt,
    description: input.description ?? `\u4ECE ${targetKindLabel(input.targetKind)}\u300C${target.targetRef}\u300D\u521B\u5EFA\u3002`,
    order: input.order,
    modelIntent: {
      source: "engine",
      target_kind: input.targetKind,
      target_ref: target.targetRef,
      ...target.scopeKind && target.scopeRef ? { scope_kind: target.scopeKind, scope_ref: target.scopeRef } : {},
      ...input.modelIntent ?? {}
    }
  });
}
function normalizeContentUnitTarget2(input) {
  if (input.targetKind !== "timeline_assembly") {
    return { targetRef: normalizeEntityRef(input.targetRef ?? input.targetId, input.targetKind) };
  }
  const scopeKind = stringValue9(input.scopeKind);
  const scopeRef = idStringValue(input.scopeRef);
  const rawRef = idStringValue(input.targetRef ?? input.targetId);
  const parsedRef = parseImplicitTimelineAssemblyRef(rawRef);
  const normalizedScopeKind = scopeKind ?? parsedRef?.scopeKind;
  const normalizedScopeRef = scopeRef ?? parsedRef?.scopeRef ?? (!parsedRef && scopeKind ? rawRef : void 0);
  if (!normalizedScopeKind || !normalizedScopeRef) {
    throw new Error("timeline_assembly target requires targetRef timeline_assembly:<scopeKind>:<scopeRef> or scopeKind/scopeRef");
  }
  return {
    targetRef: implicitTimelineAssemblyRef(normalizedScopeKind, normalizedScopeRef),
    scopeKind: normalizedScopeKind,
    scopeRef: normalizedScopeRef
  };
}
async function engineEntityId(workspaceService, entityKind, explicitId, title) {
  const id = idValue2(explicitId);
  if (id !== void 0) return id;
  const entities = await workspaceService.queryEntities({ entityKind }).catch(() => []);
  return allocateMovScriptEntityId({
    entityKind,
    title,
    existingIds: entities.flatMap((entity) => [
      idStringValue(entity.id),
      idStringValue(entity.record.id),
      idStringValue(entity.record.ID)
    ]).filter((value) => Boolean(value))
  });
}
function contentUnitTypeMatchesTarget(recordType, requestedType, targetKind, target) {
  if (recordType === requestedType) return true;
  if (targetKind !== "timeline_assembly" || requestedType !== "timeline_assembly_ref") return false;
  if (target.scopeKind === "production" && recordType === "production_ref") return true;
  if (target.scopeKind === "segment" && recordType === "segment_ref") return true;
  return false;
}
function contentUnitRecordMatchesTarget(record, targetKind, target) {
  if (targetKind === "timeline_assembly") {
    const targetRefs = compactStrings2(record.target_ref, record.targetRef);
    if (targetRefs.some((ref) => sameEntityRef2(ref, target.targetRef))) return true;
    const recordScopeKind = stringValue9(record.scope_kind ?? record.scopeKind);
    const recordScopeRef = stringValue9(record.scope_ref ?? record.scopeRef);
    if (recordScopeKind && recordScopeRef && target.scopeKind === recordScopeKind && sameEntityRef2(recordScopeRef, target.scopeRef)) return true;
    if (target.scopeKind === "production") return compactStrings2(record.production_ref, record.productionRef).some((ref) => sameEntityRef2(ref, target.scopeRef));
    if (target.scopeKind === "segment") return compactStrings2(record.segment_ref, record.segmentRef).some((ref) => sameEntityRef2(ref, target.scopeRef));
    return false;
  }
  return compactStrings2(record[primaryRefFieldForTargetKind(targetKind)]).some((ref) => sameEntityRef2(ref, target.targetRef));
}
function defaultContentUnitOutputKind(contentUnitType2) {
  switch (contentUnitType2) {
    case "asset_ref":
    case "keyframe_ref":
    case "storyboard_ref":
      return "image";
    case "timeline_assembly_ref":
    case "scence_moment_ref":
    case "scene_moment_ref":
    case "production_ref":
    case "segment_ref":
      return "video";
    case "expression_unit_ref":
      return defaultOutputKindForExpressionUnitSlot(void 0);
    default:
      return "metadata";
  }
}
function entityKindFromPath2(path) {
  if (!path) return void 0;
  if (path.endsWith("/setting.json")) return "setting";
  if (path.endsWith("/setting_state.json")) return "setting_state";
  if (path.endsWith("/asset.json")) return "asset";
  if (path.endsWith("/production.json")) return "production";
  if (path.endsWith("/segment.json")) return "segment";
  if (path.endsWith("/scene_moment.json")) return "scene_moment";
  if (path.endsWith("/expression_unit.json")) return "expression_unit";
  if (path.endsWith("/audio_cue.json")) return "audio_cue";
  if (path.endsWith("/keyframe.json")) return "keyframe";
  if (path.endsWith("/storyboard.json")) return "storyboard";
  if (path.endsWith("/shot.json")) return "shot";
  if (path.endsWith("/content_unit.json")) return "content_unit";
  return void 0;
}
function pathPreservingHierarchyRecord(targetPath, record, entityKind) {
  const kind = stringValue9(record.kind) ?? entityKind;
  return pruneUndefined13({
    ...record,
    schema: stringValue9(record.schema) ?? `movscript.${kind}.v1`,
    kind,
    id: idValue2(record.id) ?? entityIdFromTargetPath(targetPath, kind),
    title: stringValue9(record.title)
  });
}
function entityIdFromTargetPath(targetPath, entityKind) {
  const collection = collectionSegmentForSourceKind(entityKind);
  const collectionId = collection ? pathSegmentAfter6(targetPath, collection) : void 0;
  if (collectionId) return collectionId;
  const parts = normalizePath(targetPath).split("/").filter(Boolean);
  const filename = parts.at(-1);
  if (filename?.endsWith(".json")) return parts.at(-2);
  return parts.at(-1);
}
function collectionSegmentForSourceKind(entityKind) {
  switch (entityKind) {
    case "production":
      return "productions";
    case "segment":
      return "segments";
    case "scene_moment":
      return "scene_moments";
    case "expression_unit":
      return "expression_units";
    case "audio_cue":
      return "audio_cues";
    case "keyframe":
      return "keyframes";
    case "storyboard":
      return "storyboards";
    case "content_unit":
      return "content_units";
    case "setting":
      return "settings";
    case "setting_state":
      return "states";
    case "asset":
      return "assets";
    case "shot":
      return "shots";
    default:
      return void 0;
  }
}
function patchEntityBasics(record, input) {
  const next = { ...record };
  if ("title" in next || !("name" in next) && !("label" in next)) {
    next.title = input.title;
  } else if ("name" in next) {
    next.name = input.title;
  } else {
    next.label = input.title;
  }
  if ("summary" in next) {
    next.summary = input.summary;
  } else if ("description" in next || !("action_text" in next) && !("action" in next) && !("prompt" in next)) {
    next.description = input.summary;
  } else if ("action_text" in next) {
    next.action_text = input.summary;
  } else if ("action" in next) {
    next.action = input.summary;
  } else if (typeof next.prompt === "string") {
    next.prompt = input.summary;
  }
  return next;
}
function contentUnitInputFromPatchedRecord(record, input) {
  return {
    id: input.id ?? idValue2(record.id) ?? pathSegmentAfter6(input.targetPath ?? "", "content_units"),
    title: stringValue9(record.title),
    contentUnitType: stringValue9(record.content_unit_type ?? record.kind),
    outputKind: stringValue9(record.output_kind),
    assetRef: idValue2(record.asset_ref),
    sceneMomentId: idValue2(record.scene_moment_ref),
    expressionUnitId: idValue2(record.expression_unit_ref),
    keyframeId: idValue2(record.keyframe_ref),
    storyboardId: idValue2(record.storyboard_ref),
    audioCueId: idValue2(record.audio_cue_ref),
    prompt: stringValue9(record.edit_prompt) ?? stringValue9(record.prompt),
    description: stringValue9(record.description),
    modelIntent: recordValue5(record.model_intent)
  };
}
function settingRefsFromRecord(record) {
  const refs = Array.isArray(record.setting_refs) ? record.setting_refs.filter(isRecord13) : [];
  return refs.flatMap((ref) => {
    const settingId = idValue2(ref.setting_id ?? ref.settingId ?? ref.setting_ref ?? ref.settingRef);
    if (!settingId) return [];
    return [{
      id: settingId,
      settingStateId: idValue2(ref.setting_state_id ?? ref.settingStateId ?? ref.setting_state_ref ?? ref.settingStateRef),
      role: stringValue9(ref.role),
      sourceLabel: stringValue9(ref.notes ?? ref.source_label ?? ref.sourceLabel),
      kind: stringValue9(ref.setting_kind ?? ref.kind)
    }];
  });
}
function settingRefInput(input) {
  return pruneUndefined13({
    id: input.id ?? input.settingId,
    setting_state_id: input.settingStateId,
    role: input.role,
    source_label: input.sourceLabel,
    kind: input.kind
  });
}
function contentUnitTypeForTargetKind(kind) {
  if (kind === "timeline_assembly") return "timeline_assembly_ref";
  return `${kind}_ref`;
}
function primaryRefFieldForTargetKind(kind) {
  if (kind === "timeline_assembly") return "target_ref";
  return kind === "scene_moment" ? "scene_moment_ref" : `${kind}_ref`;
}
function contentUnitInputForTarget(kind, target) {
  if (kind === "timeline_assembly") {
    return {
      targetCategory: "timeline_assembly",
      targetKind: "timeline_assembly",
      targetRef: target.targetRef,
      scopeKind: target.scopeKind,
      scopeRef: target.scopeRef
    };
  }
  if (kind === "asset") return { assetRef: target.targetRef };
  if (kind === "scene_moment") return { sceneMomentId: target.targetRef };
  if (kind === "expression_unit") return { expressionUnitId: target.targetRef };
  if (kind === "keyframe") return { keyframeId: target.targetRef };
  return { storyboardId: target.targetRef };
}
function contentUnitIdForTarget(kind, targetRef) {
  return `cu_${kind}_${safeToken(targetRef)}`;
}
function targetKindLabel(kind) {
  if (kind === "timeline_assembly") return "\u526A\u8F91\u805A\u5408";
  if (kind === "asset") return "\u7D20\u6750";
  if (kind === "scene_moment") return "\u60C5\u8282";
  if (kind === "expression_unit") return "\u8868\u8FBE\u5355\u5143";
  if (kind === "keyframe") return "\u5173\u952E\u5E27";
  return "\u5206\u955C\u56FE";
}
function normalizeEntityRef(value, kind) {
  if (value === void 0 || value === null || String(value).trim() === "") throw new Error(`${kind} ref is required`);
  const raw = normalizePath(String(value));
  const collection = collectionSegmentForTargetKind(kind);
  const pathId = pathSegmentAfter6(raw, collection);
  if (pathId) return pathId;
  if (raw.endsWith(".json")) {
    const parts = raw.split("/").filter(Boolean);
    const parent = parts.at(-2);
    if (parent) return parent;
  }
  const separator = raw.indexOf(":");
  return separator > 0 ? raw.slice(separator + 1) : raw;
}
function collectionSegmentForTargetKind(kind) {
  if (kind === "timeline_assembly") return "timeline_assemblies";
  if (kind === "scene_moment") return "scene_moments";
  if (kind === "expression_unit") return "expression_units";
  if (kind === "keyframe") return "keyframes";
  if (kind === "storyboard") return "storyboards";
  return "assets";
}
function sameEntityRef2(left, right) {
  const rightAliases = new Set(entityRefAliases2(right));
  return entityRefAliases2(left).some((alias) => rightAliases.has(alias));
}
function entityRefAliases2(value) {
  if (value === void 0 || value === null || String(value).trim() === "") return [];
  const raw = normalizePath(String(value));
  const parts = raw.split("/").filter(Boolean);
  const aliases = /* @__PURE__ */ new Set([raw]);
  const tail = parts.at(-1);
  if (tail) aliases.add(tail);
  if (tail?.endsWith(".json") && parts.at(-2)) aliases.add(parts.at(-2));
  for (const marker of ["assets", "scene_moments", "expression_units", "keyframes", "storyboards", "content_units"]) {
    const segment = pathSegmentAfter6(raw, marker);
    if (segment) aliases.add(segment);
  }
  return [...aliases];
}
function compactStrings2(...values) {
  return values.flatMap((value) => {
    if (Array.isArray(value)) return value.filter((item) => typeof item === "string" && Boolean(item.trim())).map((item) => item.trim());
    return typeof value === "string" && value.trim() ? [value.trim()] : [];
  });
}
function recordValue5(value) {
  return isRecord13(value) ? value : void 0;
}
function numberValue4(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && /^-?\d+(\.\d+)?$/.test(value.trim())) return Number(value);
  return void 0;
}
function safeToken(value) {
  return String(value).trim().replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "") || "local";
}
async function listContentUnits(workspaceService, input) {
  const entities = await workspaceService.queryEntities({
    entityKind: "content_unit",
    query: input.query
  });
  const filtered = entities.filter((entity) => {
    if (input.kind && String(entity.record.content_unit_type ?? entity.record.kind) !== input.kind) return false;
    const sceneMomentRef = stringValue9(entity.record.scene_moment_ref);
    if (input.productionId !== void 0 && !pathSegmentMatches(sceneMomentRef, "productions", input.productionId, "production")) return false;
    if (input.segmentId !== void 0 && !pathSegmentMatches(sceneMomentRef, "segments", input.segmentId, "segment")) return false;
    if (input.sceneMomentId !== void 0 && !pathSegmentMatches(sceneMomentRef, "scene_moments", input.sceneMomentId, "scene_moment")) return false;
    return true;
  });
  return input.limit === void 0 ? filtered : filtered.slice(0, input.limit);
}
async function deletePlanningEntity(workspaceService, entityKind, input) {
  const entity = await findPlanningEntity(workspaceService, entityKind, input);
  await workspaceService.deleteEntity({ entity });
  return { deleted: true, entity };
}
async function findPlanningEntity(workspaceService, entityKind, input) {
  const id = String(input.id);
  const entities = await workspaceService.queryEntities(planningQuery(entityKind, input));
  const match = entities.find((entity) => normalizePath(entity.path) === normalizePath(id)) ?? entities.find((entity) => sameEntityId2(entity, id, entityKind));
  if (!match) throw new Error(`${entityKind} not found: ${id}`);
  return match;
}
function sameEntityId2(entity, id, entityKind) {
  const entityId2 = entity.id === void 0 ? void 0 : String(entity.id);
  if (entityId2 === id) return true;
  if (entityId2 === `${entityKind}_${id}`) return true;
  if (entityId2?.startsWith(`${entityKind}_`) && entityId2.slice(entityKind.length + 1) === id) return true;
  return entity.path.includes(`/${id}/`);
}
function requiredId(value, name) {
  if (value === void 0 || value === null || String(value).trim() === "") throw new Error(`${name} is required`);
  return value;
}
function normalizePath(value) {
  return value.replace(/\\/g, "/").replace(/^\.movscript\//, "").replace(/^\/+/, "").replace(/\/+$/, "");
}
function pathSegmentMatches(path, marker, id, entityKind) {
  if (!path) return false;
  const segment = pathSegmentAfter6(path, marker);
  if (!segment) return false;
  return refAliases(id, entityKind).includes(segment);
}
function pathSegmentAfter6(path, marker) {
  const parts = normalizePath(path).split("/").filter(Boolean);
  const index = parts.indexOf(marker);
  return index >= 0 ? parts[index + 1] : void 0;
}
function refAliases(value, entityKind) {
  const raw = String(value);
  const withoutPrefix = raw.startsWith(`${entityKind}_`) ? raw.slice(entityKind.length + 1) : raw;
  return [raw, withoutPrefix, `${entityKind}_${withoutPrefix}`];
}
function isRecord13(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function stringValue9(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function idValue2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function idStringValue(value) {
  const id = idValue2(value);
  return id === void 0 ? void 0 : String(id);
}
function pruneUndefined13(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0 && item !== "") output[key] = item;
  }
  return output;
}
function callRequiredOperation(operation, operationName) {
  if (!operation) {
    return Promise.reject(new Error(`MovScript engine ${operationName} operation is not configured`));
  }
  return operation().then((result) => result);
}

// ../../packages/interpreter/dist/chunk-KAPCUFU6.js
function diffMovScriptJsonValues(before, after) {
  return diffJsonValueAtPointer(before, after, "");
}
function jsonPointerToFieldPath(pointer) {
  if (pointer === "") return "*";
  return pointer.split("/").slice(1).map(unescapeJsonPointerSegment).join(".");
}
function diffJsonValueAtPointer(before, after, pointer) {
  if (jsonValueEquals(before, after)) return [];
  if (Array.isArray(before) && Array.isArray(after)) return diffJsonArrayAtPointer(before, after, pointer);
  if (isRecord14(before) && isRecord14(after)) return diffJsonObjectAtPointer(before, after, pointer);
  if (before === void 0) return [{ path: pointer, operation: "added", newValue: after }];
  if (after === void 0) return [{ path: pointer, operation: "removed", oldValue: before }];
  return [{ path: pointer, operation: "replaced", oldValue: before, newValue: after }];
}
function diffJsonObjectAtPointer(before, after, pointer) {
  const keys = [.../* @__PURE__ */ new Set([...Object.keys(before), ...Object.keys(after)])].sort();
  return keys.flatMap((key) => {
    const childPointer = appendJsonPointerSegment(pointer, key);
    if (!(key in before)) return [{ path: childPointer, operation: "added", newValue: after[key] }];
    if (!(key in after)) return [{ path: childPointer, operation: "removed", oldValue: before[key] }];
    return diffJsonValueAtPointer(before[key], after[key], childPointer);
  });
}
function diffJsonArrayAtPointer(before, after, pointer) {
  const beforeKeys = before.map(stableArrayItemKey);
  const afterKeys = after.map(stableArrayItemKey);
  const canTrackItems = beforeKeys.every((key) => key !== void 0) && afterKeys.every((key) => key !== void 0) && new Set(beforeKeys).size === beforeKeys.length && new Set(afterKeys).size === afterKeys.length;
  if (canTrackItems) {
    return diffTrackableJsonArrayAtPointer(before, after, beforeKeys, afterKeys, pointer);
  }
  const length = Math.max(before.length, after.length);
  const changes = [];
  for (let index = 0; index < length; index += 1) {
    const childPointer = appendJsonPointerSegment(pointer, String(index));
    if (index >= before.length) {
      changes.push({ path: childPointer, operation: "added", newValue: after[index] });
    } else if (index >= after.length) {
      changes.push({ path: childPointer, operation: "removed", oldValue: before[index] });
    } else {
      changes.push(...diffJsonValueAtPointer(before[index], after[index], childPointer));
    }
  }
  return changes;
}
function diffTrackableJsonArrayAtPointer(before, after, beforeKeys, afterKeys, pointer) {
  const changes = [];
  const beforeIndexByKey = new Map(beforeKeys.map((key, index) => [key, index]));
  const afterIndexByKey = new Map(afterKeys.map((key, index) => [key, index]));
  const sharedKeys = beforeKeys.filter((key) => afterIndexByKey.has(key));
  const sameMembers = beforeKeys.length === afterKeys.length && sharedKeys.length === beforeKeys.length;
  const orderChanged = sameMembers && beforeKeys.some((key, index) => key !== afterKeys[index]);
  if (orderChanged) {
    changes.push({
      path: pointer,
      operation: "reordered",
      oldValue: beforeKeys,
      newValue: afterKeys
    });
  }
  for (const key of beforeKeys) {
    if (afterIndexByKey.has(key)) continue;
    const oldIndex = beforeIndexByKey.get(key);
    if (oldIndex === void 0) continue;
    changes.push({
      path: appendJsonPointerSegment(pointer, String(oldIndex)),
      operation: "removed",
      itemKey: key,
      oldIndex,
      oldValue: before[oldIndex]
    });
  }
  for (const key of afterKeys) {
    if (beforeIndexByKey.has(key)) continue;
    const newIndex = afterIndexByKey.get(key);
    if (newIndex === void 0) continue;
    changes.push({
      path: appendJsonPointerSegment(pointer, String(newIndex)),
      operation: "added",
      itemKey: key,
      newIndex,
      newValue: after[newIndex]
    });
  }
  for (const key of sharedKeys) {
    const oldIndex = beforeIndexByKey.get(key);
    const newIndex = afterIndexByKey.get(key);
    if (oldIndex === void 0 || newIndex === void 0) continue;
    if (oldIndex !== newIndex) {
      changes.push({
        path: appendJsonPointerSegment(pointer, String(newIndex)),
        operation: "moved",
        itemKey: key,
        oldIndex,
        newIndex
      });
    }
    changes.push(...diffJsonValueAtPointer(
      before[oldIndex],
      after[newIndex],
      appendJsonPointerSegment(pointer, String(newIndex))
    ));
  }
  return changes.sort(compareJsonChanges);
}
function stableArrayItemKey(value) {
  if (isRecord14(value)) {
    const id = value.id ?? value.ID ?? value.client_id ?? value.key ?? value.name;
    if (typeof id === "string" && id.trim()) return id.trim();
    if (typeof id === "number" && Number.isFinite(id)) return String(id);
  }
  if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
    return `${typeof value}:${String(value)}`;
  }
  if (value === null) return "null:null";
  return void 0;
}
function compareJsonChanges(left, right) {
  return left.path.localeCompare(right.path) || left.operation.localeCompare(right.operation) || String(left.itemKey ?? "").localeCompare(String(right.itemKey ?? ""));
}
function appendJsonPointerSegment(pointer, segment) {
  return `${pointer}/${escapeJsonPointerSegment(segment)}`;
}
function escapeJsonPointerSegment(segment) {
  return segment.replace(/~/g, "~0").replace(/\//g, "~1");
}
function unescapeJsonPointerSegment(segment) {
  return segment.replace(/~1/g, "/").replace(/~0/g, "~");
}
function jsonValueEquals(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}
function isRecord14(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/interpreter/dist/chunk-KSQUM2ZR.js
function jsonFileChangesFromFiles(changedFiles, workingFiles, baselineFiles) {
  const workingByPath = new Map(workingFiles.map((file) => [file.path, file]));
  const baselineByRelativePath = new Map(baselineFiles.map((file) => [file.relativePath, file]));
  return changedFiles.flatMap((file) => {
    if (!file.path.endsWith(".json")) return [];
    const workingFile = workingByPath.get(file.path);
    const baselineFile = baselineByRelativePath.get(file.path);
    const before = baselineFile ? parseJsonFile(baselineFile) : void 0;
    const after = workingFile ? parseJsonFile(workingFile) : void 0;
    return [{
      path: workingFile?.path ?? file.path,
      state: file.state,
      ...before !== void 0 ? { oldValue: before } : {},
      ...after !== void 0 ? { newValue: after } : {},
      fieldChanges: fieldChangesForJsonFileChange(file.state, before, after)
    }];
  });
}
function fieldChangesForJsonFileChange(state, before, after) {
  if (state === "added") {
    return [{ field: "*", operation: "added", newValue: after }];
  }
  if (state === "deleted") {
    return [{ field: "*", operation: "deleted", oldValue: before }];
  }
  return diffMovScriptJsonValues(before, after).map(jsonChangeToFieldChange);
}
function jsonChangeToFieldChange(change) {
  return {
    field: jsonPointerToFieldPath(change.path),
    operation: workspaceOperationForJsonChange(change),
    ...change.oldValue !== void 0 ? { oldValue: change.oldValue } : {},
    ...change.newValue !== void 0 ? { newValue: change.newValue } : {},
    jsonPointer: change.path || "/",
    jsonOperation: change.operation,
    ...change.oldIndex !== void 0 ? { oldIndex: change.oldIndex } : {},
    ...change.newIndex !== void 0 ? { newIndex: change.newIndex } : {}
  };
}
function workspaceOperationForJsonChange(change) {
  if (change.operation === "added") return "added";
  if (change.operation === "removed") return "deleted";
  return "modified";
}
function parseJsonFile(file) {
  try {
    return JSON.parse(file.content);
  } catch {
    return void 0;
  }
}

// ../../packages/interpreter/dist/chunk-FHCAUCN7.js
function buildSourceDomainGraph(files) {
  const records = files.map((file) => {
    const data = parseWorkspaceDocument2(file.path, file.content);
    const entityKind = sourceEntityKindFromRelativePath(file.relativePath);
    const id = isRecord15(data) && entityKind ? sourceEntityStableId(data, entityKind) : void 0;
    return {
      file,
      data,
      entityKind,
      ...id !== void 0 ? { id } : {},
      dir: file.relativePath.replace(/\/[^/]+$/, "")
    };
  });
  const byId = /* @__PURE__ */ new Map();
  for (const record of records) {
    if (!record.entityKind) continue;
    if (record.id !== void 0) {
      for (const alias of entityRefAliases(record.id, record.entityKind)) {
        byId.set(entityKey(record.entityKind, alias), record);
      }
    }
    const directoryId = stableDirectoryIdForSourceEntity(record.file.relativePath, record.entityKind);
    if (directoryId !== void 0) {
      for (const alias of entityRefAliases(directoryId, record.entityKind)) {
        byId.set(entityKey(record.entityKind, alias), record);
      }
    }
  }
  return {
    records,
    entityPaths: new Set(records.map((record) => record.dir)),
    byId
  };
}
function changedEntitiesFromFiles(changedFiles, sourceGraph, currentGraph) {
  const sourceByPath = new Map(sourceGraph.records.map((record) => [record.file.path, record]));
  const currentByRelativePath = new Map(currentGraph.records.map((record) => [record.file.relativePath, record]));
  const jsonFileChangesByPath = new Map(jsonFileChangesFromFiles(
    changedFiles,
    sourceGraph.records.map((record) => record.file),
    currentGraph.records.map((record) => record.file)
  ).map((change) => [change.path, change]));
  return changedFiles.flatMap((file) => {
    if (!file.path.endsWith(".json")) return [];
    const sourceRecord = sourceByPath.get(file.path);
    const currentRecord = currentByRelativePath.get(file.path);
    const record = sourceRecord ?? currentRecord;
    const entity = isRecord15(record?.data) ? record.data : {};
    const entityKind = record?.entityKind ?? entityKindFromFilePath(file.path, entity);
    const id = sourceEntityStableId(entity, entityKind);
    return [{
      entityKind,
      path: sourceRecord?.file.path ?? file.path,
      ...id !== void 0 ? { id } : {},
      ...typeof entity.client_id === "string" ? { clientId: entity.client_id } : {},
      state: file.state,
      fieldChanges: jsonFileChangesByPath.get(sourceRecord?.file.path ?? file.path)?.fieldChanges ?? []
    }];
  });
}
function sourceRecordByPathOrId(graph, entityKind, ref) {
  const normalizedRef = typeof ref === "string" ? normalizeWorkspacePath(ref) : String(ref);
  return graph.records.find((record) => {
    return record.entityKind === entityKind && (record.dir === normalizedRef || record.file.relativePath === normalizedRef || String(record.id) === String(ref));
  }) ?? entityRefAliases(ref, entityKind).map((alias) => graph.byId.get(entityKey(entityKind, alias))).find((record) => record !== void 0);
}
function sourceEntityKindFromRelativePath(path) {
  const normalized = normalizeWorkspacePath(path);
  const fileName = normalized.split("/").pop();
  if (fileName === "project.json") return "project";
  if (fileName === "project_standards.json") return "project_standards";
  if (fileName === "setting.json") return "setting";
  if (fileName === "setting_state.json") return "setting_state";
  if (fileName === "asset.json") return "asset";
  if (fileName === "script.json") return "script";
  if (fileName === "script_version.json") return "script_version";
  if (fileName === "script_block.json") return "script_block";
  if (fileName === "content_unit.json") return "content_unit";
  if (fileName === "keyframe.json") return "keyframe";
  if (fileName === "production.json") return "production";
  if (fileName === "segment.json") return "segment";
  if (fileName === "scene_moment.json") return "scene_moment";
  if (fileName === "storyboard.json") return "storyboard";
  if (fileName === "audio_cue.json") return "audio_cue";
  if (fileName === "expression_unit.json") return "expression_unit";
  return void 0;
}
function stableDirectoryIdForSourceEntity(path, entityKind) {
  const parts = normalizeWorkspacePath(path).split("/");
  if (entityKind === "project" || entityKind === "project_standards") return void 0;
  if (!sourceEntityKindFromRelativePath(path)) return void 0;
  const filename = parts.at(-1);
  if (!filename?.endsWith(".json")) return void 0;
  return parts.at(-2);
}
function sourceEntityStableId(record, entityKind) {
  if (entityKind === "project") return idField7(record.project_id ?? record.ID ?? record.id);
  return idField7(record.id ?? record.ID);
}
function parseWorkspaceDocument2(path, content) {
  if (!path.endsWith(".json")) return content;
  try {
    return JSON.parse(content);
  } catch {
    return void 0;
  }
}
function entityKindFromFilePath(path, record) {
  const schema = typeof record.schema === "string" ? record.schema : void 0;
  if (schema) {
    const schemaKind = schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "");
    if (schemaKind === "shot") return "unknown";
    if (schemaKind !== "shot") return schemaKind;
  }
  const fileName = path.split("/").pop() ?? path;
  const prefix = /^([a-z_]+)_/.exec(fileName)?.[1];
  return prefix ?? fileName.replace(/\.[^.]+$/, "");
}
function entityKey(entityKind, id) {
  return `${entityKind}:${String(id ?? "")}`;
}
function idField7(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function isRecord15(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/interpreter/dist/chunk-5SHST5WH.js
function interpretWorkspaceOverview(input) {
  const project = projectInfoFromSource(input.source);
  const affectedEntityKinds = [...new Set(input.inspection.changedEntities.map((entity) => entity.entityKind))].sort();
  const interpretationStatus = !input.latestInterpretation ? "missing" : input.inspection.summary.total > 0 ? "stale" : "current";
  return {
    schema: "movscript.workspace-overview.v1",
    operation: "overview",
    createdAt: input.createdAt,
    workspace: {
      ...project.projectId !== void 0 ? { projectId: project.projectId } : {},
      ...project.title !== void 0 ? { title: project.title } : {},
      sourcePath: input.source.rootPath
    },
    source: {
      mode: input.source.mode,
      documentCount: input.source.files.length,
      entityCount: input.source.files.filter((file) => sourceEntityKindFromRelativePath(file.relativePath) !== void 0).length,
      issueCount: input.inspection.issues.length,
      hasPendingEdits: input.inspection.summary.total > 0,
      readyToInterpret: input.inspection.readyToInterpret
    },
    interpret: {
      status: interpretationStatus,
      ...input.latestInterpretation ? { lastInterpretationId: input.latestInterpretation.manifest.interpretationId, lastInterpretedAt: input.latestInterpretation.manifest.interpretedAt } : {},
      currentIsStale: interpretationStatus !== "current"
    },
    changes: {
      ...input.inspection.summary,
      affectedEntityKinds
    },
    regeneration: input.regeneration.summary,
    nextActions: nextActionsForOverview(input.inspection, input.regeneration, interpretationStatus)
  };
}
function projectInfoFromSource(source) {
  const projectFile = source.files.find((file) => file.relativePath === "project.json");
  const project = projectFile ? parseWorkspaceDocument3(projectFile.path, projectFile.content) : void 0;
  if (!isRecord16(project)) return {};
  const projectId = idField8(project.project_id ?? project.ID ?? project.id);
  return {
    ...projectId !== void 0 ? { projectId } : {},
    ...typeof project.title === "string" ? { title: project.title } : {}
  };
}
function nextActionsForOverview(inspection, regeneration, interpretationStatus) {
  if (!inspection.readyToInterpret) return ["inspect"];
  if (interpretationStatus !== "current") return ["inspect", "interpret"];
  if (regeneration.summary.staleContentUnits > 0 || regeneration.summary.affectedContentUnits > 0) return ["regen plan"];
  return [];
}
function parseWorkspaceDocument3(path, content) {
  if (!path.endsWith(".json")) return content;
  try {
    return JSON.parse(content);
  } catch {
    return void 0;
  }
}
function idField8(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function isRecord16(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/interpreter/dist/chunk-UFNIKZWY.js
import { createHash } from "crypto";
var MOVSCRIPT_CHECKPOINT_DIR = ".movscript/checkpoints";
var MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR = `${MOVSCRIPT_CHECKPOINT_DIR}/current/source`;
var MOVSCRIPT_CHECKPOINT_CURRENT_MANIFEST_PATH = `${MOVSCRIPT_CHECKPOINT_DIR}/current/manifest.json`;
async function resolveWorkspaceSource(fileRepository, options = {}) {
  const sourceFiles = filterWorkspaceSourceFiles(
    await loadWorkspaceSourceFileSnapshots(fileRepository),
    options
  );
  return { rootPath: "", mode: "source", files: sourceFiles };
}
async function loadWorkspaceFileSnapshots(fileRepository, rootPath) {
  const files = [];
  await collectWorkspaceFileSnapshots(fileRepository, rootPath, rootPath, files);
  return files.sort((left, right) => left.path.localeCompare(right.path));
}
async function loadCheckpointSourceSnapshots(fileRepository, checkpointHash, options = {}) {
  const rootDir = getNodeMovScriptWorkspaceFileRepositoryRoot(fileRepository);
  if (rootDir && (await inspectNodeMovScriptGitWorkspace(rootDir)).insideWorkTree) {
    const gitRef = checkpointHash ?? await currentNodeMovScriptGitHead(rootDir);
    if (gitRef) {
      return {
        basePath: gitRef,
        checkpointHash: gitRef,
        source: "git",
        files: filterWorkspaceSourceFiles((await readNodeMovScriptGitSourceFiles(rootDir, gitRef)).map((file) => ({
          path: file.path,
          relativePath: file.path,
          content: file.content,
          hash: contentHash(file.content)
        })), options)
      };
    }
  }
  const snapshotFiles = await loadWorkspaceFileSnapshots(fileRepository, MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR);
  const normalizedSnapshotFiles = normalizeCheckpointSnapshotFiles(snapshotFiles);
  if (normalizedSnapshotFiles.length > 0) {
    const manifest = await readJsonFile(fileRepository, MOVSCRIPT_CHECKPOINT_CURRENT_MANIFEST_PATH);
    return {
      basePath: MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR,
      ...manifest?.checkpointHash ? { checkpointHash: manifest.checkpointHash } : {},
      source: "snapshot",
      files: filterWorkspaceSourceFiles(
        normalizedSnapshotFiles.filter((file) => isMovScriptSourceRelativePath(file.relativePath)),
        options
      )
    };
  }
  return {
    basePath: "empty",
    source: "empty",
    files: []
  };
}
async function writeDebugArtifacts(fileRepository, artifacts, index, manifest, impactReportPath) {
  const files = /* @__PURE__ */ new Map();
  for (const document of index.documents) {
    files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${normalizeWorkspacePath(document.path)}`, serializeWorkspaceDocument(document.data));
  }
  files.set(MOVSCRIPT_DOMAIN_INDEX_PATH, serializeWorkspaceDocument(index));
  files.set(MOVSCRIPT_DOMAIN_TREE_PATH, serializeWorkspaceDocument(artifacts.domainTree));
  files.set(MOVSCRIPT_ASSET_INDEX_PATH, serializeWorkspaceDocument(artifacts.assetIndex));
  files.set(MOVSCRIPT_RELATION_GRAPH_PATH, serializeWorkspaceDocument(artifacts.relationGraph));
  files.set(MOVSCRIPT_EDITOR_STATE_PATH, serializeWorkspaceDocument({
    schema: "movscript.editor-state.v1",
    interpretation_id: manifest.interpretationId,
    interpreted_at: manifest.interpretedAt,
    source_status: {
      ready_to_interpret: manifest.review.readyToInterpret ?? true,
      issue_count: manifest.review.issues?.length ?? 0
    },
    summary: manifest.review.summary,
    contentUnitRuntimePanels: artifacts.contentUnitArtifacts.map(editorStateRuntimePanel),
    content_unit_runtime_panels: artifacts.contentUnitArtifacts.map(editorStateRuntimePanel)
  }));
  for (const previewTimeline of artifacts.previewTimelines) {
    files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${normalizeWorkspacePath(previewTimeline.productionPath)}/preview_timeline.json`, serializeWorkspaceDocument(previewTimeline));
  }
  for (const editPlan of artifacts.editPlans) {
    files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${normalizeWorkspacePath(editPlan.sceneMomentPath)}/edit_plan.json`, serializeWorkspaceDocument(editPlan));
  }
  for (const bundle of artifacts.contentUnitArtifacts) {
    const contentUnitDir = normalizeWorkspacePath(bundle.contentUnitPath).replace(/\/content_unit\.json$/, "");
    files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${contentUnitDir}/runtime_panel.json`, serializeWorkspaceDocument(bundle.runtimePanel));
    files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${contentUnitDir}/generation_prompt.json`, serializeWorkspaceDocument(bundle.generationPrompt));
    files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${contentUnitDir}/dependency_report.json`, serializeWorkspaceDocument(bundle.dependencyReport));
    files.set(`${MOVSCRIPT_INTERPRET_CURRENT_DIR}/${contentUnitDir}/selection_validity.json`, serializeWorkspaceDocument(bundle.selectionValidity));
  }
  for (const path of await listRepositoryFiles(fileRepository, MOVSCRIPT_INTERPRET_CURRENT_DIR)) {
    if (!files.has(path)) await fileRepository.delete({ path });
  }
  for (const [path, content] of files) {
    await fileRepository.write({ path, content });
  }
  await fileRepository.write({ path: impactReportPath, content: serializeWorkspaceDocument(artifacts.impactReport) });
  await fileRepository.write({
    path: `${MOVSCRIPT_INTERPRET_MANIFESTS_DIR}/${manifest.interpretationId}.json`,
    content: serializeWorkspaceDocument(manifest)
  });
}
function workspaceSnapshotId(files) {
  return files.length === 0 ? "empty-working-tree" : sha256(files.map((file) => `${file.relativePath}:${file.hash}`).join("\n"));
}
function normalizeCheckpointSnapshotFiles(files) {
  return files.map((file) => {
    const normalizedPath = normalizeWorkspacePath(file.path);
    const relativePath = normalizedPath.replace(new RegExp(`^${escapeRegExp(MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR)}/`), "").replace(/^checkpoints\/current\/source\//, "");
    return {
      ...file,
      relativePath
    };
  });
}
function filterWorkspaceSourceFiles(files, options) {
  if (options.includeContentUnitDecisionDocuments !== false) return files;
  return files.filter((file) => !isMovScriptContentUnitDecisionPath(file.relativePath));
}
async function loadWorkspaceSourceFileSnapshots(fileRepository) {
  const files = [];
  await collectWorkspaceFileSnapshots(fileRepository, "", "", files);
  return files.filter((file) => isMovScriptSourceRelativePath(file.relativePath)).sort((left, right) => left.path.localeCompare(right.path));
}
async function listRepositoryFiles(fileRepository, rootPath) {
  const files = [];
  await collectRepositoryFiles(fileRepository, normalizeWorkspacePath(rootPath), files);
  return files.sort((left, right) => left.localeCompare(right));
}
async function collectRepositoryFiles(fileRepository, path, out) {
  let listed;
  try {
    listed = await fileRepository.list({ path });
  } catch {
    return;
  }
  for (const entry of listed.entries) {
    if (entry.kind === "directory") {
      await collectRepositoryFiles(fileRepository, entry.path, out);
      continue;
    }
    out.push(normalizeWorkspacePath(entry.path));
  }
}
async function collectWorkspaceFileSnapshots(fileRepository, rootPath, path, out) {
  let listed;
  try {
    listed = await fileRepository.list({ path });
  } catch {
    return;
  }
  for (const entry of listed.entries) {
    if (entry.kind === "directory") {
      if (rootPath === "" && isMovScriptNonSourceRootDirectory(entry.path)) continue;
      await collectWorkspaceFileSnapshots(fileRepository, rootPath, entry.path, out);
      continue;
    }
    if (!isMovScriptSourceDocumentPath(entry.path)) continue;
    const file = await fileRepository.read({ path: entry.path });
    const normalizedPath = normalizeWorkspacePath(file.path);
    out.push({
      path: normalizedPath,
      relativePath: relativeWorkspacePath(rootPath, normalizedPath),
      content: file.content,
      hash: contentHash(file.content)
    });
  }
}
async function readJsonFile(fileRepository, path) {
  try {
    const file = await fileRepository.read({ path });
    return JSON.parse(file.content);
  } catch {
    return void 0;
  }
}
function serializeWorkspaceDocument(value) {
  if (typeof value === "string") return value;
  return `${JSON.stringify(jsonSerializable(value), null, 2)}
`;
}
function editorStateRuntimePanel(bundle) {
  return {
    ...bundle.runtimePanel,
    contentUnitId: bundle.contentUnitId,
    contentUnitPath: bundle.contentUnitPath,
    content_unit_id: bundle.runtimePanel.content_unit_id ?? bundle.contentUnitId,
    content_unit_path: bundle.contentUnitPath
  };
}
function jsonSerializable(value) {
  if (value instanceof Map) {
    return Object.fromEntries(Array.from(value.entries()).map(([key, item]) => [String(key), jsonSerializable(item)]));
  }
  if (Array.isArray(value)) return value.map(jsonSerializable);
  if (value && typeof value === "object") {
    return Object.fromEntries(Object.entries(value).map(([key, item]) => [key, jsonSerializable(item)]));
  }
  return value;
}
function relativeWorkspacePath(rootPath, path) {
  const root = normalizeWorkspacePath(rootPath);
  const normalized = normalizeWorkspacePath(path);
  if (!root) return normalized;
  return normalized === root ? "" : normalized.replace(new RegExp(`^${escapeRegExp(root)}/?`), "");
}
function isMovScriptSourceRelativePath(path) {
  return isMovScriptSourcePath(path);
}
function contentHash(content) {
  return createHash("sha256").update(content).digest("hex");
}
function sha256(value) {
  return createHash("sha256").update(value).digest("hex");
}
function escapeRegExp(value) {
  return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

// ../../packages/interpreter/dist/chunk-WNIHJWE6.js
async function loadLatestInterpretManifest(fileRepository) {
  const files = await loadWorkspaceFileSnapshots(fileRepository, MOVSCRIPT_INTERPRET_MANIFESTS_DIR);
  const manifests = (await Promise.all(files.filter((file) => file.path.endsWith(".json")).map(async (file) => {
    const manifest = parseWorkspaceDocument4(file.path, file.content);
    if (!isInterpretManifest(manifest)) return void 0;
    return { path: file.path, manifest };
  }))).filter((item) => item !== void 0);
  return manifests.sort((left, right) => {
    return right.manifest.interpretedAt.localeCompare(left.manifest.interpretedAt) || right.manifest.interpretationId.localeCompare(left.manifest.interpretationId);
  })[0];
}
async function loadLatestImpactReport(fileRepository, latestInterpretation) {
  const path = latestInterpretation?.manifest.output.impactReportPath;
  if (!path) return void 0;
  try {
    const file = await fileRepository.read({ path });
    const report = parseWorkspaceDocument4(file.path, file.content);
    return isImpactReportArtifact(report) ? report : void 0;
  } catch {
    return void 0;
  }
}
function deriveV1RegenerationPlan(input) {
  const impactReportTargets = input.impactReport ? targetsFromImpactReport(input.impactReport, input.review.staleSelections) : [];
  const legacyImpactTargets = input.impactReport ? [] : input.review.productionImpacts.filter((impact) => impact.contentUnit).map((impact) => targetFromProductionImpact(impact, input.review.staleSelections));
  const affectedContentUnits = dedupeTargets([
    ...impactReportTargets,
    ...legacyImpactTargets,
    ...input.review.staleSelections.map(targetFromStaleSelection).filter((target) => target !== void 0)
  ]);
  const promptBundles = affectedContentUnits.filter((target) => {
    return target.stale === true || target.reasons.some((reason) => reason.includes("reference") || reason.includes("selection") || reason.includes("content_unit"));
  });
  const previewTimelines = input.impactReport ? previewTimelinesFromImpactReport(input.impactReport) : previewTimelinesFromProductionImpacts(input.review.productionImpacts);
  const changedEntities = input.impactReport?.changedEntities ?? input.review.productionImpacts.map((impact) => ({
    entityKind: impact.sourceChanges[0]?.entity.kind ?? "unknown",
    id: impact.sourceChanges[0]?.entity.id,
    path: impact.sourceChanges[0]?.sourceChange.path ?? "",
    state: impact.sourceChanges[0]?.sourceChange.operation ?? "modified",
    businessImpacts: [...new Set(impact.sourceChanges.map((change) => change.businessKind).filter((kind) => typeof kind === "string"))].sort(),
    editorImpacts: [impact.kind],
    affectedContentUnits: impact.contentUnit ? [{
      entityKind: "content_unit",
      id: impact.contentUnit.id,
      path: impact.contentUnit.path
    }] : [],
    staleMarkers: []
  }));
  return {
    schema: "movscript.workspace-regeneration-plan.v1",
    operation: "regen-plan",
    createdAt: input.createdAt,
    status: "ready",
    ...input.latestInterpretation ? {
      interpret: {
        interpretationId: input.latestInterpretation.manifest.interpretationId,
        interpretedAt: input.latestInterpretation.manifest.interpretedAt,
        manifestPath: input.latestInterpretation.path,
        impactReportPath: input.latestInterpretation.manifest.output.impactReportPath
      }
    } : {},
    changedEntities,
    affectedContentUnits,
    promptBundles,
    previewTimelines,
    summary: {
      changedEntities: changedEntities.length,
      affectedContentUnits: affectedContentUnits.length,
      staleContentUnits: input.review.staleSelections.length,
      promptBundles: promptBundles.length,
      previewTimelines: previewTimelines.length
    }
  };
}
function targetsFromImpactReport(impactReport, staleSelections) {
  return impactReport.changedEntities.flatMap((change) => {
    return change.affectedContentUnits.map((contentUnit) => targetFromImpactReportChange(change, contentUnit, staleSelections));
  });
}
function targetFromImpactReportChange(change, contentUnit, staleSelections) {
  const staleSelection = staleSelections.map(selectionRecord).find((selection) => sameOptionalId(selection?.contentUnitId, contentUnit.id) || sameOptionalPath(selection?.contentUnitPath, contentUnit.path));
  return pruneUndefined14({
    contentUnitId: contentUnit.id,
    contentUnitPath: contentUnit.path,
    reasons: [
      ...change.businessImpacts,
      ...change.editorImpacts,
      ...change.staleMarkers
    ].filter(isString2),
    selected: staleSelection?.selected,
    stale: staleSelection?.stale,
    candidateId: staleSelection?.candidateId,
    resourceId: staleSelection?.resourceId,
    staleReasons: staleSelection?.staleReasons
  });
}
function targetFromProductionImpact(impact, staleSelections) {
  const staleSelection = staleSelections.map(selectionRecord).find((selection) => sameOptionalId(selection?.contentUnitId, impact.contentUnit?.id) || sameOptionalPath(selection?.contentUnitPath, impact.contentUnit?.path));
  return pruneUndefined14({
    contentUnitId: impact.contentUnit?.id,
    contentUnitPath: impact.contentUnit?.path,
    reasons: [
      impact.kind,
      ...impact.sourceChanges.map((change) => change.businessKind)
    ].filter(isString2),
    selected: staleSelection?.selected,
    stale: staleSelection?.stale,
    candidateId: staleSelection?.candidateId,
    resourceId: staleSelection?.resourceId,
    staleReasons: staleSelection?.staleReasons
  });
}
function targetFromStaleSelection(selection) {
  const record = selectionRecord(selection);
  if (!record) return void 0;
  return pruneUndefined14({
    contentUnitId: record.contentUnitId,
    contentUnitPath: record.contentUnitPath,
    reasons: ["selection_stale", ...record.staleReasons ?? []],
    selected: record.selected,
    stale: record.stale,
    candidateId: record.candidateId,
    resourceId: record.resourceId,
    staleReasons: record.staleReasons
  });
}
function previewTimelinesFromProductionImpacts(impacts) {
  const timelineEntityKinds = /* @__PURE__ */ new Set(["production", "segment", "scene_moment", "shot", "storyboard", "keyframe", "audio_cue", "expression_unit", "content_unit"]);
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const impact of impacts) {
    for (const change of impact.sourceChanges) {
      if (!timelineEntityKinds.has(change.entity.kind)) continue;
      const path = stringField14(change.sourceChange.path);
      if (!path) continue;
      const productionId = productionIdFromPath(path);
      const key = `${productionId ?? ""}:${path}`;
      if (seen.has(key)) continue;
      seen.add(key);
      out.push(pruneUndefined14({
        productionId,
        path,
        reasons: [impact.kind, change.businessKind].filter(isString2)
      }));
    }
  }
  return out;
}
function previewTimelinesFromImpactReport(impactReport) {
  const timelineEntityKinds = /* @__PURE__ */ new Set(["production", "segment", "scene_moment", "shot", "storyboard", "keyframe", "audio_cue", "expression_unit", "content_unit"]);
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const change of impactReport.changedEntities) {
    if (!timelineEntityKinds.has(change.entityKind)) continue;
    const path = stringField14(change.path);
    if (!path) continue;
    const productionId = productionIdFromPath(path);
    const key = `${productionId ?? ""}:${path}`;
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(pruneUndefined14({
      productionId,
      path,
      reasons: [...change.businessImpacts, ...change.editorImpacts].filter(isString2)
    }));
  }
  return out;
}
function dedupeTargets(targets) {
  const merged = /* @__PURE__ */ new Map();
  for (const target of targets) {
    const key = String(target.contentUnitId ?? target.contentUnitPath ?? "");
    if (!key) continue;
    const existing = merged.get(key);
    if (!existing) {
      merged.set(key, {
        ...target,
        reasons: [...new Set(target.reasons)].sort()
      });
      continue;
    }
    merged.set(key, pruneUndefined14({
      ...existing,
      ...target,
      reasons: [.../* @__PURE__ */ new Set([...existing.reasons, ...target.reasons])].sort(),
      staleReasons: [.../* @__PURE__ */ new Set([...existing.staleReasons ?? [], ...target.staleReasons ?? []])].sort()
    }));
  }
  return [...merged.values()].sort((left, right) => {
    return String(left.contentUnitId ?? left.contentUnitPath ?? "").localeCompare(String(right.contentUnitId ?? right.contentUnitPath ?? ""));
  });
}
function selectionRecord(value) {
  if (!isRecord17(value)) return void 0;
  return pruneUndefined14({
    contentUnitId: idField9(value.contentUnitId),
    contentUnitPath: stringField14(value.contentUnitPath),
    selected: typeof value.selected === "boolean" ? value.selected : void 0,
    stale: typeof value.stale === "boolean" ? value.stale : void 0,
    candidateId: idField9(value.candidateId),
    resourceId: resourceIdField3(value.resourceId),
    staleReasons: Array.isArray(value.staleReasons) ? value.staleReasons.filter(isString2) : void 0
  });
}
function productionIdFromPath(path) {
  const parts = normalizeWorkspacePath6(path).split("/");
  return parts[0] === "productions" ? parts[1] : void 0;
}
function sameOptionalId(left, right) {
  return left !== void 0 && right !== void 0 && String(left) === String(right);
}
function sameOptionalPath(left, right) {
  return typeof left === "string" && typeof right === "string" && normalizeWorkspacePath6(left).replace(/\/[^/]+$/, "") === normalizeWorkspacePath6(right).replace(/\/[^/]+$/, "");
}
function idField9(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function resourceIdField3(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function stringField14(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function isString2(value) {
  return typeof value === "string" && value.trim() !== "";
}
function normalizeWorkspacePath6(path) {
  return path.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+/g, "/").replace(/\/$/, "");
}
function pruneUndefined14(value) {
  const out = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) out[key] = item;
  }
  return out;
}
function parseWorkspaceDocument4(path, content) {
  if (!path.endsWith(".json")) return content;
  try {
    return JSON.parse(content);
  } catch {
    return void 0;
  }
}
function isInterpretManifest(value) {
  return isRecord17(value) && value.schema === "movscript.workspace-interpret.v1" && typeof value.interpretationId === "string" && typeof value.interpretedAt === "string" && isRecord17(value.output);
}
function isImpactReportArtifact(value) {
  return isRecord17(value) && value.schema === "movscript.impact-report.v1" && Array.isArray(value.changedEntities);
}
function isRecord17(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/interpreter/dist/chunk-MQMEFLUB.js
function findUncoveredGitSourceFileChanges(gitChanges, changedFiles) {
  const reviewPaths = new Set(changedFiles.flatMap((file) => {
    const path = normalizeWorkspacePath(file.path);
    const currentPath = normalizeWorkspacePath(file.currentPath);
    return [path, currentPath].filter((item) => item && !item.startsWith(`${MOVSCRIPT_CHECKPOINT_CURRENT_SOURCE_DIR}/`));
  }));
  return gitChanges.filter((change) => {
    const paths = [
      normalizeWorkspacePath(change.path),
      ...change.previousPath ? [normalizeWorkspacePath(change.previousPath)] : []
    ];
    return paths.every((path) => !reviewPaths.has(path));
  });
}
async function validateGitFileChangeCoverage(fileRepository, baseline, changedFiles, options = {}) {
  if (baseline.source !== "git" || !baseline.checkpointHash) return [];
  const rootDir = getNodeMovScriptWorkspaceFileRepositoryRoot(fileRepository);
  if (!rootDir) return [];
  const gitChanges = (await readNodeMovScriptGitSourceFileChanges(rootDir, baseline.checkpointHash)).filter((change) => {
    if (options.includeContentUnitDecisionDocuments !== false) return true;
    return !isMovScriptContentUnitDecisionPath(change.path) && (change.previousPath === void 0 || !isMovScriptContentUnitDecisionPath(change.previousPath));
  });
  if (gitChanges.length === 0) return [];
  const missing = findUncoveredGitSourceFileChanges(gitChanges, changedFiles);
  if (missing.length === 0) return [];
  return [{
    path: ".",
    severity: "warning",
    message: `git diff reported source file changes not present in review changedFiles: ${missing.map((change) => change.previousPath ? `${change.previousPath}->${change.path}` : change.path).join(", ")}`
  }];
}

// ../../packages/interpreter/dist/chunk-CFHQ5UOM.js
function projectStyleReferenceResourceIds(index) {
  const standards = firstEntity(index, "project_standards");
  if (!standards) return [];
  return uniqueIds([
    ...resourceIdsFromValue2(standards.record.style_reference_images),
    ...resourceIdsFromValue2(standards.record.style_references),
    ...resourceIdsFromValue2(standards.record.reference_resource_ids),
    ...arrayField5(standards.record.custom_rules).filter(isRecord18).filter((rule) => rule.enabled !== false).filter((rule) => stringField15(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue2(rule.value))
  ]);
}
var PROMPT_REF_PATTERN2 = /\{\{([a-z_]+)::?([^{}:\s][^{}]*)\}\}/g;
function parseContentUnitEditPromptRefs2(value) {
  const editPrompt = recordField4(value);
  const structuredText = structuredPromptTextFromUnknown(editPrompt?.structured);
  return [
    ...parsePromptRefsFromText2(stringField15(editPrompt?.text), "edit_prompt.text"),
    ...parsePromptRefsFromText2(stringField15(editPrompt?.negative_text), "edit_prompt.negative_text"),
    ...parsePromptRefsFromText2(stringField15(editPrompt?.notes), "edit_prompt.notes"),
    ...parsePromptRefsFromText2(structuredText, "edit_prompt.structured")
  ];
}
function parseUnsupportedContentUnitEditPromptRefs(value) {
  const editPrompt = recordField4(value);
  const structuredText = structuredPromptTextFromUnknown(editPrompt?.structured);
  return [
    ...parseUnsupportedPromptRefsFromText(stringField15(editPrompt?.text), "edit_prompt.text"),
    ...parseUnsupportedPromptRefsFromText(stringField15(editPrompt?.negative_text), "edit_prompt.negative_text"),
    ...parseUnsupportedPromptRefsFromText(stringField15(editPrompt?.notes), "edit_prompt.notes"),
    ...parseUnsupportedPromptRefsFromText(structuredText, "edit_prompt.structured")
  ];
}
function parsePromptRefsFromText2(text, field) {
  if (!text) return [];
  const refs = [];
  for (const match of text.matchAll(PROMPT_REF_PATTERN2)) {
    const kind = promptRefKind2(match[1]);
    const id = match[2]?.trim();
    if (!kind || !id) continue;
    refs.push({
      kind,
      id,
      raw: match[0],
      source: {
        field,
        start: match.index,
        end: match.index === void 0 ? void 0 : match.index + match[0].length
      }
    });
  }
  return refs;
}
function parseUnsupportedPromptRefsFromText(text, field) {
  if (!text) return [];
  const refs = [];
  for (const match of text.matchAll(PROMPT_REF_PATTERN2)) {
    const kindValue = match[1]?.trim();
    const id = match[2]?.trim();
    if (!kindValue || !id || promptRefKind2(kindValue)) continue;
    refs.push({
      kind: kindValue,
      id,
      raw: match[0],
      source: {
        field,
        start: match.index,
        end: match.index === void 0 ? void 0 : match.index + match[0].length
      }
    });
  }
  return refs;
}
function structuredPromptTextFromUnknown(value) {
  const record = recordField4(value);
  if (!record) return void 0;
  const json = JSON.stringify(record);
  return json === "{}" ? void 0 : json;
}
function primaryRefKindForContentUnitType2(contentUnitType2) {
  return primaryRefKindForContentUnitType(contentUnitType2);
}
function primaryRefIdsForContentUnitRecord3(record, kind) {
  return primaryRefIdsForContentUnitRecord(record, kind);
}
function primaryRefFieldNameForKind2(kind) {
  return primaryRefFieldNameForKind(kind);
}
function primaryContentUnitRefs(contentUnit, kind) {
  return primaryRefIdsForContentUnitRecord3(contentUnit.record, kind).map((id) => ({ kind, id }));
}
function hasAmbiguousPrimaryRefs(refs, kind) {
  const unique = [];
  for (const ref of refs) {
    if (unique.some((item) => samePromptRefId(item.id, ref.id, kind))) continue;
    unique.push(ref);
  }
  return unique.length > 1;
}
function expectedOutputKindForContentUnitType2(contentUnitType2) {
  return expectedOutputKindForContentUnitType(contentUnitType2);
}
function resolvePromptRefEntity(index, ref) {
  if (ref.kind === "content_unit") {
    return queryMovScriptWorkspaceEntities(index, { entityKind: "content_unit" }).find((entity) => sameEntityRef(entity.id, ref.id, "content_unit") || sameEntityRef(entityDir5(entity.path).split("/").pop(), ref.id, "content_unit"));
  }
  return findEntityByRef(index, ref.kind, ref.id);
}
function resolveContentUnitForPromptRef(index, ref) {
  if (ref.kind === "content_unit") return resolvePromptRefEntity(index, ref);
  const expectedTypes = contentUnitTypesForPromptRefKind2(ref.kind);
  return queryMovScriptWorkspaceEntities(index, { entityKind: "content_unit" }).find((entity) => {
    if (!expectedTypes.includes(String(entity.record.content_unit_type ?? ""))) return false;
    return primaryContentUnitRefs(entity, ref.kind).some((candidate) => samePromptRefId(candidate.id, ref.id, ref.kind));
  });
}
function resolveUpstreamSelectionForPromptRef(index, ref) {
  const contentUnit = resolveContentUnitForPromptRef(index, ref);
  if (!contentUnit) return void 0;
  const selection = readSelectedContentUnit(index, entityDir5(contentUnit.path));
  if (!selection) return void 0;
  const candidateId = idField10(selection.candidate_id);
  const resourceId = resourceIdField4(selection.resource_id);
  const providerAsset = ref.kind === "asset" ? activeProviderAssetCertificationForPromptRef(index, ref, { resourceId, candidateId }) : void 0;
  return {
    content_unit_ref: entityDir5(contentUnit.path),
    ...candidateId !== void 0 ? { candidate_id: candidateId } : {},
    ...resourceId !== void 0 ? { resource_id: resourceId } : {},
    ...providerAsset ? { provider_asset: providerAsset } : {},
    stale_policy: selection.stale_policy === "accept_stale" ? "accept_stale" : "strict",
    role: `${ref.kind}_ref`
  };
}
function activeProviderAssetCertificationForPromptRef(index, ref, input = {}) {
  if (ref.kind !== "asset") return void 0;
  const asset = findEntityByRef(index, "asset", ref.id);
  return activeProviderAssetCertification(asset?.record, input);
}
function activeProviderAssetCertification(assetRecord, input = {}) {
  const certifications = recordField4(assetRecord?.provider_certifications) ?? recordField4(assetRecord?.providerCertifications);
  if (!certifications) return void 0;
  for (const [provider, rawCertification] of Object.entries(certifications)) {
    const certification = recordField4(rawCertification);
    if (!certification) continue;
    if (stringField15(certification.status) !== "active") continue;
    const assetUri = providerAssetUri(certification);
    if (!assetUri) continue;
    const sourceResourceId2 = resourceIdField4(certification.source_resource_id ?? certification.sourceResourceId);
    if (input.resourceId !== void 0 && sourceResourceId2 !== void 0 && sourceResourceId2 !== input.resourceId) continue;
    const sourceCandidateId = idField10(certification.source_candidate_id ?? certification.sourceCandidateId);
    if (input.candidateId !== void 0 && sourceCandidateId !== void 0 && String(sourceCandidateId) !== String(input.candidateId)) continue;
    return {
      provider,
      status: "active",
      asset_uri: assetUri,
      ...stringField15(certification.hub_asset_id ?? certification.hubAssetId) ? { hub_asset_id: stringField15(certification.hub_asset_id ?? certification.hubAssetId) } : {},
      ...sourceResourceId2 !== void 0 ? { source_resource_id: sourceResourceId2 } : {},
      ...sourceCandidateId !== void 0 ? { source_candidate_id: sourceCandidateId } : {},
      ...stringField15(certification.source_hash ?? certification.sourceHash) ? { source_hash: stringField15(certification.source_hash ?? certification.sourceHash) } : {},
      ...stringField15(certification.certified_at ?? certification.certifiedAt) ? { certified_at: stringField15(certification.certified_at ?? certification.certifiedAt) } : {},
      ...stringField15(certification.updated_at ?? certification.updatedAt) ? { updated_at: stringField15(certification.updated_at ?? certification.updatedAt) } : {}
    };
  }
  return void 0;
}
function resolvePromptRefs(index, refs, _primaryKind) {
  const upstreamSelections = [];
  const resolvedRefs = refs.map((ref) => {
    const role = "input";
    const entity = resolvePromptRefEntity(index, ref);
    const selection = resolveUpstreamSelectionForPromptRef(index, ref);
    if (selection) upstreamSelections.push(selection);
    return {
      ...ref,
      role,
      ...entity ? {
        resolved: {
          entityKind: entity.entityKind,
          ...entity.id !== void 0 ? { id: entity.id } : {},
          path: entity.path
        }
      } : {},
      ...selection ? { selection } : {}
    };
  });
  return { refs: resolvedRefs, upstreamSelections };
}
function promptRefKind2(value) {
  return isContentUnitPromptRefKind(value) ? value : void 0;
}
function contentUnitTypesForPromptRefKind2(kind) {
  return contentUnitTypesForPromptRefKind(kind);
}
function samePromptRefId(left, right, kind) {
  return String(left) === String(right) || lastPathSegment4(left) === String(right) || lastPathSegment4(right) === String(left) || sameEntityRef(left, right, kind);
}
function lastPathSegment4(value) {
  if (typeof value !== "string" || !value.includes("/")) return void 0;
  return value.split("/").filter(Boolean).at(-1);
}
function readSelectedContentUnit(index, contentUnitRef) {
  const context = index.documents.find((document) => {
    if (!isRecord18(document.data)) return false;
    if (document.data.schema !== "movscript.decision_context.v1") return false;
    return document.data.target_kind === "content_unit" && document.data.target_ref === contentUnitRef;
  })?.data;
  const selection = recordField4(context?.selection);
  return selection ? {
    ...selection,
    stale_policy: selection.stale_policy === "accept_stale" ? "accept_stale" : "strict"
  } : void 0;
}
function readContentUnitCandidate(index, contentUnitRef, candidateId) {
  const sourceCandidate = index.documents.find((document) => {
    if (!document.path.startsWith(`${contentUnitRef}/candidates/`)) return false;
    if (!document.path.endsWith("/content_candidate.json")) return false;
    const record = recordField4(document.data);
    return record !== void 0 && String(record.id ?? "") === String(candidateId);
  })?.data;
  const sourceRecord = recordField4(sourceCandidate);
  if (sourceRecord) return sourceRecord;
  const context = index.documents.find((document) => {
    const record = recordField4(document.data);
    return record?.schema === "movscript.decision_context.v1" && record.target_kind === "content_unit" && record.target_ref === contentUnitRef;
  })?.data;
  for (const candidate of arrayField5(recordField4(context)?.candidates).map(recordField4).filter(isDefined4)) {
    if (String(candidate.id ?? "") === String(candidateId)) return candidate;
  }
  return void 0;
}
function findEntityByRef(index, entityKind, ref) {
  const value = idField10(ref);
  if (value === void 0) return void 0;
  const normalized = typeof value === "string" ? value.replace(/\/+$/, "") : String(value);
  return queryMovScriptWorkspaceEntities(index, { entityKind }).find((entity) => {
    const dir = entityDir5(entity.path);
    return dir === normalized || entity.path === `${normalized}/${entityKind}.json` || sameEntityRef(entity.id, value, entityKind);
  });
}
function requiredString(value, message) {
  const next = stringField15(value);
  if (!next) throw new Error(message);
  return next;
}
function firstEntity(index, entityKind) {
  return queryMovScriptWorkspaceEntities(index, { entityKind, limit: 1 })[0];
}
function canonicalEntities2(index) {
  return index.entities;
}
function entityDir5(path) {
  return path.replace(/\/[^/]+$/, "");
}
function recordField4(value) {
  return isRecord18(value) ? value : void 0;
}
function arrayField5(value) {
  return Array.isArray(value) ? value : [];
}
function resourceIdsFromValue2(value) {
  if (Array.isArray(value)) return value.flatMap(resourceIdsFromValue2);
  const text = stringField15(value);
  if (text) {
    const ids = [];
    const patterns = [
      /resource#([0-9]+)/gi,
      /reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi,
      /style_reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi
    ];
    for (const pattern of patterns) {
      for (const match of text.matchAll(pattern)) {
        for (const part of String(match[1] ?? "").split(",")) {
          const parsed2 = Number(part.trim());
          if (Number.isInteger(parsed2) && parsed2 > 0) ids.push(parsed2);
        }
      }
    }
    if (ids.length > 0) return ids;
    const parsed = Number(text);
    return Number.isInteger(parsed) && parsed > 0 ? [parsed] : [];
  }
  const id = resourceIdField4(value);
  if (id !== void 0) return [id];
  if (isRecord18(value)) return resourceIdsFromValue2(value.resource_id ?? value.resourceId ?? value.id);
  return [];
}
function uniqueIds(values) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const value of values) {
    const key = String(value);
    if (seen.has(key)) continue;
    seen.add(key);
    output.push(value);
  }
  return output;
}
function stringField15(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function contentUnitOutputKind2(value) {
  if (value === "image" || value === "video" || value === "audio" || value === "text" || value === "metadata") return value;
  return "metadata";
}
function idField10(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function resourceIdField4(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function providerAssetUri(certification) {
  const direct = stringField15(certification.asset_uri ?? certification.assetUri);
  if (direct) return direct;
  const hubAssetId = stringField15(certification.hub_asset_id ?? certification.hubAssetId);
  return hubAssetId ? `asset://${hubAssetId}` : void 0;
}
function stableJsonValue(value) {
  if (Array.isArray(value)) return value.map(stableJsonValue);
  if (!isRecord18(value)) return value;
  return Object.fromEntries(Object.entries(value).filter(([key]) => !key.startsWith("__workspace_")).sort(([left], [right]) => left.localeCompare(right)).map(([key, item]) => [key, stableJsonValue(item)]));
}
function isRecord18(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function isDefined4(value) {
  return value !== void 0;
}

// ../../packages/language/dist/chunk-B5YQUF4I.js
function objectSchema2(required, properties) {
  return {
    type: "object",
    additionalProperties: true,
    required,
    properties
  };
}
function entitySchema(entityKind, required, properties) {
  return objectSchema2(["schema", "kind", "id", ...required], {
    schema: { const: `movscript.${entityKind}.v1` },
    kind: { const: entityKind },
    id: { type: "string", minLength: 1 },
    title: { type: "string" },
    description: { type: "string" },
    order: { type: "number" },
    tags: { type: "array", items: { type: "string" } },
    notes: { type: "string" },
    ...properties
  });
}
var sourceRefSchema = { type: "string", minLength: 1 };
var transitionSchema = objectSchema2([], {
  in: { type: "string" },
  out: { type: "string" },
  notes: { type: "string" }
});
var projectEntitySchema = {
  id: "movscript.project.v1",
  entityKind: "project",
  title: "Project",
  version: "1.0.0",
  status: "active",
  jsonSchema: objectSchema2(["schema", "kind", "project_id", "title"], {
    schema: { const: "movscript.project.v1" },
    kind: { const: "project" },
    project_id: { type: "string", minLength: 1 },
    title: { type: "string" },
    description: { type: "string" },
    project_kind: { type: "string" },
    timeline_template: {
      enum: ["film", "series", "short_video", "course"],
      description: "Optional default timeline namespace template. It is a creation/planning hint, not an instance parent tree."
    },
    timeline_namespaces: {
      type: "array",
      description: "Project-owned timeline namespace vocabulary, such as series, season, episode, act, sequence, beat.",
      items: { type: "string", minLength: 1 }
    },
    setting_namespaces: {
      type: "array",
      description: "Project-owned setting namespace vocabulary, such as character, costume, state, or voice_state.",
      items: { type: "string", minLength: 1 }
    },
    namespace_vocabulary: objectSchema2([], {
      timeline_template: {
        enum: ["film", "series", "short_video", "course"],
        description: "Optional default timeline namespace template. It suggests creation flow but never owns concrete parent relations."
      },
      timeline_namespaces: { type: "array", items: { type: "string", minLength: 1 } },
      setting_namespaces: { type: "array", items: { type: "string", minLength: 1 } }
    }),
    logline: { type: "string" },
    language: { type: "string" }
  }),
  promptSummary: "Project is the root business boundary for a complete local film workspace. It may declare namespace vocabulary and templates for UI and agent planning, but concrete parent/containment still comes from source paths or validated explicit refs.",
  examples: [{
    title: "series_demo",
    content: {
      schema: "movscript.project.v1",
      kind: "project",
      project_id: "demo",
      title: "Demo Series",
      namespace_vocabulary: {
        timeline_template: "series",
        timeline_namespaces: ["episode", "act", "beat"],
        setting_namespaces: ["character", "costume", "state"]
      }
    }
  }]
};
var projectStandardsEntitySchema = {
  id: "movscript.project_standards.v1",
  entityKind: "project_standards",
  title: "Project Standards",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("project_standards", [], {
    aspect_ratio: { type: "string" },
    shot_size_system: { type: "array", items: { type: "string" } },
    camera_language: { type: "string" },
    visual_style: { type: "string" },
    lighting_style: { type: "string" },
    color_palette: { type: "string" },
    pacing_rules: { type: "string" },
    negative_rules: { type: "array", items: { type: "string" } },
    custom_rules: { type: "array", items: { type: "object", additionalProperties: true } }
  }),
  promptSummary: "Project standards define project-wide production, visual, camera, lighting, pacing, and prompt constraints.",
  examples: [{
    title: "vertical_drama",
    content: {
      schema: "movscript.project_standards.v1",
      kind: "project_standards",
      id: "project_standards",
      aspect_ratio: "9:16",
      shot_size_system: ["wide", "medium", "close_up", "insert"]
    }
  }]
};
var settingEntitySchema = {
  id: "movscript.setting.v1",
  entityKind: "setting",
  title: "Setting",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("setting", ["title"], {
    namespace_kind: { type: "string", description: "Project vocabulary label for this setting namespace node, such as character, location, prop, costume, or voice_identity." },
    setting_namespace_kind: { type: "string", description: "Alias of namespace_kind for setting namespace vocabulary." },
    setting_kind: { enum: ["character", "location", "prop", "world_rule", "style", "other"] },
    profile: { type: "object", additionalProperties: true }
  }),
  promptSummary: "Setting is a concrete setting namespace root for a reusable film/music entity, such as a character, prop, place, instrument, costume, or voice identity. It is not a content-unit target and must not own content-unit refs; assets belong under setting states.",
  examples: [{
    title: "hero",
    content: { schema: "movscript.setting.v1", kind: "setting", id: "hero", setting_kind: "character", title: "Hero" }
  }]
};
var settingStateEntitySchema = {
  id: "movscript.setting_state.v1",
  entityKind: "setting_state",
  title: "Setting State",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("setting_state", ["title"], {
    namespace_kind: { type: "string", description: "Project vocabulary label for this setting-state namespace node, such as base_state, costume_state, emotion_state, or voice_state." },
    setting_namespace_kind: { type: "string", description: "Alias of namespace_kind for setting namespace vocabulary." },
    state_kind: { type: "string" },
    changes: { type: "object", additionalProperties: true }
  }),
  promptSummary: "Setting state is a namespace under one setting for a named condition/version of that entity, such as base look, wet hair, damaged prop, side-view variant, or calm voice. It is not a content-unit target and must not own content-unit refs.",
  examples: [{
    title: "rain_panic",
    content: { schema: "movscript.setting_state.v1", kind: "setting_state", id: "rain_panic", title: "Rain panic" }
  }]
};
var assetEntitySchema = {
  id: "movscript.asset.v1",
  entityKind: "asset",
  title: "Asset",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("asset", ["slot"], {
    setting_id: { type: "string", description: "Concrete setting id. Usually derived from the source path." },
    setting_state_id: { type: "string", description: "Owning setting_state id. Usually derived from the source path." },
    slot: { type: "string", minLength: 1 },
    asset_kind: { enum: ["image", "video", "audio", "text", "reference", "other"] },
    prompt_hint: { type: "string" },
    resource_id: { type: ["string", "number"], description: "Optional selected stable RawResource reference for imported/manual assets. Generated asset candidates should be selected through an asset_ref content unit instead." },
    asset_ref_content_unit_id: { type: "string", description: "Optional pointer to the asset_ref content unit that carries generated candidates for this asset." },
    provider_certifications: {
      type: "object",
      description: "Provider material-library certification records keyed by provider, such as seedance2. Certifications bind to the selected asset_ref RawResource, not the abstract setting.",
      additionalProperties: {
        type: "object",
        additionalProperties: true,
        properties: {
          status: { enum: ["active", "processing", "failed", "stale", "unknown"] },
          hub_asset_id: { type: "string" },
          asset_uri: { type: "string" },
          source_resource_id: { type: ["string", "number"] },
          source_candidate_id: { type: ["string", "number"] },
          source_hash: { type: "string" },
          certified_at: { type: "string" },
          updated_at: { type: "string" },
          error: { type: ["string", "object", "null"], additionalProperties: true }
        }
      }
    },
    candidates: {
      type: "array",
      deprecated: true,
      description: "Legacy inline candidate storage only. Do not create new asset candidates here; use an asset_ref content unit and backend content-unit candidate decisions.",
      items: { type: "object", additionalProperties: true }
    },
    selection: {
      type: "object",
      deprecated: true,
      description: "Legacy inline asset selection only. New generated asset selections belong to the asset_ref content unit candidate decision flow.",
      additionalProperties: true
    }
  }),
  promptSummary: "Asset is a setting-state-owned resource slot and a carrier for asset_ref content-unit generation/review. Do not write new candidates or selections into asset.json; generated asset candidates belong to the linked asset_ref content unit and backend content-unit decision flow. Provider material-library certifications, when present, bind to the selected asset_ref resource and should be marked stale if that selected resource changes. Image assets should prefer plain white or very clean backgrounds.",
  examples: [{
    title: "portrait",
    content: { schema: "movscript.asset.v1", kind: "asset", id: "base_portrait", slot: "character_base_portrait" }
  }]
};
var scriptEntitySchema = {
  id: "movscript.script.v1",
  entityKind: "script",
  title: "Script",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("script", ["title"], {
    script_kind: { type: "string" },
    source_ref: { type: "string" },
    content: { type: "string" }
  }),
  promptSummary: "Script is a screenplay or source text root. Script and production are parallel workspaces and can reference each other.",
  examples: [{
    title: "main",
    content: { schema: "movscript.script.v1", kind: "script", id: "main", title: "Main Script" }
  }]
};
var scriptVersionEntitySchema = {
  id: "movscript.script_version.v1",
  entityKind: "script_version",
  title: "Script Version",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("script_version", ["title"], {
    version_label: { type: "string" },
    source_ref: { type: "string" }
  }),
  promptSummary: "Script version is a versioned script snapshot and can own script blocks.",
  examples: [{
    title: "v1",
    content: { schema: "movscript.script_version.v1", kind: "script_version", id: "v1", title: "V1" }
  }]
};
var scriptBlockEntitySchema = {
  id: "movscript.script_block.v1",
  entityKind: "script_block",
  title: "Script Block",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("script_block", ["text"], {
    block_kind: { type: "string" },
    text: { type: "string", minLength: 1 },
    source_range: { type: "object", additionalProperties: true }
  }),
  promptSummary: "Script block is an addressable block of script text referenced by planning entities.",
  examples: [{
    title: "opening",
    content: { schema: "movscript.script_block.v1", kind: "script_block", id: "opening", text: "INT. APARTMENT - NIGHT" }
  }]
};
var productionEntitySchema = {
  id: "movscript.production.v1",
  entityKind: "production",
  title: "Production",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("production", ["title"], {
    namespace_kind: { type: "string", description: "Project vocabulary label for this timeline namespace node, such as film, episode, act, sequence, or beat." },
    timeline_namespace_kind: { type: "string", description: "Alias of namespace_kind for timeline namespace vocabulary." },
    production_kind: { type: "string" },
    transition: transitionSchema
  }),
  promptSummary: "Production is a legacy timeline namespace source record. It contributes path-derived containment, context, and transition boundaries, but it is not directly generated; use a timeline_assembly_ref content unit when a namespace scope needs a video output.",
  examples: [{
    title: "episode",
    content: { schema: "movscript.production.v1", kind: "production", id: "p8f3", title: "Episode 1", namespace_kind: "episode" }
  }]
};
var segmentEntitySchema = {
  id: "movscript.segment.v1",
  entityKind: "segment",
  title: "Segment",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("segment", ["title", "order"], {
    namespace_kind: { type: "string", description: "Project vocabulary label for this timeline namespace node, such as act, sequence, beat, hook, lesson, or segment." },
    timeline_namespace_kind: { type: "string", description: "Alias of namespace_kind for timeline namespace vocabulary." },
    segment_kind: { enum: ["emotional_function", "rhythm_shift", "dramatic_function", "setup", "escalation", "release", "reversal", "transition"] },
    emotional_intent: { type: "string" },
    rhythm: { type: "string" },
    transition: transitionSchema
  }),
  promptSummary: "Segment is a legacy timeline namespace node inside the path tree. Directory id and path parent define containment; namespace_kind names the user-facing layer. It is not directly generated; use scene_moment, expression_unit, or timeline_assembly_ref content units for production work.",
  examples: [{
    title: "opening",
    content: { schema: "movscript.segment.v1", kind: "segment", id: "a19d", title: "Opening pressure", namespace_kind: "beat", order: 1 }
  }]
};
var sceneMomentEntitySchema = {
  id: "movscript.scene_moment.v1",
  entityKind: "scene_moment",
  title: "Scene Moment",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("scene_moment", ["title", "order"], {
    when: { type: "string" },
    where: { type: "string" },
    action: { type: "string" },
    emotion: { type: "string" },
    transition: transitionSchema
  }),
  promptSummary: "Scene moment is planning context. It owns only scene-level transition boundaries; storyboard ordering lives on storyboard entities and audio cues are independent objects.",
  examples: [{
    title: "call",
    content: {
      schema: "movscript.scene_moment.v1",
      kind: "scene_moment",
      id: "r72k",
      title: "Hero hears the unknown call",
      order: 1,
      transition: { out: "hold_then_cut" }
    }
  }]
};
var storyboardEntitySchema = {
  id: "movscript.storyboard.v1",
  entityKind: "storyboard",
  title: "Storyboard",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("storyboard", [], {
    asset_kind: { enum: ["image", "video", "reference", "other"] },
    slot: { type: "string", minLength: 1 },
    scene_moment_ref: sourceRefSchema,
    expression_unit_ref: sourceRefSchema,
    transition: transitionSchema,
    timeline: objectSchema2([], {
      gap_after_sec: { type: "number" },
      caption: { type: "string" },
      duration_sec: { type: "number" }
    }),
    graph: objectSchema2([], {
      nodes: { type: "array", items: { type: "object", additionalProperties: true } },
      edges: { type: "array", items: { type: "object", additionalProperties: true } },
      entry_node_id: { type: "string" },
      exit_node_id: { type: "string" }
    }),
    setting_refs: {
      type: "array",
      items: objectSchema2(["setting_id"], {
        setting_id: sourceRefSchema,
        setting_state_id: sourceRefSchema,
        role: { type: "string" },
        notes: { type: "string" }
      })
    },
    coverage_plan: { type: "object", additionalProperties: true },
    continuity: { type: "object", additionalProperties: true },
    storyboard_panels: { type: "array", items: { type: "object", additionalProperties: true } },
    prompt_hint: { type: "string" }
  }),
  promptSummary: "Storyboard is a graph-like visual asset for a scene moment or expression unit, similar to an asset slot: storyboard.json defines refs, graph nodes/edges, panels, prompt hints, and continuity; runtime candidates and production decisions live outside storyboard.json.",
  examples: [{
    title: "main",
    content: {
      schema: "movscript.storyboard.v1",
      kind: "storyboard",
      id: "main",
      title: "Rain call storyboard",
      slot: "main",
      asset_kind: "video",
      order: 1,
      graph: {
        nodes: [{ id: "phone_glow", kind: "panel", caption: "Phone glow returns." }],
        edges: [],
        entry_node_id: "phone_glow",
        exit_node_id: "phone_glow"
      },
      timeline: { caption: "Phone glow returns.", gap_after_sec: 0.4 },
      transition: { out: "hold_then_cut" },
      setting_refs: [{ setting_id: "hero", setting_state_id: "rain_panic", role: "subject" }]
    }
  }]
};
var audioCueEntitySchema = {
  id: "movscript.audio_cue.v1",
  entityKind: "audio_cue",
  title: "Audio Cue",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("audio_cue", ["cue_kind", "title"], {
    cue_kind: { enum: ["sound_effect", "music", "ambience", "dialogue", "foley", "other"] },
    scope_ref: sourceRefSchema,
    expression_unit_ref: sourceRefSchema,
    storyboard_ref: sourceRefSchema,
    timing: objectSchema2([], {
      start: { type: "string" },
      end: { type: "string" },
      offset_sec: { type: "number" },
      duration_sec: { type: "number" }
    }),
    prompt_hint: { type: "string" },
    asset_refs: { type: "array", items: sourceRefSchema }
  }),
  promptSummary: "Audio cue is an independent planning object for sound effects, music, ambience, dialogue cues, or foley. It can attach to a scene moment, expression unit, or storyboard through refs.",
  examples: [{
    title: "phone_vibration",
    content: {
      schema: "movscript.audio_cue.v1",
      kind: "audio_cue",
      id: "phone_vibration",
      title: "Phone vibration",
      cue_kind: "sound_effect",
      scope_ref: "productions/p8f3/segments/a19d/scene_moments/r72k",
      storyboard_ref: "productions/p8f3/segments/a19d/scene_moments/r72k/expression_units/eu_phone_closeup/storyboards/main",
      timing: { start: "after_action", duration_sec: 1.2 },
      prompt_hint: "Low, sharp phone vibration under rain ambience."
    }
  }]
};
var expressionUnitEntitySchema = {
  id: "movscript.expression_unit.v1",
  entityKind: "expression_unit",
  title: "Expression Unit",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("expression_unit", [], {
    slot_kind: { enum: ["visual", "voice", "subtitle", "audio"] },
    modality: { enum: ["visual", "verbal", "audio", "text", "interaction", "metadata"] },
    role: { type: "string", minLength: 1 },
    expression_kind: { enum: ["dialogue", "narration", "subtitle", "caption", "action", "visual_note", "shot"] },
    visual_kind: { type: "string" },
    speaker: { type: "string" },
    speaker_ref: sourceRefSchema,
    source_expression_ref: sourceRefSchema,
    text: { type: "string", minLength: 1 },
    intent: { type: "string" },
    content: { type: "object", additionalProperties: true },
    timing_intent: { type: "object", additionalProperties: true },
    voice_profile_ref: sourceRefSchema,
    span: objectSchema2([], {
      storyboard_refs: { type: "array", items: sourceRefSchema },
      expression_refs: { type: "array", items: sourceRefSchema },
      from_storyboard_id: { type: "string" },
      to_storyboard_id: { type: "string" },
      start: { type: "string" },
      end: { type: "string" }
    }),
    script_block_id: sourceRefSchema
  }),
  promptSummary: "Expression unit is the smallest form/edit slot under a scene moment. Use slot_kind=visual|voice|subtitle|audio for new records; legacy modality, role, and expression_kind fields remain compatibility hints. Content units target expression_unit_ref and declare the generated output_kind.",
  examples: [{
    title: "visual_shot_material",
    content: {
      schema: "movscript.expression_unit.v1",
      kind: "expression_unit",
      id: "eu_phone_closeup",
      slot_kind: "visual",
      modality: "visual",
      role: "shot",
      title: "Phone close-up",
      intent: "Phone lights up again while the hero freezes.",
      content: {
        shot_size: "close_up",
        camera: { movement: "slow_push_in" }
      },
      timing_intent: { duration_sec: 2.4 }
    }
  }]
};
var contentUnitEntitySchema = {
  id: "movscript.content_unit.v1",
  entityKind: "content_unit",
  title: "Content Unit",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("content_unit", ["content_unit_type", "output_kind", "title"], {
    content_unit_type: { type: "string", minLength: 1 },
    output_kind: { enum: ["image", "video", "audio", "text", "metadata"] },
    target_category: { enum: ["timeline_assembly", "system_primitive", "content_unit"] },
    target_kind: {
      enum: ["timeline_assembly", "production", "segment", "scene_moment", "expression_unit", "asset", "keyframe", "storyboard", "audio_cue", "content_unit"],
      description: "Use timeline_assembly, system primitives, or content_unit. production/segment remain legacy aliases only when paired with production_ref/segment_ref normalization. Namespace targets such as setting, setting_state, production, or segment are rejected for new generic content units."
    },
    target_ref: sourceRefSchema,
    scope_kind: { type: "string" },
    scope_ref: sourceRefSchema,
    generation_role: { type: "string" },
    production_ref: sourceRefSchema,
    segment_ref: sourceRefSchema,
    asset_ref: sourceRefSchema,
    keyframe_ref: sourceRefSchema,
    storyboard_ref: sourceRefSchema,
    audio_cue_ref: sourceRefSchema,
    scene_moment_ref: sourceRefSchema,
    expression_unit_ref: sourceRefSchema,
    content_unit_ref: sourceRefSchema,
    voice_profile_ref: sourceRefSchema,
    edit_prompt: objectSchema2([], {
      text: { type: "string" },
      negative_text: { type: "string" },
      notes: { type: "string" },
      structured: { type: "object", additionalProperties: true }
    }),
    model_intent: objectSchema2([], {
      capability: { type: "string" },
      provider: { type: "string" },
      model: { type: "string" },
      quality: { type: "string" },
      duration_sec: { type: "number" },
      aspect_ratio: { type: "string" },
      params: { type: "object", additionalProperties: true }
    })
  }),
  promptSummary: "Content unit is a project-level stable generation task. It targets a system primitive such as scene_moment, expression_unit, asset, keyframe, storyboard, or audio_cue, or it targets a timeline_assembly for rendering a namespace scope. production_ref and segment_ref are legacy aliases for timeline_assembly_ref. References written inside edit_prompt with {{type:id}} syntax are upstream inputs. Candidates copy the normalized prompt snapshot at generation time; selections and runtime candidates are stored outside content_unit.json.",
  examples: [{
    title: "expression_visual_material",
    content: {
      schema: "movscript.content_unit.v1",
      kind: "content_unit",
      id: "cu_phone_visual",
      content_unit_type: "expression_unit_ref",
      output_kind: "video",
      target_kind: "expression_unit",
      target_ref: "eu_phone_closeup",
      generation_role: "visual_material",
      title: "Phone close-up visual material",
      edit_prompt: { text: "Generate this visual expression material with selected references." }
    }
  }, {
    title: "episode_assembly",
    content: {
      schema: "movscript.content_unit.v1",
      kind: "content_unit",
      id: "cu_episode_01",
      content_unit_type: "timeline_assembly_ref",
      output_kind: "video",
      target_kind: "timeline_assembly",
      target_ref: "timeline_assembly:episode:episode_01",
      generation_role: "timeline_assembly",
      title: "Episode 01 assembly",
      edit_prompt: { text: "Assemble the selected scene moment outputs for this episode." }
    }
  }]
};
var keyframeEntitySchema = {
  id: "movscript.keyframe.v1",
  entityKind: "keyframe",
  title: "Keyframe",
  version: "1.0.0",
  status: "active",
  jsonSchema: entitySchema("keyframe", [], {
    scene_moment_ref: sourceRefSchema,
    expression_unit_ref: sourceRefSchema,
    storyboard_ref: sourceRefSchema,
    role: { type: "string" },
    visual_intent: { type: "string" },
    timing: { type: "object", additionalProperties: true },
    composition: { type: "object", additionalProperties: true },
    continuity: { type: "object", additionalProperties: true },
    reference_asset_refs: { type: "array", items: sourceRefSchema },
    reference_keyframe_refs: { type: "array", items: sourceRefSchema }
  }),
  promptSummary: "Keyframe is a visual anchor for a scene moment or expression unit. It describes timing, composition, continuity, visual intent, and reference assets. Runtime candidates and production decisions are stored outside keyframe.json.",
  examples: [{
    title: "anchor",
    content: {
      schema: "movscript.keyframe.v1",
      kind: "keyframe",
      id: "c83x",
      title: "Phone light close-up",
      visual_intent: "Phone blue light illuminates the hero face in a rainy apartment."
    }
  }]
};
var SEMANTIC_ENTITY_SCHEMA_REGISTRY = {
  [projectEntitySchema.id]: projectEntitySchema,
  [projectStandardsEntitySchema.id]: projectStandardsEntitySchema,
  [settingEntitySchema.id]: settingEntitySchema,
  [settingStateEntitySchema.id]: settingStateEntitySchema,
  [assetEntitySchema.id]: assetEntitySchema,
  [scriptEntitySchema.id]: scriptEntitySchema,
  [scriptVersionEntitySchema.id]: scriptVersionEntitySchema,
  [scriptBlockEntitySchema.id]: scriptBlockEntitySchema,
  [productionEntitySchema.id]: productionEntitySchema,
  [segmentEntitySchema.id]: segmentEntitySchema,
  [sceneMomentEntitySchema.id]: sceneMomentEntitySchema,
  [storyboardEntitySchema.id]: storyboardEntitySchema,
  [audioCueEntitySchema.id]: audioCueEntitySchema,
  [expressionUnitEntitySchema.id]: expressionUnitEntitySchema,
  [contentUnitEntitySchema.id]: contentUnitEntitySchema,
  [keyframeEntitySchema.id]: keyframeEntitySchema
};
var SEMANTIC_ENTITY_SCHEMA_IDS = Object.keys(SEMANTIC_ENTITY_SCHEMA_REGISTRY);
function getSemanticEntitySchemaEntry(schemaId) {
  return SEMANTIC_ENTITY_SCHEMA_REGISTRY[schemaId] ?? null;
}

// ../../packages/interpreter/dist/chunk-ABC645ZA.js
function validateEditableFiles(files) {
  const issues = [];
  for (const file of files) {
    if (file.path.endsWith(".json")) {
      try {
        JSON.parse(file.content);
      } catch (error) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `invalid JSON: ${error instanceof Error ? error.message : String(error)}`
        });
      }
    }
  }
  return issues;
}
function validateSourceDomainGraph(graph) {
  const issues = [];
  for (const entry of graph.records) {
    if (!entry.file.path.endsWith(".json")) continue;
    if (!isRecord19(entry.data)) continue;
    const expectedKind = entry.entityKind;
    const schemaKind = typeof entry.data.schema === "string" ? entry.data.schema.replace(/^movscript\./, "").replace(/\.v\d+$/, "") : void 0;
    const actualKind = typeof entry.data.kind === "string" ? entry.data.kind : void 0;
    if (!expectedKind) {
      issues.push({
        path: entry.file.path,
        severity: "error",
        message: "unsupported source file path for MovScript domain entity"
      });
      continue;
    }
    if (!sourcePathMatchesEntityKind(entry.file.relativePath, expectedKind)) {
      issues.push({
        path: entry.file.path,
        severity: "error",
        message: `source file name does not match workspace entity kind ${expectedKind}`
      });
    }
    const directoryId = stableDirectoryIdForSourceEntity(entry.file.relativePath, expectedKind);
    const recordId = sourceEntityStableId(entry.data, expectedKind);
    if (directoryId !== void 0 && recordId !== void 0 && String(recordId) !== directoryId) {
      issues.push({
        path: entry.file.path,
        severity: "error",
        message: `id ${String(recordId)} does not match source directory id ${directoryId}`
      });
    }
    if (!schemaKind) {
      issues.push({
        path: entry.file.path,
        severity: "error",
        message: "missing schema field"
      });
    } else if (schemaKind !== expectedKind) {
      issues.push({
        path: entry.file.path,
        severity: "error",
        message: `schema kind ${schemaKind} does not match source path entity ${expectedKind}`
      });
    } else {
      validateSemanticEntitySchema(entry.file, entry.data, issues);
    }
    if (actualKind && actualKind !== expectedKind) {
      issues.push({
        path: entry.file.path,
        severity: "error",
        message: `kind ${actualKind} does not match source path entity ${expectedKind}`
      });
    }
    if (sourceEntityStableId(entry.data, expectedKind) === void 0) {
      issues.push({
        path: entry.file.path,
        severity: "error",
        message: "missing stable id field"
      });
    }
    for (const diagnostic2 of assertNamespaceCannotOwnContentUnitRef({
      entityKind: expectedKind,
      record: entry.data,
      path: entry.file.relativePath
    })) {
      issues.push({
        path: entry.file.path,
        severity: diagnostic2.severity,
        message: diagnostic2.message
      });
    }
    for (const diagnostic2 of assertNamespaceCannotOwnProductionState({
      entityKind: expectedKind,
      record: entry.data,
      path: entry.file.relativePath
    })) {
      issues.push({
        path: entry.file.path,
        severity: diagnostic2.severity,
        message: diagnostic2.message
      });
    }
    validateExplicitPathParentRefs(entry, graph, issues);
    if (expectedKind === "content_unit") {
      validateContentUnitRefs(entry.file, entry.data, graph, issues);
    }
    if (expectedKind === "asset") {
      validateAssetOwnership(entry.file, entry.data, graph, issues);
    }
    if (expectedKind === "storyboard") {
      validateStoryboardSettingRefs(entry.file, entry.data, graph, issues);
    }
    if (expectedKind === "audio_cue") {
      validateAudioCueRefs(entry.file, entry.data, graph, issues);
    }
    if (expectedKind === "keyframe") {
      validateKeyframeReferenceAssetRefs(entry.file, entry.data, graph, issues);
    }
  }
  return issues;
}
function validateExplicitPathParentRefs(entry, graph, issues) {
  if (!entry.entityKind || !isRecord19(entry.data)) return;
  for (const ref of explicitPathParentRefs(entry.entityKind, entry.data)) {
    const pathParent = ref.parentKind ? nearestAncestorRecord(graph, entry.file.relativePath, ref.parentKind) : nearestPathParentRecord(graph, entry);
    const childId = sourceRecordStableRef(entry);
    const parentId = pathParent ? sourceRecordStableRef(pathParent) : void 0;
    for (const diagnostic2 of assertExplicitParentRefMatchesPath({
      child: {
        kind: entry.entityKind,
        ...childId !== void 0 ? { id: childId } : {},
        path: entry.file.relativePath
      },
      pathParent: pathParent ? {
        kind: pathParent.entityKind ?? "unknown",
        ...parentId !== void 0 ? { id: parentId } : {},
        path: pathParent.file.relativePath
      } : void 0,
      explicitParentRef: ref.value,
      field: ref.field,
      path: entry.file.relativePath
    })) {
      issues.push({
        path: entry.file.path,
        severity: diagnostic2.severity,
        message: diagnostic2.message
      });
    }
  }
}
function explicitPathParentRefs(entityKind, record) {
  const refs = [];
  for (const field of [
    "parent_ref",
    "parentRef",
    "parent_id",
    "parentId",
    "parent_scope_ref",
    "parentScopeRef",
    "parent_scope_id",
    "parentScopeId",
    "parent_namespace_ref",
    "parentNamespaceRef"
  ]) {
    const value = idField11(record[field]);
    if (value !== void 0) refs.push({ field, value });
  }
  if (classifyMovScriptEntityKind(entityKind) === "timeline_namespace") {
    for (const field of ["scope_ref", "scopeRef"]) {
      const value = idField11(record[field]);
      if (value !== void 0) refs.push({ field, value });
    }
  }
  if (entityKind === "setting_state") {
    for (const field of ["setting_id", "settingId", "setting_ref", "settingRef"]) {
      const value = idField11(record[field]);
      if (value !== void 0) refs.push({ field, value, parentKind: "setting" });
    }
  }
  if (entityKind === "storyboard" || entityKind === "keyframe") {
    for (const field of ["scene_moment_ref", "sceneMomentRef"]) {
      const value = idField11(record[field]);
      if (value !== void 0) refs.push({ field, value, parentKind: "scene_moment" });
    }
    for (const field of ["expression_unit_ref", "expressionUnitRef"]) {
      const value = idField11(record[field]);
      if (value !== void 0) refs.push({ field, value, parentKind: "expression_unit" });
    }
  }
  return refs;
}
function validateSemanticEntitySchema(file, record, issues) {
  const schemaId = typeof record.schema === "string" ? record.schema : void 0;
  const schema = schemaId ? getSemanticEntitySchemaEntry(schemaId) : null;
  if (!schema) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `unknown semantic entity schema: ${schemaId ?? "<missing>"}`
    });
    return;
  }
  for (const message of validateJsonSchemaValue(record, schema.jsonSchema, "$")) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `schema validation failed: ${message}`
    });
  }
}
function validateContentUnitRefs(file, record, graph, issues) {
  const contentUnitType2 = typeof record.content_unit_type === "string" ? record.content_unit_type : void 0;
  const outputKind = typeof record.output_kind === "string" ? record.output_kind : void 0;
  if (!contentUnitType2) return;
  for (const diagnostic2 of contentUnitTargetValidationDiagnostics(record)) {
    issues.push({
      path: file.path,
      severity: diagnostic2.severity,
      message: diagnostic2.message
    });
  }
  const expectedOutputKind = expectedOutputKindForContentUnitType2(contentUnitType2);
  if (expectedOutputKind && outputKind !== expectedOutputKind) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `${contentUnitType2} content_unit output_kind must be ${expectedOutputKind}`
    });
  }
  const primaryKind = primaryRefKindForContentUnitType2(contentUnitType2);
  for (const ref of parseUnsupportedContentUnitEditPromptRefs(record.edit_prompt)) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `unsupported content_unit prompt ref kind "${ref.kind}": ${ref.raw}. Namespace vocabulary is context, not a selected-resource dependency`
    });
  }
  if (!primaryKind) return;
  const primaryRefs = primaryRefIdsForContentUnitRecord3(record, primaryKind);
  const primaryFieldName = primaryRefFieldNameForKind2(primaryKind);
  if (primaryRefs.length === 0) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `${contentUnitType2} content_unit requires ${primaryFieldName}`
    });
  }
  const uniquePrimaryRefs = [];
  for (const ref of primaryRefs) {
    if (uniquePrimaryRefs.some((primaryRef) => sameRefId2(primaryRef, ref, primaryKind))) continue;
    uniquePrimaryRefs.push(ref);
  }
  if (uniquePrimaryRefs.length > 1) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `${contentUnitType2} content_unit accepts only one ${primaryFieldName}`
    });
  }
  for (const ref of primaryRefs) {
    if (!sourceRecordByPathOrId(graph, primaryKind, ref)) {
      issues.push({
        path: file.path,
        severity: "error",
        message: `${contentUnitType2} content_unit ${primaryFieldName} does not resolve: ${ref}`
      });
    }
  }
  const refs = parseContentUnitEditPromptRefs2(record.edit_prompt);
  for (const ref of refs) {
    if (ref.kind === primaryKind && primaryRefs.some((primaryRef) => sameRefId2(primaryRef, ref.id, primaryKind))) {
      issues.push({
        path: file.path,
        severity: "error",
        message: `${contentUnitType2} content_unit edit_prompt must not reference its own ${primaryFieldName}: ${ref.raw}`
      });
    }
    const resolved = sourceRecordByPathOrId(graph, ref.kind, ref.id);
    if (!resolved && ref.kind !== "content_unit") {
      issues.push({
        path: file.path,
        severity: "error",
        message: `content_unit prompt ref does not resolve: ${ref.raw}`
      });
    }
  }
}
function validateAssetOwnership(file, record, graph, issues) {
  const setting = nearestAncestorRecord(graph, file.relativePath, "setting");
  const settingState = nearestAncestorRecord(graph, file.relativePath, "setting_state");
  const pathSettingId = setting ? sourceRecordStableRef(setting) : void 0;
  const pathStateId = settingState ? sourceRecordStableRef(settingState) : void 0;
  const recordSettingId = idField11(record.setting_id ?? record.settingId ?? record.setting_ref ?? record.settingRef);
  const recordStateId = idField11(record.setting_state_id ?? record.settingStateId ?? record.setting_state_ref ?? record.settingStateRef);
  if (!setting) {
    issues.push({
      path: file.path,
      severity: "error",
      message: "asset source path setting parent does not resolve"
    });
  }
  if (!settingState) {
    issues.push({
      path: file.path,
      severity: "error",
      message: "asset source path setting state parent does not resolve"
    });
  }
  if (recordSettingId === void 0) {
    issues.push({
      path: file.path,
      severity: "error",
      message: "asset requires setting_id matching source path"
    });
  } else if (pathSettingId !== void 0 && !sameRefId2(String(recordSettingId), pathSettingId, "setting")) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `asset setting_id ${String(recordSettingId)} does not match source path setting ${pathSettingId}`
    });
  }
  if (recordStateId === void 0) {
    issues.push({
      path: file.path,
      severity: "error",
      message: "asset requires setting_state_id matching source path"
    });
  } else if (pathStateId !== void 0 && !sameRefId2(String(recordStateId), pathStateId, "setting_state")) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `asset setting_state_id ${String(recordStateId)} does not match source path state ${pathStateId}`
    });
  }
  if (setting && settingState && !isDescendantDir(settingState.dir, setting.dir)) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `asset setting_state_id does not belong to setting_id: ${pathStateId}`
    });
  }
}
function validateAudioCueRefs(file, record, graph, issues) {
  const scopeRef = typeof record.scope_ref === "string" ? normalizeWorkspacePath(record.scope_ref) : void 0;
  const expressionUnitRef = typeof record.expression_unit_ref === "string" ? normalizeWorkspacePath(record.expression_unit_ref) : void 0;
  const storyboardRef = typeof record.storyboard_ref === "string" ? normalizeWorkspacePath(record.storyboard_ref) : void 0;
  const scope = scopeRef ? sourceRecordByPathOrId(graph, "scene_moment", scopeRef) : void 0;
  const expressionUnit = expressionUnitRef ? sourceRecordByPathOrId(graph, "expression_unit", expressionUnitRef) : void 0;
  const storyboard = storyboardRef ? sourceRecordByPathOrId(graph, "storyboard", storyboardRef) : void 0;
  const cueDir = file.relativePath.replace(/\/audio_cue\.json$/, "");
  const sceneMomentDir = cueDir.replace(/\/audio_cues\/[^/]+$/, "");
  if (scopeRef && !scope) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `audio_cue scope_ref does not resolve: ${scopeRef}`
    });
  }
  if (scope && scope.dir !== sceneMomentDir) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `audio_cue scope_ref must reference the owning scene moment: ${scopeRef}`
    });
  }
  if (expressionUnitRef && !expressionUnit) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `audio_cue expression_unit_ref does not resolve: ${expressionUnitRef}`
    });
  }
  if (expressionUnit && !isDescendantDir(expressionUnit.dir, sceneMomentDir)) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `audio_cue expression_unit_ref is not under owning scene moment: ${expressionUnitRef}`
    });
  }
  if (storyboardRef && !storyboard) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `audio_cue storyboard_ref does not resolve: ${storyboardRef}`
    });
  }
  if (storyboard && !isStoryboardUnderSceneMoment(storyboard.dir, sceneMomentDir)) {
    issues.push({
      path: file.path,
      severity: "error",
      message: `audio_cue storyboard_ref is not under owning scene moment: ${storyboardRef}`
    });
  }
  const assetRefs = Array.isArray(record.asset_refs) ? record.asset_refs : [];
  for (const [index, assetRef3] of assetRefs.entries()) {
    const assetId = idField11(assetRef3);
    const asset = assetId !== void 0 ? sourceRecordByPathOrId(graph, "asset", assetId) : void 0;
    if (assetId === void 0 || !asset) {
      issues.push({
        path: file.path,
        severity: "error",
        message: `audio_cue asset_refs[${index}] does not resolve: ${String(assetRef3)}`
      });
    }
  }
}
function validateStoryboardSettingRefs(file, record, graph, issues) {
  const settingRefs = Array.isArray(record.setting_refs) ? record.setting_refs.filter(isRecord19) : [];
  for (const [index, settingRef] of settingRefs.entries()) {
    const settingId = idField11(settingRef.setting_id);
    const settingStateId = idField11(settingRef.setting_state_id);
    const setting = settingId !== void 0 ? sourceRecordByPathOrId(graph, "setting", settingId) : void 0;
    if (settingId !== void 0 && !setting) {
      issues.push({
        path: file.path,
        severity: "error",
        message: `storyboard setting_refs[${index}].setting_id does not resolve: ${String(settingId)}`
      });
    }
    if (settingStateId !== void 0) {
      const settingState = sourceRecordByPathOrId(graph, "setting_state", settingStateId);
      if (!settingState) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `storyboard setting_refs[${index}].setting_state_id does not resolve: ${String(settingStateId)}`
        });
      } else if (setting && !isDescendantDir(settingState.dir, setting.dir)) {
        issues.push({
          path: file.path,
          severity: "error",
          message: `storyboard setting_refs[${index}].setting_state_id does not belong to setting_id: ${String(settingStateId)}`
        });
      }
    }
  }
}
function isStoryboardUnderSceneMoment(storyboardDir, sceneMomentDir) {
  return storyboardDir.startsWith(`${sceneMomentDir}/storyboards/`) || /^.+\/expression_units\/[^/]+\/storyboards\/[^/]+$/.test(storyboardDir) && storyboardDir.startsWith(`${sceneMomentDir}/expression_units/`);
}
function validateKeyframeReferenceAssetRefs(file, record, graph, issues) {
  const assetRefs = Array.isArray(record.reference_asset_refs) ? record.reference_asset_refs : [];
  for (const [index, assetRef3] of assetRefs.entries()) {
    const assetId = idField11(assetRef3);
    if (assetId === void 0) {
      issues.push({
        path: file.path,
        severity: "error",
        message: `keyframe reference_asset_refs[${index}] must be a stable asset id or path`
      });
      continue;
    }
    if (!sourceRecordByPathOrId(graph, "asset", assetId)) {
      issues.push({
        path: file.path,
        severity: "error",
        message: `keyframe reference_asset_refs[${index}] does not resolve: ${String(assetId)}`
      });
    }
  }
}
function validateJsonSchemaValue(value, schema, path) {
  if (!isRecord19(schema)) return [];
  const messages = [];
  if ("const" in schema && !jsonValueEquals2(value, schema.const)) {
    messages.push(`${path} must be ${JSON.stringify(schema.const)}`);
  }
  if (Array.isArray(schema.enum) && !schema.enum.some((item) => jsonValueEquals2(value, item))) {
    messages.push(`${path} must be one of ${schema.enum.map((item) => JSON.stringify(item)).join(", ")}`);
  }
  if (schema.type !== void 0 && !jsonSchemaTypeMatches(value, schema.type)) {
    messages.push(`${path} must be ${Array.isArray(schema.type) ? schema.type.join(" or ") : String(schema.type)}`);
    return messages;
  }
  if (typeof value === "string" && typeof schema.minLength === "number" && value.length < schema.minLength) {
    messages.push(`${path} must contain at least ${schema.minLength} character${schema.minLength === 1 ? "" : "s"}`);
  }
  if (schema.type === "object" && isRecord19(value)) {
    const properties = isRecord19(schema.properties) ? schema.properties : {};
    const required = Array.isArray(schema.required) ? schema.required.filter((item) => typeof item === "string") : [];
    for (const key of required) {
      if (value[key] === void 0) messages.push(`${path}.${key} is required`);
    }
    if (schema.additionalProperties === false) {
      for (const key of Object.keys(value)) {
        if (properties[key] === void 0) messages.push(`${path}.${key} is not allowed`);
      }
    }
    for (const [key, propertySchema] of Object.entries(properties)) {
      if (value[key] !== void 0) messages.push(...validateJsonSchemaValue(value[key], propertySchema, `${path}.${key}`));
    }
  }
  if (schema.type === "array" && Array.isArray(value) && schema.items !== void 0) {
    value.forEach((item, index) => {
      messages.push(...validateJsonSchemaValue(item, schema.items, `${path}[${index}]`));
    });
  }
  return messages;
}
function jsonSchemaTypeMatches(value, type) {
  if (Array.isArray(type)) return type.some((item) => jsonSchemaTypeMatches(value, item));
  if (type === "object") return isRecord19(value);
  if (type === "array") return Array.isArray(value);
  if (type === "string") return typeof value === "string";
  if (type === "number") return typeof value === "number" && Number.isFinite(value);
  if (type === "boolean") return typeof value === "boolean";
  if (type === "null") return value === null;
  return true;
}
function jsonValueEquals2(left, right) {
  return JSON.stringify(left) === JSON.stringify(right);
}
function sourcePathMatchesEntityKind(path, entityKind) {
  const normalized = normalizeWorkspacePath(path);
  const fileName = normalized.split("/").pop();
  const fileNames = {
    project: ["project.json"],
    project_standards: ["project_standards.json"],
    setting: ["setting.json"],
    setting_state: ["setting_state.json"],
    asset: ["asset.json"],
    script: ["script.json"],
    script_version: ["script_version.json"],
    script_block: ["script_block.json"],
    content_unit: ["content_unit.json"],
    keyframe: ["keyframe.json"],
    production: ["production.json"],
    segment: ["segment.json"],
    scene_moment: ["scene_moment.json"],
    storyboard: ["storyboard.json"],
    audio_cue: ["audio_cue.json"],
    expression_unit: ["expression_unit.json"]
  };
  return fileName !== void 0 && (fileNames[entityKind] ?? []).includes(fileName);
}
function nearestAncestorRecord(graph, path, entityKind) {
  const dir = normalizeWorkspacePath(path).replace(/\/[^/]+$/, "");
  const candidates = graph.records.filter((record) => record.entityKind === entityKind && isDescendantDir(dir, record.dir)).sort((left, right) => right.dir.length - left.dir.length);
  return candidates[0];
}
function nearestPathParentRecord(graph, entry) {
  const dir = normalizeWorkspacePath(entry.file.relativePath).replace(/\/[^/]+$/, "");
  const candidates = graph.records.filter((record) => record.file.relativePath !== entry.file.relativePath).filter((record) => record.entityKind !== void 0).filter((record) => classifyMovScriptEntityKind(record.entityKind ?? "") !== void 0).filter((record) => isDescendantDir(dir, record.dir)).sort((left, right) => right.dir.length - left.dir.length);
  return candidates[0];
}
function sourceRecordStableRef(record) {
  if (record.id !== void 0) return String(record.id);
  return stableDirectoryIdForSourceEntity(record.file.relativePath, record.entityKind ?? "");
}
function isDescendantDir(child, parent) {
  const normalizedChild = normalizeWorkspacePath(child);
  const normalizedParent = normalizeWorkspacePath(parent);
  return normalizedChild === normalizedParent || normalizedChild.startsWith(`${normalizedParent}/`);
}
function idField11(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function sameRefId2(left, right, kind) {
  return sameEntityRef(left, right, kind) || lastPathSegment5(left) === right || lastPathSegment5(right) === left;
}
function lastPathSegment5(value) {
  if (typeof value !== "string" || !value.includes("/")) return void 0;
  return value.split("/").filter(Boolean).at(-1);
}
function isRecord19(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/interpreter/dist/chunk-V3CPV4UL.js
function businessChangesFromChangedEntities(changedEntities, sourceGraph, currentGraph, semanticChanges = []) {
  const semanticByEntity = groupSemanticChangesByEntity(semanticChanges);
  return changedEntities.map((entity) => {
    const record = sourceRecordForChangedEntity(sourceGraph, entity) ?? sourceRecordForChangedEntity(currentGraph, entity);
    const title = isRecord20(record?.data) ? stringField16(record.data.title) : void 0;
    const entitySemanticChanges = semanticByEntity.get(entitySemanticKey(entity.entityKind, entity.id)) ?? [];
    const businessKinds = uniqueSorted(entitySemanticChanges.map((change) => change.businessKind));
    const semanticKinds = uniqueSorted(entitySemanticChanges.map((change) => change.kind));
    return {
      ...entity,
      ...title !== void 0 ? { title } : {},
      summary: businessChangeSummary(entity, title, businessKinds),
      impactAreas: businessImpactAreasForEntityKind(entity.entityKind),
      sourcePaths: [entity.path],
      semanticKinds,
      businessKinds
    };
  });
}
function summarizeReview(changedFiles, businessChanges, issues) {
  return {
    total: changedFiles.length,
    added: changedFiles.filter((file) => file.state === "added").length,
    modified: changedFiles.filter((file) => file.state === "modified").length,
    deleted: changedFiles.filter((file) => file.state === "deleted").length,
    businessChanges: businessChanges.length,
    errors: issues.filter((issue) => issue.severity === "error").length,
    warnings: issues.filter((issue) => issue.severity === "warning").length
  };
}
function sourceRecordForChangedEntity(graph, entity) {
  return graph.records.find((record) => {
    return record.entityKind === entity.entityKind && (record.file.path === entity.path || record.file.relativePath === entity.path || record.id !== void 0 && entity.id !== void 0 && String(record.id) === String(entity.id));
  });
}
function businessChangeSummary(entity, title, businessKinds = []) {
  const label = title ?? String(entity.id ?? entity.path);
  const state = businessStateVerb(entity.state);
  const primaryBusinessKind = businessKinds[0];
  if (entity.state === "modified" && primaryBusinessKind) return `${businessKindLabel(primaryBusinessKind)}: ${label}`;
  return `${businessEntityLabel(entity.entityKind)} ${state}: ${label}`;
}
function businessStateVerb(state) {
  switch (state) {
    case "added":
      return "added";
    case "modified":
      return "changed";
    case "deleted":
      return "deleted";
    case "unchanged":
      return "unchanged";
  }
}
function businessEntityLabel(entityKind) {
  const labels = {
    project: "Project",
    project_standards: "Project standards",
    script: "Script",
    script_version: "Script version",
    script_block: "Script block",
    production: "Production",
    segment: "Segment",
    scene_moment: "Scene moment",
    shot: "Shot",
    storyboard: "Storyboard",
    audio_cue: "Audio cue",
    expression_unit: "Expression unit",
    content_unit: "Content unit",
    keyframe: "Keyframe",
    setting: "Setting",
    setting_state: "Setting state",
    asset: "Asset"
  };
  return labels[entityKind] ?? entityKind;
}
function businessImpactAreasForEntityKind(entityKind) {
  switch (entityKind) {
    case "project":
    case "project_standards":
    case "script":
    case "script_version":
    case "script_block":
      return ["workspace_context", "generation_prompts"];
    case "setting":
    case "setting_state":
    case "asset":
      return ["asset_index", "generation_context"];
    case "production":
    case "segment":
    case "scene_moment":
    case "shot":
    case "storyboard":
    case "audio_cue":
    case "expression_unit":
      return ["planning_tree", "preview_timeline", "generation_prompts"];
    case "content_unit":
    case "keyframe":
      return ["content_production", "generation_prompts", "preview_timeline"];
    default:
      return ["domain_index"];
  }
}
function groupSemanticChangesByEntity(semanticChanges) {
  const groups = /* @__PURE__ */ new Map();
  for (const change of semanticChanges) {
    const key = entitySemanticKey(change.entity.kind, change.entity.id);
    groups.set(key, [...groups.get(key) ?? [], change]);
  }
  return groups;
}
function entitySemanticKey(entityKind, id) {
  return `${entityKind}:${String(id ?? "")}`;
}
function uniqueSorted(values) {
  return [...new Set(values)].sort();
}
function businessKindLabel(kind) {
  const labels = {
    metadata_changed: "Metadata changed",
    semantic_input_changed: "Semantic input changed",
    reference_changed: "Reference changed",
    selection_changed: "Selection changed",
    sequence_reordered: "Sequence reordered",
    expression_unit_changed: "Expression unit changed",
    storyboard_changed: "Storyboard changed",
    keyframe_changed: "Keyframe changed",
    content_unit_changed: "Content unit changed",
    project_context_changed: "Project context changed",
    production_structure_changed: "Production structure changed",
    domain_entity_changed: "Domain entity changed"
  };
  return labels[kind];
}
function stringField16(value) {
  return typeof value === "string" && value.trim() ? value : void 0;
}
function isRecord20(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/interpreter/dist/chunk-ESMK3XVG.js
var FIELD_SEMANTIC_RULES = [
  {
    matches: (entity, fieldChange) => entity.entityKind === "expression_unit" && isExpressionUnitSemanticInputField(fieldChange.field),
    semanticKind: "semantic_input_changed",
    businessKind: "expression_unit_changed",
    propagation: "self"
  },
  {
    matches: (_entity, fieldChange) => isSelectionField(fieldChange.field),
    semanticKind: "selection_changed",
    businessKind: "selection_changed",
    propagation: "downstream_reference"
  },
  {
    matches: (_entity, fieldChange) => isReferenceField(fieldChange.field),
    semanticKind: "reference_changed",
    businessKind: "reference_changed",
    propagation: "downstream_reference"
  },
  {
    matches: (_entity, fieldChange) => isMetadataField(fieldChange.field),
    semanticKind: "metadata_changed",
    businessKind: "metadata_changed",
    propagation: "none"
  }
];
function semanticChangesFromEntityChanges(changedEntities) {
  return changedEntities.flatMap((entity) => {
    const fieldChanges = entity.fieldChanges?.length ? entity.fieldChanges : [{ field: "*", operation: entity.state }];
    const grouped = /* @__PURE__ */ new Map();
    for (const fieldChange of fieldChanges) {
      const semantic = semanticForFieldChange(entity, fieldChange);
      const key = `${semantic.kind}:${semantic.businessKind}:${semantic.propagation}`;
      const group = grouped.get(key) ?? {
        kind: semantic.kind,
        businessKind: semantic.businessKind,
        propagation: semantic.propagation,
        fields: []
      };
      if (!group.fields.includes(fieldChange.field)) group.fields.push(fieldChange.field);
      grouped.set(key, group);
    }
    return [...grouped.values()].map((group) => ({
      entity: {
        kind: entity.entityKind,
        ...entity.id !== void 0 ? { id: entity.id } : {}
      },
      kind: group.kind,
      businessKind: group.businessKind,
      propagation: group.propagation,
      fields: group.fields.sort(),
      sourceChange: {
        operation: entity.state,
        path: entity.path
      }
    }));
  });
}
function semanticForFieldChange(entity, fieldChange) {
  if (entity.state === "added" || entity.state === "deleted") {
    return {
      kind: "semantic_input_changed",
      businessKind: businessKindForEntityChange(entity.entityKind),
      propagation: entity.entityKind === "content_unit" ? "self" : "downstream_reference"
    };
  }
  if (fieldChange.jsonOperation === "reordered" || fieldChange.jsonOperation === "moved") {
    const base = semanticCategoryForFieldChange(entity, fieldChange);
    return {
      kind: base.kind,
      businessKind: "sequence_reordered",
      propagation: base.propagation
    };
  }
  const rule = FIELD_SEMANTIC_RULES.find((item) => item.matches(entity, fieldChange));
  if (rule) {
    return {
      kind: rule.semanticKind,
      businessKind: rule.businessKind,
      propagation: rule.propagation
    };
  }
  return semanticCategoryForFieldChange(entity, fieldChange);
}
function semanticCategoryForFieldChange(entity, fieldChange) {
  const rule = FIELD_SEMANTIC_RULES.find((item) => item.matches(entity, fieldChange));
  if (rule) {
    return {
      kind: rule.semanticKind,
      businessKind: rule.businessKind,
      propagation: rule.propagation
    };
  }
  return {
    kind: "semantic_input_changed",
    businessKind: businessKindForEntityChange(entity.entityKind),
    propagation: entity.entityKind === "content_unit" ? "self" : "downstream_reference"
  };
}
function businessKindForEntityChange(entityKind) {
  switch (entityKind) {
    case "expression_unit":
      return "expression_unit_changed";
    case "storyboard":
      return "storyboard_changed";
    case "keyframe":
      return "keyframe_changed";
    case "content_unit":
      return "content_unit_changed";
    case "project":
    case "project_standards":
    case "script":
    case "script_version":
    case "script_block":
      return "project_context_changed";
    case "production":
    case "segment":
    case "scene_moment":
      return "production_structure_changed";
    default:
      return "domain_entity_changed";
  }
}
function isExpressionUnitSemanticInputField(field) {
  return field === "*" || field === "slot_kind" || field === "slotKind" || field === "expression_kind" || field === "expressionKind" || field === "text" || field.startsWith("text.") || field === "angle" || field.startsWith("angle.") || field === "lens" || field.startsWith("lens.") || field === "camera" || field.startsWith("camera.") || field === "movement" || field.startsWith("movement.") || field === "blocking" || field.startsWith("blocking.") || field === "lighting" || field.startsWith("lighting.") || field === "sound" || field.startsWith("sound.") || field === "performance" || field.startsWith("performance.") || field === "duration" || field.startsWith("duration.") || field === "intent" || field.startsWith("intent.") || field === "visual_intent" || field.startsWith("visual_intent.") || field === "narrative_intent" || field.startsWith("narrative_intent.");
}
function isMetadataField(field) {
  return /(^|\.)metadata(\.|$)/.test(field) || /(^|\.)notes?$/.test(field) || /(^|\.)title$/.test(field) || /(^|\.)description$/.test(field) || /(^|\.)created_at$/.test(field) || /(^|\.)updated_at$/.test(field) || /(^|\.)selected_at$/.test(field) || /(^|\.)selected_by$/.test(field);
}
function isReferenceField(field) {
  return /(^|_|\.)refs?(\.|$)/.test(field) || field.endsWith("_ref") || field.includes("_ref.") || field.endsWith("_refs") || field.includes("_refs.");
}
function isSelectionField(field) {
  return field === "lock" || field.startsWith("lock.") || field === "selection" || field.startsWith("selection.") || field === "stale_policy" || field.endsWith(".candidate_id") || field.endsWith(".resource_id");
}

// ../../packages/interpreter/dist/chunk-3R2W5MR6.js
var CONTENT_UNIT_ADAPTERS = Object.fromEntries(
  MOVSCRIPT_SPECIALIZED_CONTENT_UNIT_TYPES.map((contentUnitType2) => [contentUnitType2, specializedAdapter(contentUnitType2)])
);
function hasSpecializedContentUnitAdapter(contentUnitType2) {
  return typeof contentUnitType2 === "string" && CONTENT_UNIT_ADAPTERS[contentUnitType2] !== void 0;
}
function contentUnitAdapterFor(contentUnitType2) {
  return CONTENT_UNIT_ADAPTERS[contentUnitType2] ?? genericAdapter(contentUnitType2);
}
function specializedAdapter(contentUnitType2) {
  if (contentUnitType2 === "timeline_assembly_ref") return timelineAssemblyAdapter();
  if (contentUnitType2 === "expression_unit_ref") return expressionUnitAdapter();
  const targetAdapter = contentUnitTargetAdapterFor(contentUnitType2);
  if (!targetAdapter?.primaryRefKind || !targetAdapter.outputKind) return genericAdapter(contentUnitType2);
  return refAdapter(targetAdapter.contentUnitType, targetAdapter.primaryRefKind, targetAdapter.outputKind);
}
function genericAdapter(contentUnitType2) {
  return {
    type: contentUnitType2,
    version: "generic_prompt@1",
    outputKind: "metadata",
    validate(context) {
      return contentUnitTargetIssues(context.contentUnit.record);
    },
    derivePrompt(context) {
      return basePrompt(context, {
        adapterVersion: this.version,
        outputKind: contentUnitOutputKind2(context.contentUnit.record.output_kind),
        primaryKind: void 0,
        blockers: []
      });
    },
    collectDependencies(_context, prompt) {
      return dependenciesFromPrompt(prompt);
    },
    deriveRuntimePanel(context, derivation) {
      return runtimePanelFor(context, this.version, derivation.prompt);
    }
  };
}
function refAdapter(type, primaryKind, outputKind) {
  return {
    type,
    version: `${type}@2`,
    outputKind,
    validate(context) {
      const issues = contentUnitTargetIssues(context.contentUnit.record);
      const expectedOutputKind = expectedOutputKindForContentUnitType2(type);
      if (expectedOutputKind && context.contentUnit.record.output_kind !== expectedOutputKind) {
        issues.push({ severity: "error", message: `${type} output_kind must be ${expectedOutputKind}` });
      }
      const primaryRefs = primaryContentUnitRefs(context.contentUnit, primaryKind);
      const primaryFieldName = primaryRefFieldNameForKind2(primaryKind);
      if (primaryRefs.length === 0) {
        issues.push({ severity: "error", message: `${type} requires ${primaryFieldName}` });
      }
      if (hasAmbiguousPrimaryRefs(primaryRefs, primaryKind)) {
        issues.push({ severity: "error", message: `${type} accepts only one ${primaryFieldName}` });
      }
      for (const ref of primaryRefs) {
        if (ref.kind !== "content_unit" && !findEntityByRef(context.index, ref.kind, ref.id)) {
          issues.push({ severity: "error", message: `${type} ${primaryFieldName} does not resolve: ${ref.id}` });
        }
      }
      return issues;
    },
    derivePrompt(context) {
      const prompt = basePrompt(context, {
        adapterVersion: this.version,
        outputKind,
        primaryKind,
        blockers: []
      });
      const blockers = [
        ...prompt.blockers ?? [],
        ...promptBlockers(context, prompt.refs, primaryKind)
      ];
      return {
        ...prompt,
        blockers: blockers.length > 0 ? dedupeBlockers(blockers) : void 0
      };
    },
    collectDependencies(context, prompt) {
      const dependencies = dependenciesFromPrompt(prompt);
      return {
        ...dependencies,
        blockers: prompt.blockers ?? promptBlockers(context, prompt.refs, primaryKind)
      };
    },
    deriveRuntimePanel(context, derivation) {
      return runtimePanelFor(context, this.version, derivation.prompt);
    }
  };
}
function contentUnitTargetIssues(record) {
  return contentUnitTargetValidationDiagnostics(record).map((diagnostic2) => ({ severity: diagnostic2.severity, message: diagnostic2.message }));
}
function timelineAssemblyAdapter() {
  return {
    type: "timeline_assembly_ref",
    version: "timeline_assembly_ref@1",
    outputKind: "video",
    validate(context) {
      const issues = contentUnitTargetIssues(context.contentUnit.record);
      if (context.contentUnit.record.output_kind !== "video") {
        issues.push({ severity: "error", message: "timeline_assembly_ref output_kind must be video" });
      }
      return issues;
    },
    derivePrompt(context) {
      return basePrompt(context, {
        adapterVersion: this.version,
        outputKind: "video",
        primaryKind: void 0,
        blockers: []
      });
    },
    collectDependencies(_context, prompt) {
      return dependenciesFromPrompt(prompt);
    },
    deriveRuntimePanel(context, derivation) {
      return runtimePanelFor(context, this.version, derivation.prompt);
    }
  };
}
function expressionUnitAdapter() {
  return {
    ...refAdapter("expression_unit_ref", "expression_unit", "metadata"),
    version: "expression_unit_ref@1",
    outputKind: "metadata",
    derivePrompt(context) {
      const outputKind = contentUnitOutputKind2(context.contentUnit.record.output_kind);
      const prompt = basePrompt(context, {
        adapterVersion: this.version,
        outputKind,
        primaryKind: "expression_unit",
        blockers: []
      });
      const blockers = [
        ...prompt.blockers ?? [],
        ...promptBlockers(context, prompt.refs, "expression_unit")
      ];
      return {
        ...prompt,
        blockers: blockers.length > 0 ? dedupeBlockers(blockers) : void 0
      };
    }
  };
}
function basePrompt(context, options) {
  const editPrompt = normalizedEditPrompt(context.contentUnit.record.edit_prompt);
  const refs = parseContentUnitEditPromptRefs2(context.contentUnit.record.edit_prompt);
  const unsupportedRefs = parseUnsupportedContentUnitEditPromptRefs(context.contentUnit.record.edit_prompt);
  const resolved = resolvePromptRefs(context.index, refs, options.primaryKind);
  const resolvedRefs = resolved.refs.map((ref) => annotateInputSelectionStatus(context, ref));
  const modelIntent = recordField4(context.contentUnit.record.model_intent);
  const styleReferenceResourceIds = usesVisualStyleReferences(options.outputKind) ? projectStyleReferenceResourceIds(context.index) : [];
  const semanticInputs = resolvedRefs.filter((ref) => ref.role === "input" && ref.selection?.resource_id !== void 0 && ref.selection.stale !== true).map((ref) => ({
    role: `${ref.kind}_ref`,
    kind: inputKindForRef(context, ref),
    ref: ref.raw,
    source_content_unit_ref: ref.selection?.content_unit_ref,
    candidate_id: ref.selection?.candidate_id,
    resource_id: ref.selection?.resource_id,
    provider_asset: ref.selection?.provider_asset,
    required: true
  }));
  const blockers = [
    ...options.blockers,
    ...unsupportedRefs.map((ref) => ({
      code: "unsupported_prompt_ref_kind",
      ref: ref.raw,
      message: `unsupported prompt ref kind "${ref.kind}": ${ref.raw}. Use system primitive, content_unit, candidate, or resource refs; namespace vocabulary is context, not a selected-resource dependency.`
    })),
    ...inputBlockers(context, resolvedRefs)
  ];
  const runtimeRequest = {
    capability: stringField15(modelIntent?.capability) ?? capabilityForOutputKind(options.outputKind),
    model_intent: modelIntent,
    inputs: [
      ...semanticInputs,
      ...styleReferenceResourceIds.map((resourceId) => ({
        role: "style_reference",
        kind: "image",
        resource_id: resourceId,
        required: false
      }))
    ],
    params: recordField4(modelIntent?.params),
    metadata: metadataForOutputKind(options.outputKind, modelIntent, styleReferenceResourceIds)
  };
  return pruneUndefined15({
    schema: "movscript.content_unit_prompt.v1",
    content_unit_ref: entityDir5(context.contentUnit.path),
    content_unit_id: context.contentUnit.id,
    content_unit_type: String(context.contentUnit.record.content_unit_type ?? ""),
    output_kind: options.outputKind,
    adapter_version: options.adapterVersion,
    edit_prompt: editPrompt,
    model_intent: modelIntent,
    refs: resolvedRefs,
    runtime_request: pruneUndefined15(runtimeRequest),
    blockers: blockers.length > 0 ? blockers : void 0,
    created_at: context.createdAt
  });
}
function dependenciesFromPrompt(prompt) {
  const entities = prompt.refs.reduce((out, ref) => {
    if (!ref.resolved) return out;
    out[ref.kind] = [...out[ref.kind] ?? [], {
      entityKind: ref.resolved.entityKind,
      id: ref.resolved.id,
      path: ref.resolved.path ?? "",
      index: 0,
      record: {}
    }];
    return out;
  }, {});
  return {
    entities,
    refs: prompt.refs,
    upstreamSelections: prompt.refs.map((ref) => ref.selection).filter(isDefined5),
    blockers: prompt.blockers ?? []
  };
}
function runtimePanelFor(context, adapterVersion, prompt) {
  const status = prompt.blockers?.length ? "blocked" : "ready";
  return {
    schema: "movscript.content_unit_runtime_panel.v1",
    content_unit_ref: entityDir5(context.contentUnit.path),
    content_unit_id: context.contentUnit.id,
    content_unit_type: prompt.content_unit_type,
    adapter_version: adapterVersion,
    output_kind: prompt.output_kind,
    status,
    prompt: prompt.edit_prompt,
    runtime_request: prompt.runtime_request,
    review: prompt.blockers?.length ? { blockers: prompt.blockers.map((blocker) => blocker.message) } : void 0
  };
}
function promptBlockers(context, _refs, primaryKind) {
  const blockers = [];
  const primaryRefs = primaryContentUnitRefs(context.contentUnit, primaryKind);
  const primaryFieldName = primaryRefFieldNameForKind2(primaryKind);
  if (primaryRefs.length === 0) {
    blockers.push({
      code: "primary_ref_missing",
      message: `${context.contentUnit.record.content_unit_type} requires ${primaryFieldName}`
    });
  }
  if (hasAmbiguousPrimaryRefs(primaryRefs, primaryKind)) {
    blockers.push({
      code: "primary_ref_ambiguous",
      message: `${context.contentUnit.record.content_unit_type} accepts only one ${primaryFieldName}`
    });
  }
  for (const ref of primaryRefs) {
    if (ref.kind !== "content_unit" && !findEntityByRef(context.index, ref.kind, ref.id)) {
      blockers.push({
        code: "ref_not_found",
        ref: ref.id,
        message: `primary ref does not resolve: ${ref.id}`
      });
    }
  }
  return blockers;
}
function inputBlockers(context, refs) {
  const blockers = [];
  for (const ref of refs.filter((item) => item.role === "input")) {
    if (ref.blocker) {
      blockers.push(ref.blocker);
      continue;
    }
    if (!ref.resolved) {
      blockers.push({
        code: "ref_not_found",
        ref: ref.raw,
        message: `prompt input ref does not resolve: ${ref.raw}`
      });
      continue;
    }
    const upstreamContentUnit = resolveContentUnitForPromptRef(context.index, ref);
    if (!upstreamContentUnit) {
      blockers.push({
        code: "upstream_content_unit_not_found",
        ref: ref.raw,
        message: `prompt input has no ${ref.kind}_ref content unit: ${ref.raw}`
      });
      continue;
    }
    if (!ref.selection) {
      blockers.push({
        code: "upstream_selection_missing",
        ref: ref.raw,
        message: `prompt input content unit has no selected candidate: ${ref.raw}`
      });
      continue;
    }
    if (ref.selection.stale === true) {
      blockers.push({
        code: "upstream_selection_stale",
        ref: ref.raw,
        message: `prompt input selected candidate is stale: ${ref.raw}`
      });
      continue;
    }
    if (ref.selection.resource_id === void 0) {
      blockers.push({
        code: "upstream_resource_missing",
        ref: ref.raw,
        message: `prompt input selected candidate has no resource_id: ${ref.raw}`
      });
    }
  }
  return blockers;
}
function dedupeBlockers(blockers) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const blocker of blockers) {
    const key = `${blocker.code}:${blocker.ref ?? ""}:${blocker.message}`;
    if (seen.has(key)) continue;
    seen.add(key);
    output.push(blocker);
  }
  return output;
}
function annotateInputSelectionStatus(context, ref) {
  if (ref.role !== "input" || !ref.selection) return ref;
  const status = upstreamSelectionStatus(context, ref);
  if (!status) return ref;
  return {
    ...ref,
    selection: {
      ...ref.selection,
      stale: status.stale
    },
    ...status.blocker ? { blocker: status.blocker } : {}
  };
}
function upstreamSelectionStatus(context, ref) {
  const upstreamContentUnit = resolveContentUnitForPromptRef(context.index, ref);
  if (!upstreamContentUnit) return void 0;
  const upstreamRef = entityDir5(upstreamContentUnit.path);
  const currentRef = entityDir5(context.contentUnit.path);
  const stack = context.promptStack ?? [];
  if (upstreamRef === currentRef || stack.includes(upstreamRef)) {
    return {
      stale: true,
      blocker: {
        code: "prompt_dependency_cycle",
        ref: ref.raw,
        message: `prompt input forms a dependency cycle: ${ref.raw}`
      }
    };
  }
  const selection = readSelectedContentUnit(context.index, upstreamRef);
  const candidateId = idField10(selection?.candidate_id);
  const candidate = candidateId === void 0 ? void 0 : readContentUnitCandidate(context.index, upstreamRef, candidateId);
  if (candidateId !== void 0 && !candidate) {
    return {
      stale: true,
      blocker: {
        code: "upstream_candidate_missing",
        ref: ref.raw,
        message: `prompt input selected candidate is missing: ${ref.raw}`
      }
    };
  }
  const candidatePrompt = normalizedContentUnitPrompt(candidate?.prompt_snapshot);
  if (!candidatePrompt) return { stale: true };
  const contentUnitType2 = stringField15(upstreamContentUnit.record.content_unit_type);
  if (!contentUnitType2) return { stale: true };
  const adapter = contentUnitAdapterFor(contentUnitType2);
  const currentPrompt = adapter.derivePrompt({
    ...context,
    contentUnit: upstreamContentUnit,
    promptStack: [...stack, currentRef]
  });
  return { stale: !sameCanonicalPrompt(currentPrompt, candidatePrompt) };
}
function sameCanonicalPrompt(left, right) {
  return JSON.stringify(stableJsonValue(canonicalPromptComparisonValue(left))) === JSON.stringify(stableJsonValue(canonicalPromptComparisonValue(right)));
}
function canonicalPromptComparisonValue(prompt) {
  const refs = Array.isArray(prompt.refs) ? prompt.refs : [];
  const blockers = Array.isArray(prompt.blockers) ? prompt.blockers : [];
  return {
    content_unit_type: prompt.content_unit_type,
    output_kind: prompt.output_kind,
    edit_prompt: prompt.edit_prompt,
    model_intent: prompt.model_intent,
    refs: refs.map((ref) => ({
      kind: ref.kind,
      id: ref.id,
      raw: ref.raw,
      role: ref.role,
      resolved: ref.resolved,
      selection: ref.selection ? {
        content_unit_ref: ref.selection.content_unit_ref,
        candidate_id: ref.selection.candidate_id,
        resource_id: ref.selection.resource_id,
        artifact_ref: ref.selection.artifact_ref,
        provider_asset: ref.selection.provider_asset
      } : void 0
    })),
    runtime_request: prompt.runtime_request,
    blockers: blockers.map((blocker) => ({
      code: blocker.code,
      ref: blocker.ref
    }))
  };
}
function normalizedContentUnitPrompt(value) {
  const record = recordField4(value);
  if (!record) return void 0;
  if (record.schema !== "movscript.content_unit_prompt.v1" && record.schema !== "movscript.content_unit_generation_prompt_snapshot.v1") return void 0;
  if (!Array.isArray(record.refs)) return void 0;
  if (!recordField4(record.runtime_request)) return void 0;
  return record;
}
function normalizedEditPrompt(value) {
  const record = recordField4(value);
  if (!record) return void 0;
  return pruneUndefined15({
    text: stringField15(record.text),
    negative_text: stringField15(record.negative_text),
    notes: stringField15(record.notes),
    structured: isRecord18(record.structured) ? record.structured : void 0
  });
}
function capabilityForOutputKind(outputKind) {
  switch (outputKind) {
    case "image":
      return "image";
    case "video":
      return "video";
    case "audio":
      return "audio";
    case "text":
      return "text";
    default:
      return "metadata";
  }
}
function inputKindForRef(context, ref) {
  const upstream = resolveContentUnitForPromptRef(context.index, ref);
  return contentUnitOutputKind2(upstream?.record.output_kind);
}
function usesVisualStyleReferences(outputKind) {
  return outputKind === "image" || outputKind === "video";
}
function metadataForOutputKind(outputKind, modelIntent, styleReferenceResourceIds) {
  const metadata = {};
  const duration = typeof modelIntent?.duration_sec === "number" && Number.isFinite(modelIntent.duration_sec) ? modelIntent.duration_sec : void 0;
  if (outputKind === "video" && duration !== void 0) metadata.duration_sec = duration;
  if (styleReferenceResourceIds.length > 0) metadata.style_reference_resource_ids = styleReferenceResourceIds;
  return Object.keys(metadata).length > 0 ? metadata : void 0;
}
function isDefined5(value) {
  return value !== void 0;
}
function pruneUndefined15(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}
var INTERPRETER_VERSION = "movscript-interpreter@0.2.0";
function deriveContentUnitArtifacts(index, input) {
  return canonicalEntities2(index).filter((entity) => entity.entityKind === "content_unit" && entity.id !== void 0).map((contentUnit) => deriveContentUnitArtifact(index, contentUnit, {
    createdAt: input.createdAt,
    interpreterVersion: input.interpreterVersion ?? INTERPRETER_VERSION
  }));
}
function deriveContentUnitArtifact(index, contentUnit, input) {
  if (contentUnit.id === void 0) throw new Error(`content_unit missing id: ${contentUnit.path}`);
  const contentUnitType2 = requiredString(contentUnit.record.content_unit_type, `content_unit_type missing: ${contentUnit.path}`);
  const adapter = contentUnitAdapterFor(contentUnitType2);
  const context = {
    index,
    contentUnit,
    interpreterVersion: input.interpreterVersion ?? INTERPRETER_VERSION,
    createdAt: input.createdAt
  };
  const issues = adapter.validate(context);
  const generationPrompt = adapter.derivePrompt(context);
  const dependencies = adapter.collectDependencies(context, generationPrompt);
  const runtimePanel = adapter.deriveRuntimePanel(context, { dependencies, prompt: generationPrompt });
  const dependencyReport = {
    schema: "movscript.content_unit_dependency_report.v1",
    content_unit_ref: entityDir5(contentUnit.path),
    content_unit_type: contentUnitType2,
    dependencies: Object.entries(dependencies.entities).flatMap(([role, entities]) => {
      return entities.map((entity) => ({
        role,
        entityKind: entity.entityKind,
        ...entity.id !== void 0 ? { id: entity.id } : {},
        path: entity.path
      }));
    }),
    refs: dependencies.refs,
    upstream_selections: dependencies.upstreamSelections,
    ...dependencies.blockers.length > 0 ? { blockers: dependencies.blockers } : {},
    issues
  };
  const selectionValidity = selectionValidityFor(index, contentUnit, generationPrompt);
  return {
    contentUnitId: contentUnit.id,
    contentUnitPath: contentUnit.path,
    runtimePanel,
    generationPrompt,
    dependencyReport,
    selectionValidity
  };
}
function selectionValidityFor(index, contentUnit, currentPrompt) {
  const selection = readSelectedContentUnit(index, entityDir5(contentUnit.path));
  const stalePolicy2 = selection?.stale_policy === "accept_stale" ? "accept_stale" : "strict";
  const selectedCandidateId = idField10(selection?.candidate_id);
  const selectedResourceId2 = resourceIdField4(selection?.resource_id);
  const candidate = selectedCandidateId === void 0 ? void 0 : readContentUnitCandidate(index, entityDir5(contentUnit.path), selectedCandidateId);
  const candidatePromptRecord = recordField22(candidate?.prompt_snapshot);
  const candidatePrompt = isNormalizedContentUnitPrompt(candidatePromptRecord) ? candidatePromptRecord : void 0;
  const staleReasons = selection ? staleReasonsFor(currentPrompt, {
    candidateId: selectedCandidateId,
    candidate,
    candidatePrompt
  }) : [];
  return {
    schema: "movscript.content_unit_selection_validity.v2",
    content_unit_ref: entityDir5(contentUnit.path),
    selected: Boolean(selection),
    ...selectedCandidateId !== void 0 ? { candidate_id: selectedCandidateId } : {},
    ...selectedResourceId2 !== void 0 ? { resource_id: selectedResourceId2 } : {},
    stale: staleReasons.length > 0,
    stale_policy: stalePolicy2,
    reason: stringField15(selection?.reason),
    ...staleReasons.length > 0 ? { stale_reasons: staleReasons } : {}
  };
}
function staleReasonsFor(currentPrompt, input) {
  if (input.candidateId !== void 0 && !input.candidate) return ["candidate_missing"];
  const candidatePrompt = input.candidatePrompt;
  if (!candidatePrompt) return ["candidate_prompt_missing"];
  const reasons = /* @__PURE__ */ new Set();
  if (!sameCanonical(currentPrompt.edit_prompt, candidatePrompt.edit_prompt)) reasons.add("edit_prompt_changed");
  if (!sameCanonical(currentPrompt.model_intent, candidatePrompt.model_intent)) reasons.add("model_intent_changed");
  if (!sameCanonical(refComparisonValue(currentPrompt), refComparisonValue(candidatePrompt))) reasons.add("refs_changed");
  if (!sameCanonical(currentPrompt.runtime_request, candidatePrompt.runtime_request)) reasons.add("runtime_inputs_changed");
  if ((currentPrompt.blockers?.length ?? 0) > 0) reasons.add("prompt_dependency_missing");
  return [...reasons];
}
function refComparisonValue(prompt) {
  return prompt.refs.map((ref) => ({
    kind: ref.kind,
    id: ref.id,
    raw: ref.raw,
    role: ref.role,
    resolved: ref.resolved,
    selection: ref.selection
  }));
}
function isNormalizedContentUnitPrompt(value) {
  const record = recordField22(value);
  return (record?.schema === "movscript.content_unit_prompt.v1" || record?.schema === "movscript.content_unit_generation_prompt_snapshot.v1") && Array.isArray(record.refs) && recordField22(record.runtime_request) !== void 0 && recordField22(record.edit_prompt) !== void 0;
}
function sameCanonical(left, right) {
  return JSON.stringify(stableJsonValue(left ?? null)) === JSON.stringify(stableJsonValue(right ?? null));
}
function recordField22(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value) ? value : void 0;
}
function canonicalEntities22(index) {
  return index.entities;
}
function entityRef3(entity) {
  return {
    entityKind: entity.entityKind,
    ...entity.id !== void 0 ? { id: entity.id } : {},
    path: entity.path
  };
}
function entityRefKey(ref) {
  return `${ref.entityKind}:${String(ref.id ?? ref.path ?? "")}`;
}
function entityRefMatches(left, right) {
  if (left.entityKind !== right.entityKind) return false;
  if (left.id !== void 0 && right.id !== void 0 && String(left.id) === String(right.id)) return true;
  if (left.path && right.path && left.path === right.path) return true;
  return false;
}
function nearestParentEntity(path, entitiesByDir) {
  const parent = nearestParentPath2(entityDir22(path), new Set(entitiesByDir.keys()));
  return parent ? entitiesByDir.get(parent) : void 0;
}
function nearestParentPath2(path, candidates) {
  const parts = path.split("/");
  for (let index = parts.length - 1; index > 0; index -= 1) {
    const candidate = parts.slice(0, index).join("/");
    if (candidates.has(candidate)) return candidate;
  }
  return void 0;
}
function relationTypeForParent(parentType, childType) {
  if (parentType === "setting_state" && childType === "asset") return "owns";
  return "contains";
}
function dedupeRelations(relations) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const relation of relations) {
    const key = JSON.stringify(relation);
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(relation);
  }
  return out;
}
function entityDir22(path) {
  return path.replace(/\/[^/]+$/, "");
}
function normalizedRefDir(value) {
  return typeof value === "string" ? value.replace(/\/+$/, "") : "";
}
function entityKey2(entityKind, id) {
  return `${entityKind}:${String(id ?? "")}`;
}
function findEntityByRef2(entities, entityKind, ref) {
  if (ref === void 0 || ref === null || String(ref).trim() === "") return void 0;
  return entities.find((entity) => entity.entityKind === entityKind && sameEntityRef(entity.id, ref, entityKind));
}
function parentExpressionUnitForEntity(entity, entityByPathDir, entityById) {
  const expressionUnitRef = normalizedRefDir(entity.record.expression_unit_ref);
  if (expressionUnitRef) return entityByPathDir.get(expressionUnitRef) ?? entityById.get(entityKey2("expression_unit", expressionUnitRef));
  const expressionUnitId = pathSegmentAfter7(entity.path, "expression_units");
  return expressionUnitId ? entityById.get(entityKey2("expression_unit", expressionUnitId)) : void 0;
}
function pathSegmentAfter7(path, segment) {
  const parts = path.split("/");
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function recordField32(value) {
  return isRecord25(value) ? value : void 0;
}
function arrayField6(value) {
  return Array.isArray(value) ? value : [];
}
function stringField26(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberField8(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function idField22(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function isRecord25(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function deriveAssetIndex(index) {
  return {
    schema: "movscript.asset-index.v1",
    assets: canonicalEntities22(index).filter((entity) => entity.entityKind === "asset").map((entity) => ({
      ...entity.id !== void 0 ? { id: entity.id } : {},
      path: entity.path,
      owner: assetOwnerRef(entity.path),
      slot: stringField26(entity.record.slot)
    }))
  };
}
function assetOwnerRef(path) {
  const parts = path.split("/");
  const statesIndex = parts.indexOf("states");
  if (statesIndex >= 0) return { entityKind: "setting_state", id: parts[statesIndex + 1] };
  return { entityKind: "unknown" };
}
function deriveDomainTree(index) {
  const nodes = /* @__PURE__ */ new Map();
  const roots = [];
  for (const entity of canonicalEntities22(index)) {
    if (!isTreeEntity(entity)) continue;
    nodes.set(entity.path, treeNode(entity));
  }
  for (const node of nodes.values()) {
    const parent = nearestParentNode(node.path, nodes);
    if (parent) parent.children.push(node);
    else roots.push(node);
  }
  sortTreeNodes(roots);
  return { schema: "movscript.domain-tree.v1", roots };
}
function isTreeEntity(entity) {
  return true;
}
function treeNode(entity) {
  const nodeCategory = classifyMovScriptEntityKind(entity.entityKind);
  return {
    entityKind: entity.entityKind,
    ...nodeCategory ? { nodeCategory } : {},
    nodeKind: domainNodeKind(entity, nodeCategory),
    ...entity.id !== void 0 ? { id: entity.id } : {},
    path: entity.path,
    title: stringField26(entity.record.title) ?? String(entity.id ?? entity.path),
    order: numberField8(entity.record.order),
    children: []
  };
}
function domainNodeKind(entity, _nodeCategory) {
  return projectMovScriptDomainNodeKind(entity.entityKind, entity.record);
}
function nearestParentNode(path, nodes) {
  const parent = nearestParentPath2(path, new Set(nodes.keys()));
  return parent ? nodes.get(parent) : void 0;
}
function sortTreeNodes(nodes) {
  nodes.sort((left, right) => {
    const leftOrder = left.order ?? Number.MAX_SAFE_INTEGER;
    const rightOrder = right.order ?? Number.MAX_SAFE_INTEGER;
    if (leftOrder !== rightOrder) return leftOrder - rightOrder;
    return left.path.localeCompare(right.path);
  });
  for (const node of nodes) sortTreeNodes(node.children);
}
function deriveEditPlans(index, contentUnitArtifacts) {
  const contentUnitArtifactByPath = new Map(contentUnitArtifacts.map((artifact) => [artifact.contentUnitPath, artifact]));
  const expressionUnits = queryMovScriptWorkspaceEntities(index, { entityKind: "expression_unit" });
  const expressionByRef = /* @__PURE__ */ new Map();
  for (const expressionUnit of expressionUnits) {
    expressionByRef.set(entityDir5(expressionUnit.path), expressionUnit);
    if (expressionUnit.id !== void 0) expressionByRef.set(String(expressionUnit.id), expressionUnit);
  }
  return canonicalEntities2(index).filter((entity) => entity.entityKind === "scene_moment" && entity.id !== void 0).flatMap((sceneMoment) => {
    const units = contentUnitsForSceneMoment(index, sceneMoment, expressionUnits);
    if (units.length === 0) return [];
    const production = productionForSceneMoment(index, sceneMoment);
    if (!production?.id) return [];
    const items = units.flatMap((contentUnit, index2) => {
      const artifact = contentUnitArtifactByPath.get(contentUnit.path);
      if (!artifact) return [];
      return [trackItemFor(contentUnit, artifact, sceneMoment, expressionByRef, index2)];
    }).sort((left, right) => left.order - right.order);
    const tracks = groupTrackItems(items);
    const blockers = items.flatMap((item) => {
      if (!item.selected) {
        return [{
          code: "selection_missing",
          content_unit_id: item.content_unit_id,
          message: `content unit ${String(item.content_unit_id)} has no selected candidate`
        }];
      }
      if (item.stale) {
        return [{
          code: "selection_stale",
          content_unit_id: item.content_unit_id,
          message: `content unit ${String(item.content_unit_id)} selected candidate is stale`
        }];
      }
      if (item.resource_id === void 0) {
        return [{
          code: "resource_missing",
          content_unit_id: item.content_unit_id,
          message: `content unit ${String(item.content_unit_id)} selected candidate has no resource_id`
        }];
      }
      return [];
    });
    return [{
      schema: "movscript.edit_plan.v1",
      productionId: production.id,
      productionPath: entityDir5(production.path),
      sceneMomentId: sceneMoment.id,
      sceneMomentPath: entityDir5(sceneMoment.path),
      target_ref: entityDir5(sceneMoment.path),
      status: blockers.length > 0 ? "missing_selection" : "ready_to_compose",
      tracks,
      compose_inputs: items.filter((item) => item.resource_id !== void 0).map((item) => ({
        content_unit_id: item.content_unit_id,
        resource_id: item.resource_id,
        output_kind: item.output_kind,
        track_type: trackTypeFor(item)
      })),
      ...blockers.length > 0 ? { blockers } : {}
    }];
  });
}
function contentUnitsForSceneMoment(index, sceneMoment, expressionUnits) {
  const sceneMomentDir = entityDir5(sceneMoment.path);
  const expressions = expressionUnits.filter((expressionUnit) => entityDir5(expressionUnit.path).startsWith(`${sceneMomentDir}/expression_units/`));
  return queryMovScriptWorkspaceEntities(index, { entityKind: "content_unit" }).filter((contentUnit) => {
    const targetKind = stringField15(contentUnit.record.target_kind);
    const targetRef = stringField15(contentUnit.record.target_ref);
    if (targetKind === "scene_moment" && refMatches(sceneMoment, targetRef)) return true;
    if (String(contentUnit.record.content_unit_type ?? "") === "scene_moment_ref" && refMatches(sceneMoment, contentUnit.record.scene_moment_ref)) return true;
    if (String(contentUnit.record.content_unit_type ?? "") === "scence_moment_ref" && refMatches(sceneMoment, contentUnit.record.scene_moment_ref ?? contentUnit.record.scence_moment_ref)) return true;
    if (targetKind === "expression_unit" || String(contentUnit.record.content_unit_type ?? "") === "expression_unit_ref") {
      const expressionRef = targetRef ?? stringField15(contentUnit.record.expression_unit_ref);
      return expressions.some((expressionUnit) => refMatches(expressionUnit, expressionRef));
    }
    return false;
  });
}
function productionForSceneMoment(index, sceneMoment) {
  const productionId = sceneMoment.path.split("/")[1];
  return queryMovScriptWorkspaceEntities(index, { entityKind: "production" }).find((entity) => sameEntityRef(entity.id, productionId, "production") || entityDir5(entity.path) === `productions/${productionId}`);
}
function trackItemFor(contentUnit, artifact, sceneMoment, expressionByRef, fallbackOrder) {
  const targetKind = stringField15(contentUnit.record.target_kind) ?? primaryTargetKind(contentUnit);
  const targetRef = stringField15(contentUnit.record.target_ref) ?? primaryTargetRef(contentUnit) ?? entityDir5(sceneMoment.path);
  const expressionUnit = targetKind === "expression_unit" ? expressionByRef.get(targetRef) ?? expressionByRef.get(lastPathSegment6(targetRef) ?? "") : void 0;
  return {
    id: `edit_item_${String(contentUnit.id ?? fallbackOrder)}`,
    content_unit_id: contentUnit.id ?? contentUnit.path,
    content_unit_ref: entityDir5(contentUnit.path),
    output_kind: artifact.runtimePanel.output_kind,
    target_kind: targetKind,
    target_ref: targetRef,
    ...expressionUnit ? { expression_unit_ref: entityDir5(expressionUnit.path) } : {},
    ...expressionUnit ? { expression_slot_kind: expressionUnitSlotKindFromRecord(expressionUnit.record) } : {},
    ...expressionUnit ? { expression_modality: stringField15(expressionUnit.record.modality) ?? legacyExpressionModality(expressionUnit) } : {},
    ...expressionUnit ? { expression_role: stringField15(expressionUnit.record.role) ?? stringField15(expressionUnit.record.expression_kind) } : {},
    ...artifact.selectionValidity.candidate_id !== void 0 ? { candidate_id: artifact.selectionValidity.candidate_id } : {},
    ...artifact.selectionValidity.resource_id !== void 0 ? { resource_id: artifact.selectionValidity.resource_id } : {},
    selected: artifact.selectionValidity.selected,
    stale: artifact.selectionValidity.stale,
    timing_intent: recordField4(expressionUnit?.record.timing_intent),
    generation_role: stringField15(contentUnit.record.generation_role),
    order: numberField23(contentUnit.record.order) ?? numberField23(expressionUnit?.record.order) ?? fallbackOrder
  };
}
function groupTrackItems(items) {
  const byType = /* @__PURE__ */ new Map();
  for (const item of items) {
    const type = trackTypeFor(item);
    byType.set(type, [...byType.get(type) ?? [], item]);
  }
  return [...byType.entries()].map(([type, trackItems]) => ({ type, items: trackItems }));
}
function trackTypeFor(item) {
  if (item.output_kind === "video") return "video";
  if (item.output_kind === "audio") return item.expression_slot_kind === "voice" || item.expression_role === "dialogue" || item.expression_role === "narration" ? "voice" : "audio";
  if (item.output_kind === "text") return "subtitle";
  if (item.output_kind === "image") return "image";
  if (item.expression_slot_kind === "subtitle") return "subtitle";
  return "metadata";
}
function primaryTargetKind(contentUnit) {
  const type = String(contentUnit.record.content_unit_type ?? "");
  if (type === "scene_moment_ref" || type === "scence_moment_ref") return "scene_moment";
  if (type === "expression_unit_ref") return "expression_unit";
  if (type === "content_unit_ref") return "content_unit";
  return "metadata";
}
function primaryTargetRef(contentUnit) {
  return stringField15(contentUnit.record.expression_unit_ref) ?? stringField15(contentUnit.record.scene_moment_ref) ?? stringField15(contentUnit.record.scence_moment_ref) ?? stringField15(contentUnit.record.content_unit_ref);
}
function legacyExpressionModality(expressionUnit) {
  const kind = stringField15(expressionUnit.record.expression_kind);
  if (kind === "dialogue" || kind === "narration") return "verbal";
  if (kind === "subtitle" || kind === "caption") return "text";
  if (kind === "visual_note" || kind === "action") return "visual";
  return void 0;
}
function refMatches(entity, ref) {
  if (typeof ref !== "string" && typeof ref !== "number") return false;
  return sameEntityRef(entity.id, ref, entity.entityKind) || entityDir5(entity.path) === String(ref).replace(/\/+$/, "") || lastPathSegment6(ref) === String(entity.id);
}
function lastPathSegment6(value) {
  if (typeof value !== "string") return void 0;
  return value.split("/").filter(Boolean).at(-1);
}
function numberField23(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function deriveImpactReport(changedEntities, interpretationId, createdAt, index, relationGraph, semanticChanges = []) {
  const semanticChangesByEntity = groupSemanticChangesByEntity2(semanticChanges);
  return {
    schema: "movscript.impact-report.v1",
    interpretationId,
    createdAt,
    changedEntities: changedEntities.map((entity) => {
      const affectedContentUnits = affectedContentUnitsForChangedEntity(entity, index, relationGraph);
      return {
        entityKind: entity.entityKind,
        ...entity.id !== void 0 ? { id: entity.id } : {},
        path: entity.path,
        state: entity.state,
        businessImpacts: businessImpactsForChangedEntity(entity, semanticChangesByEntity),
        editorImpacts: editorImpactsForChangedEntity(entity, affectedContentUnits),
        affectedContentUnits,
        staleMarkers: staleMarkersForChangedEntity(entity, affectedContentUnits)
      };
    })
  };
}
function groupSemanticChangesByEntity2(semanticChanges) {
  const groups = /* @__PURE__ */ new Map();
  for (const change of semanticChanges) {
    const key = entitySemanticKey2(change.entity.kind, change.entity.id);
    groups.set(key, [...groups.get(key) ?? [], change]);
  }
  return groups;
}
function businessImpactsForChangedEntity(entity, semanticChangesByEntity) {
  const changes = semanticChangesByEntity.get(entitySemanticKey2(entity.entityKind, entity.id)) ?? [];
  const labels = changes.map((change) => businessImpactLabel(change.businessKind));
  return [...new Set(labels)].sort();
}
function businessImpactLabel(businessKind) {
  const labels = {
    metadata_changed: "Metadata changed",
    semantic_input_changed: "Semantic input changed",
    reference_changed: "Reference changed",
    selection_changed: "Selection changed",
    sequence_reordered: "Sequence reordered",
    expression_unit_changed: "Expression unit changed",
    storyboard_changed: "Storyboard changed",
    keyframe_changed: "Keyframe changed",
    content_unit_changed: "Content unit changed",
    project_context_changed: "Project context changed",
    production_structure_changed: "Production structure changed",
    domain_entity_changed: "Domain entity changed"
  };
  return labels[businessKind];
}
function entitySemanticKey2(entityKind, id) {
  return `${entityKind}:${String(id ?? "")}`;
}
function editorImpactsForChangedEntity(entity, affectedContentUnits = []) {
  switch (entity.entityKind) {
    case "project_standards":
      return ["Generation prompt bundles may need reinterpretation."];
    case "setting":
    case "setting_state":
    case "asset":
      return [
        "Setting asset index should be refreshed.",
        affectedContentUnits.length > 0 ? `${affectedContentUnits.length} content production context(s) using this setting may be stale.` : "Generation contexts using this setting may be stale."
      ];
    case "production":
    case "segment":
    case "scene_moment":
    case "storyboard":
    case "audio_cue":
    case "expression_unit":
      return [
        "Production planning tree should be refreshed.",
        "Preview timeline may need reinterpretation.",
        ...affectedContentUnits.length > 0 ? [`${affectedContentUnits.length} content production prompt bundle(s) may need reinterpretation.`] : []
      ];
    case "content_unit":
      return ["Content production context should be refreshed.", "Preview timeline items using this content unit may be stale."];
    case "keyframe":
      return [
        "Visual anchors and generation reference bundles may need reinterpretation.",
        ...affectedContentUnits.length > 0 ? [`${affectedContentUnits.length} content production prompt bundle(s) may need reinterpretation.`] : []
      ];
    default:
      return ["Domain index should be refreshed."];
  }
}
function affectedContentUnitsForChangedEntity(changedEntity, index, relationGraph) {
  if (changedEntity.entityKind === "content_unit") {
    const sourceEntity = normalizeChangedEntityRef(changedEntity, index);
    const contentUnit = canonicalEntities22(index).find((entity) => entity.entityKind === "content_unit" && entityRefMatches(entityRef3(entity), sourceEntity));
    return changedEntity.id !== void 0 && hasSpecializedContentUnitAdapter(contentUnit?.record.content_unit_type) ? [{ entityKind: "content_unit", id: changedEntity.id, path: changedEntity.path }] : [];
  }
  const changedRef = normalizeChangedEntityRef(changedEntity, index);
  return canonicalEntities22(index).filter((entity) => entity.entityKind === "content_unit" && entity.id !== void 0 && hasSpecializedContentUnitAdapter(entity.record.content_unit_type)).filter((contentUnit) => contentUnitReferencesChangedEntity(entityRef3(contentUnit), changedRef, relationGraph)).map(entityRef3);
}
function contentUnitReferencesChangedEntity(contentUnit, changedRef, relationGraph) {
  const visited = /* @__PURE__ */ new Set();
  const queue = [contentUnit];
  while (queue.length > 0) {
    const current = queue.shift();
    if (!current) continue;
    const currentKey = entityRefKey(current);
    if (visited.has(currentKey)) continue;
    visited.add(currentKey);
    if (entityRefMatches(current, changedRef)) return true;
    for (const relation of relationGraph.relations) {
      if (isForwardDependencyRelation(relation, changedRef) && entityRefMatches(relation.from, current)) {
        queue.push(relation.to);
      }
      if (isAncestorContextChange(changedRef) && isParentContextRelation(relation) && entityRefMatches(relation.to, current)) {
        queue.push(relation.from);
      }
    }
  }
  return false;
}
function isForwardDependencyRelation(relation, changedRef) {
  if (relation.type === "references" || relation.type === "uses" || relation.type === "targets") return true;
  if (relation.type === "owns") return changedRef.entityKind === "asset";
  if (relation.type === "contains") {
    return changedRef.entityKind === "keyframe" || changedRef.entityKind === "expression_unit" || changedRef.entityKind === "audio_cue" || changedRef.entityKind === "storyboard" || changedRef.entityKind === "scene_moment";
  }
  return false;
}
function isAncestorContextChange(changedRef) {
  return changedRef.entityKind === "production" || changedRef.entityKind === "segment" || changedRef.entityKind === "setting" || changedRef.entityKind === "setting_state" || changedRef.entityKind === "scene_moment" || changedRef.entityKind === "expression_unit";
}
function isParentContextRelation(relation) {
  return relation.type === "contains" || relation.type === "owns";
}
function normalizeChangedEntityRef(changedEntity, index) {
  const existing = canonicalEntities22(index).find((entity) => {
    return entity.entityKind === changedEntity.entityKind && (entity.path === changedEntity.path || entity.id !== void 0 && changedEntity.id !== void 0 && String(entity.id) === String(changedEntity.id));
  });
  return existing ? entityRef3(existing) : {
    entityKind: changedEntity.entityKind,
    ...changedEntity.id !== void 0 ? { id: changedEntity.id } : {},
    path: changedEntity.path
  };
}
function staleMarkersForChangedEntity(entity, affectedContentUnits) {
  if (affectedContentUnits.length === 0) return [];
  if (entity.entityKind === "content_unit") {
    return affectedContentUnits.map((contentUnit) => `content_unit:${String(contentUnit.id ?? contentUnit.path)}:self_changed`);
  }
  const reason = entity.entityKind === "asset" || entity.entityKind === "setting" || entity.entityKind === "setting_state" ? "setting_context_changed" : entity.entityKind === "keyframe" ? "visual_anchor_changed" : "planning_context_changed";
  return affectedContentUnits.map((contentUnit) => `content_unit:${String(contentUnit.id ?? contentUnit.path)}:${reason}`);
}
function deriveMovScriptWorkspacePreviewTimelines2(index) {
  const sourceEntities = canonicalEntities3(index);
  const contentUnitsByPrimaryRef = groupContentUnitsByPrimaryRef3(index);
  return sourceEntities.filter(isProductionWithId2).map((production) => {
    const productionDir = entityDir32(production.path);
    const segments = childEntities2(index, productionDir, "segment");
    const items = [];
    let order = 0;
    for (const segment of sortEntities3(segments)) {
      const segmentItemId = timelineItemId2(segment);
      items.push({
        ...timelineItem2(segmentItemId, "segment", segment, order++),
        transition: recordField42(segment.record.transition)
      });
      const sceneMoments = childEntities2(index, entityDir32(segment.path), "scene_moment");
      for (const sceneMoment of sortEntities3(sceneMoments)) {
        const sceneMomentItemId = timelineItemId2(sceneMoment);
        const sceneMomentContentUnits = contentUnitsForEntity2(contentUnitsByPrimaryRef, "scene_moment", sceneMoment);
        items.push({
          ...timelineItem2(sceneMomentItemId, "scene_moment", sceneMoment, order++),
          parentId: segmentItemId,
          transition: recordField42(sceneMoment.record.transition),
          contentUnitIds: sceneMomentContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined22)
        });
        for (const contentUnit of sortEntities3(sceneMomentContentUnits)) {
          items.push({
            ...timelineItem2(timelineItemId2(contentUnit), "content_unit", contentUnit, order++),
            parentId: sceneMomentItemId
          });
        }
        for (const storyboard of sortEntities3(childEntities2(index, entityDir32(sceneMoment.path), "storyboard"))) {
          const storyboardItemId = timelineItemId2(storyboard);
          const contentUnits = contentUnitsForEntity2(contentUnitsByPrimaryRef, "storyboard", storyboard);
          const timeline = recordField42(storyboard.record.timeline);
          items.push({
            ...timelineItem2(storyboardItemId, "storyboard", storyboard, order++),
            parentId: sceneMomentItemId,
            caption: stringField33(timeline?.caption),
            gapAfterSec: numberField32(timeline?.gap_after_sec),
            timing: timeline,
            transition: recordField42(storyboard.record.transition),
            contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined22)
          });
          for (const contentUnit of sortEntities3(contentUnits)) {
            items.push({
              ...timelineItem2(timelineItemId2(contentUnit), "content_unit", contentUnit, order++),
              parentId: storyboardItemId
            });
          }
        }
        for (const keyframe of sortEntities3(childEntities2(index, entityDir32(sceneMoment.path), "keyframe"))) {
          const keyframeItemId = timelineItemId2(keyframe);
          const contentUnits = contentUnitsForEntity2(contentUnitsByPrimaryRef, "keyframe", keyframe);
          items.push({
            ...timelineItem2(keyframeItemId, "keyframe", keyframe, order++),
            parentId: sceneMomentItemId,
            timing: recordField42(keyframe.record.timing),
            contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined22)
          });
          for (const contentUnit of sortEntities3(contentUnits)) {
            items.push({
              ...timelineItem2(timelineItemId2(contentUnit), "content_unit", contentUnit, order++),
              parentId: keyframeItemId
            });
          }
        }
        for (const expressionUnit of sortEntities3(childEntities2(index, entityDir32(sceneMoment.path), "expression_unit"))) {
          const expressionUnitItemId = timelineItemId2(expressionUnit);
          const expressionUnitContentUnits = contentUnitsForEntity2(contentUnitsByPrimaryRef, "expression_unit", expressionUnit);
          items.push({
            ...timelineItem2(expressionUnitItemId, "expression_unit", expressionUnit, order++),
            parentId: sceneMomentItemId,
            contentUnitIds: expressionUnitContentUnits.map((contentUnit) => contentUnit.id).filter(isDefined22)
          });
          for (const contentUnit of sortEntities3(expressionUnitContentUnits)) {
            items.push({
              ...timelineItem2(timelineItemId2(contentUnit), "content_unit", contentUnit, order++),
              parentId: expressionUnitItemId
            });
          }
          for (const storyboard of sortEntities3(childEntities2(index, entityDir32(expressionUnit.path), "storyboard"))) {
            const storyboardItemId = timelineItemId2(storyboard);
            const contentUnits = contentUnitsForEntity2(contentUnitsByPrimaryRef, "storyboard", storyboard);
            const timeline = recordField42(storyboard.record.timeline);
            items.push({
              ...timelineItem2(storyboardItemId, "storyboard", storyboard, order++),
              parentId: expressionUnitItemId,
              caption: stringField33(timeline?.caption),
              gapAfterSec: numberField32(timeline?.gap_after_sec),
              timing: timeline,
              transition: recordField42(storyboard.record.transition),
              contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined22)
            });
            for (const contentUnit of sortEntities3(contentUnits)) {
              items.push({
                ...timelineItem2(timelineItemId2(contentUnit), "content_unit", contentUnit, order++),
                parentId: storyboardItemId
              });
            }
          }
          for (const keyframe of sortEntities3(childEntities2(index, entityDir32(expressionUnit.path), "keyframe"))) {
            const keyframeItemId = timelineItemId2(keyframe);
            const contentUnits = contentUnitsForEntity2(contentUnitsByPrimaryRef, "keyframe", keyframe);
            items.push({
              ...timelineItem2(keyframeItemId, "keyframe", keyframe, order++),
              parentId: expressionUnitItemId,
              timing: recordField42(keyframe.record.timing),
              contentUnitIds: contentUnits.map((contentUnit) => contentUnit.id).filter(isDefined22)
            });
            for (const contentUnit of sortEntities3(contentUnits)) {
              items.push({
                ...timelineItem2(timelineItemId2(contentUnit), "content_unit", contentUnit, order++),
                parentId: keyframeItemId
              });
            }
          }
        }
        for (const audioCue of sortEntities3(childEntities2(index, entityDir32(sceneMoment.path), "audio_cue"))) {
          items.push({
            ...timelineItem2(timelineItemId2(audioCue), "audio_cue", audioCue, order++),
            parentId: sceneMomentItemId,
            cueKind: stringField33(audioCue.record.cue_kind),
            timing: recordField42(audioCue.record.timing)
          });
        }
      }
    }
    return {
      schema: "movscript.preview_timeline.v1",
      productionId: production.id,
      productionPath: productionDir,
      items
    };
  });
}
function groupContentUnitsByPrimaryRef3(index) {
  const out = /* @__PURE__ */ new Map();
  for (const entity of canonicalEntities3(index)) {
    if (entity.entityKind !== "content_unit") continue;
    const contentUnitType2 = stringField33(entity.record.content_unit_type);
    if (!contentUnitType2 || !hasSpecializedContentUnitType(contentUnitType2)) continue;
    const assemblyScope = timelineAssemblyScopeFromContentUnitRecord(entity.record);
    if (assemblyScope) {
      for (const key of primaryRefKeys3(assemblyScope.kind, assemblyScope.ref)) {
        out.set(key, [...out.get(key) ?? [], entity]);
      }
    }
    const primaryKind = primaryRefKindForContentUnitType(contentUnitType2);
    if (!primaryKind) continue;
    const primaryRefs = primaryRefIdsForContentUnitRecord(entity.record, primaryKind);
    if (primaryRefs.length !== 1) continue;
    const primaryRef = primaryRefs[0];
    if (!primaryRef) continue;
    for (const key of primaryRefKeys3(primaryKind, primaryRef)) {
      out.set(key, [...out.get(key) ?? [], entity]);
    }
  }
  return out;
}
function contentUnitsForEntity2(contentUnitsByPrimaryRef, entityKind, entity) {
  if (entity.id === void 0) return [];
  return contentUnitsByPrimaryRef.get(primaryRefKey3(entityKind, entity.id)) ?? [];
}
function primaryRefKey3(kind, id) {
  return `${kind}:${String(id)}`;
}
function primaryRefKeys3(kind, ref) {
  const value = String(ref);
  const keys = [primaryRefKey3(kind, value)];
  const lastSegment = value.split("/").filter(Boolean).at(-1);
  if (lastSegment && lastSegment !== value) keys.push(primaryRefKey3(kind, lastSegment));
  return keys;
}
function timelineItem2(id, itemType, entity, order) {
  return {
    id,
    itemType,
    entity: entityRef22(entity),
    order,
    title: stringField33(entity.record.title) ?? String(entity.id ?? entity.path)
  };
}
function timelineItemId2(entity) {
  return `${entity.entityKind}:${String(entity.id ?? entity.path)}`;
}
function entityRef22(entity) {
  return {
    entityKind: entity.entityKind,
    ...entity.id !== void 0 ? { id: entity.id } : {},
    path: entity.path
  };
}
function childEntities2(index, parentDir, entityKind) {
  const collectionName = collectionDirForEntityKind2(entityKind);
  if (!collectionName) return [];
  return canonicalEntities3(index).filter((entity) => entity.entityKind === entityKind && entity.path.startsWith(`${parentDir}/${collectionName}/`) && entityDir32(entity.path).replace(`${parentDir}/${collectionName}/`, "").split("/").length === 1);
}
function collectionDirForEntityKind2(entityKind) {
  if (entityKind === "segment") return "segments";
  if (entityKind === "scene_moment") return "scene_moments";
  if (entityKind === "storyboard") return "storyboards";
  if (entityKind === "keyframe") return "keyframes";
  if (entityKind === "audio_cue") return "audio_cues";
  if (entityKind === "expression_unit") return "expression_units";
  return void 0;
}
function canonicalEntities3(index) {
  return index.entities;
}
function isProductionWithId2(entity) {
  return entity.entityKind === "production" && entity.id !== void 0;
}
function sortEntities3(entities) {
  return [...entities].sort((left, right) => {
    const leftOrder = numberField32(left.record.order) ?? Number.MAX_SAFE_INTEGER;
    const rightOrder = numberField32(right.record.order) ?? Number.MAX_SAFE_INTEGER;
    if (leftOrder !== rightOrder) return leftOrder - rightOrder;
    return left.path.localeCompare(right.path);
  });
}
function entityDir32(path) {
  return path.replace(/\/[^/]+$/, "");
}
function recordField42(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value) ? value : void 0;
}
function stringField33(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberField32(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function isDefined22(value) {
  return value !== void 0;
}
function deriveProductionWorkPlan(input) {
  const sourceIssues = input.sourceIssues ?? [];
  const items = [
    ...sourceIssues.map((issue, index) => fixSourceWorkItem(issue, index)),
    ...contentUnitWorkItems(input.index, input.contentUnitArtifacts),
    ...affectedOutputWorkItems(input.impactReport)
  ].sort((left, right) => left.priority - right.priority || left.id.localeCompare(right.id));
  return {
    schema: "movscript.production_work_plan.v1",
    created_at: input.createdAt,
    project: projectRef(input.index),
    source_status: {
      ready_to_interpret: !sourceIssues.some((issue) => issue.severity === "error"),
      has_pending_edits: (input.changedEntities?.length ?? input.impactReport.changedEntities.length) > 0,
      issue_count: sourceIssues.length
    },
    interpret_status: {
      status: "current",
      interpretation_id: input.interpretationId,
      interpreted_at: input.createdAt
    },
    items,
    summary: {
      open: items.filter((item) => item.status === "open" || item.status === "ready" || item.status === "blocked").length,
      blocking: items.filter((item) => item.severity === "blocking" || item.status === "blocked").length,
      human_recommended: items.filter((item) => item.recommended_actor === "human").length,
      agent_recommended: items.filter((item) => item.recommended_actor === "agent").length,
      ready_to_generate: items.filter((item) => item.kind === "generate_candidates" && item.status === "ready").length,
      stale_selections: items.filter((item) => item.kind === "review_stale_selection").length
    }
  };
}
function fixSourceWorkItem(issue, index) {
  const severity = issue.severity === "error" ? "blocking" : "warning";
  return {
    id: stableWorkItemId("fix_source", issue.path, issue.message, index),
    kind: "fix_source",
    status: issue.severity === "error" ? "blocked" : "open",
    severity,
    priority: issue.severity === "error" ? 10 : 30,
    reason: issue.message,
    target: {
      entityKind: "source_file",
      path: issue.path
    },
    blockers: issue.severity === "error" ? [{
      code: "source_issue",
      message: issue.message
    }] : void 0,
    allowed_actors: ["human", "agent"],
    recommended_actor: issue.message.includes("schema validation failed") ? "agent" : "human",
    actions: [{
      type: "open_editor",
      entityKind: "source_file",
      path: issue.path
    }],
    evidence: {
      severity: issue.severity,
      path: issue.path
    }
  };
}
function contentUnitWorkItems(index, artifacts) {
  const items = [];
  for (const artifact of artifacts) {
    const contentUnitRef = contentUnitTarget(artifact);
    const candidateCount = contentUnitCandidateCount(index, entityDir22(artifact.contentUnitPath));
    const blockers = blockersFor(artifact.dependencyReport.blockers ?? artifact.generationPrompt.blockers ?? []);
    const hasBlockingIssue = blockers.length > 0 || artifact.dependencyReport.issues.some((issue) => issue.severity === "error");
    if (hasBlockingIssue) {
      items.push({
        id: stableWorkItemId("content_unit_blocked", String(artifact.contentUnitId)),
        kind: "edit_structure",
        status: "blocked",
        severity: "blocking",
        priority: 20,
        reason: `Content unit ${String(artifact.contentUnitId)} has unresolved production inputs.`,
        target: contentUnitRef,
        blockers: blockers.length > 0 ? blockers : artifact.dependencyReport.issues.map((issue) => ({
          code: "content_unit_issue",
          message: issue.message
        })),
        allowed_actors: ["human", "agent"],
        recommended_actor: "agent",
        actions: [
          {
            type: "open_editor",
            entityKind: "content_unit",
            entityId: artifact.contentUnitId,
            path: artifact.contentUnitPath
          },
          {
            type: "derive_content_unit_artifact",
            contentUnitId: artifact.contentUnitId
          }
        ],
        evidence: {
          runtimeStatus: artifact.runtimePanel.status,
          blockerCodes: blockers.map((blocker) => blocker.code),
          issueCount: artifact.dependencyReport.issues.length
        }
      });
      continue;
    }
    if (artifact.selectionValidity.stale) {
      items.push({
        id: stableWorkItemId("review_stale_selection", String(artifact.contentUnitId), String(artifact.selectionValidity.candidate_id ?? "")),
        kind: "review_stale_selection",
        status: "open",
        severity: "warning",
        priority: 40,
        reason: `Selected candidate for content unit ${String(artifact.contentUnitId)} is stale.`,
        target: contentUnitRef,
        allowed_actors: ["human", "agent"],
        recommended_actor: "human",
        actions: [
          {
            type: "open_candidate_picker",
            contentUnitId: artifact.contentUnitId
          },
          {
            type: "agent_review_candidates",
            contentUnitId: artifact.contentUnitId
          },
          {
            type: "generate_candidates",
            contentUnitId: artifact.contentUnitId,
            capability: actionCapability(artifact.runtimePanel.output_kind),
            suggestedCandidateCount: 2
          },
          {
            type: "accept_stale",
            contentUnitId: artifact.contentUnitId
          }
        ],
        evidence: {
          candidateId: artifact.selectionValidity.candidate_id,
          resourceId: artifact.selectionValidity.resource_id,
          staleReasons: artifact.selectionValidity.stale_reasons ?? []
        }
      });
      continue;
    }
    if (!artifact.selectionValidity.selected && candidateCount > 0) {
      items.push({
        id: stableWorkItemId("select_candidate", String(artifact.contentUnitId)),
        kind: "select_candidate",
        status: "ready",
        severity: "warning",
        priority: 50,
        reason: `Content unit ${String(artifact.contentUnitId)} has candidates but no selected candidate.`,
        target: contentUnitRef,
        allowed_actors: ["human", "agent", "workflow"],
        recommended_actor: "human",
        actions: [
          {
            type: "open_candidate_picker",
            contentUnitId: artifact.contentUnitId
          },
          {
            type: "agent_review_candidates",
            contentUnitId: artifact.contentUnitId
          }
        ],
        evidence: {
          candidateCount
        }
      });
      continue;
    }
    if (artifact.runtimePanel.status === "ready" && !artifact.selectionValidity.selected) {
      items.push({
        id: stableWorkItemId("generate_candidates", String(artifact.contentUnitId)),
        kind: "generate_candidates",
        status: "ready",
        severity: "suggestion",
        priority: 60,
        reason: `Content unit ${String(artifact.contentUnitId)} is ready to generate candidates.`,
        target: contentUnitRef,
        allowed_actors: ["agent", "workflow"],
        recommended_actor: "agent",
        actions: [
          {
            type: "derive_content_unit_artifact",
            contentUnitId: artifact.contentUnitId
          },
          {
            type: "generate_candidates",
            contentUnitId: artifact.contentUnitId,
            capability: actionCapability(artifact.runtimePanel.output_kind),
            suggestedCandidateCount: 2
          }
        ],
        evidence: {
          outputKind: artifact.runtimePanel.output_kind,
          capability: artifact.runtimePanel.runtime_request?.capability
        }
      });
    }
  }
  return items;
}
function affectedOutputWorkItems(impactReport) {
  const items = [];
  const seen = /* @__PURE__ */ new Set();
  for (const change of impactReport.changedEntities) {
    for (const contentUnit of change.affectedContentUnits) {
      const key = `${String(contentUnit.id ?? contentUnit.path)}:${change.entityKind}:${String(change.id ?? change.path)}`;
      if (seen.has(key)) continue;
      seen.add(key);
      items.push({
        id: stableWorkItemId("review_affected_output", key),
        kind: "review_affected_output",
        status: "informational",
        severity: "suggestion",
        priority: 80,
        reason: `${change.entityKind} change may affect content unit ${String(contentUnit.id ?? contentUnit.path)}.`,
        target: contentUnit,
        upstream: [{
          entityKind: change.entityKind,
          ...change.id !== void 0 ? { id: change.id } : {},
          path: change.path
        }],
        allowed_actors: ["human", "workflow"],
        recommended_actor: "human",
        actions: [{
          type: "open_editor",
          entityKind: "content_unit",
          entityId: contentUnit.id,
          path: contentUnit.path
        }],
        evidence: {
          changedEntity: change,
          staleMarkers: change.staleMarkers
        }
      });
    }
  }
  return items;
}
function projectRef(index) {
  const project = canonicalEntities22(index).find((entity) => entity.entityKind === "project");
  return project ? entityRef3(project) : void 0;
}
function contentUnitTarget(artifact) {
  return {
    entityKind: "content_unit",
    id: artifact.contentUnitId,
    path: artifact.contentUnitPath
  };
}
function contentUnitCandidateCount(index, contentUnitRef) {
  const candidateIds = /* @__PURE__ */ new Set();
  for (const document of index.documents) {
    if (!document.path.startsWith(`${contentUnitRef}/candidates/`)) continue;
    if (!document.path.endsWith("/content_candidate.json")) continue;
    const record = recordField32(document.data);
    const id = idField22(record?.id);
    if (id !== void 0) candidateIds.add(String(id));
  }
  for (const document of index.documents) {
    const context = recordField32(document.data);
    if (context?.schema !== "movscript.decision_context.v1") continue;
    if (context.target_kind !== "content_unit" || context.target_ref !== contentUnitRef) continue;
    const candidates = Array.isArray(context.candidates) ? context.candidates : [];
    for (const candidate of candidates) {
      const id = idField22(recordField32(candidate)?.id);
      if (id !== void 0) candidateIds.add(String(id));
    }
  }
  return candidateIds.size;
}
function blockersFor(blockers) {
  return blockers.map((blocker) => ({
    code: blocker.code,
    message: blocker.message,
    ...stringField26(blocker.ref) ? { ref: stringField26(blocker.ref) } : {}
  }));
}
function actionCapability(outputKind) {
  if (outputKind === "image" || outputKind === "video" || outputKind === "audio" || outputKind === "text") return outputKind;
  return "text";
}
function stableWorkItemId(...parts) {
  return parts.filter((part) => part !== void 0 && String(part).trim() !== "").map((part) => String(part).replace(/[^a-zA-Z0-9_-]+/g, "_").replace(/^_+|_+$/g, "")).filter(Boolean).join(":");
}
function deriveRelationGraph(index) {
  const relations = [];
  const sourceEntities = canonicalEntities22(index);
  const entities = sourceEntities.filter((entity) => entity.id !== void 0);
  const entityByPathDir = new Map(sourceEntities.map((entity) => [entityDir22(entity.path), entity]));
  const entityByPath = new Map(sourceEntities.map((entity) => [entity.path, entity]));
  const entityById = new Map(entities.map((entity) => [entityKey2(entity.entityKind, entity.id), entity]));
  const normalizedParentRelationKeys = /* @__PURE__ */ new Set();
  const normalizedContentUnitRelationKeys = /* @__PURE__ */ new Set();
  for (const edge of index.domainEdges ?? []) {
    if (edge.relation === "parent" && edge.origin === "path") {
      const parent = edge.target.path ? entityByPath.get(edge.target.path) : void 0;
      const child = edge.source.path ? entityByPath.get(edge.source.path) : void 0;
      if (!parent || !child) continue;
      const relation = {
        type: relationTypeForParent(parent.entityKind, child.entityKind),
        from: entityRef3(parent),
        to: entityRef3(child)
      };
      normalizedParentRelationKeys.add(parentRelationKey(parent, child));
      relations.push(relation);
      continue;
    }
    const contentUnitRelation = contentUnitRelationFromDomainEdge(edge, entities, entityByPath);
    if (!contentUnitRelation) continue;
    if (edge.source.path) normalizedContentUnitRelationKeys.add(contentUnitDomainEdgeKey(edge.source.path, edge.relation));
    relations.push(contentUnitRelation);
  }
  for (const entity of sourceEntities) {
    const parent = nearestParentEntity(entity.path, entityByPathDir);
    if (parent && !normalizedParentRelationKeys.has(parentRelationKey(parent, entity))) {
      relations.push({
        type: relationTypeForParent(parent.entityKind, entity.entityKind),
        from: entityRef3(parent),
        to: entityRef3(entity)
      });
    }
    if (entity.entityKind === "content_unit" && hasSpecializedContentUnitAdapter(entity.record.content_unit_type)) {
      for (const edge of normalizeContentUnitTargetEdges({
        source: domainRefFromContentUnit(entity),
        record: entity.record,
        scopeTarget(scope) {
          return domainRefFromTimelineScope3(scope.kind, scope.ref, index, entities, entityByPath);
        }
      })) {
        if (normalizedContentUnitRelationKeys.has(contentUnitDomainEdgeKey(entity.path, edge.relation))) continue;
        const relation = contentUnitRelationFromDomainEdge(edge, entities, entityByPath);
        if (relation) {
          normalizedContentUnitRelationKeys.add(contentUnitDomainEdgeKey(entity.path, edge.relation));
          relations.push(relation);
        }
      }
      const targetKind = typeof entity.record.target_kind === "string" ? entity.record.target_kind : void 0;
      const targetRef = entity.record.target_ref;
      if (targetKind && targetRef !== void 0) {
        const target = targetKind === "content_unit" ? entities.find((candidate) => candidate.entityKind === "content_unit" && String(candidate.id ?? "") === String(targetRef)) : findEntityByRef2(entities, targetKind, targetRef) ?? entityByPathDir.get(normalizedRefDir(targetRef));
        if (target) {
          relations.push({
            type: targetKind === "content_unit" ? "references" : "uses",
            from: entityRef3(entity),
            to: entityRef3(target),
            field: "target_ref"
          });
        }
      }
      const contentUnitType2 = String(entity.record.content_unit_type ?? "");
      const primaryKind = primaryRefKindForContentUnitType2(contentUnitType2);
      if (primaryKind) {
        for (const ref of primaryContentUnitRefs(entity, primaryKind)) {
          const target = ref.kind === "content_unit" ? entities.find((candidate) => candidate.entityKind === "content_unit" && String(candidate.id ?? "") === ref.id) : findEntityByRef2(entities, ref.kind, ref.id) ?? entityByPathDir.get(normalizedRefDir(ref.id));
          if (target) {
            relations.push({
              type: ref.kind === "content_unit" ? "references" : "uses",
              from: entityRef3(entity),
              to: entityRef3(target),
              field: primaryRefFieldNameForKind2(primaryKind)
            });
          }
        }
      }
      for (const ref of parseContentUnitEditPromptRefs2(entity.record.edit_prompt)) {
        const target = ref.kind === "content_unit" ? entities.find((candidate) => candidate.entityKind === "content_unit" && String(candidate.id ?? "") === ref.id) : findEntityByRef2(entities, ref.kind, ref.id) ?? entityByPathDir.get(normalizedRefDir(ref.id));
        if (target) {
          relations.push({
            type: ref.kind === "content_unit" ? "references" : "uses",
            from: entityRef3(entity),
            to: entityRef3(target),
            field: ref.source.field
          });
        }
      }
    }
    if (entity.entityKind === "expression_unit") {
      const sourceExpression = findEntityByRef2(entities, "expression_unit", entity.record.source_expression_ref) ?? entityByPathDir.get(normalizedRefDir(entity.record.source_expression_ref));
      const speaker = findEntityByRef2(entities, "setting", entity.record.speaker_ref) ?? entityByPathDir.get(normalizedRefDir(entity.record.speaker_ref));
      if (sourceExpression) relations.push({ type: "references", from: entityRef3(entity), to: entityRef3(sourceExpression), field: "source_expression_ref" });
      if (speaker) relations.push({ type: "uses", from: entityRef3(entity), to: entityRef3(speaker), field: "speaker_ref" });
      for (const expressionRef of arrayField6(recordField32(entity.record.span)?.expression_refs)) {
        const expression = findEntityByRef2(entities, "expression_unit", expressionRef) ?? entityByPathDir.get(normalizedRefDir(expressionRef));
        if (expression) relations.push({ type: "references", from: entityRef3(entity), to: entityRef3(expression), field: "span.expression_refs" });
      }
    }
    if (entity.entityKind === "audio_cue") {
      const scope = entityByPathDir.get(normalizedRefDir(entity.record.scope_ref));
      const storyboard = entityByPathDir.get(normalizedRefDir(entity.record.storyboard_ref));
      if (scope) relations.push({ type: "references", from: entityRef3(entity), to: entityRef3(scope), field: "scope_ref" });
      if (storyboard) relations.push({ type: "references", from: entityRef3(entity), to: entityRef3(storyboard), field: "storyboard_ref" });
      for (const assetRef3 of arrayField6(entity.record.asset_refs)) {
        const asset = findEntityByRef2(entities, "asset", assetRef3) ?? entityByPathDir.get(normalizedRefDir(assetRef3));
        if (asset) relations.push({ type: "uses", from: entityRef3(entity), to: entityRef3(asset), field: "asset_refs" });
      }
    }
    if (entity.entityKind === "storyboard") {
      const expressionUnit = parentExpressionUnitForEntity(entity, entityByPathDir, entityById);
      if (expressionUnit) relations.push({ type: "references", from: entityRef3(entity), to: entityRef3(expressionUnit), field: "expression_unit_ref" });
      for (const settingRef of arrayField6(entity.record.setting_refs).filter(isRecord25)) {
        const setting = findEntityByRef2(entities, "setting", settingRef.setting_id);
        const settingState = findEntityByRef2(entities, "setting_state", settingRef.setting_state_id);
        if (setting) relations.push({ type: "uses", from: entityRef3(entity), to: entityRef3(setting), field: "setting_refs.setting_id" });
        if (settingState) relations.push({ type: "uses", from: entityRef3(entity), to: entityRef3(settingState), field: "setting_refs.setting_state_id" });
      }
    }
    if (entity.entityKind === "keyframe") {
      const expressionUnit = parentExpressionUnitForEntity(entity, entityByPathDir, entityById);
      if (expressionUnit) relations.push({ type: "references", from: entityRef3(entity), to: entityRef3(expressionUnit), field: "expression_unit_ref" });
      for (const assetRef3 of arrayField6(entity.record.reference_asset_refs)) {
        const asset = findEntityByRef2(entities, "asset", assetRef3) ?? entityByPathDir.get(normalizedRefDir(assetRef3));
        if (asset) relations.push({ type: "uses", from: entityRef3(entity), to: entityRef3(asset), field: "reference_asset_refs" });
      }
    }
  }
  return { schema: "movscript.relation-graph.v1", relations: dedupeRelations(relations) };
}
function parentRelationKey(parent, child) {
  return `${parent.entityKind}:${parent.path}->${child.entityKind}:${child.path}`;
}
function contentUnitRelationFromDomainEdge(edge, entities, entityByPath) {
  if (edge.relation !== "target" && edge.relation !== "scope") return void 0;
  const source = sourceContentUnitFromDomainEdge(edge, entities, entityByPath);
  if (!source) return void 0;
  return {
    type: edge.relation === "target" ? "targets" : "uses",
    from: entityRef3(source),
    to: entityRefFromDomainEdgeTarget(edge, entities, entityByPath),
    ...edge.field ? { field: edge.field } : {}
  };
}
function sourceContentUnitFromDomainEdge(edge, entities, entityByPath) {
  const source = edge.source.path ? entityByPath.get(edge.source.path) : findEntityByRef2(entities, "content_unit", edge.source.id);
  return source?.entityKind === "content_unit" ? source : void 0;
}
function entityRefFromDomainEdgeTarget(edge, entities, entityByPath) {
  const target = edge.target.path ? entityByPath.get(edge.target.path) : edge.target.kind && edge.target.id !== void 0 ? findEntityByRef2(entities, edge.target.kind, edge.target.id) : void 0;
  if (target) return entityRef3(target);
  return {
    entityKind: edge.target.kind,
    ...edge.target.id !== void 0 ? { id: edge.target.id } : {},
    ...edge.target.path ? { path: edge.target.path } : {}
  };
}
function contentUnitDomainEdgeKey(path, relation) {
  return `${relation}:${path}`;
}
function domainRefFromContentUnit(entity) {
  return {
    category: "content_unit",
    kind: "content_unit",
    ...entity.id !== void 0 ? { id: entity.id } : {},
    path: entity.path
  };
}
function domainRefFromTimelineScope3(scopeKind, scopeRef, index, entities, entityByPath) {
  const entity = findEntityByRef2(entities, scopeKind, scopeRef);
  if (entity) {
    return {
      category: "timeline_namespace",
      kind: entity.entityKind,
      ...entity.id !== void 0 ? { id: entity.id } : {},
      path: entity.path
    };
  }
  const node = index.domainNodes.find(
    (candidate) => candidate.category === "timeline_namespace" && candidate.kind === scopeKind && String(candidate.id ?? "") === scopeRef
  );
  if (node) {
    return {
      category: node.category,
      kind: node.kind,
      ...node.id !== void 0 ? { id: node.id } : {},
      ...node.path ? { path: node.path } : {}
    };
  }
  const pathEntity = entityByPath.get(normalizedRefDir(scopeRef));
  if (pathEntity) {
    return {
      category: "timeline_namespace",
      kind: pathEntity.entityKind,
      ...pathEntity.id !== void 0 ? { id: pathEntity.id } : {},
      path: pathEntity.path
    };
  }
  return { category: "timeline_namespace", kind: scopeKind, id: scopeRef };
}
function deriveMovScriptWorkspaceArtifacts(input) {
  const relationGraph = deriveRelationGraph(input.index);
  const impactReport = deriveImpactReport(input.changedEntities, input.interpretationId, input.createdAt, input.index, relationGraph, input.semanticChanges);
  const contentUnitArtifacts = deriveContentUnitArtifacts(input.index, { createdAt: input.createdAt });
  const editPlans = deriveEditPlans(input.index, contentUnitArtifacts);
  return {
    domainTree: deriveDomainTree(input.index),
    relationGraph,
    assetIndex: deriveAssetIndex(input.index),
    impactReport,
    previewTimelines: deriveMovScriptWorkspacePreviewTimelines2(input.index),
    editPlans,
    contentUnitArtifacts,
    productionWorkPlan: deriveProductionWorkPlan({
      index: input.index,
      contentUnitArtifacts,
      impactReport,
      sourceIssues: input.sourceIssues,
      changedEntities: input.changedEntities,
      interpretationId: input.interpretationId,
      createdAt: input.createdAt
    })
  };
}

// ../../packages/interpreter/dist/chunk-UOB52N3N.js
function productionImpactsFromSemanticChanges(semanticChanges, artifacts, staleSelections) {
  const impacts = [];
  const impactBySource = new Map(artifacts.impactReport.changedEntities.map((entity) => [
    entityImpactKey(entity.entityKind, entity.id, entity.path),
    entity
  ]));
  for (const change of semanticChanges) {
    if (change.propagation === "none") {
      impacts.push({
        kind: "diagnostic_only",
        businessKinds: [change.businessKind],
        businessImpacts: [businessImpactLabel2(change.businessKind)],
        sourceChanges: [change],
        reshootRequired: false
      });
      continue;
    }
    const staleMatches = staleSelections.filter((selection) => {
      if (change.entity.kind === "content_unit") {
        return sameOptionalId2(selection.contentUnitId, change.entity.id) || sameOptionalPath2(selection.contentUnitPath, change.sourceChange.path);
      }
      const sourceImpact2 = impactBySource.get(entityImpactKey(change.entity.kind, change.entity.id, change.sourceChange.path));
      return sourceImpact2?.affectedContentUnits.some((contentUnit) => {
        return sameOptionalId2(contentUnit.id, selection.contentUnitId) || sameOptionalPath2(contentUnit.path, selection.contentUnitPath);
      }) ?? false;
    });
    if (change.propagation === "self") {
      for (const selection of staleMatches) {
        impacts.push({
          contentUnit: {
            ...selection.contentUnitId !== void 0 ? { id: selection.contentUnitId } : {},
            ...selection.contentUnitPath !== void 0 ? { path: selection.contentUnitPath } : {}
          },
          kind: "self_selection_stale",
          businessKinds: [change.businessKind],
          businessImpacts: [businessImpactLabel2(change.businessKind)],
          sourceChanges: [change],
          reshootRequired: false
        });
      }
      if (staleMatches.length === 0) {
        impacts.push({
          kind: "diagnostic_only",
          businessKinds: [change.businessKind],
          businessImpacts: [businessImpactLabel2(change.businessKind)],
          sourceChanges: [change],
          reshootRequired: false
        });
      }
      continue;
    }
    const sourceImpact = impactBySource.get(entityImpactKey(change.entity.kind, change.entity.id, change.sourceChange.path));
    const affectedContentUnits = sourceImpact?.affectedContentUnits ?? [];
    for (const contentUnit of affectedContentUnits) {
      impacts.push({
        contentUnit: {
          ...contentUnit.id !== void 0 ? { id: contentUnit.id } : {},
          ...contentUnit.path !== void 0 ? { path: entityDir6(contentUnit.path) } : {}
        },
        kind: "downstream_reference_changed",
        businessKinds: [change.businessKind],
        businessImpacts: [businessImpactLabel2(change.businessKind)],
        sourceChanges: [change],
        reshootRequired: false
      });
    }
    if (affectedContentUnits.length === 0) {
      impacts.push({
        kind: "diagnostic_only",
        businessKinds: [change.businessKind],
        businessImpacts: [businessImpactLabel2(change.businessKind)],
        sourceChanges: [change],
        reshootRequired: false
      });
    }
  }
  return dedupeProductionImpacts(impacts);
}
function entityImpactKey(entityKind, id, path) {
  return `${entityKind}:${String(id ?? "")}:${path ? entityDir6(path) : ""}`;
}
function sameOptionalId2(left, right) {
  return left !== void 0 && right !== void 0 && String(left) === String(right);
}
function sameOptionalPath2(left, right) {
  if (typeof left !== "string" || typeof right !== "string") return false;
  return entityDir6(left) === entityDir6(right) || normalizeWorkspacePath7(left) === normalizeWorkspacePath7(right);
}
function dedupeProductionImpacts(impacts) {
  const seen = /* @__PURE__ */ new Set();
  return impacts.filter((impact) => {
    const source = impact.sourceChanges[0];
    const key = [
      impact.kind,
      impact.contentUnit?.id ?? impact.contentUnit?.path ?? "",
      source?.entity.kind ?? "",
      source?.entity.id ?? "",
      source?.businessKind ?? "",
      source?.sourceChange.path ?? "",
      source?.fields.join(",") ?? ""
    ].join("|");
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}
function entityDir6(path) {
  return normalizeWorkspacePath7(path).replace(/\/[^/]+$/, "");
}
function normalizeWorkspacePath7(path) {
  return path.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+/g, "/").replace(/\/$/, "");
}
function businessImpactLabel2(businessKind) {
  const labels = {
    metadata_changed: "Metadata changed",
    semantic_input_changed: "Semantic input changed",
    reference_changed: "Reference changed",
    selection_changed: "Selection changed",
    sequence_reordered: "Sequence reordered",
    expression_unit_changed: "Expression unit changed",
    storyboard_changed: "Storyboard changed",
    keyframe_changed: "Keyframe changed",
    content_unit_changed: "Content unit changed",
    project_context_changed: "Project context changed",
    production_structure_changed: "Production structure changed",
    domain_entity_changed: "Domain entity changed"
  };
  return labels[businessKind];
}

// ../../packages/interpreter/dist/chunk-PRU4XU7S.js
function diffMovScriptFileSnapshots(workingFiles, baselineFiles, options = {}) {
  const basePath = options.basePath ?? "";
  const workingByRelativePath = new Map(workingFiles.map((file) => [file.relativePath, file]));
  const baselineByRelativePath = new Map(baselineFiles.map((file) => [file.relativePath, file]));
  const keys = [.../* @__PURE__ */ new Set([...workingByRelativePath.keys(), ...baselineByRelativePath.keys()])].sort();
  return keys.flatMap((relativePath) => {
    const working = workingByRelativePath.get(relativePath);
    const baseline = baselineByRelativePath.get(relativePath);
    if (working && !baseline) {
      return [{
        path: working.path,
        currentPath: joinWorkspacePath(basePath, relativePath),
        state: "added",
        contentHash: working.hash
      }];
    }
    if (!working && baseline) {
      return [{
        path: relativePath,
        currentPath: baseline.path,
        state: "deleted",
        currentContentHash: baseline.hash
      }];
    }
    if (working && baseline && working.hash !== baseline.hash) {
      return [{
        path: working.path,
        currentPath: baseline.path,
        state: "modified",
        contentHash: working.hash,
        currentContentHash: baseline.hash
      }];
    }
    return [];
  });
}
function joinWorkspacePath(root, relativePath) {
  return root ? `${root.replace(/\/$/, "")}/${relativePath}` : relativePath;
}

// ../../packages/interpreter/dist/chunk-PIN77OHR.js
async function reviewMovScriptWorkspace(input) {
  const now = input.now ?? /* @__PURE__ */ new Date();
  const sourceOptions = workspaceSourceOptions(input);
  const source = await resolveWorkspaceSource(input.fileRepository, sourceOptions);
  const editFiles = source.files;
  const baseline = await loadCheckpointSourceSnapshots(input.fileRepository, input.commit ?? input.checkpointHash, sourceOptions);
  const currentFiles = baseline.files;
  const changedFiles = diffWorkspaceFiles(editFiles, currentFiles, baseline.basePath);
  const sourceGraph = buildSourceDomainGraph(editFiles);
  const currentGraph = buildSourceDomainGraph(currentFiles);
  const changedEntities = changedEntitiesFromFiles(changedFiles, sourceGraph, currentGraph);
  const issues = [
    ...validateEditableFiles(editFiles),
    ...validateSourceDomainGraph(sourceGraph),
    ...await validateGitFileChangeCoverage(input.fileRepository, baseline, changedFiles, sourceOptions)
  ];
  const semanticChanges = semanticChangesFromEntityChanges(changedEntities);
  const sourceDocuments = editFiles.map((file) => ({
    path: file.relativePath,
    data: parseWorkspaceDocument5(file.path, file.content)
  }));
  const documents = await overlayMovScriptDecisionDocuments(sourceDocuments, input.decisionStore);
  const index = deriveMovScriptWorkspaceDomainIndex(documents);
  const artifacts = deriveMovScriptWorkspaceArtifacts({
    index,
    changedEntities,
    semanticChanges,
    interpretationId: `review_${now.toISOString().replace(/[-:.TZ]/g, "")}`,
    createdAt: now.toISOString(),
    sourceIssues: issues
  });
  const selectionValidity = artifacts.contentUnitArtifacts.map((artifact) => ({
    contentUnitId: artifact.contentUnitId,
    contentUnitPath: entityDir7(artifact.contentUnitPath),
    selected: artifact.selectionValidity.selected,
    stale: artifact.selectionValidity.stale,
    candidateId: artifact.selectionValidity.candidate_id,
    resourceId: artifact.selectionValidity.resource_id,
    staleReasons: artifact.selectionValidity.stale_reasons
  }));
  const staleSelections = selectionValidity.filter((selection) => selection.stale);
  const productionImpacts = productionImpactsFromSemanticChanges(semanticChanges, artifacts, staleSelections);
  const businessChanges = businessChangesFromChangedEntities(changedEntities, sourceGraph, currentGraph, semanticChanges);
  const summary = summarizeReview(changedFiles, businessChanges, issues);
  return {
    schema: "movscript.workspace-review.v1",
    operation: "review",
    basePath: baseline.basePath,
    comparisonBase: {
      ...baseline.checkpointHash ? { from: baseline.checkpointHash } : {},
      source: baseline.source,
      workspace: {
        id: workspaceSnapshotId(source.files),
        kind: "working_tree"
      }
    },
    sourcePath: source.rootPath,
    sourceMode: source.mode,
    createdAt: now.toISOString(),
    changedFiles,
    changedEntities,
    entityChanges: changedEntities,
    semanticChanges,
    productionImpacts,
    selectionValidity,
    staleSelections,
    productionWorkPlan: artifacts.productionWorkPlan,
    reshootTargets: [],
    businessChanges,
    issues,
    readyToInterpret: summary.errors === 0,
    summary
  };
}
async function inspectMovScriptWorkspace(input) {
  const review = await reviewMovScriptWorkspace(input);
  return {
    ...review,
    schema: "movscript.workspace-inspection.v1",
    operation: "inspect"
  };
}
function entityDir7(path) {
  return normalizeWorkspacePath(path).replace(/\/[^/]+$/, "");
}
function workspaceSourceOptions(input) {
  return {
    includeContentUnitDecisionDocuments: input.decisionStore ? false : true
  };
}
function diffWorkspaceFiles(editFiles, currentFiles, basePath = MOVSCRIPT_INTERPRET_CURRENT_DIR) {
  return diffMovScriptFileSnapshots(editFiles, currentFiles, { basePath });
}
function parseWorkspaceDocument5(path, content) {
  if (!path.endsWith(".json")) return content;
  try {
    return JSON.parse(content);
  } catch {
    return void 0;
  }
}

// ../../packages/interpreter/dist/node.js
async function overviewMovScriptWorkspace(input) {
  const now = input.now ?? /* @__PURE__ */ new Date();
  const [inspection, source, latestInterpretation] = await Promise.all([
    inspectMovScriptWorkspace({ ...input, now }),
    resolveWorkspaceSource(input.fileRepository, workspaceSourceOptions2(input)),
    loadLatestInterpretManifest(input.fileRepository)
  ]);
  const impactReport = await loadLatestImpactReport(input.fileRepository, latestInterpretation);
  const regeneration = deriveV1RegenerationPlan({
    review: inspection,
    latestInterpretation,
    impactReport,
    createdAt: now.toISOString()
  });
  return interpretWorkspaceOverview({
    createdAt: now.toISOString(),
    inspection,
    source,
    latestInterpretation,
    regeneration
  });
}
async function interpretMovScriptWorkspace(input) {
  const now = input.now ?? /* @__PURE__ */ new Date();
  const review = await reviewMovScriptWorkspace({ ...input, now });
  if (!review.readyToInterpret) {
    return {
      schema: "movscript.workspace-interpret-result.v1",
      operation: "interpret",
      status: "failed",
      review
    };
  }
  const source = await resolveWorkspaceSource(input.fileRepository, workspaceSourceOptions2(input));
  const editFiles = source.files;
  const sourceDocuments = editFiles.map((file) => ({
    path: file.relativePath,
    data: parseWorkspaceDocument6(file.path, file.content)
  }));
  const documents = await overlayMovScriptDecisionDocuments(sourceDocuments, input.decisionStore);
  const index = deriveMovScriptWorkspaceDomainIndex(documents);
  const interpretationId = interpretationIdFor(now);
  const artifacts = deriveMovScriptWorkspaceArtifacts({
    index,
    changedEntities: review.changedEntities,
    semanticChanges: review.semanticChanges,
    sourceIssues: review.issues,
    interpretationId,
    createdAt: now.toISOString()
  });
  const impactReportPath = `.interpret/reviews/impact-report_${interpretationId}.json`;
  const manifest = {
    schema: "movscript.workspace-interpret.v1",
    interpretationId,
    interpretedAt: now.toISOString(),
    source: {
      sourcePath: source.rootPath,
      sourceMode: source.mode,
      sourceFileHashes: Object.fromEntries(editFiles.map((file) => [file.relativePath, file.hash]))
    },
    output: {
      currentPath: MOVSCRIPT_INTERPRET_CURRENT_DIR,
      domainIndexPath: MOVSCRIPT_DOMAIN_INDEX_PATH,
      domainTreePath: MOVSCRIPT_DOMAIN_TREE_PATH,
      editorStatePath: MOVSCRIPT_EDITOR_STATE_PATH,
      assetIndexPath: MOVSCRIPT_ASSET_INDEX_PATH,
      relationGraphPath: MOVSCRIPT_RELATION_GRAPH_PATH,
      impactReportPath
    },
    review
  };
  if (input.debugArtifacts !== false) {
    await writeDebugArtifacts(input.fileRepository, artifacts, index, manifest, impactReportPath);
  }
  return {
    schema: "movscript.workspace-interpret-result.v1",
    operation: "interpret",
    status: "refreshed",
    review,
    index,
    productionWorkPlan: artifacts.productionWorkPlan,
    ...input.debugArtifacts !== false ? { manifest } : {}
  };
}
async function planMovScriptWorkspaceRegeneration(input) {
  const now = input.now ?? /* @__PURE__ */ new Date();
  const review = await reviewMovScriptWorkspace({ ...input, now });
  const latestInterpretation = await loadLatestInterpretManifest(input.fileRepository);
  const impactReport = await loadLatestImpactReport(input.fileRepository, latestInterpretation);
  return deriveV1RegenerationPlan({
    review,
    latestInterpretation,
    impactReport,
    createdAt: now.toISOString()
  });
}
function parseWorkspaceDocument6(path, content) {
  if (!path.endsWith(".json")) return content;
  try {
    return JSON.parse(content);
  } catch {
    return void 0;
  }
}
function workspaceSourceOptions2(input) {
  return {
    includeContentUnitDecisionDocuments: input.decisionStore ? false : true
  };
}
function interpretationIdFor(date) {
  return `interpret_${date.toISOString().replace(/[^0-9]/g, "")}`;
}

// ../../packages/prompt/dist/index.js
var PROMPT_REF_PATTERN3 = /\{\{([a-z_]+)::?([^{}:\s][^{}]*)\}\}/g;
async function buildContentUnitBackendPromptById(input) {
  const contentUnit = queryMovScriptWorkspaceEntities(input.index, { entityKind: "content_unit" }).find((entity) => entity.id !== void 0 && sameEntityRef(entity.id, input.contentUnitId, "content_unit"));
  if (!contentUnit) {
    return failedPrompt({
      contentUnitRef: `content_units/${entityPathSlug(input.contentUnitId, "content_unit")}`,
      contentUnitId: input.contentUnitId,
      contentUnitType: "",
      outputKind: "metadata",
      blockers: [{
        code: "content_unit_not_found",
        content_unit_id: input.contentUnitId,
        message: `content unit does not exist: ${String(input.contentUnitId)}`
      }]
    });
  }
  return buildContentUnitBackendPrompt({
    index: input.index,
    contentUnit,
    decisionProvider: input.decisionProvider,
    ...input.promptText !== void 0 ? { promptText: input.promptText } : {}
  });
}
async function buildContentUnitBackendPrompt(input) {
  const contentUnitRef = entityDir8(input.contentUnit.path);
  const contentUnitType2 = stringField17(input.contentUnit.record.content_unit_type) ?? "";
  const outputKind = contentUnitOutputKind3(input.contentUnit.record.output_kind);
  const editPrompt = contentUnitEditPrompt(input.contentUnit.record.edit_prompt, input.promptText);
  const refs = parseContentUnitEditPromptRefs3(editPrompt);
  const unsupportedRefs = parseUnsupportedContentUnitEditPromptRefs2(editPrompt);
  const primaryKind = primaryRefKindForContentUnitType3(contentUnitType2);
  const primaryRefs = primaryKind ? primaryContentUnitRefs2(input.contentUnit, primaryKind) : [];
  const styleReferenceResourceIds = usesVisualStyleReferences2(outputKind) ? projectStyleReferenceResourceIds2(input.index) : [];
  const decisionCache = /* @__PURE__ */ new Map();
  const blockers = [];
  if (primaryKind) {
    if (primaryRefs.length === 0) {
      blockers.push({
        code: "primary_ref_missing",
        content_unit_ref: contentUnitRef,
        message: `${contentUnitType2} requires a ${primaryKind} primary ref`
      });
    }
    if (hasAmbiguousPrimaryRefs2(primaryRefs, primaryKind)) {
      blockers.push({
        code: "primary_ref_ambiguous",
        content_unit_ref: contentUnitRef,
        message: `${contentUnitType2} accepts only one ${primaryKind} primary ref`
      });
    }
  }
  if (input.contentUnit.id === void 0) {
    blockers.push({
      code: "content_unit_id_missing",
      content_unit_ref: contentUnitRef,
      message: `content unit is missing id: ${contentUnitRef}`
    });
  }
  for (const ref of unsupportedRefs) {
    blockers.push(unsupportedPromptRefBlocker(ref));
  }
  const resolvedRefs = [];
  for (const ref of refs) {
    const role = ref.role ?? "input";
    const directRef = await resolveDirectPromptResourceRef({
      ref,
      role,
      contentUnit: input.contentUnit,
      contentUnitRef,
      decisionProvider: input.decisionProvider,
      decisionCache
    });
    if (directRef) {
      if (directRef.blocker) blockers.push(directRef.blocker);
      resolvedRefs.push(directRef);
      continue;
    }
    const entity = resolvePromptRefEntity2(input.index, ref);
    const base = {
      ...ref,
      role,
      ...entity ? {
        resolved: {
          entityKind: entity.entityKind,
          ...entity.id !== void 0 ? { id: entity.id } : {},
          path: entity.path
        }
      } : {}
    };
    if (!entity) {
      const blocker = {
        code: "ref_not_found",
        ref: ref.raw,
        message: `prompt ref does not resolve: ${ref.raw}`
      };
      blockers.push(blocker);
      resolvedRefs.push({ ...base, blocker });
      continue;
    }
    const upstream = resolveContentUnitForPromptRef2(input.index, ref);
    if (!upstream || upstream.id === void 0) {
      const blocker = {
        code: "upstream_content_unit_not_found",
        ref: ref.raw,
        message: `prompt input has no ${ref.kind}_ref content unit: ${ref.raw}`
      };
      blockers.push(blocker);
      resolvedRefs.push({ ...base, blocker });
      continue;
    }
    const upstreamRef = entityDir8(upstream.path);
    if (upstreamRef === contentUnitRef) {
      const blocker = {
        code: "prompt_dependency_cycle",
        ref: ref.raw,
        content_unit_ref: upstreamRef,
        content_unit_id: upstream.id,
        message: `prompt input forms a dependency cycle: ${ref.raw}`
      };
      blockers.push(blocker);
      resolvedRefs.push({
        ...base,
        upstream_content_unit_ref: upstreamRef,
        upstream_content_unit_id: upstream.id,
        blocker
      });
      continue;
    }
    const decision = await decisionFor(input.decisionProvider, decisionCache, upstream.id, upstreamRef);
    const decisionBlocker = blockerForDecision(ref, upstream, upstreamRef, decision);
    if (decisionBlocker) {
      blockers.push(decisionBlocker);
      resolvedRefs.push({
        ...base,
        upstream_content_unit_ref: upstreamRef,
        upstream_content_unit_id: upstream.id,
        blocker: decisionBlocker
      });
      continue;
    }
    const resourceId = promptInputResourceId(decision);
    if (resourceId === void 0) {
      const blocker = {
        code: "upstream_resource_missing",
        ref: ref.raw,
        content_unit_ref: upstreamRef,
        content_unit_id: upstream.id,
        message: `prompt input selected content unit has no resource_id: ${ref.raw}`
      };
      blockers.push(blocker);
      resolvedRefs.push({
        ...base,
        upstream_content_unit_ref: upstreamRef,
        upstream_content_unit_id: upstream.id,
        blocker
      });
      continue;
    }
    resolvedRefs.push({
      ...base,
      upstream_content_unit_ref: upstreamRef,
      upstream_content_unit_id: upstream.id,
      resource_id: resourceId,
      replacement: resourceToken(resourceId, ref)
    });
  }
  const replacements = resolvedRefs.flatMap((ref) => {
    if (ref.resource_id === void 0 || !ref.replacement) return [];
    return [{
      ref: ref.raw,
      field: ref.source.field,
      resource_id: ref.resource_id,
      token: ref.replacement
    }];
  });
  const structured = normalizedPromptStructured(editPrompt?.structured, contentUnitType2, outputKind);
  const structuredText = structuredPromptText(structured, contentUnitType2, outputKind);
  const referenceAssets = referenceAssetsForResolvedRefs(resolvedRefs);
  const compiledText = compiledPromptTextWithStructured({
    text: compilePromptText(stringField17(editPrompt?.text), resolvedRefs, "edit_prompt.text"),
    structuredText: compilePromptText(structuredText, resolvedRefs, "edit_prompt.structured")
  });
  const prompt = pruneUndefined16({
    schema: "movscript.backend_prompt.v1",
    content_unit_ref: contentUnitRef,
    content_unit_id: input.contentUnit.id,
    content_unit_type: contentUnitType2,
    output_kind: outputKind,
    text: compiledText,
    negative_text: compilePromptText(stringField17(editPrompt?.negative_text), resolvedRefs, "edit_prompt.negative_text"),
    notes: compilePromptText(stringField17(editPrompt?.notes), resolvedRefs, "edit_prompt.notes"),
    structured,
    structured_text: structuredText,
    style_reference_resource_ids: styleReferenceResourceIds.length > 0 ? styleReferenceResourceIds : void 0,
    resource_ids: uniqueIds2([
      ...replacements.map((replacement) => replacement.resource_id),
      ...styleReferenceResourceIds
    ]),
    reference_assets: referenceAssets.length > 0 ? referenceAssets : void 0,
    replacements,
    refs: resolvedRefs,
    blockers: blockers.length > 0 ? dedupeBlockers2(blockers) : void 0
  });
  if (blockers.length > 0) {
    return { ok: false, prompt, blockers: dedupeBlockers2(blockers) };
  }
  return { ok: true, prompt };
}
function contentUnitEditPrompt(value, promptText) {
  const base = recordField5(value);
  if (promptText === void 0) return base;
  return {
    ...base ?? {},
    text: promptText
  };
}
function parseContentUnitEditPromptRefs3(editPrompt) {
  const prompt = recordField5(editPrompt);
  const structuredText = structuredPromptTextFromUnknown2(prompt?.structured);
  return [
    ...parsePromptRefsFromText3(stringField17(prompt?.text), "edit_prompt.text"),
    ...parsePromptRefsFromText3(stringField17(prompt?.negative_text), "edit_prompt.negative_text"),
    ...parsePromptRefsFromText3(stringField17(prompt?.notes), "edit_prompt.notes"),
    ...parsePromptRefsFromText3(structuredText, "edit_prompt.structured")
  ];
}
function parseUnsupportedContentUnitEditPromptRefs2(editPrompt) {
  const prompt = recordField5(editPrompt);
  const structuredText = structuredPromptTextFromUnknown2(prompt?.structured);
  return [
    ...parseUnsupportedPromptRefsFromText2(stringField17(prompt?.text), "edit_prompt.text"),
    ...parseUnsupportedPromptRefsFromText2(stringField17(prompt?.negative_text), "edit_prompt.negative_text"),
    ...parseUnsupportedPromptRefsFromText2(stringField17(prompt?.notes), "edit_prompt.notes"),
    ...parseUnsupportedPromptRefsFromText2(structuredText, "edit_prompt.structured")
  ];
}
function parsePromptRefsFromText3(text, field) {
  if (!text) return [];
  const refs = [];
  for (const match of text.matchAll(PROMPT_REF_PATTERN3)) {
    const kind = promptRefKind3(match[1]);
    const payload = parsePromptRefPayload(match[2]);
    if (!kind || !payload.id) continue;
    refs.push({
      kind,
      id: payload.id,
      raw: match[0],
      ...payload.role ? { role: payload.role } : {},
      ...payload.mediaType ? { media_type: payload.mediaType } : {},
      source: {
        field,
        start: match.index,
        end: match.index === void 0 ? void 0 : match.index + match[0].length
      }
    });
  }
  return refs;
}
function parseUnsupportedPromptRefsFromText2(text, field) {
  if (!text) return [];
  const refs = [];
  for (const match of text.matchAll(PROMPT_REF_PATTERN3)) {
    const kindValue = match[1]?.trim();
    const id = parsePromptRefPayload(match[2]).id;
    if (!kindValue || !id || promptRefKind3(kindValue)) continue;
    refs.push({
      kind: kindValue,
      id,
      raw: match[0],
      source: {
        field,
        start: match.index,
        end: match.index === void 0 ? void 0 : match.index + match[0].length
      }
    });
  }
  return refs;
}
function parsePromptRefPayload(value) {
  const parts = String(value ?? "").trim().split(/\s+/).filter(Boolean);
  const id = parts.shift() ?? "";
  let role = "";
  let mediaType = "";
  for (const part of parts) {
    const match = part.match(/^([a-zA-Z_][a-zA-Z0-9_-]*)=(.+)$/);
    if (!match) continue;
    const key = normalizePromptRefMetadataPart(match[1]);
    const metadataValue = normalizePromptRefMetadataPart(match[2]);
    if (!metadataValue) continue;
    if (key === "role") role = metadataValue;
    if (key === "media" || key === "media_type" || key === "mediatype") mediaType = metadataValue;
  }
  return {
    id,
    ...role ? { role } : {},
    ...mediaType ? { mediaType } : {}
  };
}
function normalizePromptRefMetadataPart(value) {
  return String(value ?? "").trim().toLowerCase().replace(/^['"]|['"]$/g, "").replace(/[^a-z0-9_-]+/g, "_").replace(/^_+|_+$/g, "");
}
function normalizedPromptStructured(value, contentUnitType2, outputKind) {
  const record = recordField5(value);
  if (!record) return void 0;
  const shotPlan = shouldCompileSceneMomentShotPlan(contentUnitType2, outputKind) ? normalizedShotPlan(record.shot_plan ?? record.shotPlan ?? record.shots) : [];
  const passthrough = pruneUndefined16({
    ...record,
    ...shotPlan.length > 0 ? { shot_plan: shotPlan } : {}
  });
  return Object.keys(passthrough).length > 0 ? passthrough : void 0;
}
function structuredPromptTextFromUnknown2(value) {
  const record = recordField5(value);
  if (!record) return void 0;
  const shotPlan = normalizedShotPlan(record.shot_plan ?? record.shotPlan ?? record.shots);
  if (shotPlan.length > 0) return renderShotPlanText(shotPlan);
  const json = JSON.stringify(record);
  return json === "{}" ? void 0 : json;
}
function structuredPromptText(structured, contentUnitType2, outputKind) {
  if (!structured || !shouldCompileSceneMomentShotPlan(contentUnitType2, outputKind)) return void 0;
  const shotPlan = Array.isArray(structured.shot_plan) ? normalizedShotPlan(structured.shot_plan) : [];
  return shotPlan.length > 0 ? renderShotPlanText(shotPlan) : void 0;
}
function compiledPromptTextWithStructured(input) {
  const parts = [input.text, input.structuredText].filter((part) => Boolean(part && part.trim()));
  return parts.length > 0 ? parts.join("\n\n") : void 0;
}
function shouldCompileSceneMomentShotPlan(contentUnitType2, outputKind) {
  return outputKind === "video" && (contentUnitType2 === "scene_moment_ref" || contentUnitType2 === "scence_moment_ref");
}
function normalizedShotPlan(value) {
  if (!Array.isArray(value)) return [];
  return value.flatMap((item, index) => {
    const record = recordField5(item);
    if (!record) return [];
    const normalized = pruneUndefined16({
      order: positiveNumber3(record.order ?? record.index ?? record.shot_number ?? record.shotNumber) ?? index + 1,
      title: stringField17(record.title ?? record.name),
      duration_sec: positiveNumber3(record.duration_sec ?? record.durationSec ?? record.duration),
      action: stringField17(record.action ?? record.description ?? record.intent),
      result: stringField17(record.result ?? record.end_state ?? record.endState),
      dialogue: stringField17(record.dialogue),
      narration: stringField17(record.narration),
      shot_size: stringField17(record.shot_size ?? record.shotSize ?? record.shot_type ?? record.shotType),
      camera_angle: stringField17(record.camera_angle ?? record.cameraAngle ?? record.angle),
      camera_motion: stringField17(record.camera_motion ?? record.cameraMotion ?? record.movement),
      lighting: stringField17(record.lighting ?? record.lighting_style ?? record.lightingStyle),
      depth_of_field: stringField17(record.depth_of_field ?? record.depthOfField ?? record.dof),
      composition: stringField17(record.composition),
      transition: stringField17(record.transition),
      notes: stringField17(record.notes ?? record.note)
    });
    const hasCreativeContent = Boolean(
      normalized.title || normalized.action || normalized.result || normalized.dialogue || normalized.narration || normalized.shot_size || normalized.camera_angle || normalized.camera_motion || normalized.lighting || normalized.depth_of_field || normalized.composition || normalized.transition || normalized.notes
    );
    return hasCreativeContent ? [normalized] : [];
  });
}
function renderShotPlanText(shotPlan) {
  const lines = [
    "LOCKED SCENE MOMENT SHOT PLAN:",
    "Generate this scene moment as one continuous video while preserving the following shot order and cinematic parameters. Do not drop, merge, reorder, or contradict non-empty shot fields."
  ];
  for (const shot of shotPlan) {
    const label = `Shot ${shot.order ?? "?"}${shot.title ? ` - ${shot.title}` : ""}`;
    const fields = [
      label,
      shot.duration_sec !== void 0 ? `duration=${shot.duration_sec}s` : void 0,
      shot.action ? `action=${shot.action}` : void 0,
      shot.result ? `result=${shot.result}` : void 0,
      shot.dialogue ? `dialogue=${shot.dialogue}` : void 0,
      shot.narration ? `narration=${shot.narration}` : void 0,
      shot.shot_size ? `shot_size=${shot.shot_size}` : void 0,
      shot.camera_angle ? `camera_angle=${shot.camera_angle}` : void 0,
      shot.camera_motion ? `camera_motion=${shot.camera_motion}` : void 0,
      shot.lighting ? `lighting=${shot.lighting}` : void 0,
      shot.depth_of_field ? `depth_of_field=${shot.depth_of_field}` : void 0,
      shot.composition ? `composition=${shot.composition}` : void 0,
      shot.transition ? `transition=${shot.transition}` : void 0,
      shot.notes ? `notes=${shot.notes}` : void 0
    ].filter((field) => Boolean(field));
    lines.push(`- ${fields.join("; ")}`);
  }
  return lines.join("\n");
}
function failedPrompt(input) {
  return {
    ok: false,
    blockers: input.blockers,
    prompt: {
      schema: "movscript.backend_prompt.v1",
      content_unit_ref: input.contentUnitRef,
      ...input.contentUnitId !== void 0 ? { content_unit_id: input.contentUnitId } : {},
      content_unit_type: input.contentUnitType,
      output_kind: input.outputKind,
      resource_ids: [],
      replacements: [],
      refs: [],
      blockers: input.blockers
    }
  };
}
function unsupportedPromptRefBlocker(ref) {
  return {
    code: "unsupported_prompt_ref_kind",
    ref: ref.raw,
    message: `unsupported prompt ref kind "${ref.kind}": ${ref.raw}. Use system primitive, content_unit, candidate, or resource refs; namespace vocabulary is context, not a selected-resource dependency.`
  };
}
async function decisionFor(provider, cache, contentUnitId, contentUnitRef) {
  const key = String(contentUnitId);
  const cached = cache.get(key);
  if (cached) return cached;
  const next = provider.getContentUnitDecision({ contentUnitId, contentUnitRef });
  cache.set(key, next);
  return next;
}
function blockerForDecision(ref, upstream, upstreamRef, decision) {
  if (!decision) {
    return {
      code: "decision_context_missing",
      ref: ref.raw,
      content_unit_ref: upstreamRef,
      content_unit_id: upstream.id,
      message: `prompt input content unit has no backend decision context: ${ref.raw}`
    };
  }
  const selection = selectedDecision(decision);
  if (!selection) {
    const candidates = Array.isArray(decision.candidates) ? decision.candidates : [];
    if (latestResourceCandidate(candidates)) return void 0;
    return candidates.length > 0 ? {
      code: "upstream_resource_missing",
      ref: ref.raw,
      content_unit_ref: upstreamRef,
      content_unit_id: upstream.id,
      message: `prompt input latest backend candidate has no resource_id: ${ref.raw}`
    } : {
      code: "upstream_selection_missing",
      ref: ref.raw,
      content_unit_ref: upstreamRef,
      content_unit_id: upstream.id,
      message: `prompt input content unit has no selected backend candidate: ${ref.raw}`
    };
  }
  if (selection.stale === true && stalePolicy(selection) !== "accept_stale") {
    return {
      code: "upstream_selection_stale",
      ref: ref.raw,
      content_unit_ref: upstreamRef,
      content_unit_id: upstream.id,
      message: `prompt input selected backend candidate is stale: ${ref.raw}`
    };
  }
  const candidateId = idField12(selection.candidate_id);
  if (candidateId !== void 0 && Array.isArray(decision.candidates)) {
    const candidate = decision.candidates.find((item) => sameId5(item.id, candidateId));
    if (!candidate) {
      return {
        code: "upstream_candidate_missing",
        ref: ref.raw,
        content_unit_ref: upstreamRef,
        content_unit_id: upstream.id,
        message: `prompt input selected backend candidate is missing: ${ref.raw}`
      };
    }
  }
  if (promptInputResourceId(decision) === void 0) {
    return {
      code: "upstream_resource_missing",
      ref: ref.raw,
      content_unit_ref: upstreamRef,
      content_unit_id: upstream.id,
      message: `prompt input selected backend candidate has no resource_id: ${ref.raw}`
    };
  }
  return void 0;
}
async function resolveDirectPromptResourceRef(input) {
  const { ref, role } = input;
  if (ref.kind === "resource") {
    const resourceId2 = resourceIdField5(ref.id);
    if (resourceId2 === void 0) {
      const blocker = {
        code: "ref_not_found",
        ref: ref.raw,
        resource_ref: ref.id,
        message: `prompt resource ref does not resolve: ${ref.raw}`
      };
      return { ...ref, role, blocker };
    }
    return {
      ...ref,
      role,
      resolved: {
        entityKind: "resource",
        id: resourceId2
      },
      resource_id: resourceId2,
      replacement: resourceToken(resourceId2, ref)
    };
  }
  if (ref.kind !== "candidate") return void 0;
  if (input.contentUnit.id === void 0) {
    const blocker = {
      code: "content_unit_id_missing",
      ref: ref.raw,
      content_unit_ref: input.contentUnitRef,
      message: `content unit is missing id for candidate ref: ${ref.raw}`
    };
    return { ...ref, role, blocker };
  }
  const decision = await decisionFor(
    input.decisionProvider,
    input.decisionCache,
    input.contentUnit.id,
    input.contentUnitRef
  );
  if (!decision) {
    const blocker = {
      code: "decision_context_missing",
      ref: ref.raw,
      content_unit_ref: input.contentUnitRef,
      content_unit_id: input.contentUnit.id,
      message: `prompt candidate ref has no backend decision context: ${ref.raw}`
    };
    return { ...ref, role, blocker };
  }
  const candidate = Array.isArray(decision.candidates) ? decision.candidates.find((item) => sameId5(item.id, ref.id)) : void 0;
  if (!candidate) {
    const blocker = {
      code: "upstream_candidate_missing",
      ref: ref.raw,
      content_unit_ref: input.contentUnitRef,
      content_unit_id: input.contentUnit.id,
      message: `prompt candidate ref is missing: ${ref.raw}`
    };
    return { ...ref, role, blocker };
  }
  const resourceId = firstCandidateResourceId2(candidate);
  if (resourceId === void 0) {
    const blocker = {
      code: "upstream_resource_missing",
      ref: ref.raw,
      content_unit_ref: input.contentUnitRef,
      content_unit_id: input.contentUnit.id,
      message: `prompt candidate ref has no resource_id: ${ref.raw}`
    };
    return { ...ref, role, blocker };
  }
  return {
    ...ref,
    role,
    resolved: {
      entityKind: "candidate",
      id: ref.id
    },
    upstream_content_unit_ref: input.contentUnitRef,
    upstream_content_unit_id: input.contentUnit.id,
    resource_id: resourceId,
    replacement: resourceToken(resourceId, ref)
  };
}
function selectedDecision(decision) {
  const selection = recordField5(decision.selection);
  return selection && Object.keys(selection).length > 0 ? selection : void 0;
}
function promptInputResourceId(decision) {
  if (decision && selectedDecision(decision)) return selectedResourceId(decision);
  return firstCandidateResourceId2(latestResourceCandidate(decision?.candidates));
}
function selectedResourceId(decision) {
  const selection = decision ? selectedDecision(decision) : void 0;
  const direct = resourceIdField5(selection?.resource_id);
  if (direct !== void 0) return direct;
  const candidateId = idField12(selection?.candidate_id);
  if (candidateId === void 0 || !Array.isArray(decision?.candidates)) return void 0;
  const candidate = decision.candidates.find((item) => sameId5(item.id, candidateId));
  return firstCandidateResourceId2(candidate);
}
function firstCandidateResourceId2(candidate) {
  const output = arrayField7(candidate?.outputs).filter(isRecord21)[0];
  return resourceIdField5(output?.resource_id);
}
function latestResourceCandidate(candidates) {
  const resourceCandidates = Array.isArray(candidates) ? candidates.filter((candidate) => firstCandidateResourceId2(candidate) !== void 0) : [];
  if (resourceCandidates.length === 0) return void 0;
  const withTimestamp = resourceCandidates.map((candidate, index) => ({
    candidate,
    index,
    timestamp: candidateTimestamp(candidate)
  }));
  return withTimestamp.sort((left, right) => {
    if (left.timestamp !== right.timestamp) return right.timestamp - left.timestamp;
    return right.index - left.index;
  })[0]?.candidate;
}
function candidateTimestamp(candidate) {
  const raw = stringField17(candidate.updated_at ?? candidate.updatedAt ?? candidate.created_at ?? candidate.createdAt);
  if (!raw) return 0;
  const parsed = Date.parse(raw);
  return Number.isFinite(parsed) ? parsed : 0;
}
function compilePromptText(text, refs, field) {
  if (!text) return void 0;
  const replacements = refs.filter((ref) => ref.source.field === field && ref.replacement).sort((left, right) => (right.source.start ?? 0) - (left.source.start ?? 0));
  let output = text;
  for (const ref of replacements) {
    if (ref.source.start === void 0 || ref.source.end === void 0 || !ref.replacement) {
      output = output.split(ref.raw).join(ref.replacement);
      continue;
    }
    output = `${output.slice(0, ref.source.start)}${ref.replacement}${output.slice(ref.source.end)}`;
  }
  return output;
}
function resolvePromptRefEntity2(index, ref) {
  if (ref.kind === "candidate" || ref.kind === "resource") return void 0;
  if (ref.kind === "content_unit") {
    return queryMovScriptWorkspaceEntities(index, { entityKind: "content_unit" }).find((entity) => entity.id !== void 0 && sameEntityRef(entity.id, ref.id, "content_unit") || sameEntityRef(entityDir8(entity.path).split("/").pop(), ref.id, "content_unit"));
  }
  return findEntityByRef3(index, ref.kind, ref.id);
}
function resolveContentUnitForPromptRef2(index, ref) {
  if (ref.kind === "candidate" || ref.kind === "resource") return void 0;
  if (ref.kind === "content_unit") return resolvePromptRefEntity2(index, ref);
  const kind = ref.kind;
  const expectedTypes = contentUnitTypesForPromptRefKind3(kind);
  return queryMovScriptWorkspaceEntities(index, { entityKind: "content_unit" }).find((entity) => {
    if (!expectedTypes.includes(String(entity.record.content_unit_type ?? ""))) return false;
    return primaryContentUnitRefs2(entity, kind).some((candidate) => samePromptRefId2(candidate.id, ref.id, kind));
  });
}
function primaryContentUnitRefs2(contentUnit, kind) {
  return flatPrimaryRefIds(contentUnit.record, kind).map((id) => ({ kind, id }));
}
function flatPrimaryRefIds(record, kind) {
  return primaryRefIdsForContentUnitRecord(record, kind);
}
function hasAmbiguousPrimaryRefs2(refs, kind) {
  const unique = [];
  for (const ref of refs) {
    if (unique.some((item) => samePromptRefId2(item.id, ref.id, kind))) continue;
    unique.push(ref);
  }
  return unique.length > 1;
}
function samePromptRefId2(left, right, kind) {
  return String(left) === String(right) || lastPathSegment7(left) === String(right) || lastPathSegment7(right) === String(left) || sameEntityRef(left, right, kind);
}
function lastPathSegment7(value) {
  if (typeof value !== "string" || !value.includes("/")) return void 0;
  return value.split("/").filter(Boolean).at(-1);
}
function findEntityByRef3(index, entityKind, ref) {
  const value = idField12(ref);
  if (value === void 0) return void 0;
  const normalized = typeof value === "string" ? value.replace(/\/+$/, "") : String(value);
  return queryMovScriptWorkspaceEntities(index, { entityKind }).find((entity) => {
    const dir = entityDir8(entity.path);
    return dir === normalized || entity.path === `${normalized}/${entityKind}.json` || sameEntityRef(entity.id, value, entityKind);
  });
}
function promptRefKind3(value) {
  if (value === "candidate" || value === "resource") return value;
  return isContentUnitPromptRefKind(value) ? value : void 0;
}
function primaryRefKindForContentUnitType3(contentUnitType2) {
  return primaryRefKindForContentUnitType(contentUnitType2);
}
function contentUnitTypesForPromptRefKind3(kind) {
  return contentUnitTypesForPromptRefKind(kind);
}
function contentUnitOutputKind3(value) {
  return outputKindForContentUnitType("", value);
}
function usesVisualStyleReferences2(outputKind) {
  return outputKind === "image" || outputKind === "video";
}
function projectStyleReferenceResourceIds2(index) {
  const standards = queryMovScriptWorkspaceEntities(index, { entityKind: "project_standards", limit: 1 })[0];
  if (!standards) return [];
  return uniqueIds2([
    ...resourceIdsFromValue3(standards.record.style_reference_images),
    ...resourceIdsFromValue3(standards.record.style_references),
    ...resourceIdsFromValue3(standards.record.reference_resource_ids),
    ...arrayField7(standards.record.custom_rules).filter(isRecord21).filter((rule) => rule.enabled !== false).filter((rule) => stringField17(rule.key) === "style_reference_images").flatMap((rule) => resourceIdsFromValue3(rule.value))
  ]);
}
function resourceIdsFromValue3(value) {
  if (Array.isArray(value)) return value.flatMap(resourceIdsFromValue3);
  const text = stringField17(value);
  if (text) {
    const ids = [];
    const patterns = [
      /resource#([0-9]+)/gi,
      /reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi,
      /style_reference_resource_ids\s*[:=]\s*\[?([0-9,\s]+)\]?/gi
    ];
    for (const pattern of patterns) {
      for (const match of text.matchAll(pattern)) {
        for (const part of String(match[1] ?? "").split(",")) {
          const parsed2 = Number(part.trim());
          if (Number.isInteger(parsed2) && parsed2 > 0) ids.push(parsed2);
        }
      }
    }
    if (ids.length > 0) return ids;
    const parsed = Number(text);
    return Number.isInteger(parsed) && parsed > 0 ? [parsed] : [];
  }
  const id = resourceIdField5(value);
  if (id !== void 0) return [id];
  if (isRecord21(value)) return resourceIdsFromValue3(value.resource_id ?? value.resourceId ?? value.id);
  return [];
}
function referenceAssetsForResolvedRefs(refs) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const ref of refs) {
    if (ref.resource_id === void 0) continue;
    const mediaType = promptRefMediaTypeForReference(ref);
    const role = promptRefRoleForReference(ref, mediaType);
    const key = `${String(ref.resource_id)}:${mediaType}:${role}`;
    if (seen.has(key)) continue;
    seen.add(key);
    output.push({
      resource_id: ref.resource_id,
      media_type: mediaType,
      role,
      source_ref: ref.raw
    });
  }
  return output;
}
function resourceToken(resourceId, ref) {
  const mediaType = ref?.media_type;
  const role = ref?.role && ref.role !== "input" ? ref.role : void 0;
  return formatResourceMention(resourceId, {
    ...mediaType ? { mediaType } : {},
    ...role ? { role } : {}
  });
}
function promptRefRoleForReference(ref, mediaType) {
  if (ref.role && ref.role !== "input") return ref.role;
  if (mediaType === "video") return "reference_video";
  if (mediaType === "audio") return "reference_audio";
  return "reference_image";
}
function promptRefMediaTypeForReference(ref) {
  if (ref.media_type) return ref.media_type;
  if (ref.role === "reference_video" || ref.role === "motion_reference" || ref.role === "source_video") return "video";
  if (ref.role === "reference_audio" || ref.role === "source_audio") return "audio";
  return "image";
}
function entityDir8(path) {
  return path.replace(/\/[^/]+$/, "");
}
function stalePolicy(selection) {
  return stringField17(selection.stale_policy) ?? "strict";
}
function sameId5(left, right) {
  const leftId = idField12(left);
  const rightId = idField12(right);
  return leftId !== void 0 && rightId !== void 0 && String(leftId) === String(rightId);
}
function uniqueIds2(values) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const value of values) {
    const key = String(value);
    if (seen.has(key)) continue;
    seen.add(key);
    output.push(value);
  }
  return output;
}
function dedupeBlockers2(blockers) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const blocker of blockers) {
    const key = `${blocker.code}:${blocker.ref ?? ""}:${blocker.content_unit_ref ?? ""}:${blocker.message}`;
    if (seen.has(key)) continue;
    seen.add(key);
    output.push(blocker);
  }
  return output;
}
function recordField5(value) {
  return isRecord21(value) ? value : void 0;
}
function arrayField7(value) {
  return Array.isArray(value) ? value : [];
}
function stringField17(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function positiveNumber3(value) {
  if (typeof value === "number" && Number.isFinite(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value.trim());
    if (Number.isFinite(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function idField12(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function resourceIdField5(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    if (Number.isInteger(parsed) && parsed > 0) return parsed;
  }
  return void 0;
}
function isRecord21(value) {
  return Boolean(value) && typeof value === "object" && !Array.isArray(value);
}
function pruneUndefined16(value) {
  const output = {};
  for (const [key, item] of Object.entries(value)) {
    if (item !== void 0) output[key] = item;
  }
  return output;
}

// ../../packages/engine/dist/node.js
var NodeMovScriptEngineRegistry = class {
  constructor(factory = createNodeMovScriptEngine) {
    this.factory = factory;
  }
  factory;
  engines = /* @__PURE__ */ new Map();
  get(input = {}) {
    const key = nodeMovScriptEngineRegistryKey(input);
    const existing = this.engines.get(key);
    if (existing) return existing;
    const engine = this.factory(input);
    this.engines.set(key, engine);
    return engine;
  }
  invalidate(input = {}) {
    this.engines.delete(typeof input === "string" ? input : nodeMovScriptEngineRegistryKey(input));
  }
  clear() {
    this.engines.clear();
  }
};
function nodeMovScriptEngineRegistryKey(input = {}) {
  return input.cacheKey ?? [
    input.projectDir ?? ""
  ].map((part) => String(part)).join("");
}
function createNodeMovScriptEngine(input = {}) {
  const workspaceService = createNodeMovScriptWorkspaceService(input);
  const fileRepository = createNodeMovScriptWorkspaceFileRepository(workspaceService.projectDir);
  const engine = createMovScriptEngine({
    workspaceService,
    overviewWorkspace: () => overviewMovScriptWorkspace({
      fileRepository,
      decisionStore: input.decisionStore,
      ...input.now ? { now: input.now() } : {}
    }),
    inspectWorkspace: (inspectInput2 = {}) => inspectMovScriptWorkspace({
      fileRepository,
      decisionStore: input.decisionStore,
      ...inspectInput2,
      ...input.now ? { now: input.now() } : {}
    }),
    reviewWorkspace: (inspectInput2 = {}) => reviewMovScriptWorkspace({
      fileRepository,
      decisionStore: input.decisionStore,
      ...inspectInput2,
      ...input.now ? { now: input.now() } : {}
    }),
    productionWorkPlan: async () => {
      const review = await reviewMovScriptWorkspace({
        fileRepository,
        decisionStore: input.decisionStore,
        ...input.now ? { now: input.now() } : {}
      });
      return review.productionWorkPlan;
    },
    interpretWorkspace: () => interpretMovScriptWorkspace({
      fileRepository,
      decisionStore: input.decisionStore,
      ...input.now ? { now: input.now() } : {}
    }),
    regenerationPlan: () => planMovScriptWorkspaceRegeneration({
      fileRepository,
      decisionStore: input.decisionStore,
      ...input.now ? { now: input.now() } : {}
    }),
    async deriveContentUnitArtifact(contentUnitId) {
      const index = await workspaceService.loadIndex();
      const contentUnit = index.byKind.get("content_unit")?.find((entity) => String(entity.id) === String(contentUnitId));
      if (!contentUnit) throw new Error(`content_unit not found: ${String(contentUnitId)}`);
      const now = input.now?.() ?? /* @__PURE__ */ new Date();
      return deriveContentUnitArtifact(index, contentUnit, { createdAt: now.toISOString() });
    },
    async buildContentUnitBackendPrompt(contentUnitId, options) {
      return buildContentUnitBackendPromptById({
        index: await workspaceService.loadIndex(),
        contentUnitId,
        decisionProvider: input.decisionStore ?? {
          async getContentUnitDecision() {
            return void 0;
          }
        },
        ...options?.promptText !== void 0 ? { promptText: options.promptText } : {}
      });
    },
    async deriveArtifacts(artifactInput = {}) {
      const now = input.now?.() ?? /* @__PURE__ */ new Date();
      const createdAt = artifactInput.createdAt ?? now.toISOString();
      const interpretationId = artifactInput.interpretationId ?? `engine_${createdAt.replace(/[-:.TZ]/g, "")}`;
      return deriveMovScriptWorkspaceArtifacts({
        index: await workspaceService.loadIndex(),
        changedEntities: [],
        interpretationId,
        createdAt
      });
    },
    ...input.publish ? { publish: input.publish } : {}
  });
  return {
    ...engine,
    projectDir: workspaceService.projectDir,
    workspaceService
  };
}

// ../../packages/core/dist/mcp/node/index.js
import { resolve as resolve6 } from "path";
import { resolve as resolve23 } from "path";
import { spawn as spawn3 } from "child_process";
import { mkdir as mkdir3, rm as rm2, writeFile as writeFile3 } from "fs/promises";
import { tmpdir as tmpdir2 } from "os";
import { join as join22 } from "path";
import { existsSync as existsSync3 } from "fs";
import { join as join8, resolve as resolve32 } from "path";
import { spawn as spawn22 } from "child_process";
import { randomUUID as randomUUID2 } from "crypto";
import { mkdir as mkdir22, readFile as readFile3, rm as rm22, stat as stat3, writeFile as writeFile22 } from "fs/promises";
import { tmpdir as tmpdir22 } from "os";
import { basename as basename2, dirname as dirname6, extname as extname2, isAbsolute, join as join32, relative as relative2, resolve as resolve42, sep } from "path";
import { readFile as readFile22, stat as stat22 } from "fs/promises";
import { basename as basename22 } from "path";
import { resolve as resolve52 } from "path";
var contextSnapshot = {
  route: { pathname: "/", search: "", hash: "" },
  project: null,
  user: null,
  selection: null,
  updatedAt: (/* @__PURE__ */ new Date(0)).toISOString()
};
var contextAuthToken = "";
function getMCPContextSnapshot() {
  return { ...contextSnapshot };
}
function getMCPFocusSnapshot() {
  return {
    ...contextSnapshot,
    route: sanitizeFocusRoute(contextSnapshot.route)
  };
}
function getMCPAuthToken() {
  return contextAuthToken;
}
function sanitizeFocusRoute(route) {
  return {
    ...route,
    search: sanitizeFocusSearch(route.search)
  };
}
function sanitizeFocusSearch(search) {
  if (!search || !search.includes("workspaceId")) return search;
  const params = new URLSearchParams(search.startsWith("?") ? search.slice(1) : search);
  if (!params.has("workspaceId")) return search;
  params.delete("workspaceId");
  const next = params.toString();
  return next ? `?${next}` : "";
}
function createMovScriptDomainRuntime(input) {
  const context = normalizeRuntimeInput(input);
  const cacheKey = runtimeKey(context);
  const projectCwd = projectCwdFromInput(context);
  const decisionStoreConfig = createMCPDecisionStoreConfigResolver(context);
  const decisionStore = createMCPDecisionStore(decisionStoreConfig);
  const engine = movScriptDomainEngineRegistry.get({
    cacheKey,
    projectDir: projectCwd,
    decisionStore
  });
  return createMovScriptDomainRuntimeFromEngine(engine, projectCwd, decisionStore, decisionStoreConfig);
}
function invalidateMovScriptDomainRuntime(input) {
  movScriptDomainEngineRegistry.invalidate(runtimeKey(normalizeRuntimeInput(input)));
}
var movScriptDomainEngineRegistry = new NodeMovScriptEngineRegistry();
function createMovScriptDomainRuntimeFromEngine(engine, projectCwd, decisionStore, decisionStoreConfig) {
  const projectService = createProjectServiceAccessor(projectCwd, decisionStoreConfig);
  return {
    ...engine.workspaceService,
    ...engine,
    projectCwd,
    projectDir: projectCwd,
    decisionStore,
    upsertProjectStandards: (input) => projectService.upsertProjectStandards(input),
    upsertSetting: (input) => projectService.sourceOperation(PROJECT_SERVICE_SETTING_UPSERT_ENDPOINT, input),
    createSetting: (input) => projectService.sourceOperation(PROJECT_SERVICE_SETTING_CREATE_ENDPOINT, input),
    createSettingState: (input) => projectService.sourceOperation(PROJECT_SERVICE_SETTING_STATE_CREATE_ENDPOINT, input),
    upsertAsset: (input) => projectService.sourceOperation(PROJECT_SERVICE_ASSET_UPSERT_ENDPOINT, input),
    createAsset: (input) => projectService.sourceOperation(PROJECT_SERVICE_ASSET_CREATE_ENDPOINT, input),
    upsertScript: (input) => projectService.upsertScript(input),
    snapshotScriptVersionFromMarkdown: (input) => projectService.snapshotScriptVersionFromMarkdown(input),
    upsertContentUnit: (input) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_UPSERT_ENDPOINT, input),
    createContentUnit: (input) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_CREATE_ENDPOINT, input),
    ensureContentUnitForEntity: (input) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_ENSURE_ENDPOINT, input),
    writeHierarchyNode: (input) => projectService.sourceOperation(isNamespaceHierarchyWrite(input) ? PROJECT_SERVICE_NAMESPACE_WRITE_ENDPOINT : PROJECT_SERVICE_HIERARCHY_WRITE_ENDPOINT, input),
    createProduction: (input) => projectService.sourceOperation(PROJECT_SERVICE_PRODUCTION_CREATE_ENDPOINT, input),
    createSegment: (input) => projectService.sourceOperation(PROJECT_SERVICE_SEGMENT_CREATE_ENDPOINT, input),
    createSceneMoment: (input) => projectService.sourceOperation(PROJECT_SERVICE_SCENE_MOMENT_CREATE_ENDPOINT, input),
    createStoryboard: (input) => projectService.sourceOperation(PROJECT_SERVICE_STORYBOARD_CREATE_ENDPOINT, input),
    createKeyframe: (input) => projectService.sourceOperation(PROJECT_SERVICE_KEYFRAME_CREATE_ENDPOINT, input),
    createAudioCue: (input) => projectService.sourceOperation(PROJECT_SERVICE_AUDIO_CUE_CREATE_ENDPOINT, input),
    createExpressionUnit: (input) => projectService.sourceOperation(PROJECT_SERVICE_EXPRESSION_UNIT_CREATE_ENDPOINT, input),
    updateContentUnitEditPrompt: (input) => projectService.sourceOperation(PROJECT_SERVICE_CONTENT_UNIT_EDIT_PROMPT_UPDATE_ENDPOINT, input),
    updateEntityTransition: (input) => projectService.sourceOperation(PROJECT_SERVICE_ENTITY_TRANSITION_UPDATE_ENDPOINT, input),
    updateStoryboardTimeline: (input) => projectService.sourceOperation(PROJECT_SERVICE_STORYBOARD_TIMELINE_UPDATE_ENDPOINT, input),
    deleteEntity: (input) => projectService.sourceOperation(PROJECT_SERVICE_ENTITY_DELETE_ENDPOINT, input).then(() => void 0),
    createContentCandidate: (input) => projectService.candidateAction(PROJECT_SERVICE_CONTENT_CANDIDATE_CREATE_ENDPOINT, input),
    selectContentUnitCandidate: (input) => projectService.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_SELECT_ENDPOINT, input),
    decideContentUnitCandidate: (input) => projectService.candidateAction(PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_DECIDE_ENDPOINT, input),
    readContentUnitGenerationPrompt: async (contentUnitId) => (await projectService.promptContext(contentUnitId)).generationPrompt,
    buildContentUnitBackendPrompt: async (contentUnitId) => (await projectService.promptContext(contentUnitId)).backendPrompt,
    reviewWorkspace: (inspectInput2 = {}) => projectService.inspectWorkspace(inspectInput2),
    inspectWorkspace: (inspectInput2 = {}) => projectService.inspectWorkspace(inspectInput2),
    interpretWorkspace: () => projectService.interpretWorkspace(),
    overviewWorkspace: () => projectService.overviewWorkspace(),
    productionWorkPlan: () => engine.productionWorkPlan(),
    regenerationPlan: () => projectService.regenerationPlan(),
    loadContentWorkspaceSnapshot: () => loadContentSourceWorkspaceSnapshotFromEngine(engine),
    loadContentWorkspace: async () => buildContentSourceWorkspaceData(await loadContentSourceWorkspaceSnapshotFromEngine(engine))
  };
}
function isNamespaceHierarchyWrite(input) {
  const category = input.category ?? input.domainCategory ?? input.domain_category;
  return category === "timeline_namespace" || category === "setting_namespace";
}
function createProjectServiceAccessor(projectDir, decisionStoreConfig) {
  let client;
  const getClient = () => {
    client ??= createProjectServiceClientFromRuntime();
    return client;
  };
  return {
    inspectWorkspace: async (input = {}) => (await getClient().inspectSource({ projectDir, ...input })).inspection,
    overviewWorkspace: async () => (await getClient().overviewSource({ projectDir })).overview,
    interpretWorkspace: async () => (await getClient().interpretSource({ projectDir })).interpretation,
    regenerationPlan: async () => (await getClient().regenerationPlan({ projectDir })).regenerationPlan,
    upsertProjectStandards: async (input = {}) => (await getClient().upsertProjectStandards({
      projectDir,
      input: sourceOperationInput(input)
    })).result,
    upsertScript: async (input = {}) => (await getClient().upsertScript({
      projectDir,
      input: sourceOperationInput(input)
    })).result,
    snapshotScriptVersionFromMarkdown: async (input = {}) => (await getClient().snapshotScriptVersionFromMarkdown({
      projectDir,
      input: sourceOperationInput(input)
    })).result,
    sourceOperation: async (endpoint, input = {}) => (await getClient().sourceOperation(endpoint, {
      projectDir,
      input: sourceOperationInput(input)
    })).result,
    candidateAction: async (endpoint, input = {}) => {
      const resolvedDecisionStoreConfig = await decisionStoreConfig?.();
      if (!resolvedDecisionStoreConfig) {
        throw new Error("content unit candidate action requires a scoped project data decisionStore");
      }
      return (await getClient().candidateAction(endpoint, {
        projectDir,
        input: sourceOperationInput(input),
        decisionStore: resolvedDecisionStoreConfig
      })).result;
    },
    promptContext: async (contentUnitId) => {
      const resolvedDecisionStoreConfig = await decisionStoreConfig?.();
      return getClient().promptContext({
        projectDir,
        contentUnitId,
        ...resolvedDecisionStoreConfig ? { decisionStore: resolvedDecisionStoreConfig } : {}
      });
    }
  };
}
function sourceOperationInput(input) {
  return input && typeof input === "object" && !Array.isArray(input) ? input : {};
}
function normalizeRuntimeInput(input) {
  return {
    ...input.workspaceDir !== void 0 ? { workspaceDir: input.workspaceDir } : {},
    ...input.projectDir !== void 0 ? { projectDir: input.projectDir } : {},
    ...input.userId !== void 0 ? { userId: input.userId } : {},
    ...input.orgId !== void 0 ? { orgId: input.orgId } : {},
    ...input.projectUid !== void 0 ? { projectUid: input.projectUid } : {},
    ...input.projectTitle !== void 0 ? { projectTitle: input.projectTitle } : {}
  };
}
function runtimeKey(input) {
  const projectCwd = projectCwdFromInput(input);
  const session = resolveMovScriptBackendSession({
    workspaceDir: input.workspaceDir,
    userId: input.userId
  });
  const token = getMCPAuthToken() || session.token || "";
  return [
    projectCwd,
    input.userId ?? "",
    input.orgId ?? "",
    input.projectUid ?? "",
    session.baseURL,
    session.userId ?? "",
    token
  ].map((part) => String(part)).join("");
}
function createMCPDecisionStore(resolveConfig) {
  if (!resolveConfig) return void 0;
  let storePromise;
  const getStore = async () => {
    storePromise ??= resolveConfig().then((config) => config ? createMovScriptScopedProjectDataDecisionStore(config) : void 0);
    return storePromise;
  };
  const requireStore = async () => {
    const store = await getStore();
    if (!store) throw new Error("content unit candidate command requires a scoped project data decisionStore");
    return store;
  };
  return {
    async getContentUnitDecision(input) {
      return (await getStore())?.getContentUnitDecision(input);
    },
    async getContentUnitDecisions(input) {
      return (await getStore())?.getContentUnitDecisions?.(input) ?? /* @__PURE__ */ new Map();
    },
    async replaceContentUnitCandidates(input) {
      return (await requireStore()).replaceContentUnitCandidates(input);
    },
    async upsertContentUnitCandidate(input) {
      return (await requireStore()).upsertContentUnitCandidate(input);
    },
    async selectContentUnitCandidate(input) {
      return (await requireStore()).selectContentUnitCandidate(input);
    },
    async clearContentUnitSelection(input) {
      return (await requireStore()).clearContentUnitSelection(input);
    }
  };
}
async function resolveMCPProjectDecisionStoreConfig(input) {
  return createMCPDecisionStoreConfigResolver(input)?.();
}
function createMCPDecisionStoreConfigResolver(input) {
  const session = resolveMovScriptBackendSession({
    workspaceDir: input.workspaceDir,
    userId: input.userId
  });
  const token = getMCPAuthToken() || session.token;
  if (!token) return void 0;
  const projectCwd = projectCwdFromInput(input);
  let configPromise;
  return async () => {
    configPromise ??= (async () => {
      const resolvedLocator = await createProjectServiceClientFromRuntime().resolveLocator({
        projectDir: projectCwd,
        ...input.workspaceDir ? { workspaceDir: input.workspaceDir } : {},
        ...input.projectUid ? { projectUid: input.projectUid } : {}
      });
      const projectUid = resolvedLocator.locator.projectUid ?? input.projectUid;
      const projectTitle = input.projectTitle ?? resolvedLocator.locator.projectTitle;
      if (!projectUid) return void 0;
      const headers = {
        ...session.userId ? { "X-User-ID": session.userId } : {},
        ...input.orgId !== void 0 ? { "X-Org-ID": String(input.orgId) } : {}
      };
      return {
        kind: "scoped-project-data",
        baseUrl: session.baseURL,
        projectUid,
        ...projectTitle ? { title: projectTitle } : {},
        ...input.orgId !== void 0 ? { scopeKind: "org", scopeId: input.orgId } : {},
        token,
        ...Object.keys(headers).length > 0 ? { headers } : {}
      };
    })();
    return configPromise;
  };
}
function projectCwdFromInput(input) {
  if (input.projectDir) return input.projectDir;
  throw new Error("projectDir or cwd is required. The legacy projectId workspace path is no longer supported.");
}
var configuredMCPDefaultWorkspaceDir;
function setMCPDefaultWorkspaceDir(workspaceDir) {
  configuredMCPDefaultWorkspaceDir = workspaceDir?.trim() || void 0;
  setMovScriptBackendDefaultWorkspaceDir(configuredMCPDefaultWorkspaceDir);
}
function resolveMCPDefaultWorkspaceDir() {
  return configuredMCPDefaultWorkspaceDir || process.env.MOVSCRIPT_HOME || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd();
}
function resolveMCPProjectWorkspaceLocator(args) {
  const workspaceDir = stringValue5(args.workspaceDir ?? args.workspace_dir) ?? resolveMCPDefaultWorkspaceDir();
  const projectDir = pathValue(args.projectDir ?? args.project_dir ?? args.projectPath ?? args.project_path ?? args.cwd);
  const projectUid = stringValue5(args.projectUid ?? args.project_uid);
  if (projectDir === void 0) throw new Error("projectDir or cwd is required for MovScript project-scoped MCP tools");
  return {
    workspaceDir,
    projectDir,
    ...projectUid !== void 0 ? { projectUid } : {}
  };
}
function pathValue(value) {
  const raw = stringValue5(value);
  return raw ? resolve6(raw) : void 0;
}
var ensuredProjectBindings = /* @__PURE__ */ new Map();
var ENSURED_PROJECT_BINDING_LIMIT = 500;
async function requireMCPBackendBoundProject(args) {
  const locator = isResolvedProjectWorkspaceLocator(args) ? args : resolveMCPProjectWorkspaceLocator(args);
  const resolvedLocator = await resolveMCPProjectBindingLocator(locator);
  const projectUid = stringField18(resolvedLocator.projectUid);
  if (!projectUid) {
    throw new Error("MovScript project requires project_uid. Open or initialize the project before using project-scoped write tools.");
  }
  const projectTitle = resolvedLocator.projectTitle ?? projectTitleFromDir(resolvedLocator.projectDir);
  const key = [
    resolvedLocator.workspaceDir,
    resolvedLocator.projectDir,
    projectUid,
    projectTitle
  ].join("");
  const cached = ensuredProjectBindings.get(key);
  if (cached) return cached;
  const pending = ensureBackendBinding({
    ...resolvedLocator,
    projectUid,
    ...projectTitle ? { projectTitle } : {},
    ...resolvedLocator.description ? { description: resolvedLocator.description } : {}
  }).catch((err) => {
    ensuredProjectBindings.delete(key);
    throw err;
  });
  rememberEnsuredProjectBinding(key, pending);
  return pending;
}
async function resolveMCPProjectBindingLocator(args) {
  const locator = isResolvedProjectWorkspaceLocator(args) ? args : resolveMCPProjectWorkspaceLocator(args);
  return resolveProjectBindingLocator(locator);
}
function isResolvedProjectWorkspaceLocator(value) {
  return isRecord4(value) && typeof value.projectDir === "string";
}
async function resolveProjectBindingLocator(locator) {
  const response = await createProjectServiceClientFromRuntime().resolveLocator({
    projectDir: locator.projectDir,
    workspaceDir: locator.workspaceDir,
    ...locator.projectUid ? { projectUid: locator.projectUid } : {}
  });
  const projectLocator2 = response.locator;
  return {
    workspaceDir: projectLocator2.workspaceDir ?? locator.workspaceDir,
    projectDir: projectLocator2.projectDir,
    ...projectLocator2.projectUid ?? locator.projectUid ? { projectUid: projectLocator2.projectUid ?? locator.projectUid } : {},
    ...projectLocator2.projectTitle ? { projectTitle: projectLocator2.projectTitle } : {},
    ...projectLocator2.description ? { description: projectLocator2.description } : {}
  };
}
async function ensureBackendBinding(input) {
  const backendProjectResponse = await backendPost2("/projects/ensure", {
    project_uid: input.projectUid,
    name: input.projectTitle ?? projectTitleFromDir(input.projectDir),
    description: input.description ?? input.projectDir
  });
  const projectDataSpace = await backendPost2("/project-data/spaces", {
    project_uid: input.projectUid,
    title: input.projectTitle ?? projectTitleFromDir(input.projectDir)
  });
  return {
    ...input,
    backendProject: isRecord4(backendProjectResponse) ? backendProjectResponse.project : void 0,
    projectDataSpace
  };
}
function projectTitleFromDir(projectDir) {
  const parts = projectDir.split(/[\\/]/).filter(Boolean);
  return parts.at(-1) || "Local Project";
}
function stringField18(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function rememberEnsuredProjectBinding(key, pending) {
  ensuredProjectBindings.set(key, pending);
  if (ensuredProjectBindings.size <= ENSURED_PROJECT_BINDING_LIMIT) return;
  const oldest = ensuredProjectBindings.keys().next().value;
  if (oldest) ensuredProjectBindings.delete(oldest);
}
var RESOURCE_AGENT_ROUTES = {
  resources: "/agent/resources",
  resourceDetail: "/agent/resources/:resourceId"
};
function agentResourceDetailPath(resourceId) {
  return `/agent/resources/${encodeURIComponent(String(resourceId))}`;
}
function createResourceLibrarySurface(args, query) {
  return createAgentBrowserSurface(args, {
    route: RESOURCE_AGENT_ROUTES.resources,
    title: "MovScript resource library",
    surface: "inspect",
    intent: "open_resource_library",
    query,
    usage: "Open url in an agent in-app browser. The page uses the local MovScript MCP proxy, which forwards requests with the active agent context."
  });
}
function createResourceDetailSurface(args, resourceId) {
  return createAgentBrowserSurface(args, {
    route: agentResourceDetailPath(resourceId),
    title: `MovScript resource #${resourceId}`,
    surface: "inspect",
    intent: "inspect_resource",
    entity: { resource_id: resourceId },
    query: { resourceId },
    usage: "Open this resource detail surface to inspect the RawResource preview, metadata, provenance, and candidate usage context."
  });
}
var CONTENT_CANDIDATE_STATUSES = /* @__PURE__ */ new Set([
  "queued",
  "running",
  "succeeded",
  "failed",
  "canceled",
  "imported"
]);
var TIMELINE_PRIMITIVE_SPECS = {
  scene_moment: {
    entityKind: "scene_moment",
    payloadName: "scene_moment",
    collection: "scene_moments",
    filename: "scene_moment.json",
    contentUnitType: "scene_moment_ref",
    outputKind: "video",
    targetKind: "scene_moment",
    refField: "sceneMomentId"
  },
  expression_unit: {
    entityKind: "expression_unit",
    payloadName: "expression_unit",
    collection: "expression_units",
    filename: "expression_unit.json",
    contentUnitType: "expression_unit_ref",
    outputKind: "video",
    targetKind: "expression_unit",
    refField: "expressionUnitId"
  },
  storyboard: {
    entityKind: "storyboard",
    payloadName: "storyboard",
    collection: "storyboards",
    filename: "storyboard.json",
    contentUnitType: "storyboard_ref",
    outputKind: "image",
    targetKind: "storyboard",
    refField: "storyboardId"
  },
  keyframe: {
    entityKind: "keyframe",
    payloadName: "keyframe",
    collection: "keyframes",
    filename: "keyframe.json",
    contentUnitType: "keyframe_ref",
    outputKind: "image",
    targetKind: "keyframe",
    refField: "keyframeId"
  },
  audio_cue: {
    entityKind: "audio_cue",
    payloadName: "audio_cue",
    collection: "audio_cues",
    filename: "audio_cue.json",
    contentUnitType: "audio_cue_ref",
    outputKind: "audio",
    targetKind: "audio_cue",
    refField: "audioCueId"
  }
};
async function editingServiceTimelineView(args, kind, input) {
  const locator = resolveMCPProjectWorkspaceLocator(args);
  const decisionStore = await resolveMCPProjectDecisionStoreConfig(locator);
  const result = await createEditingServiceClientFromRuntime().timelineView({
    projectDir: locator.projectDir,
    kind,
    ...decisionStore ? { decisionStore } : {},
    ...input
  });
  return result.result ?? void 0;
}
async function domainGetModel(args) {
  return service(args).getModel({
    entityKind: requiredString2(args.entityKind ?? args.entity_kind, "entityKind"),
    ...args.entityId !== void 0 || args.entity_id !== void 0 ? { entityId: idValue3(args.entityId ?? args.entity_id) } : {}
  });
}
async function domainQueryEntities(args) {
  return service(args).queryEntities({
    ...semanticKind(args.entityKind ?? args.entity_kind) ? { entityKind: semanticKind(args.entityKind ?? args.entity_kind) } : {},
    ...stringValue5(args.kind) ? { kind: stringValue5(args.kind) } : {},
    ...stringValue5(args.query ?? args.q) ? { query: stringValue5(args.query ?? args.q) } : {},
    ...contextIds(args),
    ...numberValue5(args.limit) !== void 0 ? { limit: numberValue5(args.limit) } : {}
  });
}
async function domainQuerySettings(args) {
  return service(args).querySettings({
    ...args.settingId !== void 0 || args.setting_id !== void 0 ? { settingId: idValue3(args.settingId ?? args.setting_id) } : {},
    ...stringValue5(args.kind) ? { kind: stringValue5(args.kind) } : {},
    ...stringValue5(args.query ?? args.q) ? { query: stringValue5(args.query ?? args.q) } : {},
    ...numberValue5(args.limit) !== void 0 ? { limit: numberValue5(args.limit) } : {}
  });
}
async function domainQueryAssets(args) {
  return service(args).queryAssets({
    ...args.assetId !== void 0 || args.asset_id !== void 0 ? { assetId: idValue3(args.assetId ?? args.asset_id) } : {},
    ...args.settingId !== void 0 || args.setting_id !== void 0 ? { settingId: idValue3(args.settingId ?? args.setting_id) } : {},
    ...args.settingStateId !== void 0 || args.setting_state_id !== void 0 ? { settingStateId: idValue3(args.settingStateId ?? args.setting_state_id) } : {},
    ...stringValue5(args.query ?? args.q) ? { query: stringValue5(args.query ?? args.q) } : {},
    ...typeof (args.includeCandidates ?? args.include_candidates) === "boolean" ? { includeCandidates: Boolean(args.includeCandidates ?? args.include_candidates) } : {},
    ...numberValue5(args.limit) !== void 0 ? { limit: numberValue5(args.limit) } : {}
  });
}
async function domainQueryProductionContext(args) {
  return service(args).queryProductionContext({
    ...contextIds(args),
    ...stringValue5(args.query ?? args.q) ? { query: stringValue5(args.query ?? args.q) } : {},
    ...Array.isArray(args.include) ? { include: args.include.filter(isString3) } : {},
    ...numberValue5(args.limit) !== void 0 ? { limit: numberValue5(args.limit) } : {}
  });
}
async function domainReadContentWorkspace(args) {
  return service(args).loadContentWorkspace();
}
async function domainReadContentWorkspaceSnapshot(args) {
  return service(args).loadContentWorkspaceSnapshot();
}
async function domainReadProjectContextSnapshot(args) {
  const locator = resolveMCPProjectWorkspaceLocator(args);
  const response = await createProjectServiceClientFromRuntime().resourceView({
    projectDir: locator.projectDir,
    kind: "project-context"
  });
  return response.items[0] ?? {
    schema: "movscript.project_context_snapshot.v1",
    kind: "project_context_snapshot"
  };
}
async function domainInterpretContentUnitArtifact(args) {
  return service(args).deriveContentUnitArtifact(requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
}
async function domainBuildContentUnitBackendPrompt(args) {
  const contentUnitId = requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
  const result = await service(args).buildContentUnitBackendPrompt(contentUnitId);
  return isRecord4(result) ? {
    ...result,
    surface: createPromptSurface(args, {
      contentUnitId,
      mode: result.ok === true ? "inspect" : "edit",
      projectId: projectIdFromArgs(args)
    })
  } : result;
}
async function domainReadPreviewTimeline(args) {
  const productionId = requiredProductionScopeId(args, "productionId");
  const result = await readProductionPreviewTimeline(args, productionId);
  return isRecord4(result) ? {
    ...result,
    surface: createPreviewTimelineSurface(args, {
      productionId,
      projectId: projectIdFromArgs(args)
    })
  } : result;
}
async function domainReadProductionTimeline(args) {
  const productionId = requiredProductionScopeId(args, "productionId");
  const bundle = await productionTimelineBundle(args, productionId);
  return {
    status: bundle.blockers.length === 0 ? "ok" : "blocked",
    production_id: productionId,
    preview_timeline: bundle.previewTimeline,
    media_editing_project: bundle.mediaEditingProject,
    compose_inputs: bundle.composeInputs,
    clips: bundle.clips,
    blockers: bundle.blockers
  };
}
async function domainReadSceneMomentEditPlan(args) {
  return readSceneMomentEditPlan(args, requiredId2(args.sceneMomentId ?? args.scene_moment_id, "sceneMomentId"));
}
async function domainReadProductionEditPlan(args) {
  const productionId = requiredProductionScopeId(args, "productionId");
  const bundle = await productionTimelineBundle(args, productionId);
  return {
    status: bundle.blockers.length === 0 ? "ok" : "blocked",
    production_id: productionId,
    preview_timeline: bundle.previewTimeline,
    edit_plan: bundle.editPlan,
    context: bundle.context,
    blockers: bundle.blockers
  };
}
async function domainCreateEditingProjectContext(args) {
  const sceneMomentId = args.sceneMomentId ?? args.scene_moment_id;
  if (sceneMomentId !== void 0) {
    const id = requiredId2(sceneMomentId, "sceneMomentId");
    const bundle2 = await sceneMomentTimelineBundle(args, id);
    return {
      status: bundle2.status,
      target_kind: "scene_moment",
      scene_moment_id: id,
      edit_plan: bundle2.editPlan,
      context: bundle2.context,
      blockers: bundle2.blockers
    };
  }
  const productionId = requiredProductionScopeId(args, "productionId");
  const bundle = await productionTimelineBundle(args, productionId);
  return {
    status: bundle.blockers.length === 0 ? "ok" : "blocked",
    target_kind: "production",
    production_id: productionId,
    preview_timeline: bundle.previewTimeline,
    edit_plan: bundle.editPlan,
    context: bundle.context,
    blockers: bundle.blockers
  };
}
async function domainReadSceneMomentTimeline(args) {
  const sceneMomentId = requiredId2(args.sceneMomentId ?? args.scene_moment_id, "sceneMomentId");
  const bundle = await sceneMomentTimelineBundle(args, sceneMomentId);
  return {
    status: bundle.status,
    scene_moment_id: sceneMomentId,
    edit_plan_status: bundle.editPlan.status,
    edit_plan: bundle.editPlan,
    media_editing_project: bundle.mediaEditingProject,
    compose_inputs: bundle.composeInputs
  };
}
async function domainReadContentUnitRuntimePanel(args) {
  return service(args).readContentUnitRuntimePanel(requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
}
async function domainReadContentUnitGenerationPrompt(args) {
  const contentUnitId = requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
  const result = await service(args).readContentUnitGenerationPrompt(contentUnitId);
  return isRecord4(result) ? {
    ...result,
    surface: createPromptSurface(args, {
      contentUnitId,
      mode: "inspect",
      projectId: projectIdFromArgs(args)
    })
  } : result;
}
async function domainReadContentUnitDependencyReport(args) {
  return service(args).readContentUnitDependencyReport(requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
}
async function domainReadContentUnitSelectionValidity(args) {
  return service(args).readContentUnitSelectionValidity(requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId"));
}
async function domainUpsertProjectStandards(args) {
  return runtimeMutation(args, (runtime) => runtime.upsertProjectStandards({
    record: optionalRecord(args.record),
    projectStyle: requiredRecord(args.projectStyle ?? args.project_style, "projectStyle")
  }));
}
async function domainUpsertSetting(args) {
  const payload = upsertPayloadRecord(args);
  return runtimeMutation(args, (runtime) => runtime.createSetting({
    id: idValue3(payload.id ?? payload.client_id),
    title: stringValue5(payload.title),
    kind: stringValue5(payload.setting_kind ?? payload.kind),
    namespaceKind: stringValue5(payload.namespace_kind ?? payload.namespaceKind),
    settingNamespaceKind: stringValue5(payload.setting_namespace_kind ?? payload.settingNamespaceKind ?? payload.namespace_kind ?? payload.namespaceKind),
    description: stringValue5(payload.description),
    alias: stringValue5(payload.alias),
    content: payload.content,
    importance: payload.importance
  }));
}
async function domainUpsertSettingState(args) {
  const payload = upsertPayloadRecord(args);
  return runtimeMutation(args, (runtime) => runtime.createSettingState({
    id: optionalId(payload.id ?? payload.client_id),
    settingId: optionalId(payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef),
    title: stringValue5(payload.title),
    stateKind: stringValue5(payload.state_kind ?? payload.kind),
    namespaceKind: stringValue5(payload.namespace_kind ?? payload.namespaceKind),
    settingNamespaceKind: stringValue5(payload.setting_namespace_kind ?? payload.settingNamespaceKind ?? payload.namespace_kind ?? payload.namespaceKind),
    description: stringValue5(payload.description)
  }));
}
async function domainUpsertAsset(args) {
  const payload = upsertPayloadRecord(args);
  return runtimeMutation(args, (runtime) => runtime.createAsset({
    id: idValue3(payload.id ?? payload.client_id),
    title: stringValue5(payload.title),
    settingId: idValue3(payload.setting_id ?? payload.settingId ?? payload.setting_ref ?? payload.settingRef),
    settingStateId: idValue3(payload.setting_state_id ?? payload.settingStateId ?? payload.setting_state_ref ?? payload.settingStateRef),
    slot: stringValue5(payload.slot ?? payload.slot_key ?? payload.slotKey),
    assetKind: stringValue5(payload.asset_kind ?? payload.kind),
    promptHint: stringValue5(payload.prompt_hint ?? payload.promptHint),
    resourceId: idValue3(payload.resource_id ?? payload.resourceId)
  }));
}
async function domainCertifyAssetProvider(args) {
  const provider = providerCertificationProvider(args.provider ?? args.provider_key ?? args.providerKey);
  const runtime = service(args);
  const index = await runtime.loadIndex();
  const assetRef3 = requiredId2(args.assetId ?? args.asset_id ?? args.assetRef ?? args.asset_ref, "assetId");
  const asset = index.entities.find((entity) => entity.entityKind === "asset" && (sameEntityRef(entity.id, assetRef3, "asset") || sameEntityRef(lastPathSegment8(entity.path), assetRef3, "asset") || normalizeWorkspacePath(entity.path.replace(/\/asset\.json$/, "")) === normalizeWorkspacePath(String(assetRef3))));
  if (!asset) throw new Error(`asset not found: ${String(assetRef3)}`);
  const selection = resolveAssetRefSelection(index, asset);
  const sourceResourceId2 = resourceIdFromUnknown(args.resourceId ?? args.resource_id) ?? selection.resourceId ?? resourceIdFromUnknown(asset.record.resource_id ?? asset.record.resourceId);
  if (sourceResourceId2 === void 0) {
    throw new Error(`asset ${String(asset.id ?? assetRef3)} has no selected asset_ref resource_id; select/adopt the asset_ref candidate before certification`);
  }
  const sourceUrl = getOptionalCertificationString(args.source_url ?? args.sourceUrl ?? args.url);
  const name = getOptionalCertificationString(args.name) ?? stringValue5(asset.record.title) ?? stringValue5(asset.record.slot) ?? String(asset.id ?? assetRef3);
  const projectId = stringValue5(args.projectId ?? args.project_id);
  const projectName = getOptionalCertificationString(args.projectName ?? args.project_name) ?? projectId;
  const settingId = getOptionalCertificationString(args.settingId ?? args.setting_id) ?? stringValue5(asset.record.setting_id ?? asset.record.settingId) ?? settingIdFromAssetPath(asset.path);
  const model = getOptionalCertificationString(args.model ?? args.model_id ?? args.modelId ?? args.public_model_id ?? args.publicModelId);
  const assetGroupID = getOptionalCertificationString(args.asset_group_id ?? args.assetGroupId ?? args.group_id ?? args.groupId);
  const assetGroupName = getOptionalCertificationString(args.asset_group_name ?? args.assetGroupName ?? args.group_name ?? args.groupName);
  const backendResult = await backendPost2(`/provider-assets/providers/${encodeURIComponent(provider)}/certify`, {
    provider,
    resource_id: sourceResourceId2,
    ...selection.candidateId !== void 0 ? { source_candidate_id: String(selection.candidateId) } : {},
    ...projectId ? { project_id: projectId } : {},
    ...projectName ? { project_name: projectName } : {},
    ...settingId ? { setting_id: settingId } : {},
    ...model ? { model } : {},
    ...assetGroupID ? { asset_group_id: assetGroupID } : {},
    ...assetGroupName ? { asset_group_name: assetGroupName } : {},
    ...sourceUrl ? { source_url: sourceUrl } : {},
    name,
    ...booleanValue(args.allow_private_urls ?? args.allowPrivateUrls) === true ? { allow_private_urls: true } : {},
    ...numberValue5(args.timeout_ms ?? args.timeoutMs) !== void 0 ? { timeout_ms: numberValue5(args.timeout_ms ?? args.timeoutMs) } : {}
  });
  const certification = isRecord4(backendResult?.certification) ? backendResult.certification : void 0;
  if (!certification) throw new Error("backend provider asset certification response did not include certification");
  const assetUri = getOptionalCertificationString(certification.asset_uri ?? certification.assetUri);
  if (!assetUri) throw new Error("backend provider asset certification response did not include asset_uri");
  const hubAssetId = getOptionalCertificationString(certification.hub_asset_id ?? certification.hubAssetId);
  const certifiedSourceResourceId = resourceIdFromUnknown(certification.source_resource_id ?? certification.sourceResourceId) ?? sourceResourceId2;
  const certifiedSourceCandidateId = idFromUnknown(certification.source_candidate_id ?? certification.sourceCandidateId) ?? selection.candidateId;
  const certifiedProvider = getOptionalCertificationString(certification.provider_id ?? certification.providerId ?? backendResult?.provider_id ?? backendResult?.providerId ?? backendResult?.provider) ?? provider;
  const normalizedCertification = {
    ...certification,
    provider: certifiedProvider,
    provider_id: certifiedProvider,
    source_resource_id: certifiedSourceResourceId,
    ...model ? { model, public_model_id: model, provider_model_id: getOptionalCertificationString(certification.provider_model_id ?? certification.providerModelId) ?? model } : {},
    ...certifiedSourceCandidateId !== void 0 ? { source_candidate_id: certifiedSourceCandidateId } : {}
  };
  const written = await patchAssetProviderCertification(runtime.projectDir, asset.path, asset.record, certifiedProvider, normalizedCertification);
  invalidateMovScriptDomainRuntime(resolveMCPProjectWorkspaceLocator(args));
  return {
    status: "succeeded",
    provider: certifiedProvider,
    provider_id: certifiedProvider,
    asset_id: asset.id,
    asset_path: asset.path,
    source_resource_id: certifiedSourceResourceId,
    ...certifiedSourceCandidateId !== void 0 ? { source_candidate_id: certifiedSourceCandidateId } : {},
    asset_uri: assetUri,
    ...hubAssetId ? { hub_asset_id: hubAssetId } : {},
    certification: written.certification,
    path: written.path,
    backend_result: backendResult,
    message: `Certified asset ${String(asset.id ?? assetRef3)} for ${certifiedProvider}; downstream generation can resolve resource #${String(certifiedSourceResourceId)} as ${assetUri}.`
  };
}
async function domainQueryRemoteAssetGroups(args) {
  const provider = providerCertificationProvider(args.provider ?? args.provider_id ?? args.providerId ?? args.provider_key ?? args.providerKey);
  const params = new URLSearchParams();
  const model = getOptionalCertificationString(args.model ?? args.model_id ?? args.modelId ?? args.public_model_id ?? args.publicModelId);
  const projectId = getOptionalCertificationString(args.projectId ?? args.project_id);
  if (model) params.set("model", model);
  if (projectId) params.set("project_id", projectId);
  const suffix = params.toString();
  return backendGet2(`/provider-assets/providers/${encodeURIComponent(provider)}/groups${suffix ? `?${suffix}` : ""}`);
}
async function domainQueryRemoteAssets(args) {
  const provider = providerCertificationProvider(args.provider ?? args.provider_id ?? args.providerId ?? args.provider_key ?? args.providerKey);
  const groupRef = requiredId2(args.groupId ?? args.group_id ?? args.groupRef ?? args.group_ref ?? args.asset_group_id ?? args.assetGroupId, "groupId");
  return backendGet2(`/provider-assets/providers/${encodeURIComponent(provider)}/groups/${encodeURIComponent(String(groupRef))}/assets`);
}
function settingIdFromAssetPath(path) {
  const normalized = normalizeWorkspacePath(path ?? "");
  const parts = normalized.split("/").filter(Boolean);
  const settingsIndex = parts.indexOf("settings");
  if (settingsIndex < 0) return void 0;
  return stringValue5(parts[settingsIndex + 1]);
}
async function domainUpsertSettingTree(args) {
  const settingPayload = requiredRecord(args.setting ?? args.payload, "setting");
  const stateItems = requiredArray(args.states ?? settingPayload.states, "states").filter(isRecord4);
  return runtimeMutation(args, async (runtime) => {
    const setting = await runtime.createSetting({
      id: optionalId(settingPayload.id ?? settingPayload.client_id),
      title: stringValue5(settingPayload.title),
      kind: stringValue5(settingPayload.setting_kind ?? settingPayload.kind),
      namespaceKind: stringValue5(settingPayload.namespace_kind ?? settingPayload.namespaceKind),
      settingNamespaceKind: stringValue5(settingPayload.setting_namespace_kind ?? settingPayload.settingNamespaceKind ?? settingPayload.namespace_kind ?? settingPayload.namespaceKind),
      description: stringValue5(settingPayload.description),
      alias: stringValue5(settingPayload.alias),
      content: settingPayload.content,
      importance: settingPayload.importance
    });
    const settingId = entityResultId(setting, settingPayload, "setting");
    const states = [];
    for (const stateItem of stateItems) {
      const statePayload = requiredRecord(stateItem.state ?? stateItem.payload ?? stateItem.record ?? stateItem.entity ?? stateItem, "state");
      const state = await runtime.createSettingState({
        id: optionalId(statePayload.id ?? statePayload.client_id),
        settingId: optionalId(statePayload.setting_id ?? statePayload.settingId ?? statePayload.setting_ref ?? statePayload.settingRef ?? settingId),
        title: stringValue5(statePayload.title),
        stateKind: stringValue5(statePayload.state_kind ?? statePayload.kind),
        namespaceKind: stringValue5(statePayload.namespace_kind ?? statePayload.namespaceKind),
        settingNamespaceKind: stringValue5(statePayload.setting_namespace_kind ?? statePayload.settingNamespaceKind ?? statePayload.namespace_kind ?? statePayload.namespaceKind),
        description: stringValue5(statePayload.description)
      });
      const stateId = entityResultId(state, statePayload, "setting_state");
      const assetItems = Array.isArray(stateItem.assets) ? stateItem.assets.filter(isRecord4) : [];
      const assets = [];
      for (const assetItem of assetItems) {
        const assetPayload = requiredRecord(assetItem.payload ?? assetItem.record ?? assetItem.entity ?? assetItem, "asset");
        assets.push(await runtime.createAsset({
          id: optionalId(assetPayload.id ?? assetPayload.client_id),
          title: stringValue5(assetPayload.title),
          settingId: optionalId(assetPayload.setting_id ?? assetPayload.settingId ?? assetPayload.setting_ref ?? assetPayload.settingRef ?? settingId),
          settingStateId: optionalId(assetPayload.setting_state_id ?? assetPayload.settingStateId ?? assetPayload.setting_state_ref ?? assetPayload.settingStateRef ?? stateId),
          slot: stringValue5(assetPayload.slot ?? assetPayload.slot_key ?? assetPayload.slotKey),
          assetKind: stringValue5(assetPayload.asset_kind ?? assetPayload.kind),
          promptHint: stringValue5(assetPayload.prompt_hint ?? assetPayload.promptHint),
          resourceId: optionalId(assetPayload.resource_id ?? assetPayload.resourceId)
        }));
      }
      states.push({ state, assets });
    }
    return { setting, states };
  });
}
async function domainUpsertScript(args) {
  return runtimeMutation(args, (runtime) => runtime.upsertScript({
    scriptId: requiredId2(args.scriptId ?? args.script_id, "scriptId"),
    record: optionalRecord(args.record),
    sourceText: requiredString2(args.sourceText ?? args.source_text, "sourceText"),
    ...optionalRecord(args.metadata) ? { metadata: optionalRecord(args.metadata) } : {}
  }));
}
async function domainReadScriptSource(args) {
  return service(args).readScriptSource({
    record: requiredRecord(args.record, "record"),
    ...optionalRecord(args.entity) ? { entity: optionalRecord(args.entity) } : {}
  });
}
async function domainSnapshotScriptVersion(args) {
  return runtimeMutation(args, (runtime) => runtime.snapshotScriptVersionFromMarkdown({
    scriptId: requiredId2(args.scriptId ?? args.script_id, "scriptId"),
    versionId: requiredId2(args.versionId ?? args.version_id, "versionId"),
    ...stringValue5(args.versionLabel ?? args.version_label) ? { versionLabel: stringValue5(args.versionLabel ?? args.version_label) } : {},
    ...stringValue5(args.sourcePath ?? args.source_path) ? { sourcePath: stringValue5(args.sourcePath ?? args.source_path) } : {}
  }));
}
async function domainUpsertContentUnit(args) {
  return runtimeMutation(args, (runtime) => runtime.createContentUnit(
    engineContentUnitInputFromRecord(requiredRecord(args.unit, "unit"))
  ));
}
async function domainUpsertProduction(args) {
  const production = requiredRecord(args.production ?? args.payload ?? args.record, "production");
  const productionId = productionIdFrom(args, production);
  const result = await runtimeMutation(args, (runtime) => runtime.createProduction({
    id: productionId,
    title: stringValue5(production.title)
  }));
  return productionWriteResult("production", { productionId }, result);
}
async function domainUpsertProductionTree(args) {
  const productionPayload = requiredRecord(args.production ?? args.payload ?? args.record, "production");
  const segmentItems = requiredArray(args.segments ?? productionPayload.segments, "segments").filter(isRecord4);
  return runtimeMutation(args, async (runtime) => {
    const productionId = productionIdFrom(args, productionPayload);
    const production = await runtime.createProduction({
      id: productionId,
      title: stringValue5(productionPayload.title)
    });
    const segments = [];
    for (const segmentItem of segmentItems) {
      const segmentPayload = treePayload(segmentItem, "segment");
      const segmentId = requiredId2(segmentPayload.id ?? segmentPayload.client_id, "segment.id");
      const segment = await runtime.createSegment({
        productionId,
        id: segmentId,
        title: stringValue5(segmentPayload.title),
        kind: stringValue5(segmentPayload.segment_kind ?? segmentPayload.kind),
        summary: stringValue5(segmentPayload.summary ?? segmentPayload.description),
        order: numberValue5(segmentPayload.order)
      });
      const sceneMomentItems = arrayRecords2(segmentItem.scene_moments ?? segmentItem.sceneMoments);
      const contentUnitItems = arrayRecords2(segmentItem.content_units ?? segmentItem.contentUnits);
      const sceneMoments = [];
      const contentUnits2 = [];
      for (const contentUnitItem of contentUnitItems) {
        const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
        contentUnits2.push(await runtime.createContentUnit(treeContentUnitInput(contentUnitPayload, {
          productionId,
          segmentId,
          contentUnitType: "segment_ref",
          outputKind: "video",
          targetKind: "segment",
          targetRef: segmentId
        })));
      }
      for (const sceneMomentItem of sceneMomentItems) {
        const sceneMomentPayload = treePayload(sceneMomentItem, "scene_moment");
        const sceneMomentId = requiredId2(sceneMomentPayload.id ?? sceneMomentPayload.client_id, "scene_moment.id");
        const sceneMoment = await runtime.createSceneMoment({
          productionId,
          segmentId,
          id: sceneMomentId,
          title: stringValue5(sceneMomentPayload.title),
          storyboardId: optionalId(sceneMomentPayload.storyboard_id ?? sceneMomentPayload.storyboardId),
          order: numberValue5(sceneMomentPayload.order),
          timeText: stringValue5(sceneMomentPayload.time_text ?? sceneMomentPayload.when),
          sceneCode: stringValue5(sceneMomentPayload.scene_code),
          locationText: stringValue5(sceneMomentPayload.location_text ?? sceneMomentPayload.where),
          conditionText: stringValue5(sceneMomentPayload.condition_text),
          actionText: stringValue5(sceneMomentPayload.action_text ?? sceneMomentPayload.action),
          mood: stringValue5(sceneMomentPayload.mood ?? sceneMomentPayload.emotion),
          description: stringValue5(sceneMomentPayload.description),
          settings: settingRefsFromRecord2(sceneMomentPayload)
        });
        const sceneContentUnits = [];
        for (const contentUnitItem of arrayRecords2(sceneMomentItem.content_units ?? sceneMomentItem.contentUnits)) {
          const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
          sceneContentUnits.push(await runtime.createContentUnit(treeContentUnitInput(contentUnitPayload, {
            productionId,
            segmentId,
            sceneMomentId,
            contentUnitType: "scene_moment_ref",
            outputKind: "video",
            targetKind: "scene_moment",
            targetRef: sceneMomentId
          })));
        }
        const expressionUnits = [];
        for (const expressionUnitItem of arrayRecords2(sceneMomentItem.expression_units ?? sceneMomentItem.expressionUnits)) {
          const expressionUnitPayload = normalizeExpressionUnitPayload(treePayload(expressionUnitItem, "expression_unit"));
          const expressionUnitId = requiredId2(expressionUnitPayload.id ?? expressionUnitPayload.client_id, "expression_unit.id");
          const expressionUnit = await runtime.createExpressionUnit({
            productionId,
            segmentId,
            sceneMomentId,
            id: expressionUnitId,
            title: stringValue5(expressionUnitPayload.title),
            slotKind: stringValue5(expressionUnitPayload.slot_kind ?? expressionUnitPayload.slotKind),
            kind: stringValue5(expressionUnitPayload.kind),
            text: stringValue5(expressionUnitPayload.text ?? expressionUnitPayload.content),
            intent: stringValue5(expressionUnitPayload.intent ?? expressionUnitPayload.summary ?? expressionUnitPayload.description),
            speaker: stringValue5(expressionUnitPayload.speaker),
            note: stringValue5(expressionUnitPayload.note),
            order: numberValue5(expressionUnitPayload.order)
          });
          const expressionContentUnits = [];
          for (const contentUnitItem of arrayRecords2(expressionUnitItem.content_units ?? expressionUnitItem.contentUnits)) {
            const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
            expressionContentUnits.push(await runtime.createContentUnit(treeContentUnitInput(contentUnitPayload, {
              productionId,
              segmentId,
              sceneMomentId,
              expressionUnitId,
              contentUnitType: "expression_unit_ref",
              outputKind: "video",
              targetKind: "expression_unit",
              targetRef: expressionUnitId
            })));
          }
          const storyboards2 = await upsertTreeStoryboards(runtime, expressionUnitItem, { productionId, segmentId, sceneMomentId, expressionUnitId });
          const keyframes2 = await upsertTreeKeyframes(runtime, expressionUnitItem, { productionId, segmentId, sceneMomentId, expressionUnitId });
          const audioCues2 = await upsertTreeAudioCues(runtime, expressionUnitItem, { productionId, segmentId, sceneMomentId, expressionUnitId });
          expressionUnits.push({ expressionUnit, contentUnits: expressionContentUnits, storyboards: storyboards2, keyframes: keyframes2, audioCues: audioCues2 });
        }
        const storyboards = await upsertTreeStoryboards(runtime, sceneMomentItem, { productionId, segmentId, sceneMomentId });
        const keyframes = await upsertTreeKeyframes(runtime, sceneMomentItem, { productionId, segmentId, sceneMomentId });
        const audioCues = await upsertTreeAudioCues(runtime, sceneMomentItem, { productionId, segmentId, sceneMomentId });
        sceneMoments.push({ sceneMoment, contentUnits: sceneContentUnits, expressionUnits, storyboards, keyframes, audioCues });
      }
      segments.push({ segment, contentUnits: contentUnits2, sceneMoments });
    }
    const contentUnits = [];
    for (const contentUnitItem of arrayRecords2(args.content_units ?? args.contentUnits ?? productionPayload.content_units ?? productionPayload.contentUnits)) {
      const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
      contentUnits.push(await runtime.createContentUnit(treeContentUnitInput(contentUnitPayload, {
        productionId,
        contentUnitType: "production_ref",
        outputKind: "video",
        targetKind: "production",
        targetRef: productionId
      })));
    }
    return { production, contentUnits, segments };
  });
}
async function domainUpsertTimelineNamespaceTree(args) {
  const namespaceItems = timelineNamespaceTreeItems(args);
  if (namespaceItems.length === 0) throw new Error("namespace, root, tree, nodes, or timeline_namespaces is required");
  return runtimeMutation(args, async (runtime) => {
    const collector = { namespaces: [], sceneMoments: [], primitives: [], contentUnits: [] };
    const tree = [];
    for (const item of namespaceItems) {
      tree.push(await upsertTimelineNamespaceTreeNode(runtime, item, { depth: 0 }, collector));
    }
    return {
      schema: "movscript.timeline_namespace_tree_upsert_result.v1",
      status: "upserted",
      tree,
      namespaces: collector.namespaces,
      sceneMoments: collector.sceneMoments,
      primitives: collector.primitives,
      contentUnits: collector.contentUnits
    };
  });
}
async function upsertTimelineNamespaceTreeNode(runtime, item, context, collector) {
  const payload = treePayload(item, "namespace");
  const namespaceId = requiredId2(payload.id ?? payload.client_id, "namespace.id");
  const namespaceKind = requiredString2(
    payload.namespace_kind ?? payload.namespaceKind ?? payload.timeline_namespace_kind ?? payload.timelineNamespaceKind ?? payload.domain_kind ?? payload.domainKind ?? payload.kind,
    "namespace.namespace_kind"
  );
  const entityKind = timelineNamespaceEntityKind(payload, context);
  const targetPath = timelineNamespaceTargetPath(item, payload, context, entityKind, namespaceId);
  const record = timelineNamespaceRecord(payload, {
    entityKind,
    namespaceId,
    namespaceKind
  });
  const node = await runtime.writeHierarchyNode({
    category: "timeline_namespace",
    namespaceKind,
    targetPath,
    record
  });
  const namespaceSummary = {
    id: namespaceId,
    namespace_kind: namespaceKind,
    entity_kind: entityKind,
    targetPath,
    node
  };
  collector.namespaces.push(namespaceSummary);
  const contentUnits = [];
  for (const contentUnitItem of arrayRecords2(item.content_units ?? item.contentUnits ?? payload.content_units ?? payload.contentUnits)) {
    const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
    const contentUnit = await runtime.createContentUnit(timelineNamespaceAssemblyContentUnitInput(contentUnitPayload, {
      namespaceId,
      namespaceKind
    }));
    contentUnits.push(contentUnit);
    collector.contentUnits.push(contentUnit);
  }
  const sceneMoments = [];
  for (const sceneMomentItem of timelineNamespaceSceneMomentItems(item, payload)) {
    sceneMoments.push(await upsertTimelinePrimitiveNode(runtime, sceneMomentItem, {
      spec: TIMELINE_PRIMITIVE_SPECS.scene_moment,
      parentDir: timelineNamespaceNodeDir(targetPath)
    }, collector));
  }
  const children = [];
  for (const child of timelineNamespaceChildItems(item, payload)) {
    children.push(await upsertTimelineNamespaceTreeNode(runtime, child, {
      depth: context.depth + 1,
      parentTargetPath: targetPath
    }, collector));
  }
  return {
    ...namespaceSummary,
    contentUnits,
    sceneMoments,
    children
  };
}
async function domainUpsertSegment(args) {
  const segment = requiredRecord(args.segment ?? args.payload, "segment");
  const production = optionalRecord(args.production);
  const productionId = productionIdFrom(args, production);
  const segmentId = requiredId2(args.segmentId ?? args.segment_id ?? segment.id ?? segment.client_id, "segmentId");
  const result = await runtimeMutation(args, (runtime) => runtime.createSegment({
    productionId,
    id: segmentId,
    title: stringValue5(segment.title),
    kind: stringValue5(segment.segment_kind ?? segment.kind),
    summary: stringValue5(segment.summary ?? segment.description),
    order: numberValue5(segment.order)
  }));
  return productionWriteResult("segment", { productionId, segmentId }, result);
}
async function domainUpsertSceneMoment(args) {
  const sceneMoment = requiredRecord(args.sceneMoment ?? args.scene_moment ?? args.payload, "sceneMoment");
  const sceneMomentId = requiredId2(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment.id ?? sceneMoment.client_id, "sceneMomentId");
  const pathTarget = sceneMomentPathTarget(args, sceneMoment, sceneMomentId);
  if (pathTarget) {
    const result2 = await runtimeMutation(args, (runtime) => runtime.writeHierarchyNode({
      targetPath: pathTarget,
      record: sceneMomentHierarchyRecord(sceneMoment, sceneMomentId)
    }));
    return {
      status: "upserted",
      entityKind: "scene_moment",
      sceneMomentId,
      targetPath: pathTarget,
      result: result2
    };
  }
  const production = optionalRecord(args.production);
  const segment = optionalRecord(args.segment);
  const productionId = productionIdFrom(args, production);
  const segmentId = requiredId2(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
  const result = await runtimeMutation(args, (runtime) => runtime.createSceneMoment({
    productionId,
    segmentId,
    id: sceneMomentId,
    title: stringValue5(sceneMoment.title),
    storyboardId: idValue3(sceneMoment.storyboard_id ?? sceneMoment.storyboardId),
    order: numberValue5(sceneMoment.order),
    timeText: stringValue5(sceneMoment.time_text ?? sceneMoment.when),
    sceneCode: stringValue5(sceneMoment.scene_code),
    locationText: stringValue5(sceneMoment.location_text ?? sceneMoment.where),
    conditionText: stringValue5(sceneMoment.condition_text),
    actionText: stringValue5(sceneMoment.action_text ?? sceneMoment.action),
    mood: stringValue5(sceneMoment.mood ?? sceneMoment.emotion),
    description: stringValue5(sceneMoment.description),
    settings: settingRefsFromRecord2(sceneMoment)
  }));
  return productionWriteResult("scene_moment", { productionId, segmentId, sceneMomentId }, result);
}
async function domainUpsertKeyframe(args) {
  const keyframe = normalizeKeyframePayload(requiredRecord(args.keyframe ?? args.payload, "keyframe"));
  const expressionUnit = optionalRecord(args.expressionUnit ?? args.expression_unit);
  const keyframeId = requiredId2(args.keyframeId ?? args.keyframe_id ?? keyframe.id ?? keyframe.client_id, "keyframeId");
  const pathTarget = timelinePrimitivePathTarget(args, keyframe, {
    spec: TIMELINE_PRIMITIVE_SPECS.keyframe,
    id: keyframeId
  });
  if (pathTarget) {
    return writeTimelinePrimitivePathTarget(args, TIMELINE_PRIMITIVE_SPECS.keyframe, keyframe, keyframeId, pathTarget);
  }
  const production = optionalRecord(args.production);
  const segment = optionalRecord(args.segment);
  const sceneMoment = optionalRecord(args.sceneMoment ?? args.scene_moment);
  const productionId = productionIdFrom(args, production);
  const segmentId = requiredId2(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
  const sceneMomentId = requiredId2(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment?.id ?? sceneMoment?.client_id, "sceneMomentId");
  const expressionUnitId = idValue3(args.expressionUnitId ?? args.expression_unit_id ?? expressionUnit?.id ?? expressionUnit?.client_id);
  const result = await runtimeMutation(args, (runtime) => runtime.createKeyframe({
    productionId,
    segmentId,
    sceneMomentId,
    ...expressionUnitId ? { expressionUnitId } : {},
    id: keyframeId,
    title: stringValue5(keyframe.title),
    role: stringValue5(keyframe.role ?? keyframe.status),
    visualIntent: stringValue5(keyframe.visual_intent ?? keyframe.visualIntent ?? keyframe.prompt_hint ?? keyframe.description),
    order: numberValue5(keyframe.order),
    timing: optionalRecord(keyframe.timing),
    composition: optionalRecord(keyframe.composition),
    continuity: optionalRecord(keyframe.continuity),
    referenceAssetRefs: Array.isArray(keyframe.reference_asset_refs) ? keyframe.reference_asset_refs : void 0,
    referenceKeyframeRefs: Array.isArray(keyframe.reference_keyframe_refs) ? keyframe.reference_keyframe_refs : void 0
  }));
  return productionWriteResult("keyframe", { productionId, segmentId, sceneMomentId, ...expressionUnitId ? { expressionUnitId } : {}, keyframeId }, result);
}
async function domainUpsertStoryboard(args) {
  const storyboard = requiredRecord(args.storyboard ?? args.payload, "storyboard");
  const expressionUnit = optionalRecord(args.expressionUnit ?? args.expression_unit);
  const storyboardId = idValue3(args.storyboardId ?? args.storyboard_id ?? storyboard.id ?? storyboard.client_id ?? "main") ?? "main";
  const pathTarget = timelinePrimitivePathTarget(args, storyboard, {
    spec: TIMELINE_PRIMITIVE_SPECS.storyboard,
    id: storyboardId
  });
  if (pathTarget) {
    const pathResult = await writeTimelinePrimitivePathTarget(args, TIMELINE_PRIMITIVE_SPECS.storyboard, storyboard, storyboardId, pathTarget);
    return {
      ...pathResult,
      storyboardId,
      storyboardPath: pathTarget
    };
  }
  const productionId = requiredId2(args.productionId ?? args.production_id, "productionId");
  const segmentId = requiredId2(args.segmentId ?? args.segment_id, "segmentId");
  const sceneMomentId = requiredId2(args.sceneMomentId ?? args.scene_moment_id, "sceneMomentId");
  const expressionUnitId = idValue3(args.expressionUnitId ?? args.expression_unit_id ?? storyboard.expression_unit_id ?? expressionUnit?.id ?? expressionUnit?.client_id);
  const result = await runtimeMutation(args, (runtime) => runtime.createStoryboard({
    productionId,
    segmentId,
    sceneMomentId,
    ...expressionUnitId ? { expressionUnitId } : {},
    id: storyboardId ?? "main",
    title: stringValue5(storyboard.title),
    visualIntent: stringValue5(storyboard.visual_intent ?? storyboard.visualIntent ?? storyboard.prompt_hint ?? storyboard.description),
    order: numberValue5(storyboard.order),
    timeline: optionalRecord(storyboard.timeline),
    graph: optionalRecord(storyboard.graph)
  }));
  return {
    status: "upserted",
    productionId,
    segmentId,
    sceneMomentId,
    ...expressionUnitId ? { expressionUnitId } : {},
    storyboardId,
    writtenPaths: result.writtenPaths,
    storyboardPath: result.writtenPaths.find((path) => path.endsWith("/storyboard.json"))
  };
}
async function domainUpsertAudioCue(args) {
  const audioCue = normalizeAudioCuePayload(requiredRecord(args.audioCue ?? args.audio_cue ?? args.payload, "audioCue"));
  const audioCueId = requiredId2(args.audioCueId ?? args.audio_cue_id ?? audioCue.id ?? audioCue.client_id, "audioCueId");
  const pathTarget = timelinePrimitivePathTarget(args, audioCue, {
    spec: TIMELINE_PRIMITIVE_SPECS.audio_cue,
    id: audioCueId
  });
  if (pathTarget) {
    return writeTimelinePrimitivePathTarget(args, TIMELINE_PRIMITIVE_SPECS.audio_cue, audioCue, audioCueId, pathTarget);
  }
  const production = optionalRecord(args.production);
  const segment = optionalRecord(args.segment);
  const sceneMoment = optionalRecord(args.sceneMoment ?? args.scene_moment);
  const productionId = productionIdFrom(args, production);
  const segmentId = requiredId2(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
  const sceneMomentId = requiredId2(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment?.id ?? sceneMoment?.client_id, "sceneMomentId");
  const result = await runtimeMutation(args, (runtime) => runtime.createAudioCue({
    productionId,
    segmentId,
    sceneMomentId,
    id: audioCueId,
    title: stringValue5(audioCue.title),
    kind: stringValue5(audioCue.cue_kind ?? audioCue.kind),
    storyboardId: idValue3(audioCue.storyboard_id ?? audioCue.storyboardId),
    expressionUnitId: idValue3(audioCue.expression_unit_ref ?? audioCue.expressionUnitRef ?? audioCue.expression_unit_id ?? audioCue.expressionUnitId),
    promptHint: stringValue5(audioCue.prompt_hint ?? audioCue.promptHint)
  }));
  return productionWriteResult("audio_cue", { productionId, segmentId, sceneMomentId, audioCueId }, result);
}
async function domainUpsertExpressionUnit(args) {
  const expressionUnit = normalizeExpressionUnitPayload(requiredRecord(args.expressionUnit ?? args.expression_unit ?? args.payload, "expressionUnit"));
  const expressionUnitId = requiredId2(args.expressionUnitId ?? args.expression_unit_id ?? expressionUnit.id ?? expressionUnit.client_id, "expressionUnitId");
  const pathTarget = timelinePrimitivePathTarget(args, expressionUnit, {
    spec: TIMELINE_PRIMITIVE_SPECS.expression_unit,
    id: expressionUnitId
  });
  if (pathTarget) {
    return writeTimelinePrimitivePathTarget(args, TIMELINE_PRIMITIVE_SPECS.expression_unit, expressionUnit, expressionUnitId, pathTarget);
  }
  const production = optionalRecord(args.production);
  const segment = optionalRecord(args.segment);
  const sceneMoment = optionalRecord(args.sceneMoment ?? args.scene_moment);
  const productionId = productionIdFrom(args, production);
  const segmentId = requiredId2(args.segmentId ?? args.segment_id ?? segment?.id ?? segment?.client_id, "segmentId");
  const sceneMomentId = requiredId2(args.sceneMomentId ?? args.scene_moment_id ?? sceneMoment?.id ?? sceneMoment?.client_id, "sceneMomentId");
  const result = await runtimeMutation(args, (runtime) => runtime.createExpressionUnit({
    productionId,
    segmentId,
    sceneMomentId,
    id: expressionUnitId,
    title: stringValue5(expressionUnit.title),
    slotKind: stringValue5(expressionUnit.slot_kind ?? expressionUnit.slotKind),
    kind: stringValue5(expressionUnit.kind),
    text: stringValue5(expressionUnit.text ?? expressionUnit.content),
    intent: stringValue5(expressionUnit.intent ?? expressionUnit.summary ?? expressionUnit.description),
    speaker: stringValue5(expressionUnit.speaker),
    note: stringValue5(expressionUnit.note),
    order: numberValue5(expressionUnit.order)
  }));
  return productionWriteResult("expression_unit", { productionId, segmentId, sceneMomentId, expressionUnitId }, result);
}
async function domainUpdateContentUnitPrompt(args) {
  const result = await runtimeMutation(args, (runtime) => runtime.updateContentUnitEditPrompt({
    targetPath: requiredString2(args.targetPath ?? args.target_path, "targetPath"),
    editPrompt: requiredRecord(args.editPrompt ?? args.edit_prompt, "editPrompt")
  }));
  const contentUnitId = args.contentUnitId ?? args.content_unit_id;
  return {
    status: "updated",
    result,
    ...contentUnitId !== void 0 ? {
      contentUnitId,
      content_unit_id: contentUnitId,
      surface: createPromptSurface(args, {
        contentUnitId: idValue3(contentUnitId),
        mode: "edit",
        projectId: projectIdFromArgs(args)
      })
    } : {},
    secondary_surfaces: [
      createImpactSurface(args, {
        projectId: projectIdFromArgs(args),
        target: contentUnitId === void 0 ? void 0 : String(contentUnitId),
        source: "domain_update_content_unit_prompt"
      })
    ]
  };
}
async function domainUpdateEntityTransition(args) {
  return runtimeMutation(args, (runtime) => runtime.updateEntityTransition({
    targetPath: requiredString2(args.targetPath ?? args.target_path, "targetPath"),
    ...optionalRecord(args.transition) ? { transition: optionalRecord(args.transition) } : {}
  }));
}
async function domainUpdateStoryboardTimeline(args) {
  return runtimeMutation(args, (runtime) => runtime.updateStoryboardTimeline({
    targetPath: requiredString2(args.targetPath ?? args.target_path, "targetPath"),
    ...optionalRecord(args.timeline) ? { timeline: optionalRecord(args.timeline) } : {}
  }));
}
async function domainAppendCandidate(args) {
  return runtimeMutation(args, (runtime) => runtime.appendCandidate({
    targetPath: requiredString2(args.targetPath ?? args.target_path, "targetPath"),
    targetKind: requiredTargetKind(args),
    payload: requiredRecord(args.payload, "payload"),
    ...args.lock !== void 0 ? { lock: args.lock } : {},
    ...stringValue5(args.nonce) ? { nonce: stringValue5(args.nonce) } : {}
  }));
}
async function domainCreateContentCandidate(args) {
  const status = contentCandidateStatus(args.status);
  const contentUnitId = requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
  const result = await runtimeMutation(args, (runtime) => runtime.createContentCandidate({
    contentUnitId,
    ...args.candidateId !== void 0 || args.candidate_id !== void 0 ? { candidateId: idValue3(args.candidateId ?? args.candidate_id) } : {},
    ...stringValue5(args.source) ? { source: stringValue5(args.source) } : {},
    ...status ? { status } : {},
    ...optionalRecord(args.producer) ? { producer: optionalRecord(args.producer) } : {},
    outputs: requiredArray(args.outputs, "outputs").filter(isRecord4),
    ...optionalRecord(args.promptSnapshot ?? args.prompt_snapshot) ? { promptSnapshot: optionalRecord(args.promptSnapshot ?? args.prompt_snapshot) } : {}
  }));
  const visibility = await withContentUnitCandidateVisibility(args, contentUnitId, result, {
    candidate_created: true,
    generation_mode: "content_unit_candidate",
    will_auto_select: false,
    requires_user_adoption: true
  });
  return {
    ...visibility,
    surface: createContentCandidatesSurface(args, {
      contentUnitId,
      ...candidateIdFromArgs(args) ? { candidateId: candidateIdFromArgs(args) } : {},
      ...firstOutputResourceId(args.outputs) !== void 0 ? { resourceId: firstOutputResourceId(args.outputs) } : {},
      projectId: projectIdFromArgs(args)
    })
  };
}
async function domainCreateContentCandidateBatch(args) {
  return runDomainBatch(args, domainCreateContentCandidate);
}
async function domainRegisterRawResourceAsContentUnitCandidate(args) {
  const resourceId = requiredResourceId5(args.resourceId ?? args.resource_id);
  const outputKind = contentCandidateOutputKind(args.outputKind ?? args.output_kind ?? args.kind);
  const contentUnitId = requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
  const candidateId = args.candidateId ?? args.candidate_id ?? `raw_${outputKind}_${resourceId}`;
  return domainCreateContentCandidate({
    ...args,
    contentUnitId,
    content_unit_id: contentUnitId,
    candidateId,
    candidate_id: candidateId,
    source: stringValue5(args.source) ?? "manual",
    status: args.status ?? "imported",
    producer: optionalRecord(args.producer) ?? { kind: "raw_resource_registration" },
    outputs: [{
      kind: outputKind,
      resource_id: resourceId,
      ...stringValue5(args.mimeType ?? args.mime_type) ? { mime_type: stringValue5(args.mimeType ?? args.mime_type) } : {},
      ...numberValue5(args.width) !== void 0 ? { width: numberValue5(args.width) } : {},
      ...numberValue5(args.height) !== void 0 ? { height: numberValue5(args.height) } : {},
      ...numberValue5(args.durationSec ?? args.duration_sec) !== void 0 ? { duration_sec: numberValue5(args.durationSec ?? args.duration_sec) } : {},
      ...optionalRecord(args.metadata) ? { metadata: optionalRecord(args.metadata) } : {}
    }]
  });
}
async function domainCreateAssetSlotCandidate(args) {
  return runtimeMutation(args, (runtime) => runtime.createAssetSlotCandidate({
    payload: requiredRecord(args.payload, "payload"),
    ...optionalRecord(args.targetRecord ?? args.target_record) ? { targetRecord: optionalRecord(args.targetRecord ?? args.target_record) } : {},
    ...stringValue5(args.nonce) ? { nonce: stringValue5(args.nonce) } : {}
  }));
}
async function domainCreateKeyframeCandidate(args) {
  return runtimeMutation(args, (runtime) => runtime.createKeyframeCandidate({
    payload: requiredRecord(args.payload, "payload"),
    ...optionalRecord(args.targetRecord ?? args.target_record) ? { targetRecord: optionalRecord(args.targetRecord ?? args.target_record) } : {},
    ...stringValue5(args.nonce) ? { nonce: stringValue5(args.nonce) } : {}
  }));
}
async function domainSelectContentUnitCandidate(args) {
  const contentUnitId = requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
  const result = await runtimeMutation(args, (runtime) => runtime.selectContentUnitCandidate({
    contentUnitId,
    candidateId: requiredId2(args.candidateId ?? args.candidate_id, "candidateId"),
    ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId5(args.resourceId ?? args.resource_id) } : {},
    ...stringValue5(args.stalePolicy ?? args.stale_policy) ? { stalePolicy: stringValue5(args.stalePolicy ?? args.stale_policy) } : {},
    ...stringValue5(args.reason) ? { reason: stringValue5(args.reason) } : {}
  }));
  const visibility = await withContentUnitCandidateVisibility(args, contentUnitId, result, {
    adoption: "selection",
    requires_user_adoption: false
  });
  return {
    ...visibility,
    surface: createContentCandidatesSurface(args, {
      contentUnitId,
      candidateId: String(requiredId2(args.candidateId ?? args.candidate_id, "candidateId")),
      ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId5(args.resourceId ?? args.resource_id) } : {},
      projectId: projectIdFromArgs(args)
    }),
    secondary_surfaces: [
      createImpactSurface(args, {
        projectId: projectIdFromArgs(args),
        target: String(contentUnitId),
        source: "domain_select_content_unit_candidate"
      })
    ]
  };
}
async function domainSelectContentUnitCandidateBatch(args) {
  return runDomainBatch(args, domainSelectContentUnitCandidate);
}
async function domainDecideContentUnitCandidate(args) {
  const contentUnitId = requiredId2(args.contentUnitId ?? args.content_unit_id, "contentUnitId");
  const decision = requiredDecision(args.decision);
  const result = await runtimeMutation(args, (runtime) => runtime.decideContentUnitCandidate({
    contentUnitId,
    candidateId: requiredId2(args.candidateId ?? args.candidate_id, "candidateId"),
    decision,
    ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId5(args.resourceId ?? args.resource_id) } : {},
    ...stringValue5(args.stalePolicy ?? args.stale_policy) ? { stalePolicy: stringValue5(args.stalePolicy ?? args.stale_policy) } : {},
    ...stringValue5(args.reason) ? { reason: stringValue5(args.reason) } : {},
    ...stringValue5(args.decidedAt ?? args.decided_at) ? { decidedAt: stringValue5(args.decidedAt ?? args.decided_at) } : {},
    ...optionalRecord(args.metadata) ? { metadata: optionalRecord(args.metadata) } : {}
  }));
  const visibility = await withContentUnitCandidateVisibility(args, contentUnitId, result, {
    adoption: decision,
    requires_user_adoption: decision !== "adopt"
  });
  return {
    ...visibility,
    surface: createContentCandidatesSurface(args, {
      contentUnitId,
      candidateId: String(requiredId2(args.candidateId ?? args.candidate_id, "candidateId")),
      ...args.resourceId !== void 0 || args.resource_id !== void 0 ? { resourceId: requiredResourceId5(args.resourceId ?? args.resource_id) } : {},
      projectId: projectIdFromArgs(args)
    }),
    ...decision === "adopt" ? {
      secondary_surfaces: [
        createImpactSurface(args, {
          projectId: projectIdFromArgs(args),
          target: String(contentUnitId),
          source: "domain_decide_content_unit_candidate"
        })
      ]
    } : {}
  };
}
async function domainSelectCandidate(args) {
  return runtimeMutation(args, (runtime) => runtime.selectCandidate({
    targetPath: requiredString2(args.targetPath ?? args.target_path, "targetPath"),
    targetKind: requiredTargetKind(args),
    candidateId: requiredString2(args.candidateId ?? args.candidate_id, "candidateId"),
    ...stringValue5(args.reason) ? { reason: stringValue5(args.reason) } : {}
  }));
}
async function domainUpdateCandidate(args) {
  return runtimeMutation(args, (runtime) => runtime.updateCandidate({
    targetPath: requiredString2(args.targetPath ?? args.target_path, "targetPath"),
    targetKind: requiredTargetKind(args),
    candidateId: requiredString2(args.candidateId ?? args.candidate_id, "candidateId"),
    payload: requiredRecord(args.payload, "payload")
  }));
}
async function domainUnlockCandidate(args) {
  return runtimeMutation(args, (runtime) => runtime.unlockCandidate({
    targetPath: requiredString2(args.targetPath ?? args.target_path, "targetPath"),
    targetKind: requiredTargetKind(args)
  }));
}
async function domainDeleteEntity(args) {
  await runtimeMutation(args, (runtime) => runtime.deleteEntity({
    entity: optionalRecord(args.entity),
    record: optionalRecord(args.record) ?? {}
  }));
  return { status: "deleted" };
}
async function domainReview(args) {
  return service(args).reviewWorkspace(inspectInput(args));
}
async function domainInspect(args) {
  return service(args).inspectWorkspace(inspectInput(args));
}
async function domainOverview(args) {
  return service(args).overviewWorkspace();
}
async function domainReadProductionWorkPlan(args) {
  return service(args).productionWorkPlan();
}
async function domainProductionStatusSummary(args) {
  const snapshot = await service(args).loadContentWorkspaceSnapshot();
  const requestedProductionId = args.productionId ?? args.production_id;
  const productionIds = requestedProductionId !== void 0 ? [String(requiredId2(requestedProductionId, "productionId"))] : snapshot.productions.map((item) => String(idValue3(item.id ?? item.record.id ?? item.record.ID ?? item.path)));
  const contentUnitSummaries = contentSourceWorkspaceContentUnitStatusSummaries(snapshot);
  const editingByProduction = new Map((snapshot.editingTimelines ?? []).filter((item) => item.targetKind === "production").map((item) => [String(item.targetId), item]));
  const projectTimelineStatus = buildContentSourceWorkspaceProjectTimelineStatus(snapshot, contentUnitSummaries);
  return {
    schema: "movscript.production_status_summary.v1",
    status: "ok",
    legacy_alias: true,
    preferred_schema: "movscript.project_timeline_status.v1",
    namespace_vocabulary: projectTimelineStatus.namespace_vocabulary,
    project_timeline_status: projectTimelineStatus,
    timeline_namespaces: projectTimelineStatus.timeline_namespaces,
    timeline_assemblies: projectTimelineStatus.timeline_assemblies,
    production_count: productionIds.length,
    productions: productionIds.map((productionId) => {
      const production = snapshot.productions.find((item) => sameId6(item.id ?? item.record.id ?? item.record.ID, productionId));
      const editing = editingByProduction.get(productionId);
      return {
        production_id: productionId,
        title: stringValue5(production?.record.title),
        path: production?.path,
        prerequisites: {
          settings_count: snapshot.settings.length,
          setting_states_count: snapshot.settingStates.length,
          assets_count: snapshot.assets.length
        },
        storyboards: snapshot.storyboards.map((item) => entityStatusLine(item)),
        keyframes: snapshot.keyframes.map((item) => entityStatusLine(item)),
        content_units: contentUnitSummaries,
        shots_videos: contentUnitSummaries.filter((item) => item.output_kind === "video" || item.content_unit_type === "scene_moment_ref" || item.content_unit_type === "scence_moment_ref"),
        job_status: "not_tracked_in_domain_summary",
        blocking_refs: Array.isArray(editing?.blockers) ? editing.blockers : [],
        stale_status: contentUnitSummaries.some((item) => item.stale_status === "stale") ? "has_stale_selection" : "ok"
      };
    }),
    surface: createProjectStatusSurface(args, {
      projectId: projectIdFromArgs(args),
      ...productionIds.length === 1 ? { productionId: productionIds[0] } : {}
    })
  };
}
async function domainInterpret(args) {
  return runtimeMutation(args, (runtime) => runtime.interpretWorkspace());
}
async function domainRegenerationPlan(args) {
  const result = await service(args).regenerationPlan();
  return isRecord4(result) ? {
    ...result,
    surface: createImpactSurface(args, {
      projectId: projectIdFromArgs(args),
      target: stringValue5(args.target ?? args.contentUnitId ?? args.content_unit_id),
      source: "domain_regeneration_plan"
    })
  } : result;
}
function service(args) {
  return createMovScriptDomainRuntime(resolveMCPProjectWorkspaceLocator(args));
}
function resolveAssetRefSelection(index, asset) {
  const assetRef3 = asset.id ?? lastPathSegment8(asset.path);
  const contentUnit = index.entities.find((entity) => entity.entityKind === "content_unit" && entity.record.content_unit_type === "asset_ref" && sameEntityRef(entity.record.asset_ref, assetRef3, "asset"));
  if (!contentUnit) return {};
  const contentUnitPath = contentUnit.path.replace(/\/content_unit\.json$/, "");
  const selection = index.documents.find((document) => {
    if (!isRecord4(document.data)) return false;
    return document.data.schema === "movscript.decision_context.v1" && document.data.target_kind === "content_unit" && document.data.target_ref === contentUnitPath;
  })?.data;
  const selectionRecord2 = isRecord4(selection) && isRecord4(selection.selection) ? selection.selection : void 0;
  return {
    contentUnitId: contentUnit.id,
    contentUnitPath,
    candidateId: idFromUnknown(selectionRecord2?.candidate_id ?? selectionRecord2?.candidateId),
    resourceId: resourceIdFromUnknown(selectionRecord2?.resource_id ?? selectionRecord2?.resourceId)
  };
}
async function patchAssetProviderCertification(projectDir, assetPath2, fallbackRecord, provider, certification) {
  const fileRepository = createNodeMovScriptWorkspaceFileRepository(projectDir);
  const path = normalizeWorkspacePath(assetPath2);
  const current = await fileRepository.read({ path }).then((file) => JSON.parse(file.content)).catch(() => fallbackRecord);
  if (!isRecord4(current)) throw new Error(`asset source is not a JSON object: ${path}`);
  const providerCertifications = isRecord4(current.provider_certifications) ? { ...current.provider_certifications } : {};
  providerCertifications[providerCertificationStorageKey(provider, certification)] = certification;
  const next = {
    ...current,
    provider_certifications: providerCertifications
  };
  await fileRepository.write({ path, content: `${JSON.stringify(next, null, 2)}
` });
  return {
    path,
    certification
  };
}
function providerCertificationStorageKey(provider, certification) {
  const model = getOptionalCertificationString(certification.model ?? certification.public_model_id ?? certification.publicModelId ?? certification.provider_model_id ?? certification.providerModelId);
  return model ? `${provider}::model:${model}` : provider;
}
function providerCertificationProvider(value) {
  const provider = getOptionalCertificationString(value) ?? "volcengine_ark_official";
  const normalized = provider.toLowerCase();
  if (normalized === "jimeng" || normalized === "jimeng2" || normalized === "seedance" || normalized === "seedance2") return "volcengine_ark_official";
  if (normalized === "yunwu" || normalized === "yunwu_gateway") return "yunwu_gateway";
  return provider;
}
function getOptionalCertificationString(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function idFromUnknown(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  return void 0;
}
function resourceIdFromUnknown(value) {
  const number = numberValue5(value);
  return number !== void 0 && Number.isInteger(number) && number > 0 ? number : void 0;
}
async function runtimeMutation(args, action) {
  const locator = await requireMCPBackendBoundProject(args);
  try {
    return await action(createMovScriptDomainRuntime(locator));
  } finally {
    invalidateMovScriptDomainRuntime(locator);
  }
}
async function runDomainBatch(args, action) {
  const items = requiredArray(args.items, "items");
  if (items.length === 0) throw new Error("items must contain at least one item");
  const continueOnError = booleanValue(args.continueOnError ?? args.continue_on_error) ?? true;
  const defaults = batchDefaults(args, /* @__PURE__ */ new Set(["items", "continueOnError", "continue_on_error"]));
  const results = [];
  for (let index = 0; index < items.length; index += 1) {
    const item = items[index];
    if (!isRecord4(item)) {
      results.push({ index, status: "error", error: `items[${index}] must be an object` });
      if (!continueOnError) break;
      continue;
    }
    try {
      results.push({ index, status: "ok", result: await action({ ...defaults, ...item }) });
    } catch (error) {
      results.push({ index, status: "error", error: errorMessage4(error) });
      if (!continueOnError) break;
    }
  }
  for (let index = results.length; index < items.length; index += 1) {
    results.push({
      index,
      status: "skipped",
      error: "Skipped because an earlier item failed and continue_on_error is false."
    });
  }
  const successCount = results.filter((item) => item.status === "ok").length;
  const failedCount = results.filter((item) => item.status === "error").length;
  return {
    status: failedCount === 0 ? "completed" : successCount > 0 ? "partial_error" : "error",
    total: items.length,
    success_count: successCount,
    failed_count: failedCount,
    items: results,
    message: `${successCount}/${items.length} domain batch item(s) completed.`
  };
}
function batchDefaults(args, excluded) {
  const defaults = {};
  for (const [key, value] of Object.entries(args)) {
    if (!excluded.has(key) && value !== void 0) defaults[key] = value;
  }
  return defaults;
}
function contextIds(args) {
  return {
    ...args.productionId !== void 0 || args.production_id !== void 0 ? { productionId: idValue3(args.productionId ?? args.production_id) } : {},
    ...args.segmentId !== void 0 || args.segment_id !== void 0 ? { segmentId: idValue3(args.segmentId ?? args.segment_id) } : {},
    ...args.sceneMomentId !== void 0 || args.scene_moment_id !== void 0 ? { sceneMomentId: idValue3(args.sceneMomentId ?? args.scene_moment_id) } : {},
    ...args.storyboardId !== void 0 || args.storyboard_id !== void 0 ? { storyboardId: idValue3(args.storyboardId ?? args.storyboard_id) } : {},
    ...args.contentUnitId !== void 0 || args.content_unit_id !== void 0 ? { contentUnitId: idValue3(args.contentUnitId ?? args.content_unit_id) } : {},
    ...args.settingId !== void 0 || args.setting_id !== void 0 ? { settingId: idValue3(args.settingId ?? args.setting_id) } : {},
    ...args.settingStateId !== void 0 || args.setting_state_id !== void 0 ? { settingStateId: idValue3(args.settingStateId ?? args.setting_state_id) } : {}
  };
}
function productionIdFrom(args, production) {
  return idValue3(args.productionId ?? args.production_id ?? production?.id ?? production?.client_id ?? "main");
}
function requiredProductionScopeId(args, field) {
  if (args.productionId !== void 0 || args.production_id !== void 0) {
    return requiredId2(args.productionId ?? args.production_id, field);
  }
  const focus = normalizeDomainFocus(args);
  if (focus.scope?.kind === "production" && focus.scope.ref) return focus.scope.ref;
  throw new Error(`Missing required argument: ${field}`);
}
function productionWriteResult(entityKind, ids, result) {
  return {
    status: "upserted",
    entityKind,
    ...ids,
    productionPath: result.productionPath,
    writtenPaths: result.writtenPaths,
    snapshot: result.snapshot
  };
}
function timelineNamespaceTreeItems(args) {
  const arrayValue22 = args.namespaces ?? args.timeline_namespaces ?? args.timelineNamespaces ?? args.nodes;
  const arrayItems = arrayRecords2(arrayValue22);
  if (arrayItems.length > 0) return arrayItems;
  const root = optionalRecord(args.namespace ?? args.root ?? args.tree ?? args.payload ?? args.record);
  return root ? [root] : [];
}
function timelineNamespaceEntityKind(payload, context) {
  const explicit = stringValue5(payload.entity_kind ?? payload.entityKind ?? payload.source_kind ?? payload.sourceKind);
  if (explicit === "production" || explicit === "segment") return explicit;
  const targetPath = stringValue5(payload.target_path ?? payload.targetPath);
  if (targetPath?.endsWith("/production.json")) return "production";
  if (targetPath?.endsWith("/segment.json")) return "segment";
  return context.depth === 0 ? "production" : "segment";
}
function timelineNamespaceTargetPath(item, payload, context, entityKind, namespaceId) {
  const explicit = stringValue5(payload.target_path ?? payload.targetPath ?? item.target_path ?? item.targetPath);
  if (explicit) return explicit.replace(/^\/+/, "");
  const idToken = pathToken(namespaceId);
  if (entityKind === "production" || !context.parentTargetPath) {
    return entityKind === "production" ? `timeline/${idToken}/production.json` : `timeline/${idToken}/segment.json`;
  }
  return `${timelineNamespaceNodeDir(context.parentTargetPath)}/segments/${idToken}/segment.json`;
}
function timelineNamespaceNodeDir(targetPath) {
  return targetPath.replace(/\/(?:production|segment)\.json$/, "");
}
function normalizedSourcePath(value) {
  return value.trim().replace(/^\/+/, "").replace(/\/+$/, "");
}
function sourceNodeDir(path) {
  const normalized = normalizedSourcePath(path);
  const namespaceDir = timelineNamespaceNodeDir(normalized);
  if (namespaceDir !== normalized) return namespaceDir;
  return normalized.endsWith(".json") ? normalized.replace(/\/[^/]+\.json$/, "") : normalized;
}
function timelineNamespaceRecord(payload, input) {
  const sanitized = stripNamespaceRecordFields(payload);
  return pruneUndefinedRecord2({
    ...sanitized,
    schema: `movscript.${input.entityKind}.v1`,
    kind: input.entityKind,
    id: input.namespaceId,
    title: stringValue5(payload.title),
    order: numberValue5(payload.order),
    intent: stringValue5(payload.intent ?? payload.summary ?? payload.description),
    namespace_kind: input.namespaceKind,
    timeline_namespace_kind: input.namespaceKind
  });
}
function timelineNamespaceChildItems(item, payload) {
  return arrayRecords2(
    item.children ?? item.namespaces ?? item.timeline_namespaces ?? item.timelineNamespaces ?? item.segments ?? payload.children ?? payload.namespaces ?? payload.timeline_namespaces ?? payload.timelineNamespaces ?? payload.segments
  );
}
function timelineNamespaceSceneMomentItems(item, payload) {
  return arrayRecords2(item.scene_moments ?? item.sceneMoments ?? payload.scene_moments ?? payload.sceneMoments);
}
function sceneMomentPathTarget(args, record, sceneMomentId) {
  const explicit = stringValue5(args.targetPath ?? args.target_path ?? record.targetPath ?? record.target_path);
  if (explicit) return normalizedSourcePath(explicit);
  const namespacePath = stringValue5(
    args.namespacePath ?? args.namespace_path ?? args.timelineNamespacePath ?? args.timeline_namespace_path ?? args.parentPath ?? args.parent_path ?? record.namespacePath ?? record.namespace_path ?? record.timelineNamespacePath ?? record.timeline_namespace_path ?? record.parentPath ?? record.parent_path
  );
  if (!namespacePath) return void 0;
  return `${sourceNodeDir(namespacePath)}/scene_moments/${pathToken(sceneMomentId)}/scene_moment.json`;
}
function sceneMomentHierarchyRecord(payload, sceneMomentId) {
  return pruneUndefinedRecord2({
    ...timelinePrimitiveRecord(payload, TIMELINE_PRIMITIVE_SPECS.scene_moment, sceneMomentId),
    storyboard_id: optionalId(payload.storyboard_id ?? payload.storyboardId),
    time_text: stringValue5(payload.time_text ?? payload.timeText ?? payload.when),
    scene_code: stringValue5(payload.scene_code ?? payload.sceneCode),
    location_text: stringValue5(payload.location_text ?? payload.locationText ?? payload.where),
    condition_text: stringValue5(payload.condition_text ?? payload.conditionText),
    action_text: stringValue5(payload.action_text ?? payload.actionText ?? payload.action),
    mood: stringValue5(payload.mood ?? payload.emotion),
    description: stringValue5(payload.description),
    settings: Array.isArray(payload.settings) ? payload.settings : void 0,
    setting_refs: Array.isArray(payload.setting_refs ?? payload.settingRefs) ? payload.setting_refs ?? payload.settingRefs : void 0
  });
}
function timelinePrimitivePathTarget(args, record, input) {
  const explicit = stringValue5(args.targetPath ?? args.target_path ?? record.targetPath ?? record.target_path);
  if (explicit) return normalizedSourcePath(explicit);
  const parentPath = timelinePrimitiveParentPath(args, record, input.spec);
  if (!parentPath) return void 0;
  return `${sourceNodeDir(parentPath)}/${input.spec.collection}/${pathToken(input.id)}/${input.spec.filename}`;
}
function timelinePrimitiveParentPath(args, record, spec) {
  const explicitParent = stringValue5(args.parentPath ?? args.parent_path ?? record.parentPath ?? record.parent_path);
  if (explicitParent) return explicitParent;
  const expressionUnitPath = stringValue5(
    args.expressionUnitPath ?? args.expression_unit_path ?? record.expressionUnitPath ?? record.expression_unit_path
  );
  if ((spec.entityKind === "keyframe" || spec.entityKind === "storyboard") && expressionUnitPath) return expressionUnitPath;
  return stringValue5(
    args.sceneMomentPath ?? args.scene_moment_path ?? record.sceneMomentPath ?? record.scene_moment_path
  );
}
async function writeTimelinePrimitivePathTarget(args, spec, payload, id, targetPath) {
  const result = await runtimeMutation(args, (runtime) => runtime.writeHierarchyNode({
    targetPath,
    record: timelinePrimitiveRecord(payload, spec, id)
  }));
  return {
    status: "upserted",
    entityKind: spec.entityKind,
    [`${camelEntityKind2(spec.entityKind)}Id`]: id,
    targetPath,
    result
  };
}
async function upsertTimelinePrimitiveNode(runtime, item, input, collector) {
  const payload = treePayload(item, input.spec.payloadName);
  const primitiveId = requiredId2(payload.id ?? payload.client_id, `${input.spec.payloadName}.id`);
  const targetPath = timelinePrimitiveTargetPath(item, payload, input.parentDir, input.spec, primitiveId);
  const targetRef = timelinePrimitiveRef(targetPath, input.spec);
  const record = timelinePrimitiveRecord(payload, input.spec, primitiveId);
  const node = await runtime.writeHierarchyNode({ targetPath, record });
  const summary = {
    id: primitiveId,
    entity_kind: input.spec.entityKind,
    targetPath,
    targetRef,
    node
  };
  if (input.spec.entityKind === "scene_moment") {
    collector.sceneMoments.push(summary);
  } else {
    collector.primitives.push(summary);
  }
  const contentUnits = [];
  for (const contentUnitItem of arrayRecords2(item.content_units ?? item.contentUnits ?? payload.content_units ?? payload.contentUnits)) {
    const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
    const contentUnit = await runtime.createContentUnit(timelinePrimitiveContentUnitInput(contentUnitPayload, input.spec, targetRef));
    contentUnits.push(contentUnit);
    collector.contentUnits.push(contentUnit);
  }
  const parentDir = timelinePrimitiveNodeDir(targetPath, input.spec);
  const expressionUnits = input.spec.entityKind === "scene_moment" ? await upsertTimelinePrimitiveChildren(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS.expression_unit, collector) : [];
  const storyboards = timelinePrimitiveSupportsSceneChildren(input.spec) ? await upsertTimelinePrimitiveChildren(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS.storyboard, collector) : [];
  const keyframes = timelinePrimitiveSupportsSceneChildren(input.spec) ? await upsertTimelinePrimitiveChildren(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS.keyframe, collector) : [];
  const audioCues = timelinePrimitiveSupportsSceneChildren(input.spec) ? await upsertTimelinePrimitiveChildren(runtime, item, payload, parentDir, TIMELINE_PRIMITIVE_SPECS.audio_cue, collector) : [];
  return {
    ...summary,
    contentUnits,
    ...expressionUnits.length > 0 ? { expressionUnits } : {},
    ...storyboards.length > 0 ? { storyboards } : {},
    ...keyframes.length > 0 ? { keyframes } : {},
    ...audioCues.length > 0 ? { audioCues } : {}
  };
}
async function upsertTimelinePrimitiveChildren(runtime, item, payload, parentDir, spec, collector) {
  const children = [];
  for (const child of timelinePrimitiveChildItems(item, payload, spec)) {
    children.push(await upsertTimelinePrimitiveNode(runtime, child, { spec, parentDir }, collector));
  }
  return children;
}
function timelinePrimitiveChildItems(item, payload, spec) {
  const camel = camelCollectionName(spec.collection);
  return arrayRecords2(item[spec.collection] ?? item[camel] ?? payload[spec.collection] ?? payload[camel]);
}
function timelinePrimitiveSupportsSceneChildren(spec) {
  return spec.entityKind === "scene_moment" || spec.entityKind === "expression_unit";
}
function timelinePrimitiveTargetPath(item, payload, parentDir, spec, primitiveId) {
  const explicit = stringValue5(payload.target_path ?? payload.targetPath ?? item.target_path ?? item.targetPath);
  if (explicit) return explicit.replace(/^\/+/, "");
  return `${parentDir}/${spec.collection}/${pathToken(primitiveId)}/${spec.filename}`;
}
function timelinePrimitiveRef(targetPath, spec) {
  return targetPath.replace(new RegExp(`/${escapeRegExp2(spec.filename)}$`), "");
}
function timelinePrimitiveNodeDir(targetPath, spec) {
  return timelinePrimitiveRef(targetPath, spec);
}
function timelinePrimitiveRecord(payload, spec, primitiveId) {
  const sanitized = stripTimelinePrimitiveRecordFields(payload);
  return pruneUndefinedRecord2({
    ...sanitized,
    schema: `movscript.${spec.entityKind}.v1`,
    kind: spec.entityKind,
    id: primitiveId,
    title: stringValue5(payload.title),
    order: numberValue5(payload.order),
    ...spec.entityKind === "scene_moment" ? {
      storyboard_id: optionalId(payload.storyboard_id ?? payload.storyboardId),
      time_text: stringValue5(payload.time_text ?? payload.timeText ?? payload.when),
      scene_code: stringValue5(payload.scene_code ?? payload.sceneCode),
      location_text: stringValue5(payload.location_text ?? payload.locationText ?? payload.where),
      condition_text: stringValue5(payload.condition_text ?? payload.conditionText),
      action_text: stringValue5(payload.action_text ?? payload.actionText ?? payload.action),
      mood: stringValue5(payload.mood ?? payload.emotion),
      description: stringValue5(payload.description)
    } : {},
    ...spec.entityKind === "expression_unit" ? {
      slot_kind: stringValue5(payload.slot_kind ?? payload.slotKind),
      expression_kind: stringValue5(payload.expression_kind ?? payload.expressionKind ?? nonSystemKind(payload.kind, spec.entityKind)),
      visual_kind: stringValue5(payload.visual_kind ?? payload.visualKind),
      speaker_ref: optionalId(payload.speaker_ref ?? payload.speakerRef),
      source_expression_ref: optionalId(payload.source_expression_ref ?? payload.sourceExpressionRef),
      text: stringValue5(payload.text ?? payload.content),
      note: stringValue5(payload.note),
      intent: stringValue5(payload.intent ?? payload.summary ?? payload.description),
      content: optionalRecord(payload.content),
      timing_intent: optionalRecord(payload.timing_intent ?? payload.timingIntent),
      voice_profile_ref: optionalId(payload.voice_profile_ref ?? payload.voiceProfileRef),
      span: optionalRecord(payload.span),
      script_block_id: optionalId(payload.script_block_id ?? payload.scriptBlockId)
    } : {},
    ...spec.entityKind === "storyboard" ? {
      visual_intent: stringValue5(payload.visual_intent ?? payload.visualIntent ?? payload.prompt_hint ?? payload.promptHint ?? payload.description),
      timeline: optionalRecord(payload.timeline),
      graph: optionalRecord(payload.graph)
    } : {},
    ...spec.entityKind === "keyframe" ? {
      role: stringValue5(payload.role ?? payload.status),
      visual_intent: stringValue5(payload.visual_intent ?? payload.visualIntent ?? payload.prompt_hint ?? payload.promptHint ?? payload.description),
      timing: optionalRecord(payload.timing),
      composition: optionalRecord(payload.composition),
      continuity: optionalRecord(payload.continuity),
      reference_asset_refs: Array.isArray(payload.reference_asset_refs ?? payload.referenceAssetRefs) ? payload.reference_asset_refs ?? payload.referenceAssetRefs : void 0,
      reference_keyframe_refs: Array.isArray(payload.reference_keyframe_refs ?? payload.referenceKeyframeRefs) ? payload.reference_keyframe_refs ?? payload.referenceKeyframeRefs : void 0
    } : {},
    ...spec.entityKind === "audio_cue" ? {
      cue_kind: stringValue5(payload.cue_kind ?? payload.cueKind ?? nonSystemKind(payload.kind, spec.entityKind)),
      storyboard_id: optionalId(payload.storyboard_id ?? payload.storyboardId),
      expression_unit_ref: optionalId(payload.expression_unit_ref ?? payload.expressionUnitRef ?? payload.expression_unit_id ?? payload.expressionUnitId),
      prompt_hint: stringValue5(payload.prompt_hint ?? payload.promptHint),
      asset_refs: Array.isArray(payload.asset_refs ?? payload.assetRefs) ? payload.asset_refs ?? payload.assetRefs : void 0
    } : {}
  });
}
function timelinePrimitiveContentUnitInput(record, spec, targetRef) {
  const input = treeContentUnitInput(record, {
    contentUnitType: spec.contentUnitType,
    outputKind: spec.outputKind,
    targetKind: spec.targetKind,
    targetRef,
    [spec.refField]: targetRef
  });
  if (input.productionId !== void 0 || input.segmentId !== void 0) {
    throw new Error("timeline namespace primitive content_units must not use production_ref, production_id, segment_ref, or segment_id");
  }
  if (input.contentUnitType === "production_ref" || input.contentUnitType === "segment_ref") {
    throw new Error("timeline namespace primitive content_units must not use content_unit_type=production_ref or segment_ref");
  }
  return {
    ...input,
    productionId: void 0,
    segmentId: void 0
  };
}
function stripTimelinePrimitiveRecordFields(record) {
  const forbidden = /* @__PURE__ */ new Set([
    "content_units",
    "contentUnits",
    "content_unit_type",
    "contentUnitType",
    "expression_units",
    "expressionUnits",
    "storyboards",
    "keyframes",
    "audio_cues",
    "audioCues",
    "target_path",
    "targetPath",
    "namespace_path",
    "namespacePath",
    "timeline_namespace_path",
    "timelineNamespacePath",
    "scene_moment_path",
    "sceneMomentPath",
    "expression_unit_path",
    "expressionUnitPath",
    "parent_path",
    "parentPath",
    "target_category",
    "targetCategory",
    "target_kind",
    "targetKind",
    "target_ref",
    "targetRef",
    "output_kind",
    "outputKind",
    "production_ref",
    "productionRef",
    "production_id",
    "productionId",
    "segment_ref",
    "segmentRef",
    "segment_id",
    "segmentId",
    "content_unit_ref",
    "contentUnitRef",
    "content_unit_refs",
    "contentUnitRefs",
    "content_unit_id",
    "contentUnitId",
    "main_content_unit_id",
    "mainContentUnitId",
    "candidate",
    "candidates",
    "selection",
    "selections",
    "selected_candidate_id",
    "selectedCandidateId",
    "selected_resource_id",
    "selectedResourceId",
    "resource_id",
    "resourceId",
    "client_id",
    "clientId",
    "storyboardId",
    "expression_unit_id",
    "expressionUnitId",
    "expression_unit_ref",
    "expressionUnitRef",
    "timeText",
    "when",
    "sceneCode",
    "locationText",
    "where",
    "conditionText",
    "actionText",
    "action",
    "emotion",
    "expressionKind",
    "visualKind",
    "speakerRef",
    "sourceExpressionRef",
    "timingIntent",
    "voiceProfileRef",
    "scriptBlockId",
    "cueKind",
    "visualIntent",
    "promptHint",
    "referenceAssetRefs",
    "referenceKeyframeRefs",
    "assetRefs"
  ]);
  return Object.fromEntries(Object.entries(record).filter(([key, value]) => !forbidden.has(key) && value !== void 0));
}
function nonSystemKind(value, entityKind) {
  const kind = stringValue5(value);
  return kind && kind !== entityKind ? kind : void 0;
}
function camelCollectionName(collection) {
  return collection.replace(/_([a-z])/g, (_, char) => char.toUpperCase());
}
function camelEntityKind2(entityKind) {
  return entityKind.replace(/_([a-z])/g, (_, char) => char.toUpperCase());
}
function escapeRegExp2(value) {
  return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function timelineNamespaceAssemblyContentUnitInput(record, scope) {
  const explicit = engineContentUnitInputFromRecord(record);
  if (explicit.contentUnitType && explicit.contentUnitType !== "timeline_assembly_ref") {
    throw new Error("timeline namespace content_units must use content_unit_type=timeline_assembly_ref");
  }
  if (explicit.targetKind && explicit.targetKind !== "timeline_assembly") {
    throw new Error("timeline namespace content_units must use target_kind=timeline_assembly");
  }
  if (explicit.productionId !== void 0 || explicit.segmentId !== void 0) {
    throw new Error("timeline namespace content_units must not use production_ref, production_id, segment_ref, or segment_id");
  }
  const targetRef = explicit.targetRef ?? `timeline_assembly:${scope.namespaceKind}:${scope.namespaceId}`;
  return pruneUndefinedRecord2({
    ...explicit,
    contentUnitType: "timeline_assembly_ref",
    outputKind: explicit.outputKind ?? "video",
    targetCategory: "timeline_assembly",
    targetKind: "timeline_assembly",
    targetRef,
    scopeKind: explicit.scopeKind ?? scope.namespaceKind,
    scopeRef: explicit.scopeRef ?? scope.namespaceId,
    productionId: void 0,
    segmentId: void 0
  });
}
function stripNamespaceRecordFields(record) {
  const forbidden = /* @__PURE__ */ new Set([
    "children",
    "namespaces",
    "timeline_namespaces",
    "timelineNamespaces",
    "segments",
    "content_units",
    "contentUnits",
    "content_unit_ref",
    "contentUnitRef",
    "content_unit_refs",
    "contentUnitRefs",
    "content_unit_id",
    "contentUnitId",
    "main_content_unit_id",
    "mainContentUnitId",
    "content_unit_type",
    "contentUnitType",
    "target_path",
    "targetPath",
    "target_category",
    "targetCategory",
    "target_kind",
    "targetKind",
    "target_ref",
    "targetRef",
    "output_kind",
    "outputKind",
    "production_ref",
    "productionRef",
    "production_id",
    "productionId",
    "segment_ref",
    "segmentRef",
    "segment_id",
    "segmentId",
    "scene_moment_ref",
    "sceneMomentRef",
    "asset_ref",
    "assetRef",
    "client_id",
    "clientId",
    "namespaceKind",
    "timelineNamespaceKind",
    "domain_kind",
    "domainKind",
    "entity_kind",
    "entityKind",
    "source_kind",
    "sourceKind",
    "candidate",
    "candidates",
    "selection",
    "selections",
    "selected_candidate_id",
    "selectedCandidateId",
    "selected_resource_id",
    "selectedResourceId",
    "resource_id",
    "resourceId"
  ]);
  return Object.fromEntries(Object.entries(record).filter(([key, value]) => !forbidden.has(key) && value !== void 0));
}
function pathToken(value) {
  const token = String(value).trim().replace(/[\\/#?]+/g, "_").replace(/\s+/g, "_");
  return token || "node";
}
function treePayload(item, name) {
  return requiredRecord(item[name] ?? item.payload ?? item.record ?? item.entity ?? item, name);
}
function arrayRecords2(value) {
  return Array.isArray(value) ? value.filter(isRecord4) : [];
}
function treeContentUnitInput(record, defaults) {
  const explicitInput = engineContentUnitInputFromRecord(record);
  if (isExplicitTimelineAssemblyContentUnit(explicitInput)) {
    return {
      ...defaults,
      ...explicitInput,
      productionId: explicitInput.productionId,
      segmentId: explicitInput.segmentId,
      sceneMomentId: explicitInput.sceneMomentId,
      expressionUnitId: explicitInput.expressionUnitId,
      storyboardId: explicitInput.storyboardId,
      keyframeId: explicitInput.keyframeId,
      audioCueId: explicitInput.audioCueId,
      assetRef: explicitInput.assetRef
    };
  }
  return {
    ...defaults,
    ...explicitInput
  };
}
function isExplicitTimelineAssemblyContentUnit(input) {
  return input.contentUnitType === "timeline_assembly_ref" || input.targetCategory === "timeline_assembly" || input.targetKind === "timeline_assembly";
}
async function upsertTreeStoryboards(runtime, parent, context) {
  const output = [];
  for (const item of arrayRecords2(parent.storyboards)) {
    const storyboard = treePayload(item, "storyboard");
    const storyboardId = optionalId(storyboard.id ?? storyboard.client_id) ?? "main";
    output.push(await runtime.createStoryboard({
      productionId: context.productionId,
      segmentId: requiredId2(context.segmentId, "segmentId"),
      sceneMomentId: requiredId2(context.sceneMomentId, "sceneMomentId"),
      ...context.expressionUnitId ? { expressionUnitId: context.expressionUnitId } : {},
      id: storyboardId,
      title: stringValue5(storyboard.title),
      visualIntent: stringValue5(storyboard.visual_intent ?? storyboard.visualIntent ?? storyboard.prompt_hint ?? storyboard.description),
      order: numberValue5(storyboard.order),
      timeline: optionalRecord(storyboard.timeline),
      graph: optionalRecord(storyboard.graph)
    }));
    for (const contentUnitItem of arrayRecords2(item.content_units ?? item.contentUnits)) {
      const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
      output.push(await runtime.createContentUnit(treeContentUnitInput(contentUnitPayload, {
        productionId: context.productionId,
        segmentId: context.segmentId,
        sceneMomentId: context.sceneMomentId,
        expressionUnitId: context.expressionUnitId,
        storyboardId,
        contentUnitType: "storyboard_ref",
        outputKind: "image",
        targetKind: "storyboard",
        targetRef: storyboardId
      })));
    }
  }
  return output;
}
async function upsertTreeKeyframes(runtime, parent, context) {
  const output = [];
  for (const item of arrayRecords2(parent.keyframes)) {
    const keyframe = normalizeKeyframePayload(treePayload(item, "keyframe"));
    const keyframeId = requiredId2(keyframe.id ?? keyframe.client_id, "keyframe.id");
    output.push(await runtime.createKeyframe({
      productionId: context.productionId,
      segmentId: requiredId2(context.segmentId, "segmentId"),
      sceneMomentId: requiredId2(context.sceneMomentId, "sceneMomentId"),
      ...context.expressionUnitId ? { expressionUnitId: context.expressionUnitId } : {},
      id: keyframeId,
      title: stringValue5(keyframe.title),
      role: stringValue5(keyframe.role ?? keyframe.status),
      visualIntent: stringValue5(keyframe.visual_intent ?? keyframe.visualIntent ?? keyframe.prompt_hint ?? keyframe.description),
      order: numberValue5(keyframe.order),
      timing: optionalRecord(keyframe.timing),
      composition: optionalRecord(keyframe.composition),
      continuity: optionalRecord(keyframe.continuity),
      referenceAssetRefs: Array.isArray(keyframe.reference_asset_refs) ? keyframe.reference_asset_refs : void 0,
      referenceKeyframeRefs: Array.isArray(keyframe.reference_keyframe_refs) ? keyframe.reference_keyframe_refs : void 0
    }));
    for (const contentUnitItem of arrayRecords2(item.content_units ?? item.contentUnits)) {
      const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
      output.push(await runtime.createContentUnit(treeContentUnitInput(contentUnitPayload, {
        productionId: context.productionId,
        segmentId: context.segmentId,
        sceneMomentId: context.sceneMomentId,
        expressionUnitId: context.expressionUnitId,
        keyframeId,
        contentUnitType: "keyframe_ref",
        outputKind: "image",
        targetKind: "keyframe",
        targetRef: keyframeId
      })));
    }
  }
  return output;
}
async function upsertTreeAudioCues(runtime, parent, context) {
  const output = [];
  for (const item of arrayRecords2(parent.audio_cues ?? parent.audioCues)) {
    const audioCue = normalizeAudioCuePayload(treePayload(item, "audio_cue"));
    const audioCueId = requiredId2(audioCue.id ?? audioCue.client_id, "audio_cue.id");
    output.push(await runtime.createAudioCue({
      productionId: context.productionId,
      segmentId: requiredId2(context.segmentId, "segmentId"),
      sceneMomentId: requiredId2(context.sceneMomentId, "sceneMomentId"),
      ...context.expressionUnitId ? { expressionUnitId: context.expressionUnitId } : {},
      id: audioCueId,
      title: stringValue5(audioCue.title),
      kind: stringValue5(audioCue.cue_kind ?? audioCue.kind),
      storyboardId: optionalId(audioCue.storyboard_id ?? audioCue.storyboardId),
      promptHint: stringValue5(audioCue.prompt_hint ?? audioCue.promptHint)
    }));
    for (const contentUnitItem of arrayRecords2(item.content_units ?? item.contentUnits)) {
      const contentUnitPayload = treePayload(contentUnitItem, "content_unit");
      output.push(await runtime.createContentUnit(treeContentUnitInput(contentUnitPayload, {
        productionId: context.productionId,
        segmentId: context.segmentId,
        sceneMomentId: context.sceneMomentId,
        expressionUnitId: context.expressionUnitId,
        audioCueId,
        contentUnitType: "expression_unit_ref",
        outputKind: "audio",
        targetKind: context.expressionUnitId ? "expression_unit" : "scene_moment",
        targetRef: context.expressionUnitId ?? context.sceneMomentId
      })));
    }
  }
  return output;
}
function normalizeKeyframePayload(record) {
  return pruneUndefinedRecord2({
    ...record,
    visual_intent: record.visual_intent ?? record.visualIntent,
    reference_asset_refs: record.reference_asset_refs ?? record.referenceAssetRefs,
    reference_keyframe_refs: record.reference_keyframe_refs ?? record.referenceKeyframeRefs
  });
}
function normalizeAudioCuePayload(record) {
  return pruneUndefinedRecord2({
    ...record,
    cue_kind: record.cue_kind ?? record.cueKind ?? record.kind,
    storyboard_id: record.storyboard_id ?? record.storyboardId,
    storyboard_ref: record.storyboard_ref ?? record.storyboardRef,
    prompt_hint: record.prompt_hint ?? record.promptHint,
    asset_refs: record.asset_refs ?? record.assetRefs
  });
}
function normalizeExpressionUnitPayload(record) {
  return pruneUndefinedRecord2({
    ...record,
    slot_kind: record.slot_kind ?? record.slotKind,
    kind: record.kind ?? record.expression_kind ?? record.expressionKind,
    script_block_id: record.script_block_id ?? record.scriptBlockId
  });
}
function pruneUndefinedRecord2(record) {
  return Object.fromEntries(Object.entries(record).filter(([, value]) => value !== void 0));
}
function engineContentUnitInputFromRecord(record) {
  const editPrompt = optionalRecord(record.edit_prompt ?? record.editPrompt);
  const modelIntent = optionalRecord(record.model_intent ?? record.modelIntent);
  return pruneUndefinedRecord2({
    id: record.id ?? record.ID ?? record.client_id,
    title: record.title,
    kind: record.kind,
    contentUnitType: record.content_unit_type ?? record.contentUnitType,
    outputKind: record.output_kind ?? record.outputKind,
    targetCategory: record.target_category ?? record.targetCategory,
    targetKind: record.target_kind ?? record.targetKind,
    targetRef: record.target_ref ?? record.targetRef,
    scopeKind: record.scope_kind ?? record.scopeKind,
    scopeRef: record.scope_ref ?? record.scopeRef,
    generationRole: record.generation_role ?? record.generationRole,
    assetRef: record.asset_ref ?? record.assetRef,
    productionId: record.production_ref ?? record.productionRef ?? record.productionId ?? record.production_id,
    segmentId: record.segment_ref ?? record.segmentRef ?? record.segmentId ?? record.segment_id,
    sceneMomentId: record.scene_moment_ref ?? record.sceneMomentRef ?? record.sceneMomentId ?? record.scene_moment_id,
    expressionUnitId: record.expression_unit_ref ?? record.expressionUnitRef ?? record.expressionUnitId ?? record.expression_unit_id,
    storyboardId: record.storyboard_ref ?? record.storyboardRef ?? record.storyboardId ?? record.storyboard_id,
    keyframeId: record.keyframe_ref ?? record.keyframeRef ?? record.keyframeId ?? record.keyframe_id,
    audioCueId: record.audio_cue_ref ?? record.audioCueRef ?? record.audioCueId ?? record.audio_cue_id,
    prompt: record.prompt ?? editPrompt?.text,
    negativePrompt: record.negative_prompt ?? record.negativePrompt ?? editPrompt?.negative_text,
    description: record.description,
    order: numberValue5(record.order),
    modelIntent
  });
}
function settingRefsFromRecord2(record) {
  const refs = Array.isArray(record.setting_refs) ? record.setting_refs.filter(isRecord4) : [];
  return refs.flatMap((ref) => {
    const settingId = ref.setting_id ?? ref.settingId ?? ref.setting_ref ?? ref.settingRef;
    if (settingId === void 0 || settingId === null || String(settingId).trim() === "") return [];
    return [{
      id: idValue3(settingId),
      ...ref.setting_state_id !== void 0 || ref.settingStateId !== void 0 || ref.setting_state_ref !== void 0 || ref.settingStateRef !== void 0 ? { settingStateId: idValue3(ref.setting_state_id ?? ref.settingStateId ?? ref.setting_state_ref ?? ref.settingStateRef) } : {},
      ...stringValue5(ref.role) ? { role: stringValue5(ref.role) } : {},
      ...stringValue5(ref.notes ?? ref.source_label ?? ref.sourceLabel) ? { sourceLabel: stringValue5(ref.notes ?? ref.source_label ?? ref.sourceLabel) } : {},
      ...stringValue5(ref.setting_kind ?? ref.kind) ? { kind: stringValue5(ref.setting_kind ?? ref.kind) } : {}
    }];
  });
}
function requiredTargetKind(args) {
  const value = requiredString2(args.targetKind ?? args.target_kind, "targetKind");
  if (value === "asset" || value === "keyframe") return value;
  if (value === "content_unit") {
    throw new Error("content_unit candidates are backend decision records; use domain_create_content_candidate/domain_select_content_unit_candidate");
  }
  throw new Error("targetKind must be asset or keyframe");
}
function semanticKind(value) {
  return stringValue5(value);
}
function inspectInput(args) {
  const commit = stringValue5(args.commit);
  const checkpointHash = stringValue5(args.checkpointHash ?? args.checkpoint_hash);
  return {
    ...commit ? { commit } : {},
    ...checkpointHash ? { checkpointHash } : {}
  };
}
function contentCandidateStatus(value) {
  const status = stringValue5(value);
  if (!status) return void 0;
  if (CONTENT_CANDIDATE_STATUSES.has(status)) return status;
  throw new Error("status must be queued, running, succeeded, failed, canceled, or imported");
}
function requiredDecision(value) {
  const decision = stringValue5(value);
  if (decision === "adopt" || decision === "reject" || decision === "defer") return decision;
  throw new Error("decision must be adopt, reject, or defer");
}
function optionalRecord(value) {
  return isRecord4(value) ? value : void 0;
}
function requiredRecord(value, name) {
  if (isRecord4(value)) return value;
  throw new Error(`${name} is required`);
}
function upsertPayloadRecord(args) {
  return requiredRecord(args.payload ?? args.record ?? args.entity, "payload");
}
function requiredArray(value, name) {
  if (Array.isArray(value)) return value;
  throw new Error(`${name} is required`);
}
function requiredString2(value, name) {
  const next = stringValue5(value);
  if (!next) throw new Error(`${name} is required`);
  return next;
}
function requiredId2(value, name) {
  if (value === void 0 || value === null || String(value).trim() === "") throw new Error(`${name} is required`);
  return idValue3(value);
}
function optionalId(value) {
  if (value === void 0 || value === null || String(value).trim() === "") return void 0;
  return idValue3(value);
}
function entityResultId(result, fallback, name) {
  const resultRecord = isRecord4(result) && isRecord4(result.record) ? result.record : {};
  return requiredId2(resultRecord.id ?? resultRecord.ID ?? fallback.id ?? fallback.client_id, `${name}.id`);
}
function idValue3(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  return String(value);
}
function requiredResourceId5(value) {
  if (typeof value === "number" && Number.isInteger(value) && value > 0) return value;
  throw new Error("resource_id must be a positive integer RawResource ID");
}
function numberValue5(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function msValue(value) {
  const number = numberValue5(value);
  return number === void 0 ? void 0 : Math.max(1, number);
}
function secToMs(value) {
  return value === void 0 ? void 0 : Math.max(1, Math.round(value * 1e3));
}
function booleanValue(value) {
  if (typeof value === "boolean") return value;
  if (typeof value !== "string") return void 0;
  if (value === "true") return true;
  if (value === "false") return false;
  return void 0;
}
async function readSceneMomentEditPlan(args, sceneMomentId) {
  return editingServiceTimelineView(args, "sceneMomentEditPlan", { sceneMomentId });
}
async function readProductionPreviewTimeline(args, productionId) {
  const previewTimeline = await editingServiceTimelineView(args, "previewTimeline", { productionId });
  return isRecord4(previewTimeline) ? previewTimeline : void 0;
}
async function sceneMomentTimelineBundle(args, sceneMomentId) {
  const result = await editingServiceTimelineView(args, "sceneMomentTimelineBundle", {
    sceneMomentId,
    id: stringValue5(args.editingProjectId ?? args.editing_project_id),
    projectId: stringValue5(args.timelineProjectId ?? args.timeline_project_id),
    title: stringValue5(args.projectName ?? args.project_name ?? args.sceneName ?? args.scene_name),
    defaultDurationMs: msValue(args.defaultDurationMs ?? args.default_duration_ms) ?? secToMs(numberValue5(args.defaultDurationSec ?? args.default_duration_sec))
  });
  if (!isRecord4(result)) throw new Error("editing service did not return scene moment timeline bundle");
  const mediaEditingProject = isRecord4(result.media_editing_project) ? result.media_editing_project : isRecord4(result.mediaEditingProject) ? result.mediaEditingProject : void 0;
  if (!mediaEditingProject) throw new Error("editing service scene moment timeline bundle did not include media_editing_project");
  const editPlan = isRecord4(result.edit_plan) ? result.edit_plan : isRecord4(result.editPlan) ? result.editPlan : void 0;
  if (!editPlan) throw new Error("editing service scene moment timeline bundle did not include edit_plan");
  const context = isRecord4(result.context) ? result.context : {};
  const composeInputs = Array.isArray(result.compose_inputs) ? result.compose_inputs.filter(isRecord4) : Array.isArray(result.composeInputs) ? result.composeInputs.filter(isRecord4) : [];
  const blockers = Array.isArray(result.blockers) ? result.blockers.filter(isRecord4) : [];
  const status = stringValue5(result.status) ?? (blockers.length === 0 ? "ok" : "blocked");
  return { status, mediaEditingProject, editPlan, context, composeInputs, blockers };
}
async function productionTimelineBundle(args, productionId) {
  const result = await editingServiceTimelineView(args, "productionTimelineBundle", {
    productionId,
    title: stringValue5(args.projectName ?? args.project_name),
    now: stringValue5(args.now),
    defaultDurationMs: msValue(args.defaultDurationMs ?? args.default_duration_ms) ?? secToMs(numberValue5(args.defaultDurationSec ?? args.default_duration_sec)) ?? 4e3
  });
  if (!isRecord4(result)) throw new Error("editing service did not return production timeline bundle");
  const mediaEditingProject = isRecord4(result.media_editing_project) ? result.media_editing_project : isRecord4(result.mediaEditingProject) ? result.mediaEditingProject : void 0;
  if (!mediaEditingProject) throw new Error("editing service production timeline bundle did not include media_editing_project");
  const editPlan = isRecord4(result.edit_plan) ? result.edit_plan : isRecord4(result.editPlan) ? result.editPlan : void 0;
  if (!editPlan) throw new Error("editing service production timeline bundle did not include edit_plan");
  const previewTimeline = isRecord4(result.preview_timeline) ? result.preview_timeline : isRecord4(result.previewTimeline) ? result.previewTimeline : void 0;
  const context = isRecord4(result.context) ? result.context : {};
  const composeInputs = Array.isArray(result.compose_inputs) ? result.compose_inputs.filter(isRecord4) : Array.isArray(result.composeInputs) ? result.composeInputs.filter(isRecord4) : [];
  const clips = Array.isArray(result.clips) ? result.clips : [];
  const blockers = Array.isArray(result.blockers) ? result.blockers.filter(isRecord4) : [];
  return { previewTimeline, mediaEditingProject, editPlan, context, composeInputs, clips, blockers };
}
function contentCandidateRecordsByContentUnitId3(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (document.path.endsWith("/content_candidate.json") && isRecord4(document.data)) {
      const contentUnitId2 = contentUnitIdForRuntimeDocument4(document.path, stringValue5(document.data.content_unit_ref));
      if (!contentUnitId2) continue;
      appendContentCandidateRecord2(output, contentUnitId2, document.data);
      continue;
    }
    if (!isContentUnitDecisionContextRecord(document.data)) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument4(document.path, stringValue5(document.data.target_ref));
    if (!contentUnitId) continue;
    for (const candidate of Array.isArray(document.data.candidates) ? document.data.candidates : []) {
      const record = normalizeDecisionContentCandidateRecord2(candidate, contentUnitId);
      if (record) appendContentCandidateRecord2(output, contentUnitId, record);
    }
  }
  return output;
}
function appendContentCandidateRecord2(output, contentUnitId, candidate) {
  const candidates = output.get(contentUnitId) ?? [];
  const candidateId = idFromUnknown(candidate.id);
  if (candidateId === void 0) {
    output.set(contentUnitId, [...candidates, candidate]);
    return;
  }
  const existingIndex = candidates.findIndex((existing) => sameId6(existing.id, candidateId));
  if (existingIndex < 0) {
    output.set(contentUnitId, [...candidates, candidate]);
    return;
  }
  output.set(contentUnitId, [
    ...candidates.slice(0, existingIndex),
    mergeContentCandidateRecords2(candidates[existingIndex], candidate),
    ...candidates.slice(existingIndex + 1)
  ]);
}
function normalizeDecisionContentCandidateRecord2(candidate, contentUnitId) {
  if (!isRecord4(candidate) || idFromUnknown(candidate.id) === void 0) return void 0;
  return {
    ...candidate,
    content_unit_ref: stringValue5(candidate.content_unit_ref) ?? `content_units/${contentUnitId}`
  };
}
function mergeContentCandidateRecords2(existing, incoming) {
  const merged = { ...existing, ...incoming };
  if (!Array.isArray(incoming.outputs) && Array.isArray(existing.outputs)) merged.outputs = existing.outputs;
  if (!isRecord4(incoming.producer) && isRecord4(existing.producer)) merged.producer = existing.producer;
  if (!isRecord4(incoming.prompt_snapshot) && isRecord4(existing.prompt_snapshot)) merged.prompt_snapshot = existing.prompt_snapshot;
  if (!stringValue5(incoming.status) && stringValue5(existing.status)) merged.status = existing.status;
  if (!stringValue5(incoming.source) && stringValue5(existing.source)) merged.source = existing.source;
  if (!stringValue5(incoming.created_at) && stringValue5(existing.created_at)) merged.created_at = existing.created_at;
  if (!stringValue5(incoming.content_unit_ref) && stringValue5(existing.content_unit_ref)) merged.content_unit_ref = existing.content_unit_ref;
  return merged;
}
function isContentUnitDecisionContextRecord(value) {
  return isRecord4(value) && value.schema === "movscript.decision_context.v1" && value.target_kind === "content_unit";
}
async function withContentUnitCandidateVisibility(args, contentUnitId, result, extra = {}) {
  const status = await readContentUnitCandidateVisibility(args, contentUnitId).catch((error) => ({
    visibility_error: errorMessage4(error),
    content_unit_candidates: [],
    selected_candidate: void 0,
    selected_raw_resource: void 0,
    stale_status: "unknown",
    frontend: { visible_in_panel: false, refresh_required: true }
  }));
  return {
    status: "ok",
    contentUnitId,
    content_unit_id: contentUnitId,
    result,
    ...status,
    ...extra
  };
}
async function readContentUnitCandidateVisibility(args, contentUnitId) {
  const snapshot = await service(args).loadContentWorkspaceSnapshot();
  const candidates = contentCandidateRecordsByContentUnitId3(snapshot.indexDocuments).get(String(contentUnitId)) ?? [];
  const selection = selectionRecordsByContentUnitId3(snapshot.indexDocuments).get(String(contentUnitId));
  const selectedCandidate = selection?.candidate_id !== void 0 ? candidates.find((candidate) => sameId6(candidate.id, selection.candidate_id)) : void 0;
  const selectedResourceId2 = numberValue5(selection?.resource_id) ?? numberValue5(firstCandidateOutput4(selectedCandidate)?.resource_id);
  return {
    content_unit_candidates: candidates,
    candidate_count: candidates.length,
    selected_candidate: selectedCandidate,
    selected_raw_resource: selectedResourceId2 === void 0 ? void 0 : { resource_id: selectedResourceId2 },
    stale_status: stringValue5(selection?.stale_policy) === "accept_stale" ? "accepted_stale" : "ok",
    frontend: {
      visible_in_panel: candidates.length > 0,
      refresh_required: true
    }
  };
}
function selectionRecordsByContentUnitId3(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!isRecord4(document.data)) continue;
    const selection = optionalRecord(document.data.selection);
    if (!selection) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument4(document.path, stringValue5(document.data.target_ref));
    if (!contentUnitId) continue;
    output.set(contentUnitId, selection);
  }
  return output;
}
function contentUnitIdForRuntimeDocument4(path, ref) {
  if (ref) return lastPathSegment8(ref) ?? ref;
  return pathSegmentAfter8(path, "content_units");
}
function entityStatusLine(entity) {
  return {
    id: idValue3(entity.id ?? entity.record.id ?? entity.record.ID ?? entity.path),
    title: stringValue5(entity.record.title),
    path: entity.path
  };
}
function contentCandidateOutputKind(value) {
  const kind = stringValue5(value);
  if (kind === "image" || kind === "video" || kind === "audio" || kind === "text" || kind === "metadata") return kind;
  return "image";
}
function firstCandidateOutput4(candidate) {
  const outputs = Array.isArray(candidate?.outputs) ? candidate.outputs : [];
  return outputs.find(isRecord4);
}
function firstOutputResourceId(outputs) {
  const items = Array.isArray(outputs) ? outputs : [];
  for (const item of items) {
    if (!isRecord4(item)) continue;
    const resourceId = numberValue5(item.resource_id ?? item.resourceId);
    if (resourceId !== void 0 && Number.isInteger(resourceId) && resourceId > 0) return resourceId;
  }
  return void 0;
}
function pathSegmentAfter8(path, segment) {
  if (!path) return void 0;
  const parts = path.split("/").filter(Boolean);
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function lastPathSegment8(path) {
  return path?.split("/").filter(Boolean).at(-1);
}
function sameId6(left, right) {
  return left !== void 0 && right !== void 0 && String(left) === String(right);
}
function errorMessage4(error) {
  return error instanceof Error ? error.message : String(error);
}
function isString3(value) {
  return typeof value === "string";
}
function makeResult(id, result) {
  return { jsonrpc: "2.0", id, result };
}
function makeError(id, code, message, data) {
  return {
    jsonrpc: "2.0",
    id,
    error: {
      code,
      message,
      ...data === void 0 ? {} : { data }
    }
  };
}
function listFocusResources() {
  return [
    {
      uri: "movscript://ui/current-route",
      name: "Current route",
      description: "Current MovScript route in the Electron renderer.",
      mimeType: "text/markdown"
    },
    {
      uri: "movscript://ui/current-selection",
      name: "Current selection",
      description: "Current selected entity, when a page has reported one.",
      mimeType: "text/markdown"
    },
    {
      uri: "movscript://project/current",
      name: "Current project",
      description: "Current MovScript project summary.",
      mimeType: "text/markdown"
    }
  ];
}
var focusResourceReaders = [
  readFocusResource
];
function readFocusResource(uri) {
  const snapshot = getMCPContextSnapshot();
  if (uri === "movscript://ui/current-route") return resourceContent(uri, snapshot.route);
  if (uri === "movscript://ui/current-selection") return resourceContent(uri, snapshot.selection);
  if (uri === "movscript://project/current") return resourceContent(uri, snapshot.project);
  return null;
}
function summarizeProject(item) {
  if (!item || typeof item !== "object") return item;
  const id = typeof item.id === "number" ? item.id : typeof item.ID === "number" ? item.ID : void 0;
  return {
    ...id !== void 0 ? { id } : {},
    ...typeof item.name === "string" ? { name: truncateLongText(item.name) } : {},
    ...typeof item.description === "string" ? { description: truncateLongText(item.description) } : {},
    ...typeof item.total_episodes === "number" ? { totalEpisodes: item.total_episodes } : typeof item.totalEpisodes === "number" ? { totalEpisodes: item.totalEpisodes } : {},
    ...typeof item.CreatedAt === "string" ? { CreatedAt: item.CreatedAt } : {},
    ...typeof item.UpdatedAt === "string" ? { UpdatedAt: item.UpdatedAt } : {}
  };
}
function summarizeResource(data) {
  if (!Array.isArray(data)) return data;
  return data.map(summarizeEntity);
}
function summarizeEntity(item) {
  if (!item || typeof item !== "object") return item;
  const summary = {};
  for (const key of [
    "ID",
    "id",
    "project_id",
    "entityKind",
    "path",
    "category",
    "kind",
    "domainCategory",
    "domainKind",
    "resourceKind",
    "legacyAlias",
    "preferredResourceKind",
    "timelineTemplate",
    "timelineNamespaces",
    "settingNamespaces",
    "namespaces",
    "order",
    "title",
    "name",
    "type",
    "script_type",
    "number",
    "status",
    "review_status",
    "description",
    "summary",
    "synopsis",
    "location",
    "time_of_day",
    "prompt",
    "final_prompt",
    "is_approved",
    "CreatedAt",
    "UpdatedAt"
  ]) {
    if (item[key] !== void 0) summary[key] = truncateLongText(item[key]);
  }
  return summary;
}
function truncateLongText(value) {
  if (typeof value !== "string") return value;
  return value.length > 1200 ? `${value.slice(0, 1200)}...` : value;
}
async function listProjects(args) {
  const limit = getOptionalNumeric(args, "limit") ?? 100;
  const projects = await backendList("/projects");
  return {
    count: projects.length,
    projects: projects.slice(0, limit).map(summarizeProject)
  };
}
async function createProject(args) {
  const name = getOptionalString(args, "name") ?? "";
  if (!name) throw new Error("name is required");
  const payload = { name };
  const description = getOptionalString(args, "description") ?? "";
  const totalEpisodes = getOptionalNumeric(args, "total_episodes");
  if (description) payload.description = description;
  if (totalEpisodes !== void 0) payload.total_episodes = totalEpisodes;
  const project = await backendPost2("/projects", payload);
  const summary = summarizeProject(project);
  return {
    status: "created",
    project: summary,
    message: isRecord4(summary) && typeof summary.id === "number" ? `\u9879\u76EE\u300C${name}\u300D\u5DF2\u521B\u5EFA\uFF08project#${summary.id}\uFF09\u3002` : `\u9879\u76EE\u300C${name}\u300D\u5DF2\u521B\u5EFA\u3002`
  };
}
async function initLocalProject(args) {
  const result = await runProjectLifecycleCommand("createProject", args);
  const backendBinding = await requireBackendBindingForLifecycleResult(result);
  return {
    ...result,
    backendProject: backendBinding.backendProject,
    projectDataSpace: backendBinding.projectDataSpace,
    message: `MovScript \u9879\u76EE\u5DF2\u521D\u59CB\u5316\uFF1A${stringField27(result.projectDir)}`
  };
}
async function fetchLocalProject(args) {
  const result = await runProjectLifecycleCommand("openProject", args);
  const backendBinding = stringField27(result.projectUid) ? await requireBackendBindingForLifecycleResult(result) : void 0;
  return {
    ...result,
    ...backendBinding ? {
      backendProject: backendBinding.backendProject,
      projectDataSpace: backendBinding.projectDataSpace
    } : {},
    message: stringField27(result.projectUid) ? `MovScript \u9879\u76EE\u5DF2\u6253\u5F00\uFF1A${stringField27(result.projectDir)}` : `\u76EE\u5F55\u5DF2\u6253\u5F00\u4F46\u6CA1\u6709\u627E\u5230 MovScript \u9879\u76EE\u5143\u6570\u636E\uFF1A${stringField27(result.projectDir)}`
  };
}
async function runProjectLifecycleCommand(command, args) {
  const projectDir = normalizeProjectDir(args);
  const response = await createProjectServiceClientFromRuntime().lifecycleCommand({
    projectDir,
    command,
    input: lifecycleInputFromArgs(args)
  });
  if (!isRecord4(response.result)) {
    throw new Error(`Project Service lifecycle command ${command} returned an invalid result`);
  }
  return response.result;
}
function lifecycleInputFromArgs(args) {
  return {
    ...getOptionalString(args, "title") ? { title: getOptionalString(args, "title") } : {},
    ...getOptionalString(args, "projectId") ? { projectId: getOptionalString(args, "projectId") } : {},
    ...getOptionalString(args, "project_id") ? { project_id: getOptionalString(args, "project_id") } : {},
    ...getOptionalString(args, "projectUid") ? { projectUid: getOptionalString(args, "projectUid") } : {},
    ...getOptionalString(args, "project_uid") ? { project_uid: getOptionalString(args, "project_uid") } : {},
    ...getOptionalString(args, "language") ? { language: getOptionalString(args, "language") } : {},
    ...isRecord4(args.standards) ? { standards: args.standards } : {},
    ...typeof args.overwrite === "boolean" ? { overwrite: args.overwrite } : {}
  };
}
function normalizeProjectDir(args) {
  const projectDir = getOptionalString(args, "projectDir") ?? getOptionalString(args, "project_dir") ?? getOptionalString(args, "cwd");
  if (!projectDir) throw new Error("projectDir is required");
  return resolve23(projectDir);
}
async function requireBackendBindingForLifecycleResult(result) {
  const projectDir = stringField27(result.projectDir);
  const projectUid = stringField27(result.projectUid);
  if (!projectDir || !projectUid) {
    throw new Error("Project Service lifecycle result must include projectDir and projectUid for MCP backend binding");
  }
  return requireMCPBackendBoundProject({ projectDir, projectUid });
}
function stringField27(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function listProjectResources() {
  const snapshot = getMCPContextSnapshot();
  const resources = [
    {
      uri: "movscript://projects",
      name: "Projects",
      description: "All visible MovScript projects.",
      mimeType: "text/markdown"
    }
  ];
  if (snapshot.project) {
    const id = snapshot.project.id;
    resources.push(
      resource(`movscript://project/${id}/summary`, "Project summary"),
      resource(`movscript://project/${id}/scripts`, "Scripts"),
      resource(`movscript://project/${id}/settings`, "Settings", "Legacy setting source records with namespace projection fields."),
      resource(`movscript://project/${id}/setting-states`, "Setting states", "Legacy setting-state alias; prefer setting-namespaces for namespace-aware work."),
      resource(`movscript://project/${id}/assets`, "Assets", "System primitive asset records."),
      resource(`movscript://project/${id}/project-context`, "Project context", "Project standards, namespace vocabulary, hash, and agent guidance from Project Service."),
      resource(`movscript://project/${id}/namespace-vocabulary`, "Namespace vocabulary", "Project namespace vocabulary, templates, aliases, and diagnostics."),
      resource(`movscript://project/${id}/timeline-namespaces`, "Timeline namespaces", "Canonical timeline namespace nodes using project vocabulary."),
      resource(`movscript://project/${id}/setting-namespaces`, "Setting namespaces", "Canonical setting namespace nodes using project vocabulary."),
      resource(`movscript://project/${id}/system-primitives`, "System primitives", "Scene moments, expression units, storyboards, keyframes, audio cues, assets, and assemblies."),
      resource(`movscript://project/${id}/domain-nodes`, "Domain nodes", "All normalized MovScript domain nodes."),
      resource(`movscript://project/${id}/domain-edges`, "Domain edges", "Normalized parent, scope, target, uses, and selection edges."),
      resource(`movscript://project/${id}/episodes`, "Episodes", "Legacy production alias; prefer timeline-namespaces for new namespace-aware work."),
      resource(`movscript://project/${id}/scenes`, "Scenes", "Legacy segment alias; prefer timeline-namespaces and system-primitives for new work."),
      resource(`movscript://project/${id}/storyboards`, "Storyboards", "System primitive storyboard records."),
      resource(`movscript://project/${id}/content-units`, "Content units", "Production tasks, candidates, and generation target refs.")
    );
  }
  return resources;
}
var projectResourceReaders = [
  readProjectResource
];
async function readProjectResource(uri) {
  if (uri === "movscript://projects") return resourceContent(uri, await listProjects({}));
  const projectResource = parseProjectResourceURI(uri);
  if (!projectResource) return null;
  const data = await readWorkspaceProjectResource(projectResource.projectId, projectResource.kind);
  return resourceContent(uri, summarizeResource(data));
}
function resource(uri, name, description) {
  return { uri, name, ...description ? { description } : {}, mimeType: "text/markdown" };
}
async function readWorkspaceProjectResource(projectId, kind) {
  const snapshot = getMCPContextSnapshot();
  const projectDir = snapshot.project && snapshot.project.id === projectId ? snapshot.project.projectDir ?? snapshot.project.projectPath ?? snapshot.project.workspacePath ?? snapshot.project.project_path ?? snapshot.project.workspace_path : void 0;
  const locator = resolveMCPProjectWorkspaceLocator({ projectDir });
  const response = await createProjectServiceClientFromRuntime().resourceView({
    projectDir: locator.projectDir,
    kind: projectResourceViewKind(kind)
  });
  return response.items;
}
function projectResourceViewKind(kind) {
  switch (kind) {
    case "summary":
      return "summary";
    case "assets":
    case "assests":
      return "assets";
    case "namespace-vocabulary":
      return "namespace-vocabulary";
    case "project-context":
      return "project-context";
    case "timeline-namespaces":
      return "timeline-namespaces";
    case "setting-namespaces":
      return "setting-namespaces";
    case "system-primitives":
      return "system-primitives";
    case "domain-nodes":
      return "domain-nodes";
    case "domain-edges":
      return "domain-edges";
    case "episodes":
    case "productions":
      return "episodes";
    case "scenes":
    case "segments":
      return "scenes";
    case "storyboards":
      return "storyboards";
    case "content-units":
      return "content-units";
    case "settings":
      return "settings";
    case "setting-states":
    case "states":
      return "setting-states";
    case "scripts":
      return "scripts";
    default:
      throw new Error(`Unsupported project resource kind: ${kind}`);
  }
}
function parseProjectResourceURI(uri) {
  const match = uri.match(/^movscript:\/\/project\/(\d+)\/([a-z-]+)$/);
  if (!match) return null;
  return {
    projectId: Number(match[1]),
    kind: match[2] ?? ""
  };
}
function ffmpegBinaryName(platform = process.platform) {
  return platform === "win32" ? "ffmpeg.exe" : "ffmpeg";
}
function resolveFFmpegPath(options = {}) {
  const envPath = process.env.FFMPEG_PATH?.trim() || process.env.MOVSCRIPT_FFMPEG_PATH?.trim();
  if (envPath && existsSync3(envPath)) return envPath;
  const platform = options.platform ?? process.platform;
  const arch = options.arch ?? process.arch;
  const resourcesPath = options.resourcesPath ?? process.resourcesPath;
  const cwd = options.cwd ?? process.cwd();
  const binary = ffmpegBinaryName(platform);
  const candidates = [
    join8(resourcesPath || "", "ffmpeg", platform, arch, binary),
    join8(resourcesPath || "", "ffmpeg", platform, binary),
    join8(resourcesPath || "", "ffmpeg", binary),
    join8(resourcesPath || "", "bin", binary),
    resolve32(cwd, "vendor/ffmpeg", platform, arch, binary),
    resolve32(cwd, "vendor/ffmpeg", platform, binary),
    resolve32(cwd, "../../apps/desktop/vendor/ffmpeg", platform, arch, binary),
    resolve32(cwd, "../../apps/desktop/vendor/ffmpeg", platform, binary),
    binary
  ];
  return candidates.find((candidate) => candidate === binary || existsSync3(candidate));
}
var DEFAULT_SCENE_THRESHOLD = 0.28;
var DEFAULT_MIN_SHOT_DURATION_SEC = 1.2;
var DEFAULT_MAX_SHOT_DURATION_SEC = 12;
var DEFAULT_TARGET_SHOT_DURATION_SEC = 6;
var DEFAULT_MAX_VIDEO_BYTES = 200 * 1024 * 1024;
var ABSOLUTE_MAX_VIDEO_BYTES = 1024 * 1024 * 1024;
async function queryShotLibrary(args) {
  const id = getOptionalNumeric(args, "shot_reference_id") ?? getOptionalNumeric(args, "shotReferenceId") ?? getOptionalNumeric(args, "id");
  const includeFull = booleanParam(args.include_full) ?? booleanParam(args.includeFull) ?? false;
  const query = getOptionalString(args, "query") ?? getOptionalString(args, "q") ?? "";
  const groupId = positiveIntegerParam(args, "group_id") ?? positiveIntegerParam(args, "groupId");
  const page = Math.floor(clampNumber(getOptionalNumeric(args, "page") ?? 1, 1, 1e4));
  const pageSize = Math.floor(clampNumber(
    getOptionalNumeric(args, "page_size") ?? getOptionalNumeric(args, "pageSize") ?? getOptionalNumeric(args, "limit") ?? getOptionalNumeric(args, "topK") ?? 20,
    1,
    100
  ));
  const pageData = await fetchShotLibraryPage({ query, page, pageSize, groupId });
  const items = Array.isArray(pageData.items) ? pageData.items : [];
  const summarized = items.map((item) => summarizeShotReference(item, { includeFull }));
  const filtered = id === void 0 ? summarized : summarized.filter((item) => shotReferenceId(item) === id);
  return {
    query,
    page: numberField9(pageData, "page") ?? page,
    pageSize: numberField9(pageData, "page_size") ?? pageSize,
    total: numberField9(pageData, "total") ?? filtered.length,
    count: filtered.length,
    items: filtered,
    ...id !== void 0 && filtered.length === 0 ? { warning: `shot_reference_id ${id} was not found on the requested page` } : {}
  };
}
async function createShotGroup(args) {
  const resourceId = requiredPositiveInteger(args, ["resource_id", "resourceId"], "resource_id");
  const body = {
    resource_id: resourceId
  };
  const title = getOptionalString(args, "title");
  const summary = getOptionalString(args, "summary");
  const cutStrategy = getOptionalString(args, "cut_strategy") ?? getOptionalString(args, "cutStrategy");
  if (title) body.title = title;
  if (summary) body.summary = summary;
  if (cutStrategy) body.cut_strategy = cutStrategy;
  const group = await backendPost2("/shot-reference-groups", body);
  const groupId = idField13(group);
  if (groupId === void 0) throw new Error("shot group create response did not include a valid group ID");
  return {
    status: "created",
    group_id: groupId,
    group,
    message: `Shot group #${groupId} created for resource #${resourceId}.`
  };
}
async function getShotGroup(args) {
  const groupId = requiredPositiveInteger(args, ["group_id", "groupId", "id"], "group_id");
  const detail = await backendGet2(`/shot-reference-groups/${encodeURIComponent(String(groupId))}`);
  const group = isRecord4(detail) && isRecord4(detail.group) ? detail.group : {};
  const shots = isRecord4(detail) && Array.isArray(detail.shots) ? detail.shots : [];
  return {
    status: "loaded",
    group_id: groupId,
    group,
    count: shots.length,
    shots: shots.map((shot) => summarizeShotReference(shot, { includeFull: true }))
  };
}
async function addShotsToGroup(args) {
  const groupId = requiredPositiveInteger(args, ["group_id", "groupId"], "group_id");
  const rawShots = Array.isArray(args.shots) ? args.shots : [];
  if (rawShots.length === 0) throw new Error("shots must contain at least one shot range or metadata object");
  const groupDetail = await backendGet2(`/shot-reference-groups/${encodeURIComponent(String(groupId))}`);
  const group = isRecord4(groupDetail) && isRecord4(groupDetail.group) ? groupDetail.group : {};
  const groupResourceId = positiveIntegerValue(group.source_resource_id) ?? positiveIntegerValue(group.sourceResourceId);
  const resourceId = positiveIntegerParam(args, "resource_id") ?? positiveIntegerParam(args, "resourceId") ?? groupResourceId;
  if (resourceId === void 0) throw new Error("resource_id is required when the shot group response has no source_resource_id");
  const body = {
    resource_id: resourceId,
    group_id: groupId,
    shots: rawShots.map(normalizeShotInput)
  };
  const durationSec = getOptionalNumeric(args, "duration_sec") ?? getOptionalNumeric(args, "durationSec");
  const width = getOptionalNumeric(args, "width");
  const height = getOptionalNumeric(args, "height");
  if (durationSec !== void 0) body.duration_sec = durationSec;
  if (width !== void 0) body.width = Math.floor(width);
  if (height !== void 0) body.height = Math.floor(height);
  const result = await backendPost2("/shot-references/from-resource", body);
  const shots = isRecord4(result) && Array.isArray(result.items) ? result.items : [];
  return {
    status: "created",
    group_id: groupId,
    count: shots.length,
    shots: shots.map((shot) => summarizeShotReference(shot, { includeFull: true })),
    message: `${shots.length} shot reference(s) added to group #${groupId}.`
  };
}
async function analyzeVideoShotCuts(args) {
  const resourceId = requiredPositiveInteger(args, ["resource_id", "resourceId", "id"], "resource_id");
  const maxVideoBytes = Math.floor(clampNumber(getOptionalNumeric(args, "max_video_bytes") ?? getOptionalNumeric(args, "maxVideoBytes") ?? DEFAULT_MAX_VIDEO_BYTES, 1, ABSOLUTE_MAX_VIDEO_BYTES));
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_video_shot_cuts_analyze but was not found");
  const source = await backendGetBinary(`/resources/${encodeURIComponent(String(resourceId))}/file`, { maxBytes: maxVideoBytes });
  const dir = join22(tmpdir2(), `movscript-shot-cuts-${Date.now()}-${Math.random().toString(16).slice(2)}`);
  const inputPath = join22(dir, `resource-${resourceId}.video`);
  await mkdir3(dir, { recursive: true });
  try {
    await writeFile3(inputPath, source.bytes);
    const probed = await probeVideo(inputPath, ffmpeg).catch(() => ({}));
    const durationSec = positiveNumber4(getOptionalNumeric(args, "duration_sec") ?? getOptionalNumeric(args, "durationSec")) ?? probed.durationSec;
    if (!durationSec) throw new Error("duration_sec is required when ffprobe cannot read video duration");
    const sceneThreshold = positiveNumber4(getOptionalNumeric(args, "scene_threshold") ?? getOptionalNumeric(args, "sceneThreshold")) ?? DEFAULT_SCENE_THRESHOLD;
    const sceneOutput = await runFFmpeg(ffmpeg, [
      "-hide_banner",
      "-i",
      inputPath,
      "-filter:v",
      `select='gt(scene,${sceneThreshold})',showinfo`,
      "-an",
      "-f",
      "null",
      "-"
    ]).catch((error) => {
      throw error instanceof Error ? error : new Error(String(error));
    });
    const sceneTimes = parseSceneDetectTimes(sceneOutput);
    const shots = buildSceneShotSegments(durationSec, sceneTimes, {
      minShotDurationSec: getOptionalNumeric(args, "min_shot_duration_sec") ?? getOptionalNumeric(args, "minShotDurationSec"),
      maxShotDurationSec: getOptionalNumeric(args, "max_shot_duration_sec") ?? getOptionalNumeric(args, "maxShotDurationSec")
    });
    const finalShots = shots.length > 0 ? shots : buildEvenShotSegments(durationSec);
    return {
      status: "analyzed",
      resource_id: resourceId,
      strategy: shots.length > 0 ? "scene_detection" : "even",
      duration_sec: roundTime(durationSec),
      count: finalShots.length,
      shots: finalShots.map((shot, index) => ({
        index: index + 1,
        start_sec: shot.startSec,
        end_sec: shot.endSec,
        startSec: shot.startSec,
        endSec: shot.endSec
      })),
      ...sceneTimes.length === 0 ? { warning: "No scene boundaries were detected; returned even fallback segments." } : {}
    };
  } finally {
    await rm2(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function readShotLibrary(uri) {
  const parsed = parseShotLibraryURI(uri);
  if (!parsed) throw new Error(`Unsupported shot library resource URI: ${uri}`);
  return queryShotLibrary({
    ...parsed.id !== void 0 ? { shot_reference_id: parsed.id, limit: 100 } : {},
    ...parsed.query ? { query: parsed.query } : {},
    ...parsed.page !== void 0 ? { page: parsed.page } : {},
    ...parsed.pageSize !== void 0 ? { page_size: parsed.pageSize } : {},
    include_full: true
  });
}
function isShotLibraryURI(uri) {
  return parseShotLibraryURI(uri) !== null;
}
function parseShotLibraryURI(uri) {
  const match = uri.match(/^movscript:\/\/shot-library(?:\/(\d+))?(?:\?(.*))?$/);
  if (!match) return null;
  const params = new URLSearchParams(match[2] ?? "");
  const page = numericParam(params.get("page"));
  const pageSize = numericParam(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit"));
  return {
    ...match[1] ? { id: Number(match[1]) } : {},
    ...params.get("query") || params.get("q") ? { query: params.get("query") ?? params.get("q") ?? "" } : {},
    ...page !== void 0 ? { page } : {},
    ...pageSize !== void 0 ? { pageSize } : {}
  };
}
async function fetchShotLibraryPage(input) {
  const params = new URLSearchParams();
  params.set("page", String(input.page ?? 1));
  params.set("page_size", String(input.pageSize ?? 20));
  if (input.query?.trim()) params.set("q", input.query.trim());
  if (input.groupId !== void 0) params.set("group_id", String(input.groupId));
  const data = await backendGet2(`/shot-references?${params.toString()}`);
  return isRecord4(data) ? data : { items: [] };
}
function summarizeShotReference(item, options) {
  if (!isRecord4(item)) return item;
  if (options.includeFull) return item;
  const resource2 = isRecord4(item.resource) ? item.resource : void 0;
  const group = isRecord4(item.group) ? item.group : void 0;
  return {
    ...picked(item, ["ID", "id", "title", "summary", "analysis_status", "analysis_source", "intent", "pattern", "shot_function", "visual_preference", "emotional_effect", "start_sec", "end_sec", "retrieval_text", "CreatedAt", "UpdatedAt"]),
    resource: resource2 ? picked(resource2, ["ID", "id", "name", "mime_type", "type", "url", "size"]) : void 0,
    group: group ? picked(group, ["ID", "id", "title", "summary", "analysis_status", "cut_strategy"]) : void 0,
    execution_details: isRecord4(item.execution_details) ? picked(item.execution_details, ["duration_sec", "resolution", "aspect_ratio", "coverage_role", "difficulty", "blocking", "requirements"]) : void 0,
    search_index: isRecord4(item.search_index) ? picked(item.search_index, ["natural_language_queries", "tags", "visual_facets", "narrative_facets", "emotion_facets", "pattern_facets", "production_facets"]) : void 0
  };
}
function picked(record, keys) {
  const result = {};
  for (const key of keys) {
    if (record[key] !== void 0) result[key] = truncateText(record[key]);
  }
  return result;
}
function truncateText(value) {
  if (typeof value !== "string") return value;
  return value.length > 1200 ? `${value.slice(0, 1200)}...` : value;
}
function shotReferenceId(item) {
  if (!isRecord4(item)) return void 0;
  return typeof item.ID === "number" ? item.ID : typeof item.id === "number" ? item.id : void 0;
}
function numberField9(record, key) {
  return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
}
function numericParam(value) {
  if (!value?.trim()) return void 0;
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : void 0;
}
function booleanParam(value) {
  if (typeof value === "boolean") return value;
  if (typeof value !== "string") return void 0;
  if (value === "true") return true;
  if (value === "false") return false;
  return void 0;
}
function normalizeShotInput(value) {
  if (!isRecord4(value)) throw new Error("each shot must be an object");
  const shot = {};
  copyString(value, shot, "title");
  copyString(value, shot, "summary");
  copyArray(value, shot, "intent");
  copyArray(value, shot, "pattern");
  copyArray(value, shot, "shot_function", "shotFunction");
  copyArray(value, shot, "visual_preference", "visualPreference");
  copyArray(value, shot, "emotional_effect", "emotionalEffect");
  copyObject(value, shot, "execution_details", "executionDetails");
  copyObject(value, shot, "visual_analysis", "visualAnalysis");
  copyObject(value, shot, "scene_semantics", "sceneSemantics");
  copyObject(value, shot, "narrative_function", "narrativeFunction");
  copyObject(value, shot, "emotional_profile", "emotionalProfile");
  copyObject(value, shot, "reusable_pattern", "reusablePattern");
  const startSec = numberValue22(value.start_sec) ?? numberValue22(value.startSec);
  const endSec = numberValue22(value.end_sec) ?? numberValue22(value.endSec);
  if (startSec !== void 0) {
    shot.start_sec = startSec;
    shot.start_sec_set = true;
  }
  if (endSec !== void 0) {
    shot.end_sec = endSec;
    shot.end_sec_set = true;
  }
  return shot;
}
function copyString(input, out, key, alias) {
  const value = stringValue23(input[key]) ?? (alias ? stringValue23(input[alias]) : void 0);
  if (value !== void 0) out[key] = value;
}
function copyArray(input, out, key, alias) {
  const value = arrayValue4(input[key]) ?? (alias ? arrayValue4(input[alias]) : void 0);
  if (value !== void 0) out[key] = value;
}
function copyObject(input, out, key, alias) {
  const value = isRecord4(input[key]) ? input[key] : alias && isRecord4(input[alias]) ? input[alias] : void 0;
  if (value !== void 0) out[key] = value;
}
function requiredPositiveInteger(args, keys, label) {
  for (const key of keys) {
    const value = positiveIntegerParam(args, key);
    if (value !== void 0) return value;
  }
  throw new Error(`${label} must be a positive integer`);
}
function positiveIntegerParam(args, key) {
  return positiveIntegerValue(args[key]);
}
function positiveIntegerValue(value) {
  const number = numberValue22(value);
  return number !== void 0 && Number.isInteger(number) && number > 0 ? number : void 0;
}
function idField13(value) {
  if (!isRecord4(value)) return void 0;
  return positiveIntegerValue(value.ID) ?? positiveIntegerValue(value.id);
}
function numberValue22(value) {
  const number = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : NaN;
  return Number.isFinite(number) ? number : void 0;
}
function positiveNumber4(value) {
  const number = numberValue22(value);
  return number !== void 0 && number > 0 ? number : void 0;
}
function stringValue23(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function arrayValue4(value) {
  return Array.isArray(value) ? value : void 0;
}
function parseSceneDetectTimes(output) {
  const times = /* @__PURE__ */ new Set();
  for (const match of output.matchAll(/\bpts_time:([0-9]+(?:\.[0-9]+)?)/g)) {
    const value = Number(match[1]);
    if (Number.isFinite(value) && value > 0) times.add(roundTime(value));
  }
  return Array.from(times).sort((a, b) => a - b);
}
function buildSceneShotSegments(durationSec, sceneTimes, input = {}) {
  const minDuration = positiveNumber4(input.minShotDurationSec) ?? DEFAULT_MIN_SHOT_DURATION_SEC;
  const maxDuration = positiveNumber4(input.maxShotDurationSec) ?? DEFAULT_MAX_SHOT_DURATION_SEC;
  const boundaries = normalizeSceneBoundaries(sceneTimes, durationSec, minDuration, maxDuration);
  return rangesFromBoundaries(durationSec, boundaries);
}
function buildEvenShotSegments(durationSec) {
  const segmentCount = Math.max(1, Math.ceil(durationSec / DEFAULT_TARGET_SHOT_DURATION_SEC));
  const segmentLength = durationSec / segmentCount;
  return Array.from({ length: segmentCount }, (_item, index) => ({
    startSec: roundTime(index * segmentLength),
    endSec: roundTime(index === segmentCount - 1 ? durationSec : (index + 1) * segmentLength)
  }));
}
function normalizeSceneBoundaries(sceneTimes, durationSec, minDurationSec, maxDurationSec) {
  const boundaries = [];
  let previous = 0;
  const candidates = sceneTimes.map(roundTime).filter((time) => time > 0 && time < durationSec).sort((a, b) => a - b);
  for (const candidate of candidates) {
    addForcedBoundaries(boundaries, previous, candidate, maxDurationSec);
    previous = boundaries[boundaries.length - 1] ?? previous;
    if (candidate - previous >= minDurationSec && durationSec - candidate >= minDurationSec) {
      boundaries.push(candidate);
      previous = candidate;
    }
  }
  addForcedBoundaries(boundaries, previous, durationSec, maxDurationSec);
  return boundaries;
}
function addForcedBoundaries(boundaries, startSec, endSec, maxDurationSec) {
  let cursor = startSec;
  while (maxDurationSec > 0 && endSec - cursor > maxDurationSec) {
    cursor = roundTime(cursor + maxDurationSec);
    if (cursor < endSec) boundaries.push(cursor);
  }
}
function rangesFromBoundaries(durationSec, boundaries) {
  const points = [0, ...boundaries, durationSec];
  const result = [];
  for (let index = 0; index < points.length - 1; index += 1) {
    const startSec = roundTime(points[index] ?? 0);
    const endSec = roundTime(points[index + 1] ?? 0);
    if (endSec > startSec) result.push({ startSec, endSec });
  }
  return result;
}
async function probeVideo(inputPath, ffmpeg) {
  const ffprobe = ffmpeg.replace(/ffmpeg(?:\.exe)?$/i, process.platform === "win32" ? "ffprobe.exe" : "ffprobe");
  const output = await runCommand(ffprobe, [
    "-v",
    "error",
    "-show_entries",
    "format=duration",
    "-of",
    "json",
    inputPath
  ]);
  const parsed = JSON.parse(output);
  const durationSec = isRecord4(parsed) && isRecord4(parsed.format) ? positiveNumber4(parsed.format.duration) : void 0;
  return durationSec ? { durationSec } : {};
}
function runFFmpeg(command, args) {
  return runCommand(command, args, true);
}
function runCommand(command, args, acceptNonZero = false) {
  return new Promise((resolve62, reject) => {
    const child = spawn3(command, args, { stdio: ["ignore", "pipe", "pipe"] });
    let stdout = "";
    let stderr = "";
    const appendStdout = (chunk) => {
      stdout += String(chunk);
    };
    const appendStderr = (chunk) => {
      stderr += String(chunk);
      if (stderr.length > 1024 * 1024) stderr = stderr.slice(stderr.length - 1024 * 1024);
    };
    child.stdout?.on("data", appendStdout);
    child.stderr?.on("data", appendStderr);
    child.on("error", (error) => reject(error instanceof Error ? error : new Error(String(error))));
    child.on("exit", (code) => {
      if (code === 0 || acceptNonZero) {
        resolve62(`${stdout}
${stderr}`);
        return;
      }
      reject(new Error(stderr.trim() || `command exited with code ${code ?? "unknown"}`));
    });
  });
}
function roundTime(value) {
  return Math.round(value * 10) / 10;
}
function listShotLibraryResources() {
  return [
    {
      uri: "movscript://shot-library",
      name: "Shot reference library",
      description: "Searchable MovScript shot reference library for reusable camera, composition, movement, narrative, emotion, and production patterns.",
      mimeType: "text/markdown"
    }
  ];
}
var shotLibraryResourceReaders = [
  readShotLibraryResource
];
async function readShotLibraryResource(uri) {
  if (!isShotLibraryURI(uri)) return null;
  return resourceContent(uri, await readShotLibrary(uri));
}
async function queryResourceLibrary(args) {
  const id = getOptionalNumeric(args, "resource_id") ?? getOptionalNumeric(args, "resourceId") ?? getOptionalNumeric(args, "id");
  const includeFull = booleanParam2(args.include_full) ?? booleanParam2(args.includeFull) ?? false;
  const query = getOptionalString(args, "query") ?? getOptionalString(args, "q") ?? "";
  const type = getOptionalString(args, "type") ?? getOptionalString(args, "media_type");
  const scope = getOptionalString(args, "scope");
  const folderId = getOptionalString(args, "folder_id") ?? getOptionalString(args, "folderId");
  const page = Math.floor(clampNumber(getOptionalNumeric(args, "page") ?? 1, 1, 1e4));
  const pageSize = Math.floor(clampNumber(
    getOptionalNumeric(args, "page_size") ?? getOptionalNumeric(args, "pageSize") ?? getOptionalNumeric(args, "limit") ?? 20,
    1,
    100
  ));
  const pageData = await fetchResourceLibraryPage({ query, type, scope, folderId, page, pageSize });
  const items = resourceItems(pageData);
  const summarized = items.map((item) => summarizeRawResource(item, { includeFull }));
  const filtered = id === void 0 ? summarized : summarized.filter((item) => rawResourceId(item) === id);
  return {
    source: "movscript_resource_library",
    query,
    ...type ? { type } : {},
    ...scope ? { scope } : {},
    ...folderId ? { folder_id: folderId } : {},
    page: numberField24(pageData, "page") ?? page,
    pageSize: numberField24(pageData, "page_size") ?? pageSize,
    total: numberField24(pageData, "total") ?? filtered.length,
    count: filtered.length,
    items: filtered,
    usage: "Use RawResource.ID values as input_resource_ids or reference_resource_ids for MovScript generation tools.",
    ...id !== void 0 && filtered.length > 0 ? { surface: createResourceDetailSurface(args, id) } : {},
    ...id !== void 0 && filtered.length === 0 ? { warning: `resource_id ${id} was not found on the requested page` } : {}
  };
}
function openResourceLibrary(args) {
  const surface = createResourceLibrarySurface(args);
  return {
    source: "movscript_resource_library",
    ...surface,
    backend_api_base_url: getMovScriptBackendAPIBaseURL()
  };
}
async function readResourceLibrary(uri) {
  const parsed = parseResourceLibraryURI(uri);
  if (!parsed) throw new Error(`Unsupported MovScript resource library URI: ${uri}`);
  return queryResourceLibrary({
    ...parsed,
    include_full: true
  });
}
function isResourceLibraryURI(uri) {
  return parseResourceLibraryURI(uri) !== null;
}
function parseResourceLibraryURI(uri) {
  const match = uri.match(/^movscript:\/\/resource-library(?:\/(\d+))?(?:\?(.*))?$/);
  if (!match) return null;
  const params = new URLSearchParams(match[2] ?? "");
  return {
    ...match[1] ? { resource_id: Number(match[1]) } : {},
    ...params.get("query") || params.get("q") ? { query: params.get("query") ?? params.get("q") ?? "" } : {},
    ...params.get("type") || params.get("media_type") ? { type: params.get("type") ?? params.get("media_type") ?? "" } : {},
    ...params.get("scope") ? { scope: params.get("scope") ?? "" } : {},
    ...params.get("folder_id") || params.get("folderId") ? { folder_id: params.get("folder_id") ?? params.get("folderId") ?? "" } : {},
    ...numericParam2(params.get("page")) !== void 0 ? { page: numericParam2(params.get("page")) } : {},
    ...numericParam2(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) !== void 0 ? { page_size: numericParam2(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) } : {}
  };
}
async function fetchResourceLibraryPage(input) {
  const params = new URLSearchParams();
  params.set("page", String(input.page ?? 1));
  params.set("page_size", String(input.pageSize ?? 20));
  if (input.query?.trim()) params.set("q", input.query.trim());
  if (input.type?.trim()) params.set("type", input.type.trim());
  if (input.scope?.trim()) params.set("scope", input.scope.trim());
  if (input.folderId?.trim()) params.set("folder_id", input.folderId.trim());
  const data = await backendGet2(`/resources?${params.toString()}`);
  return isRecord4(data) || Array.isArray(data) ? data : { items: [] };
}
function summarizeRawResource(item, options) {
  if (!isRecord4(item)) return item;
  if (options.includeFull) return item;
  return picked2(item, ["ID", "id", "name", "type", "mime_type", "size", "url", "direct_url", "folder_id", "owner_id", "org_id", "CreatedAt", "UpdatedAt"]);
}
function resourceItems(pageData) {
  if (Array.isArray(pageData)) return pageData;
  return Array.isArray(pageData.items) ? pageData.items : [];
}
function picked2(record, keys) {
  const result = {};
  for (const key of keys) {
    if (record[key] !== void 0) result[key] = record[key];
  }
  return result;
}
function rawResourceId(item) {
  if (!isRecord4(item)) return void 0;
  return typeof item.ID === "number" ? item.ID : typeof item.id === "number" ? item.id : void 0;
}
function numberField24(record, key) {
  if (!isRecord4(record)) return void 0;
  return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
}
function numericParam2(value) {
  if (!value?.trim()) return void 0;
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : void 0;
}
function booleanParam2(value) {
  if (typeof value === "boolean") return value;
  if (typeof value !== "string") return void 0;
  if (value === "true") return true;
  if (value === "false") return false;
  return void 0;
}
function listResourceLibraryResources() {
  return [
    {
      uri: "movscript://resource-library",
      name: "MovScript resource library",
      description: "Internal MovScript RawResource library. Use the movscript_resource_library_query tool for search and generation-ready resource IDs.",
      mimeType: "text/markdown"
    },
    {
      uri: "movscript://resource-library/open",
      name: "Open MovScript resource library",
      description: "Agent in-app browser URL for the MovScript resource library. Read this resource or call system_resource_library_open to get the current URL.",
      mimeType: "text/markdown"
    }
  ];
}
var resourceLibraryResourceReaders = [
  readResourceLibraryResource
];
async function readResourceLibraryResource(uri) {
  if (uri === "movscript://resource-library/open") return resourceContent(uri, openResourceLibrary({}));
  if (!isResourceLibraryURI(uri)) return null;
  return resourceContent(uri, await readResourceLibrary(uri));
}
var DEFAULT_MAX_IMAGE_BYTES = 8 * 1024 * 1024;
var ABSOLUTE_MAX_IMAGE_BYTES = 20 * 1024 * 1024;
var DEFAULT_MAX_IMAGE_WIDTH = 1568;
var DEFAULT_MAX_IMAGE_HEIGHT = 1568;
var ABSOLUTE_MAX_IMAGE_DIMENSION = 4096;
var DEFAULT_MAX_UPLOAD_BYTES = 20 * 1024 * 1024;
var ABSOLUTE_MAX_UPLOAD_BYTES = 100 * 1024 * 1024;
var DEFAULT_MAX_VIDEO_BYTES2 = 200 * 1024 * 1024;
var ABSOLUTE_MAX_VIDEO_BYTES2 = 1024 * 1024 * 1024;
var DEFAULT_VIDEO_FRAME_COUNT = 4;
var DEFAULT_VIDEO_FRAME_INTERVAL_SEC = 3;
var DEFAULT_MAX_WIDTH = 960;
var DEFAULT_MAX_FRAMES = 12;
var ABSOLUTE_MAX_VIDEO_FRAMES = 24;
var DEFAULT_RANGE_FPS = 2;
var MAX_FPS = 6;
var DEFAULT_BURST_WINDOW_SEC = 2;
var DEFAULT_ANNOTATION_WIDTH = 1024;
var DEFAULT_ANNOTATION_HEIGHT = 768;
async function readResourceImageForVision(args) {
  const resourceId = resourceIdParam(args);
  const maxBytes = clampInteger(numberParam(args.max_bytes) ?? numberParam(args.maxBytes) ?? DEFAULT_MAX_IMAGE_BYTES, 1, ABSOLUTE_MAX_IMAGE_BYTES);
  const mode = resourceImageReadModeParam(args);
  const maxWidth = clampInteger(numberParam(args.max_width) ?? numberParam(args.maxWidth) ?? DEFAULT_MAX_IMAGE_WIDTH, 1, ABSOLUTE_MAX_IMAGE_DIMENSION);
  const maxHeight = clampInteger(numberParam(args.max_height) ?? numberParam(args.maxHeight) ?? DEFAULT_MAX_IMAGE_HEIGHT, 1, ABSOLUTE_MAX_IMAGE_DIMENSION);
  const file = await downloadResourceFile(resourceId, { maxBytes });
  const sourceMimeType = normalizeImageMimeType(stringParam(args.mime_type) ?? stringParam(args.mimeType) ?? file.contentType);
  if (!sourceMimeType.startsWith("image/")) {
    throw new Error(`resource ${resourceId} is not an image resource; content-type=${file.contentType ?? "unknown"}`);
  }
  if (file.bytes.length > maxBytes) {
    throw new Error(`resource ${resourceId} image is ${file.bytes.length} bytes, above max_bytes=${maxBytes}`);
  }
  const processed = await processResourceImageForVision(resourceId, file.bytes, sourceMimeType, {
    mode,
    maxWidth,
    maxHeight
  });
  const metadata = {
    status: "image_read",
    resource_id: resourceId,
    mode,
    source_mime_type: sourceMimeType,
    source_size_bytes: file.bytes.length,
    source_width: processed.source.width,
    source_height: processed.source.height,
    mime_type: processed.mimeType,
    size_bytes: processed.bytes.length,
    width: processed.width,
    height: processed.height,
    resized: processed.resized,
    ...mode === "fit" ? { max_width: maxWidth, max_height: maxHeight } : {},
    image_payload: "sent_as_mcp_image_content"
  };
  return mcpToolResultWithImages(metadata, [{
    data: processed.bytes.toString("base64"),
    mimeType: processed.mimeType
  }], { includeText: false });
}
async function extractResourceVideoFramesForVision(args) {
  const resourceId = resourceIdParam(args);
  const maxVideoBytes = clampInteger(numberParam(args.max_video_bytes) ?? numberParam(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES2, 1, ABSOLUTE_MAX_VIDEO_BYTES2);
  const maxWidth = clampInteger(numberParam(args.max_width) ?? numberParam(args.maxWidth) ?? DEFAULT_MAX_WIDTH, 128, 1920);
  const imageFormat = stringParam(args.image_format) === "png" || stringParam(args.imageFormat) === "png" ? "png" : "jpeg";
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_extract_frames but was not found");
  const file = await downloadResourceFile(resourceId, { maxBytes: maxVideoBytes });
  const dir = await mkdtempStable("movscript-mcp-video-");
  const inputPath = join32(dir, `resource-${resourceId}.video`);
  const extension = imageFormat === "png" ? "png" : "jpg";
  const mimeType = imageFormat === "png" ? "image/png" : "image/jpeg";
  try {
    await writeFile22(inputPath, file.bytes);
    const video = await probeVideoMetadata(inputPath, ffmpeg).catch(() => ({}));
    const sampling = buildMCPFrameSamplingPlan({
      mode: modeParam(args.mode),
      count: numberParam(args.count) ?? numberParam(args.frame_count),
      maxFrames: numberParam(args.max_frames) ?? numberParam(args.maxFrames),
      timestampsSec: timestampsParam(args.timestamps_sec ?? args.timestampsSec),
      startSec: numberParam(args.start_sec) ?? numberParam(args.startSec),
      endSec: numberParam(args.end_sec) ?? numberParam(args.endSec),
      centerSec: numberParam(args.center_sec) ?? numberParam(args.centerSec),
      windowSec: numberParam(args.window_sec) ?? numberParam(args.windowSec),
      fps: numberParam(args.fps),
      intervalSec: numberParam(args.interval_sec) ?? numberParam(args.intervalSec)
    }, video);
    const frames = [];
    const images = [];
    for (let index = 0; index < sampling.timestampsSec.length; index += 1) {
      const timestamp = sampling.timestampsSec[index] ?? 0;
      const outputPath = join32(dir, `frame-${String(index + 1).padStart(3, "0")}.${extension}`);
      await runFFmpeg2(ffmpeg, [
        "-hide_banner",
        "-loglevel",
        "error",
        "-y",
        "-ss",
        timestamp.toFixed(3),
        "-i",
        inputPath,
        "-frames:v",
        "1",
        "-vf",
        `scale=${maxWidth}:-2:force_original_aspect_ratio=decrease`,
        ...imageFormat === "jpeg" ? ["-q:v", "3"] : [],
        outputPath
      ]);
      const bytes = await readFile3(outputPath);
      frames.push({
        index: index + 1,
        timestamp_sec: timestamp,
        mime_type: mimeType,
        size_bytes: bytes.length,
        image_payload: "sent_as_mcp_image_content"
      });
      images.push({
        label: `frame ${index + 1}, timestamp_sec=${timestamp}`,
        data: bytes.toString("base64"),
        mimeType
      });
    }
    const metadata = {
      status: "frames_extracted",
      resource_id: resourceId,
      source_mime_type: file.contentType ?? "video/unknown",
      source_size_bytes: file.bytes.length,
      max_video_bytes: maxVideoBytes,
      ...Object.keys(video).length > 0 ? { video: publicVideoMetadata(video) } : {},
      sampling: publicSamplingPlan({ ...sampling, returnedFrameCount: frames.length }),
      max_width: maxWidth,
      frames,
      ...sampling.warnings.length > 0 ? { warnings: sampling.warnings } : {},
      message: "Video frames were extracted by MovScript MCP and returned as MCP image content. The original video was not sent to the model."
    };
    return mcpToolResultWithImages(metadata, images);
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function probeResourceVideo(args) {
  const resourceId = resourceIdParam(args);
  const maxVideoBytes = clampInteger(numberParam(args.max_video_bytes) ?? numberParam(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES2, 1, ABSOLUTE_MAX_VIDEO_BYTES2);
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_probe but was not found");
  const file = await downloadResourceFile(resourceId, { maxBytes: maxVideoBytes });
  const dir = await mkdtempStable("movscript-mcp-video-probe-");
  const inputPath = join32(dir, `resource-${resourceId}.video`);
  try {
    await writeFile22(inputPath, file.bytes);
    const video = await probeVideoMetadata(inputPath, ffmpeg);
    return {
      status: "probed",
      resource_id: resourceId,
      source_mime_type: file.contentType ?? "video/unknown",
      source_size_bytes: file.bytes.length,
      max_video_bytes: maxVideoBytes,
      video: publicVideoMetadata(video),
      message: `Video resource #${resourceId} probed.`
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function extractResourceVideoFrameToResource(args) {
  const result = await extractResourceVideoFramesToResources({
    ...args,
    mode: "timestamps",
    timestamps_sec: [numberParam(args.timestamp_sec) ?? numberParam(args.timestampSec) ?? 0],
    max_frames: 1
  });
  const item = Array.isArray(result.items) ? result.items[0] : void 0;
  if (!isRecord26(item)) throw new Error("frame extraction did not create an image resource");
  return {
    status: "created",
    source_resource_id: result.resource_id,
    image_resource_id: item.resource_id,
    resource_id: item.resource_id,
    timestamp_sec: item.timestamp_sec,
    frame: item,
    video: result.video,
    message: `Extracted frame at ${item.timestamp_sec}s to image resource #${item.resource_id}.`
  };
}
async function extractResourceVideoFramesToResources(args) {
  const resourceId = resourceIdParam(args);
  const maxVideoBytes = clampInteger(numberParam(args.max_video_bytes) ?? numberParam(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES2, 1, ABSOLUTE_MAX_VIDEO_BYTES2);
  const maxWidth = clampInteger(numberParam(args.max_width) ?? numberParam(args.maxWidth) ?? DEFAULT_MAX_WIDTH, 128, 1920);
  const imageFormat = stringParam(args.image_format) === "png" || stringParam(args.imageFormat) === "png" ? "png" : "jpeg";
  const maxUploadBytes = clampInteger(numberParam(args.max_upload_bytes) ?? numberParam(args.maxUploadBytes) ?? DEFAULT_MAX_UPLOAD_BYTES, 1, ABSOLUTE_MAX_UPLOAD_BYTES);
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_extract_frames_to_resources but was not found");
  const file = await downloadResourceFile(resourceId, { maxBytes: maxVideoBytes });
  const dir = await mkdtempStable("movscript-mcp-video-frames-");
  const inputPath = join32(dir, `resource-${resourceId}.video`);
  const extension = imageFormat === "png" ? "png" : "jpg";
  const mimeType = imageFormat === "png" ? "image/png" : "image/jpeg";
  try {
    await writeFile22(inputPath, file.bytes);
    const video = await probeVideoMetadata(inputPath, ffmpeg).catch(() => ({}));
    const sampling = buildMCPFrameSamplingPlan({
      mode: modeParam(args.mode),
      count: numberParam(args.count) ?? numberParam(args.frame_count),
      maxFrames: numberParam(args.max_frames) ?? numberParam(args.maxFrames),
      timestampsSec: timestampsParam(args.timestamps_sec ?? args.timestampsSec),
      startSec: numberParam(args.start_sec) ?? numberParam(args.startSec),
      endSec: numberParam(args.end_sec) ?? numberParam(args.endSec),
      centerSec: numberParam(args.center_sec) ?? numberParam(args.centerSec),
      windowSec: numberParam(args.window_sec) ?? numberParam(args.windowSec),
      fps: numberParam(args.fps),
      intervalSec: numberParam(args.interval_sec) ?? numberParam(args.intervalSec)
    }, video);
    const items = [];
    for (let index = 0; index < sampling.timestampsSec.length; index += 1) {
      const timestamp = sampling.timestampsSec[index] ?? 0;
      const outputPath = join32(dir, `frame-${String(index + 1).padStart(3, "0")}.${extension}`);
      await extractVideoFrame(ffmpeg, inputPath, outputPath, {
        timestampSec: timestamp,
        maxWidth,
        imageFormat
      });
      const bytes = await readFile3(outputPath);
      const filename = frameOutputFilename(args, resourceId, timestamp, extension, index);
      const uploaded = await uploadGeneratedResourceBytes({
        bytes,
        mimeType,
        filename,
        folderId: stringParam(args.folder_id) ?? stringParam(args.folderId),
        maxBytes: maxUploadBytes,
        derivative: {
          operation: "video_extract_frame",
          tool: "movscript_resource_video_extract_frames_to_resources",
          inputResourceIds: [resourceId],
          params: {
            timestamp_sec: timestamp,
            max_width: maxWidth,
            image_format: imageFormat
          }
        }
      });
      items.push({
        index: index + 1,
        timestamp_sec: timestamp,
        resource_id: uploaded.resource_id,
        image_resource_id: uploaded.resource_id,
        mime_type: mimeType,
        size_bytes: bytes.length,
        resource: uploaded.resource,
        source: {
          operation: "video_extract_frame",
          source_resource_id: resourceId,
          timestamp_sec: timestamp,
          max_width: maxWidth,
          image_format: imageFormat
        }
      });
    }
    const resourceIds2 = items.map((item) => numberParam(item.resource_id)).filter((item) => item !== void 0);
    return {
      status: "created",
      resource_id: resourceId,
      source_resource_id: resourceId,
      source_mime_type: file.contentType ?? "video/unknown",
      source_size_bytes: file.bytes.length,
      ...Object.keys(video).length > 0 ? { video: publicVideoMetadata(video) } : {},
      sampling: publicSamplingPlan({ ...sampling, returnedFrameCount: items.length }),
      max_width: maxWidth,
      image_format: imageFormat,
      count: items.length,
      resource_ids: resourceIds2,
      image_resource_ids: resourceIds2,
      items,
      ...sampling.warnings.length > 0 ? { warnings: sampling.warnings } : {},
      message: `${items.length} frame resource(s) created from video resource #${resourceId}.`
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function trimResourceVideoToResource(args) {
  const resourceId = resourceIdParam(args);
  const maxVideoBytes = clampInteger(numberParam(args.max_video_bytes) ?? numberParam(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES2, 1, ABSOLUTE_MAX_VIDEO_BYTES2);
  const maxUploadBytes = clampInteger(numberParam(args.max_upload_bytes) ?? numberParam(args.maxUploadBytes) ?? ABSOLUTE_MAX_UPLOAD_BYTES, 1, ABSOLUTE_MAX_UPLOAD_BYTES);
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_trim_to_resource but was not found");
  const file = await downloadResourceFile(resourceId, { maxBytes: maxVideoBytes });
  const dir = await mkdtempStable("movscript-mcp-video-trim-");
  const inputPath = join32(dir, `resource-${resourceId}.video`);
  const outputPath = join32(dir, "trimmed.mp4");
  try {
    await writeFile22(inputPath, file.bytes);
    const video = await probeVideoMetadata(inputPath, ffmpeg).catch(() => ({}));
    const range = clipRangeFromArgs(args, video.durationSec);
    await trimVideoFile(ffmpeg, inputPath, outputPath, range, {
      mode: stringParam(args.mode) === "fast" ? "fast" : "accurate",
      volume: numberParam(args.volume),
      muted: booleanParam3(args.muted)
    });
    const bytes = await readFile3(outputPath);
    const filename = videoOutputFilename(args, `resource-${resourceId}-trim-${range.startSec}-${range.endSec}`);
    const uploaded = await uploadGeneratedResourceBytes({
      bytes,
      mimeType: "video/mp4",
      filename,
      folderId: stringParam(args.folder_id) ?? stringParam(args.folderId),
      maxBytes: maxUploadBytes,
      derivative: {
        operation: "video_trim",
        tool: "movscript_resource_video_trim_to_resource",
        inputResourceIds: [resourceId],
        params: {
          start_sec: range.startSec,
          end_sec: range.endSec,
          mode: stringParam(args.mode) === "fast" ? "fast" : "accurate",
          ...numberParam(args.volume) !== void 0 ? { volume: numberParam(args.volume) } : {},
          ...booleanParam3(args.muted) !== void 0 ? { muted: booleanParam3(args.muted) } : {}
        }
      }
    });
    return {
      status: "created",
      compatibility: neutralVideoResourcePreparationCompatibility(),
      source_resource_id: resourceId,
      video_resource_id: uploaded.resource_id,
      resource_id: uploaded.resource_id,
      mime_type: "video/mp4",
      size_bytes: bytes.length,
      duration_sec: roundTime2(range.endSec - range.startSec),
      start_sec: range.startSec,
      end_sec: range.endSec,
      resource: uploaded.resource,
      source: {
        operation: "video_trim",
        source_resource_id: resourceId,
        start_sec: range.startSec,
        end_sec: range.endSec
      },
      message: `Trimmed video resource #${resourceId} to video resource #${uploaded.resource_id}.`
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function composeResourceVideosToResource(args) {
  const items = videoComposeItems(args.items);
  const maxVideoBytes = clampInteger(numberParam(args.max_video_bytes) ?? numberParam(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES2, 1, ABSOLUTE_MAX_VIDEO_BYTES2);
  const maxUploadBytes = clampInteger(numberParam(args.max_upload_bytes) ?? numberParam(args.maxUploadBytes) ?? ABSOLUTE_MAX_UPLOAD_BYTES, 1, ABSOLUTE_MAX_UPLOAD_BYTES);
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_compose_to_resource but was not found");
  const dir = await mkdtempStable("movscript-mcp-video-compose-");
  try {
    const segmentPaths = [];
    const segments = [];
    for (let index = 0; index < items.length; index += 1) {
      const item = items[index];
      const file = await downloadResourceFile(item.resourceId, { maxBytes: maxVideoBytes });
      const inputPath = join32(dir, `input-${String(index + 1).padStart(3, "0")}.video`);
      const segmentPath = join32(dir, `segment-${String(index + 1).padStart(3, "0")}.mp4`);
      await writeFile22(inputPath, file.bytes);
      const video = await probeVideoMetadata(inputPath, ffmpeg).catch(() => ({}));
      const range = clipRangeFromItem(item, video.durationSec);
      await trimVideoFile(ffmpeg, inputPath, segmentPath, range, {
        mode: "accurate",
        volume: item.volume,
        muted: item.muted
      });
      segmentPaths.push(segmentPath);
      segments.push({
        index: index + 1,
        source_resource_id: item.resourceId,
        start_sec: range.startSec,
        end_sec: range.endSec,
        duration_sec: roundTime2(range.endSec - range.startSec),
        ...Object.keys(video).length > 0 ? { video: publicVideoMetadata(video) } : {}
      });
    }
    const listPath = join32(dir, "concat-list.txt");
    await writeFile22(listPath, segmentPaths.map((path) => `file '${basename2(path)}'`).join("\n"), "utf8");
    const outputPath = join32(dir, "composed.mp4");
    await runFFmpeg2(ffmpeg, [
      "-hide_banner",
      "-loglevel",
      "error",
      "-y",
      "-f",
      "concat",
      "-safe",
      "0",
      "-i",
      listPath,
      "-c",
      "copy",
      "-movflags",
      "+faststart",
      outputPath
    ]);
    const bytes = await readFile3(outputPath);
    const filename = videoOutputFilename(args, `composed-video-${randomUUID2().slice(0, 8)}`);
    const uploaded = await uploadGeneratedResourceBytes({
      bytes,
      mimeType: "video/mp4",
      filename,
      folderId: stringParam(args.folder_id) ?? stringParam(args.folderId),
      maxBytes: maxUploadBytes,
      derivative: {
        operation: "video_compose",
        tool: "movscript_resource_video_compose_to_resource",
        inputResourceIds: items.map((item) => item.resourceId),
        params: { segments }
      }
    });
    const durationSec = segments.reduce((total, item) => total + (numberParam(item.duration_sec) ?? 0), 0);
    return {
      status: "created",
      compatibility: resourceLevelVideoUtility(),
      video_resource_id: uploaded.resource_id,
      resource_id: uploaded.resource_id,
      mime_type: "video/mp4",
      size_bytes: bytes.length,
      duration_sec: roundTime2(durationSec),
      count: segments.length,
      input_resource_ids: items.map((item) => item.resourceId),
      segments,
      resource: uploaded.resource,
      source: {
        operation: "video_compose",
        input_resource_ids: items.map((item) => item.resourceId),
        segments
      },
      message: `Composed ${segments.length} video segment(s) to video resource #${uploaded.resource_id}.`
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function createResourceVideoContactSheetToResource(args) {
  const resourceId = resourceIdParam(args);
  const maxVideoBytes = clampInteger(numberParam(args.max_video_bytes) ?? numberParam(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES2, 1, ABSOLUTE_MAX_VIDEO_BYTES2);
  const maxUploadBytes = clampInteger(numberParam(args.max_upload_bytes) ?? numberParam(args.maxUploadBytes) ?? DEFAULT_MAX_UPLOAD_BYTES, 1, ABSOLUTE_MAX_UPLOAD_BYTES);
  const columns = clampInteger(numberParam(args.columns) ?? 3, 1, 8);
  const count = clampInteger(numberParam(args.count) ?? 9, 1, 64);
  const rows = Math.max(1, Math.ceil(count / columns));
  const thumbWidth = clampInteger(numberParam(args.thumb_width) ?? numberParam(args.thumbWidth) ?? 320, 64, 960);
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_contact_sheet_to_resource but was not found");
  const file = await downloadResourceFile(resourceId, { maxBytes: maxVideoBytes });
  const dir = await mkdtempStable("movscript-mcp-video-sheet-");
  const inputPath = join32(dir, `resource-${resourceId}.video`);
  const outputPath = join32(dir, "contact-sheet.jpg");
  try {
    await writeFile22(inputPath, file.bytes);
    const video = await probeVideoMetadata(inputPath, ffmpeg).catch(() => ({}));
    const durationSec = video.durationSec;
    const intervalSec = positiveNumber22(numberParam(args.interval_sec) ?? numberParam(args.intervalSec)) ?? (durationSec ? Math.max(0.1, durationSec / count) : DEFAULT_VIDEO_FRAME_INTERVAL_SEC);
    await runFFmpeg2(ffmpeg, [
      "-hide_banner",
      "-loglevel",
      "error",
      "-y",
      "-i",
      inputPath,
      "-vf",
      `fps=1/${intervalSec.toFixed(3)},scale=${thumbWidth}:-2:force_original_aspect_ratio=decrease,tile=${columns}x${rows}:padding=8:margin=8`,
      "-frames:v",
      "1",
      "-q:v",
      "3",
      outputPath
    ]);
    const bytes = await readFile3(outputPath);
    const filename = ensureExtension(stringParam(args.filename) ?? stringParam(args.name) ?? `resource-${resourceId}-contact-sheet`, "jpg");
    const uploaded = await uploadGeneratedResourceBytes({
      bytes,
      mimeType: "image/jpeg",
      filename,
      folderId: stringParam(args.folder_id) ?? stringParam(args.folderId),
      maxBytes: maxUploadBytes,
      derivative: {
        operation: "video_contact_sheet",
        tool: "movscript_resource_video_contact_sheet_to_resource",
        inputResourceIds: [resourceId],
        params: {
          columns,
          rows,
          count,
          thumb_width: thumbWidth,
          interval_sec: roundTime2(intervalSec)
        }
      }
    });
    return {
      status: "created",
      source_resource_id: resourceId,
      image_resource_id: uploaded.resource_id,
      resource_id: uploaded.resource_id,
      mime_type: "image/jpeg",
      size_bytes: bytes.length,
      video: publicVideoMetadata(video),
      sheet: {
        columns,
        rows,
        count,
        thumb_width: thumbWidth,
        interval_sec: roundTime2(intervalSec)
      },
      resource: uploaded.resource,
      source: {
        operation: "video_contact_sheet",
        source_resource_id: resourceId,
        columns,
        rows,
        count,
        thumb_width: thumbWidth,
        interval_sec: roundTime2(intervalSec)
      },
      message: `Created contact sheet image resource #${uploaded.resource_id} from video resource #${resourceId}.`
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function extractResourceVideoAudioToResource(args) {
  const resourceId = resourceIdParam(args);
  const maxVideoBytes = clampInteger(numberParam(args.max_video_bytes) ?? numberParam(args.maxVideoBytes) ?? DEFAULT_MAX_VIDEO_BYTES2, 1, ABSOLUTE_MAX_VIDEO_BYTES2);
  const maxUploadBytes = clampInteger(numberParam(args.max_upload_bytes) ?? numberParam(args.maxUploadBytes) ?? ABSOLUTE_MAX_UPLOAD_BYTES, 1, ABSOLUTE_MAX_UPLOAD_BYTES);
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_video_extract_audio_to_resource but was not found");
  const file = await downloadResourceFile(resourceId, { maxBytes: maxVideoBytes });
  const dir = await mkdtempStable("movscript-mcp-video-audio-");
  const inputPath = join32(dir, `resource-${resourceId}.video`);
  const outputPath = join32(dir, "audio.m4a");
  try {
    await writeFile22(inputPath, file.bytes);
    const range = optionalAudioRangeFromArgs(args);
    await runFFmpeg2(ffmpeg, [
      "-hide_banner",
      "-loglevel",
      "error",
      "-y",
      ...range ? ["-ss", range.startSec.toFixed(3)] : [],
      "-i",
      inputPath,
      ...range ? ["-t", (range.endSec - range.startSec).toFixed(3)] : [],
      "-vn",
      "-map",
      "0:a:0?",
      "-c:a",
      "aac",
      "-b:a",
      "128k",
      outputPath
    ]);
    const bytes = await readFile3(outputPath);
    const filename = ensureExtension(stringParam(args.filename) ?? stringParam(args.name) ?? `resource-${resourceId}-audio`, "m4a");
    const uploaded = await uploadGeneratedResourceBytes({
      bytes,
      mimeType: "audio/mp4",
      filename,
      folderId: stringParam(args.folder_id) ?? stringParam(args.folderId),
      maxBytes: maxUploadBytes,
      derivative: {
        operation: "video_extract_audio",
        tool: "movscript_resource_video_extract_audio_to_resource",
        inputResourceIds: [resourceId],
        params: {
          ...range ? { start_sec: range.startSec, end_sec: range.endSec } : {}
        }
      }
    });
    return {
      status: "created",
      source_resource_id: resourceId,
      audio_resource_id: uploaded.resource_id,
      resource_id: uploaded.resource_id,
      mime_type: "audio/mp4",
      size_bytes: bytes.length,
      ...range ? { start_sec: range.startSec, end_sec: range.endSec, duration_sec: roundTime2(range.endSec - range.startSec) } : {},
      resource: uploaded.resource,
      source: {
        operation: "video_extract_audio",
        source_resource_id: resourceId,
        ...range ? { start_sec: range.startSec, end_sec: range.endSec } : {}
      },
      message: `Extracted audio resource #${uploaded.resource_id} from video resource #${resourceId}.`
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function transformResourceImageToResource(args) {
  const resourceId = resourceIdParam(args);
  const maxSourceBytes = clampInteger(numberParam(args.max_source_bytes) ?? numberParam(args.maxSourceBytes) ?? DEFAULT_MAX_IMAGE_BYTES, 1, ABSOLUTE_MAX_IMAGE_BYTES);
  const maxUploadBytes = clampInteger(numberParam(args.max_upload_bytes) ?? numberParam(args.maxUploadBytes) ?? DEFAULT_MAX_UPLOAD_BYTES, 1, ABSOLUTE_MAX_UPLOAD_BYTES);
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_image_transform_to_resource but was not found");
  const file = await downloadResourceFile(resourceId, { maxBytes: maxSourceBytes });
  const sourceMimeType = normalizeImageMimeType(stringParam(args.mime_type) ?? stringParam(args.mimeType) ?? file.contentType);
  const outputFormat = imageOutputFormat(args, sourceMimeType);
  const outputMimeType = imageMimeTypeForFormat(outputFormat);
  const outputExtension = imageExtensionForFormat(outputFormat);
  const dir = await mkdtempStable("movscript-mcp-image-transform-");
  const inputPath = join32(dir, `resource-${resourceId}${extensionForMimeType(sourceMimeType) || ".image"}`);
  const outputPath = join32(dir, `transformed.${outputExtension}`);
  try {
    await writeFile22(inputPath, file.bytes);
    const source = await probeImageMetadata(inputPath, ffmpeg);
    await decodeImageFrame(inputPath, ffmpeg);
    const transform = imageTransformSpec(args, source);
    const filters = imageTransformFilters(transform);
    await runFFmpeg2(ffmpeg, [
      "-hide_banner",
      "-loglevel",
      "error",
      "-y",
      "-i",
      inputPath,
      "-frames:v",
      "1",
      ...filters.length > 0 ? ["-vf", filters.join(",")] : [],
      ...outputFormat === "jpeg" ? ["-q:v", "2"] : [],
      ...outputFormat === "webp" ? ["-lossless", "1"] : [],
      outputPath
    ]);
    const bytes = await readFile3(outputPath);
    const filename = ensureExtension(stringParam(args.filename) ?? stringParam(args.name) ?? `resource-${resourceId}-transform`, outputExtension);
    const uploaded = await uploadGeneratedResourceBytes({
      bytes,
      mimeType: outputMimeType,
      filename,
      folderId: stringParam(args.folder_id) ?? stringParam(args.folderId),
      maxBytes: maxUploadBytes,
      derivative: {
        operation: "image_transform",
        tool: "movscript_resource_image_transform_to_resource",
        inputResourceIds: [resourceId],
        params: { ...transform.public, output_format: outputFormat }
      }
    });
    return {
      status: "created",
      source_resource_id: resourceId,
      image_resource_id: uploaded.resource_id,
      resource_id: uploaded.resource_id,
      source_mime_type: sourceMimeType,
      source_size_bytes: file.bytes.length,
      source_width: source.width,
      source_height: source.height,
      mime_type: outputMimeType,
      size_bytes: bytes.length,
      width: transform.output.width,
      height: transform.output.height,
      resource: uploaded.resource,
      transform: { ...transform.public, output_format: outputFormat },
      message: `Transformed image resource #${resourceId} to image resource #${uploaded.resource_id}.`
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function annotateResourceImage(args) {
  const source = await loadImageAnnotationSource(args);
  const natural = parseImageSize(source.bytes, source.mimeType);
  const width = clampInteger(numberParam(args.width) ?? natural?.width ?? DEFAULT_ANNOTATION_WIDTH, 1, 16384);
  const height = clampInteger(numberParam(args.height) ?? natural?.height ?? DEFAULT_ANNOTATION_HEIGHT, 1, 16384);
  const shapes = annotationShapes(args.annotations ?? args.shapes);
  if (shapes.length === 0) throw new Error("annotations must contain at least one shape");
  const title = stringParam(args.title) ?? "MovScript agent annotation";
  const svg = renderAnnotationSVG({
    source,
    width,
    height,
    title,
    shapes,
    note: stringParam(args.note)
  });
  const output = await resolveAnnotationOutputPath(args, title);
  const outputPath = output.path;
  await mkdir22(dirname6(outputPath), { recursive: true });
  await writeFile22(outputPath, svg, "utf8");
  const bytes = Buffer.from(svg, "utf8");
  const metadata = {
    status: "annotated",
    artifact_path: outputPath,
    mime_type: "image/svg+xml",
    size_bytes: bytes.length,
    width,
    height,
    source: source.public,
    annotation_count: shapes.length,
    annotations: shapes.map(publicAnnotationShape),
    artifact_location: output.location,
    ...output.cacheDir ? { cache_dir: output.cacheDir } : {},
    image_payload: "stored_as_local_artifact",
    mcp_image_content: false,
    message: "Annotated guidance image was rendered as SVG and stored at artifact_path. The image bytes were not returned as MCP image content. Upload artifact_path with movscript_resource_upload to store it as a RawResource for generation."
  };
  return mcpToolResultMetadata(metadata);
}
async function uploadAgentImageResource(args) {
  const input = await loadUploadInput(args);
  const maxBytes = clampInteger(numberParam(args.max_bytes) ?? numberParam(args.maxBytes) ?? DEFAULT_MAX_UPLOAD_BYTES, 1, ABSOLUTE_MAX_UPLOAD_BYTES);
  if (input.bytes.length > maxBytes) {
    throw new Error(`upload input is ${input.bytes.length} bytes, above max_bytes=${maxBytes}`);
  }
  const filename = uploadFilename(args, input.mimeType, input.filename);
  const form = new FormData();
  const blob = new Blob([new Uint8Array(input.bytes)], { type: input.mimeType });
  form.append("file", blob, filename);
  const folderId = stringParam(args.folder_id) ?? stringParam(args.folderId);
  if (folderId) form.append("folder_id", folderId);
  const resource2 = await backendPostMultipart("/resources/upload", form);
  const resourceId = numericResourceId(resource2);
  if (resourceId === void 0) throw new Error("resource upload response did not include a valid resource ID");
  return {
    status: "uploaded",
    resource_id: resourceId,
    resource: resource2,
    source: input.public,
    filename,
    mime_type: input.mimeType,
    size_bytes: input.bytes.length,
    message: "Agent-created image was uploaded to MovScript RawResource library. Use resource_id in generation input_resource_ids/reference_resource_ids."
  };
}
async function uploadAgentImageResources(args) {
  const items = batchItems(args.items, "items");
  const continueOnError = booleanParam3(args.continue_on_error) ?? booleanParam3(args.continueOnError) ?? true;
  const maxConcurrency = continueOnError ? clampInteger(numberParam(args.max_concurrency) ?? numberParam(args.maxConcurrency) ?? 3, 1, 8) : 1;
  const defaults = batchDefaults2(args, /* @__PURE__ */ new Set(["items", "continue_on_error", "continueOnError", "max_concurrency", "maxConcurrency"]));
  const results = await runBatch(items, maxConcurrency, async (item, index) => {
    const result = await uploadAgentImageResource({ ...defaults, ...item });
    return {
      index,
      status: "uploaded",
      resource_id: result.resource_id,
      resource: result.resource,
      source: result.source,
      filename: result.filename,
      mime_type: result.mime_type,
      size_bytes: result.size_bytes,
      message: result.message
    };
  }, continueOnError);
  const successItems = results.filter((item) => item.status !== "error" && item.status !== "skipped");
  const failedItems = results.filter((item) => item.status === "error");
  const resourceIds2 = successItems.map((item) => numberParam(item.resource_id)).filter((value) => value !== void 0);
  return {
    status: batchStatus(successItems.length, failedItems.length),
    total: items.length,
    success_count: successItems.length,
    failed_count: failedItems.length,
    items: results,
    resource_ids: resourceIds2,
    message: `${successItems.length}/${items.length} resource upload(s) completed.`
  };
}
async function readResourceFileBlob(resourceId, options = {}) {
  const maxBytes = clampInteger(options.maxBytes ?? DEFAULT_MAX_IMAGE_BYTES, 1, ABSOLUTE_MAX_IMAGE_BYTES);
  const file = await downloadResourceFile(resourceId, { maxBytes });
  if (file.bytes.length > maxBytes) {
    throw new Error(`resource ${resourceId} is ${file.bytes.length} bytes, above maxBytes=${maxBytes}`);
  }
  return {
    mimeType: file.contentType ?? "application/octet-stream",
    blob: file.bytes.toString("base64"),
    sizeBytes: file.bytes.length
  };
}
function buildMCPFrameSamplingPlan(input, video = {}) {
  const warnings = [];
  const maxFrames = clampInteger(input.maxFrames ?? DEFAULT_MAX_FRAMES, 1, ABSOLUTE_MAX_VIDEO_FRAMES);
  const mode = resolveFrameMode(input);
  const durationSec = video.durationSec;
  const requested = requestedFrameCount(input, mode, durationSec);
  let timestampsSec = [];
  if (mode === "timestamps") {
    timestampsSec = normalizeExplicitTimestamps(input.timestampsSec, maxFrames, durationSec, warnings);
  } else if (mode === "range") {
    const range = normalizeRange(input.startSec, input.endSec, durationSec, warnings);
    timestampsSec = timestampsForRange(range.startSec, range.endSec, input, maxFrames, warnings);
    return {
      mode,
      timestampsSec,
      requestedFrameCount: requested,
      returnedFrameCount: timestampsSec.length,
      maxFrames,
      startSec: range.startSec,
      endSec: range.endSec,
      ...samplingFps(input) !== void 0 ? { fps: samplingFps(input) } : {},
      ...samplingIntervalSec(input) !== void 0 ? { intervalSec: samplingIntervalSec(input) } : {},
      warnings
    };
  } else if (mode === "burst") {
    const burst = normalizeBurst(input.centerSec, input.windowSec, durationSec, warnings);
    timestampsSec = timestampsForRange(burst.startSec, burst.endSec, input, maxFrames, warnings);
    return {
      mode,
      timestampsSec,
      requestedFrameCount: requested,
      returnedFrameCount: timestampsSec.length,
      maxFrames,
      startSec: burst.startSec,
      endSec: burst.endSec,
      centerSec: burst.centerSec,
      windowSec: burst.windowSec,
      ...samplingFps(input) !== void 0 ? { fps: samplingFps(input) } : {},
      ...samplingIntervalSec(input) !== void 0 ? { intervalSec: samplingIntervalSec(input) } : {},
      warnings
    };
  } else {
    timestampsSec = overviewTimestamps(input.count ?? DEFAULT_VIDEO_FRAME_COUNT, maxFrames, durationSec);
  }
  if (timestampsSec.length === 0) timestampsSec = durationSec && durationSec > 0 ? [roundTime2(durationSec / 2)] : [0];
  return {
    mode,
    timestampsSec,
    requestedFrameCount: requested,
    returnedFrameCount: timestampsSec.length,
    maxFrames,
    warnings
  };
}
async function extractVideoFrame(ffmpeg, inputPath, outputPath, input) {
  await runFFmpeg2(ffmpeg, [
    "-hide_banner",
    "-loglevel",
    "error",
    "-y",
    "-ss",
    input.timestampSec.toFixed(3),
    "-i",
    inputPath,
    "-frames:v",
    "1",
    "-vf",
    `scale=${input.maxWidth}:-2:force_original_aspect_ratio=decrease`,
    ...input.imageFormat === "jpeg" ? ["-q:v", "3"] : [],
    outputPath
  ]);
}
async function trimVideoFile(ffmpeg, inputPath, outputPath, range, options) {
  const durationSec = Math.max(0.1, range.endSec - range.startSec);
  if (options.mode === "fast" && !options.muted && options.volume === void 0) {
    await runFFmpeg2(ffmpeg, [
      "-hide_banner",
      "-loglevel",
      "error",
      "-y",
      "-ss",
      range.startSec.toFixed(3),
      "-i",
      inputPath,
      "-t",
      durationSec.toFixed(3),
      "-map",
      "0:v:0",
      "-map",
      "0:a?",
      "-c",
      "copy",
      "-movflags",
      "+faststart",
      outputPath
    ]);
    return;
  }
  const args = [
    "-hide_banner",
    "-loglevel",
    "error",
    "-y",
    "-i",
    inputPath,
    "-ss",
    range.startSec.toFixed(3),
    "-t",
    durationSec.toFixed(3),
    "-map",
    "0:v:0",
    "-vf",
    "setsar=1",
    "-c:v",
    "libx264",
    "-pix_fmt",
    "yuv420p",
    "-preset",
    "veryfast",
    "-movflags",
    "+faststart"
  ];
  const volume = options.volume === void 0 ? 100 : clampNumber2(options.volume, 0, 200);
  if (options.muted || volume <= 0) {
    args.push("-an");
  } else {
    args.push("-map", "0:a?");
    if (volume !== 100) args.push("-filter:a", `volume=${(volume / 100).toFixed(2)}`);
    args.push("-c:a", "aac", "-b:a", "128k");
  }
  args.push(outputPath);
  await runFFmpeg2(ffmpeg, args);
}
function clipRangeFromArgs(args, durationSec) {
  return normalizeClipRange({
    startSec: numberParam(args.start_sec) ?? numberParam(args.startSec) ?? 0,
    endSec: numberParam(args.end_sec) ?? numberParam(args.endSec),
    durationSec: numberParam(args.duration_sec) ?? numberParam(args.durationSec),
    sourceDurationSec: durationSec
  });
}
function optionalAudioRangeFromArgs(args) {
  const startSec = numberParam(args.start_sec) ?? numberParam(args.startSec);
  const endSec = numberParam(args.end_sec) ?? numberParam(args.endSec);
  const durationSec = numberParam(args.duration_sec) ?? numberParam(args.durationSec);
  if (startSec === void 0 && endSec === void 0 && durationSec === void 0) return void 0;
  return normalizeClipRange({
    startSec: startSec ?? 0,
    endSec,
    durationSec
  });
}
function clipRangeFromItem(item, durationSec) {
  return normalizeClipRange({
    startSec: item.startSec ?? 0,
    endSec: item.endSec,
    durationSec: item.durationSec,
    sourceDurationSec: durationSec
  });
}
function normalizeClipRange(input) {
  const startSec = roundTime2(Math.max(0, input.startSec));
  const endCandidate = input.endSec ?? (input.durationSec !== void 0 ? startSec + Math.max(0, input.durationSec) : input.sourceDurationSec);
  if (endCandidate === void 0) throw new Error("end_sec or duration_sec is required when video duration cannot be probed");
  const endSec = roundTime2(clampToDuration(Math.max(0, endCandidate), input.sourceDurationSec));
  if (endSec <= startSec) throw new Error("video clip range must have end_sec greater than start_sec");
  return { startSec, endSec };
}
function videoComposeItems(value) {
  if (!Array.isArray(value) || value.length === 0) throw new Error("items must contain at least one video resource item");
  return value.map((item, index) => {
    if (!isRecord26(item)) throw new Error(`items[${index}] must be an object`);
    const resourceId = optionalResourceIdParam(item);
    if (resourceId === void 0) throw new Error(`items[${index}].resource_id is required`);
    const startSec = numberParam(item.start_sec) ?? numberParam(item.startSec);
    const endSec = numberParam(item.end_sec) ?? numberParam(item.endSec);
    const durationSec = numberParam(item.duration_sec) ?? numberParam(item.durationSec);
    const volume = numberParam(item.volume);
    const muted = booleanParam3(item.muted);
    const normalized = {
      resourceId
    };
    if (startSec !== void 0) normalized.startSec = startSec;
    if (endSec !== void 0) normalized.endSec = endSec;
    if (durationSec !== void 0) normalized.durationSec = durationSec;
    if (volume !== void 0) normalized.volume = volume;
    if (muted !== void 0) normalized.muted = muted;
    return normalized;
  });
}
async function uploadGeneratedResourceBytes(input) {
  if (input.bytes.length > input.maxBytes) {
    throw new Error(`generated resource is ${input.bytes.length} bytes, above max_upload_bytes=${input.maxBytes}`);
  }
  const form = new FormData();
  const blob = new Blob([new Uint8Array(input.bytes)], { type: input.mimeType });
  form.append("file", blob, input.filename);
  if (input.folderId) form.append("folder_id", input.folderId);
  if (input.derivative) {
    form.append("derivative", JSON.stringify({
      operation: input.derivative.operation,
      ...input.derivative.tool ? { tool: input.derivative.tool } : {},
      input_resource_ids: input.derivative.inputResourceIds ?? [],
      params: input.derivative.params ?? {}
    }));
  }
  const resource2 = await backendPostMultipart("/resources/upload", form);
  const resourceId = numericResourceId(resource2);
  if (resourceId === void 0) throw new Error("resource upload response did not include a valid resource ID");
  return { resource_id: resourceId, resource: resource2 };
}
function frameOutputFilename(args, resourceId, timestampSec, extension, index) {
  const explicit = stringParam(args.filename) ?? stringParam(args.name);
  if (explicit) {
    if (index === 0) return ensureExtension(explicit, extension);
    return ensureExtension(`${safeFilename(basename2(explicit, extname2(explicit)))}-${String(index + 1).padStart(3, "0")}`, extension);
  }
  return `resource-${resourceId}-frame-${timestampSec.toFixed(3).replace(".", "_")}.${extension}`;
}
function videoOutputFilename(args, fallbackBase) {
  const explicit = stringParam(args.filename) ?? stringParam(args.name);
  return ensureExtension(explicit ?? safeFilename(fallbackBase), "mp4");
}
function ensureExtension(value, extension) {
  const normalizedExtension = extension.startsWith(".") ? extension.slice(1) : extension;
  const currentExtension = extname2(value);
  if (currentExtension.toLowerCase() === `.${normalizedExtension.toLowerCase()}`) return value;
  const base = currentExtension ? value.slice(0, -currentExtension.length) : value;
  return `${safeFilename(base)}.${normalizedExtension}`;
}
function imageOutputFormat(args, sourceMimeType) {
  const explicit = stringParam(args.output_format) ?? stringParam(args.outputFormat) ?? stringParam(args.image_format) ?? stringParam(args.imageFormat);
  switch ((explicit ?? "").toLowerCase()) {
    case "jpg":
    case "jpeg":
      return "jpeg";
    case "webp":
      return "webp";
    case "png":
      return "png";
    case "":
      if (sourceMimeType === "image/jpeg" || sourceMimeType === "image/jpg") return "jpeg";
      if (sourceMimeType === "image/webp") return "webp";
      return "png";
    default:
      throw new Error("output_format must be one of jpeg, png, or webp");
  }
}
function imageMimeTypeForFormat(format) {
  switch (format) {
    case "jpeg":
      return "image/jpeg";
    case "webp":
      return "image/webp";
    case "png":
    default:
      return "image/png";
  }
}
function imageExtensionForFormat(format) {
  return format === "jpeg" ? "jpg" : format;
}
function imageTransformSpec(args, source) {
  const cropX = clampInteger(numberParam(args.crop_x) ?? numberParam(args.cropX) ?? 0, 0, source.width);
  const cropY = clampInteger(numberParam(args.crop_y) ?? numberParam(args.cropY) ?? 0, 0, source.height);
  const cropWidth = positiveInteger3(numberParam(args.crop_width) ?? numberParam(args.cropWidth));
  const cropHeight = positiveInteger3(numberParam(args.crop_height) ?? numberParam(args.cropHeight));
  const crop = cropWidth !== void 0 || cropHeight !== void 0 ? {
    x: cropX,
    y: cropY,
    width: Math.min(cropWidth ?? source.width - cropX, source.width - cropX),
    height: Math.min(cropHeight ?? source.height - cropY, source.height - cropY)
  } : void 0;
  if (crop && (crop.width <= 0 || crop.height <= 0)) throw new Error("crop rectangle must overlap the source image");
  const base = crop ? { width: crop.width, height: crop.height } : source;
  const width = positiveInteger3(numberParam(args.width));
  const height = positiveInteger3(numberParam(args.height));
  const maxWidth = positiveInteger3(numberParam(args.max_width) ?? numberParam(args.maxWidth));
  const maxHeight = positiveInteger3(numberParam(args.max_height) ?? numberParam(args.maxHeight));
  const resize = width !== void 0 || height !== void 0 || maxWidth !== void 0 || maxHeight !== void 0 ? { width, height, maxWidth, maxHeight } : void 0;
  const output = computeImageTransformOutputDimensions(base, resize);
  const publicSpec = {
    source_width: source.width,
    source_height: source.height,
    output_width: output.width,
    output_height: output.height
  };
  if (crop) publicSpec.crop = { x: crop.x, y: crop.y, width: crop.width, height: crop.height };
  if (resize) {
    publicSpec.resize = {
      ...width !== void 0 ? { width } : {},
      ...height !== void 0 ? { height } : {},
      ...maxWidth !== void 0 ? { max_width: maxWidth } : {},
      ...maxHeight !== void 0 ? { max_height: maxHeight } : {}
    };
  }
  return { crop, resize, output, public: publicSpec };
}
function computeImageTransformOutputDimensions(source, resize) {
  if (!resize) return source;
  if (resize.width !== void 0 && resize.height !== void 0) {
    return { width: resize.width, height: resize.height };
  }
  if (resize.width !== void 0) {
    return { width: resize.width, height: Math.max(1, Math.round(source.height * (resize.width / source.width))) };
  }
  if (resize.height !== void 0) {
    return { width: Math.max(1, Math.round(source.width * (resize.height / source.height))), height: resize.height };
  }
  return fitDimensions(source, resize.maxWidth ?? source.width, resize.maxHeight ?? source.height);
}
function imageTransformFilters(transform) {
  const filters = [];
  if (transform.crop) {
    filters.push(`crop=${transform.crop.width}:${transform.crop.height}:${transform.crop.x}:${transform.crop.y}`);
  }
  if (transform.resize) {
    const resize = transform.resize;
    if (resize.width !== void 0 && resize.height !== void 0) {
      filters.push(`scale=${resize.width}:${resize.height}`);
    } else if (resize.width !== void 0) {
      filters.push(`scale=${resize.width}:-2`);
    } else if (resize.height !== void 0) {
      filters.push(`scale=-2:${resize.height}`);
    } else {
      filters.push(`scale=${resize.maxWidth ?? -2}:${resize.maxHeight ?? -2}:force_original_aspect_ratio=decrease`);
    }
  }
  return filters;
}
async function downloadResourceFile(resourceId, options = {}) {
  return backendGetBinary(`/resources/${encodeURIComponent(String(resourceId))}/file`, options);
}
async function processResourceImageForVision(resourceId, bytes, mimeType, options) {
  const ffmpeg = resolveFFmpegPath();
  if (!ffmpeg) throw new Error("ffmpeg is required for movscript_resource_image_read image validation and resizing but was not found");
  const dir = await mkdtempStable("movscript-mcp-image-");
  const inputPath = join32(dir, `resource-${resourceId}${extensionForMimeType(mimeType) || ".image"}`);
  try {
    await writeFile22(inputPath, bytes);
    const source = await probeImageMetadata(inputPath, ffmpeg);
    await decodeImageFrame(inputPath, ffmpeg);
    if (options.mode === "original") {
      return {
        bytes,
        mimeType,
        width: source.width,
        height: source.height,
        resized: false,
        source
      };
    }
    const outputDimensions = fitDimensions(source, options.maxWidth, options.maxHeight);
    if (outputDimensions.width === source.width && outputDimensions.height === source.height) {
      return {
        bytes,
        mimeType,
        width: source.width,
        height: source.height,
        resized: false,
        source
      };
    }
    const outputPath = join32(dir, "image-fit.png");
    await runFFmpeg2(ffmpeg, [
      "-hide_banner",
      "-loglevel",
      "error",
      "-y",
      "-i",
      inputPath,
      "-frames:v",
      "1",
      "-vf",
      `scale=${options.maxWidth}:${options.maxHeight}:force_original_aspect_ratio=decrease`,
      outputPath
    ]);
    const outputBytes = await readFile3(outputPath);
    return {
      bytes: outputBytes,
      mimeType: "image/png",
      width: outputDimensions.width,
      height: outputDimensions.height,
      resized: true,
      source
    };
  } finally {
    await rm22(dir, { recursive: true, force: true }).catch(() => void 0);
  }
}
async function probeImageMetadata(path, ffmpeg) {
  const ffprobe = ffprobePath(ffmpeg);
  const result = await runCommand2(ffprobe, [
    "-v",
    "error",
    "-select_streams",
    "v:0",
    "-show_entries",
    "stream=width,height",
    "-of",
    "json",
    path
  ]).catch((error) => {
    throw new Error(`unable to read image dimensions: ${error instanceof Error ? error.message : String(error)}`);
  });
  const parsed = JSON.parse(result.stdout);
  const stream = parsed.streams?.find((item) => positiveInteger3(item.width) !== void 0 && positiveInteger3(item.height) !== void 0);
  const width = positiveInteger3(stream?.width);
  const height = positiveInteger3(stream?.height);
  if (width === void 0 || height === void 0) throw new Error("unable to read image dimensions");
  return { width, height };
}
async function decodeImageFrame(path, ffmpeg) {
  await runFFmpeg2(ffmpeg, [
    "-hide_banner",
    "-loglevel",
    "error",
    "-i",
    path,
    "-frames:v",
    "1",
    "-f",
    "null",
    "-"
  ]).catch((error) => {
    throw new Error(`unable to decode image bytes: ${error instanceof Error ? error.message : String(error)}`);
  });
}
function fitDimensions(source, maxWidth, maxHeight) {
  if (source.width <= maxWidth && source.height <= maxHeight) return source;
  const scale = Math.min(maxWidth / source.width, maxHeight / source.height);
  return {
    width: Math.max(1, Math.floor(source.width * scale)),
    height: Math.max(1, Math.floor(source.height * scale))
  };
}
function mcpToolResultWithImages(metadata, images, options = {}) {
  const includeText = options.includeText ?? true;
  return {
    content: [
      ...includeText ? [{
        type: "text",
        text: JSON.stringify(metadata, null, 2)
      }] : [],
      ...images.flatMap((image) => [
        ...includeText && image.label ? [{ type: "text", text: image.label }] : [],
        { type: "image", data: image.data, mimeType: image.mimeType }
      ])
    ],
    data: metadata
  };
}
function mcpToolResultMetadata(metadata) {
  return {
    content: [
      {
        type: "text",
        text: JSON.stringify(metadata, null, 2)
      }
    ],
    data: metadata
  };
}
function runFFmpeg2(ffmpeg, args) {
  return new Promise((resolveRun, reject) => {
    const child = spawn22(ffmpeg, args, { stdio: ["ignore", "ignore", "pipe"] });
    let stderr = "";
    child.stderr?.on("data", (chunk) => {
      stderr += String(chunk);
    });
    child.on("error", (error) => reject(error instanceof Error ? error : new Error(String(error))));
    child.on("exit", (code) => {
      if (code === 0) {
        resolveRun();
        return;
      }
      reject(new Error(stderr.trim() || `ffmpeg exited with code ${code ?? "unknown"}`));
    });
  });
}
function runCommand2(command, args) {
  return new Promise((resolveRun, reject) => {
    const child = spawn22(command, args, { stdio: ["ignore", "pipe", "pipe"] });
    let stdout = "";
    let stderr = "";
    child.stdout?.on("data", (chunk) => {
      stdout += String(chunk);
    });
    child.stderr?.on("data", (chunk) => {
      stderr += String(chunk);
    });
    child.on("error", (error) => reject(error instanceof Error ? error : new Error(String(error))));
    child.on("exit", (code) => {
      if (code === 0) {
        resolveRun({ stdout });
        return;
      }
      reject(new Error(stderr.trim() || `${command} exited with code ${code ?? "unknown"}`));
    });
  });
}
async function probeVideoMetadata(path, ffmpeg) {
  const ffprobe = ffprobePath(ffmpeg);
  const result = await runCommand2(ffprobe, [
    "-v",
    "error",
    "-show_entries",
    "format=duration:stream=width,height,avg_frame_rate,r_frame_rate",
    "-of",
    "json",
    path
  ]);
  const parsed = JSON.parse(result.stdout);
  const stream = parsed.streams?.find((item) => typeof item.width === "number" && typeof item.height === "number") ?? parsed.streams?.[0];
  const durationSec = positiveNumber22(Number(parsed.format?.duration));
  const fps = parseFrameRate(stream?.avg_frame_rate) ?? parseFrameRate(stream?.r_frame_rate);
  return {
    ...durationSec !== void 0 ? { durationSec } : {},
    ...positiveInteger3(stream?.width) !== void 0 ? { width: positiveInteger3(stream?.width) } : {},
    ...positiveInteger3(stream?.height) !== void 0 ? { height: positiveInteger3(stream?.height) } : {},
    ...fps !== void 0 ? { fps } : {}
  };
}
function ffprobePath(ffmpeg) {
  if (ffmpeg === "ffmpeg") return "ffprobe";
  const suffix = process.platform === "win32" ? "ffmpeg.exe" : "ffmpeg";
  const probeSuffix = process.platform === "win32" ? "ffprobe.exe" : "ffprobe";
  return ffmpeg.endsWith(suffix) ? `${ffmpeg.slice(0, -suffix.length)}${probeSuffix}` : "ffprobe";
}
function resolveFrameMode(input) {
  if (input.mode) return input.mode;
  if (input.timestampsSec && input.timestampsSec.length > 0) return "timestamps";
  if (input.centerSec !== void 0) return "burst";
  if (input.startSec !== void 0 || input.endSec !== void 0) return "range";
  return "overview";
}
function requestedFrameCount(input, mode, durationSec) {
  if (mode === "timestamps") return input.timestampsSec?.length ?? 0;
  if (mode === "range" || mode === "burst") {
    const startSec = mode === "burst" ? Math.max(0, (input.centerSec ?? 0) - (input.windowSec ?? DEFAULT_BURST_WINDOW_SEC) / 2) : input.startSec ?? 0;
    const endSec = mode === "burst" ? (input.centerSec ?? 0) + (input.windowSec ?? DEFAULT_BURST_WINDOW_SEC) / 2 : input.endSec ?? durationSec ?? startSec;
    const interval = samplingIntervalSec(input) ?? 1 / (samplingFps(input) ?? DEFAULT_RANGE_FPS);
    return Math.max(1, Math.floor(Math.max(0, endSec - startSec) / interval) + 1);
  }
  return input.count ?? DEFAULT_VIDEO_FRAME_COUNT;
}
function normalizeExplicitTimestamps(timestampsSec, maxFrames, durationSec, warnings) {
  const explicit = (timestampsSec ?? []).filter((value) => Number.isFinite(value) && value >= 0).map((value) => clampToDuration(value, durationSec)).map(roundTime2);
  if (explicit.length > maxFrames) {
    warnings.push(`Requested ${explicit.length} timestamps; returned the first ${maxFrames} due to max_frames.`);
  }
  return uniqueSorted2(explicit).slice(0, maxFrames);
}
function overviewTimestamps(count, maxFrames, durationSec) {
  const frameCount = clampInteger(count, 1, maxFrames);
  if (!durationSec || durationSec <= 0) {
    const interval = DEFAULT_VIDEO_FRAME_INTERVAL_SEC;
    return Array.from({ length: frameCount }, (_unused, index) => roundTime2(index * interval));
  }
  if (frameCount <= 1) return [roundTime2(durationSec / 2)];
  const margin = Math.min(1, Math.max(0.1, durationSec * 0.08));
  const start = Math.min(durationSec, margin);
  const end = Math.max(start, durationSec - margin);
  const step = frameCount === 1 ? 0 : (end - start) / (frameCount - 1);
  return uniqueSorted2(Array.from({ length: frameCount }, (_unused, index) => roundTime2(start + step * index)));
}
function normalizeRange(start, end, durationSec, warnings) {
  const normalizedStart = clampToDuration(Math.max(0, start ?? 0), durationSec);
  const fallbackEnd = durationSec ?? normalizedStart;
  let normalizedEnd = clampToDuration(Math.max(0, end ?? fallbackEnd), durationSec);
  if (normalizedEnd < normalizedStart) {
    warnings.push("end_sec was before start_sec; the range was normalized to a single timestamp.");
    normalizedEnd = normalizedStart;
  }
  return { startSec: roundTime2(normalizedStart), endSec: roundTime2(normalizedEnd) };
}
function normalizeBurst(center, windowSec, durationSec, warnings) {
  const normalizedWindow = Math.max(0, windowSec ?? DEFAULT_BURST_WINDOW_SEC);
  const normalizedCenter = clampToDuration(Math.max(0, center ?? 0), durationSec);
  const startSec = clampToDuration(Math.max(0, normalizedCenter - normalizedWindow / 2), durationSec);
  let endSec = clampToDuration(normalizedCenter + normalizedWindow / 2, durationSec);
  if (endSec < startSec) {
    warnings.push("burst window could not be applied; the range was normalized to a single timestamp.");
    endSec = startSec;
  }
  return { startSec: roundTime2(startSec), endSec: roundTime2(endSec), centerSec: roundTime2(normalizedCenter), windowSec: roundTime2(normalizedWindow) };
}
function timestampsForRange(startSec, endSec, input, maxFrames, warnings) {
  const interval = samplingIntervalSec(input) ?? 1 / (samplingFps(input) ?? DEFAULT_RANGE_FPS);
  const timestamps = [];
  for (let timestamp = startSec; timestamp <= endSec + 1e-4; timestamp += interval) {
    timestamps.push(roundTime2(timestamp));
  }
  if (timestamps.length === 0) timestamps.push(roundTime2(startSec));
  if (timestamps.length > maxFrames) {
    warnings.push(`Requested ${timestamps.length} frames for the time range; downsampled to ${maxFrames} due to max_frames.`);
    return downsample(timestamps, maxFrames);
  }
  return uniqueSorted2(timestamps);
}
function samplingFps(input) {
  const fps = positiveNumber22(input.fps);
  return fps === void 0 ? void 0 : Math.min(fps, MAX_FPS);
}
function samplingIntervalSec(input) {
  return positiveNumber22(input.intervalSec);
}
function downsample(values, maxItems) {
  if (values.length <= maxItems) return uniqueSorted2(values);
  if (maxItems <= 1) return [values[0] ?? 0];
  const step = (values.length - 1) / (maxItems - 1);
  return uniqueSorted2(Array.from({ length: maxItems }, (_unused, index) => values[Math.round(index * step)] ?? values[values.length - 1] ?? 0));
}
function publicSamplingPlan(sampling) {
  return {
    mode: sampling.mode,
    timestamps_sec: sampling.timestampsSec,
    requested_frame_count: sampling.requestedFrameCount,
    returned_frame_count: sampling.returnedFrameCount,
    max_frames: sampling.maxFrames,
    ...sampling.startSec !== void 0 ? { start_sec: sampling.startSec } : {},
    ...sampling.endSec !== void 0 ? { end_sec: sampling.endSec } : {},
    ...sampling.centerSec !== void 0 ? { center_sec: sampling.centerSec } : {},
    ...sampling.windowSec !== void 0 ? { window_sec: sampling.windowSec } : {},
    ...sampling.fps !== void 0 ? { fps: sampling.fps } : {},
    ...sampling.intervalSec !== void 0 ? { interval_sec: sampling.intervalSec } : {},
    ...sampling.warnings.length > 0 ? { warnings: sampling.warnings } : {}
  };
}
function publicVideoMetadata(video) {
  return {
    ...video.durationSec !== void 0 ? { duration_sec: video.durationSec } : {},
    ...video.width !== void 0 ? { width: video.width } : {},
    ...video.height !== void 0 ? { height: video.height } : {},
    ...video.fps !== void 0 ? { fps: video.fps } : {}
  };
}
async function loadImageAnnotationSource(args) {
  const resourceId = optionalResourceIdParam(args);
  if (resourceId !== void 0) {
    const maxBytes = clampInteger(numberParam(args.max_source_bytes) ?? numberParam(args.maxSourceBytes) ?? DEFAULT_MAX_IMAGE_BYTES, 1, ABSOLUTE_MAX_IMAGE_BYTES);
    const file = await downloadResourceFile(resourceId, { maxBytes });
    const mimeType = normalizeImageMimeType(stringParam(args.mime_type) ?? stringParam(args.mimeType) ?? file.contentType);
    return {
      bytes: file.bytes,
      mimeType,
      public: { kind: "backend_resource", resource_id: resourceId, mime_type: mimeType, size_bytes: file.bytes.length }
    };
  }
  const dataUrl = stringParam(args.data_url) ?? stringParam(args.dataUrl);
  if (dataUrl) {
    const decoded = decodeDataURL(dataUrl);
    return {
      bytes: decoded.bytes,
      mimeType: normalizeImageMimeType(decoded.mimeType),
      public: { kind: "data_url", mime_type: decoded.mimeType, size_bytes: decoded.bytes.length }
    };
  }
  const localPath = stringParam(args.local_path) ?? stringParam(args.localPath) ?? stringParam(args.artifact_path) ?? stringParam(args.artifactPath);
  if (localPath) {
    const bytes = await readFile3(localPath);
    const mimeType = normalizeImageMimeType(stringParam(args.mime_type) ?? stringParam(args.mimeType) ?? mimeTypeFromFilename(localPath));
    return {
      bytes,
      mimeType,
      public: { kind: "local_path", path: localPath, mime_type: mimeType, size_bytes: bytes.length }
    };
  }
  throw new Error("annotate requires resource_id, data_url, or local_path");
}
function annotationShapes(value) {
  if (!Array.isArray(value)) return [];
  return value.map(annotationShape).filter((item) => item !== void 0);
}
function annotationShape(value) {
  if (!isRecord26(value)) return void 0;
  const type = shapeType(value.type);
  if (!type) return void 0;
  const color = stringParam(value.color) ?? (type === "highlight" ? "#facc15" : "#ef4444");
  const strokeWidth = clampNumber2(numberParam(value.stroke_width) ?? numberParam(value.strokeWidth) ?? 5, 1, 64);
  const opacity = clampNumber2(numberParam(value.opacity) ?? (type === "highlight" ? 0.32 : 0.95), 0.05, 1);
  const fontSize = clampNumber2(numberParam(value.font_size) ?? numberParam(value.fontSize) ?? 32, 8, 240);
  return {
    type,
    ...numberParam(value.x) !== void 0 ? { x: numberParam(value.x) } : {},
    ...numberParam(value.y) !== void 0 ? { y: numberParam(value.y) } : {},
    ...numberParam(value.width) !== void 0 ? { width: numberParam(value.width) } : {},
    ...numberParam(value.height) !== void 0 ? { height: numberParam(value.height) } : {},
    ...numberParam(value.cx) !== void 0 ? { cx: numberParam(value.cx) } : {},
    ...numberParam(value.cy) !== void 0 ? { cy: numberParam(value.cy) } : {},
    ...numberParam(value.r) !== void 0 ? { r: numberParam(value.r) } : {},
    ...numberParam(value.x1) !== void 0 ? { x1: numberParam(value.x1) } : {},
    ...numberParam(value.y1) !== void 0 ? { y1: numberParam(value.y1) } : {},
    ...numberParam(value.x2) !== void 0 ? { x2: numberParam(value.x2) } : {},
    ...numberParam(value.y2) !== void 0 ? { y2: numberParam(value.y2) } : {},
    ...stringParam(value.text) ? { text: stringParam(value.text) } : {},
    ...stringParam(value.fill) ? { fill: stringParam(value.fill) } : {},
    ...stringParam(value.label) ? { label: stringParam(value.label) } : {},
    color,
    strokeWidth,
    opacity,
    fontSize
  };
}
function renderAnnotationSVG(input) {
  const sourceHref = `data:${input.source.mimeType};base64,${input.source.bytes.toString("base64")}`;
  const shapeSVG = input.shapes.map(renderAnnotationShape).join("\n  ");
  const noteSVG = input.note ? `<text x="24" y="${Math.max(36, input.height - 24)}" font-family="Arial, sans-serif" font-size="24" fill="#111827" stroke="#ffffff" stroke-width="5" paint-order="stroke">${escapeXML(input.note)}</text>` : "";
  return [
    `<svg xmlns="http://www.w3.org/2000/svg" width="${input.width}" height="${input.height}" viewBox="0 0 ${input.width} ${input.height}">`,
    `  <title>${escapeXML(input.title)}</title>`,
    "  <defs>",
    '    <marker id="arrow-head" markerWidth="16" markerHeight="16" refX="12" refY="8" orient="auto" markerUnits="strokeWidth">',
    '      <path d="M 0 0 L 16 8 L 0 16 z" fill="context-stroke" />',
    "    </marker>",
    "  </defs>",
    `  <image href="${sourceHref}" x="0" y="0" width="${input.width}" height="${input.height}" preserveAspectRatio="xMidYMid meet" />`,
    `  ${shapeSVG}`,
    noteSVG ? `  ${noteSVG}` : "",
    "</svg>"
  ].filter(Boolean).join("\n");
}
function renderAnnotationShape(shape) {
  const common = `stroke="${escapeXML(shape.color)}" stroke-width="${shape.strokeWidth}" opacity="${shape.opacity}"`;
  if (shape.type === "rect" || shape.type === "highlight") {
    const x = shape.x ?? 0;
    const y = shape.y ?? 0;
    const width = shape.width ?? 1;
    const height = shape.height ?? 1;
    const fill = shape.type === "highlight" ? shape.color : shape.fill ?? "none";
    return `<rect x="${x}" y="${y}" width="${width}" height="${height}" fill="${escapeXML(fill)}" ${common} />`;
  }
  if (shape.type === "circle") {
    return `<circle cx="${shape.cx ?? shape.x ?? 0}" cy="${shape.cy ?? shape.y ?? 0}" r="${shape.r ?? 24}" fill="${escapeXML(shape.fill ?? "none")}" ${common} />`;
  }
  if (shape.type === "line" || shape.type === "arrow") {
    const marker = shape.type === "arrow" ? ' marker-end="url(#arrow-head)"' : "";
    return `<line x1="${shape.x1 ?? shape.x ?? 0}" y1="${shape.y1 ?? shape.y ?? 0}" x2="${shape.x2 ?? 0}" y2="${shape.y2 ?? 0}" fill="none" ${common}${marker} />`;
  }
  if (shape.type === "text") {
    return `<text x="${shape.x ?? 0}" y="${shape.y ?? shape.fontSize}" font-family="Arial, sans-serif" font-size="${shape.fontSize}" fill="${escapeXML(shape.color)}" stroke="#ffffff" stroke-width="${Math.max(2, Math.round(shape.strokeWidth / 2))}" paint-order="stroke" opacity="${shape.opacity}">${escapeXML(shape.text ?? shape.label ?? "")}</text>`;
  }
  return "";
}
function publicAnnotationShape(shape) {
  return {
    type: shape.type,
    ...shape.x !== void 0 ? { x: shape.x } : {},
    ...shape.y !== void 0 ? { y: shape.y } : {},
    ...shape.width !== void 0 ? { width: shape.width } : {},
    ...shape.height !== void 0 ? { height: shape.height } : {},
    ...shape.cx !== void 0 ? { cx: shape.cx } : {},
    ...shape.cy !== void 0 ? { cy: shape.cy } : {},
    ...shape.r !== void 0 ? { r: shape.r } : {},
    ...shape.x1 !== void 0 ? { x1: shape.x1 } : {},
    ...shape.y1 !== void 0 ? { y1: shape.y1 } : {},
    ...shape.x2 !== void 0 ? { x2: shape.x2 } : {},
    ...shape.y2 !== void 0 ? { y2: shape.y2 } : {},
    ...shape.text !== void 0 ? { text: shape.text } : {},
    color: shape.color,
    stroke_width: shape.strokeWidth,
    opacity: shape.opacity
  };
}
async function resolveAnnotationOutputPath(args, title) {
  const outputPath = stringParam(args.output_path) ?? stringParam(args.outputPath);
  if (outputPath) return { path: outputPath, location: "explicit_path" };
  const workspacePath5 = stringParam(args.workspace_path) ?? stringParam(args.workspacePath);
  if (workspacePath5) {
    return {
      path: await resolveWorkspaceFilePath(stringParam(args.workspaceDir), workspacePath5),
      location: "workspace_path"
    };
  }
  const dir = join32(tmpdir22(), "movscript-mcp-artifacts");
  const filename = `${safeFilename(title || "annotation")}-${randomUUID2().slice(0, 8)}.svg`;
  return { path: join32(dir, filename), location: "cache", cacheDir: dir };
}
async function loadUploadInput(args) {
  const dataUrl = stringParam(args.data_url) ?? stringParam(args.dataUrl);
  if (dataUrl) {
    const decoded = decodeDataURL(dataUrl);
    return {
      bytes: decoded.bytes,
      mimeType: decoded.mimeType,
      public: { kind: "data_url", mime_type: decoded.mimeType, size_bytes: decoded.bytes.length }
    };
  }
  const base64 = stringParam(args.base64);
  if (base64) {
    const mimeType = normalizeImageMimeType(stringParam(args.mime_type) ?? stringParam(args.mimeType));
    const bytes = Buffer.from(base64, "base64");
    return {
      bytes,
      mimeType,
      public: { kind: "base64", mime_type: mimeType, size_bytes: bytes.length }
    };
  }
  const workspacePath5 = stringParam(args.workspace_path) ?? stringParam(args.workspacePath);
  if (workspacePath5) {
    const path = await resolveWorkspaceFilePath(stringParam(args.workspaceDir), workspacePath5);
    const bytes = await readFile3(path);
    const mimeType = normalizeImageMimeType(stringParam(args.mime_type) ?? stringParam(args.mimeType) ?? mimeTypeFromFilename(path));
    return {
      bytes,
      mimeType,
      filename: basename2(path),
      public: { kind: "workspace_path", path: workspacePath5, mime_type: mimeType, size_bytes: bytes.length }
    };
  }
  const localPath = stringParam(args.local_path) ?? stringParam(args.localPath) ?? stringParam(args.artifact_path) ?? stringParam(args.artifactPath) ?? stringParam(args.path);
  if (localPath) {
    const fileStat = await stat3(localPath);
    if (!fileStat.isFile()) throw new Error("local_path must point to a file");
    const bytes = await readFile3(localPath);
    const mimeType = normalizeImageMimeType(stringParam(args.mime_type) ?? stringParam(args.mimeType) ?? mimeTypeFromFilename(localPath));
    return {
      bytes,
      mimeType,
      filename: basename2(localPath),
      public: { kind: "local_path", path: localPath, mime_type: mimeType, size_bytes: bytes.length }
    };
  }
  throw new Error("upload requires data_url, base64, workspace_path, artifact_path, or local_path");
}
async function resolveWorkspaceFilePath(workspaceDir, workspacePath5) {
  const rootDir = workspaceDir?.trim() || await resolveDefaultMovScriptWorkspaceDir();
  const workspaceRoot = resolveMovScriptWorkspaceRootPaths(rootDir);
  ensureMovScriptWorkspaceRoot(workspaceRoot);
  const rootPath = workspaceRoot.controlDir;
  const normalizedRelativePath = workspacePath5.replace(/^[/\\]+/, "");
  const absolutePath = resolve42(rootPath, normalizedRelativePath);
  const rootRelativePath = relative2(rootPath, absolutePath);
  if (rootRelativePath === "" || !rootRelativePath.startsWith(`..${sep}`) && rootRelativePath !== ".." && !isAbsolute(rootRelativePath)) {
    return absolutePath;
  }
  throw new Error("workspace_path must stay inside the MovScript workspace directory");
}
async function resolveDefaultMovScriptWorkspaceDir() {
  return resolveMCPDefaultWorkspaceDir();
}
function uploadFilename(args, mimeType, sourceFilename) {
  const explicit = stringParam(args.filename) ?? stringParam(args.name);
  if (explicit) return explicit;
  const ext = extname2(sourceFilename ?? "") || extensionForMimeType(mimeType);
  const base = sourceFilename ? safeFilename(basename2(sourceFilename, extname2(sourceFilename))) : `agent-guidance-${randomUUID2().slice(0, 8)}`;
  return `${base}${ext || ".png"}`;
}
function decodeDataURL(value) {
  const match = value.match(/^data:([^;,]+)?(;base64)?,(.*)$/s);
  if (!match) throw new Error("data_url must be a valid data URL");
  const mimeType = match[1] || "image/png";
  const isBase64 = !!match[2];
  const payload = match[3] ?? "";
  return {
    mimeType,
    bytes: isBase64 ? Buffer.from(payload, "base64") : Buffer.from(decodeURIComponent(payload), "utf8")
  };
}
function parseImageSize(bytes, mimeType) {
  if (mimeType === "image/png" && bytes.length >= 24 && bytes.toString("ascii", 1, 4) === "PNG") {
    return { width: bytes.readUInt32BE(16), height: bytes.readUInt32BE(20) };
  }
  if ((mimeType === "image/jpeg" || mimeType === "image/jpg") && bytes.length > 4) {
    return parseJPEGSize(bytes);
  }
  if (mimeType === "image/gif" && bytes.length >= 10) {
    return { width: bytes.readUInt16LE(6), height: bytes.readUInt16LE(8) };
  }
  return void 0;
}
function parseJPEGSize(bytes) {
  let offset = 2;
  while (offset + 9 < bytes.length) {
    if (bytes[offset] !== 255) return void 0;
    const marker = bytes[offset + 1];
    const length = bytes.readUInt16BE(offset + 2);
    if (marker !== void 0 && marker >= 192 && marker <= 195) {
      return { height: bytes.readUInt16BE(offset + 5), width: bytes.readUInt16BE(offset + 7) };
    }
    offset += 2 + length;
  }
  return void 0;
}
function resourceIdParam(args) {
  const value = optionalResourceIdParam(args);
  if (value === void 0) throw new Error("resource_id is required");
  return value;
}
function optionalResourceIdParam(args) {
  const value = numberParam(args.resource_id) ?? numberParam(args.resourceId) ?? numberParam(args.id);
  return value !== void 0 && value >= 1 ? Math.floor(value) : void 0;
}
function timestampsParam(value) {
  if (!Array.isArray(value)) return [];
  return value.map(numberParam).filter((item) => item !== void 0 && item >= 0).map(roundTime2);
}
function modeParam(value) {
  return value === "overview" || value === "timestamps" || value === "range" || value === "burst" ? value : void 0;
}
function resourceImageReadModeParam(args) {
  const mode = stringParam(args.mode);
  if (mode === "original" || mode === "fit") return mode;
  if (mode) throw new Error('mode must be "fit" or "original"');
  const detail = stringParam(args.detail);
  if (detail === "original") return "original";
  if (detail === void 0 || detail === "high" || detail === "auto" || detail === "low") return "fit";
  throw new Error('detail must be "original", "high", "auto", or "low"');
}
function shapeType(value) {
  return value === "rect" || value === "circle" || value === "line" || value === "arrow" || value === "text" || value === "highlight" ? value : void 0;
}
function normalizeImageMimeType(value) {
  if (!value) return "image/png";
  return value.split(";")[0]?.trim().toLowerCase() || "image/png";
}
function mimeTypeFromFilename(filename) {
  switch (extname2(filename).toLowerCase()) {
    case ".jpg":
    case ".jpeg":
      return "image/jpeg";
    case ".svg":
      return "image/svg+xml";
    case ".webp":
      return "image/webp";
    case ".gif":
      return "image/gif";
    case ".mp4":
    case ".m4v":
      return "video/mp4";
    case ".mov":
      return "video/quicktime";
    case ".webm":
      return "video/webm";
    case ".mkv":
      return "video/x-matroska";
    case ".png":
    default:
      return "image/png";
  }
}
function extensionForMimeType(mimeType) {
  switch (mimeType) {
    case "image/jpeg":
      return ".jpg";
    case "image/svg+xml":
      return ".svg";
    case "image/webp":
      return ".webp";
    case "image/gif":
      return ".gif";
    case "video/mp4":
      return ".mp4";
    case "video/quicktime":
      return ".mov";
    case "video/webm":
      return ".webm";
    case "video/x-matroska":
      return ".mkv";
    case "image/png":
    default:
      return ".png";
  }
}
function numericResourceId(resource2) {
  if (!isRecord26(resource2)) return void 0;
  return numberParam(resource2.ID) ?? numberParam(resource2.id);
}
function stringParam(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberParam(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) {
    const parsed = Number(value);
    return Number.isFinite(parsed) ? parsed : void 0;
  }
  return void 0;
}
function booleanParam3(value) {
  if (typeof value === "boolean") return value;
  if (typeof value !== "string") return void 0;
  if (value === "true") return true;
  if (value === "false") return false;
  return void 0;
}
function batchItems(value, name) {
  if (!Array.isArray(value) || value.length === 0) throw new Error(`${name} must contain at least one item`);
  return value.map((item, index) => {
    if (!isRecord26(item)) throw new Error(`${name}[${index}] must be an object`);
    return item;
  });
}
function batchDefaults2(args, excluded) {
  const defaults = {};
  for (const [key, value] of Object.entries(args)) {
    if (!excluded.has(key) && value !== void 0) defaults[key] = value;
  }
  return defaults;
}
async function runBatch(items, concurrency, worker, continueOnError) {
  const results = new Array(items.length);
  let nextIndex = 0;
  let stopped = false;
  async function runWorker() {
    while (!stopped) {
      const index = nextIndex;
      nextIndex += 1;
      if (index >= items.length) return;
      const item = items[index];
      if (!item) return;
      try {
        results[index] = await worker(item, index);
      } catch (error) {
        results[index] = {
          index,
          status: "error",
          error: errorMessage32(error)
        };
        if (!continueOnError) stopped = true;
      }
    }
  }
  await Promise.all(Array.from({ length: Math.min(concurrency, items.length) }, runWorker));
  return results.map((result, index) => result ?? {
    index,
    status: "skipped",
    error: "Skipped because an earlier item failed and continue_on_error is false."
  });
}
function batchStatus(successCount, failedCount) {
  if (failedCount === 0) return "completed";
  return successCount > 0 ? "partial_error" : "error";
}
function errorMessage32(error) {
  return error instanceof Error ? error.message : String(error);
}
function neutralVideoResourcePreparationCompatibility() {
  return {
    kind: "neutral_resource_preparation",
    message: "This tool derives a reusable RawResource for references, drafts, or generation inputs. It is not the product editing path and does not update MediaEditingProject, candidates, selections, or shot state.",
    recommended_workflow: "For timeline trim, render, export, and candidate creation use MediaEditingProject plus editing_* through Electron mediaPipeline.",
    recommended_tool_family: "editing_*"
  };
}
function resourceLevelVideoUtility() {
  return {
    kind: "resource_level_video_utility",
    message: "This tool composes generic video RawResources into another RawResource. It is not the product editing path.",
    recommended_workflow: "For product timeline editing, stitching, render, HLS packaging, export, and candidate creation use MediaEditingProject plus editing_* through Electron mediaPipeline.",
    recommended_tool_family: "editing_*"
  };
}
function clampInteger(value, min, max) {
  if (!Number.isFinite(value)) return min;
  return Math.min(max, Math.max(min, Math.floor(value)));
}
function clampNumber2(value, min, max) {
  if (!Number.isFinite(value)) return min;
  return Math.min(max, Math.max(min, value));
}
function roundTime2(value) {
  return Math.round(value * 1e3) / 1e3;
}
function clampToDuration(value, durationSec) {
  if (!durationSec || durationSec <= 0) return value;
  return Math.min(value, durationSec);
}
function uniqueSorted2(values) {
  return [...new Set(values.map(roundTime2))].sort((left, right) => left - right);
}
function positiveInteger3(value) {
  return typeof value === "number" && Number.isInteger(value) && value > 0 ? value : void 0;
}
function positiveNumber22(value) {
  return typeof value === "number" && Number.isFinite(value) && value > 0 ? value : void 0;
}
function parseFrameRate(value) {
  if (!value || value === "0/0") return void 0;
  const [num, den] = value.split("/").map(Number);
  if (!Number.isFinite(num) || !Number.isFinite(den) || !num || !den) return void 0;
  return Math.round(num / den * 1e3) / 1e3;
}
function safeFilename(value) {
  const normalized = value.trim().replace(/\.[^.]+$/, "") || "agent-guidance";
  return normalized.replace(/[^a-zA-Z0-9._-]+/g, "-").replace(/^-+|-+$/g, "") || "agent-guidance";
}
function escapeXML(value) {
  return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&apos;");
}
function isRecord26(value) {
  return !!value && typeof value === "object" && !Array.isArray(value);
}
async function mkdtempStable(prefix) {
  const dir = join32(tmpdir22(), `${prefix}${randomUUID2().slice(0, 8)}`);
  await mkdir22(dir, { recursive: true });
  return dir;
}
function listResourceMediaResources() {
  return [
    {
      uri: "movscript://resource-file/{resource_id}",
      name: "MovScript resource file",
      description: "Dynamic binary RawResource reader. Replace {resource_id} with an ID, for example movscript://resource-file/42?maxBytes=8388608. Use image/video media tools for Codex vision workflows.",
      mimeType: "application/octet-stream"
    }
  ];
}
var resourceMediaResourceReaders = [
  readResourceMediaResource
];
async function readResourceMediaResource(uri) {
  const resourceFile = parseResourceFileURI(uri);
  if (!resourceFile) return null;
  const file = await readResourceFileBlob(resourceFile.resourceId, { maxBytes: resourceFile.maxBytes });
  return {
    contents: [
      {
        uri,
        mimeType: file.mimeType,
        blob: file.blob
      }
    ],
    data: {
      resource_id: resourceFile.resourceId,
      mime_type: file.mimeType,
      size_bytes: file.sizeBytes
    }
  };
}
function parseResourceFileURI(uri) {
  const match = uri.match(/^movscript:\/\/resource-file\/(\d+)(?:\?(.*))?$/);
  if (!match) return null;
  const params = new URLSearchParams(match[2] ?? "");
  const maxBytes = numericParam3(params.get("maxBytes") ?? params.get("max_bytes"));
  return {
    resourceId: Number(match[1]),
    ...maxBytes !== void 0 ? { maxBytes } : {}
  };
}
function numericParam3(value) {
  if (!value?.trim()) return void 0;
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : void 0;
}
async function listExternalResourceSources(args) {
  const includeDisabled = booleanParam4(args.include_disabled) ?? booleanParam4(args.includeDisabled) ?? false;
  const sources = await fetchExternalResourceSources();
  const filtered = includeDisabled ? sources : sources.filter((source) => source.is_enabled !== false);
  return {
    source: "movscript_external_resource_sources",
    count: filtered.length,
    items: filtered.map(summarizeExternalResourceSource),
    usage: "Pass an enabled source ID to movscript_external_resource_search.source_id, or omit source_id to use the first enabled source."
  };
}
async function searchExternalResources(args) {
  const sourceId = getOptionalNumeric(args, "source_id") ?? getOptionalNumeric(args, "sourceId") ?? await defaultExternalResourceSourceId();
  const query = getOptionalString(args, "query") ?? getOptionalString(args, "q");
  if (!query) throw new Error("query is required");
  if (!sourceId) throw new Error("No enabled external resource source is configured");
  const mediaType = getOptionalString(args, "media_type") ?? getOptionalString(args, "mediaType");
  const orientation = getOptionalString(args, "orientation");
  const page = Math.floor(clampNumber(getOptionalNumeric(args, "page") ?? 1, 1, 1e4));
  const pageSize = Math.floor(clampNumber(
    getOptionalNumeric(args, "page_size") ?? getOptionalNumeric(args, "pageSize") ?? getOptionalNumeric(args, "limit") ?? 20,
    1,
    80
  ));
  const result = await fetchExternalResourceSearch({ sourceId, query, mediaType, orientation, page, pageSize });
  const items = Array.isArray(result.items) ? result.items : [];
  return {
    source: "movscript_external_resources",
    source_id: sourceId,
    query,
    ...mediaType ? { media_type: mediaType } : {},
    ...orientation ? { orientation } : {},
    page: numberField33(result, "page") ?? page,
    pageSize: numberField33(result, "page_size") ?? pageSize,
    total: numberField33(result, "total") ?? items.length,
    count: items.length,
    provider: typeof result.provider === "string" ? result.provider : void 0,
    source_name: typeof result.source_name === "string" ? result.source_name : void 0,
    next_page: typeof result.next_page === "string" ? result.next_page : void 0,
    items: items.map(summarizeExternalResourceItem),
    usage: "These are external search results, not MovScript RawResources. Import through MovScript before using them as generation input_resource_ids."
  };
}
async function readExternalResources(uri) {
  const parsed = parseExternalResourcesURI(uri);
  if (!parsed) throw new Error(`Unsupported external resource URI: ${uri}`);
  if (!parsed.query) return listExternalResourceSources({});
  return searchExternalResources(parsed);
}
function isExternalResourcesURI(uri) {
  return parseExternalResourcesURI(uri) !== null;
}
function parseExternalResourcesURI(uri) {
  const match = uri.match(/^movscript:\/\/external-resources(?:\?(.*))?$/);
  if (!match) return null;
  const params = new URLSearchParams(match[1] ?? "");
  return {
    ...params.get("query") || params.get("q") ? { query: params.get("query") ?? params.get("q") ?? "" } : {},
    ...numericParam4(params.get("source_id") ?? params.get("sourceId")) !== void 0 ? { source_id: numericParam4(params.get("source_id") ?? params.get("sourceId")) } : {},
    ...params.get("media_type") || params.get("mediaType") ? { media_type: params.get("media_type") ?? params.get("mediaType") ?? "" } : {},
    ...params.get("orientation") ? { orientation: params.get("orientation") ?? "" } : {},
    ...numericParam4(params.get("page")) !== void 0 ? { page: numericParam4(params.get("page")) } : {},
    ...numericParam4(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) !== void 0 ? { page_size: numericParam4(params.get("page_size") ?? params.get("pageSize") ?? params.get("limit")) } : {}
  };
}
async function defaultExternalResourceSourceId() {
  const sources = await fetchExternalResourceSources();
  const enabled = sources.find((source) => source.is_enabled !== false);
  return numericField(enabled, "ID") ?? numericField(enabled, "id");
}
async function fetchExternalResourceSources() {
  const data = await backendGet2("/external-resource-sources");
  return Array.isArray(data) ? data.filter(isRecord4) : [];
}
async function fetchExternalResourceSearch(input) {
  const params = new URLSearchParams();
  params.set("source_id", String(input.sourceId));
  params.set("q", input.query);
  params.set("page", String(input.page));
  params.set("page_size", String(input.pageSize));
  if (input.mediaType?.trim()) params.set("media_type", input.mediaType.trim());
  if (input.orientation?.trim() && input.orientation !== "all") params.set("orientation", input.orientation.trim());
  const data = await backendGet2(`/external-resources/search?${params.toString()}`);
  return isRecord4(data) ? data : { items: [] };
}
function summarizeExternalResourceSource(item) {
  return picked3(item, ["ID", "id", "name", "provider_key", "priority", "is_enabled", "CreatedAt", "UpdatedAt"]);
}
function summarizeExternalResourceItem(item) {
  if (!isRecord4(item)) return item;
  return picked3(item, ["provider_key", "external_id", "media_type", "title", "description", "thumbnail_url", "preview_url", "source_url", "width", "height", "duration_seconds", "author_name", "author_url", "attribution_text", "license_label"]);
}
function picked3(record, keys) {
  const result = {};
  for (const key of keys) {
    if (record[key] !== void 0) result[key] = truncateText2(record[key]);
  }
  return result;
}
function truncateText2(value) {
  if (typeof value !== "string") return value;
  return value.length > 1200 ? `${value.slice(0, 1200)}...` : value;
}
function numericField(record, key) {
  if (!record) return void 0;
  return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
}
function numberField33(record, key) {
  return typeof record[key] === "number" && Number.isFinite(record[key]) ? record[key] : void 0;
}
function numericParam4(value) {
  if (!value?.trim()) return void 0;
  const parsed = Number(value);
  return Number.isFinite(parsed) ? parsed : void 0;
}
function booleanParam4(value) {
  if (typeof value === "boolean") return value;
  if (typeof value !== "string") return void 0;
  if (value === "true") return true;
  if (value === "false") return false;
  return void 0;
}
function listExternalResourceResources() {
  return [
    {
      uri: "movscript://external-resources",
      name: "External media search sources",
      description: "Configured external image/video providers. Use movscript_external_resource_search for provider search; import results before generation.",
      mimeType: "text/markdown"
    }
  ];
}
var externalResourceReaders = [
  readExternalResourceResource
];
async function readExternalResourceResource(uri) {
  if (!isExternalResourcesURI(uri)) return null;
  return resourceContent(uri, await readExternalResources(uri));
}
var resourceReaders = [
  ...focusResourceReaders,
  ...projectResourceReaders,
  ...shotLibraryResourceReaders,
  ...resourceLibraryResourceReaders,
  ...resourceMediaResourceReaders,
  ...externalResourceReaders
];
function listResources() {
  return [
    ...listFocusResources(),
    ...listProjectResources(),
    ...listShotLibraryResources(),
    ...listResourceLibraryResources(),
    ...listResourceMediaResources(),
    ...listExternalResourceResources()
  ];
}
async function readResource(uri) {
  for (const reader of resourceReaders) {
    const result = await reader(uri);
    if (result) return result;
  }
  throw new Error(`Unsupported resource URI: ${uri}`);
}
function getFocus() {
  const startedAt = Date.now();
  const focusMs = Date.now() - startedAt;
  return {
    focus: getMCPFocusSnapshot(),
    timings: {
      totalMs: focusMs,
      focusMs
    }
  };
}
var currentEditingRuntimePort;
function getEditingRuntimePort() {
  return currentEditingRuntimePort;
}
async function artifactUploadExport(args) {
  const taskResolution = await resolveExportUploadTask(args);
  if ("diagnostic" in taskResolution) return taskResolution.diagnostic;
  const request = await exportUploadRequest(args, taskResolution.task);
  if (isHlsManifestExportRequest(request)) {
    return {
      status: "unsupported_output",
      code: "USE_SYSTEM_ARTIFACT_UPLOAD_HLS_STREAM",
      message: "Output is an HLS manifest. Use system_artifact_upload_hls_stream for HLS artifacts instead of uploading it as a RawResource.",
      outputPath: request.outputPath,
      output_path: request.outputPath,
      filename: request.filename,
      mimeType: request.mimeType,
      mime_type: request.mimeType
    };
  }
  const info = await stat22(request.outputPath);
  if (!info.isFile()) throw new Error(`ARTIFACT_EXPORT_FILE_INVALID: ${request.outputPath} is not a file.`);
  const bytes = await readFile22(request.outputPath);
  const form = new FormData();
  form.append("file", new Blob([new Uint8Array(bytes)], { type: request.mimeType }), request.filename);
  appendFormValue(form, "folder_id", request.folderId);
  const derivative = exportDerivativePayload(args);
  if (derivative) form.append("derivative", JSON.stringify(derivative));
  const response = await backendPostMultipart("/resources/upload", form);
  const resourceId = numericResourceId2(response);
  if (resourceId === void 0) {
    throw new Error("ARTIFACT_EXPORT_UPLOAD_RESPONSE_INVALID: resource upload response did not include a valid resource ID.");
  }
  return {
    status: "ok",
    resourceId,
    resource_id: resourceId,
    resource: response,
    outputPath: request.outputPath,
    output_path: request.outputPath,
    filename: request.filename,
    mimeType: request.mimeType,
    mime_type: request.mimeType,
    ...request.folderId !== void 0 ? { folderId: request.folderId, folder_id: request.folderId } : {}
  };
}
async function artifactUploadHlsStream(args) {
  const taskId = stringValue33(args.taskId ?? args.task_id);
  const runtime = getEditingRuntimePort();
  if (taskId && runtime?.publishHlsStream) {
    const task = runtime.getTask ? await runtime.getTask(taskId, { projectId: stringValue33(args.projectId ?? args.project_id) }) : void 0;
    const hasExplicitManifest = !!stringValue33(args.manifestPath ?? args.manifest_path);
    const hasExplicitSegments = !!stringList(args.segmentPaths ?? args.segment_paths)?.length;
    if (!task && !hasExplicitManifest) {
      return {
        status: "not_found",
        task_id: taskId,
        message: "No Electron mediaPipeline task was found for taskId. Pass projectId with taskId for persisted workspace recovery, or provide manifestPath and segmentPaths explicitly."
      };
    }
    if (isRecord34(task) && !hasExplicitManifest && !hasExplicitSegments) {
      const manifestPath = stringValue33(task.hlsManifestPath ?? task.hls_manifest_path ?? task.outputPath);
      const segmentPaths = stringList(task.hlsSegmentPaths ?? task.hls_segment_paths);
      if (!manifestPath || !segmentPaths?.length) {
        return {
          status: "pending_output",
          task_id: taskId,
          message: "The Electron mediaPipeline task does not have a complete HLS manifest/segment output yet.",
          task
        };
      }
    }
    const request2 = hlsPublishRequest(args, task);
    return runtime.publishHlsStream(request2);
  }
  const request = hlsPublishRequest(args);
  const form = new FormData();
  const manifest = await readFile22(request.manifestPath);
  form.append("manifest", new Blob([new Uint8Array(manifest)], { type: "application/vnd.apple.mpegurl" }), basename22(request.manifestPath));
  for (const segmentPath of request.segmentPaths ?? []) {
    const bytes = await readFile22(segmentPath);
    form.append("segments", new Blob([new Uint8Array(bytes)], { type: mimeTypeForFilename(segmentPath) }), basename22(segmentPath));
  }
  appendFormValue(form, "title", request.title);
  appendFormValue(form, "task_id", request.task_id ?? request.taskId);
  appendFormValue(form, "project_id", request.project_id ?? request.projectId);
  appendFormValue(form, "source_resource_id", request.source_resource_id ?? request.sourceResourceId);
  appendFormValue(form, "source_derivative_id", request.source_derivative_id ?? request.sourceDerivativeId);
  appendFormValue(form, "duration_ms", request.duration_ms ?? request.durationMs);
  appendFormValue(form, "width", request.width);
  appendFormValue(form, "height", request.height);
  appendFormValue(form, "expires_at", stringValue33(args.expiresAt ?? args.expires_at));
  appendFormValue(form, "expires_in_seconds", numberValue32(args.expiresInSeconds ?? args.expires_in_seconds));
  const response = await backendPostMultipart("/media/streams/uploads", form);
  return hlsUploadResult(response);
}
async function artifactGetStream(args) {
  const streamId = stringOrNumberValue(args.streamId ?? args.stream_id ?? args.id);
  if (streamId === void 0) throw new Error("streamId is required");
  const response = await backendGet2(`/media/streams/${encodeURIComponent(String(streamId))}`);
  const record = response && typeof response === "object" ? response : {};
  const stream = record.stream ?? record.media_stream ?? response;
  return {
    status: "ok",
    streamId: numericId3(response) ?? numericId3(stream) ?? Number(streamId),
    stream_id: numericId3(response) ?? numericId3(stream) ?? Number(streamId),
    stream,
    media_stream: stream,
    manifestUrl: stringValue33(record.manifest_url ?? record.manifestUrl),
    manifest_url: stringValue33(record.manifest_url ?? record.manifestUrl),
    presignedManifestUrl: stringValue33(record.presigned_manifest_url ?? record.presignedManifestUrl),
    presigned_manifest_url: stringValue33(record.presigned_manifest_url ?? record.presignedManifestUrl),
    segmentBaseUrl: stringValue33(record.segment_base_url ?? record.segmentBaseUrl),
    segment_base_url: stringValue33(record.segment_base_url ?? record.segmentBaseUrl),
    segments: record.segments
  };
}
async function resolveExportUploadTask(args) {
  const taskId = stringValue33(args.taskId ?? args.task_id);
  const explicitOutputPath = stringValue33(args.outputPath ?? args.output_path ?? args.filePath ?? args.file_path);
  if (!taskId || explicitOutputPath) return {};
  const runtime = getEditingRuntimePort();
  if (!runtime?.getTask) {
    return {
      diagnostic: {
        status: "unsupported_runtime",
        code: "ELECTRON_EDITING_RUNTIME_REQUIRED",
        task_id: taskId,
        message: "Resolving an Electron mediaPipeline task output requires the Electron runtime. Provide outputPath/filePath explicitly, or run this task inside MovScript Desktop with projectId and taskId."
      }
    };
  }
  const task = await runtime.getTask(taskId, { projectId: stringValue33(args.projectId ?? args.project_id) });
  if (!isRecord34(task)) {
    return {
      diagnostic: {
        status: "not_found",
        task_id: taskId,
        message: "No Electron mediaPipeline task was found for taskId. Pass projectId with taskId for persisted workspace recovery, or provide outputPath/filePath explicitly."
      }
    };
  }
  const outputPath = stringValue33(task.outputPath ?? task.output_path);
  if (!outputPath) {
    return {
      diagnostic: {
        status: "pending_output",
        task_id: taskId,
        message: "The Electron mediaPipeline task does not have a local export output path yet.",
        task
      }
    };
  }
  return { task };
}
async function exportUploadRequest(args, task) {
  const outputPath = stringValue33(args.outputPath ?? args.output_path ?? args.filePath ?? args.file_path) ?? stringValue33(task?.outputPath ?? task?.output_path);
  if (!outputPath) throw new Error("outputPath is required");
  const filename = stringValue33(args.filename) ?? stringValue33(task?.filename) ?? basename22(outputPath);
  const mimeType = stringValue33(args.mimeType ?? args.mime_type) ?? stringValue33(task?.mimeType ?? task?.mime_type) ?? mimeTypeForFilename(filename);
  return {
    outputPath,
    filename,
    mimeType,
    ...stringOrNumberValue(args.folderId ?? args.folder_id) !== void 0 ? { folderId: stringOrNumberValue(args.folderId ?? args.folder_id) } : {}
  };
}
function exportDerivativePayload(args) {
  const explicit = args.derivative;
  if (isRecord34(explicit)) {
    const operation2 = stringValue33(explicit.operation);
    if (!operation2) return void 0;
    const inputIds2 = numericIdList(explicit.input_resource_ids ?? explicit.inputResourceIds);
    return {
      operation: operation2,
      ...stringValue33(explicit.tool) ? { tool: stringValue33(explicit.tool) } : {},
      ...inputIds2.length ? { input_resource_ids: inputIds2 } : {},
      ...isRecord34(explicit.params) ? { params: explicit.params } : {}
    };
  }
  const operation = stringValue33(args.operation);
  const inputIds = [
    ...numericIdList(args.inputResourceIds ?? args.input_resource_ids),
    ...numericIdList(args.sourceResourceIds ?? args.source_resource_ids),
    ...numericIdList(args.sourceResourceId ?? args.source_resource_id)
  ];
  if (!operation && !inputIds.length && !isRecord34(args.params)) return void 0;
  return {
    operation: operation ?? "artifact_export",
    ...stringValue33(args.tool) ? { tool: stringValue33(args.tool) } : { tool: "system_artifact_upload_export" },
    ...inputIds.length ? { input_resource_ids: Array.from(new Set(inputIds)) } : {},
    ...isRecord34(args.params) ? { params: args.params } : {}
  };
}
function hlsPublishRequest(args, taskValue) {
  const task = isRecord34(taskValue) ? taskValue : void 0;
  const manifestPath = stringValue33(args.manifestPath ?? args.manifest_path) ?? stringValue33(task?.hlsManifestPath ?? task?.hls_manifest_path ?? task?.outputPath);
  const segmentPaths = stringList(args.segmentPaths ?? args.segment_paths) ?? stringList(task?.hlsSegmentPaths ?? task?.hls_segment_paths);
  if (!manifestPath) throw new Error("manifestPath is required");
  if (!segmentPaths?.length) throw new Error("segmentPaths is required");
  const request = {
    manifestPath,
    manifest_path: manifestPath,
    segmentPaths,
    segment_paths: segmentPaths
  };
  const taskId = stringValue33(args.taskId ?? args.task_id);
  const title = stringValue33(args.title);
  const projectId = stringOrNumberValue(args.projectId ?? args.project_id);
  const sourceResourceId2 = stringOrNumberValue(args.sourceResourceId ?? args.source_resource_id);
  const sourceDerivativeId = stringOrNumberValue(args.sourceDerivativeId ?? args.source_derivative_id);
  const durationMs = numberValue32(args.durationMs ?? args.duration_ms);
  const width = numberValue32(args.width);
  const height = numberValue32(args.height);
  if (taskId) {
    request.taskId = taskId;
    request.task_id = taskId;
  }
  if (title) request.title = title;
  if (projectId !== void 0) {
    request.projectId = projectId;
    request.project_id = projectId;
  }
  if (sourceResourceId2 !== void 0) {
    request.sourceResourceId = sourceResourceId2;
    request.source_resource_id = sourceResourceId2;
  }
  if (sourceDerivativeId !== void 0) {
    request.sourceDerivativeId = sourceDerivativeId;
    request.source_derivative_id = sourceDerivativeId;
  }
  if (durationMs !== void 0) {
    request.durationMs = durationMs;
    request.duration_ms = durationMs;
  }
  if (width !== void 0) request.width = width;
  if (height !== void 0) request.height = height;
  return request;
}
function hlsUploadResult(response) {
  const record = response && typeof response === "object" ? response : {};
  const stream = record.stream ?? record.media_stream ?? response;
  const streamId = numericId3(response) ?? numericId3(stream);
  if (streamId === void 0) {
    throw new Error("HLS_STREAM_UPLOAD_RESPONSE_INVALID: response did not include a valid stream ID.");
  }
  const manifestUrl = stringValue33(record.manifest_url ?? record.manifestUrl);
  const presignedManifestUrl = stringValue33(record.presigned_manifest_url ?? record.presignedManifestUrl);
  const segmentBaseUrl = stringValue33(record.segment_base_url ?? record.segmentBaseUrl);
  return {
    status: "ok",
    streamId,
    stream_id: streamId,
    stream,
    media_stream: stream,
    ...manifestUrl ? { manifestUrl, manifest_url: manifestUrl } : {},
    ...presignedManifestUrl ? { presignedManifestUrl, presigned_manifest_url: presignedManifestUrl } : {},
    ...segmentBaseUrl ? { segmentBaseUrl, segment_base_url: segmentBaseUrl } : {},
    segments: record.segments
  };
}
function mimeTypeForFilename(filename) {
  const lower = filename.toLowerCase();
  if (lower.endsWith(".m3u8")) return "application/vnd.apple.mpegurl";
  if (lower.endsWith(".mp4")) return "video/mp4";
  if (lower.endsWith(".m4s")) return "video/iso.segment";
  if (lower.endsWith(".ts")) return "video/mp2t";
  if (lower.endsWith(".aac")) return "audio/aac";
  if (lower.endsWith(".m4v")) return "video/mp4";
  if (lower.endsWith(".mov")) return "video/quicktime";
  if (lower.endsWith(".webm")) return "video/webm";
  if (lower.endsWith(".mp3")) return "audio/mpeg";
  if (lower.endsWith(".wav")) return "audio/wav";
  if (lower.endsWith(".vtt")) return "text/vtt";
  if (lower.endsWith(".srt")) return "application/x-subrip";
  if (lower.endsWith(".ass")) return "text/x-ssa";
  return "application/octet-stream";
}
function isHlsManifestExportRequest(request) {
  const outputPath = request.outputPath.toLowerCase();
  const filename = request.filename.toLowerCase();
  const mimeType = request.mimeType.toLowerCase();
  return outputPath.endsWith(".m3u8") || filename.endsWith(".m3u8") || mimeType === "application/vnd.apple.mpegurl" || mimeType === "application/x-mpegurl";
}
function appendFormValue(form, key, value) {
  if (value === void 0 || value === null || String(value).trim() === "") return;
  form.append(key, String(value));
}
function stringValue33(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function stringOrNumberValue(value) {
  if (typeof value === "string" && value.trim()) return value.trim();
  if (typeof value === "number" && Number.isFinite(value)) return value;
  return void 0;
}
function numberValue32(value) {
  const n = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
  return Number.isFinite(n) ? n : void 0;
}
function stringList(value) {
  if (!Array.isArray(value)) return void 0;
  const out = value.flatMap((item) => typeof item === "string" && item.trim() ? [item.trim()] : []);
  return out.length ? out : void 0;
}
function numericId3(value) {
  if (!value || typeof value !== "object") return void 0;
  const record = value;
  const raw = record.ID ?? record.id ?? record.stream_id ?? record.streamId;
  const n = typeof raw === "number" ? raw : typeof raw === "string" ? Number(raw) : NaN;
  return Number.isInteger(n) && n > 0 ? n : void 0;
}
function numericResourceId2(value) {
  if (!value || typeof value !== "object") return void 0;
  const record = value;
  const raw = record.ID ?? record.id ?? record.resource_id ?? record.resourceId;
  const n = typeof raw === "number" ? raw : typeof raw === "string" ? Number(raw) : NaN;
  return Number.isInteger(n) && n > 0 ? n : void 0;
}
function numericIdList(value) {
  const values = Array.isArray(value) ? value : value === void 0 || value === null ? [] : [value];
  return values.flatMap((item) => {
    const n = typeof item === "number" ? item : typeof item === "string" ? Number(item) : NaN;
    return Number.isInteger(n) && n > 0 ? [n] : [];
  });
}
function isRecord34(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
var STANDALONE_EDITING_PROJECT_ID = "standalone";
async function editingServiceProjectCommand(command, input) {
  return (await createEditingServiceClientFromRuntime().projectCommand({ command, input })).result;
}
async function editingServiceTaskRequest(taskType, input) {
  const response = await createEditingServiceClientFromRuntime().taskRequest({ taskType, input });
  if (!isRecord4(response.request)) throw new Error("editing service did not return a task request");
  return response.request;
}
async function editingServiceTaskAction(action, input) {
  const response = await createEditingServiceClientFromRuntime().taskAction({ action, input });
  return {
    status: response.status,
    result: response.result,
    request: isRecord4(response.request) ? response.request : void 0
  };
}
async function editingServiceRuntimeTaskAction(action, input) {
  const envelope = await editingServiceTaskAction(action, input);
  const request = envelope.request;
  if (!request) throw new Error("editing service did not return a task action request");
  const taskId = stringValue43(request.taskId ?? request.task_id);
  if (!taskId) throw new Error("editing service task action request did not include taskId");
  const options = isRecord4(request.options) ? request.options : {};
  return {
    taskId,
    options: {
      ...stringValue43(options.projectId ?? options.project_id) ? { projectId: stringValue43(options.projectId ?? options.project_id) } : {}
    }
  };
}
async function editingProjectCreate(args) {
  const result = await editingServiceProjectCommand("createProject", args);
  return persistCreatedEditingProject(editingProjectFromServiceResult(result), result);
}
async function persistCreatedEditingProject(editingProject, fallbackResult) {
  const saved = await editingServiceProjectCommand("saveProject", {
    editingProject
  });
  if (isRecord4(saved)) {
    const savedProject = saved.editingProject ?? saved.editing_project ?? editingProject;
    return {
      ...saved,
      editingProject: savedProject,
      editing_project: savedProject
    };
  }
  return fallbackResult;
}
async function editingProjectCreateFromEditPlan(args) {
  const result = await editingServiceProjectCommand("createProjectFromEditPlan", args);
  return persistCreatedEditingProject(editingProjectFromServiceResult(result), result);
}
async function editingProjectCreateFromEditDecisions(args) {
  const result = await editingServiceProjectCommand("createProjectFromEditDecisions", args);
  return persistCreatedEditingProject(editingProjectFromServiceResult(result), result);
}
async function editingVideoCompose(args) {
  const inputEditDecisions = objectArg(args, "editDecisions") ?? objectArg(args, "edit_decisions");
  const renderRuntime = normalizeRenderRuntime(args.renderRuntime ?? args.render_runtime ?? inputEditDecisions?.render_runtime ?? inputEditDecisions?.renderRuntime);
  if (!renderRuntime.supported) {
    return {
      status: "unsupported_runtime",
      code: "VIDEO_COMPOSE_RENDER_RUNTIME_UNSUPPORTED",
      message: `MovScript video compose currently supports movscript_media_pipeline/ffmpeg only. Requested runtime ${renderRuntime.value} must be handled by an explicit future adapter; no silent fallback was performed.`,
      render_runtime: renderRuntime.value,
      supported_render_runtimes: ["movscript_media_pipeline", "ffmpeg"]
    };
  }
  const composed = await composeEditingProjectWithCompile(args, renderRuntime.value);
  const editingProject = composed.editingProject;
  const compileResult = composed.compileResult;
  if (!editingProject) {
    return {
      status: "blocked",
      code: "VIDEO_COMPOSE_COMPILE_BLOCKED",
      message: "Video compose did not start render because TimelineAssembly compile has blockers.",
      render_runtime: renderRuntime.value,
      ...compileResult ? {
        compile_manifest: compileResult.compile_manifest,
        compile_result: compileResult,
        diagnostics: compileResult.diagnostics
      } : {}
    };
  }
  const validation = await editingServiceProjectCommand("validateTimeline", {
    editingProject
  });
  if (timelineValidationHasErrors(validation)) {
    return {
      status: "blocked",
      code: "VIDEO_COMPOSE_TIMELINE_INVALID",
      message: "Video compose did not start render because the MediaEditingProject timeline has validation errors.",
      render_runtime: renderRuntime.value,
      editing_project: editingProject,
      ...compileResult ? { compile_manifest: compileResult.compile_manifest, compile_result: compileResult } : {},
      validation
    };
  }
  const output = isRecord4(args.output) ? args.output : {};
  const format = stringValue43(output.format ?? args.format) === "hls" ? "hls" : "mp4";
  const task = await mediaPipelineTaskCreate(format === "hls" ? "timeline_hls" : "timeline_render", {
    ...args,
    editingProject,
    projectId: projectIdValue(args) ?? editingProject.projectId,
    output: {
      ...output,
      format,
      ...output.importToResource !== void 0 || output.import_to_resource !== void 0 ? {} : booleanValue2(args.importToResource ?? args.import_to_resource) === void 0 ? {} : { importToResource: booleanValue2(args.importToResource ?? args.import_to_resource) }
    }
  });
  const taskRecord = isRecord4(task) ? task : {};
  const mediaTask = isRecord4(taskRecord.task) ? taskRecord.task : void 0;
  return {
    ...task,
    render_runtime: renderRuntime.value,
    render_runtime_used: renderRuntime.value === "ffmpeg" ? "movscript_media_pipeline_ffmpeg" : renderRuntime.value,
    format,
    editing_project: editingProject,
    ...compileResult ? { compile_manifest: compileResult.compile_manifest, compile_result: compileResult } : {},
    validation,
    render_report: {
      schema: "movscript.render_report.v1",
      status: stringValue43(task.status) ?? "unknown",
      render_runtime: renderRuntime.value,
      render_runtime_used: renderRuntime.value === "ffmpeg" ? "movscript_media_pipeline_ffmpeg" : renderRuntime.value,
      format,
      project_id: editingProject.projectId,
      editing_project_id: editingProject.id,
      compile_manifest_id: compileResult?.compile_manifest.id,
      compile_input_hash: compileResult?.compile_manifest.input_hash,
      task_id: stringValue43(mediaTask?.taskId ?? mediaTask?.task_id),
      output_path: stringValue43(mediaTask?.outputPath ?? mediaTask?.output_path),
      output_resource_id: optionalNumber(mediaTask?.outputResourceId ?? mediaTask?.output_resource_id),
      candidate_created: false,
      adopted: false,
      selected: false
    },
    candidate_created: false,
    adopted: false,
    selected: false
  };
}
async function editingProjectAddAsset(args) {
  return editingServiceProjectCommand("addAsset", args);
}
async function editingProjectRemoveAsset(args) {
  return editingServiceProjectCommand("removeAsset", args);
}
async function editingProjectSave(args) {
  const project = editingProjectArg(args);
  const expectedRevision = getOptionalNumeric(args, "expectedRevision") ?? getOptionalNumeric(args, "expected_revision");
  return editingServiceProjectCommand("saveProject", {
    editingProject: project,
    ...expectedRevision !== void 0 ? { expectedRevision } : {}
  });
}
async function editingProjectGet(args) {
  const projectId = projectIdValue(args) ?? STANDALONE_EDITING_PROJECT_ID;
  const editingProjectId = editingProjectIdValue(args);
  return editingServiceProjectCommand("getProject", { projectId, editingProjectId });
}
async function editingProjectUpdateSettings(args) {
  return editingServiceProjectCommand("updateProjectSettings", args);
}
async function editingTimelineApplyCommands(args) {
  return editingServiceProjectCommand("applyTimelineCommands", args);
}
async function editingTimelineValidate(args) {
  return editingServiceProjectCommand("validateTimeline", args);
}
async function editingTimelineAddTrack(args) {
  return editingServiceProjectCommand("addTrack", args);
}
async function editingTimelineRemoveTrack(args) {
  return editingServiceProjectCommand("removeTrack", args);
}
async function editingTimelineAddClip(args) {
  return editingServiceProjectCommand("addClip", args);
}
async function editingTimelineUpdateClip(args) {
  return editingServiceProjectCommand("updateClip", args);
}
async function editingTimelineSplitClip(args) {
  return editingServiceProjectCommand("splitClip", args);
}
async function editingTimelineMoveClip(args) {
  return editingServiceProjectCommand("moveClip", args);
}
async function editingTimelineDeleteClip(args) {
  return editingServiceProjectCommand("deleteClip", args);
}
async function editingRuntimeCapabilitiesGet(args) {
  const runtime = getEditingRuntimePort();
  if (!runtime?.getCapabilities) return editingRuntimeRequired(args);
  return runtime.getCapabilities();
}
async function editingTaskRenderCreate(args) {
  return mediaPipelineTaskCreate("timeline_render", args);
}
async function editingTaskHlsCreate(args) {
  return mediaPipelineTaskCreate("timeline_hls", args);
}
async function editingTaskTranscodeCreate(args) {
  return editingTaskSourceCreate(args, "media_transcode");
}
async function editingTaskReframeCreate(args) {
  return editingTaskSourceCreate(args, "media_reframe");
}
async function editingTaskGet(args) {
  const action = await editingServiceRuntimeTaskAction("getTask", args);
  const serviceResponse = await mediaPipelineTaskAction("getTask", action);
  if (serviceResponse) {
    const task2 = serviceResponse.task;
    if (!task2) {
      return {
        status: "not_found",
        task_id: action.taskId
      };
    }
    return taskResult(task2);
  }
  const runtime = getEditingRuntimePort();
  if (!runtime) return editingRuntimeRequired(args);
  const task = await runtime.getTask(action.taskId, action.options);
  if (!task) {
    return {
      status: "not_found",
      task_id: action.taskId
    };
  }
  return taskResult(task);
}
async function editingTaskCancel(args) {
  const action = await editingServiceRuntimeTaskAction("cancelTask", args);
  const serviceResponse = await mediaPipelineTaskAction("cancelTask", action);
  if (serviceResponse?.task) return taskResult(serviceResponse.task);
  const runtime = getEditingRuntimePort();
  if (!runtime) return editingRuntimeRequired(args);
  return taskResult(await runtime.cancelTask(action.taskId, action.options));
}
async function editingTaskLogsGet(args) {
  const action = await editingServiceRuntimeTaskAction("getTaskLogs", args);
  const serviceResponse = await mediaPipelineTaskAction("getTaskLogs", action);
  if (serviceResponse?.logs) {
    return {
      ...serviceResponse.logs,
      task_id: serviceResponse.logs.taskId
    };
  }
  const runtime = getEditingRuntimePort();
  if (!runtime) return editingRuntimeRequired(args);
  if (!runtime.getTaskLogs) {
    return {
      status: "unsupported_runtime",
      code: "EDITING_TASK_LOGS_UNAVAILABLE",
      message: "The registered Electron editing runtime does not expose task logs yet.",
      task_id: action.taskId
    };
  }
  const logs = await runtime.getTaskLogs(action.taskId, action.options);
  return {
    ...logs,
    task_id: logs.taskId
  };
}
async function editingExportImportResource(args) {
  const runtime = getEditingRuntimePort();
  if (!runtime?.importExportResource) return editingRuntimeRequired(args);
  const task = await editingRuntimeTaskSnapshot(args, runtime);
  const envelope = await editingServiceTaskAction("importExportResource", {
    ...args,
    ...task ? { task } : {}
  });
  if (envelope.result !== void 0) return envelope.result;
  if (!envelope.request) throw new Error("editing service did not return an import export resource request");
  return runtime.importExportResource(envelope.request);
}
async function editingExportSaveLocal(args) {
  const runtime = getEditingRuntimePort();
  const taskId = stringValue43(args.taskId ?? args.task_id);
  const explicitOutputPath = stringValue43(args.outputPath ?? args.output_path);
  if (taskId && !explicitOutputPath && !runtime?.getTask) return editingRuntimeRequired(args);
  const task = await editingRuntimeTaskSnapshot(args, runtime);
  const envelope = await editingServiceTaskAction("saveLocalExport", {
    ...args,
    ...task ? { task } : {}
  });
  if (envelope.result !== void 0) return envelope.result;
  if (!envelope.request) throw new Error("editing service did not return a save local export request");
  if (!runtime?.saveLocalExport) return editingRuntimeRequired(args);
  const saved = await runtime.saveLocalExport(envelope.request);
  return {
    ...saved,
    task_id: stringValue43(envelope.request.taskId ?? envelope.request.task_id),
    persisted: true,
    uploaded: false,
    candidate_created: false,
    ...task ? { task } : {}
  };
}
async function editingExportPublishHls(args) {
  const runtime = getEditingRuntimePort();
  if (!runtime?.publishHlsStream) return editingRuntimeRequired(args);
  const task = await editingRuntimeTaskSnapshot(args, runtime);
  const envelope = await editingServiceTaskAction("publishHlsStream", {
    ...args,
    ...task ? { task } : {}
  });
  if (envelope.result !== void 0) return envelope.result;
  if (!envelope.request) throw new Error("editing service did not return an HLS publish request");
  return runtime.publishHlsStream(envelope.request);
}
async function editingExportCreateCandidate(args) {
  const contentUnitId = stringOrNumberValue2(args.contentUnitId ?? args.content_unit_id);
  if (contentUnitId === void 0) throw new Error("contentUnitId is required");
  const streamId = stringOrNumberValue2(args.streamId ?? args.stream_id);
  if (streamId !== void 0 && args.resourceId === void 0 && args.resource_id === void 0) {
    return {
      status: "unsupported_output",
      code: "HLS_STREAM_CANDIDATE_UNSUPPORTED",
      message: "editing_export_create_candidate currently writes RawResource-backed candidates only. HLS MediaStreamArtifact candidate outputs require a future domain candidate schema extension; keep the stream as a hosted preview or create a RawResource export before writing a candidate.",
      content_unit_id: contentUnitId,
      streamId,
      stream_id: streamId
    };
  }
  const resourceId = requiredResourceIdValue(args.resourceId ?? args.resource_id);
  const candidateId = stringOrNumberValue2(args.candidateId ?? args.candidate_id);
  const outputKind = stringValue43(args.outputKind ?? args.output_kind ?? args.kind) ?? "video";
  const mimeType = stringValue43(args.mimeType ?? args.mime_type) ?? defaultMimeTypeForOutputKind(outputKind);
  const durationMs = optionalNumber(args.durationMs ?? args.duration_ms);
  const durationSec = optionalNumber(args.durationSec ?? args.duration_sec) ?? (durationMs !== void 0 ? durationMs / 1e3 : void 0);
  const width = optionalNumber(args.width);
  const height = optionalNumber(args.height);
  const taskId = stringValue43(args.taskId ?? args.task_id);
  const outputPath = stringValue43(args.outputPath ?? args.output_path);
  const editingProjectId = stringValue43(args.editingProjectId ?? args.editing_project_id);
  const provenance = objectArg(args, "provenance");
  const promptSnapshotRaw = args.promptSnapshot ?? args.prompt_snapshot;
  const promptSnapshotInput = isRecord4(promptSnapshotRaw) ? { ...promptSnapshotRaw } : {};
  const outputMetadata = {
    operation: "editing_export_create_candidate",
    tool: "editing_export_create_candidate",
    ...taskId ? { task_id: taskId } : {},
    ...outputPath ? { output_path: outputPath } : {},
    ...editingProjectId ? { editing_project_id: editingProjectId } : {},
    ...provenance ? { provenance } : {}
  };
  const candidate = await domainCreateContentCandidate({
    ...args,
    contentUnitId,
    ...candidateId !== void 0 ? { candidateId } : {},
    source: stringValue43(args.source) ?? "editing_export",
    status: stringValue43(args.status) ?? "imported",
    producer: {
      ...isRecord4(args.producer) ? args.producer : {},
      kind: stringValue43(args.producer?.kind) ?? "editing",
      tool: "editing_export_create_candidate",
      ...taskId ? { task_id: taskId } : {},
      ...editingProjectId ? { editing_project_id: editingProjectId } : {}
    },
    outputs: [{
      kind: outputKind,
      resource_id: resourceId,
      mime_type: mimeType,
      ...durationSec !== void 0 ? { duration_sec: durationSec } : {},
      ...width !== void 0 ? { width } : {},
      ...height !== void 0 ? { height } : {},
      metadata: outputMetadata
    }],
    promptSnapshot: {
      ...promptSnapshotInput,
      schema: "movscript.editing_export_candidate.v1",
      content_unit_id: contentUnitId,
      resource_id: resourceId,
      ...candidateId !== void 0 ? { candidate_id: candidateId } : {},
      ...taskId ? { task_id: taskId } : {},
      ...outputPath ? { output_path: outputPath } : {},
      ...editingProjectId ? { editing_project_id: editingProjectId } : {}
    }
  });
  return {
    status: "created",
    candidate_created: true,
    adopted: false,
    selected: false,
    content_unit_id: contentUnitId,
    resource_id: resourceId,
    ...candidateId !== void 0 ? { candidate_id: candidateId } : {},
    candidate
  };
}
async function editingRuntimeRequired(args) {
  return {
    status: "unsupported_runtime",
    code: "ELECTRON_EDITING_RUNTIME_REQUIRED",
    message: "This editing tool requires the Electron mediaPipeline runtime. The MCP contract is registered, but runtime IPC is not connected in this process yet.",
    received: Object.keys(args).sort()
  };
}
async function editingTaskSourceCreate(args, taskType) {
  return mediaPipelineTaskCreate(taskType, args);
}
async function mediaPipelineTaskCreate(taskType, args) {
  const request = await editingServiceTaskRequest(taskType, args);
  const service2 = optionalMediaPipelineServiceClient();
  if (service2) {
    const response = await service2.createTask({ request });
    return taskResult(response.task);
  }
  const runtime = getEditingRuntimePort();
  if (!runtime) return editingRuntimeRequired(args);
  const task = await runtime.createTask(request);
  return taskResult(task);
}
async function mediaPipelineTaskAction(action, request) {
  const service2 = optionalMediaPipelineServiceClient();
  if (!service2) return void 0;
  return service2.taskAction({
    action,
    taskId: request.taskId,
    options: request.options
  });
}
function optionalMediaPipelineServiceClient() {
  try {
    return createMediaPipelineServiceClientFromRuntime();
  } catch (error) {
    if (error instanceof Error && error.message.includes("movscript.media.pipeline endpoint was not found")) {
      return void 0;
    }
    throw error;
  }
}
async function editingRuntimeTaskSnapshot(args, runtime) {
  if (!stringValue43(args.taskId ?? args.task_id) || !runtime?.getTask) return void 0;
  const action = await editingServiceRuntimeTaskAction("getTask", args);
  return runtime.getTask(action.taskId, action.options);
}
async function composeEditingProjectWithCompile(args, renderRuntime) {
  const explicitProject = objectArg(args, "editingProject") ?? objectArg(args, "editing_project") ?? objectArg(args, "project");
  if (explicitProject) return { editingProject: editingProjectArg(args) };
  const editingProjectId = stringValue43(args.editingProjectId ?? args.editing_project_id);
  if (editingProjectId && !objectArg(args, "editDecisions") && !objectArg(args, "edit_decisions")) {
    const result = await editingProjectGet({
      projectId: projectIdValue(args) ?? STANDALONE_EDITING_PROJECT_ID,
      editingProjectId
    });
    return { editingProject: editingProjectFromServiceResult(result) };
  }
  const editDecisions = objectArg(args, "editDecisions") ?? objectArg(args, "edit_decisions");
  if (editDecisions) {
    const assetManifest = objectArg(args, "assetManifest") ?? objectArg(args, "asset_manifest");
    const timelineAssembly = objectArg(args, "timelineAssembly") ?? objectArg(args, "timeline_assembly");
    const compileResult = compileTimelineAssemblyToMediaEditingProject({
      timelineAssembly,
      assetManifest,
      editDecisions,
      renderRuntime,
      runtimeLocked: true,
      renderSettings: {
        width: optionalNumber(args.width),
        height: optionalNumber(args.height),
        fps: optionalNumber(args.fps),
        background: stringValue43(args.background),
        default_duration_ms: optionalNumber(args.defaultDurationMs ?? args.default_duration_ms)
      },
      projectOptions: {
        id: stringValue43(args.id ?? args.editingProjectId ?? args.editing_project_id),
        projectId: projectIdValue(args) ?? STANDALONE_EDITING_PROJECT_ID,
        title: stringValue43(args.title),
        width: optionalNumber(args.width),
        height: optionalNumber(args.height),
        fps: optionalNumber(args.fps),
        background: stringValue43(args.background),
        defaultDurationMs: optionalNumber(args.defaultDurationMs ?? args.default_duration_ms),
        productionId: stringOrNumberValue2(args.productionId ?? args.production_id),
        productionPath: stringValue43(args.productionPath ?? args.production_path),
        targetKind: stringValue43(args.targetKind ?? args.target_kind),
        targetRef: stringValue43(args.targetRef ?? args.target_ref),
        scopeKind: stringValue43(args.scopeKind ?? args.scope_kind),
        scopeRef: stringOrNumberValue2(args.scopeRef ?? args.scope_ref)
      }
    });
    if (compileResult.status === "blocked" || !compileResult.media_editing_project) {
      return { compileResult };
    }
    const saved = await persistCreatedEditingProject(compileResult.media_editing_project, {
      status: "ok",
      editing_project: compileResult.media_editing_project
    });
    const editingProject = editingProjectFromServiceResult(saved);
    return {
      editingProject,
      compileResult: {
        ...compileResult,
        media_editing_project: editingProject
      }
    };
  }
  throw new Error("editingProject, editingProjectId, or editDecisions is required");
}
function normalizeRenderRuntime(value) {
  const runtime = stringValue43(value) ?? "movscript_media_pipeline";
  return {
    value: runtime,
    supported: runtime === "movscript_media_pipeline" || runtime === "ffmpeg"
  };
}
function timelineValidationHasErrors(validation) {
  if (!isRecord4(validation)) return true;
  if (validation.valid === false) return true;
  const diagnostics = Array.isArray(validation.diagnostics) ? validation.diagnostics : [];
  return diagnostics.some((diagnostic2) => isRecord4(diagnostic2) && diagnostic2.severity === "error");
}
function editingProjectArg(args) {
  const project = objectArg(args, "editingProject") ?? objectArg(args, "editing_project") ?? objectArg(args, "project");
  if (!project) throw new Error("editingProject is required");
  assertMediaEditingProjectEnvelope(project);
  return project;
}
function assertMediaEditingProjectEnvelope(project) {
  if (project.version !== 1 || !stringValue43(project.id) || !stringValue43(project.projectId ?? project.project_id)) {
    throw new Error("editingProject must be a MediaEditingProject v1 object");
  }
  if (!isRecord4(project.timeline) || project.timeline.version !== 1 || !Array.isArray(project.timeline.tracks)) {
    throw new Error("editingProject.timeline must be a MediaTimelineRecipe v1 object");
  }
  if (!isRecord4(project.assets) || !Array.isArray(project.assets.assets)) {
    throw new Error("editingProject.assets must contain an assets array");
  }
}
function editingProjectFromServiceResult(result) {
  const record = isRecord4(result) ? result : void 0;
  const project = record && (isRecord4(record.editing_project) ? record.editing_project : isRecord4(record.editingProject) ? record.editingProject : void 0);
  if (!project) throw new Error("editing service did not return editing_project");
  assertMediaEditingProjectEnvelope(project);
  return project;
}
function projectIdValue(args) {
  const value = args.projectId ?? args.project_id;
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return stringValue43(value);
}
function editingProjectIdValue(args) {
  const editingProjectId = stringValue43(args.editingProjectId ?? args.editing_project_id);
  if (!editingProjectId) throw new Error("editingProjectId is required");
  return editingProjectId;
}
function requiredResourceIdValue(value) {
  const resourceId = optionalNumber(value);
  if (resourceId === void 0 || !Number.isInteger(resourceId) || resourceId <= 0) throw new Error("resourceId is required");
  return resourceId;
}
function defaultMimeTypeForOutputKind(kind) {
  if (kind === "audio") return "audio/mpeg";
  if (kind === "image") return "image/png";
  if (kind === "subtitle" || kind === "text") return "text/plain";
  return "video/mp4";
}
function taskResult(task) {
  return {
    status: "ok",
    task,
    media_pipeline_task: task
  };
}
function objectArg(args, key) {
  const direct = args[key];
  if (isRecord4(direct)) return direct;
  return void 0;
}
function optionalNumber(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function booleanValue2(value) {
  if (typeof value === "boolean") return value;
  if (typeof value === "string") {
    if (value === "true") return true;
    if (value === "false") return false;
  }
  return void 0;
}
function stringValue43(value) {
  if (typeof value !== "string") return void 0;
  const trimmed = value.trim();
  return trimmed ? trimmed : void 0;
}
function stringOrNumberValue2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  return stringValue43(value);
}
async function listModels(args) {
  const rawCapability = getOptionalString(args, "capability");
  const operation = getOptionalString(args, "operation") ?? getOptionalString(args, "model_operation");
  const normalizedCapability = normalizeModelCapabilityAlias(rawCapability);
  const capability = normalizedCapability ?? (rawCapability ? void 0 : capabilityForModelOperation(operation));
  const referenceAssets = modelReferenceAssetsArg(args.reference_assets ?? args.referenceAssets);
  const providerVariants = args.provider_variants === true || args.include_provider_variants === true;
  const defaultCapabilities = ["text_generation", "image_generation", "video_generation", "audio_generation", "text", "image", "image_edit", "video", "video_i2v", "video_v2v", "audio_tts", "audio_transcribe", "audio_translate", "audio_music", "audio_sfx", "audio_chat", "voice_clone", "voice_design", "subtitle_align", "subtitle_translate"];
  if (rawCapability && !capability) {
    return {
      count: 0,
      queries: [],
      model_contracts: [],
      models: []
    };
  }
  if (operation && !capability) {
    return {
      count: 0,
      queries: [],
      model_contracts: [],
      models: []
    };
  }
  const queries = capability ? [modelListQuery(capability, { operation, providerVariants, referenceAssets })] : defaultCapabilities.map((item) => ({
    label: `capability:${item}`,
    path: modelListPath(item, { providerVariants })
  }));
  const byId = /* @__PURE__ */ new Map();
  for (const query of queries) {
    const models = await backendList(query.path);
    for (const model of models) {
      const id = Number(model?.id ?? model?.ID);
      const contract = summarizeModelContractForAgent(model);
      const key = typeof contract.model_id === "string" && contract.model_id.trim() ? contract.model_id.trim() : Number.isFinite(id) && id > 0 ? `backend.model.${id}` : `model.${byId.size}`;
      if (!byId.has(key)) byId.set(key, contract);
    }
  }
  const modelContracts = Array.from(byId.values());
  return {
    count: byId.size,
    queries: queries.map((query) => query.label),
    model_contracts: modelContracts,
    models: modelContracts
  };
}
function modelListQuery(capability, options) {
  const parts = [`capability:${capability}`];
  if (options.operation) parts.push(`operation:${options.operation}`);
  if (options.referenceAssets && options.referenceAssets.length > 0) {
    parts.push(`reference_assets:${options.referenceAssets.length}`);
  }
  return {
    label: parts.join(":"),
    path: modelListPath(capability, options)
  };
}
function modelListPath(capability, options = {}) {
  const params = new URLSearchParams();
  params.set("capability", capability);
  if (options.operation) params.set("operation", options.operation);
  if (options.providerVariants) params.set("provider_variants", "true");
  if (options.referenceAssets && options.referenceAssets.length > 0) {
    params.set("reference_assets", JSON.stringify(options.referenceAssets));
  }
  return `/models?${params.toString()}`;
}
function capabilityForModelOperation(operation) {
  const normalized = operation?.trim().toLowerCase().replace(/-/g, "_");
  switch (normalized) {
    case "text_to_image":
    case "image_to_image":
      return "image_generation";
    case "prompt_to_video":
    case "text_to_video":
    case "image_to_video":
    case "first_frame_to_video":
    case "first_last_frame_to_video":
    case "reference_to_video":
    case "video_to_video":
      return "video_generation";
    case "tts":
    case "stt":
    case "speech_translate":
    case "audio_chat":
    case "voice_clone":
    case "voice_design":
    case "dubbing":
    case "music":
    case "sfx":
    case "speech_enhancement":
      return "audio_generation";
    default:
      return void 0;
  }
}
function modelReferenceAssetsArg(value) {
  const raw = Array.isArray(value) ? value : [];
  const refs = raw.filter(isRecord4).map((item) => {
    const role = getOptionalString(item, "role") ?? "generic";
    const mediaType = getOptionalString(item, "media_type") ?? getOptionalString(item, "mediaType");
    return {
      role,
      ...mediaType ? { media_type: mediaType } : {}
    };
  }).filter((item) => item.role.trim());
  return refs.length > 0 ? refs : void 0;
}
var generationCapabilities = [
  "image_generation",
  "video_generation",
  "audio_generation",
  "image",
  "image_edit",
  "video",
  "video_i2v",
  "video_v2v",
  "audio_tts",
  "audio_transcribe",
  "audio_translate",
  "audio_music",
  "audio_sfx",
  "audio_chat",
  "voice_clone",
  "voice_design",
  "subtitle_align",
  "subtitle_translate"
];
async function generateImage(args) {
  const built = buildImageRequest(args);
  const selection = await resolveModelSelection(args, built.generationIntent?.capability ?? built.jobType, built.jobType === "image_edit" ? "image" : "image_edit", built.generationIntent?.operation);
  const submitted = await submitGenerationJob(args, selection, built, "electron.generation.image");
  return withGenerationJobSurface(args, generationSubmitResult("image", submitted.job, "generation_job_get", submitted.paramAudit));
}
async function listGenerationCapabilities(args) {
  const includeModels = args.include_models === true;
  const modelsByCapability = {};
  if (includeModels) {
    for (const capability of generationCapabilities) {
      const models = await modelsForCapability(capability);
      modelsByCapability[capability] = {
        count: models.length,
        models
      };
    }
  }
  return {
    capabilities: generationCapabilities,
    count: generationCapabilities.length,
    ...includeModels ? { models_by_capability: modelsByCapability } : {}
  };
}
async function prepareGeneration(args) {
  const capability = requiredGenerationCapability(args);
  const scope = generationScope(args);
  const operation = generationOperationArg(args);
  if ((isImageGenerationCapability(capability) || isVideoGenerationCapability(capability) || isAudioGenerationCapability(capability)) && !operation) {
    throw new Error(`${capability} operation is required for generation_prepare; choose an explicit operation instead of relying on input resources`);
  }
  const referenceAssets = generationReferenceAssetsForModelList(args);
  const models = await listModels({
    capability,
    operation,
    ...referenceAssets.length > 0 ? { reference_assets: referenceAssets } : {},
    provider_variants: args.provider_variants,
    include_provider_variants: args.include_provider_variants
  });
  if (scope === "content_unit" && (isImageGenerationCapability(capability) || isVideoGenerationCapability(capability))) {
    const contentUnitId = requiredContentUnitId(args);
    const compiled = await compiledContentUnitPrompt(args, contentUnitId);
    const blockers = Array.isArray(compiled.blockers) ? compiled.blockers : compiled.prompt?.blockers ?? [];
    return {
      status: compiled.ok === true ? "ready" : "blocked",
      capability,
      scope,
      contentUnitId,
      content_unit_id: contentUnitId,
      prompt: compiled.prompt,
      blockers,
      ...isRecord4(models) ? models : { models },
      message: compiled.ok === true ? `Content unit ${String(contentUnitId)} is ready for ${capability} generation.` : `Content unit ${String(contentUnitId)} has unresolved prompt blockers.`
    };
  }
  return {
    status: "ready",
    capability,
    scope,
    ...isRecord4(models) ? models : { models },
    message: `${capability} generation is ready to submit.`
  };
}
async function submitUnifiedGeneration(args) {
  const capability = requiredGenerationCapability(args);
  const scope = generationScope(args);
  const candidatePolicy = getOptionalString(args, "candidate_policy") ?? (scope === "content_unit" ? "auto_create" : "none");
  if (scope === "content_unit") {
    if (isImageGenerationCapability(capability)) {
      return generationV2Result(await generateContentUnitImage(args), capability, scope, "image", "content_unit_candidate", candidatePolicy);
    }
    if (isVideoGenerationCapability(capability)) {
      return generationV2Result(await generateContentUnitVideo(args), capability, scope, "video", "content_unit_candidate", candidatePolicy);
    }
    throw new Error(`scope=content_unit is currently supported by image and video generation capabilities; got ${capability}`);
  }
  switch (capability) {
    case "image_generation":
    case "image":
    case "image_edit":
      return generationV2Result(await generateImage(args), capability, scope, "image", "raw_resource", candidatePolicy);
    case "video_generation":
    case "video":
    case "video_i2v":
    case "video_v2v":
      return generationV2Result(await generateVideo(args), capability, scope, "video", "raw_resource", candidatePolicy);
    case "audio_generation":
      return submitUnifiedAudioGeneration(args, capability, scope, candidatePolicy);
    case "audio_tts":
      return generationV2Result(await generateVoiceover(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "audio_transcribe":
      return generationV2Result(await generateSubtitle(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
    case "audio_translate":
      return generationV2Result(await generateAudioTranslate(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
    case "audio_music":
      return generationV2Result(await generateMusic(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "audio_sfx":
      return generationV2Result(await generateSfx(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "audio_chat":
      return generationV2Result(await generateAudioChat(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "voice_clone":
      return generationV2Result(await generateVoiceClone(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
    case "voice_design":
      return generationV2Result(await generateVoiceDesign(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
    case "subtitle_align":
      return generationV2Result(await alignSubtitle(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
    case "subtitle_translate":
      return generationV2Result(await translateSubtitle(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
    default:
      return assertNeverGenerationCapability(capability);
  }
}
async function submitUnifiedAudioGeneration(args, capability, scope, candidatePolicy) {
  const operation = generationOperationArg(args);
  if (!operation) {
    throw new Error("audio_generation operation is required; choose a model operation such as tts, stt, music, or sfx");
  }
  switch (operation) {
    case "tts":
      return generationV2Result(await generateVoiceover(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "stt":
      return generationV2Result(await generateSubtitle(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
    case "speech_translate":
      return generationV2Result(await generateAudioTranslate(args), capability, scope, "subtitle", "raw_resource", candidatePolicy);
    case "music":
      return generationV2Result(await generateMusic(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "sfx":
      return generationV2Result(await generateSfx(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "audio_chat":
      return generationV2Result(await generateAudioChat(args), capability, scope, "audio", "raw_resource", candidatePolicy);
    case "voice_clone":
      return generationV2Result(await generateVoiceClone(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
    case "voice_design":
      return generationV2Result(await generateVoiceDesign(args), capability, scope, "voice_profile", "raw_resource", candidatePolicy);
    default:
      throw new Error(`unsupported audio_generation operation: ${operation}`);
  }
}
async function getUnifiedGenerationJob(args) {
  const capability = optionalGenerationCapability(args);
  const scope = generationScope(args);
  const outputKind = outputKindArg(args, capability);
  if (scope === "content_unit" && (outputKind === "image" || outputKind === "video")) {
    const result2 = outputKind === "image" ? await getContentUnitImageGenerationJob({ ...args, outputKind }) : await getContentUnitVideoGenerationJob({ ...args, outputKind });
    return generationV2Result(result2, capability ?? outputKind, scope, outputKind, "content_unit_candidate", getOptionalString(args, "candidate_policy") ?? "auto_create");
  }
  const kind = generationOutputJobGroup(outputKind);
  const result = withGenerationJobSurface(args, generationJobGetResult(kind, await getGenerationJob(normalizedJobId(args)), verbosityArg(args)));
  return generationV2Result(result, capability ?? outputKind, scope, outputKind, "raw_resource", getOptionalString(args, "candidate_policy") ?? "none");
}
async function getUnifiedGenerationJobs(args) {
  const jobIds = normalizedJobIds(args);
  const items = [];
  const rawItems = Array.isArray(args.items) ? args.items : [];
  for (let index = 0; index < jobIds.length; index += 1) {
    const jobId = jobIds[index];
    const itemArgs = isRecord4(rawItems[index]) ? { ...args, ...rawItems[index], jobId } : { ...args, jobId };
    try {
      const result = await getUnifiedGenerationJob(itemArgs);
      items.push({
        index,
        status: "loaded",
        jobId,
        job_id: jobId,
        terminal: isRecord4(result) ? result.terminal : void 0,
        outputResourceIds: isRecord4(result) ? result.outputResourceIds : void 0,
        output_resource_ids: isRecord4(result) ? result.output_resource_ids : void 0,
        result
      });
    } catch (error) {
      items.push({
        index,
        status: "error",
        jobId,
        job_id: jobId,
        terminal: true,
        error: errorMessage42(error)
      });
    }
  }
  const successItems = items.filter((item) => item.status !== "error");
  const failedItems = items.filter((item) => item.status === "error");
  const terminalCount = items.filter((item) => item.terminal === true).length;
  const outputResourceIds = Array.from(new Set(successItems.flatMap((item) => numericList2(item.output_resource_ids))));
  return {
    status: failedItems.length === 0 ? "loaded" : successItems.length > 0 ? "partial_error" : "error",
    total: jobIds.length,
    success_count: successItems.length,
    failed_count: failedItems.length,
    terminal_count: terminalCount,
    all_terminal: terminalCount === jobIds.length,
    output_resource_ids: outputResourceIds,
    outputResourceIds,
    items,
    message: `${successItems.length}/${jobIds.length} generation job(s) loaded.`
  };
}
async function registerGenerationResult(args) {
  const result = await domainRegisterRawResourceAsContentUnitCandidate(args);
  return {
    status: "registered",
    candidate: result,
    message: "Generation result registered as a content-unit candidate."
  };
}
async function generateContentUnitImage(args) {
  return generateContentUnitVisual(args, "image");
}
async function getContentUnitImageGenerationJob(args) {
  return getContentUnitVisualGenerationJob(args, "image");
}
async function generateVideo(args) {
  const built = buildVideoRequest(args);
  const selection = await resolveModelSelection(args, built.generationIntent?.capability ?? built.jobType, "video", built.generationIntent?.operation);
  const submitted = await submitGenerationJob(args, selection, built, "electron.generation.video");
  return withGenerationJobSurface(args, generationSubmitResult("video", submitted.job, "generation_job_get", submitted.paramAudit));
}
async function generateContentUnitVideo(args) {
  return generateContentUnitVisual(args, "video");
}
async function getContentUnitVideoGenerationJob(args) {
  return getContentUnitVisualGenerationJob(args, "video");
}
async function generateVoiceover(args) {
  return generateAudioLike(args, "audio_tts", "audio_tts", "electron.generation.voiceover");
}
async function generateMusic(args) {
  return generateAudioLike(args, "audio_music", "audio_music", "electron.generation.music");
}
async function generateSfx(args) {
  return generateAudioLike(args, "audio_sfx", "audio_sfx", "electron.generation.sfx");
}
async function generateAudioChat(args) {
  return generateAudioLike(args, "audio_chat", "audio_chat", "electron.generation.audio_chat");
}
async function generateSubtitle(args) {
  return generateAudioLike(args, "audio_transcribe", "audio_transcribe", "electron.generation.subtitle");
}
async function alignSubtitle(args) {
  return generateAudioLike(args, "subtitle_align", "subtitle_align", "electron.generation.subtitle_align");
}
async function translateSubtitle(args) {
  return generateAudioLike(args, "subtitle_translate", "subtitle_translate", "electron.generation.subtitle_translate");
}
async function generateAudioTranslate(args) {
  return generateAudioLike(args, "audio_translate", "audio_translate", "electron.generation.audio_translate");
}
async function generateVoiceClone(args) {
  return generateAudioLike(args, "voice_clone", "voice_clone", "electron.generation.voice_clone");
}
async function generateVoiceDesign(args) {
  return generateAudioLike(args, "voice_design", "voice_design", "electron.generation.voice_design");
}
async function generateAudioLike(args, jobType, fallbackCapability, featureKey) {
  const built = buildAudioRequest(args, jobType);
  const selection = await resolveModelSelection(args, built.generationIntent?.capability ?? built.jobType, fallbackCapability, built.generationIntent?.operation);
  const submitted = await submitGenerationJob(args, selection, built, featureKey);
  return withGenerationJobSurface(args, generationSubmitResult("audio", submitted.job, "generation_job_get", submitted.paramAudit));
}
async function generateContentUnitVisual(args, kind) {
  const contentUnitId = requiredContentUnitId(args);
  const compiled = await compiledContentUnitPrompt(args, contentUnitId);
  if (compiled.ok !== true) {
    return {
      status: "blocked",
      terminal: true,
      contentUnitId,
      content_unit_id: contentUnitId,
      prompt: compiled.prompt,
      blockers: Array.isArray(compiled.blockers) ? compiled.blockers : compiled.prompt?.blockers ?? [],
      surface: createPromptSurface(args, { contentUnitId, mode: "edit", projectId: projectIdFromArgs(args) }),
      message: `Content unit ${String(contentUnitId)} ${kind} generation is blocked by unresolved prompt inputs.`
    };
  }
  const prompt = compiledContentUnitGenerationPromptText(compiled.prompt);
  const providerPrompt = stripResourceMentions(prompt);
  const compiledRefIds = positiveIntegerIds2([
    ...compiledContentUnitGenerationPromptResourceIds(compiled.prompt),
    ...resourceIds(args.input_resource_ids) ?? [],
    ...resourceIds(args.reference_resource_ids) ?? []
  ]);
  const nextArgs = {
    ...args,
    prompt,
    input_resource_ids: compiledRefIds
  };
  if (kind === "image" && getOptionalString(nextArgs, "negative_prompt") === void 0) {
    const negative = typeof compiled.prompt.negative_text === "string" ? compiled.prompt.negative_text.trim() : "";
    if (negative) nextArgs.negative_prompt = negative;
  }
  const built = kind === "image" ? buildImageRequest(nextArgs) : buildVideoRequest(nextArgs);
  const selection = kind === "image" ? await resolveModelSelection(nextArgs, built.generationIntent?.capability ?? built.jobType, built.jobType === "image_edit" ? "image" : "image_edit", built.generationIntent?.operation) : await resolveModelSelection(nextArgs, built.generationIntent?.capability ?? built.jobType, "video", built.generationIntent?.operation);
  const projectScope = await resolveGenerationProjectScope(args, { required: true });
  const sharedRequest = buildContentUnitGenerationRequest({
    contentUnitId,
    outputKind: kind,
    compiledPrompt: compiled.prompt,
    modelId: selection.modelId,
    additionalInputResourceIds: [
      ...resourceIds(args.input_resource_ids) ?? [],
      ...resourceIds(args.reference_resource_ids) ?? []
    ],
    generationIntent: built.generationIntent,
    paramAudit: []
  });
  const promptSnapshot = {
    ...sharedRequest.promptSnapshot,
    ...buildContentUnitGenerationPromptSnapshot({
      contentUnitId,
      outputKind: kind,
      modelId: selection.modelId,
      compiledPrompt: compiled.prompt,
      resourceIds: sharedRequest.inputResourceIds,
      paramAudit: [],
      modelParams: {}
    })
  };
  const prepared = prepareGenerationParams(built, selection.model, parameterModeArg(nextArgs));
  const modelParams = submittedModelParams(prepared);
  Object.assign(promptSnapshot, buildContentUnitGenerationPromptSnapshot({
    contentUnitId,
    outputKind: kind,
    modelId: selection.modelId,
    compiledPrompt: compiled.prompt,
    resourceIds: sharedRequest.inputResourceIds,
    paramAudit: prepared.audit,
    modelParams
  }));
  const submitted = await submitGenerationJob({
    ...args,
    title: getOptionalString(args, "title") ?? `Content unit ${kind} generation`
  }, selection, built, contentUnitGenerationFeatureKey(kind), {
    candidate_id: getOptionalString(args, "candidate_id") ?? getOptionalString(args, "candidateId"),
    output_kind: kind,
    prompt_snapshot: promptSnapshot,
    content_unit_id: contentUnitId
  });
  const monitorTool = contentUnitGenerationSystemMonitorToolName(kind);
  const result = withGenerationJobSurface(args, generationSubmitResult(kind, submitted.job, monitorTool, prepared.audit), contentUnitId);
  const jobId = idField23(result.job_id);
  const candidateId = getOptionalString(args, "candidate_id") ?? getOptionalString(args, "candidateId");
  return {
    ...result,
    generation_mode: "content_unit_candidate",
    candidate_policy: "auto_create_on_success",
    will_auto_select: false,
    requires_user_adoption: true,
    contentUnitId,
    content_unit_id: contentUnitId,
    prompt: compiled.prompt,
    compiled_prompt_text: prompt,
    provider_prompt_text: providerPrompt,
    input_resource_ids: sharedRequest.inputResourceIds,
    inputResourceIds: sharedRequest.inputResourceIds,
    semantic_ref_replacements: semanticRefReplacements(compiled.prompt),
    provider_prompt_note: "Provider prompt text intentionally strips MovScript resource tokens; resources are passed separately through input_resource_ids.",
    monitor: {
      tool: monitorTool,
      args: {
        jobId,
        contentUnitId,
        content_unit_id: contentUnitId,
        ...candidateId ? { candidateId, candidate_id: candidateId } : {},
        outputKind: kind,
        output_kind: kind,
        projectDir: projectScope.projectDir,
        project_dir: projectScope.projectDir,
        ...projectScope.projectUid ? { projectUid: projectScope.projectUid, project_uid: projectScope.projectUid } : {},
        promptSnapshot,
        prompt_snapshot: promptSnapshot
      }
    },
    secondary_surfaces: [
      createPromptSurface(args, { contentUnitId, mode: "inspect", projectId: projectIdFromArgs(args) })
    ],
    ...candidateId ? { candidateId, candidate_id: candidateId } : {},
    message: `Content unit ${String(contentUnitId)} ${kind} generation candidate job submitted (Job #${String(jobId)}). Candidate will be refreshed automatically when the job succeeds.`
  };
}
async function getContentUnitVisualGenerationJob(args, kind) {
  const contentUnitId = requiredContentUnitId(args);
  const job = await getGenerationJob(normalizedJobId(args));
  const base = generationJobGetResult(kind, job, verbosityArg(args));
  const status = stringField34(base.status) ?? "";
  if (!isSuccessfulStatus(status)) {
    return {
      ...base,
      generation_mode: "content_unit_candidate",
      candidate_policy: "auto_create_on_success",
      will_auto_select: false,
      requires_user_adoption: true,
      contentUnitId,
      content_unit_id: contentUnitId,
      candidate_created: false,
      surface: createGenerationJobSurface(args, {
        jobId: normalizedJobId(args),
        contentUnitId,
        projectId: projectIdFromArgs(args)
      }),
      message: `${base.message}. Candidate will be created after a successful terminal result.`
    };
  }
  const outputResourceIds = numericList2(base.output_resource_ids);
  if (outputResourceIds.length === 0) {
    return {
      ...base,
      generation_mode: "content_unit_candidate",
      candidate_policy: "auto_create_on_success",
      will_auto_select: false,
      requires_user_adoption: true,
      contentUnitId,
      content_unit_id: contentUnitId,
      candidate_created: false,
      surface: createGenerationJobSurface(args, {
        jobId: normalizedJobId(args),
        contentUnitId,
        projectId: projectIdFromArgs(args)
      }),
      message: `${base.message}. No output resource is available for candidate creation yet.`
    };
  }
  const candidates = [];
  for (const resourceId of outputResourceIds) {
    const promptSnapshotValue = args.promptSnapshot ?? args.prompt_snapshot;
    const promptSnapshot = isRecord4(promptSnapshotValue) ? promptSnapshotValue : void 0;
    const requestedCandidateId = getOptionalString(args, "candidateId") ?? getOptionalString(args, "candidate_id");
    const candidateId = requestedCandidateId ?? contentUnitGenerationCandidateId(kind, idField23(base.job_id) ?? normalizedJobId(args), resourceId);
    const plan = buildContentUnitGenerationOutputCandidate({
      contentUnitId,
      outputKind: kind,
      job,
      resourceId,
      candidateId,
      ...promptSnapshot ? { promptSnapshot } : {}
    });
    const candidate = await domainCreateContentCandidate({
      ...args,
      ...plan
    });
    candidates.push({
      candidateId,
      candidate_id: candidateId,
      resourceId,
      resource_id: resourceId,
      result: candidate
    });
  }
  const visibility = await readContentUnitCandidateVisibility(args, contentUnitId).catch(() => void 0);
  const firstCandidate = candidates.length === 1 ? candidates[0] : void 0;
  const firstCandidateId = firstCandidate ? stringField34(firstCandidate.candidate_id ?? firstCandidate.candidateId) : void 0;
  const firstResourceId = firstCandidate ? idField23(firstCandidate.resource_id ?? firstCandidate.resourceId) : void 0;
  return {
    ...base,
    generation_mode: "content_unit_candidate",
    candidate_policy: "auto_create_on_success",
    will_auto_select: false,
    requires_user_adoption: true,
    contentUnitId,
    content_unit_id: contentUnitId,
    candidate_created: true,
    candidate_count: candidates.length,
    candidates,
    surface: createContentCandidatesSurface(args, {
      contentUnitId,
      ...firstCandidateId ?? candidateIdFromArgs(args) ? { candidateId: firstCandidateId ?? candidateIdFromArgs(args) } : {},
      ...firstResourceId !== void 0 ? { resourceId: firstResourceId } : {},
      projectId: projectIdFromArgs(args)
    }),
    secondary_surfaces: [
      createGenerationJobSurface(args, {
        jobId: normalizedJobId(args),
        contentUnitId,
        projectId: projectIdFromArgs(args)
      })
    ],
    ...visibility ?? {},
    message: `${base.message}. Created or refreshed ${candidates.length} content-unit candidate(s).`
  };
}
function buildImageRequest(args) {
  const { prompt, refIds } = promptAndResourceIds(args);
  const generationIntent = generationIntentArg(args, "image", refIds);
  const params = extraParamsArg(args.extra_params);
  const explicitParamKeys = new Set(Object.keys(params));
  const defaultParamKeys = /* @__PURE__ */ new Set();
  assignStringParam(args, params, explicitParamKeys, "image_size");
  assignDefaultParam(params, defaultParamKeys, "image_size", "1024x1024");
  assignStringParam(args, params, explicitParamKeys, "aspect_ratio");
  assignDefaultParam(params, defaultParamKeys, "aspect_ratio", "1:1");
  const quality = getOptionalString(args, "quality");
  if (quality) {
    params.quality = quality;
    explicitParamKeys.add("quality");
  }
  const negativePrompt = getOptionalString(args, "negative_prompt");
  if (negativePrompt) {
    params.negative_prompt = negativePrompt;
    explicitParamKeys.add("negative_prompt");
  }
  const steps = getOptionalNumeric(args, "steps");
  if (steps !== void 0) {
    params.steps = steps;
    explicitParamKeys.add("steps");
  }
  const seed = getOptionalNumeric(args, "seed");
  if (seed !== void 0) {
    params.seed = seed;
    explicitParamKeys.add("seed");
  }
  return {
    prompt,
    refIds,
    jobType: generationExecutionJobTypeForIntent(generationIntent, "image"),
    generationIntent,
    timeoutMs: getOptionalNumeric(args, "timeout_ms") ?? 18e4,
    params,
    explicitParamKeys,
    defaultParamKeys
  };
}
function buildVideoRequest(args) {
  const { prompt, refIds } = promptAndResourceIds(args);
  const generationIntent = generationIntentArg(args, "video", refIds);
  const params = { ...extraParamsArg(args.extra_params) };
  const explicitParamKeys = new Set(Object.keys(params));
  const defaultParamKeys = /* @__PURE__ */ new Set();
  assignStringParam(args, params, explicitParamKeys, "aspect_ratio");
  assignDefaultParam(params, defaultParamKeys, "aspect_ratio", "16:9");
  const duration = getOptionalNumeric(args, "duration");
  if (duration !== void 0) {
    params.duration = duration;
    explicitParamKeys.add("duration");
  } else {
    assignDefaultParam(params, defaultParamKeys, "duration", 5);
  }
  const quality = getOptionalString(args, "quality");
  if (quality) {
    params.quality = quality;
    explicitParamKeys.add("quality");
  }
  const fps = getOptionalNumeric(args, "fps");
  if (fps !== void 0) {
    params.fps = fps;
    explicitParamKeys.add("fps");
  }
  const seed = getOptionalNumeric(args, "seed");
  if (seed !== void 0) {
    params.seed = seed;
    explicitParamKeys.add("seed");
  }
  return {
    prompt,
    refIds,
    jobType: generationExecutionJobTypeForIntent(generationIntent, "video"),
    generationIntent,
    timeoutMs: getOptionalNumeric(args, "timeout_ms") ?? 6e5,
    params,
    explicitParamKeys,
    defaultParamKeys
  };
}
function generationIntentArg(args, outputKind, refIds) {
  const explicit = args.generation_intent ?? args.generationIntent;
  const topLevelOperation = getOptionalString(args, "operation") ?? getOptionalString(args, "model_operation");
  if (isRecord4(explicit)) {
    const capability = getOptionalString(explicit, "capability") ?? (outputKind === "image" ? "image_generation" : "video_generation");
    const operation2 = getOptionalString(explicit, "operation") ?? topLevelOperation;
    if (capability && operation2) {
      return {
        capability,
        operation: operation2,
        ...generationReferenceAssetsPayload(explicit.reference_assets ?? explicit.referenceAssets, refIds)
      };
    }
  }
  const operation = topLevelOperation;
  if (!operation) {
    throw new Error(`${outputKind}_generation operation is required; choose an explicit operation instead of relying on input resources`);
  }
  return {
    capability: outputKind === "image" ? "image_generation" : "video_generation",
    operation,
    ...generationReferenceAssetsPayload(args.reference_assets ?? args.referenceAssets, refIds)
  };
}
function audioGenerationIntentArg(args, jobType, refIds) {
  const explicit = args.generation_intent ?? args.generationIntent;
  const topLevelOperation = getOptionalString(args, "operation") ?? getOptionalString(args, "model_operation");
  if (isRecord4(explicit)) {
    const capability = getOptionalString(explicit, "capability") ?? "audio_generation";
    const operation2 = getOptionalString(explicit, "operation") ?? topLevelOperation ?? audioOperationForJobType(jobType);
    if (capability && operation2) {
      return {
        capability,
        operation: operation2,
        ...generationReferenceAssetsPayload(explicit.reference_assets ?? explicit.referenceAssets, refIds)
      };
    }
  }
  const operation = topLevelOperation ?? audioOperationForJobType(jobType);
  if (!operation) return void 0;
  return {
    capability: "audio_generation",
    operation,
    ...generationReferenceAssetsPayload(args.reference_assets ?? args.referenceAssets, refIds)
  };
}
function audioOperationForJobType(jobType) {
  switch (jobType) {
    case "audio_tts":
      return "tts";
    case "audio_transcribe":
      return "stt";
    case "audio_translate":
      return "speech_translate";
    case "audio_music":
      return "music";
    case "audio_sfx":
      return "sfx";
    case "audio_chat":
      return "audio_chat";
    case "voice_clone":
      return "voice_clone";
    case "voice_design":
      return "voice_design";
    default:
      return void 0;
  }
}
function generationReferenceAssetsPayload(value, refIds) {
  const explicit = Array.isArray(value) ? value.filter(isRecord4) : [];
  const source = explicit.length > 0 ? explicit.map((item, index) => ({
    role: getOptionalString(item, "role") ?? "generic",
    media_type: getOptionalString(item, "media_type") ?? getOptionalString(item, "mediaType"),
    resource_id: idField23(item.resource_id ?? item.resourceId) ?? refIds[index]
  })) : refIds.map((resourceId) => ({ role: "generic", resource_id: resourceId }));
  const complete = source.filter((item) => item.role.trim() && item.resource_id !== void 0);
  if (complete.some((item) => !item.media_type?.trim())) {
    throw new Error("reference_assets media_type is required for every input resource; pass typed reference_assets instead of bare reference_resource_ids");
  }
  const referenceAssets = complete.map((item) => ({
    role: item.role.trim(),
    media_type: item.media_type.trim(),
    resource_id: item.resource_id
  }));
  return referenceAssets.length > 0 ? { reference_assets: referenceAssets } : {};
}
function generationOperationArg(args) {
  const explicit = args.generation_intent ?? args.generationIntent;
  if (isRecord4(explicit)) {
    const operation = getOptionalString(explicit, "operation");
    if (operation) return operation;
  }
  return getOptionalString(args, "operation") ?? getOptionalString(args, "model_operation");
}
function generationReferenceAssetsForModelList(args) {
  const explicit = args.generation_intent ?? args.generationIntent;
  const raw = isRecord4(explicit) && (explicit.reference_assets !== void 0 || explicit.referenceAssets !== void 0) ? explicit.reference_assets ?? explicit.referenceAssets : args.reference_assets ?? args.referenceAssets;
  const refs = Array.isArray(raw) ? raw.filter(isRecord4) : [];
  if (refs.length === 0) return [];
  return refs.map((item) => {
    const role = getOptionalString(item, "role") ?? "generic";
    const mediaType = getOptionalString(item, "media_type") ?? getOptionalString(item, "mediaType");
    return {
      role,
      ...mediaType ? { media_type: mediaType } : {}
    };
  }).filter((item) => item.role.trim());
}
function buildAudioRequest(args, jobType = "audio_tts") {
  const { prompt, refIds } = promptAndResourceIds(args);
  const generationIntent = audioGenerationIntentArg(args, jobType, refIds);
  const params = { ...extraParamsArg(args.extra_params) };
  const explicitParamKeys = new Set(Object.keys(params));
  const defaultParamKeys = /* @__PURE__ */ new Set();
  assignStringParam(args, params, explicitParamKeys, "voice");
  assignStringParam(args, params, explicitParamKeys, "language");
  assignStringParam(args, params, explicitParamKeys, "model");
  assignStringParam(args, params, explicitParamKeys, "audio_format");
  assignStringParam(args, params, explicitParamKeys, "response_format");
  assignStringParam(args, params, explicitParamKeys, "output_format");
  assignStringParam(args, params, explicitParamKeys, "instructions");
  assignStringParam(args, params, explicitParamKeys, "target_language");
  assignStringParam(args, params, explicitParamKeys, "source_language");
  assignStringParam(args, params, explicitParamKeys, "subtitle_format");
  assignStringParam(args, params, explicitParamKeys, "style");
  const speed = getOptionalNumeric(args, "speed");
  if (speed !== void 0) {
    params.speed = speed;
    explicitParamKeys.add("speed");
  }
  return {
    prompt,
    refIds,
    jobType,
    generationIntent,
    timeoutMs: getOptionalNumeric(args, "timeout_ms") ?? 18e4,
    params,
    explicitParamKeys,
    defaultParamKeys
  };
}
async function resolveModelSelection(args, primaryCapability, fallbackCapability, operation) {
  const explicit = getOptionalString(args, "model_id");
  if (explicit) {
    const models = await modelsForCapabilities([primaryCapability, fallbackCapability], operation);
    const model2 = models.find((model3) => modelMatchesPublicId(model3, explicit));
    return { modelId: explicit, model: model2 };
  }
  const primary = await modelsForCapability(primaryCapability, operation);
  const fallback = primary.length > 0 ? primary : await modelsForCapability(fallbackCapability, operation);
  const modelId = modelPublicId(fallback[0]);
  if (!modelId) throw new Error(`No enabled generation model is configured for ${primaryCapability}`);
  const model = isRecord4(fallback[0]) ? fallback[0] : void 0;
  return { modelId, model };
}
async function modelsForCapability(capability, operation) {
  const result = await listModels({ capability, operation });
  return isRecord4(result) && Array.isArray(result.models) ? result.models : [];
}
async function modelsForCapabilities(capabilities, operation) {
  const byId = /* @__PURE__ */ new Map();
  for (const capability of Array.from(new Set(capabilities))) {
    for (const model of await modelsForCapability(capability, operation)) {
      if (!isRecord4(model)) continue;
      const key = String(idField23(model.id) ?? idField23(model.ID) ?? modelPublicId(model) ?? byId.size);
      if (!byId.has(key)) byId.set(key, model);
    }
  }
  return Array.from(byId.values());
}
async function submitGenerationJob(args, selection, built, featureKey, contentUnitCandidate) {
  const prepared = prepareGenerationParams(built, selection.model, parameterModeArg(args));
  const modelParams = submittedModelParams(prepared);
  const projectScope = await resolveGenerationProjectScope(args, { required: contentUnitCandidate !== void 0 });
  const body = {
    model_id: selection.modelId,
    job_type: built.jobType,
    generation_intent: built.generationIntent,
    feature_key: featureKey,
    prompt: built.prompt,
    input_resource_ids: built.refIds,
    extra_params: JSON.stringify(prepared.extraParams)
  };
  if (projectScope.projectUid) body.project_uid = projectScope.projectUid;
  if (projectScope.projectTitle) body.project_title = projectScope.projectTitle;
  if (projectScope.projectDir) body.project_dir = projectScope.projectDir;
  if (prepared.aspectRatio !== void 0) body.aspect_ratio = prepared.aspectRatio;
  if (prepared.duration !== void 0) body.duration = prepared.duration;
  const title = getOptionalString(args, "title");
  if (title) body.title = title;
  if (contentUnitCandidate) {
    body.content_unit_candidate = {
      ...contentUnitCandidate,
      ...projectScope.projectUid ? { project_uid: projectScope.projectUid } : {}
    };
  }
  const projectContext = projectScope.projectDir || projectScope.projectUid || projectScope.projectTitle ? {
    ...projectScope.projectDir ? { dir: projectScope.projectDir } : {},
    ...projectScope.projectUid ? { uid: projectScope.projectUid } : {},
    ...projectScope.projectTitle ? { title: projectScope.projectTitle } : {}
  } : void 0;
  body.request_context = JSON.stringify({
    ...projectContext ? { project: projectContext } : {},
    ...contentUnitCandidate ? { content_unit_candidate: contentUnitCandidate } : {}
  });
  const job = await backendPost2("/jobs", body);
  if (!isRecord4(job)) throw new Error("Generation job create returned an invalid response");
  return { job: normalizeJob(job), paramAudit: prepared.audit, modelParams };
}
async function getGenerationJob(jobId) {
  const job = await backendGet2(`/jobs/${jobId}`);
  if (!isRecord4(job)) throw new Error("Generation job get returned an invalid response");
  return normalizeJob(job);
}
function submittedModelParams(prepared) {
  return {
    ...prepared.extraParams,
    ...prepared.aspectRatio !== void 0 ? { aspect_ratio: prepared.aspectRatio } : {},
    ...prepared.duration !== void 0 ? { duration: prepared.duration } : {}
  };
}
async function resolveGenerationProjectScope(args, options = {}) {
  const rawDir = getOptionalString(args, "projectDir") ?? getOptionalString(args, "project_dir") ?? getOptionalString(args, "projectPath") ?? getOptionalString(args, "project_path") ?? getOptionalString(args, "cwd");
  const explicitProjectUid = getOptionalString(args, "projectUid") ?? getOptionalString(args, "project_uid");
  const explicitProjectTitle = getOptionalString(args, "projectTitle") ?? getOptionalString(args, "project_title");
  if (!rawDir) {
    if (options.required) throw new Error("projectDir or cwd is required for project-scoped generation tools");
    return {
      ...explicitProjectUid ? { projectUid: explicitProjectUid } : {},
      ...explicitProjectTitle ? { projectTitle: explicitProjectTitle } : {}
    };
  }
  const projectDir = resolve52(rawDir);
  if (options.required) {
    const binding = await requireMCPBackendBoundProject({ projectDir, ...explicitProjectUid ? { projectUid: explicitProjectUid } : {} });
    return {
      projectDir,
      projectUid: binding.projectUid,
      projectTitle: explicitProjectTitle ?? binding.projectTitle
    };
  }
  const locator = await resolveMCPProjectBindingLocator({
    projectDir,
    ...explicitProjectUid ? { projectUid: explicitProjectUid } : {}
  }).catch(() => void 0);
  return {
    projectDir: locator?.projectDir ?? projectDir,
    projectUid: locator?.projectUid ?? explicitProjectUid,
    projectTitle: explicitProjectTitle ?? locator?.projectTitle
  };
}
function generationSubmitResult(kind, job, monitorTool, paramAudit = []) {
  const jobId = idField23(job.id) ?? idField23(job.ID);
  if (jobId === void 0) throw new Error("Generation job create did not return a valid job id");
  const kindLabel = generationKindLabel(kind);
  return {
    status: "submitted",
    terminal: false,
    jobId,
    job_id: jobId,
    monitor: { tool: monitorTool, args: { jobId } },
    message: `${kindLabel} generation job submitted (Job #${jobId})`,
    job,
    ...paramAudit.length > 0 ? { param_audit: paramAudit, paramAudit } : {}
  };
}
function generationJobGetResult(kind, job, verbosity = "debug") {
  const jobId = idField23(job.id) ?? idField23(job.ID);
  if (jobId === void 0) throw new Error("Generation job response does not include a valid job id");
  const status = stringField34(job.status) ?? "unknown";
  const outputResourceIds = outputResourceIdsFromJob(job);
  const result = {
    status,
    terminal: isTerminalStatus(status),
    jobId,
    job_id: jobId,
    provider_status: stringField34(job.provider_status ?? job.providerStatus ?? job.external_status ?? job.externalStatus),
    outputResourceIds,
    output_resource_ids: outputResourceIds,
    ...outputResourceIds[0] ? { output_resource_id: outputResourceIds[0], outputResourceId: outputResourceIds[0] } : {},
    next_run_at: stringField34(job.next_run_at ?? job.nextRunAt),
    error_summary: errorSummary(job),
    message: `${generationKindLabel(kind)} generation job #${jobId} status: ${status}`
  };
  if (verbosity !== "summary") result.job = job;
  return result;
}
function withGenerationJobSurface(args, result, contentUnitId) {
  const jobId = idField23(result.job_id ?? result.jobId);
  if (jobId === void 0) return result;
  return {
    ...result,
    surface: createGenerationJobSurface(args, {
      jobId,
      ...contentUnitId !== void 0 ? { contentUnitId } : {},
      projectId: projectIdFromArgs(args)
    })
  };
}
function generationKindLabel(kind) {
  if (kind === "image") return "Image";
  if (kind === "video") return "Video";
  return "Audio";
}
function normalizeJob(job) {
  const outputResourceIds = outputResourceIdsFromJob(job);
  return {
    ...job,
    ...outputResourceIds.length > 0 ? { outputResourceIds, output_resource_ids: outputResourceIds } : {}
  };
}
function outputResourceIdsFromJob(job) {
  const ids = [];
  appendId(ids, job.output_resource_id);
  appendId(ids, job.outputResourceId);
  appendId(ids, isRecord4(job.output_resource) ? job.output_resource.id ?? job.output_resource.ID : void 0);
  appendId(ids, isRecord4(job.outputResource) ? job.outputResource.id ?? job.outputResource.ID : void 0);
  appendIds(ids, job.output_resource_ids);
  appendIds(ids, job.outputResourceIds);
  return Array.from(new Set(ids));
}
function requiredGenerationCapability(args) {
  const capability = optionalGenerationCapability(args);
  if (!capability) throw new Error("capability is required");
  return capability;
}
function optionalGenerationCapability(args) {
  const raw = getOptionalString(args, "capability") ?? getOptionalString(args, "job_type") ?? getOptionalString(args, "jobType");
  if (!raw) return void 0;
  const normalized = normalizeGenerationCapability(raw);
  if (!normalized) throw new Error(`unsupported generation capability: ${raw}`);
  return normalized;
}
function normalizeGenerationCapability(value) {
  const raw = value.trim();
  const aliases = {
    image_generation: "image_generation",
    video_generation: "video_generation",
    audio: "audio_tts",
    tts: "audio_tts",
    voiceover: "audio_tts",
    stt: "audio_transcribe",
    transcription: "audio_transcribe",
    audio_translation: "audio_translate",
    voice_chat: "audio_chat",
    speech_chat: "audio_chat",
    omni: "audio_chat",
    music: "audio_music",
    sfx: "audio_sfx",
    sound_effect: "audio_sfx",
    sound_effects: "audio_sfx",
    voice_cloning: "voice_clone",
    voice_profile: "voice_design"
  };
  const normalized = aliases[raw] ?? raw;
  return generationCapabilities.includes(normalized) ? normalized : void 0;
}
function generationScope(args) {
  const raw = getOptionalString(args, "scope");
  if (raw === "content_unit" || raw === "asset" || raw === "storyboard" || raw === "keyframe") return raw;
  return "free";
}
function outputKindArg(args, capability) {
  const raw = getOptionalString(args, "outputKind") ?? getOptionalString(args, "output_kind");
  if (raw === "image" || raw === "video" || raw === "audio" || raw === "subtitle" || raw === "voice_profile" || raw === "json") return raw;
  return generationCapabilityOutputKind(capability);
}
function generationCapabilityOutputKind(capability) {
  if (isImageGenerationCapability(capability)) return "image";
  if (isVideoGenerationCapability(capability)) return "video";
  if (capability === "audio_transcribe" || capability === "audio_translate" || capability === "subtitle_align" || capability === "subtitle_translate") return "subtitle";
  if (capability === "voice_clone" || capability === "voice_design") return "voice_profile";
  return "audio";
}
function isImageGenerationCapability(capability) {
  return capability === "image_generation" || capability === "image" || capability === "image_edit";
}
function isVideoGenerationCapability(capability) {
  return capability === "video_generation" || capability === "video" || capability === "video_i2v" || capability === "video_v2v";
}
function isAudioGenerationCapability(capability) {
  return capability === "audio_generation";
}
function generationOutputJobGroup(outputKind) {
  if (outputKind === "image") return "image";
  if (outputKind === "video") return "video";
  return "audio";
}
function generationV2Result(rawResult, capability, scope, outputKind, generationMode, candidatePolicy) {
  if (!isRecord4(rawResult)) {
    return {
      status: "unknown",
      capability,
      scope,
      output_kind: outputKind,
      outputKind,
      generation_mode: generationMode,
      candidate_policy: candidatePolicy,
      result: rawResult
    };
  }
  const jobId = idField23(rawResult.job_id ?? rawResult.jobId);
  const monitorArgs = {
    ...isRecord4(rawResult.monitor) && isRecord4(rawResult.monitor.args) ? rawResult.monitor.args : {},
    ...jobId !== void 0 ? { jobId, job_id: jobId } : {},
    capability,
    scope,
    outputKind,
    output_kind: outputKind
  };
  return {
    ...rawResult,
    capability,
    scope,
    output_kind: outputKind,
    outputKind,
    generation_mode: stringField34(rawResult.generation_mode) ?? generationMode,
    candidate_policy: stringField34(rawResult.candidate_policy) ?? candidatePolicy,
    monitor: jobId !== void 0 ? {
      tool: "generation_job_get",
      args: monitorArgs
    } : rawResult.monitor
  };
}
function assertNeverGenerationCapability(value) {
  throw new Error(`unsupported generation capability: ${String(value)}`);
}
function promptArg(args) {
  const prompt = getOptionalString(args, "prompt");
  if (!prompt) throw new Error("prompt is required");
  return prompt;
}
function promptAndResourceIds(args) {
  const rawPrompt = promptArg(args);
  return {
    prompt: stripResourceMentions(rawPrompt),
    refIds: positiveIntegerIds2([
      ...resourceIdsFromMentions(rawPrompt),
      ...resourceIds(args.input_resource_ids) ?? [],
      ...resourceIds(args.reference_resource_ids) ?? []
    ])
  };
}
function requiredContentUnitId(args) {
  const value = args.contentUnitId ?? args.content_unit_id;
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim()) return value.trim();
  throw new Error("contentUnitId is required");
}
async function compiledContentUnitPrompt(args, contentUnitId) {
  const result = await domainBuildContentUnitBackendPrompt({ ...args, contentUnitId });
  if (!isRecord4(result)) throw new Error("content unit prompt compiler returned an invalid response");
  const prompt = isRecord4(result.prompt) ? result.prompt : void 0;
  if (!prompt) throw new Error("content unit prompt compiler did not return prompt data");
  return {
    ok: result.ok,
    prompt,
    ...Array.isArray(result.blockers) ? { blockers: result.blockers } : {}
  };
}
function resourceIds(value) {
  if (typeof value === "string") {
    const ids = value.split(",").map((item) => Number(item.trim()));
    return positiveIntegerIds2(ids);
  }
  const values = numericValues(value);
  return values ? positiveIntegerIds2(values) : void 0;
}
function positiveIntegerIds2(values) {
  return Array.from(new Set(values.filter((id) => Number.isFinite(id) && id > 0).map((id) => Math.floor(id))));
}
function extraParamsArg(value) {
  if (isRecord4(value)) return { ...value };
  if (typeof value === "string" && value.trim()) {
    const parsed = JSON.parse(value);
    if (!isRecord4(parsed)) throw new Error("extra_params must be an object");
    return { ...parsed };
  }
  return {};
}
function assignStringParam(args, params, explicitParamKeys, key) {
  const value = getOptionalString(args, key);
  if (!value) return;
  params[key] = value;
  explicitParamKeys.add(key);
}
function assignDefaultParam(params, defaultParamKeys, key, value) {
  if (params[key] !== void 0 && params[key] !== null && params[key] !== "") return;
  params[key] = value;
  defaultParamKeys.add(key);
}
function parameterModeArg(args) {
  const raw = getOptionalString(args, "parameter_mode") ?? getOptionalString(args, "param_mode");
  return raw === "strict" ? "strict" : "compatible";
}
function verbosityArg(args) {
  const raw = getOptionalString(args, "verbosity");
  return raw === "summary" ? "summary" : "debug";
}
function semanticRefReplacements(prompt) {
  const replacements = prompt.replacements ?? prompt.semantic_ref_replacements ?? prompt.ref_replacements;
  return Array.isArray(replacements) ? replacements : [];
}
function errorSummary(job) {
  const direct = stringField34(job.error_summary ?? job.errorSummary ?? job.error ?? job.error_message ?? job.errorMessage);
  if (direct) return direct;
  const error = isRecord4(job.error) ? job.error : void 0;
  return stringField34(error?.message);
}
function prepareGenerationParams(built, model, mode) {
  const supported = supportedParamMap(model);
  const hasContract = supported !== void 0;
  const output = {};
  const audit = [];
  const pending = { ...built.params };
  if (hasContract && mode === "compatible" && !supported.has("aspect_ratio") && supported.has("image_size") && pending.aspect_ratio !== void 0 && (pending.image_size === void 0 || built.defaultParamKeys.has("image_size"))) {
    const mapped = aspectRatioToImageSize(String(pending.aspect_ratio), supported.get("image_size"));
    if (mapped) {
      pending.image_size = mapped;
      built.defaultParamKeys.delete("image_size");
      audit.push({
        key: "aspect_ratio",
        value: pending.aspect_ratio,
        reason: "mapped_unsupported_aspect_ratio_to_image_size",
        source: paramSource(built, "aspect_ratio"),
        mapped_to: "image_size",
        mapped_value: mapped
      });
    }
  }
  for (const [key, value] of Object.entries(pending)) {
    const source = paramSource(built, key);
    if (value === void 0 || value === null || value === "") continue;
    const paramDef = hasContract ? supported.get(key) : void 0;
    if (hasContract && !paramDef) {
      if (mode === "strict" && source === "explicit") {
        throw unsupportedMCPParamError(key, model, supported);
      }
      audit.push({ key, value, reason: "dropped_unsupported_parameter", source });
      continue;
    }
    const normalized = normalizeParamValue(value, paramDef);
    const checked = hasContract && paramDef ? compatibleParamValue(key, normalized, paramDef, source, mode, model, supported, audit) : normalized;
    if (checked === void 0) continue;
    output[key] = checked;
  }
  const { aspect_ratio, duration, ...extraParams } = output;
  return {
    aspectRatio: typeof aspect_ratio === "string" ? aspect_ratio : void 0,
    duration: numericDuration(duration),
    extraParams,
    audit
  };
}
function compatibleParamValue(key, value, paramDef, source, mode, model, supported, audit) {
  const options = Array.isArray(paramDef.options) ? paramDef.options.filter((item) => typeof item === "string") : [];
  if (options.length > 0 && typeof value === "string" && !options.includes(value)) {
    if (mode === "strict" && source === "explicit") {
      throw invalidMCPParamOptionError(key, value, model, options);
    }
    const fallback = typeof paramDef.default === "string" && options.includes(paramDef.default) ? paramDef.default : options[0];
    audit.push({ key, value, reason: "replaced_invalid_option", source, mapped_value: fallback });
    return fallback;
  }
  const type = typeof paramDef.type === "string" ? paramDef.type : "";
  if (type === "number" && typeof value === "number") {
    const min = typeof paramDef.min === "number" ? paramDef.min : void 0;
    const max = typeof paramDef.max === "number" ? paramDef.max : void 0;
    const clamped = Math.min(max ?? value, Math.max(min ?? value, value));
    if (clamped !== value) {
      if (mode === "strict" && source === "explicit") {
        throw new Error(`parameter "${key}" is outside the supported range for model "${modelDisplay(model)}"`);
      }
      audit.push({ key, value, reason: "clamped_numeric_range", source, mapped_value: clamped });
    }
    return clamped;
  }
  return value;
}
function supportedParamMap(model) {
  if (!model) return void 0;
  const params = Array.isArray(model.supported_params) ? model.supported_params : void 0;
  if (params) {
    const out = /* @__PURE__ */ new Map();
    for (const item of params) {
      if (!isRecord4(item)) continue;
      const key = stringField34(item.key);
      if (key) out.set(key, item);
    }
    return out;
  }
  if (Array.isArray(model.supported_param_keys)) {
    return new Map(model.supported_param_keys.flatMap((item) => {
      const key = stringField34(item);
      return key ? [[key, { key }]] : [];
    }));
  }
  const schema = isRecord4(model.params_schema) ? model.params_schema : void 0;
  const properties = isRecord4(schema?.properties) ? schema.properties : void 0;
  if (properties) {
    return new Map(Object.keys(properties).map((key) => [key, { key, ...isRecord4(properties[key]) ? properties[key] : {} }]));
  }
  return void 0;
}
function aspectRatioToImageSize(aspectRatio, imageSizeParam) {
  const options = Array.isArray(imageSizeParam?.options) ? imageSizeParam.options.filter((item) => typeof item === "string") : [];
  const byRatio = {
    "1:1": ["2048x2048", "1024x1024", "4096x4096"],
    "4:3": ["2304x1728"],
    "3:4": ["1728x2304"],
    "16:9": ["2848x1600", "1792x1024", "1536x1024", "1280x720"],
    "9:16": ["1600x2848", "1024x1792", "1024x1536", "720x1280"]
  };
  for (const candidate of byRatio[aspectRatio] ?? []) {
    if (options.includes(candidate)) return candidate;
  }
  return void 0;
}
function normalizeParamValue(value, paramDef) {
  if (!paramDef) return value;
  if (paramDef.type === "number" && typeof value === "string" && value.trim()) {
    const number = Number(value);
    return Number.isFinite(number) ? number : value;
  }
  if (paramDef.type === "boolean" && typeof value === "string") {
    if (value === "true") return true;
    if (value === "false") return false;
  }
  return value;
}
function numericDuration(value) {
  if (value === void 0 || value === null || value === "") return void 0;
  const number = typeof value === "number" ? value : Number(value);
  return Number.isFinite(number) ? number : void 0;
}
function paramSource(built, key) {
  if (built.explicitParamKeys.has(key)) return "explicit";
  if (built.defaultParamKeys.has(key)) return "default";
  return "compatible";
}
function unsupportedMCPParamError(key, model, supported) {
  const allowed = Array.from(supported.keys()).sort();
  const err = new Error(`parameter "${key}" is not supported by model "${modelDisplay(model)}"; supported parameters: ${allowed.join(", ") || "(none)"}`);
  Object.assign(err, {
    code: "UNSUPPORTED_PARAMETER",
    field: key,
    supported_params: allowed
  });
  return err;
}
function invalidMCPParamOptionError(key, value, model, options) {
  const err = new Error(`parameter "${key}" value ${JSON.stringify(value)} is not supported by model "${modelDisplay(model)}"; allowed values: ${options.join(", ")}`);
  Object.assign(err, {
    code: "INVALID_PARAMETER_OPTION",
    field: key,
    allowed_values: options
  });
  return err;
}
function normalizedJobId(args) {
  const jobId = getOptionalNumeric(args, "jobId") ?? getOptionalNumeric(args, "job_id");
  if (jobId === void 0 || !Number.isInteger(jobId) || jobId <= 0) throw new Error("jobId must be a positive integer");
  return jobId;
}
function normalizedJobIds(args) {
  const rawIds = Array.isArray(args.jobIds) ? args.jobIds : Array.isArray(args.job_ids) ? args.job_ids : void 0;
  const ids = rawIds ? rawIds.map((value) => idField23(value)).filter((value) => value !== void 0) : Array.isArray(args.items) ? args.items.map((item) => isRecord4(item) ? normalizedJobId(item) : void 0).filter((value) => value !== void 0) : [];
  const unique = Array.from(new Set(ids));
  if (unique.length === 0) throw new Error("jobIds must contain at least one positive integer");
  return unique;
}
function modelPublicId(value) {
  if (!isRecord4(value)) return void 0;
  return stringField34(value.model_id) ?? stringField34(value.logical_model_id) ?? stringField34(value.model_def_id);
}
function modelMatchesPublicId(model, publicId) {
  const ids = [
    modelPublicId(model),
    stringField34(model.model_id),
    stringField34(model.logical_model_id),
    stringField34(model.model_def_id),
    idField23(model.id) !== void 0 ? `backend.model.${idField23(model.id)}` : void 0,
    idField23(model.ID) !== void 0 ? `backend.model.${idField23(model.ID)}` : void 0
  ];
  return ids.includes(publicId);
}
function modelDisplay(model) {
  return stringField34(model?.display_name) ?? stringField34(model?.short_name) ?? (model ? modelPublicId(model) : void 0) ?? "selected model";
}
function stringField34(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function idField23(value) {
  const number = typeof value === "number" ? value : typeof value === "string" ? Number(value) : NaN;
  return Number.isInteger(number) && number > 0 ? number : void 0;
}
function appendId(ids, value) {
  const id = idField23(value);
  if (id !== void 0) ids.push(id);
}
function appendIds(ids, value) {
  if (!Array.isArray(value)) return;
  for (const item of value) appendId(ids, item);
}
function numericList2(value) {
  if (!Array.isArray(value)) return [];
  return value.map(idField23).filter((item) => item !== void 0);
}
function errorMessage42(error) {
  return error instanceof Error ? error.message : String(error);
}
function isTerminalStatus(status) {
  return TERMINAL_STATUSES.has(status.trim().toLowerCase());
}
function isSuccessfulStatus(status) {
  return SUCCESS_STATUSES.has(status.trim().toLowerCase());
}
var TERMINAL_STATUSES = /* @__PURE__ */ new Set(["succeeded", "succeed", "success", "completed", "complete", "done", "finished", "failed", "failure", "error", "cancelled", "canceled"]);
var SUCCESS_STATUSES = /* @__PURE__ */ new Set(["succeeded", "succeed", "success", "completed", "complete", "done", "finished"]);
function summarizeWorkspaceInterpretForAgent(value) {
  if (!isRecord4(value)) return { result: value };
  const review = isRecord4(value.review) ? value.review : {};
  const summary = isRecord4(review.summary) ? review.summary : void 0;
  const changedEntities = Array.isArray(review.changedEntities) ? review.changedEntities : [];
  const semanticChanges = Array.isArray(review.semanticChanges) ? review.semanticChanges : [];
  const issues = Array.isArray(review.issues) ? review.issues : [];
  return {
    schema: "movscript.workspace-interpret-refresh-agent-summary.v1",
    operation: value.operation,
    status: value.status,
    ...summary ? { summary } : {},
    changedEntities: changedEntities.map(summarizeChangedEntity).filter(Boolean),
    semanticChanges: semanticChanges.map(summarizeSemanticChange).filter(Boolean),
    issues: issues.map(summarizeIssue).filter(Boolean),
    fullResult: "available in result.data"
  };
}
function summarizeChangedEntity(value) {
  if (!isRecord4(value)) return void 0;
  const fieldChanges = Array.isArray(value.fieldChanges) ? value.fieldChanges : [];
  const fields = fieldChanges.map((item) => isRecord4(item) && typeof item.field === "string" ? item.field : void 0).filter((item) => !!item);
  return {
    entityKind: value.entityKind,
    ...value.id !== void 0 ? { id: value.id } : {},
    ...typeof value.clientId === "string" ? { clientId: value.clientId } : {},
    state: value.state,
    ...fields.length > 0 ? { fields } : {}
  };
}
function summarizeSemanticChange(value) {
  if (!isRecord4(value)) return void 0;
  return {
    ...isRecord4(value.entity) ? { entity: pick(value.entity, ["kind", "id"]) } : {},
    kind: value.kind,
    businessKind: value.businessKind,
    propagation: value.propagation,
    ...Array.isArray(value.fields) ? { fields: value.fields } : {}
  };
}
function summarizeIssue(value) {
  if (!isRecord4(value)) return void 0;
  return pick(value, ["severity", "message"]);
}
function pick(value, keys) {
  const result = {};
  for (const key of keys) {
    if (value[key] !== void 0) result[key] = value[key];
  }
  return result;
}
async function callTool(params) {
  const name = getStringParam(params, "name");
  const args = getObjectParam(params, "arguments");
  switch (name) {
    case "movscript_focus_get":
      return toolText(getFocus());
    case "system_model_list":
    case "generation_model_list":
    case "movscript_model_list":
      return toolText(await listModels(args));
    case "generation_capability_list":
      return toolText(await listGenerationCapabilities(args));
    case "generation_prepare":
      return toolText(await prepareGeneration(args));
    case "generation_submit":
      return toolText(await submitUnifiedGeneration(args));
    case "generation_job_get":
      return toolText(await getUnifiedGenerationJob(args));
    case "generation_job_get_batch":
      return toolText(await getUnifiedGenerationJobs(args));
    case "generation_result_register":
      return toolText(await registerGenerationResult(args));
    case "system_resource_library_query":
    case "movscript_resource_library_query":
      return toolText(await queryResourceLibrary(args));
    case "system_resource_library_open":
    case "movscript_resource_library_open":
      return toolText(openResourceLibrary(args));
    case "system_resource_image_read":
    case "movscript_resource_image_read":
      return await readResourceImageForVision(args);
    case "system_resource_image_transform_to_resource":
    case "movscript_resource_image_transform_to_resource":
      return toolText(await transformResourceImageToResource(args));
    case "system_resource_video_extract_frames":
    case "movscript_resource_video_extract_frames":
      return await extractResourceVideoFramesForVision(args);
    case "system_resource_video_probe":
    case "movscript_resource_video_probe":
      return toolText(await probeResourceVideo(args));
    case "system_resource_video_extract_frame_to_resource":
    case "movscript_resource_video_extract_frame_to_resource":
      return toolText(await extractResourceVideoFrameToResource(args));
    case "system_resource_video_extract_frames_to_resources":
    case "movscript_resource_video_extract_frames_to_resources":
      return toolText(await extractResourceVideoFramesToResources(args));
    case "system_resource_video_trim_to_resource":
    case "movscript_resource_video_trim_to_resource":
      return toolText(await trimResourceVideoToResource(args));
    case "system_resource_video_compose_to_resource":
    case "system_resource_video_concat_to_resource":
    case "movscript_resource_video_compose_to_resource":
    case "movscript_resource_video_concat_to_resource":
      return toolText(await composeResourceVideosToResource(args));
    case "system_resource_video_contact_sheet_to_resource":
    case "movscript_resource_video_contact_sheet_to_resource":
      return toolText(await createResourceVideoContactSheetToResource(args));
    case "system_resource_video_extract_audio_to_resource":
    case "movscript_resource_video_extract_audio_to_resource":
      return toolText(await extractResourceVideoAudioToResource(args));
    case "system_resource_image_annotate":
    case "movscript_resource_image_annotate":
      return await annotateResourceImage(args);
    case "system_resource_upload":
    case "movscript_resource_upload":
      return toolText(await uploadAgentImageResource(args));
    case "system_resource_upload_batch":
    case "movscript_resource_upload_batch":
      return toolText(await uploadAgentImageResources(args));
    case "system_shot_library_query":
    case "movscript_shot_library_query":
      return toolText(await queryShotLibrary(args));
    case "system_shot_group_create":
    case "movscript_shot_group_create":
      return toolText(await createShotGroup(args));
    case "system_shot_group_get":
    case "movscript_shot_group_get":
      return toolText(await getShotGroup(args));
    case "system_shot_group_add_shots":
    case "movscript_shot_group_add_shots":
      return toolText(await addShotsToGroup(args));
    case "system_video_shot_cuts_analyze":
    case "movscript_video_shot_cuts_analyze":
      return toolText(await analyzeVideoShotCuts(args));
    case "system_external_resource_source_list":
    case "movscript_external_resource_source_list":
      return toolText(await listExternalResourceSources(args));
    case "system_external_resource_search":
    case "movscript_external_resource_search":
      return toolText(await searchExternalResources(args));
    case "system_artifact_upload_export":
      return toolText(await artifactUploadExport(args));
    case "system_artifact_upload_hls_stream":
      return toolText(await artifactUploadHlsStream(args));
    case "system_artifact_get_stream":
      return toolText(await artifactGetStream(args));
    case "domain_get_model":
      return toolText(await domainGetModel(args));
    case "domain_query_entities":
      return toolText(await domainQueryEntities(args));
    case "domain_query_settings":
      return toolText(await domainQuerySettings(args));
    case "domain_query_assets":
      return toolText(await domainQueryAssets(args));
    case "domain_query_production_context":
      return toolText(await domainQueryProductionContext(args));
    case "domain_read_content_workspace":
      return toolText(await domainReadContentWorkspace(args));
    case "domain_read_content_workspace_snapshot":
      return toolText(await domainReadContentWorkspaceSnapshot(args));
    case "domain_read_project_context_snapshot":
      return toolText(await domainReadProjectContextSnapshot(args));
    case "domain_derive_content_unit_artifact":
      return toolText(await domainInterpretContentUnitArtifact(args));
    case "domain_build_content_unit_backend_prompt":
      return toolText(await domainBuildContentUnitBackendPrompt(args));
    case "domain_read_preview_timeline":
      return toolText(await domainReadPreviewTimeline(args));
    case "domain_read_production_timeline":
      return toolText(await domainReadProductionTimeline(args));
    case "domain_read_scene_moment_edit_plan":
      return toolText(await domainReadSceneMomentEditPlan(args));
    case "domain_read_production_edit_plan":
      return toolText(await domainReadProductionEditPlan(args));
    case "domain_create_editing_project_context":
      return toolText(await domainCreateEditingProjectContext(args));
    case "domain_read_scene_moment_timeline":
      return toolText(await domainReadSceneMomentTimeline(args));
    case "domain_read_content_unit_runtime_panel":
      return toolText(await domainReadContentUnitRuntimePanel(args));
    case "domain_read_content_unit_generation_prompt":
    case "domain_read_content_unit_input_version":
      return toolText(await domainReadContentUnitGenerationPrompt(args));
    case "domain_read_content_unit_dependency_report":
      return toolText(await domainReadContentUnitDependencyReport(args));
    case "domain_read_content_unit_selection_validity":
      return toolText(await domainReadContentUnitSelectionValidity(args));
    case "domain_upsert_project_standards":
      return toolText(await domainUpsertProjectStandards(args));
    case "domain_upsert_setting":
      return toolText(await domainUpsertSetting(args));
    case "domain_upsert_setting_state":
      return toolText(await domainUpsertSettingState(args));
    case "domain_upsert_setting_tree":
      return toolText(await domainUpsertSettingTree(args));
    case "domain_upsert_asset":
      return toolText(await domainUpsertAsset(args));
    case "domain_certify_asset_provider":
    case "domain_certify_asset_seedance2":
      return toolText(await domainCertifyAssetProvider(args));
    case "domain_query_remote_asset_groups":
      return toolText(await domainQueryRemoteAssetGroups(args));
    case "domain_query_remote_assets":
      return toolText(await domainQueryRemoteAssets(args));
    case "domain_upsert_script":
      return toolText(await domainUpsertScript(args));
    case "domain_read_script_source":
      return toolText(await domainReadScriptSource(args));
    case "domain_snapshot_script_version":
      return toolText(await domainSnapshotScriptVersion(args));
    case "domain_upsert_content_unit":
      return toolText(await domainUpsertContentUnit(args));
    case "domain_upsert_production":
      return toolText(await domainUpsertProduction(args));
    case "domain_upsert_production_tree":
      return toolText(await domainUpsertProductionTree(args));
    case "domain_upsert_timeline_namespace_tree":
      return toolText(await domainUpsertTimelineNamespaceTree(args));
    case "domain_upsert_segment":
      return toolText(await domainUpsertSegment(args));
    case "domain_upsert_scene_moment":
      return toolText(await domainUpsertSceneMoment(args));
    case "domain_upsert_keyframe":
      return toolText(await domainUpsertKeyframe(args));
    case "domain_upsert_storyboard":
      return toolText(await domainUpsertStoryboard(args));
    case "domain_upsert_audio_cue":
      return toolText(await domainUpsertAudioCue(args));
    case "domain_upsert_expression_unit":
      return toolText(await domainUpsertExpressionUnit(args));
    case "domain_update_content_unit_prompt":
      return toolText(await domainUpdateContentUnitPrompt(args));
    case "domain_update_entity_transition":
      return toolText(await domainUpdateEntityTransition(args));
    case "domain_update_storyboard_timeline":
      return toolText(await domainUpdateStoryboardTimeline(args));
    case "domain_append_candidate":
      return toolText(await domainAppendCandidate(args));
    case "domain_create_content_candidate":
      return toolText(await domainCreateContentCandidate(args));
    case "domain_register_raw_resource_as_content_unit_candidate":
      return toolText(await domainRegisterRawResourceAsContentUnitCandidate(args));
    case "domain_create_content_candidate_batch":
      return toolText(await domainCreateContentCandidateBatch(args));
    case "domain_create_asset_slot_candidate":
      return toolText(await domainCreateAssetSlotCandidate(args));
    case "domain_create_keyframe_candidate":
      return toolText(await domainCreateKeyframeCandidate(args));
    case "domain_select_content_unit_candidate":
      return toolText(await domainSelectContentUnitCandidate(args));
    case "domain_select_content_unit_candidate_batch":
      return toolText(await domainSelectContentUnitCandidateBatch(args));
    case "domain_decide_content_unit_candidate":
      return toolText(await domainDecideContentUnitCandidate(args));
    case "domain_select_candidate":
      return toolText(await domainSelectCandidate(args));
    case "domain_update_candidate":
      return toolText(await domainUpdateCandidate(args));
    case "domain_unlock_candidate":
      return toolText(await domainUnlockCandidate(args));
    case "domain_delete_entity":
      return toolText(await domainDeleteEntity(args));
    case "domain_overview":
      return toolText(await domainOverview(args));
    case "domain_read_production_work_plan":
      return toolText(await domainReadProductionWorkPlan(args));
    case "domain_production_status_summary":
      return toolText(await domainProductionStatusSummary(args));
    case "domain_inspect":
      return toolText(await domainInspect(args));
    case "domain_review":
      return toolText(await domainReview(args));
    case "domain_interpret":
      return interpretToolText(await domainInterpret(args));
    case "domain_regeneration_plan":
      return toolText(await domainRegenerationPlan(args));
    case "editing_project_create":
      return toolText(await editingProjectCreate(args));
    case "editing_project_create_from_edit_plan":
      return toolText(await editingProjectCreateFromEditPlan(args));
    case "editing_project_create_from_edit_decisions":
      return toolText(await editingProjectCreateFromEditDecisions(args));
    case "editing_video_compose":
      return toolText(await editingVideoCompose(args));
    case "editing_project_add_asset":
      return toolText(await editingProjectAddAsset(args));
    case "editing_project_remove_asset":
      return toolText(await editingProjectRemoveAsset(args));
    case "editing_project_get":
      return toolText(await editingProjectGet(args));
    case "editing_project_update_settings":
      return toolText(await editingProjectUpdateSettings(args));
    case "editing_project_save":
      return toolText(await editingProjectSave(args));
    case "editing_timeline_apply_commands":
      return toolText(await editingTimelineApplyCommands(args));
    case "editing_timeline_add_track":
      return toolText(await editingTimelineAddTrack(args));
    case "editing_timeline_remove_track":
      return toolText(await editingTimelineRemoveTrack(args));
    case "editing_timeline_add_clip":
      return toolText(await editingTimelineAddClip(args));
    case "editing_timeline_update_clip":
      return toolText(await editingTimelineUpdateClip(args));
    case "editing_timeline_split_clip":
      return toolText(await editingTimelineSplitClip(args));
    case "editing_timeline_move_clip":
      return toolText(await editingTimelineMoveClip(args));
    case "editing_timeline_delete_clip":
      return toolText(await editingTimelineDeleteClip(args));
    case "editing_timeline_validate":
      return toolText(await editingTimelineValidate(args));
    case "editing_runtime_capabilities_get":
      return toolText(await editingRuntimeCapabilitiesGet(args));
    case "editing_task_render_create":
      return toolText(await editingTaskRenderCreate(args));
    case "editing_task_hls_create":
      return toolText(await editingTaskHlsCreate(args));
    case "editing_task_transcode_create":
      return toolText(await editingTaskTranscodeCreate(args));
    case "editing_task_reframe_create":
      return toolText(await editingTaskReframeCreate(args));
    case "editing_task_get":
      return toolText(await editingTaskGet(args));
    case "editing_task_cancel":
      return toolText(await editingTaskCancel(args));
    case "editing_task_logs_get":
      return toolText(await editingTaskLogsGet(args));
    case "editing_export_import_resource":
      return toolText(await editingExportImportResource(args));
    case "editing_export_save_local":
      return toolText(await editingExportSaveLocal(args));
    case "editing_export_publish_hls":
      return toolText(await editingExportPublishHls(args));
    case "editing_export_create_candidate":
      return toolText(await editingExportCreateCandidate(args));
    case "system_project_create":
    case "movscript_project_create":
      return toolText(await createProject(args));
    case "system_project_init":
    case "movscript_project_init":
      return toolText(await initLocalProject(args));
    case "system_project_fetch":
    case "system_project_open":
    case "movscript_project_open":
    case "movscript_project_fetch":
      return toolText(await fetchLocalProject(args));
    default:
      throw new Error(`Unknown tool: ${name}`);
  }
}
function interpretToolText(value) {
  return toolText(value, summarizeWorkspaceInterpretForAgent(value));
}
function listTools() {
  const baseProjectTools = projectTools();
  const baseGenerationTools = generationTools();
  const baseResourceLibraryTools = resourceLibraryTools();
  const baseResourceMediaTools = resourceMediaTools();
  const baseShotLibraryTools = shotLibraryTools();
  const baseExternalResourceTools = externalResourceTools();
  return [
    ...systemTools({
      models: modelTools(),
      generation: baseGenerationTools,
      resourceLibrary: baseResourceLibraryTools,
      resourceMedia: baseResourceMediaTools,
      shotLibrary: baseShotLibraryTools,
      externalResources: baseExternalResourceTools,
      project: baseProjectTools
    }),
    ...domainTools(),
    ...editingTools(),
    ...artifactTools(),
    ...focusTools(),
    ...modelTools(),
    ...baseResourceLibraryTools,
    ...baseResourceMediaTools,
    ...baseShotLibraryTools,
    ...baseExternalResourceTools,
    ...baseGenerationTools,
    ...baseProjectTools
  ];
}
function systemTools(input) {
  return [
    ...renameTools(input.project, {
      movscript_project_create: "system_project_create",
      movscript_project_init: "system_project_init",
      movscript_project_open: "system_project_open",
      movscript_project_fetch: "system_project_fetch"
    }),
    ...renameTools(input.models, { generation_model_list: "system_model_list" }),
    ...renameTools(input.resourceLibrary, {
      movscript_resource_library_query: "system_resource_library_query",
      movscript_resource_library_open: "system_resource_library_open"
    }),
    ...renameTools(input.resourceMedia, {
      movscript_resource_image_read: "system_resource_image_read",
      movscript_resource_image_transform_to_resource: "system_resource_image_transform_to_resource",
      movscript_resource_video_extract_frames: "system_resource_video_extract_frames",
      movscript_resource_video_probe: "system_resource_video_probe",
      movscript_resource_video_extract_frame_to_resource: "system_resource_video_extract_frame_to_resource",
      movscript_resource_video_extract_frames_to_resources: "system_resource_video_extract_frames_to_resources",
      movscript_resource_video_trim_to_resource: "system_resource_video_trim_to_resource",
      movscript_resource_video_compose_to_resource: "system_resource_video_compose_to_resource",
      movscript_resource_video_concat_to_resource: "system_resource_video_concat_to_resource",
      movscript_resource_video_contact_sheet_to_resource: "system_resource_video_contact_sheet_to_resource",
      movscript_resource_video_extract_audio_to_resource: "system_resource_video_extract_audio_to_resource",
      movscript_resource_image_annotate: "system_resource_image_annotate",
      movscript_resource_upload: "system_resource_upload",
      movscript_resource_upload_batch: "system_resource_upload_batch"
    }),
    ...renameTools(input.shotLibrary, {
      movscript_shot_library_query: "system_shot_library_query",
      movscript_shot_group_create: "system_shot_group_create",
      movscript_shot_group_get: "system_shot_group_get",
      movscript_shot_group_add_shots: "system_shot_group_add_shots",
      movscript_video_shot_cuts_analyze: "system_video_shot_cuts_analyze"
    }),
    ...renameTools(input.externalResources, {
      movscript_external_resource_source_list: "system_external_resource_source_list",
      movscript_external_resource_search: "system_external_resource_search"
    })
  ];
}
function renameTools(tools, names) {
  return tools.filter((tool) => names[tool.name]).map((tool) => ({
    ...tool,
    name: names[tool.name],
    description: `${tool.description} New system-level alias for ${tool.name}.`
  }));
}
var MCP_DEBUG = process.env.MOVSCRIPT_MCP_DEBUG === "1";
async function handleJSONRPC(req, httpRequestId) {
  const startedAt = Date.now();
  const isNotification = !Object.prototype.hasOwnProperty.call(req, "id");
  const id = isNotification ? null : req.id ?? null;
  if (MCP_DEBUG) {
    console.info(`[mcp] rpc start httpRequestId=${httpRequestId ?? "n/a"} rpcId=${String(id)} method=${req.method ?? ""}`);
  }
  if (req.jsonrpc !== "2.0" || !req.method) {
    if (isNotification) return void 0;
    return makeError(id, -32600, "Invalid Request");
  }
  try {
    switch (req.method) {
      case "initialized":
      case "notifications/cancelled":
      case "notifications/progress":
        return void 0;
      case "initialize":
        return makeResult(id, {
          protocolVersion: "2025-06-18",
          serverInfo: { name: "movscript-core-mcp", version: "0.1.0" },
          capabilities: {
            resources: {},
            tools: {}
          }
        });
      case "resources/list":
        return makeResult(id, { resources: listResources() });
      case "resources/read":
        return makeResult(id, await readResource(getStringParam(req.params, "uri")));
      case "tools/list":
        return makeResult(id, { tools: listTools() });
      case "tools/call":
        return makeResult(id, await callTool(req.params));
      default:
        if (isNotification) return void 0;
        return makeError(id, -32601, `Method not found: ${req.method}`);
    }
  } catch (error) {
    console.error(`[mcp] rpc error httpRequestId=${httpRequestId ?? "n/a"} rpcId=${String(id)} method=${req.method} elapsedMs=${Date.now() - startedAt}`, error);
    if (isNotification) return void 0;
    return makeError(id, -32e3, error instanceof Error ? error.message : String(error), errorData(error));
  } finally {
    if (MCP_DEBUG) {
      console.info(`[mcp] rpc finish httpRequestId=${httpRequestId ?? "n/a"} rpcId=${String(id)} method=${req.method ?? ""} elapsedMs=${Date.now() - startedAt}`);
    }
  }
}
var MCP_DEBUG2 = process.env.MOVSCRIPT_MCP_DEBUG === "1";

// ../../packages/cli-commands/dist/index.js
import { resolve as resolve7 } from "path";
var LOCAL_NODE_GATEWAY_SERVICE = "movscript.local-node.gateway";
var DATA_SERVICE = "movscript.data.service";
var adminCommandSpecs = [
  {
    commandId: "admin.provider_template.list",
    mcpToolName: "admin_provider_template_list",
    cliPath: ["provider-template", "list"],
    description: "Admin-only: list provider templates that can be used to create provider accounts.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: (args) => adminPathWithQuery("/admin/provider-templates", args)
  },
  {
    commandId: "admin.provider.list",
    mcpToolName: "admin_provider_list",
    cliPath: ["provider", "list"],
    description: "Admin-only: list configured AI providers and their public configuration state.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: (args) => adminPathWithQuery("/admin/providers", args)
  },
  {
    commandId: "admin.provider.create",
    mcpToolName: "admin_provider_create",
    cliPath: ["provider", "create"],
    description: "Admin-only: create an AI provider from a backend provider payload.",
    method: "POST",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/providers"
  },
  {
    commandId: "admin.provider.credential.create",
    mcpToolName: "admin_provider_credential_create",
    cliPath: ["provider", "credential", "create"],
    description: "Admin-only: create credentials for an existing AI provider.",
    method: "POST",
    inputSchema: adminProviderCredentialSchema(true),
    path: (args) => `/admin/providers/${adminRequiredPathArg(args, ["providerID", "providerId", "provider_id"])}/credentials`
  },
  {
    commandId: "admin.provider.credential.update",
    mcpToolName: "admin_provider_credential_update",
    cliPath: ["provider", "credential", "update"],
    description: "Admin-only: update one credential key for an existing AI provider.",
    method: "PATCH",
    inputSchema: adminProviderCredentialSchema(true, true),
    path: (args) => `/admin/providers/${adminRequiredPathArg(args, ["providerID", "providerId", "provider_id"])}/credentials/${adminRequiredPathArg(args, ["credentialKey", "credential_key"])}`
  },
  {
    commandId: "admin.provider.credential.set_primary",
    mcpToolName: "admin_provider_credential_set_primary",
    cliPath: ["provider", "credential", "set-primary"],
    description: "Admin-only: mark a provider credential key as the primary credential.",
    method: "POST",
    inputSchema: adminProviderCredentialSchema(false, true),
    path: (args) => `/admin/providers/${adminRequiredPathArg(args, ["providerID", "providerId", "provider_id"])}/credentials/${adminRequiredPathArg(args, ["credentialKey", "credential_key"])}/primary`
  },
  {
    commandId: "admin.provider.asset_library.get",
    mcpToolName: "admin_provider_asset_library_get",
    cliPath: ["provider", "asset-library", "get"],
    description: "Admin-only: read provider asset-library settings used for provider-side asset references.",
    method: "GET",
    inputSchema: adminProviderSchema(),
    path: (args) => `/admin/providers/${adminRequiredPathArg(args, ["providerID", "providerId", "provider_id"])}/asset-library`
  },
  {
    commandId: "admin.provider.asset_library.update",
    mcpToolName: "admin_provider_asset_library_update",
    cliPath: ["provider", "asset-library", "update"],
    description: "Admin-only: update provider asset-library settings used for provider-side asset references.",
    method: "PUT",
    inputSchema: adminProviderSchema(true),
    path: (args) => `/admin/providers/${adminRequiredPathArg(args, ["providerID", "providerId", "provider_id"])}/asset-library`
  },
  {
    commandId: "admin.model.catalog_template.list",
    mcpToolName: "admin_model_catalog_template_list",
    cliPath: ["model", "catalog-template", "list"],
    description: "Admin-only: list model catalog templates; pass query.lab to filter by lab.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: (args) => adminPathWithQuery("/admin/model-catalog/templates", args)
  },
  {
    commandId: "admin.model.import.preview",
    mcpToolName: "admin_model_import_preview",
    cliPath: ["model", "import", "preview"],
    description: "Admin-only: preview importing provider models into catalog entries and route bindings.",
    method: "POST",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/model-imports/preview"
  },
  {
    commandId: "admin.model.import.apply",
    mcpToolName: "admin_model_import_apply",
    cliPath: ["model", "import", "apply"],
    description: "Admin-only: apply a model import into catalog entries and route bindings.",
    method: "POST",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/model-imports/apply"
  },
  {
    commandId: "admin.model.catalog.list",
    mcpToolName: "admin_model_catalog_list",
    cliPath: ["model", "catalog", "list"],
    description: "Admin-only: list model catalog entries with route binding summary.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: (args) => adminPathWithQuery("/admin/model-catalog", args)
  },
  {
    commandId: "admin.model.catalog.create",
    mcpToolName: "admin_model_catalog_create",
    cliPath: ["model", "catalog", "create"],
    description: "Admin-only: create a model catalog entry.",
    method: "POST",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/model-catalog"
  },
  {
    commandId: "admin.model.catalog.update",
    mcpToolName: "admin_model_catalog_update",
    cliPath: ["model", "catalog", "update"],
    description: "Admin-only: update a model catalog entry.",
    method: "PUT",
    inputSchema: adminCatalogEntrySchema(true),
    path: (args) => `/admin/model-catalog/${adminRequiredPathArg(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id", "id"])}`
  },
  {
    commandId: "admin.model.catalog.delete",
    mcpToolName: "admin_model_catalog_delete",
    cliPath: ["model", "catalog", "delete"],
    description: "Admin-only: delete a model catalog entry.",
    method: "DELETE",
    inputSchema: adminCatalogEntrySchema(false),
    path: (args) => `/admin/model-catalog/${adminRequiredPathArg(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id", "id"])}`
  },
  {
    commandId: "admin.model.route.diagnose",
    mcpToolName: "admin_model_route_diagnose",
    cliPath: ["model", "route", "diagnose"],
    description: "Admin-only: diagnose route/model/provider readiness for a model operation.",
    method: "POST",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/model-routes/diagnose"
  },
  {
    commandId: "admin.model.route_binding.create",
    mcpToolName: "admin_model_route_binding_create",
    cliPath: ["model", "route-binding", "create"],
    description: "Admin-only: create a route binding for a catalog entry.",
    method: "POST",
    inputSchema: adminRouteBindingSchema(true),
    path: (args) => `/admin/model-catalog/${adminRequiredPathArg(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"])}/route-bindings`
  },
  {
    commandId: "admin.model.route_binding.update",
    mcpToolName: "admin_model_route_binding_update",
    cliPath: ["model", "route-binding", "update"],
    description: "Admin-only: update a route binding for a catalog entry.",
    method: "PUT",
    inputSchema: adminRouteBindingSchema(true, true),
    path: (args) => `/admin/model-catalog/${adminRequiredPathArg(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"])}/route-bindings/${adminRequiredPathArg(args, ["bindingID", "bindingId", "binding_id"])}`
  },
  {
    commandId: "admin.model.route_binding.delete",
    mcpToolName: "admin_model_route_binding_delete",
    cliPath: ["model", "route-binding", "delete"],
    description: "Admin-only: delete a route binding from a catalog entry.",
    method: "DELETE",
    inputSchema: adminRouteBindingSchema(false, true),
    path: (args) => `/admin/model-catalog/${adminRequiredPathArg(args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"])}/route-bindings/${adminRequiredPathArg(args, ["bindingID", "bindingId", "binding_id"])}`
  },
  {
    commandId: "admin.generation_tools.settings.get",
    mcpToolName: "admin_generation_tools_settings_get",
    cliPath: ["generation-tools", "settings", "get"],
    description: "Admin-only: read generation tool server settings.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: () => "/admin/settings/generation-tools"
  },
  {
    commandId: "admin.generation_tools.settings.update",
    mcpToolName: "admin_generation_tools_settings_update",
    cliPath: ["generation-tools", "settings", "update"],
    description: "Admin-only: update generation tool server settings.",
    method: "PUT",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/settings/generation-tools"
  },
  {
    commandId: "admin.resource_access.settings.get",
    mcpToolName: "admin_resource_access_settings_get",
    cliPath: ["resource-access", "settings", "get"],
    description: "Admin-only: read resource public access profiles, including public tunnel settings.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: () => "/admin/settings/resource-access"
  },
  {
    commandId: "admin.resource_access.settings.update",
    mcpToolName: "admin_resource_access_settings_update",
    cliPath: ["resource-access", "settings", "update"],
    description: "Admin-only: update resource public access profiles, including public tunnel settings.",
    method: "PUT",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/settings/resource-access"
  },
  {
    commandId: "admin.public_tunnel.config.get",
    mcpToolName: "admin_public_tunnel_config_get",
    cliPath: ["public-tunnel", "config", "get"],
    description: "Admin-only alias: read public tunnel configuration from resource access profiles.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: () => "/admin/settings/resource-access"
  },
  {
    commandId: "admin.public_tunnel.config.update",
    mcpToolName: "admin_public_tunnel_config_update",
    cliPath: ["public-tunnel", "config", "update"],
    description: "Admin-only alias: update public tunnel configuration through resource access profiles.",
    method: "PUT",
    inputSchema: adminWriteSchema(),
    path: () => "/admin/settings/resource-access"
  },
  {
    commandId: "admin.model.gateway_key.list",
    mcpToolName: "admin_model_gateway_key_list",
    cliPath: ["model", "gateway-key", "list"],
    description: "Admin-only: list model gateway API keys.",
    method: "GET",
    inputSchema: adminReadSchema(),
    path: (args) => adminPathWithQuery("/model-gateway/api-keys", args)
  },
  {
    commandId: "admin.model.gateway_key.create",
    mcpToolName: "admin_model_gateway_key_create",
    cliPath: ["model", "gateway-key", "create"],
    description: "Admin-only: create a model gateway API key.",
    method: "POST",
    inputSchema: adminWriteSchema(),
    path: () => "/model-gateway/api-keys"
  },
  {
    commandId: "admin.model.gateway_key.update",
    mcpToolName: "admin_model_gateway_key_update",
    cliPath: ["model", "gateway-key", "update"],
    description: "Admin-only: update a model gateway API key.",
    method: "PATCH",
    inputSchema: adminGatewayKeySchema(true),
    path: (args) => `/model-gateway/api-keys/${adminRequiredPathArg(args, ["keyID", "keyId", "key_id", "id"])}`
  },
  {
    commandId: "admin.model.gateway_key.delete",
    mcpToolName: "admin_model_gateway_key_delete",
    cliPath: ["model", "gateway-key", "delete"],
    description: "Admin-only: delete a model gateway API key.",
    method: "DELETE",
    inputSchema: adminGatewayKeySchema(false),
    path: (args) => `/model-gateway/api-keys/${adminRequiredPathArg(args, ["keyID", "keyId", "key_id", "id"])}`
  }
];
var adminCommandByMCPToolName = new Map(adminCommandSpecs.map((spec) => [spec.mcpToolName, spec]));
var adminCommandById = new Map(adminCommandSpecs.map((spec) => [spec.commandId, spec]));
function isAdminMCPToolName(name) {
  return Boolean(name && adminCommandByMCPToolName.has(name));
}
async function runMovScriptAdminCommand(specOrName, args = {}) {
  const spec = typeof specOrName === "string" ? adminCommandByMCPToolName.get(specOrName) ?? adminCommandById.get(specOrName) : specOrName;
  if (!spec) throw new Error(`Unknown admin command: ${specOrName}`);
  const binding = bindAdminBackendRuntime(args);
  const path = spec.path(args);
  const data = await callAdminBackend(spec, path, args);
  return {
    schema: "movscript.command_result.v1",
    status: "ok",
    commandId: spec.commandId,
    mcpToolName: spec.mcpToolName,
    data,
    debug: {
      cli_argv: adminDebugCliArgv(spec, args),
      method: spec.method,
      path,
      cwd: process.cwd(),
      ...binding.backendEndpoint ? { runtime_endpoint: binding.backendEndpoint } : {}
    }
  };
}
function unwrapCommandDataWithDebug(execution) {
  if (isRecord27(execution.data) && !Array.isArray(execution.data)) {
    return {
      ...execution.data,
      debug: execution.debug
    };
  }
  return {
    data: execution.data,
    debug: execution.debug
  };
}
function bindAdminBackendRuntime(args) {
  const workspaceDir = stringValue10(args.workspaceDir ?? args.workspace_dir) ?? stringValue10(args.projectDir ?? args.project_dir) ?? stringValue10(args.cwd) ?? process.env.MOVSCRIPT_WORKSPACE_DIR;
  if (workspaceDir) setMovScriptBackendDefaultWorkspaceDir(resolve7(workspaceDir));
  const token = stringValue10(args.token);
  if (token) setMovScriptBackendRuntimeAuthToken(token);
  const explicitBackend = stringValue10(args.backendBaseURL ?? args.backend_base_url ?? args.server);
  if (explicitBackend) {
    setMovScriptBackendAPIBaseURL(explicitBackend);
    return { backendEndpoint: explicitBackend };
  }
  const homeDir = resolveRuntimeHomeArg(args);
  const runtimeHome = readRuntimeHomeSnapshot(homeDir);
  const gatewayEndpoint = endpointURL4(
    findRuntimeEndpoint(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE) ?? findRuntimeService(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE)?.endpoint
  );
  const dataEndpoint = endpointURL4(
    findRuntimeEndpoint(runtimeHome, DATA_SERVICE) ?? findRuntimeService(runtimeHome, DATA_SERVICE)?.endpoint
  );
  const backendEndpoint = gatewayEndpoint ?? dataEndpoint;
  if (backendEndpoint) setMovScriptBackendAPIBaseURL(backendEndpoint);
  return { backendEndpoint };
}
async function callAdminBackend(spec, path, args) {
  switch (spec.method) {
    case "GET":
      return await backendGet2(path);
    case "POST":
      return await backendPost2(path, adminPayload(args));
    case "PUT":
      return await backendPut(path, adminPayload(args));
    case "PATCH":
      return await backendPatch(path, adminPayload(args));
    case "DELETE": {
      const result = await backendDelete(path);
      return result === null ? { status: "deleted" } : result;
    }
  }
}
function adminPathWithQuery(path, args) {
  const query = isRecord27(args.query) ? args.query : {};
  const params = new URLSearchParams();
  for (const [key, value] of Object.entries(query)) {
    if (typeof value === "string" && value.trim()) params.set(key, value.trim());
    else if (typeof value === "number" && Number.isFinite(value)) params.set(key, String(value));
    else if (typeof value === "boolean") params.set(key, String(value));
  }
  const suffix = params.toString();
  return suffix ? `${path}?${suffix}` : path;
}
function adminPayload(args) {
  const payload = args.payload ?? args.body;
  if (payload === void 0) return {};
  if (!isRecord27(payload)) throw new Error("admin command payload must be an object");
  return payload;
}
function adminRequiredPathArg(args, keys) {
  for (const key of keys) {
    const value = stringValue10(args[key]);
    if (value) return encodeURIComponent(value);
  }
  throw new Error(`admin command requires one of: ${keys.join(", ")}`);
}
function adminDebugCliArgv(spec, args) {
  const argv = ["movscript", "admin", ...spec.cliPath, "--json"];
  const homeDir = stringValue10(args.homeDir ?? args.home_dir ?? args.movscriptHome ?? args.movscript_home);
  if (homeDir) argv.push("--home-dir", homeDir);
  const workspaceDir = stringValue10(args.workspaceDir ?? args.workspace_dir);
  if (workspaceDir) argv.push("--workspace", workspaceDir);
  const projectDir = stringValue10(args.projectDir ?? args.project_dir);
  if (projectDir) argv.push("--project-dir", projectDir);
  const server = stringValue10(args.backendBaseURL ?? args.backend_base_url ?? args.server);
  if (server) argv.push("--server", server);
  if (args.token !== void 0) argv.push("--token", "<redacted>");
  appendPathArg(argv, args, ["providerID", "providerId", "provider_id"], "--provider-id");
  appendPathArg(argv, args, ["credentialKey", "credential_key"], "--credential-key");
  appendPathArg(argv, args, ["catalogEntryID", "catalogEntryId", "catalog_entry_id"], "--catalog-entry-id");
  appendPathArg(argv, args, ["bindingID", "bindingId", "binding_id"], "--binding-id");
  appendPathArg(argv, args, ["keyID", "keyId", "key_id"], "--key-id");
  appendPathArg(argv, args, ["id"], "--id");
  if (isRecord27(args.query)) {
    for (const [key, value] of Object.entries(args.query)) {
      argv.push("--query", `${key}=${String(value)}`);
    }
  }
  if (args.payload !== void 0 || args.body !== void 0) argv.push("--payload", "<json>");
  return argv;
}
function appendPathArg(argv, args, keys, flag) {
  for (const key of keys) {
    const value = stringValue10(args[key]);
    if (value) {
      argv.push(flag, value);
      return;
    }
  }
}
function resolveRuntimeHomeArg(args) {
  const homeDir = stringValue10(args.homeDir ?? args.home_dir ?? args.movscriptHome ?? args.movscript_home);
  return homeDir ? resolve7(homeDir) : resolveMovScriptHomeDir();
}
function endpointURL4(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.protocol && endpoint.protocol !== "http" && endpoint.protocol !== "https") return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}
function adminReadSchema(extra = {}) {
  return objectSchema3({
    ...adminRuntimeProperties(),
    query: {
      type: "object",
      additionalProperties: true,
      description: "Optional query string parameters for this fixed admin endpoint."
    },
    ...extra
  });
}
function adminWriteSchema(extra = {}) {
  return objectSchema3({
    ...adminRuntimeProperties(),
    payload: {
      type: "object",
      additionalProperties: true,
      description: "Backend request body for this fixed admin endpoint."
    },
    body: {
      type: "object",
      additionalProperties: true,
      description: "Alias for payload."
    },
    ...extra
  });
}
function adminProviderSchema(includePayload = false) {
  const extra = {
    providerID: { type: "string", description: "Provider id." },
    providerId: { type: "string", description: "Alias for providerID." },
    provider_id: { type: "string", description: "Alias for providerID." }
  };
  return includePayload ? adminWriteSchema(extra) : adminReadSchema(extra);
}
function adminProviderCredentialSchema(includePayload = false, includeCredentialKey = false) {
  return (includePayload ? adminWriteSchema : adminReadSchema)({
    providerID: { type: "string", description: "Provider id." },
    providerId: { type: "string", description: "Alias for providerID." },
    provider_id: { type: "string", description: "Alias for providerID." },
    ...includeCredentialKey ? {
      credentialKey: { type: "string", description: "Provider credential key." },
      credential_key: { type: "string", description: "Alias for credentialKey." }
    } : {}
  });
}
function adminCatalogEntrySchema(includePayload = false) {
  return (includePayload ? adminWriteSchema : adminReadSchema)({
    catalogEntryID: { type: ["string", "number"], description: "Model catalog entry id." },
    catalogEntryId: { type: ["string", "number"], description: "Alias for catalogEntryID." },
    catalog_entry_id: { type: ["string", "number"], description: "Alias for catalogEntryID." },
    id: { type: ["string", "number"], description: "Alias for catalogEntryID." }
  });
}
function adminRouteBindingSchema(includePayload = false, includeBinding = false) {
  return (includePayload ? adminWriteSchema : adminReadSchema)({
    catalogEntryID: { type: ["string", "number"], description: "Model catalog entry id." },
    catalogEntryId: { type: ["string", "number"], description: "Alias for catalogEntryID." },
    catalog_entry_id: { type: ["string", "number"], description: "Alias for catalogEntryID." },
    ...includeBinding ? {
      bindingID: { type: ["string", "number"], description: "Route binding id." },
      bindingId: { type: ["string", "number"], description: "Alias for bindingID." },
      binding_id: { type: ["string", "number"], description: "Alias for bindingID." }
    } : {}
  });
}
function adminGatewayKeySchema(includePayload = false) {
  return (includePayload ? adminWriteSchema : adminReadSchema)({
    keyID: { type: ["string", "number"], description: "Model gateway API key id." },
    keyId: { type: ["string", "number"], description: "Alias for keyID." },
    key_id: { type: ["string", "number"], description: "Alias for keyID." },
    id: { type: ["string", "number"], description: "Alias for keyID." }
  });
}
function adminRuntimeProperties() {
  return {
    homeDir: { type: "string", description: "Optional MovScript Home directory used to discover the daemon gateway." },
    home_dir: { type: "string", description: "Alias for homeDir." },
    movscriptHome: { type: "string", description: "Alias for homeDir." },
    movscript_home: { type: "string", description: "Alias for homeDir." },
    workspaceDir: { type: "string", description: "Optional workspace directory used for backend auth lookup." },
    workspace_dir: { type: "string", description: "Alias for workspaceDir." },
    projectDir: { type: "string", description: "Optional project directory used for backend auth lookup." },
    project_dir: { type: "string", description: "Alias for projectDir." },
    backendBaseURL: { type: "string", description: "Optional backend or daemon gateway base URL." },
    backend_base_url: { type: "string", description: "Alias for backendBaseURL." },
    server: { type: "string", description: "Alias for backendBaseURL, used by the MovScript CLI global --server." },
    token: { type: "string", description: "Optional backend bearer token. MCP responses redact this from debug argv." }
  };
}
function objectSchema3(properties, required = []) {
  return {
    type: "object",
    properties,
    ...required.length > 0 ? { required } : {},
    additionalProperties: false
  };
}
function stringValue10(value) {
  if (typeof value === "string" && value.trim()) return value.trim();
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return void 0;
}
function isRecord27(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

// ../../packages/mcp-host/dist/stdio.js
var DEFAULT_LOCAL_BACKEND = "http://localhost:8766";
var MCP_HOST_DEBUG = process.env.MOVSCRIPT_MCP_HOST_DEBUG === "1";
var LOCAL_NODE_CONTROL_SERVICE = LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE;
var LOCAL_NODE_GATEWAY_SERVICE2 = LOCAL_RUNTIME_DAEMON_GATEWAY_SERVICE;
var DATA_SERVICE2 = "movscript.data.service";
var PROJECT_SERVICE = "movscript.project.service";
var EDITING_SERVICE = "movscript.editing.service";
var LOCAL_SURFACE_HOST_SERVICE = "movscript.local-surface.host";
var MEDIA_PIPELINE_SERVICE = "movscript.media.pipeline";
var adminTools = adminCommandSpecs.map((tool) => ({
  name: tool.mcpToolName,
  description: tool.description,
  inputSchema: tool.inputSchema
}));
var hostTools = [
  ...adminTools,
  {
    name: "movscript_runtime_status",
    description: "Detect MovScript local/cloud backend availability, current project source, Desktop enhancement status, and the recommended runtime mode. This tool is read-only.",
    inputSchema: objectSchema4({
      workspaceDir: { type: "string", description: "Optional MovScript workspace or project directory to inspect." },
      workspace_dir: { type: "string", description: "Alias for workspaceDir." },
      homeDir: { type: "string", description: "Optional MovScript Home directory for runtime discovery. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
      home_dir: { type: "string", description: "Alias for homeDir." },
      movscriptHome: { type: "string", description: "Alias for homeDir." },
      movscript_home: { type: "string", description: "Alias for homeDir." },
      projectDir: { type: "string", description: "Optional project source directory to inspect." },
      project_dir: { type: "string", description: "Alias for projectDir." },
      projectId: { type: "string", description: "Optional project id to use when constructing Local Surface Host URLs." },
      project_id: { type: "string", description: "Alias for projectId." },
      productionId: { type: "string", description: "Optional legacy production-scope id to include in project surface URLs. Namespace focus should use scopeKind/scopeRef or targetKind/targetRef." },
      production_id: { type: "string", description: "Alias for productionId." },
      scopeKind: { type: "string", description: "Optional timeline namespace scope kind to include in project surface URLs, for example episode or beat." },
      scope_kind: { type: "string", description: "Alias for scopeKind." },
      scopeRef: { type: "string", description: "Optional timeline namespace scope ref to include in project surface URLs." },
      scope_ref: { type: "string", description: "Alias for scopeRef." },
      targetKind: { type: "string", description: "Optional normalized target kind to include in project surface URLs, such as timeline_assembly." },
      target_kind: { type: "string", description: "Alias for targetKind." },
      targetRef: { type: "string", description: "Optional normalized target ref to include in project surface URLs." },
      target_ref: { type: "string", description: "Alias for targetRef." },
      timelineAssemblyRef: { type: "string", description: "Optional timeline assembly target ref to include in project surface URLs." },
      timeline_assembly_ref: { type: "string", description: "Alias for timelineAssemblyRef." },
      localBackendURL: { type: "string", description: "Optional local backend URL to probe." },
      local_backend_url: { type: "string", description: "Alias for localBackendURL." },
      timeoutMs: { type: "number", description: "Probe timeout in milliseconds. Defaults to 750." },
      timeout_ms: { type: "number", description: "Alias for timeoutMs." }
    })
  },
  {
    name: "movscript_runtime_configure",
    description: "Configure the MovScript MCP host backend mode, backend URL, auth token, or project directory. This does not create or initialize a business project.",
    inputSchema: objectSchema4({
      backendMode: { type: "string", enum: ["local", "cloud"], description: "Preferred backend mode." },
      backend_mode: { type: "string", enum: ["local", "cloud"], description: "Alias for backendMode." },
      backendBaseURL: { type: "string", description: "Backend base URL such as http://localhost:8766 or https://api.example." },
      backend_base_url: { type: "string", description: "Alias for backendBaseURL." },
      token: { type: "string", description: "Bearer token for the selected backend. Prefer environment variables or movcli auth for persistent secrets." },
      projectDir: { type: "string", description: "Project source directory to use as default workspace/project context." },
      project_dir: { type: "string", description: "Alias for projectDir." },
      workspaceDir: { type: "string", description: "Workspace directory to persist backend config under." },
      workspace_dir: { type: "string", description: "Alias for workspaceDir." },
      remember: { type: "boolean", description: "When true, persist backendBaseURL to .movscript/backend/config.json." },
      clearToken: { type: "boolean", description: "When true, clear persisted workspace auth for the selected workspace." },
      clear_token: { type: "boolean", description: "Alias for clearToken." }
    })
  },
  {
    name: "runtime_daemon_configure",
    description: "Canonical runtime tool for configuring the MovScript MCP host backend mode, backend URL, auth token, or project directory.",
    inputSchema: objectSchema4({
      backendMode: { type: "string", enum: ["local", "cloud"], description: "Preferred backend mode." },
      backend_mode: { type: "string", enum: ["local", "cloud"], description: "Alias for backendMode." },
      backendBaseURL: { type: "string", description: "Backend base URL such as http://localhost:8766 or https://api.example." },
      backend_base_url: { type: "string", description: "Alias for backendBaseURL." },
      token: { type: "string", description: "Bearer token for the selected backend. Prefer environment variables or movcli auth for persistent secrets." },
      projectDir: { type: "string", description: "Project source directory to use as default workspace/project context." },
      project_dir: { type: "string", description: "Alias for projectDir." },
      workspaceDir: { type: "string", description: "Workspace directory to persist backend config under." },
      workspace_dir: { type: "string", description: "Alias for workspaceDir." },
      remember: { type: "boolean", description: "When true, persist backendBaseURL to .movscript/backend/config.json." },
      clearToken: { type: "boolean", description: "When true, clear persisted workspace auth for the selected workspace." },
      clear_token: { type: "boolean", description: "Alias for clearToken." }
    })
  },
  {
    name: "runtime_daemon_discover",
    description: "Read MovScript Home runtime records and discover daemon, gateway, service, and surface endpoints without starting or probing services.",
    inputSchema: runtimeReadinessSchema()
  },
  {
    name: "runtime_local_daemon_status",
    description: "Return the persistent MovScript local runtime daemon status. The daemon is the per-user owner for local Data/Project/Editing/Canvas/Surface/Media services and is independent from MCP sessions.",
    inputSchema: objectSchema4({
      homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
      home_dir: { type: "string", description: "Alias for homeDir." },
      movscriptHome: { type: "string", description: "Alias for homeDir." },
      movscript_home: { type: "string", description: "Alias for homeDir." }
    })
  },
  {
    name: "runtime_local_daemon_ensure",
    description: "Ensure the persistent MovScript local runtime daemon is running. This is a bootstrap tool: it starts the daemon when missing and reuses it when already ready.",
    inputSchema: localDaemonBootstrapSchema()
  },
  {
    name: "runtime_local_daemon_start",
    description: "Start the persistent MovScript local runtime daemon, reusing an already ready daemon unless forceRestart is set.",
    inputSchema: localDaemonBootstrapSchema()
  },
  {
    name: "runtime_daemon_ensure",
    description: "Canonical alias for runtime_local_daemon_ensure.",
    inputSchema: localDaemonBootstrapSchema()
  },
  {
    name: "runtime_daemon_start",
    description: "Canonical alias for runtime_local_daemon_start.",
    inputSchema: localDaemonBootstrapSchema()
  },
  {
    name: "runtime_daemon_status",
    description: "Canonical alias for runtime_local_daemon_status.",
    inputSchema: localDaemonControlSchema()
  },
  {
    name: "runtime_daemon_stop",
    description: "Canonical alias for runtime_local_daemon_stop.",
    inputSchema: localDaemonControlSchema()
  },
  {
    name: "runtime_daemon_restart",
    description: "Canonical alias for runtime_local_daemon_restart.",
    inputSchema: localDaemonControlSchema()
  },
  {
    name: "runtime_descriptor_get",
    description: "Return the canonical runtime descriptor for agents: selected data plane, MCP endpoint, service readiness, surfaces, and missing capabilities.",
    inputSchema: runtimeReadinessSchema()
  },
  {
    name: "runtime_preflight_check",
    description: "Run read-only runtime preflight checks and return blockers/warnings before project, generation, admin, or editing work.",
    inputSchema: runtimeReadinessSchema({
      requireProject: { type: "boolean", description: "When false, do not treat a missing project source as a blocker." },
      require_project: { type: "boolean", description: "Alias for requireProject." }
    })
  },
  {
    name: "runtime_local_daemon_stop",
    description: "Gracefully stop the persistent MovScript local runtime daemon so services can be upgraded, ports can be released, or local resources can be freed.",
    inputSchema: localDaemonControlSchema()
  },
  {
    name: "runtime_local_daemon_restart",
    description: "Gracefully restart the persistent MovScript local runtime daemon and its local services.",
    inputSchema: localDaemonControlSchema()
  },
  {
    name: "runtime_local_node_status",
    description: "Compatibility alias for runtime_local_daemon_status.",
    inputSchema: objectSchema4({
      homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
      home_dir: { type: "string", description: "Alias for homeDir." },
      movscriptHome: { type: "string", description: "Alias for homeDir." },
      movscript_home: { type: "string", description: "Alias for homeDir." }
    })
  },
  {
    name: "runtime_local_node_stop",
    description: "Compatibility alias for runtime_local_daemon_stop.",
    inputSchema: objectSchema4({
      homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
      home_dir: { type: "string", description: "Alias for homeDir." },
      movscriptHome: { type: "string", description: "Alias for homeDir." },
      movscript_home: { type: "string", description: "Alias for homeDir." }
    })
  },
  {
    name: "runtime_local_node_restart",
    description: "Compatibility alias for runtime_local_daemon_restart.",
    inputSchema: objectSchema4({
      homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
      home_dir: { type: "string", description: "Alias for homeDir." },
      movscriptHome: { type: "string", description: "Alias for homeDir." },
      movscript_home: { type: "string", description: "Alias for homeDir." }
    })
  }
];
async function startMCPStdioHost() {
  const rl = readline.createInterface({
    input: process.stdin,
    crlfDelay: Infinity
  });
  const pending = /* @__PURE__ */ new Set();
  rl.on("line", (line) => {
    const trimmed = line.trim();
    if (!trimmed) return;
    const task = (async () => {
      try {
        const payload = JSON.parse(trimmed);
        const response = Array.isArray(payload) ? (await Promise.all(payload.map((item) => handleMCPHostJSONRPC(item)))).filter((item) => item !== void 0) : await handleMCPHostJSONRPC(payload);
        if (Array.isArray(response)) {
          if (response.length > 0) writeMessage(response);
        } else if (response !== void 0) {
          writeMessage(response);
        }
      } catch (error) {
        writeMessage(makeError(null, -32700, "Parse error", errorMessage5(error)));
      }
    })();
    pending.add(task);
    task.finally(() => pending.delete(task));
  });
  await new Promise((resolve24) => {
    rl.on("close", async () => {
      await Promise.allSettled([...pending]);
      resolve24();
    });
  });
}
async function handleMCPHostJSONRPC(req) {
  const isNotification = !Object.prototype.hasOwnProperty.call(req, "id");
  const id = isNotification ? null : req.id ?? null;
  if (MCP_HOST_DEBUG) {
    process.stderr.write(`[movscript-mcp-host] method=${req?.method ?? ""} id=${String(id)}
`);
  }
  if (req.jsonrpc !== "2.0" || !req.method) {
    if (isNotification) return void 0;
    return makeError(id, -32600, "Invalid Request");
  }
  try {
    switch (req.method) {
      case "initialize":
        return makeResult(id, {
          protocolVersion: "2025-06-18",
          serverInfo: { name: "movscript-mcp-host", version: "0.1.28" },
          capabilities: { resources: {}, tools: {} }
        });
      case "initialized":
      case "notifications/initialized":
      case "notifications/cancelled":
      case "notifications/progress":
        return void 0;
      case "ping":
        return makeResult(id, {});
      case "tools/list":
        return makeResult(id, { tools: mergeTools(hostTools, listTools()) });
      case "tools/call":
        await touchLocalNode().catch(() => void 0);
        return makeResult(id, await callMCPHostTool(req.params));
      case "resources/list":
        return makeResult(id, { resources: listResources() });
      case "resources/read":
        return makeResult(id, await readResource(stringParam2(req.params, "uri") ?? ""));
      default:
        return handleJSONRPC(req);
    }
  } catch (error) {
    if (isNotification) return void 0;
    return makeError(id, -32e3, errorMessage5(error));
  }
}
async function callMCPHostTool(params) {
  const name = stringParam2(params, "name");
  const args = objectParam(params, "arguments");
  if (name === "movscript_runtime_status") return await runtimeStatus(args);
  if (name === "movscript_runtime_configure") return runtimeConfigure(args);
  if (name === "runtime_daemon_configure") return runtimeConfigure(args);
  if (name === "runtime_daemon_discover") return runtimeDaemonDiscover(args);
  if (name === "runtime_daemon_ensure") return await localDaemonEnsure(args);
  if (name === "runtime_daemon_start") return await localDaemonEnsure(args);
  if (name === "runtime_daemon_status") return await localNodeControl(args, "GET", "/status");
  if (name === "runtime_daemon_stop") return await localNodeControl(args, "POST", "/shutdown");
  if (name === "runtime_daemon_restart") return await localNodeControl(args, "POST", "/restart");
  if (name === "runtime_descriptor_get") return await runtimeDescriptorGet(args);
  if (name === "runtime_preflight_check") return await runtimePreflightCheck(args);
  if (name === "runtime_local_daemon_ensure") return await localDaemonEnsure(args);
  if (name === "runtime_local_daemon_start") return await localDaemonEnsure(args);
  if (name === "runtime_local_daemon_status") return await localNodeControl(args, "GET", "/status");
  if (name === "runtime_local_daemon_stop") return await localNodeControl(args, "POST", "/shutdown");
  if (name === "runtime_local_daemon_restart") return await localNodeControl(args, "POST", "/restart");
  if (name === "runtime_local_node_status") return await localNodeControl(args, "GET", "/status");
  if (name === "runtime_local_node_stop") return await localNodeControl(args, "POST", "/shutdown");
  if (name === "runtime_local_node_restart") return await localNodeControl(args, "POST", "/restart");
  if (isAdminMCPToolName(name)) {
    return unwrapCommandDataWithDebug(await runMovScriptAdminCommand(name, args));
  }
  bindBackendRuntimeForCoreTools(args);
  return callTool(params);
}
async function localDaemonEnsure(args) {
  const homeDir = resolveRuntimeHomeArg2(args);
  const entrypoint = resolveLocalDaemonEntrypoint(args);
  const cwd = resolve8(stringValue11(args.cwd) || process.cwd());
  const startupTimeoutMs = numberValue6(args.startupTimeoutMs ?? args.startup_timeout_ms);
  const stopTimeoutMs = numberValue6(args.stopTimeoutMs ?? args.stop_timeout_ms);
  const result = await ensureLocalRuntimeDaemon({
    homeDir,
    entrypoint,
    runArgs: ["daemon", "run"],
    cwd,
    env: localDaemonBootstrapEnv(args),
    forceRestart: args.forceRestart === true || args.force_restart === true,
    ...startupTimeoutMs ? { startupTimeoutMs } : {},
    ...stopTimeoutMs ? { stopTimeoutMs } : {}
  });
  return {
    bootstrap: "local_daemon",
    homeDir,
    entrypoint,
    cwd,
    ...result
  };
}
function resolveLocalDaemonEntrypoint(args) {
  const explicit = stringValue11(args.entrypoint ?? args.entry_point) ?? process.env.MOVSCRIPT_DAEMON_ENTRYPOINT ?? process.env.MOVSCRIPT_AGENT_MCP_ENTRYPOINT ?? process.argv[1];
  if (!explicit) {
    throw new Error("runtime daemon startup requires an entrypoint; pass entrypoint or run through the MovScript Agent Plugin MCP entrypoint");
  }
  return resolve8(explicit);
}
function localDaemonBootstrapEnv(args) {
  const env = {};
  const dataPlane = stringValue11(args.dataPlane ?? args.data_plane);
  if (dataPlane) {
    if (!["local", "cloud", "external"].includes(dataPlane)) {
      throw new Error(`invalid runtime daemon dataPlane: ${dataPlane}`);
    }
    env.MOVSCRIPT_LOCAL_DAEMON_DATA_PLANE = dataPlane;
  }
  const dataServiceURL = stringValue11(args.dataServiceURL ?? args.data_service_url);
  if (dataServiceURL) env.MOVSCRIPT_DATA_SERVICE_URL = dataServiceURL;
  const idleTimeout = stringValue11(args.idleTimeout ?? args.idle_timeout);
  if (idleTimeout) {
    env.MOVSCRIPT_LOCAL_DAEMON_IDLE_TIMEOUT = idleTimeout;
    env.MOVSCRIPT_LOCAL_NODE_IDLE_TIMEOUT = idleTimeout;
  }
  return Object.keys(env).length > 0 ? env : void 0;
}
async function runtimeStatus(args = {}) {
  const workspaceDir = resolve8(stringValue11(args.workspaceDir ?? args.workspace_dir) || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd());
  const projectDir = resolve8(stringValue11(args.projectDir ?? args.project_dir) || workspaceDir);
  const homeDir = resolveRuntimeHomeArg2(args);
  const runtimeHome = readRuntimeHomeSnapshot(homeDir);
  const timeoutMs = numberValue6(args.timeoutMs ?? args.timeout_ms) ?? 750;
  const homeGatewayEndpoint = endpointURL5(
    findRuntimeEndpoint(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2) ?? findRuntimeService(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2)?.endpoint
  );
  const homeDataEndpoint = endpointURL5(
    findRuntimeEndpoint(runtimeHome, "movscript.data.service") ?? findRuntimeService(runtimeHome, "movscript.data.service")?.endpoint
  );
  const localBackendURL = normalizeBaseURL(stringValue11(args.localBackendURL ?? args.local_backend_url) || homeGatewayEndpoint || homeDataEndpoint || DEFAULT_LOCAL_BACKEND);
  const configuredSession = resolveMovScriptBackendSession({
    workspaceDir,
    server: process.env.MOVSCRIPT_DATA_SERVICE_URL,
    token: process.env.MOVSCRIPT_API_TOKEN ?? process.env.MOVSCRIPT_DATA_SERVICE_TOKEN
  });
  const configuredBaseURL = normalizeBaseURL(configuredSession.baseURL);
  const configuredIsLocal = isLocalBackendURL(configuredBaseURL);
  const localProbe = await probeBackend(localBackendURL, timeoutMs);
  const configuredProbe = configuredBaseURL === localBackendURL ? localProbe : await probeBackend(configuredBaseURL, timeoutMs);
  const cloudAuth = findCloudAuth(workspaceDir);
  const cloudBaseURL = configuredIsLocal ? cloudAuth.baseURL : configuredBaseURL;
  const cloudProbe = cloudBaseURL && cloudBaseURL !== localBackendURL && cloudBaseURL !== configuredBaseURL ? await probeBackend(cloudBaseURL, timeoutMs) : configuredIsLocal ? { available: false } : configuredProbe;
  const project = inspectProjectSource(projectDir);
  const mediaPipeline = mediaPipelineRuntimeStatus(runtimeHome);
  const localNode = localNodeRuntimeStatus(runtimeHome);
  const surfaceHost = surfaceHostRuntimeStatus(runtimeHome);
  const desktop = await probeDesktop(timeoutMs, runtimeHome);
  const localAvailable = localProbe.available;
  const cloudConfigured = Boolean(cloudBaseURL || cloudAuth.authenticated || !configuredIsLocal && configuredSession.token);
  const cloudAvailable = Boolean(cloudBaseURL && isRecord28(cloudProbe) && cloudProbe.available === true);
  const selected = selectedBackendMode({
    configuredIsLocal,
    localAvailable,
    cloudAvailable,
    projectAvailable: project.isMovScriptProject
  });
  const requiresUserChoice = shouldRequireUserChoice({
    localAvailable,
    cloudAvailable,
    projectAvailable: project.isMovScriptProject
  });
  const missing = missingItems({
    localAvailable,
    cloudAvailable,
    projectAvailable: project.isMovScriptProject
  });
  const runtimeOwner = runtimeOwnerStatus({
    desktopAvailable: desktop.available === true,
    localDaemonAvailable: localNode.available === true,
    localAvailable,
    cloudAvailable,
    selected
  });
  const surfaces = localSurfaceURLs({
    surfaceHost,
    project,
    projectDir,
    projectId: stringValue11(args.projectId ?? args.project_id),
    productionId: stringValue11(args.productionId ?? args.production_id),
    focusQuery: runtimeFocusQuery(args),
    runtimeOwner
  });
  return {
    status: "ok",
    home: runtimeHomeSummary(runtimeHome),
    backend: {
      local: {
        available: localAvailable,
        baseURL: localBackendURL,
        discoveredFromHome: Boolean(homeGatewayEndpoint || homeDataEndpoint),
        ...homeGatewayEndpoint ? { gatewayBaseURL: homeGatewayEndpoint } : {},
        ...homeDataEndpoint ? { dataServiceBaseURL: homeDataEndpoint } : {},
        authenticated: configuredIsLocal && Boolean(configuredSession.token),
        ...localProbe.error ? { error: localProbe.error } : {}
      },
      cloud: {
        available: cloudAvailable,
        configured: cloudConfigured,
        ...cloudBaseURL ? { baseURL: cloudBaseURL } : {},
        authenticated: Boolean(cloudAuth.authenticated || !configuredIsLocal && configuredSession.token),
        ...isRecord28(cloudProbe) && typeof cloudProbe.error === "string" ? { error: cloudProbe.error } : {}
      },
      selected
    },
    workspace: {
      cwd: process.cwd(),
      workspaceDir,
      projectDir,
      ...project
    },
    desktop,
    localDaemon: localNode,
    localNode,
    surfaceHost,
    surfaces,
    ...surfaces.primary ? { surface: surfaces.primary } : {},
    ...surfaces.secondary.length > 0 ? { secondary_surfaces: surfaces.secondary } : {},
    mediaPipeline,
    runtimeOwner,
    recommendedMode: recommendedMode(selected, project.isMovScriptProject),
    requiresUserChoice,
    missing
  };
}
function runtimeDaemonDiscover(args = {}) {
  const homeDir = resolveRuntimeHomeArg2(args);
  const runtimeHome = readRuntimeHomeSnapshot(homeDir);
  const endpoints = runtimeDiscoveredEndpoints(runtimeHome);
  const daemonAvailable = Boolean(endpoints.control || endpoints.gateway);
  return {
    schema: "movscript.runtime_daemon_discovery.v1",
    status: daemonAvailable ? "ready" : "not_running",
    homeDir,
    daemon: {
      available: daemonAvailable,
      applicationId: LOCAL_RUNTIME_DAEMON_APP_ID,
      ...endpoints.control ? { controlEndpoint: endpoints.control } : {},
      ...endpoints.gateway ? { gatewayEndpoint: endpoints.gateway } : {},
      ...endpoints.gateway ? { mcpEndpoint: runtimeMcpEndpoint(endpoints.gateway) } : {}
    },
    endpoints,
    home: runtimeHomeSummary(runtimeHome),
    recommendedNextTool: daemonAvailable ? "runtime_descriptor_get" : "runtime_daemon_ensure"
  };
}
async function runtimeDescriptorGet(args = {}) {
  const status = await runtimeStatus(args);
  return runtimeDescriptorFromStatus(status);
}
async function runtimePreflightCheck(args = {}) {
  const status = await runtimeStatus(args);
  const descriptor = runtimeDescriptorFromStatus(status);
  const checks = runtimePreflightChecks(status, args);
  const blockers = checks.filter((check) => check.status === "blocked");
  const warnings = checks.filter((check) => check.status === "warning");
  return {
    schema: "movscript.runtime_preflight.v1",
    status: blockers.length > 0 ? "blocked" : warnings.length > 0 ? "degraded" : "ready",
    ready: blockers.length === 0,
    blockers,
    warnings,
    checks,
    descriptor
  };
}
function runtimeDescriptorFromStatus(status) {
  const backend = recordValue6(status.backend);
  const localBackend = recordValue6(backend.local);
  const cloudBackend = recordValue6(backend.cloud);
  const workspace = recordValue6(status.workspace);
  const runtimeOwner = recordValue6(status.runtimeOwner);
  const localDaemon = recordValue6(status.localDaemon ?? status.localNode);
  const mediaPipeline = recordValue6(status.mediaPipeline);
  const surfaceHost = recordValue6(status.surfaceHost);
  const surfaces = recordValue6(status.surfaces);
  const gatewayEndpoint = stringValue11(localBackend.gatewayBaseURL);
  const dataServiceEndpoint = stringValue11(localBackend.dataServiceBaseURL) ?? (localBackend.discoveredFromHome === true ? stringValue11(localBackend.baseURL) : void 0);
  const selected = stringValue11(backend.selected);
  const endpointMap = {};
  if (gatewayEndpoint) {
    endpointMap.gateway = gatewayEndpoint;
    endpointMap.mcp = runtimeMcpEndpoint(gatewayEndpoint);
    endpointMap.mcp_http = runtimeMcpEndpoint(gatewayEndpoint);
  }
  if (dataServiceEndpoint) endpointMap.dataService = dataServiceEndpoint;
  if (stringValue11(localDaemon.endpoint)) endpointMap.daemonControl = stringValue11(localDaemon.endpoint);
  if (stringValue11(mediaPipeline.endpoint)) endpointMap.mediaPipeline = stringValue11(mediaPipeline.endpoint);
  if (stringValue11(surfaceHost.endpoint)) endpointMap.surfaceHost = stringValue11(surfaceHost.endpoint);
  return {
    schema: "movscript.runtime_descriptor.v1",
    status: runtimeDescriptorStatus(status),
    owner: runtimeOwner,
    dataPlane: selected ?? (cloudBackend.available === true ? "cloud" : localBackend.available === true ? "local" : "unknown"),
    endpoints: endpointMap,
    services: {
      backend: runtimeServiceDescriptor("backend", localBackend.available === true || cloudBackend.available === true, stringValue11(localBackend.baseURL ?? cloudBackend.baseURL), selected),
      daemon: runtimeServiceDescriptor("daemon", localDaemon.available === true, stringValue11(localDaemon.endpoint), stringValue11(runtimeOwner.kind)),
      dataService: runtimeServiceDescriptor(DATA_SERVICE2, localBackend.available === true, dataServiceEndpoint, selected),
      projectService: runtimeServiceDescriptor(PROJECT_SERVICE, Boolean(workspace.isMovScriptProject), stringValue11(workspace.projectDir), "project_source"),
      editingService: runtimeServiceDescriptor(EDITING_SERVICE, true, void 0, "not_probed"),
      surfaceHost: runtimeServiceDescriptor(LOCAL_SURFACE_HOST_SERVICE, surfaceHost.available === true, stringValue11(surfaceHost.endpoint), stringValue11(surfaceHost.mode)),
      mediaPipeline: runtimeServiceDescriptor(MEDIA_PIPELINE_SERVICE, mediaPipeline.available === true, stringValue11(mediaPipeline.endpoint), "render")
    },
    workspace,
    surfaces,
    recommendedMode: status.recommendedMode,
    requiresUserChoice: status.requiresUserChoice,
    missing: Array.isArray(status.missing) ? status.missing : []
  };
}
function runtimeDescriptorStatus(status) {
  const missing = Array.isArray(status.missing) ? status.missing : [];
  if (missing.includes("backend")) return "missing_backend";
  if (missing.length > 0) return "degraded";
  return "ready";
}
function runtimeServiceDescriptor(name, ready, endpoint, mode) {
  return {
    name,
    ready,
    status: ready ? "ready" : "missing",
    ...endpoint ? { endpoint } : {},
    ...mode ? { mode } : {}
  };
}
function runtimePreflightChecks(status, args) {
  const backend = recordValue6(status.backend);
  const localBackend = recordValue6(backend.local);
  const cloudBackend = recordValue6(backend.cloud);
  const workspace = recordValue6(status.workspace);
  const localDaemon = recordValue6(status.localDaemon ?? status.localNode);
  const mediaPipeline = recordValue6(status.mediaPipeline);
  const surfaceHost = recordValue6(status.surfaceHost);
  const requireProject = args.requireProject === false || args.require_project === false ? false : true;
  const checks = [];
  checks.push(runtimeCheck({
    id: "backend",
    ok: localBackend.available === true || cloudBackend.available === true,
    blocker: true,
    message: "Data backend or daemon gateway is reachable.",
    missingMessage: "No reachable Data Service, daemon gateway, or cloud backend was found.",
    owner: "runtime"
  }));
  checks.push(runtimeCheck({
    id: "daemon",
    ok: localDaemon.available === true,
    blocker: false,
    message: "Local daemon control endpoint is registered.",
    missingMessage: "Local daemon control endpoint is not registered; external/cloud runtimes may still work.",
    owner: "runtime"
  }));
  checks.push(runtimeCheck({
    id: "project_source",
    ok: workspace.isMovScriptProject === true,
    blocker: requireProject,
    message: "MovScript project source is available.",
    missingMessage: "MovScript project source was not found at the selected projectDir/workspaceDir.",
    owner: "project"
  }));
  checks.push(runtimeCheck({
    id: "surface_host",
    ok: surfaceHost.available === true,
    blocker: false,
    message: "Local surface host is available for human review.",
    missingMessage: "Local surface host is not available; browser review URLs may be unavailable.",
    owner: "surface"
  }));
  checks.push(runtimeCheck({
    id: "media_pipeline",
    ok: mediaPipeline.available === true,
    blocker: false,
    message: "Media Pipeline endpoint is registered.",
    missingMessage: "Media Pipeline endpoint is not registered; render/transcode/HLS may be unavailable.",
    owner: "media_pipeline"
  }));
  return checks;
}
function runtimeCheck(input) {
  if (input.ok) {
    return {
      id: input.id,
      status: "ready",
      owner: input.owner,
      message: input.message
    };
  }
  return {
    id: input.id,
    status: input.blocker ? "blocked" : "warning",
    owner: input.owner,
    message: input.missingMessage
  };
}
function runtimeDiscoveredEndpoints(runtimeHome) {
  const endpoints = {};
  setEndpoint(endpoints, "control", runtimeHome, LOCAL_NODE_CONTROL_SERVICE);
  setEndpoint(endpoints, "gateway", runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2);
  setEndpoint(endpoints, "dataService", runtimeHome, DATA_SERVICE2);
  setEndpoint(endpoints, "projectService", runtimeHome, PROJECT_SERVICE);
  setEndpoint(endpoints, "editingService", runtimeHome, EDITING_SERVICE);
  setEndpoint(endpoints, "surfaceHost", runtimeHome, LOCAL_SURFACE_HOST_SERVICE);
  setEndpoint(endpoints, "mediaPipeline", runtimeHome, MEDIA_PIPELINE_SERVICE);
  if (endpoints.gateway) endpoints.mcp = runtimeMcpEndpoint(endpoints.gateway);
  return endpoints;
}
function setEndpoint(output, key, runtimeHome, serviceName) {
  const endpoint = endpointURL5(
    findRuntimeEndpoint(runtimeHome, serviceName) ?? findRuntimeService(runtimeHome, serviceName)?.endpoint
  );
  if (endpoint) output[key] = endpoint;
}
function runtimeMcpEndpoint(baseURL) {
  const normalized = normalizeBaseURL(baseURL);
  return `${normalized}/v1/mcp`;
}
async function localNodeControl(args, method, path) {
  const homeDir = resolveRuntimeHomeArg2(args);
  const endpoint = endpointURL5(findRuntimeEndpoint(readRuntimeHomeSnapshot(homeDir), LOCAL_NODE_CONTROL_SERVICE));
  if (!endpoint) return { status: "not_running", homeDir };
  try {
    const response = await fetch(`${endpoint}${path}`, { method, signal: AbortSignal.timeout(3e3) });
    const payload = await response.json().catch(() => ({}));
    return {
      status: response.ok ? payload.status ?? "ok" : "error",
      homeDir,
      endpoint,
      ...payload,
      ...response.ok ? {} : { httpStatus: response.status }
    };
  } catch (error) {
    return { status: "error", homeDir, endpoint, error: errorMessage5(error) };
  }
}
async function touchLocalNode() {
  const homeDir = resolveMovScriptHomeDir();
  const endpoint = endpointURL5(findRuntimeEndpoint(readRuntimeHomeSnapshot(homeDir), LOCAL_NODE_CONTROL_SERVICE));
  if (!endpoint) return;
  await fetch(`${endpoint}/touch`, { method: "POST", signal: AbortSignal.timeout(750) });
}
function runtimeConfigure(args = {}) {
  const backendBaseURL = stringValue11(args.backendBaseURL ?? args.backend_base_url);
  const backendMode = stringValue11(args.backendMode ?? args.backend_mode);
  const workspaceDir = resolve8(stringValue11(args.workspaceDir ?? args.workspace_dir) || stringValue11(args.projectDir ?? args.project_dir) || process.env.MOVSCRIPT_WORKSPACE_DIR || process.cwd());
  const projectDir = stringValue11(args.projectDir ?? args.project_dir);
  const token = stringValue11(args.token);
  const remember = args.remember === true;
  const clearToken = args.clearToken === true || args.clear_token === true;
  if (projectDir) setMCPDefaultWorkspaceDir(resolve8(projectDir));
  else setMCPDefaultWorkspaceDir(workspaceDir);
  if (backendBaseURL) setMovScriptBackendAPIBaseURL(backendBaseURL);
  if (token) setMovScriptBackendRuntimeAuthToken(token);
  if (clearToken) {
    clearMovScriptBackendAuth(workspaceDir);
    setMovScriptBackendRuntimeAuthToken(void 0);
  }
  const persisted = backendBaseURL && remember ? writeMovScriptBackendConfig(workspaceDir, {
    baseURL: backendBaseURL,
    ...backendMode === "local" || backendMode === "cloud" ? { realm: backendMode === "local" ? { kind: "local", id: "local" } : { kind: "cloud", id: "default" } } : {}
  }) : void 0;
  return {
    status: "configured",
    workspaceDir,
    ...projectDir ? { projectDir: resolve8(projectDir) } : {},
    ...backendMode ? { backendMode } : {},
    ...backendBaseURL ? { backendBaseURL: normalizeBaseURL(backendBaseURL) } : {},
    remembered: Boolean(persisted),
    tokenConfigured: Boolean(token),
    tokenCleared: clearToken
  };
}
function inspectProjectSource(projectDir) {
  const workspacePath5 = resolve8(projectDir, "workspace.json");
  const projectPath = resolve8(projectDir, "project.json");
  const metadataPath = existsSync4(workspacePath5) ? workspacePath5 : existsSync4(projectPath) ? projectPath : void 0;
  const metadata = metadataPath ? readJSON4(metadataPath) : void 0;
  const sourceCollections = Array.from(MOVSCRIPT_SOURCE_COLLECTION_DIRS).filter((name) => existsSync4(resolve8(projectDir, name))).sort();
  const sourceRootFiles = Array.from(MOVSCRIPT_SOURCE_ROOT_FILES).filter((name) => existsSync4(resolve8(projectDir, name))).sort();
  const hasSourceDirs = sourceCollections.length > 0;
  const hasSourceRootFiles = sourceRootFiles.length > 0;
  const projectUid = isRecord28(metadata) ? stringValue11(metadata.project_uid ?? metadata.projectUid) : void 0;
  const projectTitle = isRecord28(metadata) ? stringValue11(metadata.title ?? metadata.name) : void 0;
  return {
    isMovScriptProject: Boolean(metadataPath || hasSourceDirs || hasSourceRootFiles),
    hasMetadata: Boolean(metadataPath),
    hasSourceDirs,
    hasSourceRootFiles,
    sourceCollections,
    sourceRootFiles,
    ...metadataPath ? { metadataPath } : {},
    ...projectUid ? { projectUid } : {},
    ...projectTitle ? { projectTitle } : {}
  };
}
async function probeBackend(baseURL, timeoutMs) {
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), Math.max(250, Math.min(timeoutMs, 5e3)));
  try {
    const response = await fetch(`${baseURL.replace(/\/+$/, "")}/health`, { signal: controller.signal });
    return { available: response.ok, status: response.status, ...response.ok ? {} : { error: `HTTP ${response.status}` } };
  } catch (error) {
    return { available: false, error: errorMessage5(error) };
  } finally {
    clearTimeout(timeout);
  }
}
async function probeDesktop(timeoutMs, runtimeHome) {
  const desktopApp = findRuntimeApp(runtimeHome, "movscript.desktop") ?? findRuntimeApp(runtimeHome, "movscript.desktop.app");
  const mediaPipeline = mediaPipelineRuntimeStatus(runtimeHome);
  const homeEndpoint = endpointURL5(desktopApp?.endpoint) ?? endpointURL5(findRuntimeEndpoint(runtimeHome, "movscript.mcp.host"));
  const endpoint = process.env.MOVSCRIPT_MCP_ENDPOINT || homeEndpoint || "http://127.0.0.1:18765/mcp";
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), Math.max(250, Math.min(timeoutMs, 3e3)));
  try {
    const response = await fetch(endpoint, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ jsonrpc: "2.0", id: "runtime-status-desktop-probe", method: "initialize" }),
      signal: controller.signal
    });
    return {
      available: response.ok,
      endpoint,
      discoveredFromHome: Boolean(homeEndpoint),
      applicationId: desktopApp?.applicationId,
      mediaPipeline: mediaPipeline.available,
      ...mediaPipeline.endpoint ? { mediaPipelineEndpoint: mediaPipeline.endpoint } : {},
      ...response.ok ? {} : { error: `HTTP ${response.status}` }
    };
  } catch (error) {
    return {
      available: false,
      endpoint,
      discoveredFromHome: Boolean(homeEndpoint),
      applicationId: desktopApp?.applicationId,
      mediaPipeline: mediaPipeline.available,
      ...mediaPipeline.endpoint ? { mediaPipelineEndpoint: mediaPipeline.endpoint } : {},
      error: errorMessage5(error)
    };
  } finally {
    clearTimeout(timeout);
  }
}
function mediaPipelineRuntimeStatus(runtimeHome) {
  const endpoint = endpointURL5(
    findRuntimeEndpoint(runtimeHome, "movscript.media.pipeline") ?? findRuntimeService(runtimeHome, "movscript.media.pipeline")?.endpoint
  );
  return {
    available: Boolean(endpoint),
    ...endpoint ? { endpoint } : {}
  };
}
function localNodeRuntimeStatus(runtimeHome) {
  const endpoint = endpointURL5(findRuntimeEndpoint(runtimeHome, LOCAL_NODE_CONTROL_SERVICE));
  return {
    available: Boolean(endpoint),
    ...endpoint ? { endpoint } : {}
  };
}
function surfaceHostRuntimeStatus(runtimeHome) {
  const gatewayEndpointRecord = findRuntimeEndpoint(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2);
  const gatewayServiceRecord = findRuntimeService(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2);
  const surfaceEndpointRecord = findRuntimeEndpoint(runtimeHome, LOCAL_SURFACE_HOST_SERVICE);
  const surfaceServiceRecord = findRuntimeService(runtimeHome, LOCAL_SURFACE_HOST_SERVICE);
  const endpointRecord = gatewayEndpointRecord ?? surfaceEndpointRecord;
  const serviceRecord = gatewayServiceRecord ?? surfaceServiceRecord;
  const endpoint = endpointURL5(endpointRecord ?? serviceRecord?.endpoint);
  const ownerApplicationId = endpointRecord?.applicationId ?? serviceRecord?.ownerApplicationId;
  const serviceName = endpointRecord?.serviceName ?? serviceRecord?.serviceName ?? LOCAL_SURFACE_HOST_SERVICE;
  return {
    available: Boolean(endpoint),
    serviceName,
    surfaceHostServiceName: LOCAL_SURFACE_HOST_SERVICE,
    ...endpoint ? { endpoint } : {},
    ...ownerApplicationId ? { ownerApplicationId } : {},
    ...ownerApplicationId ? { mode: surfaceHostMode(ownerApplicationId) } : {}
  };
}
function surfaceHostMode(ownerApplicationId) {
  if (ownerApplicationId === "movscript.agent-plugin") return "agent-plugin-session";
  if (ownerApplicationId === LOCAL_RUNTIME_DAEMON_APP_ID) return "local-daemon";
  if (ownerApplicationId === "movscript.desktop") return "desktop-owned";
  return "external";
}
function localSurfaceURLs(input) {
  const startupAllowed = input.runtimeOwner.surfaceHostStartupAllowed === true;
  if (!input.surfaceHost.endpoint) {
    return {
      available: false,
      openable: false,
      reason: startupAllowed ? "local_surface_host_not_ready_startup_allowed" : "local_surface_host_not_ready",
      secondary: [],
      urls: {},
      startupAllowed
    };
  }
  const baseURL = normalizeBaseURL(input.surfaceHost.endpoint);
  const projectId = input.projectId ?? stringValue11(input.project.projectUid) ?? safeProjectIdFromDir(input.projectDir);
  const commonQuery = {
    source: "runtime-status",
    projectId,
    projectDir: input.projectDir
  };
  if (input.productionId) commonQuery.productionId = input.productionId;
  for (const [key, value] of Object.entries(input.focusQuery ?? {})) {
    commonQuery[key] = value;
  }
  const home = localSurfaceURL(baseURL, "/", commonQuery);
  const projectOverview = localSurfaceURL(baseURL, `/studio/${encodeURIComponent(projectId)}/overview`, commonQuery);
  const projectContent = localSurfaceURL(baseURL, `/studio/${encodeURIComponent(projectId)}/content`, commonQuery);
  const projectTimeline = localSurfaceURL(baseURL, `/studio/${encodeURIComponent(projectId)}/timeline`, commonQuery);
  const canvas = localSurfaceURL(baseURL, "/canvases", { source: "runtime-status" });
  const editing = localSurfaceURL(baseURL, "/editing", commonQuery);
  const admin = localSurfaceURL(baseURL, "/admin/overview", { source: "runtime-status" });
  const primary = input.project.isMovScriptProject ? runtimeSurfaceLink({
    title: "MovScript project overview",
    surface: "project.overview",
    route: `/studio/${encodeURIComponent(projectId)}/overview`,
    url: projectOverview,
    usage: "Open this URL in the Codex/in-app browser when the user needs to inspect or operate the MovScript project UI."
  }) : runtimeSurfaceLink({
    title: "MovScript Local Surface Host",
    surface: "local-surface-host",
    route: "/",
    url: home,
    usage: "Open this URL in the Codex/in-app browser to choose a MovScript local surface."
  });
  const secondary = [
    runtimeSurfaceLink({
      title: "MovScript project content",
      surface: "project.content",
      route: `/studio/${encodeURIComponent(projectId)}/content`,
      url: projectContent,
      usage: "Open this URL to inspect content units and project content state."
    }),
    runtimeSurfaceLink({
      title: "MovScript project timeline",
      surface: "project.timeline",
      route: `/studio/${encodeURIComponent(projectId)}/timeline`,
      url: projectTimeline,
      usage: "Open this URL to inspect the project timeline surface."
    }),
    runtimeSurfaceLink({
      title: "MovScript canvases",
      surface: "canvas",
      route: "/canvases",
      url: canvas,
      usage: "Open this URL when the user needs to inspect or edit canvas surfaces."
    }),
    runtimeSurfaceLink({
      title: "MovScript editing",
      surface: "editing",
      route: "/editing",
      url: editing,
      usage: "Open this URL when the user needs to inspect or edit media editing projects."
    }),
    runtimeSurfaceLink({
      title: "MovScript local admin",
      surface: "admin.overview",
      route: "/admin/overview",
      url: admin,
      usage: "Open this URL when the user needs local admin/provider/job controls."
    })
  ];
  return {
    available: true,
    openable: true,
    reason: "local_surface_host_ready",
    primary,
    secondary,
    urls: {
      home,
      projectOverview,
      projectContent,
      projectTimeline,
      canvas,
      editing,
      admin
    },
    startupAllowed
  };
}
function safeProjectIdFromDir(projectDir) {
  return basename3(projectDir) || "sample-project";
}
function localSurfaceURL(baseURL, pathname, query) {
  const url = new URL(`${baseURL.replace(/\/+$/, "")}/`);
  const basePath = url.pathname.replace(/\/+$/, "");
  const routePath = pathname.startsWith("/") ? pathname : `/${pathname}`;
  url.pathname = `${basePath}${routePath}` || "/";
  for (const [key, value] of Object.entries(query)) {
    if (value) url.searchParams.set(key, value);
  }
  return url.toString();
}
function runtimeFocusQuery(args) {
  const output = {};
  setQueryValue(output, "scopeKind", args.scopeKind ?? args.scope_kind);
  setQueryValue(output, "scopeRef", args.scopeRef ?? args.scope_ref);
  setQueryValue(output, "targetKind", args.targetKind ?? args.target_kind);
  setQueryValue(output, "targetRef", args.targetRef ?? args.target_ref);
  setQueryValue(output, "timeline_assembly_ref", args.timelineAssemblyRef ?? args.timeline_assembly_ref);
  return output;
}
function setQueryValue(output, key, value) {
  const normalized = stringValue11(value);
  if (normalized) output[key] = normalized;
}
function runtimeSurfaceLink(input) {
  return {
    kind: "browser_url",
    title: input.title,
    surface: input.surface,
    route: input.route,
    url: input.url,
    usage: input.usage
  };
}
function resolveRuntimeHomeArg2(args) {
  const homeDir = stringValue11(args.homeDir ?? args.home_dir ?? args.movscriptHome ?? args.movscript_home);
  return homeDir ? resolve8(homeDir) : resolveMovScriptHomeDir();
}
function bindBackendRuntimeForCoreTools(args) {
  const homeDir = resolveRuntimeHomeArg2(args);
  const runtimeHome = readRuntimeHomeSnapshot(homeDir);
  const gatewayEndpoint = endpointURL5(
    findRuntimeEndpoint(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2) ?? findRuntimeService(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2)?.endpoint
  );
  const dataEndpoint = endpointURL5(
    findRuntimeEndpoint(runtimeHome, "movscript.data.service") ?? findRuntimeService(runtimeHome, "movscript.data.service")?.endpoint
  );
  const backendEndpoint = gatewayEndpoint ?? dataEndpoint;
  if (backendEndpoint) setMovScriptBackendAPIBaseURL(backendEndpoint);
  if (backendEndpoint && args.mcp_base_url === void 0 && args.mcpBaseURL === void 0) {
    args.mcp_base_url = backendEndpoint;
  }
  const agentSurfaceEndpoint = endpointURL5(
    findRuntimeEndpoint(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2) ?? findRuntimeService(runtimeHome, LOCAL_NODE_GATEWAY_SERVICE2)?.endpoint ?? findRuntimeEndpoint(runtimeHome, LOCAL_SURFACE_HOST_SERVICE) ?? findRuntimeService(runtimeHome, LOCAL_SURFACE_HOST_SERVICE)?.endpoint
  );
  if (agentSurfaceEndpoint && args.frontend_origin === void 0 && args.frontendOrigin === void 0) {
    args.frontend_origin = agentSurfaceEndpoint;
  }
  const workspaceDir = stringValue11(args.workspaceDir ?? args.workspace_dir) ?? stringValue11(args.projectDir ?? args.project_dir) ?? stringValue11(args.projectPath ?? args.project_path) ?? stringValue11(args.cwd) ?? process.env.MOVSCRIPT_WORKSPACE_DIR;
  if (workspaceDir) setMCPDefaultWorkspaceDir(resolve8(workspaceDir));
}
function endpointURL5(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.protocol && endpoint.protocol !== "http" && endpoint.protocol !== "https") return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}
function runtimeHomeSummary(snapshot) {
  const apps = activeAppRecords(snapshot).map((record) => ({
    applicationId: record.applicationId,
    status: record.status,
    ready: record.ready,
    ...record.profile ? { profile: record.profile } : {},
    ...record.owner ? { owner: record.owner } : {},
    ...record.endpoint ? { endpoint: endpointURL5(record.endpoint) } : {}
  }));
  const services = activeServiceRecords(snapshot).map((record) => ({
    serviceName: record.serviceName,
    instanceId: record.instanceId,
    status: record.status,
    ready: record.ready,
    ...record.profile ? { profile: record.profile } : {},
    ...record.ownerApplicationId ? { ownerApplicationId: record.ownerApplicationId } : {},
    ...record.endpoint ? { endpoint: endpointURL5(record.endpoint) } : {}
  }));
  const endpoints = activeEndpointRecords(snapshot).map((record) => ({
    ...record.serviceName ? { serviceName: record.serviceName } : {},
    ...record.applicationId ? { applicationId: record.applicationId } : {},
    status: record.status,
    ready: record.ready,
    ...endpointURL5(record) ? { endpoint: endpointURL5(record) } : {}
  }));
  return {
    homeDir: snapshot.homeDir,
    apps,
    services,
    endpoints
  };
}
function runtimeOwnerStatus(input) {
  if (input.localDaemonAvailable) {
    return {
      kind: "local_daemon",
      applicationId: LOCAL_RUNTIME_DAEMON_APP_ID,
      reason: "local_runtime_daemon_ready",
      businessSidecarStartupAllowed: false,
      surfaceHostStartupAllowed: false,
      sidecarStartupAllowed: false
    };
  }
  if (input.desktopAvailable) {
    return {
      kind: "desktop_legacy_owner",
      applicationId: "movscript.desktop",
      reason: "desktop_full_runtime_ready_without_local_daemon",
      businessSidecarStartupAllowed: false,
      surfaceHostStartupAllowed: false,
      sidecarStartupAllowed: false
    };
  }
  if (input.selected === "cloud" && input.cloudAvailable) {
    return {
      kind: "cloud",
      reason: "cloud_backend_ready",
      businessSidecarStartupAllowed: false,
      surfaceHostStartupAllowed: false,
      sidecarStartupAllowed: false
    };
  }
  if (input.selected === "local" && input.localAvailable) {
    return {
      kind: "external_local",
      reason: "local_backend_ready_without_desktop",
      businessSidecarStartupAllowed: true,
      surfaceHostStartupAllowed: true,
      sidecarStartupAllowed: true
    };
  }
  return {
    kind: "none",
    reason: "no_ready_runtime",
    businessSidecarStartupAllowed: true,
    surfaceHostStartupAllowed: true,
    sidecarStartupAllowed: true
  };
}
function findCloudAuth(workspaceDir) {
  try {
    const paths = resolveMovScriptBackendPaths(workspaceDir);
    const cloudRoot = resolve8(paths.backendRealmsDir, "cloud");
    if (!existsSync4(cloudRoot)) return { authenticated: false };
    for (const realmId of readdirSync2(cloudRoot)) {
      const authPath = resolve8(cloudRoot, realmId, "auth.json");
      const auth = readJSON4(authPath);
      if (isRecord28(auth) && stringValue11(auth.token)) return { authenticated: true };
    }
  } catch {
  }
  return { authenticated: false };
}
function selectedBackendMode(input) {
  if (input.localAvailable && input.cloudAvailable) return input.configuredIsLocal ? "local" : "cloud";
  if (input.localAvailable) return "local";
  if (input.cloudAvailable) return "cloud";
  return void 0;
}
function shouldRequireUserChoice(input) {
  return input.localAvailable && input.cloudAvailable && input.projectAvailable || input.localAvailable && !input.projectAvailable || input.cloudAvailable && !input.projectAvailable;
}
function missingItems(input) {
  const missing = [];
  if (!input.localAvailable && !input.cloudAvailable) missing.push("backend");
  if (!input.projectAvailable) missing.push("project_source");
  return missing;
}
function recommendedMode(selected, hasProject) {
  if (selected === "local" && hasProject) return "local_backend_local_source";
  if (selected === "cloud" && hasProject) return "cloud_backend_local_source";
  if (selected === "cloud") return "cloud_backend_cloud_source";
  return void 0;
}
function runtimeReadinessSchema(extra = {}) {
  return objectSchema4({
    homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
    home_dir: { type: "string", description: "Alias for homeDir." },
    movscriptHome: { type: "string", description: "Alias for homeDir." },
    movscript_home: { type: "string", description: "Alias for homeDir." },
    workspaceDir: { type: "string", description: "Optional MovScript workspace or project directory to inspect." },
    workspace_dir: { type: "string", description: "Alias for workspaceDir." },
    projectDir: { type: "string", description: "Optional project source directory to inspect." },
    project_dir: { type: "string", description: "Alias for projectDir." },
    projectId: { type: "string", description: "Optional project id to use when constructing surface URLs." },
    project_id: { type: "string", description: "Alias for projectId." },
    productionId: { type: "string", description: "Optional legacy production id to include in surface URLs." },
    production_id: { type: "string", description: "Alias for productionId." },
    scopeKind: { type: "string", description: "Optional timeline namespace scope kind." },
    scope_kind: { type: "string", description: "Alias for scopeKind." },
    scopeRef: { type: "string", description: "Optional timeline namespace scope ref." },
    scope_ref: { type: "string", description: "Alias for scopeRef." },
    targetKind: { type: "string", description: "Optional normalized target kind." },
    target_kind: { type: "string", description: "Alias for targetKind." },
    targetRef: { type: "string", description: "Optional normalized target ref." },
    target_ref: { type: "string", description: "Alias for targetRef." },
    timelineAssemblyRef: { type: "string", description: "Optional timeline assembly target ref." },
    timeline_assembly_ref: { type: "string", description: "Alias for timelineAssemblyRef." },
    localBackendURL: { type: "string", description: "Optional local backend URL to probe." },
    local_backend_url: { type: "string", description: "Alias for localBackendURL." },
    timeoutMs: { type: "number", description: "Probe timeout in milliseconds. Defaults to 750." },
    timeout_ms: { type: "number", description: "Alias for timeoutMs." },
    ...extra
  });
}
function objectSchema4(properties, required = []) {
  return {
    type: "object",
    properties,
    ...required.length > 0 ? { required } : {},
    additionalProperties: false
  };
}
function localDaemonControlSchema() {
  return objectSchema4({
    homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
    home_dir: { type: "string", description: "Alias for homeDir." },
    movscriptHome: { type: "string", description: "Alias for homeDir." },
    movscript_home: { type: "string", description: "Alias for homeDir." }
  });
}
function localDaemonBootstrapSchema() {
  return objectSchema4({
    homeDir: { type: "string", description: "Optional MovScript Home directory. Defaults to MOVSCRIPT_HOME or ~/.movscript." },
    home_dir: { type: "string", description: "Alias for homeDir." },
    movscriptHome: { type: "string", description: "Alias for homeDir." },
    movscript_home: { type: "string", description: "Alias for homeDir." },
    entrypoint: { type: "string", description: "Optional MovScript CLI entrypoint capable of running daemon run. Defaults to the current process entrypoint." },
    entry_point: { type: "string", description: "Alias for entrypoint." },
    cwd: { type: "string", description: "Optional working directory for the daemon launcher. Defaults to the current working directory." },
    dataPlane: { type: "string", enum: ["local", "cloud", "external"], description: "Optional daemon data plane." },
    data_plane: { type: "string", enum: ["local", "cloud", "external"], description: "Alias for dataPlane." },
    dataServiceURL: { type: "string", description: "Optional cloud or external Data Service URL." },
    data_service_url: { type: "string", description: "Alias for dataServiceURL." },
    idleTimeout: { type: "string", description: "Optional idle shutdown timeout, for example 30m or never." },
    idle_timeout: { type: "string", description: "Alias for idleTimeout." },
    startupTimeoutMs: { type: "number", description: "Optional daemon startup timeout in milliseconds." },
    startup_timeout_ms: { type: "number", description: "Alias for startupTimeoutMs." },
    stopTimeoutMs: { type: "number", description: "Optional timeout for stopping a stale daemon before restart, in milliseconds." },
    stop_timeout_ms: { type: "number", description: "Alias for stopTimeoutMs." },
    forceRestart: { type: "boolean", description: "When true, restart even if a matching daemon is already ready." },
    force_restart: { type: "boolean", description: "Alias for forceRestart." }
  });
}
function mergeTools(primary, secondary) {
  const seen = /* @__PURE__ */ new Set();
  const tools = [];
  for (const tool of [...primary, ...secondary]) {
    if (seen.has(tool.name)) continue;
    seen.add(tool.name);
    tools.push(tool);
  }
  return tools;
}
function objectParam(value, key) {
  if (!isRecord28(value)) return {};
  const candidate = value[key];
  return isRecord28(candidate) ? candidate : {};
}
function stringParam2(value, key) {
  if (!isRecord28(value)) return void 0;
  return stringValue11(value[key]);
}
function readJSON4(path) {
  try {
    return JSON.parse(readFileSync5(path, "utf8"));
  } catch {
    return void 0;
  }
}
function normalizeBaseURL(value) {
  return value.trim().replace(/\/+$/, "").replace(/\/api\/v1$/, "") || DEFAULT_LOCAL_BACKEND;
}
function isLocalBackendURL(value) {
  try {
    const url = new URL(value);
    return ["localhost", "127.0.0.1", "::1", "[::1]"].includes(url.hostname);
  } catch {
    return false;
  }
}
function stringValue11(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function numberValue6(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function recordValue6(value) {
  return isRecord28(value) ? value : {};
}
function isRecord28(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function writeMessage(message) {
  process.stdout.write(`${JSON.stringify(message)}
`);
}
function errorMessage5(error) {
  return error instanceof Error ? error.message : String(error);
}

// application.manifest.ts
var pluginApplicationManifest = {
  schema: MOVSCRIPT_APPLICATION_MANIFEST_SCHEMA,
  applicationId: "movscript.agent-plugin",
  name: "MovScript Agent Plugin App",
  owner: "agent-provider",
  programs: [
    "movscript.plugin.agent-launcher",
    "movscript.mcp.host",
    "movscript.data.service",
    "movscript.canvas.service",
    "movscript.project.service",
    "movscript.editing.service",
    "movscript.local-surface.host",
    "movscript.media.pipeline"
  ]
};
var application_manifest_default = pluginApplicationManifest;

// programs/agent-launcher.program.manifest.ts
var pluginAgentLauncherProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "plugin-agent-launcher",
  serviceName: "movscript.plugin.agent-launcher",
  kind: "cli",
  name: "MovScript Agent Plugin Launcher",
  profiles: ["plugin", "test"],
  entry: {
    command: "./bin/movscript",
    args: ["mcp", "stdio"]
  },
  transport: "stdio",
  health: {
    kind: "process"
  },
  dependsOn: ["movscript.mcp.host"],
  provides: ["agent-plugin-entrypoint"]
};
var agent_launcher_program_manifest_default = pluginAgentLauncherProgramManifest;

// startup.manifest.ts
var pluginDesktopCompatibilityStartupPolicy = {
  schema: MOVSCRIPT_SCENARIO_POLICY_SCHEMA,
  scenarioId: "plugin-desktop-compatible",
  applicationId: "movscript.agent-plugin",
  programs: [
    { serviceName: "movscript.plugin.agent-launcher", required: true },
    { serviceName: "movscript.mcp.host", required: true, profile: "stdio" },
    { serviceName: "movscript.local-surface.host", required: true, profile: "desktop-connected" }
  ]
};
var pluginBasicStartupPolicy = {
  schema: MOVSCRIPT_SCENARIO_POLICY_SCHEMA,
  scenarioId: "plugin-basic",
  applicationId: "movscript.agent-plugin",
  programs: [
    { serviceName: "movscript.plugin.agent-launcher", required: true },
    { serviceName: "movscript.mcp.host", required: true, profile: "stdio" }
  ]
};
var pluginFullLocalStartupPolicy = {
  schema: MOVSCRIPT_SCENARIO_POLICY_SCHEMA,
  scenarioId: "plugin-full-local",
  applicationId: "movscript.agent-plugin",
  programs: [
    { serviceName: "movscript.plugin.agent-launcher", required: true },
    { serviceName: "movscript.mcp.host", required: true, profile: "stdio" },
    { serviceName: "movscript.local-node.control", required: false, profile: "local-daemon" }
  ]
};

// src/agent-mcp.ts
var import_cli = __toESM(require_dist(), 1);

// ../../services/canvas-service/src/server.mjs
import { createServer as createServer2 } from "http";
var CANVAS_SERVICE_NAME = "movscript.canvas.service";
var CANVAS_SERVICE_CAPABILITIES_ENDPOINT = "/v1/canvas/capabilities";
var CANVAS_SERVICE_CANVASES_ENDPOINT = "/v1/canvas/canvases";
var CANVAS_SERVICE_RUNTIME_MODELS_ENDPOINT = "/v1/canvas/runtime/models";
var CANVAS_SERVICE_RUNTIME_TEXT_ENDPOINT = "/v1/canvas/runtime/text";
var CANVAS_SERVICE_RUNTIME_MEDIA_ENDPOINT = "/v1/canvas/runtime/media";
var CANVAS_SERVICE_RUNTIME_TEXT_RESOURCE_ENDPOINT = "/v1/canvas/runtime/text-resource";
var CANVAS_SERVICE_RUNTIME_JOBS_ENDPOINT = "/v1/canvas/runtime/jobs";
var CANVAS_SERVICE_CAPABILITIES = Object.freeze([
  "canvas-api",
  "canvas-storage",
  "canvas-runtime"
]);
function createCanvasServiceHandler(options = {}) {
  const serviceName = options.serviceName ?? CANVAS_SERVICE_NAME;
  const capabilities = options.capabilities ?? CANVAS_SERVICE_CAPABILITIES;
  const fetchImpl = options.fetch ?? fetch;
  const configuredDataServiceBaseURL = () => resolveDataServiceBaseURL(options);
  const modelGatewayBaseURL = normalizeOptionalBaseURL(
    options.modelGatewayBaseURL ?? options.env?.MOVSCRIPT_MODEL_GATEWAY_URL ?? options.env?.MOVSCRIPT_MODEL_GATEWAY_BASE_URL ?? process.env.MOVSCRIPT_MODEL_GATEWAY_URL ?? process.env.MOVSCRIPT_MODEL_GATEWAY_BASE_URL,
    "model gateway base URL"
  );
  return async (request, response) => {
    const url = new URL(request.url ?? "/", "http://127.0.0.1");
    try {
      if (request.method === "GET" && url.pathname === "/health") {
        writeJSON2(response, 200, {
          status: "ok",
          serviceName,
          capabilities,
          upstream: configuredDataServiceBaseURL()
        });
        return;
      }
      if (request.method === "GET" && url.pathname === CANVAS_SERVICE_CAPABILITIES_ENDPOINT) {
        writeJSON2(response, 200, {
          serviceName,
          capabilities,
          upstream: configuredDataServiceBaseURL(),
          modelGateway: modelGatewayBaseURL
        });
        return;
      }
      if (url.pathname === CANVAS_SERVICE_RUNTIME_TEXT_ENDPOINT) {
        await handleRuntimeTextRequest({
          fetchImpl,
          modelGatewayBaseURL,
          request,
          response
        });
        return;
      }
      if (url.pathname === CANVAS_SERVICE_RUNTIME_MODELS_ENDPOINT) {
        await proxyDataServiceRequest({
          dataServiceBaseURL: configuredDataServiceBaseURL(),
          fetchImpl,
          request,
          response,
          search: url.search,
          upstreamPath: "/api/v1/models"
        });
        return;
      }
      if (url.pathname === CANVAS_SERVICE_RUNTIME_MEDIA_ENDPOINT) {
        await handleRuntimeMediaRequest({
          dataServiceBaseURL: configuredDataServiceBaseURL(),
          fetchImpl,
          request,
          response
        });
        return;
      }
      if (url.pathname === CANVAS_SERVICE_RUNTIME_TEXT_RESOURCE_ENDPOINT) {
        await handleRuntimeTextResourceRequest({
          dataServiceBaseURL: configuredDataServiceBaseURL(),
          fetchImpl,
          request,
          response
        });
        return;
      }
      const runtimeJobPath = runtimeJobUpstreamPath(url.pathname);
      if (runtimeJobPath) {
        await proxyDataServiceRequest({
          dataServiceBaseURL: configuredDataServiceBaseURL(),
          fetchImpl,
          request,
          response,
          search: url.search,
          upstreamPath: runtimeJobPath
        });
        return;
      }
      const upstreamPath = canvasUpstreamPath(url.pathname);
      if (upstreamPath) {
        await proxyDataServiceRequest({
          dataServiceBaseURL: configuredDataServiceBaseURL(),
          fetchImpl,
          request,
          response,
          search: url.search,
          upstreamPath
        });
        return;
      }
      writeJSON2(response, 404, { error: "not_found" });
    } catch (error) {
      writeCanvasServiceError(response, error);
    }
  };
}
function canvasUpstreamPath(pathname) {
  if (pathname === CANVAS_SERVICE_CANVASES_ENDPOINT) return "/api/v1/canvases";
  if (pathname.startsWith(`${CANVAS_SERVICE_CANVASES_ENDPOINT}/`)) {
    const id = pathname.slice(CANVAS_SERVICE_CANVASES_ENDPOINT.length + 1);
    if (!id || id.includes("/")) return "";
    return `/api/v1/canvases/${id}`;
  }
  return "";
}
function runtimeJobUpstreamPath(pathname) {
  if (!pathname.startsWith(`${CANVAS_SERVICE_RUNTIME_JOBS_ENDPOINT}/`)) return "";
  const id = pathname.slice(CANVAS_SERVICE_RUNTIME_JOBS_ENDPOINT.length + 1);
  if (!id || id.includes("/")) return "";
  return `/api/v1/jobs/${id}`;
}
async function handleRuntimeTextRequest({ fetchImpl, modelGatewayBaseURL, request, response }) {
  if (request.method !== "POST") {
    writeJSON2(response, 405, { error: "method_not_allowed" });
    return;
  }
  if (!modelGatewayBaseURL) {
    writeJSON2(response, 503, {
      error: "model_gateway_not_configured",
      message: "MOVSCRIPT_MODEL_GATEWAY_URL is required for Canvas runtime text generation"
    });
    return;
  }
  const input = await readJSONBody2(request);
  const prompt = typeof input.prompt === "string" ? input.prompt.trim() : "";
  const model = typeof input.model_id === "string" ? input.model_id.trim() : "";
  if (!prompt) {
    writeJSON2(response, 400, { error: "prompt is required" });
    return;
  }
  if (!model) {
    writeJSON2(response, 400, { error: "model_id is required" });
    return;
  }
  const params = isRecord29(input.params) ? input.params : {};
  const upstream = await fetchImpl(`${modelGatewayBaseURL}/v1/chat/completions`, {
    method: "POST",
    headers: {
      ...proxyRequestHeaders(request),
      "content-type": "application/json"
    },
    body: JSON.stringify({
      model,
      messages: [{ role: "user", content: prompt }],
      ...Number.isFinite(params.max_tokens) ? { max_tokens: params.max_tokens } : {},
      ...Number.isFinite(params.temperature) ? { temperature: params.temperature } : {},
      ...params.json_mode === true ? { response_format: { type: "json_object" } } : {},
      ...Number.isInteger(input.project_id) ? { project_id: input.project_id } : {}
    })
  });
  const body = await upstream.text();
  if (!upstream.ok) {
    response.statusCode = upstream.status;
    response.setHeader("content-type", upstream.headers.get("content-type") ?? "application/json; charset=utf-8");
    response.end(body);
    return;
  }
  const parsed = JSON.parse(body);
  const text = parsed?.choices?.[0]?.message?.content;
  writeJSON2(response, 200, {
    type: "text",
    text: typeof text === "string" ? text : "",
    model_id: parsed?.model ?? model,
    usage: parsed?.usage
  });
}
async function handleRuntimeMediaRequest({ dataServiceBaseURL, fetchImpl, request, response }) {
  if (request.method !== "POST") {
    writeJSON2(response, 405, { error: "method_not_allowed" });
    return;
  }
  const input = await readJSONBody2(request);
  await proxyDataServiceJSON({
    dataServiceBaseURL,
    fetchImpl,
    request,
    response,
    upstreamPath: "/api/v1/jobs",
    body: input
  });
}
async function handleRuntimeTextResourceRequest({ dataServiceBaseURL, fetchImpl, request, response }) {
  if (request.method !== "POST") {
    writeJSON2(response, 405, { error: "method_not_allowed" });
    return;
  }
  const input = await readJSONBody2(request);
  const text = typeof input.text === "string" ? input.text : "";
  const name = typeof input.name === "string" && input.name.trim() ? input.name.trim() : "canvas-output.txt";
  const filename = name.endsWith(".txt") ? name : `${name}.txt`;
  const form = new FormData();
  form.append("file", new Blob([text], { type: "text/plain" }), filename);
  const upstream = await fetchImpl(`${dataServiceBaseURL}/api/v1/resources/upload`, {
    method: "POST",
    headers: proxyRequestHeaders(request, { omitContentType: true }),
    body: form
  });
  await writeUpstreamResponse(response, upstream);
}
async function proxyDataServiceJSON({ dataServiceBaseURL, fetchImpl, request, response, upstreamPath, body }) {
  const upstream = await fetchImpl(`${dataServiceBaseURL}${upstreamPath}`, {
    method: request.method,
    headers: {
      ...proxyRequestHeaders(request),
      "content-type": "application/json"
    },
    body: JSON.stringify(body)
  });
  await writeUpstreamResponse(response, upstream);
}
async function proxyDataServiceRequest({ dataServiceBaseURL, fetchImpl, request, response, search, upstreamPath }) {
  const upstreamURL = `${dataServiceBaseURL}${upstreamPath}${search}`;
  const headers = proxyRequestHeaders(request);
  const body = request.method === "GET" || request.method === "HEAD" ? void 0 : await readBody(request);
  const upstream = await fetchImpl(upstreamURL, {
    method: request.method,
    headers,
    body
  });
  await writeUpstreamResponse(response, upstream);
}
async function writeUpstreamResponse(response, upstream) {
  response.statusCode = upstream.status;
  for (const [key, value] of upstream.headers.entries()) {
    if (key.toLowerCase() === "content-encoding") continue;
    response.setHeader(key, value);
  }
  const buffer = Buffer.from(await upstream.arrayBuffer());
  response.end(buffer);
}
function proxyRequestHeaders(request, options = {}) {
  const headers = {};
  for (const [key, value] of Object.entries(request.headers)) {
    if (value === void 0) continue;
    const lower = key.toLowerCase();
    if (["host", "connection", "content-length"].includes(lower)) continue;
    if (options.omitContentType && lower === "content-type") continue;
    headers[key] = Array.isArray(value) ? value.join(", ") : value;
  }
  return headers;
}
async function readBody(request) {
  const chunks = [];
  for await (const chunk of request) {
    chunks.push(Buffer.from(chunk));
  }
  return Buffer.concat(chunks);
}
async function readJSONBody2(request) {
  const body = await readBody(request);
  if (body.length === 0) return {};
  return JSON.parse(body.toString("utf8"));
}
function isRecord29(value) {
  return value !== null && typeof value === "object" && !Array.isArray(value);
}
function normalizeOptionalBaseURL(value, label) {
  if (typeof value !== "string" || !value.trim()) {
    return "";
  }
  return normalizeBaseURL2(value, label);
}
function normalizeBaseURL2(value, label = "base URL") {
  if (typeof value !== "string" || !value.trim()) {
    throw new Error(`${label} is required`);
  }
  const url = new URL(value.trim().replace(/\/+$/, ""));
  if (url.protocol !== "http:" && url.protocol !== "https:") {
    throw new Error(`${label} must use http or https`);
  }
  return url.toString().replace(/\/+$/, "");
}
function resolveDataServiceBaseURL(options) {
  if (options.dataServiceBaseURL) {
    return normalizeBaseURL2(options.dataServiceBaseURL, "data service base URL");
  }
  const runtimeEndpoint = resolveRuntimeDataServiceBaseURL(options);
  if (runtimeEndpoint) return runtimeEndpoint;
  return normalizeBaseURL2(
    options.env?.MOVSCRIPT_DATA_SERVICE_URL ?? options.env?.MOVSCRIPT_DATA_SERVICE_BASE_URL ?? options.env?.MOVSCRIPT_API_BASE_URL ?? process.env.MOVSCRIPT_DATA_SERVICE_URL ?? process.env.MOVSCRIPT_DATA_SERVICE_BASE_URL ?? process.env.MOVSCRIPT_API_BASE_URL ?? "http://127.0.0.1:8766",
    "data service base URL"
  );
}
function resolveRuntimeDataServiceBaseURL(options) {
  const explicitHomeDir = options.homeDir ?? options.env?.MOVSCRIPT_HOME ?? process.env.MOVSCRIPT_HOME;
  const homeDir = explicitHomeDir?.trim() ? explicitHomeDir : resolveMovScriptHomeDir({ env: options.env ?? process.env });
  try {
    const snapshot = readRuntimeHomeSnapshot(homeDir);
    const endpoint = findRuntimeEndpoint(snapshot, "movscript.data.service") ?? findRuntimeService(snapshot, "movscript.data.service")?.endpoint;
    return normalizeOptionalBaseURL(endpointURL6(endpoint), "runtime data service base URL");
  } catch {
    return "";
  }
}
function endpointURL6(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}
function writeJSON2(response, statusCode, body) {
  response.statusCode = statusCode;
  response.setHeader("content-type", "application/json; charset=utf-8");
  response.end(JSON.stringify(body));
}
function writeCanvasServiceError(response, error) {
  const statusCode = Number.isInteger(error?.statusCode) ? error.statusCode : 500;
  writeJSON2(response, statusCode, {
    error: error?.code ?? "canvas_service_error",
    message: error?.message ?? "canvas service error"
  });
}
async function startCanvasService(options = {}) {
  const handler = createCanvasServiceHandler(options);
  const server = createServer2(handler);
  const host = options.host ?? "127.0.0.1";
  const port = options.port ?? 0;
  await new Promise((resolve12, reject) => {
    server.once("error", reject);
    server.listen(port, host, resolve12);
  });
  const address = server.address();
  const actualPort = typeof address === "object" && address ? address.port : port;
  return {
    server,
    url: `http://${host}:${actualPort}`,
    close: () => new Promise((resolve12, reject) => server.close((error) => error ? reject(error) : resolve12()))
  };
}
async function runCanvasServiceCLI(args = [], env = process.env) {
  const [command = "serve"] = args;
  if (command !== "serve") {
    throw new Error(`unsupported canvas-service command: ${command}`);
  }
  const host = env.MOVSCRIPT_CANVAS_SERVICE_HOST || "127.0.0.1";
  const port = Number(env.MOVSCRIPT_CANVAS_SERVICE_PORT || env.PORT || 0);
  const runtime = await startCanvasService({
    env,
    host,
    port
  });
  console.log(`[canvas-service] listening on ${runtime.url}`);
  await waitForShutdown2(runtime);
}
function waitForShutdown2(runtime) {
  return new Promise((resolve12) => {
    let closing = false;
    const close = async () => {
      if (closing) return;
      closing = true;
      await runtime.close();
      resolve12();
    };
    process.once("SIGINT", close);
    process.once("SIGTERM", close);
  });
}

// ../../services/editing-service/src/server.mjs
import { createServer as createServer3 } from "http";
import { mkdir as mkdir4, readdir as readdir3, readFile as readFile4, unlink, writeFile as writeFile4 } from "fs/promises";
import { tmpdir as tmpdir3 } from "os";
import { dirname as dirname7, join as join9, resolve as resolve9 } from "path";
var EDITING_SERVICE_CAPABILITIES = Object.freeze([
  "timeline",
  "edit-plan",
  "edit-decisions",
  "editing-project-command",
  "editing-timeline-view",
  "production-timeline-bundle",
  "timeline-assembly-bundle",
  "preview-timeline",
  "render-request",
  "media-task-action",
  "video-compose-project"
]);
function createEditingServiceHandler(options = {}) {
  const serviceName = options.serviceName ?? EDITING_SERVICE_NAME;
  const capabilities = options.capabilities ?? EDITING_SERVICE_CAPABILITIES;
  const homeDir = options.homeDir ?? process.env.MOVSCRIPT_HOME ?? join9(tmpdir3(), "movscript-editing-service");
  return async (request, response) => {
    const url = new URL(request.url ?? "/", "http://127.0.0.1");
    try {
      writeCORSHeaders2(response, request.headers.origin);
      if (request.method === "OPTIONS") {
        response.writeHead(204);
        response.end();
        return;
      }
      if (request.method === "GET" && url.pathname === "/health") {
        writeJSON3(response, 200, {
          status: "ok",
          serviceName,
          capabilities
        });
        return;
      }
      if (request.method === "GET" && url.pathname === EDITING_SERVICE_CAPABILITIES_ENDPOINT) {
        writeJSON3(response, 200, {
          serviceName,
          capabilities
        });
        return;
      }
      if (request.method === "POST" && url.pathname === EDITING_SERVICE_PROJECT_COMMAND_ENDPOINT) {
        const body = await readJSONBody3(request);
        const command = stringValue12(body.command);
        if (!command) {
          throw httpError2(400, "editing_project_command_required", "command is required");
        }
        const result = await executeEditingProjectCommand(command, recordValue7(body.input) ?? {}, { homeDir });
        writeJSON3(response, 200, {
          schema: "movscript.editing-project-command-result.v1",
          command,
          result
        });
        return;
      }
      if (request.method === "POST" && url.pathname === EDITING_SERVICE_TIMELINE_VIEW_ENDPOINT) {
        const body = await readJSONBody3(request);
        const kind = stringValue12(body.kind);
        if (!kind) {
          throw httpError2(400, "editing_timeline_view_kind_required", "kind is required");
        }
        const projectDir = projectDirFromBody(body);
        const result = await readEditingTimelineView({ projectDir, kind, input: body });
        writeJSON3(response, 200, {
          schema: "movscript.editing-timeline-view.v1",
          projectDir,
          kind,
          result: result ?? null
        });
        return;
      }
      if (request.method === "POST" && url.pathname === EDITING_SERVICE_TASK_REQUEST_ENDPOINT) {
        const body = await readJSONBody3(request);
        const taskType = stringValue12(body.taskType ?? body.task_type);
        if (!taskType) {
          throw httpError2(400, "editing_task_type_required", "taskType is required");
        }
        const taskRequest = buildEditingTaskRequest(taskType, recordValue7(body.input) ?? {});
        writeJSON3(response, 200, {
          schema: "movscript.editing-task-request.v1",
          taskType,
          request: taskRequest
        });
        return;
      }
      if (request.method === "POST" && url.pathname === EDITING_SERVICE_TASK_ACTION_ENDPOINT) {
        const body = await readJSONBody3(request);
        const action = stringValue12(body.action);
        if (!action) {
          throw httpError2(400, "editing_task_action_required", "action is required");
        }
        const taskAction = buildEditingTaskAction(action, recordValue7(body.input) ?? {});
        writeJSON3(response, 200, {
          schema: "movscript.editing-task-action.v1",
          action,
          status: taskAction.status ?? "ready",
          ...taskAction.request ? { request: taskAction.request } : {},
          ...taskAction.result ? { result: taskAction.result } : {}
        });
        return;
      }
      writeJSON3(response, 404, {
        error: "not_found"
      });
    } catch (error) {
      writeEditingServiceError(response, error);
    }
  };
}
async function readEditingTimelineView({ projectDir, kind, input }) {
  const decisionStore = optionalDecisionStoreFromBody(input);
  const workspaceService = createNodeMovScriptWorkspaceService({
    projectDir,
    ...decisionStore ? { decisionStore } : {}
  });
  switch (kind) {
    case "previewTimeline": {
      const productionId = stringOrNumberValue3(input.productionId ?? input.production_id);
      if (productionId === void 0) {
        throw httpError2(400, "editing_timeline_view_production_required", "productionId is required");
      }
      return workspaceService.readPreviewTimeline(productionId);
    }
    case "sceneMomentEditPlan": {
      const sceneMomentId = stringOrNumberValue3(input.sceneMomentId ?? input.scene_moment_id);
      if (sceneMomentId === void 0) {
        throw httpError2(400, "editing_timeline_view_scene_moment_required", "sceneMomentId is required");
      }
      return workspaceService.readSceneMomentEditPlan(sceneMomentId);
    }
    case "sceneMomentTimelineBundle": {
      const sceneMomentId = stringOrNumberValue3(input.sceneMomentId ?? input.scene_moment_id);
      if (sceneMomentId === void 0) {
        throw httpError2(400, "editing_timeline_view_scene_moment_required", "sceneMomentId is required");
      }
      return readSceneMomentTimelineBundle(workspaceService, sceneMomentId, input);
    }
    case "productionTimelineBundle": {
      const productionId = stringOrNumberValue3(input.productionId ?? input.production_id);
      if (productionId === void 0) {
        throw httpError2(400, "editing_timeline_view_production_required", "productionId is required");
      }
      return readProductionTimelineBundle(workspaceService, productionId, input);
    }
    case "timelineAssemblyBundle": {
      const target = timelineAssemblyScopeFromInput(input);
      if (target.scopeKind === "production") {
        return readProductionTimelineBundle(workspaceService, target.scopeRef, {
          ...input,
          targetKind: "timeline_assembly",
          targetRef: target.targetRef,
          scopeKind: target.scopeKind,
          scopeRef: target.scopeRef
        }, { canonical: true });
      }
      return readTimelineAssemblyBundle(workspaceService, target, input);
    }
    default:
      throw httpError2(400, "editing_timeline_view_unsupported", `unsupported editing timeline view: ${kind}`);
  }
}
async function readSceneMomentTimelineBundle(workspaceService, sceneMomentId, input) {
  const editPlan = await workspaceService.readSceneMomentEditPlan(sceneMomentId);
  if (!recordValue7(editPlan)) {
    throw httpError2(404, "editing_scene_moment_edit_plan_missing", `scene_moment ${String(sceneMomentId)} edit plan was not found; run domain_interpret first`);
  }
  const mediaEditingProject = createMediaEditingProjectFromMovScriptEditPlan(editPlan, {
    id: stringValue12(input.id ?? input.editingProjectId ?? input.editing_project_id),
    projectId: projectIdValue2(input) ?? `movscript_${String(editPlan.productionId)}`,
    title: stringValue12(input.title ?? input.projectName ?? input.project_name ?? input.sceneName ?? input.scene_name),
    width: optionalNumber2(input.width),
    height: optionalNumber2(input.height),
    fps: optionalNumber2(input.fps),
    background: stringValue12(input.background),
    defaultDurationMs: optionalNumber2(input.defaultDurationMs ?? input.default_duration_ms) ?? secToMs2(optionalNumber2(input.defaultDurationSec ?? input.default_duration_sec))
  });
  const composeInputs = buildMediaProjectComposeInputs(mediaEditingProject);
  return {
    schema: "movscript.scene-moment-timeline-bundle.v1",
    status: editPlan.status === "ready_to_compose" ? "ok" : "blocked",
    target_kind: "scene_moment",
    scene_moment_id: sceneMomentId,
    edit_plan_status: editPlan.status,
    edit_plan: editPlan,
    editPlan,
    media_editing_project: mediaEditingProject,
    mediaEditingProject,
    compose_inputs: composeInputs,
    composeInputs,
    context: editingProjectContextFromEditPlan(editPlan),
    blockers: Array.isArray(editPlan.blockers) ? editPlan.blockers : []
  };
}
async function readProductionTimelineBundle(workspaceService, productionId, input, options = {}) {
  const target = productionTimelineTargetFromInput(productionId, input);
  const [index, context, previewTimeline] = await Promise.all([
    workspaceService.loadIndex(),
    workspaceService.queryProductionContext({
      include: ["productions", "content_units"],
      limit: 1e3
    }),
    workspaceService.readPreviewTimeline(productionId)
  ]);
  const blockers = [];
  if (!previewTimeline) {
    blockers.push({
      code: "preview_timeline_missing",
      message: `production ${String(productionId)} preview timeline was not found; run domain_interpret first`
    });
  }
  const clips = previewTimeline ? productionTimelineClips({
    previewTimeline,
    contentUnits: context.content_units ?? [],
    documents: index.documents,
    blockers
  }) : [];
  const production = (context.productions ?? []).find(
    (item) => sameId7(item.id, productionId) || sameId7(item.record?.id, productionId) || previewTimeline?.productionPath && item.path.startsWith(previewTimeline.productionPath)
  );
  const productionPath = production?.path ?? previewTimeline?.productionPath;
  const title = stringValue12(input.title ?? input.projectName ?? input.project_name) ?? stringValue12(production?.record?.title);
  const mediaEditingProject = createMediaEditingProjectFromProductionTimelineClips({
    productionId,
    productionPath,
    targetKind: target.targetKind,
    targetRef: target.targetRef,
    scopeKind: target.scopeKind,
    scopeRef: target.scopeRef,
    clips,
    id: stringValue12(input.id ?? input.editingProjectId ?? input.editing_project_id),
    projectId: projectIdValue2(input),
    title,
    now: stringValue12(input.now),
    width: optionalNumber2(input.width),
    height: optionalNumber2(input.height),
    fps: optionalNumber2(input.fps),
    background: stringValue12(input.background),
    defaultDurationMs: optionalNumber2(input.defaultDurationMs ?? input.default_duration_ms) ?? secToMs2(optionalNumber2(input.defaultDurationSec ?? input.default_duration_sec)) ?? 4e3
  });
  const editPlan = productionEditPlanFromBundle({
    productionId,
    productionPath,
    target,
    projectName: title,
    clips,
    blockers
  });
  const contextView = editingProjectContextFromProductionClips(productionId, clips, blockers, target);
  const composeInputs = buildMediaProjectComposeInputs(mediaEditingProject);
  return {
    schema: options.canonical ? "movscript.timeline-assembly-bundle.v1" : "movscript.production-timeline-bundle.v1",
    preferred_schema: "movscript.timeline-assembly-bundle.v1",
    legacy_schema_alias: "movscript.production-timeline-bundle.v1",
    status: blockers.length === 0 ? "ok" : "blocked",
    target_kind: target.targetKind,
    target_ref: target.targetRef,
    scope_kind: target.scopeKind,
    scope_ref: target.scopeRef,
    legacy_alias: target.legacyTargetKind ? {
      target_kind: target.legacyTargetKind,
      target_ref: target.legacyTargetRef
    } : void 0,
    production_id: productionId,
    productionId,
    preview_timeline: previewTimeline,
    previewTimeline,
    media_editing_project: mediaEditingProject,
    mediaEditingProject,
    edit_plan: editPlan,
    editPlan,
    context: contextView,
    compose_inputs: composeInputs,
    composeInputs,
    clips,
    blockers
  };
}
async function readTimelineAssemblyBundle(workspaceService, target, input) {
  const [index, context, previewTimeline] = await Promise.all([
    workspaceService.loadIndex(),
    workspaceService.queryProductionContext({
      include: ["content_units"],
      limit: 1e3
    }),
    workspaceService.readTimelineAssemblyPreviewTimeline({
      scopeKind: target.scopeKind,
      scopeRef: target.scopeRef,
      targetRef: target.targetRef
    })
  ]);
  const blockers = [];
  if (!previewTimeline) {
    blockers.push({
      code: "timeline_assembly_preview_timeline_missing",
      scope_kind: target.scopeKind,
      scope_ref: target.scopeRef,
      target_ref: target.targetRef,
      message: `timeline assembly ${target.targetRef} preview timeline was not found; ensure the namespace scope exists and run domain_interpret first`
    });
  }
  const clips = previewTimeline ? productionTimelineClips({
    previewTimeline,
    contentUnits: context.content_units ?? [],
    documents: index.documents,
    blockers
  }) : [];
  const title = stringValue12(input.title ?? input.projectName ?? input.project_name) ?? stringValue12(previewTimeline?.scopeTitle) ?? `Timeline assembly ${target.scopeKind}:${target.scopeRef}`;
  const mediaEditingProject = createMediaEditingProjectFromTimelineAssemblyClips({
    targetKind: target.targetKind,
    targetRef: target.targetRef,
    scopeKind: target.scopeKind,
    scopeRef: target.scopeRef,
    scopePath: stringValue12(previewTimeline?.scopePath),
    clips,
    id: stringValue12(input.id ?? input.editingProjectId ?? input.editing_project_id),
    projectId: projectIdValue2(input),
    title,
    now: stringValue12(input.now),
    width: optionalNumber2(input.width),
    height: optionalNumber2(input.height),
    fps: optionalNumber2(input.fps),
    background: stringValue12(input.background),
    defaultDurationMs: optionalNumber2(input.defaultDurationMs ?? input.default_duration_ms) ?? secToMs2(optionalNumber2(input.defaultDurationSec ?? input.default_duration_sec)) ?? 4e3
  });
  const editPlan = timelineAssemblyEditPlanFromBundle({
    target,
    scopePath: stringValue12(previewTimeline?.scopePath),
    projectName: title,
    clips,
    blockers
  });
  const contextView = editingProjectContextFromAssemblyClips(clips, blockers, target, {
    source: "timeline_assembly_preview_timeline",
    scopePath: stringValue12(previewTimeline?.scopePath)
  });
  const composeInputs = buildMediaProjectComposeInputs(mediaEditingProject);
  return {
    schema: "movscript.timeline-assembly-bundle.v1",
    status: blockers.length === 0 ? "ok" : "blocked",
    target_kind: target.targetKind,
    target_ref: target.targetRef,
    scope_kind: target.scopeKind,
    scope_ref: target.scopeRef,
    scope_path: stringValue12(previewTimeline?.scopePath),
    preview_timeline: previewTimeline,
    previewTimeline,
    media_editing_project: mediaEditingProject,
    mediaEditingProject,
    edit_plan: editPlan,
    editPlan,
    context: contextView,
    compose_inputs: composeInputs,
    composeInputs,
    clips,
    blockers
  };
}
function productionTimelineClips(input) {
  const contentUnitsById = new Map(input.contentUnits.map((unit) => [String(unit.id ?? pathSegmentAfter9(unit.path, "content_units") ?? unit.path), unit]));
  const candidatesByContentUnitId = contentCandidateRecordsByContentUnitId4(input.documents);
  const selectionsByContentUnitId = selectionRecordsByContentUnitId4(input.documents);
  return input.previewTimeline.items.filter((item) => item.itemType === "scene_moment").sort((left, right) => left.order - right.order).flatMap((item, index) => {
    const contentUnitIds = productionSceneMomentContentUnitIds2(input.contentUnits, item);
    if (contentUnitIds.length === 0) {
      input.blockers.push({
        code: "scene_moment_content_unit_missing",
        scene_moment_id: item.entity.id,
        scene_moment_path: item.entity.path,
        message: `scene_moment ${String(item.entity.id ?? item.entity.path)} has no scene_moment_ref video content unit`
      });
      return [];
    }
    for (const contentUnitId of contentUnitIds) {
      const contentUnit = contentUnitsById.get(String(contentUnitId));
      const selection = selectionsByContentUnitId.get(String(contentUnitId));
      const candidateId = selection?.candidate_id;
      const candidate = candidateId !== void 0 ? candidatesByContentUnitId.get(String(contentUnitId))?.find((entry) => sameId7(entry.id, candidateId)) : void 0;
      const resourceId = selectedVideoResourceId(candidate);
      if (resourceId !== void 0) {
        const durationSec = optionalNumber2(firstCandidateOutput5(candidate)?.duration_sec) ?? 4;
        return [{
          id: `production_clip_${safeId5(String(item.entity.id ?? index))}_${safeId5(String(contentUnitId))}`,
          sceneMomentId: item.entity.id,
          sceneMomentPath: item.entity.path,
          sceneMomentTitle: previewTimelineItemTitle2(item),
          contentUnitId,
          candidateId,
          resourceId,
          title: previewTimelineItemTitle2(item) ?? String(item.entity.id ?? item.entity.path ?? `Scene ${index + 1}`),
          order: item.order,
          durationSec
        }];
      }
      input.blockers.push({
        code: candidateId === void 0 ? "scene_moment_selection_missing" : "scene_moment_resource_missing",
        scene_moment_id: item.entity.id,
        scene_moment_path: item.entity.path,
        content_unit_id: contentUnitId,
        candidate_id: candidateId,
        output_kind: stringValue12(contentUnit?.record?.output_kind),
        message: candidateId === void 0 ? `scene_moment ${String(item.entity.id ?? item.entity.path)} content unit ${String(contentUnitId)} has no selected candidate` : `scene_moment ${String(item.entity.id ?? item.entity.path)} selected candidate ${String(candidateId)} has no video resource_id`
      });
    }
    return [];
  });
}
function productionSceneMomentContentUnitIds2(contentUnits, item) {
  const fromTimeline = previewTimelineItemContentUnitIds(item);
  const scanned = contentUnits.filter((unit) => recordValue7(unit.record) && isSceneMomentVideoContentUnit2(unit.record) && sceneMomentRefMatches2(unit.record, item)).map((unit) => unit.id ?? pathSegmentAfter9(unit.path, "content_units")).filter((id) => typeof id === "string" || typeof id === "number");
  return Array.from(new Map([...fromTimeline, ...scanned].map((id) => [String(id), id])).values());
}
function previewTimelineItemTitle2(item) {
  return stringValue12(item.title);
}
function previewTimelineItemContentUnitIds(item) {
  return Array.isArray(item.contentUnitIds) ? item.contentUnitIds.filter((id) => typeof id === "string" || typeof id === "number") : [];
}
function isSceneMomentVideoContentUnit2(record) {
  const type = stringValue12(record.content_unit_type ?? record.contentUnitType);
  if (type !== "scene_moment_ref" && type !== "scence_moment_ref") return false;
  const outputKind = stringValue12(record.output_kind ?? record.outputKind);
  return outputKind === void 0 || outputKind === "video";
}
function sceneMomentRefMatches2(record, item) {
  const refs = [record.scene_moment_ref, record.sceneMomentRef, record.scence_moment_ref, record.scenceMomentRef].flatMap((value) => {
    if (typeof value === "number") return [String(value)];
    if (typeof value === "string" && value.trim()) return [value.trim()];
    return [];
  });
  return refs.some(
    (ref) => sameId7(ref, item.entity.id) || ref === item.entity.path || lastPathSegment9(ref) === lastPathSegment9(item.entity.path) || item.entity.path !== void 0 && ref.endsWith(item.entity.path)
  );
}
function contentCandidateRecordsByContentUnitId4(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!document.path.endsWith("/content_candidate.json") || !recordValue7(document.data)) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument5(document.path, stringValue12(document.data.content_unit_ref ?? document.data.contentUnitRef));
    if (!contentUnitId) continue;
    output.set(contentUnitId, [...output.get(contentUnitId) ?? [], document.data]);
  }
  return output;
}
function selectionRecordsByContentUnitId4(documents) {
  const output = /* @__PURE__ */ new Map();
  for (const document of documents) {
    if (!recordValue7(document.data)) continue;
    const selection = recordValue7(document.data.selection);
    if (!selection) continue;
    const contentUnitId = contentUnitIdForRuntimeDocument5(document.path, stringValue12(document.data.target_ref ?? document.data.targetRef));
    if (!contentUnitId) continue;
    output.set(contentUnitId, selection);
  }
  return output;
}
function contentUnitIdForRuntimeDocument5(path, ref) {
  if (ref) return lastPathSegment9(ref) ?? ref;
  return pathSegmentAfter9(path, "content_units");
}
function selectedVideoResourceId(candidate) {
  const output = firstCandidateOutput5(candidate);
  const kind = stringValue12(output?.kind);
  if (kind !== void 0 && kind !== "video") return void 0;
  return optionalNumber2(output?.resource_id ?? output?.resourceId);
}
function firstCandidateOutput5(candidate) {
  const outputs = Array.isArray(candidate?.outputs) ? candidate.outputs : [];
  return outputs.find(recordValue7);
}
function timelineAssemblyScopeFromInput(input) {
  const parsedTarget = parseTimelineAssemblyRef(stringValue12(input.targetRef ?? input.target_ref ?? input.timelineAssemblyRef ?? input.timeline_assembly_ref));
  const legacyProductionId = stringOrNumberValue3(input.productionId ?? input.production_id);
  const scopeKind = stringValue12(input.scopeKind ?? input.scope_kind) ?? parsedTarget?.scopeKind ?? (legacyProductionId !== void 0 ? "production" : void 0);
  const scopeRef = stringOrNumberValue3(input.scopeRef ?? input.scope_ref) ?? parsedTarget?.scopeRef ?? stringOrNumberValue3(input.productionId ?? input.production_id);
  if (!scopeKind || scopeRef === void 0) {
    throw httpError2(400, "editing_timeline_assembly_scope_required", "timelineAssemblyBundle requires scopeRef, targetRef, or legacy productionId");
  }
  return {
    scopeKind,
    scopeRef: String(scopeRef),
    targetKind: "timeline_assembly",
    targetRef: stringValue12(input.targetRef ?? input.target_ref ?? input.timelineAssemblyRef ?? input.timeline_assembly_ref) ?? `timeline_assembly:${scopeKind}:${String(scopeRef)}`
  };
}
function productionTimelineTargetFromInput(productionId, input) {
  const explicitTargetRef = stringValue12(input.targetRef ?? input.target_ref ?? input.timelineAssemblyRef ?? input.timeline_assembly_ref);
  const parsedTarget = parseTimelineAssemblyRef(explicitTargetRef);
  const targetKind = stringValue12(input.targetKind ?? input.target_kind) ?? "timeline_assembly";
  const scopeKind = stringValue12(input.scopeKind ?? input.scope_kind) ?? parsedTarget?.scopeKind ?? (targetKind === "timeline_assembly" ? "production" : void 0);
  const scopeRef = stringOrNumberValue3(input.scopeRef ?? input.scope_ref) ?? parsedTarget?.scopeRef ?? (scopeKind === "production" ? productionId : void 0);
  const targetRef = explicitTargetRef ?? (targetKind === "timeline_assembly" ? `timeline_assembly:${scopeKind ?? "production"}:${String(scopeRef ?? productionId)}` : String(productionId));
  return {
    targetKind,
    targetRef,
    ...scopeKind ? { scopeKind } : {},
    ...scopeRef !== void 0 ? { scopeRef: String(scopeRef) } : {},
    ...targetKind === "timeline_assembly" ? {
      legacyTargetKind: "production",
      legacyTargetRef: String(productionId)
    } : {}
  };
}
function parseTimelineAssemblyRef(value) {
  if (!value?.startsWith("timeline_assembly:")) return void 0;
  const [, scopeKind, ...scopeRefParts] = value.split(":");
  const scopeRef = scopeRefParts.join(":");
  if (!scopeKind?.trim() || !scopeRef.trim()) return void 0;
  return { scopeKind: scopeKind.trim(), scopeRef: scopeRef.trim() };
}
function productionEditPlanFromBundle(input) {
  const target = input.target ?? productionTimelineTargetFromInput(input.productionId, {});
  let cursorSec = 0;
  const items = input.clips.slice().sort((left, right) => left.order - right.order).map((clip, index) => {
    const durationSec = Math.max(0.1, clip.durationSec || 4);
    const item = {
      id: clip.id,
      content_unit_id: clip.contentUnitId,
      content_unit_ref: `content_units/${String(clip.contentUnitId)}`,
      output_kind: "video",
      target_kind: "scene_moment",
      target_ref: clip.sceneMomentPath ?? String(clip.sceneMomentId ?? clip.contentUnitId),
      expression_unit_ref: clip.sceneMomentPath,
      expression_modality: "visual",
      expression_role: "scene_moment_output",
      candidate_id: clip.candidateId,
      resource_id: clip.resourceId,
      selected: true,
      stale: false,
      generation_role: "composed_scene_moment",
      timing_intent: {
        timeline_start_sec: cursorSec,
        duration_sec: durationSec,
        source_duration_sec: durationSec
      },
      order: index + 1
    };
    cursorSec += durationSec;
    return item;
  });
  return {
    schema: "movscript.edit_plan.v1",
    target_kind: target.targetKind,
    productionId: input.productionId,
    productionPath: input.productionPath ?? `productions/${String(input.productionId)}`,
    sceneMomentId: `production_${String(input.productionId)}`,
    sceneMomentPath: input.productionPath ?? `productions/${String(input.productionId)}`,
    target_ref: target.targetRef,
    scope_kind: target.scopeKind,
    scope_ref: target.scopeRef,
    legacy_target_kind: target.legacyTargetKind,
    legacy_target_ref: target.legacyTargetRef,
    status: input.blockers.length === 0 ? "ready_to_compose" : "missing_selection",
    tracks: [{ type: "video", items }],
    compose_inputs: items.map((item) => ({
      content_unit_id: item.content_unit_id,
      resource_id: item.resource_id ?? 0,
      output_kind: "video",
      track_type: "video"
    })).filter((item) => item.resource_id > 0),
    ...input.blockers.length ? {
      blockers: input.blockers.map((blocker) => ({
        code: blocker.code === "selection_stale" || blocker.code === "resource_missing" ? blocker.code : "selection_missing",
        content_unit_id: blockerContentUnitId(blocker),
        message: stringValue12(blocker.message) ?? "Production edit plan is blocked by missing scene_moment output selection."
      }))
    } : {}
  };
}
function timelineAssemblyEditPlanFromBundle(input) {
  const target = input.target;
  let cursorSec = 0;
  const items = input.clips.slice().sort((left, right) => left.order - right.order).map((clip, index) => {
    const durationSec = Math.max(0.1, clip.durationSec || 4);
    const item = {
      id: clip.id,
      content_unit_id: clip.contentUnitId,
      content_unit_ref: `content_units/${String(clip.contentUnitId)}`,
      output_kind: "video",
      target_kind: "scene_moment",
      target_ref: clip.sceneMomentPath ?? String(clip.sceneMomentId ?? clip.contentUnitId),
      expression_unit_ref: clip.sceneMomentPath,
      expression_modality: "visual",
      expression_role: "scene_moment_output",
      candidate_id: clip.candidateId,
      resource_id: clip.resourceId,
      selected: true,
      stale: false,
      generation_role: "composed_scene_moment",
      timing_intent: {
        timeline_start_sec: cursorSec,
        duration_sec: durationSec,
        source_duration_sec: durationSec
      },
      order: index + 1
    };
    cursorSec += durationSec;
    return item;
  });
  return {
    schema: "movscript.edit_plan.v1",
    target_kind: target.targetKind,
    target_ref: target.targetRef,
    scope_kind: target.scopeKind,
    scope_ref: target.scopeRef,
    scope_path: input.scopePath,
    assemblyId: target.targetRef,
    assemblyTitle: input.projectName,
    status: input.blockers.length === 0 ? "ready_to_compose" : "missing_selection",
    tracks: [{ type: "video", items }],
    compose_inputs: items.map((item) => ({
      content_unit_id: item.content_unit_id,
      resource_id: item.resource_id ?? 0,
      output_kind: "video",
      track_type: "video"
    })).filter((item) => item.resource_id > 0),
    ...input.blockers.length ? {
      blockers: input.blockers.map((blocker) => ({
        code: blocker.code === "selection_stale" || blocker.code === "resource_missing" ? blocker.code : "selection_missing",
        content_unit_id: blockerContentUnitId(blocker),
        message: stringValue12(blocker.message) ?? "Timeline assembly edit plan is blocked by missing scene_moment output selection."
      }))
    } : {}
  };
}
function blockerContentUnitId(blocker) {
  const value = blocker.content_unit_id ?? blocker.contentUnitId;
  if (typeof value === "string" || typeof value === "number") return value;
  return "unknown";
}
function editingProjectContextFromEditPlan(editPlan) {
  const tracks = Array.isArray(editPlan.tracks) ? editPlan.tracks : [];
  const items = tracks.flatMap((track) => (Array.isArray(track.items) ? track.items : []).map((item) => ({ track, item })));
  return {
    production_id: editPlan.productionId,
    production_path: editPlan.productionPath,
    scene_moment_id: editPlan.sceneMomentId,
    scene_moment_path: editPlan.sceneMomentPath,
    target_ref: editPlan.target_ref,
    selected_content_units: uniqueBy(items.map(({ item }) => ({
      content_unit_id: item.content_unit_id,
      content_unit_ref: item.content_unit_ref,
      output_kind: item.output_kind,
      target_kind: item.target_kind,
      target_ref: item.target_ref
    })), (item) => String(item.content_unit_id)),
    selected_candidates: items.flatMap(({ item }) => item.candidate_id === void 0 ? [] : [{
      content_unit_id: item.content_unit_id,
      candidate_id: item.candidate_id,
      resource_id: item.resource_id,
      selected: item.selected,
      stale: item.stale
    }]),
    resources: items.flatMap(({ track, item }) => item.resource_id === void 0 ? [] : [{
      resource_id: item.resource_id,
      content_unit_id: item.content_unit_id,
      candidate_id: item.candidate_id,
      output_kind: item.output_kind,
      track_type: track.type
    }]),
    provenance: {
      source: "movscript_edit_plan",
      selected_candidate_ids: items.flatMap(({ item }) => item.candidate_id === void 0 ? [] : [String(item.candidate_id)]),
      input_resource_ids: items.flatMap(({ item }) => item.resource_id === void 0 ? [] : [item.resource_id])
    }
  };
}
function editingProjectContextFromProductionClips(productionId, clips, blockers, target = productionTimelineTargetFromInput(productionId, {})) {
  return {
    target_kind: target.targetKind,
    target_ref: target.targetRef,
    scope_kind: target.scopeKind,
    scope_ref: target.scopeRef,
    legacy_alias: target.legacyTargetKind ? {
      target_kind: target.legacyTargetKind,
      target_ref: target.legacyTargetRef
    } : void 0,
    production_id: productionId,
    selected_content_units: uniqueBy(clips.map((clip) => ({
      content_unit_id: clip.contentUnitId,
      scene_moment_id: clip.sceneMomentId,
      scene_moment_path: clip.sceneMomentPath,
      output_kind: "video",
      target_kind: target.targetKind,
      target_ref: target.targetRef,
      scope_kind: target.scopeKind,
      scope_ref: target.scopeRef
    })), (item) => String(item.content_unit_id)),
    selected_candidates: clips.flatMap((clip) => clip.candidateId === void 0 ? [] : [{
      content_unit_id: clip.contentUnitId,
      candidate_id: clip.candidateId,
      resource_id: clip.resourceId,
      selected: true,
      stale: false
    }]),
    resources: clips.map((clip) => ({
      resource_id: clip.resourceId,
      content_unit_id: clip.contentUnitId,
      candidate_id: clip.candidateId,
      output_kind: "video",
      track_type: "video"
    })),
    blockers,
    provenance: {
      source: "production_preview_timeline",
      target_kind: target.targetKind,
      target_ref: target.targetRef,
      scope_kind: target.scopeKind,
      scope_ref: target.scopeRef,
      legacy_target_kind: target.legacyTargetKind,
      legacy_target_ref: target.legacyTargetRef,
      selected_candidate_ids: clips.flatMap((clip) => clip.candidateId === void 0 ? [] : [String(clip.candidateId)]),
      input_resource_ids: clips.map((clip) => clip.resourceId)
    }
  };
}
function editingProjectContextFromAssemblyClips(clips, blockers, target, options = {}) {
  return {
    target_kind: target.targetKind,
    target_ref: target.targetRef,
    scope_kind: target.scopeKind,
    scope_ref: target.scopeRef,
    scope_path: options.scopePath,
    selected_content_units: uniqueBy(clips.map((clip) => ({
      content_unit_id: clip.contentUnitId,
      scene_moment_id: clip.sceneMomentId,
      scene_moment_path: clip.sceneMomentPath,
      output_kind: "video",
      target_kind: target.targetKind,
      target_ref: target.targetRef,
      scope_kind: target.scopeKind,
      scope_ref: target.scopeRef
    })), (item) => String(item.content_unit_id)),
    selected_candidates: clips.flatMap((clip) => clip.candidateId === void 0 ? [] : [{
      content_unit_id: clip.contentUnitId,
      candidate_id: clip.candidateId,
      resource_id: clip.resourceId,
      selected: true,
      stale: false
    }]),
    resources: clips.map((clip) => ({
      resource_id: clip.resourceId,
      content_unit_id: clip.contentUnitId,
      candidate_id: clip.candidateId,
      output_kind: "video",
      track_type: "video"
    })),
    blockers,
    provenance: {
      source: options.source ?? "timeline_assembly_preview_timeline",
      target_kind: target.targetKind,
      target_ref: target.targetRef,
      scope_kind: target.scopeKind,
      scope_ref: target.scopeRef,
      scope_path: options.scopePath,
      selected_candidate_ids: clips.flatMap((clip) => clip.candidateId === void 0 ? [] : [String(clip.candidateId)]),
      input_resource_ids: clips.map((clip) => clip.resourceId)
    }
  };
}
function buildMediaProjectComposeInputs(project) {
  return mediaProjectVideoItems(project).map((input) => ({
    resource_id: input.resource_id,
    ...optionalNumber2(input.start_sec) !== void 0 ? { start_sec: input.start_sec } : {},
    ...optionalNumber2(input.end_sec) !== void 0 ? { end_sec: input.end_sec } : {},
    ...optionalNumber2(input.duration_sec) !== void 0 ? { duration_sec: input.duration_sec } : {},
    trim_start_sec: input.trim_start_sec,
    trim_end_sec: input.trim_end_sec,
    timeline_start_sec: input.timeline_start_sec,
    timeline_duration_sec: input.timeline_duration_sec,
    clip_id: input.clip_id,
    track_id: input.track_id,
    content_unit_id: input.content_unit_id
  }));
}
function mediaProjectVideoItems(project) {
  return project.timeline.tracks.filter((track) => (track.type === "video" || track.type === "image") && track.locked !== true).flatMap((track) => track.clips.map((clip) => ({ track, clip }))).filter(({ clip }) => clip.assetType === "video" || clip.assetType === "image").map(({ track, clip }) => mediaProjectVideoItem(track, clip)).filter((item) => item !== void 0).sort((left, right) => (optionalNumber2(left.timeline_start_sec) ?? 0) - (optionalNumber2(right.timeline_start_sec) ?? 0) || String(left.clip_id).localeCompare(String(right.clip_id)));
}
function mediaProjectVideoItem(track, clip) {
  const asset = clip.asset;
  const resourceId = optionalNumber2(asset?.resourceId);
  if (resourceId === void 0) return void 0;
  const startMs = msValue2(clip.sourceStartMs) ?? 0;
  const durationMs = msValue2(clip.durationMs) ?? Math.max(1, (msValue2(clip.sourceEndMs) ?? startMs + 4e3) - startMs);
  const endMs = msValue2(clip.sourceEndMs) ?? startMs + durationMs;
  const movscript = recordValue7(clip.metadata?.movscript);
  return {
    resource_id: resourceId,
    start_sec: startMs / 1e3,
    end_sec: endMs / 1e3,
    duration_sec: durationMs / 1e3,
    trim_start_sec: startMs / 1e3,
    trim_end_sec: Math.max(0, endMs - startMs - durationMs) / 1e3,
    timeline_start_sec: (msValue2(clip.timelineStartMs) ?? 0) / 1e3,
    timeline_duration_sec: durationMs / 1e3,
    clip_id: clip.id,
    track_id: track.id,
    ...movscript?.contentUnitId !== void 0 ? { content_unit_id: movscript.contentUnitId } : {}
  };
}
function uniqueBy(items, keyOf) {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  for (const item of items) {
    const key = keyOf(item);
    if (seen.has(key)) continue;
    seen.add(key);
    output.push(item);
  }
  return output;
}
function startEditingService(options = {}) {
  const host = options.host ?? "127.0.0.1";
  const port = Number(options.port ?? 0);
  const server = createServer3(createEditingServiceHandler(options));
  return new Promise((resolve12, reject) => {
    server.once("error", reject);
    server.listen(port, host, () => {
      server.off("error", reject);
      const address = server.address();
      const actualPort = typeof address === "object" && address ? address.port : port;
      resolve12({
        server,
        host,
        port: actualPort,
        url: `http://${host}:${actualPort}`,
        close: () => new Promise((closeResolve, closeReject) => {
          server.close((error) => error ? closeReject(error) : closeResolve());
        })
      });
    });
  });
}
async function runEditingServiceCLI(argv = [], env = process.env) {
  const command = argv[0] ?? "serve";
  if (command !== "serve") {
    throw new Error(`unsupported editing-service command: ${command}`);
  }
  const host = env.MOVSCRIPT_EDITING_SERVICE_HOST || "127.0.0.1";
  const port = Number(env.MOVSCRIPT_EDITING_SERVICE_PORT || env.PORT || 0);
  const runtime = await startEditingService({ host, port });
  process.stdout.write(JSON.stringify({
    serviceName: EDITING_SERVICE_NAME,
    url: runtime.url
  }) + "\n");
  await waitForShutdown3(runtime);
}
function writeJSON3(response, statusCode, payload) {
  response.writeHead(statusCode, {
    "content-type": "application/json; charset=utf-8"
  });
  response.end(JSON.stringify(payload));
}
function writeCORSHeaders2(response, origin) {
  response.setHeader("Access-Control-Allow-Origin", typeof origin === "string" && origin ? origin : "*");
  response.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
  response.setHeader("Access-Control-Allow-Headers", "Content-Type, Authorization");
  response.setHeader("Access-Control-Allow-Credentials", "false");
  response.setHeader("Vary", "Origin");
}
async function executeEditingProjectCommand(command, input, context = {}) {
  switch (command) {
    case "createProject":
      return createProject2(input);
    case "createProjectFromEditPlan":
      return createProjectFromEditPlan(input);
    case "createProjectFromEditDecisions":
      return createProjectFromEditDecisions(input);
    case "createProjectFromPreviewTimeline":
      return createProjectFromPreviewTimeline(input);
    case "saveProject":
      return saveEditingProject(input, context);
    case "getProject":
      return getEditingProject(input, context);
    case "listProjects":
      return listEditingProjects(input, context);
    case "deleteProject":
      return deleteEditingProject(input, context);
    case "updateProjectSettings":
      return updateProjectSettings(input);
    case "addAsset":
      return addAsset(input);
    case "removeAsset":
      return removeAsset(input);
    case "applyTimelineCommands":
      return applyTimelineCommands(input);
    case "validateTimeline":
      return validateTimeline(input);
    case "addTrack": {
      const track = mediaTrackArg(input);
      return applySingleTimelineCommand(input, {
        type: "add_track",
        track
      }, (editingProject) => ({
        status: "ok",
        track_id: track.id,
        track,
        editing_project: editingProject
      }));
    }
    case "removeTrack":
      return applySingleTimelineCommand(input, {
        type: "remove_track",
        trackId: trackIdValue(input)
      }, (editingProject) => ({
        status: "ok",
        track_id: trackIdValue(input),
        editing_project: editingProject
      }));
    case "addClip":
      return addClip(input);
    case "updateClip":
      return applySingleTimelineCommand(input, {
        type: "update_clip",
        clipId: clipIdValue(input),
        patch: mediaClipPatchArg(input)
      }, (editingProject) => ({
        status: "ok",
        clip_id: clipIdValue(input),
        clip: findClip(editingProject, clipIdValue(input))?.clip,
        editing_project: editingProject
      }));
    case "splitClip":
      return applySingleTimelineCommand(input, {
        type: "split_clip",
        clipId: clipIdValue(input),
        splitTimeMs: requiredNumeric(input, "splitTimeMs", "split_time_ms"),
        ...retainSideValue(input.retainSide ?? input.retain_side) ? { retainSide: retainSideValue(input.retainSide ?? input.retain_side) } : {}
      }, (editingProject) => ({
        status: "ok",
        clip_id: clipIdValue(input),
        clips: relatedSplitClips(editingProject, clipIdValue(input)),
        editing_project: editingProject
      }));
    case "moveClip":
      return applySingleTimelineCommand(input, {
        type: "move_clip",
        clipId: clipIdValue(input),
        timelineStartMs: requiredNumeric(input, "timelineStartMs", "timeline_start_ms"),
        ...stringValue12(input.targetTrackId ?? input.target_track_id) ? { targetTrackId: stringValue12(input.targetTrackId ?? input.target_track_id) } : {}
      }, (editingProject) => ({
        status: "ok",
        clip_id: clipIdValue(input),
        clip: findClip(editingProject, clipIdValue(input))?.clip,
        editing_project: editingProject
      }));
    case "deleteClip":
      return applySingleTimelineCommand(input, {
        type: "delete_clip",
        clipId: clipIdValue(input)
      }, (editingProject) => ({
        status: "ok",
        clip_id: clipIdValue(input),
        editing_project: editingProject
      }));
    default:
      throw httpError2(400, "editing_project_command_unsupported", `unsupported editing project command: ${command}`);
  }
}
async function saveEditingProject(input, context) {
  const project = editingProjectArg2(input);
  const expectedRevision = optionalNumber2(input.expectedRevision ?? input.expected_revision);
  const projectPath = editingProjectPath(context.homeDir, project.id);
  if (expectedRevision !== void 0) {
    const current = await readEditingProject(projectPath);
    const currentRevision = optionalNumber2(current?.revision);
    if (current && currentRevision !== expectedRevision) {
      return {
        status: "conflict",
        code: "EDITING_PROJECT_REVISION_CONFLICT",
        message: `Editing project revision conflict: expected ${expectedRevision}, found ${currentRevision ?? "unknown"}`,
        projectId: String(project.projectId ?? "standalone"),
        project_id: String(project.projectId ?? "standalone"),
        editingProjectId: String(project.id),
        editing_project_id: String(project.id),
        expectedRevision,
        expected_revision: expectedRevision,
        ...currentRevision !== void 0 ? { currentRevision, current_revision: currentRevision } : {},
        editingProject: current,
        editing_project: current,
        projectPath,
        project_path: projectPath
      };
    }
  }
  await mkdir4(dirname7(projectPath), { recursive: true });
  await writeFile4(projectPath, `${JSON.stringify({
    schema: "movscript.editing_service_project.v1",
    editingProject: project
  }, null, 2)}
`, "utf8");
  return {
    status: "ok",
    editingProject: project,
    editing_project: project,
    projectPath,
    project_path: projectPath
  };
}
async function getEditingProject(input, context) {
  const editingProjectId = editingProjectIdValue2(input);
  const projectPath = editingProjectPath(context.homeDir, editingProjectId);
  const project = await readEditingProject(projectPath);
  if (!project) {
    const projectId = projectIdValue2(input) ?? "standalone";
    return {
      status: "not_found",
      projectId,
      project_id: projectId,
      editingProjectId,
      editing_project_id: editingProjectId,
      projectPath,
      project_path: projectPath
    };
  }
  return {
    status: "ok",
    editingProject: project,
    editing_project: project,
    projectPath,
    project_path: projectPath
  };
}
async function listEditingProjects(_input, context) {
  const root = editingProjectStoreRoot(context.homeDir);
  let entries = [];
  try {
    entries = await readdir3(root, { withFileTypes: true });
  } catch {
    return {
      status: "ok",
      projects: [],
      editingProjects: [],
      editing_projects: []
    };
  }
  const projects = [];
  for (const entry of entries) {
    if (!entry.isFile() || !entry.name.endsWith(".json")) continue;
    const projectPath = join9(root, entry.name);
    const project = await readEditingProject(projectPath);
    if (!project) continue;
    projects.push({
      status: "ok",
      editingProject: project,
      editing_project: project,
      projectPath,
      project_path: projectPath
    });
  }
  projects.sort((left, right) => {
    const leftTime = Date.parse(stringValue12(left.editingProject.updatedAt) ?? "");
    const rightTime = Date.parse(stringValue12(right.editingProject.updatedAt) ?? "");
    const leftSort = Number.isFinite(leftTime) ? leftTime : 0;
    const rightSort = Number.isFinite(rightTime) ? rightTime : 0;
    return rightSort - leftSort || String(left.editingProject.id).localeCompare(String(right.editingProject.id));
  });
  const editingProjects = projects.map((project) => project.editingProject);
  return {
    status: "ok",
    projects,
    editingProjects,
    editing_projects: editingProjects
  };
}
async function deleteEditingProject(input, context) {
  const editingProjectId = editingProjectIdValue2(input);
  const projectId = projectIdValue2(input) ?? "standalone";
  const projectPath = editingProjectPath(context.homeDir, editingProjectId);
  try {
    await unlink(projectPath);
    return {
      status: "ok",
      projectId,
      project_id: projectId,
      editingProjectId,
      editing_project_id: editingProjectId,
      projectPath,
      project_path: projectPath
    };
  } catch {
    return {
      status: "not_found",
      projectId,
      project_id: projectId,
      editingProjectId,
      editing_project_id: editingProjectId,
      projectPath,
      project_path: projectPath
    };
  }
}
async function readEditingProject(projectPath) {
  try {
    const raw = JSON.parse(await readFile4(projectPath, "utf8"));
    const project = recordValue7(raw.editingProject ?? raw.editing_project ?? raw.project);
    if (!project) return void 0;
    assertMediaEditingProjectEnvelope2(project);
    return project;
  } catch {
    return void 0;
  }
}
function editingProjectPath(homeDir, editingProjectId) {
  return join9(editingProjectStoreRoot(homeDir), `${stableEditingProjectPathPart(editingProjectId)}.json`);
}
function editingProjectStoreRoot(homeDir) {
  return join9(homeDir, "editing-service", "projects");
}
function stableEditingProjectPathPart(value) {
  return String(value).trim().replace(/[^a-zA-Z0-9._-]+/g, "_").replace(/^_+|_+$/g, "") || "editing_project";
}
function createProject2(input) {
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const projectId = projectIdValue2(input) ?? "standalone";
  const width = optionalNumber2(input.width) ?? 1080;
  const height = optionalNumber2(input.height) ?? 1920;
  const fps = optionalNumber2(input.fps) ?? 30;
  const background = stringValue12(input.background) ?? "#000000";
  const editingProject = {
    version: 1,
    id: stringValue12(input.id ?? input.editingProjectId ?? input.editing_project_id) ?? `editing_project_${Date.now()}`,
    projectId,
    title: stringValue12(input.title) ?? "Untitled edit",
    source: { kind: "manual" },
    timeline: {
      version: 1,
      id: stringValue12(input.timelineId ?? input.timeline_id) ?? `timeline_${Date.now()}`,
      fps,
      width,
      height,
      background,
      durationMs: 0,
      tracks: []
    },
    assets: { assets: [] },
    createdAt: now,
    updatedAt: now,
    revision: 1
  };
  return {
    status: "ok",
    editing_project: editingProject
  };
}
function createProjectFromEditPlan(input) {
  const editPlan = recordValue7(input.editPlan) ?? recordValue7(input.edit_plan);
  if (!editPlan) throw new Error("editPlan is required");
  const editingProject = createMediaEditingProjectFromMovScriptEditPlan(editPlan, {
    id: stringValue12(input.id ?? input.editingProjectId ?? input.editing_project_id),
    projectId: projectIdValue2(input) ?? "standalone",
    title: stringValue12(input.title),
    width: optionalNumber2(input.width),
    height: optionalNumber2(input.height),
    fps: optionalNumber2(input.fps),
    background: stringValue12(input.background),
    defaultDurationMs: optionalNumber2(input.defaultDurationMs ?? input.default_duration_ms)
  });
  return {
    status: "ok",
    editing_project: editingProject
  };
}
function createProjectFromEditDecisions(input) {
  const editDecisions = recordValue7(input.editDecisions) ?? recordValue7(input.edit_decisions);
  if (!editDecisions) throw new Error("editDecisions is required");
  const assetManifest = recordValue7(input.assetManifest) ?? recordValue7(input.asset_manifest);
  const timelineAssembly = recordValue7(input.timelineAssembly) ?? recordValue7(input.timeline_assembly);
  const suppliedCompileManifest = recordValue7(input.compileManifest) ?? recordValue7(input.compile_manifest);
  const projectOptions = {
    id: stringValue12(input.id ?? input.editingProjectId ?? input.editing_project_id),
    projectId: projectIdValue2(input) ?? "standalone",
    title: stringValue12(input.title),
    now: stringValue12(input.now),
    width: optionalNumber2(input.width),
    height: optionalNumber2(input.height),
    fps: optionalNumber2(input.fps),
    background: stringValue12(input.background),
    defaultDurationMs: optionalNumber2(input.defaultDurationMs ?? input.default_duration_ms),
    productionId: stringOrNumberValue3(input.productionId ?? input.production_id),
    productionPath: stringValue12(input.productionPath ?? input.production_path),
    targetKind: stringValue12(input.targetKind ?? input.target_kind),
    targetRef: stringValue12(input.targetRef ?? input.target_ref),
    scopeKind: stringValue12(input.scopeKind ?? input.scope_kind),
    scopeRef: stringOrNumberValue3(input.scopeRef ?? input.scope_ref),
    sourceHash: stringValue12(input.sourceHash ?? input.source_hash)
  };
  if (timelineAssembly || suppliedCompileManifest) {
    const compileResult = compileTimelineAssemblyToMediaEditingProject({
      timelineAssembly,
      assetManifest,
      editDecisions,
      renderRuntime: stringValue12(input.renderRuntime ?? input.render_runtime ?? editDecisions.render_runtime ?? editDecisions.renderRuntime),
      runtimeLocked: booleanValue3(input.runtimeLocked ?? input.runtime_locked) ?? true,
      now: stringValue12(input.now),
      renderSettings: {
        width: optionalNumber2(input.width),
        height: optionalNumber2(input.height),
        fps: optionalNumber2(input.fps),
        background: stringValue12(input.background),
        default_duration_ms: optionalNumber2(input.defaultDurationMs ?? input.default_duration_ms)
      },
      projectOptions
    });
    if (compileResult.status === "blocked" || !compileResult.media_editing_project) {
      return {
        status: "blocked",
        code: "TIMELINE_ASSEMBLY_COMPILE_BLOCKED",
        message: "TimelineAssembly compile did not create a MediaEditingProject because the compile manifest has blockers.",
        compile_manifest: compileResult.compile_manifest,
        compileManifest: compileResult.compile_manifest,
        compile_result: compileResult,
        compileResult,
        diagnostics: compileResult.diagnostics
      };
    }
    return {
      status: "ok",
      editing_project: compileResult.media_editing_project,
      compile_manifest: compileResult.compile_manifest,
      compileManifest: compileResult.compile_manifest,
      compile_result: compileResult,
      compileResult
    };
  }
  const editingProject = createMediaEditingProjectFromEditDecisions(editDecisions, {
    ...projectOptions,
    assetManifest
  });
  return {
    status: "ok",
    editing_project: editingProject
  };
}
function createProjectFromPreviewTimeline(input) {
  const productionId = stringOrNumberValue3(input.productionId ?? input.production_id);
  if (productionId === void 0) throw new Error("productionId is required");
  const clips = productionTimelineClipsArg(input);
  const editingProject = createMediaEditingProjectFromProductionTimelineClips({
    productionId,
    productionPath: stringValue12(input.productionPath ?? input.production_path),
    clips,
    id: stringValue12(input.id ?? input.editingProjectId ?? input.editing_project_id),
    projectId: projectIdValue2(input),
    title: stringValue12(input.title ?? input.projectName ?? input.project_name),
    now: stringValue12(input.now),
    width: optionalNumber2(input.width),
    height: optionalNumber2(input.height),
    fps: optionalNumber2(input.fps),
    background: stringValue12(input.background),
    defaultDurationMs: optionalNumber2(input.defaultDurationMs ?? input.default_duration_ms)
  });
  return {
    status: "ok",
    editing_project: editingProject
  };
}
function updateProjectSettings(input) {
  const next = cloneProject(editingProjectArg2(input));
  const title = stringValue12(input.title);
  const width = optionalNumber2(input.width);
  const height = optionalNumber2(input.height);
  const fps = optionalNumber2(input.fps);
  const background = stringValue12(input.background);
  const workspace = recordValue7(input.workspace);
  if (title) next.title = title;
  if (width !== void 0) next.timeline.width = width;
  if (height !== void 0) next.timeline.height = height;
  if (fps !== void 0) next.timeline.fps = fps;
  if (background) next.timeline.background = background;
  if (workspace) next.workspace = workspace;
  touchProject(next);
  return {
    status: "ok",
    editing_project: next
  };
}
function addAsset(input) {
  const project = editingProjectArg2(input);
  const asset = mediaAssetArg(input);
  const next = cloneProject(project);
  if (next.assets.assets.some((candidate) => candidate.id === asset.id)) {
    throw new Error(`Media asset already exists: ${asset.id}`);
  }
  next.assets.assets.push(asset);
  next.assets.assets.sort((left, right) => left.id.localeCompare(right.id));
  touchProject(next);
  return {
    status: "ok",
    asset,
    media_asset: asset,
    editing_project: next
  };
}
function removeAsset(input) {
  const project = editingProjectArg2(input);
  const assetId = assetIdValue(input);
  const referenced = project.timeline.tracks.flatMap((track) => track.clips.map((clip) => ({ track, clip }))).find(({ clip }) => clip.asset?.id === assetId);
  if (referenced) {
    throw new Error(`Cannot remove media asset ${assetId}; it is still referenced by clip ${referenced.clip.id} on track ${referenced.track.id}`);
  }
  const next = cloneProject(project);
  const before = next.assets.assets.length;
  next.assets.assets = next.assets.assets.filter((asset) => asset.id !== assetId);
  if (next.assets.assets.length === before) {
    return {
      status: "not_found",
      asset_id: assetId,
      editing_project: next
    };
  }
  touchProject(next);
  return {
    status: "ok",
    asset_id: assetId,
    editing_project: next
  };
}
function applyTimelineCommands(input) {
  const project = editingProjectArg2(input);
  const commands = commandList(input);
  const service2 = createMediaEditingProjectService(project);
  for (const command of commands) service2.applyCommand(command);
  return {
    status: "ok",
    editing_project: service2.getProject(),
    applied_count: commands.length
  };
}
function validateTimeline(input) {
  const project = editingProjectArg2(input);
  const diagnostics = validateMediaEditingProjectTimeline(project);
  return {
    status: diagnostics.some((diagnostic2) => diagnostic2.severity === "error") ? "diagnostics" : "ok",
    valid: diagnostics.every((diagnostic2) => diagnostic2.severity !== "error"),
    diagnostics
  };
}
function buildEditingTaskRequest(taskType, input) {
  switch (taskType) {
    case "timeline_render":
      return buildTimelineTaskRequest(input, "timeline_render", "mp4");
    case "timeline_hls":
      return buildTimelineTaskRequest(input, "timeline_hls", "hls");
    case "media_transcode":
      return buildSourceTaskRequest(input, "media_transcode");
    case "media_reframe":
      return buildSourceTaskRequest(input, "media_reframe");
    default:
      throw httpError2(400, "editing_task_type_unsupported", `unsupported editing task request type: ${taskType}`);
  }
}
function buildEditingTaskAction(action, input) {
  switch (action) {
    case "getTask":
    case "cancelTask":
    case "getTaskLogs": {
      const taskId = taskIdValue(input);
      const projectId = projectIdValue2(input);
      return {
        status: "ready",
        request: {
          action,
          taskId,
          task_id: taskId,
          options: {
            ...projectId ? { projectId, project_id: projectId } : {}
          }
        }
      };
    }
    case "importExportResource":
      return buildImportExportResourceAction(input);
    case "saveLocalExport":
      return buildSaveLocalExportAction(input);
    case "publishHlsStream":
      return buildPublishHlsStreamAction(input);
    default:
      throw httpError2(400, "editing_task_action_unsupported", `unsupported editing task action: ${action}`);
  }
}
function buildImportExportResourceAction(input) {
  const taskId = stringValue12(input.taskId ?? input.task_id);
  const task = recordValue7(input.task);
  const explicitOutputPath = stringValue12(input.outputPath ?? input.output_path);
  if (taskId && !task && !explicitOutputPath) {
    return {
      status: "not_found",
      result: {
        status: "not_found",
        task_id: taskId
      }
    };
  }
  const outputPath = explicitOutputPath ?? stringValue12(task?.outputPath);
  if (!outputPath) {
    if (taskId) {
      return {
        status: "pending_output",
        result: {
          status: "pending_output",
          task_id: taskId,
          ...task ? { task } : {}
        }
      };
    }
    throw new Error("outputPath or taskId is required");
  }
  if (isHlsTaskOutput(task, outputPath)) {
    return {
      status: "unsupported_output",
      result: {
        status: "unsupported_output",
        code: "USE_EDITING_EXPORT_PUBLISH_HLS",
        message: "Output is an HLS manifest. Use editing_export_publish_hls for HLS artifacts instead of importing it as a RawResource.",
        task_id: taskId,
        outputPath,
        output_path: outputPath,
        ...task ? { task } : {}
      }
    };
  }
  const outputName = stringValue12(input.filename) ?? stringValue12(task?.outputName);
  const mimeType = stringValue12(input.mimeType ?? input.mime_type);
  const folderId = stringOrNumberValue3(input.folderId ?? input.folder_id);
  return {
    status: "ready",
    request: {
      outputPath,
      output_path: outputPath,
      ...outputName ? { filename: outputName } : {},
      ...mimeType ? { mimeType, mime_type: mimeType } : {},
      ...folderId !== void 0 ? { folderId, folder_id: folderId } : {},
      ...exportImportDerivativeRequest(input)
    }
  };
}
function buildSaveLocalExportAction(input) {
  const explicitOutputPath = stringValue12(input.outputPath ?? input.output_path);
  const savePath = stringValue12(input.savePath ?? input.save_path ?? input.destinationPath ?? input.destination_path);
  const saveDirectory = stringValue12(input.saveDirectory ?? input.save_directory ?? input.destinationDirectory ?? input.destination_directory);
  const taskId = stringValue12(input.taskId ?? input.task_id);
  const projectId = projectIdValue2(input);
  const task = recordValue7(input.task);
  if (explicitOutputPath && !savePath && !saveDirectory) {
    return {
      status: "result",
      result: {
        status: "ok",
        outputPath: explicitOutputPath,
        output_path: explicitOutputPath,
        persisted: true,
        uploaded: false,
        candidate_created: false
      }
    };
  }
  if (!explicitOutputPath && !taskId) throw new Error("outputPath or taskId is required");
  if (taskId && !task && !explicitOutputPath) {
    return {
      status: "not_found",
      result: {
        status: "not_found",
        task_id: taskId
      }
    };
  }
  const outputPath = explicitOutputPath ?? stringValue12(task?.outputPath ?? task?.hlsManifestPath ?? task?.hls_manifest_path);
  if (!outputPath) {
    return {
      status: "pending_output",
      result: {
        status: "pending_output",
        task_id: taskId,
        ...task ? { task } : {}
      }
    };
  }
  if (isHlsTaskOutput(task, outputPath)) {
    if (saveDirectory) {
      const segmentPaths = stringList2(input.segmentPaths ?? input.segment_paths) ?? stringList2(task?.hlsSegmentPaths ?? task?.hls_segment_paths);
      const hlsDirectory = stringValue12(input.hlsDirectory ?? input.hls_directory) ?? stringValue12(task?.hlsDirectory ?? task?.hls_directory);
      return {
        status: "ready",
        request: {
          outputPath,
          output_path: outputPath,
          ...projectId ? { projectId, project_id: projectId } : {},
          ...taskId ? { taskId, task_id: taskId } : {},
          saveDirectory,
          save_directory: saveDirectory,
          ...hlsDirectory ? { hlsDirectory, hls_directory: hlsDirectory } : {},
          ...segmentPaths ? { segmentPaths, segment_paths: segmentPaths } : {},
          ...stringValue12(input.filename) ?? stringValue12(task?.outputName) ? { filename: stringValue12(input.filename) ?? stringValue12(task?.outputName) } : {}
        }
      };
    }
    return {
      status: "unsupported_output",
      result: {
        status: "unsupported_output",
        code: "USE_EDITING_EXPORT_PUBLISH_HLS",
        message: "Output is an HLS manifest. Use saveDirectory to save the complete HLS bundle locally, or use editing_export_publish_hls for hosted HLS artifacts.",
        task_id: taskId,
        outputPath,
        output_path: outputPath,
        ...task ? { task } : {}
      }
    };
  }
  if (saveDirectory) throw new Error("saveDirectory is only supported for HLS manifest outputs");
  if (savePath) {
    return {
      status: "ready",
      request: {
        outputPath,
        output_path: outputPath,
        ...projectId ? { projectId, project_id: projectId } : {},
        ...taskId ? { taskId, task_id: taskId } : {},
        savePath,
        save_path: savePath,
        ...stringValue12(input.filename) ?? stringValue12(task?.outputName) ? { filename: stringValue12(input.filename) ?? stringValue12(task?.outputName) } : {}
      }
    };
  }
  return {
    status: "result",
    result: {
      status: "ok",
      task_id: taskId,
      outputPath,
      output_path: outputPath,
      persisted: true,
      uploaded: false,
      candidate_created: false,
      ...task ? { task } : {}
    }
  };
}
function buildPublishHlsStreamAction(input) {
  const taskId = stringValue12(input.taskId ?? input.task_id);
  const task = recordValue7(input.task);
  const hasExplicitManifest = !!stringValue12(input.manifestPath ?? input.manifest_path);
  const hasExplicitSegments = !!stringList2(input.segmentPaths ?? input.segment_paths)?.length;
  if (taskId && !task && !hasExplicitManifest) {
    return {
      status: "not_found",
      result: {
        status: "not_found",
        task_id: taskId,
        message: "No Electron mediaPipeline task was found for taskId. Pass projectId with taskId for persisted workspace recovery, or provide manifestPath and segmentPaths explicitly."
      }
    };
  }
  const manifestPath = stringValue12(input.manifestPath ?? input.manifest_path) ?? stringValue12(task?.hlsManifestPath ?? task?.hls_manifest_path ?? task?.outputPath);
  const segmentPaths = stringList2(input.segmentPaths ?? input.segment_paths) ?? stringList2(task?.hlsSegmentPaths ?? task?.hls_segment_paths);
  if (taskId && task && (!manifestPath || !segmentPaths?.length) && !hasExplicitManifest && !hasExplicitSegments) {
    return {
      status: "pending_output",
      result: {
        status: "pending_output",
        task_id: taskId,
        message: "The Electron mediaPipeline task does not have a complete HLS manifest/segment output yet.",
        task
      }
    };
  }
  if (!manifestPath) throw new Error("manifestPath is required");
  if (!segmentPaths?.length) throw new Error("segmentPaths is required");
  const title = stringValue12(input.title);
  const projectId = stringOrNumberValue3(input.projectId ?? input.project_id);
  const sourceResourceId2 = stringOrNumberValue3(input.sourceResourceId ?? input.source_resource_id);
  const sourceDerivativeId = stringOrNumberValue3(input.sourceDerivativeId ?? input.source_derivative_id);
  const durationMs = optionalNumber2(input.durationMs ?? input.duration_ms);
  const width = optionalNumber2(input.width);
  const height = optionalNumber2(input.height);
  return {
    status: "ready",
    request: {
      manifestPath,
      manifest_path: manifestPath,
      segmentPaths,
      segment_paths: segmentPaths,
      ...taskId ? { taskId, task_id: taskId } : {},
      ...title ? { title } : {},
      ...projectId !== void 0 ? { projectId, project_id: projectId } : {},
      ...sourceResourceId2 !== void 0 ? { sourceResourceId: sourceResourceId2, source_resource_id: sourceResourceId2 } : {},
      ...sourceDerivativeId !== void 0 ? { sourceDerivativeId, source_derivative_id: sourceDerivativeId } : {},
      ...durationMs !== void 0 ? { durationMs, duration_ms: durationMs } : {},
      ...width !== void 0 ? { width } : {},
      ...height !== void 0 ? { height } : {}
    }
  };
}
function buildTimelineTaskRequest(input, taskType, defaultFormat) {
  const project = editingProjectArg2(input);
  return {
    projectId: projectIdValue2(input) ?? project.projectId,
    taskType,
    editingProject: project,
    timeline: project.timeline,
    ...resourceRuntimeOptions(input),
    output: outputSpec(input, defaultFormat)
  };
}
function buildSourceTaskRequest(input, taskType) {
  const projectId = projectIdValue2(input);
  if (!projectId) {
    throw new Error(`projectId is required for ${taskType} Electron media workspace tasks`);
  }
  const source = recordValue7(input.source);
  if (!source) throw new Error("source is required");
  return {
    projectId,
    taskType,
    source,
    ...taskType === "media_reframe" ? reframeRuntimeOptions(input) : {},
    ...taskType === "media_transcode" ? transcodeRuntimeOptions(input) : {},
    ...resourceRuntimeOptions(input),
    output: outputSpec(input, "mp4")
  };
}
function addClip(input) {
  const project = editingProjectArg2(input);
  const trackId = trackIdValue(input);
  const clip = mediaClipArg(input, project);
  const editingProject = applyTimelineCommand(project, { type: "add_clip", trackId, clip });
  return {
    status: "ok",
    track_id: trackId,
    clip_id: clip.id,
    clip,
    editing_project: editingProject
  };
}
function applySingleTimelineCommand(input, command, result) {
  const editingProject = applyTimelineCommand(editingProjectArg2(input), command);
  return result(editingProject);
}
function applyTimelineCommand(project, command) {
  const service2 = createMediaEditingProjectService(project);
  service2.applyCommand(command);
  return service2.getProject();
}
function editingProjectArg2(input) {
  const project = recordValue7(input.editingProject) ?? recordValue7(input.editing_project) ?? recordValue7(input.project);
  if (!project) throw new Error("editingProject is required");
  assertMediaEditingProjectEnvelope2(project);
  return project;
}
function assertMediaEditingProjectEnvelope2(project) {
  if (project.version !== 1 || !stringValue12(project.id) || !stringValue12(project.projectId ?? project.project_id)) {
    throw new Error("editingProject must be a MediaEditingProject v1 object");
  }
  if (!recordValue7(project.timeline) || project.timeline.version !== 1 || !Array.isArray(project.timeline.tracks)) {
    throw new Error("editingProject.timeline must be a MediaTimelineRecipe v1 object");
  }
  if (!recordValue7(project.assets) || !Array.isArray(project.assets.assets)) {
    throw new Error("editingProject.assets must contain an assets array");
  }
}
function commandList(input) {
  const commands = Array.isArray(input.commands) ? input.commands : void 0;
  if (commands) return commands;
  const command = recordValue7(input.command);
  if (command) return [command];
  return [];
}
function mediaAssetArg(input) {
  const asset = recordValue7(input.asset);
  if (!asset) throw new Error("asset is required");
  const sourceKind = sourceKindValue(asset.sourceKind ?? asset.source_kind);
  const assetType = assetTypeValue(asset.assetType ?? asset.asset_type);
  const resourceId = optionalNumber2(asset.resourceId ?? asset.resource_id);
  const localPath = stringValue12(asset.localPath ?? asset.local_path);
  const id = stringValue12(asset.id) ?? mediaAssetId({ sourceKind, assetType, resourceId, localPath });
  return {
    id,
    sourceKind,
    assetType,
    ...resourceId !== void 0 ? { resourceId } : {},
    ...localPath ? { localPath } : {},
    ...stringValue12(asset.mimeType ?? asset.mime_type) ? { mimeType: stringValue12(asset.mimeType ?? asset.mime_type) } : {},
    ...stringValue12(asset.checksum) ? { checksum: stringValue12(asset.checksum) } : {},
    ...stringValue12(asset.label) ? { label: stringValue12(asset.label) } : {},
    ...recordValue7(asset.metadata) ? { metadata: asset.metadata } : {}
  };
}
function mediaTrackArg(input) {
  const track = recordValue7(input.track);
  if (track) {
    return {
      id: stringValue12(track.id) ?? `track_${trackTypeValue(track.type)}_${Date.now()}`,
      type: trackTypeValue(track.type),
      zIndex: optionalNumber2(track.zIndex ?? track.z_index) ?? 0,
      ...stringValue12(track.name) ? { name: stringValue12(track.name) } : {},
      ...booleanValue3(track.muted) !== void 0 ? { muted: booleanValue3(track.muted) } : {},
      ...booleanValue3(track.locked) !== void 0 ? { locked: booleanValue3(track.locked) } : {},
      clips: Array.isArray(track.clips) ? track.clips : []
    };
  }
  const type = trackTypeValue(input.type ?? input.trackType ?? input.track_type);
  return {
    id: stringValue12(input.trackId ?? input.track_id) ?? `track_${type}_${Date.now()}`,
    type,
    zIndex: optionalNumber2(input.zIndex ?? input.z_index) ?? 0,
    ...stringValue12(input.name) ? { name: stringValue12(input.name) } : {},
    ...booleanValue3(input.muted) !== void 0 ? { muted: booleanValue3(input.muted) } : {},
    ...booleanValue3(input.locked) !== void 0 ? { locked: booleanValue3(input.locked) } : {},
    clips: []
  };
}
function mediaClipArg(input, project) {
  const clip = recordValue7(input.clip);
  const source = clip ?? input;
  const asset = recordValue7(source.asset);
  const assetId = stringValue12(source.assetId ?? source.asset_id);
  const registeredAsset = assetId ? project.assets.assets.find((candidate) => candidate.id === assetId) : void 0;
  const resolvedAsset = asset ?? registeredAsset;
  const assetType = assetTypeValue(source.assetType ?? source.asset_type ?? resolvedAsset?.assetType);
  return {
    id: stringValue12(source.id ?? source.clipId ?? source.clip_id) ?? `clip_${Date.now()}`,
    assetType,
    ...resolvedAsset ? { asset: resolvedAsset } : {},
    timelineStartMs: optionalNumber2(source.timelineStartMs ?? source.timeline_start_ms) ?? 0,
    durationMs: optionalNumber2(source.durationMs ?? source.duration_ms) ?? 4e3,
    ...optionalNumber2(source.sourceStartMs ?? source.source_start_ms) !== void 0 ? { sourceStartMs: optionalNumber2(source.sourceStartMs ?? source.source_start_ms) } : {},
    ...optionalNumber2(source.sourceEndMs ?? source.source_end_ms) !== void 0 ? { sourceEndMs: optionalNumber2(source.sourceEndMs ?? source.source_end_ms) } : {},
    ...optionalNumber2(source.volume) !== void 0 ? { volume: optionalNumber2(source.volume) } : {},
    ...booleanValue3(source.muted) !== void 0 ? { muted: booleanValue3(source.muted) } : {},
    ...fitValue(source.fit) ? { fit: fitValue(source.fit) } : {},
    ...stringValue12(source.position) ? { position: stringValue12(source.position) } : {},
    ...optionalNumber2(source.xPercent ?? source.x_percent) !== void 0 ? { xPercent: optionalNumber2(source.xPercent ?? source.x_percent) } : {},
    ...optionalNumber2(source.yPercent ?? source.y_percent) !== void 0 ? { yPercent: optionalNumber2(source.yPercent ?? source.y_percent) } : {},
    ...optionalNumber2(source.scale) !== void 0 ? { scale: optionalNumber2(source.scale) } : {},
    ...optionalNumber2(source.opacity) !== void 0 ? { opacity: optionalNumber2(source.opacity) } : {},
    ...recordValue7(source.crop) ? { crop: source.crop } : {},
    ...recordValue7(source.transition) ? { transition: source.transition } : {},
    ...recordValue7(source.text) ? { text: source.text } : {},
    ...recordValue7(source.subtitle) ? { subtitle: source.subtitle } : {},
    ...recordValue7(source.metadata) ? { metadata: source.metadata } : {}
  };
}
function mediaClipPatchArg(input) {
  const patch = recordValue7(input.patch);
  const source = patch ?? input;
  const next = {};
  for (const [inputKey, outputKey] of [
    ["timelineStartMs", "timelineStartMs"],
    ["timeline_start_ms", "timelineStartMs"],
    ["durationMs", "durationMs"],
    ["duration_ms", "durationMs"],
    ["sourceStartMs", "sourceStartMs"],
    ["source_start_ms", "sourceStartMs"],
    ["sourceEndMs", "sourceEndMs"],
    ["source_end_ms", "sourceEndMs"],
    ["volume", "volume"],
    ["xPercent", "xPercent"],
    ["x_percent", "xPercent"],
    ["yPercent", "yPercent"],
    ["y_percent", "yPercent"],
    ["scale", "scale"],
    ["opacity", "opacity"]
  ]) {
    const value = optionalNumber2(source[inputKey]);
    if (value !== void 0) next[outputKey] = value;
  }
  if (booleanValue3(source.muted) !== void 0) next.muted = booleanValue3(source.muted);
  if (fitValue(source.fit)) next.fit = fitValue(source.fit);
  if (stringValue12(source.position)) next.position = stringValue12(source.position);
  if (recordValue7(source.crop)) next.crop = source.crop;
  if (recordValue7(source.transition)) next.transition = source.transition;
  if (recordValue7(source.text)) next.text = source.text;
  if (recordValue7(source.subtitle)) next.subtitle = source.subtitle;
  if (recordValue7(source.metadata)) next.metadata = source.metadata;
  return next;
}
function trackIdValue(input) {
  const trackId = stringValue12(input.trackId ?? input.track_id);
  if (!trackId) throw new Error("trackId is required");
  return trackId;
}
function clipIdValue(input) {
  const clipId = stringValue12(input.clipId ?? input.clip_id);
  if (!clipId) throw new Error("clipId is required");
  return clipId;
}
function assetIdValue(input) {
  const assetId = stringValue12(input.assetId ?? input.asset_id);
  if (!assetId) throw new Error("assetId is required");
  return assetId;
}
function productionTimelineClipsArg(input) {
  const clips = input.clips;
  if (!Array.isArray(clips)) throw new Error("clips is required");
  return clips.map((clip, index) => {
    const record = recordValue7(clip);
    if (!record) throw new Error(`clips[${index}] must be an object`);
    const contentUnitId = stringOrNumberValue3(record.contentUnitId ?? record.content_unit_id);
    if (contentUnitId === void 0) throw new Error(`clips[${index}].contentUnitId is required`);
    const resourceId = optionalNumber2(record.resourceId ?? record.resource_id);
    if (!Number.isInteger(resourceId) || resourceId <= 0) throw new Error(`clips[${index}].resourceId is required`);
    return {
      id: stringValue12(record.id) ?? `production_clip_${index + 1}`,
      title: stringValue12(record.title) ?? `Scene ${index + 1}`,
      contentUnitId,
      resourceId,
      ...stringOrNumberValue3(record.sceneMomentId ?? record.scene_moment_id) !== void 0 ? { sceneMomentId: stringOrNumberValue3(record.sceneMomentId ?? record.scene_moment_id) } : {},
      ...stringValue12(record.sceneMomentPath ?? record.scene_moment_path) ? { sceneMomentPath: stringValue12(record.sceneMomentPath ?? record.scene_moment_path) } : {},
      ...stringOrNumberValue3(record.candidateId ?? record.candidate_id) !== void 0 ? { candidateId: stringOrNumberValue3(record.candidateId ?? record.candidate_id) } : {},
      ...optionalNumber2(record.durationSec ?? record.duration_sec) !== void 0 ? { durationSec: optionalNumber2(record.durationSec ?? record.duration_sec) } : {}
    };
  });
}
function projectIdValue2(input) {
  const value = input.projectId ?? input.project_id;
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return stringValue12(value);
}
function editingProjectIdValue2(input) {
  const editingProjectId = stringValue12(input.editingProjectId ?? input.editing_project_id);
  if (!editingProjectId) throw new Error("editingProjectId is required");
  return editingProjectId;
}
function taskIdValue(input) {
  const taskId = stringValue12(input.taskId ?? input.task_id);
  if (!taskId) throw new Error("taskId is required");
  return taskId;
}
function outputSpec(input, defaultFormat) {
  const output = recordValue7(input.output);
  const format = stringValue12(output?.format) === "hls" ? "hls" : stringValue12(output?.format) === "mp4" ? "mp4" : defaultFormat;
  return {
    format,
    ...stringValue12(output?.filename) ? { filename: stringValue12(output?.filename) } : {},
    ...booleanValue3(output?.importToResource ?? output?.import_to_resource) !== void 0 ? { importToResource: booleanValue3(output?.importToResource ?? output?.import_to_resource) } : {},
    ...stringOrNumberValue3(output?.folderId ?? output?.folder_id) !== void 0 ? { folderId: stringOrNumberValue3(output?.folderId ?? output?.folder_id) } : {},
    ...exportDerivativePayload2(output ?? {}) ? { derivative: exportDerivativePayload2(output ?? {}) } : {},
    ...Array.isArray(output?.hlsVariants ?? output?.hls_variants) ? { hlsVariants: output?.hlsVariants ?? output?.hls_variants } : {}
  };
}
function reframeRuntimeOptions(input) {
  const output = recordValue7(input.output);
  const reframe = recordValue7(input.reframe) ?? {};
  const target = stringValue12(input.target ?? reframe.target ?? output?.target);
  const mode = stringValue12(input.mode ?? reframe.mode ?? output?.mode);
  const width = optionalNumber2(input.width ?? reframe.width ?? output?.width);
  const height = optionalNumber2(input.height ?? reframe.height ?? output?.height);
  const background = stringValue12(input.background ?? reframe.background ?? output?.background);
  const spec = {
    ...reframe,
    ...target ? { target } : {},
    ...mode ? { mode } : {},
    ...width !== void 0 ? { width } : {},
    ...height !== void 0 ? { height } : {},
    ...background ? { background } : {}
  };
  return {
    ...target ? { target } : {},
    ...mode ? { mode } : {},
    ...Object.keys(spec).length ? { reframe: spec } : {}
  };
}
function transcodeRuntimeOptions(input) {
  const output = recordValue7(input.output);
  const transcode = recordValue7(input.transcode) ?? {};
  const videoCodec = stringValue12(input.videoCodec ?? input.video_codec ?? transcode.videoCodec ?? transcode.video_codec ?? output?.videoCodec ?? output?.video_codec);
  const audioCodec = stringValue12(input.audioCodec ?? input.audio_codec ?? transcode.audioCodec ?? transcode.audio_codec ?? output?.audioCodec ?? output?.audio_codec);
  const videoBitrateKbps = optionalNumber2(input.videoBitrateKbps ?? input.video_bitrate_kbps ?? transcode.videoBitrateKbps ?? transcode.video_bitrate_kbps ?? output?.videoBitrateKbps ?? output?.video_bitrate_kbps);
  const audioBitrateKbps = optionalNumber2(input.audioBitrateKbps ?? input.audio_bitrate_kbps ?? transcode.audioBitrateKbps ?? transcode.audio_bitrate_kbps ?? output?.audioBitrateKbps ?? output?.audio_bitrate_kbps);
  const spec = {
    ...transcode,
    ...videoCodec ? { videoCodec } : {},
    ...audioCodec ? { audioCodec } : {},
    ...videoBitrateKbps !== void 0 ? { videoBitrateKbps } : {},
    ...audioBitrateKbps !== void 0 ? { audioBitrateKbps } : {}
  };
  return Object.keys(spec).length ? { transcode: spec } : {};
}
function resourceRuntimeOptions(input) {
  const output = recordValue7(input.output);
  const resourceCache = recordValue7(input.resourceCache) ?? recordValue7(input.resource_cache) ?? recordValue7(output?.resourceCache) ?? recordValue7(output?.resource_cache);
  const resourceDownload = recordValue7(input.resourceDownload) ?? recordValue7(input.resource_download) ?? recordValue7(output?.resourceDownload) ?? recordValue7(output?.resource_download);
  return {
    ...resourceCache ? { resourceCache } : {},
    ...resourceDownload ? { resourceDownload } : {}
  };
}
function exportImportDerivativeRequest(input) {
  const derivative = exportDerivativePayload2(input);
  return {
    ...derivative ? { derivative } : {},
    ...stringValue12(input.operation) ? { operation: stringValue12(input.operation) } : {},
    ...stringValue12(input.tool) ? { tool: stringValue12(input.tool) } : {},
    ...idList(input.inputResourceIds) ? { inputResourceIds: idList(input.inputResourceIds) } : {},
    ...idList(input.input_resource_ids) ? { input_resource_ids: idList(input.input_resource_ids) } : {},
    ...stringOrNumberValue3(input.sourceResourceId) !== void 0 ? { sourceResourceId: stringOrNumberValue3(input.sourceResourceId) } : {},
    ...stringOrNumberValue3(input.source_resource_id) !== void 0 ? { source_resource_id: stringOrNumberValue3(input.source_resource_id) } : {},
    ...idList(input.sourceResourceIds) ? { sourceResourceIds: idList(input.sourceResourceIds) } : {},
    ...idList(input.source_resource_ids) ? { source_resource_ids: idList(input.source_resource_ids) } : {},
    ...recordValue7(input.params) ? { params: input.params } : {}
  };
}
function exportDerivativePayload2(source) {
  const explicit = source.derivative;
  if (recordValue7(explicit)) {
    const operation = stringValue12(explicit.operation);
    if (!operation) return void 0;
    const inputIds = numericIdList2(explicit.input_resource_ids ?? explicit.inputResourceIds);
    return {
      operation,
      ...stringValue12(explicit.tool) ? { tool: stringValue12(explicit.tool) } : {},
      ...inputIds.length ? { input_resource_ids: inputIds } : {},
      ...recordValue7(explicit.params) ? { params: explicit.params } : {}
    };
  }
  return void 0;
}
function idList(value) {
  if (!Array.isArray(value)) return void 0;
  const list = value.filter((item) => typeof item === "string" || typeof item === "number");
  return list.length ? list : void 0;
}
function numericIdList2(value) {
  const values = Array.isArray(value) ? value : value === void 0 ? [] : [value];
  return Array.from(new Set(values.map((item) => typeof item === "number" ? item : typeof item === "string" ? Number(item) : NaN).filter((item) => Number.isInteger(item) && item > 0)));
}
function isHlsTaskOutput(task, outputPath) {
  if (recordValue7(task) && stringValue12(task.taskType ?? task.task_type) === "timeline_hls") return true;
  return outputPath.toLowerCase().endsWith(".m3u8");
}
function requiredNumeric(input, camelKey, snakeKey) {
  const value = optionalNumber2(input[camelKey] ?? input[snakeKey]);
  if (value === void 0) throw new Error(`${camelKey} is required`);
  return value;
}
function sourceKindValue(value) {
  const kind = stringValue12(value);
  if (kind === "local_file" || kind === "raw_resource" || kind === "generated_resource" || kind === "bytes" || kind === "external_url") return kind;
  if (kind === "resource") return "raw_resource";
  return "local_file";
}
function assetTypeValue(value) {
  const type = stringValue12(value);
  if (type === "video" || type === "image" || type === "audio" || type === "subtitle" || type === "text") return type;
  return "video";
}
function trackTypeValue(value) {
  const type = stringValue12(value);
  if (type === "video" || type === "image" || type === "audio" || type === "text" || type === "subtitle" || type === "effect") return type;
  return "video";
}
function fitValue(value) {
  const fit = stringValue12(value);
  if (fit === "cover" || fit === "contain" || fit === "fill" || fit === "none") return fit;
  return void 0;
}
function retainSideValue(value) {
  const side = stringValue12(value);
  if (side === "left" || side === "right" || side === "both") return side;
  return void 0;
}
function mediaAssetId(input) {
  if (input.resourceId !== void 0) return `resource_${input.resourceId}`;
  if (input.localPath) return input.localPath.split(/[\\/]/).pop()?.replace(/[^a-zA-Z0-9_-]+/g, "_") || `${input.assetType}_${Date.now()}`;
  return `${input.sourceKind}_${input.assetType}_${Date.now()}`;
}
function findClip(project, clipId) {
  for (const track of project.timeline.tracks) {
    const clip = track.clips.find((candidate) => candidate.id === clipId);
    if (clip) return { track, clip };
  }
  return void 0;
}
function relatedSplitClips(project, clipId) {
  return project.timeline.tracks.flatMap((track) => track.clips).filter((clip) => clip.id === clipId || clip.id.startsWith(`${clipId}_right_`));
}
function cloneProject(project) {
  return JSON.parse(JSON.stringify(project));
}
function touchProject(project) {
  project.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
  project.revision = typeof project.revision === "number" ? project.revision + 1 : 1;
}
async function readJSONBody3(request) {
  const chunks = [];
  for await (const chunk of request) {
    chunks.push(Buffer.from(chunk));
  }
  const text = Buffer.concat(chunks).toString("utf8").trim();
  if (!text) return {};
  try {
    return JSON.parse(text);
  } catch {
    throw httpError2(400, "invalid_json", "request body must be valid JSON");
  }
}
function stringValue12(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function optionalNumber2(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value === "string" && value.trim() && Number.isFinite(Number(value))) return Number(value);
  return void 0;
}
function msValue2(value) {
  const number = optionalNumber2(value);
  return number === void 0 ? void 0 : Math.max(1, number);
}
function secToMs2(value) {
  return value === void 0 ? void 0 : Math.max(1, Math.round(value * 1e3));
}
function stringOrNumberValue3(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  return stringValue12(value);
}
function stringList2(value) {
  if (Array.isArray(value)) {
    const list = value.map((item) => stringValue12(item)).filter(Boolean);
    return list.length ? list : void 0;
  }
  if (typeof value === "string" && value.trim()) {
    const list = value.split(",").map((item) => item.trim()).filter(Boolean);
    return list.length ? list : void 0;
  }
  return void 0;
}
function booleanValue3(value) {
  if (typeof value === "boolean") return value;
  if (value === "true") return true;
  if (value === "false") return false;
  return void 0;
}
function recordValue7(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : void 0;
}
function stringRecord(value) {
  const record = recordValue7(value);
  if (!record) return void 0;
  return Object.fromEntries(Object.entries(record).filter(([, item]) => typeof item === "string").map(([key, item]) => [key, item]));
}
function optionalDecisionStoreFromBody(body) {
  return recordValue7(body.decisionStore ?? body.decision_store) ? decisionStoreFromBody(body) : void 0;
}
function decisionStoreFromBody(body) {
  const config = recordValue7(body.decisionStore ?? body.decision_store);
  if (!config) return void 0;
  if (config.kind !== "scoped-project-data") {
    throw httpError2(400, "editing_decision_store_unsupported", "only scoped-project-data decisionStore is supported");
  }
  const baseUrl = stringValue12(config.baseUrl ?? config.base_url);
  const projectUid = stringValue12(config.projectUid ?? config.project_uid);
  if (!baseUrl || !projectUid) {
    throw httpError2(400, "editing_decision_store_invalid", "decisionStore.baseUrl and decisionStore.projectUid are required");
  }
  return createMovScriptScopedProjectDataDecisionStore({
    baseUrl,
    projectUid,
    ...stringValue12(config.title) ? { title: stringValue12(config.title) } : {},
    ...config.scopeKind === "user" || config.scopeKind === "org" ? { scopeKind: config.scopeKind } : {},
    ...config.scopeId !== void 0 || config.scope_id !== void 0 ? { scopeId: config.scopeId ?? config.scope_id } : {},
    ...stringValue12(config.token) ? { token: stringValue12(config.token) } : {},
    ...stringRecord(config.headers) ? { headers: stringRecord(config.headers) } : {}
  });
}
function projectDirFromBody(body) {
  const projectDir = stringValue12(body.projectDir ?? body.project_dir ?? body.cwd);
  if (!projectDir) {
    throw httpError2(400, "editing_project_dir_required", "projectDir is required");
  }
  return resolve9(projectDir);
}
function pathSegmentAfter9(path, segment) {
  if (!path) return void 0;
  const parts = path.split("/").filter(Boolean);
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function lastPathSegment9(path) {
  return path?.split("/").filter(Boolean).at(-1);
}
function safeId5(value) {
  return value.replace(/[^a-zA-Z0-9_-]+/g, "_") || "item";
}
function sameId7(left, right) {
  return left !== void 0 && right !== void 0 && String(left) === String(right);
}
function httpError2(statusCode, code, message) {
  const error = new Error(message);
  error.statusCode = statusCode;
  error.code = code;
  return error;
}
function writeEditingServiceError(response, error) {
  const statusCode = Number.isInteger(error?.statusCode) ? error.statusCode : 400;
  writeJSON3(response, statusCode, {
    error: error?.code ?? "editing_service_error",
    message: error?.message ?? "editing service error"
  });
}
function waitForShutdown3(runtime) {
  return new Promise((resolve12) => {
    let closing = false;
    const close = async () => {
      if (closing) return;
      closing = true;
      await runtime.close();
      resolve12();
    };
    process.once("SIGINT", close);
    process.once("SIGTERM", close);
  });
}

// ../../services/project-service/src/server.mjs
import { createServer as createServer4 } from "http";
import { mkdir as mkdir5, readFile as readFile5, stat as stat4 } from "fs/promises";
import { basename as basename4, resolve as resolve10 } from "path";
var DATA_SERVICE_NAME = "movscript.data.service";
var LOCAL_NODE_GATEWAY_SERVICE3 = "movscript.local-node.gateway";
var CONTENT_CANVAS_DIRECTORY = "content_canvases";
var CONTENT_CANVAS_FILE_NAME = "canvas.json";
var CONTENT_CANVAS_SCHEMA = "movscript.content_canvas.v1";
var CONTENT_CANVASES_SCHEMA = "movscript.content_canvases.v1";
var CONTENT_CANVAS_TITLE_MAX_LENGTH = 80;
var CONTENT_CANVAS_TITLE_INVALID_PATTERN = /[<>:"/\\|?*\u0000-\u001F]/;
var PROJECT_SERVICE_ENGINE_CACHE_LIMIT = 32;
var PROJECT_SERVICE_ENGINE_CACHE_SEPARATOR = "";
var projectServiceEngineRegistry = new NodeMovScriptEngineRegistry();
var projectServiceEngineCacheKeys = [];
var PROJECT_SERVICE_CAPABILITIES = Object.freeze([
  "project-read-model",
  "project-home-read-model",
  "project-standards-read-model",
  "project-content-canvas-read-model",
  "project-scripts-read-model",
  "project-content-units-read-model",
  "project-lifecycle",
  "project-locator",
  "project-resource-view",
  "domain-source",
  "candidate-view",
  "prompt-context",
  "interpret",
  "content-canvas-run",
  "project-standards",
  "project-scripts",
  "workspace-candidate-actions",
  "content-candidate-actions"
]);
var PROJECT_SOURCE_OPERATION_ROUTES = /* @__PURE__ */ new Map([
  [PROJECT_SERVICE_ENTITIES_QUERY_ENDPOINT, {
    schema: "movscript.project-entities-query.v1",
    run: (engine, input) => engine.workspaceService.queryEntities(input.query)
  }],
  [PROJECT_SERVICE_SETTINGS_QUERY_ENDPOINT, {
    schema: "movscript.project-settings-query.v1",
    run: (engine, input) => engine.workspaceService.querySettings(input.query)
  }],
  [PROJECT_SERVICE_ASSETS_QUERY_ENDPOINT, {
    schema: "movscript.project-assets-query.v1",
    run: (engine, input) => engine.workspaceService.queryAssets(input.query)
  }],
  [PROJECT_SERVICE_CONTENT_WORKSPACE_SNAPSHOT_ENDPOINT, {
    schema: "movscript.project-content-workspace-snapshot.v1",
    run: (engine) => loadContentSourceWorkspaceSnapshotFromEngine(engine)
  }],
  [PROJECT_SERVICE_CONTENT_WORKSPACE_READ_ENDPOINT, {
    schema: "movscript.project-content-workspace-read.v1",
    run: async (engine) => buildContentSourceWorkspaceData(await loadContentSourceWorkspaceSnapshotFromEngine(engine))
  }],
  [PROJECT_SERVICE_SETTING_UPSERT_ENDPOINT, {
    schema: "movscript.project-setting-upsert.v1",
    run: (engine, input) => engine.workspaceService.upsertSetting(input)
  }],
  [PROJECT_SERVICE_SETTING_CREATE_ENDPOINT, {
    schema: "movscript.project-setting-create.v1",
    run: (engine, input) => engine.createSetting(input)
  }],
  [PROJECT_SERVICE_SETTING_STATE_CREATE_ENDPOINT, {
    schema: "movscript.project-setting-state-create.v1",
    run: (engine, input) => engine.createSettingState(input)
  }],
  [PROJECT_SERVICE_ASSET_UPSERT_ENDPOINT, {
    schema: "movscript.project-asset-upsert.v1",
    run: (engine, input) => engine.workspaceService.upsertAsset(input)
  }],
  [PROJECT_SERVICE_ASSET_CREATE_ENDPOINT, {
    schema: "movscript.project-asset-create.v1",
    run: (engine, input) => engine.createAsset(input)
  }],
  [PROJECT_SERVICE_PRODUCTION_SNAPSHOT_SAVE_ENDPOINT, {
    schema: "movscript.project-production-snapshot-save.v1",
    run: (engine, input) => engine.workspaceService.saveProductionSnapshot(input)
  }],
  [PROJECT_SERVICE_CONTENT_UNIT_UPSERT_ENDPOINT, {
    schema: "movscript.project-content-unit-upsert.v1",
    run: (engine, input) => engine.workspaceService.upsertContentUnit(input)
  }],
  [PROJECT_SERVICE_CONTENT_UNIT_CREATE_ENDPOINT, {
    schema: "movscript.project-content-unit-create.v1",
    run: (engine, input) => engine.createContentUnit(input)
  }],
  [PROJECT_SERVICE_CONTENT_UNIT_ENSURE_ENDPOINT, {
    schema: "movscript.project-content-unit-ensure.v1",
    run: (engine, input) => engine.ensureContentUnitForEntity(input)
  }],
  [PROJECT_SERVICE_TIMELINE_ASSEMBLY_CONTENT_UNIT_ENSURE_ENDPOINT, {
    schema: "movscript.project-timeline-assembly-content-unit-ensure.v1",
    run: (engine, input) => engine.ensureContentUnitForEntity(timelineAssemblyContentUnitInput(input))
  }],
  [PROJECT_SERVICE_CONTENT_UNIT_EDIT_PROMPT_UPDATE_ENDPOINT, {
    schema: "movscript.project-content-unit-edit-prompt-update.v1",
    run: (engine, input) => engine.workspaceService.updateContentUnitEditPrompt(input)
  }],
  [PROJECT_SERVICE_PRODUCTION_CREATE_ENDPOINT, {
    schema: "movscript.project-production-create.v1",
    run: (engine, input) => engine.createProduction(input)
  }],
  [PROJECT_SERVICE_SEGMENT_CREATE_ENDPOINT, {
    schema: "movscript.project-segment-create.v1",
    run: (engine, input) => engine.createSegment(input)
  }],
  [PROJECT_SERVICE_SCENE_MOMENT_CREATE_ENDPOINT, {
    schema: "movscript.project-scene-moment-create.v1",
    run: (engine, input) => engine.createSceneMoment(input)
  }],
  [PROJECT_SERVICE_SCENE_MOMENT_SETTING_CONNECT_ENDPOINT, {
    schema: "movscript.project-scene-moment-setting-connect.v1",
    run: (engine, input) => engine.connectSceneMomentSetting(input)
  }],
  [PROJECT_SERVICE_EXPRESSION_UNIT_CREATE_ENDPOINT, {
    schema: "movscript.project-expression-unit-create.v1",
    run: (engine, input) => engine.createExpressionUnit(input)
  }],
  [PROJECT_SERVICE_EXPRESSION_UNIT_UPDATE_ENDPOINT, {
    schema: "movscript.project-expression-unit-update.v1",
    run: (engine, input) => engine.workspaceService.updateExpressionUnitSource(input)
  }],
  [PROJECT_SERVICE_KEYFRAME_CREATE_ENDPOINT, {
    schema: "movscript.project-keyframe-create.v1",
    run: (engine, input) => engine.createKeyframe(input)
  }],
  [PROJECT_SERVICE_STORYBOARD_CREATE_ENDPOINT, {
    schema: "movscript.project-storyboard-create.v1",
    run: (engine, input) => engine.createStoryboard(input)
  }],
  [PROJECT_SERVICE_STORYBOARD_TIMELINE_UPDATE_ENDPOINT, {
    schema: "movscript.project-storyboard-timeline-update.v1",
    run: (engine, input) => engine.workspaceService.updateStoryboardTimeline(input)
  }],
  [PROJECT_SERVICE_AUDIO_CUE_CREATE_ENDPOINT, {
    schema: "movscript.project-audio-cue-create.v1",
    run: (engine, input) => engine.createAudioCue(input)
  }],
  [PROJECT_SERVICE_AUDIO_CUE_UPDATE_ENDPOINT, {
    schema: "movscript.project-audio-cue-update.v1",
    run: (engine, input) => engine.workspaceService.updateAudioCueSource(input)
  }],
  [PROJECT_SERVICE_ENTITY_BASICS_UPDATE_ENDPOINT, {
    schema: "movscript.project-entity-basics-update.v1",
    run: (engine, input) => engine.updateEntityBasics(input)
  }],
  [PROJECT_SERVICE_ENTITY_TRANSITION_UPDATE_ENDPOINT, {
    schema: "movscript.project-entity-transition-update.v1",
    run: (engine, input) => engine.workspaceService.updateEntityTransition(input)
  }],
  [PROJECT_SERVICE_ENTITY_DELETE_ENDPOINT, {
    schema: "movscript.project-entity-delete.v1",
    run: async (engine, input) => {
      await engine.workspaceService.deleteEntity(input);
      return { status: "deleted" };
    }
  }],
  [PROJECT_SERVICE_HIERARCHY_WRITE_ENDPOINT, {
    schema: "movscript.project-hierarchy-write.v1",
    run: (engine, input) => engine.writeHierarchyNode(input)
  }],
  [PROJECT_SERVICE_NAMESPACE_WRITE_ENDPOINT, {
    schema: "movscript.project-namespace-write.v1",
    run: (engine, input) => engine.writeHierarchyNode(namespaceHierarchyNodeInput(input))
  }],
  [PROJECT_SERVICE_WORKSPACE_CANDIDATE_SELECT_ENDPOINT, {
    schema: "movscript.project-workspace-candidate-select.v1",
    run: (engine, input) => engine.workspaceService.selectCandidate(input)
  }],
  [PROJECT_SERVICE_WORKSPACE_CANDIDATE_APPEND_ENDPOINT, {
    schema: "movscript.project-workspace-candidate-append.v1",
    run: (engine, input) => engine.workspaceService.appendCandidate(input)
  }],
  [PROJECT_SERVICE_WORKSPACE_ASSET_SLOT_CANDIDATE_CREATE_ENDPOINT, {
    schema: "movscript.project-workspace-asset-slot-candidate-create.v1",
    run: (engine, input) => engine.workspaceService.createAssetSlotCandidate(input)
  }],
  [PROJECT_SERVICE_WORKSPACE_KEYFRAME_CANDIDATE_CREATE_ENDPOINT, {
    schema: "movscript.project-workspace-keyframe-candidate-create.v1",
    run: (engine, input) => engine.workspaceService.createKeyframeCandidate(input)
  }]
]);
function createProjectServiceHandler(options = {}) {
  const serviceName = options.serviceName ?? PROJECT_SERVICE_NAME;
  const capabilities = options.capabilities ?? PROJECT_SERVICE_CAPABILITIES;
  const now = options.now ?? (() => /* @__PURE__ */ new Date());
  const logger = options.logger;
  return async (request, response) => {
    const url = new URL(request.url ?? "/", "http://127.0.0.1");
    const requestScope = createProjectServiceRequestScope({ request, url, logger });
    request.__projectServiceRequestScope = requestScope;
    response.__projectServiceRequestScope = requestScope;
    try {
      if (request.method === "OPTIONS") {
        writeCORSPreflight(response);
        return;
      }
      if (request.method === "GET" && url.pathname === "/health") {
        writeJSON4(response, 200, {
          status: "ok",
          serviceName,
          capabilities
        });
        return;
      }
      if (request.method === "GET" && url.pathname === PROJECT_SERVICE_CAPABILITIES_ENDPOINT) {
        writeJSON4(response, 200, {
          serviceName,
          capabilities
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SOURCE_SNAPSHOT_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const source = await resolveWorkspaceSource(context.fileRepository, context.sourceOptions);
        writeJSON4(response, 200, {
          schema: "movscript.project-source-snapshot.v1",
          projectDir: context.projectDir,
          source
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SOURCE_INSPECT_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const inspection = await inspectMovScriptWorkspace({
          fileRepository: context.fileRepository,
          decisionStore: context.decisionStore,
          now: now(),
          ...context.inspectOptions
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-source-inspection.v1",
          projectDir: context.projectDir,
          inspection
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SOURCE_OVERVIEW_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const overview = await overviewMovScriptWorkspace({
          fileRepository: context.fileRepository,
          decisionStore: context.decisionStore,
          now: now()
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-read-model-overview.v1",
          projectDir: context.projectDir,
          overview
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_READ_MODEL_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const projectReadModel = await readProjectReadModel(context, now());
        writeJSON4(response, 200, {
          schema: "movscript.project-read-model.v1",
          projectDir: context.projectDir,
          projectReadModel
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_HOME_READ_MODEL_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const projectHomeReadModel = await readProjectHomeReadModel(context, now());
        writeJSON4(response, 200, {
          schema: "movscript.project-home-read-model.v1",
          projectDir: context.projectDir,
          projectHomeReadModel
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_STANDARDS_READ_MODEL_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const projectStandardsReadModel = await readProjectStandardsReadModel(context, now());
        writeJSON4(response, 200, {
          schema: "movscript.project-standards-read-model.v1",
          projectDir: context.projectDir,
          projectStandardsReadModel
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_CANVAS_READ_MODEL_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const projectContentCanvasReadModel = await readProjectContentCanvasReadModel(context, now());
        writeJSON4(response, 200, {
          schema: "movscript.project-content-canvas-read-model.v1",
          projectDir: context.projectDir,
          projectContentCanvasReadModel
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SCRIPTS_READ_MODEL_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const projectScriptsReadModel = await readProjectScriptsReadModel(context, now());
        writeJSON4(response, 200, {
          schema: "movscript.project-scripts-read-model.v1",
          projectDir: context.projectDir,
          projectScriptsReadModel
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_UNITS_READ_MODEL_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const contentUnitIds = readModelContentUnitIdsFromBody(body);
        const projectContentUnitsReadModel = await readProjectContentUnitsReadModel({
          projectDir,
          body,
          contentUnitIds,
          decisionStore: await optionalDecisionStoreFromBody2(body, projectDir),
          requestScope: request.__projectServiceRequestScope,
          now: now()
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-content-units-read-model.v1",
          projectDir,
          projectContentUnitsReadModel
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SOURCE_INTERPRET_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const interpretation = await interpretMovScriptWorkspace({
          fileRepository: context.fileRepository,
          decisionStore: context.decisionStore,
          now: now(),
          debugArtifacts: context.debugArtifacts
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-source-interpretation.v1",
          projectDir: context.projectDir,
          interpretation
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SOURCE_REGENERATION_PLAN_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const regenerationPlan = await planMovScriptWorkspaceRegeneration({
          fileRepository: context.fileRepository,
          decisionStore: context.decisionStore,
          now: now()
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-source-regeneration-plan.v1",
          projectDir: context.projectDir,
          regenerationPlan
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_STANDARDS_UPSERT_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const engine = createProjectWorkspaceEngine(context);
        writeJSON4(response, 200, projectSourceOperationEnvelope(
          context.projectDir,
          await engine.workspaceService.upsertProjectStandards(projectSourceOperationInput(context.body)),
          "movscript.project-standards-upsert.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SCRIPT_SOURCE_READ_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const engine = createProjectWorkspaceEngine(context);
        writeJSON4(response, 200, projectSourceOperationEnvelope(
          context.projectDir,
          await engine.workspaceService.readScriptSource(projectSourceOperationInput(context.body)),
          "movscript.project-script-source-read.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SCRIPT_UPSERT_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const engine = createProjectWorkspaceEngine(context);
        writeJSON4(response, 200, projectSourceOperationEnvelope(
          context.projectDir,
          await engine.workspaceService.upsertScript(projectSourceOperationInput(context.body)),
          "movscript.project-script-upsert.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SCRIPT_VERSION_SNAPSHOT_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const engine = createProjectWorkspaceEngine(context);
        writeJSON4(response, 200, projectSourceOperationEnvelope(
          context.projectDir,
          await engine.workspaceService.snapshotScriptVersionFromMarkdown(projectSourceOperationInput(context.body)),
          "movscript.project-script-version-snapshot.v1"
        ));
        return;
      }
      const sourceOperation = PROJECT_SOURCE_OPERATION_ROUTES.get(url.pathname);
      if (request.method === "POST" && sourceOperation) {
        const context = await readProjectSourceContext(request);
        const engine = createProjectWorkspaceEngine(context);
        writeJSON4(response, 200, projectSourceOperationEnvelope(
          context.projectDir,
          await sourceOperation.run(engine, projectSourceOperationInput(context.body)),
          sourceOperation.schema
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_CANVASES_LIST_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        writeJSON4(response, 200, projectContentCanvasEnvelope(
          context.projectDir,
          await listProjectContentCanvases(context.fileRepository),
          "movscript.project-content-canvases-list.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_CANVAS_WRITE_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        writeJSON4(response, 200, projectContentCanvasEnvelope(
          context.projectDir,
          await writeProjectContentCanvas(context.fileRepository, context.body),
          "movscript.project-content-canvas-write.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_CANVAS_RENAME_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        writeJSON4(response, 200, projectContentCanvasEnvelope(
          context.projectDir,
          await renameProjectContentCanvas(context.fileRepository, context.body),
          "movscript.project-content-canvas-rename.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_CANVAS_RUN_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        const engine = createProjectWorkspaceEngine(context);
        writeJSON4(response, 200, projectContentCanvasEnvelope(
          context.projectDir,
          await runProjectContentCanvas({
            projectDir: context.projectDir,
            fileRepository: context.fileRepository,
            engine,
            input: context.body,
            now: now()
          }),
          "movscript.project-content-canvas-run.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_CANVAS_DELETE_ENDPOINT) {
        const context = await readProjectSourceContext(request);
        writeJSON4(response, 200, projectContentCanvasEnvelope(
          context.projectDir,
          await deleteProjectContentCanvas(context.fileRepository, context.body),
          "movscript.project-content-canvas-delete.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_SOURCE_COMMAND_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const command = stringValue13(body.command);
        if (!command) {
          throw httpError3(400, "project_source_command_required", "command is required");
        }
        const result = await executeProjectSourceCommand({
          projectDir,
          command,
          input: recordValue8(body.input) ?? {},
          decisionStore: await optionalDecisionStoreFromBody2(body, projectDir),
          now: now()
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-source-command-result.v1",
          projectDir,
          command,
          result
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_LIFECYCLE_COMMAND_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const command = stringValue13(body.command);
        if (!command) {
          throw httpError3(400, "project_lifecycle_command_required", "command is required");
        }
        const result = await executeProjectLifecycleCommand({
          projectDir,
          command,
          input: recordValue8(body.input) ?? {},
          now: now()
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-lifecycle-command-result.v1",
          projectDir,
          command,
          result
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_LOCATOR_RESOLVE_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const locator = await resolveProjectLocator({
          projectDir,
          workspaceDir: pathStringValue(body.workspaceDir ?? body.workspace_dir),
          projectUid: stringValue13(body.projectUid ?? body.project_uid)
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-locator.v1",
          projectDir,
          locator
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_RESOURCE_VIEW_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const kind = stringValue13(body.kind);
        if (!kind) {
          throw httpError3(400, "project_resource_kind_required", "kind is required");
        }
        const items = await readProjectResourceView({ projectDir, kind, body, requestScope: request.__projectServiceRequestScope });
        writeJSON4(response, 200, {
          schema: "movscript.project-resource-view.v1",
          projectDir,
          kind,
          items
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_CANDIDATE_CREATE_ENDPOINT) {
        const context = await readProjectCandidateActionContext(request);
        writeJSON4(response, 200, projectCandidateActionEnvelope(
          context.projectDir,
          await executeProjectCandidateAction(context, "createContentCandidate"),
          "movscript.project-content-candidate-create.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_SELECT_ENDPOINT) {
        const context = await readProjectCandidateActionContext(request);
        writeJSON4(response, 200, projectCandidateActionEnvelope(
          context.projectDir,
          await executeProjectCandidateAction(context, "selectContentUnitCandidate"),
          "movscript.project-content-unit-candidate-select.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CONTENT_UNIT_CANDIDATE_DECIDE_ENDPOINT) {
        const context = await readProjectCandidateActionContext(request);
        writeJSON4(response, 200, projectCandidateActionEnvelope(
          context.projectDir,
          await executeProjectCandidateAction(context, "decideContentUnitCandidate"),
          "movscript.project-content-unit-candidate-decide.v1"
        ));
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CANDIDATE_COMMAND_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const command = stringValue13(body.command);
        if (!command) {
          throw httpError3(400, "project_candidate_command_required", "command is required");
        }
        const decisionStore = await optionalDecisionStoreFromBody2(body, projectDir);
        if (!decisionStore) {
          throw httpError3(400, "project_candidate_decision_store_required", "decisionStore or projectUid is required");
        }
        const result = await executeProjectCandidateCommand({
          projectDir,
          command,
          input: recordValue8(body.input) ?? {},
          decisionStore
        });
        writeJSON4(response, 200, {
          schema: "movscript.project-candidate-command-result.v1",
          projectDir,
          command,
          result
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_CANDIDATE_VIEW_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const contentUnitIds = contentUnitIdsFromBody(body);
        const decisionStore = await optionalDecisionStoreFromBody2(body, projectDir);
        if (!decisionStore) {
          throw httpError3(400, "project_candidate_decision_store_required", "decisionStore or projectUid is required");
        }
        const contexts = await readCandidateContexts(decisionStore, contentUnitIds, request.__projectServiceRequestScope);
        writeJSON4(response, 200, {
          schema: "movscript.project-candidate-view.v1",
          projectDir,
          ...contentUnitIds.length === 1 ? { contentUnitId: contentUnitIds[0] } : {},
          contentUnitIds,
          contexts
        });
        return;
      }
      if (request.method === "POST" && url.pathname === PROJECT_SERVICE_PROMPT_CONTEXT_ENDPOINT) {
        const body = await readJSONBody4(request);
        const projectDir = projectDirFromBody2(body);
        const contentUnitIds = promptContextContentUnitIdsFromBody(body);
        const promptContexts = await readProjectPromptContexts({
          projectDir,
          body,
          contentUnitIds,
          include: promptContextIncludeFromBody(body),
          promptText: stringValue13(body.promptText ?? body.prompt_text),
          decisionStore: await optionalDecisionStoreFromBody2(body, projectDir),
          requestScope: request.__projectServiceRequestScope
        });
        const singleContext = promptContexts.length === 1 ? promptContexts[0] : void 0;
        writeJSON4(response, 200, {
          schema: "movscript.project-prompt-context.v1",
          projectDir,
          ...singleContext ? {
            contentUnitId: singleContext.contentUnitId,
            ...singleContext.context
          } : {},
          contentUnitIds,
          contexts: promptContexts
        });
        return;
      }
      writeJSON4(response, 404, {
        error: "not_found"
      });
    } catch (error) {
      writeProjectServiceError(response, error);
    }
  };
}
function startProjectService(options = {}) {
  const host = options.host ?? "127.0.0.1";
  const port = Number(options.port ?? 0);
  const server = createServer4(createProjectServiceHandler(options));
  return new Promise((resolve12, reject) => {
    server.once("error", reject);
    server.listen(port, host, () => {
      server.off("error", reject);
      const address = server.address();
      const actualPort = typeof address === "object" && address ? address.port : port;
      resolve12({
        server,
        host,
        port: actualPort,
        url: `http://${host}:${actualPort}`,
        close: () => new Promise((closeResolve, closeReject) => {
          server.close((error) => error ? closeReject(error) : closeResolve());
        })
      });
    });
  });
}
async function runProjectServiceCLI(argv = [], env = process.env) {
  const command = argv[0] ?? "serve";
  if (command !== "serve") {
    throw new Error(`unsupported project-service command: ${command}`);
  }
  const host = env.MOVSCRIPT_PROJECT_SERVICE_HOST || "127.0.0.1";
  const port = Number(env.MOVSCRIPT_PROJECT_SERVICE_PORT || env.PORT || 0);
  const logger = env.MOVSCRIPT_PROJECT_SERVICE_PERF_LOG === "0" ? void 0 : projectServiceConsoleLogger;
  const runtime = await startProjectService({ host, port, logger });
  process.stdout.write(JSON.stringify({
    serviceName: PROJECT_SERVICE_NAME,
    url: runtime.url
  }) + "\n");
  await waitForShutdown4(runtime);
}
function writeJSON4(response, statusCode, payload) {
  const body = JSON.stringify(payload);
  const scope = response.__projectServiceRequestScope;
  const headers = {
    "content-type": "application/json; charset=utf-8",
    "content-length": String(Buffer.byteLength(body)),
    ...projectServiceCORSHeaders()
  };
  if (scope?.requestId) headers["x-request-id"] = scope.requestId;
  response.writeHead(statusCode, {
    ...headers
  });
  response.end(body);
  logProjectServiceRequest(scope, {
    statusCode,
    responseBytes: Buffer.byteLength(body)
  });
}
function createProjectServiceRequestScope({ request, url, logger }) {
  const routeKind = projectServiceObservedRouteKind(url.pathname);
  return {
    logger: typeof logger === "function" ? logger : void 0,
    observed: Boolean(routeKind),
    requestId: projectServiceRequestId(request),
    method: request.method ?? "GET",
    endpoint: url.pathname,
    routeKind,
    startedAtMs: Date.now()
  };
}
function projectServiceRequestId(request) {
  const header = request.headers?.["x-request-id"];
  const explicit = Array.isArray(header) ? header[0] : header;
  if (typeof explicit === "string" && explicit.trim()) return explicit.trim();
  return `ps_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 8)}`;
}
function projectServiceObservedRouteKind(pathname) {
  if (pathname === PROJECT_SERVICE_READ_MODEL_ENDPOINT || pathname === PROJECT_SERVICE_HOME_READ_MODEL_ENDPOINT || pathname === PROJECT_SERVICE_STANDARDS_READ_MODEL_ENDPOINT || pathname === PROJECT_SERVICE_CONTENT_CANVAS_READ_MODEL_ENDPOINT || pathname === PROJECT_SERVICE_SCRIPTS_READ_MODEL_ENDPOINT || pathname === PROJECT_SERVICE_CONTENT_UNITS_READ_MODEL_ENDPOINT) {
    return "read-model";
  }
  if (pathname === PROJECT_SERVICE_PROMPT_CONTEXT_ENDPOINT) return "prompt";
  if (pathname === PROJECT_SERVICE_RESOURCE_VIEW_ENDPOINT) return "resource";
  if (pathname === PROJECT_SERVICE_SOURCE_SNAPSHOT_ENDPOINT || pathname === PROJECT_SERVICE_SOURCE_INSPECT_ENDPOINT || pathname === PROJECT_SERVICE_SOURCE_OVERVIEW_ENDPOINT || pathname === PROJECT_SERVICE_SOURCE_INTERPRET_ENDPOINT || pathname === PROJECT_SERVICE_SOURCE_REGENERATION_PLAN_ENDPOINT || pathname === PROJECT_SERVICE_SOURCE_COMMAND_ENDPOINT || PROJECT_SOURCE_OPERATION_ROUTES.has(pathname)) {
    return "source";
  }
  return void 0;
}
function logProjectServiceRequest(scope, fields) {
  if (!scope?.observed || !scope.logger) return;
  scope.logger({
    event: "project_service.request",
    requestId: scope.requestId,
    method: scope.method,
    endpoint: scope.endpoint,
    routeKind: scope.routeKind,
    durationMs: Math.max(0, Date.now() - scope.startedAtMs),
    ...projectServiceMetricsForLog(scope.metrics),
    ...fields
  });
}
function projectServiceConsoleLogger(event) {
  process.stderr.write(`${JSON.stringify(event)}
`);
}
function addProjectServiceMetric(scope, key, value) {
  if (!scope) return;
  scope.metrics = scope.metrics ?? {};
  if (typeof value === "number" && Number.isFinite(value)) {
    scope.metrics[key] = Number(scope.metrics[key] ?? 0) + value;
    return;
  }
  scope.metrics[key] = value;
}
function setProjectServiceMetric(scope, key, value) {
  if (!scope) return;
  scope.metrics = scope.metrics ?? {};
  scope.metrics[key] = value;
}
async function observeProjectServicePhase(scope, metricKey, operation) {
  const startedAtMs = Date.now();
  try {
    return await operation();
  } finally {
    addProjectServiceMetric(scope, metricKey, Date.now() - startedAtMs);
  }
}
function projectServiceMetricsForLog(metrics = {}) {
  return Object.fromEntries(Object.entries(metrics).map(([key, value]) => [
    key,
    typeof value === "number" && Number.isFinite(value) ? Math.round(value * 10) / 10 : value
  ]));
}
function writeCORSPreflight(response) {
  response.writeHead(204, {
    ...projectServiceCORSHeaders(),
    "content-length": "0"
  });
  response.end();
}
function projectServiceCORSHeaders() {
  return {
    "access-control-allow-origin": "*",
    "access-control-allow-methods": "GET,POST,OPTIONS",
    "access-control-allow-headers": "content-type, authorization, x-user-id, x-org-id",
    "access-control-allow-private-network": "true",
    "access-control-max-age": "86400",
    vary: "Origin, Access-Control-Request-Method, Access-Control-Request-Headers"
  };
}
async function readProjectSourceContext(request) {
  const body = await readJSONBody4(request);
  const projectDir = projectDirFromBody2(body);
  return {
    body,
    projectDir,
    requestScope: request.__projectServiceRequestScope,
    fileRepository: createNodeMovScriptWorkspaceFileRepository(projectDir),
    decisionStore: await optionalDecisionStoreFromBody2(body, projectDir),
    debugArtifacts: body.debugArtifacts !== false && body.debug_artifacts !== false,
    inspectOptions: {
      ...stringValue13(body.commit) ? { commit: stringValue13(body.commit) } : {},
      ...stringValue13(body.checkpointHash ?? body.checkpoint_hash) ? {
        checkpointHash: stringValue13(body.checkpointHash ?? body.checkpoint_hash)
      } : {}
    },
    sourceOptions: {
      includeContentUnitDecisionDocuments: body.includeContentUnitDecisionDocuments === true || body.include_content_unit_decision_documents === true
    },
    readModelOptions: {
      includeSource: body.includeSource === true || body.include_source === true,
      includeInspection: body.includeInspection === true || body.include_inspection === true
    }
  };
}
function projectContentCanvasEnvelope(projectDir, result, fallbackSchema) {
  const record = recordValue8(result) ?? {};
  return {
    ...record,
    schema: stringValue13(record.schema) ?? fallbackSchema,
    projectDir
  };
}
function projectSourceOperationEnvelope(projectDir, result, fallbackSchema) {
  return {
    schema: fallbackSchema,
    projectDir,
    result
  };
}
function createProjectWorkspaceEngine(context) {
  const cacheKey = projectWorkspaceEngineCacheKey(context);
  const cacheHit = projectServiceEngineCacheKeys.includes(cacheKey);
  setProjectServiceMetric(context.requestScope, "cacheHit", cacheHit);
  setProjectServiceMetric(context.requestScope, "engineCacheHit", cacheHit);
  rememberProjectWorkspaceEngineCacheKey(cacheKey);
  return projectServiceEngineRegistry.get({
    cacheKey,
    projectDir: context.projectDir,
    ...context.decisionStore ? { decisionStore: context.decisionStore } : {}
  });
}
function rememberProjectWorkspaceEngineCacheKey(cacheKey) {
  const existingIndex = projectServiceEngineCacheKeys.indexOf(cacheKey);
  if (existingIndex >= 0) projectServiceEngineCacheKeys.splice(existingIndex, 1);
  projectServiceEngineCacheKeys.push(cacheKey);
  while (projectServiceEngineCacheKeys.length > PROJECT_SERVICE_ENGINE_CACHE_LIMIT) {
    const evicted = projectServiceEngineCacheKeys.shift();
    if (evicted) projectServiceEngineRegistry.invalidate(evicted);
  }
}
function projectWorkspaceEngineCacheKey(context) {
  return [
    context.projectDir,
    decisionStoreCacheKeyFromBody(context.body)
  ].join(PROJECT_SERVICE_ENGINE_CACHE_SEPARATOR);
}
function decisionStoreCacheKeyFromBody(body = {}) {
  const explicit = recordValue8(body.decisionStore ?? body.decision_store);
  if (explicit) {
    return stableJSONString({
      kind: explicit.kind,
      baseUrl: explicit.baseUrl ?? explicit.base_url,
      projectUid: explicit.projectUid ?? explicit.project_uid,
      scopeKind: explicit.scopeKind ?? explicit.scope_kind,
      scopeId: explicit.scopeId ?? explicit.scope_id,
      token: explicit.token,
      headers: explicit.headers
    });
  }
  return stableJSONString({
    projectUid: body.projectUid ?? body.project_uid,
    dataServiceBaseURL: body.dataServiceBaseURL ?? body.dataServiceBaseUrl ?? body.data_service_base_url,
    scopeKind: body.scopeKind ?? body.scope_kind,
    scopeId: body.scopeId ?? body.scope_id,
    movScriptHomeDir: body.movScriptHomeDir ?? body.movscript_home_dir ?? body.workspaceDir ?? body.workspace_dir
  });
}
function stableJSONString(value) {
  if (Array.isArray(value)) return `[${value.map(stableJSONString).join(",")}]`;
  if (!recordValue8(value)) return JSON.stringify(value);
  return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${stableJSONString(value[key])}`).join(",")}}`;
}
function projectSourceOperationInput(body) {
  const explicit = recordValue8(body.input);
  if (explicit) return explicit;
  const {
    projectDir: _projectDir,
    project_dir: _project_dir,
    projectId: _projectId,
    project_id: _project_id,
    projectUid: _projectUid,
    project_uid: _project_uid,
    userId: _userId,
    user_id: _user_id,
    orgId: _orgId,
    org_id: _org_id,
    scopeId: _scopeId,
    scope_id: _scope_id,
    movScriptHomeDir: _movScriptHomeDir,
    movscript_home_dir: _movscript_home_dir,
    workspaceDir: _workspaceDir,
    workspace_dir: _workspace_dir,
    decisionStore: _decisionStore,
    decision_store: _decision_store,
    context: _context,
    command: _command,
    includeSource: _includeSource,
    include_source: _include_source,
    includeInspection: _includeInspection,
    include_inspection: _include_inspection,
    includeContentUnitDecisionDocuments: _includeContentUnitDecisionDocuments,
    include_content_unit_decision_documents: _include_content_unit_decision_documents,
    debugArtifacts: _debugArtifacts,
    debug_artifacts: _debug_artifacts,
    commit: _commit,
    checkpointHash: _checkpointHash,
    checkpoint_hash: _checkpoint_hash,
    expectedWorkspaceVersions: _expectedWorkspaceVersions,
    expected_workspace_versions: _expected_workspace_versions,
    ...input
  } = body;
  return input;
}
async function executeProjectSourceCommand({ projectDir, command, input, decisionStore, now = /* @__PURE__ */ new Date() }) {
  const fileRepository = createNodeMovScriptWorkspaceFileRepository(projectDir);
  const engine = createNodeMovScriptEngine({ projectDir, ...decisionStore ? { decisionStore } : {} });
  switch (command) {
    case "queryEntities":
      return engine.workspaceService.queryEntities(input.query);
    case "querySettings":
      return engine.workspaceService.querySettings(input.query);
    case "queryAssets":
      return engine.workspaceService.queryAssets(input.query);
    case "readScriptSource":
      return engine.workspaceService.readScriptSource(input);
    case "readContentUnitGenerationPrompt":
      return engine.workspaceService.readContentUnitGenerationPrompt(requiredContentUnitId2(input));
    case "buildContentUnitBackendPrompt":
      return engine.buildContentUnitBackendPrompt(requiredContentUnitId2(input));
    case "loadContentWorkspaceSnapshot":
      return loadContentSourceWorkspaceSnapshotFromEngine(engine);
    case "loadContentWorkspace":
      return buildContentSourceWorkspaceData(await loadContentSourceWorkspaceSnapshotFromEngine(engine));
    case "listContentCanvases":
      return listProjectContentCanvases(fileRepository);
    case "writeContentCanvas":
      return writeProjectContentCanvas(fileRepository, input);
    case "renameContentCanvas":
      return renameProjectContentCanvas(fileRepository, input);
    case "runContentCanvas":
      return runProjectContentCanvas({ projectDir, fileRepository, engine, input, now });
    case "deleteContentCanvas":
      return deleteProjectContentCanvas(fileRepository, input);
    case "upsertProjectStandards":
      return engine.workspaceService.upsertProjectStandards(input);
    case "upsertSetting":
      return engine.workspaceService.upsertSetting(input);
    case "upsertAsset":
      return engine.workspaceService.upsertAsset(input);
    case "createSetting":
      return engine.createSetting(input);
    case "createSettingState":
      return engine.createSettingState(input);
    case "createAsset":
      return engine.createAsset(input);
    case "upsertScript":
      return engine.workspaceService.upsertScript(input);
    case "snapshotScriptVersionFromMarkdown":
      return engine.workspaceService.snapshotScriptVersionFromMarkdown(input);
    case "saveProductionSnapshot":
      return engine.workspaceService.saveProductionSnapshot(input);
    case "upsertContentUnit":
      return engine.workspaceService.upsertContentUnit(input);
    case "selectCandidate":
      return engine.workspaceService.selectCandidate(input);
    case "appendCandidate":
      return engine.workspaceService.appendCandidate(input);
    case "createAssetSlotCandidate":
      return engine.workspaceService.createAssetSlotCandidate(input);
    case "createKeyframeCandidate":
      return engine.workspaceService.createKeyframeCandidate(input);
    case "createContentUnit":
      return engine.createContentUnit(input);
    case "ensureContentUnitForEntity":
      return engine.ensureContentUnitForEntity(input);
    case "ensureTimelineAssemblyContentUnit":
      return engine.ensureContentUnitForEntity(timelineAssemblyContentUnitInput(input));
    case "createProduction":
      return engine.createProduction(input);
    case "createSegment":
      return engine.createSegment(input);
    case "createSceneMoment":
      return engine.createSceneMoment(input);
    case "createStoryboard":
      return engine.createStoryboard(input);
    case "createKeyframe":
      return engine.createKeyframe(input);
    case "createAudioCue":
      return engine.createAudioCue(input);
    case "createExpressionUnit":
      return engine.createExpressionUnit(input);
    case "updateContentUnitEditPrompt":
      return engine.workspaceService.updateContentUnitEditPrompt(input);
    case "updateExpressionUnit":
      return engine.workspaceService.updateExpressionUnitSource(input);
    case "updateAudioCue":
      return engine.workspaceService.updateAudioCueSource(input);
    case "updateEntityBasics":
      return engine.updateEntityBasics(input);
    case "connectSceneMomentSetting":
      return engine.connectSceneMomentSetting(input);
    case "updateEntityTransition":
      return engine.workspaceService.updateEntityTransition(input);
    case "updateStoryboardTimeline":
      return engine.workspaceService.updateStoryboardTimeline(input);
    case "writeHierarchyNode":
      return engine.writeHierarchyNode(input);
    case "writeNamespaceNode":
      return engine.writeHierarchyNode(namespaceHierarchyNodeInput(input));
    case "syncContentWorkspace":
      return engine.interpret();
    case "deleteEntity":
      await engine.workspaceService.deleteEntity(input);
      return { status: "deleted" };
    default:
      throw httpError3(400, "project_source_command_unsupported", `unsupported project source command: ${command}`);
  }
}
function requiredContentUnitId2(input) {
  const contentUnitId = input.contentUnitId ?? input.content_unit_id;
  if (typeof contentUnitId === "string" && contentUnitId.trim()) return contentUnitId.trim();
  if (typeof contentUnitId === "number" && Number.isFinite(contentUnitId)) return contentUnitId;
  throw httpError3(400, "project_content_unit_required", "contentUnitId is required");
}
async function listProjectContentCanvases(fileRepository) {
  const root = await fileRepository.list({ path: CONTENT_CANVAS_DIRECTORY });
  const canvases = [];
  for (const entry of root.entries) {
    if (entry.kind !== "directory") continue;
    const path = `${entry.path}/${CONTENT_CANVAS_FILE_NAME}`;
    const file = await fileRepository.read({ path }).catch((error) => {
      if (isNotFoundError4(error)) return void 0;
      throw error;
    });
    if (!file) continue;
    const record = parseProjectContentCanvasFile(file.content, path);
    canvases.push({
      canvasKind: "content",
      canvas_kind: "content",
      owner: "project-service",
      path: file.path,
      version: file.version,
      updatedAt: file.updatedAt,
      record
    });
  }
  canvases.sort((left, right) => {
    const updated = String(right.record.updated_at ?? "").localeCompare(String(left.record.updated_at ?? ""));
    return updated || String(left.record.title ?? left.record.id).localeCompare(String(right.record.title ?? right.record.id));
  });
  return {
    schema: CONTENT_CANVASES_SCHEMA,
    canvases
  };
}
async function writeProjectContentCanvas(fileRepository, input) {
  const record = projectContentCanvasRecordFromInput(input);
  const titleValidationError = validateProjectContentCanvasTitle(record.title);
  if (titleValidationError) throw titleValidationError;
  const path = contentCanvasProjectFilePath(record.id);
  const source = recordValue8(input) ?? {};
  const expectedVersion = stringValue13(source.expectedVersion ?? source.expected_version);
  const written = await fileRepository.write({
    path,
    content: `${JSON.stringify(record, null, 2)}
`,
    ...expectedVersion !== void 0 ? { expectedVersion } : {}
  });
  return {
    status: "written",
    canvasKind: "content",
    canvas_kind: "content",
    path: written.path,
    version: written.version,
    title: record.title,
    normalizedTitle: record.title,
    record,
    diagnostics: []
  };
}
async function renameProjectContentCanvas(fileRepository, input) {
  const source = recordValue8(input) ?? {};
  const id = stringValue13(source.id ?? source.canvasId ?? source.canvas_id);
  if (!id) throw httpError3(400, "project_content_canvas_id_required", "canvas id is required");
  const title = normalizeProjectContentCanvasTitle(source.title ?? source.name);
  const titleValidationError = validateProjectContentCanvasTitle(title);
  if (titleValidationError) throw titleValidationError;
  const path = contentCanvasProjectFilePath(id);
  const file = await fileRepository.read({ path }).catch((error) => {
    if (isNotFoundError4(error)) return void 0;
    throw error;
  });
  if (!file) throw httpError3(404, "project_content_canvas_not_found", `content canvas not found: ${id}`);
  const current = parseProjectContentCanvasFile(file.content, path);
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const record = {
    ...current,
    title,
    name: title,
    updated_at: now
  };
  const expectedVersion = stringValue13(source.expectedVersion ?? source.expected_version);
  const written = await fileRepository.write({
    path,
    content: `${JSON.stringify(record, null, 2)}
`,
    ...expectedVersion !== void 0 ? { expectedVersion } : {}
  });
  return {
    status: "renamed",
    canvasKind: "content",
    canvas_kind: "content",
    path: written.path,
    version: written.version,
    title,
    normalizedTitle: title,
    record,
    diagnostics: []
  };
}
async function runProjectContentCanvas({ projectDir, fileRepository, engine, input, now }) {
  const source = recordValue8(input) ?? {};
  const canvasId = stringValue13(source.id ?? source.canvasId ?? source.canvas_id);
  if (!canvasId) throw httpError3(400, "project_content_canvas_id_required", "canvas id is required");
  const canvas = await readProjectContentCanvas(fileRepository, canvasId);
  const interpretation = await engine.interpret();
  const contentSnapshot = await loadContentSourceWorkspaceSnapshotFromEngine(engine);
  const contentData = buildContentSourceWorkspaceData(contentSnapshot);
  const contentUnitSummaries = contentSourceWorkspaceContentUnitStatusSummaries(contentSnapshot);
  const affectedContentUnitIds = contentCanvasRunAffectedContentUnitIds(canvas.record, contentSnapshot);
  const candidateImpact = contentCanvasRunCandidateImpact({
    affectedContentUnitIds,
    contentUnitSummaries,
    contentData
  });
  const projectTimelineStatus = buildContentSourceWorkspaceProjectTimelineStatus(contentSnapshot, contentUnitSummaries);
  const operationId = `content-canvas-run:${contentCanvasProjectPathSegment(canvas.record.id)}:${now.getTime()}`;
  return {
    schema: "movscript.content_canvas_run.v1",
    status: "completed",
    operationId,
    operation_id: operationId,
    canvasId: canvas.record.id,
    canvas_id: canvas.record.id,
    canvas: {
      canvasKind: "content",
      canvas_kind: "content",
      owner: "project-service",
      path: canvas.path,
      version: canvas.version,
      record: canvas.record
    },
    trace: {
      projectDir,
      command: "runContentCanvas",
      interpretationId: interpretation?.manifest?.interpretationId,
      interpretation_id: interpretation?.manifest?.interpretationId,
      editorStatePath: interpretation?.manifest?.output?.editorStatePath,
      completedAt: now.toISOString(),
      completed_at: now.toISOString()
    },
    readModel: {
      schema: "movscript.content_canvas_run_read_model_summary.v1",
      status: projectTimelineStatus.status,
      timelineNamespaceCount: projectTimelineStatus.timeline_namespace_count,
      timeline_namespace_count: projectTimelineStatus.timeline_namespace_count,
      timelineAssemblyCount: projectTimelineStatus.timeline_assembly_count,
      timeline_assembly_count: projectTimelineStatus.timeline_assembly_count,
      systemPrimitives: projectTimelineStatus.system_primitives,
      system_primitives: projectTimelineStatus.system_primitives
    },
    candidateImpact,
    candidate_impact: candidateImpact
  };
}
async function deleteProjectContentCanvas(fileRepository, input) {
  const id = stringValue13(input.id ?? input.canvasId ?? input.canvas_id);
  if (!id) throw httpError3(400, "project_content_canvas_id_required", "canvas id is required");
  const path = contentCanvasProjectFilePath(id);
  await fileRepository.delete({ path });
  return {
    status: "deleted",
    canvasKind: "content",
    canvas_kind: "content",
    path
  };
}
async function readProjectContentCanvas(fileRepository, id) {
  const path = contentCanvasProjectFilePath(id);
  const file = await fileRepository.read({ path }).catch((error) => {
    if (isNotFoundError4(error)) return void 0;
    throw error;
  });
  if (!file) throw httpError3(404, "project_content_canvas_not_found", `content canvas not found: ${id}`);
  return {
    canvasKind: "content",
    canvas_kind: "content",
    owner: "project-service",
    path: file.path,
    version: file.version,
    updatedAt: file.updatedAt,
    record: parseProjectContentCanvasFile(file.content, path)
  };
}
function parseProjectContentCanvasFile(content, path) {
  try {
    return projectContentCanvasRecordFromInput(JSON.parse(content), { path });
  } catch (error) {
    if (error?.statusCode) throw error;
    throw httpError3(400, "project_content_canvas_invalid", `content canvas file is invalid: ${path}`);
  }
}
function contentCanvasRunAffectedContentUnitIds(canvasRecord, snapshot) {
  const nodeRefs = /* @__PURE__ */ new Set();
  for (const node of Array.isArray(canvasRecord.nodes) ? canvasRecord.nodes : []) {
    const nodeId2 = stringValue13(node.node_id ?? node.nodeId ?? node.id);
    const kind = stringValue13(node.kind);
    if (!nodeId2) continue;
    const suffix = contentCanvasNodeIdSuffix(nodeId2);
    nodeRefs.add(nodeId2);
    nodeRefs.add(suffix);
    if (kind) {
      nodeRefs.add(`${kind}:${suffix}`);
      nodeRefs.add(`${kind}:${nodeId2}`);
    }
  }
  const affected = [];
  for (const unit of snapshot.contentUnits ?? []) {
    const unitId = idValue4(unit.id ?? unit.record?.id ?? pathSegmentAfter10(unit.path, "content_units"));
    if (unitId === void 0) continue;
    const refs = contentUnitRunRefs(unit);
    if (refs.some((ref) => nodeRefs.has(ref) || nodeRefs.has(contentCanvasNodeIdSuffix(ref)))) {
      affected.push(String(unitId));
    }
  }
  return affected;
}
function contentUnitRunRefs(unit) {
  const record = recordValue8(unit.record) ?? {};
  const id = idValue4(unit.id ?? record.id ?? pathSegmentAfter10(unit.path, "content_units"));
  const refs = [
    id,
    id !== void 0 ? `content_unit:${id}` : void 0,
    unit.path,
    id !== void 0 ? `content_units/${id}` : void 0,
    record.target_ref,
    record.targetRef,
    record.expression_unit_ref,
    record.expressionUnitRef,
    record.storyboard_ref,
    record.storyboardRef,
    record.keyframe_ref,
    record.keyframeRef,
    record.audio_cue_ref,
    record.audioCueRef,
    record.asset_ref,
    record.assetRef,
    record.scene_moment_ref,
    record.sceneMomentRef
  ].map(idValue4).filter((value) => value !== void 0).map(String);
  return [...new Set(refs.flatMap((ref) => [ref, contentCanvasNodeIdSuffix(ref)]))];
}
function contentCanvasNodeIdSuffix(value) {
  const text = String(value ?? "").trim();
  if (!text) return "";
  const colonParts = text.split(":").filter(Boolean);
  if (colonParts.length > 1) return colonParts[colonParts.length - 1];
  const slashParts = text.split("/").filter(Boolean);
  return slashParts[slashParts.length - 1] ?? text;
}
function contentCanvasRunCandidateImpact({ affectedContentUnitIds, contentUnitSummaries, contentData }) {
  const affected = new Set(affectedContentUnitIds.map(String));
  const summaries = contentUnitSummaries.filter((summary) => {
    if (affected.size === 0) return false;
    return affected.has(String(summary.content_unit_id));
  });
  const candidateCounts = Object.fromEntries(summaries.map((summary) => [
    String(summary.content_unit_id),
    numberValue7(summary.candidate_count) ?? 0
  ]));
  const selectedContentUnitIds = summaries.filter((summary) => summary.selected_candidate !== void 0).map((summary) => String(summary.content_unit_id));
  const missingSelectionContentUnitIds = summaries.filter((summary) => Array.isArray(summary.blocking_refs) && summary.blocking_refs.includes("selection_missing")).map((summary) => String(summary.content_unit_id));
  return {
    schema: "movscript.content_canvas_candidate_impact.v1",
    affectedContentUnitIds: summaries.map((summary) => String(summary.content_unit_id)),
    affected_content_unit_ids: summaries.map((summary) => String(summary.content_unit_id)),
    affectedContentUnitCount: summaries.length,
    affected_content_unit_count: summaries.length,
    candidateCounts,
    candidate_counts: candidateCounts,
    selectedContentUnitIds,
    selected_content_unit_ids: selectedContentUnitIds,
    missingSelectionContentUnitIds,
    missing_selection_content_unit_ids: missingSelectionContentUnitIds,
    totalCandidateCount: summaries.reduce((sum, summary) => sum + (numberValue7(summary.candidate_count) ?? 0), 0),
    total_candidate_count: summaries.reduce((sum, summary) => sum + (numberValue7(summary.candidate_count) ?? 0), 0),
    workspaceCandidateMapCount: Object.keys(recordValue8(contentData.contentUnitCandidates) ?? {}).length,
    workspace_candidate_map_count: Object.keys(recordValue8(contentData.contentUnitCandidates) ?? {}).length
  };
}
function projectContentCanvasRecordFromInput(input, options = {}) {
  const source = recordValue8(input);
  const record = source ? recordValue8(source.canvas ?? source.record) ?? source : void 0;
  if (!record) throw httpError3(400, "project_content_canvas_required", "content canvas record is required");
  const id = stringValue13(record.id ?? record.canvasId ?? record.canvas_id) ?? contentCanvasProjectIdFromPath(options.path) ?? createProjectContentCanvasId();
  const updatedAt = stringValue13(record.updated_at ?? record.updatedAt) ?? (/* @__PURE__ */ new Date()).toISOString();
  const titleInput = contentCanvasTitleInput(record);
  const title = titleInput === void 0 ? contentCanvasTitleFromProjectPath(options.path) ?? "Untitled Canvas" : normalizeProjectContentCanvasTitle(titleInput);
  return pruneUndefinedRecord3({
    schema: CONTENT_CANVAS_SCHEMA,
    kind: "content_canvas",
    canvasKind: "content",
    canvas_kind: "content",
    id,
    title,
    name: title,
    scope: projectContentCanvasScope(record.scope),
    nodes: projectContentCanvasNodes(record.nodes),
    layouts: projectContentCanvasLayouts(record.layouts ?? record.node_layouts ?? record.nodeLayouts),
    updated_at: updatedAt,
    created_at: stringValue13(record.created_at ?? record.createdAt)
  });
}
function contentCanvasTitleInput(record) {
  if (Object.prototype.hasOwnProperty.call(record, "title")) return record.title;
  if (Object.prototype.hasOwnProperty.call(record, "name")) return record.name;
  return void 0;
}
function normalizeProjectContentCanvasTitle(value) {
  return typeof value === "string" ? value.trim().replace(/\s+/g, " ") : "";
}
function validateProjectContentCanvasTitle(title) {
  if (!title) return httpError3(400, "project_content_canvas_title_required", "content canvas title is required");
  if (title.length > CONTENT_CANVAS_TITLE_MAX_LENGTH) {
    return httpError3(400, "project_content_canvas_title_too_long", `content canvas title must be at most ${CONTENT_CANVAS_TITLE_MAX_LENGTH} characters`);
  }
  if (CONTENT_CANVAS_TITLE_INVALID_PATTERN.test(title)) {
    return httpError3(400, "project_content_canvas_title_invalid", "content canvas title contains unsupported characters");
  }
  return void 0;
}
function projectContentCanvasScope(value) {
  const scope = recordValue8(value);
  if (!scope || scope.kind === "global") return { kind: "global" };
  if (scope.kind !== "production") return { kind: "global" };
  const productionId = stringValue13(scope.production_id ?? scope.productionId);
  if (!productionId) return { kind: "global" };
  return pruneUndefinedRecord3({
    kind: "production",
    production_id: productionId,
    production_title: stringValue13(scope.production_title ?? scope.productionTitle),
    production_node_id: stringValue13(scope.production_node_id ?? scope.productionNodeId),
    production_path: stringValue13(scope.production_path ?? scope.productionPath)
  });
}
function projectContentCanvasNodes(value) {
  const values = Array.isArray(value) ? value : Object.values(recordValue8(value) ?? {});
  return values.map(projectContentCanvasNode).filter(Boolean).sort((left, right) => left.node_id.localeCompare(right.node_id));
}
function projectContentCanvasNode(value) {
  const node = recordValue8(value);
  if (!node) return void 0;
  const nodeId2 = stringValue13(node.node_id ?? node.nodeId ?? node.id);
  if (!nodeId2) return void 0;
  return pruneUndefinedRecord3({
    node_id: nodeId2,
    kind: stringValue13(node.kind),
    added_at: stringValue13(node.added_at ?? node.addedAt)
  });
}
function projectContentCanvasLayouts(value) {
  const layouts = recordValue8(value);
  if (!layouts) return {};
  return Object.fromEntries(Object.entries(layouts).map(([nodeId2, layout]) => [nodeId2, projectContentCanvasLayout(layout)]).filter(([, layout]) => Boolean(layout)));
}
function projectContentCanvasLayout(value) {
  const layout = recordValue8(value);
  if (!layout) return void 0;
  const x = numberValue7(layout.x);
  const y = numberValue7(layout.y);
  const width = numberValue7(layout.width);
  const height = numberValue7(layout.height);
  if (x === void 0 || y === void 0 || width === void 0 || height === void 0) return void 0;
  return pruneUndefinedRecord3({
    x,
    y,
    width,
    height,
    manual: layout.manual === true,
    source: stringValue13(layout.source),
    updated_at: stringValue13(layout.updated_at ?? layout.updatedAt)
  });
}
function contentCanvasProjectFilePath(id) {
  return `${CONTENT_CANVAS_DIRECTORY}/${contentCanvasProjectPathSegment(id)}/${CONTENT_CANVAS_FILE_NAME}`;
}
function contentCanvasProjectPathSegment(id) {
  const safe = String(id).trim().replace(/[^a-zA-Z0-9._-]+/g, "_").replace(/^_+|_+$/g, "");
  return safe || "canvas";
}
function contentCanvasProjectIdFromPath(path) {
  const segment = contentCanvasProjectPathSegmentFromPath(path);
  return segment ? String(segment).trim() : void 0;
}
function contentCanvasTitleFromProjectPath(path) {
  const segment = contentCanvasProjectPathSegmentFromPath(path);
  if (!segment) return void 0;
  return segment.replace(/^canvas[_-]?/i, "").replace(/[_-]+/g, " ").trim() || void 0;
}
function contentCanvasProjectPathSegmentFromPath(path) {
  const parts = String(path ?? "").split(/[\\/]+/).filter(Boolean);
  const candidate = parts.at(-1) === CONTENT_CANVAS_FILE_NAME ? parts.at(-2) : parts.at(-1);
  return stringValue13(candidate);
}
function createProjectContentCanvasId() {
  return `canvas:${Date.now().toString(36)}:${Math.random().toString(36).slice(2, 8)}`;
}
function timelineAssemblyContentUnitInput(input) {
  return {
    ...input,
    targetKind: "timeline_assembly",
    target_kind: void 0
  };
}
function namespaceHierarchyNodeInput(input) {
  const targetPath = stringValue13(input.targetPath ?? input.target_path);
  if (!targetPath) {
    throw httpError3(400, "project_namespace_target_path_required", "targetPath is required for writeNamespaceNode");
  }
  const category = stringValue13(input.category ?? input.domainCategory ?? input.domain_category);
  if (category !== "timeline_namespace" && category !== "setting_namespace") {
    throw httpError3(400, "project_namespace_category_unsupported", "category must be timeline_namespace or setting_namespace");
  }
  const entityKind = namespaceEntityKindFromPath(targetPath);
  if (!entityKind || !namespaceCategoryAllowsEntityKind(category, entityKind)) {
    throw httpError3(400, "project_namespace_target_path_unsupported", `targetPath is not valid for ${category}`);
  }
  const record = recordValue8(input.record) ?? {};
  const namespaceKind = stringValue13(
    input.namespaceKind ?? input.namespace_kind ?? input.domainKind ?? input.domain_kind ?? input.kind ?? record.namespace_kind ?? record.namespaceKind ?? record.timeline_namespace_kind ?? record.timelineNamespaceKind ?? record.setting_namespace_kind ?? record.settingNamespaceKind
  );
  if (!namespaceKind) {
    throw httpError3(400, "project_namespace_kind_required", "namespace kind is required for writeNamespaceNode");
  }
  return {
    targetPath,
    record: pruneUndefinedRecord3({
      ...record,
      schema: `movscript.${entityKind}.v1`,
      kind: entityKind,
      id: idValue4(input.id ?? record.id) ?? namespaceEntityIdFromPath(targetPath, entityKind),
      title: stringValue13(input.title ?? record.title),
      project_id: idValue4(input.projectId ?? input.project_id ?? record.project_id),
      order: numberValue7(input.order ?? record.order),
      intent: stringValue13(input.intent ?? record.intent),
      namespace_kind: namespaceKind,
      ...category === "timeline_namespace" ? { timeline_namespace_kind: namespaceKind } : {},
      ...category === "setting_namespace" ? { setting_namespace_kind: namespaceKind } : {}
    })
  };
}
function namespaceCategoryAllowsEntityKind(category, entityKind) {
  if (category === "timeline_namespace") return entityKind === "production" || entityKind === "segment";
  if (category === "setting_namespace") return entityKind === "setting" || entityKind === "setting_state";
  return false;
}
function namespaceEntityKindFromPath(path) {
  if (path.endsWith("/production.json")) return "production";
  if (path.endsWith("/segment.json")) return "segment";
  if (path.endsWith("/setting.json")) return "setting";
  if (path.endsWith("/setting_state.json")) return "setting_state";
  return void 0;
}
function namespaceEntityIdFromPath(targetPath, entityKind) {
  const collection = {
    production: "timeline",
    segment: "segments",
    setting: "settings",
    setting_state: "states"
  }[entityKind];
  return pathSegmentAfter10(targetPath, collection) ?? targetPath.split("/").filter(Boolean).at(-2);
}
async function readProjectReadModel(context, now) {
  const engine = createProjectWorkspaceEngine(context);
  const [
    overview,
    contentSnapshot,
    source,
    inspection
  ] = await Promise.all([
    observeProjectServicePhase(context.requestScope, "deriveMs", () => overviewMovScriptWorkspace({
      fileRepository: context.fileRepository,
      decisionStore: context.decisionStore,
      now
    })),
    observeProjectServicePhase(context.requestScope, "deriveMs", () => loadContentSourceWorkspaceSnapshotFromEngine(engine)),
    context.readModelOptions.includeSource ? observeProjectServicePhase(context.requestScope, "deriveMs", () => resolveWorkspaceSource(context.fileRepository, context.sourceOptions)) : void 0,
    context.readModelOptions.includeInspection ? observeProjectServicePhase(context.requestScope, "deriveMs", () => inspectMovScriptWorkspace({
      fileRepository: context.fileRepository,
      decisionStore: context.decisionStore,
      now,
      ...context.inspectOptions
    })) : void 0
  ]);
  const projectTimelineStatus = await observeProjectServicePhase(
    context.requestScope,
    "deriveMs",
    async () => buildContentSourceWorkspaceProjectTimelineStatus(contentSnapshot)
  );
  return {
    schema: "movscript.project-read-model.v1",
    status: overview.status,
    workspace: overview.workspace,
    sourceSummary: overview.source,
    productionSummary: overview.production,
    contentSummary: overview.content,
    readiness: overview.readiness,
    projectTimelineStatus,
    project_timeline_status: projectTimelineStatus,
    overview,
    ...source ? { source } : {},
    ...inspection ? { inspection } : {}
  };
}
async function readProjectHomeReadModel(context, now) {
  const engine = createProjectWorkspaceEngine(context);
  const index = await observeProjectServicePhase(context.requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex());
  return observeProjectServicePhase(context.requestScope, "deriveMs", async () => {
    const project = queryMovScriptWorkspaceEntities(index, { entityKind: "project", limit: 1 })[0];
    const scripts = queryMovScriptWorkspaceEntities(index, { entityKind: "script", limit: 500 }).map((entity) => projectHomeRecord(entity));
    const settings = queryMovScriptWorkspaceSettings(index, { limit: 500 }).map((entity) => projectHomeRecord(entity));
    const assets = queryMovScriptWorkspaceAssets(index, { limit: 500 }).assets.map((entity) => projectHomeRecord(entity));
    const productionContext = queryMovScriptWorkspaceProductionContext(index, {
      include: ["productions", "scene_moments", "content_units"],
      limit: 1e3
    });
    const productions = (productionContext.productions ?? []).map((entity) => projectHomeRecord(entity));
    const sceneMoments = (productionContext.scene_moments ?? []).map((entity) => projectHomeRecord(entity));
    const contentUnits = (productionContext.content_units ?? []).map((entity) => projectHomeRecord(entity));
    return {
      schema: "movscript.project-home-read-model.v1",
      generatedAt: now.toISOString(),
      projectDir: context.projectDir,
      workspace: {
        projectId: stringValue13(project?.record.project_id ?? project?.record.projectId ?? project?.id),
        title: stringValue13(project?.record.title ?? project?.record.name),
        documentCount: index.documents.length
      },
      project: project ? projectHomeRecord(project) : void 0,
      scripts,
      settings,
      assets,
      productions,
      sceneMoments,
      contentUnits,
      counts: {
        scripts: scripts.length,
        settings: settings.length,
        assets: assets.length,
        productions: productions.length,
        sceneMoments: sceneMoments.length,
        contentUnits: contentUnits.length,
        library: scripts.length + settings.length + assets.length,
        pipeline: productions.length + sceneMoments.length + contentUnits.length,
        total: scripts.length + settings.length + assets.length + productions.length + sceneMoments.length + contentUnits.length
      }
    };
  });
}
async function readProjectStandardsReadModel(context, now) {
  const engine = createProjectWorkspaceEngine(context);
  const index = await observeProjectServicePhase(context.requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex());
  return observeProjectServicePhase(context.requestScope, "deriveMs", async () => {
    const project = queryMovScriptWorkspaceEntities(index, { entityKind: "project", limit: 1 })[0];
    const settings = queryMovScriptWorkspaceSettings(index, { limit: 500 }).map((entity) => projectHomeRecord(entity));
    const assets = queryMovScriptWorkspaceAssets(index, { limit: 500 }).assets.map((entity) => projectHomeRecord(entity));
    const productionContext = queryMovScriptWorkspaceProductionContext(index, {
      include: ["productions", "segments", "scene_moments", "content_units"],
      limit: 1e3
    });
    const productions = (productionContext.productions ?? []).map((entity) => projectHomeRecord(entity));
    const segments = (productionContext.segments ?? []).map((entity) => projectHomeRecord(entity));
    const sceneMoments = (productionContext.scene_moments ?? []).map((entity) => projectHomeRecord(entity));
    const contentUnits = (productionContext.content_units ?? []).map((entity) => projectHomeRecord(entity));
    return {
      schema: "movscript.project-standards-read-model.v1",
      generatedAt: now.toISOString(),
      projectDir: context.projectDir,
      workspace: {
        projectId: stringValue13(project?.record.project_id ?? project?.record.projectId ?? project?.id),
        title: stringValue13(project?.record.title ?? project?.record.name),
        documentCount: index.documents.length
      },
      project: project ? projectHomeRecord(project) : null,
      settings,
      assetSlots: assets,
      productions,
      segments,
      sceneMoments,
      contentUnits,
      creativeRelationships: [],
      settingUsages: [],
      assetSlotCandidates: [],
      counts: {
        settings: settings.length,
        assetSlots: assets.length,
        productions: productions.length,
        segments: segments.length,
        sceneMoments: sceneMoments.length,
        contentUnits: contentUnits.length,
        total: settings.length + assets.length + productions.length + segments.length + sceneMoments.length + contentUnits.length
      }
    };
  });
}
async function readProjectContentCanvasReadModel(context, now) {
  const engine = createProjectWorkspaceEngine(context);
  const [
    index,
    contentSnapshot
  ] = await Promise.all([
    observeProjectServicePhase(context.requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex()),
    observeProjectServicePhase(context.requestScope, "deriveMs", () => loadContentSourceWorkspaceSnapshotFromEngine(engine))
  ]);
  return observeProjectServicePhase(context.requestScope, "deriveMs", async () => {
    const contentData = buildContentSourceWorkspaceData(contentSnapshot);
    const project = queryMovScriptWorkspaceEntities(index, { entityKind: "project", limit: 1 })[0] ?? null;
    const projectId = contentCanvasProjectId(context, project);
    const contentUnitCandidates = contentCanvasCandidatesFromContentWorkspace(contentData);
    const editingProjectsByNodeId = contentCanvasEditingProjectsByNodeId({
      data: contentData,
      sceneMoments: contentSnapshot.sceneMoments,
      productions: contentSnapshot.productions,
      segments: contentSnapshot.segments,
      projectId
    });
    return {
      schema: "movscript.project-content-canvas-read-model.v1",
      generatedAt: now.toISOString(),
      projectDir: context.projectDir,
      workspace: {
        projectId: stringValue13(project?.record.project_id ?? project?.record.projectId ?? project?.id),
        title: stringValue13(project?.record.title ?? project?.record.name),
        documentCount: index.documents.length
      },
      projectId,
      project,
      productions: sortProjectCanvasEntities(contentSnapshot.productions),
      segments: sortProjectCanvasEntities(contentSnapshot.segments),
      sceneMoments: sortProjectCanvasEntities(contentSnapshot.sceneMoments),
      storyboards: sortProjectCanvasEntities(contentSnapshot.storyboards),
      expressionUnits: sortProjectCanvasEntities(contentSnapshot.expressionUnits),
      contentUnits: sortProjectCanvasEntities(contentSnapshot.contentUnits),
      keyframes: sortProjectCanvasEntities(contentSnapshot.keyframes),
      settings: sortProjectCanvasEntities(contentSnapshot.settings),
      settingStates: sortProjectCanvasEntities(contentSnapshot.settingStates),
      audioCues: sortProjectCanvasEntities(contentSnapshot.audioCues),
      assets: sortProjectCanvasEntities(contentSnapshot.assets),
      contentUnitCandidates,
      domainGraph: contentData.domainGraph,
      editingProjectsByNodeId,
      assetReferenceUnits: contentData.assetReferenceUnits,
      productionWorkPlan: contentData.productionWorkPlan,
      counts: {
        productions: contentSnapshot.productions.length,
        segments: contentSnapshot.segments.length,
        sceneMoments: contentSnapshot.sceneMoments.length,
        expressionUnits: contentSnapshot.expressionUnits.length,
        contentUnits: contentSnapshot.contentUnits.length,
        settings: contentSnapshot.settings.length,
        assets: contentSnapshot.assets.length,
        candidates: Object.values(contentUnitCandidates).reduce((total, candidates) => total + candidates.length, 0),
        editingProjects: Object.keys(editingProjectsByNodeId).length
      }
    };
  });
}
async function readProjectScriptsReadModel(context, now) {
  const engine = createProjectWorkspaceEngine(context);
  const index = await observeProjectServicePhase(context.requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex());
  return observeProjectServicePhase(context.requestScope, "deriveMs", async () => {
    const project = queryMovScriptWorkspaceEntities(index, { entityKind: "project", limit: 1 })[0];
    const versions = queryMovScriptWorkspaceEntities(index, { entityKind: "script_version", limit: 1e3 }).map((entity) => projectScriptVersionSummaryRecord(entity));
    const versionsByScriptRef = projectScriptVersionsByScriptRef(versions);
    const scripts = queryMovScriptWorkspaceEntities(index, { entityKind: "script", limit: 500 }).map((entity) => projectScriptSummaryRecord(entity, versionsByScriptRef));
    return {
      schema: "movscript.project-scripts-read-model.v1",
      generatedAt: now.toISOString(),
      projectDir: context.projectDir,
      workspace: {
        projectId: stringValue13(project?.record.project_id ?? project?.record.projectId ?? project?.id),
        title: stringValue13(project?.record.title ?? project?.record.name),
        documentCount: index.documents.length
      },
      project: project ? projectHomeRecord(project) : null,
      scripts,
      versions,
      counts: {
        scripts: scripts.length,
        versions: versions.length,
        total: scripts.length + versions.length
      }
    };
  });
}
async function readProjectContentUnitsReadModel({ projectDir, body = {}, contentUnitIds, decisionStore, requestScope, now }) {
  const engine = createProjectWorkspaceEngine({ projectDir, decisionStore, body, requestScope });
  const index = await observeProjectServicePhase(requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex());
  const decisionContexts = decisionStore ? await readCandidateContexts(decisionStore, contentUnitIds, requestScope) : [];
  const decisionContextByContentUnitId = projectDecisionContextsByContentUnitId(decisionContexts);
  return observeProjectServicePhase(requestScope, "deriveMs", async () => {
    const contentUnitsById = new Map(queryMovScriptWorkspaceEntities(index, { entityKind: "content_unit", limit: 5e3 }).flatMap((entity) => projectContentUnitRefValues(entity).map((ref) => [ref, entity])));
    const contentUnits = contentUnitIds.map((contentUnitId) => {
      const ref = contentUnitRefValue(contentUnitId);
      const entity = ref ? contentUnitsById.get(ref) : void 0;
      return projectContentUnitReadModelRecord({
        contentUnitId,
        entity,
        decisionContext: ref ? decisionContextByContentUnitId.get(ref) : void 0
      });
    });
    return {
      schema: "movscript.project-content-units-read-model.v1",
      generatedAt: now.toISOString(),
      projectDir,
      contentUnitIds,
      contentUnits,
      counts: {
        requested: contentUnitIds.length,
        contentUnits: contentUnits.length,
        candidates: contentUnits.reduce((total, unit) => total + unit.candidates.length, 0),
        selected: contentUnits.filter((unit) => unit.selectionState === "selected").length
      }
    };
  });
}
function contentCanvasProjectId(context, project) {
  return numberValue7(context.body?.projectId ?? context.body?.project_id) ?? numberValue7(project?.record.ID ?? project?.record.id ?? project?.record.project_id ?? project?.record.projectId ?? project?.id) ?? 1;
}
function contentCanvasCandidatesFromContentWorkspace(data) {
  const output = {};
  for (const [contentUnitId, candidates] of Object.entries(data.contentUnitCandidates ?? {})) {
    appendContentCanvasCandidates(output, contentUnitId, candidates.map(contentCanvasCandidateFromPreview));
  }
  for (const moment of data.previewMoments ?? []) {
    for (const expressionUnit of moment.expressionUnits ?? []) {
      const contentUnitId = expressionUnit.contentUnit?.id;
      if (contentUnitId === void 0 || contentUnitId === null) continue;
      appendContentCanvasCandidates(output, String(contentUnitId), (expressionUnit.contentUnit.candidates ?? []).map(contentCanvasCandidateFromPreview));
    }
  }
  for (const assetUnit of Object.values(data.assetReferenceUnits ?? {})) {
    if (!assetUnit?.contentUnitId) continue;
    appendContentCanvasCandidates(output, String(assetUnit.contentUnitId), (assetUnit.candidates ?? []).map(contentCanvasCandidateFromPreview));
  }
  return output;
}
function contentCanvasCandidateFromPreview(candidate) {
  return {
    id: String(candidate.id),
    title: stringValue13(candidate.title) ?? String(candidate.id),
    resourceId: numberValue7(candidate.resourceId ?? candidate.resource_id),
    resourceKind: stringValue13(candidate.resourceKind ?? candidate.resource_kind),
    artifactRef: stringValue13(candidate.artifactRef ?? candidate.artifact_ref),
    inputHash: stringValue13(candidate.inputHash ?? candidate.input_hash),
    source: stringValue13(candidate.source ?? candidate.model) ?? "",
    status: stringValue13(candidate.status),
    decisionStatus: stringValue13(candidate.decisionStatus ?? candidate.decision_status),
    decisionReason: stringValue13(candidate.decisionReason ?? candidate.decision_reason),
    producer: recordValue8(candidate.producer),
    outputs: Array.isArray(candidate.outputs) ? candidate.outputs : void 0,
    promptSnapshot: recordValue8(candidate.promptSnapshot ?? candidate.prompt_snapshot),
    createdAt: stringValue13(candidate.createdAt ?? candidate.created_at),
    selected: Boolean(candidate.selected),
    notes: stringValue13(candidate.note ?? candidate.notes ?? candidate.inputHash ?? candidate.input_hash) ?? ""
  };
}
function appendContentCanvasCandidates(output, contentUnitId, candidates) {
  const byId = new Map((output[contentUnitId] ?? []).map((candidate) => [contentCanvasCandidateMergeKey(candidate), candidate]));
  for (const candidate of candidates) byId.set(contentCanvasCandidateMergeKey(candidate), candidate);
  output[contentUnitId] = [...byId.values()];
}
function contentCanvasCandidateMergeKey(candidate) {
  return [
    candidate.id,
    candidate.resourceId ?? "",
    candidate.artifactRef ?? "",
    candidate.inputHash ?? "",
    candidate.source ?? ""
  ].join(":");
}
function contentCanvasEditingProjectsByNodeId(input) {
  const output = {};
  const timelineNamespaceTargets = [...input.productions, ...input.segments];
  for (const timeline of input.data.editingTimelines ?? []) {
    const editingProject = timeline.mediaEditingProject;
    const targetId = String(timeline.targetId);
    output[targetId] = editingProject;
    output[`${timeline.targetKind}:${targetId}`] = editingProject;
    if (timeline.targetRef !== void 0) {
      output[String(timeline.targetRef)] = editingProject;
      output[`${timeline.targetKind}:${String(timeline.targetRef)}`] = editingProject;
    }
    if (timeline.scopeKind !== void 0 && timeline.scopeRef !== void 0) {
      output[`${timeline.scopeKind}:${String(timeline.scopeRef)}`] = editingProject;
      output[`timeline_assembly:${timeline.scopeKind}:${String(timeline.scopeRef)}`] = editingProject;
    }
    const targets = timeline.targetKind === "scene_moment" ? input.sceneMoments : timeline.targetKind === "timeline_assembly" ? timelineNamespaceTargets : input.productions;
    const target = targets.find((item) => String(item.id ?? item.record.ID ?? item.record.id ?? "") === targetId || timeline.targetPath !== void 0 && item.path === timeline.targetPath || timeline.scopePath !== void 0 && item.path === timeline.scopePath);
    if (target) output[contentCanvasNodeIdForEntity(target, input.projectId)] = editingProject;
  }
  return output;
}
function contentCanvasNodeIdForEntity(entity, projectId) {
  return `${contentCanvasKind(entity)}:${contentCanvasEntityKey(entity, projectId)}`;
}
function contentCanvasKind(entity) {
  if (entity.entityKind === "asset") return "asset";
  if (entity.entityKind === "setting_state") return "state";
  return entity.entityKind;
}
function contentCanvasEntityKey(entity, projectId) {
  if (entity.entityKind === "project") return String(entity.id ?? entity.record.project_id ?? projectId);
  return idValue4(entity.id ?? entity.record.ID ?? entity.record.id) ?? `${entity.entityKind}:${entity.path}`;
}
function sortProjectCanvasEntities(items) {
  return [...items].sort((left, right) => {
    const leftOrder = numberValue7(left.record.order);
    const rightOrder = numberValue7(right.record.order);
    if (leftOrder !== void 0 || rightOrder !== void 0) return (leftOrder ?? 0) - (rightOrder ?? 0);
    return projectCanvasEntityTitle(left).localeCompare(projectCanvasEntityTitle(right), "zh-CN");
  });
}
function projectCanvasEntityTitle(entity) {
  return String(entity.record.title ?? entity.record.name ?? entity.record.label ?? entity.id ?? entity.path);
}
function projectHomeRecord(entity) {
  return {
    ...entity.record,
    __workspace_entity_type: entity.entityKind,
    __workspace_path: entity.path,
    ...entity.id !== void 0 ? { id: entity.id } : {},
    ...entity.clientId !== void 0 ? { client_id: entity.clientId } : {},
    ...entity.schema !== void 0 ? { schema: entity.schema } : {}
  };
}
var PROJECT_SCRIPT_SOURCE_FIELDS = /* @__PURE__ */ new Set(["source", "content", "raw_source", "blocks"]);
function projectScriptSummaryRecord(entity, versionsByScriptRef) {
  const record = stripProjectScriptSourceFields(entity.record);
  const sourceRef = stringValue13(entity.record.source_ref ?? entity.record.sourceRef) ?? "script.md";
  const sourcePath = projectScriptSourcePath(entity, sourceRef);
  const scriptRefs = projectScriptRefValues(entity, record);
  const matchingVersions = uniqueProjectScriptVersions(scriptRefs.flatMap((ref) => versionsByScriptRef.get(ref) ?? [])).sort(compareProjectScriptVersionsDescending);
  const latestVersion = matchingVersions[0];
  const inlineBodyLength = projectScriptInlineBodyLength(entity.record);
  const bodyLength = numberValue7(entity.record.bodyLength ?? entity.record.body_length ?? entity.record.sourceLength ?? entity.record.source_length) ?? inlineBodyLength;
  const currentVersion = latestVersion ? projectScriptCurrentVersionSummary(latestVersion) : void 0;
  return pruneUndefinedRecord3({
    ...record,
    entityKind: entity.entityKind,
    path: entity.path,
    __workspace_entity_type: entity.entityKind,
    __workspace_path: entity.path,
    ...entity.id !== void 0 ? { id: entity.id } : {},
    ...entity.clientId !== void 0 ? { client_id: entity.clientId } : {},
    sourceRef,
    source_ref: sourceRef,
    sourcePath,
    source_path: sourcePath,
    bodyLength,
    body_length: bodyLength,
    sourceLoaded: false,
    source_loaded: false,
    versionCount: matchingVersions.length,
    version_count: matchingVersions.length,
    currentVersion,
    current_version: currentVersion
  });
}
function projectScriptVersionSummaryRecord(entity) {
  const record = stripProjectScriptSourceFields(entity.record);
  return pruneUndefinedRecord3({
    ...record,
    entityKind: entity.entityKind,
    path: entity.path,
    __workspace_entity_type: entity.entityKind,
    __workspace_path: entity.path,
    ...entity.id !== void 0 ? { id: entity.id } : {},
    ...entity.clientId !== void 0 ? { client_id: entity.clientId } : {},
    bodyLength: projectScriptInlineBodyLength(entity.record),
    body_length: projectScriptInlineBodyLength(entity.record),
    sourceLoaded: false,
    source_loaded: false
  });
}
function stripProjectScriptSourceFields(record) {
  const output = {};
  for (const [key, value] of Object.entries(record ?? {})) {
    if (PROJECT_SCRIPT_SOURCE_FIELDS.has(key)) continue;
    output[key] = value;
  }
  return output;
}
function projectScriptSourcePath(entity, sourceRef) {
  const scriptPath = stringValue13(entity.path);
  if (!scriptPath) return void 0;
  return `${scriptPath.replace(/\/script\.json$/, "")}/${sourceRef}`.replace(/\/+/g, "/");
}
function projectScriptInlineBodyLength(record) {
  const source = stringValue13(record?.content ?? record?.raw_source ?? record?.source);
  return source ? source.length : void 0;
}
function projectScriptVersionsByScriptRef(versions) {
  const output = /* @__PURE__ */ new Map();
  for (const version of versions) {
    for (const ref of projectScriptVersionRefValues(version)) {
      const items = output.get(ref) ?? [];
      items.push(version);
      output.set(ref, items);
    }
  }
  return output;
}
function projectScriptVersionRefValues(version) {
  const refs = [
    version.script_id,
    version.scriptId,
    version.script_ref,
    version.scriptRef,
    version.record?.script_id,
    version.record?.scriptId,
    version.record?.script_ref,
    version.record?.scriptRef,
    pathSegmentAfter10(version.path, "scripts")
  ].map(scriptRefValue).filter(Boolean);
  return [...new Set(refs)];
}
function projectScriptRefValues(entity, record) {
  const refs = [
    entity.id,
    entity.clientId,
    record.ID,
    record.id,
    record.script_id,
    record.scriptId,
    record.script_ref,
    record.scriptRef,
    pathSegmentAfter10(entity.path, "scripts")
  ].map(scriptRefValue).filter(Boolean);
  return [...new Set(refs)];
}
function scriptRefValue(value) {
  const ref = idValue4(value);
  if (ref === void 0) return void 0;
  const text = String(ref);
  return text.startsWith("script_") ? text.replace(/^script_/, "") : text;
}
function uniqueProjectScriptVersions(versions) {
  const output = [];
  const seen = /* @__PURE__ */ new Set();
  for (const version of versions) {
    const key = String(version.path ?? version.id ?? version.ID ?? output.length);
    if (seen.has(key)) continue;
    seen.add(key);
    output.push(version);
  }
  return output;
}
function compareProjectScriptVersionsDescending(left, right) {
  const leftNumber = numberValue7(left.version_number ?? left.versionNumber ?? left.ID ?? left.id) ?? 0;
  const rightNumber = numberValue7(right.version_number ?? right.versionNumber ?? right.ID ?? right.id) ?? 0;
  if (leftNumber !== rightNumber) return rightNumber - leftNumber;
  return String(right.UpdatedAt ?? right.updated_at ?? "").localeCompare(String(left.UpdatedAt ?? left.updated_at ?? ""));
}
function projectScriptCurrentVersionSummary(version) {
  return pruneUndefinedRecord3({
    id: idValue4(version.id ?? version.ID),
    ID: idValue4(version.ID ?? version.id),
    version_number: numberValue7(version.version_number ?? version.versionNumber),
    version_label: stringValue13(version.version_label ?? version.versionLabel),
    title: stringValue13(version.title),
    UpdatedAt: stringValue13(version.UpdatedAt ?? version.updated_at),
    updated_at: stringValue13(version.updated_at ?? version.UpdatedAt)
  });
}
function projectDecisionContextsByContentUnitId(contexts) {
  const output = /* @__PURE__ */ new Map();
  for (const context of contexts) {
    for (const ref of projectDecisionContextContentUnitRefs(context)) {
      output.set(ref, context);
    }
  }
  return output;
}
function projectDecisionContextContentUnitRefs(context) {
  return [
    context.contentUnitId,
    context.content_unit_id,
    context.target_ref,
    context.targetRef
  ].flatMap((value) => [
    contentUnitRefValue(value),
    contentUnitRefValue(pathSegmentAfter10(value, "content_units"))
  ]).filter(Boolean);
}
function projectContentUnitReadModelRecord({ contentUnitId, entity, decisionContext }) {
  const record = recordValue8(entity?.record) ?? {};
  const id = contentUnitRefValue(entity?.id ?? record.id ?? record.ID ?? contentUnitId) ?? String(contentUnitId);
  const candidates = projectContentUnitCandidateSummaries(decisionContext);
  const selection = recordValue8(decisionContext?.selection);
  return {
    id,
    title: stringValue13(record.title ?? record.name) ?? id,
    type: stringValue13(record.content_unit_type ?? record.contentUnitType ?? record.target_kind ?? record.targetKind) ?? "content_unit",
    outputKind: stringValue13(record.output_kind ?? record.outputKind) ?? projectContentUnitOutputKindFromCandidates(candidates) ?? "unknown",
    path: stringValue13(entity?.path ?? record.__workspace_path ?? record.path) ?? `content_units/${id}/content_unit.json`,
    editPrompt: projectContentUnitEditPrompt(record),
    selectionState: projectContentUnitSelectionState({ entity, candidates, selection }),
    candidates,
    record: projectContentUnitReadModelMetadata(record)
  };
}
function projectContentUnitCandidateSummaries(decisionContext) {
  const selection = recordValue8(decisionContext?.selection);
  const selectedCandidateId = stringValue13(selection?.candidate_id ?? selection?.candidateId);
  const candidates = Array.isArray(decisionContext?.candidates) ? decisionContext.candidates.filter(recordValue8) : [];
  return candidates.map((candidate) => projectContentUnitCandidateSummary(candidate, selectedCandidateId, selection));
}
function projectContentUnitCandidateSummary(candidate, selectedCandidateId, selection) {
  const id = stringValue13(candidate.id ?? candidate.candidate_id ?? candidate.candidateId) ?? String(candidate.id ?? candidate.candidate_id ?? candidate.candidateId ?? "candidate");
  const selected = selectedCandidateId !== void 0 && String(selectedCandidateId) === String(id);
  return pruneUndefinedRecord3({
    id,
    title: stringValue13(candidate.title ?? candidate.name) ?? id,
    model: stringValue13(candidate.model ?? candidate.model_id ?? candidate.provider) ?? "",
    note: stringValue13(candidate.note ?? candidate.notes ?? candidate.reason ?? candidate.input_hash ?? candidate.inputHash) ?? "",
    selected,
    resourceId: numberValue7(candidate.resource_id ?? candidate.resourceId ?? (selected ? selection?.resource_id ?? selection?.resourceId : void 0)),
    resourceKind: stringValue13(candidate.resource_kind ?? candidate.resourceKind),
    status: stringValue13(candidate.status),
    decisionStatus: stringValue13(candidate.decision_status ?? candidate.decisionStatus)
  });
}
function projectContentUnitSelectionState({ entity, candidates, selection }) {
  if (selection?.candidate_id !== void 0 || selection?.candidateId !== void 0) return "selected";
  if (candidates.length > 0) return "ready";
  return entity ? "needs_candidate" : "ready";
}
function projectContentUnitOutputKindFromCandidates(candidates) {
  return candidates.map((candidate) => stringValue13(candidate.resourceKind)).find(Boolean);
}
function projectContentUnitEditPrompt(record) {
  const prompt = recordValue8(record.edit_prompt ?? record.editPrompt);
  return stringValue13(prompt?.text ?? prompt?.prompt ?? prompt) ?? stringValue13(record.prompt ?? record.description) ?? "";
}
function projectContentUnitReadModelMetadata(record) {
  return pruneUndefinedRecord3({
    id: idValue4(record.id ?? record.ID),
    title: stringValue13(record.title ?? record.name),
    content_unit_type: stringValue13(record.content_unit_type ?? record.contentUnitType),
    output_kind: stringValue13(record.output_kind ?? record.outputKind),
    target_kind: stringValue13(record.target_kind ?? record.targetKind),
    target_ref: stringValue13(record.target_ref ?? record.targetRef)
  });
}
function projectContentUnitRefValues(entity) {
  const record = recordValue8(entity?.record) ?? {};
  const refs = [
    entity?.id,
    entity?.clientId,
    record.ID,
    record.id,
    record.content_unit_id,
    record.contentUnitId,
    pathSegmentAfter10(entity?.path, "content_units")
  ].map(contentUnitRefValue).filter(Boolean);
  return [...new Set(refs)];
}
function contentUnitRefValue(value) {
  const ref = idValue4(value);
  if (ref === void 0) return void 0;
  const text = String(ref).trim();
  if (!text) return void 0;
  const suffix = pathSegmentAfter10(text, "content_units");
  const normalized = suffix || text;
  return normalized.startsWith("content_unit_") ? normalized.replace(/^content_unit_/, "") : normalized;
}
async function executeProjectLifecycleCommand({ projectDir, command, input, now }) {
  switch (command) {
    case "openProject":
      return openLocalProject(projectDir, now);
    case "createProject":
      return createLocalProject(projectDir, input, now);
    case "importProject":
      return importLocalProject(projectDir, input, now);
    default:
      throw httpError3(400, "project_lifecycle_command_unsupported", `unsupported project lifecycle command: ${command}`);
  }
}
async function resolveProjectLocator({ projectDir, workspaceDir, projectUid }) {
  const metadata = await readLocalProjectMetadata(projectDir);
  return {
    status: metadata.hasMetadata ? "ready" : "missing_metadata",
    projectDir,
    projectPath: projectDir,
    ...workspaceDir ? { workspaceDir } : {},
    ...metadata.projectId ? { projectId: metadata.projectId } : {},
    ...metadata.projectUid ?? projectUid ? { projectUid: metadata.projectUid ?? projectUid } : {},
    ...metadata.title ? { projectTitle: metadata.title } : {},
    ...metadata.description ? { description: metadata.description } : {}
  };
}
async function readProjectResourceView({ projectDir, kind, body = {}, requestScope }) {
  const engine = createProjectWorkspaceEngine({ projectDir, body, requestScope });
  if (kind === "project-context") {
    const [records, index2] = await Promise.all([
      observeProjectServicePhase(requestScope, "deriveMs", () => engine.workspaceService.queryEntities({ entityKind: "project_standards", limit: 1 })),
      observeProjectServicePhase(requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex())
    ]);
    return [buildProjectContextSnapshot({
      standardsEntity: records[0],
      namespaceVocabulary: index2.namespaceVocabulary
    })];
  }
  if (isProjectDomainResourceKind(kind)) {
    const index2 = await observeProjectServicePhase(requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex());
    return observeProjectServicePhase(requestScope, "deriveMs", async () => projectDomainResourceItems(index2, kind));
  }
  if (kind === "scripts") {
    const scripts = await observeProjectServicePhase(requestScope, "deriveMs", () => engine.workspaceService.queryEntities({ entityKind: "script" }));
    return observeProjectServicePhase(requestScope, "deriveMs", () => Promise.all(scripts.map(async (entity) => ({
      ...entity.record,
      entityKind: entity.entityKind,
      path: entity.path,
      source: await engine.workspaceService.readScriptSource({ record: entity.record, entity })
    }))));
  }
  if (kind === "script-versions") {
    const versions = await observeProjectServicePhase(requestScope, "deriveMs", () => engine.workspaceService.queryEntities({ entityKind: "script_version" }));
    return observeProjectServicePhase(requestScope, "deriveMs", async () => versions.map((entity) => ({
      ...entity.record,
      entityKind: entity.entityKind,
      path: entity.path
    })));
  }
  const entityKind = projectResourceEntityKind(kind);
  const index = await observeProjectServicePhase(requestScope, "indexLoadMs", () => engine.workspaceService.loadIndex());
  const domainNodeByPath = new Map(index.domainNodes.map((node) => [node.path, node]).filter(([path]) => typeof path === "string"));
  const entities = await observeProjectServicePhase(requestScope, "deriveMs", () => engine.workspaceService.queryEntities({
    entityKind,
    ...entityKind === "project" ? { limit: 1 } : {}
  }));
  return observeProjectServicePhase(requestScope, "deriveMs", async () => entities.map((entity) => projectEntityResourceItem(entity, domainNodeByPath.get(entity.path), kind)));
}
function isProjectDomainResourceKind(kind) {
  return kind === "namespace-vocabulary" || kind === "timeline-namespaces" || kind === "setting-namespaces" || kind === "system-primitives" || kind === "domain-nodes" || kind === "domain-edges";
}
function projectDomainResourceItems(index, kind) {
  if (kind === "namespace-vocabulary") {
    return projectNamespaceVocabularyResourceItems(index.namespaceVocabulary);
  }
  if (kind === "domain-edges") {
    return index.domainEdges.map((edge) => ({
      source: edge.source,
      target: edge.target,
      relation: edge.relation,
      origin: edge.origin,
      ...edge.field ? { field: edge.field } : {}
    }));
  }
  const nodes = index.domainNodes.filter((node) => {
    if (kind === "timeline-namespaces") return node.category === "timeline_namespace";
    if (kind === "setting-namespaces") return node.category === "setting_namespace";
    if (kind === "system-primitives") return node.category === "system_primitive";
    return true;
  });
  return nodes.map((node) => projectDomainNodeResourceItem(node));
}
function projectNamespaceVocabularyResourceItems(vocabulary) {
  return [
    {
      id: "timeline",
      category: "timeline_namespace",
      timelineTemplate: vocabulary.timelineTemplate,
      timelineNamespaces: vocabulary.timelineNamespaces,
      namespaces: vocabulary.timelineNamespaces,
      diagnostics: vocabulary.diagnostics
    },
    {
      id: "setting",
      category: "setting_namespace",
      settingNamespaces: vocabulary.settingNamespaces,
      namespaces: vocabulary.settingNamespaces,
      diagnostics: vocabulary.diagnostics
    }
  ];
}
function projectDomainNodeResourceItem(node) {
  return {
    id: node.id,
    title: node.title,
    path: node.path,
    category: node.category,
    kind: node.kind,
    order: node.order,
    entityKind: node.metadata?.entityKind,
    metadata: node.metadata
  };
}
function projectEntityResourceItem(entity, domainNode, resourceKind) {
  const projection = legacyResourceKindProjection(resourceKind);
  return {
    ...entity.record,
    entityKind: entity.entityKind,
    path: entity.path,
    ...projection ? {
      resourceKind,
      legacyAlias: true,
      preferredResourceKind: projection.preferredResourceKind
    } : {},
    ...domainNode ? {
      domainCategory: domainNode.category,
      domainKind: domainNode.kind,
      domainNode: projectDomainNodeResourceItem(domainNode)
    } : {}
  };
}
function legacyResourceKindProjection(kind) {
  switch (kind) {
    case "episodes":
    case "productions":
    case "scenes":
    case "segments":
      return { preferredResourceKind: "timeline-namespaces" };
    case "settings":
    case "setting-states":
    case "states":
      return { preferredResourceKind: "setting-namespaces" };
    default:
      return void 0;
  }
}
function projectResourceEntityKind(kind) {
  switch (kind) {
    case "summary":
      return "project";
    case "assets":
    case "assests":
      return "asset";
    case "episodes":
    case "productions":
      return "production";
    case "scenes":
    case "segments":
      return "segment";
    case "storyboards":
      return "storyboard";
    case "content-units":
      return "content_unit";
    case "settings":
      return "setting";
    case "setting-states":
    case "states":
      return "setting_state";
    default:
      throw httpError3(400, "project_resource_kind_unsupported", `unsupported project resource kind: ${kind}`);
  }
}
async function openLocalProject(projectDir, now) {
  const projectStat = await stat4(projectDir).catch(() => void 0);
  if (!projectStat?.isDirectory()) {
    throw httpError3(400, "project_dir_not_found", "projectDir must be an existing directory");
  }
  const metadata = await readLocalProjectMetadata(projectDir);
  return {
    status: metadata.hasMetadata ? "ready" : "missing_metadata",
    projectDir,
    projectPath: projectDir,
    projectId: metadata.projectId,
    projectUid: metadata.projectUid,
    project: localProjectSummary(projectDir, metadata, now),
    locator: localProjectLocator(projectDir, metadata)
  };
}
async function createLocalProject(projectDir, input, now) {
  await mkdir5(projectDir, { recursive: true });
  const metadata = await readLocalProjectMetadata(projectDir);
  if (metadata.hasMetadata && input.overwrite !== true) {
    throw httpError3(409, "project_lifecycle_project_exists", "project metadata already exists; set overwrite to true to replace it");
  }
  const initialized = await initializeLocalProject(projectDir, input);
  return localProjectInitializedResult("created", projectDir, initialized, now);
}
async function importLocalProject(projectDir, input, now) {
  const projectStat = await stat4(projectDir).catch(() => void 0);
  if (!projectStat?.isDirectory()) {
    throw httpError3(400, "project_dir_not_found", "projectDir must be an existing directory");
  }
  const initialized = await initializeLocalProject(projectDir, {
    ...input,
    title: stringValue13(input.title) ?? basename4(projectDir) ?? "MovScript Project"
  });
  return localProjectInitializedResult("imported", projectDir, initialized, now);
}
async function initializeLocalProject(projectDir, input) {
  const service2 = createNodeMovScriptWorkspaceService({ projectDir });
  return service2.initializeProject({
    title: stringValue13(input.title) ?? basename4(projectDir) ?? "MovScript Project",
    ...stringValue13(input.projectId ?? input.project_id) ? { projectId: stringValue13(input.projectId ?? input.project_id) } : {},
    ...stringValue13(input.projectUid ?? input.project_uid) ? { projectUid: stringValue13(input.projectUid ?? input.project_uid) } : {},
    ...stringValue13(input.language) ? { language: stringValue13(input.language) } : {},
    ...recordValue8(input.standards) ? { standards: recordValue8(input.standards) } : {},
    overwrite: input.overwrite === true
  });
}
function localProjectInitializedResult(status, projectDir, initialized, now) {
  const metadata = {
    hasMetadata: true,
    projectId: initialized.projectId,
    projectUid: initialized.projectUid,
    title: localProjectTitleFromInitialized(projectDir, initialized),
    updatedAt: now.toISOString()
  };
  return {
    status,
    projectDir,
    projectPath: projectDir,
    projectId: initialized.projectId,
    projectUid: initialized.projectUid,
    project: localProjectSummary(projectDir, metadata, now),
    locator: localProjectLocator(projectDir, metadata),
    initializedFiles: initialized.files.map((file) => ({
      path: file.path,
      status: file.status
    })),
    standardSkillFiles: initialized.standardSkillFiles
  };
}
function localProjectTitleFromInitialized(projectDir, initialized) {
  const workspaceFile = initialized.files.find((file) => file.path === "workspace.json");
  return stringValue13(workspaceFile?.record?.title) ?? basename4(projectDir) ?? "MovScript Project";
}
async function readLocalProjectMetadata(projectDir) {
  const workspace = await readJSONFile(resolve10(projectDir, "workspace.json"));
  const project = await readJSONFile(resolve10(projectDir, "project.json"));
  const hasMetadata = Boolean(recordValue8(workspace) || recordValue8(project));
  return {
    hasMetadata,
    projectId: stringValue13(workspace?.project_id ?? workspace?.projectId ?? project?.project_id ?? project?.projectId ?? project?.id),
    projectUid: stringValue13(workspace?.project_uid ?? workspace?.projectUid ?? project?.project_uid ?? project?.projectUid),
    title: stringValue13(project?.title ?? project?.name ?? workspace?.title ?? workspace?.name),
    description: stringValue13(project?.description ?? workspace?.description),
    updatedAt: stringValue13(workspace?.updated_at ?? workspace?.updatedAt ?? project?.updated_at ?? project?.updatedAt)
  };
}
function localProjectSummary(projectDir, metadata, now) {
  return {
    id: metadata.projectId,
    uid: metadata.projectUid,
    projectUid: metadata.projectUid,
    project_uid: metadata.projectUid,
    name: metadata.title || basename4(projectDir) || "Local Project",
    description: metadata.description || projectDir,
    projectDir,
    projectPath: projectDir,
    workspacePath: projectDir,
    local: true,
    updatedAt: metadata.updatedAt || now.toISOString()
  };
}
function localProjectLocator(projectDir, metadata) {
  return {
    projectDir,
    ...metadata.projectId ? { projectId: metadata.projectId } : {},
    ...metadata.projectUid ? { projectUid: metadata.projectUid } : {}
  };
}
async function executeProjectCandidateCommand({ projectDir, command, input, decisionStore }) {
  return executeProjectCandidateAction({ projectDir, input, decisionStore }, command);
}
async function executeProjectCandidateAction({ projectDir, input, decisionStore }, action) {
  const engine = createNodeMovScriptEngine({ projectDir, decisionStore });
  switch (action) {
    case "createContentCandidate":
      return engine.createContentCandidate(input);
    case "selectContentUnitCandidate":
      return engine.selectContentUnitCandidate(input);
    case "decideContentUnitCandidate":
      return engine.workspaceService.decideContentUnitCandidate(input);
    default:
      throw httpError3(400, "project_candidate_command_unsupported", `unsupported project candidate command: ${action}`);
  }
}
async function readProjectCandidateActionContext(request) {
  const body = await readJSONBody4(request);
  const projectDir = projectDirFromBody2(body);
  const decisionStore = await optionalDecisionStoreFromBody2(body, projectDir);
  if (!decisionStore) {
    throw httpError3(400, "project_candidate_decision_store_required", "decisionStore or projectUid is required");
  }
  return {
    projectDir,
    input: recordValue8(body.input) ?? {},
    decisionStore
  };
}
function projectCandidateActionEnvelope(projectDir, result, schema) {
  return {
    schema,
    projectDir,
    result
  };
}
async function readCandidateContexts(decisionStore, contentUnitIds, requestScope) {
  if (contentUnitIds.length === 1) {
    const context = await observeProjectServicePhase(
      requestScope,
      "decisionMs",
      () => decisionStore.getContentUnitDecision({ contentUnitId: contentUnitIds[0] })
    );
    return context ? [normalizeDecisionContext(context)] : [];
  }
  const contexts = await observeProjectServicePhase(
    requestScope,
    "decisionMs",
    () => decisionStore.getContentUnitDecisions({ contentUnitIds })
  );
  return contentUnitIds.map((contentUnitId) => contexts.get(String(contentUnitId))).filter(Boolean).map((context) => normalizeDecisionContext(context));
}
async function readProjectPromptContexts({ projectDir, body = {}, contentUnitIds, include, promptText, decisionStore, requestScope }) {
  const engine = createProjectWorkspaceEngine({ projectDir, decisionStore, body, requestScope });
  const workspaceService = engine.workspaceService;
  const decisionProvider = decisionStore ?? missingDecisionProvider();
  const index = include.has("backendPrompt") ? await observeProjectServicePhase(requestScope, "indexLoadMs", () => workspaceService.loadIndex()) : void 0;
  return Promise.all(contentUnitIds.map(async (contentUnitId) => ({
    contentUnitId,
    context: await readProjectPromptContext({
      workspaceService,
      index,
      contentUnitId,
      include,
      promptText,
      decisionProvider,
      requestScope
    })
  })));
}
async function readProjectPromptContext({ workspaceService, index, contentUnitId, include, promptText, decisionProvider, requestScope }) {
  const [
    runtimePanel,
    generationPrompt,
    dependencyReport,
    selectionValidity,
    backendPrompt
  ] = await Promise.all([
    include.has("runtimePanel") ? observeProjectServicePhase(requestScope, "deriveMs", () => workspaceService.readContentUnitRuntimePanel(contentUnitId)) : void 0,
    include.has("generationPrompt") ? observeProjectServicePhase(requestScope, "deriveMs", () => workspaceService.readContentUnitGenerationPrompt(contentUnitId)) : void 0,
    include.has("dependencyReport") ? observeProjectServicePhase(requestScope, "deriveMs", () => workspaceService.readContentUnitDependencyReport(contentUnitId)) : void 0,
    include.has("selectionValidity") ? observeProjectServicePhase(requestScope, "deriveMs", () => workspaceService.readContentUnitSelectionValidity(contentUnitId)) : void 0,
    include.has("backendPrompt") ? observeProjectServicePhase(requestScope, "decisionMs", () => buildContentUnitBackendPromptById({
      index,
      contentUnitId,
      decisionProvider,
      ...promptText !== void 0 ? { promptText } : {}
    })) : void 0
  ]);
  return {
    ...runtimePanel ? { runtimePanel } : {},
    ...generationPrompt ? { generationPrompt } : {},
    ...dependencyReport ? { dependencyReport } : {},
    ...selectionValidity ? { selectionValidity } : {},
    backendPrompt
  };
}
function promptContextIncludeFromBody(body) {
  const raw = Array.isArray(body.include) ? body.include : Array.isArray(body.includes) ? body.includes : [];
  const allowed = /* @__PURE__ */ new Set([
    "runtimePanel",
    "generationPrompt",
    "dependencyReport",
    "selectionValidity",
    "backendPrompt"
  ]);
  const include = new Set(raw.filter((item) => typeof item === "string" && allowed.has(item)));
  if (include.size === 0) {
    return allowed;
  }
  return include;
}
function missingDecisionProvider() {
  return {
    async getContentUnitDecision() {
      return void 0;
    }
  };
}
function decisionStoreFromBody2(body) {
  const config = recordValue8(body.decisionStore ?? body.decision_store);
  if (!config) {
    throw httpError3(400, "project_candidate_decision_store_required", "decisionStore is required");
  }
  if (config.kind !== "scoped-project-data") {
    throw httpError3(400, "project_candidate_decision_store_unsupported", "only scoped-project-data decisionStore is supported");
  }
  const baseUrl = stringValue13(config.baseUrl ?? config.base_url);
  const projectUid = stringValue13(config.projectUid ?? config.project_uid);
  if (!baseUrl || !projectUid) {
    throw httpError3(400, "project_candidate_decision_store_invalid", "decisionStore.baseUrl and decisionStore.projectUid are required");
  }
  return createMovScriptScopedProjectDataDecisionStore({
    baseUrl,
    projectUid,
    ...stringValue13(config.title) ? { title: stringValue13(config.title) } : {},
    ...config.scopeKind === "user" || config.scopeKind === "org" ? { scopeKind: config.scopeKind } : {},
    ...config.scopeId !== void 0 || config.scope_id !== void 0 ? { scopeId: config.scopeId ?? config.scope_id } : {},
    ...stringValue13(config.token) ? { token: stringValue13(config.token) } : {},
    ...recordValue8(config.headers) ? { headers: stringRecord2(config.headers) } : {}
  });
}
async function optionalDecisionStoreFromBody2(body, projectDir) {
  if (recordValue8(body.decisionStore ?? body.decision_store)) return decisionStoreFromBody2(body);
  return inferLocalProjectDecisionStore(body, projectDir);
}
async function inferLocalProjectDecisionStore(body, projectDir) {
  const metadata = await readLocalProjectMetadata(projectDir).catch(() => void 0);
  const projectUid = stringValue13(body.projectUid ?? body.project_uid) ?? metadata?.projectUid;
  if (!projectUid) return void 0;
  const baseUrl = inferredDataServiceBaseURL(body);
  if (!baseUrl) return void 0;
  const scopeKind = body.scopeKind === "org" || body.scope_kind === "org" ? "org" : "user";
  const scopeId = idValue4(body.scopeId ?? body.scope_id) ?? (scopeKind === "user" ? "1" : void 0);
  if (scopeId === void 0) return void 0;
  return createMovScriptScopedProjectDataDecisionStore({
    baseUrl,
    projectUid,
    ...metadata?.title ? { title: metadata.title } : {},
    scopeKind,
    scopeId
  });
}
function inferredDataServiceBaseURL(body) {
  const explicit = normalizeHTTPBaseURL(
    stringValue13(body.dataServiceBaseURL ?? body.dataServiceBaseUrl ?? body.data_service_base_url) ?? stringValue13(process.env.MOVSCRIPT_DATA_SERVICE_URL) ?? stringValue13(process.env.MOVSCRIPT_DATA_SERVICE_BASE_URL)
  );
  if (explicit) return explicit;
  const homeDir = stringValue13(body.movScriptHomeDir ?? body.movscript_home_dir ?? body.workspaceDir ?? body.workspace_dir) ?? resolveMovScriptHomeDir({ env: process.env });
  const snapshot = readRuntimeHomeSnapshot(homeDir);
  const endpoint = findRuntimeEndpoint(snapshot, LOCAL_NODE_GATEWAY_SERVICE3) ?? findRuntimeService(snapshot, LOCAL_NODE_GATEWAY_SERVICE3)?.endpoint ?? findRuntimeEndpoint(snapshot, DATA_SERVICE_NAME) ?? findRuntimeService(snapshot, DATA_SERVICE_NAME)?.endpoint;
  return normalizeHTTPBaseURL(endpointURL7(endpoint));
}
function endpointURL7(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}
function normalizeHTTPBaseURL(value) {
  const trimmed = stringValue13(value)?.replace(/\/+$/, "");
  if (!trimmed) return void 0;
  try {
    const url = new URL(trimmed);
    if (url.protocol !== "http:" && url.protocol !== "https:") return void 0;
    return url.toString().replace(/\/+$/, "");
  } catch {
    return void 0;
  }
}
function contentUnitIdsFromBody(body) {
  const contentUnitId = body.contentUnitId ?? body.content_unit_id;
  if (contentUnitId !== void 0 && contentUnitId !== null) return [contentUnitId];
  const contentUnitIds = Array.isArray(body.contentUnitIds) ? body.contentUnitIds : Array.isArray(body.content_unit_ids) ? body.content_unit_ids : [];
  const ids = contentUnitIds.filter((item) => typeof item === "string" || typeof item === "number");
  if (ids.length === 0) {
    throw httpError3(400, "project_candidate_content_unit_required", "contentUnitId or contentUnitIds is required");
  }
  return ids;
}
function readModelContentUnitIdsFromBody(body) {
  const contentUnitId = body.contentUnitId ?? body.content_unit_id;
  if (typeof contentUnitId === "string" && contentUnitId.trim()) return [contentUnitId.trim()];
  if (typeof contentUnitId === "number" && Number.isFinite(contentUnitId)) return [contentUnitId];
  const contentUnitIds = Array.isArray(body.contentUnitIds) ? body.contentUnitIds : Array.isArray(body.content_unit_ids) ? body.content_unit_ids : [];
  const ids = contentUnitIds.filter((item) => typeof item === "string" || typeof item === "number").map((item) => typeof item === "string" ? item.trim() : item).filter((item) => item !== "");
  if (ids.length === 0) {
    throw httpError3(400, "project_content_units_read_model_ids_required", "contentUnitId or contentUnitIds is required");
  }
  return [...new Set(ids.map((item) => String(item)))].slice(0, 200);
}
function promptContextContentUnitIdsFromBody(body) {
  const contentUnitId = body.contentUnitId ?? body.content_unit_id;
  if (typeof contentUnitId === "string" && contentUnitId.trim()) return [contentUnitId.trim()];
  if (typeof contentUnitId === "number" && Number.isFinite(contentUnitId)) return [contentUnitId];
  const contentUnitIds = Array.isArray(body.contentUnitIds) ? body.contentUnitIds : Array.isArray(body.content_unit_ids) ? body.content_unit_ids : [];
  const ids = contentUnitIds.map((item) => {
    if (typeof item === "string" && item.trim()) return item.trim();
    if (typeof item === "number" && Number.isFinite(item)) return item;
    return void 0;
  }).filter((item) => item !== void 0);
  if (ids.length === 0) {
    throw httpError3(400, "project_prompt_content_unit_required", "contentUnitId or contentUnitIds is required");
  }
  return ids;
}
function projectDirFromBody2(body) {
  const projectDir = typeof body.projectDir === "string" ? body.projectDir : typeof body.project_dir === "string" ? body.project_dir : void 0;
  if (!projectDir || !projectDir.trim()) {
    throw httpError3(400, "project_dir_required", "projectDir is required");
  }
  return resolve10(projectDir);
}
function pathStringValue(value) {
  const raw = stringValue13(value);
  return raw ? resolve10(raw) : void 0;
}
async function readJSONFile(path) {
  try {
    const parsed = JSON.parse(await readFile5(path, "utf8"));
    return recordValue8(parsed);
  } catch {
    return void 0;
  }
}
async function readJSONBody4(request) {
  const chunks = [];
  for await (const chunk of request) {
    chunks.push(Buffer.from(chunk));
  }
  const text = Buffer.concat(chunks).toString("utf8").trim();
  if (!text) return {};
  try {
    return JSON.parse(text);
  } catch {
    throw httpError3(400, "invalid_json", "request body must be valid JSON");
  }
}
function stringValue13(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function idValue4(value) {
  if (typeof value === "string" && value.trim()) return value.trim();
  if (typeof value === "number" && Number.isFinite(value)) return value;
  return void 0;
}
function numberValue7(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  if (typeof value !== "string" || !value.trim()) return void 0;
  const number = Number(value);
  return Number.isFinite(number) ? number : void 0;
}
function recordValue8(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : void 0;
}
function pathSegmentAfter10(path, segment) {
  const parts = String(path ?? "").split("/").filter(Boolean);
  const index = parts.indexOf(segment);
  return index >= 0 ? parts[index + 1] : void 0;
}
function pruneUndefinedRecord3(record) {
  return Object.fromEntries(Object.entries(record).filter(([, value]) => value !== void 0 && value !== ""));
}
function stringRecord2(value) {
  const record = recordValue8(value);
  if (!record) return {};
  return Object.fromEntries(Object.entries(record).filter(([, item]) => typeof item === "string").map(([key, item]) => [key, item]));
}
function isNotFoundError4(error) {
  return typeof error === "object" && error !== null && error.code === "ENOENT";
}
function httpError3(statusCode, code, message) {
  const error = new Error(message);
  error.statusCode = statusCode;
  error.code = code;
  return error;
}
function writeProjectServiceError(response, error) {
  if (isWorkspaceFileVersionConflict(error)) {
    writeJSON4(response, 409, {
      error: "project_workspace_file_version_conflict",
      message: "workspace file changed before the write could be committed"
    });
    return;
  }
  const statusCode = Number.isInteger(error?.statusCode) ? error.statusCode : 500;
  writeJSON4(response, statusCode, {
    error: error?.code ?? "project_service_error",
    message: error?.message ?? "project service error"
  });
}
function isWorkspaceFileVersionConflict(error) {
  return error instanceof Error && /^workspace file changed:/.test(error.message);
}
function waitForShutdown4(runtime) {
  return new Promise((resolve12) => {
    let closing = false;
    const close = async () => {
      if (closing) return;
      closing = true;
      await runtime.close();
      resolve12();
    };
    process.once("SIGINT", close);
    process.once("SIGTERM", close);
  });
}

// src/agent-mcp.ts
var LOCAL_NODE_APP_ID = LOCAL_RUNTIME_DAEMON_APP_ID;
var LOCAL_NODE_CONTROL_SERVICE2 = LOCAL_RUNTIME_DAEMON_CONTROL_SERVICE;
var LOCAL_NODE_GATEWAY_SERVICE4 = LOCAL_RUNTIME_DAEMON_GATEWAY_SERVICE;
var PROJECT_SERVICE_NAME2 = "movscript.project.service";
var EDITING_SERVICE_NAME2 = "movscript.editing.service";
var CANVAS_SERVICE_NAME2 = "movscript.canvas.service";
var DATA_SERVICE_NAME2 = "movscript.data.service";
var DEFAULT_LOCAL_NODE_GATEWAY_PORT = 8766;
var RUNTIME_ENDPOINT_WAIT_TIMEOUT_MS = 5e3;
var RUNTIME_ENDPOINT_WAIT_INTERVAL_MS = 100;
var DAEMON_RUNTIME_DESCRIPTOR_ENDPOINT = "/v1/runtime/descriptor";
var DAEMON_RUNTIME_STATUS_ENDPOINT = "/v1/runtime/status";
var DAEMON_RUNTIME_DIAGNOSTICS_ENDPOINT = "/v1/runtime/diagnostics";
var DAEMON_RUNTIME_CONFIGURE_ENDPOINT = "/v1/runtime/configure";
var DAEMON_CONTEXT_ENDPOINT = "/v1/context";
var DAEMON_CONTEXT_SESSIONS_ENDPOINT = "/v1/context/sessions";
var LOCAL_PROJECT_SERVICE_PROXY_ROUTES = /* @__PURE__ */ new Map([
  ["/v1/project/read-model", "/v1/project/read-model"],
  ["/v1/project/lifecycle/command", "/v1/project/lifecycle/command"],
  ["/v1/project/locator/resolve", "/v1/project/locator/resolve"],
  ["/v1/project/source/snapshot", "/v1/project/source/snapshot"],
  ["/v1/project/source/inspect", "/v1/project/source/inspect"],
  ["/v1/project/source/overview", "/v1/project/source/overview"],
  ["/v1/project/source/interpret", "/v1/project/source/interpret"],
  ["/v1/project/source/regeneration-plan", "/v1/project/source/regeneration-plan"],
  ["/v1/project/source/command", "/v1/project/source/command"],
  ["/v1/project/entities/query", "/v1/project/entities/query"],
  ["/v1/project/settings/query", "/v1/project/settings/query"],
  ["/v1/project/assets/query", "/v1/project/assets/query"],
  ["/v1/project/content-workspace/snapshot", "/v1/project/content-workspace/snapshot"],
  ["/v1/project/content-workspace/read", "/v1/project/content-workspace/read"],
  ["/v1/project/standards/upsert", "/v1/project/standards/upsert"],
  ["/v1/project/scripts/source/read", "/v1/project/scripts/source/read"],
  ["/v1/project/scripts/upsert", "/v1/project/scripts/upsert"],
  ["/v1/project/scripts/versions/snapshot", "/v1/project/scripts/versions/snapshot"],
  ["/v1/project/settings/upsert", "/v1/project/settings/upsert"],
  ["/v1/project/settings/create", "/v1/project/settings/create"],
  ["/v1/project/settings/states/create", "/v1/project/settings/states/create"],
  ["/v1/project/assets/upsert", "/v1/project/assets/upsert"],
  ["/v1/project/assets/create", "/v1/project/assets/create"],
  ["/v1/project/productions/snapshot/save", "/v1/project/productions/snapshot/save"],
  ["/v1/project/content-units/upsert", "/v1/project/content-units/upsert"],
  ["/v1/project/content-units/create", "/v1/project/content-units/create"],
  ["/v1/project/content-units/ensure", "/v1/project/content-units/ensure"],
  ["/v1/project/timeline-assemblies/content-unit/ensure", "/v1/project/timeline-assemblies/content-unit/ensure"],
  ["/v1/project/content-units/edit-prompt/update", "/v1/project/content-units/edit-prompt/update"],
  ["/v1/project/productions/create", "/v1/project/productions/create"],
  ["/v1/project/segments/create", "/v1/project/segments/create"],
  ["/v1/project/scene-moments/create", "/v1/project/scene-moments/create"],
  ["/v1/project/scene-moments/settings/connect", "/v1/project/scene-moments/settings/connect"],
  ["/v1/project/expression-units/create", "/v1/project/expression-units/create"],
  ["/v1/project/expression-units/update", "/v1/project/expression-units/update"],
  ["/v1/project/keyframes/create", "/v1/project/keyframes/create"],
  ["/v1/project/storyboards/create", "/v1/project/storyboards/create"],
  ["/v1/project/storyboards/timeline/update", "/v1/project/storyboards/timeline/update"],
  ["/v1/project/audio-cues/create", "/v1/project/audio-cues/create"],
  ["/v1/project/audio-cues/update", "/v1/project/audio-cues/update"],
  ["/v1/project/entities/basics/update", "/v1/project/entities/basics/update"],
  ["/v1/project/entities/transition/update", "/v1/project/entities/transition/update"],
  ["/v1/project/entities/delete", "/v1/project/entities/delete"],
  ["/v1/project/hierarchy/write", "/v1/project/hierarchy/write"],
  ["/v1/project/namespaces/write", "/v1/project/namespaces/write"],
  ["/v1/project/content-canvases/list", "/v1/project/content-canvases/list"],
  ["/v1/project/content-canvases/write", "/v1/project/content-canvases/write"],
  ["/v1/project/content-canvases/rename", "/v1/project/content-canvases/rename"],
  ["/v1/project/content-canvases/run", "/v1/project/content-canvases/run"],
  ["/v1/project/content-canvases/delete", "/v1/project/content-canvases/delete"],
  ["/v1/project/workspace-candidates/select", "/v1/project/workspace-candidates/select"],
  ["/v1/project/workspace-candidates/append", "/v1/project/workspace-candidates/append"],
  ["/v1/project/workspace-candidates/asset-slots/create", "/v1/project/workspace-candidates/asset-slots/create"],
  ["/v1/project/workspace-candidates/keyframes/create", "/v1/project/workspace-candidates/keyframes/create"],
  ["/v1/project/content-candidates/create", "/v1/project/content-candidates/create"],
  ["/v1/project/content-unit-candidates/select", "/v1/project/content-unit-candidates/select"],
  ["/v1/project/content-unit-candidates/decide", "/v1/project/content-unit-candidates/decide"],
  ["/v1/project/resources/view", "/v1/project/resources/view"],
  ["/v1/project/candidates/command", "/v1/project/candidates/command"],
  ["/v1/project/candidates/view", "/v1/project/candidates/view"],
  ["/v1/project/prompt/context", "/v1/project/prompt/context"],
  ["/local-api/project/read-model", "/v1/project/read-model"],
  ["/local-api/project/lifecycle/command", "/v1/project/lifecycle/command"],
  ["/local-api/project/locator/resolve", "/v1/project/locator/resolve"],
  ["/local-api/project/source/snapshot", "/v1/project/source/snapshot"],
  ["/local-api/project/source/inspect", "/v1/project/source/inspect"],
  ["/local-api/project/source/overview", "/v1/project/source/overview"],
  ["/local-api/project/source/interpret", "/v1/project/source/interpret"],
  ["/local-api/project/source/regeneration-plan", "/v1/project/source/regeneration-plan"],
  ["/local-api/project/source/command", "/v1/project/source/command"],
  ["/local-api/project/entities/query", "/v1/project/entities/query"],
  ["/local-api/project/settings/query", "/v1/project/settings/query"],
  ["/local-api/project/assets/query", "/v1/project/assets/query"],
  ["/local-api/project/content-workspace/snapshot", "/v1/project/content-workspace/snapshot"],
  ["/local-api/project/content-workspace/read", "/v1/project/content-workspace/read"],
  ["/local-api/project/standards/upsert", "/v1/project/standards/upsert"],
  ["/local-api/project/scripts/source/read", "/v1/project/scripts/source/read"],
  ["/local-api/project/scripts/upsert", "/v1/project/scripts/upsert"],
  ["/local-api/project/scripts/versions/snapshot", "/v1/project/scripts/versions/snapshot"],
  ["/local-api/project/settings/upsert", "/v1/project/settings/upsert"],
  ["/local-api/project/settings/create", "/v1/project/settings/create"],
  ["/local-api/project/settings/states/create", "/v1/project/settings/states/create"],
  ["/local-api/project/assets/upsert", "/v1/project/assets/upsert"],
  ["/local-api/project/assets/create", "/v1/project/assets/create"],
  ["/local-api/project/productions/snapshot/save", "/v1/project/productions/snapshot/save"],
  ["/local-api/project/content-units/upsert", "/v1/project/content-units/upsert"],
  ["/local-api/project/content-units/create", "/v1/project/content-units/create"],
  ["/local-api/project/content-units/ensure", "/v1/project/content-units/ensure"],
  ["/local-api/project/timeline-assemblies/content-unit/ensure", "/v1/project/timeline-assemblies/content-unit/ensure"],
  ["/local-api/project/content-units/edit-prompt/update", "/v1/project/content-units/edit-prompt/update"],
  ["/local-api/project/productions/create", "/v1/project/productions/create"],
  ["/local-api/project/segments/create", "/v1/project/segments/create"],
  ["/local-api/project/scene-moments/create", "/v1/project/scene-moments/create"],
  ["/local-api/project/scene-moments/settings/connect", "/v1/project/scene-moments/settings/connect"],
  ["/local-api/project/expression-units/create", "/v1/project/expression-units/create"],
  ["/local-api/project/expression-units/update", "/v1/project/expression-units/update"],
  ["/local-api/project/keyframes/create", "/v1/project/keyframes/create"],
  ["/local-api/project/storyboards/create", "/v1/project/storyboards/create"],
  ["/local-api/project/storyboards/timeline/update", "/v1/project/storyboards/timeline/update"],
  ["/local-api/project/audio-cues/create", "/v1/project/audio-cues/create"],
  ["/local-api/project/audio-cues/update", "/v1/project/audio-cues/update"],
  ["/local-api/project/entities/basics/update", "/v1/project/entities/basics/update"],
  ["/local-api/project/entities/transition/update", "/v1/project/entities/transition/update"],
  ["/local-api/project/entities/delete", "/v1/project/entities/delete"],
  ["/local-api/project/hierarchy/write", "/v1/project/hierarchy/write"],
  ["/local-api/project/namespaces/write", "/v1/project/namespaces/write"],
  ["/local-api/project/content-canvases/list", "/v1/project/content-canvases/list"],
  ["/local-api/project/content-canvases/write", "/v1/project/content-canvases/write"],
  ["/local-api/project/content-canvases/rename", "/v1/project/content-canvases/rename"],
  ["/local-api/project/content-canvases/run", "/v1/project/content-canvases/run"],
  ["/local-api/project/content-canvases/delete", "/v1/project/content-canvases/delete"],
  ["/local-api/project/workspace-candidates/select", "/v1/project/workspace-candidates/select"],
  ["/local-api/project/workspace-candidates/append", "/v1/project/workspace-candidates/append"],
  ["/local-api/project/workspace-candidates/asset-slots/create", "/v1/project/workspace-candidates/asset-slots/create"],
  ["/local-api/project/workspace-candidates/keyframes/create", "/v1/project/workspace-candidates/keyframes/create"],
  ["/local-api/project/content-candidates/create", "/v1/project/content-candidates/create"],
  ["/local-api/project/content-unit-candidates/select", "/v1/project/content-unit-candidates/select"],
  ["/local-api/project/content-unit-candidates/decide", "/v1/project/content-unit-candidates/decide"],
  ["/local-api/project/resources/view", "/v1/project/resources/view"],
  ["/local-api/project/candidates/command", "/v1/project/candidates/command"],
  ["/local-api/project/candidates/view", "/v1/project/candidates/view"],
  ["/local-api/project/prompt/context", "/v1/project/prompt/context"]
]);
var daemonContextSessions = /* @__PURE__ */ new Map();
var PLUGIN_ROOT = resolve11(import.meta.dirname, "..");
var DEV_REPO_ROOT = resolve11(import.meta.dirname, "../../..");
var BUNDLED_RUNTIME_ROOT = resolve11(PLUGIN_ROOT, "runtime");
var HAS_BUNDLED_RUNTIME = existsSync5(BUNDLED_RUNTIME_ROOT);
var AGENT_MCP_ENTRYPOINT = resolve11(import.meta.dirname, "movscript.mjs");
var RUN_CWD = HAS_BUNDLED_RUNTIME ? PLUGIN_ROOT : DEV_REPO_ROOT;
function errorMessage6(error) {
  return error instanceof Error ? error.message : String(error);
}
function currentPluginIdentity() {
  return {
    pluginVersion: readPluginVersion(resolve11(PLUGIN_ROOT, "manifest.runtime.json")) ?? readPluginVersion(resolve11(PLUGIN_ROOT, ".codex-plugin/plugin.json")) ?? "unknown",
    pluginRoot: PLUGIN_ROOT
  };
}
function readPluginVersion(manifestPath) {
  try {
    const manifest = JSON.parse(readFileSync6(manifestPath, "utf8"));
    return typeof manifest.version === "string" && manifest.version.trim() ? manifest.version : void 0;
  } catch {
    return void 0;
  }
}
async function main() {
  if (await runEmbeddedServiceCLI()) return;
  if (await runLocalDaemonCLI()) return;
  if (isLegacyAgentMCPInvocation()) {
    await runPluginMCPStdioSession();
    return;
  }
  await (0, import_cli.runMovcli)([process.argv[0] ?? "node", "movscript", ...process.argv.slice(2)]);
}
async function runPluginMCPStdioSession() {
  const homeDir = resolveMovScriptHomeDir();
  const startupPolicy = await prepareSessionStartupPolicy(homeDir);
  const runner = createScenarioApplicationRunner({
    homeDir,
    application: application_manifest_default,
    scenario: startupPolicy,
    programs: createProgramAdapters()
  });
  await runner.start();
  try {
    await startMCPStdioHost();
  } finally {
    await runner.shutdown();
  }
}
async function prepareSessionStartupPolicy(homeDir) {
  const requested = (process.env.MOVSCRIPT_PLUGIN_MODE ?? process.env.MOVSCRIPT_PLUGIN_SCENARIO ?? "").trim().toLowerCase();
  if (requested === "basic" || requested === "plugin-basic") return pluginBasicStartupPolicy;
  const startupPolicy = selectPluginStartupPolicy(homeDir);
  if (startupPolicy.scenarioId === pluginFullLocalStartupPolicy.scenarioId) {
    await ensureLocalNode(homeDir);
    return pluginBasicStartupPolicy;
  }
  return startupPolicy;
}
async function runEmbeddedServiceCLI() {
  const [command, serviceName, ...args] = process.argv.slice(2);
  if (command !== "__movscript_service") return false;
  switch (serviceName) {
    case "project-service":
      await runProjectServiceCLI(args, process.env);
      return true;
    case "editing-service":
      await runEditingServiceCLI(args, process.env);
      return true;
    case "canvas-service":
      await runCanvasServiceCLI(args, process.env);
      return true;
    case "media-pipeline":
      await runMediaPipelineServiceCLI(args, process.env);
      return true;
    default:
      throw new Error(`unknown embedded service: ${serviceName ?? ""}`);
  }
}
async function runLocalDaemonCLI() {
  const rawArgs = process.argv.slice(2);
  const [command, subcommand] = rawArgs;
  if (command === "mcp" && (subcommand === void 0 || subcommand === "stdio")) {
    await runPluginMCPStdioSession();
    return true;
  }
  if (command === "__movscript_local_node" && subcommand === "run") {
    await runPersistentLocalNode();
    return true;
  }
  if ((command === "daemon" || command === "local-node") && subcommand === "run") {
    await runPersistentLocalNode();
    return true;
  }
  if (command === "__movscript_movcli") {
    if (subcommand === "daemon" || subcommand === "local-node") {
      await runLocalDaemonCommand(subcommand, rawArgs.slice(2));
      return true;
    }
    await (0, import_cli.runMovcli)([process.argv[0] ?? "node", "movcli", ...rawArgs.slice(1)]);
    return true;
  }
  if (command === "cli") {
    const [cliCommand, ...cliArgs] = rawArgs.slice(1);
    if (!cliCommand || cliCommand === "--help" || cliCommand === "-h" || cliCommand === "help") {
      printMovScriptCLIHelp();
      return true;
    }
    if (cliCommand !== "daemon" && cliCommand !== "local-node") {
      await (0, import_cli.runMovcli)([process.argv[0] ?? "node", "movcli", ...rawArgs.slice(1)]);
      return true;
    }
    await runLocalDaemonCommand(cliCommand, cliArgs);
    return true;
  }
  if (command !== "daemon" && command !== "local-node") return false;
  await runLocalDaemonCommand(command, rawArgs.slice(1));
  return true;
}
async function runLocalDaemonCommand(command, rawArgs) {
  const parsed = parseLocalDaemonArgs(command, rawArgs);
  if (parsed.options.help) {
    printLocalDaemonHelp(command);
    return;
  }
  const homeDir = parsed.options.homeDir ?? resolveMovScriptHomeDir();
  switch (parsed.subcommand) {
    case "status":
      process.stdout.write(`${JSON.stringify(await probeLocalRuntimeDaemon(homeDir), null, 2)}
`);
      return;
    case "stop":
      process.stdout.write(`${JSON.stringify(await stopLocalRuntimeDaemon(homeDir, { force: parsed.options.force }), null, 2)}
`);
      return;
    case "restart":
      if (parsed.options.dataPlane || parsed.options.dataServiceURL || parsed.options.idleTimeout) {
        await stopLocalRuntimeDaemon(homeDir, { force: true }).catch(() => void 0);
        process.stdout.write(`${JSON.stringify(await ensureLocalNode(homeDir, parsed.options), null, 2)}
`);
        return;
      }
      process.stdout.write(`${JSON.stringify(await localRuntimeControlRequest(homeDir, "POST", "/restart"), null, 2)}
`);
      return;
    case void 0:
    case "start":
      process.stdout.write(`${JSON.stringify(await ensureLocalNode(homeDir, parsed.options), null, 2)}
`);
      return;
    default:
      throw new Error(`unknown ${command} command: ${parsed.subcommand}`);
  }
}
function parseLocalDaemonArgs(command, rawArgs) {
  const options = {};
  let subcommand;
  for (let index = 0; index < rawArgs.length; index += 1) {
    const arg = rawArgs[index];
    if (!arg) continue;
    if (!subcommand && !arg.startsWith("-")) {
      subcommand = arg;
      continue;
    }
    if (arg === "--help" || arg === "-h") {
      options.help = true;
      continue;
    }
    if (arg === "--force") {
      options.force = true;
      continue;
    }
    const optionValue = (name) => {
      if (arg.startsWith(`${name}=`)) return arg.slice(name.length + 1);
      const next = rawArgs[index + 1];
      if (!next || next.startsWith("-")) throw new Error(`${command} ${subcommand ?? "start"} requires a value for ${name}`);
      index += 1;
      return next;
    };
    if (arg === "--home" || arg === "--home-dir" || arg.startsWith("--home=") || arg.startsWith("--home-dir=")) {
      options.homeDir = resolve11(optionValue(arg.startsWith("--home-dir") ? "--home-dir" : "--home"));
      continue;
    }
    if (arg === "--data-plane" || arg.startsWith("--data-plane=")) {
      options.dataPlane = parseLocalDaemonDataPlane(optionValue("--data-plane"));
      continue;
    }
    if (arg === "--data-service-url" || arg.startsWith("--data-service-url=")) {
      options.dataServiceURL = optionValue("--data-service-url");
      continue;
    }
    if (arg === "--idle-timeout" || arg.startsWith("--idle-timeout=")) {
      options.idleTimeout = optionValue("--idle-timeout");
      continue;
    }
    throw new Error(`unknown ${command} option: ${arg}`);
  }
  return { subcommand: subcommand ?? "start", options };
}
function parseLocalDaemonDataPlane(value) {
  const normalized = value.trim().toLowerCase();
  if (normalized === "local" || normalized === "cloud" || normalized === "external") return normalized;
  throw new Error(`invalid daemon data plane: ${value}`);
}
function localDaemonEnvFromOptions(options) {
  const env = {};
  if (options.dataPlane) env.MOVSCRIPT_LOCAL_DAEMON_DATA_PLANE = options.dataPlane;
  if (options.dataServiceURL) env.MOVSCRIPT_DATA_SERVICE_URL = options.dataServiceURL;
  if (options.idleTimeout) {
    env.MOVSCRIPT_LOCAL_DAEMON_IDLE_TIMEOUT = options.idleTimeout;
    env.MOVSCRIPT_LOCAL_NODE_IDLE_TIMEOUT = options.idleTimeout;
  }
  return Object.keys(env).length > 0 ? env : void 0;
}
function printMovScriptCLIHelp() {
  process.stdout.write([
    "MovScript command line",
    "",
    "Usage:",
    "  movscript mcp stdio",
    "  movscript daemon run",
    "  movscript daemon <start|status|stop|restart> [options]",
    "  movscript admin|system|runtime|workspace ...",
    "",
    "Compatibility:",
    "  movscript-agent-mcp        # same as movscript mcp stdio",
    "  movcli ...                 # same command runner, legacy name",
    ""
  ].join("\n"));
}
function printLocalDaemonHelp(command) {
  process.stdout.write([
    `Usage: ${command} <start|status|stop|restart> [options]`,
    "",
    "Options:",
    "  --home <dir>                    MovScript home directory",
    "  --data-plane <local|cloud|external>",
    "  --data-service-url <url>        Cloud or external Data Service URL",
    "  --idle-timeout <duration>       Idle shutdown timeout, for example 30m or never",
    "  --force                        Force stop when no control endpoint responds",
    ""
  ].join("\n"));
}
function selectPluginStartupPolicy(homeDir) {
  const requested = (process.env.MOVSCRIPT_PLUGIN_MODE ?? process.env.MOVSCRIPT_PLUGIN_SCENARIO ?? "").trim().toLowerCase();
  if (requested === "full-local" || requested === "plugin-full-local") return pluginFullLocalStartupPolicy;
  if (requested === "basic" || requested === "plugin-basic") return pluginBasicStartupPolicy;
  if (requested === "desktop" || requested === "plugin-desktop-compatible" || requested === "plugin-desktop-owned") {
    return pluginDesktopCompatibilityStartupPolicy;
  }
  return pluginFullLocalStartupPolicy;
}
async function ensureLocalNode(homeDir, options = {}) {
  return await ensureLocalRuntimeDaemon({
    homeDir,
    entrypoint: AGENT_MCP_ENTRYPOINT,
    runArgs: ["daemon", "run"],
    cwd: RUN_CWD,
    env: localDaemonEnvFromOptions(options),
    identity: currentPluginIdentity()
  });
}
function isLegacyAgentMCPInvocation() {
  const invoked = process.argv[1];
  if (!invoked) return false;
  const invokedName = basename5(invoked);
  return (invokedName === "movscript-agent-mcp" || invokedName === "movscript-agent-mcp.mjs") && process.argv.length <= 2;
}
async function runPersistentLocalNode() {
  const homeDir = resolveMovScriptHomeDir();
  const idleTimeoutMs = parseIdleTimeout(process.env.MOVSCRIPT_LOCAL_DAEMON_IDLE_TIMEOUT ?? process.env.MOVSCRIPT_LOCAL_NODE_IDLE_TIMEOUT);
  const pluginIdentity = currentPluginIdentity();
  let shouldExit = false;
  let restartCount = 0;
  while (!shouldExit) {
    const dataPlane = resolveLocalDaemonDataPlane(process.env);
    const dataServiceURL = configuredDataServiceURLForDataPlane(dataPlane);
    const startupPolicy = localNodeStartupPolicyForDataPlane(dataPlane);
    let resolveAction;
    const actionPromise = new Promise((resolveActionPromise) => {
      resolveAction = resolveActionPromise;
    });
    const state = {
      homeDir,
      startedAt: /* @__PURE__ */ new Date(),
      lastActivityAt: /* @__PURE__ */ new Date(),
      idleTimeoutMs,
      dataPlane,
      dataServiceURL,
      pluginIdentity,
      restartCount,
      requestAction: (action2) => resolveAction(action2),
      snapshot: () => readRuntimeHomeSnapshot(homeDir)
    };
    const runner = createScenarioApplicationRunner({
      homeDir,
      application: localNodeApplicationManifest,
      scenario: startupPolicy,
      programs: createProgramAdapters({ includeMCPHost: false, controlState: state }),
      log: (message, metadata) => {
        if (process.env.MOVSCRIPT_LOCAL_NODE_DEBUG === "1") {
          process.stderr.write(`[movscript-local-node] ${message} ${metadata ? JSON.stringify(metadata) : ""}
`);
        }
      }
    });
    await runner.start();
    writeRuntimeAppRecord(homeDir, {
      applicationId: LOCAL_NODE_APP_ID,
      owner: "agent-provider",
      profile: startupPolicy.scenarioId,
      pid: process.pid,
      status: "ready",
      ready: true,
      metadata: {
        ...pluginIdentity,
        dataPlane,
        ...dataServiceURL ? { dataServiceURL } : {},
        idleTimeoutMs,
        restartCount
      }
    });
    const signalAction = installLocalNodeSignalHandlers(resolveAction);
    const idleAction = startIdleWatcher(state);
    const action = await actionPromise;
    idleAction();
    signalAction();
    await runner.shutdown();
    writeRuntimeAppRecord(homeDir, {
      applicationId: LOCAL_NODE_APP_ID,
      owner: "agent-provider",
      profile: startupPolicy.scenarioId,
      pid: process.pid,
      status: action.type === "restart" ? "starting" : "stopped",
      ready: action.type === "restart",
      metadata: {
        ...pluginIdentity,
        dataPlane,
        ...dataServiceURL ? { dataServiceURL } : {},
        reason: action.reason,
        idleTimeoutMs,
        restartCount
      }
    });
    if (action.type === "restart") {
      restartCount += 1;
      continue;
    }
    shouldExit = true;
  }
}
function installLocalNodeSignalHandlers(requestAction) {
  const handleSignal = (signal) => {
    requestAction({ type: "shutdown", reason: signal.toLowerCase() });
  };
  process.once("SIGTERM", handleSignal);
  process.once("SIGINT", handleSignal);
  return () => {
    process.off("SIGTERM", handleSignal);
    process.off("SIGINT", handleSignal);
  };
}
function startIdleWatcher(state) {
  if (state.idleTimeoutMs === null) return () => void 0;
  const intervalMs = Math.max(5e3, Math.min(6e4, Math.floor(state.idleTimeoutMs / 4)));
  const interval = setInterval(() => {
    if (Date.now() - state.lastActivityAt.getTime() >= state.idleTimeoutMs) {
      state.requestAction({ type: "shutdown", reason: "idle_timeout" });
    }
  }, intervalMs);
  return () => clearInterval(interval);
}
function parseIdleTimeout(value) {
  const raw = value?.trim().toLowerCase();
  if (!raw) return null;
  if (raw === "never" || raw === "0" || raw === "off") return null;
  const match = raw.match(/^(\d+(?:\.\d+)?)(ms|s|m|h)?$/);
  if (!match) throw new Error(`invalid local daemon idle timeout: ${value}`);
  const amount = Number(match[1]);
  const unit = match[2] ?? "ms";
  const factor = unit === "h" ? 60 * 60 * 1e3 : unit === "m" ? 60 * 1e3 : unit === "s" ? 1e3 : 1;
  return Math.max(1e3, Math.floor(amount * factor));
}
function resolveLocalDaemonDataPlane(env) {
  const explicit = (env.MOVSCRIPT_LOCAL_DAEMON_DATA_PLANE ?? env.MOVSCRIPT_LOCAL_NODE_DATA_PLANE ?? "").trim().toLowerCase();
  if (explicit === "local" || explicit === "cloud" || explicit === "external") return explicit;
  const mode = (env.MOVSCRIPT_PLUGIN_MODE ?? env.MOVSCRIPT_PLUGIN_SCENARIO ?? "").trim().toLowerCase();
  if (mode === "cloud" || mode === "plugin-cloud") return "cloud";
  const dataServiceURL = env.MOVSCRIPT_DATA_SERVICE_URL?.trim();
  if (dataServiceURL && !isLocalHTTPURL(dataServiceURL)) return "external";
  return "local";
}
function isLocalHTTPURL(value) {
  try {
    const url = new URL(value);
    return ["localhost", "127.0.0.1", "::1"].includes(url.hostname);
  } catch {
    return false;
  }
}
function localNodeStartupPolicyForDataPlane(dataPlane) {
  return dataPlane === "local" ? localNodeStartupPolicy : localNodeCloudDataStartupPolicy;
}
function configuredDataServiceURLForDataPlane(dataPlane) {
  const dataServiceURL = process.env.MOVSCRIPT_DATA_SERVICE_URL?.trim();
  return dataPlane === "local" ? void 0 : dataServiceURL || void 0;
}
function createProgramAdapters(options = {}) {
  const adapters = [
    {
      manifest: agent_launcher_program_manifest_default,
      instanceId: `launcher-${process.pid}`,
      start: () => ({
        pid: process.pid
      }),
      health: () => ({
        ready: true
      })
    }
  ];
  if (options.includeMCPHost !== false) {
    adapters.push({
      manifest: mcpHostProgramManifest,
      instanceId: `stdio-${process.pid}`,
      profile: "stdio",
      start: () => ({
        pid: process.pid,
        endpoint: {
          protocol: "stdio",
          url: "stdio://movscript"
        }
      }),
      health: (_context, runtime) => ({
        ready: true,
        endpoint: runtime.endpoint
      })
    });
  }
  if (options.controlState) adapters.push(createLocalNodeControlProgramAdapter(options.controlState));
  adapters.push(
    createMediaPipelineProgramAdapter(),
    createDataServiceProgramAdapter(),
    createNodeServiceProgramAdapter({
      manifest: canvasServiceProgramManifest,
      instanceIdPrefix: "canvas-service",
      embeddedServiceName: "canvas-service",
      scriptPath: resolve11(DEV_REPO_ROOT, "services/canvas-service/bin/movscript-canvas-service.mjs"),
      env: (context, endpoint) => ({
        MOVSCRIPT_CANVAS_SERVICE_HOST: "127.0.0.1",
        MOVSCRIPT_CANVAS_SERVICE_PORT: String(endpoint.port),
        MOVSCRIPT_DATA_SERVICE_URL: resolveDataServiceURL(context.homeDir) ?? resolveGatewayURL(context.homeDir) ?? "http://127.0.0.1:8766"
      })
    }),
    createNodeServiceProgramAdapter({
      manifest: projectServiceProgramManifest,
      instanceIdPrefix: "project-service",
      embeddedServiceName: "project-service",
      scriptPath: resolve11(DEV_REPO_ROOT, "services/project-service/bin/movscript-project-service.mjs"),
      env: (_context, endpoint) => ({
        MOVSCRIPT_PROJECT_SERVICE_HOST: "127.0.0.1",
        MOVSCRIPT_PROJECT_SERVICE_PORT: String(endpoint.port)
      })
    }),
    createNodeServiceProgramAdapter({
      manifest: editingServiceProgramManifest,
      instanceIdPrefix: "editing-service",
      embeddedServiceName: "editing-service",
      scriptPath: resolve11(DEV_REPO_ROOT, "services/editing-service/bin/movscript-editing-service.mjs"),
      env: (_context, endpoint) => ({
        MOVSCRIPT_EDITING_SERVICE_HOST: "127.0.0.1",
        MOVSCRIPT_EDITING_SERVICE_PORT: String(endpoint.port)
      })
    }),
    createLocalSurfaceHostProgramAdapter()
  );
  return adapters;
}
function writeLocalNodeGatewayRuntimeRecords(context, state, port) {
  const instanceId = `local-node-gateway-${process.pid}`;
  const endpoint = {
    ...httpEndpoint(context, port),
    serviceName: LOCAL_NODE_GATEWAY_SERVICE4
  };
  writeRuntimeServiceRecord(context.homeDir, {
    serviceName: LOCAL_NODE_GATEWAY_SERVICE4,
    instanceId,
    ownerApplicationId: LOCAL_NODE_APP_ID,
    profile: "local",
    pid: process.pid,
    status: "ready",
    ready: true,
    endpoint: {
      ...endpoint,
      instanceId
    },
    metadata: {
      mode: "local-daemon",
      role: "stable-local-node-gateway",
      dataPlane: state.dataPlane,
      idleTimeoutMs: state.idleTimeoutMs,
      ...state.pluginIdentity
    }
  });
  writeRuntimeEndpointRecord(context.homeDir, {
    ...endpoint,
    instanceId,
    pid: process.pid,
    status: "ready",
    ready: true,
    metadata: {
      mode: "local-daemon",
      role: "stable-local-node-gateway"
    }
  });
}
function createLocalNodeControlProgramAdapter(state) {
  return {
    manifest: localNodeControlProgramManifest,
    instanceId: `local-node-control-${process.pid}`,
    profile: "local",
    start: async (context) => {
      const runtime = await startLocalNodeControlServer(state, localSurfaceHostStaticRoot());
      writeLocalNodeGatewayRuntimeRecords(context, state, runtime.port);
      return {
        pid: process.pid,
        endpoint: httpEndpoint(context, runtime.port),
        server: runtime.server,
        metadata: {
          mode: "local-daemon",
          gatewayServiceName: LOCAL_NODE_GATEWAY_SERVICE4,
          dataPlane: state.dataPlane,
          idleTimeoutMs: state.idleTimeoutMs,
          ...state.pluginIdentity
        }
      };
    },
    health: (_context, runtime) => ({
      ready: Boolean(runtime.endpoint?.url),
      endpoint: runtime.endpoint
    }),
    stop: async (_context, runtime) => {
      await closeProgramRuntime(runtime);
    }
  };
}
function createMediaPipelineProgramAdapter() {
  return {
    manifest: mediaPipelineProgramManifest,
    instanceId: `media-pipeline-${process.pid}`,
    profile: "local",
    start: async (context) => {
      return startNodeHTTPServiceProgram({
        context,
        embeddedServiceName: "media-pipeline",
        scriptPath: resolve11(DEV_REPO_ROOT, "services/media-pipeline/bin/movscript-media-pipeline.mjs"),
        env: (_context, endpoint) => ({
          MOVSCRIPT_MEDIA_PIPELINE_HOST: "127.0.0.1",
          MOVSCRIPT_MEDIA_PIPELINE_PORT: String(endpoint.port)
        }),
        metadata: {
          mode: "plugin-full-local",
          runtime: "media_pipeline_service"
        }
      });
    },
    health: (_context, runtime) => ({
      ready: Boolean(runtime.endpoint?.url),
      endpoint: runtime.endpoint
    }),
    stop: async (_context, runtime) => {
      await closeProgramRuntime(runtime);
    }
  };
}
function createDataServiceProgramAdapter() {
  return {
    manifest: dataServiceProgramManifest,
    instanceId: `data-service-${process.pid}`,
    profile: "local",
    start: async (context) => {
      const port = await reservePort();
      const dataDir = join10(context.homeDir, "data-service");
      const encryptionKey = process.env.ENCRYPTION_KEY?.trim() || readOrCreateLocalEncryptionKey(dataDir);
      const binaryPath = process.env.MOVSCRIPT_DATA_SERVICE_BINARY ? resolve11(process.env.MOVSCRIPT_DATA_SERVICE_BINARY) : resolveFirstExisting(
        resolve11(PLUGIN_ROOT, "runtime/services/data-service/bin/movscript-server"),
        resolve11(DEV_REPO_ROOT, "services/data-service/bin/movscript-server")
      );
      if (!existsSync5(binaryPath)) {
        throw new Error(`Data Service binary was not found: ${binaryPath}`);
      }
      const child = spawn4(binaryPath, [], {
        cwd: dirname8(dirname8(binaryPath)),
        env: {
          ...process.env,
          MOVSCRIPT_HOME: context.homeDir,
          MOVSCRIPT_APP_MODE: "local",
          MOVSCRIPT_DEPLOYMENT_MODE: "local",
          MOVSCRIPT_DEPENDENCY_PROFILE: "local",
          MOVSCRIPT_DATA_DIR: dataDir,
          DB_DRIVER: "sqlite",
          DB_PATH: join10(dataDir, "movscript.db"),
          ENCRYPTION_KEY: encryptionKey,
          SERVER_PORT: String(port),
          MOVSCRIPT_AUTH_MODE: "local-owner",
          MOVSCRIPT_CORS_ALLOWED_ORIGINS: "*"
        },
        stdio: ["ignore", "ignore", "pipe"]
      });
      const stderr = collectChildStderr(child);
      child.once("exit", (code, signal) => {
        if (code !== null && code !== 0) context.log("data-service exited", { code, stderr: stderr() });
        if (signal) context.log("data-service exited by signal", { signal, stderr: stderr() });
      });
      const endpoint = httpEndpoint(context, port);
      await waitForHTTP(`${endpoint.url}/health`, child, stderr);
      return {
        pid: child.pid,
        endpoint,
        child,
        metadata: {
          mode: "plugin-full-local",
          runtime: "data_service_process"
        }
      };
    },
    health: (_context, runtime) => ({
      ready: Boolean(runtime.endpoint?.url),
      endpoint: runtime.endpoint
    }),
    stop: async (_context, runtime) => {
      await closeProgramRuntime(runtime);
    }
  };
}
function readOrCreateLocalEncryptionKey(dataDir) {
  const keyPath = join10(dataDir, "local-encryption-key.hex");
  if (existsSync5(keyPath)) {
    const existing = readFileSync6(keyPath, "utf8").trim();
    if (/^[0-9a-f]{64}$/i.test(existing)) return existing;
    throw new Error(`invalid local Data Service encryption key file: ${keyPath}`);
  }
  mkdirSync5(dataDir, { recursive: true, mode: 448 });
  const key = randomBytes(32).toString("hex");
  writeFileSync5(keyPath, `${key}
`, { encoding: "utf8", mode: 384 });
  return key;
}
function createNodeServiceProgramAdapter(options) {
  return {
    manifest: options.manifest,
    instanceId: `${options.instanceIdPrefix}-${process.pid}`,
    profile: "local",
    start: (context) => startNodeHTTPServiceProgram({
      context,
      embeddedServiceName: options.embeddedServiceName,
      scriptPath: options.scriptPath,
      env: options.env,
      metadata: {
        mode: "plugin-full-local",
        runtime: `${options.instanceIdPrefix}_process`
      }
    }),
    health: (_context, runtime) => ({
      ready: Boolean(runtime.endpoint?.url),
      endpoint: runtime.endpoint
    }),
    stop: async (_context, runtime) => {
      await closeProgramRuntime(runtime);
    }
  };
}
function createLocalSurfaceHostProgramAdapter() {
  return {
    manifest: localSurfaceHostProgramManifest,
    instanceId: `local-surface-host-${process.pid}`,
    profile: "local",
    start: async (context) => {
      const staticRoot = localSurfaceHostStaticRoot();
      const runtime = await startStaticHTTPServer(staticRoot, context.homeDir);
      return {
        pid: process.pid,
        endpoint: httpEndpoint(context, runtime.port),
        server: runtime.server,
        metadata: {
          mode: context.profile === "desktop-connected" ? "plugin-desktop-compatible" : "plugin-full-local",
          role: "agent-facing-surface-host",
          runtime: "local_surface_host_static",
          staticRoot
        }
      };
    },
    health: (_context, runtime) => ({
      ready: Boolean(runtime.endpoint?.url),
      endpoint: runtime.endpoint
    }),
    stop: async (_context, runtime) => {
      await closeProgramRuntime(runtime);
    }
  };
}
async function startNodeHTTPServiceProgram(input) {
  if (!HAS_BUNDLED_RUNTIME && !existsSync5(input.scriptPath)) throw new Error(`service script was not found: ${input.scriptPath}`);
  const port = await reservePort();
  const args = HAS_BUNDLED_RUNTIME ? [AGENT_MCP_ENTRYPOINT, "__movscript_service", input.embeddedServiceName, "serve"] : [input.scriptPath, "serve"];
  const child = spawn4(process.execPath, args, {
    cwd: RUN_CWD,
    env: {
      ...process.env,
      MOVSCRIPT_HOME: input.context.homeDir,
      ...input.env(input.context, { port })
    },
    stdio: ["ignore", "ignore", "pipe"]
  });
  const stderr = collectChildStderr(child);
  child.once("exit", (code, signal) => {
    if (code !== null && code !== 0) input.context.log("node service exited", { serviceName: input.context.program.serviceName, code, stderr: stderr() });
    if (signal) input.context.log("node service exited by signal", { serviceName: input.context.program.serviceName, signal, stderr: stderr() });
  });
  const endpoint = httpEndpoint(input.context, port);
  await waitForHTTP(`${endpoint.url}/health`, child, stderr);
  return {
    pid: child.pid,
    endpoint,
    child,
    metadata: input.metadata
  };
}
function resolveFirstExisting(...paths) {
  return paths.find((path) => existsSync5(path)) ?? paths[0] ?? "";
}
function httpEndpoint(context, port) {
  const url = `http://127.0.0.1:${port}`;
  return {
    serviceName: context.program.serviceName,
    protocol: "http",
    url,
    baseURL: url,
    port,
    applicationId: context.application.applicationId
  };
}
async function reservePort() {
  const server = createServer5();
  await new Promise((resolveListen, rejectListen) => {
    server.once("error", rejectListen);
    server.listen(0, "127.0.0.1", () => resolveListen());
  });
  const address = server.address();
  const port = typeof address === "object" && address ? address.port : 0;
  await new Promise((resolveClose, rejectClose) => {
    server.close((error) => error ? rejectClose(error) : resolveClose());
  });
  if (!port) throw new Error("failed to reserve local port");
  return port;
}
function collectChildStderr(child) {
  const chunks = [];
  child.stderr?.setEncoding("utf8");
  child.stderr?.on("data", (chunk) => {
    chunks.push(String(chunk));
    if (chunks.join("").length > 16e3) chunks.splice(0, chunks.length - 8);
  });
  return () => chunks.join("");
}
async function waitForHTTP(url, child, stderr) {
  const startedAt = Date.now();
  while (Date.now() - startedAt < 15e3) {
    if (child.exitCode !== null) {
      throw new Error(`service exited before becoming healthy: ${stderr()}`);
    }
    try {
      const response = await fetch(url);
      if (response.ok) return;
    } catch {
    }
    await new Promise((resolvePoll) => setTimeout(resolvePoll, 100));
  }
  throw new Error(`service did not become healthy at ${url}: ${stderr()}`);
}
async function closeProgramRuntime(runtime) {
  if (runtime.close) {
    await runtime.close();
    return;
  }
  if (runtime.server) {
    await new Promise((resolveClose, rejectClose) => {
      runtime.server?.close((error) => error ? rejectClose(error) : resolveClose());
    });
  }
  if (runtime.child && runtime.child.exitCode === null) {
    runtime.child.kill("SIGTERM");
    await new Promise((resolveExit) => {
      const timeout = setTimeout(() => {
        runtime.child?.kill("SIGKILL");
        resolveExit();
      }, 3e3);
      runtime.child?.once("exit", () => {
        clearTimeout(timeout);
        resolveExit();
      });
    });
  }
}
async function startLocalNodeControlServer(state, staticRoot) {
  const server = createServer5(async (request, response) => {
    const url = new URL(request.url ?? "/", "http://127.0.0.1");
    const send = (status, payload) => {
      response.writeHead(status, {
        ...corsHeadersForRequest(request),
        "content-type": "application/json; charset=utf-8"
      });
      response.end(JSON.stringify(payload));
    };
    if (request.method === "OPTIONS") {
      response.writeHead(204, corsHeadersForRequest(request));
      response.end();
      return;
    }
    if (request.method === "GET" && url.pathname === "/health") {
      state.lastActivityAt = /* @__PURE__ */ new Date();
      send(200, { status: "ok", serviceName: LOCAL_NODE_CONTROL_SERVICE2, pid: process.pid });
      return;
    }
    if (request.method === "GET" && url.pathname === "/status") {
      send(200, localNodeStatusPayload(state));
      return;
    }
    if (request.method === "POST" && url.pathname === "/touch") {
      state.lastActivityAt = /* @__PURE__ */ new Date();
      send(200, { status: "touched", lastActivityAt: state.lastActivityAt.toISOString() });
      return;
    }
    if (request.method === "POST" && url.pathname === "/shutdown") {
      send(202, { status: "stopping", pid: process.pid });
      setImmediate(() => state.requestAction({ type: "shutdown", reason: "explicit_stop" }));
      return;
    }
    if (request.method === "POST" && url.pathname === "/restart") {
      send(202, { status: "restarting", pid: process.pid });
      setImmediate(() => state.requestAction({ type: "restart", reason: "explicit_restart" }));
      return;
    }
    if (await handleLocalSurfaceGatewayRequest(staticRoot, state.homeDir, request, response, url, state)) {
      return;
    }
    send(404, { error: "not_found" });
  });
  const requestedPort = resolveLocalNodeGatewayPort();
  const port = await new Promise((resolveListen, rejectListen) => {
    server.once("error", rejectListen);
    server.listen(requestedPort, "127.0.0.1", () => {
      const address = server.address();
      resolveListen(typeof address === "object" && address ? address.port : 0);
    });
  });
  if (!port) throw new Error("failed to start local-node control server");
  return { server, port };
}
function resolveLocalNodeGatewayPort() {
  const explicit = Number(
    process.env.MOVSCRIPT_LOCAL_NODE_GATEWAY_PORT ?? process.env.MOVSCRIPT_LOCAL_NODE_PORT ?? ""
  );
  if (Number.isInteger(explicit) && explicit > 0 && explicit < 65536) return explicit;
  const legacy = Number(process.env.MOVSCRIPT_LOCAL_DAEMON_PORT ?? "");
  if (Number.isInteger(legacy) && legacy > 0 && legacy < 65536) return legacy;
  return DEFAULT_LOCAL_NODE_GATEWAY_PORT;
}
function localNodeStatusPayload(state) {
  const now = /* @__PURE__ */ new Date();
  const snapshot = state.snapshot();
  return {
    status: "ready",
    applicationId: LOCAL_NODE_APP_ID,
    pid: process.pid,
    homeDir: state.homeDir,
    ...state.pluginIdentity,
    dataPlane: state.dataPlane,
    ...state.dataServiceURL ? { dataServiceURL: state.dataServiceURL } : {},
    startedAt: state.startedAt.toISOString(),
    lastActivityAt: state.lastActivityAt.toISOString(),
    idleTimeoutMs: state.idleTimeoutMs,
    idleForMs: now.getTime() - state.lastActivityAt.getTime(),
    restartCount: state.restartCount,
    services: snapshot.services.filter((record) => record.ownerApplicationId === LOCAL_NODE_APP_ID).filter((record) => record.instanceId?.endsWith(`-${process.pid}`)).filter((record) => record.ready !== true || pidIsAlive(record.pid)).map((record) => ({
      serviceName: record.serviceName,
      status: record.status,
      ready: record.ready,
      pid: record.pid,
      endpoint: endpointURL8(record.endpoint)
    }))
  };
}
async function startStaticHTTPServer(staticRoot, homeDir) {
  if (!existsSync5(join10(staticRoot, "index.html"))) {
    throw new Error(`Local Surface Host build output was not found: ${staticRoot}`);
  }
  const server = createServer5(async (request, response) => {
    const url = new URL(request.url ?? "/", "http://127.0.0.1");
    if (request.method === "GET" && url.pathname === "/health") {
      response.writeHead(200, {
        ...corsHeadersForRequest(request),
        "content-type": "application/json; charset=utf-8"
      });
      response.end(JSON.stringify({ status: "ok", serviceName: localSurfaceHostProgramManifest.serviceName }));
      return;
    }
    if (await handleLocalSurfaceGatewayRequest(staticRoot, homeDir, request, response, url)) {
      return;
    }
    if (request.method !== "GET" && request.method !== "HEAD") {
      response.writeHead(405, { "content-type": "application/json; charset=utf-8" });
      response.end(JSON.stringify({ error: "method_not_allowed" }));
      return;
    }
    await serveLocalSurfaceStaticFile(staticRoot, request, response, url);
  });
  const port = await new Promise((resolveListen, rejectListen) => {
    server.once("error", rejectListen);
    server.listen(0, "127.0.0.1", () => {
      const address = server.address();
      resolveListen(typeof address === "object" && address ? address.port : 0);
    });
  });
  if (!port) throw new Error("failed to start Local Surface Host static server");
  return { server, port };
}
async function handleLocalSurfaceGatewayRequest(staticRoot, homeDir, request, response, url, localNodeState) {
  if (isGatewayAPIPath(url.pathname)) {
    applyCORSHeaders(response, request);
    if (request.method === "OPTIONS") {
      response.writeHead(204);
      response.end();
      return true;
    }
  }
  if (url.pathname === DAEMON_CONTEXT_ENDPOINT || url.pathname.startsWith(`${DAEMON_CONTEXT_SESSIONS_ENDPOINT}`)) {
    await handleDaemonContextRequest(homeDir, request, response, url);
    return true;
  }
  if (url.pathname === DAEMON_RUNTIME_DESCRIPTOR_ENDPOINT || url.pathname === DAEMON_RUNTIME_STATUS_ENDPOINT || url.pathname === DAEMON_RUNTIME_DIAGNOSTICS_ENDPOINT || url.pathname === DAEMON_RUNTIME_CONFIGURE_ENDPOINT) {
    await handleDaemonRuntimeRequest(homeDir, request, response, url, localNodeState);
    return true;
  }
  if (LOCAL_PROJECT_SERVICE_PROXY_ROUTES.has(url.pathname)) {
    await proxyProjectServiceRequest(homeDir, request, response, url);
    return true;
  }
  if (url.pathname === "/v1/editing" || url.pathname.startsWith("/v1/editing/")) {
    await proxyRuntimeServiceRequest(homeDir, request, response, url, {
      serviceName: EDITING_SERVICE_NAME2,
      gatewayPrefix: "/v1/editing",
      upstreamPrefix: "/v1/editing",
      unavailableCode: "editing_service_unavailable",
      failureCode: "editing_service_proxy_failed",
      serviceLabel: "Editing Service"
    });
    return true;
  }
  if (url.pathname === "/v1/media-pipeline" || url.pathname.startsWith("/v1/media-pipeline/")) {
    await proxyRuntimeServiceRequest(homeDir, request, response, url, {
      serviceName: MEDIA_PIPELINE_SERVICE_NAME,
      gatewayPrefix: "/v1/media-pipeline",
      upstreamPrefix: "/v1/media-pipeline",
      unavailableCode: "media_pipeline_unavailable",
      failureCode: "media_pipeline_proxy_failed",
      serviceLabel: "Media Pipeline"
    });
    return true;
  }
  if (request.method === "POST" && (url.pathname === "/v1/host/editing/import-file" || url.pathname === "/local-api/editing/import-file")) {
    await importLocalSurfaceEditingFile(homeDir, request, response, url);
    return true;
  }
  if ((request.method === "GET" || request.method === "HEAD") && (url.pathname === "/v1/host/editing/media-file" || url.pathname === "/local-api/editing/media-file")) {
    await serveLocalSurfaceEditingMediaFile(homeDir, request, response, url);
    return true;
  }
  if (url.pathname === "/v1/canvas" || url.pathname.startsWith("/v1/canvas/")) {
    await proxyCanvasServiceRequest(homeDir, request, response, url, "/v1/canvas");
    return true;
  }
  if (url.pathname === "/local-api/canvas" || url.pathname.startsWith("/local-api/canvas/")) {
    await proxyCanvasServiceRequest(homeDir, request, response, url, "/local-api/canvas");
    return true;
  }
  if (url.pathname === "/local-api/data" || url.pathname.startsWith("/local-api/data/")) {
    await proxyDataServiceRequest2(homeDir, request, response, url, "/local-api/data");
    return true;
  }
  if (url.pathname === "/api/v1" || url.pathname.startsWith("/api/v1/")) {
    await proxyDataServiceRequest2(homeDir, request, response, url, "");
    return true;
  }
  if (request.method === "GET" || request.method === "HEAD") {
    await serveLocalSurfaceStaticFile(staticRoot, request, response, url);
    return true;
  }
  return false;
}
function isGatewayAPIPath(pathname) {
  return pathname === "/api/v1" || pathname.startsWith("/api/v1/") || pathname === "/local-api/data" || pathname.startsWith("/local-api/data/") || pathname === "/local-api/canvas" || pathname.startsWith("/local-api/canvas/") || pathname === "/v1/canvas" || pathname.startsWith("/v1/canvas/") || pathname === DAEMON_CONTEXT_ENDPOINT || pathname === DAEMON_CONTEXT_SESSIONS_ENDPOINT || pathname.startsWith(`${DAEMON_CONTEXT_SESSIONS_ENDPOINT}/`) || pathname === DAEMON_RUNTIME_DESCRIPTOR_ENDPOINT || pathname === DAEMON_RUNTIME_STATUS_ENDPOINT || pathname === DAEMON_RUNTIME_DIAGNOSTICS_ENDPOINT || pathname === DAEMON_RUNTIME_CONFIGURE_ENDPOINT || pathname.startsWith("/v1/project/") || pathname === "/v1/editing" || pathname.startsWith("/v1/editing/") || pathname === "/v1/media-pipeline" || pathname.startsWith("/v1/media-pipeline/") || pathname === "/v1/host/editing" || pathname.startsWith("/v1/host/editing/") || pathname === "/local-api/editing" || pathname.startsWith("/local-api/editing/") || pathname.startsWith("/local-api/project/");
}
async function serveLocalSurfaceStaticFile(staticRoot, request, response, url) {
  const filePath = resolveStaticFilePath(staticRoot, url.pathname);
  if (request.method === "HEAD") {
    response.writeHead(200, { "content-type": contentType(filePath) });
    response.end();
    return;
  }
  const stream = createReadStream(filePath);
  stream.once("error", () => {
    writeLocalSurfaceJSON(response, 500, {
      error: "static_file_read_failed",
      message: `Failed to read Local Surface Host asset: ${filePath}`
    });
  });
  stream.once("open", () => {
    response.writeHead(200, { "content-type": contentType(filePath) });
  });
  stream.pipe(response);
}
function localSurfaceHostStaticRoot() {
  return resolveFirstExisting(
    resolve11(PLUGIN_ROOT, "runtime/services/local-surface-host/dist"),
    resolve11(DEV_REPO_ROOT, "services/local-surface-host/dist")
  );
}
async function handleDaemonRuntimeRequest(homeDir, request, response, url, localNodeState) {
  if (url.pathname === DAEMON_RUNTIME_CONFIGURE_ENDPOINT) {
    await handleDaemonRuntimeConfigureRequest(homeDir, request, response, localNodeState);
    return;
  }
  if (request.method !== "GET") {
    writeLocalSurfaceJSON(response, 405, {
      error: "method_not_allowed",
      message: "Daemon runtime descriptor only supports GET."
    });
    return;
  }
  const descriptor = issueDaemonRuntimeDescriptor(homeDir, request);
  if (url.pathname === DAEMON_RUNTIME_DIAGNOSTICS_ENDPOINT) {
    if (!daemonRuntimeDiagnosticsEnabled()) {
      writeLocalSurfaceJSON(response, 404, {
        error: "diagnostics_not_enabled",
        message: "Daemon runtime diagnostics are debug-only and are not enabled for this process."
      });
      return;
    }
    writeLocalSurfaceJSON(response, 200, issueDaemonRuntimeDiagnostics(homeDir, descriptor));
    return;
  }
  if (url.pathname === DAEMON_RUNTIME_STATUS_ENDPOINT) {
    writeLocalSurfaceJSON(response, 200, {
      schema: "movscript.runtime-status.v1",
      runtime: descriptor.runtime,
      gateway: descriptor.gateway,
      dataConnection: descriptor.dataConnection,
      capabilities: descriptor.capabilities,
      status: descriptor.dataConnection.status === "unavailable" ? "unavailable" : "ready"
    });
    return;
  }
  writeLocalSurfaceJSON(response, 200, descriptor);
}
async function handleDaemonRuntimeConfigureRequest(homeDir, request, response, localNodeState) {
  if (request.method !== "POST") {
    writeLocalSurfaceJSON(response, 405, {
      error: "method_not_allowed",
      message: "Daemon runtime configure only supports POST."
    });
    return;
  }
  if (!localNodeState) {
    writeLocalSurfaceJSON(response, 409, {
      error: "runtime_configure_unavailable",
      message: "Daemon runtime configure must be handled by movscript.local-node."
    });
    return;
  }
  try {
    const input = await readDaemonRuntimeConfigureInput(request);
    localNodeState.lastActivityAt = /* @__PURE__ */ new Date();
    if (daemonRuntimeConfigureMatchesCurrent(localNodeState, input)) {
      writeLocalSurfaceJSON(response, 200, {
        schema: "movscript.runtime-configure-result.v1",
        status: "ready",
        restarted: false,
        dataConnection: daemonDataConnectionContext(homeDir)
      });
      return;
    }
    process.env.MOVSCRIPT_LOCAL_DAEMON_DATA_PLANE = input.dataPlane;
    if (input.dataPlane === "local" || !input.dataServiceURL) {
      delete process.env.MOVSCRIPT_DATA_SERVICE_URL;
    } else {
      process.env.MOVSCRIPT_DATA_SERVICE_URL = input.dataServiceURL;
    }
    writeLocalSurfaceJSON(response, 202, {
      schema: "movscript.runtime-configure-result.v1",
      status: "restarting",
      restarted: true,
      dataConnection: daemonDataConnectionContext(homeDir)
    });
    setImmediate(() => localNodeState.requestAction({ type: "restart", reason: "runtime_configure" }));
  } catch (error) {
    writeLocalSurfaceJSON(response, 400, {
      error: "invalid_runtime_configure_request",
      message: errorMessage6(error)
    });
  }
}
function daemonRuntimeConfigureMatchesCurrent(state, input) {
  if (state.dataPlane !== input.dataPlane) return false;
  if (input.dataPlane === "local") return true;
  return normalizeDataServiceURL2(state.dataServiceURL) === normalizeDataServiceURL2(input.dataServiceURL);
}
async function readDaemonRuntimeConfigureInput(request) {
  const text = (await readRequestText(request)).trim();
  const value = text ? JSON.parse(text) : {};
  const record = recordFromUnknown(value) ?? {};
  const dataConnection = recordFromUnknown(record.dataConnection);
  const dataPlane = localDaemonDataPlaneValue(
    dataConnection?.kind ?? record.dataPlane ?? record.data_connection_kind ?? record.kind
  );
  if (!dataPlane) throw new Error("dataConnection.kind must be local, cloud, or external");
  const dataServiceURL = trimmedString(
    dataConnection?.url ?? record.dataServiceURL ?? record.data_service_url ?? record.url
  );
  return {
    dataPlane,
    ...dataPlane !== "local" && dataServiceURL ? { dataServiceURL } : {}
  };
}
function normalizeDataServiceURL2(value) {
  const raw = value?.trim();
  if (!raw) return void 0;
  try {
    const url = new URL(raw);
    url.hash = "";
    url.pathname = url.pathname.replace(/\/+$/, "") || "/";
    return url.toString().replace(/\/$/, "");
  } catch {
    return raw.replace(/\/+$/, "");
  }
}
function localDaemonDataPlaneValue(value) {
  return value === "local" || value === "cloud" || value === "external" ? value : void 0;
}
function daemonRuntimeDiagnosticsEnabled() {
  return process.env.MOVSCRIPT_RUNTIME_DIAGNOSTICS === "1" || process.env.MOVSCRIPT_LOCAL_NODE_DEBUG === "1";
}
function issueDaemonRuntimeDiagnostics(homeDir, descriptor) {
  const snapshot = readRuntimeHomeSnapshot(homeDir);
  return {
    schema: "movscript.runtime-diagnostics.v1",
    debugOnly: true,
    runtime: descriptor.runtime,
    gateway: descriptor.gateway,
    dataConnection: descriptor.dataConnection,
    services: snapshot.services.map((record) => ({
      serviceName: record.serviceName,
      ownerApplicationId: record.ownerApplicationId,
      profile: record.profile,
      status: record.status,
      ready: record.ready,
      pidAlive: record.pid ? pidIsAlive(record.pid) : void 0,
      endpoint: record.endpoint ? redactedEndpoint(record.endpoint) : void 0,
      updatedAt: record.updatedAt
    })),
    endpoints: snapshot.endpoints.map(redactedEndpoint)
  };
}
function redactedEndpoint(endpoint) {
  return {
    serviceName: endpoint.serviceName,
    applicationId: endpoint.applicationId,
    protocol: endpoint.protocol,
    port: endpoint.port,
    status: endpoint.status,
    ready: endpoint.ready,
    updatedAt: endpoint.updatedAt
  };
}
function issueDaemonRuntimeDescriptor(homeDir, request) {
  return {
    schema: "movscript.runtime-descriptor.v1",
    runtime: {
      owner: "movscript.local-node",
      appId: "movscript.local-node",
      name: "MovScript Local Node Daemon"
    },
    gateway: {
      baseURL: daemonGatewayBaseURL(homeDir, request),
      canonicalPrefix: "/v1"
    },
    dataConnection: daemonDataConnectionContext(homeDir),
    capabilities: {
      project: true,
      canvas: true,
      resources: true,
      editing: true,
      media: true
    }
  };
}
function daemonGatewayBaseURL(homeDir, request) {
  return trimURLTrailingSlash(
    resolveGatewayURL(homeDir) ?? requestOrigin(request) ?? `http://127.0.0.1:${DEFAULT_LOCAL_NODE_GATEWAY_PORT}`
  );
}
function requestOrigin(request) {
  const host = request?.headers.host;
  if (typeof host !== "string" || !host.trim()) return void 0;
  const protoHeader = request?.headers["x-forwarded-proto"];
  const protocol = typeof protoHeader === "string" && protoHeader.trim() ? protoHeader.trim().split(",")[0] : "http";
  return `${protocol}://${host.trim()}`;
}
function trimURLTrailingSlash(value) {
  return value.trim().replace(/\/+$/, "");
}
function daemonDataConnectionContext(homeDir) {
  const dataPlane = resolveLocalDaemonDataPlane(process.env);
  return {
    kind: dataPlane,
    authMode: dataPlane === "local" ? "local-owner" : dataPlane === "cloud" ? "session" : "external",
    status: dataPlane === "external" && !resolveDataServiceURL(homeDir) ? "degraded" : "connected",
    displayName: dataPlane === "local" ? "Local Data Plane" : dataPlane === "cloud" ? "Cloud Data Plane" : "External Data Plane"
  };
}
async function handleDaemonContextRequest(homeDir, request, response, url) {
  try {
    if (request.method === "GET" && url.pathname === DAEMON_CONTEXT_ENDPOINT) {
      const sessionId = trimmedString(url.searchParams.get("sessionId"));
      const existing = sessionId ? daemonContextSessions.get(sessionId) : void 0;
      writeLocalSurfaceJSON(response, 200, existing ?? issueDaemonContextEnvelope(homeDir, contextSessionInputFromSearch(url.searchParams)));
      return;
    }
    if (request.method === "POST" && url.pathname === DAEMON_CONTEXT_SESSIONS_ENDPOINT) {
      const input = {
        ...contextSessionInputFromSearch(url.searchParams),
        ...await readDaemonContextSessionInput(request)
      };
      const envelope = issueDaemonContextEnvelope(homeDir, input);
      if (envelope.session) daemonContextSessions.set(envelope.session.sessionId, envelope);
      writeLocalSurfaceJSON(response, 201, envelope);
      return;
    }
    const sessionMatch = new RegExp(`^${escapeRegExp3(DAEMON_CONTEXT_SESSIONS_ENDPOINT)}/([^/]+)$`).exec(url.pathname);
    if (sessionMatch) {
      const sessionId = decodeURIComponent(sessionMatch[1] ?? "");
      const existing = daemonContextSessions.get(sessionId);
      if (request.method === "GET") {
        if (!existing) {
          writeLocalSurfaceJSON(response, 404, {
            error: "context_session_not_found",
            message: `Daemon context session was not found: ${sessionId}`
          });
          return;
        }
        writeLocalSurfaceJSON(response, 200, existing);
        return;
      }
      if (request.method === "PATCH") {
        const input = {
          ...contextSessionInputFromSearch(url.searchParams),
          ...await readDaemonContextSessionInput(request),
          sessionId
        };
        const envelope = issueDaemonContextEnvelope(homeDir, input, existing);
        if (envelope.session) daemonContextSessions.set(envelope.session.sessionId, envelope);
        writeLocalSurfaceJSON(response, existing ? 200 : 201, envelope);
        return;
      }
    }
    writeLocalSurfaceJSON(response, 404, {
      error: "context_route_not_found",
      message: `Daemon context route was not found: ${url.pathname}`
    });
  } catch (error) {
    writeLocalSurfaceJSON(response, 500, {
      error: "context_request_failed",
      message: errorMessage6(error)
    });
  }
}
async function readDaemonContextSessionInput(request) {
  const text = (await readRequestText(request)).trim();
  if (!text) return {};
  const value = JSON.parse(text);
  const record = recordFromUnknown(value);
  if (!record) return {};
  const principal = recordFromUnknown(record.principal);
  const capabilities = recordFromUnknown(record.capabilities);
  return {
    sessionId: trimmedString(record.sessionId),
    windowId: trimmedString(record.windowId),
    projectId: contextStringOrNumber(record.projectId ?? record.project_id),
    projectUid: trimmedString(record.projectUid ?? record.project_uid),
    projectSlug: trimmedString(record.projectSlug ?? record.project_slug),
    projectTitle: trimmedString(record.projectTitle ?? record.title ?? record.project_name),
    projectDir: trimmedString(record.projectDir ?? record.projectPath ?? record.project_cwd),
    workspaceRootUri: trimmedString(record.workspaceRootUri ?? record.rootUri),
    workspaceKind: contextWorkspaceKind(record.workspaceKind),
    ...capabilities ? {
      capabilities: {
        ...typeof capabilities.localFileAccess === "boolean" ? { localFileAccess: capabilities.localFileAccess } : {},
        ...typeof capabilities.fileImport === "boolean" ? { fileImport: capabilities.fileImport } : {},
        ...typeof capabilities.mediaPreview === "boolean" ? { mediaPreview: capabilities.mediaPreview } : {}
      }
    } : {},
    ...principal ? {
      principal: {
        userId: trimmedString(principal.userId ?? principal.user_id),
        kind: contextPrincipalKind(principal.kind),
        accountId: trimmedString(principal.accountId ?? principal.account_id),
        displayName: trimmedString(principal.displayName ?? principal.display_name),
        scopeKind: contextScopeKind(principal.scopeKind ?? principal.scope_kind),
        scopeId: contextStringOrNumber(principal.scopeId ?? principal.scope_id)
      }
    } : {}
  };
}
function contextSessionInputFromSearch(params) {
  return {
    sessionId: trimmedString(params.get("sessionId") ?? params.get("session_id")),
    windowId: trimmedString(params.get("windowId") ?? params.get("window_id")),
    projectId: contextStringOrNumber(params.get("projectId") ?? params.get("project_id")),
    projectUid: trimmedString(params.get("projectUid") ?? params.get("project_uid")),
    projectSlug: trimmedString(params.get("projectSlug") ?? params.get("project_slug")),
    projectTitle: trimmedString(params.get("projectTitle") ?? params.get("projectName") ?? params.get("project_name")),
    projectDir: trimmedString(params.get("projectDir") ?? params.get("projectPath") ?? params.get("project_cwd")),
    workspaceRootUri: trimmedString(params.get("workspaceRootUri") ?? params.get("rootUri")),
    workspaceKind: contextWorkspaceKind(params.get("workspaceKind"))
  };
}
function issueDaemonContextEnvelope(homeDir, input = {}, previous) {
  const dataPlane = resolveLocalDaemonDataPlane(process.env);
  const dataConnection = daemonDataConnectionContext(homeDir);
  const issuedAt = (/* @__PURE__ */ new Date()).toISOString();
  const revision = (previous?.revision ?? 0) + 1;
  const session = issueDaemonWorkspaceSession(input, previous?.session, dataPlane);
  return {
    schema: "movscript.context-envelope.v1",
    contextId: `${session?.sessionId ?? "system"}:${revision}`,
    revision,
    issuedAt,
    runtime: {
      owner: "local-node",
      appId: LOCAL_NODE_APP_ID,
      gatewayPrefix: "/v1"
    },
    principal: daemonPrincipalContext(dataPlane, input.principal, previous?.principal),
    dataConnection,
    ...session ? { session } : {}
  };
}
function issueDaemonWorkspaceSession(input, previous, dataPlane) {
  const projectDir = trimmedString(input.projectDir) ?? previous?.workspace?.projectCwd;
  const explicitLocalFileAccess = input.capabilities?.localFileAccess;
  const localFileAccess = explicitLocalFileAccess ?? previous?.capabilities.localFileAccess ?? Boolean(projectDir);
  const projectCwd = localFileAccess && projectDir ? resolve11(projectDir) : void 0;
  const projectId = contextString(input.projectId) ?? previous?.project?.id ?? (projectCwd ? `local:${stableContextId(projectCwd)}` : void 0);
  const hasSessionInput = Boolean(
    input.sessionId || input.windowId || projectId || projectCwd || input.projectUid || input.projectTitle || previous
  );
  if (!hasSessionInput) return void 0;
  const sessionId = trimmedString(input.sessionId) ?? previous?.sessionId ?? `wks_${Date.now().toString(36)}_${randomBytes(4).toString("hex")}`;
  const workspaceKind = projectCwd ? "local-fs" : input.workspaceKind ?? previous?.workspace?.kind ?? (dataPlane === "cloud" ? "cloud" : dataPlane === "external" ? "external" : void 0);
  return {
    sessionId,
    ...trimmedString(input.windowId) ?? previous?.windowId ? { windowId: trimmedString(input.windowId) ?? previous?.windowId } : {},
    ...projectId ? {
      project: {
        id: projectId,
        ...trimmedString(input.projectUid) ?? previous?.project?.uid ? { uid: trimmedString(input.projectUid) ?? previous?.project?.uid } : {},
        ...trimmedString(input.projectSlug) ?? previous?.project?.slug ? { slug: trimmedString(input.projectSlug) ?? previous?.project?.slug } : {},
        ...trimmedString(input.projectTitle) ?? previous?.project?.title ? { title: trimmedString(input.projectTitle) ?? previous?.project?.title } : {}
      }
    } : {},
    ...workspaceKind ? {
      workspace: {
        kind: workspaceKind,
        ...projectCwd ? { projectCwd } : {},
        ...trimmedString(input.workspaceRootUri) ?? previous?.workspace?.rootUri ? { rootUri: trimmedString(input.workspaceRootUri) ?? previous?.workspace?.rootUri } : {}
      }
    } : {},
    capabilities: {
      localFileAccess,
      fileImport: input.capabilities?.fileImport ?? previous?.capabilities.fileImport ?? localFileAccess,
      mediaPreview: input.capabilities?.mediaPreview ?? previous?.capabilities.mediaPreview ?? localFileAccess
    }
  };
}
function daemonPrincipalContext(dataPlane, input, previous) {
  if (dataPlane === "local") {
    return {
      userId: "1",
      kind: "local-owner",
      accountId: "local-owner",
      displayName: "Local Workspace",
      scopeKind: "user",
      scopeId: 1
    };
  }
  const userId = trimmedString(input?.userId) ?? previous?.userId ?? (dataPlane === "cloud" ? "cloud-user" : "external-user");
  return {
    userId,
    kind: input?.kind ?? previous?.kind ?? (dataPlane === "cloud" ? "cloud-user" : "external-user"),
    ...trimmedString(input?.accountId) ?? previous?.accountId ? { accountId: trimmedString(input?.accountId) ?? previous?.accountId } : {},
    ...trimmedString(input?.displayName) ?? previous?.displayName ? { displayName: trimmedString(input?.displayName) ?? previous?.displayName } : {},
    ...input?.scopeKind ?? previous?.scopeKind ? { scopeKind: input?.scopeKind ?? previous?.scopeKind } : {},
    ...input?.scopeId ?? previous?.scopeId ? { scopeId: input?.scopeId ?? previous?.scopeId } : {}
  };
}
function recordFromUnknown(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value) ? value : void 0;
}
function contextStringOrNumber(value) {
  if (typeof value === "number" && Number.isFinite(value)) return value;
  return trimmedString(value);
}
function contextString(value) {
  if (typeof value === "number" && Number.isFinite(value)) return String(value);
  return trimmedString(value);
}
function trimmedString(value) {
  return typeof value === "string" && value.trim() ? value.trim() : void 0;
}
function contextWorkspaceKind(value) {
  return value === "local-fs" || value === "cloud" || value === "external" ? value : void 0;
}
function contextPrincipalKind(value) {
  return value === "local-owner" || value === "cloud-user" || value === "service-account" || value === "external-user" ? value : void 0;
}
function contextScopeKind(value) {
  return value === "user" || value === "org" || value === "local" ? value : void 0;
}
function stableContextId(value) {
  let hash = 2166136261;
  for (let index = 0; index < value.length; index += 1) {
    hash ^= value.charCodeAt(index);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(36);
}
function escapeRegExp3(value) {
  return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
async function importLocalSurfaceEditingFile(homeDir, request, response, url) {
  try {
    const fileName = safeImportFileName(url.searchParams.get("filename") ?? "media");
    const body = await readRequestBuffer(request);
    if (body.byteLength === 0) {
      writeLocalSurfaceJSON(response, 400, {
        error: "empty_import_file",
        message: "Imported file is empty."
      });
      return;
    }
    const importDir = join10(homeDir, "local-surface-host", "imports", "editing");
    mkdirSync5(importDir, { recursive: true });
    const localPath = join10(importDir, `${Date.now()}-${randomBytes(4).toString("hex")}-${fileName}`);
    writeFileSync5(localPath, body);
    writeLocalSurfaceJSON(response, 200, {
      schema: "movscript.local-surface.editing-import-file.v1",
      localPath,
      local_path: localPath,
      fileName,
      file_name: fileName,
      size: body.byteLength
    });
  } catch (error) {
    writeLocalSurfaceJSON(response, 500, {
      error: "editing_import_file_failed",
      message: errorMessage6(error)
    });
  }
}
async function serveLocalSurfaceEditingMediaFile(homeDir, request, response, url) {
  try {
    const filePath = safeImportedEditingMediaPath(homeDir, url.searchParams.get("path") ?? "");
    if (!filePath || !existsSync5(filePath)) {
      writeLocalSurfaceJSON(response, 404, {
        error: "editing_media_file_not_found",
        message: "Imported editing media file was not found."
      });
      return;
    }
    const stat5 = statSync(filePath);
    if (!stat5.isFile()) {
      writeLocalSurfaceJSON(response, 404, {
        error: "editing_media_file_not_found",
        message: "Imported editing media path is not a file."
      });
      return;
    }
    const range = parseRangeHeader(request.headers.range, stat5.size);
    const headers = {
      "accept-ranges": "bytes",
      "content-type": contentType(filePath)
    };
    if (range) {
      response.writeHead(206, {
        ...headers,
        "content-length": String(range.end - range.start + 1),
        "content-range": `bytes ${range.start}-${range.end}/${stat5.size}`
      });
      if (request.method === "HEAD") {
        response.end();
        return;
      }
      createReadStream(filePath, { start: range.start, end: range.end }).pipe(response);
      return;
    }
    response.writeHead(200, {
      ...headers,
      "content-length": String(stat5.size)
    });
    if (request.method === "HEAD") {
      response.end();
      return;
    }
    createReadStream(filePath).pipe(response);
  } catch (error) {
    writeLocalSurfaceJSON(response, 500, {
      error: "editing_media_file_read_failed",
      message: errorMessage6(error)
    });
  }
}
async function proxyCanvasServiceRequest(homeDir, request, response, url, gatewayPrefix) {
  const baseURL = await waitForRuntimeServiceURL(homeDir, CANVAS_SERVICE_NAME2);
  if (!baseURL) {
    writeLocalSurfaceJSON(response, 503, {
      error: "canvas_service_unavailable",
      message: "Canvas Service endpoint was not found in MovScript runtime records."
    });
    return;
  }
  const upstreamPath = `/v1/canvas${url.pathname.slice(gatewayPrefix.length)}${url.search}`;
  try {
    const body = request.method === "GET" || request.method === "HEAD" ? void 0 : bufferToFetchBody(await readRequestBuffer(request));
    const upstream = await fetch(`${baseURL}${upstreamPath}`, {
      method: request.method,
      headers: proxyHeaders(request),
      body
    });
    await writeProxyUpstreamResponse(response, upstream, request);
  } catch (error) {
    writeLocalSurfaceJSON(response, 502, {
      error: "canvas_service_proxy_failed",
      message: errorMessage6(error)
    });
  }
}
async function proxyRuntimeServiceRequest(homeDir, request, response, url, options) {
  const baseURL = await waitForRuntimeServiceURL(homeDir, options.serviceName);
  if (!baseURL) {
    writeLocalSurfaceJSON(response, 503, {
      error: options.unavailableCode,
      message: `${options.serviceLabel} endpoint was not found in MovScript runtime records.`
    });
    return;
  }
  const upstreamPath = `${options.upstreamPrefix}${url.pathname.slice(options.gatewayPrefix.length)}${url.search}`;
  try {
    const body = request.method === "GET" || request.method === "HEAD" ? void 0 : bufferToFetchBody(await readRequestBuffer(request));
    const upstream = await fetch(`${baseURL}${upstreamPath}`, {
      method: request.method,
      headers: proxyHeaders(request),
      body
    });
    await writeProxyUpstreamResponse(response, upstream, request);
  } catch (error) {
    writeLocalSurfaceJSON(response, 502, {
      error: options.failureCode,
      message: errorMessage6(error)
    });
  }
}
async function proxyDataServiceRequest2(homeDir, request, response, url, localPrefix) {
  const baseURL = await waitForDataServiceURL(homeDir);
  if (!baseURL) {
    writeLocalSurfaceJSON(response, 503, {
      error: "data_service_unavailable",
      message: "Data Service endpoint was not found in MovScript runtime records or MOVSCRIPT_DATA_SERVICE_URL."
    });
    return;
  }
  const upstreamPath = `${localPrefix ? url.pathname.slice(localPrefix.length) || "/" : url.pathname}${url.search}`;
  try {
    const body = request.method === "GET" || request.method === "HEAD" ? void 0 : bufferToFetchBody(await readRequestBuffer(request));
    const upstream = await fetch(`${baseURL}${upstreamPath}`, {
      method: request.method,
      headers: proxyHeaders(request),
      body
    });
    await writeProxyUpstreamResponse(response, upstream, request);
  } catch (error) {
    writeLocalSurfaceJSON(response, 502, {
      error: "data_service_proxy_failed",
      message: errorMessage6(error)
    });
  }
}
async function proxyProjectServiceRequest(homeDir, request, response, url) {
  const upstreamPath = LOCAL_PROJECT_SERVICE_PROXY_ROUTES.get(url.pathname);
  if (!upstreamPath) {
    writeLocalSurfaceJSON(response, 404, {
      error: "project_service_route_not_found",
      message: `Local Project Service proxy route was not found: ${url.pathname}`
    });
    return;
  }
  const baseURL = await waitForRuntimeServiceURL(homeDir, PROJECT_SERVICE_NAME2);
  if (!baseURL) {
    writeLocalSurfaceJSON(response, 503, {
      error: "project_service_unavailable",
      message: "Project Service endpoint was not found in MovScript runtime records."
    });
    return;
  }
  try {
    const body = request.method === "GET" || request.method === "HEAD" ? void 0 : bufferToFetchBody(await readRequestBuffer(request));
    const upstream = await fetch(`${baseURL}${upstreamPath}${url.search}`, {
      method: request.method,
      headers: proxyHeaders(request),
      body
    });
    await writeProxyUpstreamResponse(response, upstream, request);
  } catch (error) {
    writeLocalSurfaceJSON(response, 502, {
      error: "project_service_proxy_failed",
      message: errorMessage6(error)
    });
  }
}
async function writeProxyUpstreamResponse(response, upstream, request) {
  const headers = {
    ...proxyResponseHeaders(upstream.headers),
    ...corsHeadersForRequest(request)
  };
  if (!headers["content-type"]) headers["content-type"] = "application/json; charset=utf-8";
  response.writeHead(upstream.status, headers);
  if (upstream.body === null) {
    response.end();
    return;
  }
  response.end(Buffer.from(await upstream.arrayBuffer()));
}
function corsHeadersForRequest(request) {
  const origin = typeof request.headers.origin === "string" ? request.headers.origin : "";
  if (!isAllowedLocalGatewayOrigin(origin)) return {};
  const requestedHeaders = typeof request.headers["access-control-request-headers"] === "string" ? request.headers["access-control-request-headers"] : "content-type, authorization";
  return {
    "access-control-allow-origin": origin,
    "access-control-allow-methods": "GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS",
    "access-control-allow-headers": requestedHeaders,
    "access-control-allow-credentials": "true",
    "access-control-max-age": "600",
    vary: "Origin"
  };
}
function applyCORSHeaders(response, request) {
  for (const [key, value] of Object.entries(corsHeadersForRequest(request))) {
    response.setHeader(key, value);
  }
}
function isAllowedLocalGatewayOrigin(origin) {
  if (!origin) return false;
  if (origin === "movscript-admin://app") return true;
  try {
    const url = new URL(origin);
    return (url.protocol === "http:" || url.protocol === "https:") && (url.hostname === "localhost" || url.hostname === "127.0.0.1");
  } catch {
    return false;
  }
}
function proxyResponseHeaders(upstreamHeaders) {
  const headers = {};
  const allowedHeaders = [
    "accept-ranges",
    "cache-control",
    "content-disposition",
    "content-encoding",
    "content-language",
    "content-length",
    "content-range",
    "content-type",
    "etag",
    "expires",
    "last-modified"
  ];
  for (const key of allowedHeaders) {
    const value = upstreamHeaders.get(key);
    if (value) headers[key] = value;
  }
  return headers;
}
function proxyHeaders(request) {
  const headers = {};
  for (const [key, value] of Object.entries(request.headers)) {
    if (value === void 0) continue;
    const lower = key.toLowerCase();
    if (["host", "connection", "content-length"].includes(lower)) continue;
    headers[key] = Array.isArray(value) ? value.join(", ") : value;
  }
  return headers;
}
function writeLocalSurfaceJSON(response, statusCode, payload) {
  response.writeHead(statusCode, { "content-type": "application/json; charset=utf-8" });
  response.end(JSON.stringify(payload));
}
async function readRequestText(request) {
  return (await readRequestBuffer(request)).toString("utf8");
}
async function readRequestBuffer(request) {
  const chunks = [];
  for await (const chunk of request) {
    chunks.push(Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk));
  }
  return Buffer.concat(chunks);
}
function bufferToFetchBody(buffer) {
  const body = new Uint8Array(buffer.byteLength);
  body.set(buffer);
  return body;
}
function safeImportFileName(value) {
  const rawName = basename5(value.replaceAll("\\", "/")).trim();
  const safeName = rawName.replace(/[\u0000-\u001f\u007f<>:"/\\|?*]+/g, "_").replace(/^\.+$/, "media").slice(0, 160);
  return safeName || "media";
}
function safeImportedEditingMediaPath(homeDir, value) {
  const importRoot = resolve11(homeDir, "local-surface-host", "imports", "editing");
  if (!value) return void 0;
  const filePath = resolve11(value);
  return filePath === importRoot || filePath.startsWith(`${importRoot}/`) ? filePath : void 0;
}
function parseRangeHeader(rangeHeader, size) {
  if (!rangeHeader || size <= 0) return void 0;
  const match = /^bytes=(\d*)-(\d*)$/.exec(rangeHeader.trim());
  if (!match) return void 0;
  const startRaw = match[1];
  const endRaw = match[2];
  if (!startRaw && !endRaw) return void 0;
  if (!startRaw) {
    const suffixLength = Number(endRaw);
    if (!Number.isFinite(suffixLength) || suffixLength <= 0) return void 0;
    const start2 = Math.max(0, size - suffixLength);
    return { start: start2, end: size - 1 };
  }
  const start = Number(startRaw);
  const end = endRaw ? Number(endRaw) : size - 1;
  if (!Number.isFinite(start) || !Number.isFinite(end) || start < 0 || end < start || start >= size) return void 0;
  return { start, end: Math.min(end, size - 1) };
}
function safeStaticPath(staticRoot, pathname) {
  const normalized = pathname === "/" ? "/index.html" : pathname;
  const filePath = resolve11(staticRoot, `.${decodeURIComponent(normalized)}`);
  if (!filePath.startsWith(resolve11(staticRoot))) return join10(staticRoot, "index.html");
  return filePath;
}
function resolveStaticFilePath(staticRoot, pathname) {
  const filePath = safeStaticPath(staticRoot, pathname);
  try {
    if (statSync(filePath).isFile()) return filePath;
  } catch {
  }
  return join10(staticRoot, "index.html");
}
function contentType(path) {
  switch (extname3(path)) {
    case ".html":
      return "text/html; charset=utf-8";
    case ".js":
      return "text/javascript; charset=utf-8";
    case ".css":
      return "text/css; charset=utf-8";
    case ".json":
      return "application/json; charset=utf-8";
    case ".png":
      return "image/png";
    case ".svg":
      return "image/svg+xml";
    default:
      return "application/octet-stream";
  }
}
function endpointURL8(endpoint) {
  if (!endpoint) return void 0;
  if (endpoint.url) return endpoint.url;
  if (endpoint.baseURL) return endpoint.baseURL;
  if (endpoint.port && endpoint.protocol === "http") return `http://127.0.0.1:${endpoint.port}`;
  if (endpoint.port) return `http://127.0.0.1:${endpoint.port}`;
  return void 0;
}
function resolveDataServiceURL(homeDir) {
  return process.env.MOVSCRIPT_DATA_SERVICE_URL?.trim() || endpointURL8(findRuntimeEndpoint(readRuntimeHomeSnapshot(homeDir), DATA_SERVICE_NAME2));
}
function resolveGatewayURL(homeDir) {
  return endpointURL8(findRuntimeEndpoint(readRuntimeHomeSnapshot(homeDir), LOCAL_NODE_GATEWAY_SERVICE4));
}
async function waitForDataServiceURL(homeDir) {
  return waitForRuntimeEndpointURL(() => resolveDataServiceURL(homeDir));
}
async function waitForRuntimeServiceURL(homeDir, serviceName) {
  return waitForRuntimeEndpointURL(() => endpointURL8(findRuntimeEndpoint(readRuntimeHomeSnapshot(homeDir), serviceName)));
}
async function waitForRuntimeEndpointURL(resolveURL) {
  const deadline = Date.now() + RUNTIME_ENDPOINT_WAIT_TIMEOUT_MS;
  let url = resolveURL();
  while (!url && Date.now() < deadline) {
    await delay2(RUNTIME_ENDPOINT_WAIT_INTERVAL_MS);
    url = resolveURL();
  }
  return url;
}
function delay2(ms) {
  return new Promise((resolveDelay) => setTimeout(resolveDelay, ms));
}
var localNodeApplicationManifest = {
  schema: MOVSCRIPT_APPLICATION_MANIFEST_SCHEMA,
  applicationId: LOCAL_NODE_APP_ID,
  name: "MovScript Local Node",
  owner: "agent-provider",
  programs: [
    LOCAL_NODE_CONTROL_SERVICE2,
    "movscript.data.service",
    "movscript.project.service",
    "movscript.editing.service",
    "movscript.canvas.service",
    "movscript.local-surface.host",
    "movscript.media.pipeline"
  ]
};
var localNodeStartupPolicy = {
  schema: MOVSCRIPT_SCENARIO_POLICY_SCHEMA,
  scenarioId: "plugin-full-local",
  applicationId: LOCAL_NODE_APP_ID,
  programs: [
    { serviceName: LOCAL_NODE_CONTROL_SERVICE2, required: true, profile: "local" },
    { serviceName: "movscript.data.service", required: true, profile: "local" },
    { serviceName: "movscript.project.service", required: true, profile: "local" },
    { serviceName: "movscript.editing.service", required: true, profile: "local" },
    { serviceName: "movscript.canvas.service", required: true, profile: "local" },
    { serviceName: "movscript.local-surface.host", required: true, profile: "local" },
    { serviceName: "movscript.media.pipeline", required: true, profile: "local" }
  ]
};
var localNodeCloudDataStartupPolicy = {
  schema: MOVSCRIPT_SCENARIO_POLICY_SCHEMA,
  scenarioId: "local-daemon-cloud-data",
  applicationId: LOCAL_NODE_APP_ID,
  programs: [
    { serviceName: LOCAL_NODE_CONTROL_SERVICE2, required: true, profile: "local" },
    { serviceName: "movscript.project.service", required: true, profile: "local" },
    { serviceName: "movscript.editing.service", required: true, profile: "local" },
    { serviceName: "movscript.canvas.service", required: true, profile: "local" },
    { serviceName: "movscript.local-surface.host", required: true, profile: "local" },
    { serviceName: "movscript.media.pipeline", required: true, profile: "local" }
  ]
};
var dataServiceProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "data-service",
  serviceName: "movscript.data.service",
  kind: "service",
  name: "MovScript Data Service",
  profiles: ["local", "cloud", "test"],
  transport: "http",
  health: { kind: "http", target: "/health" }
};
var localNodeControlProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "local-node-control",
  serviceName: LOCAL_NODE_CONTROL_SERVICE2,
  kind: "service",
  name: "MovScript Local Node Control",
  profiles: ["local", "plugin-full-local", "test"],
  transport: "http",
  health: { kind: "http", target: "/health" }
};
var projectServiceProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "project-service",
  serviceName: "movscript.project.service",
  kind: "service",
  name: "MovScript Project Service",
  profiles: ["local", "cloud", "test"],
  transport: "http",
  health: { kind: "http", target: "/health" },
  dependsOn: ["movscript.data.service"]
};
var editingServiceProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "editing-service",
  serviceName: "movscript.editing.service",
  kind: "service",
  name: "MovScript Editing Service",
  profiles: ["local", "cloud", "test"],
  transport: "http",
  health: { kind: "http", target: "/health" },
  dependsOn: ["movscript.project.service", "movscript.data.service"]
};
var canvasServiceProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "canvas-service",
  serviceName: "movscript.canvas.service",
  kind: "service",
  name: "MovScript Canvas Service",
  profiles: ["local", "cloud", "test"],
  transport: "http",
  health: { kind: "http", target: "/health" },
  dependsOn: ["movscript.data.service"]
};
var localSurfaceHostProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "local-surface-host",
  serviceName: "movscript.local-surface.host",
  kind: "web",
  name: "MovScript Local Surface Host",
  profiles: ["local", "plugin-full-local", "desktop-connected", "desktop-embedded", "test"],
  transport: "http",
  health: { kind: "http", target: "/health" },
  provides: ["surface-host-local", "project-surface-url", "canvas-surface-local-url", "admin-surface-local-url"]
};
var mediaPipelineProgramManifest = {
  schema: MOVSCRIPT_PROGRAM_MANIFEST_SCHEMA,
  programId: "media-pipeline",
  serviceName: "movscript.media.pipeline",
  kind: "service",
  name: "MovScript Media Pipeline",
  profiles: ["local", "cloud", "desktop", "plugin-full-local", "test"],
  transport: "http",
  health: { kind: "http", target: "/health" },
  dependsOn: ["movscript.data.service"]
};
main().catch((error) => {
  process.stderr.write(`MovScript Agent MCP host failed: ${errorMessage6(error)}
`);
  process.exit(1);
});
