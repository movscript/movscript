# @movscript/shot-library

Shared shot library contracts and pure domain helpers for runtime source normalization, reference analysis, faceted search, retrieval text, and vector document generation.

This package is application-neutral. Desktop and Shot Library Surface consume it directly instead of importing shot-library behavior from `@movscript/core`.
