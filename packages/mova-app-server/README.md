# @movscript/mova-app-server

Meta package for the MovScript app-server runtime.

Install this package instead of a platform-specific runtime package. The matching runtime package is selected through npm optional dependencies for the current OS and CPU.
