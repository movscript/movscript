# @movscript/mcp-contracts

Browser-safe MCP context and protocol contracts shared by Desktop, MCP Host adapters, and future runtime clients.

This package must not import Node transports, MCP server handlers, React, Electron, or Desktop runtime code.
