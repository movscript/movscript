# @movscript/agent-chat

Provider-neutral Agent chat runtime, thread item, notification, server-request, and view-model helpers shared by Desktop and future Agent runtime clients.

This package depends on `@movscript/agent-protocol` for protocol contracts. It must not import Desktop UI, Electron, React, MCP host transports, Project mode, or Canvas code.
