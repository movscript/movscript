{
  "name": "movscript_cancel_subagent",
  "description": "取消当前 planner run 拥有的子 worker subagent 树。",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "subagentName": { "type": "string", "description": "要取消的精确 subagent 名字，必须来自 spawn/list/get_plan 返回值；不要猜 worker 这种泛称。" },
      "runId": { "type": "string" },
      "taskId": { "type": "string" },
      "reason": { "type": "string" }
    }
  },
  "permission": "agent.subagent.write",
  "risk": "write",
  "projectScoped": false,
  "defaults": { "grant": "allow", "approval": "never" },
  "source": "runtime",
  "capability": "取消 planner 拥有的 worker subagents。",
  "allowedRunRoles": ["planner"]
}
