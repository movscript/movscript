{
  "name": "runtime_operation_get",
  "description": "读取单个 runtime operation 的当前状态。不要把它理解为通用读取工具。",
  "permission": "agent.operation.read",
  "risk": "read",
  "source": "runtime",
  "category": "runtime-operation",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["operationId"],
    "properties": {
      "operationId": {
        "type": "string"
      }
    }
  }
}
