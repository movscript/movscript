{
  "name": "runtime_operation_cancel",
  "description": "取消一个可取消的 runtime operation。取消 subagent 请优先使用 movscript_cancel_subagent。",
  "permission": "agent.operation.write",
  "risk": "write",
  "source": "runtime",
  "category": "runtime-operation",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["operationId"],
    "properties": {
      "operationId": {
        "type": "string"
      }
    }
  }
}
