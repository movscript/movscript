{
  "name": "movscript_get_plan",
  "description": "读取当前会话唯一 plan 的 snapshot。",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "planId": { "type": "string" }
    }
  },
  "permission": "agent.plan.read",
  "risk": "read",
  "projectScoped": false,
  "defaults": { "grant": "allow", "approval": "never" },
  "source": "runtime",
  "capability": "读取当前会话唯一 plan 的 plan、tasks、runs、summary 等结构化状态。",
  "allowedRunRoles": ["planner"]
}
