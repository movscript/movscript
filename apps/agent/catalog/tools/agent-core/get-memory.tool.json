{
  "name": "movscript_get_memory",
  "description": "按 id 读取当前项目中的一条本地 agent memory。",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id"
    ],
    "properties": {
      "id": {
        "type": "string"
      },
      "memoryId": {
        "type": "string"
      }
    }
  },
  "permission": "memory.read",
  "risk": "read",
  "projectScoped": true,
  "defaults": {
    "grant": "allow",
    "approval": "never"
  },
  "source": "runtime",
  "capability": "按 id 读取当前项目内的一条本地 agent memory。"
}
