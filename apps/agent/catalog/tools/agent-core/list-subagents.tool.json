{
  "name": "movscript_list_subagents",
  "description": "列出当前 plan 的 worker subagents 和任务状态。",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "planId": { "type": "string" }
    }
  },
  "permission": "agent.subagent.read",
  "risk": "read",
  "projectScoped": false,
  "defaults": { "grant": "allow", "approval": "never" },
  "source": "runtime",
  "capability": "列出 planner 拥有的 worker subagents 及其任务状态。",
  "allowedRunRoles": ["planner"]
}
