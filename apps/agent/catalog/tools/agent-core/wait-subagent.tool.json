{
  "name": "movscript_wait_subagent",
  "description": "检查 worker subagent、task 或 plan 是否完成，并返回最新结构化快照。",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "subagentName": { "type": "string", "description": "要检查的精确 subagent 名字，必须来自 spawn/list/get_plan 返回值；不要猜 worker 这种泛称。" },
      "runId": { "type": "string" },
      "taskId": { "type": "string" },
      "planId": { "type": "string" },
      "timeoutMs": { "type": "number" }
    }
  },
  "permission": "agent.subagent.read",
  "risk": "read",
  "projectScoped": false,
  "defaults": { "grant": "allow", "approval": "never" },
  "source": "runtime",
  "capability": "等待或检查 planner 拥有的 worker subagent 进度。",
  "allowedRunRoles": ["planner"]
}
