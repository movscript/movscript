{
  "name": "core_subagent_cancel",
  "description": "取消当前 planner run 拥有的 child agent run 树。",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "subagentName": { "type": "string", "description": "要取消的精确 child agent 名字，必须来自 spawn/list 返回值；不要猜 worker 这种泛称。" },
      "runId": { "type": "string" },
      "taskId": { "type": "string" },
      "reason": { "type": "string" }
    }
  },
  "permission": "agent.subagent.write",
  "risk": "write",
  "projectScoped": false,
  "defaults": { "grant": "allow", "approval": "never" },
  "source": "runtime",
  "capability": "取消 planner 拥有的 child agents。",
  "allowedRunRoles": ["planner"]
}
