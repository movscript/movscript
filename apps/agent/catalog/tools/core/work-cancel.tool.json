{
  "name": "core_work_cancel",
  "description": "取消一个可取消的 runtime work。",
  "permission": "agent.work.write",
  "risk": "write",
  "source": "runtime",
  "category": "runtime-work",
  "allowedRunRoles": ["planner"],
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["workId"],
    "properties": {
      "workId": {
        "type": "string"
      }
    }
  }
}
