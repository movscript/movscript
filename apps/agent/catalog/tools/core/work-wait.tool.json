{
  "name": "core_work_wait",
  "description": "等待一个或多个 runtime works 进入终态，或直到等待超时。支持 all/any 模式。普通同步工具调用不需要 wait。",
  "permission": "agent.work.read",
  "risk": "read",
  "source": "runtime",
  "category": "runtime-work",
  "allowedRunRoles": ["planner"],
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["workIds"],
    "properties": {
      "workIds": {
        "type": "array",
        "items": { "type": "string" },
        "minItems": 1
      },
      "mode": {
        "type": "string",
        "enum": ["all", "any"]
      },
      "timeoutMs": {
        "type": "number"
      },
      "pollIntervalMs": {
        "type": "number"
      }
    }
  }
}
