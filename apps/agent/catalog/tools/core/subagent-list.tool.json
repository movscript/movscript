{
  "name": "core_subagent_list",
  "description": "列出当前 planner run 的 child agent runs；如果指定 taskGraphId，也会返回 task graph 绑定状态。",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "taskGraphId": { "type": "string" }
    }
  },
  "permission": "agent.subagent.read",
  "risk": "read",
  "projectScoped": false,
  "defaults": { "grant": "allow", "approval": "never" },
  "source": "runtime",
  "capability": "列出 planner 拥有的 child agents 及其运行状态。",
  "allowedRunRoles": ["planner"]
}
