{
  "name": "core_work_get",
  "description": "读取单个 runtime work 的当前状态。不要把它理解为通用读取工具。",
  "permission": "agent.work.read",
  "risk": "read",
  "source": "runtime",
  "category": "runtime-work",
  "allowedRunRoles": ["planner"],
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["workId"],
    "properties": {
      "workId": {
        "type": "string"
      }
    }
  }
}
