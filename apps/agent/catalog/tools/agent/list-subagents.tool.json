{
  "name": "movscript_list_subagents",
  "description": "List worker subagents and task status for the current plan.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "planId": { "type": "string" }
    }
  },
  "permission": "agent.subagent.read",
  "risk": "read",
  "projectScoped": false,
  "defaults": { "grant": "allow", "approval": "never" },
  "source": "runtime",
  "capability": "List planner-owned worker subagents and their task state.",
  "allowedRunRoles": ["planner"]
}
