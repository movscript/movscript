# Desktop Build Resources

This directory is an Electron Builder `buildResources` directory, not a compiled-output directory.

Keep these files in git:

- `icon.icns`
- `icon.png`
- `entitlements.mac.plist`
- `entitlements.mac.inherit.plist`

Generated package artifacts belong in `apps/desktop/release/`, which is ignored.
