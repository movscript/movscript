# Desktop Runtime Source

This directory contains source code for the Desktop application runtime adapter.

It is not a generated runtime bundle and should stay in source control. Generated Desktop package runtime files belong under `apps/desktop/.package-stage/`, `apps/desktop/out/`, release artifacts, or the installed application resources instead.
