import assert from 'node:assert/strict'
import test from 'node:test'
import { existsSync, readFileSync } from 'node:fs'
import { resolve } from 'node:path'

test('agent session UI keeps explicit provider session thread recovery contracts', () => {
  const panelSource = readFileSync(resolve('src/features/agent/components/AIAgentPanel.tsx'), 'utf8')
  const appSource = readFileSync(resolve('src/App.tsx'), 'utf8')
  const frontendArchitectureSource = readFileSync(resolve('src/features/agent/ARCHITECTURE.md'), 'utf8')
  const appTopControlsSource = readFileSync(resolve('src/features/app-shell/components/AppTopControls.tsx'), 'utf8')
  const headerSource = readFileSync(resolve('src/features/app-shell/components/Header.tsx'), 'utf8')
  const agentPanelDockControllerSource = readFileSync(resolve('src/features/agent/presentation/useAIAgentPanelDockController.ts'), 'utf8')
  const agentControlCenterHookSource = readFileSync(resolve('src/features/agent/presentation/useAgentControlCenter.ts'), 'utf8')
  const agentControlCenterApplicationSource = readFileSync(resolve('src/features/agent/application/agentControlCenter.ts'), 'utf8')
  const agentPanelUiStoreSource = readFileSync(resolve('src/features/agent/presentation/agentPanelUiStore.ts'), 'utf8')
  const chatViewSource = readFileSync(resolve('src/features/agent/components/AgentChatView.tsx'), 'utf8')
  const chatPanelLayoutSource = readFileSync(resolve('src/features/agent/components/AgentChatPanelLayout.tsx'), 'utf8')
  const chatPageLayoutSource = readFileSync(resolve('src/features/agent/components/AgentChatPageLayout.tsx'), 'utf8')
  const chatPresentationStateSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatPresentationState.ts'), 'utf8')
  const conversationAutoScrollSource = readFileSync(resolve('src/features/agent/presentation/useAgentConversationAutoScroll.ts'), 'utf8')
  const chatStoreBindingsSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatStoreBindings.ts'), 'utf8')
  const chatViewControllerSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatViewController.ts'), 'utf8')
  const threadSectionSource = readFileSync(resolve('src/features/agent/components/AgentConversationThreadSection.tsx'), 'utf8')
  const projectionItemsSource = readFileSync(resolve('src/features/agent/components/AgentConversationProjectionItems.tsx'), 'utf8')
  const clientSource = readFileSync(resolve('src/shared/infrastructure/providerSessionHttpClient.ts'), 'utf8')
  const sessionStoreSource = readFileSync(resolve('src/features/agent/state/agentSessionStore.ts'), 'utf8')
  const agentStoreSource = readFileSync(resolve('src/features/agent/state/agentStore.ts'), 'utf8')
  const agentConversationSource = readFileSync(resolve('src/features/agent/domain/agentConversation.ts'), 'utf8')
  const protocolSource = readFileSync(resolve('src/features/agent/domain/agentProtocol.ts'), 'utf8')
  const sendActivitySource = readFileSync(resolve('src/features/agent/application/agentSendActivity.ts'), 'utf8')
  const sendProviderSessionReadinessSource = readFileSync(resolve('src/features/agent/application/agentSendProviderSessionReadiness.ts'), 'utf8')
  const sendCompletionSource = readFileSync(resolve('src/features/agent/application/agentSendCompletion.ts'), 'utf8')
  const sendCommitSource = readFileSync(resolve('src/features/agent/application/agentSendCommit.ts'), 'utf8')
  const sendStreamSource = readFileSync(resolve('src/features/agent/application/agentSendStream.ts'), 'utf8')
  const activeRunInputSource = readFileSync(resolve('src/features/agent/application/agentActiveRunInput.ts'), 'utf8')
  const capabilityProbeSource = readFileSync(resolve('src/features/agent/application/agentChatCapabilityProbe.ts'), 'utf8')
  const runActivitySource = readFileSync(resolve('src/features/agent/domain/agentRunActivity.ts'), 'utf8')
  const liveRunActivitySource = readFileSync(resolve('src/features/agent/presentation/agentLiveRunActivity.ts'), 'utf8')
  const activityFeedBuilderSource = readFileSync(resolve('src/features/agent/presentation/agent-activity-feed/builder.ts'), 'utf8')
  const interactionControllerSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatInteractionController.ts'), 'utf8')
  const interactionInputsSource = readFileSync(resolve('src/features/agent/presentation/agentChatInteractionInputs.ts'), 'utf8')
  const actionInputsSource = readFileSync(resolve('src/features/agent/presentation/agentChatActionInputs.ts'), 'utf8')
  const sendPipelineInputsSource = readFileSync(resolve('src/features/agent/presentation/agentChatSendPipelineInputs.ts'), 'utf8')
  const sendPipelineSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatSendPipeline.ts'), 'utf8')
  const commitSendHookSource = readFileSync(resolve('src/features/agent/presentation/useAgentCommitSendWorkspace.ts'), 'utf8')
  const providerSessionContextControllerSource = readFileSync(resolve('src/features/agent/presentation/useProviderSessionContextController.ts'), 'utf8')
  const activePlanSnapshotSource = readFileSync(resolve('src/features/agent/presentation/useAgentActivePlanSnapshot.ts'), 'utf8')
  const planActionBindingsSource = readFileSync(resolve('src/features/agent/presentation/useAgentPlanActionBindings.ts'), 'utf8')
  const runInteractionActionBindingsSource = readFileSync(resolve('src/features/agent/presentation/useAgentRunInteractionActionBindings.ts'), 'utf8')
  const runResultActionsSource = readFileSync(resolve('src/features/agent/presentation/useAgentRunResultActions.ts'), 'utf8')
  const runStopActionSource = readFileSync(resolve('src/features/agent/presentation/useAgentRunStopAction.ts'), 'utf8')
  const timelineSource = readFileSync(resolve('src/features/agent/presentation/useAgentTimeline.ts'), 'utf8')
  const providerSessionChatSource = readFileSync(resolve('src/shared/infrastructure/providerSessionChat.ts'), 'utf8')
  const conversationHeaderSource = readFileSync(resolve('src/features/agent/components/AgentChatHeaderSection.tsx'), 'utf8')
  const conversationListSource = readFileSync(resolve('src/features/agent/components/AgentConversationList.tsx'), 'utf8')
  const composerSectionSource = readFileSync(resolve('src/features/agent/components/AgentComposerSection.tsx'), 'utf8')
  const mentionEditorSource = readFileSync(resolve('src/features/agent/components/AgentMentionEditor.tsx'), 'utf8')
  const conversationTabsSource = readFileSync(resolve('src/features/agent/components/AgentConversationTabs.tsx'), 'utf8')
  const providerControlsSource = readFileSync(resolve('src/features/agent/components/ProviderControls.tsx'), 'utf8')
  const conversationTabProviderSessionStatusLightsSource = readFileSync(resolve('src/features/agent/presentation/useAgentConversationTabProviderSessionStatusLights.ts'), 'utf8')
  const providerSessionStatusLightControllerSource = readFileSync(resolve('src/features/agent/presentation/providerSessionStatusLightController.ts'), 'utf8')
  const projectAgentModePageSource = readFileSync(resolve('src/features/agent/components/ProjectAgentModePage.tsx'), 'utf8')
  const appServerChatShellSource = readFileSync(resolve('src/features/agent/components/AppServerChatShell.tsx'), 'utf8')
  const agentChatDataSourceShellSource = readFileSync(resolve('src/features/agent/components/AgentChatDataSourceShell.tsx'), 'utf8')
  const globalHomePageSource = readFileSync(resolve('src/pages/home/GlobalHomePage.tsx'), 'utf8')
  const uiAgentSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/index.tsx'), 'utf8')
  const uiAgentModeSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/mode/index.tsx'), 'utf8')
  const uiAgentModeCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/mode/styles.css'), 'utf8')
  const uiAgentPanelSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/panel.tsx'), 'utf8')
  const uiAgentChatSource = [
    '../../packages/ui/src/components/business/agent/chat/index.tsx',
    '../../packages/ui/src/components/business/agent/chat/types.ts',
    '../../packages/ui/src/components/business/agent/chat/list/index.tsx',
    '../../packages/ui/src/components/business/agent/chat/tabs/index.tsx',
    '../../packages/ui/src/components/business/agent/chat/tabs/panel/index.tsx',
    '../../packages/ui/src/components/business/agent/chat/tabs/item/index.tsx',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentChatCssSource = [
    '../../packages/ui/src/components/business/agent/chat/styles.css',
    '../../packages/ui/src/components/business/agent/chat/tabs/styles.css',
    '../../packages/ui/src/components/business/agent/chat/tabs/panel/styles.css',
    '../../packages/ui/src/components/business/agent/chat/tabs/item/styles.css',
    '../../packages/ui/src/components/business/agent/chat/history/styles.css',
    '../../packages/ui/src/components/business/agent/chat/toolbar/styles.css',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentShellCssSource = [
    '../../packages/ui/src/components/business/agent/shell/styles.css',
    '../../packages/ui/src/components/business/agent/shell/layout/styles.css',
    '../../packages/ui/src/components/business/agent/shell/sidebar/styles.css',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentThreadCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/thread/foundation/styles.css'), 'utf8')
  const uiAgentComposerCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/composer/styles.css'), 'utf8')
  const uiAgentPanelCssSource = [
    '../../packages/ui/src/components/business/agent/panel/styles.css',
    '../../packages/ui/src/components/business/agent/panel/frame/styles.css',
    '../../packages/ui/src/components/business/agent/panel/card/styles.css',
    '../../packages/ui/src/components/business/agent/panel/composer-mention/styles.css',
    '../../packages/ui/src/components/business/agent/panel/shell-layout/styles.css',
    '../../packages/ui/src/components/business/agent/panel/thread-message/styles.css',
    '../../packages/ui/src/components/business/agent/panel/context/styles.css',
    '../../packages/ui/src/components/business/agent/panel/provider-session/styles.css',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentPageCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/page/styles.css'), 'utf8')
  const uiAgentGeneratedFeedbackCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/feedback/styles.css'), 'utf8')
  const uiAgentGeneratedResultSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/result-card/item/index.tsx'), 'utf8')
  const uiAgentGeneratedResultShellSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/result-card/shell/index.tsx'), 'utf8')
  const uiAgentGeneratedResultNoticeSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/result-card/notice/index.tsx'), 'utf8')
  const uiAgentGeneratedResultCssSource = [
    '../../packages/ui/src/components/business/agent/generated/result-card/header/styles.css',
    '../../packages/ui/src/components/business/agent/generated/result-card/item/styles.css',
    '../../packages/ui/src/components/business/agent/generated/result-card/notice/styles.css',
    '../../packages/ui/src/components/business/agent/generated/result-card/shell/styles.css',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentGeneratedCandidateResourceSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/candidate-dialog/resource/index.tsx'), 'utf8')
  const uiAgentGeneratedCandidateResourceCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/candidate-dialog/resource/styles.css'), 'utf8')
  const uiAgentGeneratedCandidateTargetSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/candidate-dialog/target/index.tsx'), 'utf8')
  const uiAgentGeneratedCandidateTargetCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/candidate-dialog/target/styles.css'), 'utf8')
  const uiAgentRunInteractionApprovalSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/run-interaction/card/index.tsx'), 'utf8')
  const uiAgentRunInteractionApprovalCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/run-interaction/card/styles.css'), 'utf8')
  const uiAgentRunInteractionApprovalFeedbackSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/run-interaction/feedback/index.tsx'), 'utf8')
  const uiAgentRunActivitySource = [
    '../../packages/ui/src/components/business/agent/run-activity/bubble/index.tsx',
    '../../packages/ui/src/components/business/agent/run-activity/code/index.tsx',
    '../../packages/ui/src/components/business/agent/run-activity/disclosure/index.tsx',
    '../../packages/ui/src/components/business/agent/run-activity/item/index.tsx',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentRunActivityCssSource = [
    '../../packages/ui/src/components/business/agent/run-activity/bubble/styles.css',
    '../../packages/ui/src/components/business/agent/run-activity/disclosure/styles.css',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentPlanOverviewSource = [
    '../../packages/ui/src/components/business/agent/plan-overview/disclosure/index.tsx',
    '../../packages/ui/src/components/business/agent/plan-overview/feedback/index.tsx',
    '../../packages/ui/src/components/business/agent/plan-overview/item/index.tsx',
    '../../packages/ui/src/components/business/agent/plan-overview/shell/index.tsx',
    '../../packages/ui/src/components/business/agent/plan-overview/task/index.tsx',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiAgentPageSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/page/index.tsx'), 'utf8')
  const agentManagementPageSource = [
    'src/features/agent/components/AgentConsolePage.tsx',
    'src/features/agent/components/AgentRunsPage.tsx',
    'src/features/agent/components/AIAgentRunPage.tsx',
    'src/features/agent/components/AIAgentSettingsPage.tsx',
    'src/features/agent/components/AIAgentPerformancePage.tsx',
    'src/features/agent/components/AgentModeCanvasListPage.tsx',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiLayoutSource = [
    '../../packages/ui/src/components/layout/chrome.ts',
    '../../packages/ui/src/components/layout/index.tsx',
    '../../packages/ui/src/components/layout/workspace/index.tsx',
  ].map((relativePath) => readFileSync(resolve(relativePath), 'utf8')).join('\n')
  const uiCssSource = readFileSync(resolve('../../packages/ui/src/styles.css'), 'utf8')
  const uiAgentCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/styles.css'), 'utf8')
  const cssSource = readFileSync(resolve('src/index.css'), 'utf8')
  const zhSource = readFileSync(resolve('src/i18n/locales/zh-CN.json'), 'utf8')
  const enSource = readFileSync(resolve('src/i18n/locales/en-US.json'), 'utf8')

  assert.match(protocolSource, /providerSessionMessage\?: ProviderSessionMessageRef/)
  assert.match(protocolSource, /providerSessionInput\?: ProviderSessionInputRef/)
  assert.match(protocolSource, /runtimeMessage\?: ProviderSessionMessageRef/)
  assert.match(protocolSource, /runtimeInput\?: ProviderSessionInputRef/)
  assert.match(timelineSource, /providerSessionMessage,/)
  assert.match(timelineSource, /providerSessionInput,/)
  assert.doesNotMatch(timelineSource, /runtimeMessage: providerSessionMessage|runtimeInput: providerSessionInput/)
  assert.doesNotMatch(clientSource, /getThreadOrCreate/)
  assert.match(clientSource, /RunMessageResult/)
  assert.match(clientSource, /createSessionMessageRun\(sessionId: string/)
  assert.match(clientSource, /\.then\(normalizeCreateMessageRunResult\)/)
  assert.match(clientSource, /const providerSessionInput = input\.providerSessionInput \?\? input\.runtimeInput/)
  assert.match(clientSource, /providerSessionInputAccepted/)
  assert.doesNotMatch(clientSource, /runtimeInputAccepted/)
  assert.match(clientSource, /message send no longer accepts a client-selected thread/)
  assert.match(clientSource, /message send requires a provider session/)
  assert.match(clientSource, /return await this\.runSessionMessageStream\(sessionId, input, options\)/)
  assert.match(clientSource, /\/sessions\/\$\{encodeURIComponent\(sessionId\)\}\/runs/)
  assert.doesNotMatch(clientSource, /createMessageRun\(threadId: string/)
  assert.doesNotMatch(clientSource, /postJSON\(`\/threads\/\$\{encodeURIComponent\(threadId\)\}\/runs/)
  assert.match(sendStreamSource, /currentRuntimeState/)
  assert.match(sendStreamSource, /nextRuntimeState/)
  assert.doesNotMatch(sendStreamSource, /getConversationProviderSessionState|setConversationProviderSessionState/)
  assert.match(sendCommitSource, /conversationRuntimeStates\[deps\.conversationId\]/)
  assert.doesNotMatch(sendCommitSource, /conversationProviderSessionStates\[deps\.conversationId\]|getConversationProviderSessionState/)
  assert.match(activeRunInputSource, /active-run-input:/)
  assert.doesNotMatch(activeRunInputSource, /runtime-input:/)
  assert.match(capabilityProbeSource, /JSON-RPC \/ Provider Session Stream/)
  assert.doesNotMatch(capabilityProbeSource, /JSON-RPC \/ Runtime Stream/)
  assert.match(sendActivitySource, /kind: 'provider_session'/)
  assert.doesNotMatch(sendActivitySource, /kind: 'runtime'/)
  assert.match(sendProviderSessionReadinessSource, /kind: 'provider_session'/)
  assert.doesNotMatch(sendProviderSessionReadinessSource, /kind: 'runtime'/)
  assert.match(runActivitySource, /projectLiveRunProviderSessionTraceEvent/)
  assert.match(runActivitySource, /activityLimit\?: number/)
  assert.doesNotMatch(runActivitySource, /projectLiveRunRuntimeTraceEvent|runtimeLimit\?: number|runtimeItems/)
  assert.match(liveRunActivitySource, /projectLiveRunProviderSessionTraceEvent/)
  assert.doesNotMatch(liveRunActivitySource, /projectLiveRunRuntimeTraceEvent/)
  assert.match(activityFeedBuilderSource, /providerSessionRoundSource/)
  assert.doesNotMatch(activityFeedBuilderSource, /isFailedRuntimeLineItem|isTerminalRuntimeLineItem|RuntimeActivityRoundSource|runtimeRoundSource|runtimeRoundLabel|本地 runtime|runtime 规则|runtime 重启时/)
  assert.match(agentManagementPageSource, /const providerSessionSummary = providerSessionTelemetryQuery\.data\?\.summary/)
  assert.match(agentManagementPageSource, /const providerSessionSpans = providerSessionTelemetryQuery\.data\?\.spans \?\? \[\]/)
  assert.doesNotMatch(agentManagementPageSource, /\bconst runtimeSummary\b|\bconst runtimeOperations\b|\bconst runtimeSpans\b|\bconst runtimeService\b/)
  assert.doesNotMatch(providerSessionChatSource, /runRuntimeMessage/)
  assert.doesNotMatch(providerSessionChatSource, /providerSessionClient\.ensureRunning\(\)/)
  assert.match(protocolSource, /export interface RunMessageResult[\s\S]*threadResolution: AgentThreadResolution/)
  assert.doesNotMatch(clientSource, /createMessageRunWithMissingThreadFallback/)
  assert.doesNotMatch(clientSource, /resolveMessageThread/)
  assert.match(frontendArchitectureSource, /Chat timeline renders only timeline items with `purpose: "transcript"` and `surface: "message_stream"`/)
  assert.match(frontendArchitectureSource, /must not fall back to session-level message aggregation/)
  assert.match(frontendArchitectureSource, /Timeline status, plan revisions, and diagnostics stay in raw timeline state/)
  assert.match(frontendArchitectureSource, /Components must not infer display behavior from raw `role`, `kind`, or message metadata/)
  assert.match(frontendArchitectureSource, /Active run input answers and approval echoes are filtered only from projected transcript evidence/)

  assert.doesNotMatch(sessionStoreSource, /conversationIdForProviderThread/)
  assert.match(agentConversationSource, /export function conversationIdForProviderThread/)
  assert.match(agentConversationSource, /mergeProviderSessionRuns/)
  assert.match(agentConversationSource, /resolveActionableProviderSessionRuns/)
  assert.match(agentConversationSource, /resolveCurrentProviderSessionRun/)
  assert.doesNotMatch(agentConversationSource, /mergeRuntimeRuns|resolveActionableRuntimeRuns|resolveCurrentRuntimeRun|runNeedsRuntimeUserAction/)
  assert.match(agentConversationSource, /PROVIDER_SESSION_ID_COMPAT_KEY/)
  assert.doesNotMatch(agentConversationSource, /LEGACY_RUNTIME_SESSION_ID_KEY/)
  assert.match(agentStoreSource, /export type Conversation = AgentConversation/)
  assert.match(protocolSource, /providerSessionId\?: string/)
  assert.match(protocolSource, /providerThreadId\?: string/)
  assert.doesNotMatch(agentStoreSource, /convsByUser/)
  assert.doesNotMatch(agentStoreSource, /createProviderSessionConversation: \(userId/)
  assert.match(sessionStoreSource, /activeConversationIdsByUser: Record<string, string \| null>/)
  assert.match(sessionStoreSource, /workspacesByUser: Record<string, Record<string, ConversationWorkspace>>/)
  assert.match(sessionStoreSource, /conversationThreadBindings: Record<string, AgentConversationThreadBinding>/)
  assert.match(sessionStoreSource, /conversationRuntimeStates: Record<string, AgentConversationRuntimeState>/)
  assert.match(sessionStoreSource, /setConversationProviderSessionTreeId/)
  assert.match(sessionStoreSource, /setConversationProviderThreadBindingId/)
  assert.match(sessionStoreSource, /setConversationProviderSessionId/)
  assert.match(sessionStoreSource, /setConversationProviderThreadId/)
  assert.match(agentConversationSource, /conversation\.providerSessionId === sessionId/)
  assert.match(agentConversationSource, /conversation\.providerThreadId === threadId/)
  assert.match(chatViewSource, /surface === 'page'/)
  assert.match(chatViewSource, /<AgentChatPageLayout/)
  assert.match(chatViewSource, /<AgentChatPanelLayout/)
  assert.match(chatViewControllerSource, /useAgentChatInteractionController\(buildAgentChatInteractionControllerInput\(\{/)
  assert.match(interactionControllerSource, /useAgentChatSendPipeline\(\{/)
  assert.match(interactionInputsSource, /actionBindings: buildAgentChatActionBindingsInput\(options\)/)
  assert.match(interactionInputsSource, /sendPipeline: buildAgentChatSendPipelineInput\(options\)/)
  assert.match(actionInputsSource, /runResultActions: \{/)
  assert.match(actionInputsSource, /runResultActions: \{[\s\S]*sessionId: conv\.providerSessionId/)
  assert.doesNotMatch(actionInputsSource, /upsertMessage: store\.messageStore\.upsertMessage/)
  assert.match(actionInputsSource, /runInteractionActions: \{/)
  assert.match(actionInputsSource, /runInteractionActions: \{[\s\S]*sessionId: conv\.providerSessionId/)
  assert.match(actionInputsSource, /planActions: \{/)
  assert.match(actionInputsSource, /sessionId: conv\.providerSessionId/)
  assert.match(providerSessionContextControllerSource, /providerSessionClient\.forSession\(\{ sessionId: sessionId\.trim\(\) \}\)/)
  assert.match(activePlanSnapshotSource, /providerSessionClient\.forSession\(\{ sessionId: sessionId\.trim\(\) \}\)/)
  assert.match(activePlanSnapshotSource, /providerSessionPlanClient\.streamPlan\(taskGraphId/)
  assert.match(planActionBindingsSource, /providerSessionClient\.forSession\(\{ sessionId: sessionId\.trim\(\) \}\)/)
  assert.match(planActionBindingsSource, /providerSessionPlanClient\.cancelRunTree\(runId, input\)/)
  assert.match(runInteractionActionBindingsSource, /providerSessionClient\.forSession\(\{ sessionId: sessionId\.trim\(\) \}\)/)
  assert.match(runInteractionActionBindingsSource, /providerSessionRunClient\.approveInteraction\(interactionId, decision\)/)
  assert.match(runInteractionActionBindingsSource, /providerSessionRunClient\.rejectInteraction\(interactionId\)/)
  assert.match(runInteractionActionBindingsSource, /providerSessionRunClient\.answerRunInput\(runId, input\)/)
  assert.match(runResultActionsSource, /providerSessionClient\.forSession\(\{ sessionId: sessionId\.trim\(\) \}\)/)
  assert.match(runResultActionsSource, /providerSessionRunClient\.streamRun\(runId/)
  assert.match(actionInputsSource, /stopAction: \{/)
  assert.match(actionInputsSource, /sessionId: conv\.providerSessionId/)
  assert.match(runStopActionSource, /providerSessionClient\.forSession\(\{/)
  assert.match(runStopActionSource, /providerSessionRunClient\.cancelRun\(runId, input\)/)
  assert.match(runStopActionSource, /providerSessionRunClient\.getRun\(runId\)/)
  assert.match(sendPipelineInputsSource, /workspaceBuilder: \{/)
  assert.match(sendPipelineInputsSource, /commitWorkspace: \{/)
  assert.doesNotMatch(sendPipelineInputsSource, /providerThreadHydration/)
  assert.match(sendPipelineInputsSource, /sendActions: \{/)
  assert.match(sendPipelineInputsSource, /sendActions: \{[\s\S]*loading: presentation\.loading/)
  assert.doesNotMatch(sendPipelineSource, /useAgentProviderThreadHydration/)
  assert.match(chatViewControllerSource, /useAgentTimeline\(\{/)
  assert.match(chatViewControllerSource, /useAgentTimeline\(\{[\s\S]*requireThread: true/)
  assert.match(timelineSource, /providerSessionClient\.forSession\(\{ sessionId \}\)/)
  assert.match(timelineSource, /providerSessionTimelineClient\.listSessionTimeline\(sessionId/)
  assert.match(timelineSource, /providerSessionTimelineClient\.streamSessionTimeline\(sessionId/)
  assert.match(chatStoreBindingsSource, /resolveAgentChatProviderSessionBindingIds/)
  assert.match(chatStoreBindingsSource, /input\.conversationThreadBinding\?\.providerThreadId/)
  assert.match(chatStoreBindingsSource, /input\.conversationProviderSessionState\?\.threadId/)
  assert.match(chatStoreBindingsSource, /conversationRuntimeState: runtimeStateWithLegacyFallback/)
  assert.doesNotMatch(chatStoreBindingsSource, /setConversationProviderSessionState/)
  assert.match(chatViewControllerSource, /store\.conversationRuntimeState\?\.run/)
  assert.doesNotMatch(chatViewControllerSource, /store\.conversationProviderSessionState\?\.(run|loading|building|approving|stopping|stopRequested)/)
  assert.doesNotMatch(chatViewControllerSource, /providerThreadProjectionMessages/)
  assert.doesNotMatch(chatViewControllerSource, /transientMessages/)
  assert.doesNotMatch(conversationHeaderSource, /sort\(\(a, b\) => b\.updatedAt - a\.updatedAt\)/)
  assert.match(conversationHeaderSource, /onReorderConversation/)
  assert.doesNotMatch(appTopControlsSource, /openAgentPanelNewConversation/)
  assert.doesNotMatch(appTopControlsSource, /handleNewConversationShortcut/)
  assert.doesNotMatch(chatPanelLayoutSource, /setDetailHeaderActions\(/)
  assert.doesNotMatch(chatPanelLayoutSource, /<AgentChatHeaderActions/)
  assert.match(conversationTabsSource, /<ProviderControls/)
  assert.match(conversationTabsSource, /onNewConversation=\{onNewConversation\}/)
  assert.match(conversationTabsSource, /onToggleHistory=\{onToggleHistory\}/)
  assert.match(providerControlsSource, /setNewConversationProviderId\(provider\.id\)/)
  assert.match(providerControlsSource, /agents\.chat\.selectNewConversationProvider/)
  assert.match(providerControlsSource, /showNewConversation = false/)
  assert.doesNotMatch(providerControlsSource, /selectNewConversationAgent/)
  assert.match(composerSectionSource, /<ProviderMark/)
  assert.match(chatPanelLayoutSource, /const conversationStarted = thread\.conversationStarted \|\| !!debugPreview\.workspace/)
  assert.doesNotMatch(chatPanelLayoutSource, /conversationProjection\.items\.length/)
  assert.doesNotMatch(chatPanelLayoutSource, /thread\.hasTranscriptMessages/)
  assert.doesNotMatch(chatPanelLayoutSource, /conversationProjection\.hasRenderableItems/)
  assert.doesNotMatch(chatPanelLayoutSource, /thread\.transcriptMessages/)
  assert.doesNotMatch(chatPanelLayoutSource, /thread\.transcriptMessageCount/)
  assert.doesNotMatch(chatPanelLayoutSource, /liveBlocks/)
  assert.match(chatPanelLayoutSource, /const emptyConversation = !conversationStarted/)
  assert.match(chatPanelLayoutSource, /const \[historyOpen, setHistoryOpen\] = useState\(false\)/)
  assert.match(chatPanelLayoutSource, /setHistoryOpen\(false\)/)
  assert.match(chatPanelLayoutSource, /if \(conversationStarted\) setHistoryOpen\(false\)/)
  assert.match(chatPanelLayoutSource, /historyThreads/)
  assert.match(chatPanelLayoutSource, /agents\.chat\.conversationHistory/)
  assert.match(chatPanelLayoutSource, /agents\.chat\.noHistoryConversations/)
  assert.match(chatPanelLayoutSource, /<AgentConversationThreadSection[\s\S]*\{\.\.\.thread\}[\s\S]*pinnedStatusExpanded=\{pinnedStatusExpanded\}[\s\S]*onPinnedStatusExpandedChange=\{setPinnedStatusExpanded\}/)
  assert.match(chatPanelLayoutSource, /const historyPanel = historyOpen \? \(/)
  assert.match(chatPanelLayoutSource, /<AgentComposerSection \{\.\.\.composer\} chrome="flush" \/>\s*\{historyPanel\}/)
  assert.doesNotMatch(chatPanelLayoutSource, /chrome="bottom-bar"/)
  assert.doesNotMatch(chatPanelLayoutSource, /chrome="card"/)
  assert.doesNotMatch(chatPanelLayoutSource, /const composerInContent = emptyConversation \|\| historyOpen/)
  assert.match(chatPageLayoutSource, /agent-page-chat-empty/)
  assert.match(chatPageLayoutSource, /agent-page-chat-empty-composer/)
  assert.doesNotMatch(chatPageLayoutSource, /<AgentMain[\s\S]*<AgentDebugPreviewDialog \{\.\.\.debugPreview\} \/>\s*\{pageActions\}/)
  assert.match(chatPageLayoutSource, /className="agent-page-chat-thread-shell agent-page-chat-thread-shell--empty"[\s\S]*\{pageActions\}/)
  assert.match(chatPageLayoutSource, /className="agent-page-chat-thread-shell"[\s\S]*\{pageActions\}/)
  assert.doesNotMatch(chatPageLayoutSource, /chrome="bottom-bar"/)
  assert.doesNotMatch(chatPageLayoutSource, /chrome="card"/)
  assert.doesNotMatch(composerSectionSource, /回答请求/)
  assert.doesNotMatch(composerSectionSource, /ai-agent-panel-input-header/)
  assert.match(composerSectionSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(mentionEditorSource, /AgentSurfaceBlock/)
  assert.match(mentionEditorSource, /function ComposerAttachmentChip/)
  assert.match(mentionEditorSource, /className="flex min-w-0 items-center gap-2 border-t border-border px-0 py-1/)
  assert.doesNotMatch(uiAgentPageCssSource, /ai-agent-panel-input-header/)
  assert.match(chatPageLayoutSource, /const conversationStarted = thread\.conversationStarted \|\| !!debugPreview\.workspace/)
  assert.match(chatPageLayoutSource, /const loadingConversationTimeline = thread\.showTimelineLoading && !conversationStarted/)
  assert.doesNotMatch(chatPageLayoutSource, /conversationProjection\.items\.length/)
  assert.doesNotMatch(chatPageLayoutSource, /thread\.hasTranscriptMessages/)
  assert.doesNotMatch(chatPageLayoutSource, /conversationProjection\.hasRenderableItems/)
  assert.doesNotMatch(chatPageLayoutSource, /thread\.transcriptMessages/)
  assert.doesNotMatch(chatPageLayoutSource, /thread\.transcriptMessageCount/)
  assert.match(chatPresentationStateSource, /projectionItemsScrollKey\(derived\.conversationProjection\.items\)/)
  assert.match(chatPresentationStateSource, /conversationProjectionScrollKey,/)
  assert.doesNotMatch(chatPresentationStateSource, /derived\.conversationProjection\.scrollKey/)
  assert.doesNotMatch(chatPresentationStateSource, /derived\.conversationLiveBlocks/)
  assert.doesNotMatch(chatPresentationStateSource, /messageCount: transcriptMessageCount/)
  assert.match(conversationAutoScrollSource, /conversationProjectionScrollKey: string/)
  assert.doesNotMatch(conversationAutoScrollSource, /blockCount/)
  assert.doesNotMatch(conversationAutoScrollSource, /hasStreamingAssistantContent/)
  assert.doesNotMatch(conversationAutoScrollSource, /streamingAssistantText/)
  assert.doesNotMatch(conversationAutoScrollSource, /visibleActivityEventCount/)
  assert.match(chatPageLayoutSource, /emptyAccessory/)
  assert.match(chatPageLayoutSource, /agent-page-chat-empty-accessory/)
  assert.doesNotMatch(threadSectionSource, /agents\.chat\.startChat/)
  assert.doesNotMatch(threadSectionSource, /AgentSuggestions/)
  assert.doesNotMatch(threadSectionSource, /border-l border-border/)
  assert.doesNotMatch(threadSectionSource, /pl-3/)
  assert.doesNotMatch(threadSectionSource, /className="ai-agent-panel-run-group"/)
  assert.match(projectionItemsSource, /className="ai-agent-panel-run-group"/)
  assert.match(conversationTabsSource, /<AgentConversationTabsPanel/)
  assert.match(conversationTabsSource, /transcriptMessageCount\(item\)/)
  assert.match(conversationListSource, /latestTranscriptChatMessage\(conv\)/)
  assert.match(conversationListSource, /transcriptMessageCount\(conv\)/)
  assert.match(conversationTabsSource, /onReorderConversation=\{onReorderConversation\}/)
  assert.match(conversationTabsSource, /providerSessionStatusLights/)
  assert.match(uiAgentChatSource, /draggable/)
  assert.match(uiAgentChatSource, /onReorderConversation\(draggedId, item\.id, position\)/)
  assert.match(uiAgentChatSource, /data-session-state/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-conversation-tab-close \{[\s\S]*position: absolute/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-chat-toolbar-tabs \{[\s\S]*overflow: hidden/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-chat-toolbar-actions \{[\s\S]*flex: 0 0 auto/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-chat-toolbar-actions > \.ms-button \{[\s\S]*flex: 0 0 auto/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-chat-toolbar \.ms-button:not\(\.ai-agent-panel-conversation-tab-close\):hover:not\(:disabled\)/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-shell \.ai-agent-panel-chat-header \{[\s\S]*min-height: 34px;[\s\S]*border-bottom: 0;[\s\S]*background: transparent;[\s\S]*padding: 0;/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-chat-toolbar \{[\s\S]*border: 0;[\s\S]*border-radius: 0;[\s\S]*box-shadow: none;/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-conversation-tab-main:focus-visible,[\s\S]*\.ai-agent-panel-conversation-tab-close:focus-visible \{[\s\S]*border-radius: 0;/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-conversation-tab-close \{[\s\S]*border-radius: 0;/)
  assert.match(uiAgentCssSource, /@import "\.\/chat\/styles\.css"/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-empty-history \{[\s\S]*order: 3;[\s\S]*flex: 0 0 calc\(100% \/ 3\)/)
  assert.match(uiAgentChatCssSource, /\.ai-agent-panel-empty-history-list \{[\s\S]*min-height: 0;[\s\S]*overflow-y: auto;/)
  assert.match(chatPanelLayoutSource, /useResizablePanel\(\{[\s\S]*resizeEdge: 'top'[\s\S]*collapseMode: 'after-min'/)
  assert.match(chatPanelLayoutSource, /\{\.{3}historyResize\.resizeHandleProps\}/)
  assert.doesNotMatch(chatPanelLayoutSource, /resizeStartRef/)
  assert.doesNotMatch(chatPanelLayoutSource, /document\.body\.style\.cursor/)
  assert.doesNotMatch(chatPanelLayoutSource, /window\.addEventListener\('pointermove'/)
  assert.doesNotMatch(conversationTabProviderSessionStatusLightsSource, /providerSessionClient\.streamSession/)
  assert.doesNotMatch(conversationTabProviderSessionStatusLightsSource, /providerSessionClient\.streamThread/)
  assert.match(conversationTabProviderSessionStatusLightsSource, /providerSessionStatusLightController\.setOwnerTargets/)
  assert.match(conversationTabProviderSessionStatusLightsSource, /useProviderSessionStatusLightStore/)
  assert.match(providerSessionStatusLightControllerSource, /private readonly connections = new Map<string, ProviderSessionStatusConnection>\(\)/)
  assert.match(providerSessionStatusLightControllerSource, /this\.connections\.set\(ref\.targetKey, connection\)/)
  assert.match(providerSessionStatusLightControllerSource, /this\.connections\.delete\(targetKey\)/)
  assert.match(providerSessionStatusLightControllerSource, /if \(!this\.connections\.has\(ref\.targetKey\)\) this\.startConnection\(ref\)/)
  assert.doesNotMatch(providerSessionStatusLightControllerSource, /this\.client\.getSessionProviderSessionSnapshot/)
  assert.doesNotMatch(providerSessionStatusLightControllerSource, /this\.client\.getThreadProviderSessionSnapshot/)
  assert.match(providerSessionStatusLightControllerSource, /this\.client\.forSession/)
  assert.match(providerSessionStatusLightControllerSource, /client\.streamSession/)
  assert.match(providerSessionStatusLightControllerSource, /client\.streamThread/)
  assert.doesNotMatch(providerSessionStatusLightControllerSource, /refreshDebounceMs/)
  assert.match(providerSessionStatusLightControllerSource, /runtime_status\.upserted/)
  assert.match(providerSessionStatusLightControllerSource, /providerSessionStatusLightFromStatusRecord/)
  assert.match(conversationTabProviderSessionStatusLightsSource, /conversationThreadBindings/)
  assert.doesNotMatch(conversationTabProviderSessionStatusLightsSource, /providerThreadIdsByConversation/)
  assert.doesNotMatch(conversationTabProviderSessionStatusLightsSource, /sessionIdsByConversation/)
  assert.doesNotMatch(sendPipelineSource, /providerThreadHydration/)
  assert.doesNotMatch(sendCompletionSource, /loadProviderThreadProjection/)
  assert.doesNotMatch(sendCommitSource, /setProviderThreadProjection/)
  assert.doesNotMatch(agentConversationSource, /markRuntimeMessagesRestored/)
  assert.doesNotMatch(agentConversationSource, /mergeProviderThreadProjectionMessages/)
  assert.doesNotMatch(agentConversationSource, /buildProviderThreadConversationProjection/)
  assert.equal(existsSync(resolve('src/features/agent/application/agentProviderThreadRestore.ts')), false)
  assert.match(agentConversationSource, /conversationIdForProviderSession/)
  assert.match(agentConversationSource, /conversationIdForProviderThread\(\{[\s\S]*conversationThreadBindings[\s\S]*conversationProviderSessionStates/)
  assert.doesNotMatch(agentConversationSource, /providerThreadIdsByConversation|sessionIdsByConversation/)
  assert.match(panelSource, /<AgentUnifiedChatShell/)
  assert.doesNotMatch(appSource, /AIAgentWorkspacesPage|AIWorkspacesPage|agentWorkspaces/)
  assert.match(agentManagementPageSource, /clearThreadHistory/)
  assert.match(agentControlCenterHookSource, /clearWorkspaceSessionThreadHistory\(providerSessions\)/)
  assert.match(agentControlCenterApplicationSource, /providerSessionClient[\s\S]*\.forSession\(\{[\s\S]*sessionId: session\.sessionId/)
  assert.match(agentControlCenterApplicationSource, /\.deleteAllThreads\(\)/)
  assert.match(agentControlCenterApplicationSource, /summarizeThreadClearResults/)
  assert.doesNotMatch(agentManagementPageSource, /文件级清理入口尚未接入/)
  assert.match(projectAgentModePageSource, /createAgentChatDataSourceForProvider\(newConversationProvider/)
  assert.match(projectAgentModePageSource, /APP_SERVER_THREAD_LIST_STALE_MS = 15_000/)
  assert.match(projectAgentModePageSource, /APP_SERVER_THREAD_LIST_REFRESH_MS = 30_000/)
  assert.match(projectAgentModePageSource, /APP_SERVER_THREAD_LIST_GC_MS = 5 \* 60_000/)
  assert.match(projectAgentModePageSource, /staleTime: APP_SERVER_THREAD_LIST_STALE_MS/)
  assert.match(projectAgentModePageSource, /refetchInterval: APP_SERVER_THREAD_LIST_REFRESH_MS/)
  assert.match(projectAgentModePageSource, /refetchOnWindowFocus: false/)
  assert.match(appServerChatShellSource, /autoLoadThreads=\{surface !== 'page'\}/)
  assert.match(agentChatDataSourceShellSource, /const loadDataSourceRef = useRef\(loadDataSource\)/)
  assert.doesNotMatch(agentChatDataSourceShellSource, /resolveModelForRequestRef/)
  assert.match(agentChatDataSourceShellSource, /loadDataSourceRef\.current\(\)/)
  assert.match(agentChatDataSourceShellSource, /const readHistoryThread = useCallback/)
  assert.match(agentChatDataSourceShellSource, /return dataSource\.readThread\(threadId, \{ includeTurns: true \}\)/)
  assert.doesNotMatch(agentChatDataSourceShellSource, /await dataSource\.resumeThread\(\{[\s\S]*threadId/)
  assert.match(agentChatDataSourceShellSource, /if \(autoLoadThreads\) \{[\s\S]*void loadThreadsRef\.current\(\)[\s\S]*return[\s\S]*\}[\s\S]*void restoreStoredThreadRef\.current\(\)/)
  assert.match(agentChatDataSourceShellSource, /\}, \[autoLoadThreads, dataSource\]\)/)
  assert.match(globalHomePageSource, /resolveNewConversationProvider\(providerSettings\)/)
  assert.match(globalHomePageSource, /usesAppServerProtocol\(activeProvider\)/)
  assert.match(globalHomePageSource, /if \(appServerMode\) \{[\s\S]*createAgentChatDataSourceForProvider\(activeProvider/)
  assert.match(globalHomePageSource, /openAppServerThread\(\{ threadId: thread\.id, provider: activeProvider \}\)/)
  assert.match(globalHomePageSource, /if \(appServerMode\) \{[\s\S]*return[\s\S]*\}[\s\S]*startSharedProvisionalConversation/)
  assert.doesNotMatch(projectAgentModePageSource, new RegExp(['ensure', 'Codex', 'App', 'Server', 'Rpc', 'Client'].join('')))
  assert.doesNotMatch(panelSource, /hasOpenConversations/)
  assert.doesNotMatch(panelSource, /className="hidden"/)
  assert.match(appSource, /const AIAgentPanel = React\.lazy/)
  assert.match(appSource, /const detailRightPaneOpen = detailAgentPanelOpen/)
  assert.match(appSource, /assistantPanel=\{detailAgentPanelOpen \|\| hasOpenConversations \? \([\s\S]*<React\.Suspense fallback=\{null\}>[\s\S]*<AIAgentPanel \/>[\s\S]*<\/React\.Suspense>[\s\S]*\) : undefined\}/)
  assert.match(appTopControlsSource, /const AssistantShortcutIcon = MessageSquare/)
  assert.doesNotMatch(appTopControlsSource, /listProviderSessionSummariesFromWorkspace/)
  assert.doesNotMatch(appTopControlsSource, /hasAssistantShortcutTarget/)
  assert.doesNotMatch(appTopControlsSource, /conversationCount/)
  assert.match(appTopControlsSource, /active=\{agentPanelOpen\}/)
  assert.match(appTopControlsSource, /<AssistantShortcutIcon size=\{iconSize\} \/>/)
  assert.doesNotMatch(appTopControlsSource, /showAssistantShortcut && conversationCount === 0/)
  assert.doesNotMatch(appTopControlsSource, /createConversation\(userId\)/)
  assert.match(appTopControlsSource, /const toggleAgentPanelOpen = useAgentPanelUiStore\(\(s\) => s\.toggleOpen\)/)
  assert.match(appTopControlsSource, /onClick=\{toggleAgentPanelOpen\}/)
  assert.doesNotMatch(headerSource, /title="Movscript"[\s\S]*>\s*M\s*</)
  assert.match(appTopControlsSource, /getAppRouteSurface\(pathname\)/)
  assert.match(appTopControlsSource, /workModeForRoute\(pathname, workMode\)/)
  assert.match(appTopControlsSource, /navigate\(routeForWorkMode\(nextMode, !!current\), \{ replace: routeSurface !== 'detail' \}\)/)
  assert.match(appTopControlsSource, /routeSurface === 'agent'/)
  assert.match(appSource, /function ProjectAgentModeRoute\(\)/)
  assert.match(appSource, /function ProjectAgentModeRoute\(\) \{\s*return <AgentModePage \/>\s*\}/)
  assert.match(projectAgentModePageSource, /onClick=\{\(\) => setSidebarCollapsed\(!sidebarCollapsed\)\}/)
  assert.match(projectAgentModePageSource, /sidebarCollapsed \? t\('agents\.chat\.expandAgentSidebar'\) : t\('agents\.chat\.collapseAgentSidebar'\)/)
  assert.match(projectAgentModePageSource, /<AgentModeIconSlot><MessageSquare size=\{14\} \/><\/AgentModeIconSlot>/)
  assert.doesNotMatch(projectAgentModePageSource, /<AgentModeCompactNavItem onClick=\{startNewConversation\}>[\s\S]*?新建 \{newConversationProvider\.label\} 会话[\s\S]*?<\/AgentModeCompactNavItem>/)
  assert.match(projectAgentModePageSource, /Object\.keys\(sessionState\.conversationThreadBindings\)/)
  assert.doesNotMatch(appSource, /if \(workMode !== 'agent'\) return <Navigate to=\{ROUTES\.project\.overview\} replace \/>/)
  assert.match(agentPanelDockControllerSource, /DETAIL_AGENT_PANEL_DEFAULT_WIDTH = 360/)
  assert.match(agentPanelDockControllerSource, /DETAIL_AGENT_PANEL_MAX_WIDTH = 720/)
  assert.match(agentPanelDockControllerSource, /DETAIL_AGENT_PANEL_RESERVED_WIDTH = 760/)
  assert.match(agentPanelDockControllerSource, /viewportWidth - DETAIL_AGENT_PANEL_RESERVED_WIDTH/)
  assert.match(agentPanelDockControllerSource, /const dockLayout = true/)
  assert.doesNotMatch(agentPanelDockControllerSource, /\b680\b/)
  assert.doesNotMatch(agentPanelDockControllerSource, /innerWidth >= 960/)
  assert.doesNotMatch(agentPanelDockControllerSource, /setDockLayout/)
  assert.match(uiAgentPanelSource, /data-dock-layout=\{dockLayout \? "true" : "false"\}/)
  assert.match(uiAgentPanelSource, /data-chrome=\{resolvedChrome\}/)
  assert.match(uiAgentPanelSource, /className="ai-agent-panel"/)
  assert.doesNotMatch(uiAgentPanelSource, /w-\[/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel\[data-chrome="dock"\]/)
  assert.match(uiAgentPanelCssSource, /flex: 0 0 var\(--ui-agent-panel-width\)/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel \.ai-agent-panel__resize-handle\.panel-resize-handle--left \{[\s\S]*width: 28px;[\s\S]*transform: translateX\(-14px\);/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel\[data-chrome="floating"\]/)
  assert.match(uiAgentPanelCssSource, /position: absolute/)
  assert.match(uiAgentPanelCssSource, /width: min\(var\(--ui-agent-panel-width\), calc\(100vw - 24px\)\)/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel__body > \.ai-agent-panel-shell/)
  assert.match(uiAgentPanelCssSource, /\.ms-agent-shell\.ai-agent-panel-shell \{[\s\S]*border: 0;[\s\S]*border-radius: 0;[\s\S]*box-shadow: none;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-header \{[\s\S]*border-bottom: 0;/)
  assert.match(uiAgentShellCssSource, /\.ms-agent-main \{[\s\S]*container-type: inline-size;/)
  assert.match(uiAgentPageCssSource, /\.agent-page-chat-empty \{[\s\S]*padding: clamp\(18px, 4vh, 40px\) 0 18px;/)
  assert.match(uiAgentPageCssSource, /\.agent-page-project-select-card \{[\s\S]*width: 100%;/)
  assert.match(uiAgentPageCssSource, /\.agent-page-chat-composer \{[\s\S]*padding: 8px 0 12px;/)
  assert.match(uiAgentPageCssSource, /\.agent-page-chat-empty-composer \{[\s\S]*width: 100%;/)
  assert.match(uiAgentPageCssSource, /\.agent-page-chat-thread-shell--empty \.agent-page-chat-composer \.ai-agent-panel-composer \{[\s\S]*padding: 10px 0;/)
  assert.match(uiAgentThreadCssSource, /padding: 20px 0;/)
  assert.match(uiAgentComposerCssSource, /margin: 0 0 16px;/)
  assert.match(uiAgentComposerCssSource, /padding: 9px 0;/)
  assert.match(uiAgentComposerCssSource, /\.ms-agent-shell--compact \.ms-agent-composer \{[\s\S]*margin: 0 0 12px;[\s\S]*padding: 8px 0;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-input-card\[data-chrome="flush"\] \{[\s\S]*padding: 8px 0 10px;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message \{[\s\S]*contain: none;[\s\S]*content-visibility: visible;[\s\S]*contain-intrinsic-size: none;[\s\S]*width: 100%;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--assistant \{[\s\S]*padding: 8px 0 6px;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell\.ms-agent-shell--compact \.ms-agent-message__body \{[\s\S]*width: 100%;[\s\S]*max-width: 100%;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--user \.ms-agent-message__body \{[\s\S]*width: fit-content;[\s\S]*max-width: var\(--ai-agent-panel-user-message-max-width\);[\s\S]*align-items: flex-end;[\s\S]*margin-left: auto;[\s\S]*padding-right: 0;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell\.ms-agent-shell--compact \.ms-agent-message--user \.ms-agent-message__body \{[\s\S]*width: fit-content;[\s\S]*max-width: var\(--ai-agent-panel-user-message-max-width\);[\s\S]*align-items: flex-end;[\s\S]*margin-left: auto;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--user \.ms-agent-message__content \{[\s\S]*width: auto;[\s\S]*padding: 7px 12px;/)
  assert.match(uiAgentPageCssSource, /\.project-agent-chat-shell \.ms-agent-message \{[\s\S]*contain: none;[\s\S]*content-visibility: visible;[\s\S]*contain-intrinsic-size: none;[\s\S]*gap: 0;/)
  assert.match(uiAgentPageCssSource, /\.project-agent-chat-shell\.ms-agent-shell--compact \.ms-agent-message__body \{[\s\S]*width: 100%;[\s\S]*max-width: 100%;/)
  assert.match(uiAgentPageCssSource, /\.project-agent-chat-shell\.ms-agent-shell--compact \.ms-agent-message--user \.ms-agent-message__body \{[\s\S]*width: fit-content;[\s\S]*max-width: var\(--ai-agent-panel-user-message-max-width\);[\s\S]*align-items: flex-end;[\s\S]*margin-left: auto;/)
  assert.match(uiAgentPageCssSource, /\.project-agent-chat-shell\.ms-agent-shell--compact \.ms-agent-message__content \{[\s\S]*width: 100%;[\s\S]*max-width: 100%;[\s\S]*padding-right: 0;[\s\S]*padding-left: 0;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-input-card\[data-chrome="bottom-bar"\]/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-input-card\[data-chrome="flush"\] \.ai-agent-panel-composer \{[\s\S]*margin: 0;[\s\S]*border: 1px solid[\s\S]*border-radius: var\(--ms-radius-md\);[\s\S]*padding: 8px 0;[\s\S]*box-shadow: 0 8px 22px/)
  assert.doesNotMatch(uiAgentPanelCssSource, /\.ms-agent-message--assistant \.ms-agent-surface-block,/)
  assert.doesNotMatch(uiAgentPanelCssSource, /:has\(\.ms-agent-surface-block\)/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--assistant \.ms-agent-message__body \{[\s\S]*max-width: 100%;[\s\S]*flex-basis: 100%;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--assistant \.ms-agent-message__content > \* \{[\s\S]*width: 100%;[\s\S]*max-width: 100%;/)
  assert.doesNotMatch(uiAgentPanelCssSource, /data-agent-divider-label/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--assistant \.ms-agent-run-activity,[\s\S]*\.ms-agent-plan-overview,[\s\S]*\.ms-agent-generated-result-card \{[\s\S]*background: transparent;[\s\S]*padding: 0;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--assistant \.ms-agent-run-activity-item,[\s\S]*\.ms-agent-generated-result-item,[\s\S]*\.ms-agent-plan-overview-task,[\s\S]*box-shadow: none;/)
  assert.match(uiAgentPanelCssSource, /\.ai-agent-panel-shell \.ms-agent-message--assistant \.ms-agent-run-interaction-thumb \{[\s\S]*width: 40px;[\s\S]*flex: 0 0 auto;/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-card__header \{[\s\S]*flex-wrap: wrap;[\s\S]*align-items: flex-start;/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-card__header-copy \{[\s\S]*flex: 1 1 11rem;/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-card__header-copy:last-child \{[\s\S]*justify-content: flex-end;[\s\S]*margin-left: auto;/)
  assert.doesNotMatch(uiAgentGeneratedFeedbackSource(), /AgentGeneratedIntentSurfaceBlock/)
  assert.match(uiAgentGeneratedFeedbackSource(), /function AgentGeneratedCard[\s\S]*?AgentSurfaceBlock/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-card \{[\s\S]*background: transparent;[\s\S]*box-shadow: none;/)
  assert.match(uiAgentGeneratedFeedbackSource(), /function AgentGeneratedItem[\s\S]*?AgentSurfaceBlock/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-stack > \.agent-generated-item \+ \.agent-generated-item \{[\s\S]*border-top: 1px solid/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-stat-grid \.agent-generated-item \{[\s\S]*padding: 0;/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-item__header \{[\s\S]*flex-wrap: wrap;[\s\S]*align-items: flex-start;/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-item__copy \{[\s\S]*flex: 1 1 12rem;/)
  assert.match(uiAgentGeneratedFeedbackCssSource, /\.agent-generated-item__header > \.agent-generated-status-badge \{[\s\S]*margin-left: auto;/)
  assert.doesNotMatch(uiAgentGeneratedResultSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentGeneratedResultSource, /variant/)
  assert.doesNotMatch(uiAgentGeneratedResultShellSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentGeneratedResultShellSource, /variant/)
  assert.doesNotMatch(uiAgentGeneratedResultNoticeSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentGeneratedResultNoticeSource, /variant/)
  assert.match(uiAgentGeneratedResultCssSource, /\.ms-agent-generated-result-card__header \{[\s\S]*flex-wrap: wrap;[\s\S]*align-items: flex-start;/)
  assert.match(uiAgentGeneratedResultCssSource, /\.ms-agent-generated-result-card__actions \{[\s\S]*flex-wrap: wrap;[\s\S]*margin-left: auto;/)
  assert.match(uiAgentGeneratedResultCssSource, /\.ms-agent-generated-result-item__row \{[\s\S]*flex-wrap: wrap;[\s\S]*align-items: flex-start;/)
  assert.match(uiAgentGeneratedResultCssSource, /\.ms-agent-generated-result-item__body \{[\s\S]*flex: 1 1 12rem;/)
  assert.match(uiAgentGeneratedResultCssSource, /\.ms-agent-generated-result-card \{[\s\S]*background: transparent;/)
  assert.match(uiAgentGeneratedResultCssSource, /\.ms-agent-generated-result-missing-notice \{[\s\S]*border-left: 2px solid/)
  assert.match(uiAgentGeneratedResultCssSource, /\.ms-agent-generated-result-card__list > \* \+ \* \{[\s\S]*border-top: 1px solid/)
  assert.doesNotMatch(uiAgentGeneratedCandidateResourceSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentGeneratedCandidateResourceSource, /variant/)
  assert.match(uiAgentGeneratedCandidateResourceCssSource, /\.ms-agent-generated-candidate-resource-item \+ \.ms-agent-generated-candidate-resource-item \{[\s\S]*border-top: 1px solid/)
  assert.match(uiAgentGeneratedCandidateResourceCssSource, /\.ms-agent-generated-candidate-resource-item__row \{[\s\S]*flex-wrap: wrap;[\s\S]*align-items: flex-start;/)
  assert.match(uiAgentGeneratedCandidateResourceCssSource, /\.ms-agent-generated-candidate-resource-item__body \{[\s\S]*flex: 1 1 10rem;/)
  assert.doesNotMatch(uiAgentGeneratedCandidateTargetSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentGeneratedCandidateTargetSource, /variant/)
  assert.match(uiAgentGeneratedCandidateTargetCssSource, /\.ms-agent-generated-candidate-target-list \{[\s\S]*border: 1px solid[\s\S]*border-radius: 0;/)
  assert.match(uiAgentGeneratedCandidateTargetCssSource, /\.ms-agent-generated-candidate-target-row \{[\s\S]*flex-wrap: wrap;[\s\S]*align-items: flex-start;/)
  assert.match(uiAgentGeneratedCandidateTargetCssSource, /\.ms-agent-generated-candidate-target-title \{[\s\S]*flex: 1 1 12rem;/)
  assert.match(uiAgentGeneratedCandidateTargetCssSource, /\.ms-agent-generated-candidate-selected-target \{[\s\S]*border: 1px solid[\s\S]*border-radius: 0;/)
  assert.match(uiAgentRunInteractionApprovalSource, /export const AgentRunInteractionApprovalCard[\s\S]*return <div ref=\{ref\} className=\{cn\("ms-agent-run-interaction-card"/)
  assert.doesNotMatch(uiAgentRunInteractionApprovalSource, /export const AgentRunInteractionApprovalCard[\s\S]*variant = "subtle"[\s\S]*AgentRunInteractionApprovalCard\.displayName/)
  assert.match(uiAgentRunInteractionApprovalCssSource, /\.ms-agent-run-interaction-card \{[\s\S]*background: transparent;[\s\S]*box-shadow: none;/)
  assert.match(uiAgentRunInteractionApprovalCssSource, /\.ms-agent-run-interaction-card__header \{[\s\S]*flex-wrap: wrap;[\s\S]*align-items: flex-start;/)
  assert.match(uiAgentRunInteractionApprovalCssSource, /\.ms-agent-run-interaction-card__body \{[\s\S]*flex: 1 1 12rem;/)
  assert.match(uiAgentRunInteractionApprovalCssSource, /\.ms-agent-run-interaction-card__title \{[\s\S]*flex: 1 1 12rem;/)
  assert.doesNotMatch(uiAgentRunActivitySource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentRunActivitySource, /variant="subtle"/)
  assert.doesNotMatch(uiAgentRunActivityCssSource, /box-shadow: var\(--ms-shadow-sm\)/)
  assert.doesNotMatch(uiAgentPlanOverviewSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentPlanOverviewSource, /variant="subtle"/)
  assert.doesNotMatch(uiAgentRunInteractionApprovalFeedbackSource, /AgentSurfaceBlock/)
  assert.doesNotMatch(uiAgentRunInteractionApprovalFeedbackSource, /variant="subtle"/)
  assert.match(uiLayoutSource, /export type LayoutChrome =/)
  assert.match(uiLayoutSource, /export type WorkspaceShellSurface = "agent" \| "detail" \| "canvas"/)
  assert.match(uiLayoutSource, /layoutChromeContracts/)
  assert.match(uiLayoutSource, /chrome = "workspace"/)
  assert.match(uiLayoutSource, /data-surface=\{surface\}/)
  assert.match(uiLayoutSource, /data-chrome=\{resolvedChrome\}/)
  assert.doesNotMatch(uiLayoutSource, /contentPaddingClassName/)
  assert.doesNotMatch(uiLayoutSource, /contentFrameClassName/)
  assert.match(appSource, /surface=\{shellSurface\}/)
  assert.match(appSource, /surface="canvas"/)
  assert.match(uiAgentPageSource, /export function AgentPageShell/)
  assert.match(uiAgentPageSource, /Omit<ComponentProps<typeof AppPageShell>, "chrome">/)
  assert.match(uiAgentPageSource, /<AppPageShell chrome="immersive"/)
  assert.doesNotMatch(sourceFunctionBlock(uiAgentPageSource, 'AgentWorkspaceListState'), /AgentSurfaceBlock/)
  assert.doesNotMatch(sourceFunctionBlock(uiAgentPageSource, 'AgentRunSidebarLoading'), /AgentSurfaceBlock/)
  assert.doesNotMatch(sourceFunctionBlock(uiAgentPageSource, 'AgentRunTraceEmptyState'), /AgentSurfaceBlock/)
  assert.doesNotMatch(sourceFunctionBlock(uiAgentPageSource, 'AgentRunDebugMutedNote'), /AgentSurfaceBlock/)
  assert.doesNotMatch(sourceFunctionBlock(uiAgentPageSource, 'AgentRunDebugStatusNote'), /AgentSurfaceBlock/)
  assert.doesNotMatch(sourceFunctionBlock(uiAgentPageSource, 'AgentRunDebugMutedNote'), /variant/)
  assert.doesNotMatch(sourceFunctionBlock(uiAgentPageSource, 'AgentRunDebugStatusNote'), /variant/)
  assert.match(agentManagementPageSource, /<AgentPageShell/)
  assert.doesNotMatch(agentManagementPageSource, /\bAppPageShell\b/)
  assert.match(appTopControlsSource, /toggleAgentModeContentPanelCollapsed/)
  assert.match(appTopControlsSource, /PanelRightOpen/)
  assert.match(appTopControlsSource, /PanelRightClose/)
  assert.match(agentPanelUiStoreSource, /agentModeContentPanelCollapsed: readAgentModeContentPanelCollapsed\(\)/)
  assert.match(agentPanelUiStoreSource, /return saved === null \? true : saved === 'true'/)
  assert.match(agentPanelUiStoreSource, /toggleAgentModeContentPanelCollapsed/)
  assert.doesNotMatch(projectAgentModePageSource, /pageEmptyAccessory=\{<AgentModeProjectSelectCard \/>}/)
  assert.doesNotMatch(projectAgentModePageSource, /function AgentModeProjectSelectCard/)
  assert.match(appSource, /function ProjectRequiredDialog\(\)/)
  assert.doesNotMatch(appSource, /\bforced\b/)
  assert.match(appTopControlsSource, /openProjectDialog\(\)/)
  assert.match(appTopControlsSource, /const ModeIcon = nextMode === 'agent' \? Bot : LayoutDashboard/)
  assert.match(appTopControlsSource, /onClick=\{switchMode\}/)
  assert.match(appTopControlsSource, /<ModeIcon size=\{compact \? 11 : 14\} \/>/)
  assert.match(uiAgentModeSource, /\bAgentNavItem\b/)
  assert.match(projectAgentModePageSource, /\bAgentModeConversationItem\b/)
  assert.match(projectAgentModePageSource, /useAgentConversationTabProviderSessionStatusLights\(openConversations\)/)
  assert.match(projectAgentModePageSource, /providerSessionStatusLight=\{providerSessionStatusLights\[conversation\.id\]\}/)
  assert.match(projectAgentModePageSource, /className="agent-mode-conversation-session-light"/)
  assert.match(projectAgentModePageSource, /title=\{title\}/)
  assert.match(projectAgentModePageSource, /meta=\{relativeTime\}/)
  assert.match(projectAgentModePageSource, /readAgentConversationOpenState\(userId\)/)
  assert.match(projectAgentModePageSource, /openAgentConversationIds\(conversationOpenState\)/)
  assert.match(projectAgentModePageSource, /setAgentConversationOpen\(current, \[conversation\.id\], false\)/)
  assert.doesNotMatch(projectAgentModePageSource, /compareConversationCreationOrder/)
  assert.equal(existsSync(resolve('src/features/agent/components/AgentBuiltinChatShell.tsx')), false)
  assert.equal(existsSync(resolve('src/features/agent/presentation/useAgentBuiltinChatController.ts')), false)
  assert.match(chatViewSource, /from '@\/features\/agent\/components\/agentChatHost'/)
  assert.doesNotMatch(sourceFunctionBlock(projectAgentModePageSource, 'startNewConversation'), /archiveConversations/)
  assert.doesNotMatch(sourceFunctionBlock(projectAgentModePageSource, 'selectConversation'), /archiveConversations/)
  assert.match(uiAgentModeSource, /\bAgentConversationItem\b/)
  assert.match(uiAgentSource, /export const AgentNavItem/)
  assert.match(uiAgentSource, /export const AgentConversationItem/)
  assert.match(uiAgentSource, /icon\?: React\.ReactNode/)
  assert.match(uiCssSource, /components\/business\/agent\/styles\.css/)
  assert.match(uiAgentShellCssSource, /\.ms-agent-nav-item\s*\{/)
  assert.match(uiAgentShellCssSource, /\.ms-agent-conversation__icon\s*\{/)
  assert.match(uiAgentModeCssSource, /\.agent-mode-group__list--nested \{[\s\S]*margin-left: 0;[\s\S]*border-left: 0;[\s\S]*padding-left: 2px;/)
  assert.match(uiAgentModeCssSource, /\.ms-agent-nav-item\.agent-mode-project-group__toggle\[data-density="compact"\],[\s\S]*\.ms-agent-nav-item\.agent-mode-project-group__toggle\[data-density="compact"\] \.agent-mode-label,[\s\S]*\.ms-agent-nav-item\.agent-mode-project-group__toggle\[data-density="compact"\] \.agent-mode-meta \{[\s\S]*font-size: var\(--ms-text-label\);[\s\S]*line-height: var\(--ms-leading-label\);/)
  assert.match(uiAgentModeCssSource, /\.agent-mode-conversation \{[\s\S]*grid-template-columns: minmax\(0, 1fr\) auto;[\s\S]*padding: 4px 6px 4px 10px;/)
  assert.match(uiAgentModeCssSource, /\.agent-mode-conversation\[data-has-icon="true"\] \{[\s\S]*grid-template-columns: auto minmax\(0, 1fr\) auto;/)
  assert.match(uiAgentModeCssSource, /\.agent-mode-conversation \.ms-agent-conversation__indicator \{[\s\S]*display: none;/)
  assert.match(uiAgentModeCssSource, /\.agent-mode-conversation-session-light \{[\s\S]*width: 7px;[\s\S]*height: 7px;/)
  assert.match(uiAgentModeCssSource, /\.agent-mode-conversation\[data-active="true"\] \{[\s\S]*background: color-mix\(in srgb, var\(--ms-color-surface\) 86%, var\(--ms-color-background\)\);/)
  assert.doesNotMatch(projectAgentModePageSource, /flex h-8 w-full min-w-0 items-center gap-2 rounded-md px-2/)
  assert.doesNotMatch(projectAgentModePageSource, /hover:bg-muted\/50 hover:text-foreground/)
  assert.match(appTopControlsSource, /AppTopProjectMenuContent/)
  assert.match(appTopControlsSource, /header\.titles\.projects/)
  assert.match(projectAgentModePageSource, /manageOwnWidth \? \(/)
  assert.match(projectAgentModePageSource, /collapsed[\s\S]*\? \{ width: 0, flexBasis: 0, minWidth: 0 \}/)
  assert.match(projectAgentModePageSource, /useResizablePanel\(\{[\s\S]*resizeEdge: 'right'[\s\S]*collapseMode: 'after-min'/)
  assert.match(projectAgentModePageSource, /useResizablePanel\(\{[\s\S]*resizeEdge: 'left'[\s\S]*collapseMode: 'after-min'/)
  assert.match(projectAgentModePageSource, /\{\.{3}sidebarResize\.resizeHandleProps\}[\s\S]*side="right"/)
  assert.match(projectAgentModePageSource, /\{\.{3}panelResize\.resizeHandleProps\}[\s\S]*side="left"/)
  assert.match(agentPanelDockControllerSource, /useResizablePanel\(\{[\s\S]*resizeEdge: 'left'[\s\S]*collapseMode: 'after-min'/)
  assert.match(panelSource, /resizeHandleProps=\{resizeHandleProps\}/)
  assert.match(uiAgentPanelSource, /resizeHandleProps\?: HTMLAttributes<HTMLDivElement>/)
  assert.doesNotMatch(agentPanelDockControllerSource, /window\.addEventListener\('pointermove'/)
  assert.doesNotMatch(projectAgentModePageSource, /resizeStart/)
  assert.doesNotMatch(projectAgentModePageSource, /window\.addEventListener\('pointermove'/)
  assert.match(projectAgentModePageSource, /\? \{ width: 0, flexBasis: 0, minWidth: 0 \}/)
  assert.match(projectAgentModePageSource, /width: panelWidth,[\s\S]*flexBasis: panelWidth,[\s\S]*minWidth: AGENT_MODE_CONTENT_PANEL_MIN_WIDTH/)
  assert.doesNotMatch(projectAgentModePageSource, /PanelRightOpen/)
  assert.doesNotMatch(projectAgentModePageSource, /PanelRightClose/)
  assert.match(uiAgentCssSource, /page\/styles\.css/)
  assert.match(uiAgentPageCssSource, /agent-page-project-select-card/)
  assert.match(uiAgentPageCssSource, /\.project-agent-chat-shell \.agent-page-chat-main \{[\s\S]*display: flex/)
  assert.match(uiAgentPageCssSource, /\.agent-page-chat-actions \{[\s\S]*position: absolute;[\s\S]*width: 28px;[\s\S]*height: 28px;[\s\S]*pointer-events: none;/)
  assert.match(uiAgentPageCssSource, /\.agent-page-chat-actions__trigger \{[\s\S]*pointer-events: auto;/)
  assert.match(uiAgentPageCssSource, /\.agent-page-chat-thread-shell \{[\s\S]*position: relative;/)
  assert.doesNotMatch(cssSource, /agent-page-project-select-card/)
  assert.match(zhSource, /"expandAgentContentPanel": "展开右侧内容区"/)
  assert.match(enSource, /"expandAgentContentPanel": "Expand right content panel"/)
  assert.match(conversationListSource, /onRestoreProviderThread: \(threadId: string, sessionId\?: string\) => Promise<void>/)
  assert.match(conversationListSource, /listProviderSessionSummariesFromWorkspace/)
  assert.match(conversationListSource, /const threadId = session\.interactiveThreadId \?\? session\.rootThreadId \?\? session\.activeThreadId/)
  assert.match(conversationListSource, /restoreThread\(threadId, session\.id\)/)
  assert.match(sessionStoreSource, /removeProviderSessionConversation: \(userId: string, conversationId: string\) => void/)
  assert.match(sessionStoreSource, /clearConversationProviderSessionState: \(conversationId: string\) => void/)
  assert.match(uiAgentChatSource, /deleteConversationLabel: string/)

  assert.match(sendActivitySource, /function threadResolutionActivityEvent/)
  assert.match(sendActivitySource, /Provider 线程不存在，已创建新线程/)
  assert.doesNotMatch(agentConversationSource, /completeRuntimeSendRunResult/)
  assert.doesNotMatch(agentConversationSource, /appendAssistantRunResultMessage/)
  assert.doesNotMatch(sendCompletionSource, /completeRuntimeSendRunResult/)
  assert.doesNotMatch(sendCompletionSource, /setProviderThreadProjection/)
  assert.doesNotMatch(sendCompletionSource, /mergeProviderThreadProjectionMessages/)
  assert.match(sendCompletionSource, /threadResolutionActivityEvent/)
  assert.match(sendPipelineSource, /useAgentCommitSendWorkspace\(\{/)
  assert.doesNotMatch(sendPipelineInputsSource, /messageStore/)
  assert.match(commitSendHookSource, /commitAgentSendWorkspace\(workspace, \{/)
  assert.doesNotMatch(commitSendHookSource, /messageStore/)
  assert.doesNotMatch(agentConversationSource, /interface AgentConversationMessageStore/)
  assert.doesNotMatch(agentConversationSource, /sendRuntimeInputMessage/)
  assert.doesNotMatch(sendCommitSource, /setProviderThreadProjection/)
  assert.match(sendCommitSource, /completeSendRunResult\(\{/)
})

function sourceFunctionBlock(source: string, functionName: string): string {
  const start = source.indexOf(`function ${functionName}(`)
  assert.ok(start >= 0, `missing function ${functionName}`)
  const bodyStart = source.indexOf('{', start)
  assert.ok(bodyStart >= 0, `missing function body for ${functionName}`)
  let depth = 0
  for (let index = bodyStart; index < source.length; index += 1) {
    const character = source[index]
    if (character === '{') depth += 1
    if (character === '}') {
      depth -= 1
      if (depth === 0) return source.slice(bodyStart, index + 1)
    }
  }
  assert.fail(`unterminated function ${functionName}`)
}

function uiAgentGeneratedFeedbackSource(): string {
  return readFileSync(resolve('../../packages/ui/src/components/business/agent/generated/feedback/index.tsx'), 'utf8')
}

test('agent session UI keeps worker trace drilldown contracts', () => {
  const planOverviewSource = readFileSync(resolve('src/features/agent/components/AgentPlanOverviewPanel.tsx'), 'utf8')

  assert.match(planOverviewSource, /const \[traceSummaries, setTraceSummaries\]/)
  assert.match(planOverviewSource, /const \[traceEventsByRunId, setTraceEventsByRunId\]/)
  assert.match(planOverviewSource, /providerSessionClient\.forSession\(\{ sessionId: snapshotSessionId \}\)/)
  assert.match(planOverviewSource, /providerSessionTraceClient\.getRunTraceSummary\(runId\)/)
  assert.match(planOverviewSource, /providerSessionTraceClient\.getRunTraceEvents\(runId, \{ limit: 8/)
  assert.match(planOverviewSource, /agentRunPath\(runId, \{ sessionId: snapshot\.taskGraph\.sessionId \}\)/)
  assert.match(planOverviewSource, /traceEventHasMoreByRunId/)
  assert.match(planOverviewSource, /traceEventKindFilters/)
  assert.match(planOverviewSource, /轨迹统计/)
  assert.match(planOverviewSource, /运行事件/)
  assert.match(planOverviewSource, /加载更多/)
})

test('agent session UI pins live task, plan, and subagent status at top of the thread', () => {
  const threadSectionSource = readFileSync(resolve('src/features/agent/components/AgentConversationThreadSection.tsx'), 'utf8')
  const layoutPropsSource = readFileSync(resolve('src/features/agent/presentation/agentChatViewLayoutProps.ts'), 'utf8')
  const conversationLiveBlocksSource = readFileSync(resolve('src/features/agent/domain/agentConversationLiveBlocks.ts'), 'utf8')
  const pinnedStatusViewSource = readFileSync(resolve('src/features/agent/presentation/agentPinnedStatusView.ts'), 'utf8')
  const pinnedStatusShelfSource = readFileSync(resolve('src/features/agent/components/AgentPinnedStatusShelf.tsx'), 'utf8')
  const chatBubblesSource = readFileSync(resolve('src/features/agent/components/AgentChatBubbles.tsx'), 'utf8')
  const pinnedStatusCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/pinned-status/styles.css'), 'utf8')
  const uiAgentPageCssSource = readFileSync(resolve('../../packages/ui/src/components/business/agent/page/styles.css'), 'utf8')
  const derivedStateSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatDerivedState.ts'), 'utf8')
  const generationProgressViewStateSource = readFileSync(resolve('src/features/agent/presentation/agentChatGenerationProgressViewState.ts'), 'utf8')

  assert.match(threadSectionSource, /<AgentPinnedStatusShelf/)
  assert.match(layoutPropsSource, /generationProgressStates: presentation\.generationProgressStates/)
  assert.match(derivedStateSource, /buildAgentChatGenerationProgressViewState\(\{/)
  assert.match(derivedStateSource, /\.\.\.generationProgressViewState/)
  assert.doesNotMatch(derivedStateSource, /generationProgressStatesForPinnedStatus/)
  assert.doesNotMatch(derivedStateSource, /generationProgressState \?/)
  assert.match(generationProgressViewStateSource, /generationProgressStatesForPinnedStatus\(\{/)
  assert.match(generationProgressViewStateSource, /generationProgressScrollKey\(generationProgressState\)/)
  assert.match(conversationLiveBlocksSource, /showLiveRunActivity/)
  assert.doesNotMatch(conversationLiveBlocksSource, /blocks\.push\(\.\.\.generationProgressStates/)
  assert.match(pinnedStatusShelfSource, /data-testid="agent-pinned-status-shelf"/)
  assert.match(pinnedStatusShelfSource, /agents\.chat\.pinnedStatus\.tabs\.generation/)
  assert.match(pinnedStatusShelfSource, /agents\.chat\.pinnedStatus\.tabs\.worker/)
  assert.match(pinnedStatusShelfSource, /agents\.chat\.pinnedStatus\.workersCount/)
  assert.match(pinnedStatusShelfSource, /agents\.chat\.pinnedStatus\.tabs\.plan/)
  assert.match(pinnedStatusShelfSource, /agents\.chat\.pinnedStatus\.viewPlanDetails/)
  assert.match(pinnedStatusShelfSource, /resolveAgentPinnedStatusView\(selectedView, \{/)
  assert.match(pinnedStatusShelfSource, /presentation\/agentPinnedStatusView/)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentPinnedStatusView.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentPinnedStatusView.test.ts')), false)
  assert.match(pinnedStatusViewSource, /export function resolveAgentPinnedStatusView/)
  assert.match(pinnedStatusViewSource, /if \(currentView && agentPinnedStatusViewAvailable\(currentView, availability\)\) return currentView/)
  assert.match(pinnedStatusShelfSource, /const \[collapsed, setCollapsed\] = useState\(!defaultExpanded\)/)
  assert.match(pinnedStatusShelfSource, /const isExpanded = expanded \?\? !collapsed/)
  assert.match(pinnedStatusShelfSource, /aria-expanded=\{isExpanded\}/)
  assert.match(pinnedStatusShelfSource, /aria-controls="agent-pinned-status-shelf-body"/)
  assert.match(pinnedStatusShelfSource, /event\.target !== event\.currentTarget/)
  assert.match(pinnedStatusShelfSource, /activeCount > 0 && <AgentPinnedStatusActiveCount>/)
  assert.match(pinnedStatusShelfSource, /<AgentPinnedStatusSummaryRow>/)
  assert.match(pinnedStatusShelfSource, /<AgentPinnedStatusBody id="agent-pinned-status-shelf-body">/)
  assert.match(threadSectionSource, /const showPinnedStatus = useMemo\(\(\) => hasAgentPinnedStatus\(\{/)
  assert.doesNotMatch(threadSectionSource, /showPinnedStatus[\s\S]*pinnedStatusExpanded \?\? true/)
  assert.match(pinnedStatusCssSource, /\.agent-pinned-status-root \{[\s\S]*border-bottom: 1px solid var\(--ms-color-border\);/)
  assert.match(uiAgentPageCssSource, /\.project-agent-chat-shell \.agent-pinned-status-root \{[\s\S]*border-bottom: 1px solid var\(--ms-color-border\);/)
  assert.match(pinnedStatusCssSource, /\.agent-pinned-status-surface \{[\s\S]*border-radius: 0;[\s\S]*background: transparent;[\s\S]*box-shadow: none;/)
  assert.doesNotMatch(pinnedStatusCssSource, /border-radius: 0 0 var\(--ms-radius-md\) var\(--ms-radius-md\)/)
  assert.match(chatBubblesSource, /useAgentMessageBubbleModel\(\{/)
  assert.match(chatBubblesSource, /activity\.liveRun/)
  assert.match(chatBubblesSource, /activity\.historicalActivity/)
  assert.doesNotMatch(chatBubblesSource, /<RunActivityTitleBubble/)
  assert.match(chatBubblesSource, /<AgentActivityFeedView[\s\S]*activity=\{activity\.historicalActivity\}/)
})

test('desktop bootstrap does not auto-start a workspace-global provider session', () => {
  const managedBootstrapSource = readFileSync(resolve('electron/managedServices/bootstrap.ts'), 'utf8')
  const settingsIpcSource = readFileSync(resolve('electron/ipc/settingsIpc.ts'), 'utf8')

  assert.doesNotMatch(managedBootstrapSource, new RegExp(['ensure', 'Agent', 'Runtime', 'Running'].join('')))
  assert.doesNotMatch(managedBootstrapSource, new RegExp(['start', 'Agent', 'Runtime', 'On', 'App', 'Ready'].join('')))
  assert.doesNotMatch(managedBootstrapSource, /registerDesktopMCPProviderWithAgent/)
  assert.match(managedBootstrapSource, /provider sessions will use this backend by default/)

  assert.doesNotMatch(settingsIpcSource, new RegExp(['ensure', 'Agent', 'Runtime', 'Running'].join('')))
  assert.doesNotMatch(settingsIpcSource, new RegExp(['get', 'Agent', 'Runtime', 'Launch', 'Policy'].join('')))
  assert.match(settingsIpcSource, /ensureMCPServerReady/)
})

test('agent session UI routes pending input answers before starting a new run', () => {
  const chatViewSource = readFileSync(resolve('src/features/agent/components/AgentChatView.tsx'), 'utf8')
  const chatViewControllerSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatViewController.ts'), 'utf8')
  const chatPanelLayoutSource = readFileSync(resolve('src/features/agent/components/AgentChatPanelLayout.tsx'), 'utf8')
  const chatPageLayoutSource = readFileSync(resolve('src/features/agent/components/AgentChatPageLayout.tsx'), 'utf8')
  const chatViewLayoutPropsSource = readFileSync(resolve('src/features/agent/presentation/agentChatViewLayoutProps.ts'), 'utf8')
  const chatThreadViewStateSource = readFileSync(resolve('src/features/agent/presentation/agentChatThreadViewState.ts'), 'utf8')
  const interactionControllerSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatInteractionController.ts'), 'utf8')
  const derivedStateSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatDerivedState.ts'), 'utf8')
  const composerViewStateSource = readFileSync(resolve('src/features/agent/presentation/agentChatComposerViewState.ts'), 'utf8')
  const generationProgressViewStateSource = readFileSync(resolve('src/features/agent/presentation/agentChatGenerationProgressViewState.ts'), 'utf8')
  const providerSessionWorkViewStateSource = readFileSync(resolve('src/features/agent/presentation/agentChatProviderSessionWorkViewState.ts'), 'utf8')
  const conversationProjectionStateSource = readFileSync(resolve('src/features/agent/presentation/agentChatConversationProjectionState.ts'), 'utf8')
  const liveRunActivitySource = readFileSync(resolve('src/features/agent/presentation/agentLiveRunActivity.ts'), 'utf8')
  const runInteractionStateSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatRunInteractionState.ts'), 'utf8')
  const chatBubblesSource = readFileSync(resolve('src/features/agent/components/AgentChatBubbles.tsx'), 'utf8')
  const composerSource = readFileSync(resolve('src/features/agent/components/AgentComposerSection.tsx'), 'utf8')
  const threadSectionSource = readFileSync(resolve('src/features/agent/components/AgentConversationThreadSection.tsx'), 'utf8')
  const projectionItemsSource = readFileSync(resolve('src/features/agent/components/AgentConversationProjectionItems.tsx'), 'utf8')
  const projectionContentItemSource = readFileSync(resolve('src/features/agent/components/AgentConversationProjectionContentItem.tsx'), 'utf8')
  const projectionActionsSource = readFileSync(resolve('src/features/agent/components/AgentConversationProjectionActions.ts'), 'utf8')
  const projectedLiveBubblesSource = readFileSync(resolve('src/features/agent/components/AgentProjectedLiveBubbles.tsx'), 'utf8')
  const projectedMessageBubbleSource = readFileSync(resolve('src/features/agent/components/AgentProjectedMessageBubble.tsx'), 'utf8')
  const projectedRunBubblesSource = readFileSync(resolve('src/features/agent/components/AgentProjectedRunBubbles.tsx'), 'utf8')
  const projectionActivityFiltersSource = readFileSync(resolve('src/features/agent/components/AgentConversationProjectionActivityFilters.ts'), 'utf8')
  const activityFeedComponentSource = readFileSync(resolve('src/features/agent/components/AgentActivityFeed.tsx'), 'utf8')
  const activityFeedSource = readFileSync(resolve('src/features/agent/presentation/agentActivityFeed.ts'), 'utf8')
  const messageBoundariesSource = readFileSync(resolve('src/features/agent/domain/agentMessageBoundaries.ts'), 'utf8')
  const messageFactsSource = readFileSync(resolve('src/features/agent/domain/agentMessageFacts.ts'), 'utf8')
  const messageFactsTestSource = readFileSync(resolve('src/features/agent/domain/agentMessageFacts.test.ts'), 'utf8')
  const messageBubbleModelSource = readFileSync(resolve('src/features/agent/presentation/agentMessageBubbleModel.ts'), 'utf8')
  const messageBubbleModelTestSource = readFileSync(resolve('src/features/agent/presentation/agentMessageBubbleModel.test.ts'), 'utf8')
  const messageFactsHookSource = readFileSync(resolve('src/features/agent/presentation/useAgentMessageFactsModel.ts'), 'utf8')
  const messageDividerSource = readFileSync(resolve('src/features/agent/presentation/agentMessageDivider.ts'), 'utf8')
  const thinkingStateSource = readFileSync(resolve('src/features/agent/domain/agentThinkingState.ts'), 'utf8')
  const asyncWorkHandoffSource = readFileSync(resolve('src/features/agent/domain/agentAsyncWorkHandoff.ts'), 'utf8')
  const activeRunInputMessagesSource = readFileSync(resolve('src/features/agent/domain/agentActiveRunInputMessages.ts'), 'utf8')
  const activeRunInputDeliveryBadgeSource = readFileSync(resolve('src/features/agent/presentation/agentActiveRunInputDeliveryBadge.ts'), 'utf8')
  const conversationProjectionSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjection.ts'), 'utf8')
  const conversationTranscriptSource = readFileSync(resolve('src/features/agent/domain/agentConversationTranscript.ts'), 'utf8')
  const conversationProjectionFinalizeSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionFinalize.ts'), 'utf8')
  const conversationProjectionPrepareSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionPrepare.ts'), 'utf8')
  const conversationProjectionInteractionsSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionInteractions.ts'), 'utf8')
  const conversationProjectionLiveBlocksSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionLiveBlocks.ts'), 'utf8')
  const conversationProjectionMessagesSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionMessages.ts'), 'utf8')
  const conversationProjectionRunInteractionsSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionRunInteractions.ts'), 'utf8')
  const conversationProjectionRunTurnsSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionRunTurns.ts'), 'utf8')
  const conversationProjectionTypesSource = readFileSync(resolve('src/features/agent/domain/agentConversationProjectionTypes.ts'), 'utf8')
  const conversationProjectionRenderWindowSource = readFileSync(resolve('src/features/agent/components/AgentConversationProjectionRenderWindow.ts'), 'utf8')
  const threadRenderWindowSource = readFileSync(resolve('src/features/agent/components/AgentThreadRenderWindow.ts'), 'utf8')
  const conversationProjectionScrollKeySource = readFileSync(resolve('src/features/agent/presentation/agentConversationProjectionScrollKey.ts'), 'utf8')
  const renderEqualitySource = readFileSync(resolve('src/features/agent/components/AgentRenderEquality.ts'), 'utf8')
  const projectedMessageRenderEqualitySource = readFileSync(resolve('src/features/agent/components/AgentProjectedMessageRenderEquality.ts'), 'utf8')
  const timelinePlanSource = readFileSync(resolve('src/features/agent/domain/agentTimelinePlan.ts'), 'utf8')
  const timelineActivityItemsSource = readFileSync(resolve('src/features/agent/domain/agentTimelineActivityItems.ts'), 'utf8')
  const transcriptMessageItemsSource = readFileSync(resolve('src/features/agent/domain/agentTranscriptMessageItems.ts'), 'utf8')
  const threadItemsSource = readFileSync(resolve('src/features/agent/domain/agentConversationThreadItems.ts'), 'utf8')
  const runInteractionAnchorsSource = readFileSync(resolve('src/features/agent/domain/agentRunInteractionAnchors.ts'), 'utf8')
  const chatActionBindingsSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatActionBindings.ts'), 'utf8')
  const sendActionsSource = readFileSync(resolve('src/features/agent/presentation/useAgentSendActions.ts'), 'utf8')
  const activeRunInputSource = readFileSync(resolve('src/features/agent/application/agentActiveRunInput.ts'), 'utf8')
  const runInteractionBindingsSource = readFileSync(resolve('src/features/agent/presentation/useAgentRunInteractionActionBindings.ts'), 'utf8')
  const runInteractionActionsSource = readFileSync(resolve('src/features/agent/application/agentRunInteractionActions.ts'), 'utf8')
  const commitSendHookSource = readFileSync(resolve('src/features/agent/presentation/useAgentCommitSendWorkspace.ts'), 'utf8')
  const runPageSource = readFileSync(resolve('src/features/agent/components/AIAgentRunPage.tsx'), 'utf8')
  const projectAgentModePageSource = readFileSync(resolve('src/features/agent/components/ProjectAgentModePage.tsx'), 'utf8')
  const sessionStoreSource = readFileSync(resolve('src/features/agent/state/agentSessionStore.ts'), 'utf8')
  const providerSessionThreadConversationSource = readFileSync(resolve('src/features/agent/presentation/providerSessionThreadConversation.ts'), 'utf8')

  assert.match(messageBoundariesSource, /providerSessionMessageRefs/)
  assert.match(activeRunInputMessagesSource, /providerSessionMessageRefs/)
  assert.match(activeRunInputDeliveryBadgeSource, /providerSessionMessageRefs/)
  assert.match(messageBubbleModelSource, /providerSessionMessageRefs/)
  assert.match(runInteractionAnchorsSource, /providerSessionMessageRefs/)
  assert.match(activeRunInputSource, /providerSessionInputMode: 'soft'/)
  assert.doesNotMatch(activeRunInputSource, /runtimeInputMode/)
  assert.doesNotMatch(messageBoundariesSource, /meta\?\.runtimeMessage|meta\?\.runtimeInput/)
  assert.doesNotMatch(activeRunInputMessagesSource, /meta\?\.runtimeMessage|meta\?\.runtimeInput/)
  assert.doesNotMatch(activeRunInputDeliveryBadgeSource, /meta\?\.runtimeInput/)
  assert.doesNotMatch(activeRunInputDeliveryBadgeSource, /\['runtimeInput'\]/)
  assert.doesNotMatch(messageBubbleModelSource, /meta\?\.runtimeMessage/)
  assert.doesNotMatch(messageBubbleModelSource, /AgentMessageRuntimeAttributes|agentMessageRuntimeAttributes|runtimeAttributes/)
  assert.doesNotMatch(messageBubbleModelSource, /runtimeInputBadge|hasRuntimeInputBadge/)
  assert.doesNotMatch(chatBubblesSource, /runtimeInputBadge/)
  assert.doesNotMatch(runInteractionAnchorsSource, /meta\?\.runtimeMessage/)
  assert.doesNotMatch(runInteractionActionsSource, /runtime approval interaction|runtime rejection interaction/)
  assert.doesNotMatch(runPageSource, /缺少 runtime interaction/)
  assert.doesNotMatch(projectAgentModePageSource, /runtime conversation|cleanupDeletedRuntimeConversations/)
  assert.doesNotMatch(sessionStoreSource, /isRuntimeTerminalRun/)

  assert.match(derivedStateSource, /useAgentChatRunInteractionState\(\{/)
  assert.match(runInteractionStateSource, /const activePendingInputRequest = firstPendingInputRequest\(actionableRun\)/)
  assert.match(runInteractionStateSource, /const answeringPendingInput = !!activePendingInputRequest/)
  assert.match(chatViewSource, /useAgentChatViewController\(props\)/)
  assert.match(chatViewControllerSource, /conversationWithTimelineTranscript\(conv, timeline\.transcriptMessages\)/)
  assert.doesNotMatch(chatViewControllerSource, /transcriptMessages: timeline\.transcriptMessages/)
  assert.doesNotMatch(chatViewControllerSource, /transcriptMessageCount: timeline\.transcriptMessages\.length/)
  assert.match(conversationTranscriptSource, /function conversationWithTimelineTranscript/)
  assert.match(conversationTranscriptSource, /transcriptMessages,\s*\n\s*transcriptMessageCount: transcriptMessages\.length/)
  assert.doesNotMatch(providerSessionThreadConversationSource, /function conversationWithTimelineTranscript/)
  assert.match(chatViewControllerSource, /useAgentChatInteractionController\(buildAgentChatInteractionControllerInput\(\{/)
  assert.match(interactionControllerSource, /useAgentChatActionBindings\(actionBindings\)/)
  assert.match(chatActionBindingsSource, /useAgentRunInteractionActionBindings\(\{/)
  assert.match(runInteractionBindingsSource, /answerRunInteractionInputAction\(\{/)
  assert.doesNotMatch(runInteractionActionsSource, /sourceMessageId: localMessageId/)
  assert.match(runInteractionActionsSource, /answerRunInput\(run\.id, \{[\s\S]*requestId,[\s\S]*\.\.\.answer,[\s\S]*\}\)/)
  assert.match(runInteractionActionsSource, /optimisticInputAnswerRun\(run, requestId, answer\)/)
  assert.doesNotMatch(runInteractionStateSource, /runInteractionAnswerEchoesForMessages/)
  assert.doesNotMatch(runInteractionStateSource, /buildInteractionRunsByResultMessageId/)
  assert.doesNotMatch(runInteractionStateSource, /conversationProjectionRunInteractions/)
  assert.doesNotMatch(runInteractionStateSource, /standaloneRuns/)
  assert.match(conversationProjectionStateSource, /buildAgentConversationProjectionRunInteractions\(\{/)
  assert.doesNotMatch(conversationProjectionStateSource, /runInteractionAnswerEchoesForMessages/)
  assert.doesNotMatch(conversationProjectionStateSource, /buildInteractionRunsByResultMessageId/)
  assert.doesNotMatch(conversationProjectionStateSource, /standaloneRuns: input\.interactionRuns\.filter/)
  assert.match(conversationProjectionRunInteractionsSource, /export interface AgentConversationProjectionRunInteractions/)
  assert.match(conversationProjectionRunInteractionsSource, /function buildAgentConversationProjectionRunInteractions/)
  assert.match(conversationProjectionRunInteractionsSource, /runInteractionAnswerEchoesForMessages\(/)
  assert.match(conversationProjectionRunInteractionsSource, /buildInteractionRunsByResultMessageId\(\{/)
  assert.match(conversationProjectionRunInteractionsSource, /standaloneRuns: input\.interactionRuns\.filter/)
  assert.match(conversationProjectionRunInteractionsSource, /timelineActivitiesFromItems\(input\.timelineItems\)/)
  assert.match(derivedStateSource, /buildAgentChatConversationProjectionState\(\{[\s\S]*interactionRuns: runInteractionState\.interactionRuns/)
  assert.doesNotMatch(derivedStateSource, /conversationProjectionRunInteractions/)
  assert.doesNotMatch(derivedStateSource, /buildAgentConversationProjection/)
  assert.doesNotMatch(derivedStateSource, /buildAgentConversationLiveBlocks/)
  assert.doesNotMatch(derivedStateSource, /filterActivityEventsForRun/)
  assert.doesNotMatch(derivedStateSource, /agentTimelineItemsContainRunActivity/)
  assert.doesNotMatch(derivedStateSource, /timelineItemsContainRunActivity/)
  assert.doesNotMatch(derivedStateSource, /conversationLiveBlocks,/)
  assert.match(conversationProjectionStateSource, /buildAgentConversationLiveBlocks\(\{/)
  assert.match(conversationProjectionStateSource, /buildAgentConversationProjection\(\{/)
  assert.match(conversationProjectionStateSource, /visibleStreamingAssistantTextForTranscript\(\{/)
  assert.match(conversationProjectionStateSource, /filterActivityEventsForRun/)
  assert.match(conversationProjectionStateSource, /timelineItemsContainRunActivity/)
  assert.match(conversationProjectionStateSource, /getAgentThinkingState\(input\.activeRun, activeRunVisibleActivityEvents\)/)
  assert.doesNotMatch(conversationProjectionStateSource, /assistantMessageCompletesStreamingRun/)
  assert.doesNotMatch(conversationProjectionStateSource, /getThinkingBubbleState/)
  assert.doesNotMatch(conversationProjectionStateSource, /agentThinkingBubbleState/)
  assert.doesNotMatch(conversationProjectionStateSource, /function visibleStreamingTextForTranscript/)
  assert.doesNotMatch(conversationProjectionStateSource, /function runIdFromStreamingAssistantMessageId/)
  assert.doesNotMatch(conversationProjectionStateSource, /function filterActivityEventsForRun/)
  assert.doesNotMatch(conversationProjectionStateSource, /function agentTimelineItemsContainRunActivity/)
  assert.doesNotMatch(conversationProjectionStateSource, /function timelineItemsContainRunActivity/)
  assert.doesNotMatch(conversationProjectionStateSource, /function assistantMessageCompletesStreamingRun/)
  assert.match(thinkingStateSource, /function getAgentThinkingState/)
  assert.match(thinkingStateSource, /export interface AgentThinkingState/)
  assert.doesNotMatch(thinkingStateSource, /AgentConversationProjectionThinkingState/)
  assert.match(thinkingStateSource, /latestModelRetryStatus/)
  assert.match(thinkingStateSource, /latestReasoningStatus/)
  assert.match(liveRunActivitySource, /AgentThinkingState/)
  assert.doesNotMatch(liveRunActivitySource, /AgentLivePendingAssistantState/)
  assert.match(commitSendHookSource, /getAgentThinkingState\(run, \[\]\)/)
  assert.doesNotMatch(commitSendHookSource, /getThinkingBubbleState/)
  assert.match(derivedStateSource, /buildAgentChatComposerViewState\(\{/)
  assert.match(derivedStateSource, /\.\.\.composerViewState/)
  assert.doesNotMatch(derivedStateSource, /buildPendingActiveRunInputQueueItems/)
  assert.doesNotMatch(derivedStateSource, /isStoppableAgentRun/)
  assert.doesNotMatch(derivedStateSource, /isTerminalAgentRun/)
  assert.match(composerViewStateSource, /buildPendingActiveRunInputQueueItems\(input\.messages\)/)
  assert.match(composerViewStateSource, /isStoppableAgentRun\(input\.activeRun\)/)
  assert.match(composerViewStateSource, /isTerminalAgentRun\(input\.activeRun\)/)
  assert.match(derivedStateSource, /buildAgentChatGenerationProgressViewState\(\{/)
  assert.match(derivedStateSource, /\.\.\.generationProgressViewState/)
  assert.doesNotMatch(derivedStateSource, /generationProgressStatesForPinnedStatus/)
  assert.match(generationProgressViewStateSource, /generationProgressStatesForPinnedStatus\(\{/)
  assert.match(generationProgressViewStateSource, /generationProgressScrollKey\(generationProgressState\)/)
  assert.match(derivedStateSource, /buildAgentChatProviderSessionWorkViewState\(\{/)
  assert.match(derivedStateSource, /\.\.\.providerSessionWorkViewState/)
  assert.doesNotMatch(derivedStateSource, /isAgentAsyncWorkHandoffRun/)
  assert.doesNotMatch(derivedStateSource, /const inputBlockingLoading =/)
  assert.match(providerSessionWorkViewStateSource, /domain\/agentAsyncWorkHandoff/)
  assert.match(providerSessionWorkViewStateSource, /isAgentAsyncWorkHandoffRun\(input\.activeRun\)/)
  assert.match(providerSessionWorkViewStateSource, /inputBlockingLoading = input\.loading && !isAgentAsyncWorkHandoffRun/)
  assert.match(chatBubblesSource, /useAgentMessageBubbleModel\(\{/)
  assert.match(chatBubblesSource, /visibility\.hasRenderableBubble/)
  assert.match(chatBubblesSource, /footer\.hasFooter/)
  assert.match(chatBubblesSource, /sections\.contentText/)
  assert.match(chatBubblesSource, /sections\.contentAttachments/)
  assert.match(chatBubblesSource, /sections\.largeMediaAttachments/)
  assert.match(chatBubblesSource, /sections\.workspaceArtifacts/)
  assert.match(chatBubblesSource, /sections\.diagnosticValidationErrors/)
  assert.match(chatBubblesSource, /sections\.diagnosticParamAudits/)
  assert.match(chatBubblesSource, /sections\.userAttachments/)
  assert.doesNotMatch(chatBubblesSource, /activeRunInputDeliveryBadge/)
  assert.doesNotMatch(chatBubblesSource, /agentActiveRunInputMessages/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleModel\(presentation, msg, \{/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageActivityContentState\(presentation, \{/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleActivity\(activityState, presentation, sections\)/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleAction\(presentation, activityState, msg\)/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleShell\(presentation, msg, \{ time \}\)/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleFooter\(presentation, msg\)/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleSections\(presentation\)/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleVisibility\(presentation, \{/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageBubbleChrome\(presentation, \{ time \}\)/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageRuntimeAttributes\(msg\)/)
  assert.doesNotMatch(chatBubblesSource, /chrome\.footerAlign/)
  assert.doesNotMatch(chatBubblesSource, /chrome\.showCopyAction \?/)
  assert.doesNotMatch(chatBubblesSource, /chrome\.role/)
  assert.doesNotMatch(chatBubblesSource, /chrome\.avatar/)
  assert.doesNotMatch(chatBubblesSource, /chrome\.author/)
  assert.doesNotMatch(chatBubblesSource, /chrome\.time/)
  assert.doesNotMatch(chatBubblesSource, /chrome\.headLabel/)
  assert.doesNotMatch(chatBubblesSource, /runtimeAttributes\./)
  assert.doesNotMatch(chatBubblesSource, /data-agent-message-id=\{msg\.id\}/)
  assert.doesNotMatch(chatBubblesSource, /navigator\.clipboard\.writeText\(msg\.content\)/)
  assert.doesNotMatch(chatBubblesSource, /AgentActivityDividerMenu activity=\{historicalTimelineActivity\}/)
  assert.doesNotMatch(chatBubblesSource, /timelineActivity: historicalTimelineActivity/)
  assert.doesNotMatch(chatBubblesSource, /const liveActivityRun =/)
  assert.doesNotMatch(chatBubblesSource, /activityState\.showLiveActivity/)
  assert.doesNotMatch(chatBubblesSource, /activityState\.showHistoricalActivity/)
  assert.doesNotMatch(chatBubblesSource, /sections\.activityClassName/)
  assert.doesNotMatch(chatBubblesSource, /const hasMessageBody =/)
  assert.doesNotMatch(chatBubblesSource, /const hasFooter =/)
  assert.doesNotMatch(chatBubblesSource, /const activityState =/)
  assert.doesNotMatch(chatBubblesSource, /displayContent\.trim\(\)/)
  assert.doesNotMatch(chatBubblesSource, /contextLabels\.length > 0 \|\|/)
  assert.doesNotMatch(chatBubblesSource, /buildAgentActivityFeed/)
  assert.doesNotMatch(chatBubblesSource, /function runActivityHasVisibleContent/)
  assert.doesNotMatch(chatBubblesSource, /const hasActivityContent =/)
  assert.doesNotMatch(chatBubblesSource, /workspaceArtifacts,/)
  assert.doesNotMatch(chatBubblesSource, /generationJobs,/)
  assert.doesNotMatch(chatBubblesSource, /generationParamAudits,/)
  assert.doesNotMatch(chatBubblesSource, /generationValidationErrors,/)
  assert.doesNotMatch(chatBubblesSource, /messageAttachments,/)
  assert.doesNotMatch(chatBubblesSource, /generatedMediaAttachments,/)
  assert.doesNotMatch(chatBubblesSource, /compactAttachments,/)
  assert.doesNotMatch(chatBubblesSource, /displayContent,/)
  assert.doesNotMatch(chatBubblesSource, /text=\{displayContent\}/)
  assert.doesNotMatch(chatBubblesSource, /attachments=\{messageAttachments\}/)
  assert.doesNotMatch(chatBubblesSource, /attachments=\{generatedMediaAttachments\}/)
  assert.doesNotMatch(chatBubblesSource, /artifacts=\{workspaceArtifacts\}/)
  assert.doesNotMatch(chatBubblesSource, /errors=\{generationValidationErrors\}/)
  assert.doesNotMatch(chatBubblesSource, /audits=\{generationParamAudits\}/)
  assert.doesNotMatch(chatBubblesSource, /msg\.role === 'user'/)
  assert.doesNotMatch(chatBubblesSource, /agentMessageDividerLabel/)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentMessageDivider.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentMessageDivider.test.ts')), false)
  assert.match(messageDividerSource, /export function agentMessageDividerLabel/)
  assert.match(messageDividerSource, /export function formatAgentDividerTime/)
  assert.doesNotMatch(chatBubblesSource, /\{showModelSetupAction &&/)
  assert.doesNotMatch(chatBubblesSource, /\{hasResultSection &&/)
  assert.doesNotMatch(chatBubblesSource, /\{hasDiagnosticSection &&/)
  assert.doesNotMatch(chatBubblesSource, /\{showLargeMedia &&/)
  assert.doesNotMatch(chatBubblesSource, /compactAttachments\.length > 0/)
  assert.doesNotMatch(chatBubblesSource, /compactAttachments\.length > 1/)
  assert.doesNotMatch(chatBubblesSource, /defaultOpen=\{!displayContent\}/)
  assert.doesNotMatch(chatBubblesSource, /presentation\.isUser &&/)
  assert.doesNotMatch(chatBubblesSource, /msg\.meta\?\.runtimeMessage/)
  assert.doesNotMatch(chatBubblesSource, /transcriptAssistantProviderSessionRunId/)
  assert.match(messageFactsSource, /export interface AgentMessageFacts/)
  assert.match(messageFactsSource, /export function buildAgentMessageFacts/)
  assert.match(messageFactsSource, /hasAgentAsyncWorkHandoffActivity\(\{ activity: timelineActivity \}\)/)
  assert.doesNotMatch(messageFactsSource, /runtimeStatusMessageFromRunActivity/)
  assert.doesNotMatch(messageFactsSource, /legacyLocalRuntime|localRuntime/)
  assert.doesNotMatch(messageFactsSource, new RegExp(['Agent', 'Runtime', 'Status', 'Message'].join('')))
  assert.match(messageFactsTestSource, /buildAgentMessageFacts/)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentMessagePresentation.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentMessagePresentation.test.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/' + ['agent', 'Runtime', 'Status', 'Message.ts'].join(''))), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/' + ['agent', 'Runtime', 'Status', 'Message.test.ts'].join(''))), false)
  assert.match(asyncWorkHandoffSource, /function hasAgentAsyncWorkHandoffActivity/)
  assert.match(asyncWorkHandoffSource, /function isAgentAsyncWorkHandoffRun/)
  assert.doesNotMatch(asyncWorkHandoffSource, new RegExp(['Agent', 'Runtime', 'Status', 'Message'].join('')))
  assert.doesNotMatch(asyncWorkHandoffSource, /runtimeStatusMessageFromRunActivity/)
  assert.doesNotMatch(asyncWorkHandoffSource, /异步任务已提交/)
  assert.doesNotMatch(messageFactsTestSource, /agentMessageBubbleModel/)
  assert.doesNotMatch(messageFactsTestSource, /presentation\/agentMessageBubbleModel/)
  assert.doesNotMatch(messageFactsSource, /function agentMessageActivityContentState/)
  assert.doesNotMatch(messageFactsSource, /buildAgentActivityFeed\(input\)/)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentActivityFeed.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentActivityFeed.test.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/agent-activity-feed')), false)
  assert.match(activityFeedSource, /export \{ buildAgentActivityFeed \}/)
  assert.match(activityFeedComponentSource, /presentation\/agentActivityFeed/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleModel/)
  assert.doesNotMatch(messageFactsSource, /hiddenActivityActionItemIds/)
  assert.doesNotMatch(messageFactsSource, /needsModelSetupAction/)
  assert.doesNotMatch(messageFactsSource, /compactAttachments/)
  assert.doesNotMatch(messageFactsSource, /showModelSetupAction/)
  assert.doesNotMatch(messageFactsSource, /showLargeMedia/)
  assert.doesNotMatch(messageFactsSource, /hasUsableGeneratedResource/)
  assert.doesNotMatch(messageFactsSource, /hasResultSection/)
  assert.doesNotMatch(messageFactsSource, /hasProcessSection/)
  assert.doesNotMatch(messageFactsSource, /hasDiagnosticSection/)
  assert.doesNotMatch(messageFactsSource, /export interface AgentMessage\s/)
  assert.doesNotMatch(messageFactsSource, /export type AgentMessage/)
  assert.doesNotMatch(messageFactsSource, /export function hideGeneratedResultTechnicalSummary/)
  assert.match(messageBubbleModelSource, /export function agentMessageBubbleModel/)
  assert.match(messageBubbleModelTestSource, /agentMessageBubbleModel/)
  assert.match(messageBubbleModelTestSource, /presentation\/agentMessageBubbleModel/)
  assert.match(messageBubbleModelTestSource, /buildAgentMessageFacts/)
  assert.match(messageBubbleModelSource, /function agentMessageActivityContentState/)
  assert.match(messageBubbleModelSource, /presentation\/agentActivityFeed/)
  assert.match(messageBubbleModelSource, /buildAgentActivityFeed\(input\)/)
  assert.match(messageFactsHookSource, /agentMessageBubbleModel\(facts, message, \{/)
  assert.match(messageFactsHookSource, /presentation\/agentMessageBubbleModel/)
  assert.match(messageFactsHookSource, /buildAgentMessageFacts\(message, \{ timelineActivity \}\)/)
  assert.match(messageFactsHookSource, /toLocaleTimeString\(locale, \{ hour: '2-digit', minute: '2-digit' \}\)/)
  assert.match(messageBubbleModelSource, /function agentMessageBubbleAction/)
  assert.match(messageBubbleModelSource, /function agentMessageBubbleActivity/)
  assert.match(messageBubbleModelSource, /function agentMessageBubbleChrome/)
  assert.match(messageBubbleModelSource, /function agentMessageBubbleShell/)
  assert.match(messageBubbleModelSource, /presentation\/agentMessageDivider/)
  assert.match(messageBubbleModelSource, /agentMessageDividerLabel\(input\.time, facts\.timelineActivity\)/)
  assert.match(messageBubbleModelSource, /function agentMessageBubbleFooter/)
  assert.match(messageBubbleModelSource, /presentation\/agentActiveRunInputDeliveryBadge/)
  assert.match(messageBubbleModelSource, /activeRunInputDeliveryBadge\(message\)/)
  assert.match(messageBubbleModelSource, /activeRunInputBadge/)
  assert.match(messageBubbleModelSource, /function agentMessageBubbleSections/)
  assert.match(messageBubbleModelSource, /needsModelSetupAction\(message\.content\)/)
  assert.match(messageBubbleModelSource, /function compactMessageAttachments/)
  assert.match(messageBubbleModelSource, /const showResultSection = !facts\.isUser/)
  assert.match(messageBubbleModelSource, /const showDiagnosticSection = !facts\.isUser/)
  assert.match(messageBubbleModelSource, /function attachmentGridColumns/)
  assert.match(messageBubbleModelSource, /function agentMessageBubbleVisibility/)
  assert.match(messageBubbleModelSource, /function agentMessageProviderSessionAttributes/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageActivityContentState/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleAction/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleActivity/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleChrome/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleShell/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleFooter/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleSections/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageBubbleVisibility/)
  assert.doesNotMatch(messageFactsSource, /export function agentMessageRuntimeAttributes/)
  assert.doesNotMatch(messageBoundariesSource, /transcriptAssistantRuntimeMessageRunId/)
  assert.doesNotMatch(threadItemsSource, /isPendingRuntimeInputMessage/)
  assert.doesNotMatch(messageFactsSource, /transcriptAssistantProviderSessionRunId\(message\)/)
  assert.doesNotMatch(messageFactsSource, /hasRenderableBubble: hasMessageBody \|\| hasFooter/)
  assert.match(messageBubbleModelSource, /transcriptAssistantProviderSessionRunId\(message\)/)
  assert.match(messageBubbleModelSource, /hasRenderableBubble: hasMessageBody \|\| hasFooter/)
  assert.doesNotMatch(chatBubblesSource, /msg\.meta\?\.runtimeInput/)
  assert.doesNotMatch(chatBubblesSource, /正在同步到运行中对话/)
  assert.doesNotMatch(chatBubblesSource, /已加入运行中对话/)
  assert.doesNotMatch(chatBubblesSource, /同步失败/)
  assert.match(chatBubblesSource, /AgentThinkingState/)
  assert.doesNotMatch(chatBubblesSource, /agentThinkingBubbleState/)
  assert.match(composerSource, /AgentPendingActiveRunInputQueueItem/)
  assert.match(composerSource, /agentActiveRunInputMessages/)
  assert.match(threadItemsSource, /activeRunInputIsWaitingForDelivery\(message\)/)
  assert.match(threadItemsSource, /agentActiveRunInputMessages/)
  assert.doesNotMatch(threadItemsSource, /function buildPendingActiveRunInputQueueItems/)
  assert.doesNotMatch(threadItemsSource, /function activeRunInputDisplayDeliveryStatus/)
  assert.match(activeRunInputMessagesSource, /function buildPendingActiveRunInputQueueItems/)
  assert.match(activeRunInputMessagesSource, /function activeRunInputDisplayDeliveryStatus/)
  assert.doesNotMatch(activeRunInputMessagesSource, /AgentActiveRunInputDeliveryBadge/)
  assert.doesNotMatch(activeRunInputMessagesSource, /function activeRunInputDeliveryBadge/)
  assert.doesNotMatch(activeRunInputMessagesSource, /正在同步到运行中对话/)
  assert.match(activeRunInputDeliveryBadgeSource, /interface AgentActiveRunInputDeliveryBadge/)
  assert.match(activeRunInputDeliveryBadgeSource, /function activeRunInputDeliveryBadge/)
  assert.match(activeRunInputDeliveryBadgeSource, /activeRunInputDisplayDeliveryStatus\(message\)/)
  assert.match(activeRunInputMessagesSource, /function activeRunInputIsWaitingForDelivery/)
  assert.match(chatViewSource, /<AgentChatPanelLayout/)
  assert.match(chatViewSource, /<AgentChatPageLayout/)
  assert.match(chatPanelLayoutSource, /<AgentConversationThreadSection/)
  assert.match(chatPageLayoutSource, /<AgentConversationThreadSection/)
  assert.match(chatViewLayoutPropsSource, /buildAgentChatThreadViewState\(\{/)
  assert.match(chatViewLayoutPropsSource, /conversationProjection: presentation\.conversationProjection/)
  assert.match(chatViewLayoutPropsSource, /hasTranscriptMessages: conversationHasTranscriptMessages\(conv\)/)
  assert.match(chatViewLayoutPropsSource, /conversationStarted: threadViewState\.conversationStarted/)
  assert.match(chatViewLayoutPropsSource, /currentPlan: threadViewState\.currentPlan/)
  assert.match(chatViewLayoutPropsSource, /showTimelineLoading: threadViewState\.showTimelineLoading/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /transcriptMessageCount/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /latestPlanFromTimelineItems/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /canShowTimelineLoading/)
  assert.doesNotMatch(chatThreadViewStateSource, /transcriptMessageCount/)
  assert.doesNotMatch(chatThreadViewStateSource, /from '@\/features\/agent\/state\/agentStore'/)
  assert.match(chatThreadViewStateSource, /hasTranscriptMessages/)
  assert.match(chatThreadViewStateSource, /activeRun: AgentRun \| null/)
  assert.match(chatThreadViewStateSource, /const hasProjectionItems = conversationProjection\.items\.length > 0/)
  assert.match(chatThreadViewStateSource, /latestPlanFromTimelineItems\(timelineItems\)/)
  assert.doesNotMatch(chatThreadViewStateSource, /conversationProjection\.canShowTimelineLoading/)
  assert.doesNotMatch(chatThreadViewStateSource, /conversationProjection\.hasRenderableItems/)
  assert.match(chatViewLayoutPropsSource, /activeRun: activeRun/)
  assert.match(conversationTranscriptSource, /function conversationHasTranscriptMessages/)
  assert.match(conversationTranscriptSource, /transcriptMessageCount\(\{/)
  assert.doesNotMatch(providerSessionThreadConversationSource, /function conversationHasTranscriptMessages/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /buildAgentConversationProjection/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /conversationProjectionRunInteractions/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /runInteractionAnswerEchoes/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /interactionRunsByResultMessageId/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /showActiveRunInteraction/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /interactionRunsWithoutResultMessage/)
  assert.doesNotMatch(chatViewLayoutPropsSource, /thinkingState/)
  assert.doesNotMatch(threadSectionSource, /buildAgentConversationProjection\(\{/)
  assert.doesNotMatch(threadSectionSource, /buildAgentConversationThreadItems\(\{/)
  assert.doesNotMatch(threadSectionSource, /activeRun/)
  assert.doesNotMatch(threadSectionSource, /hasActiveRun/)
  assert.doesNotMatch(threadSectionSource, /conversationProjection\.items\.length/)
  assert.doesNotMatch(threadSectionSource, /canShowTimelineLoading/)
  assert.doesNotMatch(threadSectionSource, /hasTranscriptMessages/)
  assert.doesNotMatch(threadSectionSource, /conversationStarted/)
  assert.doesNotMatch(threadSectionSource, /buildAgentThreadRenderWindow/)
  assert.doesNotMatch(threadSectionSource, /keepItemIds/)
  assert.doesNotMatch(threadSectionSource, /run-group:/)
  assert.doesNotMatch(threadSectionSource, /transcriptMessages/)
  assert.doesNotMatch(threadSectionSource, /transcriptMessageCount/)
  assert.doesNotMatch(threadSectionSource, /timelineItems/)
  assert.doesNotMatch(threadSectionSource, /latestPlanFromTimelineItems/)
  assert.doesNotMatch(chatPanelLayoutSource, /thread\.timelineItems/)
  assert.doesNotMatch(chatPageLayoutSource, /thread\.timelineItems/)
  assert.match(chatPanelLayoutSource, /plan: thread\.currentPlan/)
  assert.match(chatPageLayoutSource, /plan: thread\.currentPlan/)
  assert.doesNotMatch(threadSectionSource, /ThinkingBubbleState/)
  assert.doesNotMatch(threadSectionSource, /thinkingState/)
  assert.doesNotMatch(threadSectionSource, /showActiveRunInteraction/)
  assert.doesNotMatch(threadSectionSource, /interactionRunsWithoutResultMessage/)
  assert.doesNotMatch(threadSectionSource, /embeddedInteractionRunIds/)
  assert.doesNotMatch(threadSectionSource, /liveActivityRunIds/)
  assert.doesNotMatch(threadSectionSource, /liveActivityEventsByRunId/)
  assert.doesNotMatch(threadSectionSource, /liveInteractionRuns/)
  assert.doesNotMatch(threadSectionSource, /agentTranscriptMessageItemHasInteractionRuns/)
  assert.doesNotMatch(threadSectionSource, /agentTranscriptMessageItemsEqual/)
  assert.doesNotMatch(threadSectionSource, /agentProjectedTranscriptMessageItemsEqual/)
  assert.doesNotMatch(threadSectionSource, /agentProjectedTranscriptMessageItemHasInteractionRuns/)
  assert.doesNotMatch(threadSectionSource, /MessageBubble/)
  assert.doesNotMatch(threadSectionSource, /StreamingAssistantBubble/)
  assert.doesNotMatch(threadSectionSource, /ThinkingBubble/)
  assert.doesNotMatch(threadSectionSource, /LiveRunActivityBubble/)
  assert.doesNotMatch(threadSectionSource, /beforeMessageInteractionRuns/)
  assert.doesNotMatch(threadSectionSource, /afterMessageInteractionRuns/)
  assert.doesNotMatch(threadSectionSource, /renderedBeforeInteractionRuns/)
  assert.doesNotMatch(threadSectionSource, /renderedAfterInteractionRuns/)
  assert.doesNotMatch(threadSectionSource, /renderedInteractionRunsAreActionable/)
  assert.doesNotMatch(threadSectionSource, /beforeMessages/)
  assert.doesNotMatch(threadSectionSource, /afterMessages/)
  assert.doesNotMatch(threadSectionSource, /liveBlocks/)
  assert.doesNotMatch(threadSectionSource, /transient_blocks/)
  assert.doesNotMatch(threadSectionSource, /standalone_interaction/)
  assert.doesNotMatch(threadSectionSource, /item\.type === 'run_activity'/)
  assert.doesNotMatch(threadSectionSource, /item\.type === 'run_interaction'/)
  assert.match(threadSectionSource, /<AgentConversationProjectionItems/)
  assert.doesNotMatch(threadSectionSource, /AgentProjectedConversationBlock/)
  assert.doesNotMatch(threadSectionSource, /block\.type/)
  assert.doesNotMatch(threadSectionSource, /renderedEmbeddedInteractionEvents/)
  assert.doesNotMatch(threadSectionSource, /renderProjectionContentItem/)
  assert.doesNotMatch(threadSectionSource, /<ProviderSessionRunInteractionBubble/)
  assert.match(projectionItemsSource, /<AgentConversationProjectionContentItemView/)
  assert.doesNotMatch(projectionItemsSource, /item\.type === 'run_activity'/)
  assert.doesNotMatch(projectionItemsSource, /item\.type === 'run_interaction'/)
  assert.match(projectionActionsSource, /export interface AgentConversationProjectionActions/)
  assert.match(projectionActionsSource, /onApproveRun/)
  assert.match(projectionActionsSource, /onRejectRun/)
  assert.match(projectionActionsSource, /onAnswerRunInput/)
  assert.match(projectionActionsSource, /function agentProjectionRunActivityActions/)
  assert.match(projectionActionsSource, /function agentProjectionRunInteractionActions/)
  assert.match(projectionActionsSource, /item\.source !== 'live'/)
  assert.match(projectionItemsSource, /AgentConversationProjectionActions/)
  assert.match(projectionContentItemSource, /AgentConversationProjectionActions/)
  assert.match(projectedMessageBubbleSource, /AgentConversationProjectionActions/)
  assert.match(projectedRunBubblesSource, /AgentConversationProjectionActions/)
  assert.doesNotMatch(projectedRunBubblesSource, /export interface AgentProjectedRunBubbleActions/)
  assert.doesNotMatch(projectedMessageBubbleSource, /onApproveRun: \(runId: string/)
  assert.doesNotMatch(projectedRunBubblesSource, /onApproveRun: \(runId: string/)
  assert.doesNotMatch(projectionItemsSource, /renderedEmbeddedInteractionEvents/)
  assert.doesNotMatch(projectionItemsSource, /agentProjectedTranscriptMessageItemsEqual/)
  assert.doesNotMatch(projectionItemsSource, /agentProjectedTranscriptMessageItemHasInteractionRuns/)
  assert.match(projectionItemsSource, /agentConversationProjectionTypes/)
  assert.doesNotMatch(projectionItemsSource, /<AgentProjectedMessageBubble/)
  assert.doesNotMatch(projectionItemsSource, /<AgentProjectedAssistantStreamBubble/)
  assert.doesNotMatch(projectionItemsSource, /<AgentProjectedThinkingBubble/)
  assert.doesNotMatch(projectionItemsSource, /<AgentProjectedRunActivityBubble/)
  assert.doesNotMatch(projectionItemsSource, /<AgentProjectedRunInteractionBubble/)
  assert.match(projectionContentItemSource, /item\.type === 'run_activity'/)
  assert.match(projectionContentItemSource, /item\.type === 'run_interaction'/)
  assert.match(projectionContentItemSource, /<AgentProjectedMessageBubble/)
  assert.match(projectionContentItemSource, /<AgentProjectedAssistantStreamBubble/)
  assert.match(projectionContentItemSource, /<AgentProjectedThinkingBubble/)
  assert.match(projectionContentItemSource, /<AgentProjectedRunActivityBubble/)
  assert.match(projectionContentItemSource, /<AgentProjectedRunInteractionBubble/)
  assert.doesNotMatch(projectionItemsSource, /<StreamingAssistantBubble/)
  assert.doesNotMatch(projectionItemsSource, /<ThinkingBubble/)
  assert.doesNotMatch(projectionItemsSource, /content=\{item\.content\}/)
  assert.doesNotMatch(projectionItemsSource, /state=\{item\.state\}/)
  assert.doesNotMatch(projectionItemsSource, /<ProviderSessionRunInteractionBubble/)
  assert.doesNotMatch(projectionItemsSource, /<LiveRunActivityBubble/)
  assert.doesNotMatch(projectionItemsSource, /item\.actionable/)
  assert.doesNotMatch(projectionItemsSource, /item\.source/)
  assert.doesNotMatch(projectionItemsSource, /hiddenActionItemIds=\{item\.hiddenActionItemIds\}/)
  assert.match(projectionItemsSource, /hiddenActivityActionItemIds: Set<string>/)
  assert.match(projectionItemsSource, /hiddenActivityActionItemIds=\{hiddenActivityActionItemIds\}/)
  assert.match(threadSectionSource, /hiddenActivityActionItemIdsFromProjectionItems\(conversationProjection\.items\)/)
  assert.match(threadSectionSource, /hiddenActivityActionItemIds=\{hiddenActivityActionItemIds\}/)
  assert.match(projectedLiveBubblesSource, /<StreamingAssistantBubble/)
  assert.match(projectedLiveBubblesSource, /<ThinkingBubble/)
  assert.match(projectedLiveBubblesSource, /content=\{item\.content\}/)
  assert.match(projectedLiveBubblesSource, /state=\{item\.state\}/)
  assert.match(projectedMessageBubbleSource, /activity\.embeddedInteractionEvents/)
  assert.doesNotMatch(projectedMessageBubbleSource, /renderedEmbeddedInteractionEvents/)
  assert.doesNotMatch(projectedMessageBubbleSource, /renderedEmbeddedInteractionRun/)
  assert.doesNotMatch(projectedMessageBubbleSource, /renderedHiddenActivityActionItemIds/)
  assert.match(projectedMessageBubbleSource, /agentProjectedTranscriptMessageItemsEqual/)
  assert.match(projectedMessageBubbleSource, /agentProjectedTranscriptMessageItemHasInteractionRuns/)
  assert.match(projectedRunBubblesSource, /<ProviderSessionRunInteractionBubble/)
  assert.match(projectedRunBubblesSource, /<LiveRunActivityBubble/)
  assert.match(projectedRunBubblesSource, /agentProjectionRunActivityActions\(item, actions\)/)
  assert.match(projectedRunBubblesSource, /agentProjectionRunInteractionActions\(item, actions\)/)
  assert.doesNotMatch(projectedRunBubblesSource, /item\.actionable/)
  assert.doesNotMatch(projectedRunBubblesSource, /item\.source/)
  assert.doesNotMatch(projectedRunBubblesSource, /item\.run &&/)
  assert.doesNotMatch(projectedRunBubblesSource, /hiddenActionItemIds=\{item\.hiddenActionItemIds\}/)
  assert.match(projectedRunBubblesSource, /hiddenActionItemIds=\{hiddenActivityActionItemIds\}/)
  assert.match(conversationProjectionSource, /buildAgentConversationThreadItems\(\{/)
  assert.doesNotMatch(conversationProjectionSource, /runIdsWithTimelineActivityItems/)
  assert.doesNotMatch(conversationProjectionSource, /const activityMessageRunIds = runIdsWithTimelineActivityItems\(input\.timelineItems\)/)
  assert.doesNotMatch(conversationProjectionSource, /if \(activityMessageRunIds\.has\(runId\)\) return false/)
  assert.match(conversationProjectionSource, /prepareAgentConversationProjectionContext\(\{/)
  assert.doesNotMatch(conversationProjectionSource, /renderableLiveBlocksForProjection\(\{/)
  assert.match(conversationProjectionPrepareSource, /renderableLiveBlocksForProjection\(\{/)
  assert.match(conversationProjectionLiveBlocksSource, /runIdsWithTimelineActivityItems/)
  assert.match(conversationProjectionLiveBlocksSource, /agentTimelineActivityItems/)
  assert.doesNotMatch(conversationProjectionLiveBlocksSource, /agentConversationThreadItems/)
  assert.match(conversationProjectionLiveBlocksSource, /const activityMessageRunIds = runIdsWithTimelineActivityItems\(input\.timelineItems\)/)
  assert.match(conversationProjectionLiveBlocksSource, /if \(activityMessageRunIds\.has\(runId\)\) return false/)
  assert.match(timelineActivityItemsSource, /function runIdsWithTimelineActivityItems/)
  assert.match(timelineActivityItemsSource, /function timelineActivityByMessageId/)
  assert.match(timelineActivityItemsSource, /function timelineActivitiesFromItems/)
  assert.match(timelineActivityItemsSource, /function timelineItemsContainRunActivity/)
  assert.match(timelineActivityItemsSource, /function filterActivityEventsForRun/)
  assert.doesNotMatch(threadItemsSource, /function runIdsWithTimelineActivityItems/)
  assert.doesNotMatch(conversationProjectionSource, /splitRunGroupItemsForLiveBlocks/)
  assert.doesNotMatch(threadItemsSource, /function splitRunGroupItemsForLiveBlocks/)
  assert.doesNotMatch(conversationProjectionSource, /type: 'run_turn'/)
  assert.doesNotMatch(conversationProjectionSource, /activeRunHasThreadGroup/)
  assert.doesNotMatch(conversationProjectionSource, /hasUserMessage/)
  assert.match(conversationProjectionSource, /projectionItemsForThreadItems\(\{/)
  assert.match(conversationProjectionRunTurnsSource, /splitRunGroupItemsForLiveBlocks/)
  assert.match(conversationProjectionRunTurnsSource, /type: 'run_turn'/)
  assert.match(conversationProjectionRunTurnsSource, /activeRunHasThreadGroup/)
  assert.doesNotMatch(conversationProjectionRunTurnsSource, /hasUserMessage/)
  assert.doesNotMatch(conversationProjectionTypesSource, /hasUserMessage/)
  assert.match(projectionItemsSource, /function projectionRunTurnHasUserMessage/)
  assert.match(projectionItemsSource, /data-has-user=\{projectionRunTurnHasUserMessage\(item\.items\)/)
  assert.doesNotMatch(conversationProjectionSource, /type: 'run_interaction'/)
  assert.doesNotMatch(conversationProjectionSource, /type: 'message'/)
  assert.doesNotMatch(conversationProjectionSource, /projectionRunInteractionItem/)
  assert.doesNotMatch(conversationProjectionSource, /projectTranscriptMessageItem/)
  assert.doesNotMatch(conversationProjectionSource, /projectionInteractionRunsForMessageItem/)
  assert.doesNotMatch(conversationProjectionSource, /projectionMessageItems/)
  assert.match(conversationProjectionRunTurnsSource, /projectionMessageItems/)
  assert.match(conversationProjectionMessagesSource, /function projectionMessageItems/)
  assert.match(conversationProjectionMessagesSource, /type: 'run_interaction'/)
  assert.match(conversationProjectionMessagesSource, /type: 'message'/)
  assert.match(conversationProjectionMessagesSource, /projectionRunInteractionItem/)
  assert.match(conversationProjectionMessagesSource, /projectTranscriptMessageItem/)
  assert.match(conversationProjectionMessagesSource, /projectionInteractionRunsForMessageItem/)
  assert.doesNotMatch(conversationProjectionSource, /function projectionInteractionRunsForMessageItem/)
  assert.doesNotMatch(conversationProjectionSource, /function interactionRunEmbedsInMessage/)
  assert.doesNotMatch(conversationProjectionSource, /function collectInteractionActionItemIds/)
  assert.doesNotMatch(conversationProjectionSource, /function attachHiddenActivityActionItemIdsToItems/)
  assert.match(conversationProjectionInteractionsSource, /function projectionInteractionRunsForMessageItem/)
  assert.match(conversationProjectionInteractionsSource, /function interactionRunEmbedsInMessage/)
  assert.doesNotMatch(conversationProjectionInteractionsSource, /function collectInteractionActionItemIds/)
  assert.doesNotMatch(conversationProjectionInteractionsSource, /function attachHiddenActivityActionItemIdsToItems/)
  assert.match(conversationProjectionInteractionsSource, /transcriptMessageItemRelatedRunId/)
  assert.doesNotMatch(conversationProjectionInteractionsSource, /item\.timelineActivity\?\.runId/)
  assert.doesNotMatch(conversationProjectionSource, /renderedInteractionRunsAreActionable: !!item\.liveInteractionRuns\?\.length/)
  assert.doesNotMatch(conversationProjectionSource, /agentProjectedTranscriptMessageItemsEqual/)
  assert.doesNotMatch(conversationProjectionSource, /agentProjectedTranscriptMessageItemHasInteractionRuns/)
  assert.doesNotMatch(conversationProjectionSource, /AgentProjectedMessageRenderEquality/)
  assert.doesNotMatch(conversationProjectionSource, /export interface AgentProjectedTranscriptMessageItem \{/)
  assert.doesNotMatch(conversationProjectionSource, /export type AgentConversationProjectionContentItem/)
  assert.doesNotMatch(conversationProjectionSource, /export interface AgentConversationProjection \{/)
  assert.match(conversationProjectionSource, /finalizeAgentConversationProjection\(\{/)
  assert.doesNotMatch(conversationProjectionSource, /scrollKey: projectionItemsScrollKey\(items\)/)
  assert.doesNotMatch(conversationProjectionSource, /projectionItemsScrollKey/)
  assert.doesNotMatch(conversationProjectionSource, /canShowTimelineLoading:/)
  assert.doesNotMatch(conversationProjectionSource, /keepItemIds:/)
  assert.doesNotMatch(conversationProjectionFinalizeSource, /scrollKey:/)
  assert.doesNotMatch(conversationProjectionFinalizeSource, /projectionItemsScrollKey/)
  assert.doesNotMatch(conversationProjectionFinalizeSource, /canShowTimelineLoading:/)
  assert.doesNotMatch(conversationProjectionFinalizeSource, /hasRenderableItems/)
  assert.doesNotMatch(conversationProjectionFinalizeSource, /keepItemIds:/)
  assert.doesNotMatch(conversationProjectionSource, /buildAgentConversationProjectionRenderWindow/)
  assert.doesNotMatch(conversationProjectionSource, /AgentConversationProjectionRenderWindow/)
  assert.doesNotMatch(conversationProjectionSource, /function projectionItemsScrollKey/)
  assert.doesNotMatch(conversationProjectionSource, /function projectionContentItemScrollKey/)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentMessageRenderWindow.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentMessageRenderWindow.test.ts')), false)
  assert.match(conversationProjectionRenderWindowSource, /buildAgentConversationProjectionRenderWindow/)
  assert.match(conversationProjectionRenderWindowSource, /buildAgentThreadRenderWindow\(\{/)
  assert.match(conversationProjectionRenderWindowSource, /components\/AgentThreadRenderWindow/)
  assert.match(threadRenderWindowSource, /export function buildAgentThreadRenderWindow/)
  assert.match(threadRenderWindowSource, /keepItemIds = \[\]/)
  assert.match(conversationProjectionRenderWindowSource, /keepItemIds: projectionLiveRunTurnItemIds\(input\.projection\.items\)/)
  assert.match(conversationProjectionRenderWindowSource, /function projectionLiveRunTurnItemIds/)
  assert.match(threadSectionSource, /components\/AgentConversationProjectionRenderWindow/)
  assert.match(conversationProjectionScrollKeySource, /function projectionItemsScrollKey/)
  assert.match(conversationProjectionScrollKeySource, /function projectionContentItemScrollKey/)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentRenderEquality.ts')), false)
  assert.equal(existsSync(resolve('src/features/agent/domain/agentRenderEquality.test.ts')), false)
  assert.match(renderEqualitySource, /export function shallowReferenceArrayEqual/)
  assert.doesNotMatch(renderEqualitySource, /setValuesEqual/)
  assert.match(projectedMessageRenderEqualitySource, /agentProjectedTranscriptMessageItemsEqual/)
  assert.match(projectedMessageRenderEqualitySource, /agentProjectedTranscriptMessageItemHasInteractionRuns/)
  assert.match(projectedMessageRenderEqualitySource, /components\/AgentRenderEquality/)
  assert.match(projectedMessageRenderEqualitySource, /shallowReferenceArrayEqual/)
  assert.match(conversationProjectionTypesSource, /export type AgentConversationProjectionContentItem/)
  assert.doesNotMatch(conversationProjectionTypesSource, /hiddenActionItemIds: Set<string>/)
  assert.doesNotMatch(conversationProjectionTypesSource, /hiddenActivityActionItemIds: Set<string>/)
  assert.doesNotMatch(conversationProjectionTypesSource, /hasRenderableItems: boolean/)
  assert.doesNotMatch(conversationProjectionTypesSource, /canShowTimelineLoading: boolean/)
  assert.doesNotMatch(conversationProjectionTypesSource, /keepItemIds: string\[\]/)
  assert.doesNotMatch(conversationProjectionTypesSource, /scrollKey: string/)
  assert.match(conversationProjectionTypesSource, /source: 'live' \| 'historical'/)
  assert.doesNotMatch(conversationProjectionTypesSource, /actionable: boolean/)
  assert.match(conversationProjectionMessagesSource, /const interactionSource = item\.liveInteractionRuns \? 'live' : 'historical'/)
  assert.doesNotMatch(conversationProjectionMessagesSource, /interactionsAreActionable/)
  assert.match(conversationProjectionTypesSource, /export interface AgentConversationProjection \{/)
  assert.doesNotMatch(conversationProjectionTypesSource, /AgentConversationProjectionRunInteractions/)
  assert.doesNotMatch(conversationProjectionTypesSource, /AgentConversationProjectionThinkingState/)
  assert.doesNotMatch(conversationProjectionTypesSource, /AgentProjectedTranscriptMessageItem/)
  assert.doesNotMatch(conversationProjectionTypesSource, /interface AgentProjectedTranscriptMessageItem extends AgentTranscriptMessageItem/)
  assert.doesNotMatch(conversationProjectionTypesSource, /showMessage/)
  assert.doesNotMatch(conversationProjectionSource, /renderedBeforeInteractionRuns/)
  assert.doesNotMatch(conversationProjectionSource, /renderedAfterInteractionRuns/)
  assert.doesNotMatch(conversationProjectionSource, /renderedInteractionRunsAreActionable/)
  assert.doesNotMatch(conversationProjectionSource, /hiddenActivityActionItemIdsFromProjectedMessages/)
  assert.doesNotMatch(conversationProjectionFinalizeSource, /hiddenActivityActionItemIdsFromProjectedMessages/)
  assert.doesNotMatch(conversationProjectionFinalizeSource, /hiddenActivityActionItemIds/)
  assert.doesNotMatch(conversationProjectionInteractionsSource, /function hiddenActivityActionItemIdsFromProjectedMessages/)
  assert.match(projectionActivityFiltersSource, /function hiddenActivityActionItemIdsFromProjectionItems/)
  assert.match(projectionActivityFiltersSource, /function collectInteractionActionItemIds/)
  assert.doesNotMatch(conversationProjectionSource, /liveActivityEventsByRunIdFromBlocks/)
  assert.match(conversationProjectionPrepareSource, /liveActivityEventsByRunIdFromBlocks/)
  assert.doesNotMatch(conversationProjectionSource, /function liveActivityEventsByRunIdFromBlocks/)
  assert.doesNotMatch(conversationProjectionSource, /function liveActivityRunIdsFromBlocks/)
  assert.doesNotMatch(conversationProjectionSource, /function projectionLiveBlockItem/)
  assert.match(conversationProjectionLiveBlocksSource, /function liveActivityEventsByRunIdFromBlocks/)
  assert.match(conversationProjectionLiveBlocksSource, /function liveActivityRunIdsFromBlocks/)
  assert.match(conversationProjectionLiveBlocksSource, /function projectionLiveBlockItem/)
  assert.match(conversationProjectionLiveBlocksSource, /type: 'assistant_stream'/)
  assert.match(conversationProjectionLiveBlocksSource, /type: 'thinking'/)
  assert.doesNotMatch(conversationProjectionSource, /suppressedInteractionRunIdsForActiveRun/)
  assert.match(conversationProjectionPrepareSource, /suppressedInteractionRunIdsForActiveRun/)
  assert.doesNotMatch(conversationProjectionSource, /isTerminalAgentRunStatus/)
  assert.doesNotMatch(conversationProjectionSource, /status !== 'requires_action'/)
  assert.doesNotMatch(conversationProjectionSource, /function suppressedInteractionRunIdsForActiveRun/)
  assert.match(conversationProjectionInteractionsSource, /function suppressedInteractionRunIdsForActiveRun/)
  assert.match(conversationProjectionInteractionsSource, /isTerminalAgentRunStatus/)
  assert.match(conversationProjectionInteractionsSource, /status !== 'requires_action'/)
  assert.doesNotMatch(conversationProjectionSource, /standaloneInteractionRunsForProjection/)
  assert.match(conversationProjectionFinalizeSource, /standaloneInteractionRunsForProjection/)
  assert.match(conversationProjectionInteractionsSource, /function standaloneInteractionRunsForProjection/)
  assert.match(timelinePlanSource, /latestPlanFromTimelineItems/)
  assert.match(timelinePlanSource, /isPlanStatusTimelineItem/)
  assert.match(messageBoundariesSource, /transcriptAssistantProviderSessionRunId/)
  assert.match(messageBoundariesSource, /assistantMessageCompletesStreamingRun/)
  assert.match(messageBoundariesSource, /streamingAssistantRunIdFromMessageId/)
  assert.match(messageBoundariesSource, /visibleStreamingAssistantTextForTranscript/)
  assert.match(messageBoundariesSource, /transcriptAssistantRelatedRunId/)
  assert.match(messageBoundariesSource, /transcriptUserRelatedRunId/)
  assert.match(messageBoundariesSource, /transcriptMessageItemRelatedRunId/)
  assert.match(messageBoundariesSource, /transcriptMessageItemThreadRunId/)
  assert.doesNotMatch(messageBoundariesSource, /ActivityRunId/)
  assert.match(transcriptMessageItemsSource, /isRunInteractionAnswerEchoMessage\(message, runInteractionAnswerEchoes\)/)
  assert.match(transcriptMessageItemsSource, /timelineActivityByMessageId\(timelineItems\)/)
  assert.match(transcriptMessageItemsSource, /runInteractionPlacementForMessage\(run, message\) === 'after'/)
  assert.match(transcriptMessageItemsSource, /runInteractionFromActivity\(timelineActivity\)/)
  assert.match(transcriptMessageItemsSource, /export interface AgentTranscriptMessageItem/)
  assert.match(transcriptMessageItemsSource, /function buildAgentTranscriptMessageItems/)
  assert.doesNotMatch(transcriptMessageItemsSource, /showMessage/)
  assert.match(conversationProjectionSource, /buildAgentTranscriptMessageItems\(\{/)
  assert.match(conversationProjectionSource, /buildAgentConversationThreadItems\(\{[\s\S]*transcriptMessageItems,/)
  assert.doesNotMatch(threadItemsSource, /buildAgentTranscriptMessageItems/)
  assert.doesNotMatch(threadItemsSource, /function buildAgentTranscriptMessageItems/)
  assert.doesNotMatch(threadItemsSource, /export interface AgentTranscriptMessageItem/)
  assert.doesNotMatch(threadItemsSource, /isRunInteractionAnswerEchoMessage\(message, runInteractionAnswerEchoes\)/)
  assert.doesNotMatch(threadItemsSource, /kind: 'plan_revision'/)
  assert.match(threadItemsSource, /transcriptMessageItemThreadRunId\(item\)/)
  assert.match(threadItemsSource, /transcriptUserRelatedRunId\(item\.message\)/)
  assert.doesNotMatch(threadItemsSource, /timelineActivityByMessageId\(timelineItems\)/)
  assert.doesNotMatch(threadItemsSource, /function timelineActivityByMessageId/)
  assert.doesNotMatch(threadItemsSource, /function userMessageRunId/)
  assert.doesNotMatch(threadItemsSource, /runtimeInput\?\.runId/)
  assert.doesNotMatch(threadItemsSource, /item\.timelineActivity\?\.runId/)
  assert.doesNotMatch(threadSectionSource, /item\.timelineActivity\?\.runId/)
  assert.doesNotMatch(threadSectionSource, /activeRun\?\.status !== 'requires_action'/)
  assert.doesNotMatch(runInteractionAnchorsSource, /kind: 'plan_revision'/)
  assert.match(runInteractionAnchorsSource, /isTranscriptAssistantChatMessage\(message\)/)
  assert.match(runInteractionAnchorsSource, /transcriptAssistantRelatedRunId\(message\)/)
  assert.match(runInteractionAnchorsSource, /runInteractionDisplayAnchorPlacementForMessage/)
  assert.match(runInteractionAnchorsSource, /runInteractionPlacementForMessage/)
  assert.match(runInteractionAnchorsSource, /message\.role === 'user' \? 'after' : 'before'/)
  assert.doesNotMatch(threadItemsSource, /runInteractionPlacementForMessage\(run, message\) === 'after'/)
  assert.doesNotMatch(threadItemsSource, /runInteractionDisplayAnchorPlacementForMessage\(run, message\)/)
  assert.doesNotMatch(threadItemsSource, /function interactionRunBelongsAfterMessage/)
  assert.doesNotMatch(threadItemsSource, /approval\.displayAnchor/)
  assert.doesNotMatch(threadItemsSource, /request\.displayAnchor/)
  assert.doesNotMatch(threadItemsSource, /runInteractionFromActivity\(timelineActivity\)/)
  assert.match(chatPanelLayoutSource, /<AgentComposerSection/)
  assert.match(chatPageLayoutSource, /<AgentComposerSection/)
  assert.match(composerSource, /answeringPendingInput \? '回答' : debugBeforeSend/)
  assert.match(composerSource, /buildingSendWorkspace \|\| \(answeringPendingInput && !canAnswerPendingInputWithText\)/)
  assert.match(sendActionsSource, /answerActiveRunInput\(activePendingInputRequest\.id, \{ text \}\)/)
  assert.match(sendActionsSource, /if \(canSendActiveRunInput\)/)
  assert.match(sendActionsSource, /sendActiveRunInput\(\{ content, attachments \}\)/)
  assert.match(activeRunInputSource, /notifyAgentTimelineAcceptedSource\(result\.message, result\.run\)/)

  const pendingBranch = sendActionsSource.indexOf('if (answeringPendingInput && activePendingInputRequest)')
  const activeRunInputPath = sendActionsSource.indexOf('if (canSendActiveRunInput)')
  const newRunPath = sendActionsSource.indexOf('const workspace = await buildSendWorkspace')
  assert.ok(pendingBranch > 0, 'pending input answer branch must exist in send()')
  assert.ok(newRunPath > pendingBranch, 'pending input must be handled before building a new send workspace')
  assert.ok(activeRunInputPath > pendingBranch, 'active run input branch must run after pending input answers')
  assert.ok(newRunPath > activeRunInputPath, 'active run messages must be routed as active run input before building a new send workspace')

  assert.match(runPageSource, /import \{ ProviderSessionInputRequestCard \}/)
  assert.match(runPageSource, /<ProviderSessionInputRequestCard/)
  assert.doesNotMatch(runPageSource, /inputWorkspaces/)
})

test('agent session UI renders generation job approvals as summary cards', () => {
  const runInteractionBubbleSource = readFileSync(resolve('src/features/agent/components/AgentRunInteractionBubble.tsx'), 'utf8')

  assert.match(runInteractionBubbleSource, /GenerationJobApprovalDetails/)
  assert.match(runInteractionBubbleSource, /generationJobApprovalView\(approval\)/)
  assert.match(runInteractionBubbleSource, /isGenerationSubmitApproval\(approval\.toolName, kind\)/)
  assert.match(runInteractionBubbleSource, /toolName === 'core_work_start' && kind === 'generation_job'/)
  assert.match(runInteractionBubbleSource, /toolName === 'generation_job_create'/)
  assert.match(runInteractionBubbleSource, /view\.targetLabel/)
  assert.match(runInteractionBubbleSource, /view\.prompt/)
  assert.match(runInteractionBubbleSource, /referenceResourceIds/)
  assert.match(runInteractionBubbleSource, /ApprovalResourceThumbnail resourceId=\{previewResourceId\}/)
  assert.match(runInteractionBubbleSource, /ResourceFileImage/)
  assert.match(runInteractionBubbleSource, /safeJSONStringify\(args\)/)
})

test('agent session UI renders asset slot candidate approvals as summary cards', () => {
  const runInteractionBubbleSource = readFileSync(resolve('src/features/agent/components/AgentRunInteractionBubble.tsx'), 'utf8')

  assert.match(runInteractionBubbleSource, /AssetSlotCandidateApprovalDetails/)
  assert.match(runInteractionBubbleSource, /assetSlotCandidateApprovalView\(approval\)/)
  assert.match(runInteractionBubbleSource, /approval\.toolName !== 'asset_candidate_write'/)
  assert.match(runInteractionBubbleSource, /素材槽 #/)
  assert.match(runInteractionBubbleSource, /只加入候选集，不会锁定、采纳或替换当前素材。/)
  assert.match(runInteractionBubbleSource, /output_resource_id/)
  assert.match(runInteractionBubbleSource, /resource_id/)
  assert.match(runInteractionBubbleSource, /ResourceFileImage/)
})

test('agent panel keeps recent resources as mention candidates, not automatic context labels', () => {
  const chatViewSource = readFileSync(resolve('src/features/agent/components/AgentChatView.tsx'), 'utf8')
  const composerStateSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatComposerState.ts'), 'utf8')
  const composerControllerSource = readFileSync(resolve('src/features/agent/presentation/useAgentComposerController.ts'), 'utf8')
  const sendWorkspaceSource = readFileSync(resolve('src/features/agent/application/agentSendWorkspace.ts'), 'utf8')

  assert.doesNotMatch(chatViewSource, /recentResourcesCount/)
  assert.doesNotMatch(composerStateSource, /recentResourcesCount/)
  assert.match(composerStateSource, /recentResources,\s*fileRef,\s*inputRef/)
  assert.match(composerControllerSource, /for \(const resource of recentResources\)[\s\S]*attachmentFromResource\(resource\)/)
  assert.match(composerControllerSource, /input\.matchAll\(RESOURCE_MENTION_RE\)/)
  assert.match(sendWorkspaceSource, /recentResources: \[\]/)
  assert.match(sendWorkspaceSource, /includeProviderSessionPreview\?: boolean/)
  assert.doesNotMatch(sendWorkspaceSource, /includeRuntimePreview/)
})

test('agent composer supports clipboard file uploads with a blocking resource transfer dialog', () => {
  const composerControllerSource = readFileSync(resolve('src/features/agent/presentation/useAgentComposerController.ts'), 'utf8')
  const composerSectionSource = readFileSync(resolve('src/features/agent/components/AgentComposerSection.tsx'), 'utf8')
  const mentionEditorSource = readFileSync(resolve('src/features/agent/components/AgentMentionEditor.tsx'), 'utf8')
  const layoutPropsSource = readFileSync(resolve('src/features/agent/presentation/agentChatViewLayoutProps.ts'), 'utf8')

  assert.match(composerControllerSource, /function clipboardFiles\(event: ClipboardEvent\): File\[\]/)
  assert.match(composerControllerSource, /event\.clipboardData\.files/)
  assert.match(composerControllerSource, /item\.getAsFile\(\)/)
  assert.match(composerControllerSource, /async function handleComposerPaste\(event: ClipboardEvent\)/)
  assert.match(composerControllerSource, /await uploadFiles\(files\)/)
  assert.match(composerControllerSource, /setUploading\(true\)[\s\S]*setUploadingFileNames/)
  assert.match(composerControllerSource, /setUploadedFileCount\(index \+ 1\)/)
  assert.match(composerSectionSource, /showAttachmentTools = true/)
  assert.match(composerSectionSource, /showMentionTools = true/)
  assert.match(composerSectionSource, /showDebugPreview = true/)
  assert.match(composerSectionSource, /\{showAttachmentTools \? \(/)
  assert.match(composerSectionSource, /\{showMentionTools \? \(/)
  assert.match(composerSectionSource, /\{showDebugPreview \? \(/)
  assert.match(composerSectionSource, /<Dialog open=\{uploading\}>/)
  assert.match(composerSectionSource, /hideClose/)
  assert.match(composerSectionSource, /onEscapeKeyDown=\{\(event\) => event\.preventDefault\(\)\}/)
  assert.match(composerSectionSource, /agents\.chat\.uploadDialogDescription/)
  assert.match(composerSectionSource, /onPaste=\{onComposerPaste\}/)
  assert.match(mentionEditorSource, /onPaste\?\.\(event\)/)
  assert.match(mentionEditorSource, /if \(event\.defaultPrevented\) return/)
  assert.match(layoutPropsSource, /onComposerPaste: composer\.handleComposerPaste/)
})

test('agent panel no longer renders the context card surface', () => {
  const chatViewSource = readFileSync(resolve('src/features/agent/components/AgentChatView.tsx'), 'utf8')
  const chatViewControllerSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatViewController.ts'), 'utf8')
  const chatPanelLayoutSource = readFileSync(resolve('src/features/agent/components/AgentChatPanelLayout.tsx'), 'utf8')
  const chatPageLayoutSource = readFileSync(resolve('src/features/agent/components/AgentChatPageLayout.tsx'), 'utf8')
  const contextStateSource = readFileSync(resolve('src/features/agent/presentation/useAgentChatContextState.ts'), 'utf8')

  assert.match(chatViewSource, /useAgentChatViewController\(props\)/)
  assert.match(chatViewControllerSource, /useAgentChatContextState\(\{/)
  assert.match(contextStateSource, /useAgentContextSummary\(\{/)
  assert.match(chatViewSource, /<AgentChatPanelLayout/)
  assert.match(chatViewSource, /<AgentChatPageLayout/)
  assert.doesNotMatch(chatPanelLayoutSource, /AgentContextSection/)
  assert.doesNotMatch(chatPanelLayoutSource, /context:/)
  assert.doesNotMatch(chatPageLayoutSource, /AgentContextSection/)
  assert.doesNotMatch(chatPageLayoutSource, /context:/)
  assert.doesNotMatch(contextStateSource, /useAgentContextPaneController/)
})
