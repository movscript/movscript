appId: com.movscript.desktop
productName: Movscript
asar: true
icon: build/icon.png

directories:
  output: release
  buildResources: build

files:
  - out/**
  - package.json

extraResources:
  - from: ../../assets/logo.png
    to: logo.png
  - from: ../agent
    to: movscript-agent
    filter:
      - dist/**
      - package.json
      - node_modules/**

mac:
  icon: build/icon.png
  target:
    - dmg
    - zip
  category: public.app-category.productivity

win:
  target:
    - nsis
    - portable

dmg:
  iconSize: 128

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
